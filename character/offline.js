import { lib, get, _status, game, ui } from "noname";
import html from "dedent";
const characters = {
  lx_simazhao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["lxqiantun", "lxxiezheng", "lxweisi"],
    dieAudios: ["jsrg_simazhao"],
    names: "司马|昭"
  },
  lx_caomao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["lxqianlong", "lxweitong"],
    isZhugong: true,
    dieAudios: ["mb_caomao"]
  },
  lx_mb_shen_machao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["lxyuli", "lxtingwei", "lxjimie"],
    img: "image/character/mb_shen_machao.jpg",
    dieAudios: ["mb_shen_machao"]
  },
  lx_simafu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["lxpanxiang", "lxchenjie"],
    dieAudios: ["mb_simafu"],
    names: "司马|孚"
  },
  lx_simazhou: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["lxbifeng", "lxsuwang"],
    dieAudios: ["mb_simazhou"],
    names: "司马|伷"
  },
  lx_guanqiujian: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["lxcuizhen", "lxkuili"],
    dieAudios: ["mb_sp_guanqiujian"],
    names: "毌丘|俭"
  },
  lx_wenqin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["lxbeiming", "lxchoumang"],
    dieAudios: ["mb_wenqin"]
  },
  lx_lizhaojiaobo: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["lxzuoyou", "lxshishou"],
    names: "李|昭-焦|伯",
    dieAudios: ["lizhaojiaobo"]
  },
  lx_chengji: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["lxkuangli", "lxxiongsi"],
    groupBorder: "jin",
    dieAudios: ["chengji"]
  },
  lx_jiachong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["lxbeini", "lxdingfa"],
    groupBorder: "jin",
    dieAudios: ["jiachong"]
  },
  pe_xiangyu: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["pegaishi", "pefuqin", "peyinghao"],
    isZhugong: true
  },
  ps_shen_jiangwei: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["psfenzhi", "psjiucheng", "psranjin"]
  },
  ps_devil_jiangwei: {
    sex: "male",
    group: "devil",
    hp: 4,
    skills: ["psmoyan", "psjiusheng", "pshunjin"]
  },
  scl_zhangxiu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["sclfudi", "sclcongjian"],
    dieAudios: ["zhangxiu"]
  },
  shangguanwaner: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["pecaishi", "peculv"],
    names: "上官|婉儿"
  },
  zh_shen_zhangjiao: {
    sex: "male",
    group: "shen",
    hp: 3,
    skills: ["zhyuanlin", "zhkuanglei", "zhqiongshan"],
    groupInGuozhan: "qun"
  },
  ps_shen_caocao: {
    sex: "male",
    group: "shen",
    hp: 3,
    skills: ["psguixin", "psshenzun", "psfeiying"]
  },
  wxdl_caopi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["wxdlyishi", "wxdlnianjun", "wxdlqianqian"]
  },
  pe_wuzetian: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["peersheng", "pelinchao"]
  },
  ym_zhaoxiang: {
    sex: "female",
    group: "shu",
    hp: 4,
    skills: ["ymqianling", "ymqueying"]
  },
  ym_guanyinping: {
    sex: "female",
    group: "shu",
    hp: 4,
    skills: ["ymsaxue", "ymxianfeng"]
  },
  ym_zhangxingcai: {
    sex: "female",
    group: "shu",
    hp: 4,
    skills: ["ymhengren", "ymdanjue"]
  },
  ym_jsrg_diaochan: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["ymdiyu", "ymfuyi", "ymjiuji"]
  },
  ym_friend_zhugeliang: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["ymfriendyance", "ymfriendfangqiu", "ymfriendzhugelianggongli"],
    names: "诸葛|亮",
    dieAudios: ["friend_zhugeliang"],
    clans: ["琅琊诸葛氏"]
  },
  pe_shen_zhonghui: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["pelinjie", "peduzhang", "pejianghuo"],
    groupInGuozhan: "wei",
    dieAudios: ["shen_zhonghui"]
  },
  ps_shen_weiyan: {
    sex: "male",
    group: "shen",
    hp: 4,
    maxHp: 7,
    skills: ["psjimou", "psyuxue", "pskuifa"],
    groupInGuozhan: "shu"
  },
  ps_devil_weiyan: {
    sex: "male",
    group: "devil",
    hp: 2,
    hujia: 4,
    skills: ["psnimou", "pskexue", "psjiefa"],
    groupInGuozhan: "shu"
  },
  sm_shenmo_sunquan: {
    sex: "male",
    group: "shen",
    hp: 6,
    skills: ["smsibian", "smqihua", "smdue"],
    doubleGroup: ["shen", "devil"],
    groupBorder: "devil",
    groupInGuozhan: "wu"
  },
  pe_liuhui: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pejieshu", "pegeyuan", "pegusuan"],
    dieAudios: ["liuhui"]
  },
  pe_baiqi: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["pewuan", "peshashen"]
  },
  pe_huamulan: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["perongbian", "peliezhan", "petijun"]
  },
  pe_yangyuhuan: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["peyichuan", "pexiuhua"]
  },
  pe_xiahoulan: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["pexunji", "peshuangfeng"],
    names: "夏侯|岚"
  },
  pe_lishimin: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pepozhen", "petaoge", "pezhenguan"]
  },
  ylyg_xuhuang: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["ylyg_duanliang", "ylyg_zier"]
  },
  ylyg_zhurong: {
    sex: "female",
    group: "shu",
    hp: 4,
    skills: ["ylyg_lieren", "ylyg_juxiang"],
    names: "null|null"
  },
  wxdl_caozhi: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["wxdl_huamao", "wxdl_qishen"]
  },
  hl_shen_guanyu: {
    sex: "male",
    group: "shen",
    hp: 5,
    skills: ["pewushen", "new_wuhun"],
    groupInGuozhan: "shu",
    dieAudio: ["shen_guanyu"]
  },
  hl_shen_zhugeliang: {
    sex: "male",
    group: "shen",
    hp: 3,
    skills: ["qixing", "pekuangfeng", "petianlin"],
    groupInGuozhan: "shu",
    dieAudio: ["shen_zhugeliang"],
    names: "诸葛|亮",
    clans: ["琅琊诸葛氏"]
  },
  hl_shen_lvmeng: {
    sex: "male",
    group: "shen",
    hp: 3,
    skills: ["peshelie", "pegongxin"],
    groupInGuozhan: "wu",
    dieAudio: ["shen_lvmeng"]
  },
  hl_shen_zhouyu: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["peqinyin", "peyeyan"],
    groupInGuozhan: "wu",
    dieAudio: ["shen_zhouyu"]
  },
  hl_shen_lvbu: {
    sex: "male",
    group: "shen",
    hp: 8,
    skills: ["peshenwei", "pexiuluo", "peduomo"],
    groupInGuozhan: "qun"
  },
  ylyg_pangtong: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["ylygxiangxing", "ylyglianhuan", "ylygniepan"]
  },
  ylyg_dianwei: {
    sex: "male",
    group: "wei",
    hp: 5,
    skills: ["ylygqiangxi"]
  },
  le_shen_jiaxu: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["jxlianpo", "jxzhaoluan"],
    groupInGuozhan: "qun"
  },
  boss_zhaoyun: {
    sex: "male",
    group: "shen",
    hp: 1,
    skills: ["boss_juejing", "xinlonghun", "zhanjiang"],
    groupInGuozhan: "shu"
  },
  shen_diaochan: {
    sex: "female",
    group: "shen",
    hp: 3,
    skills: ["meihun", "huoxin"],
    groupInGuozhan: "qun",
    names: "null|null"
  },
  sm_shen_machao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["sm_tuanlian", "sm_jingji", "sm_kuangchi"]
  },
  sp_sm_shen_machao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["sm_kulian", "sm_lema", "sm_chaoxuan", "sm_wandou"],
    img: "image/character/sm_shen_machao.jpg"
  },
  zc26_shen_huangyueying: {
    sex: "female",
    group: "shen",
    hp: 3,
    skills: ["zc26_cangqiao", "zc26_shenxie", "zc26_huaxiu"],
    groupInGuozhan: "shu"
  },
  mark_shen_machao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["mark_shouli", "mark_hengwu"],
    groupInGuozhan: "shu",
    dieAudios: ["shen_machao"]
  },
  ps_shen_machao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["psshouli", "pshengwu"],
    groupInGuozhan: "qun"
  },
  shen_jiaxu: {
    sex: "male",
    group: "shen",
    hp: 3,
    skills: ["weimu", "zybishi", "zyjianbing"],
    groupInGuozhan: "qun"
  },
  jx_shen_caoren: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["jxjushou"],
    groupInGuozhan: "wei"
  },
  jx_shen_liubiao: {
    sex: "male",
    group: "shen",
    hp: 2,
    skills: ["jxxiongju", "jxfujing", "jxyongrong"],
    groupInGuozhan: "qun"
  },
  ylyg_xiaoqiao: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["ylygtianxiang", "ylyghongyan"],
    names: "桥|null",
    dieAudios: ["xiaoqiao"]
  },
  ylyg_yuji: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["ylygguhuo"],
    dieAudios: ["reyuji"]
  },
  ol_manchong: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["xinjunxing", "yuce"],
    dieAudios: ["manchong"]
  },
  sm_chixia: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["sm_dayuan", "sm_tianwu", "chixia_jipao"],
    names: "null|null"
  },
  sm_qiulin: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["sm_hualiu", "sm_dianteng", "qiulin_jipao"],
    names: "null|null"
  },
  sm_zhuahuang: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["sm_feidian", "sm_xianxing", "zhuahuang_jipao"],
    names: "null|null"
  },
  sm_zijian: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["sm_zixin", "sm_zhiyin", "zijian_jipao"],
    names: "null|null"
  },
  sm_jieying: {
    sex: "female",
    group: "wei",
    hp: 4,
    skills: ["sm_jueying", "sm_xuneng", "jieying_jipao"],
    names: "null|null"
  },
  sm_liaoyuan: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["sm_chitu", "sm_aozhan", "liaoyuan_jipao"],
    names: "null|null"
  },
  sm_dailu: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["sm_dilu", "sm_boxing", "dailu_jipao"],
    names: "null|null"
  },
  wxdl_caocao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["wxdl_kejie", "wxdl_hongqi"],
    isZhugong: true
  },
  shinin_zhenji: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["shinin_luoshen", "qingguo", "zhenji_moli"],
    dieAudios: ["zhenji"]
  },
  awaken_shinin_zhenji: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["shinin_jinghong", "shinin_youlong", "shinin_haiyou"],
    isUnseen: true
  },
  shinin_wuguotai: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["reganlu", "buyi", "wuguotai_moli"],
    dieAudios: ["wuguotai"],
    names: "丁|null"
  },
  awaken_shinin_wuguotai: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["shinin_fengshou", "shinin_susheng"],
    names: "丁|null",
    isUnseen: true
  },
  shinin_ruiji: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["dcwangyuan", "dclingyin", "dcliying", "ruiji_moli"],
    dieAudios: ["ruiji"],
    names: "芮|null"
  },
  awaken_shinin_ruiji: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["shinin_shengcai", "shinin_guanghui"],
    names: "芮|null",
    isUnseen: true
  },
  shinin_lvlingqi: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["guowu", "zhuangrong", "lvlingqi_moli"],
    tempname: ["lvlingqi"],
    dieAudios: ["lvlingqi"]
  },
  awaken_shinin_lvlingqi: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["shinin_henghui", "shinin_moqi"],
    tempname: ["lvlingqi"],
    isUnseen: true
  },
  shinin_dongwan: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["dcshengdu", "dcjieling", "dongwan_moli"],
    dieAudios: ["dongwan"]
  },
  awaken_shinin_dongwan: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["shinin_shengyan", "shinin_gongming"],
    isUnseen: true
  },
  lord_goblin: {
    sex: "male",
    group: "devil",
    hp: 6,
    skills: ["goblin_yibao", "goblin_rulin", "goblin_kuangbao", "goblin_feisheng"],
    names: "null|null"
  },
  goblin: {
    sex: "male",
    group: "devil",
    hp: 4,
    skills: ["goblin_shiqiang", "goblin_lingruo"],
    names: "null|null"
  },
  zc26_sp_xushi: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["zc26_longchen", "zc26_tianqi"],
    dieAudios: ["xushi"]
  },
  zc26_lusu: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["zc26_haoshi", "zc26_dimeng"]
  },
  zc26_zhanghe: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["zc26_qiaobian"]
  },
  zc26_taishici: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["zc26_tianyi", "zc26_dangmo"]
  },
  zc26_jiaxu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zc26_wansha", "zc26_weimu", "luanwu"]
  },
  scl_jiaxu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["sclwansha", "reluanwu", "reweimu"],
    dieAudios: ["re_jiaxu"]
  },
  pe_guozhao: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["pepianchong", "pezunwei"],
    dieAudios: ["guozhao"]
  },
  xy_caocao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["xy_shengju", "xy_mintong"]
  },
  xy_chengyu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["xy_liaofu", "xy_jinshou"]
  },
  xy_xunyu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["xy_jianzu", "xy_dishou"],
    clans: ["颍川荀氏"]
  },
  xy_chengong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["remingce", "xy_jizheng"]
  },
  xy_zhangkai: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["xy_luejin"]
  },
  xy_lvbu: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["xy_xiaoxi", "xy_fenqi"]
  },
  xy_zhangmiao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["xy_mouni", "xy_zongfan"]
  },
  xy_caosong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xy_lilu", "xy_yizheng"]
  },
  wn_shen_machao: {
    sex: "male",
    group: "shen",
    hp: 5,
    skills: ["wn_qiangshu", "wn_yuma"],
    groupInGuozhan: "shu"
  },
  wn_shen_xuzhu: {
    sex: "male",
    group: "shen",
    hp: 5,
    skills: ["wn_zhuanzhan", "wn_huwei"],
    groupInGuozhan: "wei"
  },
  wn_zhuling: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["wn_zhanyi"]
  },
  zj_huangchong: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["zj_juxian", "zj_lijun"],
    img: "image/character/std_huangchong.jpg"
  },
  zj_dingfeng: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["zj_duanbing", "zj_fenxun"]
  },
  zj_liaohua: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["zj_dangxian", "zj_fuli"]
  },
  zj_zhanghu: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["zj_cuijian"],
    dieAudios: ["zhanghu"]
  },
  zj_yuechen: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["zj_porui"],
    dieAudios: ["yuechen"]
  },
  zj_hulie: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["zj_chengxi", "zj_zhaoe"]
  },
  zj_zhugexu: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["zj_tuizhi", "zj_qianjun", "zj_guluo"]
  },
  zj_lukang: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["drlt_qianjie", "zj_jueyan", "zj_huairou"],
    dieAudios: ["lukang"],
    clans: ["吴郡陆氏"]
  },
  zj_zhaotong: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["zj_yizan", "zj_shuge"]
  },
  zj_zhaoguang: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["zj_yizan", "zj_zhengui"]
  },
  zj_zhugezhan: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["zj_zhongwang", "zj_fuyin"],
    dieAudios: ["zhugezhan"]
  },
  zj_liuyin: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["zj_guguo"]
  },
  zj_huoyi: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["zj_zhongjue", "zj_qingming"]
  },
  zj_yaokehui: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["zj_qiangdu"]
  },
  xiahouhan: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["zj_zhuhui", "zj_hanci"]
  },
  zj_xiahouhan: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["zj_jieyi", "zj_linei", "zj_tongxin"],
    img: "image/character/xiahouhan.jpg"
  },
  zj_zhonghui: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["zj_quanwei", "zj_quanshu", "zj_quanqing"],
    clans: ["颍川钟氏"]
  },
  zj_jiangwei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["zj_juta", "zj_linze", "zj_fuji"]
  },
  zj_liuyuan: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zj_jianxi", "zj_zhaofu", "zj_tuicheng"],
    groupBorder: "devil"
  },
  zj_zhugeliang: {
    sex: "male",
    //花里胡哨的蜀势力得了
    group: "shu",
    hp: 4,
    maxHp: 7,
    skills: ["zj_jindao", "zj_hanshi", "zj_wuzhe"],
    groupBorder: "devil",
    dieAudios: ["wu_zhugeliang"]
  },
  xia_caopi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["peqinyi", "pejixin", "pejiwei"]
  },
  eu_kaisa: {
    sex: "male",
    group: "western",
    hp: 4,
    skills: ["eu_ducai", "eu_zhitong", "eu_jiquan"],
    isZhugong: true,
    names: "尤利乌斯|盖乌斯"
  },
  eu_Macrinus: {
    sex: "male",
    group: "western",
    hp: 4,
    skills: ["eu_dengtian", "eu_mingshu", "eu_juedou"],
    names: "马尔库斯|欧佩里乌斯"
  },
  eu_Ardashir: {
    sex: "male",
    group: "western",
    hp: 4,
    skills: ["eu_wanwang", "eu_sashan", "eu_nagong"],
    names: "阿尔达希尔|null"
  },
  eu_makang: {
    sex: "male",
    group: "western",
    hp: 4,
    skills: ["eu_xiru", "eu_zongma", "mashu"],
    names: "马米科尼扬|抗"
  },
  eu_yujin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["eu_jieyue"]
  },
  eu_zhangliao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["eu_tuxi"]
  },
  eu_yuejin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["eu_xiaoguo"]
  },
  pe_xiahouxuan: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["pehuanfu", "olqingyi", "olzeyue"]
  },
  wn_zhanghe: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["wn_qiaobian"]
  },
  wn_jiaxu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["wn_jianshu", "wn_zhenlve"]
  },
  wn_xuhuang: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["wn_zhuying", "wn_chiyuan"]
  },
  wn_caocao: {
    sex: "male",
    group: "wei",
    hp: 4,
    isZhugong: true,
    skills: ["wn_dingluan", "wn_zhuijiang"]
  },
  wn_hansui: {
    sex: "male",
    group: "qun",
    hp: 4,
    maxHp: 5,
    isZhugong: true,
    skills: ["wn_jubing", "wn_xiongju"]
  },
  wn_houxuan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["wn_zhongtao", "mashu"]
  },
  wn_chengyi: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["wn_dutan"]
  },
  wn_yangqiú: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["wn_qifeng"]
  },
  pe_jun_liubei: {
    sex: "male",
    group: "shu",
    hp: 4,
    isZhugong: true,
    skills: ["jun_renwang", "jijiang"],
    dieAudios: ["re_liubei"]
  },
  pe_jun_caocao: {
    sex: "male",
    group: "wei",
    hp: 4,
    isZhugong: true,
    skills: ["jun_xiongtu", "hujia"],
    dieAudios: ["sb_caocao"]
  },
  pe_jun_sunce: {
    sex: "male",
    group: "wu",
    hp: 4,
    isZhugong: true,
    skills: ["jun_jiang", "jun_zhiyang", "zhiba"],
    dieAudios: ["sb_sunce"]
  },
  pe_jun_yuanshao: {
    sex: "male",
    group: "qun",
    hp: 4,
    isZhugong: true,
    skills: ["jun_hefa", "xueyi"],
    dieAudios: ["ol_sb_yuanshao"]
  },
  pe_jun_zhangjiao: {
    sex: "male",
    group: "qun",
    hp: 3,
    isZhugong: true,
    skills: ["jun_huanlei", "jun_xiandao", "huangtian"],
    dieAudios: ["re_zhangjiao"]
  },
  pe_jun_caopi: {
    sex: "male",
    group: "wei",
    hp: 3,
    isZhugong: true,
    skills: ["jun_cuanzun", "jun_liufang", "songwei"],
    dieAudios: ["sb_caopi"]
  },
  pe_jun_liushan: {
    sex: "male",
    group: "shu",
    hp: 3,
    isZhugong: true,
    skills: ["jun_fuxiang", "jun_leling", "ruoyu"],
    tempname: ["re_liushan"],
    dieAudios: ["re_liushan"]
  },
  pe_jun_sunquan: {
    sex: "male",
    group: "wu",
    hp: 4,
    isZhugong: true,
    skills: ["jun_henglv", "jiuyuan"],
    dieAudios: ["re_sunquan"]
  },
  pe_jun_liuxie: {
    sex: "male",
    group: "qun",
    hp: 3,
    isZhugong: true,
    skills: ["jun_tianze", "jun_zhaoshou", "twzhuiting"],
    dieAudios: ["liuxie"]
  },
  pe_jun_liuhong: {
    sex: "male",
    group: "qun",
    hp: 4,
    isZhugong: true,
    skills: ["jun_gezhi", "jun_julian"],
    dieAudios: ["jsrg_liuhong"]
  },
  pe_que: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["peyingzhen", "peyuanjue", "peaoyong"],
    names: "null|null"
  },
  yj_caocaoyuanshao: {
    sex: "male",
    group: "wei",
    doubleGroup: ["wei", "qun"],
    hp: 4,
    skills: ["yjguibei", "yjjiechu", "yjdaojue", "yjtuonan"],
    names: "曹|操-袁|绍"
  },
  ps_caocao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["psjuebing", "psfengxie"]
  },
  ps_liubei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["pshuji", "pshoufa"]
  },
  ps_sunquan: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["pszhanlun", "psjueyi"]
  },
  ca_shen_wangyun: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["caanchao", "cayurong", "cadingxi"],
    clans: ["太原王氏"]
  },
  ca_shen_caocao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["cazhaoshao", "caxiaoxiong"],
    groupInGuozhan: "wei"
  },
  ca_shen_lijueguosi: {
    sex: "male",
    group: "shen",
    hp: 5,
    skills: ["caweijue", "cachuxiong"],
    names: "李|傕-郭|汜"
  },
  zombie_jiaxu: {
    sex: "male",
    group: "shen",
    hp: 3,
    skills: ["zombiesangluan", "zombieshibao", "zombiechuce", "zombielongmu"],
    groupInGuozhan: "qun"
  },
  zombie_zombie: {
    sex: "male",
    group: "qun",
    hp: 2,
    maxHp: 4,
    skills: ["zombieshibian", "zombieganran"],
    isUnseen: true
  },
  ca_fanchou: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["caxingwei", "caqianmu"]
  },
  ca_zhangji: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["casilve", "casuibian"]
  },
  ca_guosi: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["casixi", "calvedao"]
  },
  ca_lijue: {
    sex: "male",
    group: "qun",
    hp: 6,
    skills: ["cacuixi", "cajujun"]
  },
  ca_lvbu: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["wushuang", "caliyu"]
  },
  ca_wangyun: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["calianji", "camoucheng"],
    clans: ["太原王氏"],
    img: "image/character/wangyun.jpg"
  },
  hs_caohuan: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["hsjunwei", "hsmoran"]
  },
  hs_liuxuan: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["hssifen", "hsfunan"],
    isZhugong: true
  },
  hs_liuxie: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["hsjixu", "hsyouchong"]
  },
  hs_sunhao: {
    sex: "male",
    group: "wu",
    hp: 5,
    skills: ["hsshezuo"]
  },
  chenshou: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["hschenzhi", "hsdianmo", "hszaibi"]
  },
  drag_huaci: {
    sex: "male",
    group: "qun",
    hp: 3,
    maxHp: 6,
    skills: ["dragshiyao", "dragzuoyu", "dragduyi", "dragjuliao"]
  },
  drag_guansuo: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["dragqianfu", "dragchengyuan", "dragyuxian"]
  },
  drag_guanping: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["draglongyin", "dragjiezhong"]
  },
  drag_guanyinping: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["dragxueji", "draghuxiao", "tywuji"],
    dieAudios: ["guanyinping"]
  },
  drag_zhoucang: {
    sex: "male",
    group: "shu",
    hp: 4,
    maxHp: 5,
    skills: ["dragzhongyong"]
  },
  drag_liaohua: {
    sex: "male",
    group: "shu",
    hp: 4,
    maxHp: 5,
    skills: ["dragzhawang", "dragxigui"]
  },
  drag_shendan: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["draglianxiang", "dragpingmeng"]
  },
  drag_shenyi: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["draglianxiang", "dragpanfeng"]
  },
  drag_luxun: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["dragqianxun", "draglianying"],
    clans: ["吴郡陆氏"]
  },
  drag_xusheng: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["dragpojun"]
  },
  she_lvmeng: {
    sex: "male",
    group: "wu",
    hp: 3,
    maxHp: 4,
    hujia: 1,
    skills: ["shefujing", "shefujiang", "shetonglu"]
  },
  ps_zhangrang: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pstaoluan", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_zhaozhong: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pschiyan", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_sunzhang: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pszimou", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_bilan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["psbicai", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_xiayun: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["psyaozhuo", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_hankui: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["psxiaolu", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_lisong: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pskuiji", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_duangui: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pschihe", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_guosheng: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["psniqu", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  ps_gaowang: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["psmiaoyu", "pschangshi"],
    dieAudios: ["shichangshi"]
  },
  jy_caocao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["jylijun", "jytongbei"]
  },
  jy_caoren: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["jybeirong", "jyyujun"]
  },
  jy_zhugeliang: {
    sex: "male",
    group: "shu",
    hp: 3,
    maxHp: 4,
    skills: ["jyqibian", "jycailve"],
    names: "诸葛|亮",
    clans: ["琅琊诸葛氏"]
  },
  jy_pangtong: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["jylianhuan", "jysuozhou", "jyyuhuo"]
  },
  jy_lusu: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["jydimeng", "jyzhouji"]
  },
  jy_zhangzhao: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["jyboyan", "jymushi"]
  },
  jy_zhouyu: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["jysashuang", "jyhuoce"]
  },
  jy_huanggai: {
    sex: "male",
    group: "wu",
    hp: 4,
    maxHp: 5,
    skills: ["jyliezhou", "jyzhaxiang"]
  },
  yj_chezhou: {
    sex: "male",
    group: "wei",
    hp: 6,
    skills: ["psanmou", "pstousuan"]
  },
  yj_yongkai: {
    sex: "male",
    group: "shu",
    hp: 5,
    skills: ["psxiaofan", "psjiaohu", "psquanpan", "pshuoluan"]
    //doubleGroup: ["qun", "shu", "wu"],
  },
  hm_shen_zhangjiao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["hm_fudao", "hm_zongfu", "hm_dangjing", "hm_sanshou"]
  },
  hm_shen_zhangbao: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["hm_zhouyuan", "hm_zhaobing", "hm_sanshou"]
  },
  hm_shen_zhangliang: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["hm_jijun", "hm_fengtong", "hm_sanshou"]
  },
  hm_shen_huangfusong: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["hm_shice", "hm_podai"],
    names: "皇甫|嵩"
  },
  hm_shen_yl_luzhi: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["hm_jigan", "hm_weizhu", "hm_guiquan"]
  },
  hm_shen_zhujun: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["hm_cheji", "hm_daicui", "hm_kuixiang"]
  },
  hm_guigu: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["hm_tuntian", "hm_qianjun"]
  },
  hm_bosi: {
    sex: "male",
    group: "qun",
    hp: 4,
    maxHp: 6,
    skills: ["hm_weiluan", "hm_tianpan", "hm_gaiming"]
  },
  hm_nanhualaoxian: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["hm_yufeng", "hm_tianshu"],
    names: "庄|周"
  },
  hm_yudu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["hm_dafu", "hm_jipin"]
  },
  hm_taosheng: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["hm_zhannei", "hm_qianwei"]
  },
  hm_fuyun: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["hm_shuiqu", "hm_yure"]
  },
  hm_tangzhou: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["hm_jukou", "hm_weichenn"]
  },
  hm_chengyuanzhi: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["hm_wuxiao", "hm_qianhu"]
  },
  hm_gaosheng: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["hm_xiongshi", "hm_difeng"]
  },
  hm_heman: {
    sex: "male",
    group: "qun",
    hp: 5,
    maxHp: 6,
    skills: ["hm_juedian", "hm_nitian"]
  },
  hm_yanzheng: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["hm_didao", "hm_xianxiang"]
  },
  hm_bocai: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["hm_kunjun", "hm_yingzhan", "hm_cuiji"]
  },
  hm_bairao: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["hm_huoyin"]
  },
  hm_dengmao: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["hm_paoxi", "hm_houying"]
  },
  xk_luoli: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["xkjuluan", "xkxianxing"]
  },
  xk_cuilian: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xktanlu", "xkjubian"]
  },
  xk_penghu: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["xkjuqian", "xkkanpo", "xkyizhong"]
  },
  xk_shanfu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xkbimeng", "xkzhue", "xkfuzhu"],
    doubleGroup: ["shu", "qun"],
    names: "徐|庶"
  },
  xk_pengqi: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["xkjushou", "xkliaoluan", "xkhuaying", "xkjizhong"]
  },
  xk_zulang: {
    sex: "male",
    group: "wu",
    hp: 5,
    skills: ["xkxijun", "xkhaokou", "xkronggui"],
    doubleGroup: ["qun", "wu"]
  },
  yy_quyi: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yyfuqi", "jiaozi"],
    dieAudios: ["quyi"]
  },
  yy_gongsunzan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yyqizhen", "yicong", "yymujun"],
    isZhugong: true,
    names: "公孙|瓒"
  },
  yy_wenchou: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yyxuezhan", "yyyazhen"]
  },
  yy_gongsunyuan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yyxuanshi", "yyxiongye"],
    isZhugong: true,
    names: "公孙|渊"
  },
  yy_yuanshao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yysudi", "yyqishe", "yylinzhen"],
    isZhugong: true
  },
  yy_simayi: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["yyyanggu", "yyzuifu"],
    names: "司马|懿"
  },
  yy_caorui: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["yyhuituo", "yymingjian", "xingshuai"],
    isZhugong: true,
    dieAudios: ["caorui"]
  },
  yy_zhaoyun: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["longdan", "chongzhen", "yicong"],
    dieAudios: ["zhaoyun"]
  },
  yy_quancong: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["yyyaoming"],
    dieAudios: ["xin_quancong"]
  },
  scl_lvbu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["scls_zhanshen"]
  },
  scl_pangdegong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["scls_yinshi", "scls_pingcai"],
    dieAudios: ["pangdegong"]
  },
  scl_sunhanhua: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["scls_chongxu", "scls_miaojian", "scls_lianhua"],
    dieAudios: ["sunhanhua"]
  },
  scl_miheng: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["scls_kuangcai", "scls_shejian"],
    dieAudios: ["miheng"]
  },
  scl_peixiu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xingtu", "scls_juezhi"],
    dieAudios: ["peixiu"],
    groupBorder: "jin"
  },
  scl_caoying: {
    sex: "female",
    group: "wei",
    hp: 4,
    skills: ["scls_lingren"],
    tempname: ["caoying"],
    dieAudios: ["caoying"]
  },
  scl_luotong: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["scls_qinzheng"],
    dieAudios: ["luotong"]
  },
  ty_shen_liubei: {
    sex: "male",
    group: "shen",
    hp: 6,
    skills: ["tylongnu", "nzry_jieying", "tytaoyuan"],
    groupInGuozhan: "shu"
  },
  ty_shen_zhangfei: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["tyshencai", "tyxunshi"],
    groupInGuozhan: "shu"
  },
  ty_shen_guanyu: {
    sex: "male",
    group: "shen",
    hp: 5,
    skills: ["tywushen", "tywuhun"],
    groupInGuozhan: "shu"
  },
  ty_anyingx: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["tyliupo", "tyzhuiling", "tyxihun", "tyxianqi", "tyfansheng"],
    names: "null|null",
    img: "image/character/ty_anying.jpg"
  },
  ty_anying: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["tyliupo", "tyzhuiling", "tyxihun", "tyxianqi"],
    names: "null|null"
  },
  ty_wuque: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["tyansha", "tycangshen", "tyxiongren"],
    names: "null|null"
  },
  ty_yanque: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["tysiji", "tycangshen"],
    names: "null|null"
  },
  ty_wangque: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["tydaifa", "tycangshen"],
    names: "null|null"
  },
  ty_fanjiang: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["tybianta", "tybenxiang", "tyxiezhan"]
  },
  ty_zhangda: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["tyxingsha", "tyxianshou", "tyxiezhan"]
  },
  ty_liue: {
    sex: "male",
    group: "wu",
    hp: 5,
    skills: ["tyxiyu"]
  },
  ty_tanxiong: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["tylengjian", "tysheju"]
  },
  ty_buzhi: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["tyhongde", "tydingpan"]
  },
  ty_ganning: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["tyqixi", "tyfenwei"]
  },
  ty_luxun: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["tyqianshou", "tytanlong", "tyxibei"],
    clans: ["吴郡陆氏"]
  },
  ty_guanyinping: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["xueji", "huxiao", "tywuji"],
    dieAudios: ["guanyinping"]
  },
  ty_shamoke: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["gzjili", "tymanyong"],
    names: "null|null"
  },
  ty_guanxing: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyconglong", "tyzhaowu"]
  },
  ty_shicong: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyjinzhong"],
    names: "null|null"
  },
  ty_wuban: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyyoujun", "tyjicheng"],
    clans: ["陈留吴氏"],
    img: "image/character/wuban.jpg"
  },
  ty_huangzhong: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["xinliegong", "tyyizhuang"]
  },
  ty_liaohua: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tydangxian", "tyfuli"]
  },
  ty_fengxí: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyqingkou"],
    img: "image/character/tw_fengxí.jpg"
  },
  ty_zhangnan: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyfenwu"],
    img: "image/character/tw_zhangnan.jpg"
  },
  ty_zhaorong: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyyuantao"]
  },
  ty_chengjix: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["tyzhongen", "tyliebao"]
  },
  ty_chenshi: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyzhuan"]
  },
  ty_liubei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["tyqingshi", "tyyilin", "tychengming"],
    isZhugong: true
  },
  ty_sunquan: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["tyfuhan", "tychende", "tywansu"]
  },
  ty_guanyu: {
    sex: "male",
    group: "shu",
    hp: 5,
    skills: ["tywusheng", "tychengshi", "tyfuwei"]
  },
  jd_sb_xuhuang: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["jdsbduanliang", "sbshipo"],
    dieAudios: ["sb_xuhuang"]
  },
  jd_jin_wangyuanji: {
    sex: "female",
    group: "jin",
    hp: 3,
    skills: ["jdshiren", "jdyanxi"],
    hasHiddenSkill: true,
    dieAudios: ["jin_wangyuanji"]
  },
  jd_ol_huaxin: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["jdcaozhao", "olxibing"],
    dieAudios: ["ol_huaxin"]
  },
  jd_sp_yangwan: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["jdmingxuan", "spxianchou"],
    dieAudios: ["sp_yangwan"]
  },
  jd_sb_huangyueying: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["jdjizhi", "jdqicai"],
    dieAudios: ["sb_huangyueying"]
  },
  jd_sb_zhaoyun: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jdlongdan", "jdjizhu"],
    dieAudios: ["sb_zhaoyun"]
  },
  jd_sb_ganning: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["jdqixi", "jdfenwei"],
    dieAudios: ["sb_ganning"]
  },
  jd_sb_pangtong: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["jdlianhuan", "oldniepan"],
    tempname: ["sb_pangtong"],
    dieAudios: ["sb_pangtong"]
  },
  jd_hanlong: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["duwang", "jdcibei"],
    dieAudios: ["hanlong"]
  },
  jd_jin_xiahouhui: {
    sex: "female",
    group: "jin",
    hp: 3,
    skills: ["jdbaoqie", "jyishi", "shiduo"],
    hasHiddenSkill: true,
    dieAudios: ["jin_xiahouhui"],
    names: "夏侯|徽"
  },
  jd_jin_simazhao: {
    sex: "male",
    group: "jin",
    hp: 3,
    skills: ["tuishi", "choufa", "zhaoran", "chengwu"],
    isZhugong: true,
    hasHiddenSkill: true,
    dieAudios: ["jin_simazhao"],
    names: "司马|昭"
  },
  jd_sb_caocao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["jdjianxiong", "sbqingzheng", "sbhujia"],
    isZhugong: true,
    dieAudios: ["sb_caocao"]
  },
  jd_sb_sp_zhugeliang: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["jdhuoji", "jdkanpo"],
    dieAudios: ["sb_sp_zhugeliang"],
    names: "诸葛|亮",
    clans: ["琅琊诸葛氏"]
  },
  jd_jin_simashi: {
    sex: "male",
    group: "jin",
    hp: 3,
    maxHp: 4,
    skills: ["taoyin", "yimie", "jdtairan", "ruilve"],
    isZhugong: true,
    hasHiddenSkill: true,
    dieAudios: ["jin_simashi"],
    names: "司马|师"
  },
  jd_sb_menghuo: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jdsbhuoshou", "jdsbzaiqi"],
    dieAudios: ["sb_menghuo"],
    doubleGroup: ["shu", "qun"]
  },
  jd_sb_yuanshao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["jdsbluanji", "jdsbxueyi"],
    isZhugong: true,
    dieAudios: ["sb_yuanshao"]
  },
  jd_sb_yujin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["sbxiayuan", "jdsbjieyue"],
    dieAudios: ["sb_yujin"]
  },
  jd_sb_sunshangxiang: {
    sex: "female",
    group: "shu",
    hp: 4,
    skills: ["jdsbjieyin", "jdsbliangzhu", "sbxiaoji"],
    dieAudios: ["sb_sunshangxiang"],
    groupBorder: "wu"
  },
  jd_sb_liubei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["sbrende", "jdsbzhangwu", "sbjijiang"],
    isZhugong: true,
    dieAudios: ["sb_liubei"]
  },
  jd_sb_fazheng: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["jdsbxuanhuo", "jdsbenyuan"],
    dieAudios: ["sb_fazheng"]
  },
  jd_sb_zhangfei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jdsbpaoxiao", "sbxieji"],
    dieAudios: ["sb_zhangfei"]
  },
  jd_sb_sunquan: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["jdsbzhiheng", "jdsbtongye", "jdsbjiuyuan"],
    isZhugong: true,
    dieAudios: ["sb_sunquan"]
  },
  jd_sb_xiaoqiao: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["jdsbtianxiang", "jdsbhongyan"],
    dieAudios: ["sb_xiaoqiao"],
    names: "桥|null"
  },
  jd_sb_guanyu: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jdsbwusheng", "jdsbyijue"],
    dieAudios: ["sb_guanyu"]
  },
  jd_sb_jiangwei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jdsbtiaoxin", "jdsbzhiji"],
    dieAudios: ["sb_jiangwei"]
  },
  jd_sb_daqiao: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["jdsbguose", "jdsbliuli"],
    dieAudios: ["sb_daqiao"],
    names: "桥|null"
  },
  jd_simayan: {
    sex: "male",
    group: "jin",
    hp: 3,
    skills: ["jdjuqi", "jdfengtu", "jdtaishi"],
    isZhugong: true,
    names: "司马|炎"
  },
  jx_zhouyu: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["jxxiongzi", "jxzhanyan"]
  },
  jx_guanyu: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jxwusheng", "jsrgguanjue", "nuzhan"]
  },
  yj_tianchuan: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["pshuying", "psqianjing", "psbianchi"]
  },
  yj_zhonghui: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["psmouchuan", "pszizhong", "psjizun", "psqingsuan"],
    isZhugong: true,
    clans: ["颍川钟氏"]
  },
  yj_ehuan: {
    sex: "male",
    group: "qun",
    hp: 5,
    skills: ["psdiwan", "pssuiluan", "psconghan"],
    doubleGroup: ["shu", "qun"]
  },
  yj_zhouji: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["psyanmou", "pszhanyan", "psyuhuo"],
    names: "周|null"
  },
  drag_pangde: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["dragtaiguan", "mashu"]
  },
  drag_guanyu: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["dragchaojue", "dragjunshen"]
  },
  drag_caoren: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["draglizhong", "dragjuesui"]
  },
  drag_lvchang: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["dragjuwu", "dragshouxiang"]
  },
  jsp_ganfuren: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["shushen", "shenzhi"],
    img: "image/character/gz_ganfuren.jpg",
    names: "甘|null"
  },
  ol_xinxianying: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["xincaishi", "xinzhongjian"],
    dieAudios: ["xinxianying"]
  },
  ol_liuyu: {
    sex: "male",
    group: "qun",
    hp: 2,
    skills: ["xinzhige", "xinzongzuo"],
    dieAudios: ["liuyu"]
  },
  ol_zhangrang: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xintaoluan"],
    trashBin: ["sex:male_castrated"],
    dieAudios: ["zhangrang"]
  },
  zhangliang: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["old_jijun", "old_fangtong"]
  },
  jsp_caoren: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["kuiwei", "yanzheng"]
  },
  old_machao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["zhuiji", "oldcihuai"]
  },
  vtb_xiaosha: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["vtbguisha", "vtbshuli"],
    names: "null|杀"
  },
  vtb_xiaoshan: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["vtbshanwu", "vtbxianli"],
    names: "null|闪"
  },
  vtb_xiaotao: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["vtbtaoyan", "vtbyanli"],
    names: "null|桃"
  },
  vtb_xiaole: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["vtbleyu", "vtbyuanli"],
    names: "null|乐"
  },
  vtb_xiaojiu: {
    sex: "female",
    group: "qun",
    hp: 4,
    skills: ["vtbmeiniang", "vtbyaoli"],
    names: "null|酒"
  },
  mateng: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mashu", "xiongyi"]
  },
  tianfeng: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["sijian", "gzsuishi"]
  },
  jiling: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["shuangren"]
  },
  kongrong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zymingshi", "lirang"]
  },
  jiangqing: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["zyshangyi"]
  },
  pk_sp_duyu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["pkwuku", "pksanchen"],
    groupBorder: "jin"
  },
  ps_lvbu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["wushuang", "pssheji"]
  },
  ps_jiaxu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["wansha", "psqupo", "psbaoquan"]
  },
  ps_machao: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["mashu", "tieji", "psweihou"]
  },
  ps2066_zhugeliang: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["pszhiji", "psjiefeng", "kongcheng"],
    names: "诸葛|亮",
    clans: ["琅琊诸葛氏"]
  },
  ps_zhugeliang: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["psguanxing", "pslongyin"],
    names: "诸葛|亮",
    clans: ["琅琊诸葛氏"]
  },
  ps_simayi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["reguicai", "pshuxiao"],
    names: "司马|懿"
  },
  ps2068_simayi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["refankui", "reguicai", "pszhonghu"],
    names: "司马|懿"
  },
  ps_caopi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["psjianwei", "fangzhu", "songwei"],
    isZhugong: true
  },
  ps_jin_simayi: {
    sex: "male",
    group: "jin",
    hp: 3,
    skills: ["smyyingshi", "psquanyi"],
    names: "司马|懿"
  },
  ps_caozhi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["psliushang", "psqibu"]
  },
  ps1062_zhouyu: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["yingzi", "psoldshiyin"]
  },
  ps2080_zhouyu: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["psshiyin", "psquwu", "psliaozou"]
  },
  ps2063_zhaoyun: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["psqijin", "psqichu", "pslongxin"]
  },
  ps2067_zhaoyun: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["longdan", "pshuiqiang", "pshuntu"]
  },
  ps1059_guojia: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["tiandu", "psqizuo"]
  },
  ps2070_guojia: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["yiji", "psquanmou"]
  },
  ps_guanyu: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["wusheng", "pszhonghun", "nuzhan"]
  },
  pe_wenqin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["gzjinfa"]
  },
  pe_sunchen: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["zyshilu", "zyxiongnve"]
  },
  pe_mengda: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["qiuan", "liangfan"]
  },
  pe_zhonghui: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["zyquanji", "zypaiyi"],
    clans: ["颍川钟氏"]
  },
  pe_wangyun: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zylianji", "zymoucheng"],
    clans: ["太原王氏"]
  },
  yj_zhenji: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["yjluoshen", "qingguo"]
  },
  yj_jiaxu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["yjzhenlve", "yjjianshu", "yjyongdi"]
  },
  yj_xuyou: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["yjshicai", "yjchenggong", "yjzezhu"]
  },
  yj_dongzhuo: {
    sex: "male",
    group: "qun",
    hp: 7,
    skills: ["yjtuicheng", "yjyaoling", "yjshicha", "yjyongquan"],
    isZhugong: true
  },
  yongjian_ganning: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["yjjielve"]
  },
  yj_zhangfei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["yjmangji"]
  },
  yj_caohong: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["yjlifeng"]
  },
  yj_liru: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["yjdumou", "yjweiquan", "yjrenwang"]
  },
  yj_caocao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yjxiandao", "yjsancai", "yjyibing"]
  },
  longyufei: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["longyi", "zhenjue"]
  },
  sp_liubei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["zhaolie", "shichou"],
    isZhugong: true
  },
  sp_zhangfei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jie", "dahe"]
  },
  sp_lvmeng: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["tanhu", "mouduan"]
  },
  sp_xiahoudun: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["fenyong", "xuehen"],
    names: "夏侯|惇"
  },
  sp_ganning: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yinling", "junwei"]
  },
  sp_daqiao: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["yanxiao", "anxian"],
    names: "桥|null"
  },
  sp_pangtong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xinmanjuan", "zuixiang"]
  },
  huangjinleishi: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["fulu", "fuji"],
    names: "null|null-null|null"
  },
  jsp_zhaoyun: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["chixin", "reyicong", "suiren"]
  },
  sp_xiahoushi: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["xinfu_yanyu", "xinfu_xiaode"],
    names: "夏侯|null"
  },
  sp_gongsunzan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["spyicong", "sptuji"],
    names: "公孙|瓒"
  },
  sp_simazhao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["spzhaoxin", "splanggu"],
    names: "司马|昭",
    groupBorder: "jin"
  },
  sp_wangyuanji: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["spfuluan", "spshude"],
    groupBorder: "jin"
  },
  sp_xinxianying: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["spmingjian", "spyinzhi"]
  },
  sp_liuxie: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["sphuangen", "sphantong"]
  },
  ns_lijue: {
    sex: "male",
    group: "qun",
    hp: 4,
    maxHp: 6,
    skills: ["nsfeixiong", "nscesuan"]
  },
  ns_zhangji: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["nslulve"]
  },
  ns_fanchou: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["nsyangwu"]
  },
  ns_jiaxu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["nsyice", "luanwu"]
  },
  ns_chendao: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["nsjianglie"]
  },
  yj_caoang: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["yjxuepin"]
  },
  ns_caoanmin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["nskuishe"]
  },
  jsp_liubei: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["jsprende"]
  },
  yj_jiling: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["yjshuangren"]
  },
  jun_lvbu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["wushuang", "stdqingjiao"],
    isZhugong: true,
    dieAudios: ["lvbu"]
  }
};
const cards = {
  sm_prettyDerby: {
    audio: true,
    fullskin: true,
    derivation: "sm_shen_machao",
    type: "equip",
    distance: {
      globalFrom: -1,
      globalTo: 1
    },
    selectTarget: -1,
    filterTarget(card, player, target) {
      if (player !== target) {
        return false;
      }
      const ranges = Array.from(Array(5)).map((value, index) => `equip${index + 1}`);
      if (get.is.mountCombined()) {
        ranges.removeArray(["equip3", "equip4"]);
        ranges.add("equip3_4");
      }
      if (get.itemtype(card) === "card") {
        const owner = get.owner(card, "judge");
        if (owner && !lib.filter.canBeGained(card, player, owner)) {
          return false;
        }
      }
      return ranges.some((range) => player.countEquipableSlot(range));
    },
    async prepareEquip(event, trigger, player) {
      if (!event.card.subtypes?.length) {
        const choices = [];
        for (let i = 0; i <= 5; i++) {
          if (player.hasEquipableSlot(i)) {
            choices.push(`equip${i}`);
          }
        }
        if (!choices.length) {
          return;
        }
        const result = await player.chooseControl(choices).set("prompt", "请选择置入【赛马】的装备栏").set("ai", () => _status.event.controls.randomGet()).forResult();
        event.card.subtypes = [result.control];
      }
    },
    ai: {
      equipValue: 7.5,
      basic: {
        equipValue: 7.5
      }
    }
  },
  //sm-赛马
  sm_mabian: {
    derivation: "sp_sm_shen_machao",
    fullskin: true,
    type: "equip",
    subtype: "equip5",
    async onEquip(event, trigger, player) {
      const { card } = event;
      const skill = "sm_mabian_skill";
      if (event.getParent().name !== "equip") {
        return;
      }
      const evt = event.getParent(2);
      const target = evt.player;
      if (!get.info(evt.name)?.transformSkill) {
        return;
      }
      const skills2 = [];
      for (const name of get.nameList(target)) {
        const list = get.character(name, 3);
        if (!list?.length || !list.includes(evt.name)) {
          continue;
        }
        if (get.characterTitle(name) !== "赛马娘") {
          continue;
        }
        skills2.add(list[0]);
      }
      player.addSkill(skill);
      const map = player.getStorage(skill, /* @__PURE__ */ new Map());
      map.set(card, skills2);
      player.setStorage(skill, map);
      player.addAdditionalSkill(skill, Array.from(map.values()).flat());
    },
    forceDie: true,
    async onLose(event, trigger, player) {
      const { card } = event;
      const skill = "sm_mabian_skill";
      const map = player.getStorage(skill, /* @__PURE__ */ new Map());
      map.delete(card);
      player.setStorage(skill, map);
      player.addAdditionalSkill(skill, Array.from(map.values()).flat());
      if (!map.size) {
        player.removeSkill(skill);
      }
    },
    cardPrompt(card, player) {
      if (!card || !player) {
        return lib.translate["sm_mabian_info"];
      }
      const skill = "sm_mabian_skill";
      const map = player.getStorage(skill, /* @__PURE__ */ new Map());
      const vcard = card[card.cardSymbol];
      if (!vcard || !map.has(vcard) || !map.get(vcard).length) {
        return lib.translate["sm_mabian_info"];
      }
      const skills2 = map.get(vcard);
      return `你视为拥有着${skills2.map((name) => get.poptip(name))}`;
    },
    ai: {
      basic: {
        equipValue: 7
      }
    }
  },
  //26神黄月英的升级装备
  zc26_zhuge: {
    fullskin: true,
    type: "equip",
    subtype: "equip1",
    derivation: "zc26_shen_huangyueying",
    skills: ["zc26_zhuge_skill"],
    ai: {
      order() {
        return get.order({ name: "sha" }) + 0.1;
      },
      equipValue(card, player) {
        if (player._zhuge_temp) {
          return 1;
        }
        player._zhuge_temp = true;
        const result = (() => {
          if (!game.hasPlayer((current) => get.distance(player, current) <= 1 && player.canUse("sha", current) && get.effect(current, { name: "sha" }, player, player) > 0)) {
            return 1.5;
          }
          if (player.hasSha() && _status.currentPhase === player) {
            if (player.getEquip("zhuge") && player.countUsed("sha") || player.getCardUsable("sha") === 0) {
              return 10.5;
            }
          }
          const num = player.countCards("h", "sha");
          if (num > 1) {
            return 6.5 + num;
          }
          return 3.5 + num;
        })();
        delete player._zhuge_temp;
        return result;
      },
      basic: {
        equipValue: 6
      },
      tag: {
        valueswap: 1.5
      }
    }
  },
  zc26_bagua: {
    fullskin: true,
    type: "equip",
    subtype: "equip2",
    derivation: "zc26_shen_huangyueying",
    skills: ["zc26_bagua_skill"],
    ai: {
      basic: {
        equipValue: 8
      }
    }
  },
  zc26_lingling: {
    name: "zc26_lingling",
    fullskin: true,
    type: "equip",
    subtype: "equip4",
    derivation: "zc26_shen_huangyueying",
    skills: ["zc26_lingling_skill"],
    distance: { globalFrom: -2 },
    ai: {
      value(card, player) {
        if (!game.hasPlayer((current) => get.damageEffect(current, player, player, "thunder") > 0)) {
          return 0;
        }
        return 8;
      },
      equipValue(card, player) {
        if (!game.hasPlayer((current) => get.damageEffect(current, player, player, "thunder") > 0)) {
          return 0;
        }
        return 8;
      },
      basic: {
        equipValue: 2
      }
    }
  },
  shengbei_left_yin: {
    fullskin: true,
    noname: true
  },
  shengbei_left_yang: {
    fullskin: true,
    noname: true
  },
  shengbei_right_yin: {
    fullskin: true,
    noname: true
  },
  shengbei_right_yang: {
    fullskin: true,
    noname: true
  },
  yiguzuoqi: {
    derivation: "xy_lvbu",
    audio: true,
    fullskin: true,
    type: "trick",
    enable: true,
    selectTarget: -1,
    cardcolor: "red",
    toself: true,
    filterTarget(card, player, target) {
      return target === player;
    },
    modTarget: true,
    async content(event, trigger, player) {
      const name = event.name;
      player.addTip(name, "一鼓作气", true);
      player.addTempSkill("yiguzuoqi_effect");
    },
    ai: {
      wuxie(target, card, player, viewer) {
        if (target.countCards("h") < 4) {
          return 0;
        }
      },
      basic: {
        order: 11.2,
        useful: 4.5,
        value(card, player) {
          const cards2 = player.getCards("hs", (card2) => {
            if (get.name(card2) !== "sha") {
              return false;
            }
            return player.hasValueTarget(card2, false);
          });
          if (cards2.length < player.getCardUsable("sha", true)) {
            return 0;
          }
          return 1.4 * Math.max(5, player.countCards("hs"));
        }
      },
      result: {
        target: 2
      }
    }
  },
  zongma_attack: {
    derivation: "eu_makang",
    fullskin: true,
    image: "image/card/makang_zongma.png",
    type: "equip",
    subtype: "equip4",
    skills: ["eu_zongma_attack"]
  },
  zongma_defend: {
    derivation: "eu_makang",
    fullskin: true,
    image: "image/card/makang_zongma.png",
    type: "equip",
    subtype: "equip3",
    skills: ["eu_zongma_defend"]
  },
  hschenzhi_poker: {
    type: "poker",
    fullskin: true,
    noname: true,
    image: (card) => `image/card/lukai_${card.suit}.png`
  },
  chunqiubi: {
    derivation: "chenshou",
    cardcolor: "heart",
    type: "equip",
    subtype: "equip5",
    skills: ["chunqiubi_skill"],
    fullskin: true,
    destroy: true,
    ai: {
      basic: {
        equipValue: 5,
        order: (card, player) => {
          const equipValue = get.equipValue(card, player) / 20;
          return player && player.hasSkillTag("reverseEquip") ? 8.5 - equipValue : 8 + equipValue;
        },
        useful: 2,
        value: (card, player, index, method) => {
          if (!player.getCards("e").includes(card) && !player.canEquip(card, true)) {
            return 0.01;
          }
          const info = get.info(card);
          const current = player.getEquip(info.subtype);
          const value = current && card !== current && get.value(current, player);
          let equipValue = info.ai.equipValue || info.ai.basic.equipValue;
          if (typeof equipValue === "function") {
            if (method === "raw") {
              return equipValue(card, player);
            }
            if (method === "raw2") {
              return equipValue(card, player) - value;
            }
            return Math.max(0.1, equipValue(card, player) - value);
          }
          if (typeof equipValue !== "number") {
            equipValue = 0;
          }
          if (method === "raw") {
            return equipValue;
          }
          if (method === "raw2") {
            return equipValue - value;
          }
          return Math.max(0.1, equipValue - value);
        }
      },
      result: {
        target: (player, target, card) => get.equipResult(player, target, card)
      }
    },
    async onLose({ player }) {
      if (player.getStat().skill.chunqiubi_skill) {
        delete player.getStat().skill.chunqiubi_skill;
      }
    }
  },
  sclc_wolong: {
    type: "takaramono",
    fullskin: true,
    cardimage: "wolong_card"
    //derivation:"scl_pangdegong",
  },
  sclc_fengchu: {
    type: "takaramono",
    fullskin: true,
    cardimage: "fengchu_card"
    //derivation:"scl_pangdegong",
  },
  sclc_shuijing: {
    fullskin: true,
    type: "takaramono",
    cardimage: "shuijing_card"
    //derivation:"scl_pangdegong",
  },
  sclc_xuanjian: {
    fullskin: true,
    type: "takaramono",
    cardimage: "xuanjian_card"
    //derivation:"scl_pangdegong",
  },
  yanxiao_card: {
    type: "special_delay",
    fullimage: true,
    noEffect: true,
    ai: {
      basic: {
        order: 1,
        useful: 1,
        value: 8
      },
      result: {
        target: 1
      }
    }
  },
  jingxiangshengshi: {
    audio: true,
    fullskin: true,
    derivation: "jx_shen_liubiao",
    type: "trick",
    enable: true,
    filterTarget: lib.filter.notMe,
    selectTarget() {
      return game.countGroup();
    },
    complexTarget: true,
    async contentBefore(event, trigger, player) {
      const { targets } = event;
      if (!targets.length) {
        return;
      }
      const num = game.countPlayer();
      const cards2 = get.cards(num);
      const next = game.cardsGotoOrdering(cards2);
      next.relatedEvent = event.getParent();
      const dialog = ui.create.dialog("荆襄盛世", cards2, true);
      _status.dieClose.push(dialog);
      dialog.videoId = lib.status.videoId++;
      game.addVideo("cardDialog", null, ["荆襄盛世", get.cardsInfo(cards2), dialog.videoId]);
      event.getParent().preResult = dialog.videoId;
      game.broadcast(
        (cards3, id) => {
          const dialog2 = ui.create.dialog("荆襄盛世", cards3, true);
          _status.dieClose.push(dialog2);
          dialog2.videoId = id;
        },
        cards2,
        dialog.videoId
      );
      game.log(event.card, "亮出了", cards2);
      await next;
    },
    async content(event, trigger, player) {
      const { target } = event;
      const dialog = ui.dialogs.find((dialog2) => dialog2.videoId === event.preResult);
      if (!dialog || !dialog.buttons.length) {
        return;
      }
      let card;
      if (dialog.buttons.length === 1) {
        card = dialog.buttons[0].link;
      } else {
        const result = await target.chooseButton(true).set("ai", (button2) => {
          const current = _status.event.player;
          const card2 = button2.link;
          let value = get.value(card2, current);
          if (!get.tag(card2, "recover")) {
            return value;
          }
          value += game.countPlayer((target2) => target2.hp < 2 && get.attitude(current, target2) > 0 && lib.filter.cardSavable(card2, current, target2));
          if (current.hp <= 2 && game.checkMod(card2, current, "unchanged", "cardEnabled2", current)) {
            value *= 2;
          }
          return value;
        }).set("dialog", event.preResult).set("closeDialog", false).set("dialogdisplay", true).forResult();
        const selectedButton = dialog.buttons.find((button2) => button2.link === result.links[0]);
        if (selectedButton) {
          card = selectedButton.link;
        } else {
          card = dialog.buttons[0].link;
        }
      }
      const button = dialog.buttons.find((button2) => button2.link === card);
      if (button) {
        const innerHTML = target.getName(true);
        game.createButtonCardsetion(innerHTML, button);
        dialog.buttons.remove(button);
      }
      const capt = `${get.translation(target)}选择了${get.translation(button.link)}`;
      let gainEvent;
      if (card) {
        gainEvent = target.gain(card, "visible");
        target.$gain2(card);
        game.broadcast(
          (card2, id, name, capt2) => {
            const dialog2 = get.idDialog(id);
            if (dialog2) {
              dialog2.content.firstChild.innerHTML = capt2;
              const button2 = dialog2.buttons.find((button3) => button3.link === card2);
              if (button2) {
                game.createButtonCardsetion(name, button2);
                dialog2.buttons.remove(button2);
              }
            }
          },
          card,
          dialog.videoId,
          target.getName(true),
          capt
        );
      }
      dialog.content.firstChild.innerHTML = capt;
      game.addVideo("dialogCapt", null, [dialog.videoId, dialog.content.firstChild.innerHTML]);
      game.log(target, "选择了", button.link);
      const delayEvent = game.delay();
      if (gainEvent) {
        await gainEvent;
      }
      await delayEvent;
    },
    async contentAfter(event, trigger, player) {
      const remained = [];
      const dialog = ui.dialogs.find((dialog2) => dialog2.videoId === event.preResult);
      if (dialog) {
        dialog.close();
        _status.dieClose.remove(dialog);
        remained.push(...dialog.buttons.map((button) => button.link));
      }
      game.broadcast((id) => {
        const dialog2 = get.idDialog(id);
        if (dialog2) {
          dialog2.close();
          _status.dieClose.remove(dialog2);
        }
      }, event.preResult);
      game.addVideo("cardDialog", null, event.preResult);
      if (remained.length) {
        await player.gain(remained, "gain2");
      }
    },
    //ai简略，待补充
    ai: {
      wuxie() {
        if (Math.random() < 0.5) {
          return 0;
        }
      },
      basic: {
        order: 3,
        useful: 0.5
      },
      result: {
        player(player, target) {
          return game.countPlayer() / game.countGroup() - 1;
        },
        target(player, target) {
          return 1.8 / Math.sqrt(1 + get.distance(player, target, "absolute"));
        }
      },
      tag: {
        draw: 1,
        multitarget: 1
      }
    }
  },
  xingbian: {
    audio: true,
    fullskin: true,
    derivation: "yj_tianchuan",
    type: "equip",
    skills: ["xingbian_skill"],
    selectTarget: -1,
    filterTarget(card, player, target) {
      if (player !== target) {
        return false;
      }
      const ranges = Array.from(Array(5)).map((value, index) => `equip${index + 1}`);
      if (get.is.mountCombined()) {
        ranges.removeArray(["equip3", "equip4"]);
        ranges.add("equip3_4");
      }
      if (get.itemtype(card) === "card") {
        const owner = get.owner(card, "judge");
        if (owner && !lib.filter.canBeGained(card, player, owner)) {
          return false;
        }
      }
      return ranges.some((range) => player.countEquipableSlot(range));
    },
    async prepareEquip(event, trigger, player) {
      if (!event.card.subtypes?.length) {
        const choices = [];
        for (let i = 0; i <= 5; i++) {
          if (player.hasEquipableSlot(i)) {
            choices.push(`equip${i}`);
          }
        }
        if (!choices.length) {
          return;
        }
        const result = await player.chooseControl(choices).set("prompt", "请选择置入【刑鞭】的装备栏").set("ai", () => _status.event.controls.randomGet()).forResult();
        event.card.subtypes = [result.control];
      }
    },
    ai: {
      equipValue(card, player) {
        if (get.nameList(player).includes("yj_tianchuan")) {
          return 5;
        }
        return 0;
      },
      basic: {
        equipValue: 5
      }
    }
  },
  yy_baimaxiaoqi: {
    audio: true,
    fullskin: true,
    type: "equip",
    subtype: "equip6",
    subtypes: ["equip3", "equip4"],
    nomod: true,
    nopower: true,
    distance: {
      globalFrom: -1,
      globalTo: 1
    },
    skills: ["yy_baimaxiaoqi_skill"],
    ai: {
      equipValue(card, player) {
        return 5 + player.countCards("e");
      },
      basic: {
        equipValue: 6
      }
    }
  },
  hm_zhong_heart: {
    audio: true,
    fullskin: true,
    derivation: "hm_shen_zhangjiao",
    type: "equip",
    skills: ["hm_zhong_heart_skill"],
    destroy: "discardPile",
    selectTarget: -1,
    filterTarget(card, player, target) {
      if (player !== target) {
        return false;
      }
      const ranges = Array.from(Array(5)).map((value, index) => `equip${index + 1}`);
      if (get.is.mountCombined()) {
        ranges.removeArray(["equip3", "equip4"]);
        ranges.add("equip3_4");
      }
      if (get.itemtype(card) === "card") {
        const owner = get.owner(card, "judge");
        if (owner && !lib.filter.canBeGained(card, player, owner)) {
          return false;
        }
      }
      return ranges.some((range) => player.countEquipableSlot(range));
    },
    async prepareEquip(event, trigger, player) {
      if (!event.card.subtypes?.length) {
        const choices = [];
        for (let i = 0; i <= 5; i++) {
          if (player.hasEquipableSlot(i)) {
            choices.push(`equip${i}`);
          }
        }
        if (!choices.length) {
          return;
        }
        const result = await player.chooseControl(choices).set("prompt", "请选择置入【众】的装备栏").set("ai", () => _status.event.controls.randomGet()).forResult();
        event.card.subtypes = [result.control];
      }
    },
    ai: {
      equipValue(card, player) {
        return 5;
      },
      basic: {
        equipValue: 5,
        order: (card, player) => {
          const equipValue = get.equipValue(card, player) / 20;
          return player && player.hasSkillTag("reverseEquip") ? 8.5 - equipValue : 8 + equipValue;
        },
        useful: 2,
        value: (card, player, index, method) => {
          if (!player.getCards("e").includes(card) && !player.canEquip(card, true)) {
            return 0.01;
          }
          const info = get.info(card);
          const current = player.getEquip(info.subtype);
          const value = current && card !== current && get.value(current, player);
          let equipValue = info.ai.equipValue || info.ai.basic.equipValue;
          if (typeof equipValue === "function") {
            if (method === "raw") {
              return equipValue(card, player);
            }
            if (method === "raw2") {
              return equipValue(card, player) - value;
            }
            return Math.max(0.1, equipValue(card, player) - value);
          }
          if (typeof equipValue !== "number") {
            equipValue = 0;
          }
          if (method === "raw") {
            return equipValue;
          }
          if (method === "raw2") {
            return equipValue - value;
          }
          return Math.max(0.1, equipValue - value);
        }
      },
      result: {
        target: (player, target, card) => get.equipResult(player, target, card)
      }
    },
    enable: true,
    modTarget: true,
    allowMultiple: false,
    toself: true
  },
  hm_zhong_diamond: {
    audio: true,
    fullskin: true,
    derivation: "hm_shen_zhangjiao",
    type: "equip",
    skills: ["hm_zhong_diamond_skill"],
    destroy: "discardPile",
    selectTarget: -1,
    filterTarget(card, player, target) {
      if (player !== target) {
        return false;
      }
      const ranges = Array.from(Array(5)).map((value, index) => `equip${index + 1}`);
      if (get.is.mountCombined()) {
        ranges.removeArray(["equip3", "equip4"]);
        ranges.add("equip3_4");
      }
      if (get.itemtype(card) === "card") {
        const owner = get.owner(card, "judge");
        if (owner && !lib.filter.canBeGained(card, player, owner)) {
          return false;
        }
      }
      return ranges.some((range) => player.countEquipableSlot(range));
    },
    async prepareEquip(event, trigger, player) {
      if (!event.card.subtypes?.length) {
        const choices = [];
        for (let i = 0; i <= 5; i++) {
          if (player.hasEquipableSlot(i)) {
            choices.push(`equip${i}`);
          }
        }
        if (!choices.length) {
          return;
        }
        const result = await player.chooseControl(choices).set("prompt", "请选择置入【众】的装备栏").set("ai", () => _status.event.controls.randomGet()).forResult();
        event.card.subtypes = [result.control];
      }
    },
    ai: {
      equipValue(card, player) {
        return 5;
      },
      basic: {
        equipValue: 5,
        order: (card, player) => {
          const equipValue = get.equipValue(card, player) / 20;
          return player && player.hasSkillTag("reverseEquip") ? 8.5 - equipValue : 8 + equipValue;
        },
        useful: 2,
        value: (card, player, index, method) => {
          if (!player.getCards("e").includes(card) && !player.canEquip(card, true)) {
            return 0.01;
          }
          const info = get.info(card);
          const current = player.getEquip(info.subtype);
          const value = current && card !== current && get.value(current, player);
          let equipValue = info.ai.equipValue || info.ai.basic.equipValue;
          if (typeof equipValue === "function") {
            if (method === "raw") {
              return equipValue(card, player);
            }
            if (method === "raw2") {
              return equipValue(card, player) - value;
            }
            return Math.max(0.1, equipValue(card, player) - value);
          }
          if (typeof equipValue !== "number") {
            equipValue = 0;
          }
          if (method === "raw") {
            return equipValue;
          }
          if (method === "raw2") {
            return equipValue - value;
          }
          return Math.max(0.1, equipValue - value);
        }
      },
      result: {
        target: (player, target, card) => get.equipResult(player, target, card)
      }
    },
    enable: true,
    modTarget: true,
    allowMultiple: false,
    toself: true
  },
  hm_zhong_club: {
    audio: true,
    fullskin: true,
    derivation: "hm_shen_zhangjiao",
    type: "equip",
    skills: ["hm_zhong_club_skill"],
    destroy: "discardPile",
    selectTarget: -1,
    filterTarget(card, player, target) {
      if (player !== target) {
        return false;
      }
      const ranges = Array.from(Array(5)).map((value, index) => `equip${index + 1}`);
      if (get.is.mountCombined()) {
        ranges.removeArray(["equip3", "equip4"]);
        ranges.add("equip3_4");
      }
      if (get.itemtype(card) === "card") {
        const owner = get.owner(card, "judge");
        if (owner && !lib.filter.canBeGained(card, player, owner)) {
          return false;
        }
      }
      return ranges.some((range) => player.countEquipableSlot(range));
    },
    async prepareEquip(event, trigger, player) {
      if (!event.card.subtypes?.length) {
        const choices = [];
        for (let i = 0; i <= 5; i++) {
          if (player.hasEquipableSlot(i)) {
            choices.push(`equip${i}`);
          }
        }
        if (!choices.length) {
          return;
        }
        const result = await player.chooseControl(choices).set("prompt", "请选择置入【众】的装备栏").set("ai", () => _status.event.controls.randomGet()).forResult();
        event.card.subtypes = [result.control];
      }
    },
    ai: {
      equipValue(card, player) {
        return 5;
      },
      basic: {
        equipValue: 5,
        order: (card, player) => {
          const equipValue = get.equipValue(card, player) / 20;
          return player && player.hasSkillTag("reverseEquip") ? 8.5 - equipValue : 8 + equipValue;
        },
        useful: 2,
        value: (card, player, index, method) => {
          if (!player.getCards("e").includes(card) && !player.canEquip(card, true)) {
            return 0.01;
          }
          const info = get.info(card);
          const current = player.getEquip(info.subtype);
          const value = current && card !== current && get.value(current, player);
          let equipValue = info.ai.equipValue || info.ai.basic.equipValue;
          if (typeof equipValue === "function") {
            if (method === "raw") {
              return equipValue(card, player);
            }
            if (method === "raw2") {
              return equipValue(card, player) - value;
            }
            return Math.max(0.1, equipValue(card, player) - value);
          }
          if (typeof equipValue !== "number") {
            equipValue = 0;
          }
          if (method === "raw") {
            return equipValue;
          }
          if (method === "raw2") {
            return equipValue - value;
          }
          return Math.max(0.1, equipValue - value);
        }
      },
      result: {
        target: (player, target, card) => get.equipResult(player, target, card)
      }
    },
    enable: true,
    modTarget: true,
    allowMultiple: false,
    toself: true
  },
  hm_zhong_spade: {
    audio: true,
    fullskin: true,
    derivation: "hm_shen_zhangjiao",
    type: "equip",
    skills: ["hm_zhong_spade_skill"],
    destroy: "discardPile",
    selectTarget: -1,
    filterTarget(card, player, target) {
      if (player !== target) {
        return false;
      }
      const ranges = Array.from(Array(5)).map((value, index) => `equip${index + 1}`);
      if (get.is.mountCombined()) {
        ranges.removeArray(["equip3", "equip4"]);
        ranges.add("equip3_4");
      }
      if (get.itemtype(card) === "card") {
        const owner = get.owner(card, "judge");
        if (owner && !lib.filter.canBeGained(card, player, owner)) {
          return false;
        }
      }
      return ranges.some((range) => player.countEquipableSlot(range));
    },
    async prepareEquip(event, trigger, player) {
      if (!event.card.subtypes?.length) {
        const choices = [];
        for (let i = 0; i <= 5; i++) {
          if (player.hasEquipableSlot(i)) {
            choices.push(`equip${i}`);
          }
        }
        if (!choices.length) {
          return;
        }
        const result = await player.chooseControl(choices).set("prompt", "请选择置入【众】的装备栏").set("ai", () => _status.event.controls.randomGet()).forResult();
        event.card.subtypes = [result.control];
      }
    },
    ai: {
      equipValue(card, player) {
        return 5;
      },
      basic: {
        equipValue: 5,
        order: (card, player) => {
          const equipValue = get.equipValue(card, player) / 20;
          return player && player.hasSkillTag("reverseEquip") ? 8.5 - equipValue : 8 + equipValue;
        },
        useful: 2,
        value: (card, player, index, method) => {
          if (!player.getCards("e").includes(card) && !player.canEquip(card, true)) {
            return 0.01;
          }
          const info = get.info(card);
          const current = player.getEquip(info.subtype);
          const value = current && card !== current && get.value(current, player);
          let equipValue = info.ai.equipValue || info.ai.basic.equipValue;
          if (typeof equipValue === "function") {
            if (method === "raw") {
              return equipValue(card, player);
            }
            if (method === "raw2") {
              return equipValue(card, player) - value;
            }
            return Math.max(0.1, equipValue(card, player) - value);
          }
          if (typeof equipValue !== "number") {
            equipValue = 0;
          }
          if (method === "raw") {
            return equipValue;
          }
          if (method === "raw2") {
            return equipValue - value;
          }
          return Math.max(0.1, equipValue - value);
        }
      },
      result: {
        target: (player, target, card) => get.equipResult(player, target, card)
      }
    },
    enable: true,
    modTarget: true,
    allowMultiple: false,
    toself: true
  }
};
const pinyins = {};
const skills$E = {
  //PE神钟会
  pelinjie: {
    audio: "dclinjie",
    marktext: "凛",
    intro: {
      name: "凛界（凛）",
      name2: "凛",
      content: "mark"
    },
    group: "pelinjie_effect",
    trigger: { global: "roundStart" },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: `###${get.prompt(event.skill)}###对一名角色造成1点伤害然后令其获得一个「凛」标记`,
        ai(target) {
          return get.damageEffect(target, get.player(), get.player());
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.damage();
      target.addMark(event.name, 1);
    },
    subSkill: {
      effect: {
        audio: "dclinjie",
        forced: true,
        trigger: { global: "damageEnd" },
        filter(event, player) {
          const target = event.player;
          return target !== player && target.hasMark("pelinjie") && target.hasDiscardableCards(target, "h");
        },
        logTarget: "player",
        async content(event, trigger, player) {
          const target = event.targets[0];
          const hs = target.getDiscardableCards(target, "h");
          if (hs.length) {
            const damage = target.countCards("h") === 1;
            await target.chooseToDiscard({ forced: true, position: "h" });
            if (damage) {
              await target.damage();
            }
          }
        }
      }
    }
  },
  peduzhang: {
    audio: "dcduzhang",
    mod: {
      maxHandcard(player, num) {
        return num += player.countMark("pelinjie");
      }
    },
    locked: false,
    forced: true,
    trigger: { target: "useCardToTargeted" },
    filter(event, player) {
      return get.color(event.card) === "black" && event.targets?.length === 1;
    },
    async content(event, trigger, player) {
      await player.draw({ num: 1 });
      player.addMark("pelinjie", 1);
    }
  },
  pejianghuo: {
    audio: "dcjianghuo",
    derivation: "pelishi",
    juexingji: true,
    forced: true,
    skillAnimation: true,
    animationColor: "thunder",
    trigger: { player: "phaseBegin" },
    filter(event, player) {
      const num = game.filterPlayer((target) => target.hasMark("pelinjie")).map((target) => target.countMark("pelinjie")).reduce((sum, cur) => sum + cur, 0);
      return num > game.players.length;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const num = game.filterPlayer((target) => target !== player && target.hasMark("pelinjie")).map((target) => target.countMark("pelinjie")).reduce((sum, cur) => sum + cur, 0);
      game.filterPlayer((target) => target !== player).forEach((target) => target.clearMark("pelinjie"));
      if (num > 0) {
        player.addMark("pelinjie", num);
      }
      await player.draw({ num: player.countMark("pelinjie") });
      await player.gainMaxHp();
      await player.changeSkills(["pelishi"], ["pelinjie"]);
      player.markSkill("pelinjie");
    },
    ai: { combo: "pelinjie" }
  },
  pelishi: {
    audio: "dclishi",
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return !player.hasMark("pelinjie") || game.hasPlayer((current) => !player.getStorage("pelishi_effect").includes(current));
    },
    async cost(event, trigger, player) {
      if (!player.hasMark("pelinjie")) {
        event.result = {
          bool: true
        };
      } else {
        event.result = await player.chooseTarget({
          prompt: get.prompt(event.skill),
          prompt2: "你可失去任意枚“凛”并选择等量名角色令其于其下回合开始时跳过一个阶段",
          filterTarget(card, player2, target) {
            return !player2.getStorage("pelishi_effect").includes(target);
          },
          selectTarget: [1, player.countMark("pelinjie")],
          ai(target) {
            if (game.hasPlayer((current) => get.attitude(player, current) > 2 && current.hasCards("j", (card) => ["lebu", "bingliang", "shandian"].includes(card.name)))) {
              return get.attitude(player, target) * (target.hasCards("j", (card) => get.name(card) === "lebu") ? 2 : 0);
            }
            return -get.attitude(get.player(), target);
          }
        }).forResult();
      }
    },
    async content(event, trigger, player) {
      if (!player.hasMark("pelinjie")) {
        await player.damage("thunder");
      } else {
        const targets = event.targets;
        player.removeMark("pelinjie", targets.length);
        player.addSkill(`${event.name}_effect`);
        player.markAuto(`${event.name}_effect`, targets);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        intro: { content: "$的回合开始前，你可跳过其一个阶段" },
        trigger: { global: "phaseBefore" },
        filter(event, player) {
          return player.getStorage("pelishi_effect").includes(event.player);
        },
        logTarget: "player",
        async cost(event, trigger, player) {
          player.unmarkAuto(event.skill, [trigger.player]);
          const list = ["phaseZhunbei", "phaseJudge", "phaseDraw", "phaseUse", "phaseDiscard", "phaseJieshu"];
          const result = await player.chooseButton({
            createDialog: [`你可令${get.translation(trigger.player)}跳过本回合的一个阶段`, [list.map((phase) => [phase, get.translation(phase)]), "tdnodes"]],
            ai(button) {
              const { player: player2, target } = get.event();
              if (get.attitude(player2, target) > 0) {
                if (target.hasCards("j", (card) => ["shandian", "lebu", "bingliang"].includes(get.name(card)))) {
                  return button.link === "phaseJudge";
                }
                return button.link === "phaseDiscard";
              }
              if (target.countCards("h") > 4) {
                return button.link === "phaseUse";
              }
              return button.link === ["phaseUse", "phaseDraw"].randomGet();
            }
          }).set("target", trigger.player).forResult();
          if (result?.bool && result.links?.length) {
            event.result = {
              bool: true,
              cost_data: result.links
            };
          }
        },
        async content(event, trigger, player) {
          const {
            cost_data: [phase],
            targets: [target]
          } = event;
          target.skip(phase);
          game.log(target, "跳过了", `#y${get.translation(phase)}`);
        }
      }
    },
    ai: { combo: "pelinjie" }
  },
  //线下标记神马
  mark_shouli: {
    audio: "shouli",
    addMark(player, name) {
      const next = game.createEvent("gainShouli", false);
      next.player = player;
      next.num = 1;
      next.mark = name;
      next.setContent("emptyEvent");
      if (player.countMark(name)) {
        next.hasMark = true;
      }
      player.addMark(name, 1);
      return next;
    },
    changeMark(player, target, name) {
      const num = player.countMark(name);
      const next = game.createEvent("gainShouli", false);
      next.player = target;
      next.num = num;
      next.mark = name;
      next.setContent("emptyEvent");
      if (target.countMark(name)) {
        next.hasMark = true;
      }
      player.removeMark(name, num, false);
      target.addMark(name, num, false);
      game.log(player, "的", `#g“${get.translation(name)}”`, "被移动给了", target);
      return next;
    },
    enable: ["chooseToUse", "chooseToRespond"],
    hiddenCard(player, name) {
      if (name !== "sha" && name !== "shan") {
        return false;
      }
      return !player.getStorage("mark_shouli_used").includes(name) && game.hasPlayer((current) => current !== player && current.hasMark(`mark_shouli_${name === "sha" ? "jun" : "li"}`));
    },
    filter(event, player) {
      if (event.responded || event.mark_shouli || event.type === "wuxie") {
        return false;
      }
      return ["sha", "shan"].some((name) => {
        if (!game.hasPlayer((current) => current !== player && current.hasMark(`mark_shouli_${name === "sha" ? "jun" : "li"}`))) {
          return false;
        }
        if (player.getStorage("mark_shouli_used").includes(name)) {
          return false;
        }
        return event.filterCard(get.autoViewAs({ name, storage: { mark_shouli: true }, isCard: true }, "unsure"), player, event);
      });
    },
    filterTarget(card, player, target) {
      if (ui.selected.targets?.length) {
        const owner = ui.selected.targets[0];
        return target === owner.getNext() || target === owner.getPrevious();
      }
      if (target === player) {
        return false;
      }
      const event = _status.event;
      let evt = event;
      if (event._backup) {
        evt = event._backup;
      }
      return ["sha", "shan"].some((name) => {
        const card2 = get.autoViewAs({ name, storage: { mark_shouli: true }, isCard: true }, "unsure");
        const mark = `mark_shouli_${name === "sha" ? "jun" : "li"}`;
        if (!target.hasMark(mark) || player.getStorage("mark_shouli_used").includes(name)) {
          return false;
        }
        if (!evt.filterCard(card2, player, event)) {
          return false;
        }
        if (name === "sha") {
          return !evt.filterTarget || game.hasPlayer((current) => evt.filterTarget(card2, player, current));
        }
        return true;
      });
    },
    selectTarget: 2,
    complexTarget: true,
    multitarget: true,
    delay: false,
    locked: false,
    prompt: "将一名其他角色的所有“骏”/“骊”移动至其上家或下家，视为使用或打出一张【杀】/【闪】",
    async content(event, trigger, player) {
      const evt = event.getParent(2);
      evt.set("mark_shouli", true);
      const list = [];
      const backupx = _status.event;
      _status.event = evt;
      ["sha", "shan"].forEach((name2) => {
        const card = get.autoViewAs({ name: name2, storage: { mark_shouli: true }, isCard: true }, "unsure");
        const mark2 = `mark_shouli_${name2 === "sha" ? "jun" : "li"}`;
        if (!event.targets[0].hasMark(mark2) || player.getStorage("mark_shouli_used").includes(name2)) {
          return false;
        }
        if (name2 === "sha") {
          if (!evt.filterCard(card, player, evt)) {
            return false;
          }
          if (evt.filterTarget && !game.hasPlayer((current) => evt.filterTarget(card, player, current))) {
            return false;
          }
        } else if (!evt.filterCard(card, player, event)) {
          return false;
        }
        list.push(["", "", name2]);
      });
      _status.event = backupx;
      const result = list.length > 1 ? await player.chooseButton(["狩骊：选择你要视为使用的牌", [list, "vcard"]], true).set("ai", (button) => Math.random()).forResult() : {
        bool: true,
        links: list
      };
      if (!result?.bool) {
        return;
      }
      const name = result.links[0][2];
      const mark = `mark_shouli_${name === "sha" ? "jun" : "li"}`;
      get.info(event.name).changeMark(...event.targets, mark);
      player.addTempSkill("mark_shouli_used");
      player.markAuto("mark_shouli_used", name);
      if (evt.name === "chooseToUse") {
        game.broadcastAll((name2) => {
          lib.skill.mark_shouli_backup.viewAs = {
            name: name2,
            storage: { mark_shouli: true },
            isCard: true
          };
          lib.skill.mark_shouli_backup.prompt = `选择${get.translation(name2)}的目标`;
        }, name);
        evt.set("_backupevent", "mark_shouli_backup");
        evt.backup("mark_shouli_backup");
        evt.set("openskilldialog", `选择${get.translation(name)}的目标`);
        evt.set("norestore", true);
        evt.set("custom", {
          add: {},
          replace: { window() {
          } }
        });
      } else {
        delete evt.result.used;
        delete evt.result.skill;
        evt.result.card = get.autoViewAs({
          name,
          storage: { mark_shouli: true },
          isCard: true
        });
        evt.result.cards = [];
        evt.redo();
        return;
      }
      evt.goto(0);
    },
    mod: {
      targetInRange(card, player) {
        if (card?.storage?.mark_shouli) {
          return true;
        }
      },
      cardUsable(card, player) {
        if (card?.storage?.mark_shouli) {
          return Infinity;
        }
      }
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player, tag) {
        return get.info("mark_shouli").hiddenCard(player, tag === "respondSha" ? "sha" : "shan");
      },
      order: 2,
      result: {
        player(player, target) {
          const att = Math.max(8, get.attitude(player, target));
          if (ui.selected.targets?.length) {
            return 10 + att;
          }
          if (_status.event.type !== "phase") {
            return 9 - att;
          }
          if (!player.hasValueTarget({ name: "sha" }, false)) {
            return 0;
          }
          return 9 - att;
        }
      }
    },
    derivation: ["mark_shouli_jun", "mark_shouli_li"],
    group: "mark_shouli_init",
    global: "mark_shouli_effect",
    subSkill: {
      effect: {
        trigger: {
          player: ["phaseDrawBegin2", "useCardToPlayer", "damageBegin3", "damageBegin4"],
          source: "damageBegin1"
        },
        filter(event, player, name) {
          const target = name === "damageBegin1" ? event.source : event.player;
          if (!target?.isIn() || !game.hasPlayer((current) => current.hasSkill("mark_shouli"))) {
            return false;
          }
          const jun = target.countMark("mark_shouli_jun");
          const li = target.countMark("mark_shouli_li");
          if (name === "damageBegin4") {
            if (jun + li <= 0) {
              return false;
            }
            if (event.hasNature()) {
              return true;
            }
            return event.card?.name && ["wanjian", "nanman"].includes(event.card.name);
          }
          if (name === "useCardToPlayer") {
            return event.card?.name === "sha" && jun > 2;
          }
          if (event.name === "damage") {
            return li > 2;
          }
          return (jun > 1 || li > 1) && !event.numFixed;
        },
        async cost(event, trigger, player) {
          event.result = {
            bool: true,
            skill_popup: false
          };
        },
        async content(event, trigger, player) {
          const name = event.triggername;
          const target = trigger[name === "damageBegin1" ? "source" : "player"];
          const jun = target.countMark("mark_shouli_jun");
          const li = target.countMark("mark_shouli_li");
          const num = game.countPlayer((current) => current.hasSkill("mark_shouli"));
          switch (name) {
            case "phaseDrawBegin2": {
              if (jun > 1) {
                trigger.num += num;
              }
              if (li > 1) {
                trigger.num += num;
              }
              break;
            }
            case "useCardToPlayer": {
              trigger.target.addTempSkill("fengyin");
              break;
            }
            case "damageBegin1": {
              if (li > 2) {
                game.setNature(trigger, "thunder");
              }
              if (li > 3) {
                trigger.num += num;
              }
              break;
            }
            case "damageBegin3": {
              if (li > 2) {
                game.setNature(trigger, "thunder");
              }
              if (li > 3) {
                trigger.num += num;
              }
              break;
            }
            default: {
              if (jun > 0 && target.getPrevious()?.isIn() && target.getPrevious() !== target) {
                get.info("mark_shouli").changeMark(target, target.getPrevious(), "mark_shouli_jun");
              }
              if (li > 0 && target.getNext()?.isIn() && target.getNext() !== target) {
                get.info("mark_shouli").changeMark(target, target.getNext(), "mark_shouli_li");
              }
              break;
            }
          }
        },
        locked: false,
        mod: {
          globalFrom(from, to, distance) {
            if (!from.countMark("mark_shouli_jun")) {
              return;
            }
            const num = game.countPlayer((current) => current.hasSkill("mark_shouli"));
            return distance - num;
          },
          globalTo(from, to, distance) {
            if (!to.countMark("mark_shouli_li")) {
              return;
            }
            const num = game.countPlayer((current) => current.hasSkill("mark_shouli"));
            return distance + num;
          }
        }
      },
      used: {
        charlotte: true,
        onremove: true
      },
      backup: {
        async precontent(event, trigger, player) {
          event.result._apply_args = { addSkillCount: false };
          player.popup(event.result.card.name, "metal");
          await game.delayx();
          event.getParent().addCount = false;
        },
        filterCard: () => false,
        prompt: "请选择【杀】的目标",
        selectCard: -1,
        log: false
      },
      init: {
        audio: "mark_shouli",
        trigger: {
          player: "enterGame",
          global: "phaseBefore"
        },
        filter(event, player) {
          if (!game.hasPlayer((current) => current !== player)) {
            return false;
          }
          return event.name !== "phase" || game.phaseNumber === 0;
        },
        async cost(event, trigger, player) {
          event.result = {
            bool: true,
            targets: game.filterPlayer((current) => current !== player)
          };
        },
        async content(event, trigger, player) {
          const marks = [];
          for (let i = 0; i < 4; i++) {
            marks.push("mark_shouli_li");
            if (i < 3) {
              marks.push("mark_shouli_jun");
            }
          }
          for (const target of event.targets) {
            if (!marks.length) {
              break;
            }
            const mark = marks.randomRemove();
            await get.info("mark_shouli").addMark(target, mark);
          }
        }
      },
      jun: {
        markimage2: "image/card/chitu.png",
        nopop: true,
        intro: {
          name: "骏",
          content(storage, player) {
            const list = ["⚡你计算与其他角色的距离-1", "⚡摸牌阶段你额外摸一张牌", "⚡你使用【杀】指定目标时，令其本回合非锁定技失效"];
            const str = "⚡当你受到属性伤害或【南蛮入侵】、【万箭齐发】造成的伤害时，你将所有“骏”移动至你上家";
            if (typeof storage !== "number" || storage <= 0) {
              return str;
            }
            return `${str}<br>${list.slice(0, storage).join("<br>")}`;
          }
        }
      },
      li: {
        markimage2: "image/card/dilu.png",
        nopop: true,
        intro: {
          name: "骊",
          content(storage, player) {
            const list = ["⚡其他角色计算与你的距离+1", "⚡摸牌阶段你额外摸一张牌", "⚡你造成或受到的伤害视为雷电伤害", "⚡你造成或受到的伤害+1"];
            const str = "⚡当你受到属性伤害或【南蛮入侵】、【万箭齐发】造成的伤害时，你将所有“骊”移动至你下家";
            if (typeof storage !== "number" || storage <= 0) {
              return str;
            }
            return `${str}<br>${list.slice(0, storage).join("<br>")}`;
          }
        }
      }
    }
  },
  mark_hengwu: {
    audio: "hengwu",
    trigger: {
      global: "gainShouli"
    },
    filter(event, player) {
      const mark = event.mark;
      return event.hasMark && event.player.countMark(mark);
    },
    forced: true,
    logTarget: "player",
    async content(event, trigger, player) {
      await player.draw(trigger.player.countMark(trigger.mark));
    },
    ai: {
      combo: "mark_shouli"
    }
  },
  //SM神马超
  sm_tuanlian: {
    audio: 2,
    trigger: {
      global: "phaseBefore",
      player: ["enterGame", "damageEnd"],
      source: "damageSource"
    },
    filter(event, player, name) {
      if (event.name === "damage") {
        const key = name === "damageSource" ? "sourceDamage" : "damage";
        return player.getHistory(key, (evt) => evt.num > 0).indexOf(event) === 0;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    forced: true,
    async content(event, trigger, player) {
      const info = get.info(event.name);
      const characters2 = info.getCharacters(trigger.name === "damage" ? 1 : 5);
      info.addVisitors(characters2, player);
      const next = game.createEvent("addPrettyDerby", false);
      next.player = player;
      next.characters = characters2;
      next.setContent("emptyEvent");
      await next;
    },
    onremove(player, skill) {
      get.info(skill).removeVisitors(player.getStorage(skill), player);
    },
    getCharacters(num) {
      if (!_status.characterlist) {
        game.initCharacterList();
      }
      const list = _status.characterlist.filter((name) => {
        const title = get.characterTitle(name);
        if (title.includes("马")) {
          return true;
        }
        const surnames = get.characterSurname(name).map((list2) => list2.join(""));
        return surnames.length && surnames.some((surname) => surname.includes("马"));
      });
      if (!list.length) {
        return [];
      }
      return list.randomGets(Math.min(list.length, num));
    },
    getSkills(characters2, player) {
      if (!player.hasSkill("sm_jingji")) {
        return [];
      }
      const list = [];
      for (const name of characters2) {
        const { skills: skills2 } = get.character(name);
        if (Array.isArray(skills2) && skills2.length) {
          list.add(skills2[0]);
        }
      }
      return list;
    },
    addVisitors(characters2, player) {
      _status.characterlist.removeArray(characters2);
      game.log(player, "将", `#y${get.translation(characters2)}`, "加入了", "#g“赛马”");
      game.broadcastAll(
        (player2, characters3) => {
          player2.tempname.addArray(characters3);
          player2.$draw(
            characters3.map((name) => {
              const cardname = `huashen_card_${name}`;
              lib.card[cardname] = {
                fullimage: true,
                image: `character:${name}`
              };
              lib.translate[cardname] = get.rawName2(name);
              return game.createCard(cardname, " ", " ");
            }),
            "nobroadcast"
          );
        },
        player,
        characters2
      );
      player.markAuto("sm_tuanlian", characters2);
    },
    removeVisitors(characters2, player) {
      if (Array.isArray(player.tempname)) {
        game.broadcastAll((player2, characters3) => player2.tempname.removeArray(characters3), player, characters2);
      }
      player.unmarkAuto("sm_tuanlian", characters2);
      _status.characterlist.addArray(characters2);
    },
    marktext: "马",
    intro: {
      name: "赛马",
      mark(dialog, storage, player) {
        if (!storage || !storage.length) {
          return "当前没有“赛马”";
        }
        dialog.addSmall([storage, "character"]);
        const skills2 = lib.skill.sm_tuanlian.getSkills(storage, player);
        if (skills2.length) {
          dialog.addText(`<li>当前可用技能：${get.translation(skills2)}`, false);
        }
      }
    },
    ai: {
      combo: "sm_jingji"
    }
  },
  sm_jingji: {
    audio: 2,
    enable: "chooseToUse",
    filter(event, player) {
      if (!player.getStorage("sm_tuanlian").length) {
        return false;
      }
      const equip = get.autoViewAs({ name: "sm_prettyDerby", isCard: true });
      if (event.filterCard(equip, player, event)) {
        return true;
      }
      return get.inpileVCardList((info) => {
        if (!["trick", "basic"].includes(info[0])) {
          return false;
        }
        const card = get.autoViewAs({ name: info[2], nature: info[3], isCard: true });
        return event.filterCard(card, player, event);
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (!["trick", "basic"].includes(info[0])) {
            return false;
          }
          const card = get.autoViewAs({ name: info[2], nature: info[3], isCard: true });
          return event.filterCard(card, player, event);
        });
        const equip = get.autoViewAs({ name: "sm_prettyDerby", isCard: true });
        if (event.filterCard(equip, player, event)) {
          list.add(["equip", "", "sm_prettyDerby"]);
        }
        const dialog = ui.create.dialog("竞激", [list, "vcard"], "hidden");
        return dialog;
      },
      check(button) {
        if (get.event().getParent().type !== "phase") {
          return 1;
        }
        const card = get.autoViewAs({ name: button.link[2], nature: button.link[3], isCard: true });
        return get.player().getUseValue(card);
      },
      prompt(links, player) {
        return `移去一张“赛马”，然后视为使用${get.translation(links[0][3] || "")}${get.translation(links[0][2])}`;
      },
      backup(links, player) {
        return {
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            isCard: true
          },
          selectCard: -1,
          filterCard: () => false,
          popname: true,
          log: false,
          manualConfirm: true,
          async precontent(event, trigger, player2) {
            const characters2 = player2.getStorage("sm_tuanlian").slice(0);
            const result = characters2.length > 1 ? await player2.chooseButton(["竟激：移去一张“赛马”", [characters2, "character"]], true).forResult() : {
              bool: true,
              links: characters2
            };
            if (result?.bool && result.links?.length) {
              player2.logSkill("sm_jingji");
              get.info("sm_tuanlian").removeVisitors(result.links, player2);
              game.log(player2, "移去了", `#y${get.translation(result.links)}`);
              const next = game.createEvent("removePrettyDerby", false);
              next.player = player2;
              next.characters = result.links;
              next.setContent("emptyEvent");
              await next;
            }
            const type = get.type(event.result.card);
            player2.when({
              player: "useCard"
            }).filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player3) => {
              if (type === "basic") {
                trigger2.baseDamage ??= 1;
                trigger2.baseDamage++;
              }
              if (type === "trick") {
                await player3.draw();
              }
            });
          }
        };
      }
    },
    hiddenCard(player, name) {
      if (!["trick", "basic"].includes(get.type(name))) {
        return false;
      }
      return player.getStorage("sm_tuanlian").length;
    },
    init(player, skill) {
      player.addSkill("sm_jingji_load");
    },
    onremove(player, skill) {
      const skills2 = get.info("sm_tuanlian").getSkills(player.getStorage("sm_tuanlian"), player);
      if (skills2?.length) {
        player.removeInvisibleSkill(skills2);
      }
      player.removeSkill("sm_jingji_load");
    },
    ai: {
      order: 2,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
        }
      },
      combo: "sm_tuanlian"
    },
    group: ["sm_jingji_remove", "sm_jingji_trigger"],
    subSkill: {
      remove: {
        audio: "sm_jingji",
        trigger: { player: ["useSkill", "logSkillBegin"] },
        forced: true,
        locked: false,
        filter(event, player) {
          const skill = get.sourceSkillFor(event);
          const name = "sm_tuanlian";
          if (!player.invisibleSkills.includes(skill)) {
            return false;
          }
          return get.info(name).getSkills(player.getStorage(name), player).includes(skill);
        },
        async content(event, trigger, player) {
          const name = "sm_tuanlian";
          const skill = get.sourceSkillFor(trigger);
          const info = get.info(name);
          const visitors = player.getStorage(name).filter((namex) => get.character(namex).skills?.includes(skill));
          if (!visitors?.length) {
            return;
          }
          const result = visitors.length > 1 ? await player.chooseButton(["竟激：移去一张“赛马”", [visitors, "character"]], true).forResult() : {
            bool: true,
            links: visitors
          };
          if (result?.bool && result.links?.length) {
            info.removeVisitors(result.links, player);
            game.log(player, "移去了", `#y${get.translation(result.links[0])}`);
            const next = game.createEvent("removePrettyDerby", false);
            next.player = player;
            next.characters = result.links;
            next.setContent("emptyEvent");
            await next;
          }
        }
      },
      trigger: {
        trigger: { player: "triggerInvisible" },
        forced: true,
        forceDie: true,
        popup: false,
        charlotte: true,
        priority: 10,
        filter(event, player, name, indexedData) {
          if (event.revealed) {
            return false;
          }
          const info = get.info(event.skill);
          if (info.charlotte) {
            return false;
          }
          const skills2 = lib.skill.sm_tuanlian.getSkills(player.getStorage("sm_tuanlian"), player);
          game.expandSkills(skills2);
          if (!skills2.includes(event.skill)) {
            return false;
          }
          return lib.filter.filterTrigger(event, player, name, event.skill, indexedData);
        },
        async content(evt, event, player) {
          const info = get.info(event.skill);
          if (info.slient) {
            return;
          }
          const trigger = evt._trigger;
          info.check;
          let str;
          if (info.prompt) {
            str = info.prompt;
          } else {
            if (typeof info.logTarget === "string") {
              str = get.prompt(event.skill, trigger[info.logTarget], player);
            } else if (typeof info.logTarget === "function") {
              const logTarget = info.logTarget(trigger, player, trigger.triggername, trigger.indexedData);
              if (get.itemtype(logTarget)?.indexOf("player") === 0) {
                str = get.prompt(event.skill, logTarget, player);
              }
            } else {
              str = get.prompt(event.skill, null, player);
            }
          }
          if (typeof str === "function") {
            str = str(trigger, player, trigger.triggername, trigger.indexedData);
          }
          const next = player.chooseBool(`竟激：${str}`);
          next.set("yes", !info.check || info.check(trigger, player, trigger.triggername, trigger.indexedData));
          next.set("hsskill", event.skill);
          next.set("forceDie", true);
          next.set("ai", () => _status.event.yes);
          if (typeof info.prompt2 === "function") {
            next.set("prompt2", info.prompt2(trigger, player, trigger.triggername, trigger.indexedData));
          } else if (typeof info.prompt2 === "string") {
            next.set("prompt2", info.prompt2);
          } else if (info.prompt2 !== false) {
            if (lib.dynamicTranslate[event.skill]) {
              next.set("prompt2", lib.dynamicTranslate[event.skill](player, event.skill));
            } else if (lib.translate[`${event.skill}_info`]) {
              next.set("prompt2", lib.translate[`${event.skill}_info`]);
            }
          }
          if (trigger.skillwarn) {
            if (next.prompt2) {
              next.set("prompt2", `<span class="thundertext">${trigger.skillwarn}。</span>${next.prompt2}`);
            } else {
              next.set("prompt2", trigger.skillwarn);
            }
          }
          const result = await next.forResult();
          if (result?.bool) {
            if (!info.cost) {
              trigger.revealed = true;
            }
          } else {
            trigger.untrigger();
            trigger.cancelled = true;
          }
        }
      },
      load: {
        trigger: {
          player: ["addPrettyDerby", "removePrettyDerby"]
        },
        filter(event, player) {
          return event.characters?.length;
        },
        direct: true,
        firstDo: true,
        charlotte: true,
        init(player, skill) {
          player.addSkillBlocker(skill);
        },
        onremove(player, skill) {
          player.removeSkillBlocker(skill);
        },
        skillBlocker(skill, player) {
          if (!player.invisibleSkills.includes(skill) || skill === "sm_tuanlian" || skill === "sm_jingji") {
            return false;
          }
          return !player.hasSkill("sm_jingji");
        },
        async content(event, trigger, player) {
          const skills2 = get.info("sm_tuanlian").getSkills(trigger.characters, player);
          if (!skills2?.length) {
            return;
          }
          if (trigger.name === "addPrettyDerby") {
            player.addInvisibleSkill(skills2);
          } else {
            player.removeInvisibleSkill(skills2);
          }
        }
      }
    }
  },
  sm_kuangchi: {
    audio: 2,
    trigger: {
      source: "dieAfter"
    },
    filter(event, player) {
      const target = event.player;
      if (event.reserveOut || target.maxHp <= 0) {
        return false;
      }
      return player.getStorage("sm_tuanlian").length;
    },
    logTarget: "player",
    async cost(event, trigger, player) {
      const names = player.getStorage("sm_tuanlian");
      const target = trigger.player;
      const result = await player.chooseButton([get.prompt(event.skill, target), [names, "character"]]).set("ai", () => Math.random()).forResult();
      if (result?.bool && result.links?.length) {
        event.result = {
          bool: true,
          cost_data: result.links[0]
        };
      }
    },
    async content(event, trigger, player) {
      const target = trigger.player;
      const name = event.cost_data;
      trigger.cancel();
      const names = get.nameList(target);
      const result = names.length > 1 ? await player.chooseControl(names).set("ai", () => {
        const { controls } = get.event();
        return controls.slice().sort((a, b) => get.rank(b, true) - get.rank(a, true))[0];
      }).set("prompt", "请选择替换的武将牌").forResult() : { control: names[0] };
      if (result.control) {
        get.info("sm_tuanlian").removeVisitors([name], player);
        game.log(player, "移去了", `#y${get.translation(name)}`);
        const next = game.createEvent("removePrettyDerby", false);
        next.player = player;
        next.characters = [name];
        next.setContent("emptyEvent");
        await next;
        await target.reviveEvent(2);
        let doubleDraw = false;
        const num = (get.character(name).maxHp || get.character(name).hp) - (get.character(result.control).maxHp || get.character(result.control).hp);
        if (num !== 0) {
          if (typeof target.singleHp === "boolean") {
            if (num % 2 !== 0) {
              if (target.singleHp) {
                target.maxHp += (num + 1) / 2;
                target.singleHp = false;
              } else {
                target.maxHp += (num - 1) / 2;
                target.singleHp = true;
                doubleDraw = true;
              }
            } else {
              target.maxHp += num / 2;
            }
          } else {
            target.maxHp += num;
          }
          target.update();
        }
        await target.reinitCharacter(result.control, name);
        const owner = player["zombieshibian"] || player;
        game.broadcastAll(
          (player2, target2) => {
            target2["zombieshibian"] = player2;
            const identity = target2.identity = ((identity2) => {
              switch (identity2) {
                case "zhu":
                case "mingzhong":
                  return "zhong";
                case "zhu_false":
                  return "zhong_false";
                case "bZhu":
                  return "bZhong";
                case "rZhu":
                  return "rZhong";
                default:
                  return identity2;
              }
            })(player2.identity);
            if (!lib.translate[identity]) {
              lib.translate[identity] = "马";
            }
            const goon = player2 !== game.me && target2 !== game.me && player2.node.identity.classList.contains("guessing") && !player2.identityShown;
            if (goon) {
              if (target2.identityShown) {
                delete target2.identityShown;
              }
              if (!target2.node.identity.classList.contains("guessing")) {
                target2.node.identity.classList.add("guessing");
              }
            }
            target2.setIdentity(goon ? "cai" : void 0);
            if (target2.node.dieidentity) {
              target2.node.dieidentity.innerHTML = get.translation(target2.identity + 2);
            }
            if (typeof player2.ai?.shown === "number" && target2.ai) {
              target2.ai.shown = player2.ai.shown;
            }
            if (typeof player2.side === "boolean") {
              target2.side = player2.side;
              target2.node.identity.firstChild.innerHTML = player2.node.identity.firstChild.innerHTML;
              target2.node.identity.dataset.color = player2.node.identity.dataset.color;
            }
            if (_status._zombieshibian) {
              return;
            }
            _status.zombieshibian = true;
            if (typeof game.checkResult === "function") {
              const origin_checkResult = game.checkResult;
              game.checkResult = function() {
                const player3 = game.me._trueMe || game.me;
                if (game.players.filter((i) => i !== player3).every((i) => i["zombieshibian"] === (player3["zombieshibian"] || player3))) {
                  game.over(true);
                }
                return origin_checkResult.apply(this, arguments);
              };
            }
            if (typeof game.checkOnlineResult === "function") {
              const origin_checkOnlineResult = game.checkOnlineResult;
              game.checkOnlineResult = function(player3) {
                if (game.players.filter((i) => i !== player3).every((i) => i["zombieshibian"] === (player3["zombieshibian"] || player3))) {
                  return true;
                }
                return origin_checkOnlineResult.apply(this, arguments);
              };
            }
            if (typeof lib.element.player.getFriends === "function") {
              const origin_getFriends = lib.element.player.getFriends;
              const getFriends = function(func, includeDie) {
                const player3 = this;
                return [...origin_getFriends.apply(this, arguments), ...game[includeDie ? "filterPlayer2" : "filterPlayer"]((target3) => (target3["zombieshibian"] || target3) === (player3["zombieshibian"] || player3))].filter((i) => i !== player3 || func === true).unique().sortBySeat(player3);
              };
              lib.element.player.getFriends = getFriends;
              [...game.players, ...game.dead].forEach((i) => i.getFriends = getFriends);
            }
            if (typeof lib.element.player.isFriendOf === "function") {
              const origin_isFriendOf = lib.element.player.isFriendOf;
              const isFriendOf = function(player3) {
                if ((this["zombieshibian"] || this) === (player3["zombieshibian"] || player3)) {
                  return true;
                }
                return origin_isFriendOf.apply(this, arguments);
              };
              lib.element.player.isFriendOf = isFriendOf;
              [...game.players, ...game.dead].forEach((i) => i.isFriendOf = isFriendOf);
            }
            if (typeof lib.element.player.getEnemies === "function") {
              const origin_getEnemies = lib.element.player.getEnemies;
              const getEnemies = function(func, includeDie) {
                if (this["zombieshibian"]) {
                  return this["zombieshibian"].getEnemies(func, includeDie);
                }
                const player3 = this;
                return [...origin_getEnemies.apply(this, arguments), ...game[includeDie ? "filterPlayer2" : "filterPlayer"]((target3) => origin_getEnemies.apply(this, arguments).includes(target3["zombieshibian"] || target3))].filter((i) => player3 !== (i["zombieshibian"] || i)).unique().sortBySeat(player3);
              };
              lib.element.player.getEnemies = getEnemies;
              [...game.players, ...game.dead].forEach((i) => i.getEnemies = getEnemies);
            }
          },
          owner,
          target
        );
        target.ai.modAttitudeFrom = (from, to) => {
          if (to === from["zombieshibian"]) {
            return 114514;
          }
          return get.attitude(from["zombieshibian"] || from, to["zombieshibian"] || to);
        };
        target.ai.modAttitudeTo = (from, to, att) => {
          if (from === to["zombieshibian"]) {
            return 7;
          }
          return get.attitude(from["zombieshibian"] || from, to["zombieshibian"] || to);
        };
        if (doubleDraw) {
          await target.doubleDraw();
        }
      }
    },
    ai: {
      combo: "sm_tuanlian"
    }
  },
  sm_kulian: {
    audio: 2,
    trigger: {
      player: "enterGame",
      global: "phaseBefore"
    },
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    forced: true,
    logTarget(event, player) {
      return game.filterPlayer(() => true);
    },
    async content(event, trigger, player) {
      const func = async (target) => {
        const card = get.cardPile2((card3) => get.subtypes(card3).containsSome("equip3", "equip4", "equip6"), "random");
        if (card) {
          target.$gain2(card);
          await target.equip(card);
        }
        const card2 = game.createCard2("sm_mabian", "heart", 13);
        target.$gain2(card2);
        await target.equip(card2);
        await game.delayx();
      };
      await game.doAsyncInOrder(event.targets, func);
      game.addGlobalSkill("sm_kulian_prettyDerby");
    },
    derivation: "sm_kulian_reward",
    subSkill: {
      prettyDerby: {
        trigger: {
          global: ["roundStart", "roundEnd", "loseAsyncAfter", "gainAfter", "addJudgeAfter", "equipAfter", "addToExpansionAfter"],
          source: "damageSource",
          player: "loseAfter"
        },
        filter(event, player, name) {
          if (name === "roundStart") {
            return !event._PerttyDerbyed;
          }
          if (name === "roundEnd") {
            return _status.prettyDerbyDoing?.length;
          }
          if (event.name === "damage") {
            if (!player.getEquip("sm_mabian") || player.hasSkill("sm_kulian_damaged")) {
              return false;
            }
            return event.player !== player && event.player.getEquip("sm_mabian");
          }
          const es = event.getl(player)?.es;
          return es?.length && es.some((card) => card.name === "sm_mabian");
        },
        direct: true,
        async content(event, trigger, player) {
          switch (event.triggername) {
            case "roundStart": {
              trigger.set("_PerttyDerbyed", true);
              game.log("#y新一轮赛马比赛开始！");
              const list = get.info(event.name).initList(event.name);
              list.forEach((obj, index) => {
                game.log(`本轮赛马奖励${index + 1}：`, `#g${obj.info}[奖励：${obj.reward}]`);
              });
              game.broadcastAll((list2) => {
                _status.prettyDerbyDoing = list2;
              }, list);
              const target = game.findPlayer((current) => current.hasSkill("sm_kulian"));
              const func = async (target2) => {
                target2.markSkill(event.name);
              };
              if (target) {
                await func(target);
              } else {
                await game.doAsyncInOrder(
                  game.filterPlayer(() => true),
                  func
                );
              }
              return;
            }
            case "roundEnd": {
              game.log("#y赛马比赛结束");
              await game.doAsyncInOrder(
                game.filterPlayer(() => true),
                async (target) => {
                  target.unmarkSkill(event.name);
                }
              );
              const next = game.createEvent("perttyDerbyEnd", false);
              next.set("rewards", _status.prettyDerbyDoing);
              next.setContent("emptyEvent");
              await next;
              while (_status.prettyDerbyDoing.length) {
                const map = _status.prettyDerbyDoing.shift();
                game.broadcastAll((list) => {
                  _status.prettyDerbyDoing = list;
                }, _status.prettyDerbyDoing);
                const targets = game.filterPlayer((current) => current.getEquip("sm_mabian")).sort((a, b) => map.filter(b) - map.filter(a));
                if (!targets.length) {
                  continue;
                }
                if (targets.length > 1 && map.filter(targets[0]) === map.filter(targets[1])) {
                  continue;
                }
                const target = targets[0];
                game.log(target, "执行了赛马奖励：", `#g${map.reward}`);
                await map.content(target);
              }
              return;
            }
            case "damageSource": {
              player.addTempSkill("sm_kulian_damaged");
              player.logSkill("sm_kulian", trigger.player);
              await player.draw();
              return;
            }
            default: {
              game.log(player, "#y退赛了！");
              await event.trigger("withdrawPrettyDerby");
              return;
            }
          }
        },
        mod: {
          targetInRange(card, player) {
            if (player.getEquip("sm_mabian") && ["equip3", "equip4", "equip6"].some((slot) => player.getEquip(slot))) {
              return true;
            }
          }
        },
        intro: {
          content(_storage) {
            const list = _status.prettyDerbyDoing;
            if (!list?.length) {
              return "未进行比赛";
            }
            return list.map((obj, index) => App({ obj, index })).join("<br />");
            function App(props) {
              const { obj, index } = props;
              const html2 = String.raw;
              return html2`
								目标${index + 1}: ${obj.info}
								<br />
								<ul>
									<li><span style="font-family: yuanli">奖励：${obj.reward}</span></li>
								</ul>
							`;
            }
          }
        },
        rewardList: [
          {
            info: "受到伤害唯一最多",
            reward: "回复全部体力",
            filter(player) {
              return player.getRoundHistory("damage").reduce((sum, evt) => sum + evt.num, 0);
            },
            async content(player) {
              if (player.isDamaged()) {
                await player.recoverTo(player.maxHp);
              }
            }
          },
          {
            info: "手牌数唯一最多",
            reward: "手牌上限改为体力上限",
            filter(player) {
              return player.countCards("h");
            },
            async content(player) {
              player.addSkill("sm_kulian_yingzi");
            }
          },
          {
            info: "体力值唯一最高",
            reward: "增加1点体力上限",
            filter(player) {
              return player.getHp();
            },
            async content(player) {
              await player.gainMaxHp();
            }
          },
          {
            info: "装备区牌数唯一最多",
            reward: "获得一张其他角色的装备牌",
            filter(player) {
              return player.countCards("e");
            },
            async content(player) {
              const targets = game.filterPlayer((current) => current !== player && current.countGainableCards(player, "e"));
              if (!targets?.length) {
                return;
              }
              const result = targets.length > 1 ? await player.chooseTarget("获得一名其他角色一张装备牌", (card, player2, target2) => player2 !== target2 && target2.countGainableCards(player2, "e"), true).set("ai", (target2) => {
                const player2 = get.player();
                return get.effect(target2, { name: "shunshou_copy2", position: "e" }, player2, player2);
              }).forResult() : {
                bool: true,
                targets
              };
              if (!result?.bool || !result.targets?.length) {
                return;
              }
              const target = result.targets[0];
              await player.gainPlayerCard(target, "e", true);
            }
          },
          {
            info: "击杀数唯一最多",
            reward: "执行一个仅有出牌阶段的额外回合",
            filter(player) {
              return game.getRoundHistory("everything", (evt) => evt.name === "die" && evt.source === player).length;
            },
            async content(player) {
              const next = player.insertPhase("sm_kulian");
              next.phaseList = ["phaseUse"];
              next._noTurnOver = true;
            }
          },
          {
            info: "使用牌数唯一最多",
            reward: "摸五张牌",
            filter(player) {
              return player.getRoundHistory("useCard").length;
            },
            async content(player) {
              await player.draw(5);
            }
          },
          {
            info: "造成伤害唯一最多",
            reward: "使用【杀】造成伤害+1",
            filter(player) {
              return player.getRoundHistory("sourceDamage").reduce((sum, evt) => sum + evt.num, 0);
            },
            async content(player) {
              const skill = "sm_kulian_sha";
              player.addSkill(skill);
              player.addMark(skill, 1, false);
            }
          }
        ],
        initList(skill) {
          if (!_status.prettyDerbyList || _status.prettyDerbyList.length < 2) {
            _status.prettyDerbyList = get.info(skill).rewardList.slice(0);
          }
          const list = _status.prettyDerbyList.randomRemove(2);
          game.broadcastAll((list2) => {
            _status.prettyDerbyList = list2;
          }, _status.prettyDerbyList);
          return list;
        }
      },
      yingzi: {
        charlotte: true,
        mark: true,
        intro: {
          content: "你的手牌上限改为体力上限"
        },
        mod: {
          maxHandcardBase(player, num) {
            return player.maxHp;
          }
        }
      },
      sha: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "使用杀造成的伤害+#"
        },
        trigger: {
          source: "damageBegin1"
        },
        filter(event, player) {
          return event.card?.name === "sha" && player.countMark("sm_kulian_sha");
        },
        forced: true,
        async content(event, trigger, player) {
          trigger.num += player.countMark(event.name);
        }
      },
      damaged: {
        charlotte: true
      },
      reward: {
        nobracket: true,
        nopop: true
      }
    }
  },
  sm_lema: {
    audio: 2,
    enable: "chooseToUse",
    filter(event, player) {
      if (event.type === "wuxie") {
        return false;
      }
      return get.inpileVCardList((info) => get.type(info[2]) === "basic").some((card) => event.filterCard(get.autoViewAs({ name: card[2], nature: card[3], isCard: true }), player, event));
    },
    usable: 1,
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => get.type(info[2]) === "basic").filter((card) => event.filterCard(get.autoViewAs({ name: card[2], nature: card[3], isCard: true }), player, event));
        return ui.create.dialog("乐马", [list, "vcard"], "hidden");
      },
      check(button) {
        const event = get.event().getParent();
        if (event.type !== "phase") {
          return 1;
        }
        return get.player().getUseValue(get.autoViewAs({ name: button.link[2], nature: button.link[3], isCard: true }));
      },
      prompt(links) {
        const num = Math.max(
          1,
          game.countPlayer((current) => current.getCards("e", (card) => get.subtypes(card).containsSome("equip3", "equip4", "equip6")).length)
        );
        return `视为使用${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}并摸${get.cnNumber(num)}张牌`;
      },
      backup(links, player) {
        return {
          audio: "sm_lema",
          selectCard: -1,
          filterCard: () => false,
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            isCard: true
          },
          popname: true,
          log: false,
          async precontent(event, trigger, player2) {
            player2.logSkill("sm_lema");
            const num = Math.max(
              1,
              game.countPlayer((current) => current.getCards("e", (card) => get.subtypes(card).containsSome("equip3", "equip4", "equip6")).length)
            );
            await player2.draw(num);
          }
        };
      }
    },
    hiddenCard(player, name) {
      if (player.getStat("skill").sm_lema) {
        return false;
      }
      return get.type(name) === "basic";
    },
    ai: {
      order: 10,
      respondShan: true,
      respondSha: true,
      skillTagFilter(player, tag, arg) {
        if (arg === "respond") {
          return false;
        }
        return get.info("sm_lema").hiddenCard(player, tag.slice("respond".length).toLowerCase());
      },
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      backup: {}
    }
  },
  sm_chaoxuan: {
    audio: 2,
    trigger: {
      global: "perttyDerbyEnd"
    },
    filter(event, player) {
      return event.rewards?.length;
    },
    forced: true,
    async content(event, trigger, player) {
      for (const reward of trigger.rewards) {
        game.log(player, "执行了赛马奖励：", `#g${reward.reward}`);
        await reward.content(player);
      }
    }
  },
  sm_wandou: {
    audio: 2,
    trigger: {
      global: "withdrawPrettyDerby"
    },
    filter(event, player) {
      return event.player.getHp() !== 1;
    },
    check(event, player) {
      const bool1 = event.player.getHp() > 1;
      const bool2 = get.attitude(player, event.player) > 0;
      return bool1 !== bool2;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = event.targets[0];
      const num = target.getHp() - 1;
      if (num > 0) {
        await target.loseHp(num);
      } else if (num < 0) {
        await target.recoverTo(1);
      }
    }
  },
  sm_mabian_skill: {
    equipSkill: true,
    onremove: true,
    charlotte: true
  },
  //神贾诩
  jxlianpo: {
    init: () => {
      game.addGlobalSkill("jxlianpo_global");
    },
    onremove: () => {
      if (!game.hasPlayer((i) => i.hasSkill("jxlianpo", null, null, false), true)) {
        game.removeGlobalSkill("jxlianpo_global");
      }
    },
    trigger: { global: "dieAfter" },
    filter(event, player) {
      if (lib.skill.jxlianpo.getMax(event.player).length <= 1) {
        return false;
      }
      return event.source && event.source.isIn();
    },
    forced: true,
    logTarget: "source",
    getMax: (dead) => {
      const curs = game.players.slice(0);
      if (get.itemtype(dead) === "player" && !curs.includes(dead)) {
        curs.push(dead);
      }
      const map = {
        zhu: curs.reduce((count, current) => {
          let num = 0;
          if (["zhu", "zhong", "mingzhong"].includes(current.identity)) {
            num++;
          }
          num += current.countMark("jxlianpo_mark_zhong");
          return num + count;
        }, 0),
        fan: curs.reduce((count, current) => {
          let num = 0;
          if (current.identity === "fan") {
            num++;
          }
          num += current.countMark("jxlianpo_mark_fan");
          return num + count;
        }, 0),
        nei: curs.reduce((count, current) => {
          let num = 0;
          if (current.identity === "nei") {
            num++;
          }
          num += current.countMark("jxlianpo_mark_nei");
          return num + count;
        }, 0),
        commoner: curs.reduce((count, current) => {
          let num = 0;
          if (current.identity === "commoner") {
            num++;
          }
          num += current.countMark("jxlianpo_mark_commoner");
          return num + count;
        }, 0)
      };
      let population = 0;
      let identities = [];
      for (const i in map) {
        const curPopulation = map[i];
        if (curPopulation >= population) {
          if (curPopulation > population) {
            identities = [];
          }
          identities.add(i);
          population = curPopulation;
        }
      }
      return identities;
    },
    group: "jxlianpo_show",
    async content(event, trigger, player) {
      const source = trigger.source;
      source.chooseDrawRecover(2, true);
    },
    mark: true,
    intro: {
      content: () => `场上最大阵营为${lib.skill.jxlianpo.getMax().map((i) => {
        if (i === "zhu") {
          return "主忠";
        }
        return get.translation(`${i}2`);
      }).join("、")}`
    },
    $createButton(item, type, position, noclick, node) {
      node = ui.create.identityCard(item, position, noclick);
      node.link = item;
      return node;
    },
    subSkill: {
      show: {
        trigger: { global: "roundStart" },
        filter(event, player) {
          const list = lib.config.mode_config.identity.identity.lastItem.slice();
          list.removeArray(
            game.filterPlayer().map((i) => {
              let identity = i.identity;
              if (identity === "mingzhong") {
                identity = "zhong";
              }
              return identity;
            })
          );
          return list.length;
        },
        forced: true,
        async content(event, trigger, player) {
          const list = lib.config.mode_config.identity.identity.lastItem.slice();
          const needRemoved = game.filterPlayer().map((current) => {
            const identity = current.identity;
            return identity === "mingzhong" ? "zhong" : identity;
          });
          list.removeArray(needRemoved).unique();
          const cards2 = [list, createCard];
          const title = '###炼魄：请选择一个身份###<div class="text center">你选择的身份对应的阵营角色数于本轮内视为+1</div>';
          const next = player.chooseButton([title, cards2], true);
          const result = await next.forResult();
          const choice = result.links[0];
          const mark = `jxlianpo_mark_${choice}`;
          player.when({ global: "roundStart" }, false).assign({
            firstDo: true
          }).filter((evt) => evt !== trigger).step(async (event2, trigger2, player2) => {
            for (const storage in player2.storage) {
              if (storage.startsWith("jxlianpo_mark_")) {
                player2.clearMark(storage);
              }
            }
          });
          player.addMark(mark, 1, false);
          event.videoId = lib.status.videoId++;
          const createDialog = (player2, identity, id) => {
            const dialog = ui.create.dialog(`${get.translation(player2)}展示了“${get.translation(`${identity}2`)}”的身份牌<br>`, "forcebutton");
            dialog.videoId = id;
            ui.create.spinningIdentityCard(identity, dialog);
          };
          game.broadcastAll(createDialog, player, choice, event.videoId);
          let color = "";
          if (choice === "zhong") {
            color = "#y";
          } else if (choice === "fan") {
            color = "#g";
          } else if (choice === "nei") {
            color = "#b";
          }
          game.log(player, "展示了", `${color}${get.translation(`${choice}2`)}`, "的身份牌");
          await game.delay(3);
          game.broadcastAll("closeDialog", event.videoId);
          return;
          function createCard(item, type, position, noclick, node) {
            return lib.skill.jxlianpo.$createButton(item, type, position, noclick, node);
          }
        }
      },
      global: {
        mod: {
          maxHandcard(player, num) {
            if (!lib.skill.jxlianpo.getMax().includes("fan")) {
              return;
            }
            return num - game.countPlayer((current) => current !== player && current.hasSkill("jxlianpo"));
          },
          cardUsable(card, player, num) {
            if (card.name === "sha") {
              if (!lib.skill.jxlianpo.getMax().includes("fan")) {
                return;
              }
              return num + game.countPlayer((current) => current.hasSkill("jxlianpo"));
            }
          },
          attackRange(player, num) {
            if (!lib.skill.jxlianpo.getMax().includes("fan")) {
              return;
            }
            return num + game.countPlayer((current) => current.hasSkill("jxlianpo"));
          },
          cardSavable(card, player, target) {
            if (card.name === "tao" && !player.hasSkill("jxlianpo")) {
              if (!lib.skill.jxlianpo.getMax().includes("zhu")) {
                return;
              }
              if (player === target) {
                return;
              }
              return false;
            }
          },
          playerEnabled(card, player, target) {
            if (card.name === "tao" && !player.hasSkill("jxlianpo")) {
              if (!lib.skill.jxlianpo.getMax().includes("zhu")) {
                return;
              }
              if (player === target) {
                return;
              }
              return false;
            }
          }
        },
        trigger: { player: "dieAfter" },
        filter: () => !game.hasPlayer((i) => i.hasSkill("jxlianpo", null, null, false), true),
        silent: true,
        forceDie: true,
        content: () => {
          game.removeGlobalSkill("jxlianpo_global");
        }
      }
    }
  },
  jxzhaoluan: {
    audio: 4,
    logAudio: () => 2,
    trigger: { global: "dieBegin" },
    filter(event, player) {
      return event.getParent().name === "dying" && event.player.isIn();
    },
    limited: true,
    skillAnimation: true,
    animationColor: "metal",
    logTarget: "player",
    check(event, player) {
      if (event.source && event.source.isIn() && get.attitude(player, event.source) > 0 && player.identity === "fan") {
        return false;
      }
      return get.attitude(player, event.player) > 3.5;
    },
    async content(event, trigger, player) {
      const target = trigger.player;
      player.awakenSkill(event.name);
      trigger.cancel();
      const skills2 = target.getSkills(null, false, false).filter((skill) => {
        const info = get.info(skill);
        if (info && !info.charlotte && !get.is.locked(skill)) {
          return true;
        }
      });
      if (skills2.length) {
        await target.removeSkills(skills2);
      }
      await target.gainMaxHp(3);
      const num = 3 - target.getHp(true);
      if (num > 0) {
        await target.recover(num);
      }
      target.draw(4);
      player.addSkill("jxzhaoluan_effect");
      player.markAuto("jxzhaoluan_effect", target);
    },
    ai: {
      expose: 0.5,
      threaten: 3
    },
    subSkill: {
      effect: {
        audio: ["jxzhaoluan3.mp3", "jxzhaoluan4.mp3"],
        enable: "phaseUse",
        filter(event, player) {
          return player.getStorage("jxzhaoluan_effect").some((i) => i.isIn());
        },
        filterTarget(card, player, target) {
          return !player.getStorage("jxzhaoluan_hit").includes(target);
        },
        line: false,
        locked: true,
        charlotte: true,
        promptfunc() {
          const bodies = _status.event.player.getStorage("jxzhaoluan_effect").filter((i) => i.isIn());
          return `选择一名角色，你令${get.translation(bodies)}${bodies.length > 1 ? "中的一人" : ""}减1点体力上限，然后你对选择的角色造成1点伤害。`;
        },
        delay: false,
        async content(event, trigger, player) {
          const bodies = player.getStorage("jxzhaoluan_effect").filter((target2) => target2.isIn());
          let result;
          if (bodies.length === 1) {
            result = { bool: true, targets: bodies };
          } else {
            result = await player.chooseTarget("兆乱：请选择被减上限的傀儡", true, (card, player2, target2) => get.event().targets.includes(target2)).set("targets", bodies).set("ai", (target2) => 8 - get.attitude(_status.event.player, target2)).forResult();
          }
          if (!result.bool) {
            return;
          }
          const target = result.targets[0];
          player.line(target);
          await target.loseMaxHp();
          await game.delayex();
          player.line(target);
          await event.targets[0].damage();
          if (!player.storage.jxzhaoluan_hit) {
            player.when("phaseUseAfter").step(async (event2, trigger2, player2) => {
              delete player2.storage.jxzhaoluan_hit;
            });
          }
          player.markAuto("jxzhaoluan_hit", event.targets);
        },
        ai: {
          order: 9,
          result: {
            player(player) {
              const bodies = player.getStorage("jxzhaoluan_effect").filter((i) => i.isIn());
              let body;
              if (bodies.length === 1) {
                body = bodies[0];
              } else {
                body = bodies.sort((a, b) => get.attitude(player, a) - get.attitude(player, b))[0];
              }
              if (get.attitude(player, body) > 4 && !body.isDamaged() && body.getHp() <= 2) {
                return -10;
              }
              return 0;
            },
            target(player, target) {
              return Math.sign(get.damageEffect(target, player, target));
            }
          }
        }
      }
    }
  },
  //26珍藏神黄月英
  zc26_cangqiao: {
    trigger: {
      player: "useCard",
      global: "roundStart"
    },
    filter(event, player) {
      if (event.name === "useCard") {
        if (!["duanjian", "serafuku", "yonglv"].includes(event.card.name)) {
          return false;
        }
        return player.countCards("h") < player.maxHp;
      }
      return true;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseBool(get.prompt(event.skill), () => true).forResult();
    },
    async content(event, trigger, player) {
      if (trigger.name === "useCard") {
        await player.drawTo(player.maxHp);
      } else {
        if (!_status.zc26_cangqiao) {
          game.broadcastAll(() => {
            _status.zc26_cangqiao = [
              { name: "duanjian", number: 13, suit: "club" },
              { name: "serafuku", number: 9, suit: "heart" },
              { name: "yonglv", number: 13, suit: "club" }
            ];
            for (const info of _status.zc26_cangqiao) {
              if (!lib.inpile.includes(info.name)) {
                lib.inpile.add(info.name);
              }
            }
          });
        }
        const list = ["duanjian", "serafuku", "yonglv"];
        const cards2 = [];
        for (const name of list) {
          let card = get.discardPile(name);
          if (card) {
            cards2.add(card);
          } else {
            const info = _status.zc26_cangqiao.find((i) => i.name === name);
            if (info) {
              game.broadcastAll((info2) => {
                _status.zc26_cangqiao.remove(info2);
              }, info);
              card = game.createCard2(name, info.suit, info.number);
              card.addCardtag("gifts");
              cards2.add(card);
            }
          }
        }
        if (cards2.length) {
          await player.gain(cards2, "draw2");
        }
      }
    }
  },
  zc26_shenxie: {
    usable: 1,
    trigger: { global: "useCardAfter" },
    filter(event, player) {
      if (!event.targets.includes(player) || event.targets.length !== 1) {
        return false;
      } else if (get.color(event.card) !== "black") {
        return false;
      }
      const storage = player.getStorage(
        "zc26_shenxie",
        lib.inpile.filter((name) => get.type(name) === "delay")
      );
      if (!storage.some((name) => player.hasUseTarget(name))) {
        return false;
      }
      return game.hasPlayer((current) => current.countCards("ej", { type: "equip" }));
    },
    async cost(event, trigger, player) {
      const storage = player.getStorage(
        event.skill,
        lib.inpile.filter((name) => get.type(name) === "delay")
      ).filter((name) => player.hasUseTarget(name));
      const choice = storage.map((name) => [name, player.getUseValue(get.autoViewAs({ name, isCard: false }, "unsure"))]).reduce(
        (max, info) => {
          if (max[1] < info[1]) {
            return info;
          }
          return max;
        },
        [null, 0]
      )[0];
      const result = await player.chooseTarget(get.prompt2(event.skill), (_, player2, target) => target.countCards("ej", { type: "equip" })).set("ai", (target) => {
        const { player: player2, choice: choice2 } = get.event();
        const es = target.getCards("ej", { type: "equip" });
        if (!choice2) {
          return 0;
        }
        if (get.attitude(player2, target) > 0) {
          return 10 - Math.min(...es.map((card) => get.equipValue(card)));
        }
        return Math.max(...es.map((card) => get.equipValue(card)));
      }).set("choice", choice).forResult();
      event.result = {
        bool: result?.bool,
        targets: result?.targets,
        cost_data: choice
      };
    },
    async content(event, trigger, player) {
      const {
        targets: [target],
        cost_data: choice
      } = event;
      const result = await player.choosePlayerCard(target, `###神械###将${get.translation(target)}场上的一张牌当作延时锦囊牌使用`, "ej", true).set("filterButton", ({ link }) => get.type(link) === "equip").set("ai", ({ link }) => {
        const { player: player2, target: target2 } = get.event();
        if (get.attitude(player2, target2) > 0) {
          return 10 - get.equipValue(link);
        }
        return get.equipValue(link);
      }).forResult();
      if (result?.bool && result.cards?.length) {
        const storage = player.getStorage(
          event.name,
          lib.inpile.filter((name) => get.type(name) === "delay")
        ).filter((name) => player.hasUseTarget(name));
        const { links } = await player.chooseVCardButton(true, "神械：请选择要使用的延时锦囊牌", storage.slice()).set("ai", ({ link: [_, __, name] }) => {
          const { player: player2, choice: choice2 } = get.event();
          if (choice2) {
            return name === choice2;
          }
          return player2.getUseValue(name);
        }).set("choice", choice).forResult();
        if (links?.length) {
          const name = links[0][2];
          storage.remove(name);
          if (!storage.length) {
            storage.addArray(lib.inpile.filter((name2) => get.type(name2) === "delay"));
          }
          player.setStorage(event.name, storage, true);
          await player.chooseUseTarget({ name, storage: { equipEnable: true }, isCard: false }, result.cards, true);
        }
      }
    }
  },
  zc26_huaxiu: {
    usable: 1,
    enable: "phaseUse",
    onChooseToUse(event) {
      if (game.online) {
        return;
      }
      event.set(
        "zc26_huaxiu",
        ["duanjian", "serafuku", "yonglv"].filter((i) => i in lib.card)
      );
    },
    filter(event, player) {
      return event.zc26_huaxiu?.length;
    },
    manualConfirm: true,
    async content(event, trigger, player) {
      const list = event.getParent(2).zc26_huaxiu.map((name) => [get.type(name), "", name]);
      const result = await player.chooseButton(true, ["化朽", "选择要升级的装备", [list, "vcard"]]).set("ai", (button) => {
        const player2 = get.player();
        const name = button.link[2];
        const num = game.countPlayer((current) => {
          const hs = current.countVCards("h", (card) => name === card.name);
          const es = current.countVCards("e", (card) => name === card.name);
          const js = current.countVCards("j", (card) => get.type(card) === "delay" && card.storage.equipEnable && name === get.name(card, false));
          return get.sgnAttitude(player2, current) * (es + js + current === player2 ? hs : 0);
        });
        return num;
      }).forResult();
      if (result?.bool && result.links?.length) {
        let check = function(name2, target, method) {
          if (method === "e") {
            return target.hasVCard({ name: name2 }, "e");
          } else if (method === "j") {
            return target.hasVCard((card) => {
              if (!card.storage?.equipEnable) {
                return false;
              }
              return card.cards.some((cardx) => cardx.name === name2);
            }, "j");
          }
          return false;
        };
        const name = result.links[0][2];
        const map = {
          duanjian: "zc26_zhuge",
          serafuku: "zc26_bagua",
          yonglv: "zc26_lingling"
        };
        game.log(player, "将", `#y${get.translation({ name })}`, "升级为", `#y${get.translation({ name: map[name] })}`);
        player.addTempSkill("zc26_huaxiu_restore", { player: "phaseBegin" });
        game.broadcastAll(
          (name2, player2, map2) => {
            if (!_status.zc26_huaxiu_origin) {
              _status.zc26_huaxiu_origin = {};
              for (const name3 of ["duanjian", "serafuku", "yonglv"]) {
                _status.zc26_huaxiu_origin[name3] = {
                  info: lib.card[name3],
                  translate: lib.translate[name3],
                  translate2: lib.translate[`${name3}_info`]
                };
              }
            }
            lib.card[name2] = lib.card[map2[name2]];
            lib.translate[name2] = lib.translate[map2[name2]];
            lib.translate[`${name2}_info`] = lib.translate[`${map2[name2]}_info`];
            _status.zc26_huaxiu ??= {};
            _status.zc26_huaxiu[name2] ??= [];
            _status.zc26_huaxiu[name2].add(player2);
            lib.init.sheet(`
							.card[data-card-name = "${name2}"]>.image {
								background-image: url(${lib.assetURL}image/card/${map2[name2]}.png) !important;
							}
						`);
          },
          name,
          player,
          map
        );
        const removeSkill = get.skillsFromEquips([{ name }]);
        const addSkill = get.skillsFromEquips([{ name: map[name] }]);
        for (const current of game.players) {
          const keepSkills = Object.values(current.additionalSkills).flat();
          const removeSkill2 = removeSkill.slice().removeArray(keepSkills);
          if (removeSkill2.length) {
            current.removeSkill(removeSkill2);
          }
          if (check(name, current, "j")) {
            current.addSkill(addSkill);
          }
          if (check(name, current, "e")) {
            current.addEquipTrigger({ name: map[name] });
          }
          const vcards = current.getVCards("e", { name });
          while (vcards.length) {
            const vcard = vcards.shift();
            current.$addVirtualEquip(vcard, vcard.cards);
          }
        }
      }
    },
    subSkill: {
      restore: {
        charlotte: true,
        onremove(player, skill) {
          get.info(skill).contentx.apply(this, [null, null, player]);
        },
        trigger: { player: "phaseBegin" },
        filter(event, player) {
          for (const name of ["duanjian", "serafuku", "yonglv"]) {
            if (_status.zc26_huaxiu?.[name]?.includes(player)) {
              return true;
            }
          }
          return false;
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          get.info(event.name).contentx.apply(this, arguments);
        },
        contentx(event, trigger, player) {
          game.broadcastAll((player2) => {
            for (const name of ["duanjian", "serafuku", "yonglv"]) {
              if (_status.zc26_huaxiu?.[name]?.includes(player2)) {
                _status.zc26_huaxiu[name].remove(player2);
                lib.init.sheet(`
									.card[data-card-name = "${name}"]>.image {
										background-image: url(${lib.assetURL}image/card/${name}.png) !important;
									}
								`);
              }
            }
          }, player);
          function check(name, target, method) {
            if (method === "e") {
              return target.hasVCard({ name }, "e");
            } else if (method === "j") {
              return target.hasVCard((card) => {
                if (!card.storage?.equipEnable) {
                  return false;
                }
                return card.cards.some((cardx) => cardx.name === name);
              }, "j");
            }
            return false;
          }
          const map = {
            duanjian: "zc26_zhuge",
            serafuku: "zc26_bagua",
            yonglv: "zc26_lingling"
          };
          for (const name of ["duanjian", "serafuku", "yonglv"]) {
            if (name in _status.zc26_huaxiu && !_status.zc26_huaxiu[name].length) {
              game.log(`#y${get.translation({ name })}`, "的效果还原了");
              game.broadcastAll((name2) => {
                delete _status.zc26_huaxiu[name2];
              }, name);
              lib.card[name] = _status.zc26_huaxiu_origin[name].info;
              lib.translate[name] = _status.zc26_huaxiu_origin[name].translate;
              lib.translate[`${name}_info`] = _status.zc26_huaxiu_origin[name].translate2;
              const addSkill = get.skillsFromEquips([{ name }]);
              const removeSkill = get.skillsFromEquips([{ name: map[name] }]);
              for (const current of game.players) {
                const keepSkills = Object.values(current.additionalSkills).flat();
                const removeSkill2 = removeSkill.slice().removeArray(keepSkills);
                if (removeSkill2.length) {
                  current.removeSkill(removeSkill2);
                }
                if (check(name, current, "j")) {
                  current.addSkill(addSkill);
                } else if (check(name, current, "e")) {
                  current.addEquipTrigger({ name });
                }
                const vcards = current.getVCards("e", { name });
                while (vcards.length) {
                  const vcard = vcards.shift();
                  current.$addVirtualEquip(vcard, vcard.cards);
                }
              }
            }
          }
        }
      }
    },
    ai: {
      order: 10,
      result: {
        player: 1
      }
    }
  },
  zc26_zhuge_skill: {
    equipSkill: true,
    firstDo: true,
    locked: true,
    audio: "zhuge_skill",
    mod: {
      cardUsable(card, player, num) {
        const cards2 = player.getCards("e", (card2) => get.name(card2) === "zc26_zhuge");
        if (card.name === "sha") {
          if (!cards2.length || player.hasSkill("zc26_zhuge_skill", null, false) || cards2.some((card2) => card2 !== _status.zc26_zhuge_temp && !ui.selected.cards.includes(card2))) {
            if (get.is.versus() || get.is.changban()) {
              return num + 3;
            }
            return Infinity;
          }
        }
      },
      cardEnabled2(card, player) {
        if (!_status.event.addCount_extra || player.hasSkill("zc26_zhuge_skill", null, false)) {
          return;
        }
        const cards2 = player.getCards("e", (card2) => get.name(card2) === "zc26_zhuge");
        if (card && cards2.includes(card)) {
          let cardz;
          try {
            cardz = get.card();
          } catch (e) {
            return;
          }
          if (!cardz || cardz.name !== "sha") {
            return;
          }
          _status.zc26_zhuge_temp = card;
          const bool = lib.filter.cardUsable(get.autoViewAs(cardz, ui.selected.cards.concat([card])), player);
          delete _status.zc26_zhuge_temp;
          if (!bool) {
            return false;
          }
        }
      }
    },
    trigger: { player: ["useCard1", "useCardToPlayered"] },
    filter(event, player, triggername) {
      if (event.card.name !== "sha") {
        return false;
      }
      if (event.name === "useCard") {
        return !event.audioed && player.countUsed("sha", true) > 1 && event.getParent().type === "phase";
      }
      return game.dead.length && event.target.countCards("h");
    },
    async cost(event, trigger, player) {
      if (trigger.name === "useCard") {
        event.result = { bool: true };
      } else {
        event.result = await player.chooseTarget(`###${get.prompt(event.skill)}###令任意名死亡角色依次观看${get.translation(trigger.target)}手牌并可以重铸其中一张牌`, [1, game.dead.length]).set("filterTarget", (_, player2, target) => target.isDead()).set("ai", (target) => get.attitude(get.player(), target) > 0).set("deadTarget", true).forResult();
      }
    },
    async content(event, trigger, player) {
      if (trigger.name === "useCard") {
        trigger.audioed = true;
      } else {
        event.targets.sortBySeat(_status.currentPhase);
        for (const current of event.targets) {
          if (!current.isDead()) {
            continue;
          }
          await current.viewHandcards(trigger.target);
          const cards2 = trigger.target.getCards("h", (card) => lib.filter.cardRecastable(card, trigger.target, trigger.target));
          if (!cards2.length) {
            return;
          }
          const result = await current.chooseCardButton(`请选择重铸${get.translation(trigger.target)}的一张手牌`, cards2).set("ai", ({ link }) => {
            const { player: player2, target } = get.event();
            if (get.attitude(player2, target) > 0) {
              return 20 - get.value(link);
            }
            return get.value(link);
          }).set("target", trigger.target).set("forceDie", true).forResult();
          if (result?.bool && result.links?.length) {
            await trigger.target.recast(result.links);
          }
        }
      }
    }
  },
  zc26_bagua_skill: {
    equipSkill: true,
    audio: "bagua_skill",
    trigger: { player: ["chooseToRespondBegin", "chooseToUseBegin"] },
    filter(event, player) {
      if (event.responded) {
        return false;
      }
      if (event.zc26_bagua_skill) {
        return false;
      }
      if (!event.filterCard || !event.filterCard(get.autoViewAs({ name: "shan" }, []), player, event)) {
        return false;
      }
      if (event.name === "chooseToRespond" && !lib.filter.cardRespondable(get.autoViewAs({ name: "shan" }, []), player, event)) {
        return false;
      }
      if (player.hasSkillTag("unequip2")) {
        return false;
      }
      const evt = event.getParent();
      if (evt.player && evt.player.hasSkillTag("unequip", false, {
        name: evt.card ? evt.card.name : null,
        target: player,
        card: evt.card
      })) {
        return false;
      }
      return true;
    },
    check(event, player) {
      if (!event) {
        return true;
      }
      if (event.ai) {
        const ai = event.ai;
        const tmp = _status.event;
        _status.event = event;
        const result = ai({ name: "shan" }, _status.event.player, event);
        _status.event = tmp;
        return result > 0;
      }
      const type = event.name === "chooseToRespond" ? "respond" : "use";
      const evt = event.getParent();
      if (player.hasSkillTag("noShan", null, type)) {
        return false;
      }
      if (!evt || !evt.card || !evt.player || player.hasSkillTag("useShan", null, type)) {
        return true;
      }
      if (evt.card && evt.player && player.isLinked() && game.hasNature(evt.card) && get.attitude(player, evt.player._trueMe || evt.player) > 0) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player) {
      trigger.zc26_bagua_skill = true;
      if (game.dead.length) {
        const { targets } = await player.chooseTarget(`###${get.prompt(event.name)}###令一名死亡角色卜算3`).set("filterTarget", (_, player2, target) => target.isDead()).set("ai", (target) => get.attitude(get.player(), target) > 0).set("deadTarget", true).forResult();
        if (targets?.length) {
          player.line(targets[0]);
          game.log(player, "令", targets[0], "卜算3");
          await targets[0].chooseToGuanxing(3).set("forceDie", true);
        }
      }
      const result = await player.judge("zc26_bagua", (card) => get.color(card) === "red" ? 1.5 : -0.5).set("judge2", (result2) => result2.bool).forResult();
      if (result.bool > 0) {
        trigger.untrigger();
        trigger.set("responded", true);
        trigger.result = { bool: true, card: get.autoViewAs({ name: "shan", isCard: true }, []), cards: [] };
      }
    },
    ai: {
      respondShan: true,
      freeShan: true,
      skillTagFilter(player, tag, arg) {
        if (tag !== "respondShan" && tag !== "freeShan") {
          return;
        }
        if (player.hasSkillTag("unequip2")) {
          return false;
        }
        if (!arg || !arg.player) {
          return true;
        }
        if (arg.player.hasSkillTag("unequip", false, {
          target: player
        })) {
          return false;
        }
        return true;
      },
      effect: {
        target(card, player, target, effect) {
          if (target.hasSkillTag("unequip2")) {
            return;
          }
          if (player.hasSkillTag("unequip", false, {
            name: card ? card.name : null,
            target,
            card
          }) || player.hasSkillTag("unequip_ai", false, {
            name: card ? card.name : null,
            target,
            card
          })) {
            return;
          }
          if (get.tag(card, "respondShan")) {
            return 0.5;
          }
        }
      }
    }
  },
  zc26_lingling_skill: {
    equipSkill: true,
    trigger: {
      player: "phaseZhunbeiBegin",
      global: "roundEnd"
    },
    getIndex(event, player) {
      if (event.name === "phaseZhunbei") {
        return 1;
      }
      const es = player.getCards("e", (card) => get.info(card)?.name === "zc26_lingling");
      const js = player.getCards("j", (card) => {
        if (get.type(card) !== "delay") ;
        const vcard = card[card.cardSymbol];
        if (!vcard || !vcard.storage?.equipEnable) {
          return false;
        }
        return vcard.cards.some((cardx) => get.info(cardx)?.name === "zc26_lingling");
      });
      return es.concat(js);
    },
    filter(event, player, triggername, card) {
      if (event.name === "phaseZhunbei") {
        return true;
      }
      if (!game.dead.length) {
        return false;
      }
      return !event.next[event.next.length - 1]?.zc26_lingling?.includes(card);
    },
    forced: true,
    async content(event, trigger, player) {
      if (trigger.name === "phaseZhunbei") {
        const { targets } = await player.chooseTarget(`軨軨：选择一名角色对其造成1点雷电伤害`, true).set("ai", (target) => get.damageEffect(target, get.player(), get.player(), "thunder")).forResult();
        if (targets?.length) {
          await targets[0].damage(player, "thunder");
        }
      } else {
        trigger.next[trigger.next.length - 1].zc26_lingling ??= [];
        trigger.next[trigger.next.length - 1].zc26_lingling.add(event.indexedData);
        const targets = game.dead.slice();
        const map = await game.chooseAnyOL(targets, get.info(event.name).chooseControl, [player, event.indexedData]).forResult();
        for (const target of targets) {
          const source = game.findPlayer((current) => current.hasCard((card) => card === event.indexedData, "ej"));
          let aim;
          const control = map.get(target).control;
          if (control === "上家") {
            aim = source?.previous;
          } else if (control === "下家") {
            aim = source?.next;
          }
          if (!source || !aim) {
            return;
          }
          await target.moveCard(true, source, aim, (card) => {
            const cardx = get.event().card;
            if (get.itemtype(card) === "card") {
              return card === cardx;
            }
            return card === cardx[cardx.cardSymbol];
          }).set("card", event.indexedData).set("forceDie", true).setContent(async (event2, trigger2, player2) => {
            if (player2.canMoveCard(null, event2.nojudge, event2.sourceTargets, event2.aimTargets, event2.filter, event2.canReplace ? "canReplace" : "noReplace")) {
              const source2 = event2.sourceTargets[0];
              const aim2 = event2.aimTargets[0];
              let position = "j";
              event2.result = {
                bool: true,
                links: [event2.card],
                card: event2.card
              };
              if (source2.getCards("e").includes(event2.card)) {
                position = "e";
                await aim2.equip(event2.card);
              } else {
                await aim2.addJudge(event2.card, event2.card?.cards);
              }
              if (event2.card.cards?.length) {
                source2.$give(event2.card.cards, aim2, false);
              }
              game.log(source2, "的", event2.card, "被移动给了", aim2);
              event2.result.position = position;
              await game.delay();
            }
          });
        }
      }
    },
    chooseControl(player, source, card, eventId) {
      return player.chooseControl(["上家", "下家"]).set("prompt", "軨軨：秘密选择一个方向").set("prompt2", `令${get.translation(source)}的${get.translation(card)}移动至其上家或下家`).set("ai", () => {
        const controls = get.event().controls.slice();
        return get.event().getRand() < 0.5 ? controls[0] : controls[1];
      }).set("id", eventId).set("_global_waiting", true);
    }
  },
  //神貂蝉&高达一号
  xinlonghun: {
    audio: "longhun",
    mod: {
      aiOrder(player, card, num) {
        if (num <= 0 || !player.isPhaseUsing() || player.needsToDiscard() < 2) {
          return num;
        }
        const suit = get.suit(card, player);
        if (suit === "heart") {
          return num - 3.6;
        }
      },
      aiValue(player, card, num) {
        if (num <= 0) {
          return num;
        }
        const suit = get.suit(card, player);
        if (suit === "heart") {
          return num + 3.6;
        }
        if (suit === "club") {
          return num + 1;
        }
        if (suit === "spade") {
          return num + 1.8;
        }
      },
      aiUseful(player, card, num) {
        if (num <= 0) {
          return num;
        }
        const suit = get.suit(card, player);
        if (suit === "heart") {
          return num + 3;
        }
        if (suit === "club") {
          return num + 1;
        }
        if (suit === "spade") {
          return num + 1;
        }
      }
    },
    locked: false,
    enable: ["chooseToUse", "chooseToRespond"],
    prompt: "将♦手牌当做火【杀】，♥手牌当做【桃】，♣手牌当做【闪】，♠手牌当做【无懈可击】使用或打出",
    viewAs(cards2, player) {
      if (cards2.length) {
        let name = false;
        let nature = null;
        switch (get.suit(cards2[0], player)) {
          case "club":
            name = "shan";
            break;
          case "diamond":
            name = "sha";
            nature = "fire";
            break;
          case "spade":
            name = "wuxie";
            break;
          case "heart":
            name = "tao";
            break;
        }
        if (name) {
          return { name, nature };
        }
      }
      return null;
    },
    check(card) {
      const player = _status.event.player;
      if (_status.event.type === "phase") {
        let max = 0;
        let name2;
        const list = ["sha", "tao"];
        const map = { sha: "diamond", tao: "heart" };
        for (const name of list) {
          if (player.countCards("hs", (card2) => (name !== "sha" || get.value(card2) < 5) && get.suit(card2, player) === map[name]) > 0 && player.getUseValue({ name, nature: name === "sha" ? "fire" : null }) > 0) {
            const temp = get.order({ name, nature: name === "sha" ? "fire" : null });
            if (temp > max) {
              max = temp;
              name2 = map[name];
            }
          }
        }
        if (name2 === get.suit(card, player)) {
          return name2 === "diamond" ? 5 - get.value(card) : 20 - get.value(card);
        }
        return 0;
      }
      return 1;
    },
    position: "hs",
    filterCard(card, player, event) {
      event = event || _status.event;
      const filter = event._backup.filterCard;
      const name = get.suit(card, player);
      if (name === "club" && filter({ name: "shan", cards: [card] }, player, event)) {
        return true;
      }
      if (name === "diamond" && filter({ name: "sha", cards: [card], nature: "fire" }, player, event)) {
        return true;
      }
      if (name === "spade" && filter({ name: "wuxie", cards: [card] }, player, event)) {
        return true;
      }
      if (name === "heart" && filter({ name: "tao", cards: [card] }, player, event)) {
        return true;
      }
      return false;
    },
    filter(event, player) {
      const filter = event.filterCard;
      if (filter(get.autoViewAs({ name: "sha", nature: "fire" }, "unsure"), player, event) && player.countCards("hs", { suit: "diamond" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "shan" }, "unsure"), player, event) && player.countCards("hs", { suit: "club" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "tao" }, "unsure"), player, event) && player.countCards("hs", { suit: "heart" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "wuxie" }, "unsure"), player, event) && player.countCards("hs", { suit: "spade" })) {
        return true;
      }
      return false;
    },
    logAudio(event, player) {
      return `longhun${4 - lib.suit.indexOf(get.suit(event.cards[0], player))}.mp3`;
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player, tag) {
        let name;
        switch (tag) {
          case "respondSha":
            name = "diamond";
            break;
          case "respondShan":
            name = "club";
            break;
          case "save":
            name = "heart";
            break;
        }
        if (!player.countCards("hs", { suit: name })) {
          return false;
        }
      },
      order(item, player) {
        if (player && _status.event.type === "phase") {
          let max = 0;
          const list = ["sha", "tao"];
          const map = { sha: "diamond", tao: "heart" };
          for (const name of list) {
            if (player.countCards("hs", (card) => (name !== "sha" || get.value(card) < 5) && get.suit(card, player) === map[name]) > 0 && player.getUseValue({
              name,
              nature: name === "sha" ? "fire" : null
            }) > 0) {
              const temp = get.order({
                name,
                nature: name === "sha" ? "fire" : null
              });
              if (temp > max) {
                max = temp;
              }
            }
          }
          max /= 1.1;
          return max;
        }
        return 2;
      }
    },
    hiddenCard(player, name) {
      if (name === "wuxie" && _status.connectMode && player.countCards("hs") > 0) {
        return true;
      }
      if (name === "wuxie") {
        return player.countCards("hs", { suit: "spade" }) > 0;
      }
      if (name === "tao") {
        return player.countCards("hs", { suit: "heart" }) > 0;
      }
    }
  },
  zhanjiang: {
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player) {
      const players = game.filterPlayer();
      for (const current of players) {
        if (current !== player && current.getEquips("qinggang").length > 0) {
          return true;
        }
      }
    },
    async content(event, trigger, player) {
      const players = game.filterPlayer();
      for (const current of players) {
        if (current === player) {
          continue;
        }
        const equips = current.getEquips("qinggang");
        if (equips.length > 0) {
          player.line(current, "green");
          await player.gain(equips, current, "give", "bySelf");
        }
      }
    }
  },
  boss_juejing: {
    audio: "juejing",
    audioname2: {
      dc_zhaoyun: "dcjuejing"
    },
    trigger: { player: "phaseDrawBefore" },
    forced: true,
    async content(event, trigger, player) {
      trigger.cancel();
    },
    ai: {
      noh: true,
      nogain: true
    },
    group: "boss_juejing2"
  },
  boss_juejing2: {
    audio: "juejing",
    sourceSkill: "boss_juejing",
    audioname2: {
      dc_zhaoyun: "dcjuejing"
    },
    mod: {
      aiOrder(player, card, num) {
        if (num > 0) {
          return num;
        }
        if (card.name === "zhuge" && player.getCardUsable("sha", true) < 6) {
          return 1;
        }
      },
      aiValue(player, card, num) {
        if (card.name === "zhuge") {
          return 60 / (1 + player.getCardUsable("sha", true));
        }
      },
      aiUseful(player, card, num) {
        if (card.name === "zhuge") {
          return 60 / (1 + player.getCardUsable("sha", true));
        }
      }
    },
    trigger: {
      player: "loseAfter",
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    forced: true,
    filter(event, player) {
      if (event.name === "gain" && event.player === player) {
        return player.countCards("h") > 4;
      }
      let evt = event.getl(player);
      if (!evt || !evt.hs || evt.hs.length === 0 || player.countCards("h") >= 4) {
        return false;
      }
      evt = event;
      for (let i = 0; i < 4; i++) {
        evt = evt.getParent("boss_juejing2");
        if (evt.name !== "boss_juejing2") {
          return true;
        }
      }
      return false;
    },
    async content(event, trigger, player) {
      const num = 4 - player.countCards("h");
      if (num > 0) {
        await player.draw({ num, nodelay: true });
      } else {
        await player.chooseToDiscard("h", true, -num, "allowChooseAll");
      }
    },
    ai: {
      freeSha: true,
      freeShan: true,
      skillTagFilter() {
        return true;
      }
    }
  },
  dcjuejing: { audio: 2 },
  meihun: {
    audio: 2,
    trigger: {
      player: "phaseJieshuBegin",
      target: "useCardToTargeted"
    },
    direct: true,
    filter(event, player) {
      if (event.name !== "phaseJieshu" && event.card.name !== "sha") {
        return false;
      }
      return game.hasPlayer((current) => current !== player && current.countCards("h"));
    },
    async content(event, trigger, player) {
      let result = await player.chooseTarget(get.prompt2("meihun"), (card, player2, target2) => target2 !== player2 && target2.countCards("h") > 0).set("ai", (target2) => {
        const player2 = _status.event.player;
        const att = get.attitude(player2, target2);
        if (att > 0) {
          return 0;
        }
        return 0.1 - att / target2.countCards("h");
      }).forResult();
      if (!result.bool) {
        return;
      }
      const target = result.targets[0];
      player.logSkill("meihun", target);
      event.target = target;
      result = await player.chooseControl(lib.suit).set("prompt", "请选择一种花色").set("ai", () => lib.suit.randomGet()).forResult();
      const suit = result.control;
      player.chat(get.translation(suit + 2));
      game.log(player, "选择了", `#y${get.translation(suit + 2)}`);
      if (target.countCards("h", { suit })) {
        result = await target.chooseCard("h", `交给${get.translation(player)}一张${get.translation(suit)}花色的手牌`, true, (card, player2) => get.suit(card, player2) === _status.event.suit).set("suit", suit).forResult();
      } else {
        await player.discardPlayerCard(target, true, "h", "visible");
        return;
      }
      if (result.bool && result.cards && result.cards.length) {
        await target.give(result.cards, player, "give");
      }
    }
  },
  //Connect Mode support after Angel Beats! -2nd beat-
  huoxin: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      if (game.countPlayer() < 3) {
        return false;
      }
      for (const suit of lib.suit) {
        if (player.countCards("h", { suit }) > 1) {
          return true;
        }
      }
      return false;
    },
    complexCard: true,
    position: "h",
    filterCard(card, player) {
      if (!ui.selected.cards.length) {
        const suit = get.suit(card);
        return player.countCards("h", (card2) => card !== card2 && get.suit(card2, player) === suit) > 0;
      }
      return get.suit(card, player) === get.suit(ui.selected.cards[0], player);
    },
    selectCard: 2,
    selectTarget: 2,
    filterTarget: lib.filter.notMe,
    multitarget: true,
    multiline: true,
    delay: false,
    discard: false,
    lose: false,
    check(card) {
      return 6 - get.value(card);
    },
    targetprompt: ["拼点发起人", "拼点目标"],
    async content(event, trigger, player) {
      const { targets, cards: cards2 } = event;
      const list = [];
      for (const [index, target] of targets.entries()) {
        list.push([target, cards2[index]]);
      }
      await game.loseAsync({
        gain_list: list,
        player,
        cards: cards2,
        giver: player,
        animate: "giveAuto"
      }).setContent("gaincardMultiple");
      await game.delayx();
      if (!targets[0].canCompare(targets[1])) {
        return;
      }
      const result = await targets[0].chooseToCompare(targets[1]).forResult();
      if (result.winner !== targets[0]) {
        targets[0].addMark("huoxin", 1);
      }
      if (result.winner !== targets[1]) {
        targets[1].addMark("huoxin", 1);
      }
    },
    marktext: "魅",
    intro: {
      name: "魅惑",
      name2: "魅惑",
      content: "mark"
    },
    group: "huoxin_control",
    ai: {
      order: 1,
      result: {
        target(player, target) {
          if (target.hasMark("huoxin")) {
            return -2;
          }
          return -1;
        }
      }
    }
  },
  huoxin_control: {
    audio: "huoxin",
    forced: true,
    trigger: { global: "phaseBeginStart" },
    sourceSkill: "huoxin",
    filter(event, player) {
      return player !== event.player && !event.player._trueMe && event.player.countMark("huoxin") > 1;
    },
    logTarget: "player",
    skillAnimation: true,
    animationColor: "key",
    async content(event, trigger, player) {
      trigger.player.removeMark("huoxin", trigger.player.countMark("huoxin"));
      trigger.player._trueMe = player;
      game.addGlobalSkill("autoswap");
      if (trigger.player === game.me) {
        game.notMe = true;
        if (!_status.auto) {
          ui.click.auto();
        }
      }
      trigger.player.addSkill("huoxin2");
    }
  },
  huoxin2: {
    trigger: {
      player: ["phaseAfter", "dieAfter"],
      global: "phaseBeforeStart"
    },
    lastDo: true,
    charlotte: true,
    forceDie: true,
    forced: true,
    silent: true,
    sourceSkill: "huoxin",
    async content(event, trigger, player) {
      player.removeSkill("huoxin2");
    },
    onremove(player) {
      if (player === game.me) {
        if (!game.notMe) {
          game.swapPlayerAuto(player._trueMe);
        } else {
          delete game.notMe;
        }
        if (_status.auto) {
          ui.click.auto();
        }
      }
      delete player._trueMe;
    }
  },
  //官盗S特015神马超
  psshouli: {
    audio: "shouli",
    enable: ["chooseToUse", "chooseToRespond"],
    hiddenCard(player, name) {
      if (player !== _status.currentPhase && (name === "sha" || name === "shan")) {
        return true;
      }
    },
    filter(event, player) {
      if (event.responded || event.psshouli || event.type === "wuxie") {
        return false;
      }
      if (game.hasPlayer((current) => current.getEquips(4).length > 0) && event.filterCard(
        get.autoViewAs(
          {
            name: "sha",
            storage: { psshouli: true }
          },
          "unsure"
        ),
        player,
        event
      )) {
        return true;
      }
      if (game.hasPlayer((current) => current.getEquips(3).length > 0) && event.filterCard(
        get.autoViewAs(
          {
            name: "shan",
            storage: { psshouli: true }
          },
          "unsure"
        ),
        player,
        event
      )) {
        return true;
      }
      return false;
    },
    delay: false,
    locked: true,
    filterTarget(card, player, target) {
      const event = get.event();
      let evt = event;
      if (event._backup) {
        evt = event._backup;
      }
      const equip3 = target.getCards("e", (card2) => get.is.defendingMount(card2, false));
      const equip4 = target.getCards("e", (card2) => get.is.attackingMount(card2, false));
      if (equip3.length && equip3.some(
        (card2) => evt.filterCard(
          get.autoViewAs(
            {
              name: "shan",
              storage: { psshouli: true }
            },
            [card2]
          ),
          player,
          event
        )
      )) {
        return true;
      }
      return equip4.some((card2) => {
        const sha = get.autoViewAs(
          {
            name: "sha",
            storage: { psshouli: true }
          },
          [card2]
        );
        if (!evt.filterCard(sha, player, event)) {
          return false;
        }
        if (!evt.filterTarget) {
          return true;
        }
        return game.hasPlayer((current) => evt.filterTarget(sha, player, current));
      });
    },
    prompt: "将场上的一张坐骑牌当做【杀】或【闪】使用或打出",
    async content(event, trigger, player) {
      const { target } = event;
      const evt = event.getParent(2);
      if (evt == null) {
        throw new ReferenceError(`【${get.translation(event.name)}】无法找到使用牌的事件`);
      }
      evt.set("psshouli", true);
      const list = [];
      const equip3 = target.getCards("e", (card) => get.is.defendingMount(card, false));
      const equip4 = target.getCards("e", (card) => get.is.attackingMount(card, false));
      const backupx = _status.event;
      _status.event = evt;
      try {
        if (equip3.length && equip3.some((card) => {
          const shan = get.autoViewAs(
            {
              name: "shan",
              storage: { psshouli: true }
            },
            [card]
          );
          return evt.filterCard(shan, player, event);
        })) {
          list.push("shan");
        }
        if (equip4.length && equip4.some((card) => {
          const sha = get.autoViewAs(
            {
              name: "sha",
              storage: { psshouli: true }
            },
            [card]
          );
          if (evt.filterCard(sha, player, evt) && (!evt.filterTarget || game.hasPlayer((current) => evt.filterTarget(sha, player, current)))) {
            return true;
          }
          return false;
        })) {
          list.push("sha");
        }
      } catch (e) {
        game.print(e);
      }
      _status.event = backupx;
      let result;
      let cardName;
      if (list.length === 1) {
        cardName = list[0];
        const cards2 = cardName === "shan" ? equip3 : equip4;
        if (cards2.length === 1) {
          result = {
            bool: true,
            links: [cards2[0]]
          };
        } else {
          result = await player.choosePlayerCard({
            target,
            filterButton(button) {
              return _status.event.cards.includes(button.link);
            },
            position: "e",
            forced: true
          }).set("cards", cards2).forResult();
        }
      } else {
        result = await player.choosePlayerCard({
          target,
          filterButton(button) {
            return get.is.attackingMount(button.link) || get.is.defendingMount(button.link);
          },
          position: "e",
          forced: true
        }).forResult();
      }
      if (result.bool && result.links && result.links.length) {
        const name = cardName || (get.is.attackingMount(result.links[0]) ? "sha" : "shan");
        if (evt.name === "chooseToUse") {
          game.broadcastAll(
            (result2, name2) => {
              lib.skill.psshouli_backup.viewAs = {
                name: name2,
                cards: [result2],
                storage: { psshouli: true }
              };
              lib.skill.psshouli_backup.prompt = `选择${get.translation(name2)}（${get.translation(result2)}）的目标`;
            },
            result.links[0],
            name
          );
          evt.set("_backupevent", "psshouli_backup");
          evt.backup("psshouli_backup");
          evt.set("openskilldialog", `选择${get.translation(name)}（${get.translation(result.links[0])}）的目标`);
          evt.set("norestore", true);
          evt.set("custom", {
            add: {},
            replace: { window() {
            } }
          });
        } else {
          delete evt.result.used;
          delete evt.result.skill;
          evt.result.card = get.autoViewAs(
            {
              name,
              cards: [result.links[0]],
              storage: { psshouli: true }
            },
            result.links
          );
          evt.result.cards = [result.links[0]];
          target.$give(result.links[0], player, false);
          if (player !== target) {
            target.addTempSkill("fengyin");
          }
          target.addTempSkill("psshouli_thunder");
          player.addTempSkill("psshouli_thunder");
          evt.redo();
          return;
        }
      }
      evt.goto(0);
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player, tag) {
        const func = get.is[tag === "respondSha" ? "attackingMount" : "defendingMount"];
        return game.hasPlayer((current) => current.hasCard((card) => func(card, false), "e"));
      },
      order: 2,
      result: {
        player(player, target) {
          const att = Math.max(8, get.attitude(player, target));
          if (_status.event.type !== "phase") {
            return 9 - att;
          }
          if (!player.hasValueTarget({ name: "sha" })) {
            return 0;
          }
          return 9 - att;
        }
      }
    },
    group: "psshouli_init",
    subSkill: {
      thunder: {
        charlotte: true,
        trigger: { player: "damageBegin1" },
        forced: true,
        mark: true,
        async content(event, trigger, player) {
          trigger.num++;
          game.setNature(trigger, "thunder");
        },
        marktext: "⚡",
        intro: { content: "受到的伤害+1且改为雷属性" },
        ai: {
          effect: {
            target: (card, player, target) => {
              if (!get.tag(card, "damage")) {
                return;
              }
              if (target.hasSkillTag("nodamage", null, {
                natures: ["thunder"]
              }) || target.hasSkillTag("nothunder")) {
                return "zeroplayertarget";
              }
              if (target.hasSkillTag("filterDamage", null, {
                player,
                card: new lib.element.VCard(
                  {
                    name: card.name,
                    nature: "thunder"
                  },
                  [card]
                )
              })) {
                return;
              }
              return 2;
            }
          }
        }
      },
      init: {
        audio: "psshouli",
        trigger: {
          global: "phaseBefore",
          player: "enterGame"
        },
        forced: true,
        filter(event, player) {
          return event.name !== "phase" || game.phaseNumber === 0;
        },
        logTarget: () => game.filterPlayer(),
        equips: [
          ["heart", 5, "chitu"],
          ["diamond", 13, "zixin"],
          ["spade", 5, "jueying"],
          ["diamond", 13, "hualiu"],
          ["club", 5, "dilu"],
          ["spade", 13, "dawan"],
          ["heart", 13, "zhuahuang"],
          ["heart", 3, "jingfanma"]
        ],
        async content(event, trigger, player) {
          const targets = game.filterPlayer().sortBySeat(_status.firstAct2 || game.zhong || game.zhu || _status.firstAct || player);
          await game.delayx();
          for (const target of targets) {
            player.line(target, "green");
            const result = await target.chooseToUse({
              prompt: "狩骊：使用一张坐骑牌并摸一张牌，或使用一张坐骑牌指示物",
              filterCard(card2, player2, event2) {
                if (get.subtype(card2) !== "equip3" && get.subtype(card2) !== "equip4" && get.subtype(card2) !== "equip6") {
                  return false;
                }
                return lib.filter.filterCard(card2, player2, event2);
              }
            }).set("ai", () => 1).forResult();
            if (result.bool) {
              await target.draw();
              continue;
            }
            const cardInfo = lib.skill.psshouli_init.equips.randomRemove();
            if (!cardInfo) {
              return;
            }
            const card = game.createCard({
              suit: cardInfo[0],
              number: cardInfo[1],
              name: cardInfo[2]
            });
            if (!_status.psshouli_equips) {
              _status.psshouli_equips = [];
            }
            _status.psshouli_equips.push(card.cardid);
            if (card) {
              const next = target.chooseUseTarget({
                card,
                forced: true,
                nopopup: true,
                animate: false
              });
              player.addSkill("psshouli_remove");
              await next;
            }
          }
        }
      },
      remove: {
        trigger: { global: ["loseAfter", "loseAsyncAfter", "cardsDiscardAfter", "equipAfter"] },
        forced: true,
        charlotte: true,
        popup: false,
        firstDo: true,
        forceDie: true,
        filter(event, player) {
          if (!_status.psshouli_equips || !_status.psshouli_equips.length) {
            return false;
          }
          const cards2 = event.getd();
          return cards2.filter((i) => _status.psshouli_equips.includes(i.cardid)).length;
        },
        async content(event, trigger, player) {
          const cards2 = trigger.getd();
          const remove = [];
          for (const card of cards2) {
            if (_status.psshouli_equips.includes(card.cardid)) {
              _status.psshouli_equips.remove(card.cardid);
              remove.push(card);
            }
          }
          if (remove.length) {
            game.cardsGotoSpecial(remove);
            lib.skill.psshouli_init.equips.addArray(remove.map((i) => [i.suit, i.number, i.name]));
            game.log("坐骑指示物", remove, "被移出了游戏");
          }
        }
      },
      backup: {
        async precontent(event, trigger, player) {
          event.result._apply_args = { throw: false, addSkillCount: false };
          const cards2 = event.result.card.cards;
          event.result.cards = cards2;
          const owner = get.owner(cards2[0]);
          owner.$throw(cards2[0]);
          player.popup(event.result.card.name, "metal");
          const next = game.delayx();
          event.getParent().addCount = false;
          await next;
          if (player !== owner) {
            owner.addTempSkill("fengyin");
          }
          owner.addTempSkill("psshouli_thunder");
          player.addTempSkill("psshouli_thunder");
        },
        filterCard: () => false,
        prompt: "请选择【杀】的目标",
        selectCard: -1,
        log: false
      }
    }
  },
  pshengwu: {
    audio: "hengwu",
    mod: {
      aiOrder: (player, card, num) => {
        if (num > 0 && get.tag(card, "draw") && ui.cardPile.childNodes.length + ui.discardPile.childNodes.length < 20) {
          return 0;
        }
      },
      aiValue: (player, card, num) => {
        if (num > 0 && card.name === "zhuge") {
          return 20;
        }
      },
      aiUseful: (player, card, num) => {
        if (num > 0 && card.name === "zhuge") {
          return 10;
        }
      }
    },
    trigger: { player: ["useCard", "respond"] },
    direct: true,
    locked: false,
    filter(event, player) {
      return game.hasPlayer((i) => i.hasCards("ej", (cardx) => get.type(cardx) === "equip" && get.suit(event.card) === get.suit(cardx)));
    },
    async content(event, trigger, player) {
      const suit = get.suit(trigger.card);
      const extra = game.filterPlayer().map((i) => i.countCards("ej", (cardx) => get.type(cardx) === "equip" && get.suit(trigger.card) === get.suit(cardx))).reduce((p, c) => p + c);
      const prompt2 = `弃置任意张${get.translation(suit)}手牌，然后摸X张牌（X为你弃置的牌数+${extra}）`;
      const result = await player.chooseToDiscard({
        prompt: get.prompt("pshengwu"),
        prompt2,
        filterCard: (card) => get.suit(card) === _status.event.suit,
        selectCard: [1, player.countCards("h", { suit })],
        position: "h",
        allowChooseAll: true
      }).set("suit", suit).set("ai", (card) => {
        if (_status.event.tie) {
          return 0;
        }
        const current = _status.event.player;
        if (_status.event.goon) {
          return 12 - get.value(card);
        }
        if (current === _status.currentPhase) {
          if (["shan", "caochuan", "tao", "wuxie"].includes(card.name)) {
            return 8 - get.value(card);
          }
          return 6 - get.value(card);
        }
        return 5.5 - get.value(card);
      }).set("goon", player.countCards("h", { suit }) === 1).set("tie", extra > ui.cardPile.childNodes.length + ui.discardPile.childNodes.length).set("logSkill", "pshengwu").forResult();
      if (!result.bool || !result.cards?.length) {
        return;
      }
      const currentExtra = game.filterPlayer().map((i) => i.countCards("ej", (cardx) => get.type(cardx) === "equip" && get.suit(trigger.card) === get.suit(cardx))).reduce((p, c) => p + c);
      await player.draw(result.cards.length + currentExtra);
    },
    ai: {
      threaten: 100,
      reverseEquip: true,
      effect: {
        player_use(card, player, target) {
          if (typeof card !== "object") {
            return;
          }
          const suit = get.suit(card);
          if (!lib.suit.includes(suit) || player.hasCard((i) => get.suit(i, player) === suit, "h")) {
            return;
          }
          return [1, game.countPlayer((current) => current.countCards("e", (card2) => get.suit(card2, current) === suit))];
        },
        target(card, player, target) {
          if (card.name === "sha" && !player.hasSkillTag(
            "directHit_ai",
            true,
            {
              target,
              card
            },
            true
          ) && game.hasPlayer((current) => current.hasCard((cardx) => get.subtype(cardx) === "equip3", "e"))) {
            return [0, -0.5];
          }
        }
      }
    }
  },
  //一战成名·群雄逐鹿·长安之战专属神贾诩
  zybishi: {
    trigger: { target: "useCardToTargeted" },
    filter(event, player) {
      return event.card.name === "sha" && event.player !== player;
    },
    check(event, player) {
      let effect = 0;
      for (const target2 of event.targets || []) {
        effect += get.effect(target2, event.card, event.player, player);
      }
      if (effect >= 0) {
        return false;
      }
      const target = event.targets[0];
      if (target === player) {
        return !player.hasCards("h", "shan");
      }
      return target.hp === 1 || target.countCards("h") <= 2 && target.hp <= 2;
    },
    async content(event, trigger, player) {
      player.line(trigger.player, "green");
      const next = trigger.player.draw();
      const evt = trigger.getParent();
      evt.all_excluded = true;
      game.log(evt.card, "被无效了");
      await next;
    }
  },
  zyjianbing: {
    trigger: { global: "damageBegin3" },
    logTarget: "player",
    filter(event, player) {
      return event.player !== player && event.player.isIn() && event.card && event.card.name === "sha" && event.player.hasGainableCards(player, "he");
    },
    async content(event, trigger, player) {
      const result = await player.gainPlayerCard({
        target: trigger.player,
        position: "he",
        forced: true
      }).forResult();
      if (!result.bool || !result.cards || !result.cards.length) {
        return;
      }
      const card = result.cards[0];
      if (get.suit(card, trigger.player) === "heart") {
        await trigger.player.recover();
      }
    }
  }
};
const skills$D = {
  //王战张绣
  sclfudi: {
    audio: 2,
    trigger: { player: "damageEnd" },
    filter(event, player) {
      return event.source?.isIn() && event.source != player && player.hasCards("h");
    },
    async cost(event, trigger, player) {
      const source = trigger.source;
      let targets = game.filterPlayer((current) => current.group == source.group).sort((a, b) => b.hp - a.hp);
      targets = targets.filter((current) => current.hp >= player.hp && current.hp == targets[0].hp);
      event.result = await player.chooseToGive({
        prompt: `附敌：交给${get.translation(source)}一张手牌然后对${targets.length ? get.translation(targets) : "滚木"}${targets.length > 1 ? "中的一名角色" : ""}造成1点伤害`,
        position: "h",
        target: source,
        ai(card) {
          const { player: player2, targets: targets2, source: source2 } = get.event();
          if (!targets2.length && get.attitude(player2, source2) > 0) {
            return 7 - get.value(card);
          }
          if (targets2.some((target) => get.damageEffect(target, player2, player2) > 0)) {
            return 10 - get.value(card);
          }
          return -get.value(card);
        }
      }).set("targets", targets).set("source", source).forResult();
    },
    async content(event, trigger, player) {
      const source = trigger.source;
      let targets = game.filterPlayer((current) => current.group == source.group).sort((a, b) => b.hp - a.hp);
      targets = targets.filter((current) => current.hp >= player.hp && current.hp == targets[0].hp);
      if (!targets.length) {
        return;
      }
      const result = targets.length > 1 ? player.chooseTarget({
        prompt: `附敌：对${get.translation(targets)}中的一名角色造成一点伤害`,
        filterTarget(card, player2, target) {
          return get.event().targets.includes(target);
        },
        forced: true,
        ai(target) {
          return get.damageEffect(target, get.player(), get.player());
        }
      }).set("targets", targets).forResult() : { bool: true, targets };
      if (result?.bool && result.targets?.length) {
        const target = result.targets[0];
        player.line(target);
        await target.damage();
      }
    },
    ai: {
      maixie: true,
      maixie_defend: true,
      effect: {
        target(card, player, target) {
          if (get.tag(card, "damage") && target.hp > 1) {
            if (player.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            if (!target.countCards("h")) {
              return [1, -1];
            }
            if (game.countPlayer((current) => {
              return current.group == player.group && current.hp >= target.hp - 1 && get.attitude(player, current) > 0 && current.hp <= 2;
            })) {
              return [1, 0, 0, -2];
            }
          }
        }
      }
    }
  },
  sclcongjian: {
    audio: "drlt_congjian",
    trigger: {
      player: "damageBegin3",
      source: "damageBegin1"
    },
    forced: true,
    filter(event, player, name) {
      if (name == "damageBegin1" && _status.currentPhase != player) {
        return true;
      }
      if (name == "damageBegin3" && _status.currentPhase == player) {
        return true;
      }
      return false;
    },
    async content(event, trigger, player) {
      trigger.num++;
    }
  },
  //王战贾诩
  sclwansha: {
    audio: "rewansha",
    forced: true,
    trigger: {
      global: "dying"
    },
    filter(event, player) {
      return _status.currentPhase === player && player !== event.player;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const [target] = event.targets;
      await target.die(trigger.reason);
    }
  },
  //SCL
  scls_zhanshen: {
    audio: 2,
    trigger: {
      player: "phaseZhunbeiBegin"
    },
    filter(event, player) {
      return ["draw", "damage", "target"].some((suffix) => !player.hasSkill(`scls_zhanshen_${suffix}`, null, null, false));
    },
    forced: true,
    async content(event, trigger, player) {
      const list = [
        ["draw", "摸牌阶段额外摸一张牌"],
        ["damage", "使用【杀】造成的伤害+1"],
        ["target", "使用【杀】可以额外指定一个目标"]
      ].filter((suffix) => !player.hasSkill(`scls_zhanshen_${suffix[0]}`, null, null, false));
      const result = list.length > 1 ? await player.chooseButton(["战神：选择一项", [list, "textbutton"]], true).set("ai", (button) => [null, "target", "damage", "draw"].indexOf(button.link)).forResult() : {
        bool: true,
        links: [list[0][0]]
      };
      if (result.bool) {
        result.links.forEach((suffix) => player.addSkill(`scls_zhanshen_${suffix}`));
      }
    },
    subSkill: {
      draw: {
        audio: "scls_zhanshen",
        trigger: {
          player: "phaseDrawBegin2"
        },
        filter(event) {
          return !event.numFixed;
        },
        forced: true,
        charlotte: true,
        async content(event, trigger, player) {
          trigger.num++;
        }
      },
      damage: {
        audio: "scls_zhanshen",
        trigger: {
          source: "damageBegin1"
        },
        filter(event) {
          return event.card?.name === "sha";
        },
        forced: true,
        charlotte: true,
        async content(event, trigger, player) {
          trigger.num++;
        }
      },
      target: {
        audio: "scls_zhanshen",
        forced: true,
        charlotte: true,
        mod: {
          selectTarget(card, player, range) {
            if (card.name === "sha" && range[1] !== -1) {
              range[1]++;
            }
          }
        }
      }
    }
  },
  scls_yinshi: {
    //audio: "xinfu_pdgyingshi",
    mod: {
      targetEnabled(card, player, target) {
        if (get.mode() === "doudizhu" && get.type(card) === "delay") {
          return false;
        }
      }
    },
    trigger: {
      player: "phaseJudgeBefore"
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.cancel();
      game.log(player, "跳过了判定阶段");
    },
    ai: {
      effect: {
        target(card, player, target) {
          if (get.type(card) === "delay") {
            return "zeroplayertarget";
          }
        }
      }
    }
  },
  scls_pingcai: {
    audio: ["xinfu_pingcai", 5],
    enable: "phaseUse",
    usable: 1,
    filterCard: true,
    selectCard() {
      if (get.mode() === "doudizhu") {
        return 0;
      }
      return 1;
    },
    check(card) {
      const suit = get.suit(card);
      return lib.skill.scls_pingcai.takaramonoValue(suit, get.event().player);
    },
    discard: false,
    lose: false,
    delay: false,
    derivation: ["sclc_wolong", "sclc_fengchu", "sclc_shuijing", "sclc_xuanjian"],
    async sclc_wolong(player) {
      const ingame = get.mode() === "doudizhu" && game.hasPlayer((cur) => {
        const names = get.characterSurname(cur.name1);
        names.addArray(get.characterSurname(cur.name2));
        for (const [surname, name] of names) {
          if (surname === "诸葛" && name === "亮") {
            return true;
          }
        }
      });
      const result = await player.chooseTarget(`请选择${ingame ? "至多两名" : "一名"}角色，对其造成1点火焰伤害`, ingame ? [1, 2] : [1, 1], true).set("ai", (target) => {
        const player2 = get.event().player;
        return get.damageEffect(target, player2, player2, "fire");
      }).forResult();
      if (result.bool && result.targets.length) {
        player.line(result.targets, "fire");
        result.targets.sortBySeat();
        for (const target of result.targets) {
          await target.damage("fire");
        }
      }
    },
    async sclc_fengchu(player) {
      const ingame = get.mode() === "doudizhu" && game.hasPlayer((cur) => {
        const names = get.characterSurname(cur.name1);
        names.addArray(get.characterSurname(cur.name2));
        for (const [surname, name] of names) {
          if (surname === "庞" && name === "统") {
            return true;
          }
        }
      });
      const result = await player.chooseTarget(`请选择至多${ingame ? "四名" : "三名"}要横置的角色`, ingame ? [1, 4] : [1, 3], (card, player2, target) => !target.isLinked(), true).set("ai", (target) => {
        const player2 = get.event().player;
        return get.effect(target, { name: "tiesuo" }, player2, player2);
      }).forResult();
      if (result.bool && result.targets.length) {
        player.line(result.targets, "green");
        result.targets.sortBySeat();
        for (const target of result.targets) {
          await target.link();
        }
      }
    },
    async sclc_shuijing(player) {
      const equip = get.mode() !== "doudizhu" || game.hasPlayer((cur) => {
        const names = get.characterSurname(cur.name1);
        names.addArray(get.characterSurname(cur.name2));
        for (const [surname, name] of names) {
          if (surname === "司马" && name === "徽") {
            return true;
          }
        }
      });
      if (equip && !player.canMoveCard(null, true)) {
        return;
      }
      if (!equip && !player.canMoveCard(null, true, (card) => get.subtype(card) === "equip2")) {
        return;
      }
      const { bool, targets } = await player.chooseTarget(2, (card, player2, target) => {
        if (ui.selected.targets.length) {
          if (!get.event().equip) {
            const cards2 = ui.selected.targets[0].getEquips(2);
            return cards2.some((card2) => target.canEquip(card2));
          }
          const from = ui.selected.targets[0];
          if (target.isMin()) {
            return false;
          }
          const es = from.getCards("e");
          for (const card2 of es) {
            if (target.canEquip(card2)) {
              return true;
            }
          }
          return false;
        }
        if (!get.event().equip) {
          return target.getEquips(2).length > 0;
        }
        return target.countCards("e") > 0;
      }).set("forced", true).set("multitarget", true).set("targetprompt", ["被移走", "移动目标"]).set("prompt", `将一名角色装备区的一张${equip ? "" : "防具"}牌移动到另一名角色的装备区中`).set("ai", (target) => {
        const player2 = get.event().player;
        const att = get.attitude(player2, target);
        if (ui.selected.targets.length === 0) {
          if (att < 0 && game.hasPlayer((current) => {
            if (get.attitude(player2, current) <= 0) {
              return;
            }
            const es = target.getCards("e");
            return es.some((card) => current.canEquip(card));
          })) {
            return -att;
          }
          return 0;
        }
        if (att > 0) {
          const es = ui.selected.targets[0].getCards("e");
          if (!es.some((card) => target.canEquip(card))) {
            return 0;
          }
        }
        return -att * get.attitude(player2, ui.selected.targets[0]);
      }).set("equip", equip).forResult();
      if (!bool || targets.length !== 2) {
        return;
      }
      player.line2(targets, "green");
      await game.delay();
      let result;
      if (equip) {
        result = await player.choosePlayerCard("e", true, (button) => get.equipValue(button.link), targets[0]).set("targets0", targets[0]).set("targets1", targets[1]).set("filterButton", (button) => {
          const targets1 = _status.event.targets1;
          return targets1.canEquip(button.link);
        }).forResult();
      } else {
        const cards2 = targets[0].getEquips(2);
        if (cards2.length === 1) {
          result = {
            bool: true,
            links: cards2
          };
        } else {
          result = await player.choosePlayerCard("e", true, (button) => get.equipValue(button.link), targets[0]).set("targets0", targets[0]).set("targets1", targets[1]).set("filterButton", (button) => {
            if (!get.subtypes(button.link, false).includes("equip2")) {
              return false;
            }
            const targets1 = _status.event.targets1;
            return targets1.canEquip(button.link);
          }).forResult();
        }
      }
      if (result.bool && result.links.length) {
        const link = result.links[0];
        if (get.position(link) === "e") {
          await targets[1].equip(link);
        } else if (link.viewAs) {
          await targets[1].addJudge({ name: link.viewAs }, [link]);
        } else {
          await targets[1].addJudge(link);
        }
        targets[0].$give(link, targets[1], false);
        await game.delay();
      }
    },
    async sclc_xuanjian(player) {
      const ingame = get.mode() === "doudizhu" && game.hasPlayer((cur) => {
        const names = get.characterSurname(cur.name1);
        names.addArray(get.characterSurname(cur.name2));
        for (const [surname, name] of names) {
          if (surname === "徐" && name === "庶") {
            return true;
          }
        }
      });
      const result = await player.chooseTarget(`请选择一名角色，令其回复1点体力并摸一张牌${ingame ? "，然后你摸一张牌" : ""}`, true).set("ai", (target) => {
        const player2 = get.event().player;
        let eff = get.effect(target, { name: "draw" }, player2, player2);
        if (target.isDamaged()) {
          eff += get.recoverEffect(target, player2, player2);
        }
        if (get.event().ingame) {
          eff += get.effect(player2, { name: "draw" }, player2, player2);
        }
        return eff;
      }).set("ingame", ingame).forResult();
      if (result.bool && result.targets.length) {
        player.line(result.targets, "thunder");
        const target = result.targets[0];
        await target.draw();
        await target.recover();
        if (ingame) {
          await player.draw();
        }
      }
    },
    takaramonoValue(name, player) {
      switch (name) {
        case "sclc_wolong":
        case "diamond":
          return Math.max(...game.filterPlayer((cur) => cur !== player).map((tar) => get.damageEffect(tar, player, player)));
        case "sclc_fengchu":
        case "club":
          return game.filterPlayer((cur) => cur !== player && !cur.isLinked()).map((tar) => get.effect(tar, { name: "tiesuo" }, player, player)).sort((a, b) => b - a).slice(0, 3).reduce((acc, val) => acc + val, 0);
        case "sclc_shuijing":
        case "spade":
          if (player.canMoveCard(true)) {
            return 12;
          }
          return 0;
        case "sclc_xuanjian":
        case "heart":
          return Math.max(...game.filterPlayer().map((tar) => get.recoverEffect(tar, player, player) + get.effect(tar, { name: "draw" }, player, player)));
        default:
          return 0;
      }
    },
    logAudio(event, player) {
      const suit = get.suit(event.cards[0], player);
      switch (suit) {
        case "diamond":
          return "xinfu_pingcai2.mp3";
        case "club":
          return "xinfu_pingcai3.mp3";
        case "spade":
          return "xinfu_pingcai4.mp3";
        case "heart":
          return "xinfu_pingcai5.mp3";
        default:
          return "xinfu_pingcai1.mp3";
      }
    },
    async content(event, trigger, player) {
      let name;
      if (get.mode() === "doudizhu") {
        const result = await player.chooseButton(["评才：选择你要擦拭的宝物", [lib.skill.scls_pingcai.derivation.map((name2) => ["", "", name2]), "vcard"]]).set("ai", (button) => lib.skill.scls_pingcai.takaramonoValue(button.link[2], get.event().player)).forResult();
        if (!result.bool) {
          return;
        }
        name = result.links[0][2];
      } else {
        await player.showCards(event.cards);
        name = lib.skill.scls_pingcai.derivation[["diamond", "club", "spade", "heart"].indexOf(get.suit(event.cards[0], player))];
      }
      if (name) {
        await lib.skill.scls_pingcai[name](player);
      }
    },
    ai: {
      order: 10,
      result: {
        player: 3
      }
    }
  },
  scls_chongxu: {
    audio: "chongxu",
    enable: "phaseUse",
    usable: 1,
    async content(event, trigger, player) {
      const cards2 = get.cards(3, true);
      await player.showCards(cards2, `${get.translation(player)}发动了【冲虚】`);
      const {
        bool,
        links: [card]
      } = await player.chooseCardButton("冲虚：选择要获得的牌", true, cards2).set("ai", (button) => {
        const color = get.color(button.link);
        const need = get.event().color;
        if (need && color !== need) {
          return 0.1;
        }
        return get.buttonValue(button);
      }).set(
        "color",
        (() => {
          if (!player.hasSkill("scls_chongxu_lianhua")) {
            if (player.hp < 2 || player.hp + player.hujia < 3 && !player.hasCard((i) => {
              const name = get.name(i, player);
              return name === "shan" || name === "tao";
            }) || get.threaten(player) > 2) {
              return "red";
            }
          }
          if (!player.hasSkill("scls_chongxu_miaojian")) {
            if (player.canUse({ name: "wuzhong" }, player)) {
              return "black";
            }
          }
          return false;
        })()
      ).forResult();
      if (!bool) {
        return;
      }
      const skill = get.color(card) === "red" ? "scls_lianhua" : "scls_miaojian";
      await player.gain(card, "gain2");
      if (!player.hasMark(skill)) {
        player.addMark(skill, 1, false);
      }
      if (skill === "scls_miaojian") {
        player.addTempSkill("scls_chongxu_miaojian");
      }
      player.addTempSkill("scls_chongxu_lianhua", { player: "phaseBegin" });
      game.log(player, "修改了技能", `#g【${get.translation(skill)}】`);
    },
    ai: {
      order: 10,
      result: {
        player: 1
      }
    },
    subSkill: {
      miaojian: {
        charlotte: true,
        onremove(player) {
          player.removeMark("scls_miaojian", player.countMark("scls_miaojian"), false);
        }
      },
      lianhua: {
        charlotte: true,
        onremove(player) {
          player.removeMark("scls_lianhua", player.countMark("scls_lianhua"), false);
        }
      }
    }
  },
  scls_miaojian: {
    audio: "miaojian",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      const level = player.hasMark("scls_miaojian");
      if (event.filterCard({ name: "sha", nature: "stab" }, player, event)) {
        if (level) {
          return true;
        }
        return player.hasCard((card) => get.type2(card) === "basic", "hs");
      }
      if (event.filterCard({ name: "wuzhong" }, player, event)) {
        if (level) {
          return true;
        }
        return player.hasCard((card) => get.type2(card) !== "basic", "hes");
      }
      return false;
    },
    chooseButton: {
      dialog() {
        return ui.create.dialog("妙剑", [
          [
            ["基本", "", "sha", "stab"],
            ["锦囊", "", "wuzhong"]
          ],
          "vcard"
        ]);
      },
      filter(button, player) {
        const event = _status.event.getParent();
        const level = player.hasMark("scls_miaojian");
        if (button.link[2] === "sha") {
          if (!event.filterCard({ name: "sha", nature: "stab" }, player, event)) {
            return false;
          }
          if (level) {
            return true;
          }
          return player.hasCard((card) => get.type2(card) === "basic", "hs");
        }
        if (button.link[2] === "wuzhong") {
          if (!event.filterCard({ name: "wuzhong" }, player, event)) {
            return false;
          }
          if (level) {
            return true;
          }
          return player.hasCard((card) => get.type2(card) !== "basic", "hes");
        }
      },
      check(button) {
        const card = { name: button.link[2], nature: button.link[3] };
        const player = _status.event.player;
        return get.value(card, player) * get.sgn(player.getUseValue(card));
      },
      backup(links, player) {
        const index = links[0][2] === "sha" ? 0 : 1;
        const level = player.countMark("scls_miaojian");
        const next = {
          audio: "miaojian",
          filterCard: [
            [(card) => get.type(card) === "basic", () => false],
            [(card) => get.type(card) !== "basic", () => false]
          ][index][level],
          position: "hes",
          check(card) {
            if (card) {
              return 6.5 - get.value(card);
            }
            return 1;
          },
          viewAs: [
            {
              name: "sha",
              nature: "stab"
            },
            {
              name: "wuzhong"
            }
          ][index]
        };
        if (level) {
          next.selectCard = -1;
          next.viewAs.isCard = true;
        }
        return next;
      },
      prompt(links, player) {
        const index = links[0][2] === "sha" ? 0 : 1;
        const level = player.countMark("scls_miaojian");
        return [
          ["将一张基本牌当做刺【杀】使用", "请选择刺【杀】的目标"],
          ["将一张非基本牌当做【无中生有】使用", "请选择【无中生有】的目标"]
        ][index][level];
      }
    },
    ai: {
      order: 7,
      result: { player: 1 }
    },
    onremove: true,
    derivation: ["miaojian2"],
    subSkill: { backup: { audio: "miaojian" } }
  },
  scls_lianhua: {
    audio: "shhlianhua",
    trigger: {
      target: "useCardToTargeted"
    },
    filter: (event) => event.card.name === "sha",
    forced: true,
    locked: false,
    derivation: ["shhlianhua2"],
    async content(event, trigger, player) {
      await player.draw();
      if (!player.hasMark("scls_lianhua")) {
        return;
      }
      const result = await trigger.player.chooseToDiscard("he", `弃置一张牌，或令${get.translation(trigger.card)}对${get.translation(player)}无效`).set("ai", (card) => {
        if (_status.event.eff > 0) {
          return 9 - get.value(card);
        }
        return 0;
      }).set("eff", get.effect(player, trigger.card, trigger.player, trigger.player)).forResult();
      if (result.bool === false) {
        trigger.getParent().excluded.add(player);
      }
    },
    ai: {
      effect: {
        target_use(card, player, target, current) {
          if (card.name === "sha" && current < 0) {
            return [target.hasMark("scls_lianhua") ? 0.7 : 1, 1, 1, 0];
          }
        }
      }
    }
  },
  scls_kuangcai: {
    audio: "kuangcai",
    mod: {
      targetInRange(card, player) {
        if (player.isPhaseUsing()) {
          return true;
        }
      },
      aiOrder(player, card, num) {
        const name = get.name(card);
        if (name === "tao") {
          return num + 7 + Math.pow(player.getDamagedHp(), 2);
        }
        if (name === "sha") {
          return num + 6;
        }
        if (get.subtype(card) === "equip2") {
          return num + get.value(card) / 3;
        }
      },
      cardUsable(card, player) {
        if (!player.isPhaseUsing()) {
          return false;
        }
        if (get.info(card) && get.info(card).forceUsable) {
          return;
        }
        return Infinity;
      }
    },
    trigger: {
      player: "useCard1"
    },
    filter(event, player) {
      return player.isPhaseUsing();
    },
    locked: false,
    prompt2(event, player) {
      return `摸一张牌${player.hasMark("scls_kuangcai_mark") ? "" : "，本回合至多使用五张牌"}`;
    },
    frequent: true,
    async content(event, trigger, player) {
      player.addTempSkill("scls_kuangcai_mark");
      await player.draw("nodelay");
    },
    ai: {
      threaten: 4.5
    },
    subSkill: {
      mark: {
        mod: {
          cardEnabled(card, player) {
            if (player.countMark("scls_kuangcai_mark") >= 5) {
              return false;
            }
          },
          cardSavable(card, player) {
            if (player.countMark("scls_kuangcai_mark") >= 5) {
              return false;
            }
          }
        },
        init(player, skill) {
          const num = player.getHistory("useCard").length - 1;
          player.setMark(skill, num, false);
        },
        onremove: true,
        intro: {
          content(storage, player) {
            return `本回合还可使用${get.cnNumber(5 - storage)}张牌`;
          }
        },
        charlotte: true,
        trigger: { player: "useCard1" },
        silent: true,
        async content(event, trigger, player) {
          player.addMark(event.name, 1, false);
        }
      }
    }
  },
  scls_shejian: {
    audio: "shejian",
    trigger: {
      player: "phaseDiscardEnd"
    },
    filter(event, player) {
      const cards2 = [];
      player.getHistory("lose", (evt) => {
        if (evt.type === "discard" && evt.getParent("phaseDiscard") === event) {
          cards2.addArray(evt.cards2);
        }
      });
      if (!cards2.length) {
        return false;
      }
      const suits = [];
      for (const card of cards2) {
        const suit = get.suit(card);
        if (suits.includes(suit)) {
          return false;
        }
        suits.push(suit);
      }
      return true;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt(event.name.slice(0, -5)), "弃置一名其他角色的一张牌", (card, player2, target) => {
        if (player2 === target) {
          return false;
        }
        return target.countDiscardableCards(player2, "he") > 0;
      }).set("ai", (target) => {
        const player2 = get.player();
        let att = get.attitude(player2, target);
        if (att >= 0) {
          return 0;
        }
        if (target.countDiscardableCards(player2, "h") > 0 && target.hasSkillTag("noh")) {
          att /= 6;
        }
        if (target.countDiscardableCards(player2, "e") > 0 && target.hasSkillTag("noe")) {
          att /= 4;
        }
        return -att;
      }).forResult();
    },
    async content(event, trigger, player) {
      await player.discardPlayerCard(event.targets[0], "he", true);
    }
  },
  scls_juezhi: {
    audio: "juezhi",
    enable: "phaseUse",
    filter(event, player) {
      return player.countCards("he") > 1;
    },
    filterCard: lib.filter.cardDiscardable,
    position: "he",
    selectCard: [2, Infinity],
    check(card) {
      if (ui.selected.cards.length > 1) {
        return 0;
      }
      const player = get.event().player;
      if (player.hasSkill("xingtu") && player.storage.xingtu) {
        const number = get.number(card);
        return player.getHp() - player.getUseValue(card, null, number % (player.storage.xingtu_mark || 13) !== 0);
      }
      return 5 - get.value(card);
    },
    allowChooseAll: true,
    async content(event, trigger, player) {
      const cards2 = get.cards(event.cards.length, true);
      await player.showCards(cards2, `${get.translation(player)}发动了【爵制】`);
      const result = await player.chooseCardButton({
        prompt: "爵制：选择要获得的牌",
        cards: cards2,
        forced: true,
        ai(button) {
          const player2 = get.player();
          const number = get.number(button.link);
          return player2.getUseValue(button.link, null, number % (player2.storage.xingtu_mark || 13) !== 0);
        }
      }).forResult();
      if (result?.links?.length) {
        await player.gain({
          cards: result.links,
          animate: "gain2"
        });
      }
    },
    ai: {
      order: 1,
      result: {
        player: 1
      }
    }
  },
  scls_lingren: {
    audio: "xinfu_lingren",
    inherit: "xinfu_lingren",
    filter(event, player) {
      if (!player.isPhaseUsing()) {
        return false;
      }
      if (event.getParent().triggeredTargets3.length > 1) {
        return false;
      }
      if (event.card.name === "sha") {
        return true;
      }
      return get.tag(event.card, "damage") && get.type(event.card) === "trick";
    },
    derivation: ["jianxiong", "xingshang"],
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = await player.chooseButton(["凌人：猜测其有哪些类别的手牌", [["basic", "trick", "equip"].map((i) => `caoying_${i}`), "vcard"]], [0, 3], true).set("ai", (button) => {
        const type = button.link[2].slice(8);
        return get.event().choice.includes(type);
      }).set(
        "choice",
        (() => {
          const choice = [];
          const known = target.getKnownCards(player);
          const cards2 = target.countCards("h", (card) => !known.includes(card));
          for (const card of known) {
            choice.add(get.type2(card, target));
          }
          if (!cards2 || choice.length > 2) {
            return choice;
          }
          if (!choice.includes("basic") && cards2 > 2 * Math.random()) {
            choice.push("basic");
          }
          if (!choice.includes("trick") && cards2 > 3 * Math.random()) {
            choice.push("trick");
          }
          if (!choice.includes("equip") && cards2 > 6 * Math.random()) {
            choice.push("equip");
          }
          return choice;
        })()
      ).forResult();
      if (!result?.bool) {
        return;
      }
      const choices = result.links.map((i) => i[2].slice(8));
      await target.showHandcards();
      let num = 0;
      ["basic", "trick", "equip"].forEach((type) => {
        if (choices.includes(type) === target.hasCard((card) => get.type2(card, target) === type, "h")) {
          num++;
        }
      });
      player.popup(`猜对${get.cnNumber(num)}项`);
      game.log(player, `猜对了${get.cnNumber(num)}项`);
      if (num > 0) {
        await player.draw(2);
      }
      if (num > 1) {
        const map = trigger.customArgs;
        const id = target.playerid;
        map[id] ??= {};
        if (typeof map[id].extraDamage !== "number") {
          map[id].extraDamage = 0;
        }
        map[id].extraDamage++;
      }
      if (num > 2) {
        await player.addTempSkills(get.info(event.name).derivation, { player: "phaseBegin" });
      }
    }
  },
  scls_qinzheng: {
    audio: "qinzheng",
    trigger: {
      player: ["useCard", "respond"]
    },
    filter(event, player) {
      const num = player.getAllHistory("useCard").length + player.getAllHistory("respond").length;
      return num % 3 === 0 || num % 5 === 0 || num % 8 === 0;
    },
    forced: true,
    async content(event, trigger, player) {
      let num = 0;
      const total = player.getAllHistory("useCard").length + player.getAllHistory("respond").length;
      if (total % 3 === 0) {
        num++;
      }
      if (total % 5 === 0) {
        num++;
      }
      if (total % 8 === 0) {
        num++;
      }
      if (num) {
        await player.draw(num);
      }
    },
    group: "scls_qinzheng_count",
    intro: {
      content: "本局游戏已使用或打出过#张牌"
    },
    subSkill: {
      count: {
        trigger: {
          player: ["useCard1", "respond"]
        },
        silent: true,
        firstDo: true,
        noHidden: true,
        async content(event, trigger, player) {
          player.storage.scls_qinzheng = player.getAllHistory("useCard").length + player.getAllHistory("respond").length;
          player.markSkill("scls_qinzheng");
        }
      }
    }
  }
};
const skills$C = {
  xinfu_yanyu: {
    trigger: {
      global: "phaseUseBegin"
    },
    direct: true,
    filter(event, player) {
      return player.hasCards("he");
    },
    async content(event, trigger, player) {
      const next = player.chooseToDiscard({
        prompt: get.prompt2("xinfu_yanyu"),
        position: "he"
      }).set("logSkill", "xinfu_yanyu");
      if (player === trigger.player) {
        const map = {
          basic: 0,
          trick: 0.1
        };
        const hs = trigger.player.getCards("h");
        let sha = false;
        for (const card of hs) {
          if (!trigger.player.hasValueTarget(card)) {
            continue;
          }
          if (card.name === "sha" && !sha) {
            sha = true;
            map.basic += 2;
          }
          if (card.name === "tao") {
            map.basic += 6;
          }
          if (card.name === "jiu") {
            map.basic += 2.5;
          }
          if (get.type(card) === "trick") {
            map.trick += get.value(card, player, "raw");
          }
        }
        next.set("goon", map).set("ai", (card) => {
          const map2 = _status.event.goon;
          const type = get.type(card, "trick");
          if (!map2[type]) {
            return -1;
          }
          return map2[type] - get.value(card);
        });
      } else {
        next.set("ai", (cardx) => {
          const map = {
            basic: 0,
            trick: 0
          };
          const hs = trigger.player.getCards("h");
          let sha = false;
          for (const card of hs) {
            if (card === cardx || !trigger.player.hasValueTarget(card)) {
              continue;
            }
            if (card.name === "sha" && !sha) {
              sha = true;
              map.basic += 2;
            }
            if (card.name === "tao") {
              map.basic += 6;
            }
            if (card.name === "jiu") {
              map.basic += 3;
            }
            if (get.type(card) === "trick") {
              map.trick += player.getUseValue(card);
            }
          }
          const type = get.type(cardx, "trick");
          if (!map[type]) {
            return -get.value(cardx);
          }
          return map[type] - get.value(cardx);
        });
      }
      const result = await next.forResult();
      if (!result.bool || !result.cards?.length) {
        return;
      }
      player.storage.xinfu_yanyu = get.type(result.cards[0], "trick");
      player.addTempSkill("xinfu_yanyu2", "phaseUseAfter");
    }
  },
  xinfu_yanyu2: {
    init(player, skill) {
      player.storage[skill] = 0;
    },
    onremove(player, skill) {
      delete player.storage.xinfu_yanyu;
      delete player.storage.xinfu_yanyu2;
    },
    trigger: {
      global: ["loseAfter", "cardsDiscardAfter", "loseAsyncAfter", "equipAfter"]
    },
    direct: true,
    sourceSkill: "xinfu_yanyu",
    filter(event, player) {
      if (player.storage.xinfu_yanyu2 >= 3) {
        return false;
      }
      const type = player.storage.xinfu_yanyu;
      const cards2 = event.getd();
      return cards2.some((card) => get.type(card, "trick") === type && get.position(card, true) === "d");
    },
    async content(event, trigger, player) {
      const type = player.storage.xinfu_yanyu;
      const cards2 = trigger.getd().filter((card) => get.type(card, "trick") === type && get.position(card, true) === "d");
      let logged = false;
      for (; cards2.length && player.storage.xinfu_yanyu2 < 3; ) {
        const cardResult = await player.chooseCardButton({
          cards: cards2,
          prompt: "【燕语】：是否将其中的一张牌交给一名角色？",
          ai: (button) => button.link.name === "du" ? 10 : get.value(button.link)
        }).forResult();
        if (!cardResult.bool) {
          break;
        }
        player.storage.xinfu_yanyu2++;
        if (!logged) {
          player.logSkill("xinfu_yanyu");
          player.addExpose(0.25);
          logged = true;
        }
        const card = cardResult.links[0];
        cards2.remove(card);
        const targetResult = await player.chooseTarget({
          forced: true,
          prompt: `请选择要获得${get.translation(card)}的角色`,
          ai: (target2) => {
            const player2 = _status.event.player;
            const card2 = _status.event.card;
            const value = get.value(card2);
            let attitude = get.attitude(player2, target2);
            if (player2.storage.xinfu_yanyu2 < 3 && target2 === _status.currentPhase && target2.hasValueTarget(card2, null, true)) {
              attitude *= 5;
            } else if (target2 === player2 && !player2.hasJudge("lebu") && get.type(card2) === "trick") {
              attitude *= 3;
            }
            if (target2.hasSkillTag("nogain")) {
              attitude /= 10;
            }
            return attitude * value;
          }
        }).set("card", card).forResult();
        const target = targetResult.targets[0];
        player.line(target, "green");
        await target.gain({ cards: [card], animate: "gain2" });
      }
    }
  },
  xinfu_xiaode: {
    trigger: { global: "dieAfter" },
    filter(event, player) {
      return !player.hasSkill("xinfu_xiaode_remove");
    },
    async cost(event, trigger, player) {
      const mainSkills = lib.character[trigger.player.name1 ?? trigger.player.name][3];
      const viceSkills = trigger.player.name2 != null ? lib.character[trigger.player.name2][3] : [];
      const skills2 = mainSkills.concat(viceSkills);
      const isSkillAvailable = (skill) => {
        const info = get.info(skill);
        return !(info.charlotte || info.zhuSkill || info.unique && !info.limited || info.juexingji || info.dutySkill || info.hiddenSkill);
      };
      const list = skills2.filter(isSkillAvailable);
      if (!list.length) {
        return;
      }
      const result = await player.chooseControl({
        controls: [...list, "cancel2"],
        prompt: get.prompt2("xinfu_xiaode"),
        ai: () => list.randomGet()
      }).forResult();
      if (result.control) {
        event.result = {
          bool: result.control !== "cancel2",
          cost_data: {
            control: result.control
          }
        };
      }
    },
    async content(event, trigger, player) {
      const { control } = event.cost_data;
      player.popup(control, "thunder");
      game.log(player, "获得了技能", `#g【${get.translation(control)}】`);
      player.addAdditionalSkill("xinfu_xiaode", [control]);
      player.addSkill("xinfu_xiaode_remove");
    },
    subSkill: {
      remove: {
        trigger: { player: "phaseAfter" },
        charlotte: true,
        silent: true,
        async content(event, trigger, player) {
          player.removeAdditionalSkill("xinfu_xiaode");
          player.removeSkill("xinfu_xiaode_remove");
        }
      }
    }
  },
  chixin: {
    group: ["chixin1", "chixin2"],
    mod: {
      cardUsableTarget(card, player, target) {
        if (card.name === "sha" && !target.hasSkill("chixin3") && player.inRange(target)) {
          return true;
        }
      }
    },
    trigger: { player: "useCardToPlayered" },
    silent: true,
    firstDo: true,
    locked: false,
    async content(event, trigger, player) {
      trigger.target.addTempSkill("chixin3");
    }
  },
  chixin1: {
    enable: ["chooseToRespond", "chooseToUse"],
    filterCard: { suit: "diamond" },
    position: "hes",
    viewAs: { name: "sha" },
    prompt: "将一张♦牌当杀使用或打出",
    sourceSkill: "chixin",
    check(card) {
      return 5 - get.value(card);
    },
    ai: {
      respondSha: true
    }
  },
  chixin2: {
    enable: ["chooseToUse", "chooseToRespond"],
    filterCard: { suit: "diamond" },
    viewAs: { name: "shan" },
    position: "hes",
    prompt: "将一张♦牌当闪使用或打出",
    sourceSkill: "chixin",
    check(card) {
      return 5 - get.value(card);
    },
    ai: {
      respondShan: true,
      effect: {
        target(card, player, target, current) {
          if (get.tag(card, "respondShan") && current < 0) {
            return 0.8;
          }
        }
      }
    }
  },
  chixin3: { charlotte: true },
  suiren: {
    trigger: { player: "phaseZhunbeiBegin" },
    skillAnimation: true,
    animationColor: "gray",
    filter(event, player) {
      return !player.storage.suiren;
    },
    direct: true,
    limited: true,
    async content(event, trigger, player) {
      const check = player.hp === 1 || player.hp === 2 && player.countCards("h") <= 1;
      const result = await player.chooseTarget({
        prompt: get.prompt2("suiren"),
        ai: (target) => check ? get.attitude(player, target) : 0
      }).forResult();
      if (!result.bool) {
        return;
      }
      player.storage.suiren = true;
      player.awakenSkill(event.name);
      player.logSkill("suiren", result.targets);
      await player.removeSkills("reyicong");
      await player.gainMaxHp();
      await player.recover();
      await result.targets[0].draw(3);
    }
  },
  xinmanjuan: {
    audio: "manjuan",
    forced: true,
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player) {
      const hs = player.getCards("h");
      return event.type !== "xinmanjuan" && event.getg(player).some((card) => hs.includes(card));
    },
    async content(event, trigger, player) {
      const hs = player.getCards("h");
      const cards2 = trigger.getg(player).filter((card) => hs.includes(card));
      const rawCards = cards2.slice();
      await player.loseToDiscardpile({ cards: cards2 });
      if (_status.currentPhase !== player) {
        return;
      }
      for (const card of cards2) {
        const cardsToGain = [];
        const number = get.number(card);
        for (const current of ui.discardPile.childNodes) {
          if (!rawCards.includes(current) && get.number(current) === number) {
            cardsToGain.push(current);
          }
        }
        if (!cardsToGain.length) {
          continue;
        }
        const result = await player.chooseButton({
          createDialog: ["是否获得其中的一张牌？", cardsToGain],
          ai: (button) => get.value(button.link)
        }).forResult();
        if (!result.bool) {
          continue;
        }
        await player.gain({ cards: [result.links[0]], animate: "gain2" }).set("type", "xinmanjuan");
      }
    },
    ai: {
      threaten: 4.2,
      nogain: 1,
      skillTagFilter(player) {
        return player !== _status.currentPhase;
      }
    }
  },
  manjuan: {
    audio: true,
    trigger: { global: "loseAfter" },
    filter(event, player) {
      if (event.type !== "discard") {
        return false;
      }
      if (event.player === player) {
        return false;
      }
      if (!player.countCards("he")) {
        return false;
      }
      return event.cards2.some((card) => get.position(card, true) === "d");
    },
    direct: true,
    gainable: true,
    async content(event, trigger, player) {
      if (trigger.delay === false) {
        await game.delay();
      }
      const cards2 = [];
      const suits = ["club", "spade", "heart", "diamond"];
      for (const card of trigger.cards2) {
        if (get.position(card, true) === "d") {
          cards2.push(card);
          suits.remove(get.suit(card));
        }
      }
      if (!cards2.length) {
        return;
      }
      let maxValue = 0;
      for (const card of cards2) {
        maxValue = Math.max(maxValue, get.value(card));
      }
      maxValue += cards2.length - 1;
      const result = await player.chooseToDiscard({
        position: "he",
        filterCard: { suit: suits },
        ai: (card) => _status.event.maxval - get.value(card)
      }).set("maxval", maxValue).set("dialog", [get.prompt(event.name), "hidden", cards2]).set("logSkill", event.name).forResult();
      if (!result.bool) {
        return;
      }
      await player.gain({ cards: cards2, animate: "gain2", log: true });
    },
    ai: {
      threaten: 1.3
    }
  },
  zuixiang: {
    skillAnimation: true,
    animationColor: "gray",
    audio: true,
    limited: true,
    trigger: { player: "phaseZhunbeiBegin" },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const cards2 = get.cards(3);
      const showEvent = player.showCards(cards2);
      const expansionEvent = player.addToExpansion({
        cards: cards2,
        animate: "gain2",
        gaintag: ["zuixiang2"]
      });
      await showEvent;
      await expansionEvent;
      if (lib.skill.zuixiang.filterSame(cards2)) {
        await player.gain({ cards: cards2, animate: "gain2" }).set("type", "xinmanjuan");
        return;
      }
      trigger._zuixiang = true;
      player.addSkill("zuixiang2");
    },
    filterSame(c) {
      const numbers = c.map((card) => get.number(card));
      return new Set(numbers).size !== numbers.length;
    }
  },
  zuixiang2: {
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    mod: {
      cardEnabled(card, player) {
        const type = get.type2(card);
        if (player.getExpansions("zuixiang2").some((card2) => get.type2(card2, false) === type)) {
          return false;
        }
      },
      cardRespondable(card, player) {
        const type = get.type2(card);
        if (player.getExpansions("zuixiang2").some((card2) => get.type2(card2, false) === type)) {
          return false;
        }
      },
      cardSavable(card, player) {
        const type = get.type2(card);
        if (player.getExpansions("zuixiang2").some((card2) => get.type2(card2, false) === type)) {
          return false;
        }
      }
    },
    trigger: {
      player: "phaseZhunbeiBegin",
      target: "useCardToBefore"
    },
    forced: true,
    charlotte: true,
    sourceSkill: "zuixiang",
    filter(event, player) {
      if (event.name === "phaseZhunbei") {
        return !event._zuixiang;
      }
      const type = get.type2(event.card);
      return player.getExpansions("zuixiang2").some((card) => get.type2(card) === type);
    },
    async content(event, trigger, player) {
      if (event.triggername === "useCardToBefore") {
        trigger.cancel();
        return;
      }
      const newCards = get.cards(3);
      await player.addToExpansion({
        cards: newCards,
        animate: "gain2",
        gaintag: ["zuixiang2"]
      });
      const cards2 = player.getExpansions("zuixiang2");
      const showEvent = player.showCards(cards2);
      if (!lib.skill.zuixiang.filterSame(cards2)) {
        await showEvent;
        return;
      }
      const gainEvent = player.gain({ cards: cards2, animate: "gain2", log: true }).set("type", "xinmanjuan");
      player.removeSkill("zuixiang2");
      await showEvent;
      await gainEvent;
    },
    ai: {
      effect: {
        target(card, player, target) {
          const type = get.type2(card);
          if (target.getExpansions("zuixiang2").some((card2) => get.type2(card2) === type)) {
            return "zeroplayertarget";
          }
        }
      }
    }
  },
  yanxiao: {
    audio: 2,
    enable: "phaseUse",
    filterCard: { suit: "diamond" },
    filterTarget(card, player, target) {
      return target.canAddJudge({ name: "yanxiao_card" });
    },
    check(card) {
      return 7 - get.value(card);
    },
    position: "he",
    filter(event, player) {
      return player.hasCards("he", { suit: "diamond" });
    },
    discard: false,
    lose: false,
    delay: false,
    prepare: "give",
    async content(event, trigger, player) {
      const { target, cards: cards2 } = event;
      game.addGlobalSkill("yanxiao_global");
      await target.addJudge({ name: "yanxiao_card" }, cards2);
      await game.delay();
    },
    ai: {
      order: 8,
      result: {
        target(player, target) {
          const harmfulJudge = target.hasCards(
            "j",
            (card) => get.effect(
              target,
              {
                name: card.viewAs || card.name,
                cards: [card]
              },
              target,
              target
            ) < 0
          );
          return harmfulJudge ? 1 : 0;
        }
      }
    }
  },
  yanxiao_global: {
    trigger: { player: "phaseJudgeBegin" },
    forced: true,
    filter(event, player) {
      return player.hasCards("j") && player.hasJudge("yanxiao_card");
    },
    async content(event, trigger, player) {
      await player.gain({ cards: player.getCards("j"), animate: "gain2" });
    },
    ai: {
      effect: {
        target_use(card, player, target) {
          if (get.type(card) === "delay" && target.hasJudge("yanxiao_card")) {
            return [0, 0.1];
          }
        }
      }
    }
  },
  anxian: {
    audio: 2,
    group: ["anxian_source", "anxian_target"],
    subSkill: {
      source: {
        audio: "anxian",
        trigger: { source: "damageBegin2" },
        filter(event, player) {
          return event.card && event.card.name === "sha";
        },
        check(event, player) {
          return get.damageEffect(event.player, player, player) <= 0;
        },
        async content(event, trigger, player) {
          if (trigger.player.hasCards("h")) {
            await trigger.player.chooseToDiscard({ forced: true });
          }
          const drawEvent = player.draw();
          trigger.cancel();
          await drawEvent;
        }
      },
      target: {
        audio: "anxian",
        trigger: { target: "useCardToTargeted" },
        direct: true,
        filter(event, player) {
          return event.card.name === "sha" && player.hasCards("h");
        },
        async content(event, trigger, player) {
          const result = await player.chooseToDiscard({
            prompt: get.prompt2("anxian"),
            ai: (card) => {
              if (get.attitude(player, trigger.player) > 0) {
                return 9 - get.value(card);
              }
              if (player.hasCards("h", { name: "shan" })) {
                return -1;
              }
              return 7 - get.value(card);
            }
          }).set("logSkill", "anxian").forResult();
          if (!result.bool) {
            return;
          }
          const drawEvent = trigger.player.draw();
          trigger.getParent()?.excluded.push(player);
          await drawEvent;
        }
      }
    }
  },
  junwei: {
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return player.countExpansions("yinling") >= 3;
    },
    async cost(event, trigger, player) {
      const cards2 = player.getExpansions("yinling");
      if (cards2.length > 3) {
        event.result = await player.chooseButton({
          selectButton: 3,
          createDialog: [get.prompt("junwei"), "hidden", cards2],
          ai: () => 1
        }).forResult();
        event.result.cards = event.result.links;
      } else {
        event.result = await player.chooseBool({ choice: true }).set("createDialog", [get.prompt("junwei"), "hidden", cards2]).set("dialogselectx", true).forResult();
        event.result.cards = cards2;
      }
    },
    async content(event, trigger, player) {
      const cards2 = event.cards;
      await player.loseToDiscardpile({ cards: cards2 });
      const result = await player.chooseTarget({
        forced: true,
        filterTarget: (_card, player2, target2) => player2 !== target2,
        ai: (target2) => -get.attitude(_status.event.player, target2) / Math.sqrt(1 + target2.hp)
      }).forResult();
      if (!result.bool || !result.targets?.length) {
        return;
      }
      const target = result.targets[0];
      player.line(result.targets);
      const shan = _status.connectMode || target.hasCards("h", (card) => card.name === "shan");
      let result2 = { bool: false };
      if (shan) {
        result2 = await target.chooseCard({
          prompt: `交给${get.translation(player)}一张【闪】，或失去1点体力`,
          filterCard: (card) => card.name === "shan",
          ai: () => get.event().target.hp < 3 ? 1 : 0
        }).set("target", target).forResult();
      }
      if (result2.bool) {
        await game.delay();
      }
      ui.clear();
      if (result2.bool) {
        const shanCards = result2.cards;
        target.$throw(shanCards);
        const recipientResult = await player.chooseTarget({
          prompt: `将${get.translation(shanCards)}交给一名角色`,
          forced: true,
          filterTarget: (_card, _player, current) => current !== target,
          ai: (current) => get.attitude(_status.event.player, current) / (current.countCards("h", "shan") + 1)
        }).forResult();
        player.line(recipientResult.targets, "green");
        await recipientResult.targets?.[0].gain({ cards: shanCards, animate: "gain2" }).set("giver", player);
        game.log(player, "将", shanCards, "交给", recipientResult.targets?.[0]);
        return;
      }
      await target.loseHp();
      if (!target.hasCards("e")) {
        return;
      }
      const equipmentResult = await player.choosePlayerCard({
        position: "e",
        prompt: `将${get.translation(target)}的一张装备牌移出游戏`,
        forced: true,
        target
      }).forResult();
      if (!equipmentResult.bool) {
        return;
      }
      const expansionEvent = target.addToExpansion({
        cards: [equipmentResult.links?.[0]],
        source: target,
        animate: "give",
        gaintag: ["junwei2"]
      });
      target.addSkill("junwei2");
      await expansionEvent;
    },
    ai: {
      combo: "yinling"
    }
  },
  junwei2: {
    mark: true,
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile({ cards: cards2 });
      }
    },
    trigger: { player: "phaseJieshuBegin" },
    forced: true,
    charlotte: true,
    sourceSkill: "junwei",
    async content(event, trigger, player) {
      const getEquippableCard = () => player.getExpansions("junwei2").find((card) => player.canEquip(card, true));
      for (let card = getEquippableCard(); card != null; card = getEquippableCard()) {
        player.$give(card, player, false);
        await game.delay(0.5);
        await player.equip(card);
      }
      player.removeSkill("junwei2");
    }
  },
  yinling: {
    enable: "phaseUse",
    filterCard: { color: "black" },
    position: "he",
    marktext: "锦",
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile({ cards: cards2 });
      }
    },
    filter(event, player) {
      return player.hasCards("he", { color: "black" }) && player.countExpansions("yinling") < 4;
    },
    filterTarget(card, player, target) {
      return target.hasCards("he") && target !== player;
    },
    check(card) {
      return 6 - get.value(card);
    },
    async content(event, trigger, player) {
      const { target } = event;
      const result = await player.choosePlayerCard({
        target,
        position: "hej",
        forced: true
      }).forResult();
      if (!result.bool || !result.links?.length) {
        return;
      }
      await player.addToExpansion({
        cards: result.links,
        source: target,
        animate: "give",
        gaintag: ["yinling"]
      });
    },
    ai: {
      order: 10.1,
      expose: 0.1,
      result: {
        target(player, target) {
          if (target.hasSkill("tuntian")) {
            return 0;
          }
          const es = target.getCards("e");
          const nh = target.countCards("h");
          const noe = es.length === 0 || target.hasSkillTag("noe");
          const noe2 = es.length === 1 && es[0].name === "baiyin" && target.hp < target.maxHp;
          const noh = nh === 0 || target.hasSkillTag("noh");
          if (noh && noe) {
            return 0;
          }
          if (noh && noe2) {
            return 0.01;
          }
          if (get.attitude(player, target) <= 0) {
            return target.countCards("he") ? -1.5 : 1.5;
          }
          const js = target.getCards("j");
          if (js.length) {
            const jj = js[0].viewAs ? { name: js[0].viewAs } : js[0];
            if (jj.name === "guohe") {
              return 3;
            }
            if (js.length === 1 && get.effect(target, jj, target, player) >= 0) {
              return -1.5;
            }
            return 2;
          }
          return -1.5;
        }
      }
    }
  },
  fenyong: {
    audio: 2,
    trigger: { player: "damageEnd" },
    frequent: true,
    filter(event, player) {
      return !player.hasSkill("fenyong_mark");
    },
    async content(event, trigger, player) {
      player.addSkill("fenyong_mark");
    },
    subSkill: {
      mark: {
        audio: "fenyong",
        mark: true,
        intro: {
          content: "防止你受到的所有伤害"
        },
        trigger: { player: "damageBegin3" },
        charlotte: true,
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          trigger.cancel();
        },
        ai: {
          maixie: true,
          maixie_hp: true,
          nofire: true,
          nothunder: true,
          nodamage: true,
          effect: {
            target(card, player, target, current) {
              if (get.tag(card, "damage")) {
                return "zeroplayertarget";
              }
            }
          }
        }
      }
    }
  },
  xuehen: {
    audio: 2,
    trigger: { global: "phaseJieshuBegin" },
    forced: true,
    locked: false,
    filter(event, player) {
      return player.hasSkill("fenyong_mark") && event.player.isIn();
    },
    async content(event, trigger, player) {
      player.removeSkill("fenyong_mark");
      const list = [];
      if (trigger.player.countDiscardableCards(player, "he") && player.isDamaged()) {
        list.add("弃牌");
      }
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      const targets = game.filterPlayer((current) => player.canUse(card, current, false));
      if (targets.length) {
        list.add("出杀");
      }
      if (!list.length) {
        return;
      }
      const result = list.length > 1 ? await player.chooseControl("弃牌", "出杀").set("prompt", `###雪恨###弃置${get.translation(trigger.player)}${get.cnNumber(player.getDamagedHp())}张牌，或对任意一名角色使用一张杀`).set("ai", () => get.event().resultx).set(
        "resultx",
        (() => {
          const getV = (current) => get.effect(current, card, player, player);
          const target = targets.maxBy(getV);
          const eff = getV(target);
          const eff2 = get.effect(trigger.player, { name: "guohe_copy2" }, player, player);
          if (eff < 0) {
            return 0;
          }
          if (eff2 < 0) {
            return 1;
          }
          return eff > eff2 ? 1 : 0;
        })()
      ).forResult() : {
        control: list[0]
      };
      if (!result) {
        return;
      }
      if (result.control === "弃牌") {
        player.line(trigger.player, "green");
        const num = Math.min(player.getDamagedHp(), trigger.player.countDiscardableCards(player, "he"));
        if (num > 0) {
          player.discardPlayerCard(trigger.player, true, "he", num);
        }
      } else {
        await player.chooseUseTarget(card, true, false, "nodistance");
      }
    },
    ai: {
      combo: "fenyong"
    }
  },
  mouduan: {
    audio: 1,
    init2(player) {
      game.broadcastAll((player2) => {
        player2._mouduan_mark = player2.mark("武", {
          content: "拥有技能【激昂】、【谦逊】"
        });
      }, player);
      player.addAdditionalSkill("mouduan", ["jiang", "qianxun"]);
    },
    derivation: ["jiang", "qianxun", "yingzi", "keji"],
    onremove(player) {
      game.broadcastAll((player2) => {
        if (!player2._mouduan_mark) {
          return;
        }
        player2._mouduan_mark.delete();
        delete player2._mouduan_mark;
      }, player);
      player.removeAdditionalSkills("mouduan");
    },
    trigger: { player: "loseEnd" },
    forced: true,
    locked: false,
    filter(event, player) {
      return player._mouduan_mark && player._mouduan_mark.name === "武" && player.countCards("h") <= 2;
    },
    async content(event, trigger, player) {
      game.broadcastAll((player2) => {
        if (!player2._mouduan_mark) {
          return;
        }
        player2._mouduan_mark.name = "文";
        player2._mouduan_mark.skill = "文";
        player2._mouduan_mark.firstChild.innerHTML = "文";
        player2._mouduan_mark.info.content = "拥有技能【英姿】、【克己】";
      }, player);
      player.addAdditionalSkills("mouduan", ["yingzi", "keji"]);
    },
    group: "mouduan2"
  },
  mouduan2: {
    audio: 1,
    trigger: { global: "phaseZhunbeiBegin" },
    sourceSkill: "mouduan",
    //priority:5,
    filter(event, player) {
      return player._mouduan_mark && player._mouduan_mark.name === "文" && player.countCards("h") > 2;
    },
    direct: true,
    async content(event, trigger, player) {
      const result = await player.chooseToDiscard({
        position: "he",
        prompt: "谋断：是否弃置一张牌将标记变为“武”？",
        ai: () => -1
      }).forResult();
      if (!result.bool || player.countCards("h") <= 2) {
        return;
      }
      game.broadcastAll((player2) => {
        if (!player2._mouduan_mark) {
          return;
        }
        player2._mouduan_mark.name = "武";
        player2._mouduan_mark.skill = "武";
        player2._mouduan_mark.firstChild.innerHTML = "武";
        player2._mouduan_mark.info.content = "拥有技能【激昂】、【谦逊】";
      }, player);
      player.addAdditionalSkills("mouduan", ["jiang", "qianxun"]);
    }
  },
  tanhu: {
    audio: 1,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    filter(event, player) {
      return player.countCards("h") > 0;
    },
    async content(event, trigger, player) {
      const { target } = event;
      const result = await player.chooseToCompare(target).forResult();
      if (!result.bool) {
        return;
      }
      target.addTempSkill("tanhu2");
    },
    ai: {
      result: {
        target(player, target) {
          const hs = player.getCards("h");
          if (hs.length < 3) {
            return 0;
          }
          const hasGoodCompareCard = hs.some((card) => card.number >= 9 && get.value(card) < 7);
          return hasGoodCompareCard ? -1 : 0;
        }
      },
      order: 9
    },
    group: "tanhu3"
  },
  tanhu2: {
    mark: true,
    intro: {
      content: "已成为探虎目标"
    }
  },
  tanhu3: {
    mod: {
      globalFrom(from, to) {
        if (to.hasSkill("tanhu2")) {
          return -Infinity;
        }
      },
      wuxieRespondable(card, player, target) {
        if (target && target.hasSkill("tanhu2")) {
          return false;
        }
      }
    }
  },
  jie: {
    audio: 1,
    trigger: { source: "damageBegin1" },
    filter(event) {
      return event.card?.name === "sha" && get.color(event.card) === "red";
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.num++;
    }
  },
  dahe: {
    audio: true,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    filter(event, player) {
      return game.hasPlayer((current) => get.info("dahe").filterTarget(null, player, current));
    },
    async content(event, trigger, player) {
      const { target } = event;
      const result = await player.chooseToCompare(target).set("preserve", "win").forResult();
      if (result?.bool) {
        target.addTempSkill(`${event.name}_effect`);
        const card = result?.target;
        if (get.itemtype(card) === "card") {
          const result2 = await player.chooseTarget(`将${get.translation(card)}交给一名角色`, (card2, player2, target2) => target2.hp <= player2.hp).set("ai", (target2) => {
            const { player: player2, du } = get.event();
            const att = get.attitude(player2, target2);
            if (du) {
              return -att;
            }
            return att;
          }).set("du", card.name === "du").forResult();
          if (result2?.bool && result2?.targets?.length) {
            player.line(result2.targets, "green");
            await result2.targets[0].gain(card, "gain2");
          }
        }
      } else if (player.countCards("h")) {
        await player.showHandcards();
        if (player.countDiscardableCards(player, "h")) {
          await player.chooseToDiscard("h", true);
        }
      }
    },
    ai: {
      result: {
        target(player, target) {
          const hs = player.getCards("h");
          if (hs.length < 3) {
            return 0;
          }
          const hasGoodCompareCard = hs.some((card) => card.number >= 9 && get.value(card) < 7);
          if (!hasGoodCompareCard) {
            return 0;
          }
          if (player.canUse("sha", target) && player.countCards("h", "sha")) {
            return -2;
          }
          return -0.5;
        }
      },
      order: 9
    },
    subSkill: {
      effect: {
        charlotte: true,
        mark: true,
        intro: { content: "非红桃闪无效" },
        mod: {
          cardRespondable(card, player) {
            if (card.name === "shan") {
              const suit = get.suit(card);
              if (suit !== "heart" && suit !== "unsure") {
                return false;
              }
            }
          },
          cardEnabled(card, player) {
            if (card.name === "shan") {
              const suit = get.suit(card);
              if (suit !== "heart" && suit !== "unsure") {
                return false;
              }
            }
          }
        }
      }
    }
  },
  shichou: {
    initSkill(skill) {
      if (!lib.skill[skill]) {
        lib.skill[skill] = {
          mod: {
            aiOrder(player, card, num) {
              if (typeof card === "object" && get.tag(card, "recover")) {
                return num / 114514;
              }
            }
          },
          charlotte: true,
          onremove: true,
          mark: true,
          marktext: "誓",
          intro: {
            markcount: () => 0,
            content: (storage) => `已为${get.translation(storage)}李代桃僵`
          }
        };
        lib.translate[skill] = "誓仇";
        lib.translate[`${skill}_bg`] = "仇";
      }
    },
    audio: true,
    skillAnimation: true,
    animationColor: "orange",
    limited: true,
    trigger: { player: "phaseZhunbeiBegin" },
    zhuSkill: true,
    filter(event, player) {
      if (!player.hasZhuSkill("shichou")) {
        return false;
      }
      if (player.countCards("he") < 2) {
        return false;
      }
      return game.hasPlayer((current) => current !== player && current.group === "shu");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        prompt: get.prompt2(event.skill),
        selectCard: 2,
        filterTarget(card, player2, target) {
          return target.group === "shu" && target !== player2;
        },
        filterCard: true,
        position: "he",
        ai1(card) {
          return 7 - get.value(card);
        },
        ai2(target) {
          const player2 = get.player();
          if (player2.hasUnknown()) {
            return 0;
          }
          if (target.hasSkillTag("nodamage")) {
            return 10;
          }
          const att = get.attitude(player2, target);
          if (att <= 0) {
            if (target.hp === 1) {
              return (10 - att) / 2;
            }
            return 10 - att;
          }
          if (target.hp === 1) {
            return 0;
          }
          return (10 - att) / 4;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const {
        targets: [target],
        cards: cards2
      } = event;
      player.awakenSkill(event.name);
      await player.give(cards2, target);
      player.addSkill(`${event.name}_effect`);
      const skill = `${event.name}_${player.playerid}`;
      game.broadcastAll(lib.skill.shichou.initSkill, skill);
      for (const current of game.filterPlayer()) {
        current.removeSkill(skill);
        if (current === target) {
          target.addSkill(skill);
          target.storage[skill] = player;
          target.markSkill(skill);
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        trigger: {
          global: ["dying", "die"],
          player: "damageBegin4"
        },
        filter(event, player) {
          const target = game.findPlayer((current) => current.storage[`shichou_${player.playerid}`] === player);
          if (!target) {
            return false;
          }
          if (event.name === "damage") {
            return target.isIn();
          }
          return event.player === target;
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          const target = game.findPlayer((current) => current.storage[`shichou_${player.playerid}`] === player);
          if (trigger.name === "damage") {
            trigger.cancel();
            await game.delay(0.5);
            await target.damage(trigger.source?.isIn() ? trigger.source : "nosource", trigger.nature, trigger.num).set("card", trigger.card).set("cards", trigger.cards);
            await target.draw(trigger.num);
          } else {
            target.removeSkill(`shichou_${player.playerid}`);
            player.removeSkill(event.name);
          }
        },
        ai: {
          effect: {
            target(card, player, target, current) {
              if (get.tag(card, "damage")) {
                if (player.hasSkillTag("jueqing", false, target)) {
                  return [1, -2];
                }
                if (get.attitude(player, target) > 0) {
                  return [0, 0];
                }
                const targetx = game.findPlayer((current2) => current2.storage[`shichou_${target.playerid}`] === target);
                if (!targetx?.isIn()) {
                  return;
                }
                const bool = game.hasPlayer((current2) => current2.hasCard((card2) => current2.canSaveCard(card2, targetx), "hs") && get.attitude(current2, targetx) > 0);
                const num = -1;
                if (targetx.hp >= 4) {
                  return [0, num * 2];
                }
                if (targetx.hp === 3) {
                  return [0, num * 1.5];
                }
                if (targetx.hp <= 2) {
                  return [0, bool ? num : -num];
                }
              }
            }
          }
        }
      }
    }
  },
  zhaolie: {
    trigger: { player: "phaseDrawBegin2" },
    filter(event, player) {
      return event.num > 0 && !event.numFixed && game.hasPlayer((current) => player.inRange(current));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => player2.inRange(target)).set("ai", (target) => {
        const player2 = get.player();
        if (get.attitude(player2, target) > 0) {
          return 0;
        }
        return get.damageEffect(target, player2, player2);
      }).forResult();
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      trigger.num--;
      if (trigger.num <= 0) {
        await game.delay();
      }
      player.when({ player: "phaseDrawEnd" }).filter((evt) => trigger === evt).step(async () => {
        let cards2 = get.cards(3);
        await game.cardsGotoOrdering(cards2);
        await player.showCards(cards2);
        const cards22 = cards2.filter((card) => get.type(card) !== "basic" || get.name(card) === "tao");
        const num = cards2.filter((card) => get.type(card) !== "basic").length;
        if (cards22.length) {
          cards2.removeArray(cards22);
          await game.cardsDiscard(cards22);
        }
        cards2 = cards2.filter((card) => get.type(card) === "basic");
        if (!target.isIn()) {
          return;
        }
        let result;
        if (!num) {
          if (!cards2.length) {
            return;
          }
          result = await target.chooseTarget((card, player2, target2) => get.event().list.includes(target2), `选择一个目标获得${get.translation(cards2)}`, true).set("ai", (target2) => {
            const { player: player2, cardsx } = get.event();
            return get.attitude(player2, target2) * get.value(cardsx, target2);
          }).set("list", [player, target]).set("cardsx", cards2).forResult();
          if (result?.bool && result?.targets?.length) {
            await result.targets[0].gain(cards2, "gain2");
          }
        } else {
          let str = `弃置${get.cnNumber(num)}张牌`;
          if (cards2.length) {
            str += `并令${get.translation(player)}获得${get.translation(cards2)}`;
          }
          str += `，或受到${get.translation(player)}的${num}点伤害`;
          if (cards2.length) {
            str += `并获得${get.translation(cards2)}`;
          }
          result = target.countCards("he") < num ? { bool: false } : await target.chooseToDiscard(num, "he", get.prompt("zhaolie"), str).set("ai", (card) => {
            const { goon } = get.event();
            return goon ? 8 - get.value(card) : 0;
          }).set("goon", get.damageEffect(target, player, target) < 0 && target.getHp() <= 2 * num || num >= 2 && !target.countCards("hs", (card) => target.canSaveCard(card, target)) >= num).forResult();
          if (result?.bool) {
            if (cards2.length) {
              await player.gain(cards2, "gain2");
            }
          } else {
            if (num) {
              await target.damage(num);
            }
            if (cards2.length) {
              if (target.isIn()) {
                await target.gain(cards2, "gain2");
              } else {
                await game.cardsDiscard(cards2);
              }
            }
          }
        }
      });
    }
  },
  fulu: {
    trigger: { player: "useCard1" },
    filter(event, player) {
      if (event.card.name === "sha" && !game.hasNature(event.card)) {
        return true;
      }
    },
    audio: true,
    check(event, player) {
      let eff = 0;
      for (const target of event.targets) {
        const eff1 = get.damageEffect(target, player, player);
        const eff2 = get.damageEffect(target, player, player, "thunder");
        eff += eff2;
        eff -= eff1;
      }
      return eff >= 0;
    },
    async content(event, trigger, player) {
      game.setNature(trigger.card, "thunder");
      if (get.itemtype(trigger.card) === "card") {
        const next = game.createEvent("fulu_clear");
        next.card = trigger.card;
        event.next.remove(next);
        trigger.after.push(next);
        next.setContent(async ({ card }) => {
          game.setNature(card, []);
        });
      }
    }
  },
  fuji: {
    trigger: { global: "damageBegin1" },
    filter(event) {
      return event.source && event.source.isIn() && event.hasNature("thunder");
    },
    check(event, player) {
      return get.attitude(player, event.source) > 0 && get.attitude(player, event.player) < 0;
    },
    prompt(event) {
      return `${get.translation(event.source)}即将对${get.translation(event.player)}造成伤害，${get.prompt("fuji")}`;
    },
    logTarget: "source",
    async content(event, trigger, player) {
      trigger.source.judge().callback = lib.skill.fuji.callback;
    },
    async callback(event, trigger, player) {
      const evt = event.getParent(2);
      switch (event.judgeResult.color) {
        case "black":
          evt._trigger.num++;
          break;
        case "red":
          evt._trigger.source.gain(event.card, "gain2");
          break;
      }
    }
  }
};
const skills$B = {
  //桌游志贴纸
  spyinzhi: {
    trigger: { player: "damageEnd" },
    frequent: true,
    filter(event, player) {
      return event.num > 0;
    },
    getIndex: (event) => event.num,
    async content(event, trigger, player) {
      let cards2 = get.cards(2);
      await game.cardsGotoOrdering(cards2);
      await player.showCards(cards2);
      const { source } = trigger;
      let count = cards2.filter((card) => get.suit(card) === "spade").length;
      while (count-- && source?.isIn() && game.hasPlayer((current) => current !== source && source.countGainableCards(current, "h"))) {
        const result = await player.chooseTarget(`令一名角色获得${get.translation(source)}的一张手牌`, (card, player2, target2) => {
          const source2 = get.event().source;
          return target2 !== source2 && source2.countGainableCards(target2, "h");
        }).set("source", source).set("ai", (target2) => {
          const { player: player2, source: source2 } = get.event();
          return get.effect(target2, { name: "shunshou_copy", position: "h" }, source2, player2);
        }).forResult();
        if (!result?.targets?.length) {
          continue;
        }
        const [target] = result.targets;
        player.line([source, target], "green");
        if (source.countGainableCards(target, "h")) {
          await target.gainPlayerCard(source, "h", true);
        }
      }
      cards2 = cards2.filter((card) => get.suit(card) !== "spade");
      if (cards2.length) {
        await player.gain(cards2, "gain2", "log");
      }
    }
  },
  spmingjian: {
    trigger: { global: "phaseBegin" },
    filter(event, player) {
      return player.hasCards("he");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard({
        prompt: get.prompt2("spmingjian", trigger.player),
        position: "he",
        ai: (card) => {
          if (get.attitude(player, trigger.player) > 0 && trigger.player.countCards("j") > 0) {
            return 5 - get.value(card);
          }
          return -1;
        },
        filterCard: (card, cardPlayer, chooseEvent) => get.position(card) !== "e" || lib.filter.cardDiscardable(card, cardPlayer, chooseEvent)
      }).forResult();
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const card = event.cards[0];
      let index;
      if (get.position(card) === "e") {
        index = 0;
      } else if (!lib.filter.cardDiscardable(card, player, event)) {
        index = 1;
      } else {
        const name = get.translation(trigger.player);
        const controlResult = await player.chooseControl({
          choiceList: [`令${name}跳过本回合的判定阶段`, `令${name}于本回合的判定中不触发「判定结果生效前」的时机`],
          ai: () => 0
        }).forResult();
        index = controlResult.index;
      }
      if (index === 0) {
        const discardEvent = player.discard({ cards: [card] });
        trigger.player.skip("phaseJudge");
        await discardEvent;
        return;
      }
      const expansionEvent = trigger.player.addToExpansion({
        cards: [card],
        source: player,
        animate: "giveAuto",
        gaintag: ["spmingjian_charlotte"]
      });
      trigger.player.addSkill("spmingjian_charlotte");
      await expansionEvent;
    },
    ai: {
      expose: 0.25
    }
  },
  spmingjian_charlotte: {
    trigger: { player: ["judgeBefore", "phaseAfter"] },
    forced: true,
    firstDo: true,
    silent: true,
    popup: false,
    charlotte: true,
    sourceSkill: "spmingjian",
    async content(event, trigger, player) {
      if (trigger.name === "phase") {
        player.removeSkill(event.name);
      } else {
        trigger.noJudgeTrigger = true;
      }
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile({ cards: cards2 });
      }
    },
    marktext: "鉴",
    intro: {
      name: "明鉴",
      content: "expansion",
      markcount: "expansion"
    }
  },
  spshude: {
    trigger: { player: "phaseJieshuBegin" },
    frequent: true,
    filter(event, player) {
      return player.countCards("h") < player.maxHp;
    },
    async content(event, trigger, player) {
      await player.drawTo(player.maxHp);
    }
  },
  spfuluan: {
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return player.inRange(target);
    },
    selectCard: 3,
    position: "he",
    check(card) {
      return 5 - get.value(card);
    },
    complexCard: true,
    filterCard(card, player) {
      if (!ui.selected.cards.length) {
        return player.countCards("he", { suit: get.suit(card) }) > 2;
      }
      return get.suit(card) === get.suit(ui.selected.cards[0]);
    },
    async content(event, trigger, player) {
      await event.target.turnOver();
      await player.addTempSkill("spfuluan2");
    },
    ai: {
      order: 1,
      result: {
        target(player, target) {
          if (target.isTurnedOver()) {
            return 2;
          }
          return -1;
        }
      }
    }
  },
  spfuluan2: {
    mod: {
      cardEnabled(card) {
        if (card.name === "sha") {
          return false;
        }
      }
    }
  },
  spzhaoxin: {
    trigger: { player: "phaseDrawEnd" },
    check(event, player) {
      return player.getUseValue({ name: "sha", isCard: true }) > 0;
    },
    filter(event, player) {
      return player.hasCards("h");
    },
    async content(event, trigger, player) {
      await player.showHandcards();
      await player.chooseUseTarget({
        card: get.autoViewAs({ name: "sha" }),
        addCount: false
      });
    }
  },
  splanggu: {
    trigger: { player: "damageEnd" },
    filter(event, player) {
      return get.itemtype(event.source) === "player";
    },
    logTarget: "source",
    async content(event, trigger, player) {
      const result = await player.judge().forResult();
      if (!trigger.source.hasCards("h")) {
        return;
      }
      await player.discardPlayerCard({
        target: trigger.source,
        position: "h",
        selectButton: [1, Infinity],
        allowChooseAll: true,
        filterButton: (button) => get.suit(button.link) === get.event().suit,
        visible: true
      }).set("suit", result.suit);
    },
    group: "splanggu_rewrite"
  },
  splanggu_rewrite: {
    trigger: { player: "judge" },
    sourceSkill: "splanggu",
    filter(event, player) {
      return player.hasCards("hs") && event.getParent()?.name === "splanggu";
    },
    direct: true,
    async content(event, trigger, player) {
      const cardResult = await player.chooseCard({
        prompt: `狼顾的判定结果为${get.translation(trigger.player.judging[0])}，是否打出一张手牌进行代替？`,
        position: "hs",
        filterCard(card, player2) {
          const event2 = get.event();
          const mod2 = game.checkMod(card, player2, event2, "unchanged", "cardEnabled2", player2);
          if (mod2 !== "unchanged") {
            return Boolean(mod2);
          }
          const mod = game.checkMod(card, player2, "unchanged", "cardRespondable", player2);
          if (mod !== "unchanged") {
            return Boolean(mod);
          }
          return true;
        },
        ai: () => -1
      }).forResult();
      if (!cardResult.bool || !cardResult.cards?.length) {
        return;
      }
      await player.respond({
        cards: cardResult.cards,
        highlight: true,
        skill: "splanggu",
        noOrdering: true
      });
      const judgedCard = trigger.player.judging[0];
      if (judgedCard.clone) {
        judgedCard.clone.classList.remove("thrownhighlight");
        game.broadcast((card) => {
          if (card.clone) {
            card.clone.classList.remove("thrownhighlight");
          }
        }, judgedCard);
        game.addVideo("deletenode", player, get.cardsInfo([judgedCard.clone]));
      }
      const discardEvent = game.cardsDiscard(judgedCard);
      trigger.player.judging[0] = cardResult.cards[0];
      trigger.orderingCards.addArray(cardResult.cards);
      game.log(trigger.player, "的判定牌改为", cardResult.cards[0]);
      const delayEvent = game.delay(2);
      await discardEvent;
      await delayEvent;
    }
  },
  sphantong: {
    trigger: {
      player: "loseEnd"
    },
    frequent: true,
    filter(event, player) {
      return event.type === "discard" && event.getParent(3)?.name === "phaseDiscard" && event.cards.filterInD("d").length > 0;
    },
    async content(event, trigger, player) {
      if (!player.storage.sphantong) {
        player.storage.sphantong = [];
      }
      const cards2 = trigger.cards.filterInD("d");
      player.storage.sphantong.addArray(cards2);
      player.$gain2(cards2);
      game.log(player, "将", cards2, "置于武将牌上");
      player.markSkill("sphantong");
    },
    group: ["sphantong_gain"],
    derivation: ["hujia", "jijiang", "jiuyuan", "xueyi"],
    marktext: "诏",
    intro: {
      content: "cards",
      onunmark: "throw"
    }
  },
  sphantong_gain: {
    trigger: { global: "phaseBegin" },
    direct: true,
    sourceSkill: "sphantong",
    filter(event, player) {
      return player.storage.sphantong && player.storage.sphantong.length > 0;
    },
    async content(event, trigger, player) {
      const cardResult = await player.chooseButton({
        createDialog: [get.prompt("sphantong"), player.storage.sphantong],
        ai: () => {
          const player2 = get.event().player;
          if (_status.currentPhase === player2) {
            if ((player2.hasJudge("lebu") || player2.skipList.includes("phaseUse")) && game.hasPlayer((current) => current !== player2 && current.group === "qun")) {
              return 1;
            }
            if (!player2.hasJudge("lebu") && !player2.skipList.includes("phaseUse") && game.hasPlayer((current) => current !== player2 && current.group === "shu" && current.hasSha() && get.attitude(player2, current) > 0 && get.attitude(current, player2) > 0) && game.hasPlayer((target) => player2.canUse({ name: "sha" }, target) && get.effect(target, { name: "sha" }, player2, player2) > 0)) {
              return 1;
            }
          } else if (!player2.hasShan("all") && game.hasPlayer((current) => current !== player2 && current.group === "wei" && current.mayHaveShan(player2, "respond") && get.attitude(player2, current) > 0 && get.attitude(current, player2) > 0)) {
            return 1;
          }
          return -1;
        }
      }).forResult();
      if (!cardResult.bool) {
        return;
      }
      player.logSkill("sphantong");
      const card = cardResult.links[0];
      player.$throw(card);
      game.log(player, "将", card, "置入了弃牌堆");
      player.storage.sphantong.remove(card);
      player[player.storage.sphantong.length > 0 ? "markSkill" : "unmarkSkill"]("sphantong");
      const discardEvent = game.cardsDiscard(card);
      const list = ["hujia", "jijiang", "jiuyuan", "xueyi"].filter((skill) => !player.hasSkill(skill));
      await discardEvent;
      if (!list.length) {
        return;
      }
      const controlResult = await player.chooseControl({
        controls: list,
        prompt: "选择获得以下技能中的一个",
        ai: () => {
          const player2 = get.event().player;
          if (_status.currentPhase === player2) {
            if ((player2.hasJudge("lebu") || player2.skipList.includes("phaseUse")) && game.hasPlayer((current) => current !== player2 && current.group === "qun")) {
              return "xueyi";
            }
            if (!player2.hasJudge("lebu") && !player2.skipList.includes("phaseUse") && game.hasPlayer((current) => current !== player2 && current.group === "shu" && current.hasSha() && get.attitude(player2, current) > 0 && get.attitude(current, player2) > 0) && game.hasPlayer((target) => player2.canUse({ name: "sha" }, target) && get.effect(target, { name: "sha" }, player2, player2) > 0)) {
              return "jijiang";
            }
          } else if (!player2.hasShan("all") && game.hasPlayer((current) => current !== player2 && current.group === "wei" && current.mayHaveShan(player2, "respond") && get.attitude(player2, current) > 0 && get.attitude(current, player2) > 0)) {
            return "hujia";
          }
        }
      }).forResult();
      player.addTempSkills(controlResult.control);
    }
  },
  sphuangen: {
    trigger: { global: "useCardToPlayered" },
    filter(event, player) {
      if (!event.isFirstTarget) {
        return false;
      }
      if (get.type(event.card) !== "trick") {
        return false;
      }
      if (get.info(event.card).multitarget) {
        return false;
      }
      if (event.targets.length < 2) {
        return false;
      }
      return player.hp > 0;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt("sphuangen"),
        selectTarget: [1, Math.min(player.hp, trigger.targets.length)],
        filterTarget: (_card, _player, target) => get.event().targets.includes(target),
        ai: (target) => -get.effect(target, trigger.card, trigger.player, player)
      }).set("targets", trigger.targets).forResult();
    },
    async content(event, trigger, player) {
      trigger.excluded.addArray(event.targets);
      await player.draw();
    },
    ai: { threaten: 3.5 },
    global: "sphuangen_ai",
    subSkill: {
      ai: {
        ai: {
          effect: {
            player_use(card, player) {
              if (typeof card !== "object" || !game.hasPlayer((target) => target.hasSkill("sphuangen") && (get.attitude(player, target) < 0 || get.attitude(target, player) < 0)) || game.countPlayer((target) => player.canUse(card, target)) < 2) {
                return;
              }
              if (get.info(card)?.type !== "trick") {
                return;
              }
              const select = get.info(card).selectTarget;
              let range;
              if (select === void 0) {
                range = [1, 1];
              } else if (typeof select === "number") {
                range = [select, select];
              } else if (get.itemtype(select) === "select") {
                range = select;
              } else if (typeof select === "function") {
                range = select(card, player);
                if (typeof range === "number") {
                  range = [range, range];
                }
              }
              game.checkMod(card, player, range, "selectTarget", player);
              if (range[1] === -1 || range[1] > 1 && ui.selected.targets && ui.selected.targets.length) {
                return "zeroplayertarget";
              }
            }
          }
        }
      }
    }
  },
  spyicong: {
    trigger: { player: "phaseDiscardEnd" },
    locked: false,
    filter(event, player) {
      return player.hasCards("he");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard({
        position: "he",
        selectCard: [1, player.countCards("he")],
        prompt: get.prompt2("spyicong"),
        allowChooseAll: true,
        ai: (card) => {
          if (card.name === "du") {
            return 10;
          }
          if (ui.selected.cards.length) {
            return -1;
          }
          return 4 - get.value(card);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      await player.addToExpansion({
        cards: event.cards,
        source: player,
        animate: "give",
        gaintag: ["spyicong"]
      });
    },
    mod: {
      globalTo(from, to, num) {
        return num + to.getExpansions("spyicong").length;
      }
    },
    marktext: "扈",
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile({ cards: cards2 });
      }
    },
    intro: {
      name: "义从",
      content(storage, player) {
        return `共有${get.cnNumber(player.getExpansions("spyicong").length)}张“扈”`;
      },
      markcount: "expansion"
    }
  },
  sptuji: {
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    locked: false,
    filter(event, player) {
      return player.hasExpansions("spyicong");
    },
    async content(event, trigger, player) {
      const cards2 = player.getExpansions("spyicong");
      const num = cards2.length;
      player.addMark("sptuji2", num, false);
      player.addTempSkill("sptuji2");
      player.loseToDiscardpile({ cards: cards2 });
      if (num <= 1) {
        await player.draw();
      }
    },
    ai: {
      combo: "spyicong"
    }
  },
  sptuji2: {
    onremove: true,
    charlotte: true,
    mod: {
      globalFrom(from, to, num) {
        return num - from.countMark("sptuji2");
      }
    },
    marktext: "突",
    intro: {
      name: "突骑",
      content: "至其他角色的距离-#"
    }
  }
};
const skills$A = {
  //文和乱武
  nsyangwu: {
    enable: "phaseUse",
    usable: 1,
    filterCard: { suit: "heart" },
    filterTarget(card, player, target) {
      return target !== player && target.countCards("h") > player.countCards("h");
    },
    filter(event, player) {
      const info = lib.skill.nsyangwu;
      return player.hasCards("h", info.filterCard) && game.hasPlayer((target) => info.filterTarget(null, player, target));
    },
    check(card) {
      let num = 0;
      const player = _status.event.player;
      for (const current of game.filterPlayer()) {
        if (current !== player && get.attitude(player, current) < 0) {
          num = Math.max(num, current.countCards("h") - player.countCards("h"));
        }
      }
      return Math.ceil((num + 1) / 2) * 2 + 4 - get.value(card);
    },
    async content(event, trigger, player) {
      const target = event.target;
      const num = Math.ceil((target.countCards("h") - player.countCards("h")) / 2);
      if (num) {
        await player.gainPlayerCard({
          target,
          selectButton: num,
          position: "h",
          forced: true,
          visible: true
        });
      }
    },
    ai: {
      order: 4,
      result: {
        target(player, target) {
          return player.countCards("h") - target.countCards("h");
        }
      }
    }
  },
  nslulve: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.hasPlayer((current) => current.countCards("e") > 0 && current.countCards("e") <= player.countCards("he"));
    },
    filterCard() {
      if (ui.selected.targets.length) {
        return false;
      }
      return true;
    },
    position: "he",
    selectCard: [1, Infinity],
    complexSelect: true,
    complexCard: true,
    filterTarget(card, player, target) {
      return target !== player && target.countCards("e") > 0 && ui.selected.cards.length === target.countCards("e");
    },
    check(card) {
      const player = _status.event.player;
      if (game.hasPlayer((current) => current !== player && current.countCards("e") > 0 && ui.selected.cards.length === current.countCards("e") && get.damageEffect(current, player, player) > 0)) {
        return 0;
      }
      switch (ui.selected.cards.length) {
        case 0:
          return 8 - get.value(card);
        case 1:
          return 6 - get.value(card);
        case 2:
          return 3 - get.value(card);
        default:
          return 0;
      }
    },
    async content(event, trigger, player) {
      event.target.damage({ nocard: true });
    },
    ai: {
      damage: true,
      order: 2,
      result: {
        target(player, target) {
          return get.damageEffect(target, player);
        }
      },
      expose: 0.3
    }
  },
  nsfeixiong: {
    trigger: { player: "phaseUseBegin" },
    filter(event, player) {
      return player.hasCards("h") && game.hasPlayer((current) => current !== player && player.canCompare(current));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt2("nsfeixiong"),
        filterTarget(card, player2, target) {
          return player2 !== target && player2.canCompare(target);
        },
        ai(target) {
          const player2 = get.player();
          const hs = player2.getCards("h").sort((a, b) => b.number - a.number);
          const ts = target.getCards("h").sort((a, b) => b.number - a.number);
          if (!hs.length || !ts.length) {
            return 0;
          }
          if (hs[0].number > ts[0].number) {
            return get.damageEffect(target, player2, player2);
          }
          return 0;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      if (get.mode() !== "identity" || player.identity !== "nei") {
        player.addExpose(0.2);
      }
      const compareResult = await player.chooseToCompare(target).forResult();
      if (compareResult.tie) {
        return;
      }
      const damaged = compareResult.bool ? target : player;
      const source = compareResult.bool ? player : target;
      await damaged.damage({ source });
    }
  },
  nscesuan: {
    trigger: { player: "damageBegin3" },
    forced: true,
    async content(event, trigger, player) {
      trigger.cancel();
      const loseEvent = player.loseMaxHp();
      await loseEvent;
      if (loseEvent.loseHp) {
        await player.draw();
      }
    },
    ai: {
      neg: true,
      filterDamage: true,
      skillTagFilter(player, tag, arg) {
        if (tag !== "filterDamage" || !arg?.player) {
          return;
        }
        if (arg.player.hasSkillTag("jueqing", false, player)) {
          return false;
        }
      }
    }
  }
};
const skills$z = {
  //用间篇豪华版盒子甄姬
  yjluoshen: {
    audio: "luoshen",
    trigger: { player: "phaseZhunbeiBegin" },
    frequent: true,
    async content(event, trigger, player) {
      event.cards = [];
      while (true) {
        const judgeEvent = player.judge({
          judge(card) {
            const color = get.color(card);
            const parentEvent = get.event().getParent("yjluoshen");
            if (!parentEvent) {
              return 1;
            }
            if (!parentEvent.color) {
              parentEvent.color = color;
            } else if (parentEvent.color !== color) {
              return -1;
            }
            return 1;
          },
          judge2: (result) => result.bool
        });
        if (get.mode() !== "guozhan" && !player.hasSkillTag("rejudge")) {
          judgeEvent.set("callback", async (event2, trigger2, player2) => {
            if (get.position(event2.card, true) === "o") {
              await player2.gain({
                cards: [event2.card],
                animate: "gain2"
              });
            }
          });
        } else {
          judgeEvent.set("callback", async (event2) => {
            event2.getParent().orderingCards.remove(event2.card);
          });
        }
        const judgeResult = await judgeEvent.forResult();
        if (judgeResult.judge <= 0) {
          event.cards = event.cards.filter((card) => get.position(card, true) === "o");
          if (event.cards.length) {
            await player.gain({ cards: event.cards, animate: "gain2" });
          }
          return;
        }
        event.cards.push(judgeResult.card);
        const continueResult = await player.chooseBool({ prompt: "是否再次发动【洛神】？" }).set("frequentSkill", "yjluoshen").forResult();
        if (!continueResult.bool) {
          if (event.cards.length) {
            await player.gain({ cards: event.cards, animate: "gain2" });
          }
          return;
        }
      }
    }
  },
  //用间篇豪华版盒子贾诩
  yjzhenlve: {
    audio: "zhenlue",
    inherit: "zhenlue",
    async content(event, trigger, player) {
      trigger.directHit.addArray(game.players);
    }
  },
  yjjianshu: {
    audio: "jianshu",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.hasCards("h");
    },
    filterTarget(card, player, target) {
      if (!ui.selected.targets.length) {
        return target !== player;
      }
      const firstTarget = ui.selected.targets[0];
      return target !== player && firstTarget !== target && !firstTarget.hasSkillTag("noCompareSource") && target.hasCards("h") && !target.hasSkillTag("noCompareTarget");
    },
    filterCard: true,
    discard: false,
    lose: false,
    delay: false,
    check(card) {
      if (_status.event.player.hp === 1) {
        return 8 - get.value(card);
      }
      return 6 - get.value(card);
    },
    selectTarget: 2,
    targetprompt: ["发起者", "拼点对象"],
    multitarget: true,
    async content(event, trigger, player) {
      const cards2 = event.cards;
      const firstTarget = event.targets[0];
      const secondTarget = event.targets[1];
      await player.give(cards2, firstTarget, true);
      if (!firstTarget.canCompare(secondTarget)) {
        return;
      }
      const result = await firstTarget.chooseToCompare(secondTarget).forResult();
      if (result.tie) {
        await firstTarget.loseHp();
        await secondTarget.loseHp();
        return;
      }
      const loser = result.bool ? secondTarget : firstTarget;
      await loser.loseHp();
    },
    ai: {
      expose: 0.4,
      order: 4,
      result: {
        target(player, target) {
          if (ui.selected.targets.length) {
            return -1;
          }
          return -0.5;
        }
      }
    }
  },
  yjyongdi: {
    audio: "yongdi",
    limited: true,
    trigger: { player: "phaseZhunbeiBegin" },
    animationColor: "thunder",
    skillAnimation: "legend",
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt2(event.skill),
        filterTarget: (card, player2, target) => target.hasSex("male") || target.name === "key_yuri",
        ai: (target) => {
          if (!_status.event.goon) {
            return 0;
          }
          const currentPlayer = _status.event.player;
          const attitude = get.attitude(currentPlayer, target);
          if (attitude <= 1) {
            return 0;
          }
          const mode = get.mode();
          if (mode === "identity" || mode === "versus" && _status.mode === "four") {
            const hasZhuSkill = target.getStockSkills(true, true).some((skill) => {
              if (target.hasSkill(skill)) {
                return false;
              }
              const info = get.info(skill);
              return info && info.zhuSkill;
            });
            if (hasZhuSkill) {
              return attitude * 2;
            }
          }
          return attitude;
        }
      }).set("goon", !player.hasUnknown(Math.round(game.players.length / 4 - 0.2))).forResult();
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const target = event.targets[0];
      const mode = get.mode();
      if (player !== target && (mode !== "identity" || player.identity !== "nei")) {
        player.addExpose(0.3);
      }
      await target.gainMaxHp({ forced: true });
      await target.recover();
      if (mode === "identity" || mode === "versus" && _status.mode === "four" || mode === "doudizhu") {
        const skills2 = target.getStockSkills(true, true).filter((skill) => {
          if (target.hasSkill(skill)) {
            return false;
          }
          const info = get.info(skill);
          return info && info.zhuSkill;
        });
        if (skills2.length) {
          await target.addSkills(skills2);
        }
      }
    }
  },
  //用间篇豪华版盒子许攸
  yjshicai: {
    audio: "spshicai",
    enable: "phaseUse",
    usable: 1,
    filterCard: true,
    position: "he",
    prompt() {
      const card = _status.pileTop;
      if (get.itemtype(card) === "card") {
        return `弃置一张牌，然后获得${get.translation(card)}`;
      }
      return "弃置一张牌，然后获得牌堆顶的一张牌";
    },
    check(card) {
      const player = _status.event.player;
      const cardx = _status.pileTop;
      if (get.itemtype(cardx) !== "card") {
        return 0;
      }
      const val = player.getUseValue(cardx, null, true);
      if (!val) {
        return 0;
      }
      const val2 = player.getUseValue(card, null, true);
      return (val - val2) / Math.max(0.1, get.value(card));
    },
    group: ["yjshicai_mark"],
    async content(event, trigger, player) {
      const card = get.cards()[0];
      const gainEvent = player.gain({
        cards: [card],
        animate: "gain2",
        gaintag: ["yjshicai_clear"]
      });
      player.addTempSkill("yjshicai_clear", "phaseUseAfter");
      await gainEvent;
    },
    ai: {
      order: 3,
      result: { player: 1 }
    },
    subSkill: {
      mark: {
        trigger: { player: "phaseBegin" },
        silent: true,
        firstDo: true,
        async content(event, trigger, player) {
          player.addTempSkill("spshicai2");
        }
      },
      clear: {
        trigger: {
          player: "loseAfter",
          global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
        },
        onremove(player, skill) {
          player.removeGaintag(skill);
        },
        forced: true,
        charlotte: true,
        popup: false,
        filter(event, player) {
          if (event.name === "lose") {
            return Object.values(event.gaintag_map).some((gaintags) => gaintags.includes("yjshicai_clear"));
          }
          return player.hasHistory("lose", (evt) => {
            if (evt.getParent() !== event) {
              return false;
            }
            return Object.values(evt.gaintag_map).some((gaintags) => gaintags.includes("yjshicai_clear"));
          });
        },
        async content(event, trigger, player) {
          delete player.getStat("skill").yjshicai;
        }
      }
    }
  },
  yjchenggong: {
    audio: "chenggong",
    trigger: {
      global: "useCardToPlayered"
    },
    filter(event, player) {
      return event.isFirstTarget && event.targets.length > 1 && event.player.isIn();
    },
    check(event, player) {
      return get.attitude(player, event.player) > 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await trigger.player.draw();
    },
    ai: { expose: 0.2 }
  },
  yjzezhu: {
    audio: "zezhu",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      const zhu = get.zhu(player);
      if (!zhu) {
        return false;
      }
      return zhu.hasGainableCards(player, zhu === player ? "ej" : "hej");
    },
    filterTarget(card, player, target) {
      const zhu = get.zhu(player);
      return target === zhu;
    },
    selectTarget: 1,
    async content(event, trigger, player) {
      const target = event.target;
      await player.gainPlayerCard({
        target,
        position: player === target ? "ej" : "hej",
        forced: true
      });
      if (!player.hasCards("he") || player === target) {
        return;
      }
      const result = await player.chooseCard({
        prompt: `择主：交给${get.translation(target)}一张牌`,
        position: "he",
        forced: true
      }).forResult();
      await player.give(result.cards, target);
    },
    ai: {
      order: 2.9,
      result: { player: 1 }
    }
  },
  //用间beta董卓
  yjtuicheng: {
    enable: "phaseUse",
    viewAs: { name: "tuixinzhifu", isCard: true },
    filterCard: () => false,
    selectCard: -1,
    log: false,
    async precontent(event, trigger, player) {
      player.logSkill("yjtuicheng");
      await player.loseHp();
    },
    ai: {
      effect: {
        player(card, player) {
          if (get.name(card) !== "tuixinzhifu" || _status.event.skill !== "yjtuicheng") {
            return;
          }
          if (player.hp < 3) {
            return "zeroplayertarget";
          }
          if (player.hasSkill("yjshicha") && !player.hasHistory("useSkill", (evt) => evt.skill === "yjtuicheng")) {
            return [1, 2];
          }
          return "zeroplayertarget";
        }
      }
    }
  },
  yjyaoling: {
    trigger: {
      player: "phaseUseEnd"
    },
    async cost(event, trigger, player) {
      const check = player.maxHp >= 2 && (player.hasSkill("yjshicha") && !player.hasHistory("useSkill", (evt) => evt.skill === "yjtuicheng") || player.maxHp > 2 && player.getDamagedHp() > 1);
      event.result = await player.chooseTarget({
        prompt: get.prompt("yjyaoling"),
        prompt2: "减1点体力上限，选择一名其他角色A和一名角色B，令A选择对B使用杀或被你弃牌",
        selectTarget: 2,
        filterTarget: (card, player2, target) => {
          if (!ui.selected.targets.length) {
            return target !== player2;
          }
          return ui.selected.targets[0].canUse("sha", target, false);
        },
        ai: (target) => {
          if (!get.event().check) {
            return -1;
          }
          const currentPlayer = _status.event.player;
          if (!ui.selected.targets.length) {
            return get.effect(target, { name: "guohe_copy2" }, currentPlayer, currentPlayer);
          }
          const firstTarget = ui.selected.targets[0];
          return get.effect(target, { name: "sha" }, firstTarget, currentPlayer) + 5;
        }
      }).set("targetprompt", ["打人", "被打"]).set("complexSelect", true).set("check", check).forResult();
    },
    async content(event, trigger, player) {
      const targets = event.targets;
      const firstTarget = targets[0];
      const secondTarget = targets[1];
      player.line2(targets);
      await player.loseMaxHp();
      const useResult = await firstTarget.chooseToUse({
        prompt: `耀令：对${get.translation(secondTarget)}使用一张杀，或令${get.translation(player)}弃置你的一张牌`,
        filterCard: (card, player2, event2) => {
          if (get.name(card) !== "sha") {
            return false;
          }
          return lib.filter.filterCard(card, player2, event2);
        },
        complexTarget: true,
        filterTarget: (card, player2, target) => {
          const source = _status.event.sourcex;
          if (target !== source && !ui.selected.targets.includes(source)) {
            return false;
          }
          return lib.filter.filterTarget(card, player2, target);
        }
      }).set("targetRequired", true).set("sourcex", secondTarget).forResult();
      if (!useResult.bool && firstTarget.countDiscardableCards(player, "he")) {
        await player.discardPlayerCard({
          target: firstTarget,
          position: "he",
          forced: true
        });
      }
    }
  },
  yjshicha: {
    trigger: { player: "phaseDiscardBegin" },
    forced: true,
    filter(event, player) {
      return !player.hasHistory("useSkill", (evt) => evt.skill === "yjtuicheng" || evt.skill === "yjyaoling");
    },
    async content(event, trigger, player) {
      player.addTempSkill("yjshicha_limit");
    },
    subSkill: {
      limit: {
        charlotte: true,
        mark: true,
        intro: { content: "本回合手牌上限为1" },
        mod: {
          maxHandcard: () => 1
        }
      }
    },
    ai: {
      neg: true
    }
  },
  yjyongquan: {
    trigger: { player: "phaseJieshuBegin" },
    zhuSkill: true,
    filter(event, player) {
      return player.hasZhuSkill("yjyongquan") && game.hasPlayer((current) => current !== player && player.hasZhuSkill(current) && current.group === "qun");
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => current !== player && player.hasZhuSkill(current) && current.group === "qun");
    },
    async content(event, trigger, player) {
      const targets = lib.skill.yjyongquan.logTarget(trigger, player);
      for (const target of targets) {
        const result = await target.chooseCard({
          prompt: `拥权：是否交给${get.translation(player)}一张牌？`,
          position: "he",
          ai: (card) => {
            if (_status.event.goon) {
              return 4.5 - get.value(card);
            }
            return 0;
          }
        }).set("goon", get.attitude(target, player) > 3).forResult();
        if (!result.bool) {
          continue;
        }
        target.line(player);
        await target.give(result.cards, player);
      }
    }
  },
  //用间beta甘宁的新版
  yjjielve: {
    enable: "phaseUse",
    viewAs: { name: "chenghuodajie" },
    filterCard(card, player) {
      if (ui.selected.cards.length) {
        return get.color(card) === get.color(ui.selected.cards[0]);
      }
      const cards2 = player.getCards("hes");
      return cards2.some((cardx) => card !== cardx && get.color(card) === get.color(cardx));
    },
    position: "hes",
    selectCard: 2,
    complexCard: true,
    check(card) {
      return 5 - get.value(card);
    },
    onuse(links, player) {
      player.addTempSkill("yjjielve_check");
    },
    subSkill: {
      check: {
        trigger: { source: "damageSource" },
        forced: true,
        charlotte: true,
        popup: false,
        filter(event, player) {
          return event.card && event.card.name === "chenghuodajie" && event.getParent().skill === "yjjielve";
        },
        async content(event, trigger, player) {
          player.tempBanSkill("yjjielve");
        }
      }
    }
  },
  //用间beta张飞
  yjmangji: {
    trigger: {
      player: ["loseAfter", "changeHpAfter"],
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    filter(event, player) {
      if (player.hp < 1 || !player.countDiscardableCards(player, "h")) {
        return false;
      }
      if (event.name === "changeHp") {
        return event.changedHp !== 0;
      }
      const evt = event.getl(player);
      if (event.name === "equip" && event.player === player) {
        return !evt || evt.cards.length !== 1;
      }
      if (!evt?.es.length) {
        return false;
      }
      return game.hasPlayer((current) => player.canUse({ name: "sha", isCard: true }, current, false));
    },
    direct: true,
    forced: true,
    async content(event, trigger, player) {
      if (!player.countDiscardableCards(player, "h") || !game.hasPlayer((current) => player.canUse({ name: "sha", isCard: true }, current, false))) {
        return;
      }
      const result = await player.chooseCardTarget({
        prompt: "莽击：弃置一张手牌，视为对一名其他角色使用一张【杀】",
        forced: true,
        filterCard: lib.filter.cardDiscardable,
        filterTarget(card, player2, target2) {
          return player2.canUse({ name: "sha", isCard: true }, target2, false);
        },
        ai2(target2) {
          return get.effect(target2, { name: "sha" }, _status.event.player);
        }
      }).forResult();
      if (!result?.bool) {
        return;
      }
      const target = result.targets[0];
      const cards2 = result.cards;
      player.logSkill(event.name, target);
      await player.discard({ cards: cards2 });
      if (player.canUse({ name: "sha", isCard: true }, target, false)) {
        await player.useCard({
          card: { name: "sha", isCard: true },
          targets: [target],
          addCount: false
        });
      }
    }
  },
  //用间beta曹洪
  yjlifeng: {
    enable: "phaseUse",
    usable: 1,
    locked: false,
    filter(event, player) {
      for (const card of ui.discardPile.childNodes) {
        if (get.type(card) === "equip") {
          return true;
        }
      }
      return false;
    },
    async content(event, trigger, player) {
      const cards2 = Array.from(ui.discardPile.childNodes).filter((card2) => get.type(card2) === "equip");
      const result = await player.chooseButton({
        createDialog: ["厉锋：获得一张装备牌", cards2],
        forced: cards2.length > 0,
        ai: get.buttonValue
      }).forResult();
      if (!result.bool) {
        return;
      }
      const card = result.links[0];
      await player.gain({
        cards: [card],
        animate: "gain2"
      });
    },
    ai: {
      order: 10,
      result: { player: 1 },
      effect: {
        target(card, player, target) {
          if (card && get.type(card) === "equip" && _status.event.skill === "_gifting") {
            return 0;
          }
        }
      }
    },
    mod: {
      cardGiftable(card, player) {
        return get.type(card) === "equip";
      }
    }
  },
  //用间篇李儒
  yjdumou: {
    forced: true,
    mod: {
      cardname(card, player, name) {
        if (player === _status.currentPhase && card.name === "du") {
          return "guohe";
        }
      },
      aiValue(player, card, num) {
        if (card.name === "du") {
          return get.value({ name: "guohe" });
        }
      }
    },
    init: () => {
      game.addGlobalSkill("yjdumou_du");
      game.addGlobalSkill("g_du");
    },
    onremove: () => {
      if (!game.hasPlayer((i) => i.hasSkill("yjdumou", null, null, false), true)) {
        game.removeGlobalSkill("yjdumou_du");
      }
    },
    subSkill: {
      du: {
        mod: {
          cardname(card, player, name) {
            if (_status.currentPhase && player !== _status.currentPhase && _status.currentPhase.hasSkill("yjdumou") && get.color(card) === "black") {
              return "du";
            }
          },
          aiValue(player, card, num) {
            if (get.name(card) === "du" && card.name !== "du") {
              return get.value({ name: card.name });
            }
          }
        },
        trigger: { player: "dieAfter" },
        filter: () => !game.hasPlayer((current) => current.hasSkill("yjdumou", null, null, false), true),
        silent: true,
        forceDie: true,
        content: async () => {
          game.removeGlobalSkill("yjdumou_du");
        }
      }
    },
    ai: { threaten: 2.1 }
  },
  yjweiquan: {
    enable: "phaseUse",
    skillAnimation: true,
    animationColor: "soil",
    filterTarget: true,
    limited: true,
    selectTarget: () => [1, game.roundNumber],
    async contentBefore(event, trigger, player) {
      const targets = event.targets;
      player.awakenSkill("yjweiquan");
      const result = await player.chooseTarget({
        prompt: "威权：选择获得牌的角色",
        forced: true,
        ai: (target) => {
          const attitude = get.attitude(_status.event.player, target);
          const num = target.needsToDiscard(targets.filter((current) => current !== target && current.countCards("h")).length);
          if (attitude > 0 && num <= 2) {
            return 0;
          }
          if (attitude < 0 && target.needsToDiscard(-5)) {
            return -attitude - Math.sqrt(num);
          }
          return attitude - Math.sqrt(num);
        }
      }).forResult();
      event.getParent()._yjweiquan = result.targets[0];
    },
    async content(event, trigger, player) {
      const target = event.target;
      const recipient = event.getParent()._yjweiquan;
      if (target === recipient || !target.countCards("h")) {
        return;
      }
      const result = await target.chooseCard({
        prompt: `威权：将一张手牌交给${get.translation(recipient)}`,
        forced: true
      }).forResult();
      if (!result.bool) {
        return;
      }
      await target.give(result.cards, recipient);
    },
    async contentAfter(event, trigger, player) {
      const recipient = event.getParent()._yjweiquan;
      if (recipient.countCards("h") <= recipient.hp) {
        return;
      }
      const next = recipient.phase();
      event.next.remove(next);
      event.getParent().after.push(next);
      next.player = recipient;
      next._noTurnOver = true;
      next._triggered = null;
      next.setContent(async (event2, trigger2, player2) => {
        game.broadcastAll(() => {
          if (ui.tempnowuxie) {
            ui.tempnowuxie.close();
            delete ui.tempnowuxie;
          }
        });
        player2.phaseDiscard();
        if (!player2.noPhaseDelay) {
          game.delayx();
        }
        delete player2._noSkill;
      });
    },
    ai: {
      order: 6,
      result: {
        player(player) {
          const num = game.countPlayer((current) => get.attitude(player, current) < 0 && current.countCards("h"));
          if (game.roundNumber < num && player.hp > 2 || !game.hasPlayer(
            (current) => get.attitude(player, current) > 0 && current.needsToDiscard(num) < 2 || get.attitude(player, current) < 0 && current.needsToDiscard(-5)
          )) {
            return -10;
          }
          return 1;
        },
        target: -1
      }
    }
  },
  yjrenwang: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      for (const card of ui.discardPile.childNodes) {
        if (get.color(card) === "black" && get.type(card) === "basic") {
          return true;
        }
      }
      return false;
    },
    async content(event, trigger, player) {
      const cards2 = Array.from(ui.discardPile.childNodes).filter((card2) => get.color(card2) === "black" && get.type(card2) === "basic");
      const cardResult = await player.chooseButton({
        createDialog: ["人望：选择一张黑色基本牌", cards2],
        forced: cards2.length > 0,
        ai: get.buttonValue
      }).forResult();
      if (!cardResult.bool) {
        return;
      }
      const card = cardResult.links[0];
      const targetResult = await player.chooseTarget({
        prompt: `选择一名角色获得${get.translation(card)}`,
        forced: true,
        ai: (target2) => get.attitude(_status.event.player, target2)
      }).forResult();
      if (!targetResult.bool) {
        return;
      }
      const target = targetResult.targets[0];
      player.line(target);
      await target.gain({
        cards: [card],
        animate: "gain2"
      });
    },
    ai: {
      order: 10,
      result: { player: 1 }
    }
  },
  //群曹操
  yjxiandao: {
    trigger: { player: "giftAccepted" },
    usable: 1,
    forced: true,
    locked: false,
    filter: (event, player) => event.target !== player && event.target.isIn(),
    logTarget: "target",
    async content(event, trigger, player) {
      const target = trigger.target;
      const card = trigger.card;
      target.markAuto("yjxiandao_block", [get.suit(card, false)]);
      target.addTempSkill("yjxiandao_block");
      const type = get.type(card);
      if (type === "trick") {
        await player.draw(2);
        return;
      }
      if (type !== "equip") {
        return;
      }
      if (target.hasGainableCards(player, "he", (cardx) => cardx !== card)) {
        await player.gainPlayerCard({
          target,
          position: "he",
          forced: true,
          filterButton: (button) => button.link !== _status.event.card
        }).set("card", card);
      }
      if (get.subtype(card, false) === "equip1") {
        await target.damage();
      }
    },
    subSkill: {
      block: {
        charlotte: true,
        onremove: true,
        mod: {
          cardEnabled(card, player) {
            if (player.getStorage("yjxiandao_block").includes(get.suit(card))) {
              return false;
            }
          },
          cardRespondable(card, player) {
            if (player.getStorage("yjxiandao_block").includes(get.suit(card))) {
              return false;
            }
          },
          cardSavable(card, player) {
            if (player.getStorage("yjxiandao_block").includes(get.suit(card))) {
              return false;
            }
          }
        },
        mark: true,
        intro: { content: "不能使用或打出$牌" }
      }
    }
  },
  yjsancai: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.hasCards("h");
    },
    async content(event, trigger, player) {
      await player.showHandcards();
      const hs = player.getCards("h");
      if (hs.length > 1) {
        const type = get.type2(hs[0], player);
        for (const card of hs.slice(1)) {
          if (get.type(card) !== type) {
            return;
          }
        }
      }
      const result = await player.chooseCardTarget({
        prompt: "是否赠予一张手牌？",
        filterCard: true,
        filterTarget: lib.filter.notMe
      }).forResult();
      if (!result.bool) {
        return;
      }
      const target = result.targets[0];
      player.line(target, "green");
      await player.gift(result.cards, target);
    },
    ai: {
      combo: "yixiandao"
    }
  },
  yjyibing: {
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    direct: true,
    filter(event, player) {
      if (event.getParent().name === "gift") {
        return false;
      }
      if (event.getParent("yjyibing").player === player) {
        return false;
      }
      const phaseDrawEvent = event.getParent("phaseDraw");
      const handCards = player.getCards("h");
      const cards2 = event.getg(player);
      return cards2.length > 0 && (!phaseDrawEvent || phaseDrawEvent.player !== player) && cards2.every((card) => handCards.includes(card) && game.checkMod(card, player, "unchanged", "cardEnabled2", player) !== false) && player.hasUseTarget(
        {
          name: "sha",
          cards: event.cards
        },
        false
      );
    },
    async content(event, trigger, player) {
      const cards2 = trigger.getg(player);
      await player.chooseUseTarget({
        prompt: get.prompt("yjyibing"),
        prompt2: `将${get.translation(cards2)}当做【杀】使用`,
        card: { name: "sha" },
        cards: cards2,
        addCount: false,
        nodistance: true
      }).set("logSkill", "yjyibing");
    }
  },
  //用间篇
  yjxuepin: {
    enable: "phaseUse",
    usable: 1,
    filterTarget(event, player, target) {
      return player.inRange(target) && target.hasDiscardableCards(player, "he");
    },
    async content(event, trigger, player) {
      const target = event.target;
      await player.loseHp();
      if (!target.hasDiscardableCards(player, "he")) {
        return;
      }
      const result = await player.discardPlayerCard({
        target,
        selectButton: 2,
        position: "he",
        forced: true
      }).forResult();
      if (!result.bool || result.cards?.length !== 2) {
        return;
      }
      const firstCard = result.cards[0];
      const secondCard = result.cards[1];
      const firstType = get.type2(firstCard, firstCard.original === "h" ? target : false);
      const secondType = get.type2(secondCard, secondCard.original === "h" ? target : false);
      if (firstType !== secondType) {
        return;
      }
      await player.recover();
    },
    ai: {
      order: 4,
      result: {
        player(player, target) {
          if (player.hp === 1) {
            return -8;
          }
          if (target.countCards("e") > 1) {
            return 0;
          }
          if (player.hp > 2 || target.countCards("h") > 1) {
            return -0.5;
          }
          return -2;
        },
        target(player, target) {
          if (target.countDiscardableCards(player, "he") < 2) {
            return 0;
          }
          return -2;
        }
      }
    }
  },
  nsjianglie: {
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      return event.card.name === "sha" && event.target.hasCards("h");
    },
    check(event, player) {
      return get.attitude(player, event.target) < 0;
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const target = trigger.target;
      await target.showHandcards();
      const cards2 = target.getCards("h");
      const colors = [...new Set(cards2.map((card) => get.color(card)))];
      let color;
      if (colors.length === 1) {
        color = colors[0];
      } else {
        colors.sort();
        const result = await target.chooseControl({
          controls: colors,
          prompt: "选择弃置一种颜色的所有手牌",
          ai: (event2, player2) => get.value(player2.getCards("h", { color: "red" })) >= get.value(player2.getCards("h", { color: "black" })) ? "black" : "red"
        }).forResult();
        color = result.control;
      }
      await target.discard({ cards: target.getCards("h", { color }) });
    }
  }
};
const skills$y = {
  //战役篇田丰
  gzsuishi: {
    audio: "suishi",
    preHidden: ["gzsuishi2"],
    trigger: { global: "dying" },
    forced: true,
    logAudio: () => 1,
    filter(event, player) {
      return event.player !== player && event.parent.name === "damage" && event.parent.source && event.parent.source.group === player.group;
    },
    async content(event, trigger, player) {
      player.draw();
    },
    ai: {
      halfneg: true
    },
    group: "gzsuishi2"
  },
  gzsuishi2: {
    audio: "suishi",
    trigger: { global: "dieAfter" },
    forced: true,
    sourceSkill: "gzsuishi",
    logAudio: () => 2,
    filter(event, player) {
      return event.player.group === player.group;
    },
    async content(event, trigger, player) {
      player.loseHp();
    }
  },
  //战役篇孔融
  zymingshi: {
    audio: "mingshi",
    forced: true,
    trigger: { target: "useCardToBefore" },
    priority: 15,
    filter(event, player) {
      if (!player.hasEmptySlot(2)) {
        return false;
      }
      if (event.card.name !== "sha") {
        return false;
      }
      return game.hasNature(event.card);
    },
    async content(event, trigger, player) {
      trigger.cancel();
    },
    ai: {
      effect: {
        target(card, player, target) {
          if (card.name === "sha" && game.hasNature(card) && target.hasEmptySlot(2)) {
            return "zeroplayertarget";
          }
          if (get.subtype(card) === "equip2" && target.isEmpty(2)) {
            return [0.6, -0.8];
          }
        }
      }
    }
  },
  //战役篇蒋钦
  zyshangyi: {
    audio: "shangyi",
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return player !== target;
    },
    async content(event, trigger, player) {
      const { target } = event;
      await target.viewHandcards(player);
      if (!target.hasCards("h")) {
        return;
      }
      const result = await player.chooseCardButton({
        cards: target.getCards("h")
      }).forResult();
      if (!result.bool || !result.links?.length) {
        return;
      }
      await target.discard(result.links[0]);
    },
    ai: {
      order: 11,
      result: {
        target(player, target) {
          return -target.countCards("h");
        }
      },
      threaten: 1.1
    }
  },
  //战役篇国战将转身份
  //钟会
  zyquanji: {
    audio: "gzquanji",
    trigger: {
      player: "damageEnd",
      source: "damageSource"
    },
    frequent: true,
    filter(event, player, name) {
      if (name === "damageEnd") {
        return true;
      }
      const evt = event.getParent();
      if (evt.player !== player) {
        return false;
      }
      return evt.card && evt.type === "card" && evt.targets.length === 1;
    },
    async content(event, trigger, player) {
      await player.draw();
      const hs = player.getCards("he");
      if (!hs.length) {
        return;
      }
      const result = hs.length === 1 ? { bool: true, cards: hs } : await player.chooseCard("he", true, "选择一张牌作为“权”").forResult();
      if (result?.bool && result?.cards?.length) {
        const next = player.addToExpansion(result.cards, player, "give");
        next.gaintag.add(event.name);
        await next;
      }
    },
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    locked: false,
    mod: {
      maxHandcard(player, num) {
        return num + player.getExpansions("zyquanji").length;
      }
    },
    ai: { notemp: true }
  },
  zypaiyi: {
    audio: "gzpaiyi",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.getExpansions("zyquanji").length > 0;
    },
    chooseButton: {
      dialog(event, player) {
        return ui.create.dialog("排异", player.getExpansions("zyquanji"), "hidden");
      },
      backup(links, player) {
        return {
          audio: "gzpaiyi",
          filterTarget: true,
          filterCard() {
            return false;
          },
          selectCard: -1,
          card: links[0],
          delay: false,
          content: lib.skill.zypaiyi.contentx,
          ai: {
            order: 10,
            result: {
              target(player2, target) {
                if (target !== player2) {
                  return 0;
                }
                if (player2.getExpansions("zyquanji").length <= 1 || player2.needsToDiscard() && !player2.getEquip("zhuge") && !player2.hasSkill("new_paoxiao")) {
                  return 0;
                }
                return 1;
              }
            }
          }
        };
      },
      prompt() {
        return "请选择【排异】的目标";
      }
    },
    async contentx(event, trigger, player) {
      const { target } = event;
      const card = lib.skill.zypaiyi_backup.card;
      await player.loseToDiscardpile(card);
      const num = player.getExpansions("zyquanji").length;
      if (num > 0) {
        await target.draw(Math.min(7, num));
      }
      if (target.countCards("h") > player.countCards("h")) {
        await target.damage();
      }
    },
    ai: {
      order(item, player) {
        const num = player.getExpansions("zyquanji").length;
        if (num === 1) {
          return 8;
        }
        return 1;
      },
      result: {
        player: 1
      },
      combo: "zyquanji"
    }
  },
  //孙綝
  zyshilu: {
    init() {
      if (!_status.characterlist) {
        game.initCharacterList();
      }
    },
    audio: 2,
    preHidden: true,
    trigger: { global: "dieAfter" },
    prompt2(event, player) {
      return `将其的所有武将牌${player === event.source ? "及武将牌库里的一张随机武将牌" : ""}置于武将牌上作为“戮”`;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const list = [];
      const target = trigger.player;
      if (target.name1 && !target.isUnseen(0) && target.name1.indexOf("gz_shibing") !== 0 && _status.characterlist.includes(target.name1)) {
        list.push(target.name1);
      }
      if (target.name2 && !target.isUnseen(1) && target.name2.indexOf("gz_shibing") !== 0 && _status.characterlist.includes(target.name1)) {
        list.push(target.name2);
      }
      _status.characterlist.removeArray(list);
      if (player === trigger.source) {
        list.addArray(_status.characterlist.randomRemove(1));
      }
      if (list.length) {
        player.markAuto("zyshilu", list);
        game.log(player, "将", `#g${get.translation(list)}`, "置于武将牌上作为", "#y“戮”");
        game.broadcastAll(
          (player2, list2) => {
            const cards2 = [];
            for (const name of list2) {
              const cardname = `huashen_card_${name}`;
              lib.card[cardname] = {
                fullimage: true,
                image: `character:${name}`
              };
              lib.translate[cardname] = get.rawName2(name);
              cards2.push(game.createCard(cardname, "", ""));
            }
            player2.$draw(cards2, "nobroadcast");
          },
          player,
          list
        );
      }
    },
    marktext: "戮",
    intro: {
      content: "character",
      onunmark(storage, player) {
        if (storage && storage.length) {
          _status.characterlist.addArray(storage);
          storage.length = 0;
        }
      },
      mark(dialog, storage, player) {
        if (storage && storage.length) {
          dialog.addSmall([storage, "character"]);
        } else {
          return "没有“戮”";
        }
      }
      // content: (storage, player) => `共有${get.cnNumber(storage.length)}张“戮”`,
    },
    group: "zyshilu_zhiheng",
    subSkill: {
      zhiheng: {
        audio: "zyshilu",
        trigger: { player: "phaseZhunbeiBegin" },
        filter(event, player) {
          return player.getStorage("zyshilu").length > 0 && player.countCards("he") > 0;
        },
        direct: true,
        async content(event, trigger, player) {
          const num = Math.min(player.getStorage("zyshilu").length, player.countCards("he"));
          const next = player.chooseToDiscard("he", get.prompt("zyshilu"), `弃置至多${get.cnNumber(num)}张牌并摸等量的牌`, [1, num], "allowChooseAll");
          next.logSkill = "zyshilu_zhiheng";
          const result = await next.forResult();
          if (!result.bool || !result.cards?.length) {
            return;
          }
          await player.draw(result.cards.length);
        }
      }
    }
  },
  zyxiongnve: {
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    direct: true,
    filter(event, player) {
      return player.getStorage("zyshilu").length > 0;
    },
    async content(event, trigger, player) {
      const characterResult = await player.chooseButton({
        createDialog: [get.prompt("zyxiongnve"), [player.storage.zyshilu, "character"]],
        ai() {
          return get.event().goon ? 1 : 0;
        }
      }).set("goon", player.countCards("hs", (card) => get.tag(card, "damage") && player.hasValueTarget(card)) > 1).forResult();
      if (!characterResult.bool) {
        return;
      }
      player.logSkill("zyxiongnve");
      lib.skill.zyxiongnve.throwCharacter(player, characterResult.links);
      await game.delayx();
      const controlResult = await player.chooseControl({
        prompt: "选择获得一项效果",
        choiceList: ["本回合造成的伤害+1", "本回合造成伤害时，获得其一张牌", "本回合使用牌没有次数限制"],
        ai() {
          const player2 = get.player();
          if (player2.countCards("hs", (card) => get.name(card) === "sha" && player2.hasValueTarget(card)) > player2.getCardUsable("sha")) {
            return 0;
          }
          return get.rand(1, 2);
        }
      }).forResult();
      const skill = `zyxiongnve_effect${controlResult.index}`;
      player.addTempSkill(skill);
      game.log(player, "本回合", `#g${lib.skill[skill].promptx}`);
    },
    group: "zyxiongnve_end",
    throwCharacter(player, list) {
      player.unmarkAuto("zyshilu", list);
      _status.characterlist.addArray(list);
      game.log(player, "从", "#y“戮”", "中移去了", `#g${get.translation(list)}`);
      game.broadcastAll(
        (player2, list2) => {
          const cards2 = [];
          for (const name of list2) {
            const cardname = `huashen_card_${name}`;
            lib.card[cardname] = {
              fullimage: true,
              image: `character:${name}`
            };
            lib.translate[cardname] = get.rawName2(name);
            cards2.push(game.createCard(cardname, "", ""));
          }
          player2.$throw(cards2, 1e3, "nobroadcast");
        },
        player,
        list
      );
    },
    subSkill: {
      effect0: {
        promptx: "造成的伤害+1",
        charlotte: true,
        onremove: true,
        audio: "zyxiongnve",
        intro: {
          content: "当你造成伤害时，此伤害+1"
        },
        trigger: { source: "damageBegin1" },
        forced: true,
        logTarget: "player",
        async content(event, trigger, player) {
          trigger.num++;
        }
      },
      effect1: {
        promptx: "造成伤害后，获得其一张牌",
        charlotte: true,
        onremove: true,
        audio: "zyxiongnve",
        intro: {
          content: "对其他角色造成伤害时，获得其一张牌"
        },
        trigger: { source: "damageBegin1" },
        forced: true,
        filter(event, player) {
          return player !== event.player && event.player.countGainableCards(player, "he") > 0;
        },
        logTarget: "player",
        async content(event, trigger, player) {
          await player.gainPlayerCard({
            target: trigger.player,
            position: "he",
            forced: true
          });
        }
      },
      effect2: {
        promptx: "使用牌没有次数限制",
        charlotte: true,
        onremove: true,
        intro: {
          content: "使用牌没有次数限制"
        },
        mod: {
          cardUsable: () => Infinity
        }
      },
      effect3: {
        charlotte: true,
        audio: "zyxiongnve",
        mark: true,
        intro: {
          content: "受到的伤害-1"
        },
        trigger: { player: "damageBegin4" },
        forced: true,
        filter(event, player) {
          return event.source && event.source !== player && event.source.isIn();
        },
        async content(event, trigger, player) {
          trigger.num--;
        },
        ai: {
          effect: {
            target(card, player, target) {
              if (player.hasSkillTag("jueqing", false, target)) {
                return;
              }
              const num = get.tag(card, "damage");
              if (num) {
                if (num > 1) {
                  return 0.5;
                }
                return 0;
              }
            }
          }
        }
      },
      end: {
        trigger: { player: "phaseUseEnd" },
        direct: true,
        filter(event, player) {
          return player.getStorage("zyshilu").length > 1;
        },
        async content(event, trigger, player) {
          const result = await player.chooseButton({
            createDialog: ["凶虐：是否移去两张“戮”获得减伤？", [player.storage.zyshilu, "character"]],
            selectButton: 2,
            ai() {
              const player2 = get.player();
              if (game.countPlayer() * 1.5 + player2.storage.zyshilu.length / 2 > 8) {
                return 1;
              }
              if (player2.hp <= 2) {
                return 1;
              }
              return 0;
            }
          }).forResult();
          if (!result.bool) {
            return;
          }
          player.logSkill("zyxiongnve");
          lib.skill.zyxiongnve.throwCharacter(player, result.links);
          player.addTempSkill("zyxiongnve_effect3", { player: "phaseBegin" });
          await game.delayx();
        }
      }
    },
    ai: {
      combo: "zyshilu"
    }
  },
  //孟达
  qiuan: {
    audio: 2,
    trigger: { player: "damageBegin4" },
    filter(event, player) {
      return event.cards && event.cards.filterInD().length > 0 && !player.getExpansions("qiuan").length;
    },
    check(event, player) {
      if (get.damageEffect(player, event.source || player, player, event.nature) >= 0) {
        return false;
      }
      return true;
    },
    preHidden: true,
    async content(event, trigger, player) {
      const cards2 = trigger.cards.filterInD();
      await player.addToExpansion({
        cards: cards2,
        animate: "gain2",
        gaintag: ["qiuan"]
      });
      trigger.cancel();
    },
    ai: {
      combo: "liangfan"
    },
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    marktext: "函"
  },
  liangfan: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    filter(event, player) {
      return player.getExpansions("qiuan").length > 0;
    },
    async content(event, trigger, player) {
      const cards2 = player.getExpansions("qiuan");
      const next = player.gain({
        cards: cards2,
        animate: "gain2",
        gaintag: ["liangfan"]
      });
      player.addTempSkill("liangfan2");
      await next;
      await player.loseHp();
    },
    ai: {
      combo: "qiuan"
    }
  },
  liangfan2: {
    audio: "liangfan",
    mark: true,
    mod: {
      aiOrder(player, card, num) {
        if (get.itemtype(card) === "card" && card.hasGaintag("liangfan")) {
          return num + 0.1;
        }
      }
    },
    intro: { content: "使用“量反”牌造成伤害后，可获得目标角色的一张牌" },
    trigger: { source: "damageEnd" },
    logTarget: "player",
    charlotte: true,
    sourceSkill: "liangfan",
    onremove(player) {
      player.removeGaintag("liangfan");
    },
    prompt: (event) => `量反：是否获得${get.translation(event.player)}的一张牌？`,
    filter(event, player) {
      const evt = event.getParent(2);
      if (evt.name !== "useCard" || evt.card !== event.card) {
        return false;
      }
      if (!event.player.countGainableCards(player, "he")) {
        return false;
      }
      return player.getHistory("lose", (evt2) => {
        if (evt2.getParent() !== evt) {
          return false;
        }
        for (const i in evt2.gaintag_map) {
          if (evt2.gaintag_map[i].includes("liangfan")) {
            return true;
          }
        }
        return false;
      }).length > 0;
    },
    marktext: "反",
    async content(event, trigger, player) {
      await player.gainPlayerCard({
        target: trigger.player,
        position: "he",
        forced: true
      });
    }
  },
  //文钦
  gzjinfa: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.hasCards("he") && game.hasPlayer((current) => current !== player && current.hasCards("he"));
    },
    filterCard: true,
    position: "he",
    filterTarget(card, player, target) {
      return target !== player && target.hasCards("he");
    },
    check(card) {
      return 6 - get.value(card);
    },
    async content(event, trigger, player) {
      const { target } = event;
      const result = await target.chooseCard({
        prompt: `交给${get.translation(player)}一张装备牌，或令其获得你的一张牌`,
        filterCard(card) {
          return get.type(card) === "equip";
        },
        position: "he",
        ai(card) {
          if (_status.event.goon && get.suit(card) === "spade") {
            return 8 - get.value(card);
          }
          return 5 - get.value(card);
        }
      }).set("goon", target.canUse("sha", player, false) && get.effect(player, { name: "sha" }, target, target) > 0).forResult();
      if (!result.bool || !result.cards?.length) {
        await player.gainPlayerCard({
          target,
          position: "he",
          forced: true
        });
        return;
      }
      const result2 = await target.give(result.cards, player).forResult();
      if (result2.bool && result2.cards && result2.cards.length && target.isIn() && player.isIn() && get.suit(result2.cards[0], target) === "spade" && target.canUse("sha", player, false)) {
        await target.useCard({
          card: get.autoViewAs({ name: "sha", isCard: true }),
          targets: [player],
          addCount: false
        });
      }
    },
    ai: {
      order: 6,
      result: {
        player(player, target) {
          if (target.countCards("e", (card) => get.suit(card) === "spade" && get.value(card) < 8) && target.canUse("sha", player, false)) {
            return get.effect(player, { name: "sha" }, target, player);
          }
          return 0;
        },
        target(player, target) {
          const es = target.getCards("e").sort((a, b) => get.value(b, target) - get.value(a, target));
          if (es.length) {
            return -Math.min(2, get.value(es[0]));
          }
          return -2;
        }
      }
    }
  },
  //战役篇改王允
  zylianji: {
    audio: "wylianji",
    trigger: { player: "phaseUseEnd" },
    filter(event, player) {
      return player.hasHistory("useCard", (evt) => evt.getParent("phaseUse") === event);
    },
    direct: true,
    async content(event, trigger, player) {
      let logged = false;
      const num = player.getHistory("useCard", (evt) => evt.getParent("phaseUse") === trigger).map((evt) => get.type2(evt.card)).unique().length;
      if (num > 0) {
        const result = await player.chooseTarget(get.prompt("zylianji"), "令一名角色摸一张牌").set("ai", (target) => {
          const player2 = get.player();
          if (target === player2 && player2.needsToDiscard(1)) {
            return 1;
          }
          return get.effect(target, { name: "draw" }, player2, player2);
        }).forResult();
        if (result?.targets?.length) {
          const target = result.targets[0];
          if (!logged) {
            logged = true;
            player.logSkill("zylianji", target);
          }
          await result.targets[0].draw();
        }
      }
      if (num > 1 && player.isDamaged()) {
        const result = await player.chooseBool(get.prompt("zylianji"), "回复1点体力").forResult();
        if (result?.bool) {
          if (!logged) {
            logged = true;
            player.logSkill("zylianji");
          }
          await player.recover();
        }
      }
      if (num > 2) {
        let list;
        const evt = trigger.getParent("phase", true);
        if (evt) {
          list = evt.phaseList.slice(evt.num + 1);
        }
        if (!list.length) {
          return;
        }
        const result = await player.chooseTarget(get.prompt("zylianji"), `跳过本回合的剩余阶段，然后令一名其他角色执行一个只有${get.translation(list)}的回合`, lib.filter.notMe).set("ai", (target) => {
          const att = get.attitude(_status.event.player, target);
          const num2 = target.needsToDiscard();
          const numx = player.needsToDiscard();
          if (att < 0 && num2 > 0) {
            return -att * Math.sqrt(num2) / 3 + numx;
          }
          const skills2 = target.getSkills();
          let val = 0;
          for (const skill of skills2) {
            const info = get.info(skill);
            if (info.trigger && info.trigger.player && (info.trigger.player.indexOf("phaseJieshu") === 0 || Array.isArray(info.trigger.player) && info.trigger.player.some((i) => i.indexOf("phaseJieshu") === 0))) {
              const threaten = info.ai && info.ai.threaten ? info.ai.threaten : 1;
              if (info.ai && info.ai.neg) {
                val -= 3 * threaten;
              } else if (info.ai && info.ai.halfneg) {
                val -= 1.5 * threaten;
              } else {
                val += threaten;
              }
            }
          }
          return att * val / 2 + numx;
        }).forResult();
        if (result?.targets?.length) {
          const target = result.targets[0];
          if (!logged) {
            logged = true;
            player.logSkill("zylianji", target);
          } else {
            player.line(target);
          }
          list = list.map((name) => name.split("|")[0]);
          list.forEach((name) => player.skip(name));
          game.log(player, "跳过了", list);
          target.insertPhase().set("phaseList", list)._noTurnOver = true;
        }
      }
    }
  },
  zymoucheng: {
    audio: "moucheng",
    enable: "phaseUse",
    usable: 1,
    viewAs: { name: "jiedao" },
    filterCard: { color: "black" },
    position: "he",
    check(card) {
      return 4.5 - get.value(card);
    }
  },
  //曹安民
  nskuishe: {
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return target !== player && target.hasCards("he");
    },
    async content(event, trigger, player) {
      const { target } = event;
      const cardResult = await player.choosePlayerCard({
        target,
        position: "he",
        forced: true,
        ai(button) {
          return get.buttonValue(button);
        }
      }).forResult();
      if (!cardResult.bool || !cardResult.cards?.length) {
        return;
      }
      const card = cardResult.cards[0];
      const targetResult = await player.chooseTarget({
        prompt: `将${get.translation(target)}的${get.position(card) === "h" && !player.hasSkillTag("viewHandcard", null, target, true) ? "手牌" : get.translation(card)}交给一名角色`,
        filterTarget(card2, player2, target3) {
          return target3 !== _status.event.getParent()?.target;
        },
        forced: true,
        ai(target3) {
          const att = get.attitude(_status.event.player, target3);
          if (_status.event.du) {
            return target3.hasSkillTag("nodu") ? 0 : -att;
          }
          if (target3.hasSkillTag("nogain")) {
            return 0.1;
          }
          if (att > 0) {
            return att + Math.max(0, 5 - target3.countCards("h"));
          }
          return att;
        }
      }).set("du", card.name === "du").forResult();
      if (!targetResult.bool || !targetResult.targets?.length) {
        return;
      }
      const target2 = targetResult.targets[0];
      target.line(target2, "green");
      const gainEvent = target2.gain({
        cards: [card],
        source: target,
        animate: "giveAuto"
      });
      gainEvent.giver = player;
      await gainEvent;
      await target.chooseToUse({
        prompt: `是否对${get.translation(player)}使用一张杀？`,
        filterCard: (card2, player2, event2) => get.name(card2) === "sha" && lib.filter.filterCard(card2, player2, event2)
      }).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("filterTarget", (card2, player2, target3) => (target3 === _status.event.sourcex || ui.selected.targets.includes(_status.event.sourcex)) && lib.filter.filterTarget(card2, player2, target3)).set("sourcex", player);
    },
    ai: {
      order: 6,
      expose: 0.2,
      result: {
        target: -1.5,
        player(player, target) {
          if (!target.canUse("sha", player)) {
            return 0;
          }
          if (target.countCards("h") === 1) {
            return 0.1;
          }
          if (player.hasShan()) {
            return -0.5;
          }
          if (player.hp <= 1) {
            return -2;
          }
          if (player.hp <= 2) {
            return -1;
          }
          return 0;
        }
      }
    }
  }
};
const skills$x = {
  //祖郎
  xkxijun: {
    enable: ["chooseToUse", "chooseToRespond"],
    trigger: { player: "damageEnd" },
    filter(event, player) {
      if (player.countMark("xkxijun_used") >= 2) {
        return false;
      }
      if (!player.countCards("hes", { color: "black" })) {
        return false;
      }
      if (event.name === "damage") {
        return ["sha", "juedou"].some((name) => player.countCards("hes", (card) => get.color(card) === "black" && player.hasUseTarget(get.autoViewAs({ name }, [card]), false, false)));
      }
      if (!player.isPhaseUsing()) {
        return false;
      }
      return ["sha", "juedou"].some((name) => event.filterCard(get.autoViewAs({ name }, "unsure"), player, event));
    },
    direct: true,
    async content(event, trigger, player) {
      const result = await player.chooseButton([
        get.prompt2("xkxijun"),
        [
          [
            ["基本", "", "sha"],
            ["锦囊", "", "juedou"]
          ],
          "vcard"
        ]
      ]).set("filterButton", (button) => {
        const name = button.link[2];
        const player2 = get.player();
        return player2.countCards("hes", (card2) => get.color(card2) === "black" && player2.hasUseTarget(get.autoViewAs({ name }, [card2]), false, false));
      }).set("ai", (button) => {
        const name = button.link[2];
        const player2 = get.player();
        return player2.getUseValue({ name });
      }).forResult();
      if (!result?.bool || !result?.links?.length) {
        return;
      }
      const card = { name: result.links[0][2], storage: { xkxijun: true } };
      game.broadcastAll((card2) => {
        lib.skill.xkxijun_backup.viewAs = card2;
      }, card);
      const next = player.chooseToUse();
      next.set("openskilldialog", `袭军：是否将一张黑色牌当做${get.translation(card)}使用？`);
      next.set("norestore", true);
      next.set("addCount", false);
      next.set("_backupevent", "xkxijun_backup");
      next.set("custom", {
        add: {},
        replace: { window() {
        } }
      });
      next.backup("xkxijun_backup");
      await next;
    },
    chooseButton: {
      dialog(event, player) {
        const list = [];
        for (const name of ["sha", "juedou"]) {
          if (event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
            list.push([get.type(name), "", name]);
          }
        }
        return ui.create.dialog("袭军", [list, "vcard"]);
      },
      check(button) {
        const player = _status.event.player;
        return player.getUseValue({
          name: button.link[2]
        });
      },
      backup(links, player) {
        const backup = get.copy(lib.skill["xkxijun_backup"]);
        backup.viewAs = { name: links[0][2], storage: { xkxijun: true } };
        return backup;
      },
      prompt(links, player) {
        return `将一张黑色牌当做${get.translation(links[0][2])}使用或打出`;
      }
    },
    hiddenCard(player, name) {
      if (!player.isPhaseUsing()) {
        return false;
      }
      if (!["sha", "juedou"].includes(name)) {
        return false;
      }
      return player.countMark("xkxijun_used") < 2 && player.countCards("hes", { color: "black" });
    },
    ai: {
      respondSha: true,
      skillTagFilter(player) {
        if (!player.isPhaseUsing()) {
          return false;
        }
        if (player.countMark("xkxijun_used") >= 2 || !player.countCards("hes", { color: "black" })) {
          return false;
        }
      },
      order: 1,
      result: { player: 1 }
    },
    group: "xkxijun_effect",
    subSkill: {
      norecover: {
        charlotte: true,
        mark: true,
        intro: { content: "不能回复体力" },
        trigger: { player: "recoverBefore" },
        forced: true,
        firstDo: true,
        async content(event, trigger, player) {
          trigger.cancel();
        },
        ai: {
          effect: {
            target(card, player, target) {
              if (get.tag(card, "recover")) {
                return "zeroplayertarget";
              }
            }
          }
        }
      },
      effect: {
        trigger: { global: "damageEnd" },
        filter(event, player) {
          if (!event.player.isIn()) {
            return false;
          }
          return event.card?.storage?.xkxijun;
        },
        firstDo: true,
        logTarget: "player",
        forced: true,
        async content(event, trigger, player) {
          event.targets[0].addTempSkill("xkxijun_norecover");
        }
      },
      used: {
        onremove: true,
        charlotte: true
      },
      backup: {
        audio: "xkxijun",
        filterCard: (card) => get.itemtype(card) === "card" && get.color(card) === "black",
        popname: true,
        check(card) {
          return 8 - get.value(card);
        },
        position: "hes",
        async precontent(event, trigger, player) {
          player.addTempSkill("xkxijun_used");
          player.addMark("xkxijun_used", 1, false);
        }
      }
    }
  },
  xkhaokou: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    groupSkill: "qun",
    forced: true,
    filter(event, player) {
      if (player.group !== "qun") {
        return false;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async content(event, trigger, player) {
      await lib.skill.xk_qiyijun.qiyi(player);
    },
    group: "xkhaokou_change",
    subSkill: {
      change: {
        trigger: {
          player: "removeQiyi"
        },
        groupSkill: "qun",
        forced: true,
        filter(event, player) {
          if (player.group !== "qun") {
            return false;
          }
          return true;
        },
        async content(event, trigger, player) {
          player.changeGroup("wu");
        }
      }
    }
  },
  xkronggui: {
    groupSkill: "wu",
    trigger: {
      global: "useCardToPlayer"
    },
    filter(event, player) {
      if (event.player.group !== "wu" || player.group !== "wu") {
        return false;
      }
      if (!event.isFirstTarget) {
        return false;
      }
      if (!(event.card.name === "juedou" || event.card.name === "sha" && get.color(event.card) === "red")) {
        return false;
      }
      if (!player.countCards("he", { type: "basic" })) {
        return false;
      }
      if (game.hasPlayer((current) => !event.targets.includes(current) && lib.filter.targetEnabled2(event.card, event.player, current))) {
        return true;
      }
      return false;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        prompt: get.prompt2(event.skill),
        filterCard(card, player2) {
          return get.type(card) === "basic" && lib.filter.canBeDiscarded(card, player2, player2);
        },
        filterTarget(card, player2, target) {
          const trigger2 = get.event().getTrigger();
          return !trigger2.targets.includes(target) && lib.filter.targetEnabled2(trigger2.card, trigger2.player, target);
        },
        ai1(card) {
          return 6 - get.value(card);
        },
        ai2(target) {
          const trigger2 = get.event().getTrigger();
          const player2 = get.player();
          return get.effect(target, trigger2.card, trigger2.player, player2);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const { cards: cards2, targets } = event;
      await player.discard(cards2);
      trigger.targets.addArray(targets);
    }
  },
  //彭绮
  xkjushou: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    forced: true,
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async content(event, trigger, player) {
      await lib.skill.xk_qiyijun.qiyi(player);
      if (game.hasPlayer((target) => target.getSeatNum() !== 1 && !target.hasSkill("xk_qiyijun") && target !== player)) {
        const result = await player.chooseTarget("聚首：令至多两名其他角色选择是否成为起义军", [1, 2], true, (card, player2, target) => target.getSeatNum() !== 1 && !target.hasSkill("xk_qiyijun") && target !== player2).set("ai", (target) => Math.random()).forResult();
        if (result.bool && result.targets) {
          for (const target of result.targets) {
            const result2 = await target.chooseBool(`是否响应${get.translation(player)}的号召，成为起义军？`).forResult();
            if (result2.bool) {
              await lib.skill.xk_qiyijun.qiyi(target);
            } else {
              player.line(target, "green");
              await player.gainPlayerCard(target, "h", true);
            }
          }
        }
      }
    }
  },
  xkliaoluan: {
    global: "xkliaoluan_global",
    subSkill: {
      global: {
        enable: "phaseUse",
        filter(event, player) {
          if (!game.hasPlayer((current) => current.hasSkill("xkliaoluan"))) {
            return false;
          }
          if (!player.hasSkill("xk_qiyijun")) {
            return false;
          }
          if (player.hasSkill("xkliaoluan_used")) {
            return false;
          }
          return game.hasPlayer((current) => !current.hasSkill("xk_qiyijun") && player.inRange(current));
        },
        filterTarget(card, player, target) {
          return !target.hasSkill("xk_qiyijun") && player.inRange(target);
        },
        async content(event, trigger, player) {
          player.addSkill("xkliaoluan_used");
          await player.turnOver();
          await event.target.damage();
        },
        ai: {
          order: 1,
          result: {
            target: -1,
            player(player, target) {
              if (player.isTurnedOver()) {
                return 1;
              }
              return -1;
            }
          }
        }
      },
      used: {
        charlotte: true
      }
    }
  },
  xkhuaying: {
    trigger: {
      global: "dieAfter"
    },
    filter(event, player) {
      if (!event.player.hasSkill("xk_qiyijun")) {
        return false;
      }
      if (!event.source || event.source === event.player) {
        return false;
      }
      return game.hasPlayer((current) => current.hasSkill("xk_qiyijun"));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => target.hasSkill("xk_qiyijun")).set("ai", (target) => {
        const player2 = get.player();
        let eff = get.attitude(player2, target);
        if (eff <= 0) {
          return 0;
        }
        if (target.isTurnedOver()) {
          eff *= 2;
        }
        if (target.isLinked()) {
          eff += 2;
        }
        if (target.hasSkill("xkliaoluan_used")) {
          eff += 2;
        }
        return eff;
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      if (target.isTurnedOver()) {
        await target.turnOver(false);
      }
      if (target.isLinked()) {
        await target.link(false);
      }
      if (target.hasSkill("xkliaoluan_used")) {
        target.removeSkill("xkliaoluan_used");
      }
    }
  },
  xkjizhong: {
    locked: true,
    global: "xkjizhong_global",
    subSkill: {
      global: {
        trigger: { player: "phaseDrawBegin2" },
        forced: true,
        filter(event, player) {
          if (!player.hasSkill("xk_qiyijun")) {
            return false;
          }
          if (!game.hasPlayer((current) => current.hasSkill("xkjizhong"))) {
            return false;
          }
          return !event.numFixed;
        },
        async content(event, trigger, player) {
          trigger.num += game.countPlayer((current) => current.hasSkill("xkjizhong"));
        },
        mod: {
          globalFrom(from, to, distance) {
            if (!from.hasSkill("xk_qiyijun")) {
              return;
            }
            const num = game.countPlayer((current) => current.hasSkill("xkjizhong"));
            return distance - num;
          }
        }
      }
    }
  },
  //单福
  xkbimeng: {
    enable: "phaseUse",
    filter(event, player) {
      if (player.countCards("hs") < player.hp || player.hasSkill("xkbimeng_used")) {
        return false;
      }
      for (const i of lib.inpile) {
        const type = get.type(i);
        if ((type === "basic" || type === "trick") && event.filterCard(get.autoViewAs({ name: i }, "unsure"), player, event)) {
          return true;
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player) {
        const list = [];
        for (const name of lib.inpile) {
          if (name === "sha") {
            if (event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
              list.push(["基本", "", "sha"]);
            }
            for (const nature of lib.inpile_nature) {
              if (event.filterCard(get.autoViewAs({ name, nature }, "unsure"), player, event)) {
                list.push(["基本", "", "sha", nature]);
              }
            }
          } else if (get.type(name) === "trick" && event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
            list.push(["锦囊", "", name]);
          } else if (get.type(name) === "basic" && event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
            list.push(["基本", "", name]);
          }
        }
        return ui.create.dialog("弊蒙", [list, "vcard"]);
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = _status.event.player;
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard: true,
          audio: "xkbimeng",
          popname: true,
          check(card) {
            return 8 - get.value(card);
          },
          selectCard() {
            const player2 = get.player();
            return player2.hp;
          },
          position: "hs",
          viewAs: { name: links[0][2], nature: links[0][3] },
          async precontent(event, trigger, player2) {
            player2.addTempSkill("xkbimeng_used", "phaseUseAfter");
          }
        };
      },
      prompt(links, player) {
        return `将${get.cnNumber(player.hp)}张牌当做${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用`;
      }
    },
    ai: {
      fireAttack: true,
      skillTagFilter(player) {
        if (player.countCards("sh") < player.hp || player.hasSkill("xkbimeng_used")) {
          return false;
        }
      },
      order: 1,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      used: {
        charlotte: true
      }
    }
  },
  xkzhue: {
    groupSkill: "qun",
    usable: 1,
    trigger: {
      global: "useCard"
    },
    filter(event, player) {
      if (event.player.group !== "qun" || player.group !== "qun") {
        return false;
      }
      if (get.type(event.card) === "equip") {
        return false;
      }
      return true;
    },
    check(event, player) {
      if (get.attitude(player, event.player) <= 0) {
        return false;
      }
      if (!get.tag(event.card, "damage")) {
        return true;
      }
      if (!event.targets?.length) {
        return false;
      }
      let eff = 0;
      for (const target of event.targets) {
        eff += get.effect(target, event.card, event.player, player);
      }
      return eff >= 5;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await trigger.player.draw();
      trigger.directHit.addArray(game.players);
      player.when({ global: "useCardAfter" }).filter((evt) => evt.card === trigger.card).step(async (event2, trigger2, player2) => {
        if (game.hasPlayer2((current) => current.hasHistory("damage", (evt) => evt.card === trigger2.card))) {
          await player2.changeGroup("shu");
        }
      });
    }
  },
  xkfuzhu: {
    groupSkill: "shu",
    usable: 1,
    trigger: {
      global: "useCardAfter"
    },
    filter(event, player) {
      if (player.group !== "shu") {
        return false;
      }
      return player.countCards("he") && get.is.convertedCard(event.card);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard(get.prompt2(event.skill, trigger.player), "he").set("ai", (card) => {
        const player2 = get.player();
        const trigger2 = get.event().getTrigger();
        if (get.attitude(player2, trigger2.player) <= 0) {
          return 0;
        }
        if (get.type2(card) !== "trick") {
          return 4 - get.value(card);
        }
        return trigger2.player.getUseValue(card);
      }).forResult();
      event.result.targets = [trigger.player];
    },
    async content(event, trigger, player) {
      const { cards: cards2, targets } = event;
      player.$throw(get.position(cards2[0]) === "e" ? cards2[0] : 1, 1e3);
      game.log(player, "将", get.position(cards2[0]) === "e" ? cards2[0] : "#y一张手牌", "置于了牌堆顶");
      await player.lose(cards2, ui.cardPile, "insert");
      game.updateRoundNumber();
      const cardx = game.cardsGotoOrdering(get.cards(4)).cards;
      const target = targets[0];
      await player.showCards(cardx, `${get.translation(player)}发动了【辅主】`);
      const putback = [];
      for (const card of cardx) {
        if (get.type2(card) === "trick" && target.hasUseTarget(card)) {
          await target.chooseUseTarget(card, true);
        } else {
          putback.push(card);
        }
      }
      if (putback.length) {
        const next = player.chooseToMove("辅主：点击或拖动将牌置于牌堆顶或牌堆底", true);
        next.set("list", [["牌堆顶", putback], ["牌堆底"]]);
        next.set("processAI", (list) => {
          const cards3 = list[0][1].slice(0).sort((a, b) => get.value(b) - get.value(a));
          return [cards3, []];
        });
        const result = await next.forResult();
        if (result?.bool) {
          const top = result.moved[0];
          const bottom = result.moved[1];
          top.reverse();
          await game.cardsGotoPile(top.concat(bottom), ["top_cards", top], (event2, card) => {
            if (event2.top_cards.includes(card)) {
              return ui.cardPile.firstChild;
            }
            return null;
          });
          await game.delayx();
        }
      }
    }
  },
  //彭虎
  xkjuqian: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    forced: true,
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async content(event, trigger, player) {
      await lib.skill.xk_qiyijun.qiyi(player);
      if (game.hasPlayer((target) => target.getSeatNum() !== 1 && !target.hasSkill("xk_qiyijun") && target !== player)) {
        const result = await player.chooseTarget("聚黔：令至多两名其他角色选择是否成为起义军", [1, 2], true, (card, player2, target) => target.getSeatNum() !== 1 && !target.hasSkill("xk_qiyijun") && target !== player2).set("ai", (target) => Math.random()).forResult();
        if (result.bool && result.targets) {
          for (const target of result.targets) {
            const result2 = await target.chooseBool(`是否响应${get.translation(player)}的号召，成为起义军？`).forResult();
            if (result2.bool) {
              await lib.skill.xk_qiyijun.qiyi(target);
            } else {
              player.line(target, "green");
              await target.damage(player);
            }
          }
        }
      }
    }
  },
  xkkanpo: {
    trigger: {
      source: "damageSource"
    },
    filter(event, player) {
      if (!game.hasPlayer((current) => current.hasSkill("xk_qiyijun"))) {
        return false;
      }
      return !event.player.isIn() || event.player.hp <= player.hp;
    },
    forced: true,
    usable: 1,
    async content(event, trigger, player) {
      const num = game.countPlayer((current) => current.hasSkill("xk_qiyijun"));
      await player.draw(num);
    }
  },
  xkyizhong: {
    trigger: {
      global: "becomeQiyi"
    },
    filter(event, player) {
      return event.player.hujia < 5;
    },
    logTarget: "player",
    forced: true,
    async content(event, trigger, player) {
      await trigger.player.changeHujia(1, null, true);
    }
  },
  //崔廉
  xktanlu: {
    trigger: {
      global: "phaseBegin"
    },
    logTarget: "player",
    filter(event, player) {
      return event.player !== player;
    },
    check(event, player) {
      return get.attitude(player, event.player) < 0;
    },
    async content(event, trigger, player) {
      const target = trigger.player;
      const num = Math.abs(target.hp - player.hp);
      let result;
      if (num === 0 || num > target.countCards("h")) {
        result = {
          index: 1
        };
      } else {
        result = await target.chooseControl("交给牌", "造成伤害").set("prompt", "贪赂：请选择一项").set("choiceList", [`交给${get.translation(player)}${get.cnNumber(num)}张手牌`, `令${get.translation(player)}对你造成1点伤害，然后弃置其一张手牌`]).set("target", player).set("ai", () => {
          const { player: player2, target: target2 } = get.event();
          const eff1 = get.effect(player2, { name: "shunshou_copy2" }, target2, player2);
          const eff2 = get.damageEffect(player2, target2, player2) + get.effect(target2, { name: "guohe_copy2" }, player2, player2);
          if (eff1 >= eff2) {
            return "交给牌";
          }
          return "造成伤害";
        }).forResult();
      }
      if (result.index === 0) {
        await target.chooseToGive(player, "h", num, true);
      } else {
        await target.damage(player);
        await target.discardPlayerCard(player, "h", true);
      }
    }
  },
  xkjubian: {
    trigger: { player: "damageBegin4" },
    filter(event, player) {
      return player.countCards("h") > player.hp;
    },
    forced: true,
    async content(event, trigger, player) {
      await player.chooseToDiscard("h", true, player.countCards("h") - player.hp, "allowChooseAll");
      trigger.cancel();
    }
  },
  //罗历
  xkjuluan: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    forced: true,
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async content(event, trigger, player) {
      await lib.skill.xk_qiyijun.qiyi(player);
      if (game.hasPlayer((target) => target.getSeatNum() !== 1 && !target.hasSkill("xk_qiyijun") && target !== player)) {
        const result = await player.chooseTarget("聚乱：令至多两名其他角色选择是否成为起义军", [1, 2], true, (card, player2, target) => target.getSeatNum() !== 1 && !target.hasSkill("xk_qiyijun") && target !== player2).set("ai", (target) => Math.random()).forResult();
        if (result.bool && result.targets) {
          for (const target of result.targets) {
            const result2 = await target.chooseBool(`是否响应${get.translation(player)}的号召，成为起义军？`).forResult();
            if (result2.bool) {
              await lib.skill.xk_qiyijun.qiyi(target);
            } else {
              player.line(target, "green");
              await player.discardPlayerCard(target, "h", true);
            }
          }
        }
      }
    },
    group: "xkjuluan_damage",
    subSkill: {
      damage: {
        trigger: {
          source: "damageBegin1",
          player: "damageBegin3"
        },
        forced: true,
        filter(event, player, name) {
          const key = name === "damageBegin1" ? "sourceDamage" : "damage";
          return player.getHistory(key).length === 1;
        },
        async content(event, trigger, player) {
          trigger.num++;
        }
      }
    }
  },
  xkxianxing: {
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      if (!event.targets || event.targets.length !== 1) {
        return false;
      }
      if (event.target === player) {
        return false;
      }
      if (!event.card || !get.is.damageCard(event.card)) {
        return false;
      }
      return player.isPhaseUsing();
    },
    logTarget: "target",
    async content(event, trigger, player) {
      player.addTempSkill(`${event.name}_used`);
      player.addMark(`${event.name}_used`, 1, false);
      const num = player.countMark(`${event.name}_used`);
      await player.draw(num);
      if (num > 1) {
        player.when("useCardAfter").filter((evt) => evt.card === trigger.card).step(async () => {
          if (!game.hasPlayer2((current) => current.hasHistory("damage", (evt) => evt.card === trigger.card))) {
            const { index } = await player.chooseControl(`失去${num - 1}点体力`, "此技能本回合失效").set("prompt", "险行：选择一项").set("ai", () => {
              if (get.event().num > 1) {
                return 1;
              }
              return [0, 1].randomGet();
            }).set("num", num).forResult();
            if (index === 0) {
              await player.loseHp(num - 1);
            } else {
              player.tempBanSkill("xkxianxing");
            }
          }
        });
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "已发动过#次"
        }
      }
    }
  },
  xk_qiyijun: {
    charlotte: true,
    nopop: true,
    forced: true,
    trigger: {
      player: "phaseEnd"
    },
    filter(event, player) {
      if (player.hasHistory("sourceDamage", (evt) => evt.player && !evt.player.hasSkill("xk_qiyijun"))) {
        return false;
      }
      if (player.hasHistory("useCard", (evt) => evt.card.name === "sha" && evt.targets?.some((target) => !target.hasSkill("xk_qiyijun")))) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player) {
      const result = await player.chooseControl("失去“起义军”", "失去1点体力").set("prompt", "起义军：请选择一项").set("choiceList", ["失去“起义军”并弃置所有手牌", "失去1点体力"]).set("ai", () => {
        if (player.hp <= 1) {
          return "失去“起义军”";
        }
        return ["失去“起义军”", "失去1点体力"].randomGet();
      }).forResult();
      if (result.index === 0) {
        await lib.skill.xk_qiyijun.unQiyi(player);
        const hs = player.getDiscardableCards(player, "h");
        if (hs.length) {
          await player.discard(hs);
        }
      } else {
        await player.loseHp();
      }
    },
    mod: {
      cardUsable(card, player, num) {
        if (card.name === "sha") {
          return num + 1;
        }
      }
    },
    global: "xk_qiyijun_effect",
    qiyi(player) {
      player.addSkill("xk_qiyijun");
      player.markSkillCharacter("xk_qiyijun", "shibing1", "起义军", "已决定起义<br>未起义的角色对你使用【杀】次数+1");
      const next = game.createEvent("becomeQiyi");
      next.player = player;
      next.setContent("emptyEvent");
      return next;
    },
    unQiyi(player) {
      player.removeSkill("xk_qiyijun");
      const next = game.createEvent("removeQiyi");
      next.player = player;
      next.setContent("emptyEvent");
      return next;
    },
    subSkill: {
      effect: {
        trigger: {
          player: "useCard"
        },
        filter(event, player) {
          if (!player.isPhaseUsing() || event.card.name !== "sha") {
            return false;
          }
          player._countPrenum = true;
          const num = player.getCardUsable("sha");
          delete player._countPrenum;
          if (num >= 0) {
            return false;
          }
          return event.targets?.some((target) => target.hasSkill("xk_qiyijun"));
        },
        charlotte: true,
        direct: true,
        async content(event, trigger, player) {
          if (!player.getStorage("xk_qiyijun").length) {
            player.when({ global: ["phaseBefore", "phaseAfter", "phaseUseBefore", "phaseUseAfter"] }).step(async () => {
              player.unmarkAuto("xk_qiyijun", player.getStorage("xk_qiyijun"));
            });
          }
          player.markAuto(
            "xk_qiyijun",
            trigger.targets.filter((target) => target.hasSkill("xk_qiyijun"))
          );
        },
        mod: {
          cardUsable(card, player, num) {
            if (player._countPrenum || player.hasSkill("xk_qiyijun")) {
              return;
            }
            if (card.name === "sha") {
              return num + game.countPlayer((current) => current.hasSkill("xk_qiyijun"));
            }
          },
          playerEnabled(card, player, target) {
            if (card.name !== "sha") {
              return;
            }
            player._countPrenum = true;
            const num = player.getCardUsable(card);
            delete player._countPrenum;
            if (num > 0) {
              return;
            }
            if (game.checkMod(card, player, target, false, "cardUsableTarget", player)) {
              return;
            }
            if (player.getStorage("xk_qiyijun").includes(target)) {
              return false;
            }
            if (!target.hasSkill("xk_qiyijun")) {
              return false;
            }
          }
        }
      }
    }
  }
};
const skills$w = {
  //君霸天下
  //君刘备
  jun_renwang: {
    audio: "rerende",
    enable: "phaseUse",
    filterCard: true,
    selectCard: [2, Infinity],
    position: "he",
    discard: false,
    lose: false,
    delay: 0,
    filterTarget(card, player, target) {
      return player !== target;
    },
    check(card) {
      if (ui.selected.cards.length > 1) {
        return 0;
      }
      const bool = ui.selected.cards?.some((cardx) => cardx.name === "du");
      if (bool && card.name === "du") {
        return 20;
      }
      return 10 - get.value(card);
    },
    allowChooseAll: true,
    async content(event, trigger, player) {
      await player.give(event.cards, event.target);
      await player.recover();
      player.addTempSkill("jun_renwang_effect");
      player.addMark("jun_renwang_effect", 1, false);
    },
    ai: {
      order(skill, player) {
        if (player.hp < player.maxHp && player.countCards("h") > 1) {
          return 10;
        }
        return 1;
      },
      result: {
        target(player, target) {
          if (target.hasSkillTag("nogain")) {
            return 0;
          }
          if (ui.selected.cards.length && ui.selected.cards[0].name === "du") {
            return target.hasSkillTag("nodu") ? 0 : -10;
          }
          if (target.hasJudge("lebu")) {
            return 0;
          }
          const nh = target.countCards("h");
          const np = player.countCards("h");
          if (player.hp === player.maxHp || player.countCards("h") <= 1) {
            if (nh >= np - 1 && np <= player.hp && !target.hasSkill("haoshi")) {
              return 0;
            }
          }
          return Math.max(1, 5 - nh);
        }
      },
      effect: {
        target_use(card, player, target) {
          if (player === target && get.type(card) === "equip") {
            if (player.countCards("e", { subtype: get.subtype(card) })) {
              const players = game.filterPlayer();
              for (const current of players) {
                if (current !== player && get.attitude(player, current) > 0) {
                  return 0;
                }
              }
            }
          }
        }
      },
      threaten: 0.8
    },
    subSkill: {
      effect: {
        onremove: true,
        charlotte: true,
        marktext: "武",
        intro: {
          content: "出杀次数+#"
        },
        mod: {
          cardUsable(card, player, num) {
            if (card.name === "sha") {
              return num + player.countMark("jun_renwang_effect");
            }
          }
        }
      }
    }
  },
  //君曹操
  jun_xiongtu: {
    audio: "sbjianxiong",
    trigger: { player: "changeHpAfter" },
    filter(event, player) {
      return event.changedHp !== 0;
    },
    frequent: true,
    async content(event, trigger, player) {
      const list = [];
      for (const position of ["c", "d", "ej"]) {
        const card = get.cardPile((card2) => get.is.damageCard(card2) && position.includes(get.position(card2, true)), "field", "random");
        if (card) {
          if (position === "ej") {
            const owner = get.owner(card);
            if (owner) {
              player.line(owner, "green");
              owner.$give(card, player, false);
              await game.delayx();
            }
          }
          list.push(card);
        }
      }
      const cards2 = Array.from(ui.ordering.childNodes).slice(0).filter((card) => card && get.tag(card, "damage") && get.position(card, true) === "o");
      if (cards2.length) {
        list.push(cards2.randomGet());
      }
      if (list.length) {
        await player.gain(list, "gain2");
      }
    }
  },
  //君孙策
  jun_jiang: {
    audio: "sbjiang",
    trigger: {
      player: "useCard"
    },
    filter(event, player) {
      return get.color(event.card) === "red";
    },
    frequent: true,
    async content(event, trigger, player) {
      await player.draw();
    }
  },
  jun_zhiyang: {
    audio: ["yingyang1.mp3", "yingyang2.mp3"],
    trigger: {
      player: "damageEnd"
    },
    filter(event, player) {
      return event.source?.canCompare(player);
    },
    logTarget: "source",
    async content(event, trigger, player) {
      await trigger.source.useSkill("zhiba_global", [player]);
      await trigger.source.useSkill("zhiba_global", [player]);
    },
    derivation: ["zhiba"],
    group: "jun_zhiyang_number",
    subSkill: {
      number: {
        audio: "jun_zhiyang",
        trigger: {
          player: "compare",
          target: "compare"
        },
        filter(event, player) {
          if (event.player === player) {
            return !event.iwhile && get.color(event.card1) === "red";
          }
          return get.color(event.card2) === "red";
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          game.log(player, "拼点牌点数视为", "#yK");
          if (player === trigger.player) {
            trigger.num1 = 13;
          } else {
            trigger.num2 = 13;
          }
        }
      }
    }
  },
  //君袁绍
  jun_hefa: {
    audio: "olsbhetao",
    enable: "phaseUse",
    usable: 1,
    viewAs: {
      name: "sha",
      storage: {
        hefa: true
      }
    },
    viewAsFilter(player) {
      return player.countCards("hs") > 0;
    },
    selectCard() {
      return [1, Infinity];
    },
    selectTarget() {
      const card = get.card();
      const player = get.player();
      if (card === void 0) {
        return;
      }
      const range = [1, Math.max(1, ui.selected.cards.length)];
      game.checkMod(card, player, range, "selectTarget", player);
      return range;
    },
    complexCard: true,
    filterCard: true,
    filterOk() {
      if (!ui.selected.targets.length) {
        return false;
      }
      const card = get.card();
      const player = get.player();
      if (card === void 0) {
        return;
      }
      const range = [1, Math.max(1, ui.selected.cards.length)];
      game.checkMod(card, player, range, "selectTarget", player);
      if (range[0] <= ui.selected.targets.length && range[1] >= ui.selected.targets.length || range[0] === -1) {
        return true;
      }
      return false;
    },
    check(card) {
      const player = _status.event.player;
      const cardx = get.autoViewAs({ name: "sha" }, ui.selected.cards.concat(card));
      if (game.countPlayer((current) => (_status.event.filterTarget || lib.filter.filterTarget)(cardx, player, current) && get.effect_use(current, cardx, player, player) > 0) <= ui.selected.cards.length) {
        return 0;
      }
      return 7 - get.value(card);
    },
    position: "hs",
    onuse(links, player) {
      _status.event.addCount = false;
    },
    ai: {
      order: () => get.order({ name: "sha" }) - 0.1
    },
    locked: false,
    group: "jun_hefa_draw",
    subSkill: {
      draw: {
        trigger: {
          source: "damageSource"
        },
        filter(event, player) {
          return event?.card?.storage?.hefa && player.countCards("h") < player.maxHp;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          await player.drawTo(player.maxHp);
        }
      }
    },
    mod: {
      cardUsable(card, player, num) {
        if (card.storage?.hefa) {
          return Infinity;
        }
      },
      targetInRange(card, player, num) {
        if (card.storage?.hefa) {
          return true;
        }
      }
    }
  },
  //君张角
  jun_huanlei: {
    audio: "xinleiji",
    trigger: {
      player: ["judgeEnd", "useCardAfter", "respondAfter"]
    },
    disableReason: ["暴虐", "助祭", "弘仪", "孤影", "唤雷"],
    filter(event, player) {
      if (!game.hasPlayer((current) => current !== player)) {
        return false;
      }
      if (event.name === "judge") {
        return !lib.skill.xinleiji_misa.disableReason.includes(event.judgestr);
      }
      return ["shan", "shandian"].includes(event.card.name);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), lib.filter.notMe).set("ai", (target) => {
        const player2 = get.player();
        if (target.hasSkill("hongyan")) {
          return 1;
        }
        return get.damageEffect(target, player2, player2, "thunder");
      }).forResult();
    },
    async content(event, trigger, player) {
      const [target] = event.targets;
      const next = target.judge((card) => {
        if (get.suit(card) === "spade") {
          return -4;
        }
        return -1;
      });
      const result = await next.forResult();
      if (result?.suit === "spade") {
        await target.damage(2, "thunder");
      } else if (target.countCards("h")) {
        await player.gainPlayerCard(target, "h", true);
      }
    },
    ai: {
      mingzhi: false,
      useShan: true,
      effect: {
        target_use(card, player, target, current) {
          if (get.tag(card, "respondShan") && !player.hasSkillTag(
            "directHit_ai",
            true,
            {
              target,
              card
            },
            true
          ) && game.hasPlayer((current2) => get.attitude(target, current2) < 0 && get.damageEffect(current2, target, target, "thunder") > 0)) {
            if (card.name === "sha") {
              if (!target.mayHaveShan(player, "use")) {
                return;
              }
            } else if (!target.mayHaveShan(player)) {
              return 1 - 0.1 * Math.min(5, target.countCards("hs"));
            }
            if (!target.hasSkillTag("rejudge")) {
              return [1, 1];
            }
            const pos = player.hasSkillTag("viewHandcard", null, target, true) ? "hes" : "e";
            if (target.hasCard((cardx) => get.suit(cardx) === "spade", pos)) {
              return [1, 4];
            }
            if (pos === "e") {
              return [1, Math.min(4, 1 + 0.75 * Math.max(1, target.countCards("hs")))];
            }
            return [1, 1];
          }
        }
      }
    }
  },
  jun_xiandao: {
    audio: "xinguidao",
    mod: {
      aiOrder(player, card, num) {
        if (num > 0 && get.itemtype(card) === "card" && get.color(card) === "black" && get.type(card) === "equip") ;
      },
      aiValue(player, card, num) {
        if (num > 0 && get.itemtype(card) === "card" && get.color(card) === "black") {
          return num * 1.15;
        }
      },
      aiUseful(player, card, num) {
        if (num > 0 && get.itemtype(card) === "card" && get.color(card) === "black") {
          return num * 1.35;
        }
      }
    },
    locked: false,
    trigger: { global: "judge" },
    filter(event, player) {
      return player.countCards("hes", { color: "black" }) > 0;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard(`${get.translation(trigger.player)}的${trigger.judgestr || ""}判定为${get.translation(trigger.player.judging[0])}，${get.prompt(event.skill)}`, "hes", (card) => {
        const player2 = get.player();
        if (get.color(card) !== "black") {
          return false;
        }
        const mod2 = game.checkMod(card, player2, "unchanged", "cardEnabled2", player2);
        if (mod2 !== "unchanged") {
          return mod2;
        }
        const mod = game.checkMod(card, player2, "unchanged", "cardRespondable", player2);
        if (mod !== "unchanged") {
          return mod;
        }
        return true;
      }).set("ai", (card) => {
        const trigger2 = get.event().getTrigger();
        const { player: player2, judging } = get.event();
        const result = trigger2.judge(card) - trigger2.judge(judging);
        const attitude = get.attitude(player2, trigger2.player);
        if (attitude === 0 || result === 0) {
          if (trigger2.player !== player2) {
            return 0;
          }
          if (game.hasPlayer((current) => get.attitude(player2, current) < 0)) {
            const checkx = lib.skill.xinleiji.judgeCheck(card, true) - lib.skill.xinleiji.judgeCheck(judging);
            if (checkx > 0) {
              return checkx;
            }
          }
          return 0;
        }
        let val = get.value(card);
        if (get.subtype(card) === "equip2") {
          val /= 2;
        } else {
          val /= 7;
        }
        if (attitude === 0 || result === 0) {
          return 0;
        }
        if (attitude > 0) {
          return result - val;
        }
        return -result - val;
      }).set("judging", trigger.player.judging[0]).forResult();
    },
    popup: false,
    async content(event, trigger, player) {
      const next = player.respond(event.cards, event.name, "highlight", "noOrdering");
      await next;
      const { cards: cards2 } = next;
      if (cards2?.length) {
        player.$gain2(trigger.player.judging[0]);
        await player.gain(trigger.player.judging[0]);
        await player.draw("nodelay");
        trigger.player.judging[0] = cards2[0];
        trigger.orderingCards.addArray(cards2);
        game.log(trigger.player, "的判定牌改为", cards2);
        await game.delay(2);
      }
    },
    ai: {
      rejudge: true,
      tag: { rejudge: 1 }
    }
  },
  //君曹丕
  jun_cuanzun: {
    audio: "sbxingshang",
    trigger: {
      global: "die"
    },
    frequent(event) {
      return !event.player.countCards("he", "du");
    },
    logTarget: "player",
    filter(event, player) {
      if (event.player === player) {
        return false;
      }
      return player.isDamaged() || event.player.countCards("he") > 0;
    },
    async content(event, trigger, player) {
      const cards2 = trigger.player.getCards("he");
      if (cards2.length) {
        await player.gain(cards2, trigger.player, "giveAuto", "bySelf");
      }
      if (player.isDamaged()) {
        await player.recover();
      }
    }
  },
  jun_liufang: {
    audio: "sbfangzhu",
    enable: "phaseUse",
    trigger: {
      player: "damageEnd"
    },
    filter(event, player) {
      if (!game.hasPlayer((current) => current !== player)) {
        return false;
      }
      return event.name === "damage" || !player.hasSkill("jun_liufang_used");
    },
    async cost(event, trigger, player) {
      const draw = player.getDamagedHp();
      event.result = await player.chooseTarget(get.prompt(event.skill), `令一名其他角色翻面${draw > 0 ? `并摸${get.cnNumber(draw)}张牌` : ""}`, (card, player2, target) => player2 !== target).setHiddenSkill(event.skill).set("ai", (target) => {
        if (target.hasSkillTag("noturn")) {
          return 0;
        }
        const player2 = _status.event.player;
        const current = _status.currentPhase;
        const dis = current ? get.distance(current, target, "absolute") : 1;
        const draw2 = player2.getDamagedHp();
        const att = get.attitude(player2, target);
        if (att === 0) {
          return target.hasJudge("lebu") ? Math.random() / 3 : Math.sqrt(get.threaten(target)) / 5 + Math.random() / 2;
        }
        if (att > 0) {
          if (target.isTurnedOver()) {
            return att + draw2;
          }
          if (draw2 < 4) {
            return -1;
          }
          if (current && target.getSeatNum() > current.getSeatNum()) {
            return att + draw2 / 3;
          }
          return 10 * Math.sqrt(Math.max(0.01, get.threaten(target))) / (3.5 - draw2) + dis / (2 * game.countPlayer());
        }
        if (target.isTurnedOver()) {
          return att - draw2;
        }
        if (draw2 >= 5) {
          return -1;
        }
        if (current && target.getSeatNum() <= current.getSeatNum()) {
          return -att + draw2 / 3;
        }
        return (4.25 - draw2) * 10 * Math.sqrt(Math.max(0.01, get.threaten(target))) + 2 * game.countPlayer() / dis;
      }).forResult();
    },
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player) {
      if (trigger?.name !== "damage") {
        player.addTempSkill("jun_liufang_used", "phaseChange");
      }
      const target = event.targets[0];
      const num = player.getDamagedHp();
      if (num > 0) {
        await target.draw(num);
      }
      await target.turnOver();
      if (num > 1) {
        await target.executeDelayCardEffect("shandian");
      }
    },
    ai: {
      order: 2,
      result: {
        target(player, target) {
          if (target.isTurnedOver()) {
            return 5;
          }
          return -3;
        }
      }
    },
    subSkill: {
      used: {
        charlotte: true
      }
    }
  },
  //君刘禅
  jun_fuxiang: {
    audio: "refangquan",
    trigger: {
      player: "phaseUseBefore"
    },
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), lib.filter.notMe).set("ai", (target) => {
        const player2 = get.player();
        if (target.hasJudge("lebu") || get.attitude(player2, target) <= 4) {
          return 0;
        }
        return get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards("h") + 1) > 0;
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      trigger.cancel();
      player.when("phaseDiscardAfter").filter((evt) => evt.getParent() === trigger.getParent()).step(async (event2, trigger2, player2) => {
        const cards2 = [];
        game.getGlobalHistory("cardMove", (evt) => {
          if (evt.getParent("phaseDiscard") !== trigger2) {
            return false;
          }
          if (evt.name === "lose" && (evt.type !== "discard" || evt.position !== ui.discardPile)) {
            return false;
          }
          cards2.addArray(evt.cards.filterInD("d"));
        });
        if (cards2.length) {
          player2.logSkill("jun_fuxiang", [target]);
          await target.gain(cards2, "gain2");
        }
      });
      target.insertPhase();
    }
  },
  jun_leling: {
    audio: "xiangle_re_liushan",
    trigger: {
      target: "useCardToTarget"
    },
    forced: true,
    preHidden: true,
    logTarget: "player",
    filter(event, player) {
      return event.card.name === "sha" || get.type(event.card) === "delay";
    },
    async content(event, trigger, player) {
      const eff = get.effect(player, trigger.card, trigger.player, trigger.player);
      const type = get.type2(trigger.card);
      const result = trigger.player === player ? {
        bool: false
      } : await trigger.player.chooseToGive(player, `乐陵：交给${get.translation(player)}一张${get.translation(type)}手牌，否则此牌对其无效`, (card) => get.type2(card) === get.event().cardType).set("ai", (card) => {
        if (get.event().eff > 0) {
          return 10 - get.value(card);
        }
        return 0;
      }).set("cardType", type).set("eff", eff).forResult();
      if (!result?.bool) {
        trigger.getParent().excluded.add(player);
      }
    },
    ai: {
      effect: {
        target(card, player, target, current) {
          if ((card.name === "sha" || get.type(card) === "delay") && get.attitude(player, target) < 0) {
            if (_status.event.name === "jun_leling") {
              return;
            }
            if (get.attitude(player, target) > 0 && current < 0) {
              return "zerotarget";
            }
            const bs = player.getCards("h", { type: get.type2(card) });
            bs.remove(card);
            if (card.cards) {
              bs.removeArray(card.cards);
            } else {
              bs.removeArray(ui.selected.cards);
            }
            if (!bs.length) {
              return "zerotarget";
            }
            if (player.hasSkill("jiu") || player.hasSkill("tianxianjiu")) {
              return;
            }
            if (bs.length <= 2) {
              for (const cardx of bs) {
                if (get.value(cardx) < 7) {
                  return [1, 0.5, 1, -0.5];
                }
              }
              return [1, 0, 0.3, 0];
            }
            return [1, 0.5, 1, -0.5];
          }
        }
      }
    }
  },
  //君孙权
  jun_henglv: {
    audio: "rezhiheng",
    enable: "phaseUse",
    filter(event, player) {
      return player.countCards("h");
    },
    filterCard: lib.filter.cardDiscardable,
    selectCard: [1, Infinity],
    lose: false,
    discard: false,
    check(card) {
      const player = _status.event.player;
      if (get.position(card) === "e") {
        const subs = get.subtypes(card);
        if (subs.includes("equip2") || subs.includes("equip3")) {
          return player.getHp() - get.value(card);
        }
      }
      return 6 - get.value(card);
    },
    allowChooseAll: true,
    async content(event, trigger, player) {
      const num = (player.getStat("skill")[event.name] || 1) - 1;
      if (num > 0) {
        await player.loseHp(num);
      }
      event.cards = event.cards.filterInD("h");
      await player.discard(event.cards);
      const cards2 = event.cards.filter((card) => card.name === "tao");
      while (cards2.length) {
        const result = cards2.length > 1 ? await player.chooseButtonTarget({
          createDialog: [`衡虑：是否分配弃置的【桃】？`, cards2],
          selectButton: [1, Infinity],
          cardsx: cards2,
          filterTarget: true,
          ai1(button) {
            return get.value(button.link);
          },
          ai2(target) {
            const player2 = get.player();
            const card = ui.selected.buttons[0].link;
            if (card) {
              let eff = get.value(card, target) * get.attitude(player2, target);
              if (player2.hasSkill("jiuyuan") && target !== player2) {
                eff *= 2;
              }
              return eff;
            }
            return 1;
          }
        }).forResult() : await player.chooseTarget(`衡虑：是否令一名角色获得${get.translation(cards2)}？`).set("ai", (target) => {
          const { player: player2, cardx: card } = get.event();
          let eff = get.value(card, target) * get.attitude(player2, target);
          if (player2.hasSkill("jiuyuan") && target !== player2) {
            eff *= 2;
          }
          return eff;
        }).set("cardx", cards2[0]).forResult();
        if (result?.bool) {
          if (!result?.links?.length) {
            result.links = cards2.slice(0);
          }
          cards2.removeArray(result.links);
          player.line(result.targets, "green");
          const gainEvent = result.targets[0].gain(result.links, "gain2");
          gainEvent.giver = player;
          await gainEvent;
        } else {
          break;
        }
      }
      await player.draw(event.cards.length + num);
    },
    ai: {
      order: 1,
      result: {
        player(player, target) {
          const num = player.getStat("skill").jun_henglv || 0;
          if (num > 1 || player.hp < 2 && !player.countCards("hs", "tao")) {
            return 0;
          }
          return 1;
        }
      }
    }
  },
  //君刘协
  jun_tianze: {
    audio: "tianming",
    trigger: {
      target: "useCardToTargeted"
    },
    check(event, player) {
      const cards2 = player.getCards("h");
      if (cards2.length <= 2) {
        for (const card of cards2) {
          if (card.name === "shan" || card.name === "tao") {
            return false;
          }
        }
      }
      return true;
    },
    filter(event, player) {
      return event.card.name === "sha";
    },
    async cost(event, trigger, player) {
      const num = Math.min(2, player.countCards("he"));
      let bool = true;
      if (num <= 2 && player.getCards("he").some((card) => ["shan", "tao"].includes(card.name))) {
        bool = false;
      }
      event.result = num > 0 ? await player.chooseToDiscard(get.prompt2(event.skill), num, "he").set("boolx", bool).set("ai", (card) => {
        if (get.event().boolx) {
          return 11 - get.value(card);
        }
        return 0;
      }).set("chooseonly", true).forResult() : await player.chooseBool(get.prompt2(event.skill)).forResult();
    },
    async content(event, trigger, player) {
      if (event?.cards?.length) {
        await player.discard(event.cards);
      }
      const cards2 = get.cards(4, true);
      const result = await player.chooseButton(["天择：获得其中两张", cards2], 2, true).set("ai", (button) => get.buttonValue(button)).forResult();
      if (result.bool) {
        await player.gain(result.links, "draw");
      }
      const targets = game.filterPlayer((current) => {
        if (current === player) {
          return false;
        }
        return current.isMaxHandcard() || current.isMaxHp();
      });
      if (!targets.length) {
        return;
      }
      const result2 = targets.length > 1 ? await player.chooseTarget("令一名手牌数或体力值最大的其他角色执行【天择】", true, (card, player2, target) => {
        if (target === player2) {
          return false;
        }
        return target.isMaxHandcard() || target.isMaxHp();
      }).set("ai", (target) => {
        const player2 = get.player();
        return get.attitude(player2, target);
      }).forResult() : {
        bool: true,
        targets
      };
      if (result2.bool) {
        const target = result2.targets[0];
        player.line(target, "green");
        if (target.countCards("he")) {
          await target.chooseToDiscard(2, "he", true);
        }
        const cards22 = get.cards(4, true);
        const result3 = await target.chooseButton(["天择：获得其中两张", cards22], 2, true).set("ai", (button) => get.buttonValue(button)).forResult();
        if (result3.bool) {
          await target.gain(result3.links, "draw");
        }
      }
    },
    ai: {
      effect: {
        target_use(card, player, target, current) {
          if (card.name === "sha") {
            return [1, 0.5];
          }
        }
      }
    }
  },
  jun_zhaoshou: {
    audio: "mizhao",
    enable: "phaseUse",
    filter(event, player) {
      return player.countCards("h") > 0;
    },
    filterCard: true,
    selectCard: -1,
    filterTarget(card, player, target) {
      return player !== target;
    },
    discard: false,
    lose: false,
    delay: false,
    ai: {
      order: 1,
      result: {
        player: 0,
        target(player, target) {
          if (target.hasSkillTag("nogain")) {
            return 0;
          }
          return 1;
        }
      }
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const cards2 = event.cards;
      await player.give(cards2, target, false);
      if (!target.countCards("h")) {
        return;
      }
      if (!game.hasPlayer((current) => target.canCompare(current) && current !== player)) {
        return;
      }
      const result = await player.chooseTarget(true, "选择拼点目标", (card, player2, target2) => get.event().targetx.canCompare(target2) && target2 !== player2).set("ai", (target2) => {
        const { player: player2, targetx: targetx2 } = get.event();
        const eff = get.effect(target2, { name: "sha" }, targetx2, player2);
        const att = get.attitude(player2, target2);
        if (att > 0) {
          return eff - 10;
        }
        return eff;
      }).set("targetx", target).set("forceDie", true).forResult();
      if (!result.bool || !result?.targets?.length) {
        return;
      }
      const targetx = result.targets[0];
      target.line(targetx, "green");
      const result2 = await target.chooseToCompare(targetx).forResult();
      if (!result2.tie) {
        const user = result2.bool ? target : targetx;
        const card = new lib.element.VCard({ name: "sha", isCard: true });
        if (user.hasUseTarget(card, false)) {
          await user.chooseUseTarget(card, "nodistance", false, true);
        }
      }
    }
  },
  jun_gezhi: {
    audio: "twgezhi",
    trigger: {
      player: "useCard"
    },
    filter(event, player) {
      return player.getCards("he").map((card) => get.type2(card)).toUniqued().length > 2;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard(get.prompt2(event.skill), 3, "he").set("filterCard", (card, player2) => {
        if (!lib.filter.cardRecastable(card, player2)) {
          return false;
        }
        return !ui.selected.cards?.some((cardx) => get.type2(cardx) === get.type2(card));
      }).set("complexCard", true).set("ai", (card) => 9 - get.value(card)).forResult();
    },
    async content(event, trigger, player) {
      await player.recast(event.cards);
      const result = await player.chooseButtonTarget({
        createDialog: [
          "革制：请选择一项",
          [
            [
              ["recover", "回复1点体力"],
              ["limit", "使用下一张牌无次数限制"],
              ["damage", "对一名其他角色造成1点伤害"]
            ],
            "textbutton"
          ]
        ],
        filterButton(button) {
          const { player: player2, targetx: targets } = get.event();
          if (button.link === "recover") {
            return player2.isDamaged();
          }
          if (button.link === "damage") {
            return game.hasPlayer((current) => player2 !== current && !targets.includes(current));
          }
          return true;
        },
        filterTarget(card, player2, target) {
          if (ui.selected.buttons[0]?.link !== "damage") {
            return false;
          }
          const { targetx: targets } = get.event();
          return target !== player2 && !targets.includes(target);
        },
        forced: true,
        complexSelect: true,
        complexTarget: true,
        selectTarget() {
          if (ui.selected.buttons[0]?.link !== "damage") {
            return -1;
          }
          return 1;
        },
        ai1(button) {
          const { player: player2, targetx: targets } = get.event();
          if (button.link === "recover") {
            return get.recoverEffect(player2, player2, player2);
          }
          if (button.link === "damage") {
            const target = game.filterPlayer((current) => player2 !== current && !targets.includes(current)).maxBy((current) => get.damageEffect(current, player2, player2));
            return get.damageEffect(target, player2, player2);
          }
          return 1;
        },
        ai2(target) {
          const player2 = get.player();
          return get.damageEffect(target, player2, player2);
        }
      }).set("targetx", player.getStorage("jun_gezhi_used")).forResult();
      if (result?.bool) {
        if (result.links[0] === "recover") {
          await player.recover();
        }
        if (result.links[0] === "limit") {
          player.addSkill("jun_gezhi_unlimit");
        }
        if (result.links[0] === "damage") {
          player.addSkill("jun_gezhi_used");
          player.markAuto("jun_gezhi_used", result.targets);
          player.line(result.targets, "green");
          await result.targets[0].damage();
        }
      }
    },
    onremove(player) {
      player.removeSkill("jun_gezhi_used");
    },
    subSkill: {
      unlimit: {
        charlotte: true,
        mod: {
          cardUsable: () => Infinity
        },
        trigger: {
          player: "useCard1"
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player) {
          player.removeSkill(event.name);
          if (trigger.addCount !== false) {
            trigger.addCount = false;
            const stat = player.getStat().card;
            const name = trigger.card.name;
            if (typeof stat[name] === "number") {
              stat[name]--;
            }
          }
        },
        mark: true,
        intro: {
          content: "使用的下一张牌无任何次数限制"
        }
      },
      used: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "已对$造成过伤害"
        }
      }
    }
  },
  jun_julian: {
    audio: "jsrgjulian",
    trigger: {
      player: "phaseJieshuBegin"
    },
    zhuSkill: true,
    filter(event, player, name, target) {
      return player.hasZhuSkill("jun_julian") && target.isIn();
    },
    getIndex(event, player) {
      return game.filterPlayer((current) => current !== player && current.group === "qun");
    },
    async cost(event, trigger, player) {
      const target = event.indexedData;
      event.result = await target.chooseBool(get.prompt2(event.skill, target, player)).forResult();
      event.result.targets = [target];
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.draw(2);
      if (target.countCards("he")) {
        await player.gainPlayerCard(target, "he", true);
      }
    }
  }
};
const skills$v = {
  //虎牢神关羽
  pewushen: {
    audio: "wushen",
    mod: {
      cardname(card, player, name) {
        if (get.suit(card) === "heart") {
          return "sha";
        }
      },
      cardnature(card, player) {
        if (get.suit(card) === "heart") {
          return false;
        }
      },
      targetInRange(card) {
        if (card.name === "sha") {
          const suit = get.suit(card);
          if (suit === "heart" || suit === "unsure") {
            return true;
          }
        }
      },
      cardUsable(card) {
        if (card.name === "sha") {
          const suit = get.suit(card);
          if (suit === "heart" || suit === "unsure") {
            return Infinity;
          }
        }
      }
    },
    trigger: { player: "useCard" },
    forced: true,
    filter(event, player) {
      return event.card.name === "sha" && get.suit(event.card) === "heart";
    },
    async content(event, trigger, player) {
      trigger.directHit.addArray(game.players);
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        if (player.stat[player.stat.length - 1].card.sha > 0) {
          player.stat[player.stat.length - 1].card.sha--;
        }
      }
    },
    group: ["pewushen_gain"],
    ai: {
      effect: {
        target(card, player, target, current) {
          if (get.tag(card, "respondSha") && current < 0) {
            return 0.6;
          }
        }
      },
      directHit_ai: true,
      skillTagFilter(player, tag, arg) {
        return arg.card.name === "sha" && get.suit(arg.card) === "heart";
      }
    },
    subSkill: {
      gain: {
        audio: "wushen",
        trigger: {
          global: ["loseAfter", "loseAsyncAfter"]
        },
        filter(event, player) {
          return get.info("pewushen_gain").getCards(event, player)?.length > 0;
        },
        getCards(event, player) {
          const cards2 = [];
          if (event.type !== "discard") {
            return cards2;
          }
          game.filterPlayer2((target) => target !== player, void 0, true).forEach((target) => {
            const cardsx = event.getd(target, "cards2").filter((card) => get.suit(card) === "heart" && get.position(card) === "d");
            if (cardsx.length) {
              cards2.addArray(cardsx);
            }
          });
          return cards2;
        },
        prompt2(event, player) {
          return `获得${get.translation(get.info("pewushen_gain").getCards(event, player))}`;
        },
        check: () => true,
        async content(event, trigger, player) {
          const cards2 = get.info(event.name).getCards(trigger, player);
          await player.gain({ cards: cards2, animate: "gain2" });
        }
      }
    }
  },
  //虎牢神诸葛亮
  pekuangfeng: {
    audio: "kuangfeng",
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return player.getExpansions("qixing").length;
    },
    async cost(event, trigger, player) {
      const {
        bool,
        targets,
        links: cost_data
      } = await player.chooseButtonTarget({
        createDialog: [get.prompt2(event.skill), player.getExpansions("qixing")],
        selectButton: [1, game.countPlayer()],
        filterTarget: true,
        selectTarget() {
          return ui.selected.buttons.length;
        },
        complexSelect: true,
        ai1(button) {
          if (game.hasPlayer((target) => get.attitude(get.player(), target) < 0)) {
            return 1;
          }
          return 0;
        },
        ai2(target) {
          return -get.attitude(get.player(), target);
        }
      }).forResult();
      event.result = {
        bool,
        targets: targets?.sortBySeat(),
        cost_data
      };
    },
    async content(event, trigger, player) {
      const { targets, cost_data: cards2 } = event;
      targets.forEach((target) => {
        target.addAdditionalSkill(`${event.name}_${player.playerid}`, `${event.name}_mark`);
        target.markAuto(`${event.name}_mark`, [player]);
      });
      player.addTempSkill(`${event.name}_effect`, { player: "phaseBeginStart" });
      await player.loseToDiscardpile(cards2);
      await game.doAsyncInOrder(game.filterPlayer(), async (target) => {
        const card = get.autoViewAs({ name: "sha", nature: "fire", isCard: true });
        if (target.hasUseTarget(card, false, false)) {
          await target.chooseUseTarget({ card, addCount: false, nodistance: true, forced: true });
        }
      });
    },
    subSkill: {
      mark: {
        charlotte: true,
        intro: {
          content(storage) {
            return `共有${storage.length}枚标记`;
          }
        },
        ai: {
          effect: {
            target(card, player, target, current) {
              if (get.tag(card, "fireDamage") && current < 0) {
                return 1.5;
              }
            }
          }
        }
      },
      effect: {
        audio: "pekuangfeng",
        trigger: { global: "damageBegin3" },
        filter(event, player) {
          return event.hasNature("fire") && event.player.getStorage("pekuangfeng_mark").includes(player);
        },
        charlotte: true,
        forced: true,
        logTarget: "player",
        async content(event, trigger, player) {
          trigger.num++;
        },
        onremove(player) {
          game.countPlayer2((current) => {
            if (current.getStorage("pekuangfeng_mark").includes(player)) {
              current.unmarkAuto("pekuangfeng_mark", player);
              current.removeAdditionalSkill(`pekuangfeng_${player.playerid}`);
            }
          }, true);
        }
      }
    },
    ai: {
      combo: "qixing"
    }
  },
  petianlin: {
    audio: "dawu",
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return player.getExpansions("qixing").length;
    },
    async cost(event, trigger, player) {
      const {
        bool,
        targets,
        links: cost_data
      } = await player.chooseButtonTarget({
        createDialog: [get.prompt2(event.skill), player.getExpansions("qixing")],
        selectButton: [1, game.countPlayer()],
        filterTarget: true,
        selectTarget() {
          return ui.selected.buttons.length;
        },
        complexSelect: true,
        ai1(button) {
          const { player: player2, allUse } = get.event();
          const targets2 = game.filterPlayer((target) => {
            if (target.isMin() || target.hasSkill("biantian2") || target.hasSkill("petianlin_mark")) {
              return false;
            }
            const att = get.attitude(player2, target);
            if (att >= 4) {
              if (target.hp > 2 && (target.isHealthy() || target.hasSkillTag("maixie"))) {
                return false;
              }
              if (allUse || target.hp === 1) {
                return true;
              }
              if (target.hp === 2 && target.countCards("he") <= 2) {
                return true;
              }
            }
            return false;
          });
          if (ui.selected.buttons.length < targets2.length) {
            return 1;
          }
          return 0;
        },
        ai2(target) {
          const { player: player2, allUse } = get.event();
          if (target.isMin() || target.hasSkill("biantian2") || target.hasSkill("petianlin_mark")) {
            return 0;
          }
          const att = get.attitude(player2, target);
          if (att >= 4) {
            if (target.hp > 2 && (target.isHealthy() || target.hasSkillTag("maixie"))) {
              return 0;
            }
            if (allUse || target.hp === 1) {
              return att;
            }
            if (target.hp === 2 && target.countCards("he") <= 2) {
              return att * 0.7;
            }
            return 0;
          }
          return -1;
        }
      }).set("allUse", player.getExpansions("qixing").length >= game.countPlayer((current) => get.attitude(player, current) > 4) * 2).forResult();
      event.result = {
        bool,
        targets: targets?.sortBySeat(),
        cost_data
      };
    },
    async content(event, trigger, player) {
      const { targets, cost_data: cards2 } = event;
      targets.forEach((target) => {
        target.addAdditionalSkill(`${event.name}_${player.playerid}`, `${event.name}_mark`);
        target.markAuto(`${event.name}_mark`, [player]);
      });
      player.addTempSkill(`${event.name}_effect`, { player: "phaseBeginStart" });
      await player.loseToDiscardpile(cards2);
    },
    ai: {
      combo: "qixing"
    },
    subSkill: {
      effect: {
        audio: "dawu",
        trigger: { global: "damageBegin4" },
        filter(event, player) {
          return !event.hasNature("thunder") && event.player.getStorage("petianlin_mark").includes(player);
        },
        forced: true,
        charlotte: true,
        logTarget: "player",
        async content(event, trigger, player) {
          trigger.cancel();
          await player.draw({ num: player.countExpansions("qixing") });
        },
        onremove(player) {
          game.countPlayer2((current) => {
            if (current.getStorage("petianlin_mark").includes(player)) {
              current.unmarkAuto("petianlin_mark", [player]);
              current.removeAdditionalSkill(`petianlin_${player.playerid}`);
            }
          }, true);
        }
      },
      mark: {
        charlotte: true,
        ai: {
          nofire: true,
          nodamage: true,
          effect: {
            target(card, player, target, current) {
              if (get.tag(card, "damage") && !get.tag(card, "thunderDamage")) {
                return "zeroplayertarget";
              }
            }
          }
        },
        intro: {
          content(storage) {
            return `共有${storage.length}枚标记`;
          }
        }
      }
    }
  },
  //虎牢神吕蒙
  peshelie: {
    audio: "shelie",
    trigger: {
      player: "phaseDrawBegin1",
      source: "damageBegin2"
    },
    filter(event, player) {
      if (event.name === "phaseDraw") {
        return !event.numFixed;
      }
      return true;
    },
    async content(event, trigger, player) {
      if (trigger.name === "phaseDraw") {
        trigger.changeToZero();
      }
      const cards2 = get.cards(5, true);
      await player.showCards(cards2, `${get.translation(player)}发动了【${get.translation(event.name)}】`, true).set("clearArena", false);
      const list = cards2.map((card) => get.suit(card)).unique();
      const result = await player.chooseCardButton(`涉猎：获取花色各不相同的牌`, cards2, list.length, true).set("filterButton", (button) => {
        for (const selectedButton of ui.selected.buttons) {
          if (get.suit(selectedButton.link) === get.suit(button.link)) {
            return false;
          }
        }
        return true;
      }).set("ai", (button) => get.value(button.link, _status.event.player)).forResult();
      game.broadcastAll(ui.clear);
      if (result?.links?.length) {
        await player.gain(result.links, "gain2");
      }
    },
    ai: {
      threaten: 1.2
    }
  },
  pegongxin: {
    audio: "gongxin",
    enable: "phaseUse",
    usable(skill, player) {
      if (get.event().name === "chooseToUse" && get.event().type === "phase") {
        return 1;
      }
      return Infinity;
    },
    filterTarget(card, player, target) {
      return target !== player && target.countCards("h");
    },
    trigger: { player: "damageBegin4" },
    filter(event, player) {
      if (event.name === "damage") {
        return event.source?.isIn() && event.source.countCards("h") > 0;
      }
      return true;
    },
    logTarget: "source",
    check(event, player) {
      return get.attitude(player, event.source) < 0;
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      const cards2 = target.getCards("h");
      const result = await player.chooseToMove_new("攻心").set("list", [
        [`${get.translation(target)}的手牌`, cards2],
        [["弃置"], ["置于牌堆顶"]]
      ]).set(
        "filterOk",
        (moved) => moved[1].slice().concat(moved[2]).filter((card) => get.suit(card) === "heart").length === 1
      ).set("filterMove", (from, to, moved) => {
        if (moved[0].includes(from.link) && moved[1].length + moved[2].length >= 1 && [1, 2].includes(to)) {
          return false;
        }
        return get.suit(from) === "heart";
      }).set("processAI", (list) => {
        const card = list[0][1].slice().filter((card2) => get.suit(card2) === "heart").sort((a, b) => get.value(b) - get.value(a))[0];
        if (!card) {
          return false;
        }
        return [list[0][1].slice().remove(card), [card], []];
      }).forResult();
      if (result.bool) {
        if (result.moved[1].length) {
          await target.discard(result.moved[1]);
        } else {
          await player.showCards(result.moved[2], `${get.translation(player)}对${get.translation(target)}发动了【攻心】`);
          await target.lose(result.moved[2], ui.cardPile, "visible", "insert");
        }
      }
    },
    ai: {
      threaten: 1.5,
      result: {
        target(player, target) {
          return -target.countCards("h");
        }
      },
      order: 10,
      expose: 0.4
    }
  },
  //虎牢神周瑜
  peqinyin: {
    audio: "qinyin",
    trigger: {
      player: "phaseDiscardEnd"
    },
    async cost(event, trigger, player) {
      const result = await player.chooseControl({
        prompt: get.prompt2(event.skill),
        controls: ["失去体力", "回复体力", "cancel2"],
        choice: (() => {
          let recover = 0;
          let lose = 0;
          const players = game.filterPlayer();
          for (const current of players) {
            if (current.hp < current.maxHp) {
              if (get.attitude(player, current) > 0) {
                if (current.hp < 2) {
                  lose--;
                  recover += 0.5;
                }
                lose--;
                recover++;
              } else if (get.attitude(player, current) < 0) {
                if (current.hp < 2) {
                  lose++;
                  recover -= 0.5;
                }
                lose++;
                recover--;
              }
            } else {
              if (get.attitude(player, current) > 0) {
                lose--;
              } else if (get.attitude(player, current) < 0) {
                lose++;
              }
            }
          }
          if (lose > recover && lose > 0) {
            return 0;
          }
          if (lose < recover && recover > 0) {
            return 1;
          }
          return 2;
        })()
      }).forResult();
      if (result.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.index
        };
      }
    },
    async content(event, trigger, player) {
      const { cost_data: index } = event;
      await game.doAsyncInOrder(game.filterPlayer(), async (target) => {
        if (index === 0) {
          return target.loseHp();
        }
        return target.recover();
      });
    },
    ai: {
      expose: 0.1,
      threaten: 2
    }
  },
  peyeyan: {
    audio: "yeyan",
    limited: true,
    enable: "phaseUse",
    async precontent(event, trigger, player) {
      const name = event.name.slice(4);
      const evt = event.getParent();
      const num = 3 - player.countMark("peyeyan_count");
      evt.set("_backupevent", `${name}_backup`);
      evt.set("openskilldialog", `###${get.translation(name)}###你可将至多${num}点火焰伤害分配给至多三名角色，若有角色因此技能死亡，你重置此技能。`);
      evt.backup(`${name}_backup`);
      evt.set("norestore", true);
      evt.set("custom", {
        add: {
          confirm(bool) {
            const event2 = get.event();
            if (bool === false) {
              if (event2.skillDialog && get.objtype(event2.skillDialog) === "div") {
                event2.skillDialog.close();
              }
              if (typeof event2.dialog === "string" && event2.isMine()) {
                event2.dialog = ui.create.dialog(event2.dialog);
              }
              if (_status.event.type === "phase" && ui.confirm) {
                ui.confirm.classList.add("removing");
              }
              event2.restore();
              const cards2 = event2.player.getCards("hej");
              for (const card of cards2) {
                card.recheck("useSkill");
              }
              if (event2.endButton) {
                event2.endButton.close();
                delete event2.endButton;
              }
              game.uncheck();
              event2.custom = {
                add: {},
                replace: {}
              };
              event2.set("peyeyan_exclude", true);
              event2.goto(0);
            }
          }
        },
        replace: {
          target(target, e) {
            const event2 = get.event();
            if (!event2.isMine() || !event2.filterTarget(void 0, event2.player, target)) {
              return;
            }
            if (target.classList.contains("selectable") === false) {
              return;
            }
            target.unprompt();
            target.classList.add("selected");
            ui.selected.targets.push(target);
            const count = get.numOf(ui.selected.targets, target);
            target.prompt(`伤害×${count}`);
            game.check();
          }
        }
      });
      evt.goto(0);
    },
    ai: {
      order(item, player) {
        if (get.event().peyeyan_exclude) {
          return 0;
        }
        return game.hasPlayer((target) => {
          const eff = get.damageEffect(target, player, player, "fire");
          return eff > 0 && 3 - player.countMark("peyeyan_count") >= target.getHp();
        }) ? 10 : 1;
      },
      result: {
        player: 1
      }
    },
    subSkill: {
      backup: {
        audio: "peyeyan",
        line: "fire",
        forceDie: true,
        animationColor: "metal",
        skillAnimation: "legend",
        filterTarget(card, player, target) {
          const { targets } = ui.selected;
          if (targets?.length >= 3 - get.player().countMark("peyeyan_count")) {
            return false;
          }
          return true;
        },
        complexSelect: true,
        selectTarget() {
          const num = 3 - get.player().countMark("peyeyan_count");
          return [1, num + 1];
        },
        ai1: () => 1,
        ai2(target) {
          const eff = get.damageEffect(target, get.player(), get.player(), "fire");
          if (get.numOf(ui.selected.targets, target) >= target.getHp()) {
            return eff;
          }
          return eff + 2 + 1 / target.getHp();
        },
        multitarget: true,
        multiline: true,
        async content(event, trigger, player) {
          const name = event.name.slice(0, -7);
          player.awakenSkill(name);
          const { targets } = event;
          const realTargets = targets.toUniqued();
          await game.doAsyncInOrder(realTargets, async (target) => target.damage({ num: get.numOf(targets, target), nature: "fire" }));
          if (game.hasGlobalHistory("everything", (evt) => {
            if (evt.name !== "die") {
              return false;
            }
            const evtx = evt.reason;
            return evtx?.name === "damage" && (evtx.getParent() === event || !evtx.lianhuanable && evtx.getTrigger()?.getParent() === event);
          })) {
            player.refreshSkill(name);
            player.addMark(`${name}_count`, 1, false);
          }
        }
      },
      count: {
        intro: {
          markcount: (storage) => -storage,
          content: "可分配伤害-#"
        }
      }
    }
  },
  //虎牢神吕布
  peshenwei: {
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player) {
      return player.getHp() > 0;
    },
    async cost(event, trigger, player) {
      const len = Math.min(4, player.getHp());
      event.result = await player.chooseTarget({
        prompt: get.prompt2(event.skill),
        selectTarget: [1, len],
        filterTarget(card, player2, target) {
          return target.countDiscardableCards(target, "he") > 0;
        },
        ai(target) {
          const player2 = get.player();
          if (player2.hp <= ui.selected.targets?.length) {
            return 0;
          }
          return get.effect(target, { name: "guohe_copy2" }, player2, player2);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const { targets } = event;
      await player.loseHp(targets.length);
      await game.doAsyncInOrder(targets, async (target) => {
        const result = await target.chooseToDiscard({
          position: "he",
          selectCard: 2,
          forced: true
        }).forResult();
        const { cards: cards2 } = result;
        if (!cards2.some((card) => get.name(card) === "shan")) {
          await player.turnOver();
          await player.gainPlayerCard({ target, position: "h", forced: true });
        }
      });
    }
  },
  pexiuluo: {
    forced: true,
    mod: {
      cardname(card, player) {
        if (["tao", "jiu"].includes(card.name)) {
          return "sha";
        }
      },
      cardnature(card, player) {
        if (card.name === "tao") {
          return "fire";
        }
        if (card.name === "jiu") {
          return "thunder";
        }
      },
      cardUsable(card, player) {
        if (get.is.ordinaryCard(card) && ["tao", "jiu"].includes(card.cards[0].name) && get.name(card) === "sha") {
          return Infinity;
        }
      }
    },
    trigger: {
      player: ["damageBegin3", "useCard1"],
      source: "damageBegin1"
    },
    filter(event, player) {
      if (event.name === "useCard") {
        return get.name(event.card) === "sha" && event.modSkill.cardname === "pexiuluo" && event.addCount !== false;
      }
      return true;
    },
    async content(event, trigger, player) {
      if (trigger.name === "useCard") {
        trigger.addCount = false;
        const stat = player.getStat().card;
        const name = trigger.card.name;
        if (typeof stat[name] === "number") {
          stat[name]--;
        }
        game.log(trigger.card, "不计入次数");
      } else {
        trigger.num++;
      }
    }
  },
  peduomo: {
    limited: true,
    skillAnimation: true,
    animationColor: "metal",
    trigger: {
      player: "dying"
    },
    check: () => true,
    derivation: ["pedaojue"],
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      if (player.countCards("h") > 0) {
        await player.recoverTo(player.countCards("h"));
      }
      await player.addSkills(get.info(event.name).derivation);
    }
  },
  pedaojue: {
    trigger: {
      player: "useCardToPlayer"
    },
    filter(event, player) {
      return get.name(event.card) === "sha" && event.targets.length === 1 && get.info("pedaojue").getSkills(player).length > 0 && get.info("pedaojue").getSkills(event.target).length > 0;
    },
    getSkills(player) {
      return player.getStockSkills(true, true).filter((i) => !get.info(i).charlotte && player.hasSkill(i, null, false, false));
    },
    async cost(event, trigger, player) {
      const { target } = trigger;
      const list1 = get.info(event.skill).getSkills(player);
      const list2 = get.info(event.skill).getSkills(target);
      const result = await player.chooseButton({
        createDialog: [get.prompt2(event.skill, target), `<div class="text center">你的技能</div>`, [list1.map((i) => [i, player.name]), "skill"], `<div class="text center">${get.translation(target)}的技能</div>`, [list2.map((i) => [i, target.name]), "skill"]],
        selectButton: 2,
        filterButton(button) {
          if (!ui.selected.buttons?.length) {
            return get.event().list1.includes(button.link);
          }
          return get.event().list2.includes(button.link);
        },
        ai(button) {
          return Math.random() - 0.5;
        }
      }).set("list1", list1).set("list2", list2).forResult();
      if (result.bool && result.links?.length) {
        event.result = {
          bool: true,
          cost_data: result.links
        };
      }
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const {
        targets: [target],
        cost_data: list
      } = event;
      const map = player.getStorage(`${event.name}clear`, {});
      map[target.playerid] ??= [[], []];
      map[target.playerid][0].push(list[0]);
      map[target.playerid][1].push(list[1]);
      player.addTempSkill(`${event.name}_clear`);
      player.setStorage(`${event.name}_clear`, map, true);
      await player.changeSkills([list[1]], [list[0]]).set("forceDie", true).set("includeOut", true);
      await target.changeSkills([list[0]], [list[1]]).set("forceDie", true).set("includeOut", true);
    },
    subSkill: {
      clear: {
        charlotte: true,
        intro: {
          content(map, player) {
            if (!map) {
              return "无";
            }
            let str = "";
            for (const id in map) {
              const list = map[id];
              const target = (_status.connectMode ? lib.playerOL : game.playerMap)[id];
              str += `<li>${get.translation(target)}：${get.translation(list[0])}|${get.translation(list[1])}<br>`;
            }
            return str;
          }
        },
        onremove(player, skill) {
          const map = player.getStorage(skill, {});
          if (Object.keys(map)?.length) {
            for (const id in map) {
              const list = map[id];
              const target = (_status.connectMode ? lib.playerOL : game.playerMap)[id];
              player.changeSkills(list[0], list[1]).set("forceDie", true).set("includeOut", true).set("$handle", (player2, addSkill, removeSkill) => {
                player2.addSkillLog(addSkill);
                game.broadcastAll((player3) => {
                  player3.skills.sort((a, b) => {
                    const getNum = (skill2, player4) => {
                      const skills2 = player4.getStockSkills(true, true);
                      return skills2.includes(skill2) ? skills2.indexOf(skill2) : skills2.length;
                    };
                    return getNum(a, player3) - getNum(b, player3);
                  });
                }, player2);
                player2.update();
                player2.removeSkillLog(removeSkill);
              });
              target.changeSkills(list[1], list[0]).set("forceDie", true).set("includeOut", true).set("$handle", (player2, addSkill, removeSkill) => {
                player2.addSkillLog(addSkill);
                game.broadcastAll((player3) => {
                  player3.skills.sort((a, b) => {
                    const getNum = (skill2, player4) => {
                      const skills2 = player4.getStockSkills(true, true);
                      return skills2.includes(skill2) ? skills2.indexOf(skill2) : skills2.length;
                    };
                    return getNum(a, player3) - getNum(b, player3);
                  });
                }, player2);
                player2.update();
                player2.removeSkillLog(removeSkill);
              });
            }
            delete player.storage[skill];
          }
        }
      }
    }
  }
};
const skills$u = {
  //项羽
  pegaishi: {
    audio: 2,
    forced: true,
    trigger: {
      player: "useCard",
      global: "damageBegin4"
    },
    filter(event, player) {
      const num = player.getSeatNum();
      if (event.name == "damage") {
        return event.source == player && (event.player.getSeatNum() > num || player.hp == 1);
      }
      return game.hasPlayer((current) => current.getSeatNum() < num || player.hp == 1);
    },
    logTarget(event, player) {
      const num = player.getSeatNum();
      return event.name == "damage" ? event.player : game.filterPlayer((current) => current.getSeatNum() < num || player.hp == 1).sortBySeat();
    },
    async content(event, trigger, player) {
      if (trigger.name == "damage") {
        trigger.num++;
      } else {
        const targets = event.targets;
        trigger.directHit.addArray(targets);
        game.log(targets, "不可响应", trigger.card);
      }
    }
  },
  pefuqin: {
    audio: 2,
    derivation: ["dcfencheng", "jsrgyansha"],
    dutySkill: true,
    frequent: true,
    trigger: { player: "phaseUseBegin" },
    async content(event, trigger, player) {
      const num = 1 + Array.from(ui.discardPile.childNodes).filter((card) => get.number(card) == 13).length + game.filterPlayer().flatMap((current) => current.getCards("ej", (card) => get.number(card) == 13)).length;
      await player.draw({ num });
      player.addTempSkill(event.name + "_use", "phaseAnyAfter");
      player.addMark(event.name + "_use", num, false);
    },
    group: ["pefuqin_achieve"],
    subSkill: {
      achieve: {
        audio: "pefuqin",
        forced: true,
        locked: false,
        skillAnimation: true,
        animationColor: "wood",
        trigger: { global: "die" },
        async content(event, trigger, player) {
          if (trigger.source == player) {
            player.awakenSkill("pefuqin");
          }
          await player.addSkills(["dcfencheng", "jsrgyansha"]);
          game.broadcastAll((skill) => {
            const info = get.info(skill);
            if (info) {
              info.audioname2 ??= {};
              info.audioname2.pe_xiangyu = "pefuqin";
            }
          }, "dcfencheng");
        }
      },
      use: {
        charlotte: true,
        onremove: true,
        intro: { content: "本阶段使用的下#张牌无次数限制" },
        mod: {
          cardUsable: () => Infinity
        },
        silent: true,
        firstDo: true,
        popup: false,
        trigger: { player: "useCard" },
        filter(event, player) {
          return player.hasMark("pefuqin_use");
        },
        async content(event, trigger, player) {
          player.removeMark(event.name);
          if (trigger.addCount != false) {
            const stat = player.getStat("card"), name = trigger.card.name;
            if (typeof stat[name] == "number" && stat[name] > 0) {
              stat[name]--;
            }
          }
          if (!player.hasMark(event.name)) {
            player.removeSkill(event.name);
          }
        }
      }
    }
  },
  peyinghao: {
    audio: 2,
    zhuSkill: true,
    locked: false,
    mod: {
      attackRange(player, num) {
        return num + game.countPlayer((current) => current.group == player.group);
      }
    }
  },
  //上官婉儿
  pecaishi: {
    audio: 2,
    enable: "chooseToUse",
    filter(event, player) {
      return player.hasCards("hes", (card) => {
        if (player.getStorage("pecaishi_used").includes(get.cardNameLength(card))) {
          return false;
        }
        return get.inpileVCardList((info) => {
          if (!["basic", "trick"].includes(info[0])) {
            return false;
          }
          const cardx = get.autoViewAs({ name: info[2], nature: info[3] }, [card]);
          return get.cardNameLength(cardx) === get.cardNameLength(card) && event.filterCard(cardx, player, event);
        }).length;
      });
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (!["basic", "trick"].includes(info[0])) {
            return false;
          }
          return player.hasCards("hes", (card) => {
            if (player.getStorage("pecaishi_used").includes(get.cardNameLength(card))) {
              return false;
            }
            const cardx = get.autoViewAs({ name: info[2], nature: info[3] }, [card]);
            return get.cardNameLength(cardx) === get.cardNameLength(card) && event.filterCard(cardx, player, event);
          });
        });
        return ui.create.dialog("才诗", [list, "vcard"]);
      },
      filter(button, player) {
        return _status.event.getParent().filterCard(
          {
            name: button.link[2],
            nature: button.link[3]
          },
          player,
          _status.event.getParent()
        );
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = get.player();
        if (["wugu", "zhulu_card", "yiyi", "lulitongxin", "lianjunshengyan", "diaohulishan"].includes(button.link[2])) {
          return 0;
        }
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard(card, player2) {
            return get.cardNameLength(card) === get.cardNameLength(lib.skill.pecaishi_backup.name);
          },
          name: links[0][2],
          audio: "pecaishi",
          popname: true,
          check(card) {
            return 10 - get.value(card);
          },
          position: "hse",
          viewAs: {
            name: links[0][2],
            nature: links[0][3]
          },
          log: false,
          async precontent(event, trigger, player2) {
            player2.logSkill("pecaishi");
            player2.addTempSkill("pecaishi_used");
            player2.markAuto("pecaishi_used", [get.cardNameLength(event.result.card)]);
          }
        };
      },
      prompt(links, player) {
        return `将一张字数相同的牌当做${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用`;
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name)) {
        return false;
      }
      const type = get.type(name);
      return ["basic", "trick"].includes(type) && player.hasCards("hes", (card) => get.cardNameLength(card) === get.cardNameLength(name)) && !player.getStorage("pecaishi_used").includes(get.cardNameLength(name));
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player, tag, arg) {
        if (arg === "respond") {
          return false;
        }
        const name = tag === "respondSha" ? "sha" : "shan";
        return lib.skill["pecaishi"].hiddenCard(player, name);
      },
      order: 9,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      backup: {},
      used: { charlotte: true, onremove: true, intro: { content: "本回合已使用过$点的牌" } }
    }
  },
  peculv: {
    audio: 2,
    forced: true,
    init(player, skill) {
      player.addSkill(`${skill}_mark`);
      game.addGlobalSkill(`${skill}_ai`);
    },
    onremove(player, skill) {
      player.removeSkill(`${skill}_mark`);
      game.countPlayer((current) => {
        current.removeSkill(`${skill}_mark2`);
      });
      if (!game.hasPlayer((i) => i.hasSkill(skill, null, null, false), true)) {
        game.removeGlobalSkill(`${skill}_ai`);
      }
    },
    trigger: { global: "useCard" },
    filter(event, player) {
      if (event.player !== _status.currentPhase) {
        return false;
      }
      const history = game.getAllGlobalHistory("useCard");
      const index = history.indexOf(event);
      if (index < 1) {
        return false;
      }
      const evt = history[index - 1];
      return get.cardNameLength(evt.card) === get.cardNameLength(event.card) || get.type2(evt.card) === get.type2(event.card);
    },
    async content(event, trigger, player) {
      await player.draw();
      trigger.player.addTempSkill(`${event.name}_mark2`);
      trigger.player.addMark(`${event.name}_mark2`, 1, false);
    },
    group: "peculv_effect",
    subSkill: {
      mark2: { charlotte: true, onremove: true, intro: { content: "本回合已触发过#次促律" } },
      ai: {
        mod: {
          aiOrder(player, card, num) {
            const history = game.getAllGlobalHistory("useCard");
            const index = history.length;
            if (index < 1) {
              return;
            }
            const evt = history[index - 1];
            if (get.itemtype(card) === "card" && (get.type2(card) === get.type2(evt.card) || get.cardNameLength(card) === get.cardNameLength(evt.card)) && game.hasPlayer((current) => {
              return current.hasSkill("peculv") && get.attitude(player, current) > 0;
            })) {
              return num + 114514;
            }
          }
        },
        trigger: {
          player: "dieAfter"
        },
        filter: () => {
          return !game.hasPlayer((i) => i.hasSkill("peculv", null, null, false), true);
        },
        silent: true,
        forceDie: true,
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          game.removeGlobalSkill(event.name);
        }
      },
      effect: {
        forced: true,
        trigger: { global: "phaseEnd" },
        filter(event, player) {
          let num = 0;
          game.countPlayer((current) => {
            num += current.countHistory("useSkill", (evt) => evt.skill === "peculv");
          });
          return num >= 3;
        },
        logTarget: "player",
        async content(event, trigger, player) {
          const target = event.targets[0];
          await target.recover();
          await target.draw(2);
        }
      },
      mark: {
        charlotte: true,
        silent: true,
        init(player, skill) {
          const history = game.getAllGlobalHistory("useCard");
          const length = history.length;
          if (!length) {
            return;
          }
          const card = history[length - 1].card;
          player.storage[skill] = card;
          player.markSkill(skill);
          game.broadcastAll(
            (player2, type) => {
              if (player2.marks.peculv_mark) {
                player2.marks.peculv_mark.firstChild.innerHTML = get.translation(type).slice(0, 1);
              }
            },
            player,
            get.type2(card)
          );
        },
        onremove(player, skill) {
          delete player.storage[skill];
        },
        intro: {
          markcount(storage, player) {
            return get.cardNameLength(storage);
          },
          content(storage, player) {
            let str = "";
            str += `<li>类型：${get.translation(get.type2(storage))}`;
            str += `<br><li>牌名字数：${get.cardNameLength(storage)}`;
            return str;
          }
        },
        trigger: { global: "useCard1" },
        async content(event, trigger, player) {
          lib.skill[event.name].init(player, event.name);
        }
      }
    }
  },
  //武则天
  peersheng: {
    audio: 2,
    forced: true,
    locked: false,
    trigger: {
      global: ["gainAfter", "loseAsyncAfter", "phaseEnd"]
    },
    filter(event, player) {
      const target = game.findPlayer((current) => current.getSeatNum() === 1);
      if (!target?.isIn()) {
        return false;
      }
      if (event.name === "phase") {
        const cards3 = target.getHistory("useCard").filter((evt) => ["basic", "trick"].includes(get.type(evt.card))).flatMap((evt) => get.autoViewAs({ name: evt.card.name, nature: evt.card.nature, isCard: true }, "unsure")).unique();
        return cards3.some((card) => player.hasUseTarget(card));
      }
      const cards2 = event.getg?.(target);
      return event.getParent(2)?.name !== "peersheng" && cards2?.length > 0;
    },
    async content(event, trigger, player) {
      const target = game.findPlayer((current) => current.getSeatNum() === 1);
      if (trigger.name === "phase") {
        const cards2 = target.getHistory("useCard").filter((evt) => ["basic", "trick"].includes(get.type(evt.card))).flatMap((evt) => get.autoViewAs({ name: evt.card.name, nature: evt.card.nature, isCard: true }, "unsure"));
        while (true) {
          if (cards2.some((card) => player.hasUseTarget(card))) {
            const result = await player.chooseButton({
              createDialog: ["二圣：你可以视为使用一张牌", [cards2, "vcard"]],
              filterButton(button, player2) {
                const card = button.link;
                return player2.hasUseTarget(card);
              },
              ai(button) {
                const player2 = get.player();
                const card = button.link;
                if (card.name === "jiu") {
                  return 114514;
                }
                return player2.getUseValue(card);
              }
            }).forResult();
            if (result?.bool && result.links?.length) {
              const card = result.links[0];
              cards2.removeArray(cards2.filter((cardx) => cardx.name === card.name));
              await player.chooseUseTarget(card, true, false);
            } else {
              break;
            }
          } else {
            break;
          }
        }
      } else {
        await player.draw({ num: trigger.getg(target).length });
      }
    }
  },
  pelinchao: {
    audio: 2,
    limited: true,
    derivation: "penvdi",
    skillAnimation: true,
    animationColor: "wood",
    trigger: { global: "dieBegin" },
    filter(event, player) {
      const target = game.findPlayer((current) => current.getSeatNum() === 1);
      return event.player === target;
    },
    check(event, player) {
      return get.attitude(player, event.player) > 0 || player === event.player;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      if (player === trigger.player) {
        trigger.cancel();
      }
      await player.recoverTo(player.maxHp);
      let evt = _status.event.getParent("phaseUse");
      if (evt && evt.name === "phaseUse") {
        evt.skipped = true;
      }
      evt = event.getParent("phase", true);
      if (evt) {
        if (_status.currentPhase) {
          game.log(_status.currentPhase, "结束了回合");
        }
        evt.num = evt.phaseList.length;
        evt.goto(11);
      }
      await player.addSkills("penvdi");
      player.insertPhase();
      if (player !== trigger.player && player.identity !== trigger.player.identity && ["identity", "doudizhu", "brawl"].includes(get.mode())) {
        game.broadcastAll(
          (player2, target) => {
            player2.identity = target.identity;
            if (target.isZhu) {
              delete target.isZhu;
            }
            if (player2.identity === "zhu") {
              game.zhu = player2;
            }
            player2.showIdentity();
          },
          player,
          trigger.player
        );
        if (player.identity === "zhu") {
          await event.trigger("zhuUpdate");
        }
      }
    }
  },
  penvdi: {
    audio: 2,
    zhuSkill: true,
    forced: true,
    trigger: {
      global: ["phaseDrawBegin2", "damageBegin3"],
      player: "useCardToPlayer"
    },
    filter(event, player) {
      if (event.name === "useCardToPlayer") {
        return event.target.hasAllHistory("useCard", (evt) => get.is.damageCard(evt.card) && evt.targets?.includes(player));
      }
      if (!event.player.hasSex("female")) {
        return false;
      }
      if (event.name === "damage") {
        return game.getGlobalHistory("everything", (evt) => evt.name === "damage" && evt.player === event.player, event).indexOf(event) === 0;
      }
      return !event.numFixed;
    },
    logTarget(event, player) {
      return event.name === "useCardToPlayer" ? event.target : event.player;
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      if (trigger.name === "useCardToPlayer") {
        if (trigger.getParent().addCount !== false) {
          trigger.getParent().addCount = false;
          const stat = player.getStat("card");
          const name = trigger.card.name;
          if (typeof stat[name] === "number" && stat[name] > 0) {
            stat[name]--;
          }
        }
        const num = player.countAllHistory("useCard", (evt) => evt.targets?.includes(target)) + target.countAllHistory("useCard", (evt) => evt.targets?.includes(player));
        if (num > 0) {
          await target.loseMaxHp(num);
        }
      } else if (trigger.name === "damage") {
        trigger.cancel();
      } else {
        trigger.num++;
      }
    },
    mod: {
      cardUsableTarget(card, player, target) {
        if (target.hasAllHistory("useCard", (evt) => get.is.damageCard(evt.card) && evt.targets?.includes(player))) {
          return Infinity;
        }
      }
    }
  },
  //PE刘徽
  pejieshu: {
    audio: "dcjieshu",
    locked: false,
    mod: {
      aiOrder(player, card, num) {
        if (get.itemtype(card) === "card" && card.hasGaintag("pejieshu_tag")) {
          return num + 0.1;
        }
      },
      aiValue(player, card, num) {
        if (get.itemtype(card) === "card" && card.hasGaintag("pejieshu_tag")) {
          return num * 1.1;
        }
      },
      aiUseful() {
        return lib.skill.pejieshu.mod.aiValue.apply(this, arguments);
      }
    },
    trigger: { player: "useCardToPlayered" },
    onremove(player, skill) {
      player.removeGaintag(`${skill}_tag`);
    },
    check(event, player) {
      return get.type(event.card) !== "equip";
    },
    filter(event, player) {
      const cards2 = player.getHistory("lose", (evt) => evt.getParent() === event.getParent() && evt.cards2?.length && Object.values(evt.gaintag_map).flat().includes("pejieshu_tag")).flatMap((evt) => evt.cards2 || []);
      return event.cards?.filterInD("od")?.some((card) => !cards2.includes(card));
    },
    prompt2(event, player) {
      const cards2 = player.getHistory("lose", (evt) => evt.getParent() === event.getParent() && evt.cards2?.length && Object.values(evt.gaintag_map).flat().includes("pejieshu_tag")).map((evt) => evt.cards2).reduce((a, b) => b.addArray(a), []);
      return `获得${get.translation(event.cards?.filterInD("od")?.filter((card) => !cards2.includes(card)))}`;
    },
    async content(event, trigger, player) {
      const cardx = player.getHistory("lose", (evt) => evt.getParent() === trigger.getParent() && evt.cards2?.length && Object.values(evt.gaintag_map).flat().includes("pejieshu_tag")).map((evt) => evt.cards2).reduce((a, b) => b.addArray(a), []);
      const cards2 = trigger.cards?.filterInD("od")?.filter((card) => !cardx.includes(card));
      const targets = trigger.targets;
      await player.gain({ cards: cards2, animate: "gain2", gaintag: ["pejieshu_tag"] });
      player.addTempSkill(`${event.name}_effect2`, "roundStart");
      cards2.forEach((card) => {
        player.markAuto(`${event.name}_effect2`, [[card, targets]]);
      });
      player.addTempSkill(`${event.name}_effect`);
      player.markAuto(`${event.name}_effect`, cards2);
    },
    subSkill: {
      tag: {},
      effect: {
        charlotte: true,
        onremove: true,
        mod: {
          targetInRange(card, player) {
            if ([card].concat(card.cards || []).some((cardx) => get.itemtype(cardx) === "card" && player.getStorage("pejieshu_effect").includes(cardx))) {
              return true;
            }
          }
        }
      },
      effect2: {
        charlotte: true,
        onremove: true,
        //感谢戏佬qwq
        mod: {
          playerEnabled(card, player, target) {
            if ([card].concat(card.cards || []).some((cardx) => get.itemtype(cardx) === "card" && player.getStorage("pejieshu_effect2").some(([cardxx, targets]) => targets.includes(target) && cardx === cardxx))) {
              return false;
            }
          }
        }
      }
    }
  },
  pegeyuan: {
    audio: "dcgeyuan",
    forced: true,
    init(player, skill) {
      player.addSkill(`${skill}_mark`);
    },
    onremove(player, skill) {
      player.removeSkill(`${skill}_mark`);
    },
    trigger: { player: "useCardToPlayer" },
    filter(event, player) {
      return player.storage.pegeyuan_mark && player.hasHistory("lose", (evt) => evt.getParent() === event.getParent() && evt.cards2?.length && Object.values(evt.gaintag_map).flat().includes("pejieshu_tag"));
    },
    async content(event, trigger, player) {
      const target = trigger.target;
      if (get.distance(player, target) !== get.distance(player, player.storage.pegeyuan_mark) && player.hasDiscardableCards(player, "he")) {
        await player.chooseToDiscard({ forced: true, position: "he", selectCard: 2 });
      } else {
        await player.draw({ num: 2 });
      }
      if (!player.getStorage(event.name).some((i) => i[0] === target && i[1] === player.storage.pegeyuan_mark || i[1] === target && i[0] === player.storage.pegeyuan_mark)) {
        player.markAuto(event.name, [[target, player.storage.pegeyuan_mark]]);
      }
    },
    subSkill: {
      mark: {
        charlotte: true,
        silent: true,
        lastDo: true,
        trigger: { player: "useCardToPlayer" },
        intro: {
          content(storage, player) {
            let str = "当前没有“割圆”目标";
            if (player.storage.pegeyuan_mark) {
              str = `上个割圆目标为${get.translation(player.storage.pegeyuan_mark)}`;
            }
            if (player.getStorage("pegeyuan").length) {
              str += "<br>割圆组：";
              for (const i of player.getStorage("pegeyuan")) {
                str += `<li>${get.translation(i)}`;
              }
            }
            return str;
          }
        },
        onremove(player, skill) {
          player.setStorage(skill, false, true);
          player.removeTip(skill);
        },
        filter(event, player) {
          return player.hasHistory("lose", (evt) => evt.getParent() === event.getParent() && evt.cards2?.length && Object.values(evt.gaintag_map).flat().includes("pejieshu_tag"));
        },
        async content(event, trigger, player) {
          const target = trigger.target;
          if (!player.storage[event.name]) {
            player.setStorage(event.name, target, true);
            player.addTip(event.name, `${get.translation(event.name)}：${get.translation(target)}`);
          } else {
            player.storage[event.name] = false;
            player.removeTip(event.name);
          }
        }
      }
    },
    ai: { combo: "pejieshu" }
  },
  pegusuan: {
    audio: "dcgusuan",
    juexingji: true,
    forced: true,
    skillAnimation: true,
    animationColor: "purple",
    trigger: { player: "pegeyuanAfter" },
    filter(event, player) {
      const cards2 = player.getAllHistory("lose", (evt) => evt.type === "discard" && evt.getParent(3)?.name === "pegeyuan" && evt.cards2?.length).map((evt) => evt.cards2).reduce((a, b) => b.addArray(a), []);
      return player.getStorage("pegeyuan").length >= 3 && cards2.length < 4;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.draw(5);
      const targets = game.filterPlayer((current) => current !== player);
      if (!targets.length) {
        return;
      }
      const result = targets.length > 2 ? await player.chooseTarget({
        forced: true,
        prompt: "选择两名其他角色与你的距离互为1",
        filterTarget: lib.filter.notMe,
        selectTarget: 2,
        ai(target) {
          return -get.attitude(get.player(), target);
        }
      }).forResult() : { bool: true, targets };
      if (result?.bool && result.targets?.length) {
        const targets2 = result.targets;
        player.line(targets2);
        player.addSkill(`${event.name}_effect`);
        player.markAuto(`${event.name}_effect`, targets2);
        targets2.forEach((target) => {
          target.addSkill(`${event.name}_effect`);
          target.markAuto(`${event.name}_effect`, [player]);
        });
        player.addSkill(`${event.name}_effect2`);
      }
    },
    ai: { combo: ["pejieshu", "pegeyuan"] },
    subSkill: {
      effect: {
        intro: { content: "与$距离互为1" },
        mod: {
          globalFrom(from, to) {
            if (from.getStorage("pegusuan_effect").includes(to)) {
              return -Infinity;
            }
          }
        }
      },
      effect2: {
        mod: {
          cardUsableTarget(card, player, target) {
            if (get.distance(player, target) <= 1) {
              return Infinity;
            }
          }
        }
      }
    }
  },
  //白起
  pewuan: {
    audio: 2,
    forced: true,
    trigger: { source: "damageSource" },
    logTarget: "player",
    filter(event, player) {
      return event.num > 0;
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      player.addSkill(`${event.name}_dam`);
      player.addMark(`${event.name}_dam`, 1, false);
      player.markAuto(`${event.name}_dam2`, [target]);
      target.addTempSkill(`${event.name}_fengyin`);
    },
    subSkill: {
      dam: {
        audio: "pewuan",
        charlotte: true,
        forced: true,
        onremove: true,
        mod: {
          targetInRange(card, player, target) {
            if (player.getStorage("pewuan_dam2").includes(target)) {
              return true;
            }
          },
          cardUsableTarget(card, player, target) {
            if (player.getStorage("pewuan_dam2").includes(target)) {
              return Infinity;
            }
          }
        },
        intro: {
          content(storage, player) {
            let str = `<li>造成伤害+${storage}`;
            if (player.getStorage("pewuan_dam2").length) {
              str += `<li>对${get.translation(player.getStorage("pewuan_dam2"))}使用牌无距离与次数限制`;
            }
            return str;
          }
        },
        trigger: {
          source: "damageBegin2",
          player: "useCard1"
        },
        filter(event, player) {
          return event.name === "damage" || event.addCount !== false && event.targets?.some((target) => player.getStorage("pewuan_dam2").includes(target));
        },
        async content(event, trigger, player) {
          if (trigger.name === "damage") {
            trigger.num += player.countMark(event.name);
          } else {
            trigger.addCount = false;
            const stat = player.getStat("card");
            const name = trigger.card.name;
            if (typeof stat[name] === "number" && stat[name] > 0) {
              stat[name]--;
            }
          }
        }
      },
      fengyin: {
        charlotte: true,
        inherit: "fengyin"
      }
    }
  },
  peshashen: {
    audio: 2,
    forced: true,
    trigger: { source: "dying" },
    logTarget: "player",
    filter(event, player) {
      return player !== event.player;
    },
    async content(event, trigger, player) {
      trigger.cancel();
      await event.targets[0].die(trigger);
      player.when({ global: ["phaseAfter", "phaseBefore"] }).step(async (event2, trigger2, player2) => {
        player2.removeSkill(event2.name);
        if (event2.triggername === "phaseAfter") {
          player2.insertPhase();
        }
      });
    }
  },
  //花木兰
  perongbian: {
    audio: 2,
    forced: true,
    trigger: { player: "useCardAfter" },
    filter(event, player) {
      return get.type(event.card) === "equip";
    },
    async content(event, trigger, player) {
      if (!_status.characterlist) {
        game.initCharacterList();
      }
      _status.characterlist.randomSort();
      const list = _status.characterlist.filter((character) => get.character(character, 0) === "female");
      if (!list.length) {
        player.popup("没有符合条件的武将牌");
        return;
      }
      const name = list.randomGet();
      get.info(event.name).addVisitors([name], player);
      const skills2 = get.character(name).skills.filter((skill) => !player.hasSkill(skill, null, null, false));
      if (skills2.length) {
        const list2 = skills2.map((skill) => [
          skill,
          html`
						<div class="popup text" style="width:calc(100% - 10px);display:inline-block">
							<div class="skill">【${get.translation(skill)}】</div>
							<div>${lib.translate[`${skill}_info`]}</div>
						</div>
					`
        ]);
        const result = await player.chooseButton({
          forced: true,
          createDialog: ["戎弁：选择获得一个技能", [list2, "textbutton"]],
          ai(button) {
            return 1 + Math.random();
          }
        }).forResult();
        if (result?.bool && result.links?.length) {
          const skill = result.links[0];
          await player.addAdditionalSkills(event.name, skill);
          lib.card[`huashen_card_${name}`].skills.push(skill);
        }
      }
    },
    onremove(player, skill) {
      if (player.getStorage(skill).length) {
        get.info(skill).removeVisitors(player.getStorage(skill), player);
      }
    },
    addVisitors(characters2, player) {
      _status.characterlist.removeArray(characters2);
      game.log(player, "将", `#y${get.translation(characters2)}`, "加入了", "#g“戎弁”");
      game.broadcastAll(
        (player2, characters3) => {
          player2.tempname.addArray(characters3);
          player2.$draw(
            characters3.map((name) => {
              const cardname = `huashen_card_${name}`;
              lib.card[cardname] = {
                fullimage: true,
                image: `character:${name}`,
                skills: []
              };
              lib.translate[cardname] = get.rawName2(name);
              return game.createCard(cardname, " ", " ");
            }),
            "nobroadcast"
          );
        },
        player,
        characters2
      );
      player.markAuto("perongbian", characters2);
    },
    removeVisitors(characters2, player) {
      if (Array.isArray(player.tempname)) {
        game.broadcastAll((player2, characters3) => player2.tempname.removeArray(characters3), player, characters2);
      }
      player.unmarkAuto("perongbian", characters2);
      _status.characterlist.addArray(characters2);
      game.log(player, "移去了", `#y${get.translation(characters2)}`);
      for (const character of characters2) {
        const skills2 = lib.card[`huashen_card_${character}`]?.skills;
        if (skills2?.length) {
          player.removeSkills(skills2);
        }
      }
    },
    intro: {
      name: "戎弁",
      mark(dialog, storage, player) {
        if (!storage || !storage.length) {
          return "当前没有女将牌";
        }
        dialog.addSmall([storage, "character"]);
      }
    }
  },
  peliezhan: {
    audio: 2,
    enable: ["chooseToUse"],
    filter(event, player) {
      if (!player.getStorage("perongbian").length) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (!["basic", "trick"].includes(info[0])) {
          return false;
        }
        return event.filterCard(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3],
              storage: { peliezhan: true }
            },
            "unsure"
          ),
          player,
          event
        );
      }).length > 0;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (!["basic", "trick"].includes(info[0])) {
            return false;
          }
          return event.filterCard(
            get.autoViewAs(
              {
                name: info[2],
                nature: info[3],
                storage: { peliezhan: true }
              },
              "unsure"
            ),
            player,
            event
          );
        });
        return ui.create.dialog("烈瞻", [list, "vcard"]);
      },
      filter(button, player) {
        return _status.event.getParent().filterCard(
          {
            name: button.link[2],
            storage: { peliezhan: true }
          },
          player,
          _status.event.getParent()
        );
      },
      prompt(links, player) {
        return `将一张女将牌当做${get.translation(links[0][3] || "")}${get.translation(links[0][2])}使用`;
      },
      backup(links, player) {
        return {
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            storage: { peliezhan: true }
          },
          selectCard: -1,
          filterCard: () => false,
          popname: true,
          log: false,
          manualConfirm: true,
          async precontent(event, trigger, player2) {
            event.getParent().addCount = false;
            const characters2 = player2.getStorage("perongbian").slice(0);
            const result = characters2.length > 1 ? await player2.chooseButton({
              createDialog: ["烈瞻：选择一张女将牌", [characters2, "character"]],
              forced: true,
              ai(button) {
                return 1 + Math.random();
              }
            }).forResult() : { bool: true, links: characters2 };
            if (result?.bool && result.links?.length) {
              player2.logSkill("peliezhan");
              get.info("perongbian").removeVisitors(result.links, player2);
              player2.when("useCardAfter").filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player3) => {
                await player3.draw({ num: 2 });
              });
            }
          }
        };
      }
    },
    hiddenCard(player, name) {
      if (!["trick", "basic"].includes(get.type(name))) {
        return false;
      }
      return player.getStorage("perongbian").length;
    },
    locked: false,
    mod: {
      cardUsable(card) {
        if (card?.storage?.peliezhan) {
          return Infinity;
        }
      },
      targetInRange(card) {
        if (card?.storage?.peliezhan) {
          return true;
        }
      }
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player) {
        if (!player.getStorage("perongbian").length) {
          return false;
        }
      },
      order: 7,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: { backup: {} }
  },
  petijun: {
    audio: 2,
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    enable: "phaseUse",
    filter(event, player) {
      return game.hasPlayer((current) => get.info("petijun").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return target.hasSex("male") && target.hasGainableCards(player, "e");
    },
    async content(event, trigger, player) {
      const { target } = event;
      player.awakenSkill(event.name);
      const cards2 = target.getGainableCards(player, "e");
      await player.gain({
        cards: cards2,
        source: target,
        animate: "giveAuto",
        bySelf: true
      });
      await player.recoverTo(player.maxHp);
      await target.recoverTo(target.maxHp);
      player.addSkill(`${event.name}_die`);
      player.markAuto(`${event.name}_die`, [target]);
    },
    ai: {
      order: 13,
      result: {
        player: 1,
        target(player, target) {
          if (game.hasPlayer((current) => get.attitude(current, player) < 0 && current.hasGainableCards(player, "e") && current.getDamagedHp() < 2)) {
            return -target.countGainableCards(player, "e") / Math.min(1, target.getDamagedHp());
          }
          return target.countGainableCards(player, "e");
        }
      }
    },
    subSkill: {
      die: {
        forced: true,
        charlotte: true,
        onremove: true,
        audio: "petijun",
        mark: true,
        intro: { content: "杀死$外的角色后重置此技能" },
        trigger: { source: "dieEnd" },
        filter(event, player) {
          return !player.getStorage("petijun_die").includes(event.player) && player.awakenedSkills.includes("petijun");
        },
        async content(event, trigger, player) {
          player.removeSkill(event.name);
          player.restoreSkill("petijun");
          game.log(player, "重置了", "#g【替君】");
        }
      }
    }
  },
  //杨玉环
  peyichuan: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    chooseButton: {
      dialog(event, player) {
        return ui.create.dialog(get.prompt2("peyichuan"), "hidden");
      },
      chooseControl(event, player) {
        return ["basic", "trick", "equip", "cancel2"];
      },
      check(event, player) {
        return "basic";
      },
      backup(result, player) {
        return {
          audio: "peyichuan",
          control: result.control,
          async content(event, trigger, player2) {
            const { control: type } = get.info(event.name);
            const targets = game.filterPlayer((current) => current !== player2).sortBySeat();
            player2.line(targets);
            const map = await game.chooseAnyOL(targets, get.info("peyichuan").chooseToGive, [player2, type]).forResult();
            if (!map.size) {
              return;
            }
            const cards2 = [];
            let num = 0;
            const gainMax = [];
            for (const target of Array.from(map.keys())) {
              const result2 = map.get(target);
              if (result2?.bool && result2.cards?.length) {
                cards2.addArray(result2.cards);
                if (result2.cards.length >= num) {
                  if (result2.cards.length > num) {
                    num = result2.cards.length;
                    gainMax.length = 0;
                  }
                  gainMax.push(target);
                }
              } else {
                target.addSkill("peyichuan_direct");
              }
            }
            if (cards2.length) {
              await player2.gain({
                cards: cards2,
                animate: "giveAuto"
              });
            }
            const next = game.createEvent("PeyichuanAfter", false);
            next.player = player2;
            next.targets = gainMax;
            next.setContent("emptyEvent");
          }
        };
      }
    },
    chooseToGive(target, player, type) {
      const next = target.chooseCard({
        prompt: `驿传：是否交给${get.translation(player)}任意张${get.translation(type)}牌`,
        position: "he",
        filterCard(card) {
          return get.type2(card) === get.event().type;
        },
        selectCard: [1, Infinity],
        allowChooseAll: true
      });
      next.set("type", type);
      next.set("att", get.attitude(target, player));
      next.set("ai", (card) => {
        const { att, player: player2 } = get.event();
        if (att > 0) {
          return 15 - get.value(card);
        }
        if (!player2.hasSkill("peyichuan_direct")) {
          return 5 - get.value(card);
        }
        return 0;
      });
      return next;
    },
    ai: {
      order: 9,
      result: {
        player: 1
      }
    },
    subSkill: {
      backup: {},
      direct: {
        silent: true,
        charlotte: true,
        mark: true,
        intro: {
          content: "不能响应1号位使用的牌"
        },
        trigger: { global: "useCard" },
        filter(event, player) {
          return event.player.getSeatNum() === 1;
        },
        async content(event, trigger, player) {
          trigger.directHit.add(player);
        }
      }
    }
  },
  pexiuhua: {
    audio: 2,
    forced: true,
    trigger: {
      player: ["gainAfter", "PeyichuanAfter"],
      global: "loseAsyncAfter"
    },
    filter(event, player) {
      if (event.name === "PeyichuanAfter") {
        return event.targets?.length > 0;
      }
      return game.hasPlayer((current) => {
        if (current === player) {
          return false;
        }
        return event.getl?.(current)?.cards2?.some((card) => event.getg?.(player)?.includes(card));
      });
    },
    getIndex(event, player) {
      if (event.name === "PeyichuanAfter") {
        return 1;
      }
      if (event.name === "loseAsync" && event.type !== "gain") {
        return [];
      }
      return game.filterPlayer((current) => {
        if (current === player) {
          return false;
        }
        return event.getl?.(current)?.cards2?.some((card) => event.getg?.(player)?.includes(card));
      });
    },
    logTarget(event, player, name, target) {
      if (event.name === "PeyichuanAfter") {
        return event.targets.sortBySeat();
      }
      return target;
    },
    async content(event, trigger, player) {
      if (trigger.name === "PeyichuanAfter") {
        await game.doAsyncInOrder(trigger.targets.concat([player]), async (target) => {
          await target.recover();
          await target.draw({ num: 3 });
        });
      } else {
        const target = event.targets[0];
        const num = trigger.getl(target).cards2.filter((card) => trigger.getg(player)).length;
        if (num > 0) {
          await target.draw({ num });
        }
      }
    }
  },
  //夏侯岚
  pexunji: {
    audio: 2,
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player) {
      if (player !== _status.currentPhase || !event.getg?.(player)?.length) {
        return false;
      }
      return lib.phaseName.some(
        (phase) => player.getHistory("gain", (evt) => evt.getParent(phase) === event.getParent(phase)).map((evt) => event.name === "gain" ? evt : evt.getParent()).indexOf(event) === 0
      );
    },
    async cost(event, trigger, player) {
      const list = get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        return player.hasUseTarget(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3]
            },
            "unsure"
          ),
          false,
          false
        );
      });
      const result = await player.chooseButton({
        createDialog: ["迅击：你可以视为使用一张基本牌", [list, "vcard"]],
        ai(button) {
          return get.player().getUseValue({
            name: button.link[2],
            nature: button.link[3]
          });
        }
      }).forResult();
      if (result?.bool && result.links?.length) {
        event.result = {
          bool: true,
          cost_data: result.links
        };
      }
    },
    async content(event, trigger, player) {
      const { cost_data: links } = event;
      await player.chooseUseTarget(
        get.autoViewAs(
          {
            name: links[0][2],
            nature: links[0][3]
          },
          "unsure"
        ),
        true,
        false,
        "nodistance"
      ).set("oncard", () => {
        const event2 = _status.event;
        event2.directHit.addArray(game.players);
      });
    }
  },
  peshuangfeng: {
    audio: 2,
    derivation: ["peguiren", "peyichi"],
    trigger: { player: "phaseBegin" },
    async cost(event, trigger, player) {
      const result = await player.chooseControl({
        controls: ["phaseDraw", "phaseUse", "cancel2"],
        prompt: "双锋：将本回合所有阶段改为一个阶段",
        ai() {
          const { controls, player: player2 } = get.event();
          if (!player2.hasCard((card) => get.is.damageCard(card) && player2.hasUseTarget(card, false, false), "hs")) {
            controls.remove("phaseUse");
          }
          return controls.slice(0).remove("cancel2").randomGet();
        }
      }).forResult();
      if (typeof result?.control === "string" && result.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.control
        };
      }
    },
    async content(event, trigger, player) {
      const { cost_data: phase } = event;
      let num = 0;
      while (true) {
        if (trigger.phaseList[num]) {
          trigger.phaseList[num] = `${phase}|${event.name}`;
        } else {
          break;
        }
        num++;
      }
      player.addTempSkills(phase === "phaseDraw" ? "peyichi" : "peguiren");
    }
  },
  peguiren: {
    audio: 2,
    forced: true,
    locked: false,
    trigger: { source: "damageSource" },
    async content(event, trigger, player) {
      player.addTempSkill(`${event.name}_effect`, "phaseAnyAfter");
      const result = await player.chooseTarget({
        prompt: "归刃：选择一名角色与其各摸一张牌",
        forced: true,
        ai(target) {
          const player2 = get.player();
          return get.effect(target, { name: "draw" }, player2, player2);
        }
      }).forResult();
      if (result?.bool && result.targets?.length) {
        const target = result.targets[0];
        player.line(target);
        await game.asyncDraw([player, target]);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        mark: true,
        intro: { content: "本阶段使用牌无次数限制" },
        mod: {
          cardUsable: () => Infinity
        }
      }
    }
  },
  peyichi: {
    audio: 2,
    trigger: { player: "phaseDrawEnd" },
    filter(event, player) {
      return game.hasPlayer((current) => current !== player && current.isDamaged());
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "选择一名其他角色令其回复1点体力",
        filterTarget(card, player2, target) {
          return target !== player2 && target.isDamaged();
        },
        ai(target) {
          const player2 = get.player();
          return get.recoverEffect(target, player2, player2);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      await event.targets[0].recover();
    }
  },
  //天策上将-李世民
  pepozhen: {
    audio: 2,
    persevereSkill: true,
    forced: true,
    locked: false,
    trigger: { source: "damageSource" },
    filter(event, player) {
      const target = event.player;
      if (player === target || !target?.isIn()) {
        return false;
      }
      if (!target.getStorage("pepozhen_used").includes("选项一") && !player.getStorage("pepozhen_use").includes(target)) {
        return true;
      }
      if (!target.getStorage("pepozhen_used").includes("选项二") && target.hasGainableCards(player, "hej")) {
        return true;
      }
      if (!target.getStorage("pepozhen_used").includes("选项三")) {
        return true;
      }
      return false;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = trigger.player;
      const list = [];
      const choiceList = [`令${get.translation(player)}对你使用牌无距离次数限制`, `交给${get.translation(player)}一个区域内的所有牌`, "体力上限调整为1且失去所有技能"];
      if (!target.getStorage("pepozhen_used").includes("选项一") && !player.getStorage("pepozhen_use").includes(target)) {
        list.push("选项一");
      } else {
        choiceList[0] = `<span style="opacity:0.5">${choiceList[0]}</span>`;
      }
      if (!target.getStorage("pepozhen_used").includes("选项二") && target.hasGainableCards(player, "hej")) {
        list.push("选项二");
      } else {
        choiceList[1] = `<span style="opacity:0.5">${choiceList[1]}</span>`;
      }
      if (!target.getStorage("pepozhen_used").includes("选项三")) {
        list.push("选项三");
      } else {
        choiceList[2] = `<span style="opacity:0.5">${choiceList[2]}</span>`;
      }
      if (list.length) {
        let result;
        result = list.length > 1 ? await target.chooseControl({
          prompt: `${get.translation(player)}对你发动了【破阵】，请选择一项`,
          choiceList,
          controls: list,
          ai() {
            const { controls, player: player2, source } = get.event();
            if (get.attitude(player2, source) > 0) {
              if (controls.includes("选项二")) {
                return "选项二";
              } else if (controls.includes("选项一")) {
                return "选项一";
              }
            }
            if (controls.includes("选项一")) {
              return "选项一";
            } else if (controls.includes("选项二")) {
              return "选项二";
            }
            return controls.slice(0).randomGet();
          }
        }).set("source", player).forResult() : { control: list[0] };
        if (typeof result?.control === "string") {
          const control = result.control;
          target.addTempSkill(`${event.name}_used`);
          target.markAuto(`${event.name}_used`, [control]);
          if (control === "选项一") {
            player.addSkill(`${event.name}_use`);
            player.markAuto(`${event.name}_use`, [target]);
          } else if (control === "选项二") {
            const map = { h: "手牌区", e: "装备区", j: "判定区" };
            const pos = ["h", "e", "j"].filter((i) => target.countGainableCards(player, i));
            if (pos.length) {
              result = pos.length > 1 ? await target.chooseControl({
                controls: pos.map((i) => map[i]),
                prompt: `选择一个区域内的所有牌交给${get.translation(player)}`,
                ai() {
                  const { player: player2, source, controls } = get.event();
                  if (get.attitude(player2, source) > 0) {
                    if (controls.includes("h") && player2.countGainableCards(source, "h") > 3) {
                      return "h";
                    }
                  }
                  if (controls.includes("j")) {
                    return "j";
                  }
                  if (controls.includes("e") && player2.countGainableCards(source, "e") < 3) {
                    return "e";
                  }
                  return controls.slice(0).randomGet();
                }
              }).set("source", player).forResult() : { control: pos[0] };
              const reverseMap = Object.fromEntries(Object.entries(map).map(([k, v]) => [v, k]));
              const cards2 = target.getGainableCards(player, reverseMap[result.control]);
              if (cards2?.length) {
                await target.give(cards2, player);
              }
            }
          } else {
            if (target.maxHp > 1) {
              await target.loseMaxHp(target.maxHp - 1);
            } else if (target.maxHp < 1) {
              await target.gainMaxHp(1 - target.maxHp);
            }
            await target.removeSkills(
              target.getSkills(null, false, false).filter((skill) => {
                const info = get.info(skill);
                return info && !info.charlotte;
              })
            );
          }
        }
      }
    },
    subSkill: {
      used: { charlotte: true, onremove: true },
      use: {
        charlotte: true,
        onremove: true,
        intro: { content: "对$使用牌无距离次数限制" },
        mod: {
          targetInRange(card, player, target) {
            if (player.getStorage("pepozhen_use").includes(target)) {
              return true;
            }
          },
          cardUsableTarget(card, player, target) {
            if (player.getStorage("pepozhen_use").includes(target)) {
              return Infinity;
            }
          }
        }
      }
    }
  },
  petaoge: {
    audio: 2,
    enable: ["chooseToUse"],
    filter(event, player) {
      if (!["basic", "trick"].some((type) => player.hasCard((card) => get.type2(card) === type, "he") && !player.getStorage("petaoge_used").includes(type))) {
        return false;
      }
      return get.inpileVCardList((info) => {
        const types = ["basic", "trick"].removeArray(player.getStorage("petaoge_used"));
        if (!types.includes(get.type2(info[2]))) {
          return false;
        }
        return event.filterCard(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3]
            },
            "unsure"
          ),
          player,
          event
        );
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          const types = ["basic", "trick"].removeArray(player.getStorage("petaoge_used")).filter((i) => player.hasCard((card) => get.type2(card) === i, "he"));
          if (!types.includes(get.type2(info[2]))) {
            return false;
          }
          return event.filterCard(
            get.autoViewAs(
              {
                name: info[2],
                nature: info[3]
              },
              "unsure"
            ),
            player,
            event
          );
        });
        return ui.create.dialog("韬戈", [list, "vcard"]);
      },
      filter(button, player) {
        return _status.event.getParent().filterCard(
          {
            name: button.link[2]
          },
          player,
          _status.event.getParent()
        );
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = get.player();
        if (["wugu", "zhulu_card", "yiyi", "lulitongxin", "lianjunshengyan", "diaohulishan"].includes(button.link[2])) {
          return 0;
        }
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          link: links[0][2],
          filterCard(card) {
            return get.type2(card) === get.type2(lib.skill.petaoge_backup.link);
          },
          audio: "petaoge",
          popname: true,
          check(card) {
            return 8 - get.value(card);
          },
          position: "he",
          viewAs: {
            name: links[0][2],
            nature: links[0][3]
          },
          log: false,
          async precontent(event, trigger, player2) {
            player2.logSkill("petaoge");
            player2.addTempSkill("petaoge_used");
            player2.markAuto("petaoge_used", [get.type2(event.result.card)]);
          }
        };
      },
      prompt(links, player) {
        return `将一张牌当做${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用`;
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name) || !player.countCards("he")) {
        return false;
      }
      const type = get.type2(name);
      return ["basic", "trick"].removeArray(player.getStorage("petaoge_used")).includes(type);
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player) {
        if (!player.countCards("he") || player.getStorage("petaoge_used").includes("basic")) {
          return false;
        }
      },
      order: 7,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      backup: {},
      used: { charlotte: true, onremove: true }
    }
  },
  pezhenguan: {
    audio: 2,
    zhuSkill: true,
    forced: true,
    locked: false,
    trigger: {
      global: ["roundStart", "roundEnd"]
    },
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    async content(event, trigger, player) {
      const groups = game.filterPlayer((current) => current !== player).sortBySeat().map((current) => current.group).unique();
      while (groups.length) {
        const group = groups.shift();
        if (game.hasPlayer((current) => current !== player && current.group === group)) {
          const result = game.countPlayer((current) => current !== player && current.group === group) > 1 ? await player.chooseTarget({
            prompt2: `贞观：令一名${get.translation(group)}势力的其他角色摸一张牌`,
            filterTarget(card, player2, target) {
              return target !== player2 && target.group === group;
            },
            forced: true,
            ai(target) {
              return get.attitude(get.player(), target);
            }
          }).forResult() : { bool: true, targets: game.filterPlayer((current) => current !== player && current.group === group) };
          if (result?.bool && result.targets?.length) {
            const target = result.targets[0];
            player.line(target);
            await target.draw({ num: 1 });
          }
        }
      }
      const num = game.countGroup();
      await player.draw({ num });
    }
  },
  //e郭照
  pepianchong: {
    trigger: { player: "phaseDrawBegin1" },
    check(event, player) {
      return true;
    },
    filter(event, player) {
      return !event.numFixed;
    },
    async content(event, trigger, player) {
      const card = get.bottomCards();
      const next = player.gain(card, "gain2");
      next.gaintag.add(`${event.name}_effect`);
      await next;
      await player.draw();
      trigger.changeToZero();
      player.addTempSkill("pepianchong_effect", { player: "phaseBegin" });
    },
    subSkill: {
      effect: {
        trigger: {
          player: ["loseAfter"],
          global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
        },
        forced: true,
        charlotte: true,
        getIndex(event, player) {
          return event.getl?.(player).hs.map((card) => card.cardid) || [];
        },
        async content(event, trigger, player) {
          let next;
          const evt = trigger.getl(player);
          if (evt.gaintag_map?.[event.indexedData]?.includes(event.name)) {
            next = player.draw("nodelay");
          } else {
            next = player.gain(get.bottomCards(), "gain2", false);
            next.gaintag.add(event.name);
          }
          await next;
        },
        ai: {
          noh: true
        }
      }
    }
  },
  pezunwei: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      const filter = (index) => {
        const target = game.findPlayer((current) => current[`isMax${["Handcard", "Equip", "Hp"][index]}`]());
        if (!target || target === player || player.getStorage("pezunwei_used").includes(index)) {
          return false;
        }
        const count = (current) => [current.countCards("h"), current.countCards("e"), current.getHp()][index];
        return count(target) > count(player);
      };
      return [0, 1, 2].some(filter);
    },
    chooseButton: {
      dialog(event, player) {
        const list = ["将手牌摸至全场最多", "随机使用牌堆中的装备牌，直至你的装备区牌数为全场最多", "将体力回复至全场最多"];
        const choiceList = ui.create.dialog("尊位：请选择一项", "forcebutton", "hidden");
        choiceList.add([
          list.map((item, i) => {
            if (player.getStorage("pezunwei_used").includes(i)) {
              item = `<span style="text-decoration: line-through;">${item}</span>`;
            }
            return [i, item];
          }),
          "textbutton"
        ]);
        return choiceList;
      },
      check(button) {
        const player = get.player();
        const target = game.findPlayer((current) => current[`isMax${["Handcard", "Equip", "Hp"][button.link]}`]());
        if (!target || target === player) {
          return 0;
        }
        const count = (current) => [current.countCards("h"), current.countCards("e"), current.getHp()][button.link];
        return (count(target) - count(player)) * button.link;
      },
      filter(button) {
        const player = get.player();
        const filter = (index) => {
          const target = game.findPlayer((current) => current[`isMax${["Handcard", "Equip", "Hp"][index]}`]());
          if (!target || target === player || player.getStorage("pezunwei_used").includes(index)) {
            return false;
          }
          const count = (current) => [current.countCards("h"), current.countCards("e"), current.getHp()][index];
          return count(target) > count(player);
        };
        return filter(button.link);
      },
      backup(links) {
        return {
          index: links[0],
          async content(event, trigger, player) {
            const { index } = get.info(event.name);
            player.addSkill("pezunwei_used");
            player.markAuto("pezunwei_used", index);
            switch (index) {
              case 0: {
                const target = game.findPlayer((current) => current.isMaxHandcard());
                if (target) {
                  await player.drawTo(target.countCards("h"));
                }
                break;
              }
              case 1: {
                do {
                  const card = get.cardPile2((card2) => get.type(card2) === "equip" && player.canUse(card2, player) && player.canEquip(card2));
                  if (card) {
                    const next = player.chooseUseTarget(card, true);
                    next.nopopup = true;
                    await next;
                  } else {
                    break;
                  }
                } while (!player.isMaxEquip() && [1, 2, 3, 4, 5].some((i) => player.hasEmptySlot(i)));
                break;
              }
              case 2: {
                const target = game.findPlayer((current) => current.isMaxHp());
                if (target) {
                  await player.recoverTo(target.getHp());
                }
                break;
              }
            }
          }
        };
      }
    },
    ai: {
      order: 1,
      result: {
        player(player, target) {
          const value = (index2) => {
            const target2 = game.findPlayer((current) => current[`isMax${["Handcard", "Equip", "Hp"][index2]}`]());
            if (!target2 || target2 === player || player.getStorage("pezunwei_used").includes(index2)) {
              return 0;
            }
            const count = (current) => [current.countCards("h"), current.countCards("e"), current.getHp()][index2];
            return (count(target2) - count(player)) * index2;
          };
          const index = [0, 1, 2].maxBy(value);
          if (value(index) > 3) {
            return 1;
          }
          return 0;
        }
      }
    },
    subSkill: {
      backup: {},
      used: {
        charlotte: true,
        onremove: true
      }
    }
  },
  //线下官盗E系列10-侠曹丕
  //史阿你看看你教了个什么东西
  peqinyi: {
    trigger: {
      source: "damageSource",
      player: "damageEnd"
    },
    filter(event, player, name) {
      const evt = name === "damageSource" ? "sourceDamage" : "damage";
      return player.getHistory(evt).indexOf(event) === 0 && get.info("peqinyi").getList(player).length > 0;
    },
    getList(player) {
      const list = get.inpileVCardList((info) => {
        if (!["basic", "trick"].includes(info[0]) || player.getStorage("peqinyi").includes(info[2])) {
          return false;
        }
        const card = new lib.element.VCard({ name: info[2], nature: info[3], isCard: true });
        return player.hasUseTarget(card) || get.info(card).notarget && lib.filter.cardEnabled(card, player);
      });
      return list;
    },
    async cost(event, trigger, player) {
      const list = get.info(event.skill).getList(player);
      if (!list.length) {
        event.result = { bool: false };
        return;
      }
      const { links } = await player.chooseButton(["勤艺：是否视为使用一张未以此法使用过的基本牌或普通锦囊牌？", [list, "vcard"]]).set("ai", (button) => {
        const { link } = button;
        return get.player().getUseValue(new lib.element.VCard({ name: link[2], nature: link[3] })) + 1;
      }).forResult();
      event.result = {
        bool: links?.length ? true : false,
        cost_data: links
      };
    },
    async content(event, trigger, player) {
      const [[taofen, lulu, name, nature]] = event.cost_data;
      const card = new lib.element.VCard({ name, nature, isCard: true });
      if (player.hasUseTarget(card) || get.info(card).notarget && lib.filter.cardEnabled(card, player)) {
        player.markAuto("peqinyi", name);
        await player.chooseUseTarget(`${get.translation(event.name)}：请选择${get.translation(card)}的目标`, card, true, false);
      }
    },
    marktext: "艺",
    intro: { content: "已以此法使用过$" }
  },
  //武鹿可以直接撕了
  pejixin: {
    trigger: { player: "useCardAfter" },
    filter(event, player) {
      const name = event.card.name;
      return name && player.getAllHistory("useCard", (evt) => evt.card.name === event.card.name).indexOf(event) === 0;
    },
    //妈妈再也不用担心我一不小心就摸空牌堆辣！
    frequent(event, player) {
      const num = ui.cardPile.childElementCount + ui.discardPile.childElementCount;
      const num2 = player.getRoundHistory("useSkill", (evt) => evt.skill === "pejixin").length + 1;
      return num > num2;
    },
    async content(event, trigger, player) {
      const skill = "pejixin_count";
      player.addTempSkill(skill, "roundStart");
      player.addMark(skill, 1, false);
      const num = player.countMark(skill);
      const result = player.draw(num);
      result.gaintag.add("pejixin_effect");
      const cards2 = await result.forResult().cards;
      await player.showCards(`${get.translation(player)}发动了【技新】`, cards2);
      player.addSkill("pejixin_effect");
    },
    init(player, skill) {
      player.addSkill("pejixin_mark");
      const num1 = player.getAllHistory("useCard").map((evt) => evt.card.name).unique();
      const num2 = player.getRoundHistory("useSkill", (evt) => evt.skill === skill).length;
      if (num1 > 0) {
        player.setStorage(`${skill}_mark`, num1, true);
      }
      if (num2 > 0) {
        player.setStorage(`${skill}_count`, num2, true);
      }
    },
    onremove(player, skill) {
      delete player.storage[skill];
      player.removeSkill(`${skill}_mark`);
      player.removeSkill(`${skill}_count`);
    },
    subSkill: {
      mark: {
        charlotte: true,
        onremove: true,
        trigger: { player: "useCard1" },
        forced: true,
        silent: true,
        async content(event, trigger, player) {
          player.markAuto(event.name, [trigger.card.name]);
        },
        marktext: "新",
        intro: { content: "本局游戏已使用牌名：$" }
      },
      count: {
        charlotte: true,
        onremove: true,
        mark: true,
        marktext: "技",
        intro: { content: "本轮已发动过$次〖技新〗" }
      },
      effect: {
        charlotte: true,
        onremove(player, skill) {
          player.removeGaintag(skill);
        },
        trigger: { player: "useCard0" },
        filter(event, player) {
          if (event.addCount === false) {
            return false;
          }
          return player.hasHistory("lose", (evt) => {
            if (evt.getParent() !== event) {
              return false;
            }
            return Object.values(evt.gaintag_map).flat().includes("pejixin_effect");
          });
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player) {
          trigger.addCount = false;
          const stat = player.getStat().card;
          const name = trigger.card.name;
          if (typeof stat[name] === "number") {
            stat[name]--;
          }
        },
        mod: {
          targetInRange(card, player, target) {
            if (get.number(card) === "unsure" || card.cards?.every((card2) => card2.hasGaintag("pejixin_effect"))) {
              return true;
            }
          },
          cardUsable(card, player, num) {
            if (get.number(card) === "unsure" || card.cards?.every((card2) => card2.hasGaintag("pejixin_effect"))) {
              return Infinity;
            }
          },
          ignoredHandcard(card, player) {
            if (card.hasGaintag("pejixin_effect")) {
              return true;
            }
          },
          cardDiscardable(card, player, name) {
            if (name === "phaseDiscard" && card.hasGaintag("pejixin_effect")) {
              return false;
            }
          }
        }
      }
    }
  },
  pejiwei: {
    init() {
      if (!_status.pejiweiList) {
        _status.pejiweiList = [];
        if (!_status.characterlist) {
          game.initCharacterList();
        }
        for (const name of _status.characterlist) {
          const character = get.character(name);
          const { group, doubleGroup } = character;
          let { skills: skills2 } = character;
          if (group === "wei" || doubleGroup.includes("wei")) {
            skills2 = skills2.filter((skill) => {
              const info = get.info(skill);
              return info?.zhuSkill && !info.ai?.combo;
            });
            if (skills2.length) {
              skills2.forEach((skill) => _status.pejiweiList.add(skill));
            }
          }
        }
      }
    },
    trigger: {
      global: "phaseEnd"
    },
    isFirst(target) {
      if (game.hasPlayer2((current) => current.getSeatNum() > 0, true)) {
        return target.getSeatNum() === 1;
      }
      return target === _status.roundStart;
    },
    forced: true,
    juexingji: true,
    skillAnimation: true,
    animationColor: "thunder",
    filter(event, player) {
      const taofen = game.findPlayer2((current) => get.info("pejiwei").isFirst(current), true);
      return taofen && player.countMark("pejixin_count") >= taofen.getHp();
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.gainMaxHp();
      await player.recoverTo(player.maxHp);
      if (!_status.pejiweiList) {
        get.info(event.name).init();
      }
      await game.delayx();
      const list = _status.pejiweiList.slice().filter((skill) => !player.hasSkill(skill, null, false, false));
      if (!list.length) {
        return;
      }
      const num = Math.min(5, list.length);
      const { links } = await player.chooseButton([`继魏：是否选择并获得${num}个魏势力主公技？`, [list, "skill"]], num).set("ai", (button) => get.skillRank(button.link[0], "inout")).forResult();
      if (links) {
        await game.delayx();
        player.chat(`${get.cnNumber(num, true)}灵威力，变身！`);
        player.addSkills(links);
      }
    },
    ai: { combo: "pejixin" }
  },
  //夏侯玄
  pehuanfu: {
    trigger: {
      player: "useCardToPlayered",
      target: "useCardToTargeted"
    },
    filter(event, player) {
      return event.card.name === "sha";
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard(get.prompt2(event.skill), "he", [1, player.maxHp], "chooseonly", "allowChooseAll").set("ai", (card) => 6 - get.value(card)).forResult();
    },
    async content(event, trigger, player) {
      const cards2 = (await player.modedDiscard(event.cards).forResult()).cards;
      await player.draw(cards2.length * 2);
      game.log(trigger.card, "的伤害改为", `#y${cards2.length}`);
      player.addTempSkill(`${event.name}_damage`);
      trigger.card.storage[`${event.name}_damage`] = cards2.length;
    },
    subSkill: {
      damage: {
        trigger: {
          global: "damageBegin1"
        },
        filter(event, player) {
          return event.card?.storage?.pehuanfu_damage > 0 && event.getParent(2)?.targets?.includes(event.player);
        },
        charlotte: true,
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          trigger.num = trigger.card.storage.pehuanfu_damage;
        }
      }
    }
  },
  //曲阿小将
  peyingzhen: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player) {
      if (!game.countPlayer((current) => current !== player)) {
        return false;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async cost(event, trigger, player) {
      const targets = game.filterPlayer((current) => current !== player);
      event.result = targets.length > 1 ? await player.chooseTarget(
        get.prompt2(event.skill),
        (card, player2, target) => {
          if (target === player2) {
            return false;
          }
          if (ui.selected.targets.length) {
            const targetx = ui.selected.targets[0];
            return target === targetx.getPrevious() || target === targetx.getNext();
          }
          return true;
        },
        true,
        2
      ).set("targetprompt", ["执行回合", "交换位置"]).set("complexTarget", true).set("ai", (target) => {
        const att = get.attitude(get.player(), target);
        if (ui.selected.targets.length) {
          return -att;
        }
        return att;
      }).forResult() : {
        bool: true,
        targets
      };
    },
    async content(event, trigger, player) {
      const targets = event.targets;
      if (targets.length > 1) {
        game.broadcastAll(
          (target1, target2) => {
            game.swapSeat(target1, target2);
          },
          player,
          targets[1]
        );
      }
      await game.delay(3);
      const evt = player.insertPhase();
      evt.pushHandler("onPhase", (event2, option) => {
        if (event2.step === 0 && option.state === "begin") {
          event2.step = 1;
        }
      });
      targets[0].insertPhase();
      if (trigger.name === "phase" && !trigger._finished) {
        const first = game.findPlayer((current) => current.getSeatNum() === 1) || trigger.player;
        trigger.finish();
        trigger._finished = true;
        trigger._triggered = 5;
        const evtx = first.insertPhase();
        delete evtx.skill;
        const evt2 = trigger.getParent();
        if (evt2.name === "phaseLoop" && evt2._isStandardLoop) {
          evt2.player = first;
        }
      }
    }
  },
  peyuanjue: {
    trigger: {
      player: "phaseDrawBefore"
    },
    zhuanhuanji: true,
    mark: true,
    marktext: "☯",
    intro: {
      content(storage, player, skill) {
        if (storage) {
          return "所有角色的基本牌视为无次数限制的【杀】";
        } else if (storage === false) {
          return "所有角色与你互相计算距离为1，你视为拥有〖同忾〗";
        }
        return get.skillInfoTranslation(skill, player, false);
      }
    },
    async content(event, trigger, player) {
      trigger.cancel();
      player.changeZhuanhuanji(event.name);
      if (player.getStorage(event.name, false)) {
        player.removeAdditionalSkill(event.name);
      } else {
        player.addAdditionalSkill(event.name, ["petongkai"]);
      }
    },
    locked: false,
    mod: {
      globalFrom(from, to) {
        if (from.storage.peyuanjue === false) {
          return -Infinity;
        }
      },
      globalTo(from, to) {
        if (to.storage.peyuanjue === false) {
          return -Infinity;
        }
      }
    },
    global: "peyuanjue_viewas",
    derivation: "petongkai",
    subSkill: {
      viewas: {
        mod: {
          cardname(card, player) {
            if (game.hasPlayer((current) => current.hasSkill("peyuanjue") && current.getStorage("peyuanjue") === true) && lib.card[card.name]?.type === "basic") {
              return "sha";
            }
          },
          cardUsable(card, player) {
            if (!game.hasPlayer((current) => current.hasSkill("peyuanjue") && current.getStorage("peyuanjue") === true) || card.name !== "sha") {
              return;
            }
            if (!card.cards || card.cards.length !== 1) {
              return;
            }
            if (get.suit(card) === "unsure" || lib.card[card.cards[0].name]?.type === "basic") {
              return Infinity;
            }
          }
        }
      }
    }
  },
  peaoyong: {
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    persevereSkill: true,
    filter(event, player) {
      return event.getParent(2, true)?.name !== "peaoyong" && event.getg(player)?.length;
    },
    async cost(event, trigger, player) {
      const result = await player.chooseControl(["选项一", "选项二", "选项三", "背水！", "cancel2"]).set("choiceList", ["选项一：摸一张牌", "选项二：回复1点体力", "选项三：使用一张牌", "背水，减少1点体力上限"]).set("displayIndex", false).set("ai", () => get.player().hp <= 2 ? "选项二" : "选项一").set("prompt", get.prompt(event.skill)).forResult();
      event.result = {
        bool: result.index !== 4,
        cost_data: result.index
      };
    },
    async content(event, trigger, player) {
      const num = event.cost_data;
      if (num === 3) {
        player.popup("背水", "thunder");
      }
      if (num === 0 || num === 3) {
        await player.draw();
      }
      if (num === 1 || num === 3) {
        await player.recover();
      }
      if (num === 2 || num === 3) {
        await player.chooseToUse({
          filterCard(card, player2, event2) {
            if (get.itemtype(card) !== "card") {
              return false;
            }
            return lib.filter.filterCard.apply(this, arguments);
          },
          prompt: "鏊勇：是否使用一张牌？",
          addCount: false
        });
      }
      if (num === 3) {
        await player.loseMaxHp();
      }
    }
  },
  petongkai: {
    trigger: { global: "useCardToTargeted" },
    filter(event, player) {
      return get.is.damageCard(event.card) && get.distance(player, event.target) <= 1 && event.target.isIn();
    },
    logTarget: "target",
    async content(event, trigger, player) {
      await player.draw();
      if (trigger.target === player || !trigger.target?.isIn() || !player.countCards("he")) {
        return;
      }
      const result = await player.chooseToGive(true, "he", trigger.target).set("visibleMove", true).forResult();
      if (!result?.bool) {
        return;
      }
      await game.delay();
      const card = result.cards[0];
      if (trigger.target.getCards("h").includes(card) && get.type(card) === "equip") {
        trigger.target.chooseUseTarget(card);
      }
    },
    ai: { threaten: 1.1 }
  },
  // 曹操＆袁绍
  yjguibei: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    forced: true,
    filter(event, player) {
      return game.hasPlayer((current) => current !== player) && (event.name !== "phase" || game.phaseNumber === 0);
    },
    async content(event, trigger, player) {
      await player.draw(4);
      const zhu = game.findPlayer((i) => i.getSeatNum() === 1);
      if (zhu && zhu.getPrevious() !== player) {
        game.broadcastAll(
          (target1, target2) => {
            game.swapSeat(target1, target2);
          },
          player,
          zhu.getPrevious()
        );
      }
      const newzhu = game.findPlayer((i) => i.getSeatNum() === 1);
      if (trigger.name === "phase" && newzhu !== zhu && !trigger._finished) {
        trigger.finish();
        trigger._finished = true;
        trigger._triggered = 5;
        const evt = newzhu.insertPhase();
        delete evt.skill;
        const evt2 = trigger.getParent();
        if (evt2.name === "phaseLoop" && evt2._isStandardLoop) {
          evt2.player = newzhu;
        }
        evt.pushHandler("onPhase", (event2, option) => {
          if (event2.step === 0 && option.state === "begin") {
            event2.step = 1;
          }
        });
      }
      await game.delay();
    }
  },
  yjjiechu: {
    mark: true,
    zhuanhuanji: true,
    marktext: "☯",
    intro: {
      content(storage, player, skill) {
        if (storage) {
          return "当你成为【杀】的目标时，你可以弃置一张手牌改变【杀】的花色和属性。";
        }
        return "出牌阶段，你可以视为使用一张【顺手牵羊】，结算结束后成为目标的角色可以对你使你一张【杀】。";
      }
    },
    group: ["yjjiechu_use", "yjjiechu_sha"],
    subSkill: {
      sha: {
        trigger: {
          target: "useCardToTarget"
        },
        filter(event, player) {
          return player.storage?.yjjiechu && event?.card.name === "sha";
        },
        async cost(event, trigger, player) {
          const suits = [];
          let bool = false;
          game.getAllGlobalHistory("everything", (evt) => {
            if (evt.name === "damage" && evt.player === player && evt.card) {
              suits.add(get.suit(evt.card));
            }
          });
          if (suits.includes(get.suit(trigger.card)) && player.hasSkill("yjdaojue")) {
            bool = true;
          }
          const list = lib.linked.slice(0).remove("kami");
          if (list.some((nature) => {
            if (nature === trigger.card?.nature) {
              return false;
            }
            const card = new lib.element.VCard(trigger.card, trigger.cards, null, nature);
            return get.effect(player, card, trigger.player, player) > get.effect(player, trigger.card, trigger.player, player);
          })) {
            bool = true;
          }
          event.result = await player.chooseToDiscard(get.prompt("yjjiechu"), "chooseonly", "h").set("prompt2", `你可以弃置一张手牌，改变${get.translation(trigger.card)}的花色和属性。`).set("resultx", bool).set("ai", (card) => {
            if (!get.event().resultx) {
              return 0;
            }
            return 7 - get.value(card);
          }).forResult();
        },
        async content(event, trigger, player) {
          player.changeZhuanhuanji("yjjiechu");
          await player.discard(event.cards);
          const suits = lib.suit.slice(0).remove(get.suit(trigger.card));
          const natures = lib.linked.slice(0).remove("kami").remove(trigger.card.nature);
          const records = suits.slice(0);
          game.getAllGlobalHistory("everything", (evt) => {
            if (evt.name === "damage" && evt.player === player && evt.card) {
              records.remove(get.suit(evt.card));
            }
          });
          let nature = trigger.card.nature;
          for (const i of natures) {
            const card1 = new lib.element.VCard(trigger.card, trigger.cards, null, i);
            const card2 = new lib.element.VCard(trigger.card, trigger.cards, null, nature);
            const eff1 = get.effect(player, card1, trigger.player, player);
            const eff2 = get.effect(player, card2, trigger.player, player);
            if (eff1 > eff2) {
              nature = i;
            }
          }
          const result = await player.chooseButton(["劫出：改变此牌的花色和属性", [suits.map((i) => ["", "", `lukai_${i}`]), "vcard"], [natures.map((i) => [i, get.translation(i)]), "tdnodes"]], 2, true).set("filterButton", (button) => {
            if (!ui.selected.buttons.length) {
              return true;
            }
            const bool = (link) => Array.isArray(link);
            return bool(button.link) !== bool(ui.selected.buttons[0].link);
          }).set("records", records).set("nature", nature).set("ai", (button) => {
            const { records: records2, nature: nature2 } = get.event();
            if (Array.isArray(button.link)) {
              if (records2.includes(button.link[2].slice(6))) {
                return 1 + Math.random();
              }
              return Math.random();
            }
            if (nature2 === button.link) {
              return 1 + Math.random();
            }
            return Math.random();
          }).forResult();
          if (!result.bool) {
            return;
          }
          if (Array.isArray(result.links[0])) {
            result.links.reverse();
          }
          const naturex = result.links[0];
          const suit = result.links[1][2].slice(6);
          player.popup(get.translation(suit));
          game.log(trigger.card, "被转为了", `#y${get.translation(suit)}`, "花色");
          game.broadcastAll(
            (card, suit2) => {
              card.suit = suit2;
            },
            trigger.card,
            suit
          );
          player.popup(`${get.translation(naturex)}杀`, naturex);
          game.log(trigger.card, "被转为了", `#y${get.translation(naturex)}`, "属性");
          game.setNature(trigger.card, naturex);
        }
      },
      effect: {
        trigger: {
          player: "useCardAfter"
        },
        filter(event, player) {
          return event.targets.length && event?.card?.storage?.yjjiechu;
        },
        charlotte: true,
        silent: true,
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          for (const c of trigger.targets.filter((c2) => c2.isIn()).sortBySeat()) {
            await c.chooseToUse(
              (card, player2, event2, ...args) => {
                if (get.name(card) !== "sha") {
                  return false;
                }
                return lib.filter.filterCard(card, player2, event2, ...args);
              },
              `你可对${get.translation(player)}使用一张杀`
            ).set("targetRequired", true).set("complexTarget", true).set("complexSelect", true).set("filterTarget", (card, player2, target, ...args) => {
              const sourcex = get.event().sourcex;
              if (target !== sourcex && !ui.selected.targets.includes(sourcex)) {
                return false;
              }
              return lib.filter.filterTarget(card, player2, target, ...args);
            }).set("sourcex", player);
          }
        }
      },
      use: {
        enable: "phaseUse",
        prompt: "你可以视为使用一张【顺手牵羊】，结算结束后成为目标的角色可以对你使用一张【杀】。",
        viewAs: {
          name: "shunshou",
          storage: {
            yjjiechu: true
          },
          isCard: true
        },
        filterCard: () => false,
        selectCard: -1,
        viewAsFilter(player) {
          return !player.storage?.yjjiechu;
        },
        async precontent(event, trigger, player) {
          player.addTempSkill("yjjiechu_effect");
          player.changeZhuanhuanji("yjjiechu");
        }
      }
    }
  },
  yjdaojue: {
    dutySkill: true,
    onremove(player, skill) {
      player.removeTip(skill);
    },
    derivation: ["yjqingzheng", "yjzhian", "feiying", "rehujia", "olsbshenli", "yjzhuni", "olsbshishou"],
    group: ["yjdaojue_effect", "yjdaojue_achieve", "yjdaojue_fail"],
    subSkill: {
      fail: {
        trigger: {
          player: "useCardAfter"
        },
        filter(event, player) {
          return player.getAllHistory("useCard", (evt) => evt.getParent(3).skill === "yjdaojue_effect").length === 3;
        },
        skillAnimation: true,
        animationColor: "metal",
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          player.awakenSkill("yjdaojue");
          player.removeTip("yjdaojue");
          player.popup("袁绍");
          game.log(player, "使命失败");
          await player.removeSkills("yjjiechu");
          await player.changeGroup("qun");
          await player.addSkills(["olsbshenli", "yjzhuni", "olsbshishou"]);
        }
      },
      achieve: {
        trigger: {
          player: "gainAfter",
          global: "loseAsyncAfter"
        },
        skillAnimation: true,
        animationColor: "metal",
        forced: true,
        locked: false,
        filter(event, player) {
          return player.getAllHistory("gain", (evt) => evt.getParent(2).skill === "yjdaojue_effect").map((c) => c.cards.length).reduce((sum, cur) => sum + cur, 0) >= 3;
        },
        async content(event, trigger, player) {
          player.awakenSkill("yjdaojue");
          player.removeTip("yjdaojue");
          player.popup("曹操");
          game.log(player, "成功完成使命");
          await player.removeSkills("yjjiechu");
          await player.changeGroup("wei");
          await player.addSkills(["yjzhian", "yjqingzheng", "rehujia"]);
        }
      },
      effect: {
        audio: "yjdaojue",
        trigger: {
          player: "damageBegin4"
        },
        forced: true,
        locked: false,
        filter(event, player) {
          return lib.suit.includes(get.suit(event.card)) && game.getAllGlobalHistory("everything", (evt) => evt.name === "damage" && evt.player === player && get.suit(evt?.card, player) === get.suit(event?.card, player), event).length === 1;
        },
        async content(event, trigger, player) {
          trigger.cancel();
          player.addTip(
            "yjdaojue",
            `道抉 ${game.getAllGlobalHistory("everything", (evt) => evt.name === "damage" && evt.player === player && lib.suit.includes(get.suit(evt?.card, player)), trigger).map((c) => get.suit(c?.card, player)).unique().reduce((str, i) => str + get.translation(i), "")}`
          );
          const card = trigger.cards.filter((c) => get.position(c, true) === "o");
          if (!card.length) {
            await player.chooseToUse((card2, player2, event2, ...args) => {
              if (get.name(card2) !== "sha") {
                return false;
              }
              return lib.filter.filterCard(card2, player2, event2, ...args);
            }, "是否对所有角色使用一张杀？").set("targetRequired", true).set("complexSelect", true).set("selectTarget", -1).set("filterTarget", (card2, player2, target, ...args) => lib.filter.targetEnabled(card2, player2, target, ...args));
            return;
          }
          const result = await player.chooseControlList(get.prompt(event.name), [`获得${get.translation(card)}`, "使用一张指定所有其他角色的【杀】。"], true).set("ai", () => {
            if (get.event().card.map((c) => get.value(c)).reduce((sum, cur) => sum + cur, 0) > 4) {
              return 0;
            }
            return 1;
          }).set("card", card).forResult();
          if (result.index === 0) {
            await player.gain(card, "gain2", "log");
          } else if (player.hasUsableCard("sha", "use")) {
            await player.chooseToUse(
              (card2, player2, event2, ...args) => {
                if (get.name(card2) !== "sha") {
                  return false;
                }
                return lib.filter.filterCard(card2, player2, event2, ...args);
              },
              "对所有角色使用一张杀",
              true
            ).set("targetRequired", true).set("complexSelect", true).set("selectTarget", -1).set("filterTarget", (card2, player2, target, ...args) => lib.filter.targetEnabled(card2, player2, target, ...args));
          }
        }
      }
    }
  },
  yjzhian: {
    audio: "twzhian",
    trigger: {
      global: "useCardAfter"
    },
    filter(event, player) {
      if (get.type(event.card) === "basic") {
        return false;
      }
      return player.isDamaged() || player.countCards("h") || !player.hasSkill("yjzhian_delete");
    },
    usable(skill, player) {
      return Math.max(1, player.getDamagedHp());
    },
    async cost(event, trigger, player) {
      const cards2 = trigger.cards?.filterInD("ejod") || [];
      const list = ["选项一", "选项二", "选项三", "cancel2"];
      const choiceList = [`弃置一张手牌${cards2.length ? `并获得${get.translation(cards2)}` : ""}`, "回复1点体力，此技能本轮失效", `对${get.translation(trigger.player)}造成${get.cnNumber(Math.max(1, player.getDamagedHp()))}点伤害，获得〖飞影〗并删去此项`];
      let choice = "cancel2";
      if (player.isDamaged()) {
        if (player.hp <= 1) {
          choice = "选项二";
        }
      } else {
        list.remove("选项二");
        choiceList[1] = `<span style="opacity:0.5">${choiceList[1]}</span>`;
      }
      if (!player.countCards("h")) {
        list.remove("选项一");
        choiceList[0] = `<span style="opacity:0.5">${choiceList[0]}</span>`;
      } else {
        if (cards2.length && player.hp > 1) {
          let result2 = 0;
          const card = trigger.card;
          const owner = get.owner(card);
          if (owner) {
            if (get.position(card) === "j") {
              const vcard = { name: card.viewAs || card.name, cards: [card] };
              result2 += get.effect(player, vcard, player, player) * get.sgn(get.attitude(player, owner));
            } else {
              result2 += (get.value(card, owner) - 0.01) * get.sgn(get.attitude(player, owner));
            }
          } else {
            result2 += cards2.reduce((sum, i) => sum + get.value(i, player), 0);
          }
          result2 -= get.recoverEffect(player);
          if (result2 > player.getCards("h").map((i) => get.value(i)).minBy((i) => i)) {
            choice = "选项一";
          }
        }
      }
      if (player.hasSkill("yjzhian_delete")) {
        list.remove("选项三");
        choiceList[2] = `<span style="opacity:0.5">${choiceList[2]}</span>`;
      } else if (get.damageEffect(trigger.player, player) > 0 && choice === "cancel2") {
        choice = "选项三";
      }
      const result = await player.chooseControl(list).set("prompt", get.prompt(event.skill, trigger.player)).set("choiceList", choiceList).set("ai", () => get.event().choice).set("choice", choice).forResult();
      event.result = {
        bool: result.control !== "cancel2",
        cost_data: result.control,
        targets: [trigger.player]
      };
    },
    async content(event, trigger, player) {
      switch (event.cost_data) {
        case "选项一": {
          await player.chooseToDiscard("h", true);
          await player.gain(trigger.cards.filterInD("odej"), "gain2");
          break;
        }
        case "选项二": {
          await player.recover();
          player.tempBanSkill(event.name, "roundEnd");
          break;
        }
        case "选项三": {
          player.addSkill("yjzhian_delete");
          await trigger.player.damage(Math.max(1, player.getDamagedHp()));
          await player.addSkills("feiying");
        }
      }
    },
    subSkill: {
      delete: {
        charlotte: true
      }
    },
    derivation: "feiying"
  },
  yjqingzheng: {
    audio: "sbqingzheng",
    inherit: "mbcmqingzheng",
    async content(event, trigger, player) {
      const {
        targets: [target],
        cards: cards1
      } = event;
      await player.showHandcards();
      await player.discard(cards1);
      if (!target.countCards("h") || lib.suits.slice().filter((suit) => target.hasCard((card, playerx) => get.suit(card, playerx) === suit, "h")).every((suit) => target.hasCard((card, playerx) => get.suit(card, playerx) === suit && !lib.filter.cardDiscardable(card, player), "h"))) {
        if (target.countCards("h")) {
          await target.showHandcards();
        }
        return;
      }
      const list = get.addNewRowList(target.getCards("h"), "suit", target);
      const result = await player.chooseButton(
        [
          [
            [[`清正：弃置${get.translation(target)}一种花色的所有牌`], "addNewRow"],
            [
              (dialog) => {
                dialog.classList.add("fullheight");
                dialog.forcebutton = false;
                dialog._scrollset = false;
              },
              "handle"
            ],
            list.map((item) => [Array.isArray(item) ? item : [item], "addNewRow"])
          ]
        ],
        true
      ).set("filterButton", (button) => {
        const player2 = get.player();
        if (!button.links.length || button.links.some((card) => !lib.filter.cardDiscardable(card, player2, get.event().getParent().name))) {
          return false;
        }
        return true;
      }).set("ai", (button) => {
        get.player();
        return button.links.length;
      }).forResult();
      if (!result?.links?.length) {
        return;
      }
      await target.showHandcards();
      const cards2 = target.getCards("h", (card) => result.links.includes(get.suit(card, target)));
      if (cards2.length) {
        await target.modedDiscard(cards2, player);
      }
      if (cards1.length > cards2.length) {
        await target.damage(player);
      }
    },
    persevereSkill: false
  },
  yjzhuni: {
    inherit: "jsrgzhuni",
    async contentAfter(event, trigger, player) {
      const num = event.getParent().maxTicket;
      if (num && typeof num === "number") {
        await player.draw(num);
      }
    }
  },
  yjtuonan: {
    trigger: {
      player: "dying"
    },
    mark: true,
    skillAnimation: true,
    limited: true,
    animationColor: "orange",
    unique: true,
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.recover();
      const skills2 = player.getSkills(null, false, false).filter((skill) => {
        const info = get.info(skill);
        if (!info || info.charlotte || get.skillInfoTranslation(skill, player).length === 0) {
          return false;
        }
        const list = get.skillCategoriesOf(skill, player);
        list.removeArray(["Charlotte"]);
        return list.length;
      });
      if (!skills2.length) {
        return;
      }
      const result = skills2.length > 1 ? await player.chooseButton(["脱难：失去一个有标签的技能", [skills2, "skill"]], true).set("displayIndex", false).set("ai", (button) => {
        const { link } = button;
        if (link === "petuonan") {
          return 2;
        }
        return 1;
      }).forResult() : { bool: true, links: skills2 };
      if (result?.bool && result?.links?.length) {
        await player.removeSkills(result.links);
      }
    }
  },
  // 车胄
  // 沟槽的秘密指定还在追我
  psanmou: {
    trigger: {
      player: "enterGame",
      global: "phaseBefore"
    },
    forced: true,
    filter(event, player) {
      return (event.name !== "phase" || game.phaseNumber === 0) && game.hasPlayer((target) => target !== player);
    },
    async content(event, trigger, player) {
      const result = await player.chooseTarget(`暗谋：请暗中指定一名其他角色，然后你与其互相对对方使用牌无次数限制`, true, lib.filter.notMe).set("ai", (target) => -get.attitude(get.player(), target)).set("animate", false).forResult();
      if (result?.targets) {
        const target = result.targets[0];
        const skill = `${event.name}_effect`;
        player.addSkill(skill);
        player.updateStorage(skill, (storage) => storage.concat([target]).sortBySeat(), false);
        if (!player.storage[skill]) {
          player.storage[skill] = [];
        }
        player.storage[skill].add(target);
        player.storage[skill].sortBySeat();
        const func = (player2, target2, skill2) => {
          player2.markSkill(skill2, null, null, true);
        };
        if (player === game.me) {
          func(player, target, skill);
        } else if (event.isOnline()) {
          player.send(func, player, target, skill);
        }
        target.addSkill(skill);
        target.updateStorage(skill, (storage) => storage.concat([player]).sortBySeat(), false);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "players"
        },
        mod: {
          cardUsableTarget(card, player, target) {
            if (player.getStorage("psanmou_effect").includes(target)) {
              return Infinity;
            }
          }
        }
      }
    }
  },
  pstousuan: {
    trigger: { global: "damageBegin1" },
    filter(event, player) {
      const skill = "psanmou_effect";
      if (!event.source) {
        return false;
      }
      if (event.source !== player && event.player !== player) {
        return false;
      }
      return event.source.getStorage(skill).includes(event.player) && !event.source.getRoundHistory("sourceDamage", (evt) => evt.player === player).length;
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.num++;
      await player.draw(3);
      await player.removeSkills(event.name);
    },
    ai: {
      combo: "psanmou"
    }
  },
  // 雍闿
  // 你也有xiaofan？
  psxiaofan: {
    trigger: { global: ["loseAfter", "equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter", "damageEnd"] },
    filter(event, player, name, target) {
      if (!target?.isIn()) {
        return false;
      }
      if (event.name === "damage") {
        return target.group === "shu" && event.getParent().name !== "psxiaofan";
      }
      return true;
    },
    getIndex(event, player, name) {
      if (event.name === "damage") {
        return [event.player];
      }
      return game.filterPlayer((current) => {
        if (current.group !== "wu") {
          return false;
        }
        return event.getl?.(current)?.cards2?.some((card) => get.type(card) === "equip");
      }).sortBySeat();
    },
    logTarget: (event, player, triggername, target) => target,
    check(event, player, triggername, target) {
      if (event.name === "damage") {
        return get.damageEffect(target, player, player) > 0;
      }
      return [player, target].reduce((sum, i) => sum + get.effect(i, { name: "draw" }, player, player), 0) > 0;
    },
    prompt2(event, player, triggername, target) {
      if (event.name === "damage") {
        return `对${get.translation(target)}造成1点伤害${player.group !== "wu" ? "，然后你变更势力至吴" : ""}`;
      }
      return `与${get.translation(target)}各摸一张牌${player.group !== "qun" ? "，然后你变更势力至群" : ""}`;
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      if (trigger.name === "damage") {
        await target.damage();
        if (player.group !== "wu") {
          await player.changeGroup("wu");
        }
      } else {
        await game.asyncDraw([player, target].sortBySeat());
        if (player.group !== "qun") {
          await player.changeGroup("qun");
        }
      }
    },
    group: "psxiaofan_source",
    subSkill: {
      source: {
        trigger: { global: "damageSource" },
        filter(event, player) {
          return event.source?.group === "qun" && event.cards?.someInD();
        },
        prompt2(event, player) {
          return `获得${get.translation(event.cards.filterInD())}${player.group !== "shu" ? "，然后你变更势力至蜀" : ""}`;
        },
        async content(event, trigger, player) {
          if (trigger.cards?.someInD()) {
            await player.gain(trigger.cards.filterInD(), "gain2");
          }
          if (player.group !== "shu") {
            await player.changeGroup("shu");
          }
        }
      }
    }
  },
  psjiaohu: {
    groupSkill: "shu",
    trigger: { player: "phaseDrawBegin2" },
    filter(event, player) {
      return !event.numFixed && player.group === "shu";
    },
    forced: true,
    async content(event, trigger, player) {
      const target = game.findPlayer((current) => get.info("jsrgzhenglve").isFirst(current));
      let num = 1;
      if (target?.getDamagedHp()) {
        num += target.getDamagedHp();
      }
      trigger.num += num;
    }
  },
  psquanpan: {
    groupSkill: "wu",
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player) {
      if (!game.hasPlayer((current) => current !== player) || player.group !== "wu") {
        return false;
      }
      return event.getg?.(player).some((card) => get.owner(card) === player && get.position(card) === "h" && get.type(card) === "equip");
    },
    async cost(event, trigger, player) {
      const cards2 = trigger.getg(player).filter((card) => get.owner(card) === player && get.position(card) === "h" && get.type(card) === "equip");
      event.result = await player.chooseCardTarget({
        prompt: get.prompt(event.skill),
        prompt2: "展示并交给一名其他角色其中一张牌",
        filterCard(card) {
          return get.event().cards.includes(card);
        },
        filterTarget: lib.filter.notMe,
        ai1(card) {
          return 3 / (Math.abs(get.value(card)) + 0.1);
        },
        ai2(target) {
          const player2 = get.player();
          return get.value(ui.selected.cards, target) * get.attitude(player2, target);
        }
      }).set("cards", cards2).forResult();
    },
    async content(event, trigger, player) {
      const {
        cards: cards2,
        targets: [target]
      } = event;
      await player.showCards(cards2, `${get.translation(player)}发动了【${get.translation(event.name)}】`);
      await player.give(cards2, target);
    }
  },
  pshuoluan: {
    groupSkill: "qun",
    trigger: { global: "damageBegin1" },
    filter(event, player) {
      const { player: target, source } = event;
      if (!source) {
        return false;
      }
      const list = [target, source];
      if (!list.includes(player) || player.group !== "qun") {
        return false;
      }
      if (player === source) {
        list.reverse();
      }
      return list[1].group === "shu";
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.num++;
    }
  },
  //e系列纪灵
  yjshuangren: {
    audio: "shuangren",
    enable: "phaseUse",
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    filter(event, player) {
      return player.countCards("h") > 0;
    },
    async content(event, trigger, player) {
      const { bool } = await player.chooseToCompare(event.target).forResult();
      if (bool) {
        player.addTempSkill("yjshuangren_count");
        player.addMark("yjshuangren_count", 1, false);
        let num = 0;
        while (num < player.countMark("yjshuangren_count")) {
          num++;
          const card = { name: "sha", isCard: true };
          if (player.hasUseTarget(card)) {
            const result = await player.chooseUseTarget(card, false).forResult();
            if (!result.bool) {
              break;
            }
          } else {
            break;
          }
        }
      } else {
        player.addTempSkill("yjshuangren_viewas");
      }
    },
    ai: {
      order(name, player) {
        const cards2 = player.getCards("h");
        for (const card of cards2) {
          if (get.number(card) > 11 && get.value(card) < 7) {
            return 9;
          }
        }
        return get.order({ name: "sha" }) - 0.1;
      },
      result: {
        player(player) {
          const num = player.countCards("h");
          if (num > player.hp) {
            return 0;
          }
          if (num === 1) {
            return -1;
          }
          if (num === 2) {
            return -0.7;
          }
          return -0.5;
        },
        target(player, target) {
          const num = target.countCards("h");
          if (num === 1) {
            return -2;
          }
          if (num === 2) {
            return -1;
          }
          return -0.7;
        }
      },
      threaten: 1.3
    },
    subSkill: {
      count: {
        charlotte: true,
        onremove: true
      },
      viewas: {
        charlotte: true,
        mod: {
          cardname(card, player) {
            if (card.name === "sha") {
              return "shan";
            }
          },
          cardnumber(card) {
            if (card.name === "sha") {
              return 13;
            }
          }
        }
      }
    }
  },
  //线下幻系列
  yjqingjiao: {
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return player.hasHistory("sourceDamage", (evt) => evt.player.group === "qun" && evt.player !== player);
    },
    forced: true,
    zhuSkill: true,
    async content(event, trigger, player) {
      player.draw();
    }
  },
  //线下E系列
  //钟会
  psmouchuan: {
    trigger: {
      global: "roundStart"
    },
    async content(event, trigger, player) {
      await player.draw(2);
      if (!player.countCards("he") || !game.hasPlayer((current) => current !== player)) {
        return;
      }
      const { cards: cards2, targets } = await player.chooseCardTarget({
        forced: true,
        prompt: get.prompt("psmouchuan"),
        prompt2: "将一张牌交给一名其他角色",
        filterTarget: lib.filter.notMe,
        filterCard: true,
        position: "he",
        ai1(card) {
          return 6 - get.value(card);
        },
        ai2(target2) {
          const player2 = get.player();
          return get.attitude(player2, target2);
        }
      }).forResult();
      if (!cards2 || !cards2.length || !targets || !targets.length) {
        return;
      }
      const [target] = targets;
      await player.give(cards2, target);
      if ([player, target].some((i) => !i.countCards("h"))) {
        return;
      }
      let card1;
      let card2;
      if (player.countCards("h")) {
        const { cards: cardp } = await player.chooseCard("请展示一张手牌", true, "h").forResult();
        await player.showCards(cardp);
        card1 = cardp[0];
      }
      if (target.countCards("h")) {
        const { cards: cardt } = await target.chooseCard("请展示一张手牌", true, "h").forResult();
        await target.showCards(cardt);
        card2 = cardt[0];
      }
      if (card1 && card2) {
        const skill = get.color(card1, player) === get.color(card2, target) ? "psdaohe" : "pszhiyi";
        await player.addTempSkills(skill, "roundStart");
      }
    },
    derivation: ["psdaohe", "pszhiyi"]
  },
  pszizhong: {
    mod: {
      maxHandcard(player, num) {
        return num + get.info("jsrgjuxia").countSkill(player);
      }
    },
    trigger: {
      player: ["useCard", "respond"]
    },
    filter(event, player) {
      const num = get.info("jsrgjuxia").countSkill(player) - 2;
      if (num <= 0 || get.type(event.card) === "equip") {
        return false;
      }
      const name = get.name(event.card);
      const stat = player.getRoundHistory("useCard", (evt) => evt !== event && get.name(evt.card) === name).length + player.getRoundHistory("respond", (evt) => evt !== event && get.name(evt.card) === name).length;
      return stat === 0;
    },
    forced: true,
    async content(event, trigger, player) {
      const num = get.info("jsrgjuxia").countSkill(player) - 2;
      await player.draw(num);
    }
  },
  psjizun: {
    trigger: { player: "dyingAfter" },
    filter(event, player) {
      return player.isDamaged() || !player.hasSkill("psqingsuan");
    },
    forced: true,
    juexingji: true,
    skillAnimation: true,
    animationColor: "orange",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      if (!player.hasSkill("psqingsuan")) {
        await player.addSkills("psqingsuan");
      } else {
        await player.recoverTo(player.maxHp);
      }
    },
    derivation: "psqingsuan"
  },
  psqingsuan: {
    locked: true,
    zhuSkill: true,
    getEnemies(player) {
      const enemies = [];
      player.checkAllHistory("damage", (evt) => {
        if (evt.source && player.group !== evt.source.group) {
          enemies.add(evt.source);
        }
      });
      return enemies;
    },
    mod: {
      targetInRange(card, player, target) {
        if (get.info("psqingsuan").getEnemies(player).includes(target)) {
          return true;
        }
      },
      cardUsableTarget(card, player, target) {
        if (get.info("psqingsuan").getEnemies(player).includes(target)) {
          return true;
        }
      }
    }
  },
  psdaohe: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.hasPlayer((current) => current !== player && current.countCards("h"));
    },
    filterTarget(card, player, target) {
      return target !== player && target.countCards("h");
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.chooseToGive(player, "h", [1, Infinity], true, "allowChooseAll").set("ai", (card) => {
        const player2 = get.player();
        const target2 = get.event().target;
        const att = get.attitude(player2, target2);
        if (att <= 0) {
          if (ui.selected.cards.length) {
            return 0;
          }
          return 6 - get.value(card);
        }
        return target2.getUseValue(card);
      });
      await target.recover();
    },
    ai: {
      order: 6,
      result: {
        player(player, target) {
          if (target.isHealthy()) {
            return get.effect(target, { name: "shunshou_copy2" }, player, player);
          }
          return get.recoverEffect(target, player, player);
        }
      }
    }
  },
  pszhiyi: {
    enable: "phaseUse",
    usable: 1,
    filterTarget: true,
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.draw();
      await target.damage();
    },
    ai: {
      order: 1,
      result: {
        player(player, target) {
          return get.effect(target, { name: "draw" }, player, player) + get.damageEffect(target, player, player);
        }
      }
    }
  },
  //鄂焕
  psdiwan: {
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      return event.card.name === "sha" && event.isFirstTarget;
    },
    frequent: true,
    usable: 1,
    async content(event, trigger, player) {
      player.draw(trigger.targets.length);
    }
  },
  pssuiluan: {
    trigger: { player: "useCard2" },
    filter(event, player) {
      if (player.group !== "qun" || event.card.name !== "sha") {
        return false;
      }
      return game.countPlayer((target) => !event.targets.includes(target) && lib.filter.targetEnabled2(event.card, player, target) && lib.filter.targetInRange(event.card, player, target)) > 1;
    },
    groupSkill: "qun",
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(
        get.prompt2(event.skill),
        (card, player2, target) => {
          const event2 = get.event().getTrigger();
          return !event2.targets.includes(target) && lib.filter.targetEnabled2(event2.card, player2, target) && lib.filter.targetInRange(event2.card, player2, target);
        },
        2
      ).set("ai", (target) => {
        const player2 = get.event().player;
        const event2 = get.event().getTrigger();
        return get.effect(target, event2.card, player2);
      }).forResult();
    },
    async content(event, trigger, player) {
      trigger.targets.addArray(event.targets);
      player.addTempSkill("pssuiluan_effect");
      trigger.card.pssuiluan = true;
    },
    subSkill: {
      effect: {
        charlotte: true,
        trigger: { player: ["useCardAfter", "damageEnd"] },
        filter(event, player) {
          if (event.name === "damage") {
            return player.group !== "shu" && event.getParent(4).name === "pssuiluan_effect";
          }
          return event.card.pssuiluan && (event.targets || []).some((i) => i.isIn());
        },
        forced: true,
        popup: false,
        forceDie: true,
        async content(event, trigger, player) {
          if (trigger.name === "damage") {
            await player.changeGroup("shu");
            return;
          }
          const targets = trigger.targets.filter((i) => i.isIn()).sortBySeat();
          for (const target of targets) {
            await target.chooseToUse(
              (card, player2, event2, ...args) => {
                if (get.name(card) !== "sha") {
                  return false;
                }
                return lib.filter.filterCard(card, player2, event2, ...args);
              },
              `随乱：是否对${get.translation(player)}使用一张【杀】？`
            ).set("filterTarget", (card, player2, target2, ...args) => {
              if (target2 !== _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
                return false;
              }
              return lib.filter.filterTarget(card, player2, target2, ...args);
            }).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("sourcex", player);
          }
        }
      }
    }
  },
  psconghan: {
    trigger: { global: "damageSource" },
    filter(event, player) {
      if (player.group !== "shu" || !event.source || !event.player.isIn()) {
        return false;
      }
      return event.source.getSeatNum() === 1 && (player.hasSha() || _status.connectMode && player.countCards("hs"));
    },
    direct: true,
    groupSkill: "shu",
    seatRelated: true,
    clearTime: true,
    async content(event, trigger, player) {
      player.chooseToUse(
        (card, player2, event2, ...args) => {
          if (get.name(card) !== "sha") {
            return false;
          }
          return lib.filter.filterCard(card, player2, event2, ...args);
        },
        get.prompt2("psconghan", trigger.player)
      ).set("filterTarget", (card, player2, target, ...args) => {
        if (target !== _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
          return false;
        }
        return lib.filter.filterTarget(card, player2, target, ...args);
      }).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("logSkill", ["psconghan", trigger.player]).set("sourcex", trigger.player);
    }
  },
  //肘击
  psyanmou: {
    getCards(event, player) {
      const cards2 = [];
      if (event.name === "cardsDiscard") {
        const evt = event.getParent().relatedEvent;
        if (evt && evt.name === "judge" && evt.player !== player) {
          cards2.addArray(event.cards.filter((i) => get.position(i, true) === "d"));
        }
      } else {
        if (event.type === "discard" && event.getlx !== false) {
          for (const target of game.filterPlayer2()) {
            if (target === player) {
              continue;
            }
            const evt = event.getl(target);
            if (evt && (evt.cards2 || []).length) {
              cards2.addArray((evt.cards2 || []).filter((i) => i.original !== "j" && get.position(i, true) === "d"));
            }
          }
        }
      }
      return cards2.filter((card) => card.name === "huogong" || card.name === "sha" && game.hasNature(card, "fire"));
    },
    trigger: { global: ["cardsDiscardAfter", "loseAfter", "loseAsyncAfter"] },
    filter(event, player) {
      return lib.skill.psyanmou.getCards(event, player).length;
    },
    prompt2(event, player) {
      return `获得${get.translation(lib.skill.psyanmou.getCards(event, player))}`;
    },
    frequent: true,
    async content(event, trigger, player) {
      player.gain(lib.skill.psyanmou.getCards(trigger, player), "gain2");
    },
    group: "psyanmou_chooseToUse",
    subSkill: {
      chooseToUse: {
        trigger: {
          player: "gainAfter",
          global: "loseAsyncAfter"
        },
        filter(event, player) {
          return event.getg && event.getg(player).length;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          let cards2 = trigger.getg(player);
          await player.showCards(cards2, `${get.translation(player)}发动了【炎谋】`);
          cards2 = cards2.filter((card) => {
            if (!player.hasUseTarget(card) || get.owner(card) !== player) {
              return false;
            }
            return get.name(card) === "huogong" || get.name(card) === "sha" && game.hasNature(card, "fire");
          });
          if (cards2.length) {
            await player.chooseToUse((card, player2, event2, ...args) => {
              if (get.itemtype(card) !== "card" || !get.event().cards.includes(card)) {
                return false;
              }
              return lib.filter.filterCard(card, player2, event2, ...args);
            }, "炎谋：选择使用其中的一张【火攻】或火【杀】").set("cards", cards2).set("filterTarget", (card, player2, target, ...args) => lib.filter.filterTarget(card, player2, target, ...args)).set("targetRequired", true).set("complexSelect", true).set("forced", true).set("addCount", false);
          }
        }
      }
    }
  },
  pszhanyan: {
    enable: "phaseUse",
    filter(event, player) {
      return game.hasPlayer((target) => player.inRange(target));
    },
    usable: 1,
    filterTarget(card, player, target) {
      return player.inRange(target);
    },
    selectTarget: -1,
    multitarget: true,
    multiline: true,
    delay: 0,
    async content(event, trigger, player) {
      const targets = event.targets.sortBySeat();
      let damages = 0;
      let puts = 0;
      player.line(targets);
      await game.delay();
      for (const target of targets) {
        const dialog = ["绽焰：将手牌中或弃牌堆中的一张【火攻】或火【杀】置于牌堆顶，或受到1点火焰伤害"];
        const Tcards = target.getCards("h", (card) => get.name(card) === "huogong" || get.name(card) === "sha" && game.hasNature(card, "fire"));
        const Pcards = Array.from(ui.discardPile.childNodes).filter((card) => card.name === "huogong" || card.name === "sha" && game.hasNature(card, "fire"));
        if (Tcards.length) {
          dialog.push('<div class="text center">手牌区</div>');
          dialog.push(Tcards);
        }
        if (Pcards.length) {
          dialog.push('<div class="text center">弃牌堆</div>');
          dialog.push(Pcards);
        }
        let result;
        if (Tcards.length + Pcards.length === 0) {
          result = { bool: false };
        } else {
          result = await target.chooseButton(dialog).set("ai", (button) => {
            const player2 = get.event().player;
            const source = get.event().getParent().player;
            if (get.damageEffect(source, player2, player2) <= 0 && get.attitude(player2, source) <= 0) {
              return 0;
            }
            if (!get.owner(button.link)) {
              return 114514;
            }
            return 20 - get.value(button.link);
          }).forResult();
        }
        if (result.bool) {
          puts++;
          const card = result.links[0];
          target.$throw([card], 1e3);
          if (get.owner(card)) {
            await get.owner(card).lose([card], ui.cardPile);
          } else {
            ui.discardPile.removeChild(card);
          }
          await game.cardsGotoPile(card, "insert");
          game.updateRoundNumber();
          game.log(target, `将${get.translation(card)}置于牌堆顶`);
        } else {
          damages++;
          await target.damage(1, "fire");
        }
        await game.delay(0.5);
      }
      const num = Math.min(damages, puts);
      if (num) {
        await player.draw(num);
      }
    },
    ai: {
      order: 9,
      result: { player: 1 }
    }
  },
  psyuhuo: {
    trigger: { player: "damageBegin4" },
    filter(event) {
      return event.hasNature("fire");
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.cancel();
    },
    ai: {
      nofire: true,
      effect: {
        target(card, player, target, current) {
          if (get.tag(card, "fireDamage")) {
            return "zeroplayertarget";
          }
        }
      }
    },
    mod: {
      cardDiscardable(card, player, name) {
        if (name === "phaseDiscard" && (get.name(card) === "huogong" || get.name(card) === "sha" && game.hasNature(card, "fire"))) {
          return false;
        }
      },
      ignoredHandcard(card, player) {
        if (get.name(card) === "huogong" || get.name(card) === "sha" && game.hasNature(card, "fire")) {
          return true;
        }
      }
    }
  },
  //田钏
  pshuying: {
    trigger: {
      global: ["phaseBefore", "dieAfter"],
      player: "enterGame"
    },
    forced: true,
    filter(event, player) {
      if (event.name === "die") {
        return event.player !== player;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async content(event, trigger, player) {
      const cards2 = [];
      const num = trigger.name === "die" ? 1 : 2;
      while (cards2.length < num) {
        const card = game.createCard2("xingbian", "spade", 9);
        cards2.push(card);
      }
      if (cards2.length) {
        await player.gain(cards2, "gain2");
      }
    },
    mod: {
      ignoredHandcard(card, player) {
        if (card.name === "xingbian") {
          return true;
        }
      },
      cardDiscardable(card, player, name) {
        if (name === "phaseDiscard" && card.name === "xingbian") {
          return false;
        }
      },
      globalTo(from, to, num) {
        let count = 0;
        game.filterPlayer((current) => {
          count += current.countCards("ej", (card) => card.name === "xingbian");
        });
        return num + count;
      }
    }
  },
  psqianjing: {
    trigger: {
      player: "damageEnd",
      source: "damageSource"
    },
    filter(event, player) {
      if (!player.countCards("h", (card) => card.name === "xingbian")) {
        return false;
      }
      return game.hasPlayer((current) => current.hasEnabledSlot());
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        filterCard(card) {
          return card.name === "xingbian";
        },
        position: "h",
        prompt: get.prompt(event.skill),
        prompt2: "将手牌中的一张【刑鞭】置入一名角色装备区",
        filterTarget(card, player2, target) {
          return target.hasEnabledSlot();
        },
        ai1(card) {
          return 10 - get.value(card);
        },
        ai2(target) {
          const player2 = get.player();
          if (target === player2) {
            return 1;
          }
          if (get.attitude(player2, target) < 0) {
            return 3;
          }
          return 0;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const cardx = event.cards[0];
      const choices = [];
      for (const i of [0, 1, 2, 3, 4, 5]) {
        if (target.hasEquipableSlot(i)) {
          choices.push(`equip${i}`);
        }
      }
      if (!choices.length) {
        return;
      }
      const result = await player.chooseControl(choices).set("prompt", `请选择为${get.translation(target)}置入【刑鞭】的装备栏`).set("ai", () => _status.event.controls.randomGet()).forResult();
      const card = get.autoViewAs(cardx);
      card.subtypes = [result.control];
      player.$give(card, target, false);
      await target.equip(card);
      if (target === player) {
        await player.draw();
      }
    },
    group: "psqianjing_use",
    subSkill: {
      use: {
        enable: "chooseToUse",
        filter(event, player) {
          if (!event.filterCard(get.autoViewAs({ name: "sha" }, "unsure"), player, event)) {
            return false;
          }
          if (player.countCards("h", (card) => card.name === "xingbian")) {
            return true;
          }
          return game.hasPlayer((current) => current.countCards("ej", (card) => card.name === "xingbian"));
        },
        delay: false,
        locked: false,
        prompt: "将场上或你手牌中的一张【刑鞭】当作【杀】使用",
        filterTarget(card, player, target) {
          const event = _status.event;
          let evt = event;
          if (event._backup) {
            evt = event._backup;
          }
          const pos = target === player ? "hej" : "ej";
          return target.countCards(pos, (card2) => {
            if (card2.name !== "xingbian") {
              return false;
            }
            const sha = get.autoViewAs({ name: "sha", storage: { qianjing: true } }, [card2]);
            if (evt.filterCard(sha, player, event)) {
              return game.hasPlayer((current) => evt.filterTarget(sha, player, current));
            }
          });
        },
        async content(event, trigger, player) {
          const evt = event.getParent(2);
          const target = event.targets[0];
          evt.set("xingbian", true);
          const result = await player.choosePlayerCard(true, target, target === player ? "hej" : "ej").set("filterButton", (button) => {
            const card = button.link;
            return card.name === "xingbian";
          }).forResult();
          game.broadcastAll(
            (result2, name) => {
              lib.skill.psqianjing_backup.viewAs = {
                name,
                cards: [result2],
                storage: { qianjing: true }
              };
              lib.skill.psqianjing_backup.prompt = `选择${get.translation(name)}（${get.translation(result2)}）的目标`;
            },
            result.links[0],
            "sha"
          );
          evt.set("_backupevent", "psqianjing_backup");
          evt.backup("psqianjing_backup");
          evt.set("openskilldialog", `选择杀（${get.translation(result.links[0])}）的目标`);
          evt.set("norestore", true);
          evt.set("custom", {
            add: {},
            replace: { window() {
            } }
          });
          evt.goto(0);
        },
        ai: {
          respondSha: true,
          skillTagFilter(player, tag) {
            const func = (card) => card.name === "xingbian";
            return game.hasPlayer((current) => current.countCards(current === player ? "hej" : "ej", func));
          },
          order: 1,
          result: {
            player(player, target) {
              if (_status.event.type !== "phase") {
                return 1;
              }
              if (!player.hasValueTarget({ name: "sha" })) {
                return 0;
              }
              return 0.1;
            }
          }
        }
      },
      backup: {
        async precontent(event, trigger, player) {
          const cards2 = event.result.card.cards;
          event.result.cards = cards2;
          const owner = get.owner(cards2[0]);
          event.target = owner;
          owner.$give(cards2[0], player, false);
          player.popup(event.result.card.name, "metal");
          game.delayx();
          event.getParent().addCount = false;
        },
        filterCard: () => false,
        prompt: "请选择【杀】的目标",
        selectCard: -1,
        log: false
      }
    }
  },
  psbianchi: {
    trigger: {
      player: "phaseJieshuEnd"
    },
    limited: true,
    skillAnimation: true,
    animationColor: "metal",
    logTarget(event, player) {
      return game.filterPlayer((current) => current.countCards("ej", (card) => card.name === "xingbian"));
    },
    filter(event, player) {
      const targets = lib.skill.psbianchi.logTarget(event, player);
      return targets && targets.length;
    },
    check(event, player) {
      const targets = lib.skill.psbianchi.logTarget(event, player);
      let eff = 0;
      for (const target of targets) {
        eff += get.sgnAttitude(player, target);
      }
      return eff < 0;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const lose_list = [];
      for (const target of event.targets) {
        lose_list.push([target, target.getCards("ej", (card) => card.name === "xingbian")]);
      }
      await game.loseAsync({
        lose_list,
        discarder: player
      }).setContent("discardMultiple");
      for (const target of event.targets) {
        const result = await target.chooseControl().set("choiceList", [`令${get.translation(player)}操控你执行一个仅能使用两张牌的出牌阶段`, "失去2点体力"]).set(
          "choice",
          (() => {
            if (get.attitude(target, player) > 0) {
              return "选项一";
            }
            if (get.effect(target, { name: "losehp" }, target, target) > 0 && target.hp > 2) {
              return "选项二";
            }
            return "选项一";
          })()
        ).set("ai", () => _status.event.choice).forResult();
        if (result.control === "选项一") {
          target.addTempSkill("psbianchi_control", { player: "phaseUseEnd" });
          const next = target.phaseUse();
          next.owner = ["psbianchi", player];
          await next;
        } else {
          await target.loseHp(2);
        }
      }
    },
    subSkill: {
      control: {
        forced: true,
        charlotte: true,
        direct: true,
        trigger: {
          player: "phaseUseBefore"
        },
        filter(event, player) {
          return !player._trueMe && event?.owner?.[1].isIn() && player !== event.owner[1];
        },
        async content(event, trigger, player) {
          const owner = trigger.owner[1];
          player._trueMe = owner;
          game.addGlobalSkill("autoswap");
          if (player === game.me) {
            game.notMe = true;
            if (!_status.auto) {
              ui.click.auto();
            }
          }
        },
        mod: {
          cardEnabled(card, player) {
            const history = player.getHistory("useCard", (evt) => {
              const phaseUse = evt.getParent("phaseUse", true);
              return phaseUse?.player === player && phaseUse.owner?.[0] === "psbianchi";
            });
            if (history?.length >= 2) {
              return false;
            }
          },
          cardUsable(card, player) {
            return lib.skill.psbianchi_control.mod.cardEnabled.apply(this, arguments);
          },
          cardSavable(card, player) {
            return lib.skill.psbianchi_control.mod.cardEnabled.apply(this, arguments);
          }
        },
        onremove(player) {
          if (player._trueMe) {
            if (player === game.me) {
              if (!game.notMe) {
                game.swapPlayerAuto(player._trueMe);
              } else {
                delete game.notMe;
              }
              if (_status.auto) {
                ui.click.auto();
              }
            }
            delete player._trueMe;
          }
        }
      }
    }
  },
  xingbian_skill: {
    equipSkill: true,
    mod: {
      attackRange(player, distance) {
        return distance + player.countCards("e", (card) => card.name === "xingbian");
      }
    },
    trigger: {
      player: "phaseUseBegin"
    },
    forced: true,
    intro: {
      content(storage, player) {
        let str = "";
        for (const arg of storage) {
          str += `${get.translation(arg[0])}命你攻击${get.translation(arg[1])}<br>`;
        }
        return str.slice(0, -4);
      }
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => get.nameList(current).includes("yj_tianchuan"));
    },
    filter(event, player) {
      const targets = lib.skill.xingbian_skill.logTarget(event, player);
      return targets && targets.length;
    },
    async content(event, trigger, player) {
      for (const target of event.targets) {
        const result = await target.chooseTarget(`刑鞭：为${get.translation(player)}指定塔塔开目标`, true, (card, player2, targetx) => targetx !== _status.event.owner).set("owner", player).set("ai", (target2) => get.distance(_status.event.owner, target2) + 1).forResult();
        if (result.bool) {
          if (!player.getStorage("xingbian_skill").length) {
            player.when("phaseJieshuBegin").step(async () => {
              while (player.storage.xingbian_skill.length) {
                const args = player.storage.xingbian_skill.shift();
                let damage = true;
                if (player.getHistory("useCard", (evt) => evt.card.name === "sha" && evt.targets?.includes(args[1])).length) {
                  damage = false;
                }
                if (player.getHistory("sourceDamage", (evt) => evt.player === args[1]).length) {
                  damage = false;
                }
                if (damage === true) {
                  args[0].chat("该罚！");
                  args[0].line(player, "green");
                  await player.damage(player);
                }
              }
              player.unmarkSkill("xingbian_skill");
              delete player.storage.xingbian_skill;
            });
            player.storage.xingbian_skill = [];
          }
          target.line(result.targets[0], "green");
          player.storage.xingbian_skill.push([target, result.targets[0]]);
          player.markSkill("xingbian_skill");
        }
      }
    }
  }
};
const skills$t = {
  //神魔孙权（魔不如神这一块）
  smsibian: {
    audio: 2,
    forced: true,
    juexingji: true,
    skillAnimation: true,
    animationColor: "thunder",
    initGroup: "shen",
    trigger: { player: "phaseBegin" },
    filter(event, player) {
      return player.getHp() % 2 === 1 && player.countMark("smqihua_shen") > player.countMark("smqihua_mo") || player.getHp() % 2 === 0 && player.countMark("smqihua_shen") < player.countMark("smqihua_mo");
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const num = player.getHp();
      if (player.getHp() % 2 === 1 && player.countMark("smqihua_shen") > player.countMark("smqihua_mo")) {
        player.changeSkin({ characterName: "sm_shenmo_sunquan" }, "sm_shen_sunquan");
        await player.changeSkills(["smshenjiang", "smshengshou", "smshifeng"], ["smsibian", "smqihua", "smdue"]);
        await player.changeGroup("shen");
        player.maxHp = 10;
        const next = game.createEvent("SmhuashenAfter", false);
        next.player = player;
        next.num = num;
        next.setContent("emptyEvent");
      } else {
        player.changeSkin({ characterName: "sm_shenmo_sunquan" }, "sm_mo_sunquan");
        await player.changeSkills(["smmobian", "smyanshi", "smpoyu"], ["smsibian", "smqihua", "smdue"]);
        await player.changeGroup("devil");
        player.maxHp = 3;
        const next = game.createEvent("SmrumoAfter", false);
        next.player = player;
        next.num = num;
        next.setContent("emptyEvent");
      }
    }
  },
  smqihua: {
    audio: 2,
    trigger: { global: "damageBegin4" },
    filter(event, player) {
      if (!event.num || !event.player?.isIn()) {
        return false;
      }
      return event.player !== player && !player.getStorage("smqihua_used").includes("other") || player === event.player && !player.getStorage("smqihua_used").includes("me");
    },
    async cost(event, trigger, player) {
      const target = trigger.player;
      const num = trigger.num;
      if (player !== target) {
        event.result = await player.chooseBool({
          prompt: get.prompt(event.skill, target),
          prompt2: `失去${num}点体力防止此伤害并获得等量“神格”`,
          ai() {
            const { player: player2, target: target2, num: num2 } = get.event();
            if (get.attitude(player2, target2) > 2 && player2.getHp() > num2) {
              return 1;
            }
            return 0;
          }
        }).set("target", target).set("num", num).forResult();
        if (event.result?.bool) {
          event.result.targets = [target];
        }
      } else {
        event.result = await player.chooseTarget({
          prompt: get.prompt(event.skill),
          prompt2: `获得${num}个“魔心”并令一名其他角色失去等量体力`,
          filterTarget: lib.filter.notMe,
          ai(target2) {
            const player2 = get.player();
            return get.effect(target2, { name: "losehp" }, player2, player2);
          }
        }).forResult();
      }
    },
    async content(event, trigger, player) {
      const target = trigger.player;
      const num = trigger.num;
      player.addTempSkill(`${event.name}_used`);
      player.markAuto(`${event.name}_used`, [player === target ? "me" : "other"]);
      if (player !== target) {
        await player.loseHp(num);
        trigger.cancel();
        player.addMark(`${event.name}_shen`, num);
      } else {
        player.addMark(`${event.name}_mo`, num);
        await event.targets[0].loseHp(num);
      }
    },
    subSkill: {
      used: { charlotte: true, onremove: true },
      shen: { marktext: "神格", intro: { content: "mark" } },
      mo: { marktext: "魔心", intro: { content: "mark" } }
    }
  },
  smdue: {
    audio: 2,
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    trigger: { player: "changeHpAfter" },
    filter(event, player) {
      return ["smqihua_mo", "smqihua_shen"].some((mark) => player.hasMark(mark)) && player.countMark("smqihua_mo") !== player.countMark("smqihua_shen");
    },
    check(event, player) {
      return player.countMark("smqihua_mo") > player.countMark("smqihua_shen");
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const num1 = player.countMark("smqihua_mo");
      const num2 = player.countMark("smqihua_shen");
      player.clearMark("smqihua_mo");
      player.clearMark("smqihua_shen");
      if (num1 > 0) {
        player.addMark("smqihua_shen", num1);
      }
      if (num2 > 0) {
        player.addMark("smqihua_mo", num2);
      }
    }
  },
  smshenjiang: {
    audio: 2,
    forced: true,
    trigger: { player: "SmhuashenAfter" },
    async content(event, trigger, player) {
      if (trigger.num > 0 && player.getHp() < 2 * trigger.num) {
        await player.recoverTo(trigger.num * 2);
      }
      if (player.getHp() > 2 * trigger.num) {
        await player.loseHp(player.getHp() - 2 * trigger.num);
      }
      const cards2 = player.getCards("e", (card) => ["equip1", "equip4"].includes(get.subtype(card)));
      if (cards2.length) {
        await player.modedDiscard(cards2);
      }
    }
  },
  smshengshou: {
    audio: 2,
    enable: "phaseUse",
    usable(skill, player) {
      return player.countMark("smqihua_shen");
    },
    filter(event, player) {
      if (player !== _status.currentPhase) {
        return false;
      }
      return game.hasPlayer((current) => get.info("smshengshou").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return get.inpileVCardList((info) => {
        if (["equip", "delay"].includes(info[0])) {
          return false;
        }
        const card2 = get.autoViewAs({ name: info[2], nature: info[3], isCard: true }, "unsure");
        if (get.is.damageCard(card2)) {
          return false;
        }
        return target.hasUseTarget(card2);
      }).length;
    },
    async content(event, trigger, player) {
      const target = event.target;
      const list = get.inpileVCardList((info) => {
        if (["equip", "delay"].includes(info[0])) {
          return false;
        }
        const card = get.autoViewAs({ name: info[2], nature: info[3], isCard: true }, "unsure");
        if (get.is.damageCard(card)) {
          return false;
        }
        return target.hasUseTarget(card);
      });
      const result = await player.chooseButton({
        createDialog: [`圣授：令${get.translation(target)}视为使用一张非伤害牌`, [list, "vcard"]],
        filterButton(button) {
          const card = get.autoViewAs({ name: button.link[2], nature: button.link[3], isCard: true }, "unsure");
          return get.event().target.hasUseTarget(card);
        },
        ai(button) {
          const card = get.autoViewAs({ name: button.link[2], nature: button.link[3], isCard: true }, "unsure");
          return get.event().target.getUseValue(card);
        },
        forced: true
      }).set("target", target).forResult();
      if (result?.bool && result.links?.length) {
        const card = get.autoViewAs({ name: result.links[0][2], nature: result.links[0][3], isCard: true }, "unsure");
        if (target.hasUseTarget(card)) {
          await target.chooseUseTarget(card, true);
        }
      }
    },
    ai: {
      order: 10,
      result: {
        player: 1,
        target: 1
      }
    }
  },
  smshifeng: {
    audio: 2,
    forced: true,
    trigger: { player: "equipBegin" },
    filter(event, player) {
      return ["equip1", "equip4"].includes(get.subtype(event.card));
    },
    async content(event, trigger, player) {
      trigger.cancel();
      if (trigger.cards?.length) {
        await player.modedDiscard(trigger.cards);
      }
      const skill = get.info(event.name).derivation.slice(0).removeArray(player.getSkills(null, false, false)).randomGet();
      if (skill) {
        await player.addSkills(skill);
      }
    },
    derivation: ["yinghun", "hongde", "bingyi", "smshifeng_guanwei", "bizheng", "anguo", "shelie", "wengua", "rebotu", "rezhiheng", "mbjiexun", "xiashu", "rejieyin", "oldimeng", "xinfu_guanchao", "drlt_jueyan", "lanjiang", "anxu"],
    subSkill: {
      //小巧思观微
      guanwei: {
        audio: "xinfu_guanwei",
        inherit: "xinfu_guanwei",
        filter(event, player) {
          const history = event.player.getHistory("useCard");
          let num = 0;
          let suit = false;
          for (const evt of history) {
            const suit2 = get.suit(evt.card);
            if (suit && suit !== suit2) {
              return false;
            }
            suit = suit2;
            num++;
          }
          return num > 1;
        }
      }
    }
  },
  smmobian: {
    audio: 2,
    forced: true,
    trigger: { player: "SmrumoAfter" },
    async content(event, trigger, player) {
      const num = Math.floor(trigger.num / 2);
      if (player.maxHp > num) {
        await player.loseMaxHp(player.maxHp - num);
      }
      if (player.maxHp < num) {
        await player.gainMaxHp(num - player.maxHp);
      }
      if (num > 0 && player.getHp() < num) {
        await player.recoverTo(num);
      }
      const cards2 = player.getCards("e", (card) => ["equip2", "equip3"].includes(get.subtype(card))).concat(player.getCards("j"));
      if (cards2.length) {
        await player.modedDiscard(cards2);
      }
    }
  },
  smyanshi: {
    audio: 2,
    enable: "chooseToUse",
    locked: false,
    mod: {
      cardUsable(card) {
        if (card.storage?.smyanshi) {
          return Infinity;
        }
      },
      targetInRange(card) {
        if (card.storage?.smyanshi) {
          return true;
        }
      }
    },
    getUsed: () => game.getGlobalHistory("useCard", (evt) => get.is.damageCard(evt.card)).map((evt) => get.name(evt.card)).toUniqued(),
    hiddenCard(player, name) {
      if (player !== _status.currentPhase) {
        return false;
      }
      if (["equip", "delay"].includes(get.type(name))) {
        return false;
      }
      if (get.info("smyanshi").getUsed().includes(name)) {
        return false;
      }
      return lib.inpile.includes(name) && get.is.damageCard(name);
    },
    usable(skill, player) {
      return Math.max(1, player.countMark("smqihua_mo") - player.maxHp);
    },
    filter(event, player) {
      if (player !== _status.currentPhase) {
        return false;
      }
      return get.inpileVCardList((info) => {
        const card = get.autoViewAs({ name: info[2], nature: info[3], storage: { smyanshi: true } }, "unsure");
        if (["equip", "delay"].includes(info[0])) {
          return false;
        }
        if (!get.is.damageCard(card)) {
          return false;
        }
        return !(event.smyanshi || []).includes(info[2]) && event.filterCard(card, player, event);
      }).length;
    },
    onChooseToUse(event) {
      if (!game.online && !event.smyanshi) {
        event.player;
        event.set("smyanshi", get.info("smyanshi").getUsed());
      }
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          const card = get.autoViewAs({ name: info[2], nature: info[3], storage: { smyanshi: true } }, "unsure");
          if (["equip", "delay"].includes(info[0])) {
            return false;
          }
          if (!get.is.damageCard(card)) {
            return false;
          }
          return !(event.smyanshi || []).includes(info[2]) && event.filterCard(card, player, event);
        });
        return ui.create.dialog("魇噬", [list, "vcard"]);
      },
      check(button) {
        if (get.event().getParent().type !== "phase") {
          return 1;
        }
        return get.event().player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          audio: "smyanshi",
          filterCard: () => false,
          selectCard: -1,
          popname: true,
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            storage: { smyanshi: true }
          },
          async precontent(event, trigger, player2) {
            event.getParent().addCount = false;
          }
        };
      },
      prompt(links, player2) {
        return `视为使用一张${get.translation(links[0][3] || "")}【${get.translation(links[0][2])}】`;
      }
    },
    ai: {
      order: 5,
      result: {
        player: 1
      }
    }
  },
  smpoyu: {
    audio: 2,
    forced: true,
    trigger: { player: "equipBegin" },
    filter(event, player) {
      return ["equip2", "equip3"].includes(get.subtype(event.card));
    },
    async content(event, trigger, player) {
      trigger.cancel();
      if (trigger.cards?.length) {
        await player.modedDiscard(trigger.cards);
      }
      const choices = ["baihong", "qingming", "bixie", "zidian", "baili", "liuxing"].removeArray(player.getStorage(`${event.name}_effect`));
      if (!choices.length) {
        return;
      }
      const result = choices.length > 1 ? await player.chooseButton({
        createDialog: [
          `###破御###<div class="text center">请为伤害牌永久添加一个“吴六剑”效果</div>`,
          [
            [
              ["baihong", "白虹：基础伤害改为2"],
              ["qingming", "青冥：可以额外指定一个目标"],
              ["bixie", "辟邪：无视防具"],
              ["zidian", "紫电：不可响应"],
              ["baili", "百里：额外结算一次"],
              ["liuxing", "流星：无次数限制"]
            ],
            "textbutton"
          ]
        ],
        filterButton(button, player2) {
          return !player2.getStorage("smpoyu_effect").includes(button.link);
        },
        ai(button) {
          const player2 = get.player();
          if (!player2.getStorage("smpoyu_effect").includes("baili")) {
            return button.link === "baili";
          }
          return 1 + Math.random();
        }
      }).forResult() : { bool: true, links: choices };
      if (result?.bool && result.links?.length) {
        player.addSkill(`${event.name}_effect`);
        player.markAuto(`${event.name}_effect`, result.links);
      }
    },
    subSkill: {
      effect: {
        audio: "smpoyu",
        charlotte: true,
        forced: true,
        onremove: true,
        trigger: { player: "useCard" },
        marktext: "吴六剑",
        intro: {
          name: "吴六剑",
          content(storage, player) {
            let str = "当前拥有效果：";
            const list = [
              ["baihong", "白虹：基础伤害改为2"],
              ["qingming", "青冥：可以额外指定一个目标"],
              ["bixie", "辟邪：无视防具"],
              ["zidian", "紫电：不可响应"],
              ["baili", "百里：额外结算一次"],
              ["liuxing", "流星：无次数限制"]
            ];
            for (const i of list) {
              if (storage.includes(i[0])) {
                str += `<li>${i[1]}`;
              }
            }
            return str;
          }
        },
        filter(event, player) {
          return get.is.damageCard(event.card) && player.getStorage("smpoyu_effect").length;
        },
        async content(event, trigger, player) {
          const storage = player.getStorage("smpoyu_effect");
          if (storage.includes("baihong")) {
            trigger.baseDamage ??= 1;
            trigger.baseDamage++;
            game.log(trigger.card, "基础伤害为2");
          }
          if (storage.includes("qingming")) {
            const targets = game.filterPlayer((target) => {
              if (!trigger.targets || trigger.targets.includes(target)) {
                return false;
              }
              return lib.filter.targetEnabled2(trigger.card, player, target) && lib.filter.targetInRange(trigger.card, player, target);
            });
            if (targets.length) {
              const result = await player.chooseTarget({
                prompt: `吴六剑：你可以为${get.translation(trigger.card)}额外指定一个目标`,
                filterTarget(card, player2, target) {
                  const trigger2 = get.event().triggerx;
                  if (trigger2.targets?.includes(target)) {
                    return false;
                  }
                  return lib.filter.targetEnabled2(trigger2.card, player2, target) && lib.filter.targetInRange(trigger2.card, player2, target);
                },
                ai(target) {
                  const { player: player2, triggerx: trigger2 } = get.event();
                  return get.effect(target, trigger2.card, player2, player2);
                }
              }).set("triggerx", trigger).forResult();
              if (result?.bool && result.targets?.length) {
                const targets2 = result.targets.sortBySeat();
                player.line(targets2);
                trigger.targets.addArray(targets2);
                game.log(targets2, "成为了", trigger.card, "的额外目标");
              }
            }
          }
          if (storage.includes("bixie")) {
            for (const target of trigger.targets) {
              target.addTempSkill("qinggang2");
              target.storage.qinggang2.add(trigger.card);
              target.markSkill("qinggang2");
            }
            game.log(trigger.card, "无视防具");
          }
          if (storage.includes("zidian")) {
            trigger.directHit.addArray(game.players);
            game.log(trigger.card, "不可被响应");
          }
          if (storage.includes("baili")) {
            trigger.effectCount++;
            game.log(trigger.card, "额外结算一次");
          }
          if (storage.includes("liuxing") && trigger.addCount !== false) {
            trigger.addCount = false;
            const stat = player.getStat().card;
            const name = trigger.card.name;
            if (typeof stat[name] === "number") {
              stat[name]--;
            }
          }
        },
        mod: {
          cardUsable(card, player) {
            if (get.is.damageCard(card) && player.getStorage("smpoyu_effect").includes("liuxing")) {
              return Infinity;
            }
          }
        },
        ai: {
          unequip_ai: true,
          skillTagFilter(player, tag, arg) {
            if (!arg?.card || !arg.target || !get.is.damageCard(arg.card)) {
              return false;
            }
            return player.getStorage("smpoyu_effect").includes("bixie");
          }
        }
      }
    }
  }
};
const skills$s = {
  //神曹操------by 清风
  psguixin: {
    audio: "guixin",
    trigger: {
      global: "roundStart",
      player: "damageEnd"
    },
    filter(event, player) {
      return game.hasPlayer((current) => current !== player && current.hasGainableCards(player, "he"));
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => current !== player && current.hasGainableCards(player, "he")).sortBySeat();
    },
    async content(event, trigger, player) {
      const targets = event.targets;
      await player.gainMultiple(targets, "he");
    },
    ai: {
      maixie: true,
      maixie_hp: true,
      threaten(player2, target) {
        if (target.hp === 1) {
          return 2.5;
        }
        return 0.5;
      },
      effect: {
        target(card, player2, target) {
          if (!target._psguixin_eff && get.tag(card, "damage") && target.hp > (player2.hasSkillTag("damageBonus", true, { card, target }) ? 2 : 1)) {
            if (player2.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            target._psguixin_eff = true;
            const gain = game.countPlayer((current) => {
              if (target === current) {
                return 0;
              }
              if (get.attitude(target, current) > 0) {
                if (current.hasCard((cardx) => lib.filter.canBeGained(cardx, target, current, "psguixin") && get.effect(current, cardx, current, current) < 0, "e")) {
                  return 1.3;
                }
                return 0;
              }
              if (current.hasCard((cardx) => lib.filter.canBeGained(cardx, target, current, "psguixin") && get.effect(current, cardx, current, current) > 0, "e")) {
                return 1.1;
              }
              if (current.hasCard((cardx) => lib.filter.canBeGained(cardx, target, current, "psguixin"), "h")) {
                return 0.9;
              }
              return 0;
            });
            delete target._psguixin_eff;
            return [1, Math.max(0, gain)];
          }
        }
      }
    }
  },
  psshenzun: {
    audio: 2,
    trigger: { source: "damageBegin2" },
    filter(event, player) {
      return event.player?.isIn() && player.group !== event.player.group && player.hasDiscardableCards(player, "he");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard({
        prompt: get.prompt(event.skill),
        prompt2: "弃置一张牌令此伤害+1",
        position: "he",
        ai(card) {
          const { player: player2, target } = get.event();
          if (get.damageEffect(target, player2, player2) > 0) {
            return 8 - get.value(card);
          }
          return 0;
        },
        chooseonly: true
      }).set("target", trigger.player).forResult();
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const cards2 = event.cards;
      await player.modedDiscard(cards2);
      trigger.num++;
      if (trigger.card && trigger.getParent(2).addCount !== false) {
        trigger.getParent(2).addCount = false;
        const stat = player.getStat("card");
        const name = trigger.card.name;
        if (typeof stat[name] === "number" && stat[name] > 0) {
          stat[name]--;
        }
      }
    }
  },
  psfeiying: {
    locked: true,
    mod: {
      targetInRange: () => true
    }
  },
  //官盗S系列关羽
  pszhonghun: {
    audio: "zhongyi",
    trigger: { player: ["useCard", "respond"] },
    filter(event, player) {
      return get.color(event.card) === "red";
    },
    frequent: true,
    async content(event, trigger, player) {
      const card = game.cardsGotoOrdering(get.cards()).cards[0];
      game.updateRoundNumber();
      await player.showCards(card, `${get.translation(player)}发动了【忠魂】`);
      if (get.color(card) === "red") {
        await player.gain({
          cards: [card],
          animate: "gain2"
        });
      }
    }
  },
  //官盗S系列郭嘉·一版
  psqizuo: {
    trigger: { global: ["damageBegin1", "damageBegin3"] },
    filter(event, player, name) {
      return name === "damageBegin1" && event.source && event.source.isIn() && player.inRange(event.source) || name === "damageBegin3" && event.player && event.player.isIn() && player.inRange(event.player);
    },
    direct: true,
    async content(event, trigger, player) {
      const name = event.triggername;
      const source = get.translation(trigger.source);
      const target = get.translation(trigger.player);
      const num = trigger.num;
      const targetx = trigger[name === "damageBegin1" ? "source" : "player"];
      const description = name === "damageBegin1" ? `${source}即将对${target}造成${num}点伤害` : `${target}即将受到${source}造成的${num}点伤害`;
      const effect = get.damageEffect(trigger.player, trigger.source, player);
      const goon = effect > 5 && !trigger.player.hasSkillTag("filterDamage", null, {
        player,
        card: trigger.card
      }) || effect < -5;
      const discardResult = await player.chooseToDiscard({
        prompt: get.prompt("psqizuo", targetx),
        prompt2: `${description}，是否弃置一张牌并判定，若结果颜色与此牌相同，你可以令此伤害+1或-1？`,
        position: "he"
      }).set("ai", (card) => {
        if (_status.event.goon) {
          return 5.25 - get.value(card) + (get.color(card) === get.color(_status.pileTop) ? 0.75 : 0);
        }
        return 0;
      }).set("goon", goon).set("logSkill", ["psqizuo", targetx]).forResult();
      if (!discardResult.bool) {
        return;
      }
      event.color = get.color(discardResult.cards[0], player);
      const judgeResult = await player.judge((card) => get.color(card) === _status.event.getParent("psqizuo").color ? 1 : 0).forResult();
      if (!judgeResult.bool) {
        return;
      }
      const controlResult = await player.chooseControl("+1", "-1", "cancel2").set("prompt", "是否令此伤害+1或-1？").set("ai", () => _status.event.eff < 0 ? 1 : 0).set("eff", get.damageEffect(trigger.player, trigger.source, player)).forResult();
      if (controlResult.index === 0) {
        trigger.num++;
        player.popup(" +1 ", "fire");
        game.log(player, "令此伤害+1");
      } else if (controlResult.index === 1) {
        trigger.num--;
        player.popup(" -1 ", "water");
        game.log(player, "令此伤害-1");
      }
    },
    ai: {
      threaten: 0.8
    }
  },
  //官盗S系列郭嘉·二版
  psquanmou: {
    trigger: {
      global: "useCardAfter"
    },
    direct: true,
    filter(event, player) {
      return get.type2(event.card) === "trick" && event.player !== player && event.targets?.includes(player) && event.cards.filterInD("odj").length > 0 && player.hasCards("h");
    },
    async content(event, trigger, player) {
      const discardResult = await player.chooseToDiscard(get.prompt("psquanmou"), `弃置一张${get.translation(get.color(trigger.card))}手牌，获得${get.translation(trigger.cards)}`, "h", (card) => get.color(card) === _status.event.color).set("ai", (card) => _status.event.value - get.value(card)).set("logSkill", "psquanmou").set("value", get.value(trigger.cards, player)).set("color", get.color(trigger.card)).forResult();
      if (!discardResult.bool) {
        return;
      }
      const cards2 = trigger.cards.filterInD("odj");
      if (cards2.filterInD("od").length) {
        await player.gain(cards2.filterInD("od"), "gain2");
      }
      if (cards2.filterInD("j").length) {
        await player.gain(cards2.filterInD("j"), get.owner(cards2.filterInD("j")[0]), "give");
      }
    }
  },
  //官盗S赵云·一版
  pshuiqiang: {
    trigger: { player: ["shaMiss", "eventNeutralized"] },
    direct: true,
    clearTime: true,
    filter(event, player) {
      if (!event.card || event.card.name !== "sha") {
        return false;
      }
      return event.target.isIn() && player.canUse("sha", event.target, false) && (player.hasSha() || _status.connectMode && player.hasCards("h"));
    },
    async content(event, trigger, player) {
      await player.chooseToUse({
        prompt: get.prompt2("pshuiqiang", trigger.target),
        filterCard(card, player2, event2) {
          if (get.name(card) !== "sha") {
            return false;
          }
          return lib.filter.filterCard.apply(this, arguments);
        },
        filterTarget: trigger.target,
        selectTarget: -1
      }).set("addCount", false).set("logSkill", "pshuiqiang");
    }
  },
  pshuntu: {
    trigger: { source: "damageSource" },
    usable: 1,
    filter(event, player) {
      return event.card && event.card.name === "sha" && event.getParent(2).player === player && event.notLink() && player.isPhaseUsing();
    },
    direct: true,
    clearTime: true,
    async content(event, trigger, player) {
      const result = await player.chooseToUse({
        prompt: get.prompt2("pshuntu", trigger.player),
        filterCard(card, player2, event2) {
          return get.name(card) === "sha" && lib.filter.filterCard.apply(this, arguments);
        },
        filterTarget: trigger.player,
        selectTarget: -1
      }).set("addCount", false).set("logSkill", "pshuntu").forResult();
      if (!result.bool) {
        player.storage.counttrigger.pshuntu--;
      }
    }
  },
  //官盗S赵云·二版
  psqijin: {
    trigger: { player: "phaseDrawBegin1" },
    filter(event, player) {
      return !event.numFixed;
    },
    async content(event, trigger, player) {
      trigger.changeToZero();
      const cards2 = get.cards(7);
      const orderingEvent = game.cardsGotoOrdering(cards2);
      const videoId = lib.status.videoId++;
      game.broadcastAll(
        (player2, id, cards3) => {
          const title = player2 === game.me && !_status.auto ? "七进：获得一种颜色的所有牌" : "七进";
          const dialog = ui.create.dialog(title, cards3);
          dialog.videoId = id;
        },
        player,
        videoId,
        cards2
      );
      const startTime = get.utc();
      game.addVideo("showCards", player, ["七进", get.cardsInfo(cards2)]);
      game.addVideo("delay", null, 2);
      await orderingEvent;
      const list = [];
      for (const card of cards2) {
        list.add(get.color(card, false));
      }
      list.sort();
      let choice = list[0];
      if (list.length > 0) {
        const cards1 = cards2.filter((card) => get.color(card) === list[0]);
        const cards22 = cards2.filter((card) => get.color(card) === list[1]);
        choice = get.value(cards1) * cards1.length > get.value(cards22) * cards22.length ? list[0] : list[1];
      }
      const result = await player.chooseControl(list).set("ai", () => _status.event.choice).set("choice", choice).forResult();
      const remainingTime = 1e3 - (get.utc() - startTime);
      if (remainingTime > 0) {
        await game.delay(0, remainingTime);
      }
      game.broadcastAll("closeDialog", videoId);
      await player.gain(
        cards2.filter((card) => get.color(card, false) === result.control),
        "gain2"
      );
    },
    ai: {
      threaten: 1.5
    }
  },
  psqichu: {
    enable: ["chooseToUse", "chooseToRespond"],
    hiddenCard(player, name) {
      if (player !== _status.currentPhase && !player.hasSkill("psqichu_used") && get.type(name) === "basic" && lib.inpile.includes(name)) {
        return true;
      }
    },
    filter(event, player) {
      if (event.responded || player === _status.currentPhase || player.hasSkill("psqichu_used")) {
        return false;
      }
      for (const name of lib.inpile) {
        if (get.type(name) === "basic" && event.filterCard({ name }, player, event)) {
          return true;
        }
      }
      return false;
    },
    delay: false,
    async content(event, trigger, player) {
      player.addTempSkill("psqichu_used");
      const parentEvent = event.getParent(2);
      const cards2 = get.cards(2, true);
      const hasAozhan = player.hasSkill("aozhan");
      const buttonResult = await player.chooseButton([`七出：选择要${parentEvent.name === "chooseToUse" ? "使用" : "打出"}的牌`, cards2]).set("filterButton", (button) => _status.event.cards.includes(button.link)).set(
        "cards",
        cards2.filter((card) => {
          if (get.type(card) !== "basic") {
            return false;
          }
          if (hasAozhan && card.name === "tao") {
            return parentEvent.filterCard(
              {
                name: "sha",
                isCard: true,
                cards: [card]
              },
              parentEvent.player,
              parentEvent
            ) || parentEvent.filterCard(
              {
                name: "shan",
                isCard: true,
                cards: [card]
              },
              parentEvent.player,
              parentEvent
            );
          }
          return parentEvent.filterCard(card, parentEvent.player, parentEvent);
        })
      ).set("ai", (button) => {
        const chooseEvent = _status.event.getParent(3);
        if (!chooseEvent?.ai) {
          return 1;
        }
        const currentEvent = _status.event;
        _status.event = chooseEvent;
        const result = (chooseEvent.ai || event.ai1)(button.link, _status.event.player, chooseEvent);
        _status.event = currentEvent;
        return result;
      }).forResult();
      if (!buttonResult.bool || !buttonResult.links?.length) {
        parentEvent.goto(0);
        return;
      }
      const selectedCard = buttonResult.links[0];
      let name = selectedCard.name;
      const aozhan = player.hasSkill("aozhan") && name === "tao";
      if (aozhan) {
        name = parentEvent.filterCard(
          {
            name: "sha",
            isCard: true,
            cards: [event.card]
          },
          parentEvent.player,
          parentEvent
        ) ? "sha" : "shan";
      }
      if (parentEvent.name !== "chooseToUse") {
        delete parentEvent.result.used;
        delete parentEvent.result.skill;
        parentEvent.result.card = get.autoViewAs(selectedCard);
        if (aozhan) {
          parentEvent.result.card.name = name;
        }
        parentEvent.result.cards = [selectedCard];
        parentEvent.redo();
        return;
      }
      game.broadcastAll(
        (card, name2) => {
          lib.skill.psqichu_backup.viewAs = {
            name: name2,
            cards: [card],
            isCard: true
          };
          lib.skill.psqichu_backup.prompt = `选择${get.translation(card)}的目标`;
        },
        selectedCard,
        name
      );
      parentEvent.set("_backupevent", "psqichu_backup");
      parentEvent.backup("psqichu_backup");
      parentEvent.goto(0);
    },
    ai: {
      effect: {
        target(card, player, target, effect) {
          if (target.hasSkill("psqichu_used")) {
            return;
          }
          if (get.tag(card, "respondShan")) {
            return 0.7;
          }
          if (get.tag(card, "respondSha")) {
            return 0.7;
          }
        }
      },
      order: 11,
      respondShan: true,
      respondSha: true,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      backup: {
        async precontent(event) {
          const name = event.result.card.name;
          event.result.cards = event.result.card.cards;
          event.result.card = get.autoViewAs(event.result.cards[0]);
          event.result.card.name = name;
          event.result._apply_args = { addSkillCount: false };
        },
        filterCard: () => false,
        selectCard: -1,
        log: false
      },
      used: { charlotte: true }
    }
  },
  pslongxin: {
    trigger: { player: "phaseJudgeBegin" },
    filter(event, player) {
      return player.hasCards("j") && player.hasCards("h");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard({
        prompt: get.prompt2("pslongxin"),
        filterCard: { type: "equip" },
        position: "he",
        chooseonly: true,
        ai: (card) => _status.event.goon ? 15 - get.value(card) : 0
      }).set(
        "goon",
        player.hasCard((card) => {
          const cardj = card.viewAs ? { name: card.viewAs } : card;
          return get.effect(player, cardj, player, player) < 0;
        }, "j")
      ).forResult();
    },
    async content(event, trigger, player) {
      await player.discard({
        cards: event.cards,
        discarder: player
      });
      await player.discardPlayerCard({
        target: player,
        position: "j",
        forced: true
      });
    }
  },
  //官盗S周瑜·一版
  psoldshiyin: {
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    frequent: true,
    filter(event, player) {
      if (player !== _status.currentPhase) {
        return false;
      }
      return event.getg(player).filter((i) => get.owner(i) === player).length > 0;
    },
    async content(event, trigger, player) {
      await player.showCards(
        trigger.getg(player).filter((i) => get.owner(i) === player),
        `${get.translation(player)}发动了【识音】`
      );
      const suits = [];
      const cards2 = trigger.getg(player).filter((i) => get.owner(i) === player);
      for (const card of cards2) {
        suits.add(get.suit(card, player));
      }
      player.addTempSkill("psoldshiyin_effect");
      if (!player.storage.psoldshiyin_effect) {
        player.storage.psoldshiyin_effect = 0;
      }
      player.storage.psoldshiyin_effect = Math.max(player.storage.psoldshiyin_effect, suits.length);
      if (suits.length >= 2) {
        player.addMark("psoldshiyin_damage", 1, false);
      }
    },
    subSkill: {
      effect: {
        trigger: { player: "useCard" },
        charlotte: true,
        forced: true,
        onremove: ["psoldshiyin_effect", "psoldshiyin_damage"],
        async content(event, trigger, player) {
          const num = player.countMark("psoldshiyin_effect");
          if (num >= 1) {
            trigger.directHit.addArray(game.players);
          }
          if (num >= 2 && get.tag(trigger.card, "damage")) {
            trigger.baseDamage += player.countMark("psoldshiyin_damage");
          }
          if (num >= 3) {
            player.draw();
          }
          player.removeSkill("psoldshiyin_effect");
        },
        mod: {
          aiOrder(player, card, num) {
            const numx = player.countMark("psoldshiyin_effect");
            if (numx >= 2 && get.tag(card, "damage")) {
              return num + 10;
            }
          }
        }
      }
    }
  },
  //官盗S周瑜·二版
  psshiyin: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    locked: false,
    group: "psshiyin_change",
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard({
        prompt: get.prompt("psshiyin"),
        prompt2: "将一张手牌置于武将牌上，称为“杂音”牌",
        ai: (card) => 20 - get.value(card)
      }).forResult();
    },
    async content(event, trigger, player) {
      await player.addToExpansion({
        cards: event.cards,
        source: player,
        animate: "give",
        gaintag: ["psshiyin"]
      });
    },
    marktext: "音",
    intro: {
      name: "杂音",
      name2: "杂音",
      content: "expansion",
      markcount: "expansion"
    },
    subSkill: {
      change: {
        trigger: { player: "phaseUseBegin" },
        direct: true,
        filter(event, player) {
          return player.hasExpansions("psshiyin") && player.hasCards("h");
        },
        async content(event, trigger, player) {
          const card = player.getExpansions("psshiyin")[0];
          const suits = lib.suit.slice().map((suit2) => [suit2, (get.suit(card) === suit2 ? 1 : 0) + player.countCards("h", { suit: suit2 })]).filter((item) => item[1] > 0);
          suits.sort((a, b) => a[1] - b[1]);
          const suit = suits.length > 0 ? suits[0][0] : null;
          const result = await player.chooseCard({
            prompt: get.prompt("psshiyin"),
            prompt2: `用一张手牌替换“杂音”牌（${get.translation(card)}）`,
            ai: (card2) => _status.event.suit && get.suit(card2) === _status.event.suit ? 8 - get.value(card2) : 0
          }).set("suit", suit).forResult();
          if (!result.bool) {
            return;
          }
          player.logSkill("psshiyin");
          const cardToGain = player.getExpansions("psshiyin")[0];
          await player.addToExpansion({
            cards: [result.cards[0]],
            source: player,
            animate: "give",
            gaintag: ["psshiyin"]
          });
          if (!cardToGain) {
            return;
          }
          await player.gain({
            cards: [cardToGain],
            animate: "gain2"
          });
        }
      }
    },
    ai: {
      combo: "psliaozou"
    }
  },
  psquwu: {
    forced: true,
    trigger: { target: "useCardToBefore" },
    filter(event, player) {
      return player.hasExpansions("psshiyin") && get.suit(player.getExpansions("psshiyin")[0]) === get.suit(event.card);
    },
    async content(event, trigger, player) {
      trigger.cancel();
    },
    ai: {
      threaten: 1.1,
      combo: "psshiyin",
      effect: {
        target(card, player, target, current) {
          const list = target.getExpansions("psshiyin");
          for (const cardx of list) {
            if (get.suit(cardx) === get.suit(card)) {
              return "zeroplayertarget";
            }
          }
        }
      }
    },
    mod: {
      cardEnabled2(card, player) {
        const list = player.getExpansions("psshiyin");
        for (const cardx of list) {
          if (get.suit(cardx) === get.suit(card)) {
            return false;
          }
        }
      },
      cardRespondable(card, player) {
        const list = player.getExpansions("psshiyin");
        for (const cardx of list) {
          if (get.suit(cardx) === get.suit(card)) {
            return false;
          }
        }
      },
      cardSavable(card, player) {
        const list = player.getExpansions("psshiyin");
        for (const cardx of list) {
          if (get.suit(cardx) === get.suit(card)) {
            return false;
          }
        }
      }
    }
  },
  psliaozou: {
    enable: "phaseUse",
    locked: false,
    filter(event, player) {
      return !player.hasSkill("psliaozou_blocker", null, null, false) && player.hasExpansions("psshiyin");
    },
    async content(event, trigger, player) {
      await player.showHandcards(`${get.translation(player)}发动了【聊奏】`);
      const cards2 = player.getExpansions("psshiyin");
      const hasMatchingSuit = cards2.some((card) => {
        const suit = get.suit(card);
        return player.hasCard((cardx) => get.suit(cardx) === suit);
      });
      if (hasMatchingSuit) {
        player.addTempSkill("psliaozou_blocker", {
          player: ["useCard1", "useSkillBegin", "phaseUseEnd"]
        });
        return;
      }
      await player.draw();
    },
    subSkill: {
      blocker: { charlotte: true }
    },
    mod: {
      aiValue(player, card, num) {
        const suit = get.suit(card);
        if (player.isPhaseUsing() && player.getExpansions("psshiyin").some((i) => get.suit(i) === suit)) {
          return num / 5;
        }
      },
      aiUseful() {
        return lib.skill.psliaozou.mod.aiValue.apply(this, arguments);
      }
    },
    ai: {
      combo: "psshiyin",
      order: 9.9,
      result: {
        player(player) {
          const cards2 = player.getExpansions("psshiyin");
          for (const card of cards2) {
            const suit = get.suit(card);
            if (player.hasCard((cardx) => get.suit(cardx) === suit)) {
              return 0;
            }
          }
          return 1;
        }
      }
    }
  },
  //官盗S武将传晋司马
  psquanyi: {
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    group: "psquanyi_tianbian",
    async content(event, trigger, player) {
      const { target } = event;
      const result = await player.chooseToCompare(target, (card) => {
        if (typeof card === "string" && lib.skill[card]) {
          const ais = lib.skill[card].check || (() => 0);
          return ais();
        }
        const cardOwner = get.owner(card);
        const getn = (card2) => {
          if (cardOwner.hasSkill("tianbian") && get.suit(card2) === "heart") {
            return 13;
          }
          return get.number(card2);
        };
        const compareEvent = _status.event.getParent();
        const to = cardOwner === compareEvent.player ? compareEvent.target : compareEvent.player;
        let addi = get.value(card) >= 8 && get.type(card) !== "equip" ? -6 : 0;
        if (card.name === "du") {
          addi -= 5;
        }
        if (get.color(card) === "black") {
          addi -= 6;
        }
        if (cardOwner === compareEvent.player) {
          if (compareEvent.small) {
            return -getn(card) - get.value(card) / 2 + addi;
          }
          return getn(card) - get.value(card) / 2 + addi;
        }
        if (get.attitude(cardOwner, to) <= 0 === Boolean(compareEvent.small)) {
          return -getn(card) - get.value(card) / 2 + addi;
        }
        return getn(card) - get.value(card) / 2 + addi;
      }).forResult();
      if (result.tie) {
        return;
      }
      const targets = [player, target];
      if (!result.bool) {
        targets.reverse();
      }
      const suits = [result.player, result.target].map((card) => get.suit(card, false));
      if (suits.includes("heart") && targets[1].countGainableCards("hej", targets[0]) > 0) {
        await targets[0].gainPlayerCard(targets[1], "hej", true);
      }
      if (suits.includes("diamond")) {
        await targets[1].damage(targets[0]);
      }
      if (suits.includes("spade")) {
        await targets[0].loseHp();
      }
      if (suits.includes("club") && targets[0].countDiscardableCards(targets[0], "he")) {
        await targets[0].chooseToDiscard(2, true, "he");
      }
    },
    ai: {
      order: 6,
      result: {
        target: -1
      }
    },
    subSkill: {
      tianbian: {
        audio: "psquanyi",
        enable: "chooseCard",
        check(event) {
          const player = _status.event.player;
          if (player.hasSkill("smyyingshi")) {
            const card = ui.cardPile.childNodes[0];
            if (get.color(card) === "black" && get.number(card) <= 4 || get.color(card) === "red" && get.number(card) >= 11) {
              return 20;
            }
          }
          return player.hasCard((card) => {
            const val = get.value(card);
            return val < 0 || get.color(card) === "black" && val <= 4 || get.color(card) === "red" && get.number(card) >= 11;
          }, "h") ? 0 : 20;
        },
        filter(event) {
          return event.type === "compare" && !event.directresult;
        },
        onCompare(player) {
          return game.cardsGotoOrdering(get.cards()).cards;
        }
      }
    }
  },
  //官盗S曹植
  psliushang: {
    trigger: { player: "phaseDrawBegin1" },
    forced: true,
    filter(event, player) {
      return !event.numFixed;
    },
    group: "psliushang_give",
    async content(event, trigger, player) {
      trigger.changeToZero();
      const drawEvent = player.draw(1 + Math.max(3, game.countPlayer()));
      const targets = game.filterPlayer((current) => current !== player);
      await drawEvent;
      for (const current of targets) {
        if (!player.hasCards("h")) {
          return;
        }
        const result = await player.chooseCardTarget({
          prompt: `流殇：将一张牌置于${get.translation(current)}武将牌上`,
          current,
          filterCard: true,
          forced: true,
          filterTarget(card, player2, target) {
            return target === _status.event.current;
          },
          selectTarget: -1,
          ai1(card) {
            const current2 = _status.event.current;
            return get.value(card, current2) * get.attitude(_status.event.player, current2);
          },
          ai2: () => 1
        }).forResult();
        if (result.bool) {
          const next = result.targets[0].addToExpansion(result.cards, player, "give");
          next.gaintag.add("psliushang");
          await next;
        }
      }
    },
    marktext: "殇",
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    subSkill: {
      give: {
        trigger: { global: "phaseZhunbeiBegin" },
        filter(event, player) {
          return event.player !== player && event.player.getExpansions("psliushang").length;
        },
        forced: true,
        logTarget: "player",
        async content(event, trigger, player) {
          const cards2 = trigger.player.getExpansions("psliushang");
          const name = get.translation(cards2);
          let choice = 0;
          if (get.damageEffect(player, trigger.player, trigger.player) > 0 && (get.value(cards2, trigger.player) < 0 || trigger.player.hasCard((card) => get.tag(card, "damage") && trigger.player.canUse(card, player) && get.effect(player, card, trigger.player, trigger.player) > 0, "hs"))) {
            choice = 1;
          }
          const result = await trigger.player.chooseControl().set("choiceList", [`获得${name}，且于本回合防止对${get.translation(player)}的伤害`, `将${name}置入弃牌堆`]).set("ai", () => _status.event.choice).set("choice", choice).forResult();
          if (result.index === 0) {
            const next = trigger.player.gain(cards2, "gain2");
            trigger.player.addTempSkill("psliushang_prevent");
            trigger.player.markAuto("psliushang_prevent", [player]);
            await next;
          } else {
            await trigger.player.loseToDiscardpile(cards2);
          }
          await game.delayx();
        }
      },
      prevent: {
        trigger: { source: "damageBegin2" },
        filter(event, player) {
          return player.getStorage("psliushang_prevent").includes(event.player);
        },
        forced: true,
        onremove: true,
        charlotte: true,
        logTarget: "player",
        async content(event, trigger, player) {
          trigger.cancel();
        },
        ai: {
          effect: {
            target(card, player, target, current) {
              if (player.getStorage("psliushang_prevent").includes(target) && get.tag(card, "damage")) {
                return "zeroplayertarget";
              }
            }
          }
        }
      }
    }
  },
  psqibu: {
    trigger: { player: "dying" },
    filter(event, player) {
      return player.hp <= 0;
    },
    limited: true,
    skillAnimation: true,
    animationColor: "water",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const cards2 = game.cardsGotoOrdering(get.cards(7)).cards;
      game.updateRoundNumber();
      await player.showCards(cards2, `${get.translation(player)}发动了【流殇】`);
      const num = cards2.filter((card) => get.suit(card) === "heart").length;
      const gains = cards2.filter((card) => get.suit(card) === "club");
      if (num > 0) {
        await player.recover(num);
      }
      if (gains.length) {
        await player.gain({
          cards: gains,
          animate: "gain2"
        });
      }
    }
  },
  //官盗S曹丕
  psjianwei: {
    trigger: { player: "phaseBegin" },
    skillAnimation: true,
    animationColor: "water",
    limited: true,
    filter(event, player) {
      return player.hp >= 1;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt2("psjianwei"),
        filterTarget: lib.filter.notMe,
        ai(target) {
          const player2 = get.player();
          if (player2.hp === 1 && !player2.canSave(player2)) {
            return 0;
          }
          const sgn = get.sgnAttitude(player2, target);
          const valMine = [0, 0];
          const valHis = [0, 0];
          for (const card of player2.iterableGetCards("hej")) {
            if (get.position(card) === "j") {
              valMine[0] += get.effect(player2, card, player2);
              valMine[1] += get.effect(target, card, player2);
            } else {
              valMine[0] += get.value(card, player2);
              valMine[1] += get.value(card, target) * sgn;
            }
          }
          for (const card of target.iterableGetCards("hej")) {
            if (get.position(card) === "j") {
              valHis[0] += get.effect(player2, card, player2);
              valHis[1] += get.effect(target, card, player2);
            } else {
              valHis[0] += get.value(card, player2);
              valHis[1] += get.value(card, target) * sgn;
            }
          }
          return valMine[1] - valMine[0] + valHis[0] - valHis[1] >= 60 ? 1 : 0;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      player.awakenSkill(event.name);
      await player.loseHp();
      if (!player.isIn() || !target.isIn()) {
        return;
      }
      const next = game.createEvent("psjianwei_swap");
      next.player = player;
      next.target = target;
      next.set("cards1", player.getCards("hej"));
      next.set("cards2", target.getCards("hej"));
      next.setContent(lib.skill.psjianwei.swapRegioncards);
      await next;
    },
    async swapRegioncards(event, trigger, player) {
      const { target, cards1, cards2 } = event;
      player.$giveAuto(cards1, target);
      target.$giveAuto(cards2, player);
      const h1 = cards1.filter((card) => get.position(card) === "h");
      const e1 = cards1.filter((card) => get.position(card) === "e");
      const j1 = cards1.filter((card) => get.position(card) === "j");
      const h2 = cards2.filter((card) => get.position(card) === "h");
      const e2 = cards2.filter((card) => get.position(card) === "e");
      const j2 = cards2.filter((card) => get.position(card) === "j");
      await game.loseAsync({
        lose_list: [
          [player, cards1],
          [target, cards2]
        ]
      }).setContent("chooseToCompareLose");
      const toDiscard = [];
      for (const card of j1) {
        if (target.isDisabledJudge() || target.hasJudge(card.viewAs || card.name)) {
          toDiscard.push(card);
        }
      }
      for (const card of j2) {
        if (player.isDisabledJudge() || player.hasJudge(card.viewAs || card.name)) {
          toDiscard.push(card);
        }
      }
      if (toDiscard.length) {
        await game.cardsDiscard(toDiscard);
      }
      const nextEvents = [
        game.loseAsync({
          gain_list: [
            [player, h2.filter((card) => get.position(card, true) === "o")],
            [target, h1.filter((card) => get.position(card, true) === "o")]
          ]
        }).setContent("gaincardMultiple")
      ];
      for (const card of e2) {
        if (get.position(card, true) !== "o") {
          continue;
        }
        nextEvents.push(player.equip(card));
      }
      for (const card of e1) {
        if (get.position(card, true) !== "o") {
          continue;
        }
        nextEvents.push(target.equip(card));
      }
      for (const card of j2) {
        if (get.position(card, true) !== "o") {
          continue;
        }
        nextEvents.push(player.addJudge(card));
      }
      for (const card of j1) {
        if (get.position(card, true) !== "o") {
          continue;
        }
        nextEvents.push(target.addJudge(card));
      }
      for (const next of nextEvents) {
        await next;
      }
      await game.delayx();
    }
  },
  //官盗S司马懿
  pszhonghu: {
    trigger: { global: "dieAfter" },
    global: "pszhonghu_skip",
    filter(event, player) {
      return player !== _status.currentPhase;
    },
    async content(event, trigger, player) {
      const evt = trigger.getParent("phaseUse");
      if (evt && evt.name === "phaseUse") {
        evt.skipped = true;
      }
      const evtx = trigger.getParent("phase");
      if (evtx) {
        game.log(evtx.player, "结束了回合");
        evtx.num = evtx.phaseList.length;
        evtx.goto(11);
      }
      _status._pszhonghu = player;
    },
    subSkill: {
      skip: {
        trigger: { player: "phaseBeforeStart" },
        forced: true,
        priority: Infinity,
        popup: false,
        firstDo: true,
        filter(event, player) {
          if (_status._pszhonghu && !_status._pszhonghu.isIn() || event.player === _status._pszhonghu) {
            delete _status._pszhonghu;
          }
          return _status._pszhonghu && event.player !== _status._pszhonghu;
        },
        async content(event, trigger, player) {
          trigger.cancel(null, null, "notrigger");
        }
      }
    }
  },
  //官盗S虎啸龙吟司马懿&诸葛亮
  pshuxiao: {
    trigger: { player: "phaseBegin" },
    frequent: true,
    async content(event, trigger, player) {
      const result = await player.judge({
        judge: (card) => ["basic", "trick"].includes(get.type(card)) ? 3 : -1
      }).forResult();
      if (!result.bool) {
        return;
      }
      player.addTempSkill("pshuxiao_use");
      player.storage.pshuxiao_use = {
        card: { name: result.name, nature: result.card.nature },
        number: result.number,
        suit: result.suit
      };
    },
    subSkill: {
      use: {
        charlotte: true,
        onremove: true,
        enable: "chooseToUse",
        popname: true,
        position: "hs",
        hiddenCard(player, name) {
          return player.storage.pshuxiao_use.card.name === name;
        },
        filter(event, player) {
          if (!player.storage.pshuxiao_use) {
            return false;
          }
          if (!player.countCards("h")) {
            return false;
          }
          return event.filterCard(player.storage.pshuxiao_use.card, player, event);
        },
        viewAs(cards2, player) {
          return player.storage.pshuxiao_use.card;
        },
        filterCard(card, player) {
          return get.number(card) === player.storage.pshuxiao_use.number || get.suit(card) === player.storage.pshuxiao_use.suit;
        },
        prompt(event) {
          const player = _status.event.player;
          const { suit, number, card } = player.storage.pshuxiao_use;
          return `将一张${get.translation(suit)}牌或点数为${get.strNumber(number)}的牌当作${get.translation(card)}使用`;
        }
      }
    }
  },
  psguanxing: {
    audio: "guanxing",
    trigger: { player: "phaseZhunbeiBegin" },
    frequent: true,
    preHidden: true,
    async content(event, trigger, player) {
      const result = await player.chooseToGuanxing(5).set("prompt", "观星：点击或拖动将牌移动到牌堆顶或牌堆底").forResult();
      if (!result.bool || !result.moved[0].length) {
        player.addTempSkill("guanxing_fail");
      }
    },
    ai: {
      threaten: 1.2,
      guanxing: true
    }
  },
  pslongyin: {
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      if (!player.countCards("hse") || player.hasSkill("pslongyin_used")) {
        return false;
      }
      for (const i of lib.inpile) {
        const type = get.type(i);
        if ((type === "basic" || type === "trick") && event.filterCard(get.autoViewAs({ name: i }, "unsure"), player, event)) {
          return true;
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player) {
        const list = [];
        for (const name of lib.inpile) {
          if (name === "sha") {
            if (event.filterCard({ name }, player, event)) {
              list.push(["基本", "", "sha"]);
            }
            for (const j of lib.inpile_nature) {
              if (event.filterCard({ name, nature: j }, player, event)) {
                list.push(["基本", "", "sha", j]);
              }
            }
          } else if (get.type(name) === "trick" && event.filterCard({ name }, player, event)) {
            list.push(["锦囊", "", name]);
          } else if (get.type(name) === "basic" && event.filterCard({ name }, player, event)) {
            list.push(["基本", "", name]);
          }
        }
        return ui.create.dialog("虎啸", [list, "vcard"]);
      },
      filter(button, player) {
        return _status.event.getParent().filterCard({ name: button.link[2], nature: button.link[3] }, player, _status.event.getParent());
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = _status.event.player;
        if (["wugu", "zhulu_card", "yiyi", "lulitongxin", "lianjunshengyan", "diaohulishan"].includes(button.link[2])) {
          return 0;
        }
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard(card, player2) {
            let num = 0;
            for (const selectedCard of ui.selected.cards) {
              num += get.number(selectedCard);
            }
            return get.number(card) + num <= 13;
          },
          selectCard: [1, Infinity],
          filterOk() {
            let num = 0;
            for (const selectedCard of ui.selected.cards) {
              num += get.number(selectedCard);
            }
            return num === 13;
          },
          audio: "pslongyin",
          popname: true,
          complexCard: true,
          check(card) {
            let num = 0;
            for (const selectedCard of ui.selected.cards) {
              num += get.number(selectedCard);
            }
            if (num + get.number(card) === 13) {
              return 5.5 - get.value(card);
            }
            if (ui.selected.cards.length === 0) {
              const cards2 = _status.event.player.getCards("h");
              for (const [index, firstCard] of cards2.entries()) {
                for (const secondCard of cards2.slice(index + 1)) {
                  if (get.number(firstCard) + get.number(secondCard) === 13 && (firstCard === card || secondCard === card)) {
                    return 6 - get.value(card);
                  }
                }
              }
            }
            return 0;
          },
          position: "hes",
          viewAs: { name: links[0][2], nature: links[0][3] },
          async precontent(event, trigger, player2) {
            player2.addTempSkill("pslongyin_used");
          }
        };
      },
      prompt(links, player) {
        return `将任意张点数和为13牌当做${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用`;
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name)) {
        return false;
      }
      const type = get.type(name);
      return (type === "basic" || type === "trick") && player.countCards("she") > 0 && !player.hasSkill("pslongyin_used");
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player) {
        if (!player.countCards("hse") || player.hasSkill("pslongyin_used")) {
          return false;
        }
      },
      order: 1,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      used: { charlotte: true }
    }
  },
  //官盗S武将传诸葛亮
  pszhiji: {
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return !ui.selected.targets.length || target.group !== ui.selected.targets[0].group;
    },
    selectTarget: 2,
    complexTarget: true,
    multitarget: true,
    multiline: true,
    filterCard: true,
    selectCard: 2,
    check(card) {
      return 6 - get.value(card);
    },
    async content(event, trigger, player) {
      const { targets } = event;
      targets.sortBySeat();
      if (targets[0].canUse("sha", targets[1], false)) {
        await targets[0].useCard({
          card: get.autoViewAs({ name: "sha", isCard: true }),
          targets: [targets[1]],
          addCount: false,
          noai: true
        });
      }
      if (targets[1].canUse("sha", targets[0], false)) {
        await targets[1].useCard({
          card: get.autoViewAs({ name: "sha", isCard: true }),
          targets: [targets[0]],
          addCount: false,
          noai: true
        });
      }
    },
    ai: {
      order: 2.5,
      result: {
        player: 1,
        target(player, target) {
          if (!ui.selected.targets.length) {
            return -1;
          }
          const targetx = ui.selected.targets[0];
          if (get.effect(targetx, { name: "sha" }, target, player) + get.effect(target, { name: "sha" }, targetx, player) < 0) {
            return 0;
          }
          return -1;
        }
      }
    }
  },
  psjiefeng: {
    enable: "phaseUse",
    filterCard: true,
    selectCard: 2,
    check(card) {
      return 6 - get.value(card);
    },
    async content(event, trigger, player) {
      const orderingEvent = game.cardsGotoOrdering(get.cards(5));
      const { cards: cards2 } = orderingEvent;
      event.cards = cards2;
      await orderingEvent;
      await player.showCards(cards2, `${get.translation(player)}发动了【借风】`);
      if (cards2.filter((card) => get.color(card) === "red").length < 2) {
        return;
      }
      await player.chooseUseTarget({
        card: get.autoViewAs({ name: "wanjian", isCard: true }),
        forced: true
      });
    },
    ai: {
      order: 9,
      result: {
        player(player) {
          if (player.getUseValue({ name: "wanjian" }) < 0) {
            return 0;
          }
          return 1;
        }
      }
    }
  },
  //官盗S马超
  psweihou: {
    trigger: { player: "judgeBegin" },
    filter(event, player) {
      return !event.directresult;
    },
    async content(event, trigger, player) {
      const cards2 = get.cards(2, true);
      const videoId = lib.status.videoId++;
      game.broadcastAll(
        (player2, id, cards3) => {
          const title = player2 === game.me && !_status.auto ? "威侯：选择一张作为本次判定结果" : `${get.translation(player2)}发动了【威侯】`;
          const dialog = ui.create.dialog(title, cards3);
          dialog.videoId = id;
        },
        player,
        videoId,
        cards2
      );
      game.addVideo("showCards", player, ["威侯", get.cardsInfo(cards2)]);
      if (!event.isMine() && !event.isOnline()) {
        await game.delayx();
      }
      const result = await player.chooseButton({
        createDialog: ["威侯：选择一张作为本次判定结果", cards2],
        forced: true,
        ai(button) {
          return _status.event.getTrigger().judge(button.link);
        }
      }).set("dialog", videoId).forResult();
      game.broadcastAll("closeDialog", videoId);
      if (result.bool) {
        trigger.directresult = result.links[0];
        await game.cardsDiscard(cards2.removeArray(result.links).filter((card) => get.position(card) === "c"));
      }
      game.updateRoundNumber();
    }
  },
  //官盗S1066★贾诩
  psqupo: {
    trigger: { global: "phaseBegin" },
    filter(event, player) {
      return player.countCards("he") && game.countPlayer() > 2;
    },
    async cost(event, trigger, player) {
      player.getCards("he");
      const { player: current } = trigger;
      const targets = game.filterPlayer((currentx) => {
        if (currentx === current || current === player) {
          return false;
        }
        return !current.canUse("sha", currentx) || get.effect(currentx, { name: "sha" }, current, player) > 0 && get.attitude(player, currentx) > -3;
      });
      const targets2 = game.filterPlayer((currentx) => {
        if (currentx === current || current === player) {
          return false;
        }
        return current.hasCard((card) => current.canUse(card, currentx) && get.effect(currentx, card, current, player) > 0 && get.color(card) === "red" && get.tag(card, "damage") && get.type(card) !== "delay", "hs");
      });
      event.result = await player.chooseCardTarget({
        filterCard: true,
        position: "he",
        prompt: get.prompt2(event.skill),
        current,
        targets1: targets,
        targets2,
        filterTarget(card, player2, target) {
          return player2 !== target && target !== get.event().current;
        },
        ai1(card) {
          const { player: player2, current: current2, targets1, targets2: targets22 } = get.event();
          const color = get.color(card);
          if (!targets22.length) {
            if (get.effect(current2, { name: "losehp" }, player2, player2) < 0) {
              return 0;
            }
            if (color !== "black" || !targets1.length) {
              return 0;
            }
            return 5.5 - get.value(card);
          }
          targets22.sort((a, b) => get.threaten(b, current2) - get.threaten(a, current2));
          if (!targets1.length) {
            if (color !== "red") {
              return 0;
            }
            if (get.attitude(player2, current2) <= 0) {
              return 0;
            }
            return 5.5 - get.value(card);
          }
          const target = targets22[0];
          const color1 = get.effect(current2, { name: "losehp" }, player2, player2) > Math.max(0, get.effect(target, { name: "losehp" }, player2, player2)) ? "black" : "red";
          if (color !== color1) {
            return 0;
          }
          return 6 - get.value(card);
        },
        ai2(target) {
          if (!ui.selected.cards.length) {
            return 0;
          }
          const { player: player2, current: current2, targets1, targets2: targets22 } = get.event();
          const color = get.color(ui.selected.cards[0]);
          if (!["red", "black"].includes(color)) {
            return 0;
          }
          if (color === "black") {
            if (!targets1.includes(target)) {
              return 0;
            }
            return get.attitude(player2, target) + 0.1;
          }
          if (!targets22.includes(target)) {
            return 0;
          }
          return get.effect(target, { name: "losehp" }, player2, player2);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const {
        targets: [target],
        cards: cards2
      } = event;
      await player.give(cards2, target);
      const color = get.color(cards2[0]);
      const skill = `${event.name}_${color}`;
      if (color === "black") {
        trigger.player.addTempSkill(skill);
        trigger.player.markAuto(skill, [target]);
      } else if (color === "red") {
        target.addTempSkill(skill);
        target.addMark(skill, 1, false);
      }
    },
    subSkill: {
      black: {
        trigger: { player: "useCardToPlayer" },
        charlotte: true,
        onremove: true,
        forced: true,
        popup: false,
        filter(event, player) {
          if (event.card.name !== "sha") {
            return false;
          }
          return !player.getStorage("psqupo_black").includes(event.target);
        },
        async content(event, trigger, player) {
          player.loseHp();
        },
        intro: { content: "本回合使用【杀】指定不为$的目标时失去1点体力" }
      },
      red: {
        trigger: { player: "damageBegin3" },
        charlotte: true,
        onremove: true,
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          player.loseHp(player.countMark(event.name));
          player.removeSkill(event.name);
        },
        intro: { content: "本回合下一次受到伤害时失去#点体力" }
      }
    }
  },
  psbaoquan: {
    trigger: { player: "damageBegin4" },
    filter(event, player) {
      return _status.connectMode || player.hasCards("h", { type: ["trick", "delay"] });
    },
    direct: true,
    async content(event, trigger, player) {
      const result = await player.chooseToDiscard({
        prompt: get.prompt2("psbaoquan"),
        filterCard: { type: ["trick", "delay"] },
        ai: (card) => _status.event.goon ? 7 - get.value(card) : 0
      }).set("logSkill", "psbaoquan").set("goon", get.damageEffect(player, trigger.source, player) < -5).forResult();
      if (!result.bool) {
        return;
      }
      trigger.cancel();
    }
  },
  //官盗S吕布
  pssheji: {
    enable: "phaseUse",
    filterCard: true,
    selectCard: -1,
    position: "h",
    locked: false,
    filter(event, player) {
      if (player.hasSkill("pssheji_used")) {
        return false;
      }
      const hs = player.getCards("h");
      if (!hs.length) {
        return false;
      }
      for (const card of hs) {
        if (game.checkMod(card, player, "unchanged", "cardEnabled2", player) === false) {
          return false;
        }
      }
      return event.filterCard(get.autoViewAs({ name: "sha" }, hs));
    },
    viewAs: {
      name: "sha",
      storage: { pssheji: true }
    },
    onuse(links, player) {
      player.addTempSkill("pssheji_used", "phaseUseAfter");
    },
    ai: {
      order: 1,
      threaten: 1.1
    },
    mod: {
      targetInRange(card, player, target) {
        if (card.storage && card.storage.pssheji) {
          return true;
        }
      }
    },
    subSkill: {
      used: {
        audio: "pssheji",
        trigger: { source: "damageSource" },
        charlotte: true,
        forced: true,
        popup: false,
        logTarget: "player",
        filter(event, player) {
          return event.card.storage && event.card.storage.pssheji && event.player.hasCard((card) => lib.filter.canBeGained(card, player, event.player) && ["equip1", "equip3", "equip4", "equip6"].includes(get.subtype(card)), "e");
        },
        async content(event, trigger, player) {
          const cards2 = trigger.player.getCards("e", (card) => lib.filter.canBeGained(card, player, trigger.player) && ["equip1", "equip3", "equip4", "equip6"].includes(get.subtype(card)));
          if (!cards2.length) {
            return;
          }
          await player.gain({ cards: cards2, source: trigger.player, animate: "giveAuto" });
        }
      }
    }
  },
  //龙羽飞
  longyi: {
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      if (event.type === "wuxie") {
        return false;
      }
      const hs = player.getCards("h");
      if (!hs.length) {
        return false;
      }
      for (const i of hs) {
        if (game.checkMod(i, player, "unchanged", "cardEnabled2", player) === false) {
          return false;
        }
      }
      for (const i of lib.inpile) {
        if (i !== "du" && get.type(i) === "basic" && event.filterCard({ name: i, cards: hs }, player, event)) {
          return true;
        }
        if (i === "sha") {
          const list = ["fire", "thunder", "ice"];
          for (const j of list) {
            if (event.filterCard({ name: i, nature: j, cards: hs }, player, event)) {
              return true;
            }
          }
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player) {
        const vcards = [];
        const hs = player.getCards("h");
        for (const i of lib.inpile) {
          if (i !== "du" && get.type(i) === "basic" && event.filterCard({ name: i, cards: hs }, player, event)) {
            vcards.push(["基本", "", i]);
          }
          if (i === "sha") {
            for (const j of lib.inpile_nature) {
              if (event.filterCard({ name: i, nature: j, cards: hs }, player, event)) {
                vcards.push(["基本", "", i, j]);
              }
            }
          }
        }
        return ui.create.dialog("龙裔", [vcards, "vcard"]);
      },
      check(button, player) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        return _status.event.player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          audio: "longyi",
          popname: true,
          viewAs: { name: links[0][2], nature: links[0][3] },
          filterCard: true,
          selectCard: -1,
          position: "h"
        };
      },
      prompt(links, player) {
        return `将所有手牌当做${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用或打出`;
      }
    },
    hiddenCard(player, name) {
      return name !== "du" && get.type(name) === "basic" && player.countCards("h") > 0;
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player) {
        return player.countCards("h") > 0;
      },
      order: 0.5,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          if (_status.event.type === "respondShan") {
            return 1;
          }
          let val = 0;
          const hs = player.getCards("h");
          let max = 0;
          for (const i of hs) {
            val += get.value(i, player);
            if (get.type(i, null, player) === "trick") {
              max += 5;
            }
          }
          if (player.hasSkill("zhenjue")) {
            max += 7;
          }
          return val <= max ? 1 : 0;
        }
      }
    },
    group: "longyi_effect",
    subSkill: {
      effect: {
        trigger: { player: ["useCard", "respond"] },
        forced: true,
        charlotte: true,
        popup: false,
        filter(event, player) {
          if (event.skill !== "longyi_backup") {
            return false;
          }
          for (const i of event.cards) {
            const type = get.type2(i, player);
            if (type === "equip" || type === "trick") {
              return true;
            }
          }
          return false;
        },
        async content(event, trigger, player) {
          const map = {};
          for (const i of trigger.cards) {
            map[get.type2(i, player)] = true;
          }
          if (map.trick) {
            player.draw();
          }
          if (map.equip && trigger.directHit) {
            trigger.directHit.addArray(game.players);
          }
        }
      },
      backup: {}
    }
  },
  zhenjue: {
    trigger: { global: "phaseJieshuBegin" },
    filter(event, player) {
      return !player.hasCards("h");
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const result = await trigger.player.chooseToDiscard({
        position: "he",
        prompt: `弃置一张牌，或令${get.translation(player)}摸一张牌`,
        ai: (card) => _status.event.goon ? 7 - get.value(card) : -get.value(card)
      }).set("goon", get.attitude(trigger.player, player) < 0).forResult();
      if (result.bool) {
        return;
      }
      await player.draw();
    }
  },
  //群刘备
  jsprende: {
    audio: "rerende",
    enable: "phaseUse",
    filterCard: true,
    selectCard: [1, Infinity],
    allowChooseAll: true,
    discard: false,
    lose: false,
    delay: false,
    filterTarget(card, player, target) {
      return player !== target;
    },
    onremove: true,
    check(card) {
      if (ui.selected.cards.length && ui.selected.cards[0].name === "du") {
        return 0;
      }
      if (!ui.selected.cards.length && card.name === "du") {
        return 20;
      }
      const player = get.owner(card);
      if (player == null) {
        return 0;
      }
      if (ui.selected.cards.length >= Math.max(2, player.countCards("h") - player.hp)) {
        return 0;
      }
      if (player.hp === player.maxHp || player.storage.jsprende < 0 || player.countCards("h") <= 1) {
        const players = game.filterPlayer();
        for (const current of players) {
          if (current.hasSkill("haoshi") && !current.isTurnedOver() && !current.hasJudge("lebu") && get.attitude(player, current) >= 3 && get.attitude(current, player) >= 3) {
            return 11 - get.value(card);
          }
        }
        if (player.countCards("h") > player.hp) {
          return 10 - get.value(card);
        }
        if (player.countCards("h") > 2) {
          return 6 - get.value(card);
        }
        return -1;
      }
      return 10 - get.value(card);
    },
    async content(event, trigger, player) {
      const { cards: cards2, target } = event;
      const phaseUseEvent = event.getParent("phaseUse");
      if (phaseUseEvent?.name === "phaseUse" && !phaseUseEvent.jsprende) {
        const clearEvent = game.createEvent("jsprende_clear");
        event.next.remove(clearEvent);
        phaseUseEvent.after.push(clearEvent);
        phaseUseEvent.jsprende = true;
        clearEvent.player = player;
        clearEvent.setContent(async ({ player: player2 }) => {
          delete player2.storage.jsprende;
        });
      }
      const giveEvent = player.give(cards2, target);
      if (typeof player.storage.jsprende !== "number") {
        player.storage.jsprende = 0;
      }
      if (player.storage.jsprende < 0) {
        await giveEvent;
        return;
      }
      player.storage.jsprende += cards2.length;
      if (player.storage.jsprende < 2) {
        await giveEvent;
        return;
      }
      const list = [];
      const chooseToUseEvent = event.getParent("chooseToUse");
      if (lib.filter.cardUsable({ name: "sha", isCard: true }, player, chooseToUseEvent) && game.hasPlayer((current) => player.canUse("sha", current))) {
        list.push(["基本", "", "sha"]);
      }
      for (const nature of lib.inpile_nature) {
        const sha = { name: "sha", nature, isCard: true };
        if (lib.filter.cardUsable(sha, player, chooseToUseEvent) && game.hasPlayer((current) => player.canUse(sha, current))) {
          list.push(["基本", "", "sha", nature]);
        }
      }
      if (lib.filter.cardUsable({ name: "tao", isCard: true }, player, chooseToUseEvent) && game.hasPlayer((current) => player.canUse("tao", current))) {
        list.push(["基本", "", "tao"]);
      }
      if (lib.filter.cardUsable({ name: "jiu", isCard: true }, player, chooseToUseEvent) && game.hasPlayer((current) => player.canUse("jiu", current))) {
        list.push(["基本", "", "jiu"]);
      }
      player.storage.jsprende = -1;
      await giveEvent;
      if (!list.length) {
        return;
      }
      const result = await player.chooseButton({
        createDialog: ["是否视为使用一张基本牌？", [list, "vcard"]],
        ai: (button) => {
          const player2 = _status.event.player;
          const card2 = {
            name: button.link[2],
            nature: button.link[3],
            isCard: true
          };
          if (card2.name === "tao") {
            if (player2.hp === 1 || player2.hp === 2 && !player2.hasShan("all") || player2.needsToDiscard()) {
              return 5;
            }
            return 1;
          }
          if (card2.name === "sha") {
            if (!game.hasPlayer((current) => player2.canUse(card2, current) && get.effect(current, card2, player2, player2) > 0)) {
              return 0;
            }
            if (card2.nature === "fire") {
              return 2.95;
            }
            if (card2.nature === "thunder" || card2.nature === "ice") {
              return 2.92;
            }
            return 2.9;
          }
          if (card2.name === "jiu") {
            return 0.5;
          }
          return 0;
        }
      }).forResult();
      if (!result?.bool || !result.links?.[0]) {
        return;
      }
      const card = get.autoViewAs({ name: result.links[0][2], nature: result.links[0][3], isCard: true });
      await player.chooseUseTarget({
        card,
        forced: true
      });
    },
    ai: {
      fireAttack: true,
      order(skill, player) {
        if (player == null) {
          return 0;
        }
        if (player.hp < player.maxHp && player.storage.jsprende < 2 && player.countCards("h") > 1) {
          return 10;
        }
        return 4;
      },
      result: {
        target(player, target) {
          if (target.hasSkillTag("nogain")) {
            return 0;
          }
          if (ui.selected.cards.length && ui.selected.cards[0].name === "du") {
            if (target.hasSkillTag("nodu")) {
              return 0;
            }
            return -10;
          }
          if (target.hasJudge("lebu")) {
            return 0;
          }
          const nh = target.countCards("h");
          const np = player.countCards("h");
          if (player.hp === player.maxHp || player.storage.jsprende < 0 || player.countCards("h") <= 1) {
            if (nh >= np - 1 && np <= player.hp && !target.hasSkill("haoshi")) {
              return 0;
            }
          }
          return Math.max(1, 5 - nh);
        }
      },
      effect: {
        target_use(card, player, target) {
          if (player !== target || get.type(card) !== "equip") {
            return;
          }
          if (!player.hasCards("e", { subtype: get.subtype(card) })) {
            return;
          }
          if (game.hasPlayer((current) => current !== player && get.attitude(player, current) > 0)) {
            return 0;
          }
        }
      },
      threaten: 0.8
    }
  },
  //S贾诩
  nsyice: {
    trigger: {
      player: "loseAfter",
      global: ["cardsDiscardAfter", "loseAsyncAfter"]
    },
    filter(event, player) {
      if (event.name === "cardsDiscard") {
        const evt2 = event.getParent();
        if (evt2.name !== "orderingDiscard" || !evt2.relatedEvent || evt2.relatedEvent.player !== player || !["useCard", "respond"].includes(evt2.relatedEvent.name)) {
          return false;
        }
        return event.cards.filterInD("d").length > 0;
      }
      if (event.type !== "discard") {
        return false;
      }
      const evt = event.getl(player);
      return evt.cards2 && evt.cards2.filterInD("d").length > 0;
    },
    forced: true,
    async content(event, trigger, player) {
      const relatedEvent = trigger.getParent().relatedEvent;
      if (trigger.name === "discard" && !trigger.delay || relatedEvent && relatedEvent.name === "respond") {
        await game.delayx();
      }
      let discardedCards;
      if (trigger.getl) {
        discardedCards = trigger.getl(player).cards2.filterInD("d");
      } else {
        discardedCards = trigger.cards.filterInD("d");
      }
      let result;
      if (discardedCards.length === 1) {
        result = { bool: true, links: discardedCards };
      } else {
        const dialog = ["遗策：选择要放置的卡牌", '<div class="text center">（从左到右为从旧到新，后选择的后置入）</div>', discardedCards];
        const expansionCards = player.getExpansions("nsyice");
        expansionCards.reverse();
        if (expansionCards.length) {
          dialog.push('<div class="text center">原有“策”</div>');
          dialog.push(expansionCards);
        }
        result = await player.chooseButton(dialog, true, discardedCards.length).set("filterButton", (button) => _status.event.cards.includes(button.link)).set("cards", discardedCards).forResult();
      }
      const expansionEvent = player.addToExpansion(result.links, "gain2");
      expansionEvent.gaintag.add("nsyice");
      await expansionEvent;
      const storage = player.getExpansions("nsyice");
      let matchedCards;
      for (const [startIndex, startCard] of storage.entries()) {
        const followingCards = storage.slice(startIndex + 1).reverse();
        for (const [reverseIndex, endCard] of followingCards.entries()) {
          if (get.number(startCard) !== get.number(endCard)) {
            continue;
          }
          const endIndex = storage.length - 1 - reverseIndex;
          matchedCards = storage.splice(startIndex, endIndex - startIndex + 1);
          break;
        }
        if (matchedCards) {
          break;
        }
      }
      if (!matchedCards) {
        return;
      }
      const edgeCards = [matchedCards.shift(), matchedCards.pop()];
      if (matchedCards.length) {
        await player.gain(matchedCards, "gain2");
      }
      const arrangeResult = await player.chooseButton(["将一张牌置于牌堆顶，将另一张牌置于牌堆底", edgeCards], true).forResult();
      const loseEvent = player.lose(edgeCards, ui.cardPile).set("topper", arrangeResult.links[0]);
      loseEvent.insert_index = (event2, card) => card === event2.topper ? ui.cardPile.firstChild : null;
      const canDamage = !_status.dying.length;
      await loseEvent;
      if (!canDamage) {
        return;
      }
      const targetResult = await player.chooseTarget("对一名角色造成1点伤害", true).set("ai", (target) => get.damageEffect(target, _status.event.player, _status.event.player)).forResult();
      if (targetResult.bool) {
        const target = targetResult.targets[0];
        player.line(target);
        await target.damage("nocard");
      }
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    marktext: "策",
    intro: {
      content: "expansion",
      markcount: "expansion"
    }
  }
};
const skills$r = {
  //神姜维
  psfenzhi: {
    audio: 2,
    forced: true,
    trigger: {
      source: "damageBegin1",
      global: "damageEnd"
    },
    filter(event, player, name) {
      if (name == "damageBegin1") {
        return !event.hasNature("fire");
      }
      return event.hasNature("fire");
    },
    async content(event, trigger, player) {
      if (event.triggername == "damageBegin1") {
        game.setNature(trigger, "fire");
      } else {
        const zhu = game.findPlayer((current) => current.getSeatNum() == 1);
        const result = zhu?.isIn() && zhu.isDamaged() ? await player.chooseBool({
          prompt: `焚志：令${get.translation(zhu)}回复1点体力或点击“取消”摸一张牌`,
          ai() {
            const { player: player2, zhu: zhu2 } = get.event();
            if (get.attitude(player2, zhu2) > 0) {
              return 1;
            }
            return 0;
          }
        }).set("zhu", zhu).forResult() : { bool: false };
        if (result?.bool) {
          await zhu.recover();
        } else {
          await player.draw();
        }
      }
    }
  },
  psjiucheng: {
    audio: 2,
    enable: "chooseToUse",
    locked: false,
    mod: {
      cardUsable(card) {
        if (card?.storage?.psjiucheng) {
          return Infinity;
        }
      },
      targetInRange(card) {
        if (card?.storage?.psjiucheng) {
          return true;
        }
      }
    },
    filter(event, player) {
      if (player.hasSkill("psjiucheng_blocker")) return false;
      return get.inpileVCardList((info) => {
        if (!["basic", "trick"].includes(info[0])) {
          return false;
        }
        return event.filterCard(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3],
              storage: { psjiucheng: true }
            },
            "unsure"
          ),
          player,
          event
        );
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (!["basic", "trick"].includes(info[0])) {
            return false;
          }
          return event.filterCard(
            get.autoViewAs(
              {
                name: info[2],
                nature: info[3],
                storage: { psjiucheng: true }
              },
              "unsure"
            ),
            player,
            event
          );
        });
        return ui.create.dialog("九乘", [list, "vcard"]);
      },
      filter(button, player) {
        return _status.event.getParent().filterCard(
          {
            name: button.link[2],
            nature: button.link[3],
            storage: { psjiucheng: true }
          },
          player,
          _status.event.getParent()
        );
      },
      check(button) {
        if (_status.event.getParent().type != "phase") {
          return 1;
        }
        const player = get.player();
        if (["wugu", "zhulu_card", "yiyi", "lulitongxin", "lianjunshengyan", "diaohulishan"].includes(button.link[2])) {
          return 0;
        }
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard: () => false,
          selectCard: -1,
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            storage: { psjiucheng: true }
          },
          log: false,
          async precontent(event, trigger, player2) {
            event.getParent().addCount = false;
            await player2.draw(3);
            player2.addSkill("psjiucheng_blocker");
            player2.when({
              player: ["useCardAfter"]
            }).filter((evt) => evt.getParent() == event.getParent()).step(async (event2, trigger2, player3) => {
              player3.addMark("psjiucheng_blocker", 9, false);
            }).assign({
              lastDo: true
            });
            player2.logSkill("psjiucheng");
          }
        };
      },
      prompt(links, player) {
        return "视为使用一张" + (get.translation(links[0][3]) || "") + get.translation(links[0][2]);
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name)) {
        return false;
      }
      const type = get.type(name);
      return ["basic", "trick"].includes(type) && !player.hasSkill("psjiucheng_blocker");
    },
    ai: {
      fireAttack: true,
      skillTagFilter(player) {
        if (player.hasSkill("psjiucheng_blocker")) {
          return false;
        }
      },
      order: 13,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      backup: {},
      blocker: {
        charlotte: true,
        onremove: true,
        silent: true,
        trigger: { player: "useCardAfter" },
        firstDo: true,
        intro: { content: (storage, player) => `当前进度：${9 - storage}/9` },
        filter(event, player) {
          return player.hasMark("psjiucheng_blocker");
        },
        async content(event, trigger, player) {
          player.removeMark(event.name, 1, false);
          if (!player.hasMark(event.name)) {
            player.removeSkill(event.name);
          }
        }
      }
    }
  },
  psranjin: {
    audio: 2,
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    trigger: { player: "phaseEnd" },
    check(event, player) {
      if (!game.hasPlayer((current) => get.attitude(player, current) < 0)) {
        return false;
      }
      return player.countCards("hs", (card) => card.name == "sha") > 2 || player.hp <= 2;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.when({ player: "phaseBefore" }).filter((evt) => evt.skill == event.name).step(async (event2, trigger2, player2) => {
        player2.addTempSkill("psranjin_effect", "phaseAfter");
      });
      player.insertPhase();
    },
    subSkill: {
      effect: {
        charlotte: true,
        mark: true,
        forced: true,
        intro: { content: `本回合使用牌无次数限制，若此回合结束时你杀死过其他角色，你重新发动一次${get.poptip("psranjin")}，否则你死亡` },
        trigger: { player: "phaseEnd" },
        async content(event, trigger, player) {
          if (player.getStat("kill") > 0) {
            await player.useSkill({ skill: "psranjin", targets: [player] });
          } else {
            await player.die();
          }
        },
        mod: {
          cardUsable: () => Infinity
        }
      }
    }
  },
  //魔姜维
  psmoyan: {
    audio: 2,
    forced: true,
    trigger: {
      global: ["loseHpAfter", "damageBefore"]
    },
    filter(event, player) {
      return event.num > 0 && [event.source, event.player].includes(player) || event.name == "loseHp";
    },
    async content(event, trigger, player) {
      if (trigger.name == "loseHp") {
        await player.draw();
      } else {
        trigger.cancel();
        const target = trigger.source == player ? trigger.player : player;
        await target.loseHp(trigger.num);
      }
    },
    ai: {
      jueqing: true
    }
  },
  psjiusheng: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player) {
      if (player.hasSkill("psjiusheng_blocker")) return false;
      return game.hasPlayer((current) => get.info("psjiusheng").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      const cardx = get.autoViewAs({ name: "sha", isCard: true }, "unsure");
      return target != player && target.canUse(cardx, player, false, false);
    },
    selectTarget: [1, Infinity],
    multiline: true,
    multitarget: true,
    async content(event, trigger, player) {
      const targets = event.targets.sortBySeat(), cardx = get.autoViewAs({ name: "sha", isCard: true }, "unsure");
      await player.draw({ num: targets.length });
      await game.doAsyncInOrder(targets, async (target) => {
        if (target.canUse(cardx, player, false, false) && target.isIn()) {
          await target.useCard({ card: cardx, targets: [player] });
        }
      });
      if (targets.some((target) => target.isIn() && target.hasCards("he"))) {
        const result = await player.chooseTarget({
          prompt: "九牲：弃置任意名角色各一张牌",
          filterTarget(card, player2, target) {
            return get.event().targets.includes(target) && target.hasCards("he");
          },
          selectTarget: [1, Infinity],
          forced: true,
          ai(target) {
            const player2 = get.player();
            return get.effect(target, { name: "guohe_copy", position: "he" }, player2, player2);
          }
        }).set("targets", targets).forResult();
        if (result?.bool && result.targets?.length) {
          const targetx = result.targets.sortBySeat();
          await game.doAsyncInOrder(targetx, async (target) => {
            await player.discardPlayerCard({ forced: true, position: "he", target });
          });
        }
      }
      player.addSkill(event.name + "_blocker");
      player.addMark(event.name + "_blocker", 9, false);
    },
    subSkill: {
      blocker: {
        charlotte: true,
        onremove: true,
        silent: true,
        trigger: { global: "loseHpAfter" },
        firstDo: true,
        intro: { content: (storage, player) => `当前进度：${9 - storage}/9` },
        async content(event, trigger, player) {
          player.removeMark(event.name, 1, false);
          if (!player.hasMark(event.name)) {
            player.removeSkill(event.name);
          }
        }
      }
    },
    ai: {
      order: 8,
      result: {
        player(player) {
          if (player.hp <= 1) {
            return -1;
          }
          return 1;
        },
        target: -1
      }
    }
  },
  pshunjin: {
    audio: 2,
    limited: true,
    skillAnimation: true,
    animationColor: "thunder",
    trigger: { player: "phaseEnd" },
    filter(event, player) {
      return player.maxHp > 1;
    },
    async cost(event, trigger, player) {
      let num = player.maxHp - 1;
      if (!game.hasPlayer((current) => get.attitude(player, current) < 0)) {
        num = 0;
      }
      const result = await player.chooseNumbers("魂蛊", [
        {
          prompt: "你可以失去任意点体力上限并进行等量个额外回合，这些额外回合结束后，你死亡",
          min: 1,
          max: player.maxHp - 1
        }
      ]).set("processAI", () => {
        const num2 = get.event().num;
        if (num2 == 0) {
          return false;
        }
        return [num2];
      }).set("num", num).forResult();
      if (result?.bool && result.numbers?.length) {
        event.result = {
          bool: true,
          cost_data: result.numbers[0]
        };
      }
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const num = event.cost_data;
      await player.loseMaxHp(num);
      player.addSkill(event.name + "_effect");
      player.addMark(event.name + "_effect", num, false);
      for (let i = 0; i < num; i++) {
        player.insertPhase();
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        forced: true,
        trigger: { player: "phaseBefore" },
        intro: { content: "#个额外回合后，你就会死亡且额外回合中你使用牌无次数限制" },
        filter(event, player) {
          return event.skill == "pshunjin";
        },
        async content(event, trigger, player) {
          player.removeMark(event.name, 1, false);
          player.addTempSkill("pshunjin_effect2", "phaseAfter");
          if (!player.hasMark(event.name)) {
            player.when({ player: "phaseAfter" }).filter((evt) => evt.skill == "pshunjin").step(async (event2, trigger2, player2) => {
              await player2.die();
            });
          }
        }
      },
      effect2: {
        charlotte: true,
        mod: {
          cardUsable: () => Infinity
        }
      }
    }
  },
  //神魏延
  psjimou: {
    audio: 2,
    limited: true,
    skillAnimation: true,
    animationColor: "thunder",
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player) {
      return player.maxHp > 1 && game.players.length > 1;
    },
    async cost(event, trigger, player) {
      let result;
      result = await player.chooseNumbers(get.prompt(event.skill), [
        {
          prompt: "失去任意点体力上限，令除你与你选择的角色外的其他角色进入“修整”状态",
          min: 1,
          max: player.maxHp - 1
        }
      ]).set("processAI", () => {
        const player2 = get.player();
        if (game.players.length <= 2 || !game.hasPlayer((current) => get.attitude(player2, current) < 0)) {
          return false;
        }
        return [player2.getDamagedHp()];
      }).forResult();
      if (result?.bool && result.numbers?.length) {
        const num = result.numbers[0];
        result = await player.chooseTarget({
          prompt: "选择一名其他角色令除你与其外的角色进入“修整”状态",
          filterTarget: lib.filter.notMe,
          forced: true,
          ai(target) {
            return -get.attitude(get.player(), target);
          }
        }).forResult();
        if (result?.bool && result.targets?.length) {
          event.result = {
            bool: true,
            targets: result.targets,
            cost_data: num
          };
        }
      }
    },
    async content(event, trigger, player) {
      const {
        targets: [target],
        cost_data: num
      } = event;
      player.awakenSkill(event.name);
      await player.loseMaxHp(num);
      const targetx = game.filterPlayer((current) => ![player, target].includes(current) && !current.isRest());
      if (targetx.length) {
        await game.doAsyncInOrder(targetx, async (target2) => {
          target2.popup("开始坐牢");
          await target2.rest({ type: "phase", count: -1 });
        });
        player.addSkill(`${event.name}_restend`);
        player.markAuto(`${event.name}_restend`, [target, player]);
        player.markAuto(`${event.name}_restend3`, targetx);
        player.addMark(`${event.name}_restend2`, num, false);
      }
    },
    subSkill: {
      restend: {
        charlotte: true,
        forced: true,
        silent: true,
        popup: false,
        onremove(player, skill) {
          delete player.storage[skill];
          delete player.storage[`${skill}3`];
          player.clearMark(`${skill}2`, false);
        },
        forceDie: true,
        trigger: {
          global: ["phaseAfter", "dieEnd"]
        },
        filter(event, player) {
          return event.name === "die" || player.getStorage("psjimou_restend").includes(event.player);
        },
        async content(event, trigger, player) {
          if (trigger.name === "phase") {
            player.removeMark(`${event.name}2`, 1, false);
          }
          if (trigger.name === "die" || !player.hasMark(`${event.name}2`)) {
            await game.doAsyncInOrder(player.getStorage(`${event.name}3`).sortBySeat(), async (target) => {
              target.popup("坐牢结束啦！");
              await target.restEnd({ hp: target.getHp() });
            });
          }
        }
      }
    }
  },
  psyuxue: {
    audio: 2,
    enable: "chooseToUse",
    usable: 1,
    locked: false,
    mod: {
      targetInRange(card) {
        if (card?.storage?.psyuxue) {
          return true;
        }
      }
    },
    filter(event, player) {
      if (!player.hasCards("he", (card) => !get.is.damageCard(card))) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (info[2] !== "sha") {
          return false;
        }
        return event.filterCard(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3],
              storage: { psyuxue: true }
            },
            "unsure"
          ),
          player,
          event
        );
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (info[2] !== "sha") {
            return false;
          }
          return event.filterCard(
            get.autoViewAs(
              {
                name: info[2],
                nature: info[3],
                storage: { psyuxue: true }
              },
              "unsure"
            ),
            player,
            event
          );
        });
        return ui.create.dialog("浴血", [list, "vcard"]);
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = get.player();
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard(card) {
            return !get.is.damageCard(card);
          },
          selectCard: [1, Infinity],
          position: "he",
          audio: "psyuxue",
          popname: true,
          link: links[0],
          complexCard: true,
          filterTarget(card, player2, target) {
            const link = get.info("psyuxue_backup").link;
            const cardx = get.autoViewAs(
              {
                name: link[2],
                nature: link[3],
                storage: { psyuxue: true }
              },
              "unsure"
            );
            return player2.canUse(cardx, target);
          },
          selectTarget() {
            return ui.selected.cards?.length;
          },
          ai1(card) {
            const link = get.info("psyuxue_backup").link;
            const cardx = get.autoViewAs(
              {
                name: link[2],
                nature: link[3],
                storage: { psyuxue: true }
              },
              "unsure"
            );
            const player2 = get.player();
            const num = game.countPlayer((current) => get.effect(current, cardx, player2, player2) > 0);
            if (!ui.selected.cards?.length || ui.selected.cards.length < num) {
              return 10 - get.value(card);
            }
            return 0;
          },
          ai2(target) {
            const link = get.info("psyuxue_backup").link;
            const cardx = get.autoViewAs(
              {
                name: link[2],
                nature: link[3],
                storage: { psyuxue: true }
              },
              "unsure"
            );
            const player2 = get.player();
            return get.effect(target, cardx, player2, player2);
          },
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            storage: { psyuxue: true }
          },
          log: false,
          multiline: true,
          multitarget: true,
          async precontent(event, trigger, player2) {
            const { targets } = event.result;
            player2.logSkill("psyuxue", targets);
          }
        };
      },
      prompt(links, player) {
        return `将任意张非伤害牌当做可指定等量目标的${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用`;
      },
      hiddenCard(player, name) {
        return name === "sha" && player.hasCards("he", (card) => !get.is.damageCard(card));
      }
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      skillTagFilter(player) {
        if (!player.hasCards("he", (card) => !get.is.damageCard(card))) {
          return false;
        }
      },
      order(item, player) {
        player ??= get.player();
        return get.order({ name: "sha" }, player) + 0.1;
      },
      result: {
        player: 1
      }
    },
    group: "psyuxue_jiqu",
    subSkill: {
      backup: {},
      jiqu: {
        audio: "psyuxue",
        trigger: { source: "damageSource" },
        filter(event, player) {
          return event.card?.storage?.psyuxue && event.player?.isIn();
        },
        logTarget: "player",
        check(event, player) {
          return -get.attitude(player, event.player);
        },
        prompt2: "汲取其1点体力上限",
        async content(event, trigger, player) {
          const target = event.targets[0];
          await player.gainMaxHp();
          await target.loseMaxHp();
        }
      }
    }
  },
  pskuifa: {
    audio: 2,
    trigger: { player: "useCard" },
    onremove: true,
    kuifa(card) {
      const info = lib.card[card.name];
      if (!info || info.notarget) {
        return false;
      }
      if (info.selectTarget !== void 0) {
        if (Array.isArray(info.selectTarget)) {
          if (info.selectTarget[0] < 0) {
            return !info.toself;
          }
          return info.selectTarget[0] !== 1 || info.selectTarget[1] !== 1;
        }
        if (info.selectTarget < 0) {
          return !info.toself;
        }
        return info.selectTarget !== 1;
      }
      return false;
    },
    filter(event, player) {
      const targets = event.targets;
      if (!targets?.length) {
        return false;
      }
      const num = game.countPlayer((current) => !targets.includes(current));
      const bool1 = targets.length < game.players.length;
      const bool2 = targets.length >= player.getHp() && num > 0;
      if (!bool1 && !bool2) {
        return false;
      }
      return get.is.damageCard(event.card) && get.info("pskuifa").kuifa(event.card);
    },
    async cost(event, trigger, player) {
      const targets = trigger.targets;
      const num = game.countPlayer((current) => !targets.includes(current));
      const beishui = player.countMark(event.skill);
      const list = [];
      const choiceList = ["令本回合使用【杀】的次数+1", `摸${num}张牌`, `背水：失去${beishui}点体力`];
      if (targets.length < game.players.length) {
        list.push("选项一");
      } else {
        choiceList[0] = `<span style="opacity:0.5">${choiceList[0]}</span>`;
      }
      if (targets.length >= player.getHp() && num > 0) {
        list.push("选项二");
      } else {
        choiceList[1] = `<span style="opacity:0.5">${choiceList[1]}</span>`;
      }
      if (!list.length) {
        return;
      }
      list.push("背水！");
      list.push("cancel2");
      const result = await player.chooseControl({
        controls: list,
        prompt: "馈伐：你可执行一项",
        choiceList,
        ai() {
          const { player: player2, controls, num: num2 } = get.event();
          if (num2 >= player2.getHp() && controls.length > 3) {
            controls.remove("背水！");
          }
          if (!player2.hasCards("sha")) {
            controls.remove("选项一");
          }
          return controls.slice().remove("cancel2").randomGet();
        }
      }).set("num", beishui).forResult();
      if (typeof result?.control === "string" && result.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.control
        };
      }
    },
    async content(event, trigger, player) {
      const { cost_data: link } = event;
      const targets = trigger.targets;
      const num = game.countPlayer((current) => !targets.includes(current));
      const beishui = player.countMark(event.name);
      if (link === "背水！") {
        player.popup("背水！");
        if (beishui > 0) {
          await player.loseHp(beishui);
        }
        player.addMark(event.name, 1, false);
      }
      if (["背水！", "选项一"].includes(link)) {
        player.addTempSkill(`${event.name}_sha`);
        player.addMark(`${event.name}_sha`, 1, false);
      }
      if (["背水！", "选项二"].includes(link) && num > 0) {
        await player.draw({ num });
      }
      if (targets.length < game.players.length && targets.length >= player.getHp()) {
        player.popup("乘势！");
        trigger.directHit.addArray(game.players);
        game.log(trigger.card, "不可被响应");
      }
    },
    subSkill: {
      sha: {
        charlotte: true,
        onremove: true,
        mod: {
          cardUsable(card, player, num) {
            if (card.name === "sha") {
              return num + player.countMark("pskuifa_sha");
            }
          }
        },
        mark: true,
        intro: { content: "本回合使用【杀】的次数+#" }
      }
    }
  },
  //魔魏延
  psnimou: {
    audio: 2,
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player) {
      return player.maxHp > 1 && game.players.length > 1;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "失去任意体力上限并选择等量名其他角色令其进入“修整”状态",
        filterTarget: lib.filter.notMe,
        selectTarget: [1, get.player().maxHp - 1],
        complexTarget: true,
        ai(target) {
          const { player: player2 } = get.event();
          if (ui.selected.targets?.length < player2.getDamagedHp()) {
            return get.attitude(player2, target);
          }
          return 0;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const { targets } = event;
      await player.loseMaxHp(targets.length);
      await game.doAsyncInOrder(targets, async (target) => {
        target.popup("开始坐牢");
        await target.rest({ type: "phase", count: -1 });
      });
      const targetx = game.filterPlayer((current) => !targets.includes(current));
      player.addSkill(`${event.name}_restend`);
      player.markAuto(`${event.name}_restend`, targets);
      player.markAuto(`${event.name}_restend2`, targetx);
    },
    subSkill: {
      restend: {
        forced: true,
        silent: true,
        popup: false,
        charlotte: true,
        onremove(player, skill) {
          delete player.getStorage(skill);
          delete player.getStorage(`${skill}2`);
        },
        forceDie: true,
        trigger: {
          global: ["dieEnd", "phaseAfter"]
        },
        async content(event, trigger, player) {
          if (trigger.name === "phase") {
            player.unmarkAuto(`${event.name}2`, trigger.player);
          }
          if (trigger.name === "die" || !player.getStorage(`${event.name}2`).length) {
            await game.doAsyncInOrder(player.getStorage(event.name).sortBySeat(), async (target) => {
              target.popup("坐牢结束啦！");
              await target.restEnd({ hp: target.getHp() });
            });
          }
        }
      }
    }
  },
  pskexue: {
    audio: 2,
    enable: "chooseToUse",
    usable: 1,
    filter(event, player) {
      if (!player.hasCards("he", (card) => !get.is.damageCard(card))) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (info[2] !== "sha") {
          return false;
        }
        return event.filterCard(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3]
            },
            "unsure"
          ),
          player,
          event
        );
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (info[2] !== "sha") {
            return false;
          }
          return event.filterCard(
            get.autoViewAs(
              {
                name: info[2],
                nature: info[3]
              },
              "unsure"
            ),
            player,
            event
          );
        });
        return ui.create.dialog("渴血", [list, "vcard"]);
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = get.player();
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard(card) {
            return !get.is.damageCard(card);
          },
          selectCard: [1, Infinity],
          position: "he",
          audio: "pskexue",
          popname: true,
          link: links[0],
          check(card) {
            return 8 - get.value(card);
          },
          viewAs: {
            name: links[0][2],
            nature: links[0][3]
          },
          log: false,
          async precontent(event, trigger, player2) {
            const num = event.result.cards.length;
            player2.logSkill("pskexue");
            event.result._apply_args = {
              shanReq: num,
              oncard: () => {
                const evt2 = get.event();
                for (const target of game.filterPlayer(null, null, true)) {
                  const id = target.playerid;
                  const map = evt2.customArgs;
                  if (!map[id]) {
                    map[id] = {};
                  }
                  map[id].shanRequired = evt2.shanReq;
                }
              }
            };
          }
        };
      },
      prompt(links, player) {
        return `将任意张非伤害牌当做须等量【闪】抵消的${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用`;
      },
      hiddenCard(player, name) {
        return name === "sha" && player.hasCards("he", (card) => !get.is.damageCard(card));
      }
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      skillTagFilter(player) {
        if (!player.hasCards("he", (card) => !get.is.damageCard(card))) {
          return false;
        }
      },
      order(item, player) {
        player ??= get.player();
        return get.order({ name: "sha" }, player) + 0.1;
      },
      result: {
        player: 1
      }
    },
    group: "pskexue_jiqu",
    subSkill: {
      backup: {},
      jiqu: {
        audio: "pskexue",
        trigger: { source: "damageSource" },
        filter(event, player) {
          const target = event.player;
          if (!target?.isIn() || event.getParent(2).skill !== "pskexue_backup") {
            return false;
          }
          const evtx = event.getParent(2);
          const card = event.card;
          card?.name;
          if (!card || card.name !== "sha") {
            return false;
          }
          return !target.hasHistory("useCard", (evt2) => evt2.card.name === "shan" && evt2.respondTo && evt2.getParent(3) === evtx);
        },
        logTarget: "player",
        check(event, player) {
          return -get.attitude(player, event.player);
        },
        prompt2: "汲取其1点体力上限",
        async content(event, trigger, player) {
          const target = event.targets[0];
          await player.gainMaxHp();
          await target.loseMaxHp();
        }
      }
    }
  },
  psjiefa: {
    audio: 2,
    trigger: { player: "useCardToPlayer" },
    onremove: true,
    filter(event, player) {
      const targets = event.targets;
      if (!targets?.length || targets.length !== 1) {
        return false;
      }
      const target = event.target;
      const bool1 = target.hp > game.players.length && game.hasPlayer((current) => current !== target && lib.filter.targetEnabled2(event.card, player, current));
      const bool2 = player.maxHp < target.maxHp;
      if (!bool1 && !bool2) {
        return false;
      }
      return get.is.damageCard(event.card);
    },
    async cost(event, trigger, player) {
      const target = trigger.target;
      const beishui = player.countMark(event.skill);
      const list = [];
      const choiceList = [`令${get.translation(trigger.card)}可额外指定一个目标`, `令此牌额外结算一次`, `背水：失去${beishui}点体力`];
      if (target.hp > game.players.length && game.hasPlayer((current) => current !== target && lib.filter.targetEnabled2(trigger.card, player, current))) {
        list.push("选项一");
      } else {
        choiceList[0] = `<span style="opacity:0.5">${choiceList[0]}</span>`;
      }
      if (player.maxHp < target.maxHp) {
        list.push("选项二");
      } else {
        choiceList[1] = `<span style="opacity:0.5">${choiceList[1]}</span>`;
      }
      if (!list.length) {
        return;
      }
      list.push("背水！");
      list.push("cancel2");
      const result = await player.chooseControl({
        controls: list,
        prompt: "竭伐：你可执行一项",
        choiceList,
        ai() {
          const { player: player2, controls, num } = get.event();
          if (num < 2 && controls.length > 3 && player2.hp > num) {
            return "背水！";
          }
          return controls.slice().removeArray(["cancel2", "背水！"]).randomGet();
        }
      }).set("num", beishui).forResult();
      if (typeof result?.control === "string" && result.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.control
        };
      }
    },
    async content(event, trigger, player) {
      const { cost_data: link } = event;
      const target = trigger.target;
      const beishui = player.countMark(event.name);
      if (link === "背水！") {
        player.popup("背水！");
        if (beishui > 0) {
          await player.loseHp(beishui);
        }
        player.addMark(event.name, 1, false);
      }
      if (["背水！", "选项一"].includes(link)) {
        const result = await player.chooseTarget({
          prompt: `竭伐：为${get.translation(trigger.card)}额外指定一个目标`,
          filterTarget(card, player2, target2) {
            return target2 !== get.event().targetx && lib.filter.targetEnabled2(get.event().cardx, player2, target2);
          },
          ai(target2) {
            return get.effect(target2, get.event().cardx, get.player(), get.player());
          }
        }).set("targetx", target).set("cardx", trigger.card).forResult();
        if (result?.bool) {
          const targetx = result.targets[0];
          player.line(targetx);
          trigger.targets.push(targetx);
        }
      }
      if (["背水！", "选项二"].includes(link)) {
        trigger.getParent().effectCount++;
      }
      if (player.maxHp < target.maxHp && target.hp > game.players.length) {
        player.popup("乘势！");
        const list = [];
        if (player.isDamaged()) {
          list.push("recover_hp");
        }
        if (player.countCards("h") < player.maxHp) {
          list.push("draw_card");
        }
        if (!list.length) {
          return;
        }
        const result = list.length > 1 ? await player.chooseControl({
          controls: list,
          prompt: "竭伐：回复体力至上限或将手牌摸至体力上限",
          ai() {
            if (get.player().getHp() < 2) {
              return "recover_hp";
            }
            return get.event().controls.slice().randomGet();
          }
        }).forResult() : { control: list[0] };
        if (typeof result?.control === "string") {
          if (result.control === "recover_hp") {
            await player.recoverTo(player.maxHp);
          } else {
            await player.drawTo(player.maxHp);
          }
        }
      }
    }
  }
};
const skills$q = {
  //官盗K系列杜预
  pkwuku: {
    audio: "spwuku",
    trigger: { global: "useCard" },
    forced: true,
    preHidden: true,
    filter(event, player) {
      if (get.type(event.card) !== "equip") {
        return false;
      }
      return player.countMark("pkwuku") < 3;
    },
    async content(event, trigger, player) {
      player.addMark("pkwuku", 1);
    },
    marktext: "库",
    intro: {
      content: "mark"
    },
    ai: {
      combo: "pkmiewu",
      threaten: 3.6
    }
  },
  pksanchen: {
    audio: "spsanchen",
    trigger: { player: "phaseJieshuBegin" },
    forced: true,
    juexingji: true,
    skillAnimation: true,
    animationColor: "gray",
    filter(event, player) {
      return player.countMark("pkwuku") > 2;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.gainMaxHp();
      player.recover();
      player.addSkills("pkmiewu");
    },
    ai: {
      combo: "pkwuku"
    },
    derivation: "pkmiewu"
  },
  pkmiewu: {
    audio: "spmiewu",
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      if (!player.countMark("pkwuku") || player.hasSkill("pkmiewu_used")) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (!["basic", "trick"].includes(info[0])) {
          return false;
        }
        return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player, event);
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (!["basic", "trick"].includes(info[0])) {
            return false;
          }
          return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player, event);
        });
        return ui.create.dialog("灭吴", [list, "vcard"]);
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = get.player();
        if (["wugu", "zhulu_card", "yiyi", "lulitongxin", "lianjunshengyan", "diaohulishan"].includes(button.link[2])) {
          return 0;
        }
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          audio: "spmiewu",
          filterCard: () => false,
          selectCard: -1,
          popname: true,
          viewAs: { name: links[0][2], nature: links[0][3], isCard: true },
          log: false,
          async precontent(event, trigger, player2) {
            player2.when({ player: ["useCardAfter", "respondAfter"] }).filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player3) => {
              player3.removeSkill(event2.name);
              await player3.draw();
            });
            player2.addTempSkill("pkmiewu_used");
            player2.logSkill("pkmiewu");
            player2.removeMark("pkwuku", 1);
          }
        };
      },
      prompt(links, player) {
        return `视为使用${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}并摸一张牌`;
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name)) {
        return false;
      }
      const type = get.type(name);
      return ["basic", "trick"].includes(type) && player.countMark("pkwuku") > 0 && !player.hasSkill("pkmiewu_used");
    },
    ai: {
      combo: "pkwuku",
      fireAttack: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player) {
        if (!player.countMark("pkwuku") || player.hasSkill("pkmiewu_used")) {
          return false;
        }
      },
      order: 7,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      backup: {},
      used: { charlotte: true }
    }
  }
};
const skills$p = {
  //天书乱斗虚拟偶像线下化
  //小杀
  vtbguisha: {
    audio: 1,
    trigger: { global: "useCard" },
    direct: true,
    filter(event, player) {
      return event.player !== player && event.card.name === "sha" && player.countCards("he") > 0 && event.player.isPhaseUsing();
    },
    async content(event, trigger, player) {
      let go = false;
      let d1 = false;
      if (get.attitude(player, trigger.player) > 0) {
        for (const target of trigger.targets) {
          const isDirectHit = !target.mayHaveShan(player, "use") || trigger.player.hasSkillTag("directHit_ai", true, { target, card: trigger.card }, true);
          if (!isDirectHit) {
            continue;
          }
          if (get.attitude(player, target) >= 0 || trigger.player.hasSkillTag("jueqing", false, target) || target.hasSkillTag("filterDamage", null, {
            player: trigger.player,
            card: trigger.card
          })) {
            continue;
          }
          d1 = true;
          break;
        }
        if (trigger.addCount === false || !trigger.player.isPhaseUsing()) {
          go = false;
        } else if (!trigger.player.hasSkill("paoxiao") && !trigger.player.hasSkill("tanlin3") && !trigger.player.hasSkill("zhaxiang2") && !trigger.player.hasSkill("fengnu") && !trigger.player.getEquip("zhuge")) {
          const nh = trigger.player.countCards("h");
          if (player === trigger.player) {
            go = player.countCards("h", "sha") > 0;
          } else if (nh >= 4) {
            go = true;
          } else if (player.countCards("h", "sha")) {
            if (nh === 3) {
              go = Math.random() < 0.8;
            } else if (nh === 2) {
              go = Math.random() < 0.5;
            }
          } else if (nh >= 3) {
            if (nh === 3) {
              go = Math.random() < 0.5;
            } else if (nh === 2) {
              go = Math.random() < 0.2;
            }
          }
        }
      }
      go = Number(go) * Math.random() + Number(d1) * Math.random() > 0.4;
      if (go && !event.isMine() && !event.isOnline() && player.hasCard((card) => get.value(card) < 6 && lib.filter.cardDiscardable(card, player, event.name), "he")) {
        await game.delayx();
      }
      const result = await player.chooseToDiscard({
        prompt: get.prompt("vtbguisha"),
        prompt2: `弃置一张牌，令${get.translation(trigger.player)}本次使用的【杀】不计入使用次数，且对${get.translation(trigger.targets)}造成的伤害+1`,
        position: "he",
        ai: (card) => {
          if (get.event().go) {
            return 6 - get.value(card);
          }
          return 0;
        }
      }).set("logSkill", ["vtbguisha", trigger.player]).set("go", go).forResult();
      if (!result.bool) {
        return;
      }
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        const stat = trigger.player.getStat().card;
        const name = trigger.card.name;
        if (typeof stat[name] === "number") {
          stat[name]--;
        }
      }
      trigger.player.addTempSkill("vtbguisha_bonus");
      if (!trigger.card.storage) {
        trigger.card.storage = {};
      }
      trigger.card.storage.vtbguisha_targets = trigger.targets;
    },
    ai: {
      expose: 0.2
    },
    subSkill: {
      bonus: {
        trigger: {
          source: "damageBegin1"
        },
        forced: true,
        charlotte: true,
        onremove: true,
        filter(event, player) {
          return event.card && event.card.name === "sha" && event.card.storage && event.card.storage.vtbguisha_targets && event.card.storage.vtbguisha_targets.includes(event.player);
        },
        async content(event, trigger, player) {
          trigger.num++;
        }
      }
    }
  },
  vtbshuli: {
    audio: 1,
    trigger: {
      global: "damageSource"
    },
    usable: 2,
    filter(event, player) {
      return event.source && event.source !== player && event.card && event.card.name === "sha" && event.source.isIn();
    },
    logTarget: "source",
    check(event, player) {
      return get.attitude(player, event.source) >= 0 || get.attitude(player, event.source) >= -4 && get.distance(_status.currentPhase, player, "absolute") > get.distance(_status.currentPhase, event.source, "absolute");
    },
    async content(event, trigger, player) {
      const drawers = [trigger.source, player].sortBySeat(_status.currentPhase);
      await game.asyncDraw(drawers);
    }
  },
  //小闪
  vtbshanwu: {
    audio: 1,
    trigger: {
      global: "useCardToTarget"
    },
    filter(event, player) {
      return event.card.name === "sha" && event.target !== player && event.isFirstTarget && player.hasCard((card) => get.name(card) === "shan" || _status.connectMode);
    },
    direct: true,
    async content(event, trigger, player) {
      let effect = 0;
      for (const target of trigger.targets) {
        let eff = get.effect(target, trigger.card, trigger.player, player);
        if (!target.mayHaveShan(player, "use") || trigger.player.hasSkillTag("directHit_ai", true, { target, card: trigger.card }, true)) {
          eff *= 1.25;
        }
        if (target.hp <= 2) {
          eff *= 1.1;
        }
        effect += eff;
      }
      const result = await player.chooseToDiscard({
        prompt: get.prompt("vtbshanwu"),
        prompt2: `弃置一张【闪】，取消此【杀】对${get.translation(trigger.targets)}的目标`,
        filterCard: { name: "shan" },
        ai: (card) => {
          if (get.event().goon) {
            return 6 - get.value(card);
          }
          return 0;
        }
      }).set("logSkill", "vtbshanwu").set("goon", effect < 0).forResult();
      if (!result.bool) {
        return;
      }
      game.log(player, "取消了", trigger.card, "的所有目标");
      trigger.targets.length = 0;
      trigger.getParent().triggeredTargets2.length = 0;
      trigger.untrigger();
    },
    ai: {
      expose: 0.2
    }
  },
  vtbxianli: {
    audio: 1,
    trigger: {
      player: "loseAfter",
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    usable: 2,
    filter(event, player) {
      if (!_status.currentPhase || !_status.currentPhase.isIn() || !_status.currentPhase.countGainableCards(player, "he")) {
        return false;
      }
      const evt = event.getl(player);
      return evt && evt.cards2 && evt.cards2.some((card) => get.name(card, false) === "shan");
    },
    check(event, player) {
      return get.effect(_status.currentPhase, { name: "shunshou_copy2" }, player, player) > 0;
    },
    prompt2(event, player) {
      return `获得${get.translation(_status.currentPhase)}的一张牌`;
    },
    logTarget: () => _status.currentPhase,
    async content(event, trigger, player) {
      await player.gainPlayerCard({
        target: _status.currentPhase,
        position: "he",
        forced: true
      });
    },
    ai: {
      expose: 0.15
    }
  },
  //小桃
  vtbtaoyan: {
    audio: 1,
    trigger: {
      player: "phaseBegin"
    },
    direct: true,
    async content(event, trigger, player) {
      if (!_status.vtbtaoyan_count) {
        _status.vtbtaoyan_count = 6;
      }
      const result = await player.chooseTarget({
        prompt: get.prompt("vtbtaoyan"),
        prompt2: "令一或两名其他角色摸一张牌并从游戏外获得一张【桃】指示物",
        filterTarget: lib.filter.notMe,
        selectTarget: [1, 2],
        ai: (target) => get.recoverEffect(target, player, player) / 2 + get.attitude(player, target)
      }).forResult();
      if (!result.bool) {
        return;
      }
      const targets = result.targets.slice().sortBySeat();
      player.logSkill("vtbtaoyan", targets);
      game.broadcastAll(() => {
        if (!lib.inpile.includes("tao")) {
          lib.inpile.add("tao");
        }
      });
      player.addSkill("vtbtaoyan_remove");
      const nextEvents = [];
      for (const target of targets) {
        nextEvents.push(target.draw());
        if (!_status.vtbtaoyan_count) {
          continue;
        }
        if (!_status.vtbtaoyan_cards) {
          _status.vtbtaoyan_cards = [];
        }
        _status.vtbtaoyan_count--;
        const card = game.createCard("tao");
        _status.vtbtaoyan_cards.push(card.cardid);
        nextEvents.push(target.gain({ cards: [card], animate: "gain2" }));
      }
      for (const nextEvent of nextEvents) {
        await nextEvent;
      }
    },
    ai: {
      expose: 0.3,
      threaten: 3.2
    },
    subSkill: {
      remove: {
        trigger: {
          global: ["loseAfter", "loseAsyncAfter", "cardsDiscardAfter", "equipAfter"]
        },
        forced: true,
        charlotte: true,
        popup: false,
        firstDo: true,
        forceDie: true,
        filter(event, player) {
          if (typeof _status.vtbtaoyan_count !== "number") {
            return false;
          }
          const cards2 = event.getd();
          return cards2.some((card) => _status.vtbtaoyan_cards.includes(card.cardid));
        },
        async content(event, trigger, player) {
          const cards2 = trigger.getd();
          const remove = [];
          for (const card of cards2) {
            if (_status.vtbtaoyan_cards.includes(card.cardid)) {
              _status.vtbtaoyan_cards.remove(card.cardid);
              remove.push(card);
            }
          }
          if (!remove.length) {
            return;
          }
          for (const card of remove) {
            card.remove();
            _status.vtbtaoyan_count++;
          }
          game.log(remove, "被移出了游戏");
        }
      }
    }
  },
  vtbyanli: {
    audio: 1,
    trigger: {
      global: "dying"
    },
    filter(event, player) {
      if (player.hasSkill("vtbyanli_used")) {
        return false;
      }
      if (_status.currentPhase === player) {
        return false;
      }
      return event.player.hp <= 0;
    },
    check(event, player) {
      return get.recoverEffect(event.player, player, player) > 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      player.addTempSkill("vtbyanli_used", "roundStart");
      const recoverEvent = trigger.player.recover(1 - trigger.player.hp);
      const drawEvent = trigger.player.draw();
      await recoverEvent;
      await drawEvent;
    },
    subSkill: {
      used: {
        charlotte: true
      }
    }
  },
  //小乐
  vtbleyu: {
    audio: 1,
    trigger: {
      global: "phaseBegin"
    },
    filter(event, player) {
      return player.countCards("he") >= 3;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard({
        prompt: get.prompt2("vtbleyu", trigger.player),
        selectCard: 3,
        position: "he",
        chooseonly: true,
        ai: (card) => {
          if (ui.selected.cards.length === 2) {
            return 10 - get.value(card);
          }
          if (get.event().effect > 0) {
            return 6 - get.value(card);
          }
          return 0;
        }
      }).set("effect", trigger.player.hasJudge("lebu") ? 0 : get.effect(trigger.player, { name: "lebu" }, player, player)).forResult();
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await player.discard({ cards: event.cards });
      const judgeResult = await trigger.player.judge({
        judge: lib.card.lebu.judge,
        judge2: lib.card.lebu.judge2
      }).forResult();
      if (!judgeResult.bool) {
        trigger.player.skip("phaseUse");
      }
    },
    ai: {
      expose: 0.3,
      threaten: 2.9
    }
  },
  vtbyuanli: {
    audio: 1,
    trigger: { global: ["phaseUseSkipped", "phaseUseCancelled"] },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt2("vtbyuanli"),
        filterTarget: lib.filter.notMe,
        ai: (target) => get.attitude(player, target) + 1
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await game.asyncDraw([player, target].sortBySeat(_status.currentPhase));
    },
    ai: {
      expose: 0.1
    }
  },
  vtbmeiniang: {
    audio: 1,
    trigger: { global: "phaseUseBegin" },
    filter(event, player) {
      return event.player !== player;
    },
    check(event, player) {
      return get.attitude(player, event.player) > 0 && event.player.getUseValue("jiu") >= 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await trigger.player.chooseUseTarget({
        card: { name: "jiu", isCard: true },
        forced: true,
        addCount: false
      });
    }
  },
  vtbyaoli: {
    audio: 1,
    trigger: { global: "useCardAfter" },
    filter(event, player) {
      return event.card.name === "jiu" && event.player !== player && event.player.isPhaseUsing();
    },
    logTarget: "player",
    check(event, player) {
      return get.attitude(player, event.player) > 0;
    },
    async content(event, trigger, player) {
      trigger.player.addTempSkill(`${event.name}_effect`);
      trigger.player.addMark(`${event.name}_effect`, 1, false);
    },
    ai: { expose: 0.15 },
    subSkill: {
      effect: {
        audio: "vtbyaoli",
        charlotte: true,
        trigger: { player: "useCard2" },
        forced: true,
        onremove: true,
        direct: true,
        filter(event, player) {
          return event.card.name === "sha" && player.countMark("vtbyaoli_effect") > 0;
        },
        async content(event, trigger, player) {
          trigger.directHit.addArray(game.filterPlayer());
          const num = player.countMark(event.name);
          player.removeSkill(event.name);
          const targets = game.filterPlayer((current) => !trigger.targets.includes(current) && lib.filter.targetEnabled2(trigger.card, player, current));
          if (!targets.length) {
            return;
          }
          const result = await player.chooseTarget({
            prompt: `媱丽：是否为${get.translation(trigger.card)}额外指定${num > 1 ? "至多" : ""}${get.cnNumber(num)}个目标？`,
            selectTarget: num === 1 ? 1 : [1, num],
            filterTarget: (_card, player2, target) => !get.event().sourcex.includes(target) && player2.canUse(get.event().card, target),
            ai: (target) => {
              const { player: player2, card } = get.event();
              return get.effect(target, card, player2, player2);
            }
          }).set("sourcex", trigger.targets).set("card", trigger.card).forResult();
          if (!result.targets?.length) {
            return;
          }
          if (!event.isMine() && !event.isOnline()) {
            await game.delayx();
          }
          player.logSkill(event.name, result.targets);
          trigger.targets.addArray(result.targets);
        },
        marktext: "媱",
        intro: { content: "下一张【杀】不可被响应且可以额外指定&个目标" },
        ai: {
          directHit_ai: true,
          skillTagFilter(player, tag, arg) {
            return arg?.card?.name === "sha";
          }
        }
      }
    }
  }
};
const skills$o = {
  zj_juxian: {
    trigger: {
      player: ["damageBegin3", "loseBefore"]
    },
    usable: 20,
    //红桃七惨案
    filter(event, player) {
      if (event.name === "damage") {
        return player.countCards("h") === 1;
      }
      return event.cards.length && event.cards.someInD("he");
    },
    prompt2(event, player) {
      if (event.name === "damage") {
        return "防止此伤害";
      }
      return `即将失去${get.translation(event.cards.filterInD("he"))}，失去1点体力并防止之`;
    },
    check(event, player) {
      if (event.name === "damage") {
        return true;
      }
      if (player.hp <= 1) {
        return false;
      }
      return event.cards.filterInD("he").reduce((sum, card) => sum + player.getUseValue(card), -10) > 0;
    },
    async content(event, trigger, player) {
      if (trigger.name === "damage") {
        trigger.cancel();
        return;
      }
      await player.loseHp();
      if (trigger.cards.everyInD("he")) {
        trigger.cancel();
      } else {
        for (let i = 0; i < trigger.cards.length; i++) {
          const pos = get.position(trigger.cards[i]);
          if ("he".includes(pos)) {
            trigger.cards.splice(i--, 1);
          }
        }
      }
    }
  },
  zj_lijun: {
    trigger: {
      player: ["phaseZhunbeiBegin", "phaseJieshuBegin"]
    },
    filter(event, player) {
      if (event.name === "phaseZhunbei") {
        return game.hasPlayer((current) => current.isDamaged());
      }
      return !player.getHistory("lose", (evt) => evt.type === "discard").length;
    },
    async cost(event, trigger, player) {
      if (trigger.name === "phaseZhunbei") {
        event.result = await player.chooseTarget(get.prompt(event.skill), "令任意名已受伤的角色各摸一张牌", (card, player2, target) => target.isDamaged(), [1, Infinity]).set("ai", (target) => get.attitude(get.player(), target)).forResult();
      } else {
        event.result = await player.chooseBool(get.prompt(event.skill), "回复1点体力").forResult();
      }
    },
    async content(event, trigger, player) {
      if (trigger.name === "phaseZhunbei") {
        await game.asyncDraw(event.targets.sortBySeat(player));
      } else {
        await player.recover();
      }
    }
  },
  zj_duanbing: {
    audio: "duanbing",
    trigger: {
      player: ["useCard2", "useCardToPlayered"],
      source: "damageSource"
    },
    filter(event, player) {
      if (event.name === "damage") {
        if (get.distance(player, event.player) > 1) {
          return false;
        }
        return player.getHistory("sourceDamage", (evt) => evt.player === event.player).indexOf(event) === 0;
      }
      if (event.card?.name !== "sha") {
        return false;
      }
      if (event.name === "useCard") {
        return game.hasPlayer((current) => !event.targets.includes(current) && get.distance(player, current) <= 1 && lib.filter.targetEnabled2(event.card, event.player, current));
      }
      return !event.getParent().directHit.includes(event.target) && get.distance(player, event.target) <= 1;
    },
    async cost(event, trigger, player) {
      if (trigger.name === "useCard") {
        event.result = await player.chooseTarget(`###${get.prompt(event.skill)}###为${get.translation(trigger.card)}增加一个目标`, (card, player2, target) => {
          const { sourcex, card: cardx } = get.event();
          return !sourcex.includes(target) && get.distance(player2, target) <= 1 && lib.filter.targetEnabled2(cardx, player2, target);
        }).set("sourcex", trigger.targets).set("ai", (target) => {
          const { player: player2, card } = get.event();
          return get.effect(target, card, player2, player2);
        }).set("card", trigger.card).setHiddenSkill(event.skill).forResult();
      } else {
        event.result = {
          bool: true,
          targets: [trigger[trigger.name === "damage" ? "player" : "target"]]
        };
      }
    },
    async content(event, trigger, player) {
      switch (trigger.name) {
        case "useCard": {
          if (!event.isMine() && !event.isOnline()) {
            await game.delayx();
          }
          trigger.targets.addArray(event.targets);
          break;
        }
        case "damage": {
          await player.draw();
          break;
        }
        default: {
          const id = trigger.target.playerid;
          const map = trigger.getParent().customArgs;
          map[id] ??= {};
          if (typeof map[id].shanRequired === "number") {
            map[id].shanRequired++;
          } else {
            map[id].shanRequired = 2;
          }
          break;
        }
      }
    },
    ai: {
      effect: {
        directHit_ai: true,
        skillTagFilter(player, tag, arg) {
          if (arg.card.name !== "sha" || arg.target.countCards("h", "shan") > 1 || get.distance(player, arg.target) > 1) {
            return false;
          }
        },
        player_use(card, player, target, current, isLink) {
          if (!isLink && card.name === "sha") {
            if (player._reduanbingtmp) {
              return;
            }
            player._reduanbingtmp = true;
            if (get.effect(target, card, player, player) <= 0) {
              delete player._reduanbingtmp;
              return;
            }
            if (game.hasPlayer((current2) => current2 !== target && get.distance(player, current2) <= 1 && player.canUse(card, current2) && get.effect(current2, card, player, player) > 0)) {
              delete player._reduanbingtmp;
              return [1, 1];
            }
            delete player._reduanbingtmp;
          }
        }
      }
    }
  },
  zj_fenxun: {
    audio: "fenxun",
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return target !== player;
    },
    async content(event, trigger, player) {
      const target = event.target;
      player.markAuto("fenxun2", [target]);
      player.addTempSkill("fenxun2");
    },
    ai: {
      order: 4,
      result: {
        player(player, target) {
          if (get.distance(player, target) <= 1) {
            return 0;
          }
          const hs = player.getCards("h", "shunshou");
          if (hs.length && player.canUse(hs[0], target, false)) {
            return 1;
          }
          const geteff = (current) => player.canUse("sha", current, false, true) && get.effect(current, { name: "sha" }, player, player) > 0;
          if (player.hasSha() && geteff(target)) {
            const num = game.countPlayer((current) => current !== player && get.distance(player, current) <= 1 && geteff(current));
            if (num === 0) {
              if (game.hasPlayer((current) => player.canUse("sha", current) && geteff(current) && current !== target)) {
                return 1;
              }
            } else if (num === 1) {
              return 1;
            }
          }
          return 0;
        }
      }
    }
  },
  zj_dangxian: {
    audio: "dangxian",
    trigger: {
      player: "phaseBegin"
    },
    forced: true,
    async content(event, trigger, player) {
      const cards2 = Array.from(ui.discardPile.childNodes).filter((card) => card.name === "sha");
      if (cards2.length) {
        const result = await player.chooseButton(["获得一张杀", cards2], true).forResult();
        if (result?.bool && result?.links?.length) {
          await player.gain(result.links, "gain2");
        }
      }
      game.updateRoundNumber();
      trigger.phaseList.splice(trigger.num, 0, `phaseUse|${event.name}`);
    },
    mod: {
      targetInRange(card, player) {
        const evt = get.event().getParent("phaseUse", true);
        if (evt?._extraPhaseReason) {
          return true;
        }
      }
    }
  },
  zj_fuli: {
    audio: "refuli",
    skillAnimation: true,
    animationColor: "soil",
    limited: true,
    enable: "chooseToUse",
    filter(event, player) {
      if (event.type !== "dying") {
        return false;
      }
      if (player !== event.dying) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.recoverTo(2);
      await player.draw(2);
      const next = player.insertPhase();
      next.phaseList = ["phaseUse"];
    },
    ai: {
      order: 3,
      save: true,
      skillTagFilter(player, arg, target) {
        return player === target;
      },
      result: { player: 10 },
      threaten(player, target) {
        if (!target.storage.zj_fuli) {
          return 0.9;
        }
      }
    }
  },
  //绝代智将
  //张虎乐綝
  zj_cuijian: {
    audio: "cuijian",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.hasPlayer((current) => lib.skill.zj_cuijian.filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return target !== player && target.countCards("h") > 0;
    },
    async content(event, trigger, player) {
      const target = event.target;
      const { cards: cards2 } = await target.showHandcards().forResult();
      const gains = cards2.filter((card) => get.name(card, target) === "shan" || get.subtype(card) === "equip2");
      if (gains?.length) {
        await player.gain(gains, "give", target, "bySelf");
      }
      if (!player.hasHistory("gain", (evt) => evt.getParent() === event)) {
        await player.draw(2);
      }
    },
    ai: {
      order: 4,
      result: {
        target(player, target) {
          return target.countCards("h") * -0.5;
        }
      }
    }
  },
  zj_porui: {
    audio: "dcporui",
    trigger: {
      global: "phaseJieshuBegin"
    },
    filter(event, player) {
      if (player === event.player) {
        return false;
      }
      if (player.countMark("zj_porui_round") >= 2 || player.countCards("he") <= 1) {
        return false;
      }
      return game.hasPlayer((current) => {
        if (current === player) {
          return false;
        }
        return current.hasHistory("lose", (evt) => evt.cards2.length > 0);
      });
    },
    async cost(event, trigger, player) {
      const map = /* @__PURE__ */ new Map();
      game.countPlayer((current) => {
        if (current === player) {
          return false;
        }
        const num = current.getHistory("lose").reduce((sum, evt) => {
          if (evt?.cards2?.length) {
            return sum + evt.cards2.length;
          }
          return sum;
        }, 1);
        map.set(current, Math.min(5, num));
      });
      event.result = await player.chooseCardTarget({
        prompt: get.prompt2(event.skill),
        filterCard(card, player2) {
          return lib.filter.cardDiscardable(card, player2, "zj_porui");
        },
        selectCard: 2,
        position: "he",
        filterTarget(card, player2, target) {
          const { map: map2 } = get.event();
          if (map2.has(target) && map2.get(target) > 1) {
            target.prompt(`破锐${map2.get(target)}`);
            return true;
          }
          return false;
        },
        complexTarget: true,
        ai1(card) {
          return 7 - get.value(card);
        },
        ai2(target) {
          const { player: player2, map: map2 } = get.event();
          const num = map2.get(target);
          let eff = get.effect(target, { name: "sha" }, player2, player2);
          if (num > 1 && eff !== 0) {
            eff -= 10 / target.getHp() * Math.pow(2, num);
          }
          return eff * num;
        }
      }).set("map", map).forResult();
    },
    async content(event, trigger, player) {
      player.addTempSkill("zj_porui_round", "roundStart");
      player.addMark("zj_porui_round", 1, false);
      const {
        cards: cards2,
        targets: [target]
      } = event;
      await player.modedDiscard(cards2);
      const num = Math.min(
        5,
        target.getHistory("lose").reduce((sum, evt) => {
          if (evt?.cards2?.length) {
            return sum + evt.cards2.length;
          }
          return sum;
        }, 1)
      );
      let count = 0;
      while (count < num) {
        count++;
        const card = new lib.element.VCard({ name: "sha", isCard: true });
        if (target?.isIn() && player.canUse(card, target, false)) {
          await player.useCard(card, target);
        }
      }
    },
    subSkill: {
      round: { charlotte: true, onremove: true }
    },
    ai: {
      expose: 0.4,
      threaten: 3.8
    }
  },
  //胡烈
  zj_chengxi: {
    trigger: {
      player: "useCardToPlayer"
    },
    logTarget: "target",
    filter(event, player) {
      return event.targets?.length === 1 && event.target.countCards("h");
    },
    check(event, player) {
      return get.effect(event.target, event.card, event.player, player) * get.attitude(player, event.target) >= 0;
    },
    async content(event, trigger, player) {
      const target = trigger.target;
      const { cards: cards2 } = await target.showHandcards(`${get.translation(player)}发动了【乘袭】`).forResult();
      if (!target.countCards("he")) {
        return;
      }
      const eff = lib.skill.dcshixian.filterx(trigger) ? get.effect(target, trigger.card, trigger.player, target) : 0;
      const result = await target.chooseCard([1, Infinity], "重铸至少一张牌", true, lib.filter.cardRecastable, "he", "allowChooseAll").set("ai", (card) => {
        const { player: player2, showCards: cards3, eff: eff2 } = get.event();
        const suit = get.suit(card);
        const selects = ui.selected.cards ?? [];
        if (eff2 >= 0 && selects.length) {
          return 0;
        }
        if (selects.some((cardx) => get.suit(cardx) === suit) || cards3.every((cardx) => get.suit(cardx) !== suit)) {
          return 4 - get.value(card);
        }
        return 8 - get.value(card);
      }).set("complexCard", true).set("showCards", cards2).set("eff", eff).forResult();
      if (result.bool && result.cards?.length) {
        await target.recast(result.cards);
        const suits1 = cards2.map((card) => get.suit(card)).toUniqued();
        const suits2 = result.cards.map((card) => get.suit(card)).toUniqued();
        const num = suits1.removeArray(suits2).length;
        if (num > 0 && lib.skill.dcshixian.filterx(trigger)) {
          trigger.getParent().effectCount += num;
          game.log(player, "令", trigger.card, `额外结算了${num}次`);
        }
      }
    }
  },
  zj_zhaoe: {
    trigger: { global: "phaseEnd" },
    filter(event, player) {
      if (!_status.currentPhase?.isIn() || !_status.currentPhase.countCards("h")) {
        return false;
      }
      return player.hasHistory("lose", (evt) => evt?.cards2?.length);
    },
    skillAnimation: true,
    animationColor: "thunder",
    limited: true,
    logTarget: () => _status.currentPhase,
    check(event, player) {
      if (!_status.currentPhase || get.attitude(player, _status.currentPhase) >= 0) {
        return false;
      }
      return _status.currentPhase.countCards("h") >= game.players.length / 2;
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      player.awakenSkill(event.name);
      const { cards: cards2 } = await target.showHandcards(`${get.translation(player)}发动了【昭恶】`).forResult();
      const num = cards2.filter((card) => get.is.damageCard(card)).length;
      if (num <= 0) {
        return;
      }
      const result = await player.chooseTarget(`令至多${get.cnNumber(num)}名角色对其使用${get.cnNumber(num)}张杀`, [1, num], true).set("ai", (target2) => {
        const { player: player2, targetx } = get.event();
        const card = get.autoViewAs({ name: "sha" }, "unsure");
        if (get.attitude(target2, targetx) >= 0) {
          return 0.1;
        }
        let eff = get.effect(targetx, card, target2, player2);
        if (target2.mayHaveSha(player2, "use", null, "bool")) {
          eff *= 1.5;
        }
        return eff;
      }).set("targetx", target).forResult();
      if (result?.bool && result.targets?.length) {
        player.line(result.targets);
        await game.delay(2);
        for (const current of result.targets.sortBySeat(target)) {
          let count = 0;
          while (count < num) {
            count++;
            const result2 = await current.chooseToUse(
              function(card, player2, event2) {
                if (get.name(card) !== "sha") {
                  return false;
                }
                return lib.filter.filterCard.apply(this, arguments);
              },
              `昭恶：是否对${get.translation(target)}使用杀？（${count}/${num}）`
            ).set("targetRequired", true).set("complexTarget", true).set("filterTarget", function(card, player2, target2) {
              const { preTarget } = get.event();
              if (target2 !== preTarget && !ui.selected.targets?.includes(preTarget)) {
                return false;
              }
              return lib.filter.targetEnabled.apply(this, arguments);
            }).set("preTarget", target).forResult();
            if (!result2?.bool) {
              break;
            }
          }
        }
      }
    }
  },
  //诸葛绪
  zj_tuizhi: {
    enable: "chooseToUse",
    hiddenCard(player, name) {
      if (get.type(name) === "basic" && lib.inpile.includes(name) && player.countCards("h")) {
        return true;
      }
    },
    filter(event, player) {
      if (event.responded || !player.countCards("h")) {
        return false;
      }
      for (const i of lib.inpile) {
        if (get.type(i) === "basic" && event.filterCard({ name: i, isCard: true }, player, event)) {
          return true;
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          const card = get.autoViewAs({ name: info[2], nature: info[3] }, "unsure");
          return event.filterCard(card, player, event);
        });
        return ui.create.dialog("退制", [list, "vcard"], "hidden");
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        if (button.link[2] === "shan") {
          return 3;
        }
        const player = _status.event.player;
        if (button.link[2] === "jiu") {
          if (player.getUseValue({ name: "jiu" }) <= 0) {
            return 0;
          }
          if (player.countCards("h", "sha")) {
            return player.getUseValue({ name: "jiu" });
          }
        }
        return player.getUseValue({ name: button.link[2], nature: button.link[3] }) / 4;
      },
      backup(links, player) {
        return {
          filterCard(card, player2) {
            return player2.getCards("h").every((cardx) => {
              if (get.color(cardx) !== get.color(card)) {
                return true;
              }
              return lib.filter.cardDiscardable(cardx, player2);
            });
          },
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            suit: "none",
            number: null,
            isCard: true
          },
          position: "h",
          ignoreMod: true,
          check(card) {
            return player.getCards("h", (cardx) => get.color(cardx) === get.color(card)).reduce((sum, cardx) => sum - get.value(cardx), 10);
          },
          log: false,
          async precontent(event, trigger, player2) {
            player2.logSkill("zj_tuizhi");
            const cards2 = player2.getCards("h", (card) => get.color(card) === get.color(event.result.cards[0]));
            await player2.showCards(cards2);
            await player2.discard(cards2);
            event.result.card = {
              name: event.result.card.name,
              nature: event.result.card.nature,
              isCard: true
            };
            event.result.cards = [];
            player2.when({
              player: "useCardAfter"
            }).filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player3) => {
              if (game.hasPlayer2((current) => current.hasHistory("sourceDamage", (evt) => evt.card === trigger2.card), true)) {
                await player3.draw();
              }
            });
          }
        };
      },
      prompt(links, player) {
        const [_1, _2, name, nature] = links[0];
        return `弃置一种颜色的所有手牌，然后视为使用${get.translation(nature) || ""}${get.translation(name)}`;
      }
    },
    ai: {
      order: 1,
      respondShan: true,
      respondSha: true,
      skillTagFilter(player) {
        if (!player.countCards("h")) {
          return false;
        }
      },
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    }
  },
  zj_qianjun: {
    trigger: {
      global: "useCardAfter"
    },
    filter(event, player) {
      if (event.player === player || event.card?.name !== "sha") {
        return false;
      }
      const card = get.autoViewAs({ name: "sha" }, "unsure");
      return event.targets?.some((target) => player.canUse(card, target, false));
    },
    direct: true,
    clearTime: true,
    async content(event, trigger, player) {
      const next = player.chooseToUse(function(card, player2, event2) {
        if (get.name(card) !== "sha") {
          return false;
        }
        return lib.filter.filterCard.apply(this, arguments);
      }, get.prompt2(event.name)).set("targetRequired", true).set("filterTarget", function(card, player2, target) {
        const { preTargets: targets } = get.event();
        if (!targets.includes(target)) {
          return false;
        }
        return lib.filter.targetEnabled.apply(this, arguments);
      }).set("logSkill", event.name).set("preTargets", trigger.targets);
      const result = await next.forResult();
      if (!result?.bool) {
        return;
      }
      const evts = player.getHistory("useCard", (evt) => evt.getParent() === next);
      if (evts.length && evts[0]?.targets?.length) {
        const evt = evts[0];
        for (const target of evt.targets) {
          if (!target.hasHistory("damage", (evtx) => evtx.card === evt.card)) {
            await player.discardPlayerCard(target, "he", true);
          }
        }
      }
    }
  },
  zj_guluo: {
    trigger: {
      player: "dying"
    },
    filter(event, player) {
      return !player.countCards("h");
    },
    forced: true,
    async content(event, trigger, player) {
      await player.loseMaxHp();
      await player.recoverTo(1);
    }
  },
  //陆抗
  zj_jueyan: {
    audio: "drlt_jueyan",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.hasEnabledSlot(1) || player.hasEnabledSlot(2) || player.hasEnabledSlot(5) || player.hasEnabledSlot("horse");
    },
    async content(event, trigger, player) {
      const { control } = await player.chooseToDisable(true).set("ai", (event2, player2, list) => {
        if (list.includes("equip2")) {
          return "equip2";
        }
        if (list.includes("equip1") && player2.countCards("h", (card) => get.name(card, player2) === "sha" && player2.hasUseTarget(card)) - player2.getCardUsable("sha") > 1) {
          return "equip1";
        }
        if (list.includes("equip5") && player2.countCards("h", (card) => get.type2(card, player2) === "trick" && player2.hasUseTarget(card)) > 1) {
          return "equip5";
        }
      }).forResult();
      switch (control) {
        case "equip1":
          player.addTempSkill("zj_jueyan_limit");
          break;
        case "equip2": {
          await player.draw(3);
          const evt = event.getParent("phase", true);
          if (evt?.player === player) {
            for (const [index, phase] of evt.phaseList.entries()) {
              if (index < evt.num) {
                continue;
              }
              if (phase.startsWith("phaseDiscard")) {
                evt.phaseList[index] = phase.replace("phaseDiscard", `skipDiscard-${event.name}`);
              }
            }
          }
          break;
        }
        case "equip3_4":
          player.addTempSkill("zj_jueyan_distance");
          break;
        case "equip5":
          await player.addTempSkills("rejizhi");
          break;
      }
    },
    ai: {
      order: 13,
      result: {
        player(player) {
          if (player.hasEnabledSlot("equip2")) {
            return 1;
          }
          if (player.hasEnabledSlot("equip1") && player.countCards("h", (card) => get.name(card, player) === "sha" && player.hasValueTarget(card)) - player.getCardUsable("sha") > 1) {
            return 1;
          }
          if (player.hasEnabledSlot("equip5") && player.countCards("h", (card) => get.type2(card, player) === "trick" && player.hasUseTarget(card)) > 1) {
            return 1;
          }
          return -1;
        }
      }
    },
    derivation: "rejizhi",
    subSkill: {
      limit: {
        mod: {
          cardUsable: () => Infinity
        },
        mark: true,
        marktext: "决",
        charlotte: true,
        locked: false,
        intro: {
          name: "决堰 - 武器",
          content: "本回合使用牌无次数限制"
        }
      },
      distance: {
        trigger: {
          player: "useCard"
        },
        forced: true,
        async content(event, trigger, player) {
          trigger.directHit.addArray(game.players);
        },
        mod: {
          targetInRange: () => true
        },
        mark: true,
        marktext: "决",
        charlotte: true,
        locked: false,
        intro: {
          name: "决堰 - 坐骑",
          content: "本回合使用牌无距离限制且不可被响应"
        }
      }
    }
  },
  zj_huairou: {
    audio: "drlt_huairou",
    enable: "phaseUse",
    position: "he",
    filter(event, player) {
      return player.hasCard((card) => get.info("zj_huairou").filterCard(card, player), "he");
    },
    filterCard(card, player) {
      return get.type(card) === "equip" && player.canRecast(card);
    },
    check(card) {
      if (get.position(card) === "e") {
        return 0.5 - get.value(card, get.player());
      }
      if (!get.player().hasEquipableSlot(get.subtype(card))) {
        return 5;
      }
      return 3 - get.value(card);
    },
    async content(event, trigger, player) {
      const next = player.recast(event.cards).set("skill", event.name);
      await next;
      if (game.getGlobalHistory("everything", (evt) => {
        if (evt.name !== "recast") {
          return false;
        }
        return evt.player === player && evt.skill === event.name;
      }).indexOf(next) !== 0) {
        return;
      }
      const cards2 = get.inpileVCardList((info) => info[0] === "trick" && player.hasUseTarget(info[2]));
      if (!cards2?.length) {
        return;
      }
      const result = await player.chooseButton(["怀柔：是否视为使用一张普通锦囊牌？", [cards2, "vcard"]]).set("ai", (button) => get.player().getUseValue(button.link[2])).forResult();
      if (result?.bool) {
        const card = new lib.element.VCard({ name: result.links[0][2], isCard: true });
        if (player.hasUseTarget(card)) {
          await player.chooseUseTarget(card, true);
        }
      }
    },
    discard: false,
    lose: false,
    delay: false,
    ai: {
      order: 10,
      result: {
        player: 1
      }
    }
  },
  //赵统赵广
  zj_yizan: {
    audio: "yizan_respond_shan",
    enable: ["chooseToUse", "chooseToRespond"],
    hiddenCard(player, name) {
      if (get.type(name) !== "basic") {
        return false;
      }
      return player.countCards("esh") > 1;
    },
    filter(event, player) {
      if (player.countCards("hes") < 2) {
        return false;
      }
      for (const name of lib.inpile) {
        if (get.type(name) !== "basic") {
          continue;
        }
        if (event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
          return true;
        }
        if (name === "sha") {
          for (const nature of lib.inpile_nature) {
            if (event.filterCard(get.autoViewAs({ name, nature }, "unsure"), player, event)) {
              return true;
            }
          }
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          const card = get.autoViewAs({ name: info[2], nature: info[3] }, "unsure");
          return event.filterCard(card, player, event);
        });
        return ui.create.dialog("翊赞", [list, "vcard"], "hidden");
      },
      check(button) {
        const player = _status.event.player;
        const card = { name: button.link[2], nature: button.link[3] };
        if (_status.event.getParent().type !== "phase" || game.hasPlayer((current) => player.canUse(card, current) && get.effect(current, card, player, player) > 0)) {
          switch (button.link[2]) {
            case "tao":
            case "shan":
              return 5;
            case "jiu": {
              if (player.countCards("hes") > 2) {
                return 3;
              }
              return 0;
            }
            case "sha":
              if (button.link[3] === "fire") {
                return 2.95;
              } else if (button.link[3] === "thunder" || button.link[3] === "ice") {
                return 2.92;
              }
              return 2.9;
          }
        }
        return 0;
      },
      backup(links, player) {
        return {
          audio: "zj_yizan",
          filterCard: true,
          selectCard: 2,
          check(card) {
            return 6 - get.value(card);
          },
          viewAs: { name: links[0][2], nature: links[0][3] },
          async precontent(event) {
            event.result.skill = "zj_yizan";
          },
          position: "hes",
          popname: true
        };
      },
      prompt(links, player) {
        return `将两张牌当做${get.translation(links[0][3] || "")}${get.translation(links[0][2])}使用或打出`;
      }
    },
    ai: {
      order() {
        const player = _status.event.player;
        const event = _status.event;
        if (event.filterCard({ name: "jiu" }, player, event) && get.effect(player, { name: "jiu" }) > 0 && player.countCards("hes") > 2) {
          return 3.3;
        }
        return 3.1;
      },
      skillTagFilter(player, tag, arg) {
        if (tag === "fireAttack") {
          return true;
        }
        if (player.countCards("hes") < 2) {
          return false;
        }
      },
      result: {
        player: 1
      },
      respondSha: true,
      respondShan: true,
      fireAttack: true
    }
  },
  zj_shuge: {
    trigger: {
      global: "phaseEnd"
    },
    filter(event, player) {
      if (!player.hasHistory("useSkill", (evt) => evt.skill === "zj_yizan")) {
        return false;
      }
      return game.hasPlayer((current) => current.group === "shu");
    },
    async cost(event, trigger, player) {
      const num = player.getHistory("useSkill", (evt) => evt.skill === "zj_yizan").length;
      event.result = await player.chooseTarget(get.prompt2(event.skill), [1, num], (card, player2, target) => target.group === "shu").set("ai", (target) => {
        const player2 = get.player();
        return get.effect(target, { name: "draw" }, player2, player2);
      }).forResult();
    },
    async content(event, trigger, player) {
      await game.asyncDraw(event.targets);
    }
  },
  zj_zhengui: {
    trigger: {
      global: "phaseEnd"
    },
    filter(event, player) {
      const targets = get.info("zj_zhengui").logTarget(event, player);
      return targets?.length;
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => {
        if (!current.hasHistory("sourceDamage", (evt) => evt.player?.group === "shu")) {
          return false;
        }
        const card = new lib.element.VCard({ name: "juedou", isCard: true });
        return player.canUse(card, current);
      }).sortBySeat();
    },
    check(event, player) {
      const card = new lib.element.VCard({ name: "juedou", isCard: true });
      const targets = get.info("zj_zhengui").logTarget(event, player);
      return targets.reduce((eff, target) => eff + get.effect(target, card, player, player), 0) > 0;
    },
    async content(event, trigger, player) {
      for (const target of event.targets) {
        const card = new lib.element.VCard({ name: "juedou", isCard: true });
        if (player.canUse(card, target)) {
          await player.useCard(card, target);
        }
      }
    }
  },
  //诸葛瞻
  zj_zhongwang: {
    trigger: {
      player: ["phaseZhunbeiBegin", "phaseEnd"]
    },
    locked: true,
    filter(event, player) {
      return event.name !== "phase" || event?.zhongwangMap?.has(player);
    },
    async cost(event, trigger, player) {
      if (trigger.name === "phase") {
        const targets = trigger.zhongwangMap?.get(player)?.filter((target) => target?.isIn());
        let num = 0;
        if (player.isMinHandcard()) {
          num++;
        }
        if (player.hasHistory("sourceDamage")) {
          num++;
        }
        if (!player.hasHistory("lose", (evt) => evt.type === "discard")) {
          num++;
        }
        event.result = {
          bool: true,
          cost_data: num >= 2,
          targets: [...targets]
        };
        if (num < 2) {
          event.result.targets.add(player);
        }
        if (!event.result.targets?.length) {
          event.result.bool = false;
        }
        event.result.targets.sortBySeat();
      } else {
        event.result = {
          bool: true,
          targets: game.filterPlayer((current) => current !== player).sortBySeat()
        };
      }
    },
    async content(event, trigger, player) {
      if (trigger.name === "phase") {
        let doing;
        if (event.cost_data) {
          player.popup("不负众望", "fire");
          doing = (target) => target.draw(2);
        } else {
          player.popup("有负重托", "wood");
          doing = (target) => target.loseHp();
        }
        await game.doAsyncInOrder(event.targets, doing);
        return;
      }
      const list = [];
      for (const target of event.targets) {
        const result = await target.chooseCard(`###众望：是否相信${get.translation(player)}？###将至少一张牌置于牌堆顶`, [1, Infinity]).set("allowChooseAll", true).set("ai", (card) => {
          const { player: player2, att } = get.event();
          if (att <= 0 || ui.selected.cards?.length) {
            return 0;
          }
          if (get.tag(card, "damage") && card.name !== "sha") {
            return 10 - get.value(card);
          }
          return player2.getUseValue(card);
        }).set("att", get.attitude(target, player)).forResult();
        if (result.bool) {
          list.add(target);
          const cards2 = result.cards;
          target.$throw(cards2, 1e3);
          game.log(target, "将", cards2, "置于了牌堆顶");
          await target.lose(cards2, ui.cardPile, "insert", "visible");
        }
      }
      const map = trigger.getParent().zhongwangMap || /* @__PURE__ */ new Map();
      map.set(player, list);
      trigger.getParent().zhongwangMap = map;
      await game.delayx(4);
      await player.draw(5);
    }
  },
  zj_fuyin: {
    trigger: {
      target: "useCardToTarget"
    },
    filter(event, player) {
      return event.card.name === "sha";
    },
    forced: true,
    async content(event, trigger, player) {
      const history = game.getGlobalHistory("useCard", (evt) => evt.card?.name === "sha" && evt.targets?.includes(player));
      if (history.indexOf(trigger.getParent()) === 0) {
        trigger.getParent().excluded.add(player);
      } else {
        player.addTempSkill("zj_fuyin_ranji");
      }
    },
    mod: {
      maxHandcard(player, num) {
        return num + game.countPlayer((current) => current.group === "shu");
      }
    },
    subSkill: {
      ranji: {
        charlotte: true,
        mark: true,
        intro: { content: "不能回复体力" },
        trigger: { player: "recoverBefore" },
        forced: true,
        firstDo: true,
        async content(event, trigger, player) {
          trigger.cancel();
        },
        ai: {
          effect: {
            target(card, player, target) {
              if (get.tag(card, "recover")) {
                return "zeroplayertarget";
              }
            }
          }
        }
      }
    }
  },
  //柳隐
  zj_guguo: {
    trigger: {
      target: "useCardToTargeted"
    },
    filter(event, player) {
      if (event.player === player) {
        return false;
      }
      return event.card?.name === "sha" || get.type2(event.card) === "trick";
    },
    frequent: true,
    async content(event, trigger, player) {
      await player.draw();
      player.when({
        global: "useCardAfter"
      }).filter((evt) => evt === trigger.getParent()).step(async (event2, trigger2, player2) => {
        if (game.hasPlayer2((current) => current.hasHistory("sourceDamage", (evt) => evt.card === trigger2.card), true)) {
          return;
        }
        if (!trigger2.player.countDiscardableCards(player2, "he")) {
          return;
        }
        await player2.discardPlayerCard(trigger2.player, "he", `###固国###是否弃置${get.translation(trigger2.player)}一张牌？`);
      });
    },
    ai: {
      effect: {
        target(card, player, target) {
          if (player === target) {
            return;
          }
          const bool = get.attitude(target, player) <= 0 && !get.tag(card, "damage");
          return [1, 0.3, 1, bool ? -0.5 : 0];
        }
      }
    }
  },
  //霍弋
  zj_zhongjue: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player) {
      if (!game.hasPlayer((current) => current !== player)) {
        return false;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async cost(event, trigger, player) {
      const targets = game.filterPlayer((current) => current !== player);
      event.result = targets.length > 1 ? await player.chooseTarget("###忠绝###令一名其他角色使用牌无次数限制并获得其武将牌上的主公技", true, lib.filter.notMe).set("ai", (target) => get.attitude(get.player(), target) + 0.1).forResult() : {
        bool: true,
        targets
      };
    },
    onremove(player, skill) {
      player.getStorage(skill).forEach((target) => {
        player.removeTip(`${skill}_${target.playerid}`);
      });
      player.setStorage(skill, []);
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      target.addSkill("zj_zhongjue_effect");
      target.markSkillCharacter("zj_zhongjue_effect", player, "忠绝", "使用牌无次数限制");
      player.markAuto(event.name, target);
      player.addTip(`${event.name}_${target.playerid}`, `忠绝 ${get.translation(target)}`);
      const skills2 = target.getStockSkills(true, true).filter((skill) => {
        if (target.hasSkill(skill, null, null, false)) {
          return false;
        }
        const info = get.info(skill);
        return info && info.zhuSkill;
      });
      if (skills2.length) {
        await target.addSkills(skills2);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        mod: {
          cardUsable: () => Infinity
        }
      }
    }
  },
  zj_qingming: {
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player) {
      const history = game.getAllGlobalHistory("useCard");
      if (!history?.length) {
        return false;
      }
      return player.getStorage("zj_zhongjue")?.some((target) => target.isIn() && target.countCards("h"));
    },
    async cost(event, trigger, player) {
      const targets = player.getStorage("zj_zhongjue").filter((target) => target?.isIn() && target.countCards("h"));
      const card = game.getAllGlobalHistory("useCard").slice(-1)[0].card;
      const prompt = `与一名“忠绝”角色进行议事，且你的议事牌改为${get.translation(card)}`;
      if (targets.length > 1) {
        event.result = await player.chooseTarget(get.prompt(event.skill), prompt, (card2, player2, target) => get.event().targets.includes(target)).set("targets", targets).set("ai", (target) => get.attitude(get.player(), target) * target.countCards("h")).forResult();
      } else {
        event.result = await player.chooseBool(get.prompt(event.skill, targets), prompt).forResult();
        event.result.targets = targets;
      }
      event.result.cost_data = card;
    },
    async content(event, trigger, player) {
      const { targets, cost_data: card } = event;
      const next = player.chooseToDebate([player, ...targets]).set("callback", async (event2, trigger2, player2) => {
        const result = event2.debateResult;
        if (result.opinions?.filter((color) => result[color]?.length)?.length === 1) {
          await player2.draw(2);
          await player2.addSkills("zj_liefa");
          const evt = event2.getParent("phase", true);
          if (evt?.player === player2) {
            for (const [index, phase] of evt.phaseList.entries()) {
              if (index < evt.num) {
                continue;
              }
              if (phase.startsWith("phaseDiscard")) {
                evt.phaseList[index] = phase.replace("phaseDiscard", "skipDiscard-zj_qingming");
              }
            }
          }
        }
      }).set("card", card).set("ai", (card2) => {
        const evt = get.event().getParent(2);
        if (evt.card && get.color(card2) === get.color(evt.card)) {
          return Math.random() + get.sgnAttitude(get.player(), evt.player);
        }
        return Math.random();
      });
      next.fixedResult ??= [];
      next.fixedResult.add([player, card]);
      await next;
    },
    derivation: "zj_liefa"
  },
  zj_liefa: {
    enable: "chooseToUse",
    filter(event, player) {
      if (event.type === "wuxie") {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        const card = get.autoViewAs({ name: info[2], nature: info[3], storage: { liefa: true } }, "unsure");
        return event.filterCard(card, player, event);
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const vcards = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          const card = get.autoViewAs({ name: info[2], nature: info[3], storage: { liefa: true } }, "unsure");
          return event.filterCard(card, player, event);
        });
        return ui.create.dialog("烈伐", [vcards, "vcard"]);
      },
      check(button) {
        if (get.event().getParent().type !== "phase") {
          return 1;
        }
        return get.player().getUseValue({ name: button.link[2], nature: button.link[3], storage: { liefa: true } });
      },
      backup(links, player) {
        return {
          audio: "zj_liefa",
          popname: true,
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            storage: {
              liefa: true
            },
            isCard: true
          },
          filterCard: () => false,
          selectCard: -1,
          async precontent(event, trigger, player2) {
            player2.when({
              player: "useCardAfter"
            }).filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player3) => {
              const list = ["选项一"];
              const choiceList = ["失去1点体力", "弃置两张牌", "失去【烈伐】"];
              if (player3.countDiscardableCards(player3, "he") > 1) {
                list.push("选项二");
              } else {
                choiceList[1] = `<span style="opacity:0.5">${choiceList[1]}</span>`;
              }
              if (player3.hasSkill("zj_liefa", null, null, false)) {
                list.push("选项三");
              } else {
                choiceList[2] = `<span style="opacity:0.5">${choiceList[2]}</span>`;
              }
              const result = list.length > 1 ? await player3.chooseControl(list).set("choiceList", choiceList).set("ai", () => 1).forResult() : {
                control: list[0]
              };
              switch (result.control) {
                case "选项一": {
                  await player3.loseHp();
                  break;
                }
                case "选项二": {
                  await player3.chooseToDiscard("he", 2, true);
                  break;
                }
                case "选项三": {
                  await player3.removeSkills("zj_liefa");
                  break;
                }
              }
            });
          }
        };
      },
      prompt(links, player) {
        return `视为使用一张${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}`;
      }
    },
    hiddenCard(player, name) {
      return get.type(name) === "basic";
    },
    ai: {
      order: 6,
      save: true,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    locked: false,
    mod: {
      playerEnabled(card, player, target) {
        if (card?.storage?.liefa && player === target) {
          return false;
        }
      }
    },
    subSkill: {
      backup: {}
    }
  },
  //姚柯回
  zj_qiangdu: {
    trigger: {
      global: "phaseUseBegin"
    },
    filter(event, player) {
      return event.player !== player;
    },
    check(event, player) {
      return get.attitude(player, event.player) > 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = trigger.player;
      await player.draw();
      if (!player.countCards("he")) {
        return;
      }
      await player.chooseToGive(target, "he", true);
      player.when({
        global: ["useCardAfter", "phaseAfter"]
      }).filter((evt, player2) => {
        if (evt.name === "phase") {
          return true;
        }
        if (evt.targets?.length !== 1 || evt.player !== target) {
          return false;
        }
        return evt.card?.name === "sha" || get.type(evt.card) === "trick";
      }).step(async (event2, trigger2, player2) => {
        if (trigger2.name === "phase") {
          return;
        }
        const card = new lib.element.VCard({ name: trigger2.card.name, nature: trigger2.card.nature, isCard: true });
        if (player2.hasUseTarget(card, false)) {
          trigger2.targets.forEach((target2) => {
            target2.prompt("羌督目标", "fire");
          });
          const result = await player2.chooseUseTarget(card, "nodistance", false).set("ai", (target2) => {
            if (get.event().name === "chooseBool") {
              const { targets2, card: card3, player: player4, preTargets: preTargets2 } = target2;
              let eff2 = 0;
              targets2.forEach((target22) => {
                eff2 += get.effect(target22, card3, player4, player4);
              });
              if (!targets2.containsSome(...preTargets2)) {
                let losehp = get.effect(player4, { name: "losehp" }, player4, player4);
                if (player4.hp <= 1 && losehp < 0) {
                  losehp *= 2;
                }
                eff2 += losehp;
              }
              return eff2 > 0;
            }
            const { player: player3, _get_card: card2 } = get.event();
            const { preTargets } = get.event().getParent();
            let eff = get.effect(target2, card2, player3, player3);
            if (!preTargets?.includes(target2) && !ui.selected.targets?.containsSome(...preTargets)) {
              eff += get.effect(player3, { name: "losehp" }, player3, player3);
            }
            return eff;
          }).set("preTargets", trigger2.targets).forResult();
          if (result?.bool && !result.targets?.containsAll(...trigger2.targets)) {
            await player2.loseHp();
          }
        }
      });
    }
  },
  //夏侯含
  zj_zhuhui: {
    round: 1,
    trigger: {
      global: "phaseBegin"
    },
    filter(event, player) {
      return event.player.hasSex("male");
    },
    logTarget: "player",
    check(event, player) {
      return get.attitude(player, event.player) <= 0;
    },
    async content(event, trigger, player) {
      const choiceList = [
        ["new_qingjian", `${get.translation(player)}本轮获得【清俭】，然后你交给其至少一张手牌`],
        ["jijiu", `${get.translation(player)}本轮获得【急救】，然后你选择受到至少1点伤害`]
      ];
      const target = trigger.player;
      const result = await target.chooseButton(["烛晦：选择一项", [choiceList, "textbutton"]], true).set("ai", (button) => {
        const { player: player2, source } = get.event();
        const name = button.link;
        if (name === "new_qingjian") {
          if (!player2.countCards("h")) {
            return 10;
          }
          return get.effect(player2, { name: "shunshou_copy2" }, source, player2);
        }
        return get.damageEffect(player2);
      }).set("source", player).forResult();
      if (!result?.bool) {
        return;
      }
      await player.addTempSkills(result.links, "roundStart");
      if (result.links[0] === "jijiu") {
        const result2 = await target.chooseNumbers("烛晦", [{ prompt: "请选择你要受到的伤害值", min: 1, max: target.getHp() }], true).set("processAI", () => [1]).forResult();
        if (result2?.numbers?.length) {
          await target.damage(result2.numbers[0], target);
        }
      } else if (target.countGainableCards(player, "h")) {
        await target.chooseToGive(`###烛晦###交给${get.translation(player)}至少一张牌`, player, "h", [1, Infinity], true).set("allowChooseAll", true).set("ai", (card) => {
          if (ui.selected.cards?.length) {
            return 0;
          }
          return 5 - get.value(card);
        });
      }
    },
    derivation: ["new_qingjian", "jijiu"]
  },
  zj_hanci: {
    trigger: {
      global: "changeSkillsAfter"
    },
    getIndex(event, player) {
      if (event.addSkill?.length) {
        return event.addSkill;
      }
      return [];
    },
    filter(event, player) {
      return event.player?.isIn();
    },
    logTarget: "player",
    forced: true,
    async content(event, trigger, player) {
      await player.draw();
      await trigger.player.draw();
    }
  },
  zj_jieyi: {
    trigger: {
      global: "roundStart"
    },
    filter(event, player) {
      return game.hasPlayer((current) => current.hasSex("male") && current !== player && current.countCards("he"));
    },
    intro: {
      content: "结依角色：$"
    },
    onremove: true,
    async cost(event, trigger, player) {
      player.setStorage(event.skill, [], true);
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => target.hasSex("male") && target !== player2 && target.countCards("he")).set("ai", (target) => get.attitude(get.player(), target) + 0.1).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.chooseToGive(`###结依###交给${get.translation(player)}至少一张牌`, true, [1, Infinity], player, "he").set("ai", (card) => {
        if (ui.selected.cards?.length) {
          return 0;
        }
        return 6 - get.value(card);
      }).set("allowChooseAll", true);
      player.markAuto(event.name, target);
      const num = target.getHistory("lose", (evt) => evt.getParent(3) === event).reduce((sum, evt) => {
        const length = evt?.cards2?.length || 0;
        return sum + length;
      }, 0);
      if (target.countCards("h") > num) {
        const result = await player.chooseBool("是否失去1点体力并令你本轮发动【理内】次数+1？").set("choice", false).forResult();
        if (result.bool) {
          await player.loseHp();
          player.addTempSkill("zj_linei_count", "roundStart");
          player.addMark("zj_linei_count", 1, false);
        }
      }
    }
  },
  zj_linei: {
    trigger: {
      global: ["gainAfter", "loseAfter", "loseAsyncAfter"]
    },
    filter(event, player, name, target) {
      if (!player.getStorage("zj_jieyi").includes(target)) {
        return false;
      }
      const num = player.getRoundHistory("useSkill", (evt) => evt.skill === "zj_linei").length;
      if (num > player.countMark("zj_linei_count")) {
        return false;
      }
      return target.countCards("h") > target.hp;
    },
    getIndex(event, player) {
      return game.filterPlayer((current) => event.getg?.(current)?.length);
    },
    check(event, player, name, target) {
      return get.recoverEffect(target, player, player) > 4;
    },
    logTarget: (_1, _2, _3, target) => target,
    async content(event, trigger, player) {
      const target = event.indexedData;
      const num = Math.min(target.countGainableCards(player, "he"), Math.max(3, target.countCards("h") - target.hp));
      await player.gainPlayerCard(target, num, true, "he", "allowChooseAll");
      await target.recover();
    },
    subSkill: {
      count: {
        charlotte: true,
        onremove: true
      }
    }
  },
  zj_tongxin: {
    trigger: {
      global: "phaseDrawEnd"
    },
    getIndex(event, player) {
      if (event.player === player) {
        return player.getStorage("zj_jieyi");
      }
      return [event.player];
    },
    filter(event, player, name, target) {
      if (event.player === player) {
        if (!target?.isIn()) {
          return false;
        }
      } else {
        if (!player.getStorage("zj_jieyi").includes(target)) {
          return false;
        }
      }
      return event.player.hasHistory("gain", (evt) => {
        if (evt.getParent(event.name) !== event) {
          return false;
        }
        return evt.getParent()?.name === "draw" && evt.cards?.length;
      });
    },
    logTarget: (_1, _2, _3, target) => target,
    forced: true,
    async content(event, trigger, player) {
      const num = trigger.player.getHistory("gain", (evt) => {
        if (evt.getParent(trigger.name) !== trigger) {
          return false;
        }
        return evt.getParent()?.name === "draw" && evt.cards?.length;
      }).reduce((sum, evt) => sum + evt.cards.length, 0);
      if (num <= 0) {
        return;
      }
      const target = trigger.player === player ? event.indexedData : player;
      await target.draw(num);
    }
  },
  //忠会
  zj_quanwei: {
    trigger: {
      player: "phaseZhunbeiBegin"
    },
    filter(event, player) {
      return player.countCards("h") && game.hasPlayer((current) => current !== player && current.countCards("h"));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        prompt: get.prompt2(event.skill),
        filterCard: true,
        position: "h",
        filterTarget(card, player2, target) {
          return target !== player2 && target.countCards("h");
        },
        ai1(card) {
          return Math.random();
        },
        ai2(target) {
          return 10 + get.attitude(get.player(), target);
        }
      }).forResult();
    },
    multitarget: true,
    multiline: true,
    lose: false,
    discard: false,
    async content(event, trigger, player) {
      const {
        cards: [card],
        targets
      } = event;
      await player.showCards(card, `${get.translation(player)}发动了【权威】`);
      await player.chooseToDebate([player, ...targets]).set("callback", async (event2, trigger2, player2) => {
        const result = event2.debateResult;
        if (result.bool && result.opinion === event2.getParent().color) {
          const evt = event2.getParent("phase", true);
          if (!evt) {
            return;
          }
          const list = evt.phaseList.map((name, index) => [index + 1, "", name.split("|")[0]]).filter((info) => {
            if (info[2].startsWith("skip")) {
              return false;
            }
            return info[0] - 1 > evt.num;
          });
          let num = 0;
          while (list?.length) {
            const result2 = await player2.chooseButton([
              `###权威###是否跳过一个阶段？（已跳过${num}个阶段）`,
              [
                list,
                (item, type, position, noclick, node) => {
                  const showCard = [item[0], item[1], `lusu_${item[2]}`];
                  node = ui.create.buttonPresets.vcard(showCard, type, position, noclick);
                  node.node.info.innerHTML = `<span style = "color:#ffffff">${item[0]}</span>`;
                  node.node.info.style["font-size"] = "20px";
                  node._link = node.link = item;
                  node._customintro = (uiintro) => {
                    uiintro.add(get.translation(node._link[2]));
                    uiintro.addText(`此阶段为本回合第${get.cnNumber(node._link[0], true)}个阶段`);
                    return uiintro;
                  };
                  return node;
                }
              ]
            ]).set("ai", (button) => {
              if (["phaseDiscard", "phaseJudge"].includes(button.link[2])) {
                return 1;
              }
              if (button.link[2] === "phaseJieshu") {
                if (player2.hasSkill("zj_quanqing")) {
                  return 0;
                }
                return 1;
              }
              return 0;
            }).set("forceAuto", true).forResult();
            if (result2?.bool) {
              const [index, _1, name] = result2.links[0];
              const phase = evt.phaseList[index - 1].replace(name, `skip${name.slice(5)}-zj_quanwei`);
              evt.phaseList[index - 1] = phase;
              list.remove(result2.links[0]);
              num++;
            } else {
              break;
            }
          }
          const targets2 = game.filterPlayer((current) => current.isDamaged());
          if (num <= 0 || targets2.length <= 0) {
            return;
          }
          const result3 = targets2.length > 1 ? await player2.chooseTarget(`###权威###令至多两名角色恢复${get.cnNumber(num)}点体力`, true, [1, 2], (card2, player3, target) => target.isDamaged()).set("ai", (target) => get.recoverEffect(target, get.player(), get.player())).forResult() : {
            bool: true,
            targets: targets2
          };
          if (result3?.bool) {
            player2.line(result3.targets);
            for (const target of result3.targets.sortBySeat()) {
              await target.recover(num);
            }
          }
        } else {
          const color = result.opinions.find((color2) => result[color2].some((info) => info[0] === player2));
          if (!color || !result?.[color]?.length) {
            return;
          }
          const targets2 = result.targets.removeArray(result[color].map((info) => info[0]));
          if (!targets2?.length) {
            return;
          }
          const result2 = await player2.chooseBool(`###权威###是否减少1点体力上限并获得${get.translation(targets2)}的所有手牌`).set(
            "choice",
            player2.maxHp > 1 && targets2.reduce((sum, target) => {
              if (get.attitude(player2, target) > 0) {
                return sum - target.countGainableCards(player2, "h");
              }
              return sum + target.countGainableCards(player2, "h");
            }, 0) > 0
          ).forResult();
          if (result2?.bool) {
            await player2.loseMaxHp();
            const cards2 = [];
            targets2.forEach((target) => {
              if (target.countGainableCards(player2, "h")) {
                cards2.addArray(target.getGainableCards(player2, "h"));
              }
            });
            if (cards2?.length) {
              await player2.gain(cards2, "giveAuto");
            }
            await game.delayx();
          }
        }
      }).set("color", get.color(card)).set("ai", (card2) => {
        const evt = get.event().getParent(2);
        if (get.color(card2) === evt?.color) {
          return 1 + Math.random();
        }
        return Math.random();
      });
    }
  },
  zj_quanshu: {
    trigger: {
      player: "damageEnd",
      global: "chooseToDebateAfter"
    },
    filter(event, player) {
      return event.name === "damage" || event.targets.includes(player);
    },
    frequent(event, player) {
      return player.countCards("ej");
    },
    async content(event, trigger, player) {
      const num = player.countCards("ej");
      if (num > 0) {
        await player.draw(num);
      }
      if (player.countCards("h") && !player.isDisabledJudge()) {
        const result = await player.chooseCard("###权术###蓄谋一张牌", true).set("ai", (card) => {
          const player2 = get.player();
          if (player2.hasValueTarget(card)) {
            return player2.getUseValue(card);
          }
          return 0.1;
        }).forResult();
        if (result?.bool && result?.cards?.length) {
          await player.addJudge({ name: "xumou_jsrg" }, result.cards);
        }
      }
    },
    locked: false,
    mod: {
      maxHandcard(player, num) {
        return num + player.countCards("ej");
      }
    }
  },
  zj_quanqing: {
    trigger: {
      player: "phaseJieshuBegin"
    },
    limited: true,
    skillAnimation: true,
    animationColor: "thunder",
    filter(event, player) {
      return player.countCards("h") && get.info("zj_quanqing")?.logTarget(event, player)?.length;
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => player.inRange(current) && current.countCards("h"));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard(get.prompt2(event.skill, get.info(event.skill).logTarget(trigger, player)), "h").set("ai", (card) => 7 - get.value(card)).forResult();
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const { cards: cards2, targets } = event;
      const result = {
        cards: cards2,
        targets,
        skill: "zj_quanwei"
      };
      await player.useResult(result, event);
      const groups = lib.group.slice(0);
      const maxGroup = groups.slice().sort((a, b) => game.countPlayer((current) => current.group === b && get.attitude(player, current) <= 0) - game.countPlayer((current) => current.group === a && get.attitude(player, current) <= 0))[0];
      const result2 = await player.chooseControl(groups.concat(["cancel2"])).set("prompt", "权倾：是否变更势力并获得【伐异】？").set("ai", () => get.event().choice).set("choice", maxGroup).forResult();
      if (result2.control !== "cancel2") {
        const group = result2.control;
        await player.changeGroup(group);
        player.popup(`${group}2`, get.groupnature(group, "raw"));
        await player.addSkills("zj_qiangyi");
      }
    },
    derivation: "zj_qiangyi"
  },
  zj_qiangyi: {
    zhuSkill: true,
    enable: "chooseToUse",
    viewAsFilter(player) {
      return player.countCards("hes", "ying") > 0;
    },
    filterCard(card) {
      return get.name(card) === "ying";
    },
    position: "hes",
    viewAs: {
      name: "yushijiesui",
      storage: {
        zjfayi: true
      }
    },
    prompt: "将一张【影】当【玉石皆碎】对与你势力相同的角色使用",
    check(card) {
      return 5 - get.value(card);
    },
    locked: false,
    mod: {
      playerEnabled(card, player, target) {
        if (!card?.storage?.zjfayi) {
          return;
        }
        if (target.group !== player.group) {
          return false;
        }
      }
    },
    subfrequent: ["gain"],
    group: "zj_qiangyi_gain",
    subSkill: {
      gain: {
        zhuSkill: true,
        trigger: {
          player: "addJudgeAfter"
        },
        filter(event, player) {
          return event?.card?.name === "xumou_jsrg";
        },
        frequent: true,
        async content(event, trigger, player) {
          await player.gain(lib.card.ying.getYing(1), "gain2");
        }
      }
    }
  },
  //姜维
  zj_juta: {
    trigger: {
      target: "useCardToTarget",
      player: "useCardAfter"
    },
    filter(event, player, name) {
      if (name === "useCardAfter") {
        return event.card?.name === "sha";
      }
      return event.player !== player;
    },
    async cost(event, trigger, player) {
      event.result = {
        bool: true
      };
      if (event.triggername !== "useCardAfter") {
        event.result.targets = [trigger.player];
      }
    },
    locked: true,
    async content(event, trigger, player) {
      if (event.triggername === "useCardAfter") {
        await player.changeSkills(["zj_buji"], ["zj_juta"]);
      } else {
        const target = trigger.player;
        const num = get.distance(player, target);
        const result = target.countDiscardableCards(target, "he") >= num ? await target.chooseToDiscard(`###据沓###弃置${get.cnNumber(num)}张牌或令${get.translation(trigger.card)}对${get.translation(player)}无效`, num).set("ai", (card) => {
          const { player: player2, source, numx } = get.event();
          const trigger2 = get.event().getTrigger();
          if (get.effect(source, trigger2.card, player2, player2) < numx) {
            return 0;
          }
          return 5 - get.value(card);
        }).set("numx", num).set("source", player).forResult() : {
          bool: false
        };
        if (!result?.bool) {
          trigger.getParent().excluded.add(player);
        }
      }
    },
    mod: {
      globalTo(from, to, current) {
        return current + 1;
      }
    },
    ai: {
      effect: {
        target(card, player, target, current) {
          const distance = get.distance(target, player);
          const cards2 = player.getCards("he");
          cards2.remove(card);
          if (card.cards) {
            cards2.removeArray(card.cards);
          } else {
            cards2.removeArray(ui.selected.cards);
          }
          if (cards2?.length < distance) {
            return "zerotarget";
          }
          return [1, 0, 1, distance * -0.4];
        }
      }
    },
    derivation: "zj_buji"
  },
  zj_buji: {
    trigger: {
      player: "gainAfter",
      global: ["loseAfter", "loseAsyncAfter"]
    },
    filter(event, player) {
      const canUse = (card) => {
        if (player.hasUseTarget(card)) {
          return true;
        }
        return get.info(card).notarget && lib.filter.cardEnabled(card, player);
      };
      if (event.type === "discard") {
        return event.getl?.(player)?.cards2?.some((card) => canUse(card));
      }
      return event.getg(player)?.some((card) => canUse(card));
    },
    async cost(event, trigger, player) {
      const cards2 = [];
      if (trigger.type === "discard") {
        cards2.addArray(trigger.getl(player).cards2);
      } else {
        cards2.addArray(trigger.getg(player));
      }
      const canUse = (card) => {
        if (player.hasUseTarget(card)) {
          return true;
        }
        return get.info(card).notarget && lib.filter.cardEnabled(card, player);
      };
      const cardx = cards2.filter(canUse);
      const result = await player.chooseButton([get.prompt2(event.skill), cards2]).set("filterButton", (button) => get.event().cardx.includes(button.link)).set("cardx", cardx).set("ai", (button) => {
        const player2 = get.player();
        const val = player2.getUseValue(button.link);
        if (player2.hp <= 1 && !get.tag(button.link, "damage")) {
          return 0;
        }
        return val - get.effect(player2, { name: "losehp" }, player2, player2) * 0.5;
      }).forResult();
      if (result.bool) {
        event.result = {
          bool: true,
          cards: result.links
        };
      }
    },
    async content(event, trigger, player) {
      const card = event.cards[0];
      await player.showCards(card, `${get.translation(player)}发动了【不戢】`);
      await player.chooseUseTarget(card, true, false);
      if (!player.hasHistory("sourceDamage", (evt) => evt.cards?.includes(card) && evt.getParent(event.name) === event)) {
        await player.loseHp();
      }
    }
  },
  zj_linze: {
    init(player, skill) {
      player.setStorage("kunfen", true);
      const skills2 = [];
      const hp = player.getHp();
      const dhp = player.getDamagedHp();
      if (hp >= dhp) {
        skills2.add("retiaoxin");
      }
      if (hp <= dhp) {
        skills2.add("kunfen");
      }
      const currentSkills = player?.additionalSkills?.[skill];
      if (!currentSkills?.length || !currentSkills.containsAll(...skills2) || !skills2.containsAll(...currentSkills)) {
        player.addAdditionalSkill(skill, skills2);
      }
    },
    trigger: { player: ["changeHpAfter", "gainMaxHpAfter", "loseMaxHpAfter"] },
    forced: true,
    popup: false,
    async content(event, trigger, player) {
      get.info(event.name).init(player, event.name);
    },
    derivation: ["retiaoxin", "kunfenx"]
  },
  zj_fuji: {
    trigger: {
      player: "phaseJieshuBegin"
    },
    init(player, skill) {
      player.addSkill("zj_fuji_effect");
    },
    filter(event, player) {
      const bool = game.hasPlayer((current) => current.getSeatNum() > 0);
      const first = game.findPlayer((current) => bool ? current.getSeatNum() === 1 : current === _status.roundStart);
      if (!first) {
        return false;
      }
      return first.hasAllHistory("lose", (evt) => {
        if (evt.type !== "discard" || !evt?.cards2?.length) {
          return false;
        }
        return evt.getParent("phase", true)?.player === first;
      });
    },
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    check(event, player) {
      return game.hasPlayer((current) => {
        if (get.attitude(player, current) >= 0) {
          return false;
        }
        const num = player.countCards("hs", (card) => get.tag(card, "damage") && player.canUse(card, current)) + 1;
        return current.getHp() <= num;
      });
    },
    async content(event, trigger, player) {
      player.awakenSkill("zj_fuji");
      player.insertPhase();
    },
    subSkill: {
      effect: {
        trigger: {
          source: "dieAfter"
        },
        filter(event, player) {
          const phase = event.getParent("phase", true);
          return phase?.skill === "zj_fuji" && phase.player === player;
        },
        charlotte: true,
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          await player.draw(3);
          await player.recoverTo(player.maxHp);
          player.restoreSkill("zj_fuji");
          game.log(player, "重置了", "【扶稷】");
        }
      }
    }
  },
  zj_jindao: {
    audio: "dcjincui",
    trigger: {
      player: "phaseDrawBegin1"
    },
    forced: true,
    filter(event, player) {
      return !event.numFixed;
    },
    async content(event, trigger, player) {
      trigger.changeToZero();
      if (player.countCards("h") < 7) {
        await player.drawTo(7);
      }
      if (player.countCards("h", (card) => _status.connectMode || get.number(card) === 7)) {
        const result = await player.chooseCard([1, Infinity], "展示任意张点数为7的手牌并回复等量体力", (card) => get.number(card) === 7, "allowChooseAll").set("ai", (card) => 1).forResult();
        if (result.bool) {
          const cards2 = result.cards;
          await player.showCards(cards2, `${get.translation(player)}发动了【尽道】`);
          await player.recover(cards2.length);
        }
      }
      await player.chooseToGuanxing(7);
    }
  },
  zj_hanshi: {
    audio: "dcqingshi",
    trigger: {
      player: "useCard"
    },
    filter(event, player) {
      if (!player.isPhaseUsing()) {
        return false;
      }
      if (player.getStorage("zj_hanshi_used").includes(event.card.name)) {
        return false;
      }
      return player.countCards("he", (card) => get.name(card) === event.card.name);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard(get.prompt2(event.skill), (card) => get.name(card) === get.event().cardName).set("cardName", trigger.card.name).set("ai", () => 1).forResult();
    },
    async content(event, trigger, player) {
      await player.showCards(event.cards, `${get.translation(player)}发动了【汉势】`);
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        const stat = player.getStat("card");
        const name = trigger.card.name;
        if (typeof stat[name] === "number") {
          stat[name]--;
        }
      }
      player.addSkill("zj_hanshi_damage");
      player.markAuto("zj_hanshi_damage", trigger.card);
      player.addTempSkill("zj_hanshi_used", "phaseChange");
      player.markAuto("zj_hanshi_used", trigger.card.name);
    },
    subSkill: {
      damage: {
        charlotte: true,
        onremove: true,
        firstDo: true,
        trigger: {
          source: "damageBegin1",
          player: "useCardAfter"
        },
        filter(event, player) {
          return event.card && player.getStorage("zj_hanshi_damage").includes(event.card) && event.notLink();
        },
        async cost(event, trigger, player) {
          if (trigger.name === "damage") {
            trigger.num++;
          } else {
            player.unmarkAuto("zj_hanshi_damage", trigger.card);
          }
        }
      },
      used: {
        charlotte: true,
        onremove: true
      }
    }
  },
  zj_wuzhe: {
    audio: "dczhizhe",
    enable: "phaseUse",
    limited: true,
    filter(event) {
      return get.inpileVCardList((info) => ["trick", "delay"].includes(info[0])).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => ["trick", "delay"].includes(info[0]));
        return ui.create.dialog("武哲", [list, "vcard"], "hidden");
      },
      select: [1, 2],
      check(button) {
        if (button.link[2] === "wuxie") {
          return 114;
        }
        return get.player().getUseValue({ name: button.link[2] });
      },
      backup(links, player) {
        return {
          audio: "zj_wuzhe",
          names: links.map((info) => info[2]),
          manualConfirm: true,
          skillAnimation: true,
          animationColor: "fire",
          async content(event, trigger, player2) {
            player2.awakenSkill("zj_wuzhe");
            const { names } = get.info(event.name);
            player2.markAuto("zj_wuzhe_effect", names);
            player2.addSkill("zj_wuzhe_effect");
            game.addGlobalSkill("zj_wuzhe_ai");
          }
        };
      },
      prompt(links, player) {
        return `###是否发动【武哲】？###其他角色使用${links.map((info) => `【${get.translation(info[2])}】`).join("或")}时，你可令此牌无效，然后于此牌置入弃牌堆后获得之`;
      }
    },
    ai: {
      order: 9,
      result: {
        player: 1
      }
    },
    subSkill: {
      backup: {},
      effect: {
        audio: "zj_wuzhe",
        trigger: {
          global: "useCard"
        },
        init(player, skill) {
          const list = player.getStorage(skill);
          if (list.length) {
            player.addTip(skill, list.map((name) => `武哲 ${get.translation(name)}`).join("\n"));
          }
        },
        onremove(player, skill) {
          delete player.storage[skill];
          player.removeTip(skill);
        },
        filter(event, player) {
          if (event.player === player) {
            return false;
          }
          return player.getStorage("zj_wuzhe_effect").includes(event.card.name);
        },
        prompt2(event, player) {
          return `令${get.translation(event.card)}无效，然后于此牌进入弃牌堆后获得之`;
        },
        check(event, player) {
          if (event.targets?.length) {
            return event.targets.reduce((sum, current) => sum + get.effect(current, event.card, event.player, player), 0) <= 0;
          }
          return get.attitude(player, event.player) <= 0;
        },
        charlotte: true,
        logTarget: "player",
        async content(event, trigger, player) {
          trigger.targets.length = 0;
          trigger.all_excluded = true;
          player.when({
            global: "cardsDiscardAfter"
          }).filter((evt) => {
            if (!evt.cards.someInD("od")) {
              return false;
            }
            const evtx = evt.getParent();
            if (evtx.name !== "orderingDiscard") {
              return false;
            }
            const evtxx = evtx.relatedEvent || evtx.getParent();
            return evtxx.getParent() === (trigger.relatedEvent || trigger.getParent());
          }).step(async (event2, trigger2, player2) => {
            await player2.gain(trigger2.cards.filterInD("od"), "gain2");
          });
        }
      },
      ai: {
        effect: {
          player_use(card, player) {
            if (typeof card === "object" && game.hasPlayer((target) => target.getStorage("zj_wuzhe_effect").includes(card.name) && get.attitude(target, player) < 0)) {
              return "zeroplayertarget";
            }
          }
        }
      }
    }
  },
  zj_jianxi: {
    audio: 2,
    trigger: { player: "damageEnd" },
    frequent: true,
    initSkill(name) {
      game.broadcastAll(() => {
        _status.jianxiSkill ??= {};
      });
      if (!_status.characterlist) {
        game.initCharacterList();
      }
      if (!(name in _status.jianxiSkill)) {
        const characters2 = _status.characterlist.slice(0);
        const skills2 = [];
        characters2.forEach((character) => {
          const skillsx = get.character(character, 3);
          skillsx.forEach((skill) => {
            const info = get.info(skill);
            if (!info || info.charlotte) {
              return false;
            }
            if (info.derivation) {
              let list = info.derivation;
              if (!Array.isArray(list)) {
                list = [list];
              }
              list.forEach((deriSkill) => {
                const infox = get.info(deriSkill);
                if (!infox || infox.charlotte) {
                  return false;
                }
                if (!get.skillInfoTranslation(deriSkill).includes(`【${get.translation(name)}】`)) {
                  return false;
                }
                skills2.add([deriSkill, character]);
              });
            }
            if (!get.skillInfoTranslation(skill).includes(`【${get.translation(name)}】`)) {
              return false;
            }
            skills2.add([skill, character]);
          });
        });
        game.broadcastAll((skills3) => _status.jianxiSkill[name] = skills3, skills2);
      }
      return _status.jianxiSkill[name];
    },
    async content(event, trigger, player) {
      let result = (await player.draw().forResult()).cards;
      if (get.itemtype(result) !== "cards") {
        return;
      }
      await player.showCards(result, `${get.translation(player)}发动了【兼习】`);
      const name = get.name(result[0]);
      result = await player.chooseButton([
        [
          (dialog) => {
            dialog.add("兼习", "forcebutton");
            dialog.addText(`声明一个含有【${get.translation(get.event().cardName)}】的技能，或点击“取消”令你使用基本牌的数值+1`);
            const caption = ui.create.div(".searcher.caption");
            const input = document.createElement("input").css({
              textAlign: "center",
              border: "solid 2px #294510",
              borderRadius: "6px",
              fontWeight: "bold",
              fontSize: "21px"
            });
            input.type = "text";
            input.placeholder = "请输入技能id/技能名";
            input.spellcheck = false;
            const find = ui.create.button(["find", "确定"], "tdnodes");
            find.style.display = "inline";
            const updateFind = () => {
              const value = input.value.trim();
              caption.link = value;
              ui.selected.buttons.add(caption);
              ui.create.confirm();
              ui.click.ok();
            };
            find.addEventListener("click", updateFind);
            input.onkeydown = (e) => {
              e.stopPropagation();
              if (e.key === "Enter") {
                updateFind();
              }
            };
            input.onmousedown = (e) => e.stopPropagation();
            caption.append(input, find);
            dialog.content.appendChild(caption);
          },
          "handle"
        ]
      ]).set("cardName", name).set("processAI", () => {
        const skills2 = get.info("zj_jianxi").initSkill(get.event().cardName).filter((skill2) => !game.hasPlayer((current) => current.hasSkill(skill2[0]))).sort((a, b) => {
          const value = (list) => {
            const [skill2, name2] = list;
            let value2 = get.skillRank(skill2, "inout") * get.rank(name2, true);
            if (["relonghun", "dunshi", "olfuhun", "mbjuejin", "dcjiushi"].includes(skill2)) {
              value2 *= 24;
            }
            const info = get.info(skill2);
            if (info?.ai?.neg) {
              value2 = 0;
            }
            if (info?.ai?.combo) {
              let skills3 = info.ai.combo;
              if (!Array.isArray(skills3)) {
                skills3 = [skills3];
              }
              if (!skills3.every((skill3) => player.hasSkill(skill3, null, null, false))) {
                value2 = 0;
              }
            }
            return value2;
          };
          return value(b) - value(a);
        });
        const choice = skills2?.length ? skills2[0][0] : ["wusheng", "jiang", "hunzi"].randomGet();
        return { bool: true, links: [choice] };
      }).set("switchToAuto", () => {
        _status.event.result = "ai";
        _status.event.dialog?.close();
        ui.confirm?.close();
      }).forResult();
      if (!result?.links?.length) {
        player.addSkill("zj_jianxi_effect");
        player.addMark("zj_jianxi_effect", 1, false);
        return;
      }
      let skill;
      const str = `【${get.translation(name)}】`;
      const [link] = result.links;
      if (link in lib.skill) {
        skill = link;
      } else {
        const skills2 = get.info(event.name).initSkill(name).filter((skill2) => {
          if (game.hasPlayer((current) => current.hasSkill(skill2[0]))) {
            return false;
          }
          return get.translation(skill2[0], "skill") === link;
        });
        if (skills2.length) {
          const result2 = skills2.length === 1 ? { links: skills2 } : await player.chooseButton(
            [
              "选择你要声明的技能",
              [
                skills2,
                (item, type, position, noclick, node) => {
                  node = ui.create.buttonPresets.skill(item, "skill", position, noclick);
                  node._customintro = (uiintro, evt) => {
                    const skill2 = node.link;
                    uiintro.add(get.translation(skill2));
                    if (node.owner) {
                      uiintro.add(`持有者 ${get.slimName(node.owner)}`);
                    }
                  };
                  return node;
                }
              ]
            ],
            true
          ).forResult();
          if (result2?.links?.length) {
            skill = Array.isArray(result2.links[0]) ? result2.links[0][0] : result2.links[0];
          }
        }
      }
      if (skill) {
        game.log(player, "声明了技能", `【${get.translation(skill)}】`);
        player.chat(`我声明技能【${get.translation(skill)}】`);
        if (get.skillInfoTranslation(skill).includes(str) && !game.hasPlayer((current) => current.hasSkill(skill))) {
          const skills2 = get.info(event.name).initSkill(name);
          const list = skills2.find((info) => info[0] === skill);
          if (list[1]) {
            player.flashAvatar(event.name, list[1]);
          }
          await player.addSkills(skill);
          return;
        }
        game.log("可是", `【${get.translation(skill)}】`, "并不符合条件！");
        player.popup("杯具");
        await game.delay();
      }
      player.addMark("zj_jianxi_effect", 1, false);
    },
    ai: {
      maixie: true,
      effect: {
        target(card, player, target) {
          if (get.tag(card, "damage")) {
            if (player.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            if (target.hp > 1 || target.hasSkillTag("save", true, target)) {
              return [1, 0.7];
            }
          }
        }
      }
    },
    //onremove: ["zj_jianxi_effect"],
    //group: "zj_jianxi_effect",
    subSkill: {
      effect: {
        charlotte: true,
        intro: { content: "使用基本牌的数值+#" },
        trigger: { player: ["useCard", "respond"] },
        locked: false,
        forced: true,
        filter(event, player) {
          return get.type(event.card) === "basic" && player.hasMark("zj_jianxi_effect");
        },
        async content(event, trigger, player) {
          trigger.baseDamage += player.countMark(event.name);
        }
      }
    }
  },
  zj_zhaofu: {
    audio: 2,
    trigger: { player: ["useCardAfter", "damageBegin4"] },
    forced: true,
    isMinSkill(player) {
      const countSkill = (current) => current.getSkills(null, false, false).filter((skill) => {
        const info = get.info(skill);
        if (!info || info.charlotte) {
          return false;
        }
        return true;
      }).length;
      const bool = game.hasPlayer((current) => current.getSeatNum() > 0);
      const first = game.findPlayer((current) => bool ? current.getSeatNum() === 1 : current === _status.roundStart);
      return first && countSkill(player) < countSkill(first);
    },
    filter(event, player) {
      if (event.name === "damage") {
        return event.num > 1;
      }
      if (!get.is.damageCard(event.card)) {
        return false;
      }
      return get.info("zj_zhaofu").isMinSkill(player);
    },
    async content(event, trigger, player) {
      if (trigger.name === "damage") {
        trigger.num = 1;
      } else {
        await player.loseMaxHp();
      }
    },
    mod: {
      aiOrder(player, card, num) {
        const bool = get.info("zj_zhaofu").isMinSkill(player);
        if (bool && get.itemtype(card) === "card" && get.tag(card, "damage")) {
          return Math.min(num, 0);
        }
      },
      aiValue(player, card, num) {
        const bool = get.info("zj_zhaofu").isMinSkill(player);
        if (bool && get.itemtype(card) === "card" && get.tag(card, "damage")) {
          return Math.min(num, 0);
        }
      },
      aiUseful(player, card, num) {
        const bool = get.info("zj_zhaofu").isMinSkill(player);
        if (bool && get.itemtype(card) === "card" && get.tag(card, "damage")) {
          return Math.min(num, 0);
        }
      }
    }
  },
  zj_tuicheng: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      if (event.type === "wuxie") {
        return false;
      }
      if (!player.countCards("hes", (card) => get.is.damageCard(card))) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        const card = get.autoViewAs({ name: info[2], nature: info[3], storage: { tuicheng: true } }, "unsure");
        return event.filterCard(card, player, event) && game.countPlayer((current) => lib.filter.targetEnabled2(card, player, current)) > 1;
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const vcards = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          const card = get.autoViewAs({ name: info[2], nature: info[3], storage: { tuicheng: true } }, "unsure");
          return event.filterCard(card, player, event) && game.countPlayer((current) => lib.filter.targetEnabled2(card, player, current)) > 1;
        });
        return ui.create.dialog("推诚", [vcards, "vcard"]);
      },
      check(button) {
        if (get.event().getParent().type !== "phase") {
          return 1;
        }
        return get.player().getUseValue({ name: button.link[2], nature: button.link[3], storage: { tuicheng: true } });
      },
      backup(links, player) {
        return {
          audio: "zj_tuicheng",
          popname: true,
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            storage: {
              tuicheng: true
            }
          },
          filterTarget(card, player2, target) {
            if (!card) {
              card = get.card();
            }
            return lib.filter.targetEnabled2(card, player2, target) && lib.filter.targetInRange(card, player2, target);
          },
          filterCard(card, player2) {
            return get.is.damageCard(card);
          },
          selectCard: 1,
          position: "hes",
          selectTarget: 2,
          async precontent(event, trigger, player2) {
            event.getParent().addCount = false;
            player2.when({
              player: "useCardAfter"
            }).filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player3) => {
              const targets = trigger2.targets.filter((target) => target.isIn());
              const cards2 = trigger2.cards?.filterInD("od");
              if (!targets?.length || !cards2?.length) {
                return;
              }
              const result = targets.length > 1 ? await player3.chooseTarget(`令一名目标角色获得${get.translation(cards2)}`, true, (card, player4, target) => get.event().targets.includes(target)).set("targets", targets).set("ai", (target) => get.attitude(get.player(), target)).forResult() : {
                bool: true,
                targets
              };
              if (result?.bool && result.targets?.length) {
                await result.targets[0].gain(cards2, "gain2");
              }
            });
          }
        };
      },
      prompt(links, player) {
        return `将一张伤害牌当${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}对两名角色使用`;
      }
    },
    ai: {
      order: 6,
      result: {
        player: 1
      }
    },
    locked: false,
    mod: {
      cardUsable(card, player, num) {
        if (card?.storage?.tuicheng) {
          return Infinity;
        }
      }
    }
  }
};
const skills$n = {
  //龙起襄樊
  //龙庞德
  dragtaiguan: {
    audio: "juesi",
    enable: "phaseUse",
    usable(skill, player) {
      return Math.max(1, player.getDamagedHp());
    },
    filterCard: true,
    filterTarget(card, player, target) {
      return player.inRange(target) && target.countDiscardableCards(target, "he");
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = await target.chooseToDiscard("he", true).forResult();
      if (result.cards[0].name !== "sha" && player.getHp() <= target.getHp()) {
        await player.chooseUseTarget("juedou", true, [target]);
      }
    }
  },
  //关羽
  //界界关羽
  dragchaojue: {
    trigger: { player: "phaseZhunbeiBegin" },
    frequent: true,
    /*filter(event, player) {
    	if (!game.hasPlayer(target => target !== player)) return false;
    	return player.countCards("h", card => _status.connectMode || lib.filter.cardDiscardable(card, player));
    },
    async cost(event, trigger, player) {
    	event.result = await player
    		.chooseToDiscard(get.prompt2(event.skill), "h")
    		.set("ai", card => {
    			const player = get.event().player;
    			if (!game.hasPlayer(target => get.attitude(player, target) < 0)) return 0;
    			if (get.suit(card, player) === "diamond") return 8 - get.value(card);
    			return 7.5 - get.value(card);
    		})
    		.set("logSkill", event.skill)
    		.forResult();
    },
    popup: false,*/
    async content(event, trigger, player) {
      await player.draw();
      if (!player.countCards("h")) {
        return;
      }
      const result = await player.chooseCard("超绝：展示一张手牌", "h", true).set("ai", (card) => {
        const player2 = get.event().player;
        if (get.suit(card, player2) === "diamond") {
          return 3;
        }
        return 1;
      }).forResult();
      if (!result.bool) {
        return;
      }
      await player.showCards(result.cards, `${get.translation(player)}发动了【超绝】`);
      const targets = game.filterPlayer((target) => target !== player).sortBySeat();
      if (targets.length) {
        const suits = result.cards.reduce((list, card) => list.add(get.suit(card, player)), []).sort((a, b) => lib.suit.indexOf(b) - lib.suit.indexOf(a));
        player.line(targets);
        for (const i of targets) {
          i.addTempSkill("dragchaojue_buff");
          i.markAuto("dragchaojue_buff", suits);
        }
        for (const target of targets) {
          const { bool } = await target.chooseToGive(player, (card, player2) => get.event().suits.includes(get.suit(card)), "h", "give").set("suits", suits).set("ai", (card) => {
            const player2 = get.event().player;
            const target2 = get.event().getParent().player;
            const att = get.attitude(player2, target2);
            if (att > 0) {
              return 7.5 - get.value(card);
            }
            if (att > -1) {
              return 0;
            }
            if (att < 0 && get.attitude(target2, player2) < 0 && player2.getSkills(null, false, false).some((skill) => {
              if (get.is.locked(skill, player2)) {
                return false;
              }
              const info = get.info(skill);
              return info && info.ai && (info.ai.maixie || info.ai.maixie_hp || info.ai.maixie_defend);
            }) && player2.getHp() <= 2) {
              return 7.5 - get.value(card);
            }
            return 0;
          }).set("prompt", `超绝：交给${get.translation(player)}一张${get.translation(suits)}手牌，或本回合非锁定技失效`).forResult();
          if (!bool) {
            target.addTempSkill("fengyin");
          }
        }
      }
    },
    subSkill: {
      buff: {
        onremove: true,
        charlotte: true,
        mod: {
          cardEnabled2(card, player) {
            if (player.getStorage("dragchaojue_buff").includes(get.suit(card))) {
              return false;
            }
          }
        },
        marktext: "绝",
        intro: { content: "本回合内不能使用或打出$牌" }
      }
    }
  },
  dragjunshen: {
    mod: {
      targetInRange(card, player) {
        if (get.suit(card) === "diamond" && card.name === "sha") {
          return true;
        }
      }
    },
    locked: false,
    enable: ["chooseToUse", "chooseToRespond"],
    filterCard(card, player) {
      return get.color(card) === "red";
    },
    viewAsFilter(player) {
      return player.countCards("hes", { color: "red" });
    },
    position: "hes",
    viewAs: { name: "sha" },
    prompt: "将一张红色牌当作【杀】使用或打出",
    check(card) {
      const val = get.value(card);
      if (_status.event.name === "chooseToRespond") {
        return 1 / Math.max(0.1, val);
      }
      return 5 - val;
    },
    ai: {
      order(item, player) {
        if (!player || !_status.event.type || _status.event.type !== "phase") {
          return 0.1;
        }
        return get.order({ name: "sha" }, player) + 0.3;
      },
      respondSha: true,
      skillTagFilter(player) {
        if (!player.countCards("hes", { color: "red" })) {
          return false;
        }
      }
    },
    group: ["dragjunshen_add", "dragjunshen_damage"],
    subSkill: {
      add: {
        trigger: { player: "useCard2" },
        filter(event, player) {
          if (event.card.name !== "sha" || get.suit(event.card) !== "heart") {
            return false;
          }
          return game.hasPlayer((target) => target !== player && !event.targets.includes(target) && lib.filter.targetEnabled2(event.card, player, target) && lib.filter.targetInRange(event.card, player, target));
        },
        async cost(event, trigger, player) {
          event.result = await player.chooseTarget(get.prompt(event.skill), `为${get.translation(trigger.card)}额外指定一个目标`, (card, player2, target) => {
            const evt = get.event().getTrigger();
            return target !== player2 && !evt.targets.includes(target) && lib.filter.targetEnabled2(evt.card, player2, target) && lib.filter.targetInRange(evt.card, player2, target);
          }).set("ai", (target) => get.effect(target, _status.event.getTrigger().card, _status.event.player)).forResult();
        },
        async content(event, trigger, player) {
          trigger.targets.addArray(event.targets);
        }
      },
      damage: {
        trigger: { source: "damageBegin1" },
        filter(event, player) {
          const evt = event.getParent(2);
          return evt.name === "useCard" && evt.skill === "dragjunshen";
        },
        logTarget: "player",
        prompt2(event, player) {
          const prompt = "弃置其一张手牌，或令此伤害+1";
          if (!event.player.countDiscardableCards(event.player, "e")) {
            return prompt;
          }
          return `令${get.translation(event.player)}弃置装备区所有牌，然后你选择等量次：${prompt}`;
        },
        async content(event, trigger, player) {
          const target = trigger.player;
          await target.discard(target.getDiscardableCards(target, "e"));
          const num = Math.max(
            1,
            target.getHistory("lose", (evt) => evt.type === "discard" && evt.getParent(2) === event).reduce((sum, evt) => sum += evt.getl(target)?.cards2?.length, 0)
          );
          let count = 0;
          while (count < num) {
            count++;
            let result;
            if (!target.countDiscardableCards(player, "h")) {
              result = { index: 1 };
            } else {
              result = await player.chooseControl().set("choiceList", [`弃置${get.translation(target)}一张手牌`, "令此伤害+1"]).set("ai", () => {
                const player2 = get.event().player;
                const trigger2 = get.event().getTrigger();
                const eff1 = get.effect(trigger2.player, { name: "guohe_copy2" }, player2, player2);
                const eff2 = get.damageEffect(trigger2.player, player2, player2);
                if (eff1 > eff2) {
                  return 0;
                }
                return 1;
              }).forResult();
            }
            if (result.index === 0) {
              await player.discardPlayerCard(target, "h", true);
            } else {
              trigger.num++;
            }
          }
        }
      }
    }
  },
  //龙曹仁
  draglizhong: {
    trigger: { player: "phaseJieshuBegin" },
    async cost(event, trigger, player) {
      const choiceList = ["将任意张装备牌至于任意名角色的装备区", "令你或任意名装备区里有牌的角色摸一张牌"];
      const choices = ["置入装备", "团体摸牌", "cancel2"];
      if (!player.countCards("he", (card) => {
        if (get.type(card) !== "equip") {
          return false;
        }
        return game.hasPlayer((target) => target.canEquip(card));
      })) {
        choices.shift();
        choiceList[0] = `<span style="opacity:0.5">${choiceList[0]}</span>`;
      }
      const { control } = await player.chooseControl(choices).set("prompt", `###${get.prompt(event.skill)}###选择首先执行的一项`).set("choiceList", choiceList).set("ai", () => get.event().controls[0]).forResult();
      event.result = { bool: control !== "cancel2", cost_data: control };
    },
    async content(event, trigger, player) {
      let choices = ["置入装备", "团体摸牌"];
      let used = false;
      if (event.cost_data === "团体摸牌") {
        choices.reverse();
      }
      choices.push(event.cost_data);
      for (let i = 1; i <= 3; i++) {
        if (i === 3 && used) {
          break;
        }
        switch (choices[i - 1]) {
          case "置入装备": {
            while (player.hasCard((card) => {
              if (get.type(card) !== "equip") {
                return false;
              }
              return game.hasPlayer((target) => target.canEquip(card));
            }, "he")) {
              const { bool, cards: cards2, targets } = await player.chooseCardTarget({
                prompt: "厉战：将一张装备牌置于一名角色的装备区",
                filterCard(card) {
                  return get.type(card) === "equip";
                },
                position: "he",
                filterTarget(card, player2, target) {
                  return target.canEquip(card);
                },
                ai1(card) {
                  return 6 - get.value(card);
                },
                ai2(target) {
                  const player2 = get.event().player;
                  const att = get.attitude(player2, target);
                  if (att <= 0 || target.countCards("e")) {
                    return 0;
                  }
                  return att * (target === player2 ? 1 : 3);
                }
              }).forResult();
              if (bool) {
                if (i === 1 && !used) {
                  used = true;
                }
                const card = cards2[0];
                const target = targets[0];
                player.line(target);
                if (target !== player) {
                  player.$give(card, target, false);
                }
                await game.delay(0.5);
                await target.equip(card);
              } else {
                break;
              }
            }
            break;
          }
          case "团体摸牌": {
            const result = await player.chooseTarget(
              "厉战：令你或任意名装备区有牌的角色摸一张牌",
              (card, player2, target) => {
                if (target !== player2 && !target.countCards("e")) {
                  return false;
                }
                if (ui.selected.targets.length) {
                  const choose = ui.selected.targets[0];
                  if (choose === player2 && !player2.countCards("e")) {
                    return false;
                  }
                }
                return true;
              },
              [1, Infinity]
            ).set("multitarget", true).set("complexTarget", true).set("ai", (target) => {
              const player2 = get.event().player;
              if (!player2.countCards("e")) {
                if (game.countPlayer((choose) => choose.countCards("e") && get.attitude(player2, choose) > 0) > 1 && target === player2) {
                  return 0;
                }
              }
              return get.attitude(player2, target);
            }).forResult();
            if (result.bool) {
              if (i === 1 && !used) {
                used = true;
              }
              const targets = result.targets.sortBySeat();
              player.line(targets);
              choices.addArray(targets);
              for (const target of targets) {
                await target.draw("nodelay");
              }
              await game.delayx();
            }
            break;
          }
        }
      }
      choices = choices.slice(3);
      if (choices.length) {
        choices.sortBySeat();
        player.line(choices);
        for (const target of choices) {
          target.addTempSkill("draglizhong_effect", "roundStart");
        }
        await game.delayx();
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        mod: {
          maxHandcard(player, num) {
            return num + 2;
          }
        },
        enable: "chooseToUse",
        filterCard: true,
        position: "e",
        viewAs: { name: "wuxie" },
        filter(event, player) {
          return player.countCards("e") > 0;
        },
        viewAsFilter(player) {
          return player.countCards("e") > 0;
        },
        prompt: "将一张装备区的牌当作【无懈可击】使用",
        check(card) {
          return 8 - get.equipValue(card);
        },
        mark: true,
        marktext: "守",
        intro: { content: "手牌上限+2，可将装备区的牌当作【无懈可击】使用" }
      }
    }
  },
  //撅碎（难视
  dragjuesui: {
    trigger: { global: "dying" },
    filter(event, player) {
      return !player.getStorage("dragjuesui").includes(event.player) && event.player.hasEnabledSlot();
    },
    check(event, player) {
      const target = event.player;
      if (get.attitude(player, target) <= 0) {
        return false;
      }
      return player.countCards("hs", (card) => player.canSaveCard(card, target)) + target.countCards("hs", (card) => target.canSaveCard(card, target)) < 1 - target.hp;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = trigger.player;
      player.markAuto("dragjuesui", [target]);
      const { bool } = await target.chooseBool("是否将体力值回复至1点并废除装备栏？").forResult();
      if (bool) {
        await target.recoverTo(1);
        const disables = [];
        for (let i = 1; i <= 5; i++) {
          for (let j = 0; j < target.countEnabledSlot(i); j++) {
            disables.push(i);
          }
        }
        if (disables.length) {
          await target.disableEquip(disables);
        }
        target.addSkill("dragjuesui_wusheng");
      } else {
        target.chat("拒绝！");
      }
    },
    init(player) {
      if (player.getStorage("dragjuesui").length) {
        player.markSkill("dragjuesui");
      }
    },
    intro: { content: "已对$发动过此技能" },
    subSkill: {
      wusheng: {
        charlotte: true,
        mark: true,
        marktext: "碎",
        intro: { content: "殊死一搏！可将黑色非基本牌当作无次数限制的【杀】使用" },
        mod: {
          cardUsable(card, player, num) {
            if (card.storage && card.storage.dragjuesui) {
              return Infinity;
            }
          }
        },
        enable: ["chooseToUse", "chooseToRespond"],
        filterCard(card, player) {
          return get.color(card) === "black" && get.type(card) !== "basic";
        },
        position: "hse",
        viewAs: { name: "sha", storage: { dragjuesui: true } },
        viewAsFilter(player) {
          if (!player.countCards("hes", (card) => get.color(card) === "black" && get.type(card) !== "basic")) {
            return false;
          }
        },
        prompt: "将一张黑色非基本牌当作无次数限制的【杀】使用或打出",
        check(card) {
          return 7 - get.value(card);
        },
        ai: {
          order(item, player) {
            if (!player || !_status.event.type || _status.event.type !== "phase") {
              return 0.1;
            }
            return get.order({ name: "sha" }, player) * 0.99;
          },
          respondSha: true,
          skillTagFilter(player) {
            if (!player.countCards("hes", (card) => get.color(card) === "black" && get.type(card) !== "basic")) {
              return false;
            }
          }
        }
      }
    }
  },
  //吕常×SP淳于琼√
  dragjuwu: {
    trigger: { target: "shaBefore" },
    filter(event, player) {
      return !game.hasNature(event.card) && game.countPlayer((target) => event.player.inRange(target)) >= 3;
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.cancel();
    },
    ai: {
      effect: {
        target_use(card, player, target) {
          if (card.name === "sha" && !game.hasNature(card) && game.countPlayer((targetx) => player.inRange(targetx)) >= 3) {
            return "zerotarget";
          }
        }
      }
    }
  },
  dragshouxiang: {
    trigger: { player: "phaseDrawBegin2" },
    filter(event, player) {
      if (!game.hasPlayer((target) => target.inRange(player))) {
        return false;
      }
      return !event.numFixed;
    },
    frequent: true,
    async content(event, trigger, player) {
      const num = Math.min(
        3,
        game.countPlayer((target) => target.inRange(player))
      );
      trigger.num += num;
      const result = await player.chooseTarget(`守襄：选择至多${num}名角色`, true, [1, num]).set("ai", (target) => {
        const player2 = get.player();
        return get.attitude(player2, target);
      }).forResult();
      if (result.bool) {
        player.line(result.targets, "green");
        for (const target of result.targets.sortBySeat(_status.currentPhase)) {
          player.when({
            player: "phaseDrawEnd"
          }).filter((evt) => evt === trigger).step(async (event2, trigger2, player2) => {
            const bool1 = player2.countCards("h") && player2 !== target;
            const bool2 = player2.countCards("hes", (card) => {
              if (get.name(card, player2) !== "sha") {
                return false;
              }
              const tao = get.autoViewAs({ name: "tao" }, [card]);
              if (!lib.filter.cardEnabled(tao, player2, "forceEnable")) {
                return false;
              }
              return lib.filter.targetEnabled2(tao, player2, target);
            });
            let result2 = bool1 ? bool2 ? 2 : 0 : bool2 ? 1 : -1;
            if (result2 === -1) {
              return;
            }
            if (result2 === 2) {
              const result22 = await player2.chooseControl().set("choiceList", [`交给${get.translation(target)}一张牌`, `将一张【杀】当作【桃】对${get.translation(target)}使用`]).set("eff", get.effect(target, { name: "tao" }, player2, player2)).set("ai", () => {
                if (get.event().eff > 0) {
                  return 1;
                }
                return 0;
              }).forResult();
              result2 = result22.index;
            }
            if (result2 === 1) {
              const result22 = await player2.chooseCard({
                prompt: `将一张【杀】当作【桃】对${get.translation(target)}使用`,
                position: "hes",
                filterCard(card) {
                  if (get.name(card, get.player()) !== "sha") {
                    return false;
                  }
                  const tao = get.autoViewAs({ name: "tao" }, [card]);
                  const target2 = get.event().target;
                  if (!lib.filter.cardEnabled(tao, player2, "forceEnable")) {
                    return false;
                  }
                  return lib.filter.targetEnabled2(tao, player2, target2);
                },
                forced: true,
                ai(card) {
                  return 7 - get.value(card);
                },
                target
              }).forResult();
              if (result22.bool) {
                await player2.useCard(get.autoViewAs({ name: "tao" }, result22.cards), result22.cards, [target]);
              }
            } else {
              await player2.chooseToGive("h", true, target);
            }
          });
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        trigger: { player: "phaseDiscardBegin" },
        filter(event, player) {
          return game.hasPlayer((target) => target.inRange(player));
        },
        forced: true,
        async content(event, trigger, player) {
          const num = Math.min(
            5,
            game.countPlayer((target) => target.inRange(player))
          );
          if (num) {
            if (_status.connectMode) {
              game.broadcastAll(() => _status.noclearcountdown = true);
            }
            const list = [];
            while (num - list.length > 0 && player.hasCard((card) => !list.some((list2) => list2[1] === card), "h") && game.hasPlayer((target) => target !== player && !list.some((list2) => list2[0] === target))) {
              const { bool, targets, cards: cards2 } = await player.chooseCardTarget({
                prompt: "守襄：你可以交给任意名角色各一张手牌",
                prompt2: `（还可分配${num - list.length}张）`,
                position: "h",
                animate: false,
                filterCard(card, player2) {
                  return !get.event().list.some((list2) => list2[1] === card);
                },
                filterTarget(card, player2, target) {
                  return target !== player2 && !get.event().list.some((list2) => list2[0] === target);
                },
                ai1(card) {
                  if (card.name === "shan") {
                    return 1;
                  }
                  return Math.random();
                },
                ai2(target) {
                  return get.attitude(get.event().player, target);
                }
              }).set("list", list).forResult();
              if (bool) {
                list.push([targets[0], cards2[0]]);
                player.addGaintag(cards2, "olsujian_given");
              } else {
                break;
              }
            }
            if (_status.connectMode) {
              game.broadcastAll(() => {
                delete _status.noclearcountdown;
                game.stopCountChoose();
              });
            }
            if (list.length) {
              await game.loseAsync({
                gain_list: list,
                player,
                cards: list.slice().flatMap((list2) => list2[1]),
                giver: player,
                animate: "giveAuto"
              }).setContent("gaincardMultiple");
            }
          }
        }
      }
    }
  }
};
const skills$m = {
  //蛇吕蒙
  shefujing: {
    trigger: {
      global: "roundStart"
    },
    filter(event, player, name) {
      return game.roundNumber === 1;
    },
    forced: true,
    async content(event, trigger, player) {
      await player.turnOver();
      await player.draw(3);
      const result = await player.chooseTarget("令一名其他角色获得“伏”", (card, player2, target) => !target.getStorage("shefujing_mark").length && target !== player2, true).forResult();
      if (result.bool) {
        const target = result.targets[0];
        player.line(target, "green");
        target.addSkill("shefujing_mark");
        target.markAuto("shefujing_mark", player);
      }
    },
    derivation: "shefujing_fu",
    subSkill: {
      mark: {
        charlotte: true,
        onremove: true,
        trigger: {
          player: "damageEnd",
          source: "damageSource"
        },
        filter(event, player, name) {
          if (!player.getStorage("shefujing_mark").some((p) => p.isIn())) {
            return false;
          }
          const key = name === "damageEnd" ? "damage" : "sourceDamage";
          return player.getHistory(key, () => true, event).indexOf(event) === 0;
        },
        mark: true,
        marktext: "伏",
        intro: {
          markcount: () => null,
          content(storage, player) {
            const targets = player.getStorage("shefujing_mark").filter((i) => i.isIn());
            if (!targets.length) {
              return "无效果";
            }
            return `你每回合首次受到/造成伤害后，${get.translation(targets[0])}可以选择一项：
						1.摸一张牌；2.获得1点护甲；3.${get.poptip("rule_beishui")}，失去1点体力并对你发动一次〖攻心〗`;
          }
        },
        async cost(event, trigger, player) {
          const target = player.getStorage("shefujing_mark").find((p) => p.isIn());
          const result = await target.chooseControl("选项一", "选项二", "背水！", "cancel2").set("prompt", get.prompt(event.skill, player, target)).set("choiceList", ["摸一张牌", "获得1点护甲", `背水：失去1点体力并对${get.translation(player)}发动一次【攻心】`]).set("ai", () => {
            const player2 = get.player();
            if (player2.hujia > 4) {
              return "选项一";
            }
            if (player2.hp > 1) {
              return "背水！";
            }
            return "选项二";
          }).forResult();
          event.result = {
            bool: result.control !== "cancel2",
            cost_data: result.index,
            skill_popup: false,
            targets: [target]
          };
        },
        async content(event, trigger, player) {
          const target = event.targets[0];
          const index = event.cost_data;
          target.logSkill("shefujing", player);
          if (index !== 1) {
            await target.draw();
          }
          if (index !== 0) {
            await target.changeHujia();
          }
          if (index === 2) {
            await target.loseHp();
            await target.useSkill("gongxin", [player]);
          }
        }
      },
      fu: {}
    }
  },
  shefujiang: {
    trigger: {
      global: "damageEnd"
    },
    usable: 1,
    filter(event, player) {
      return event.player.isIn();
    },
    check(event, player) {
      return get.attitude(player, event.player) < 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      for (const target of [trigger.player.getPrevious(), trigger.player.getNext()]) {
        if (!target.isIn() || !trigger.player.isIn()) {
          continue;
        }
        await target.chooseToUse(
          `浮江：是否对${get.translation(trigger.player)}使用一张【杀】？`,
          function(card, player2, event2) {
            if (get.name(card) !== "sha") {
              return false;
            }
            return lib.filter.filterCard.apply(this, arguments);
          },
          trigger.player,
          -1
        ).set("addCount", false);
      }
    }
  },
  shetonglu: {
    skillAnimation: true,
    animationColor: "wood",
    juexingji: true,
    trigger: {
      global: "roundEnd"
    },
    forced: true,
    filter(event, player) {
      return player.countCards("h") > player.hp;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.loseMaxHp();
      await player.addSkills(["gongxin", "rebotu", "sbduojing"]);
      const target = game.findPlayer((current) => current.getStorage("shefujing_mark").includes(player));
      if (!target) {
        return;
      }
      const result = await player.chooseBool(`是否令${get.translation(target)}移动至你的上家或下家`).forResult();
      if (!result.bool) {
        return;
      }
      const result2 = await target.chooseControl("上家", "下家").set("prompt", `选择移动至${get.translation(player)}的上家或下家`).set("ai", () => ["上家", "下家"].randomGet()).forResult();
      const point = result2.index === 1 ? player.getNext() : player;
      if (target === point.getPrevious()) {
        return;
      }
      game.broadcastAll(
        (target1, target2) => {
          game.swapSeat(target1, target2, null, true);
        },
        target,
        point
      );
      player.insertPhase();
    },
    derivation: ["gongxin", "rebotu", "sbduojing"]
  },
  //龙大宝
  dragpojun: {
    audio: "repojun",
    trigger: {
      player: "useCardToPlayered"
    },
    filter(event, player) {
      return event.card.name === "sha" && event.target.hp > 0 && event.target.countCards("he") > 0;
    },
    preHidden: true,
    async cost(event, trigger, player) {
      const num = Math.min(trigger.target.hp, trigger.target.countCards("he"));
      event.result = await player.choosePlayerCard(trigger.target, "he", [1, num], get.prompt(event.skill, trigger.target), "allowChooseAll").set("ai", (button) => {
        if (!_status.event.goon) {
          return 0;
        }
        const val = get.value(button.link);
        if (button.link === _status.event.target.getEquip(2)) {
          return 2 * (val + 3);
        }
        return val;
      }).set("goon", get.attitude(player, trigger.target) <= 0).set("forceAuto", true).setHiddenSkill(event.skill).forResult();
      event.result.targets = [trigger.target];
    },
    async content(event, trigger, player) {
      const target = trigger.target;
      await player.viewCards(`${get.translation(target)}的“破军”牌`, event.cards);
      target.addSkill("repojun2");
      const next = target.addToExpansion("giveAuto", event.cards, target);
      next.gaintag.add("repojun2");
      await next;
      if (event.cards.some((card) => get.type2(card) === "equip")) {
        const result = await player.chooseButton(["选择一张牌置入弃牌堆", event.cards.filter((card) => get.type(card) === "equip")], true).set("ai", (button) => get.value(button.link, _status.event.getTrigger().target)).forResult();
        if (result.bool) {
          await target.loseToDiscardpile(result.links);
        }
      }
      if (event.cards.some((card) => get.type2(card) === "trick")) {
        await player.draw();
      }
    },
    ai: {
      unequip_ai: true,
      directHit_ai: true,
      skillTagFilter(player, tag, arg) {
        if (get.attitude(player, arg.target) > 0) {
          return false;
        }
        if (tag === "directHit_ai") {
          return arg.target.hp >= Math.max(1, arg.target.countCards("h") - 1);
        }
        if (arg && arg.name === "sha" && arg.target.getEquip(2)) {
          return true;
        }
        return false;
      }
    },
    group: "repojun3"
  },
  //龙陆逊
  dragqianxun: {
    trigger: { player: "phaseJudgeBegin" },
    filter(event, player) {
      return player.countCards("h") > 1;
    },
    enable: "phaseUse",
    selectCard: 2,
    filterCard: true,
    prompt: "弃置两张手牌并摸一张牌",
    check(card) {
      return 4 - get.value(card);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard("h", 2, get.prompt(event.skill), "弃置两张手牌并弃置判定区里的一张牌").set("ai", (card) => {
        if (_status.event.goon) {
          return 6 - get.value(card);
        }
        return 0;
      }).set(
        "goon",
        (() => {
          if (player.hasSkillTag("rejudge") && player.countCards("j") < 2) {
            return false;
          }
          return player.hasCard((card) => {
            if (get.tag(card, "damage") && get.damageEffect(player, player, get.player(), get.natureList(card)) >= 0) {
              return false;
            }
            const cardx = { name: card.viewAs || card.name, cards: [card] };
            return get.effect(player, cardx, player, player) < 0;
          }, "j");
        })()
      ).set("chooseonly", true).forResult();
    },
    async content(event, trigger, player) {
      if (trigger?.name === "phaseJudge") {
        await player.discard(event.cards);
        await player.discardPlayerCard(player, "j", true);
      } else {
        await player.draw();
      }
    },
    ai: {
      order: 9,
      result: {
        player(player, target) {
          if (player.countCards("h") > player.getHandcardLimit()) {
            return 1;
          }
          return 0;
        }
      }
    }
  },
  draglianying: {
    trigger: {
      player: "loseAfter",
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    frequent: true,
    filter(event, player) {
      if (player.countCards("h")) {
        return false;
      }
      const skills2 = player.getSkills(null, false, false).filter((skill) => {
        const info = get.info(skill);
        if (!info || info.charlotte || get.skillInfoTranslation(skill, player).length === 0) {
          return false;
        }
        return true;
      });
      const evtx = event.getParent("useSkill", true);
      if (evtx && skills2.includes(evtx.skill)) {
        return false;
      }
      if (skills2.some((skill) => event.getParent(skill, true, true))) {
        return false;
      }
      const evt = event.getl(player);
      return evt?.player === player && evt?.hs?.length > 0;
    },
    async content(event, trigger, player) {
      await player.draw(2);
      const result = await player.chooseToDiscard([1, 2], "弃置至多两张牌，若弃置两张牌则令一种牌名的牌对你无效", "he", true).forResult();
      if (result?.cards?.length > 1) {
        player.addTempSkill("draglianying_effect", { player: "phaseBegin" });
        const list = get.inpileVCardList((info) => !player.getStorage("draglianying_effect").includes(info[2]) && !info[3]);
        if (!list.length) {
          return;
        }
        const result2 = await player.chooseButton(["令一种牌名对你无效", [list, "vcard"]], true).set("ai", (button) => {
          const player2 = get.player();
          return -get.effect(player2, { name: button.link[2] }, player2, player2);
        }).forResult();
        if (!result2.bool) {
          return;
        }
        player.markAuto("draglianying_effect", result2.links[0][2]);
      }
    },
    ai: {
      threaten: 0.8,
      effect: {
        player_use(card, player, target) {
          if (player.countCards("h") === 1) {
            return [1, 0.8];
          }
        },
        target(card, player, target) {
          if (get.tag(card, "loseCard") && target.countCards("h") === 1) {
            return 0.5;
          }
        }
      },
      noh: true,
      freeSha: true,
      freeShan: true,
      skillTagFilter(player, tag) {
        if (player.countCards("h") !== 1) {
          return false;
        }
      }
    },
    subSkill: {
      effect: {
        trigger: {
          target: "useCardToBefore"
        },
        forced: true,
        onremove: true,
        charlotte: true,
        mark: true,
        intro: {
          content(storage, player) {
            if (!storage?.length) {
              return "无效果";
            }
            return `直到你的下回合开始前，${storage.map((i) => get.translation(i)).join("、")}对你无效`;
          }
        },
        filter(event, player) {
          return player.getStorage("draglianying_effect").includes(event.card.name);
        },
        async content(event, trigger, player) {
          trigger.cancel();
        },
        ai: {
          effect: {
            target(card, player, target) {
              if (target.getStorage("draglianying_effect").includes(card.name)) {
                return "zeroplayertarget";
              }
            }
          }
        }
      }
    }
  },
  //申耽申仪
  draglianxiang: {
    trigger: { target: "useCardToTarget" },
    filter(event, player) {
      return event.player !== player && player.countCards("he");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard("he", get.prompt2(event.skill)).set("ai", (card) => {
        const player2 = get.player();
        const targets = game.filterPlayer((current) => {
          if (get.attitude(player2, current) <= 0) {
            return false;
          }
          const count = (c) => (c === player2 ? -1 : 0) + c.countCards("h");
          return !game.hasPlayer((currentx) => count(currentx) > count(current));
        });
        if (targets.length) {
          return 7 - get.value(card);
        }
        return 0;
      }).set("chooseonly", true).forResult();
    },
    async content(event, trigger, player) {
      await player.discard(event.cards);
      const targets = game.filterPlayer((current) => current.isMaxHandcard());
      if (!targets.length) {
        return;
      }
      let target = targets[0];
      if (targets.length > 1) {
        const result = await player.chooseTarget("令场上手牌数最多的一名角色摸一张牌", true, (card, player2, target2) => target2.isMaxHandcard()).set("ai", (target2) => get.attitude(get.player(), target2)).forResult();
        if (result.bool) {
          target = result.targets[0];
        }
      }
      player.line(target, "green");
      await target.draw();
    }
  },
  dragpingmeng: {
    trigger: {
      player: ["loseAfter", "useCard"],
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    frequent: true,
    filter(event, player) {
      if (player.countCards("h") >= player.maxHp) {
        return false;
      }
      if (event.name === "useCard") {
        return event.card.name === "sha";
      }
      if (player.countCards("h")) {
        return false;
      }
      const evt = event.getl(player);
      return evt?.player === player && evt?.hs?.length > 0;
    },
    async content(event, trigger, player) {
      const list = lib.group.slice().remove(player.group);
      const result = await player.chooseControl(list).set("prompt", "平孟：请选择要变更为的势力").set("ai", () => _status.event.choice).set("choice", list.randomGet()).forResult();
      await player.changeGroup(result.control);
      if (player.countCards("h") < player.maxHp) {
        await player.drawTo(player.maxHp);
      }
      player.addTempSkill("dragpingmeng_effect");
    },
    subSkill: {
      effect: {
        trigger: {
          global: "phaseJieshuBegin"
        },
        direct: true,
        charlotte: true,
        async content(event, trigger, player) {
          const card = get.autoViewAs({ name: "sha", isCard: true });
          if (player.hasUseTarget(card)) {
            await player.chooseUseTarget(card);
          }
        }
      }
    },
    ai: {
      threaten: 0.8,
      effect: {
        player_use(card, player, target) {
          if (player.countCards("h") === 1) {
            return [1, 0.8];
          }
        },
        target(card, player, target) {
          if (get.tag(card, "loseCard") && target.countCards("h") === 1) {
            return 0.5;
          }
        }
      },
      noh: true,
      skillTagFilter(player, tag) {
        if (tag === "noh") {
          if (player.countCards("h") !== 1) {
            return false;
          }
        }
      }
    }
  },
  dragpanfeng: {
    trigger: {
      player: ["loseAfter", "useCard"],
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    frequent: true,
    filter(event, player) {
      if (player.countCards("h") >= player.maxHp) {
        return false;
      }
      if (event.name === "useCard") {
        return event.card.name === "sha";
      }
      if (player.countCards("h")) {
        return false;
      }
      const evt = event.getl(player);
      return evt?.player === player && evt?.hs?.length > 0;
    },
    async content(event, trigger, player) {
      const list = lib.group.slice().remove(player.group);
      let targetGroup = game.findPlayer((current) => {
        if (current.group === player.group) {
          return false;
        }
        return !game.hasPlayer((currentx) => {
          if (currentx.group === player.group) {
            return false;
          }
          return get.damageEffect(currentx, player, player) > get.damageEffect(current, player, player);
        });
      })?.[0]?.group;
      if (!list.includes(targetGroup)) {
        targetGroup = list.randomGet();
      }
      const result = await player.chooseControl(list).set("prompt", "叛封：请选择要变更为的势力").set("ai", () => _status.event.choice).set("choice", targetGroup).forResult();
      await player.changeGroup(result.control);
      if (player.countCards("h") < player.maxHp) {
        await player.drawTo(player.maxHp);
      }
      const targets = game.filterPlayer((cur) => cur.group === player.group);
      if (!targets.length) {
        return;
      }
      if (targets.length === 1) {
        const target = targets[0];
        player.line(target, "green");
        await target.damage(player);
        return;
      }
      const result2 = await player.chooseTarget("对一名与你势力相同的角色造成1点伤害", true, (card, player2, target) => target.group === player2.group).set("ai", (target) => get.damageEffect(target, get.player(), get.player())).forResult();
      if (result2.bool) {
        const target = result2.targets[0];
        player.line(target, "green");
        await target.damage(player);
      }
    },
    ai: {
      threaten: 0.8,
      effect: {
        player_use(card, player, target) {
          if (player.countCards("h") === 1) {
            return [1, 0.8];
          }
        },
        target(card, player, target) {
          if (get.tag(card, "loseCard") && target.countCards("h") === 1) {
            return 0.5;
          }
        }
      },
      noh: true,
      skillTagFilter(player, tag) {
        if (tag === "noh") {
          if (player.countCards("h") !== 1) {
            return false;
          }
        }
      }
    }
  },
  //龙廖化
  dragzhawang: {
    trigger: { player: "phaseBegin" },
    async cost(event, trigger, player) {
      const result = await player.chooseControl("选项一", "选项二", "背水！", "cancel2").set("prompt", get.prompt(event.skill)).set("choiceList", ["本回合获得〖诈降〗", "失去1点体力，将本回合的准备阶段和结束阶段改为摸牌阶段", "背水！减少1点体力上限"]).set("ai", () => {
        const player2 = get.player();
        if (player2.maxHp > 4) {
          return "背水！";
        }
        if (player2.hasSkill("dragxigui") || player2.hp > 1) {
          return "选项二";
        }
        return "cancel2";
      }).forResult();
      event.result = {
        bool: result.control !== "cancel2",
        cost_data: result.index
      };
    },
    async content(event, trigger, player) {
      const index = event.cost_data;
      if (index !== 1) {
        await player.addTempSkills(["zhaxiang"]);
      }
      if (index !== 0) {
        await player.loseHp();
        player.addTempSkill("dragzhawang_draw");
      }
      if (index === 2) {
        await player.loseMaxHp();
      }
    },
    derivation: "zhaxiang",
    subSkill: {
      draw: {
        trigger: {
          player: "phaseChange"
        },
        direct: true,
        charlotte: true,
        filter(event, player) {
          return ["phaseZhunbei", "phaseJieshu"].some((key) => event.phaseList[event.num].indexOf(key) !== -1);
        },
        async content(event, trigger, player) {
          trigger.phaseList[trigger.num] = `phaseDraw|${event.name}`;
        }
      }
    }
  },
  dragxigui: {
    trigger: {
      global: "roundEnd",
      player: "dying"
    },
    check(event, player) {
      return event.name === "dying";
    },
    limited: true,
    filter(event, player) {
      return event.name !== "dying" || event.player.hp <= 0;
    },
    skillAnimation: true,
    animationColor: "fire",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.recoverTo(player.maxHp);
      const next = game.createEvent("xiguiContent", false);
      next.player = player;
      next.setContent(() => {
        player.when({ global: "roundEnd" }).step(async (event2, trigger2, player2) => {
          await player2.changeSkills(["tydangxian"], ["dragzhawang"]);
        });
      });
      if (trigger.name !== "dying") {
        event.next.remove(next);
        trigger.next.push(next);
      }
    },
    derivation: ["tydangxian"]
  },
  //龙周仓
  dragzhongyong: {
    audio: "mobilezhongyong",
    trigger: {
      global: "useCardToTarget"
    },
    filter(event, player) {
      if (event.target.group !== player.group || event.targets?.includes(player)) {
        return false;
      }
      if (event.player === event.target) {
        return false;
      }
      if (!["trick", "basic"].includes(get.type2(event.card))) {
        return false;
      }
      return lib.filter.targetEnabled(event.card, event.player, player);
    },
    check(event, player) {
      const type = get.type2(event.card);
      if (get.tag(event.card, "damage") && player.hp <= (type === "trick" ? 2 : 1)) {
        return false;
      }
      if (type === "trick" && player.hp <= 1) {
        return false;
      }
      return get.effect(event.target, event.card, event.player, player) <= get.effect(player, event.card, event.player, player) + 1;
    },
    usable(skill, player) {
      return 1 + player.getDamagedHp();
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const target = trigger.target;
      const type = get.type2(trigger.card);
      const evt = trigger.getParent();
      evt.triggeredTargets2.remove(target);
      evt.targets.remove(target);
      evt.targets.push(player);
      if (type === "basic") {
        await target.draw();
      } else {
        await player.loseHp();
      }
    }
  },
  //龙关银屏
  dragxueji: {
    audio: "xueji",
    enable: "phaseUse",
    usable: 1,
    filterTarget: true,
    selectTarget() {
      const player = _status.event.player;
      return [1, Math.max(1, player.getDamagedHp())];
    },
    multiline: true,
    line: "fire",
    async content(event, trigger, player) {
      await event.target.damage("fire", "nocard");
    },
    ai: {
      damage: true,
      fireAttack: true,
      threaten: 1.5,
      order: 7,
      result: {
        target(player, target) {
          return get.damageEffect(target, player, target, "fire");
        }
      }
    }
  },
  draghuxiao: {
    audio: "huxiao",
    trigger: { source: "damageSource" },
    forced: true,
    filter(event, player) {
      if (!event.player.isIn()) {
        return false;
      }
      return event.hasNature("fire") && !player.hasHistory("sourceDamage", (evt) => evt.hasNature("fire") && evt !== event, event);
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await player.draw();
      player.addTempSkill("draghuxiao_effect");
      player.markAuto("draghuxiao_effect", trigger.player);
    },
    subSkill: {
      effect: {
        onremove: true,
        mark: true,
        intro: {
          content: "players"
        },
        mod: {
          cardUsableTarget(card, player, target) {
            if (player.getStorage("draghuxiao_effect").includes(target)) {
              return true;
            }
          }
        }
      }
    }
  },
  //龙关平
  draglongyin: {
    audio: "relongyin",
    trigger: {
      global: "useCard"
    },
    filter(event, player) {
      if (event.card.name !== "sha" || !event.player.isPhaseUsing()) {
        return false;
      }
      if (get.color(event.card) !== "red") {
        return false;
      }
      return true;
    },
    usable: 1,
    logTarget: "player",
    check(event, player) {
      return get.attitude(player, event.player) > 0;
    },
    async content(event, trigger, player) {
      const result = (await player.draw().forResult()).cards;
      if (!result) {
        return;
      }
      await player.showCards(result, `${get.translation(player)}发动了【龙吟】`);
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        const stat = trigger.player.getStat().card;
        const name = trigger.card.name;
        if (typeof stat[name] === "number") {
          stat[name]--;
        }
      }
      if (get.color(result[0]) === get.color(trigger.card)) {
        trigger.effectCount++;
      }
    }
  },
  dragjiezhong: {
    audio: "jiezhong",
    trigger: {
      global: "phaseZhunbeiBegin"
    },
    limited: true,
    skillAnimation: true,
    animationColor: "orange",
    check(event, player) {
      if (event.player === player) {
        return !game.hasPlayer((current) => get.attitude(player, current) >= 0 && current !== player);
      }
      return get.attitude(player, event.player) > 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await trigger.player.draw(player.hp);
      if (trigger.player !== player) {
        player.when({ global: "roundStart" }).step(async () => {
          player.restoreSkill("dragjiezhong");
        });
      }
    }
  },
  //龙关索
  dragqianfu: {
    enable: "chooseToUse",
    filter(event, player) {
      if (event.type === "wuxie") {
        return false;
      }
      return get.inpileVCardList((info) => get.type(info[2]) === "basic").some((card) => event.filterCard({ name: card[2], nature: card[3] }, player, event));
    },
    usable: 1,
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => get.type(info[2]) === "basic").filter((card) => event.filterCard({ name: card[2], nature: card[3] }, player, event));
        return ui.create.dialog("搴芙", [list, "vcard"], "hidden");
      },
      check(button) {
        const event = get.event().getParent();
        if (event.type !== "phase") {
          return 1;
        }
        return get.player().getUseValue({ name: button.link[2], nature: button.link[3] });
      },
      prompt(links) {
        return `视为使用${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}并摸三张牌`;
      },
      backup(links, player) {
        return {
          selectCard: -1,
          filterCard: () => false,
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            isCard: true,
            storage: {
              dragqianfu: true
            }
          },
          async precontent(event, trigger, player2) {
            player2.logSkill("dragqianfu");
            player2.addTempSkill("dragqianfu_effect");
          }
        };
      }
    },
    hiddenCard(player, name) {
      if (player.getStat("skill").dragqianfu) {
        return false;
      }
      return get.type(name) === "basic" && lib.inpile.includes(name);
    },
    ai: {
      order: 10,
      respondShan: true,
      respondSha: true,
      skillTagFilter(player, tag, arg) {
        if (arg === "respond") {
          return false;
        }
        return get.info("dragqianfu").hiddenCard(player, tag.slice("respond".length).toLowerCase());
      },
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        trigger: {
          player: ["useCardToPlayer", "useCardAfter"]
        },
        filter(event, player, name) {
          if (!event?.card?.storage?.dragqianfu) {
            return false;
          }
          return name === "useCardAfter" || event.target.hasSex("female");
        },
        forced: true,
        direct: true,
        async content(event, trigger, player) {
          if (event.triggername === "useCardAfter") {
            await player.draw(3);
          } else {
            await trigger.target.gainMaxHp();
          }
        }
      }
    }
  },
  dragchengyuan: {
    trigger: {
      global: "dying"
    },
    check(event, player) {
      if (get.attitude(player, event.player) < 4) {
        return false;
      }
      if (event.player === player || event.player === get.zhu(player)) {
        return true;
      }
      return !player.hasUnknown();
    },
    limited: true,
    filter(event, player) {
      if (event.player !== player && !event.player.hasSex("female")) {
        return false;
      }
      return event.player.hp <= 0;
    },
    skillAnimation: true,
    animationColor: "fire",
    logTarget: "player",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await trigger.player.recoverTo(trigger.player.maxHp);
    }
  },
  dragyuxian: {
    dutySkill: true,
    derivation: ["dragyinglong", "dragxiefang"],
    group: ["dragyuxian_phase", "dragyuxian_achieve", "dragyuxian_fail"],
    subSkill: {
      phase: {
        trigger: {
          global: "phaseAfter"
        },
        prompt2: "执行一个额外回合",
        filter(event, player) {
          if (player.hasSkill("dragyuxian_used")) {
            return false;
          }
          return game.hasGlobalHistory("changeHp", (evt) => evt.getParent().name === "recover" && [event.player, player].some((p) => p === evt.player));
        },
        async content(event, trigger, player) {
          player.addTempSkill("dragyuxian_used", "roundStart");
          player.insertPhase();
        }
      },
      used: {
        charlotte: true
      },
      achieve: {
        trigger: {
          player: "phaseZhunbeiBegin"
        },
        filter(event, player) {
          return player.actionHistory.filter((i) => i.isMe && !i.isSkipped).length >= 4;
        },
        forced: true,
        locked: false,
        skillAnimation: true,
        animationColor: "fire",
        async content(event, trigger, player) {
          player.awakenSkill("dragyuxian");
          game.log(player, "成功完成使命");
          player.addSkills(["dragyinglong", "dragxiefang"]);
        }
      },
      fail: {
        trigger: {
          player: "dying"
        },
        forced: true,
        filter(event, player) {
          return event.source?.hasSex("female");
        },
        locked: false,
        async content(event, trigger, player) {
          player.awakenSkill("dragyuxian");
          game.log(player, "使命失败");
        }
      }
    }
  },
  dragyinglong: {
    trigger: {
      global: "useCard"
    },
    filter(event, player) {
      return get.is.convertedCard(event.card) || get.is.virtualCard(event.card);
    },
    usable: 1,
    async cost(event, trigger, player) {
      const target = trigger.player;
      const result = await player.chooseControl("cancel2").set("prompt", get.prompt(event.skill, target)).set("choiceList", [`令${get.translation(target)}本回合使用${get.translation(get.type2(trigger.card))}牌不可被响应`, `令${get.translation(target)}本回合使用${get.translation(get.color(trigger.card))}牌无次数限制`]).set("ai", () => {
        const player2 = get.player();
        const trigger2 = get.event().getTrigger();
        if (get.attitude(player2, trigger2.player) <= 0) {
          return "cancel2";
        }
        if (get.color(trigger2.card) === "none") {
          return "选项一";
        }
        return target.countCards("h") > Math.random() * 4 ? "选项二" : "选项一";
      }).forResult();
      event.result = {
        bool: result.control !== "cancel2",
        targets: [target],
        cost_data: result.control
      };
    },
    async content(event, trigger, player) {
      const target = trigger.player;
      const result = event.cost_data === "选项一" ? "dragyinglong_directHit" : "dragyinglong_unlimit";
      target.addTempSkill(result);
      target.markAuto(result, trigger.card);
    },
    subSkill: {
      directHit: {
        onremove: true,
        trigger: {
          player: "useCard"
        },
        filter(event, player) {
          return player.getStorage("dragyinglong_directHit").some((card) => get.type2(card) === get.type2(event.card));
        },
        marktext: "威",
        intro: {
          markcount: () => null,
          content(storage, player) {
            const list = player.getStorage("dragyinglong_directHit").map((card) => get.translation(get.type2(card))).toUniqued();
            if (list.length) {
              return `本回合使用${list.join("、")}牌不可被响应`;
            }
            return "无效果";
          }
        },
        charlotte: true,
        forced: true,
        async content(event, trigger, player) {
          trigger.directHit.addArray(game.players);
          game.log(trigger.card, "不可被响应");
        },
        ai: {
          directHit_ai: true,
          skillTagFilter(player, tag, arg) {
            if (!player.getStorage("dragyinglong_directHit").some((card) => get.type2(card) === get.type2(arg.card))) {
              return false;
            }
          }
        }
      },
      unlimit: {
        onremove: true,
        marktext: "武",
        intro: {
          markcount: () => null,
          content(storage, player) {
            const list = player.getStorage("dragyinglong_unlimit").map((card) => get.translation(get.color(card))).toUniqued();
            if (list.length) {
              return `本回合使用${list.join("、")}牌无次数限制`;
            }
            return "无效果";
          }
        },
        charlotte: true,
        mod: {
          cardUsable(card, player) {
            const color = get.color(card);
            if (color === "unsure" || player.getStorage("dragyinglong_unlimit").some((cardx) => get.color(cardx) === color)) {
              return Infinity;
            }
          }
        }
      }
    }
  },
  dragxiefang: {
    locked: false,
    trigger: {
      player: "useCard2"
    },
    filter(event, player) {
      if (!["basic", "trick"].includes(get.type(event.card))) {
        return false;
      }
      if (!event.targets) {
        return false;
      }
      const info = get.info(event.card);
      if (info.allowMultiple === false) {
        return false;
      }
      if (event.targets && !info.multitarget && game.hasPlayer((current) => !event.targets.includes(current) && lib.filter.targetEnabled2(event.card, player, current) && lib.filter.targetInRange(event.card, player, current))) {
        return true;
      }
      return false;
    },
    async cost(event, trigger, player) {
      const num = Math.max(
        1,
        game.countPlayer((current) => current.hasSex("female"))
      );
      event.result = await player.chooseTarget(
        get.prompt(event.skill),
        `为${get.translation(trigger.card)}额外指定至多${get.cnNumber(num)}个目标`,
        (card, player2, target) => {
          const { targets, card: cardx } = get.event();
          if (targets.includes(target)) {
            return false;
          }
          return lib.filter.targetEnabled2(cardx, player2, target) && lib.filter.targetInRange(cardx, player2, target);
        },
        [1, num]
      ).set("autodelay", true).set("ai", (target) => {
        const trigger2 = get.event().getTrigger();
        const player2 = get.player();
        return get.effect(target, trigger2.card, player2, player2);
      }).set("targets", trigger.targets).set("card", trigger.card).forResult();
    },
    async content(event, trigger, player) {
      trigger.targets.addArray(event.targets);
    },
    mod: {
      globalFrom(from, to, distance) {
        const num = Math.max(
          1,
          game.countPlayer((current) => current.hasSex("female"))
        );
        return distance - num;
      }
    }
  },
  //华雌
  dragshiyao: {
    global: "dragshiyao_global",
    subSkill: {
      global: {
        enable: "phaseUse",
        usable: 1,
        filterTarget(card, player, target) {
          return target.hasSkill("dragshiyao");
        },
        filter(event, player) {
          const num = game.countPlayer((current) => current.hasSkill("dragshiyao"));
          return num > 0;
        },
        selectTarget() {
          const num = game.countPlayer((current) => current.hasSkill("dragshiyao"));
          if (num > 1) {
            return 1;
          }
          return -1;
        },
        async content(event, trigger, player) {
          const target = event.target;
          await player.draw();
          const card = game.createCard("du", lib.suit.randomGet(), Math.ceil(Math.random() * 13));
          if (card) {
            await target.gain(card, "gain2");
          }
          const result = await player.judge((card2) => {
            if (get.color(card2) === "red") {
              return 2;
            }
            return 1;
          }).forResult();
          if (result.color === "red" && player.hasUseTarget({ name: "tao" })) {
            const next = player.chooseToUse();
            next.set("openskilldialog", "试药：是否将一张红色牌当做【桃】使用？");
            next.set("norestore", true);
            next.set("addCount", false);
            next.set("_backupevent", "dragshiyao_backup");
            next.set("custom", {
              add: {},
              replace: { window() {
              } }
            });
            next.backup("dragshiyao_backup");
          } else if (result.color === "black") {
            await target.loseMaxHp();
            const cardx = game.createCard("du", lib.suit.randomGet(), Math.ceil(Math.random() * 13));
            if (cardx) {
              await player.gain(cardx, "gain2");
            }
          }
        },
        ai: {
          order: 1,
          result: {
            player: 1
          }
        }
      },
      backup: {
        filterCard(card) {
          return get.itemtype(card) === "card" && get.color(card) === "red";
        },
        viewAs: {
          name: "tao"
        },
        position: "hes",
        selectCard: 1,
        check: (card) => 7 - get.value(card),
        popname: true
      }
    }
  },
  dragzuoyu: {
    trigger: {
      global: ["loseHpAfter", "recoverAfter"]
    },
    usable: 1,
    filter(event, player) {
      if (event.player === _status.currentPhase || event.player === player) {
        return false;
      }
      if (event.name === "recover") {
        return true;
      }
      if (!player.countCards("hes", "du") && !_status.connectMode) {
        return false;
      }
      if (!event.player.isIn()) {
        return false;
      }
      return ["sha", "guaguliaodu", "yidugongdu"].some((name) => {
        if (player.getStorage("dragduyi_used").includes(name)) {
          return false;
        }
        return player.canUse(get.autoViewAs({ name }, "unsure"), event.player, false);
      });
    },
    derivation: "dragduyi",
    direct: true,
    async content(event, trigger, player) {
      if (trigger.name === "recover") {
        const result = await player.chooseBool(get.prompt("dragzuoyu"), "增加1点体力上限").forResult();
        if (result.bool) {
          player.logSkill(event.name);
          await player.gainMaxHp();
          return;
        }
      } else {
        const list = [];
        const target = trigger.player;
        for (const name of ["sha", "guaguliaodu", "yidugongdu"]) {
          if (player.getStorage("dragduyi_used").includes(name)) {
            continue;
          }
          if (player.canUse(get.autoViewAs({ name }, "unsure"), target, false)) {
            list.push([get.type(name), "", name]);
          }
        }
        if (list.length) {
          const result = await player.chooseButton([get.prompt("dragzuoyu", target), [list, "vcard"]]).set("ai", (button) => {
            const player2 = _status.event.player;
            return player2.getUseValue({
              name: button.link[2]
            });
          }).forResult();
          if (result.bool) {
            const card = { name: result.links[0][2] };
            game.broadcastAll((card2) => {
              lib.skill.dragduyi_backup.viewAs = card2;
            }, card);
            const next = player.chooseToUse();
            next.set("openskilldialog", `毒医：是否将一张【毒】当做【${get.translation(card)}】对${get.translation(target)}使用`);
            next.set("norestore", true);
            next.set("addCount", false);
            next.set("_backupevent", "dragduyi_backup");
            next.set("custom", {
              add: {},
              replace: { window() {
              } }
            });
            next.set("onlyTarget", target);
            next.backup("dragduyi_backup");
            const result2 = await next.forResult();
            if (result2.bool) {
              return;
            }
          }
        }
      }
      player.storage.counttrigger.dragzuoyu--;
    }
  },
  dragduyi: {
    enable: "chooseToUse",
    filter(event, player) {
      if (!player.countCards("hes", "du") && !_status.connectMode) {
        return false;
      }
      return ["sha", "guaguliaodu", "yidugongdu"].some((name) => {
        if (player.getStorage("dragduyi_used").includes(name)) {
          return false;
        }
        return event.filterCard(get.autoViewAs({ name }, "unsure"), player, event);
      });
    },
    chooseButton: {
      dialog(event, player) {
        const list = [];
        for (const name of ["sha", "guaguliaodu", "yidugongdu"]) {
          if (player.getStorage("dragduyi_used").includes(name)) {
            continue;
          }
          if (event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
            list.push([get.type(name), "", name]);
          }
        }
        return ui.create.dialog("毒医", [list, "vcard"]);
      },
      check(button) {
        const player = _status.event.player;
        return player.getUseValue({
          name: button.link[2]
        });
      },
      backup(links, player) {
        const backup = get.copy(lib.skill["dragduyi_backup"]);
        backup.viewAs = { name: links[0][2] };
        return backup;
      },
      prompt(links, player) {
        return `将一张【毒】当做【${get.translation(links[0][2])}】使用`;
      }
    },
    hiddenCard(player, name) {
      if (!["sha", "guaguliaodu", "yidugongdu"].includes(name)) {
        return false;
      }
      if (player.getStorage("dragduyi_used").includes(name)) {
        return false;
      }
      return player.countCards("hes", "du");
    },
    ai: {
      respondSha: true,
      skillTagFilter(player) {
        if (player.getStorage("dragduyi_used").includes("sha")) {
          return false;
        }
        if (!player.countCards("hes", "du")) {
          return false;
        }
      },
      order: 1,
      result: { player: 1 }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      },
      backup: {
        filterCard: (card) => get.itemtype(card) === "card" && get.name(card) === "du",
        popname: true,
        check(card) {
          return 8 - get.value(card);
        },
        filterTarget(card, player, target) {
          const targetx = get.event().onlyTarget;
          if (targetx && targetx !== target) {
            return false;
          }
          return lib.filter.filterTarget.apply(this, arguments);
        },
        position: "hes",
        async precontent(event, trigger, player) {
          const evt = event.getParent("chooseToUse", null, true);
          if (evt?.onlyTarget) {
            player.logSkill("dragzuoyu", [evt.onlyTarget]);
          }
        },
        onuse(event, player) {
          player.addTempSkill("dragduyi_used");
          player.markAuto("dragduyi_used", event.card.name);
        }
      }
    }
  },
  dragjuliao: {
    trigger: {
      player: "loseHpBegin",
      global: "useCardToTargeted"
    },
    forced: true,
    filter(event, player) {
      if (event.name === "loseHp") {
        return event.type === "du";
      }
      if (event.player !== player || event.target === player) {
        return false;
      }
      return get.is.convertedCard(event.card);
    },
    logTarget(event, player) {
      return event[event.name === "loseHp" ? "player" : "target"];
    },
    async content(event, trigger, player) {
      if (trigger.name === "loseHp") {
        trigger.cancel();
      } else {
        const target = trigger.target;
        const bool = target.maxHp > 1 && get.effect(target, trigger.card, player, target) <= -7;
        const result = await target.chooseBool(`拒疗：是否减少1点体力上限，令【${get.translation(trigger.card)}】对你无效？`).set("choice", bool).forResult();
        if (result.bool) {
          await target.loseMaxHp();
          trigger.getParent().excluded.add(target);
        }
      }
    },
    ai: {
      nodu: true
    }
  }
};
const skills$l = {
  //桃园挽歌
  //铠甲合体
  _taoyuanwange: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player) {
      if (event.name === "phase" && game.phaseNumber !== 0) {
        return false;
      }
      return Object.keys(lib.skill._taoyuanwange.getEquip).some((name) => get.nameList(player).includes(name));
    },
    direct: true,
    getEquip: {
      ty_liubei: ["dilu", "ty_feilongduofeng"],
      ty_luxun: ["shangfangbaojian"],
      ty_sunquan: ["qingmingjian"]
    },
    getAudio: {
      ty_liubei: "jizhao2",
      ty_luxun: "nzry_cuike2",
      ty_sunquan: "sbzhiheng2"
    },
    async content(event, trigger, player) {
      const list = get.nameList(player);
      const info = lib.skill._taoyuanwange;
      const names = Object.keys(info.getEquip);
      for (const name of names) {
        if (list.includes(name)) {
          const equips = [];
          for (const card of info.getEquip[name]) {
            const cardx = get.cardPile((cardx2) => cardx2.name === card && player.canEquip(cardx2));
            if (cardx) {
              equips.push(cardx);
            }
          }
          if (equips.length) {
            game.broadcastAll((audio) => {
              if (lib.config.background_speak) {
                game.playAudio("skill", audio);
              }
            }, info.getAudio[name]);
            player.$gain2(equips);
            await player.equip(equips);
          }
        }
      }
    }
  },
  //刺客×4
  //孩子们，我分身了
  tyliupo: {
    mark: true,
    zhuanhuanji: true,
    marktext: "☯",
    intro: {
      content(storage, player, skill) {
        if (storage) {
          return "回合开始时，你可令本轮所有所有即将造成的伤害均视为体力流失";
        }
        return "回合开始时，你可令所有角色不能使用【桃】";
      }
    },
    trigger: {
      player: "phaseBegin"
    },
    logTarget: () => game.players,
    async content(event, trigger, player) {
      player.changeZhuanhuanji(event.name);
      const skill = `${event.name}_${player.storage[event.name] ? "wansha" : "jueqing"}`;
      for (const i of game.players) {
        i.addTempSkill(skill, "roundStart");
      }
    },
    subSkill: {
      wansha: {
        charlotte: true,
        mod: {
          cardSavable(card, player) {
            if (card.name === "tao") {
              return false;
            }
          },
          cardEnabled(card, player) {
            if (card.name === "tao") {
              return false;
            }
          }
        },
        mark: true,
        marktext: '<span style="text-decoration: line-through;">桃</span>',
        intro: {
          content: "不能使用桃"
        }
      },
      jueqing: {
        trigger: { player: "damageBefore" },
        forced: true,
        charlotte: true,
        async content(event, trigger, player) {
          trigger.cancel();
          trigger.player.loseHp(trigger.num);
        },
        ai: {
          jueqing: true
        },
        mark: true,
        marktext: '<span style="text-decoration: line-through;">伤</span>',
        intro: {
          content: "造成伤害改为失去体力"
        }
      }
    }
  },
  tyzhuiling: {
    trigger: {
      global: "loseHpEnd"
    },
    filter(event, player) {
      return player.countMark("tyzhuiling") < 3 && event.num > 0;
    },
    forced: true,
    logTarget: "player",
    async content(event, trigger, player) {
      const num = Math.min(3 - player.countMark(event.name), trigger.num);
      player.addMark(event.name, num);
    },
    marktext: "魂",
    intro: {
      name: "魂",
      content: "mark"
    },
    mod: {
      cardUsableTarget(card, player, target) {
        if (!target.countCards("h")) {
          return Infinity;
        }
      },
      targetInRange(card, player, target) {
        if (!target.countCards("h")) {
          return true;
        }
      }
    }
  },
  tyxihun: {
    trigger: { global: "roundEnd" },
    forced: true,
    async content(event, trigger, player) {
      for (const target of game.players) {
        if (target === player) {
          continue;
        }
        const result2 = await target.chooseToDiscard(2, "h", "弃置两张手牌，或点取消失去1点体力").set("ai", (card) => {
          const player2 = get.player();
          if (get.effect(player2, { name: "losehp" }, player2, player2) > 0) {
            return 0;
          }
          return 6 - get.value(card);
        }).forResult();
        if (!result2.bool) {
          await target.loseHp();
        }
      }
      if (!player.hasMark("tyzhuiling")) {
        return;
      }
      const list = [];
      for (let i = 1; i <= player.countMark("tyzhuiling"); i++) {
        list.push(get.cnNumber(i, true));
      }
      const result = await player.chooseControl(list).set("prompt", "吸魂：选择要移去的“魂”数").set("ai", () => {
        const player2 = get.player();
        return get.cnNumber(Math.max(1, Math.min(player2.countMark("tyzhuiling"), player2.getDamagedHp())), true);
      }).forResult();
      const num = result.index + 1;
      player.removeMark("tyzhuiling", num);
      if (player.isDamaged()) {
        await player.recover(num);
      }
    }
  },
  tyxianqi: {
    global: "tyxianqi_damage",
    subSkill: {
      damage: {
        enable: "phaseUse",
        usable: 1,
        prompt: "弃置两张牌或对自身造成1点伤害，然后令有【献气】的其他角色受到1点伤害",
        filterCard: true,
        position: "he",
        selectCard: [0, 2],
        filter(event, player) {
          return game.hasPlayer((current) => current.hasSkill("tyxianqi") && current !== player);
        },
        filterTarget(card, player, target) {
          if (ui.selected.cards?.length === 1) {
            return false;
          }
          return target.hasSkill("tyxianqi") && target !== player;
        },
        selectTarget() {
          if (ui.selected.cards?.length === 1) {
            return 114514;
          }
          return -1;
        },
        chessForceAll: true,
        check(card) {
          const player = get.player();
          if (get.damageEffect(player, player, player) > 0) {
            return 0;
          }
          return 8 - get.value(card);
        },
        complexTarget: true,
        async contentBefore(event, trigger, player) {
          if (!event.cards || !event.cards.length) {
            await player.damage();
          }
        },
        async content(event, trigger, player) {
          await event.target.damage();
        },
        ai: {
          order: 6,
          result: {
            player(player, target) {
              if (ui.selected.cards.length) {
                return 0;
              }
              if (player.hp >= target.hp) {
                return -0.9;
              }
              if (player.hp <= 2) {
                return -10;
              }
              return -2;
            },
            target(player, target) {
              if (!ui.selected.cards.length) {
                if (player.hp < 2) {
                  return 0;
                }
                if (player.hp === 2 && target.hp >= 2) {
                  return 0;
                }
                if (target.hp > player.hp) {
                  return 0;
                }
              }
              return get.damageEffect(target, player);
            }
          }
        }
      }
    }
  },
  tyfansheng: {
    trigger: {
      player: "dying"
    },
    filter(event, player) {
      return game.getAllGlobalHistory("everything", (evt) => evt.name === "dying" && evt.player === player).indexOf(event) === 0;
    },
    forced: true,
    skillAnimation: true,
    animationColor: "metal",
    async content(event, trigger, player) {
      await player.recoverTo(1);
      for (const target of game.players) {
        if (target === player) {
          continue;
        }
        const list = [];
        if (target.countCards("h")) {
          list.push("手牌区");
        }
        if (target.countCards("e")) {
          list.push("装备区");
        }
        if (list.length === 0) {
          continue;
        }
        let result;
        if (list.length === 1) {
          result = { control: list[0] };
        } else {
          result = await target.chooseControl(list).set("prompt", "返生：弃置一个区域的所有牌").set("ai", () => [0, 1].randomGet()).forResult();
        }
        const pos = result.control === "手牌区" ? "h" : "e";
        const cards2 = target.getCards(pos);
        if (cards2.length) {
          await target.discard(cards2);
        }
      }
    }
  },
  tyansha: {
    inherit: "tysiji",
    trigger: { global: "phaseBegin" },
    filter(event, player) {
      return player.countCards("hes") && player.canUse(
        {
          name: "sha",
          nature: "stab"
        },
        event.player
      );
    },
    group: "tyansha_range",
    subSkill: {
      range: {
        trigger: { player: "useCardAfter" },
        filter(event, player) {
          return event.skill === "tyansha_backup" && event.targets?.some((current) => current.isIn() && !player.getStorage("tyansha_effect").includes(current));
        },
        firstDo: true,
        silent: true,
        async content(event, trigger, player) {
          player.addTempSkill("tyansha_effect", "roundStart");
          player.markAuto(
            "tyansha_effect",
            trigger.targets.filter((current) => current.isIn() && !player.getStorage("tyansha_effect").includes(current))
          );
        }
      },
      backup: {
        filterCard(card) {
          return get.itemtype(card) === "card";
        },
        viewAs: {
          name: "sha",
          nature: "stab"
        },
        selectCard: 1,
        position: "hes",
        ai1(card) {
          return 7 - get.value(card);
        },
        log: false
      },
      effect: {
        charlotte: true,
        onremove: true,
        mod: {
          globalTo(from, to, num) {
            if (to.getStorage("tyansha_effect").includes(from)) {
              return -Infinity;
            }
          }
        },
        intro: { content: "$本轮计算与你的距离视为1" }
      }
    }
  },
  tycangshen: {
    forced: true,
    trigger: {
      player: "useCardAfter"
    },
    filter(event, player) {
      return event.card.name === "sha";
    },
    async content(event, trigger, player) {
      player.tempBanSkill("tycangshen", "roundStart");
    },
    mod: {
      globalTo(from, to, num) {
        if (!to.isTempBanned("tycangshen")) {
          return num + 1;
        }
      }
    }
  },
  tyxiongren: {
    trigger: {
      source: "damageBegin1"
    },
    filter(event, player) {
      if (get.distance(event.player, player) <= 1) {
        return false;
      }
      return event.card?.name === "sha";
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.num++;
    },
    mod: {
      cardUsableTarget(card, player, target) {
        if (get.distance(target, player) <= 1) {
          return Infinity;
        }
      },
      targetInRange(card, player, target) {
        if (get.distance(target, player) <= 1) {
          return true;
        }
      }
    }
  },
  tysiji: {
    trigger: { global: "phaseEnd" },
    filter(event, player) {
      if (!event.player.hasHistory("lose", (evt) => {
        const evtx = evt.relatedEvent || evt.getParent();
        return !["useCard", "respond"].includes(evtx.name);
      })) {
        return false;
      }
      return player.countCards("hes") && player.canUse(
        {
          name: "sha",
          nature: "stab"
        },
        event.player,
        false
      );
    },
    direct: true,
    clearTime: true,
    async content(event, trigger, player) {
      const next = player.chooseToUse();
      next.set("openskilldialog", `${get.translation(event.name)}：是否将一张牌当作刺【杀】对${get.translation(trigger.player)}使用？`);
      next.set("norestore", true);
      next.set("_backupevent", `${event.name}_backup`);
      next.set("custom", {
        add: {},
        replace: { window() {
        } }
      });
      next.backup(`${event.name}_backup`);
      next.set("targetRequired", true);
      next.set("complexSelect", true);
      next.set("complexTarget", true);
      next.set("filterTarget", function(card, player2, target) {
        const { sourcex } = get.event();
        if (target !== sourcex && !ui.selected.targets.includes(sourcex)) {
          return false;
        }
        return lib.filter.targetEnabled.apply(this, arguments);
      });
      next.set("sourcex", trigger.player);
      next.set("addCount", false);
      next.set("logSkill", event.name);
      await next;
    },
    subSkill: {
      backup: {
        filterCard(card) {
          return get.itemtype(card) === "card";
        },
        viewAs: {
          name: "sha",
          nature: "stab"
        },
        selectCard: 1,
        position: "hes",
        ai1(card) {
          return 7 - get.value(card);
        },
        log: false
      }
    }
  },
  tydaifa: {
    inherit: "tysiji",
    filter(event, player) {
      if (!game.hasPlayer2((current) => {
        if (current === event.player) {
          return false;
        }
        if (current.hasHistory("lose", (evt) => {
          if (evt.type !== "gain") {
            return false;
          }
          const evtx = evt.getParent();
          if (evtx.giver || evtx.getParent().name === "gift") {
            return false;
          }
          const cards2 = evtx.getg(event.player);
          if (!cards2.length) {
            return false;
          }
          const cards22 = evtx.getl(current).cards2;
          for (const card of cards22) {
            if (cards2.includes(card)) {
              return true;
            }
          }
          return false;
        })) {
          return true;
        }
      })) {
        return false;
      }
      return player.countCards("hes") && player.canUse(
        {
          name: "sha",
          nature: "stab"
        },
        event.player,
        false
      );
    }
  },
  //桃神关羽
  tywushen: {
    audio: "wushen",
    enable: ["chooseToRespond", "chooseToUse"],
    filterCard(card, player) {
      return get.suit(card) === "heart";
    },
    position: "hes",
    viewAs: {
      name: "sha",
      storage: {
        tywushen: true
      }
    },
    viewAsFilter(player) {
      if (!player.countCards("hes", { suit: "heart" })) {
        return false;
      }
    },
    prompt: "将一张红桃牌当杀使用或打出",
    check(card) {
      const val = get.value(card);
      if (_status.event.name === "chooseToRespond") {
        return 1 / Math.max(0.1, val);
      }
      return 5 - val;
    },
    ai: {
      skillTagFilter(player) {
        if (!player.countCards("hes", { suit: "heart" })) {
          return false;
        }
      },
      respondSha: true
    },
    locked: false,
    mod: {
      cardUsable(card, player) {
        if (card?.storage?.tywushen) {
          return Infinity;
        }
      },
      targetInRange(card, player) {
        if (card?.storage?.tywushen) {
          return true;
        }
      }
    },
    group: "tywushen_respond",
    subSkill: {
      respond: {
        trigger: { player: "useCard" },
        direct: true,
        forced: true,
        filter(event, player) {
          return event.card?.storage?.tywushen;
        },
        async content(event, trigger, player) {
          trigger.directHit.addArray(game.players);
          if (trigger.addCount !== false) {
            trigger.addCount = false;
            if (player.stat[player.stat.length - 1].card.sha > 0) {
              player.stat[player.stat.length - 1].card.sha--;
            }
          }
        }
      }
    }
  },
  tywuhun: {
    audio: "wuhun2",
    trigger: { player: "damageEnd" },
    filter(event, player) {
      return event.source && event.source.isIn();
    },
    forced: true,
    logTarget: "source",
    async content(event, trigger, player) {
      trigger.source.addMark("tywuhun", trigger.num);
    },
    group: "tywuhun_die",
    ai: {
      notemp: true,
      effect: {
        target: (card, player, target) => {
          if (!target.hasFriend()) {
            return;
          }
          const rec = get.tag(card, "recover");
          const damage = get.tag(card, "damage");
          if (!rec && !damage) {
            return;
          }
          if (damage && player.hasSkillTag("jueqing", false, target)) {
            return 1.7;
          }
          const die = [null, 1];
          for (const current of game.filterPlayer()) {
            let count = current.countMark("new_wuhun");
            if (current === player && target.hp + target.hujia > 1) {
              count++;
            }
            if (count > die[1] || count === die[1] && (!die[0] || get.attitude(target, current) < get.attitude(target, die[0]))) {
              die[0] = current;
              die[1] = count;
            }
          }
          if (!die[0]) {
            return;
          }
          if (damage) {
            return [1, 0, 1, -6 * get.sgnAttitude(player, die[0]) / Math.max(1, target.hp)];
          }
          return [1, 6 * get.sgnAttitude(player, die[0]) / Math.max(1, target.hp)];
        }
      }
    },
    marktext: "魇",
    intro: {
      name: "梦魇",
      content: "mark",
      onunmark: true
    },
    subSkill: {
      die: {
        audio: "wuhun2",
        trigger: { player: "die" },
        filter(event, player) {
          return event.source || game.hasPlayer((current) => current !== player && current.hasMark("tywuhun"));
        },
        forced: true,
        direct: true,
        forceDie: true,
        skillAnimation: true,
        animationColor: "soil",
        async content(event, trigger, player) {
          let num = 0;
          for (const current of game.players) {
            if (current !== player && current.countMark("tywuhun") > num) {
              num = current.countMark("tywuhun");
            }
          }
          const result = await player.chooseTarget({
            forced: true,
            prompt: "请选择【武魂】的目标",
            prompt2: "令其进行判定，若判定结果不为【桃】，则其死亡",
            filterTarget: (card, player2, target2) => target2 !== player2 && (target2 === trigger.source || target2.countMark("tywuhun") === num),
            ai: (target2) => -get.attitude(player, target2)
          }).set("forceDie", true).set("num", num).forResult();
          if (!result.bool) {
            return;
          }
          const target = result.targets[0];
          event.target = target;
          player.logSkill("tywuhun_die", target);
          player.line(target, { color: [255, 255, 0] });
          await game.delay(2);
          const judgeResult = await target.judge({
            judge: (card) => ["tao"].includes(card.name) ? 10 : -10,
            judge2: (result2) => result2.bool === false
          }).forResult();
          if (!judgeResult.bool) {
            await target.die();
          }
        }
      }
    }
  },
  //桃神张飞
  tyshencai: {
    audio: "shencai",
    enable: "phaseUse",
    filter(event, player) {
      if (player.countMark("tyshencai") > player.countMark("shencai")) {
        return false;
      }
      return true;
    },
    filterTarget: lib.filter.notMe,
    onremove: true,
    prompt: "选择一名其他角色进行地狱审判",
    async content(event, trigger, player) {
      player.addMark("tyshencai", 1, false);
      player.addTempSkill("tyshencai_used", "phaseChange");
      const next = event.target.judge();
      next.callback = lib.skill.shencai.contentx;
    },
    ai: {
      order: 8,
      result: {
        target(player, target) {
          const att = get.sgnAttitude(player, target);
          const hp = target.getHp(true) + 0.1;
          const hs = target.countCards("h") + 0.1;
          if (att < 0) {
            return att * hp * hs / 100;
          }
          return 0;
        }
      }
    },
    group: "tyshencai_wusheng",
    subSkill: {
      used: {
        onremove: ["tyshencai"],
        charlotte: true
      },
      wusheng: {
        audio: "shencai",
        enable: ["chooseToRespond", "chooseToUse"],
        filterCard(card, player) {
          return get.suit(card) === "none";
        },
        position: "hes",
        viewAs: {
          name: "sha",
          color: "none",
          suit: "none"
        },
        viewAsFilter(player) {
          if (!player.countCards("hes", { suit: "none" })) {
            return false;
          }
        },
        prompt: "将一张无色牌当杀使用或打出",
        check(card) {
          const val = get.value(card);
          if (_status.event.name === "chooseToRespond") {
            return 1 / Math.max(0.1, val);
          }
          return 5 - val;
        },
        ai: {
          skillTagFilter(player) {
            if (!player.countCards("hes", { color: "none" })) {
              return false;
            }
          },
          respondSha: true
        }
      }
    }
  },
  tyxunshi: {
    audio: "xunshi",
    mod: {
      suit(card) {
        if (lib.skill.xunshi.isXunshi(card)) {
          return "none";
        }
      },
      targetInRange(card) {
        const suit = get.color(card);
        if (suit === "none" || suit === "unsure") {
          return true;
        }
      },
      cardUsable(card) {
        const suit = get.color(card);
        if (suit === "none" || suit === "unsure") {
          return Infinity;
        }
      }
    },
    init(player, skill) {
      player.addSkill("tyxunshi_mark");
    },
    onremove(player, skill) {
      player.removeSkill("tyxunshi_mark");
    },
    trigger: { player: "useCard2" },
    forced: true,
    filter(event, player) {
      return get.color(event.card) === "none";
    },
    async content(event, trigger, player) {
      if (player.countMark("shencai") < 4 && player.hasSkill("tyshencai", null, null, false)) {
        player.addMark("shencai", 1, false);
      }
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        const stat = player.getStat().card;
        const name = trigger.card.name;
        if (typeof stat[name] === "number") {
          stat[name]--;
        }
      }
      const info = get.info(trigger.card);
      if (info.allowMultiple === false || !trigger.targets || info.multitarget) {
        return;
      }
      if (!game.hasPlayer((current) => !trigger.targets.includes(current) && lib.filter.targetEnabled2(trigger.card, player, current))) {
        return;
      }
      const prompt2 = `为${get.translation(trigger.card)}增加任意个目标`;
      const result = await player.chooseTarget({
        prompt: get.prompt("xunshi"),
        prompt2,
        filterTarget(_card, player2, target) {
          const event2 = get.event();
          return !event2.targets.includes(target) && lib.filter.targetEnabled2(event2.card, event2.player, target);
        },
        selectTarget: [1, Infinity],
        ai: (target) => get.effect(target, trigger.card, player, player)
      }).set("card", trigger.card).set("targets", trigger.targets).forResult();
      if (!result.bool) {
        return;
      }
      if (!event.isMine() && !event.isOnline()) {
        await game.delayx();
      }
      player.line(result.targets, "fire");
      trigger.targets.addArray(result.targets);
    },
    subSkill: {
      mark: {
        charlotte: true,
        trigger: {
          player: "gainAfter",
          global: "loseAsyncAfter"
        },
        filter(event, player, name) {
          return event.getg(player).length && player.countCards("h");
        },
        direct: true,
        firstDo: true,
        async content(event, trigger, player) {
          const cards1 = [];
          const cards2 = [];
          player.getCards("h").forEach((card) => {
            const bool1 = lib.skill.xunshi.isXunshi(card);
            const bool2 = card.hasGaintag("tyxunshi_tag");
            if (bool1 && !bool2) {
              cards1.add(card);
            }
            if (!bool1 && bool2) {
              cards2.add(card);
            }
          });
          if (cards1.length) {
            player.addGaintag(cards1, "tyxunshi_tag");
          }
          if (cards2.length) {
            cards2.forEach((card) => card.removeGaintag("tyxunshi_tag"));
          }
        }
      }
    }
  },
  //范强张达
  tybianta: {
    trigger: { target: "useCardToTargeted" },
    usable: 1,
    filter(event, player) {
      return get.is.damageCard(event.card) && event.cards?.length;
    },
    marktext: "怨",
    intro: {
      name: "怨",
      content: "expansion",
      markcount: "expansion"
    },
    init(player, skill) {
      if (player.getExpansions("tyxingsha").length) {
        for (const card of player.getExpansions("tyxingsha")) {
          card.gaintag.remove("tyxingsha");
          card.gaintag.add(skill);
        }
        player.markSkill(skill);
      }
    },
    onremove(player, skill) {
      if (!_status.event.getParent("tyxiezhan", true)) {
        const cards2 = player.getExpansions(skill);
        if (cards2.length) {
          player.loseToDiscardpile(skill);
        }
      }
    },
    async content(event, trigger, player) {
      const next = player.addToExpansion(trigger.cards, "gain2");
      next.gaintag.add("tybianta");
      await next;
    },
    group: "tybianta_jieshu",
    subSkill: {
      jieshu: {
        trigger: {
          player: "phaseJieshuBegin"
        },
        filter(event, player) {
          return player.getExpansions("tybianta").length;
        },
        prompt2: "依次使用或打出你所有的“怨”",
        async content(event, trigger, player) {
          player.addTempSkill("tybianta_use");
          while (player.getExpansions("tybianta").length) {
            const card = player.getExpansions("tybianta")[0];
            if (player.hasUseTarget(card)) {
              const result = await player.chooseUseTarget(card).forResult();
              if (!result.bool) {
                break;
              }
            } else {
              break;
            }
          }
          player.removeSkill("tybianta_use");
        }
      },
      use: {
        enable: ["chooseToUse", "chooseToRespond"],
        filter(event, player) {
          if (!event.tybianta) {
            return false;
          }
          const card = event.tybianta;
          return event.filterCard(card, player, event);
        },
        onChooseToUse(event) {
          if (game.online) {
            return;
          }
          const player = event.player;
          if (!player.getExpansions("tybianta").length) {
            event.set("tybianta", false);
          } else {
            event.set("tybianta", player.getExpansions("tybianta")[0]);
          }
        },
        onChooseToRespond(event) {
          if (game.online) {
            return;
          }
          const player = event.player;
          if (!player.getExpansions("tybianta").length) {
            event.set("tybianta", false);
          } else {
            event.set("tybianta", player.getExpansions("tybianta")[0]);
          }
        },
        filterCard(card, player) {
          return card === _status.event.tybianta;
        },
        selectCard: -1,
        position: "x",
        viewAs(cards2, player) {
          const card = _status.event.tybianta;
          if (card) {
            return card;
          }
          return null;
        },
        prompt(event, player) {
          const card = _status.event.tybianta;
          return `是否使用${get.translation(card)}？`;
        },
        async precontent(event) {
          event.result.card = event.result.cards[0];
        },
        hiddenCard(player, name) {
          if (!player.getExpansions("tybianta").length) {
            return false;
          }
          return get.name(player.getExpansions("tybianta")[0], false) === name;
        },
        ai: {
          respondSha: true,
          respondShan: true,
          skillTagFilter(player, tag) {
            const name = tag.slice(7).toLowerCase();
            if (!player.getExpansions("tybianta").length) {
              return false;
            }
            return get.name(player.getExpansions("tybianta")[0], false) === name;
          }
        }
      }
    }
  },
  tybenxiang: {
    trigger: {
      source: "die"
    },
    locked: true,
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.name.slice(0, -5)), lib.filter.notMe, true).set("ai", (target) => get.effect(target, { name: "draw" }, get.player(), get.player())).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.draw(3);
    }
  },
  tyxiezhan: {
    audio: "juesheng",
    trigger: {
      player: ["phaseUseBegin", "enterGame"],
      global: "phaseBefore"
    },
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    locked: true,
    async cost(event, trigger, player) {
      const list = get.nameList(player);
      let bool = trigger.name === "phaseUse";
      if (bool) {
        if (list.includes("ty_fanjiang")) {
          event.result = {
            bool: true,
            cost_data: "ty_zhangda"
          };
        } else if (list.includes("ty_zhangda")) {
          event.result = {
            bool: true,
            cost_data: "ty_fanjiang"
          };
        } else {
          bool = false;
        }
      }
      if (!bool) {
        const result = await player.chooseControl("范疆", "张达").set("prompt", "协战：请变身").set("ai", () => [0, 1].randomGet()).forResult();
        event.result = {
          bool: true,
          cost_data: result.control === "范疆" ? "ty_fanjiang" : "ty_zhangda"
        };
      }
    },
    async content(event, trigger, player) {
      let prename = player.name1;
      if (player.name2 && get.character(player.name2, 3).includes("tyxiezhan")) {
        prename = player.name2;
      }
      await player.reinitCharacter(prename, event.cost_data);
      await game.delay();
    }
  },
  tyxingsha: {
    marktext: "怨",
    intro: {
      name: "怨",
      content: "expansion",
      markcount: "expansion"
    },
    init(player, skill) {
      if (player.getExpansions("tybianta").length) {
        for (const card of player.getExpansions("tybianta")) {
          card.gaintag.remove("tybianta");
          card.gaintag.add(skill);
        }
        player.markSkill(skill);
      }
    },
    onremove(player, skill) {
      if (!_status.event.getParent("tyxiezhan", true)) {
        const cards2 = player.getExpansions(skill);
        if (cards2.length) {
          player.loseToDiscardpile(skill);
        }
      }
    },
    enable: "phaseUse",
    filter(event, player) {
      return player.countCards("he") && !player.hasSkill("tyxingsha_used");
    },
    filterCard: true,
    selectCard: [1, 2],
    lose: false,
    discard: false,
    async content(event, trigger, player) {
      player.addTempSkill("tyxingsha_used");
      const next = player.addToExpansion(event.cards, player, "give");
      next.gaintag.add("tyxingsha");
      await next;
    },
    group: "tyxingsha_use",
    subSkill: {
      used: {
        charlotte: true
      },
      use: {
        trigger: {
          player: "phaseJieshuBegin"
        },
        filter(event, player) {
          if (!player.hasUseTarget(get.autoViewAs({ name: "sha" }, "unsure"), false)) {
            return false;
          }
          return player.getExpansions("tyxingsha").length;
        },
        async cost(event, trigger, player) {
          const result = await player.chooseButton(["刑杀：是否将两张“怨”当作杀使用？", player.getExpansions("tyxingsha")], 2).set("ai", (button) => {
            const player2 = get.player();
            const eff = player2.getUseValue(get.autoViewAs({ name: "sha" }, "unsure"), false);
            if (eff <= 0) {
              return 0;
            }
            return player2.getHp() - player2.getUseValue(button.link);
          }).forResult();
          event.result = {
            bool: result.bool,
            cards: result.links
          };
        },
        async content(event, trigger, player) {
          const card = get.autoViewAs({ name: "sha" }, event.cards);
          await player.chooseUseTarget(card, event.cards, false, "nodistance");
        }
      }
    }
  },
  tyxianshou: {
    trigger: {
      source: "die"
    },
    locked: true,
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.name.slice(0, -5)), lib.filter.notMe, true).set("ai", (target) => get.effect(target, { name: "recover" }, get.player(), get.player())).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      if (target.isDamaged()) {
        await target.recover(2);
      }
    }
  },
  //刘阿
  tyxiyu: {
    trigger: {
      global: "useCardToPlayered"
    },
    filter(event, player) {
      return event.isFirstTarget && (get.is.convertedCard(event.card) || get.is.virtualCard(event.card));
    },
    frequent: true,
    async content(event, trigger, player) {
      await player.draw();
    }
  },
  //谭雄
  tylengjian: {
    trigger: {
      player: "useCardToTargeted"
    },
    filter(event, player) {
      if (event.card.name !== "sha") {
        return false;
      }
      return !player.getStorage("tylengjian").includes(event.target);
    },
    intro: {
      content: "本回合已对$使用过【杀】"
    },
    forced: true,
    logTarget: "target",
    async content(event, trigger, player) {
      const target = event.targets[0];
      if (!player.getStorage("tylengjian").length) {
        player.when({ global: "phaseEnd" }).step(async () => {
          player.unmarkSkill("tylengjian");
          delete player.storage.tylengjian;
        });
      }
      player.markAuto("tylengjian", target);
      if (player.inRange(target)) {
        const id = target.playerid;
        const map = trigger.getParent().customArgs;
        if (!map[id]) {
          map[id] = {};
        }
        if (typeof map[id].extraDamage !== "number") {
          map[id].extraDamage = 0;
        }
        map[id].extraDamage++;
      } else {
        trigger.getParent().directHit.push(target);
      }
    },
    mod: {
      targetInRange(card, player, target) {
        if (card.name === "sha" && !player.inRange(target)) {
          if (!player.getStorage("tylengjian").includes(target)) {
            return true;
          }
        }
      }
    }
  },
  tysheju: {
    trigger: { player: "useCardAfter" },
    filter(event, player) {
      if (event.card.name !== "sha") {
        return false;
      }
      return event.targets?.some((current) => current.isIn() && current.countDiscardableCards(player, "he"));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => get.event().getTrigger().targets.includes(target) && target.countDiscardableCards(player2, "he")).set("ai", (target) => get.effect(target, { name: "guohe_copy2" }, get.player(), get.player())).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = await player.discardPlayerCard(target, "he", true).forResult();
      if (!result?.bool || !result?.links?.length) {
        return;
      }
      const subtype = get.subtype(result.links[0]);
      if (subtype && ["equip3", "equip4", "equip6"].includes(subtype)) {
        return;
      }
      target.addTempSkill("tysheju_range");
      target.addMark("tysheju_range", 1, false);
      if (target.inRange(player)) {
        await target.chooseToUse(
          function(card, player2, event2) {
            if (get.name(card) !== "sha") {
              return false;
            }
            return lib.filter.filterCard.apply(this, arguments);
          },
          `是否对${get.translation(player)}使用一张杀？`
        ).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("filterTarget", function(card, player2, target2) {
          if (target2 !== _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
            return false;
          }
          return lib.filter.targetEnabled.apply(this, arguments);
        }).set("sourcex", player);
      }
    },
    subSkill: {
      range: {
        charlotte: true,
        onremove: true,
        mark: true,
        intro: { content: "本回合攻击范围+#" },
        mod: {
          attackFrom(from, to, distance) {
            return distance - from.countMark("tysheju_range");
          }
        }
      }
    }
  },
  //buzhi
  tyhongde: {
    audio: "hongde",
    trigger: {
      player: ["loseAfter", "gainAfter"],
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    filter(event, player) {
      let num = event.getl(player).cards2.length;
      if (event.getg) {
        num = Math.max(num, event.getg(player).length);
      }
      return num > 1;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill)).set("ai", (target) => {
        const player2 = get.player();
        const name = get.attitude(player2, target) > 0 ? "draw" : "guohe_copy2";
        return get.effect(target, { name }, player2, player2);
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = await player.chooseControl("摸一张牌", "弃置一张牌").set("prompt", `令${get.translation(target)}执行一项`).set("target", target).set("ai", () => {
        const player2 = get.player();
        const target2 = get.event().target;
        const eff1 = get.effect(target2, { name: "guohe_copy2" }, player2, player2);
        const eff2 = get.effect(target2, { name: "draw" }, player2, player2);
        if (eff1 > eff2) {
          return 1;
        }
        return 0;
      }).forResult();
      if (result.index === 0) {
        await target.draw();
      } else if (target.countCards("he")) {
        await target.chooseToDiscard("he", true);
      }
    }
  },
  tydingpan: {
    audio: "dingpan",
    enable: "phaseUse",
    usable(skill, player) {
      return get.event().tydingpan?.length;
    },
    filter(event, player) {
      return game.hasPlayer((current) => current.countCards("e"));
    },
    filterTarget(event, player, target) {
      return target.countCards("e");
    },
    onChooseToUse(event) {
      if (event.type !== "phase" || game.online) {
        return;
      }
      const list = [];
      const player = event.player;
      player.getHistory("useCard", (evt) => {
        list.add(get.type2(evt.card));
      });
      event.set("tydingpan", list);
    },
    async content(event, trigger, player) {
      const { target } = event;
      await target.draw();
      let goon = get.damageEffect(target, player, target) >= 0;
      if (!goon && target.hp >= 4 && get.attitude(player, target) < 0) {
        const es = target.getCards("e");
        goon = es.some((card) => get.equipValue(card, target) >= 8);
      }
      const result = await target.chooseControl(() => {
        if (_status.event.goon) {
          return "选项二";
        }
        return "选项一";
      }).set("goon", goon).set("prompt", "定叛").set("choiceList", [`令${get.translation(player)}弃置你两张牌`, "获得你装备区内的所有牌并受到1点伤害"]).forResult();
      if (result.index === 0) {
        await player.discardPlayerCard(target, "he", Math.min(target.countCards("he"), 2), true);
      } else {
        await target.gain(target.getCards("e"), "gain2");
        await target.damage();
      }
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          if (get.damageEffect(target, player, target) >= 0) {
            return 2;
          }
          const att = get.attitude(player, target);
          if (att === 0) {
            return 0;
          }
          const es = target.getCards("e");
          if (att > 0 && (target.countCards("h") > 2 || target.needsToDiscard(1))) {
            return 0;
          }
          if (es.length === 1 && att > 0) {
            return 0;
          }
          for (const card of es) {
            const val = get.equipValue(card, target);
            if (val <= 4) {
              if (att > 0) {
                return 1;
              }
            } else if (val >= 7) {
              if (att < 0) {
                return -1;
              }
            }
          }
          return 0;
        }
      }
    }
  },
  //甘宁
  tyqixi: {
    audio: "qixi",
    inherit: "qixi",
    group: "tyqixi_nowuxie",
    subSkill: {
      nowuxie: {
        trigger: {
          player: "useCard"
        },
        filter(event, player) {
          if (event.card.name !== "guohe" || !get.is.convertedCard(event.card)) {
            return false;
          }
          return event.cards?.some((card) => get.type(card) !== "basic");
        },
        direct: true,
        async content(event, trigger, player) {
          trigger.directHit.addArray(game.players);
        }
      }
    }
  },
  tyfenwei: {
    skillAnimation: true,
    animationColor: "wood",
    audio: "fenwei",
    trigger: { global: "useCardToPlayered" },
    filter(event, player) {
      if (event.getParent().triggeredTargets3.length > 1) {
        return false;
      }
      if (get.type(event.card) !== "trick") {
        return false;
      }
      if (get.info(event.card).multitarget) {
        return false;
      }
      if (event.targets.length < 2) {
        return false;
      }
      return true;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt(event.skill), [1, trigger.targets.length], (card, player2, target) => _status.event.targets.includes(target)).set("ai", (target) => {
        const trigger2 = _status.event.getTrigger();
        if (game.phaseNumber > game.players.length * 2 && trigger2.targets.length >= game.players.length - 1 && !trigger2.excluded.includes(target)) {
          return -get.effect(target, trigger2.card, trigger2.player, _status.event.player);
        }
        return -1;
      }).set("targets", trigger.targets).forResult();
    },
    async content(event, trigger, player) {
      trigger.getParent().excluded.addArray(event.targets);
      const num = Math.max(1, player.getAllHistory("useSkill", (evt) => evt.skill === event.name).length - 1);
      const result = await player.chooseBool(`失去${num}点体力，或点取消失去【奋威】`).set("choice", player.hp > num).forResult();
      if (result.bool) {
        await player.loseHp(num);
      } else {
        await player.removeSkills(event.name);
      }
    }
  },
  //陆逊
  tyqianshou: {
    mark: true,
    zhuanhuanji: true,
    marktext: "☯",
    intro: {
      content(storage) {
        if (storage) {
          return "其他角色的回合开始时，若其体力值大于你，或其未处于横置状态，你可令其展示并交给你一张牌，若此牌不为黑色，你失去1点体力。";
        }
        return "其他角色的回合开始时，若其体力值大于你，或其未处于横置状态，你可展示并交给其一张红色牌，本回合你不能使用手牌且你与其不能成为牌的目标。";
      }
    },
    trigger: {
      global: "phaseBegin"
    },
    filter(event, player) {
      if (event.player === player) {
        return false;
      }
      if (event.player.hp <= player.hp && event.player.isLinked()) {
        return false;
      }
      if (player.storage.tyqianshou) {
        return event.player.countCards("he");
      }
      return player.countCards("he", { color: "red" });
    },
    async cost(event, trigger, player) {
      if (player.storage.tyqianshou) {
        event.result = await player.chooseBool(get.prompt2(event.skill, trigger.player)).set("choice", get.attitude(player, trigger.player) > 0 || player.hp > 1).forResult();
      } else {
        event.result = await player.chooseCard(get.prompt2(event.skill, trigger.player), "he", (card) => get.color(card) === "red").set(
          "canGive",
          (() => {
            const att = get.attitude(player, trigger.player) > 0;
            if (trigger.player.hp >= 3) {
              return att;
            }
            if (trigger.player.countCards("h") < 4) {
              return att;
            }
            return false;
          })()
        ).set("ai", (card) => {
          if (get.event().canGive) {
            return 6 - get.value(card);
          }
          return 0;
        }).forResult();
      }
      event.result.targets = [trigger.player];
    },
    async content(event, trigger, player) {
      player.changeZhuanhuanji(event.name);
      if (player.storage[event.name]) {
        await player.showCards(`${get.translation(player)}发动了【谦守】`, event.cards);
        await player.give(event.cards, event.targets[0]);
        player.addTempSkill("tyqianshou_use");
        for (const target of [player].concat(event.targets)) {
          target.addTempSkill("tyqianshou_target");
        }
      } else {
        const target = event.targets[0];
        const result = await target.chooseCard("he", true, `交给${get.translation(player)}一张牌，若不为黑色其失去1点体力`).set("att", get.attitude(target, player)).set("ai", (card) => {
          const att = _status.event.att;
          let val = 7 - get.value(card);
          if (get.color(card) === "black") {
            val += att;
          }
          return val;
        }).forResult();
        await target.showCards(`${get.translation(player)}发动了【谦守】`, result.cards);
        await target.give(result.cards, player);
        if (!player.getHistory("gain", (evt) => evt?.cards?.includes(result.cards[0]) && evt.getParent(event.name) === event).length) {
          return;
        }
        if (get.color(result.cards[0]) !== "black") {
          await player.loseHp();
        }
      }
    },
    subSkill: {
      use: {
        mark: true,
        marktext: '<span style="text-decoration: line-through;">谦</span>',
        mod: {
          cardEnabled2(card) {
            if (get.position(card) === "h") {
              return false;
            }
          }
        },
        charlotte: true,
        intro: {
          content: "不能使用或打出手牌"
        }
      },
      target: {
        charlotte: true,
        mark: true,
        marktext: '<span style="text-decoration: line-through;">守</span>',
        intro: { content: "本回合无法成为牌的目标" },
        mod: { targetEnabled: () => false }
      }
    }
  },
  tytanlong: {
    enable: "phaseUse",
    usable(skill, player) {
      return 1 + game.countPlayer((current) => current.isLinked());
    },
    filter(event, player) {
      return game.hasPlayer((current) => player.canCompare(current));
    },
    filterTarget(event, player, target) {
      return player.canCompare(target);
    },
    async content(event, trigger, player) {
      const { target } = event;
      const next = player.chooseToCompare(target);
      if (get.attitude(player, target) > 0) {
        next.set("small", true);
      }
      const result = await next.forResult();
      if (result.tie) {
        return;
      }
      const winner = result.bool ? player : target;
      const card = result[result.bool ? "target" : "player"];
      if (winner?.isIn() && card && [card].filterInD("d")) {
        let bool = get.attitude(winner, player) > 0;
        if (winner.getUseValue(card) >= 4) {
          bool = true;
        }
        const result2 = await winner.chooseBool(`是否获得${get.translation(card)}并视为对自己使用一张【铁索连环】？`).set("choice", bool).forResult();
        if (!result2.bool) {
          return;
        }
        await winner.gain(card, "gain2");
        const cardx = { name: "tiesuo", isCard: true };
        if (winner.canUse(cardx, winner)) {
          await winner.useCard(cardx, winner);
        }
      }
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          return get.effect(target, { name: "tiesuo" }, target, target);
        }
      }
    }
  },
  tyxibei: {
    trigger: {
      global: ["gainAfter", "loseAsyncAfter"]
    },
    getIndex(event, player) {
      if (!event.getg) {
        return [];
      }
      return game.filterPlayer((current) => {
        if (current === player) {
          return false;
        }
        if (event.name === "gain") {
          return event.getg(current)?.length && event.notFromCardpile;
        }
        return event.getg(current)?.some((card) => card.original !== "c");
      }).sortBySeat();
    },
    logTarget(event, player, name, target) {
      return target;
    },
    frequent: true,
    async content(event, trigger, player) {
      await player.draw("nodelay");
      if (!player.isPhaseUsing()) {
        return;
      }
      const result = await player.chooseCard("h", "是否展示一张锦囊牌，令此牌视为【火烧连营】？", (card) => get.type2(card) === "trick").set("ai", (card) => {
        const player2 = get.player();
        if (player2.getUseValue("huogong") > 0) {
          return 6 - get.value(card);
        }
        return 0;
      }).forResult();
      if (!result.bool) {
        return;
      }
      game.broadcastAll((cards2) => {
        cards2.forEach((card) => card.addGaintag("tyxibei"));
      }, result.cards);
      player.addTempSkill("tyxibei_viewAs");
    },
    group: "tyxibei_record",
    subSkill: {
      record: {
        trigger: { global: "gainBefore" },
        direct: true,
        filter(event, player) {
          if (player === event.player) {
            return false;
          }
          if (event.cards?.length) {
            if (event.getParent().name === "draw") {
              return false;
            }
            for (const card of event.cards) {
              if (get.position(card) !== "c" || !get.position(card) && card.original !== "c") {
                return true;
              }
            }
          }
          return false;
        },
        async content(event, trigger) {
          trigger.notFromCardpile = true;
        }
      },
      viewAs: {
        mod: {
          cardname(card, player) {
            if (card.hasGaintag("tyxibei")) {
              return "lx_huoshaolianying";
            }
          }
        },
        charlotte: true,
        onremove(player) {
          player.removeGaintag("tyxibei");
        }
      }
    }
  },
  //神刘
  tylongnu: {
    mark: true,
    zhuanhuanji: true,
    marktext: "☯",
    intro: {
      content(storage) {
        if (storage) {
          return "出牌阶段开始时，你可以减少1点体力上限并摸一张牌，然后本阶段内你可以将锦囊牌当作无次数限制雷【杀】使用或打出";
        }
        return "锁定技，出牌阶段开始时，你可以失去1点体力并摸一张牌，然后本阶段内你可以将红色手牌当作无距离限制的火【杀】使用或打出";
      }
    },
    audio: "nzry_longnu",
    trigger: {
      player: "phaseUseBegin"
    },
    async content(event, trigger, player) {
      player.changeZhuanhuanji("tylongnu");
      await player.draw();
      if (!player.storage.tylongnu) {
        await player.loseMaxHp();
        player.addTempSkill("tylongnu_yang", "phaseUseAfter");
      } else {
        await player.loseHp();
        player.addTempSkill("tylongnu_yin", "phaseUseAfter");
      }
    },
    group: "tylongnu_change",
    subSkill: {
      change: {
        audio: "nzry_longnu",
        trigger: {
          global: "phaseBefore",
          player: "enterGame"
        },
        filter(event, player) {
          return event.name !== "phase" || game.phaseNumber === 0;
        },
        prompt2(event, player) {
          return `切换【龙怒】为状态${player.storage.tylongnu ? "阴" : "阳"}`;
        },
        check: () => Math.random() > 0.5,
        async content(event, trigger, player) {
          player.changeZhuanhuanji("tylongnu");
        }
      },
      yang: {
        mod: {
          cardUsable(card, player) {
            if (card?.storage?.tylongnu) {
              return Infinity;
            }
          }
        },
        charlotte: true,
        locked: false,
        audio: "nzry_longnu",
        enable: ["chooseToUse", "chooseToRespond"],
        filterCard(card, player) {
          return get.type2(card) === "trick";
        },
        position: "hes",
        viewAs: {
          name: "sha",
          nature: "thunder",
          storage: {
            tylongnu: true
          }
        },
        viewAsFilter(player) {
          if (!player.countCards("hes", (card) => get.type2(card) === "trick")) {
            return false;
          }
        },
        prompt: "将一张锦囊牌当雷【杀】使用或打出",
        check(card) {
          const val = get.value(card);
          return 5 - val;
        },
        ai: {
          effect: {
            target(card, player, target, current) {
              if (get.tag(card, "respondSha") && current < 0) {
                return 0.6;
              }
            }
          },
          respondSha: true
        }
      },
      yin: {
        mod: {
          targetInRange(card) {
            if (card?.storage?.tylongnu) {
              return true;
            }
          }
        },
        charlotte: true,
        locked: false,
        audio: "nzry_longnu",
        enable: ["chooseToUse", "chooseToRespond"],
        filterCard(card, player) {
          return get.color(card) === "red";
        },
        position: "hs",
        viewAs: {
          name: "sha",
          nature: "fire",
          storage: {
            tylongnu: true
          }
        },
        viewAsFilter(player) {
          if (!player.countCards("hs", { color: "red" })) {
            return false;
          }
        },
        prompt: "将一张红色手牌当火【杀】使用或打出",
        check(card) {
          const val = get.value(card);
          return 5 - val;
        },
        ai: {
          effect: {
            target(card, player, target, current) {
              if (get.tag(card, "respondSha") && current < 0) {
                return 0.6;
              }
            }
          },
          respondSha: true
        }
      }
    },
    ai: {
      fireAttack: true,
      halfneg: true,
      threaten: 1.05
    }
  },
  tytaoyuan: {
    enable: "phaseUse",
    usable: 1,
    filterCard: true,
    selectCard: 2,
    position: "he",
    filterTarget: true,
    check(card) {
      return 4 - get.value(card);
    },
    async content(event, trigger, player) {
      const card = game.createCard("taoyuan", "heart", 1);
      if (card) {
        await event.target.gain(card, "gain2");
      }
    },
    ai: {
      order: 4,
      result: {
        target(player, target) {
          if (target.getUseValue("taoyuan") * get.sgnAttitude(player, target) >= player.getUseValue("wuzhong")) {
            return 1;
          }
          return 0;
        }
      }
    }
  },
  //关银屏
  tywuji: {
    skillAnimation: true,
    animationColor: "orange",
    audio: "wuji",
    trigger: { player: "phaseJieshuBegin" },
    forced: true,
    juexingji: true,
    filter(event, player) {
      return player.getStat("damage") >= 3;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.gainMaxHp();
      await player.recover();
      await player.removeSkills(["huxiao", "draghuxiao"]);
      const result = await player.chooseControl("获得青龙刀", "摸两张牌").set("prompt", "武继：选择一项").set("ai", () => 1).forResult();
      if (result.index === 0) {
        const card = game.createCard("qinglong", "spade", 5);
        if (card) {
          await player.gain(card, "gain2", "log");
        }
      } else {
        await player.draw(2);
      }
    }
  },
  //沙和尚
  tymanyong: {
    onremove: true,
    trigger: {
      player: ["phaseZhunbeiBegin", "phaseJieshuBegin"]
    },
    filter(event, player) {
      const hasCard = player.getEquips("tiejili").length > 0;
      if (event.name === "phaseZhunbei") {
        return !hasCard;
      }
      return hasCard;
    },
    async content(event, trigger, player) {
      if (trigger.name === "phaseZhunbei") {
        const card = game.createCard("tiejili", "spade", 5);
        if (card) {
          player.$gain2(card);
          await player.chooseUseTarget(card, true);
        }
      } else {
        const cards2 = player.getEquips("tiejili");
        if (cards2?.length) {
          await player.discard(cards2);
        }
      }
    }
  },
  //关兴
  tyconglong: {
    trigger: {
      global: ["useCard", "damageBegin1", "phaseEnd"]
    },
    filter(event, player) {
      if (event.name === "phase") {
        let num = 0;
        player.getHistory("lose", (evt) => {
          if (evt.type === "discard") {
            num += evt.cards2.length;
          }
        });
        return num >= 2;
      }
      if (!event.card || event.card.name !== "sha" || get.color(event.card) !== "red") {
        return false;
      }
      return player.countCards("he", (card) => get.type2(card) === (event.name === "damage" ? "equip" : "trick"));
    },
    frequent: true,
    async cost(event, trigger, player) {
      if (trigger.name === "phase") {
        event.result = await player.chooseBool(get.prompt(event.skill), "摸一张牌").set("frequentSkill", event.skill).forResult();
      } else {
        const eff1 = get.damageEffect(trigger.player, trigger.source, player);
        const eff2 = get.attitude(player, trigger.player);
        event.result = await player.chooseToDiscard("he", (card) => {
          const type = _status.event.typex;
          return get.type2(card) === type;
        }).set("typex", trigger.name === "damage" ? "equip" : "trick").set("prompt", get.prompt(event.skill)).set("prompt2", trigger.name === "damage" ? "令此伤害+1" : "令此牌不可响应").set("eff", trigger.name === "damage" ? eff1 : eff2).set("ai", (card) => {
          if (get.event().eff <= 0) {
            return 0;
          }
          if (get.color(card) === "red") {
            return 4 - get.value(card);
          }
          return 8 - get.value(card);
        }).set("chooseonly", true).forResult();
      }
    },
    async content(event, trigger, player) {
      if (trigger.name === "phase") {
        await player.draw();
      } else {
        await player.discard(event.cards);
        if (trigger.name === "damage") {
          trigger.num++;
        } else {
          trigger.directHit.addArray(game.players);
        }
      }
    }
  },
  tyzhaowu: {
    trigger: { player: "damageEnd" },
    filter(event, player) {
      if (!player.countCards("he")) {
        return false;
      }
      return event.source && event.source !== player;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard("he", get.prompt2(event.skill, trigger.source)).set("ai", (card) => {
        const player2 = get.player();
        const target = get.event().getTrigger().source;
        if (get.attitude(player2, target) >= 0 || player2.getStorage("tyzhaowu").includes(target)) {
          return 0;
        }
        return 7 - get.value(card);
      }).set("chooseonly", true).forResult();
      event.result.targets = [trigger.source];
    },
    async content(event, trigger, player) {
      await player.discard(event.cards);
      player.addTempSkill("tyzhaowu_wusheng", "roundStart");
      player.markAuto("tyzhaowu_wusheng", event.targets);
    },
    subSkill: {
      wusheng: {
        mark: true,
        intro: {
          content: "本轮可对$使用父亲的力量"
        },
        charlotte: true,
        onremove: true,
        mod: {
          targetInRange(card, player, target) {
            if (player.getStorage("tyzhaowu_wusheng").includes(target)) {
              return true;
            }
          },
          playerEnabled(card, player, target) {
            if (player.getStorage("tyzhaowu_wusheng").includes(target)) {
              return;
            }
            if (card.storage?.tyzhaowu) {
              return false;
            }
          }
        },
        locked: false,
        audio: "new_rewusheng",
        enable: "chooseToUse",
        filterCard(card, player) {
          return get.color(card) === "red";
        },
        position: "hes",
        viewAs: {
          name: "sha",
          storage: {
            tyzhaowu: true
          }
        },
        viewAsFilter(player) {
          if (!player.countCards("hes", { color: "red" })) {
            return false;
          }
        },
        prompt: "将一张红色牌当杀使用或打出",
        check(card) {
          const val = get.value(card);
          return 5 - val;
        },
        ai: {
          respondSha: true,
          skillTagFilter(player) {
            if (!player.countCards("hes", { color: "red" })) {
              return false;
            }
          }
        }
      }
    }
  },
  //侍从
  tyjinzhong: {
    trigger: {
      player: ["phaseUseBegin", "damageEnd"]
    },
    filter(event, player) {
      if (game.hasPlayer((i) => i.getSeatNum() === 1 || get.nameList(i).some((name) => get.rawName(name) === "刘备"))) {
        return true;
      }
      if (player.countCards("h")) {
        return true;
      }
      return false;
    },
    seatRelated: true,
    async cost(event, trigger, player) {
      const result = await player.chooseControl("选项一", "选项二", "cancel2").set("choiceList", ["失去1点体力，令一号位或“刘备”回复1点体力", "交给一名角色至多两张手牌"]).set("prompt", get.prompt(event.skill)).set(
        "choice",
        (() => {
          const targets = game.filterPlayer((i) => i.getSeatNum() === 1 || get.nameList(i).some((name) => get.rawName(name) === "刘备"));
          if (targets?.length && targets.some((i) => get.attitude(player, i) > 0 && i.hp <= player.hp && i.isDamaged())) {
            return "选项一";
          }
          if (game.hasPlayer((i) => get.attitude(player, i) > 0 && player.countCards("h") > Math.min(2, player.hp))) {
            return "选项二";
          }
          return "cancel2";
        })()
      ).set("ai", () => get.event().choice).forResult();
      if (result.control === "cancel2") {
        event.result = { bool: false };
        return;
      }
      if (result.control === "选项一") {
        event.result = await player.chooseTarget("尽忠：是否失去1点体力并令一号位或“刘备”回复1点体力？", (card, player2, target) => target.getSeatNum() === 1 || get.nameList(target).some((name) => get.rawName(name) === "刘备")).set("ai", (target) => {
          const player2 = get.player();
          if (get.attitude(player2, target) <= 0) {
            return 0;
          }
          return player2.hp + 1 - target.hp;
        }).forResult();
      } else {
        event.result = await player.chooseCardTarget({
          filterCard: true,
          selectCard: [1, 2],
          position: "h",
          filterTarget: lib.filter.notMe,
          prompt: "尽忠：是否交给一名其他角色至多两张牌？",
          ai1(card) {
            return 8 - get.value(card);
          },
          ai2(target) {
            const player2 = _status.event.player;
            const card = ui.selected.cards[0];
            const att = get.attitude(player2, target);
            if (att <= 0) {
              return 0;
            }
            return target.getUseValue(card) + 4;
          }
        }).forResult();
      }
    },
    async content(event, trigger, player) {
      if (event.cards?.length > 0) {
        await player.give(event.cards, event.targets[0]);
      } else {
        await player.loseHp();
        await event.targets[0].recover();
      }
    }
  },
  //吴班
  tyyoujun: {
    audio: "dcyouzhan",
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return target.countGainableCards(player, "he") && target !== player;
    },
    async content(event, trigger, player) {
      const target = event.target;
      await player.gainPlayerCard(target, "he", true);
      const result = await target.chooseBool(`是否令你所有手牌视为杀，然后视为对${get.translation(player)}使用决斗？`).set("choice", get.effect(player, { name: "juedou" }, target, target) > 0).forResult();
      if (result.bool) {
        target.addTempSkill("tyyoujun_sha");
        const card = { name: "juedou", isCard: true };
        if (target.canUse(card, player)) {
          await target.useCard(card, player);
        }
      }
    },
    subSkill: {
      sha: {
        charlotte: true,
        mod: {
          cardname(card) {
            return "sha";
          }
        }
      }
    },
    ai: {
      order: 5,
      result: {
        target(player, target) {
          if (player.hp === 1) {
            return 0;
          }
          return get.effect(target, { name: "shunshou_copy2" }, player, target);
        }
      }
    }
  },
  tyjicheng: {
    skillAnimation: true,
    animationColor: "fire",
    limited: true,
    trigger: {
      player: "damageEnd"
    },
    filter(event, player) {
      if (player.hp > 2) {
        return false;
      }
      return event.card && get.type(event.card) === "trick";
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.chooseDrawRecover(2, true);
    }
  },
  //黄忠
  tyyizhuang: {
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player) {
      return player.countCards("j");
    },
    check(event, player) {
      return player.hp > 1 && player.countCards("j", (card) => card.viewAs || card.name !== "xumou_jsrg");
    },
    async content(event, trigger, player) {
      await player.damage();
      await player.discardPlayerCard(player, "j", true, player.countCards("j"));
    }
  },
  //廖化
  tydangxian: {
    trigger: { player: "phaseBegin" },
    forced: true,
    audio: "dangxian",
    async content(event, trigger, player) {
      const cards2 = Array.from(ui.discardPile.childNodes).filter((card) => card.name === "sha");
      if (cards2.length) {
        const result = await player.chooseButton(["获得一张杀", cards2], true).forResult();
        if (result?.bool && result?.links?.length) {
          await player.gain(result.links, "gain2");
        }
      }
      game.updateRoundNumber();
      trigger.phaseList.splice(trigger.num, 0, `phaseUse|${event.name}`);
    }
  },
  tyfuli: {
    audio: "xinfuli",
    skillAnimation: true,
    animationColor: "soil",
    limited: true,
    enable: "chooseToUse",
    filter(event, player) {
      if (event.type !== "dying") {
        return false;
      }
      if (player !== event.dying) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.recoverTo(2);
      await player.drawTo(2);
    },
    ai: {
      order: 3,
      save: true,
      skillTagFilter(player, arg, target) {
        return player === target;
      },
      result: { player: 10 },
      threaten(player, target) {
        if (!target.storage.tyfuli) {
          return 0.9;
        }
      }
    }
  },
  //冯习
  tyqingkou: {
    audio: "twqingkou",
    trigger: {
      player: "phaseJieshuBegin"
    },
    frequent: true,
    async content(event, trigger, player) {
      const result = (await player.draw("bottom").forResult()).cards;
      await player.showCards(`${get.translation(player)}发动了【轻寇】`, result);
      if (result?.length !== 1) {
        return;
      }
      const list = [];
      const card = result[0];
      for (const name of lib.inpile) {
        if (get.type(name) === "trick" && get.cardNameLength(name) === player.hp) {
          list.push(["锦囊", "", name]);
        }
      }
      list.push(["基本", "", "sha"]);
      const result2 = await player.chooseButton([`是否将${get.translation(result)}当作其中一张使用？`, [list, "vcard"]]).set("filterButton", (button) => {
        const card2 = get.autoViewAs({ name: button.link[2], nature: button.link[3] }, get.event().resultCard);
        return get.player().hasUseTarget(card2);
      }).set("resultCard", [card]).set("ai", (button) => {
        const card2 = get.autoViewAs({ name: button.link[2], nature: button.link[3] }, get.event().resultCard);
        return get.player().getUseValue(card2);
      }).forResult();
      if (result2.bool && player.getCards("h").includes(card)) {
        const cardx = { name: result2.links[0][2], nature: result2.links[0][3] };
        game.broadcastAll((card2) => {
          lib.skill.tyqingkou_backup.viewAs = card2;
          lib.skill.tyqingkou_backup.prompt = `是否将此牌当作${get.translation(card2)}使用？`;
        }, cardx);
        const next = player.chooseToUse();
        next.set("cards", result);
        next.set("openskilldialog", `是否将此牌当作${get.translation(cardx)}使用？`);
        next.set("norestore", true);
        next.set("_backupevent", "tyqingkou_backup");
        next.set("custom", {
          add: {},
          replace: { window() {
          } }
        });
        next.backup("tyqingkou_backup");
        await next;
      }
    },
    subSkill: {
      backup: {
        filterCard(card) {
          return get.itemtype(card) === "card" && get.event().cards.includes(card);
        },
        position: "h",
        selectCard: 1,
        check: (card) => 7 - get.value(card),
        popname: true
      }
    }
  },
  //张南
  tyfenwu: {
    audio: "twfenwu",
    trigger: { player: "phaseZhunbeiBegin" },
    frequent: true,
    async content(event, trigger, player) {
      const result = (await player.draw().forResult()).cards;
      if (get.itemtype(result) !== "cards") {
        return;
      }
      await player.showCards(`${get.translation(player)}发动了【奋武】`, result);
      if (result?.length !== 1) {
        return;
      }
      const list = [];
      const card = result[0];
      for (const name of lib.inpile) {
        if (get.type(name) === "basic" && get.cardNameLength(name) === get.cardNameLength(card)) {
          list.push(["基本", "", name]);
          if (name === "sha") {
            for (const nature of lib.inpile_nature) {
              list.push(["基本", "", name, nature]);
            }
          }
        }
      }
      list.push(["锦囊", "", "juedou"]);
      const result2 = await player.chooseButton([`是否将${get.translation(result)}当作其中一张使用？`, [list, "vcard"]]).set("filterButton", (button) => {
        const card2 = get.autoViewAs({ name: button.link[2], nature: button.link[3] }, get.event().resultCard);
        return get.player().hasUseTarget(card2);
      }).set("resultCard", [card]).set("ai", (button) => {
        const card2 = get.autoViewAs({ name: button.link[2], nature: button.link[3] }, get.event().resultCard);
        return get.player().getUseValue(card2);
      }).forResult();
      if (result2.bool && result2?.links?.length && player.getCards("h").includes(card)) {
        const cardx = { name: result2.links[0][2], nature: result2.links[0][3] };
        game.broadcastAll((card2) => {
          lib.skill.tyfenwu_backup.viewAs = card2;
          lib.skill.tyfenwu_backup.prompt = `是否将此牌当作${get.translation(card2)}使用？`;
        }, cardx);
        const next = player.chooseToUse();
        next.set("cards", result);
        next.set("openskilldialog", `是否将此牌当作${get.translation(cardx)}使用？`);
        next.set("norestore", true);
        next.set("_backupevent", "tyfenwu_backup");
        next.set("custom", {
          add: {},
          replace: { window() {
          } }
        });
        next.backup("tyfenwu_backup");
        await next;
      }
    },
    subSkill: {
      backup: {
        filterCard(card) {
          return get.itemtype(card) === "card" && get.event().cards.includes(card);
        },
        position: "h",
        selectCard: 1,
        check: (card) => 7 - get.value(card),
        popname: true,
        log: false
      }
    }
  },
  //赵融
  tyyuantao: {
    trigger: {
      global: "useCard"
    },
    usable: 1,
    filter(event, player) {
      if (!event.targets.length) {
        return false;
      }
      return get.type(event.card) === "basic";
    },
    check(event, player) {
      return get.effect(event.targets[0], event.card, event.player, player) > 0 && player.hp > 2;
    },
    async content(event, trigger, player) {
      trigger.effectCount++;
      player.when({ global: "phaseEnd" }).step(async () => {
        await player.loseHp();
      });
    }
  },
  //程畿
  tyzhongen: {
    trigger: {
      global: "phaseJieshuBegin"
    },
    filter(event, player) {
      return player.getHistory("gain").length + player.getHistory("lose", (evt) => evt.hs?.length).length;
    },
    async cost(event, trigger, player) {
      const target = trigger.player;
      const goon = player.hasCard((card) => {
        if (_status.connectMode) {
          return true;
        }
        return get.name(card, player) === "sha" && game.checkMod(card, player, "unchanged", "cardEnabled2", player) !== false && lib.filter.targetEnabled2(get.autoViewAs({ name: "wuzhong" }, [card]), player, target);
      }, "hs");
      const list = [];
      if (goon) {
        list.push("选项一");
      }
      list.addArray(["选项二", "cancel2"]);
      const result = await player.chooseControl(list).set("choiceList", [`将一张【杀】当【无中生有】对${get.translation(target)}使用`, `使用一张无距离限制的【杀】`]).set("ai", () => {
        const player2 = get.event().player;
        const target2 = get.event().getTrigger().player;
        return get.effect(target2, { name: "wuzhong" }, player2, player2) > player2.getUseValue({ name: "sha" }) ? 0 : 1;
      }).forResult();
      event.result = {
        bool: result.control !== "cancel2",
        skill_popup: false,
        cost_data: result.control
      };
    },
    async content(event, trigger, player) {
      const index = event.cost_data;
      const target = trigger.player;
      if (index === "选项一") {
        const { bool, cards: cards2 } = await player.chooseCard(
          "hes",
          true,
          (card, player2) => {
            if (get.name(card, player2) !== "sha") {
              return false;
            }
            if (!game.checkMod(card, player2, "unchanged", "cardEnabled2", player2)) {
              return false;
            }
            return lib.filter.targetEnabled2(get.autoViewAs({ name: "wuzhong" }, [card]), player2, get.event().target);
          },
          `将一张【杀】当作【无中生有】对${get.translation(target)}使用`
        ).set("ai", (card) => {
          const player2 = get.event().player;
          const target2 = get.event().target;
          return get.effect(target2, get.autoViewAs({ name: "wuzhong" }, [card]), player2, player2) / Math.max(1, get.value(card));
        }).set("target", target).forResult();
        if (bool) {
          player.logSkill(event.name, target);
          await player.useCard({ name: "wuzhong" }, cards2, target, false);
        }
      } else {
        await player.chooseToUse(function(card, player2, event2) {
          if (get.name(card) !== "sha") {
            return false;
          }
          return lib.filter.filterCard.apply(this, arguments);
        }, "忠恩：是否使用一张【杀】？").set("targetRequired", true).set("complexSelect", true).set("filterTarget", function(card, player2, target2) {
          return lib.filter.targetEnabled.apply(this, arguments);
        }).set("logSkill", event.name).set("addCount", false).forResult();
      }
    }
  },
  tyliebao: {
    trigger: { global: "useCardToTarget" },
    filter(event, player) {
      if (event.card.name !== "sha" || event.targets?.includes(player)) {
        return false;
      }
      if (!event.target.isMinHandcard()) {
        return false;
      }
      return lib.filter.targetEnabled(event.card, event.player, player);
    },
    check(event, player) {
      return get.attitude(player, event.target) > 0 && (player.hp > 2 || player.countCards("h", "shan"));
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const target = event.targets[0];
      const evt = trigger.getParent();
      evt.triggeredTargets2.remove(target);
      evt.targets.remove(target);
      evt.targets.push(player);
      await player.draw();
      target.when({ global: "useCardAfter" }).filter((evt2) => evt2.card === trigger.card).step(async () => {
        if (!player.hasHistory("damage", (evtx) => evtx.getParent("useCard") === evt) && target.isDamaged()) {
          await target.recover();
        }
      });
    }
  },
  //龙果子
  tyzhuan: {
    audio: "qingbei",
    enable: "phaseUse",
    filterCard(card, player) {
      return get.name(card, player) === "sha";
    },
    filterTarget(card, player, target) {
      return target !== player && target.countCards("ej", (card2) => get.type(card2, target) === "equip");
    },
    check(card) {
      return 5 - get.value(card);
    },
    filter(event, player) {
      if (!player.countCards("he", (card) => lib.skill.tyzhuan.filterCard(card, player))) {
        return false;
      }
      return game.hasPlayer((target) => lib.skill.tyzhuan.filterTarget(event, player, target));
    },
    async content(event, trigger, player) {
      const target = event.target;
      if (!target.countCards("ej", (card) => get.type(card, target) === "equip")) {
        return;
      }
      await player.gainPlayerCard("ej", target, true).set("filterButton", (button) => get.type(button.link, get.owner(button.link)) === "equip");
    },
    ai: {
      order(item, player) {
        if (!player.hasCard((card) => player.hasValueTarget(card), "h")) {
          return 9;
        }
        return 1;
      },
      result: {
        target: -1
      }
    },
    subfrequent: ["draw"],
    group: "tyzhuan_draw",
    subSkill: {
      draw: {
        audio: "tyzhuan",
        trigger: { global: "useCardAfter" },
        filter(event, player) {
          return get.type(event.card, null, false) === "equip";
        },
        frequent: true,
        prompt2: "摸一张牌",
        async content(event, trigger, player) {
          await player.draw({ nodelay: true });
        }
      }
    }
  },
  //龙刘备
  tyqingshi: {
    trigger: { player: "phaseZhunbeiBegin" },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget([1, player.hp], get.prompt2(event.skill), (card, player2, target) => target.countCards("h")).set("ai", (target) => Math.max(0.1, get.attitude(get.player(), target))).forResult();
    },
    async content(event, trigger, player) {
      await player.chooseToDebate(event.targets.sortBySeat()).set("callback", lib.skill.tyqingshi.callback);
    },
    async callback(event, trigger, player) {
      const result = event.debateResult;
      if (result.bool && result.opinion) {
        if (!["red", "black"].includes(result.opinion)) {
          return;
        }
        const targets = result[result.opinion].map((i) => i[0]);
        if (result.opinion === "red") {
          for (const target of targets) {
            target.addTempSkill("tyqingshi_distance", "roundStart");
            target.addMark("tyqingshi_distance", 1, false);
          }
        } else {
          await player.draw(targets.length);
          const gains = [];
          const give_map = [];
          while (true) {
            const result2 = await player.chooseCardTarget({
              filterCard(card) {
                return get.itemtype(card) === "card" && !card.hasGaintag("mbjiejian_tag");
              },
              filterTarget(card, player2, target) {
                return get.event().canGain(target) && target !== player2;
              },
              prompt: "倾师：是否分配手牌？",
              prompt2: "请选择要分配的卡牌和目标",
              ai1(card) {
                return 8 - get.value(card);
              },
              ai2(target) {
                const player2 = _status.event.player;
                const card = ui.selected.cards[0];
                const att = get.attitude(player2, target);
                if (att <= 0) {
                  return 0;
                }
                return target.getUseValue(card) + 4;
              }
            }).set("canGain", (target) => targets.includes(target) && !gains.includes(target)).forResult();
            if (result2.bool && result2.targets?.length) {
              give_map.add([result2.targets[0], result2.cards]);
              player.addGaintag(result2.cards, "mbjiejian_tag");
              gains.addArray(result2.targets);
            } else {
              break;
            }
            if (!game.hasPlayer((i) => i !== player && targets.includes(i) && !gains.includes(i))) {
              break;
            }
          }
          await game.loseAsync({
            gain_list: give_map,
            player,
            cards: give_map.map((i) => i[1]).flat(),
            giver: player,
            animate: "giveAuto"
          }).setContent("gaincardMultiple");
        }
      }
    },
    subSkill: {
      distance: {
        charlotte: true,
        mod: {
          globalFrom(from, to, distance) {
            return distance + from.countMark("tyqingshi_distance");
          },
          globalTo(from, to, distance) {
            return distance + to.countMark("tyqingshi_distance");
          }
        },
        onremove: true,
        mark: true,
        intro: {
          content: "你与其他角色的相互距离+$"
        }
      }
    }
  },
  tyyilin: {
    trigger: {
      global: ["gainAfter", "loseAsyncAfter"]
    },
    getIndex(event, player) {
      if (event.name === "loseAsync" && event.type !== "gain") {
        return [];
      }
      if (!event.getl || !event.getg) {
        return [];
      }
      const cardsx = event.getl(player).cards2;
      const cardsy = event.getg(player);
      return game.filterPlayer((current) => {
        if (current === player) {
          return false;
        }
        if (player.getHistory("useSkill", (evt) => evt.skill === "tyyilin" && evt.targets?.includes(current)).length) {
          return false;
        }
        if (cardsx.length) {
          const cards2 = event.getg(current);
          if (cards2?.length && cards2.some((card) => cardsx.includes(card))) {
            return true;
          }
        }
        if (cardsy.length) {
          const evt = event.getl(current);
          if (evt?.cards2?.length && evt.cards2.some((card) => cardsy.includes(card))) {
            return true;
          }
        }
        return false;
      }).sortBySeat();
    },
    logTarget(event, player, name, target) {
      return target;
    },
    check(event, player, name, target) {
      return get.attitude(player, target) > 0;
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const cards1 = trigger.getl(player)?.cards2;
      const cards2 = trigger.getg(player);
      const cardsx = trigger.getl(target)?.cards2;
      const cardsy = trigger.getg(target);
      if (cards1?.some((card) => cardsy.includes(card))) {
        await target.chooseToUse({
          filterCard(card) {
            if (get.itemtype(card) !== "card" || !get.event().useCard.includes(card)) {
              return false;
            }
            return lib.filter.filterCard.apply(this, arguments);
          },
          prompt: "是否使用获得的一张牌？"
        }).set("useCard", cards1);
      }
      if (cardsx?.some((card) => cards2.includes(card))) {
        await player.chooseToUse({
          filterCard(card) {
            if (get.itemtype(card) !== "card" || !get.event().useCard.includes(card)) {
              return false;
            }
            return lib.filter.filterCard.apply(this, arguments);
          },
          prompt: "是否使用获得的一张牌？"
        }).set("useCard", cardsx);
      }
    }
  },
  tychengming: {
    skillAnimation: true,
    animationColor: "fire",
    trigger: { player: "dying" },
    zhuSkill: true,
    filter(event, player) {
      if (player.hp > 0) {
        return false;
      }
      if (!player.hasZhuSkill("tychengming")) {
        return false;
      }
      return game.hasPlayer((current) => current !== player && current.group === "shu");
    },
    limited: true,
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => target !== player2 && target.group === "shu").set("ai", (target) => Math.max(1, get.attitude(get.player(), target))).forResult();
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const cards2 = player.getCards("hej");
      const target = event.targets[0];
      if (cards2.length) {
        await target.gain(cards2, "give");
      }
      await player.recoverTo(1);
      const skills2 = target.getSkills(null, false, false).filter((skill) => {
        const info = get.info(skill);
        if (!info || info.charlotte || !get.is.locked(skill) || get.skillInfoTranslation(skill, target).length === 0) {
          return false;
        }
        return true;
      });
      if (skills2.length) {
        await target.addSkills("rerende");
      }
    }
  },
  //蜀孙权-孩子们，其实我早就是蜀国人了
  tyfuhan: {
    trigger: {
      global: ["gainAfter", "loseAsyncAfter"]
    },
    getIndex(event, player) {
      if (event.name === "loseAsync" && event.type !== "gain") {
        return [];
      }
      if (!event.getl || !event.getg) {
        return [];
      }
      const cardsx = event.getl(player).cards2;
      const cardsy = event.getg(player);
      return game.filterPlayer((current) => {
        if (current === player) {
          return false;
        }
        if (cardsx.length) {
          const cards2 = event.getg(current);
          if (cards2?.length && cards2.some((card) => cardsx.includes(card))) {
            return true;
          }
        }
        if (cardsy.length) {
          const evt = event.getl(current);
          if (evt?.cards2?.length && evt.cards2.some((card) => cardsy.includes(card))) {
            return true;
          }
        }
        return false;
      }).sortBySeat();
    },
    filter(event, player, name, target) {
      if (!target.isIn()) {
        return false;
      }
      return true;
    },
    async cost(event, trigger, player) {
      const target = event.indexedData;
      const dialog = [get.prompt(event.skill, player, target)];
      const list1 = [];
      const list2 = [];
      let bool = false;
      for (let i = 1; i < 6; i++) {
        if (player.hasEnabledSlot(i)) {
          list1.push(i);
        }
        if (player.hasDisabledSlot(i)) {
          list2.push(i);
        }
      }
      if (list2.length && trigger.getl(player)?.cards2?.length && trigger.getg(target).some((card) => trigger.getl(player).cards2.includes(card))) {
        dialog.push("恢复其一个装备栏");
        dialog.push([list2.map((i) => [[i, true], `${get.translation(`equip${i}`)}栏`]), "tdnodes"]);
        bool = true;
      }
      if (list1.length && trigger.getg(player) && trigger.getl(target)?.cards2.some((card) => trigger.getg(player).includes(card))) {
        dialog.push("废除其一个装备栏");
        dialog.push([list1.map((i) => [[i, false], `${get.translation(`equip${i}`)}栏`]), "tdnodes"]);
        bool = true;
      }
      if (bool) {
        const result = await target.chooseButton(dialog).set("ai", (button) => {
          const type = button.link[1];
          if (_status.event.att > 0) {
            if (!_status.event.used || type) {
              return 1 + Math.random();
            }
            return 0;
          }
          return type ? 0 : 1 + Math.random();
        }).set("used", player.getHistory("useSkill", (evt) => evt.skill === "tyfuhan").length > 0).set("att", get.attitude(target, _status.currentPhase)).forResult();
        event.result = {
          bool: result.bool,
          targets: [target],
          cost_data: result.links?.[0]
        };
      } else {
        event.result = { bool: false };
      }
    },
    async content(event, trigger, player) {
      const cost = event.cost_data;
      if (cost[1]) {
        await player.enableEquip(cost[0]);
      } else {
        await player.disableEquip(cost[0]);
      }
    },
    group: "tyfuhan_draw",
    subSkill: {
      draw: {
        trigger: {
          global: "phaseEnd"
        },
        filter(event, player) {
          if (!_status.currentPhase || _status.currentPhase.countCards("h") >= _status.currentPhase.maxHp) {
            return false;
          }
          return player.getHistory("useSkill", (evt) => evt.skill === "tyfuhan").length;
        },
        forced: true,
        locked: true,
        logTarget: () => _status.currentPhase,
        async content(event, trigger, player) {
          await event.targets[0].drawTo(event.targets[0].maxHp);
        }
      }
    }
  },
  tychende: {
    enable: "phaseUse",
    filterCard: true,
    selectCard: [2, Infinity],
    filter(event, player) {
      return player.countCards("h") > 1;
    },
    check(card) {
      const player = get.player();
      if (ui.selected.cards.length >= 2) {
        return 0;
      }
      if (player.getUseValue(card)) {
        return 10 - get.value(card);
      }
      return 6 - get.value(card);
    },
    position: "h",
    lose: false,
    delay: false,
    discard: false,
    filterTarget: lib.filter.notMe,
    allowChooseAll: true,
    async content(event, trigger, player) {
      const target = event.target;
      const cards2 = event.cards;
      await player.showCards(`${get.translation(player)}发动了【臣德】`, cards2);
      await player.give(cards2, target, true);
      const list = [];
      for (const card of cards2) {
        if (player.hasUseTarget(card, true, true) && ["trick", "basic"].includes(get.type(card))) {
          list.push([get.type(card), "", get.name(card, false), get.nature(card, false)]);
        }
      }
      if (!list.length) {
        return;
      }
      const result = await player.chooseButton(["臣德：是否视为使用其中一张？", [list, "vcard"]]).set("ai", (button) => get.player().getUseValue(button.link[2])).forResult();
      if (result.bool) {
        await player.chooseUseTarget({ name: result.links[0][2], nature: result.links[0][3] }, true);
      }
    },
    ai: {
      order: 6,
      result: {
        target: 1
      }
    }
  },
  tywansu: {
    trigger: {
      global: ["useCard", "damageBefore"]
    },
    filter(event, player) {
      if (!event.card || !get.is.virtualCard(event.card)) {
        return false;
      }
      if (event.name === "useCard") {
        return game.players.some((target) => target.hasDisabledSlot());
      }
      return true;
    },
    forced: true,
    logTarget(event, player) {
      if (event.name === "useCard") {
        return game.players.filter((target) => target.hasDisabledSlot());
      }
      return event.player;
    },
    async content(event, trigger, player) {
      if (trigger.name === "useCard") {
        trigger.directHit.addArray(event.targets);
      } else {
        trigger.cancel();
        trigger.player.loseHp(trigger.num);
      }
    },
    ai: {
      jueqing: true
    }
  },
  //神秘将军-孩子们，其实我没有死
  tywusheng: {
    enable: ["chooseToRespond", "chooseToUse"],
    filterCard(card, player) {
      return get.color(card) === "red";
    },
    position: "hes",
    viewAs: {
      name: "sha",
      storage: {
        tywusheng: true
      }
    },
    viewAsFilter(player) {
      if (!player.countCards("hes", { color: "red" })) {
        return false;
      }
    },
    async precontent(event, trigger, player) {
      const targets = event.result.targets;
      for (const target of targets) {
        target.addTempSkill("tywusheng_guanjue");
      }
    },
    prompt: "将一张红色牌当杀使用或打出",
    check(card) {
      const val = get.value(card);
      if (_status.event.name === "chooseToRespond") {
        return 1 / Math.max(0.1, val);
      }
      return 5 - val;
    },
    ai: {
      skillTagFilter(player) {
        if (!player.countCards("hes", { color: "red" })) {
          return false;
        }
      },
      respondSha: true
    },
    subSkill: {
      guanjue: {
        mod: {
          cardEnabled(card, player) {
            let evt = _status.event;
            if (evt.name !== "chooseToUse") {
              evt = evt.getParent("chooseToUse");
            }
            if (!evt || !evt.respondTo) {
              return;
            }
            const cardx = evt.respondTo[1];
            if (!cardx.storage?.tywusheng) {
              return;
            }
            const suit = get.suit(card);
            if (suit !== "unsure" && suit !== get.suit(cardx)) {
              return false;
            }
          }
        },
        charlotte: true
      }
    }
  },
  tychengshi: {
    trigger: {
      source: "damageSource"
    },
    forced: true,
    usable: 1,
    filter(event, player) {
      if (!_status.currentPhase) {
        return false;
      }
      if (_status.currentPhase !== player && !event.player.isIn()) {
        return false;
      }
      return event.card?.name === "sha" && get.color(event.card) === "red";
    },
    async content(event, trigger, player) {
      if (player === _status.currentPhase) {
        const evt = trigger.getParent("useCard", true);
        if (evt?.addCount !== false) {
          evt.addCount = false;
          const stat = evt.player.getStat().card;
          const name = evt.card.name;
          if (typeof stat[name] === "number") {
            stat[name]--;
          }
        }
      } else if (trigger.player.isIn()) {
        trigger.player.addTempSkill("tychengshi_tiaoxin", { global: "phaseAnyEnd" });
        trigger.player.markAuto("tychengshi_tiaoxin", [player]);
      }
    },
    subSkill: {
      tiaoxin: {
        mark: true,
        intro: {
          content(storage, player) {
            if (!storage || !storage.length) {
              return "无记录";
            }
            if (storage.length > 1) {
              return "不能使用伤害类牌指定任何人";
            }
            return `不能使用伤害类牌指定${get.translation(storage)}以外的角色`;
          }
        },
        charlotte: true,
        onremove: true,
        mod: {
          playerEnabled(card, player, target) {
            if (!get.tag(card, "damage")) {
              return;
            }
            const storage = player.getStorage("tychengshi_tiaoxin");
            if (!storage.length) {
              return;
            }
            if (storage.length > 1 || !player.getStorage("tychengshi_tiaoxin").includes(target)) {
              return false;
            }
          }
        }
      }
    }
  },
  tyfuwei: {
    trigger: {
      global: "damageEnd"
    },
    filter(event, player) {
      if (!player.countCards("he") || !event.player.isIn() || event.player === player) {
        return false;
      }
      if (event.player.getSeatNum() === 1) {
        return true;
      }
      if (get.nameList(event.player).some((name) => get.rawName(name) === "刘备")) {
        return true;
      }
    },
    usable: 1,
    seatRelated: true,
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        prompt: get.prompt(event.skill, trigger.player),
        prompt2: `交给其至多${trigger.num}张牌，然后可以对伤害来源使用至多${trigger.num}张杀`,
        filterCard: true,
        selectCard: [1, trigger.num],
        position: "he",
        filterTarget(card, player2, target) {
          return target === _status.event.getTrigger().player;
        },
        ai1(card) {
          return 4 - get.value(card);
        },
        ai2(target) {
          const att = get.attitude(_status.event.player, target);
          return Math.max(0, att);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const cards2 = event.cards;
      await player.give(cards2, target);
      if (!trigger.source || !trigger.source.isIn() || !player.canUse(get.autoViewAs({ name: "sha" }, "unsure"), trigger.source, false)) {
        return;
      }
      let num = 0;
      while (num < trigger.num) {
        const result = await player.chooseToUse(
          function(card, player2, event2) {
            if (get.name(card) !== "sha") {
              return false;
            }
            return lib.filter.filterCard.apply(this, arguments);
          },
          `抚危：是否对${get.translation(trigger.source)}使用一张杀？（${num}/${trigger.num}）`
        ).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("filterTarget", function(card, player2, target2) {
          if (target2 !== _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
            return false;
          }
          return lib.filter.targetEnabled.apply(this, arguments);
        }).set("sourcex", trigger.source).forResult();
        if (result.bool === false) {
          break;
        }
        num++;
      }
    }
  }
};
const skills$k = {
  //【众】
  hm_zhong_heart_skill: {
    equipSkill: true,
    forced: true,
    trigger: {
      player: "damageEnd"
    },
    async content(event, trigger, player) {
      player.addSkill("hm_zhong_heart_skill_buff");
      player.addMark("hm_zhong_heart_skill_buff", player.countCards("e", { name: "hm_zhong_heart" }));
    },
    subSkill: {
      buff: {
        charlotte: true,
        mod: {
          attackRange(player, num) {
            return num + player.countMark("hm_zhong_heart_skill_buff");
          },
          targetInRange(card) {
            if (card.name === "sha") {
              return true;
            }
          }
        }
      }
    }
  },
  hm_zhong_diamond_skill: {
    equipSkill: true,
    forced: true,
    trigger: {
      player: "damageEnd"
    },
    async content(event, trigger, player) {
      await player.draw(2 * player.countCards("e", { name: "hm_zhong_diamond" }));
    },
    ai: {
      maixie: true,
      maixie_hp: true,
      effect: {
        target(card, player, target) {
          if (get.tag(card, "damage")) {
            if (player.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            if (!target.hasFriend()) {
              return;
            }
            const num = target.countCards("e", { name: "hm_zhong_diamond" });
            if (target.hp >= 4) {
              return [1, num * 2];
            }
            if (target.hp === 3) {
              return [1, num * 1.5];
            }
            if (target.hp === 2) {
              return [1, num * 0.5];
            }
          }
        }
      }
    }
  },
  hm_zhong_club_skill: {
    equipSkill: true,
    forced: true,
    trigger: {
      player: "damageEnd"
    },
    async content(event, trigger, player) {
      player.addSkill("hm_zhong_club_skill_buff");
      player.addMark("hm_zhong_club_skill_buff", player.countCards("e", { name: "hm_zhong_club" }));
    },
    subSkill: {
      buff: {
        charlotte: true,
        mod: {
          maxHandcard(player, num) {
            return num + player.countMark("hm_zhong_club_skill_buff");
          }
        }
      }
    }
  },
  hm_zhong_spade_skill: {
    equipSkill: true,
    forced: true,
    trigger: {
      player: "damageEnd"
    },
    async content(event, trigger, player) {
      player.addSkill("hm_zhong_spade_skill_buff");
      player.addMark("hm_zhong_spade_skill_buff", player.countCards("e", { name: "hm_zhong_spade" }));
    },
    subSkill: {
      buff: {
        charlotte: true,
        mod: {
          globalFrom(from, to, current) {
            return current - from.countMark("hm_zhong_club_skill_buff");
          },
          globalTo(from, to, current) {
            return current + to.countMark("hm_zhong_club_skill_buff");
          }
        }
      }
    }
  },
  //白绕
  hm_huoyin: {
    mod: {
      cardUsableTarget(card, player, target, result) {
        if (player.inRange(target) && target.inRange(player)) {
          if (card.name === "sha") {
            return true;
          }
        }
      }
    },
    group: "hm_huoyin_damageSource",
    subSkill: {
      damageSource: {
        trigger: {
          source: "damageSource"
        },
        filter(event) {
          const { source, player } = event;
          return player.inRange(source) && source.inRange(player);
        },
        forced: true,
        async content(event, trigger, player) {
          await player.draw();
          await trigger.player.chooseToUse("【祸引】：是否使用一张牌？");
        }
      }
    }
  },
  //唐周
  hm_jukou: {
    enable: "phaseUse",
    usable: 1,
    filterTarget: lib.filter.notMe,
    subSkill: {
      sha: {
        charlotte: true,
        mod: {
          cardEnabled(card, player) {
            if (card.name === "sha") {
              return false;
            }
          }
        }
      },
      handcard: {
        charlotte: true,
        mod: {
          cardEnabled(card) {
            if (get.position(card) === "h") {
              return false;
            }
          }
        }
      }
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const name = get.translation(target.name);
      const dialog = [
        "请选择一项",
        [
          [
            ["draw", `令${name}摸一张牌`],
            ["gain", `令${name}获得武将牌上所有的牌`]
          ],
          "textbutton"
        ]
      ];
      const next = player.chooseButton(dialog, true);
      next.set("ai", (button) => Math.random());
      next.set("targetx", target);
      next.set("filterButton", (button) => {
        const evt = _status.event;
        const { targetx } = evt;
        if (button.link === "gain") {
          return targetx.countCards("xs", (card) => !card._cardid) > 0;
        }
        return true;
      });
      const result = await next.forResult();
      if (result.bool) {
        switch (result.links[0]) {
          case "gain":
            await target.gain(
              target.getCards("xs", (card) => !card._cardid),
              "draw"
            );
            target.addTempSkill("hm_jukou_handcard");
            break;
          case "draw":
            await target.draw();
            target.addTempSkill("hm_jukou_sha");
            break;
        }
      }
    },
    ai: {
      order: 1,
      result: {
        target: 1
      },
      threaten: 1.5
    }
  },
  hm_weichenn: {
    limited: true,
    enable: "phaseUse",
    filterTarget: lib.filter.notMe,
    selectTarget: 2,
    targetprompt: ["展示手牌", "摸牌"],
    complexTarget: true,
    multitarget: true,
    async content(event, trigger, player) {
      const { targets } = event;
      player.awakenSkill(event.name);
      await player.showCards(targets[0].getCards("h"));
      await game.asyncDraw([player, targets[1]], 3);
      for (const target of targets) {
        target.addTempSkill("hm_weichenn_buff");
      }
      while (true) {
        const list = targets[0].getCards("h", (card) => get.is.damageCard(card));
        if (!list.some((card) => targets[0].canUse(card, targets[1], true))) {
          break;
        }
        const next2 = await targets[0].chooseToUse(
          function(card, player2, event2) {
            const bool = get.is.damageCard(card);
            if (!bool) {
              return false;
            }
            return lib.filter.cardEnabled.apply(this, arguments);
          },
          `违谶：对${get.translation(targets[1])}使用一张伤害牌`
        ).set("complexSelect", true).set("filterTarget", function(card, player2, target) {
          if (target !== _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
            return false;
          }
          return lib.filter.targetEnabled.apply(this, arguments);
        }).set("sourcex", targets[1]).set("forced", true);
        const result2 = await next2.forResult();
        if (!result2?.bool) {
          break;
        }
      }
    },
    subSkill: {
      buff: {
        charlotte: true,
        mod: {
          cardUsable(card, player, num) {
            return Infinity;
          }
        }
      }
    }
  },
  //浮云
  hm_shuiqu: {
    trigger: { global: "phaseDiscardBegin" },
    forced: true,
    filter(event, player) {
      const hs = player.getCards("h");
      if (!hs.length) {
        return false;
      }
      return hs.every((card) => lib.filter.cardDiscardable(card, player, "hm_shuiqu"));
    },
    async content(event, trigger, player) {
      await player.chooseToDiscard(true, "h", player.countCards("h"));
      let result;
      if (player.isDamaged()) {
        result = await player.chooseControl("baonue_hp", "baonue_maxHp", (event2, player2) => {
          if (player2.hp === player2.maxHp) {
            return "baonue_maxHp";
          }
          if (player2.hp < player2.maxHp - 1 || player2.hp <= 2) {
            return "baonue_hp";
          }
          return "baonue_hp";
        }).set("prompt", "随去：回复1点体力或加1点体力上限").forResult();
      } else {
        result = { control: "baonue_maxHp" };
      }
      if (!result?.control) {
        return;
      }
      if (result.control === "baonue_hp") {
        await player.recover();
      } else {
        await player.gainMaxHp();
      }
    }
  },
  hm_yure: {
    limited: true,
    trigger: {
      player: "loseAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player) {
      if (event.type !== "discard" || event.getlx === false || !game.hasPlayer((current) => player !== current)) {
        return false;
      }
      return event.getl?.(player)?.cards2?.someInD("d");
    },
    async cost(event, trigger, player) {
      const cards2 = trigger.getl(player).cards2.filterInD("d");
      event.result = await player.chooseTarget(lib.filter.notMe, get.prompt(event.skill), `将${get.translation(cards2)}交给一名其他角色`).set("ai", (target) => {
        const { player: player2, cards: cards3 } = get.event();
        if (cards3.length < 3) {
          return 0;
        }
        let att = get.attitude(player2, target);
        if (att < 3) {
          return 0;
        }
        if (target.hasSkillTag("nogain")) {
          att /= 10;
        }
        if (target.hasJudge("lebu")) {
          att /= 5;
        }
        if (target.hasSha() && cards3.some((card) => card.name === "sha")) {
          att /= 5;
        }
        if (target.needsToDiscard(1) && cards3.some((card) => card.name === "wuxie")) {
          att /= 5;
        }
        return att / (1 + get.distance(player2, target, "absolute"));
      }).set("cards", cards2).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      player.awakenSkill(event.name);
      await target.gain(trigger.getl(player).cards2.filterInD("d"), "gain2").set("giver", player);
    }
  },
  //陶升
  hm_zhannei: {
    limited: true,
    enable: "phaseUse",
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.addTempSkill("hm_zhannei_distance", { player: "dying" });
      player.storage.hm_zhannei_distance = event.targets[0];
    },
    subSkill: {
      distance: {
        charlotte: true,
        onremove: true,
        mod: {
          globalFrom(from, to) {
            if (from.storage.hm_zhannei_distance === to) {
              return -Infinity;
            }
          }
        }
      }
    }
  },
  hm_qianwei: {
    enable: "phaseUse",
    usable: 1,
    discard: false,
    filter(event, player) {
      return player.countCards("he", (card) => !get.is.damageCard(card));
    },
    filterCard(card) {
      return !get.is.damageCard(card);
    },
    position: "he",
    selectCard: [1, Infinity],
    filterTarget(_, player, target) {
      return player.distanceTo(target) === 1;
    },
    allowChooseAll: true,
    async content(event, trigger, player) {
      const { cards: cards2, target } = event;
      await player.showCards(cards2);
      await player.give(cards2, target, true);
      await player.draw(cards2.length);
      const list = [].concat(cards2);
      while (true) {
        if (!list.some((card) => target.hasUseTarget(card))) {
          break;
        }
        const next2 = target.chooseCardButton(list);
        next2.set("prompt", "选择一张牌使用之");
        next2.set("filterButton", (button) => get.player().hasUseTarget(button.link));
        const result2 = await next2.forResult();
        if (result2?.bool) {
          list.removeArray(result2.links);
          await target.chooseUseTarget(result2.links[0]);
        } else {
          break;
        }
      }
    }
  },
  //于毒
  hm_dafu: {
    trigger: { player: "useCardToPlayered" },
    prompt2(event, player) {
      const target = event.target;
      return `令${get.translation(target)}摸一张牌并令其不能响应此牌？`;
    },
    filter(event, player) {
      return get.is.damageCard(event.card);
    },
    check(event, player) {
      return get.attitude(player, event.target) < 0;
    },
    logTarget: "target",
    async content(event, trigger, player) {
      await trigger.target.draw();
      trigger.getParent().directHit.add(trigger.target);
    }
  },
  hm_jipin: {
    trigger: {
      source: "damageSource"
    },
    filter(event, player) {
      return event.player.countCards("h") > player.countCards("h");
    },
    async cost(event, trigger, player) {
      const next = player.gainPlayerCard(trigger.player, "h");
      const result = await next.forResult();
      event.result = {
        bool: result.bool,
        cards: result.cards,
        targets: [trigger.player]
      };
    },
    async content(event, trigger, player) {
      const next = player.chooseCardTarget();
      next.set("prompt", "将此牌交给一名其他角色");
      next.set("cardx", event.cards[0]);
      next.set("filterCard", (card) => {
        const evt = _status.event;
        return card === evt.cardx;
      });
      next.set("filterTarget", lib.filter.notMe);
      const result = await next.forResult();
      if (result.bool) {
        player.give(result.cards, result.targets[0], false);
      }
    }
  },
  //南华老仙
  hm_tianshu: {
    audio: "tianshu",
    trigger: { player: "phaseUseBegin" },
    filter(event, player) {
      return player.countCards("he") && !game.hasPlayer((current) => current.countCards("ej", "taipingyaoshu"));
    },
    async cost(event, trigger, player) {
      const next = player.chooseCardTarget({
        prompt: get.prompt2(event.skill),
        filterCard: true,
        filterTarget(card, player2, target) {
          return target.canEquip("taipingyaoshu");
        },
        position: "he",
        ai1(card) {
          return 5 - get.value(card);
        },
        ai2(target) {
          const player2 = _status.event.player;
          if (get.attitude(player2, target) > 0 && !target.hasEmptySlot(2)) {
            return 0;
          }
          return get.attitude(player2, target);
        }
      });
      const result = await next.forResult();
      event.result = result;
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      let card;
      player.discard(event.cards);
      if (!lib.inpile.includes("taipingyaoshu")) {
        lib.inpile.push("taipingyaoshu");
        card = game.createCard2("taipingyaoshu", "heart", 3);
      } else {
        card = get.cardPile((card2) => card2.name === "taipingyaoshu");
      }
      if (card) {
        target.equip(card);
      }
    }
  },
  hm_yufeng: {
    usable: 1,
    enable: "phaseUse",
    async content(event, trigger, player) {
      const next = player.judge((card) => {
        if (["diamond", "club"].includes(get.suit(card))) {
          return 2;
        }
        return 0;
      });
      const { suit } = await next.forResult();
      switch (suit) {
        case "spade":
          {
            if (!game.hasPlayer((current) => player !== current)) {
              return;
            }
            const result = await player.chooseTarget("【御风】：令一名其他角色跳过其下个回合的出牌阶段和弃牌阶段", lib.filter.notMe, true).set("ai", (target) => {
              const player2 = get.player();
              const att = get.attitude(player2, target);
              return att * lib.skill.yijin.getValue(player2, "yijin_jinmi", target);
            }).forResult();
            if (result?.bool && result?.targets?.length) {
              const {
                targets: [target]
              } = result;
              target.skip("phaseUse");
              target.skip("phaseDiscard");
              target.addTempSkill(`${event.name}_skipUse`, { player: ["phaseUseSkipped", "phaseDiscardSkipped"] });
              game.log(target, "跳过其下个回合的出牌阶段和弃牌阶段");
            }
          }
          break;
        case "heart":
          {
            if (!game.hasPlayer((current) => player !== current)) {
              return;
            }
            const result = await player.chooseTarget("【御风】：令一名其他角色跳过其下个回合的摸牌阶段", lib.filter.notMe, true).set("ai", (target) => {
              const player2 = get.player();
              const att = get.attitude(player2, target);
              return att * lib.skill.yijin.getValue(player2, "yijin_yongbi", target);
            }).forResult();
            if (result?.bool && result?.targets?.length) {
              const {
                targets: [target]
              } = result;
              target.skip("phaseDraw");
              target.addTempSkill(`${event.name}_skipDraw`, { player: "phaseDrawSkipped" });
              game.log(target, "跳过其下个回合的摸牌阶段");
            }
          }
          break;
        case "diamond":
        case "club":
          await player.draw();
          if (!player.getStat().skill.hm_yufeng) {
            return;
          }
          delete player.getStat().skill.hm_yufeng;
          game.log(player, "重置了", "#g【御风】");
          break;
      }
    },
    ai: {
      order: 13,
      result: { player: 1 },
      threaten: 1.5
    },
    subSkill: {
      skipDraw: {
        charlotte: true,
        mark: true,
        intro: { content: "跳过下回合的摸牌阶段" }
      },
      skipUse: {
        charlotte: true,
        mark: true,
        intro: { content: "跳过下回合的出牌和弃牌阶段" }
      }
    }
  },
  //卜巳
  hm_weiluan: {
    trigger: {
      player: ["phaseZhunbeiBegin", "phaseDrawBegin2", "phaseUseBegin"]
    },
    forced: true,
    filter(event, player, triggername) {
      if (triggername === "phaseDrawBegin2") {
        return !event.numFixed;
      }
      return true;
    },
    async content(event, trigger, player) {
      const next = player.judge((card) => {
        const suit2 = get.suit(card);
        if (suit2 === "spade") {
          return 4;
        }
        return 0;
      });
      next.judge2 = (result) => result.bool === false;
      const { suit } = await next.forResult();
      if (suit === "spade") {
        switch (event.triggername) {
          case "phaseZhunbeiBegin":
            player.addTempSkill("hm_weiluan_attackRange");
            break;
          case "phaseDrawBegin2":
            trigger.num++;
            break;
          case "phaseUseBegin":
            player.addTempSkill("hm_weiluan_sha");
            break;
        }
      }
    },
    subSkill: {
      attackRange: {
        charlotte: true,
        mod: {
          attackRange(player, num) {
            return num++;
          }
        }
      },
      sha: {
        charlotte: true,
        mod: {
          cardUsable(card, player, num) {
            if (card.name === "sha") {
              return num + 1;
            }
          }
        }
      }
    }
  },
  hm_tianpan: {
    trigger: {
      player: "judgeEnd"
    },
    forced: true,
    async content(event, trigger, player) {
      if (trigger.result.suit === "spade") {
        if (get.position(trigger.result.card, true) === "o") {
          await player.gain(trigger.result.card, "gain2");
        }
        const { control } = await player.chooseControl("baonue_hp", "baonue_maxHp", (event2, player2) => {
          if (player2.hp === player2.maxHp) {
            return "baonue_maxHp";
          }
          if (player2.hp < player2.maxHp - 1 || player2.hp <= 2) {
            return "baonue_hp";
          }
          return "baonue_hp";
        }).set("prompt", "天判：恢复1点体力或加1点体力上限").forResult();
        if (control === "baonue_hp") {
          await player.recover();
        } else {
          await player.gainMaxHp(true);
        }
      } else {
        const { control } = await player.chooseControl("baonue_hp", "baonue_maxHp", (event2, player2) => {
          if (player2.hp === player2.maxHp) {
            return "baonue_hp";
          }
          if (player2.hp < player2.maxHp - 1 || player2.hp <= 2) {
            return "baonue_maxHp";
          }
          return "baonue_hp";
        }).set("prompt", "天判：失去1点体力或减1点体力上限").forResult();
        if (control === "baonue_hp") {
          await player.loseHp();
        } else {
          await player.loseMaxHp(true);
        }
      }
    }
  },
  hm_gaiming: {
    usable: 1,
    trigger: {
      player: "judge"
    },
    filter(event, player) {
      const card = player.judging[0];
      return get.suit(card, player) !== "spade";
    },
    prompt2: "亮出牌堆顶的一张牌代替判定牌",
    async content(event, trigger, player) {
      const card = get.cards(1)[0];
      await player.respond([card], "hm_gaiming", "highlight", "noOrdering");
      if (player.judging[0].clone) {
        player.judging[0].clone.classList.remove("thrownhighlight");
        game.broadcast((card2) => {
          if (card2.clone) {
            card2.clone.classList.remove("thrownhighlight");
          }
        }, player.judging[0]);
        game.addVideo("deletenode", player, get.cardsInfo([player.judging[0].clone]));
      }
      await game.cardsDiscard(player.judging[0]);
      player.judging[0] = card;
      trigger.orderingCards.add(card);
      game.log(player, "的判定牌改为", card);
      await game.delay(2);
    }
  },
  //珪固
  hm_tuntian: {
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    async content(event, trigger, player) {
      player.addTempSkill("hm_tuntian_temp", { player: "hm_qianjunAfter" });
      player.addMark("hm_tuntian_temp", 1, false);
    },
    subSkill: {
      temp: {
        charlotte: true,
        mod: {
          maxHandcard(player, num) {
            return num + player.countMark("hm_tuntian_temp");
          }
        },
        trigger: {
          player: ["phaseDrawBegin2", "damageBegin3"]
        },
        forced: true,
        filter(event, player) {
          if (event.name === "phaseDraw") {
            return !event.numFixed;
          }
          return game.getGlobalHistory("everything", (evt) => evt.name === "damage" && evt.player === player).indexOf(event) === 0;
        },
        async content(event, trigger, player) {
          trigger.num += player.countMark(event.name);
        },
        onremove: true,
        mark: true,
        intro: { content: "本局游戏的摸牌阶段摸牌数、手牌上限、本回合首次受到的伤害+#" }
      }
    }
  },
  hm_qianjun: {
    limited: true,
    enable: "phaseUse",
    seatRelated: "changeSeat",
    skillAnimation: true,
    animationColor: "orange",
    derivation: "olluanji",
    filter(event, player) {
      return player.countCards("e");
    },
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player) {
      const target = event.targets[0];
      player.awakenSkill(event.name);
      await target.gain(player.getCards("e"), "gain2").set("giver", player);
      game.broadcastAll(
        (target1, target2) => {
          game.swapSeat(target1, target2);
        },
        player,
        target
      );
      await player.recover();
      await player.addSkills("olluanji");
    },
    ai: {
      order: 13,
      result: {
        player: 1,
        target: 2
      },
      threaten: 1.5
    }
  },
  //神朱儁
  hm_cheji: {
    usable: 1,
    enable: "phaseUse",
    filterCard: lib.filter.cardRecastable,
    selectCard: [1, Infinity],
    position: "he",
    discard: false,
    prompt: "重铸任意张牌，然后令一名其他角色重铸等量张手牌",
    filterTarget: lib.filter.notMe,
    allowChooseAll: true,
    async content(event, trigger, player) {
      const { cards: cards2, targets } = event;
      await player.recast(cards2);
      const target = targets[0];
      const next = target.chooseCard(`选择重铸${cards2.length}张牌`, "h", cards2.length, true, (card, player2) => player2.canRecast(card), "allowChooseAll").set("ai", (card) => 6.5 - get.value(card));
      const result = await next.forResult();
      if (result.bool) {
        await target.recast(result.cards);
        const cardname = result.cards.map((c) => c.name).unique();
        if (cardname.includes("sha")) {
          await target.damage("fire", player);
        }
        if (cardname.includes("shan") && target.hasUseTarget({ name: "sha", isCard: true })) {
          const next2 = player.chooseTarget(`撤击：请选择${get.translation(target)}使用【杀】的目标`, true);
          next2.set("filterTarget", (card, player2, targetx) => lib.filter.filterTarget({ name: "sha", isCard: true }, get.event().target, targetx));
          next2.set("ai", (targetx) => get.effect(targetx, { name: "sha", isCard: true }, get.event().target, get.player()));
          next2.set("target", target);
          const result2 = await next2.forResult();
          if (result2.bool) {
            await target.chooseUseTarget({ name: "sha", isCard: true }, true, result2.targets);
          }
        }
        if (cardname.includes("tao")) {
          await game.asyncDraw([player, target], 2);
        }
      }
    },
    ai: {
      order: 1,
      result: {
        player: 1
      },
      threaten: 1.5
    }
  },
  hm_daicui: {
    trigger: {
      global: "useCardToPlayered"
    },
    filter(event, player) {
      if (_status.currentPhase !== player || !get.discarded().length) {
        return false;
      }
      return event.card.name === "sha" && event.card.hasNature();
    },
    forced: true,
    async content(event, trigger, player) {
      const { target } = trigger;
      const discarded = get.discarded();
      const min = Math.max(0, discarded.length);
      const next = target.chooseCard("he", [min, Infinity], true);
      next.set("prompt2", "置于武将牌上直到回合结束");
      const { bool, cards: cards2 } = await next.forResult();
      if (bool) {
        target.addSkill("hm_daicui_expansion");
        target.addToExpansion("giveAuto", cards2, target).gaintag.add("hm_daicui_expansion");
        trigger.getParent().baseDamage++;
      }
    },
    subSkill: {
      expansion: {
        trigger: {
          global: "phaseEnd"
        },
        forced: true,
        popup: false,
        charlotte: true,
        filter(event, player) {
          return player.getExpansions("hm_daicui_expansion").length > 0;
        },
        async content(event, trigger, player) {
          const cards2 = player.getExpansions("hm_daicui_expansion");
          await player.gain(cards2, "draw");
          game.log(player, `收回了${get.cnNumber(cards2.length)}张“怠摧”牌`);
          player.removeSkill("hm_daicui_expansion");
        },
        intro: {
          markcount: "expansion",
          mark(dialog, storage, player) {
            const cards2 = player.getExpansions("hm_daicui_expansion");
            if (player.isUnderControl(true)) {
              dialog.addAuto(cards2);
            } else {
              return `共有${get.cnNumber(cards2.length)}张牌`;
            }
          }
        }
      }
    }
  },
  hm_kuixiang: {
    trigger: {
      global: "dyingAfter"
    },
    prompt2(event, player) {
      return `对${get.translation(event.player.name)}造成1点伤害`;
    },
    filter(event, player) {
      if (event.player === player || !event.player.isIn()) {
        return false;
      }
      return !player.getStorage("hm_kuixiang_used").includes(event.player);
    },
    onremove(player) {
      player.removeSkill("hm_kuixiang_used");
    },
    logTarget: "player",
    check(event, player) {
      return get.attitude(player, event.player) < 0;
    },
    async content(event, trigger, player) {
      const target = trigger.player;
      player.addSkill("hm_kuixiang_used");
      player.markAuto("hm_kuixiang_used", [target]);
      await target.damage(player);
      if (game.getGlobalHistory("everything", (evt) => {
        if (evt.name !== "die" || evt.player !== target) {
          return false;
        }
        return evt.reason?.getParent() === event;
      }).length > 0 && target.isDead()) {
        const next = player.chooseBool("〖溃降〗：是否摸三张牌？");
        const { bool } = await next.forResult();
        if (bool) {
          await player.draw(3);
        }
      }
    },
    subSkill: {
      used: {
        intro: {
          content: "已对$发动过〖溃降〗"
        },
        charlotte: true,
        onremove: true
      }
    }
  },
  //神皇甫嵩---配音来自b站up主孤独妙妙
  hm_shice: {
    audio: 2,
    zhuanhuanji: true,
    mark: true,
    marktext: "☯",
    intro: {
      content(storage, player, skill) {
        if (!storage) {
          return "当你受到属性伤害时，若你的技能数不大于伤害来源，你可以防止此伤害并视为使用一张【火攻】。";
        }
        return "当你不因此技能使用牌指定唯一目标后，你可以令其弃置装备区任意张牌，然后此牌额外结算X次（X为其装备区的牌数）。";
      }
    },
    trigger: { player: ["damageBegin4", "useCardToPlayered"] },
    filter(event, player) {
      const storage = player.storage.hm_shice;
      if (!storage && event.name === "damage") {
        const { source } = event;
        if (!source) {
          return false;
        }
        return event.hasNature() && lib.skill.jsrgjuxia.countSkill(source) >= lib.skill.jsrgjuxia.countSkill(player);
      } else if (storage && event.name === "useCardToPlayered") {
        return event.getParent(3).name !== "hm_shice" && event.targets?.length === 1 && event.targets[0].countCards("e");
      }
      return false;
    },
    async cost(event, trigger, player) {
      const { source, target, card, nature } = trigger;
      if (trigger.name === "damage") {
        event.result = await player.chooseBool(get.prompt(event.skill), "防止此伤害并视为使用一张【火攻】").set("choice", get.damageEffect(player, source, player, nature) < 0).forResult();
      } else {
        const { bool } = await player.chooseBool(get.prompt(event.skill, target), "令其弃置装备区任意张牌，然后此牌额外结算X次（X为其装备区的牌数）").set("choice", get.effect(target, card, player, player) > 0).forResult();
        event.result = {
          bool,
          targets: [target]
        };
      }
    },
    async content(event, trigger, player) {
      player.changeZhuanhuanji(event.name);
      if (trigger.name === "damage") {
        trigger.cancel();
        const huogong = get.autoViewAs({ name: "huogong", isCard: true });
        if (player.hasUseTarget(huogong)) {
          await player.chooseUseTarget(huogong, true);
        }
      } else {
        const { target, card } = trigger;
        if (target.countCards("e")) {
          await target.chooseToDiscard("e", [1, Infinity], `弃置装备区任意张牌，然后${get.translation(card)}额外结算X次（X为你装备区的牌数）`, "allowChooseAll").set("ai", (card2) => {
            if (get.event().goon) {
              return 0;
            }
            return 7 - get.value(card2);
          }).set("goon", get.effect(target, card, player, target) > 0);
        }
        const num = target.countCards("e");
        if (!num) {
          return;
        }
        trigger.getParent().effectCount += num;
        game.log(card, `额外结算${num}次`);
      }
    }
  },
  //看不懂，破怠了
  hm_podai: {
    audio: 2,
    trigger: { global: ["phaseBegin", "phaseEnd"] },
    filter(event, player) {
      const storage = player.getStorage("hm_podai_round");
      if (!event.player.isIn() || storage.length > 1) {
        return false;
      }
      return !storage.includes("draw") || !storage.includes("disable") && lib.skill.hm_podai.getSkills(event.player).length;
    },
    infoTranslationIncludesString(skill, list, player) {
      const text = get.skillInfoTranslation(skill, player);
      return list.some((key) => text.includes(key));
    },
    derivation: "hm_podai_faq",
    getSkills(player) {
      return player.getSkills(null, false, true).filter((skill) => {
        const info = get.info(skill);
        const list = ["1", "2", "3", "4", "5", "6", "7", "8", "9", "0"].concat(["零", "一", "二", "两", "三", "四", "五", "六", "七", "八", "九", "十"]).concat(["壹", "贰", "叁", "肆", "伍", "陆", "柒", "捌", "玖", "拾", "佰", "仟", "万", "亿"]).concat(get.cardTranslation((c) => get.type(c) === "basic"));
        if (!info || info.charlotte || info.persevereSkill) {
          return false;
        }
        return lib.skill.hm_podai.infoTranslationIncludesString(skill, list, player);
      });
    },
    async cost(event, trigger, player) {
      const { player: target } = trigger;
      const storage = player.getStorage("hm_podai_round");
      const name = get.translation(target);
      const dialog = [
        "请选择一项",
        [
          [
            ["disable", `令${name}描述中含有基本牌名或数字的一个技能失效`],
            ["draw", `令${name}摸三张牌，然后对其造成1点火焰伤害。`]
          ],
          "textbutton"
        ]
      ];
      const next = player.chooseButton(dialog);
      next.set("ai", (button) => {
        const { player: player2, target: target2 } = get.event();
        const { link } = button;
        if (link === "disable") {
          return -(get.threaten(target2, player2) * get.attitude(player2, target2));
        }
        if (get.attitude(player2, target2) > 0 && (target2.hasSkillTag("nofire") || target2.hasSkillTag("nodamage", null, {
          source: player2,
          natures: ["fire"]
        }))) {
          return 1;
        }
        return get.damageEffect(target2, player2, player2, "fire") + get.effect(target2, { name: "draw" }, player2, player2) * 3;
      });
      next.set("filterButton", (button) => {
        const { player: player2, target: target2, storage: storage2 } = get.event();
        const { link } = button;
        if (storage2.includes(link)) {
          return false;
        }
        const skill = lib.skill.hm_podai.getSkills(target2);
        return link !== "disable" || skill.length;
      });
      next.set("target", target);
      next.set("storage", storage);
      const result = await next.forResult();
      event.result = {
        bool: result.bool,
        cost_data: result.links?.[0]
      };
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const { cost_data } = event;
      const { player: target } = trigger;
      player.addTempSkill("hm_podai_round", "roundStart");
      player.markAuto("hm_podai_round", [cost_data]);
      if (cost_data === "disable") {
        const list = lib.skill.hm_podai.getSkills(target);
        if (!list.length) {
          return;
        }
        const next = player.chooseButton([`令${get.translation(target)}的一个技能失效`, [list, "skill"]], true);
        const result = await next.forResult();
        if (result.bool) {
          target.addSkill("hm_podai_sb");
          target.markAuto("hm_podai_sb", [result.links[0]]);
        }
      } else if (cost_data === "draw") {
        await target.draw(3);
        await target.damage("fire", player);
      }
    },
    subSkill: {
      round: {
        charlotte: true,
        onremove: true
      },
      sb: {
        init(player, skill) {
          player.storage[skill] = [];
          player.addSkillBlocker(skill);
        },
        onremove(player, skill) {
          player.removeSkillBlocker(skill);
        },
        charlotte: true,
        skillBlocker(skill, player) {
          return player.getStorage("hm_podai_sb").includes(skill);
        },
        mark: true,
        intro: {
          content(storage, player, skill) {
            const list = player.getSkills(null, false, false).filter((i) => lib.skill.hm_podai_sb.skillBlocker(i, player));
            if (list.length) {
              return `失效技能：${get.translation(list)}`;
            }
            return "无失效技能";
          }
        }
      }
    }
  },
  //神卢植
  hm_jigan: {
    trigger: { global: "phaseAfter" },
    setDistanceObj(player) {
      const obj = {};
      for (const i of game.players) {
        if (!obj[i.playerid]) {
          obj[i.playerid] = {};
        }
        for (const j of game.players) {
          obj[i.playerid][j.playerid] = get.distance(i, j);
        }
      }
      player.storage["hm_jigan"] = obj;
    },
    filter(event, player) {
      let storage = player.storage["hm_jigan"];
      if (!storage) {
        lib.skill["hm_jigan"].setDistanceObj(player);
        storage = player.storage["hm_jigan"];
      }
      let bool = false;
      if (game.getGlobalHistory("everything", (event2) => {
        if (event2.name !== "gain") {
          return false;
        }
        return event2.giver;
      }).length) {
        bool = true;
      }
      for (const i of game.players) {
        for (const j of game.players) {
          if (storage[i.playerid]?.[j.playerid] !== get.distance(i, j)) {
            bool = true;
          }
        }
      }
      return bool;
    },
    async cost(event, trigger, player) {
      const giver = [];
      game.getGlobalHistory("everything", (event2) => {
        if (event2.name !== "gain") {
          return false;
        }
        return event2.giver;
      }).forEach((evt) => {
        giver.add(evt.giver);
      });
      const storage = player.storage["hm_jigan"];
      const distanceChanged = [];
      for (const i of game.players) {
        for (const j of game.players) {
          if (storage[i.playerid]?.[j.playerid] !== get.distance(i, j)) {
            distanceChanged.add(i);
          }
        }
      }
      const targetsx = [].concat(giver).concat(distanceChanged);
      const next = player.chooseTarget("令其中两名角色分别视为使用一张基本牌");
      next.set("ai", (target) => {
        const player2 = get.player();
        const list = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          return target.hasValueTarget({ name: info[2], nature: info[3], isCard: true });
        });
        if (!list.length) {
          return 0;
        }
        return get.attitude(player2, target);
      });
      next.set("selectTarget", [2, 2]);
      next.set("targetsx", targetsx);
      next.set("filterTarget", (card, player2, target) => {
        const evt = get.event();
        return evt.targetsx.includes(target);
      });
      const result = await next.forResult();
      lib.skill["hm_jigan"].setDistanceObj(player);
      event.result = result;
    },
    async content(event, trigger, player) {
      const { targets } = event;
      for (const target of targets.sortBySeat()) {
        const list = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          return target.hasUseTarget({ name: info[2], nature: info[3], isCard: true });
        });
        if (list.length) {
          const next = target.chooseButton(["是否视为使用一张基本牌？", [list, "vcard"]]);
          next.set("ai", (button) => {
            const player2 = get.player();
            const card2 = {
              name: button.link[2],
              nature: button.link[3],
              isCard: true
            };
            return player2.getUseValue(card2);
          });
          const { links } = await next.forResult();
          if (!links?.length) {
            return;
          }
          const card = { name: links[0][2], nature: links[0][3], isCard: true };
          await target.chooseUseTarget(card, true);
        }
      }
    },
    group: "hm_jigan_gameStart",
    subSkill: {
      gameStart: {
        charlotte: true,
        trigger: {
          global: "phaseBefore",
          player: "enterGame"
        },
        silent: true,
        filter(event, player) {
          return event.name !== "phase" || game.phaseNumber === 0;
        },
        async content(event, trigger, player) {
          lib.skill["hm_jigan"].setDistanceObj(player);
        }
      }
    }
  },
  hm_weizhu: {
    enable: "phaseUse",
    usable: 1,
    filterCard: lib.filter.cardRecastable,
    selectCard: [1, Infinity],
    discard: false,
    lose: false,
    allowChooseAll: true,
    async content(event, trigger, player) {
      const { cards: cards2 } = event;
      await player.recast(cards2);
      const equip = Array.from(ui.discardPile.childNodes).filter((c) => get.type(c) === "equip");
      if (cards2.length >= equip.length) {
        await player.gain(equip);
      } else {
        const next = player.chooseCardButton(equip, true);
        next.set("selectButton", cards2.length);
        const result = await next.forResult();
        if (result.bool) {
          await player.gain(result.links);
        }
      }
      if (_status.connectMode) {
        game.broadcastAll(() => _status.noclearcountdown = true);
      }
      const map = /* @__PURE__ */ new Map();
      while (player.countCards("he")) {
        const result = await player.chooseCardTarget({
          forced: true,
          map,
          position: "he",
          prompt: "围铸：请选择要分配的卡牌和目标",
          filterTarget(card, player2, target) {
            return player2 !== target && !get.event().map.has(target);
          },
          filterCard(card) {
            return !card.hasGaintag("hm_weizhu_tag");
          },
          ai1(card) {
            const { player: player2, map: map2 } = get.event();
            return Math.max(...game.filterPlayer().map((target) => [...ui.selected.cards, card, ...map2.get(target) || []].reduce((sum, cardx) => sum + get.value(cardx, target) * get.attitude(player2, target), 0)));
          },
          ai2(target) {
            const player2 = get.player();
            const card = ui.selected.cards[0];
            if (card) {
              return get.value(card, target) * get.attitude(player2, target);
            }
            return 0;
          }
        }).forResult();
        if (result?.bool && result.targets?.length && result.cards?.length) {
          const target = result.targets[0];
          player.addGaintag(result.cards, "hm_weizhu_tag");
          map.set(target, result.cards);
          if (map.size >= cards2.length || map.size >= game.countPlayer((target2) => target2 !== player)) {
            break;
          }
        } else {
          break;
        }
      }
      if (_status.connectMode) {
        game.broadcastAll(() => {
          delete _status.noclearcountdown;
          game.stopCountChoose();
        });
      }
      if (map.size) {
        const gain_list = [];
        map.forEach((cards3, source) => {
          player.line(source, "green");
          gain_list.push([source, cards3]);
          source.addTempSkill("hm_weizhu_buff", "roundStart");
        });
        await game.loseAsync({
          gain_list,
          player,
          giver: player,
          animate: "giveAuto"
        }).setContent("gaincardMultiple");
      }
    },
    subSkill: {
      tag: {},
      buff: {
        charlotte: true,
        mod: {
          globalFrom(from, to, distance) {
            return distance - 1;
          }
        }
      }
    }
  },
  hm_guiquan: {
    enable: "chooseToUse",
    init(player, skill) {
      player.storage[skill] = [];
    },
    filter(event, player) {
      const cards2 = player.getCards("hes", { type: "equip" });
      if (!cards2.length) {
        return false;
      }
      return lib.inpile.some((name) => {
        if (player.getStorage("hm_guiquan").includes(name)) {
          return false;
        }
        if (get.type(name) !== "trick") {
          return false;
        }
        let bool = false;
        for (const card of cards2) {
          const vcard = get.autoViewAs({ name }, card);
          if (event.filterCard(vcard, player, event)) {
            bool = true;
          }
        }
        return bool;
      });
    },
    chooseButton: {
      dialog(event, player) {
        const list = [];
        for (const name of lib.inpile) {
          if (player.getStorage("hm_guiquan").includes(name)) {
            continue;
          }
          if (get.type(name) === "trick") {
            list.push(["锦囊", "", name]);
          }
        }
        return ui.create.dialog(get.translation("hm_guiquan"), [list, "vcard"]);
      },
      filter(button, player) {
        const event = _status.event.getParent();
        const card = get.autoViewAs({
          name: button.link[2]
        });
        return event.filterCard(card, player, event);
      },
      backup(links, player) {
        return {
          filterCard(card) {
            return get.type(card) === "equip";
          },
          filterTarget(card, player2, target) {
            if (target.hp > player2.hp) {
              return false;
            }
            return lib.filter.filterTarget.apply(this, arguments);
          },
          selectCard: 1,
          position: "hes",
          popname: true,
          viewAs: { name: links[0][2] },
          onuse(result, player2) {
            const { card } = result;
            player2.getStorage("hm_guiquan").add(card.name);
          }
        };
      },
      prompt(links, player) {
        return `将一张装备牌当作${get.translation(links[0][2])}使用`;
      }
    },
    ai: {
      order: 1,
      result: {
        //Waiting For 157
        player(player) {
          const num = 0;
          return 12 - num;
        }
      },
      threaten: 1.6
    }
  },
  //程远志
  hm_wuxiao: {
    trigger: {
      global: ["loseAfter", "equipAfter", "loseAsyncAfter", "cardsDiscardAfter"]
    },
    filter(event, player, name) {
      if (!event.getd?.().some((card) => get.color(card, false) === "red")) {
        return false;
      }
      return game.getGlobalHistory("everything", (evt) => evt.getd?.()?.some((card) => get.color(card, false) === "red")).length === 1;
    },
    forced: true,
    async content(event, trigger, player) {
      player.addTempSkill(["hm_wuxiao_buff", "hm_wuxiao_debuff"]);
    },
    subSkill: {
      buff: {
        charlotte: true,
        forced: true,
        trigger: {
          source: "damageBegin1"
        },
        async content(event, trigger, player) {
          trigger.num++;
          player.removeSkill("hm_wuxiao_buff");
        }
      },
      debuff: {
        charlotte: true,
        forced: true,
        trigger: {
          player: "damageBegin3"
        },
        async content(event, trigger, player) {
          trigger.num++;
          player.removeSkill("hm_wuxiao_debuff");
        }
      }
    }
  },
  hm_qianhu: {
    enable: "phaseUse",
    filterCard: (card) => get.color(card) === "red",
    filter(event, player) {
      return player.countCards("he", { color: "red" }) > 1 && player.hasUseTarget("juedou");
    },
    selectCard: 2,
    position: "he",
    check(card) {
      return 6 - get.value(card);
    },
    async content(event, trigger, player) {
      await player.chooseUseTarget("juedou", true);
    },
    group: "hm_qianhu_draw",
    subSkill: {
      draw: {
        trigger: {
          source: "damageSource"
        },
        filter(event, player) {
          return event.getParent(5).skill === "hm_qianhu";
        },
        silent: true,
        charlotte: true,
        async content(event, trigger, player) {
          await player.draw();
        }
      }
    },
    ai: {
      order: 9,
      result: {
        player(player) {
          if (player.getUseValue({ name: "juedou" }) < 0) {
            return 0;
          }
          return 1;
        }
      }
    }
  },
  //高升
  hm_xiongshi: {
    global: "hm_xiongshi_global",
    mark: true,
    intro: {
      markcount: "expansion",
      mark(dialog, content, player) {
        const cards2 = player.getExpansions("hm_xiongshi");
        if (cards2 && cards2.length) {
          if (player === game.me || player.isUnderControl()) {
            dialog.addAuto(cards2);
          } else {
            return `共有${get.cnNumber(cards2.length)}张“凶势”`;
          }
        }
      },
      content(content, player) {
        const cards2 = player.getExpansions("hm_xiongshi");
        if (cards2 && cards2.length) {
          if (player === game.me || player.isUnderControl()) {
            return get.translation(cards2);
          }
          return `共有${get.cnNumber(cards2.length)}张“凶势”`;
        }
      }
    },
    subSkill: {
      global: {
        audio: "hm_xiongshi",
        enable: "phaseUse",
        filter(event, player) {
          if (player.hasSkill("hm_xiongshi_used")) {
            return false;
          }
          return player.countCards("he") && game.hasPlayer((current) => current.hasSkill("hm_xiongshi"));
        },
        log: false,
        delay: false,
        filterCard: true,
        discard: false,
        lose: false,
        position: "he",
        prompt() {
          const player = _status.event.player;
          const list = game.filterPlayer((current) => current.hasSkill("hm_xiongshi"));
          if (list.length === 1 && list[0] === player) {
            return "将一张牌置于你的武将牌上";
          }
          let str = `将一张牌置于${get.translation(list)}`;
          if (list.length > 1) {
            return str += "其中一人的武将牌上";
          }
          return str += "的武将牌上";
        },
        filterTarget(card, player, target) {
          return target.hasSkill("hm_xiongshi");
        },
        check(card) {
          return 8 - get.value(card);
        },
        async content(event, trigger, player) {
          const { targets, cards: cards2 } = event;
          targets[0].addToExpansion(cards2, player, "giveAuto").gaintag.add("hm_xiongshi");
          player.addTempSkill("hm_xiongshi_used", { player: "phaseUseAfter" });
        },
        ai: {
          order: 2,
          threaten: 1.5,
          result: {
            player(player, target) {
              const owner = game.findPlayer((current) => current.hasSkill("hm_xiongshi"));
              if (owner) {
                return get.attitude(player, owner);
              }
            }
          }
        }
      },
      used: {
        charlotte: true
      }
    }
  },
  hm_difeng: {
    trigger: {
      global: ["loseAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    getIndex(event, player) {
      return game.filterPlayer((target) => event.getl?.(target)?.cards2?.length > 0);
    },
    filter(event, player, name, target) {
      if (!target?.isIn()) {
        return false;
      }
      if (event.name === "addToExpansion") {
        return true;
      }
      return event.getlx !== false && (event.toStorage === true || event.type === "addToExpansion");
    },
    forced: true,
    logTarget(event, player, name, target) {
      return target;
    },
    async content(event, trigger, player) {
      await game.asyncDraw([player, ...event.targets].sortBySeat());
    },
    group: "hm_difeng_damage",
    subSkill: {
      damage: {
        trigger: {
          player: "damageBegin3",
          source: "damageBegin1"
        },
        filter(event, player) {
          return event.source?.isIn() && player.countCards("xs") > 0;
        },
        async cost(event, trigger, player) {
          const { source } = trigger;
          const cards2 = player.getCards("xs", (card) => !card._cardid);
          const next = source.chooseCardButton("弃置一张牌令此伤害+1", cards2);
          const result = await next.forResult();
          event.result = {
            bool: result.bool,
            cost_data: result.links
          };
        },
        async content(event, trigger, player) {
          const { cost_data } = event;
          await player.loseToDiscardpile(cost_data);
          trigger.num++;
        }
      }
    }
  },
  //何曼
  hm_juedian: {
    trigger: { source: "damageSource" },
    filter(event, player) {
      const { player: target } = event;
      const juedou = get.autoViewAs({ name: "juedou", isCard: true });
      if (!target.isIn() || !player.canUse(juedou, target, false)) {
        return false;
      }
      return event.getParent().type === "card" && event.getParent(2)?.targets?.length === 1 && player.getHistory("sourceDamage", (evt) => evt.getParent(2)?.targets?.length === 1).indexOf(event) === 0;
    },
    locked: true,
    async cost(event, trigger, player) {
      const { player: target } = trigger;
      const { control } = await player.chooseControl("baonue_hp", "baonue_maxHp", "背水！").set("prompt", `决巅：选择一项并视为对${get.translation(target)}使用一张【决斗】`).set("ai", () => {
        const { player: player2, target: target2 } = get.event();
        const bool1 = player2.getHp() > 2;
        const bool2 = player2.isDamaged() && player2.maxHp > 3;
        const eff = get.effect(target2, { name: "juedou", isCard: true }, player2, player2);
        if (eff > 0 && (bool1 && bool2 && target2.mayHaveSha(player2, "respond", null, "count") <= player2.mayHaveSha(player2, "respond", null, "count") || player2.hasSkill("hm_nitian_buff"))) {
          return "背水！";
        }
        if (bool2) {
          return "baonue_maxHp";
        }
        return "baonue_hp";
      }).set("target", target).forResult();
      event.result = {
        bool: true,
        cost_data: control,
        targets: [target]
      };
    },
    async content(event, trigger, player) {
      const {
        targets: [target],
        cost_data: control
      } = event;
      const juedou = get.autoViewAs({ name: "juedou", isCard: true });
      if (["baonue_hp", "背水！"].includes(control)) {
        await player.loseHp();
      }
      if (["baonue_maxHp", "背水！"].includes(control)) {
        await player.loseMaxHp(true);
      }
      if (player.canUse(juedou, target, false)) {
        const next = player.useCard(juedou, target, false);
        next.baseDamage = control === "背水！" ? 2 : 1;
        await next;
      }
    }
  },
  hm_nitian: {
    enable: "phaseUse",
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.addTempSkill(`${event.name}_buff`);
    },
    /*ai: {
    	order(item, player) {
    		if (
    			game.hasPlayer(current => {
    				if (get.attitude(player, current) > 0) {
    					return false;
    				}
    				if (current.getHp() > 3) {
    					return false;
    				}
    				return player.hasCard(card => get.tag(card, "damage") && get.type(card) !== "delay" && player.canUse(card, current) && get.effect(current, card, player, player) > 0, "hs");
    			})
    		) {
    			return 10;
    		}
    		return 0.1;
    	},
    	result: {
    		player(player, target) {
    			return player.hasCard(card => get.tag(card, "damage") && get.type(card) !== "delay" && player.hasValueTarget(card), "hs") ? 1 : 0;
    		},
    	},
    },*/
    subSkill: {
      buff: {
        trigger: { player: ["useCard", "phaseJieshuBegin"] },
        filter(event, player) {
          if (event.name === "useCard") {
            return true;
          }
          return !player.getStat("kill");
        },
        silent: true,
        charlotte: true,
        async content(event, trigger, player) {
          if (trigger.name === "useCard") {
            trigger.customArgs.default.directHit2 = true;
          } else {
            await player.die();
          }
        }
      }
    }
  },
  //严政
  hm_didao: {
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player) {
      return player.isDamaged() && player.countCards("h");
    },
    limited: true,
    skillAnimation: true,
    animationColor: "metal",
    direct: true,
    clearTime: true,
    async content(event, trigger, player) {
      await player.chooseToUse().set("openskilldialog", get.prompt2(event.name)).set("norestore", true).set("_backupevent", `${event.name}_backup`).set("custom", {
        add: {},
        replace: { window() {
        } }
      }).backup(`${event.name}_backup`).set("addCount", false).set("oncard", () => {
        get.event().baseDamage = get.event().cards.length;
      });
    },
    subSkill: {
      backup: {
        filterCard(card) {
          return get.itemtype(card) === "card";
        },
        filterTarget(card, player, target) {
          return lib.filter.targetEnabled.apply(this, arguments);
        },
        selectCard: -1,
        position: "h",
        viewAs: {
          name: "sha"
        },
        prompt: "将所有手牌当一张【杀】使用",
        check(card) {
          return 7 - get.value(card);
        },
        precontent(event, trigger, player) {
          player.trySkillAnimate("hm_didao", "hm_didao", player.checkShow("hm_didao"));
          player.awakenSkill("hm_didao");
        }
      }
    }
  },
  hm_xianxiang: {
    trigger: {
      source: "die"
    },
    forced: true,
    filter(event, player) {
      if (!event.player.countCards("hej")) {
        return false;
      }
      return game.countPlayer((target) => target !== player) >= 1;
    },
    async content(event, trigger, player) {
      const next = player.chooseTarget(`令另一名其他角色获得${get.translation(trigger.player.name)}区域内的所有牌`, lib.filter.notMe, true);
      const result = await next.forResult();
      if (result.bool) {
        const togain = trigger.player.getCards("hej");
        await result.targets[0].gain(togain, trigger.player, "giveAuto");
      }
    }
  },
  //波才
  hm_kunjun: {
    trigger: { global: "useCard" },
    filter(event, player) {
      if (get.type(event.card) !== "trick" && (get.type(event.card) !== "basic" || ["shan", "tao", "jiu", "du"].includes(event.card.name))) {
        return false;
      }
      if (event.player === player) {
        return game.hasPlayer((current) => player.countCards("h") > current.countCards("h"));
      }
      return event.player.countCards("h") > player.countCards("h");
    },
    forced: true,
    async content(event, trigger, player) {
      if (trigger.player === player) {
        trigger.directHit.addArray(game.filterPlayer((current) => player.countCards("h") > current.countCards("h")));
      } else {
        trigger.directHit.add(player);
      }
    },
    ai: {
      halfneg: true
    },
    group: "hm_kunjun_advent",
    subSkill: {
      advent: {
        trigger: { global: "gameDrawBegin" },
        forced: true,
        async content(event, trigger, player) {
          const me = player;
          const numx = trigger.num;
          trigger.num = (player2) => (typeof numx === "function" ? numx(player2) : numx) + (player2 === me ? 4 : 0);
        }
      }
    }
  },
  hm_yingzhan: {
    trigger: {
      player: "damageBegin3",
      source: "damageBegin1"
    },
    forced: true,
    filter(event, player) {
      return event.hasNature();
    },
    async content(event, trigger, player) {
      trigger.num++;
    }
  },
  hm_cuiji: {
    trigger: {
      global: "phaseUseBegin"
    },
    filter(event, player) {
      if (event.player === player) {
        return false;
      }
      const card = new lib.element.VCard({ name: "sha", nature: "thunder" });
      return player.countCards("h") > event.player.countCards("h") && lib.filter.targetEnabled(card, player, event.player);
    },
    direct: true,
    async content(event, trigger, player) {
      const next = player.chooseToUse();
      next.set("targets", [trigger.player]);
      next.set("openskilldialog", get.prompt2("hm_cuiji"));
      next.set("norestore", true);
      next.set("_backupevent", "hm_cuiji_backup");
      next.set("custom", {
        add: {},
        replace: { window() {
        } }
      });
      next.backup("hm_cuiji_backup");
      await next;
    },
    group: "hm_cuiji_draw",
    subSkill: {
      backup: {
        filterCard(card) {
          return get.itemtype(card) === "card";
        },
        selectCard: [1, Infinity],
        position: "hs",
        viewAs: {
          name: "sha",
          nature: "thunder"
        },
        filterTarget(card, player, target) {
          return _status.event.targets && _status.event.targets.includes(target) && lib.filter.targetEnabled.apply(this, arguments);
        },
        allowChooseAll: true,
        prompt: "将任意张手牌当一张雷【杀】使用",
        check(card) {
          return 7 - get.value(card);
        }
      },
      draw: {
        trigger: {
          player: "useCardAfter"
        },
        silent: true,
        charlotte: true,
        filter(event, player) {
          return event.skill === "hm_cuiji_backup" && player.getHistory("sourceDamage", (card) => card.card === event.card).length > 0;
        },
        async content(event, trigger, player) {
          player.draw(trigger.cards?.length);
        }
      }
    }
  },
  //邓茂
  hm_houying: {
    enable: "phaseUse",
    filterCard: (card) => get.color(card) === "black",
    filter(event, player) {
      return player.countCards("he", { color: "black" }) > 1 && player.hasUseTarget("sha");
    },
    selectCard: 2,
    position: "he",
    check(card) {
      return 6 - get.value(card);
    },
    async content(event, trigger, player) {
      await player.chooseUseTarget("sha", true, false);
    },
    group: "hm_houying_draw",
    subSkill: {
      draw: {
        trigger: {
          source: "damageSource"
        },
        filter(event, player) {
          return event.getParent(5).skill === "hm_houying";
        },
        silent: true,
        charlotte: true,
        async content(event, trigger, player) {
          await player.draw();
        }
      }
    },
    ai: {
      order: 9,
      result: {
        player(player) {
          if (player.getUseValue({ name: "sha" }) < 0) {
            return 0;
          }
          return 1;
        }
      }
    }
  },
  hm_paoxi: {
    usable: 1,
    trigger: {
      player: "useCardToPlayered",
      target: "useCardToPlayered"
    },
    forced: true,
    filter(event, player) {
      const history = game.getGlobalHistory("useCard");
      const index = history.indexOf(event.getParent()) - 1;
      if (index < 0) {
        return false;
      }
      const evt = history[index];
      if (!evt || !evt.targets || !evt.targets.length) {
        return false;
      }
      if (evt.targets.includes(event.target)) {
        return true;
      }
      return false;
    },
    async content(event, trigger, player) {
      player.addTempSkill(["hm_paoxi_buff", "hm_paoxi_debuff"]);
    },
    subSkill: {
      buff: {
        charlotte: true,
        forced: true,
        trigger: {
          source: "damageBegin1"
        },
        async content(event, trigger, player) {
          trigger.num++;
          player.removeSkill("hm_paoxi_buff");
        }
      },
      debuff: {
        charlotte: true,
        forced: true,
        trigger: {
          player: "damageBegin3"
        },
        async content(event, trigger, player) {
          trigger.num++;
          player.removeSkill("hm_paoxi_debuff");
        }
      }
    }
  },
  //神张角
  hm_fudao: {
    trigger: {
      global: ["phaseBefore", "useSkill_hm_fudao"],
      player: "enterGame"
    },
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async cost(event, trigger, player) {
      const result = await player.chooseVCardButton(["hm_zhong_heart", "hm_zhong_diamond", "hm_zhong_club", "hm_zhong_spade"]).set("ai", (button) => Math.random()).forResult();
      event.result = {
        bool: result.bool,
        cost_data: result.links
      };
    },
    async content(event, trigger, player) {
      const { cost_data } = event;
      const result = await player.chooseControl(lib.suit.slice()).set("prompt", "请选择【众】的花色").forResult();
      const card = game.createCard(cost_data[0][2], result.control, 1);
      player.chooseUseTarget(card, true);
    }
  },
  hm_zongfu: {
    trigger: { global: "roundStart" },
    async cost(event, trigger, player) {
      const next = player.chooseButton(["###众附：是否声明一种花色？###", [lib.suit.map((i) => ["", "", `lukai_${i}`]), "vcard"]]);
      next.set("ai", (button) => Math.random());
      const result = await next.forResult();
      event.result = {
        bool: result.bool,
        cost_data: result.links
      };
    },
    async content(event, trigger, player) {
      const { cost_data } = event;
      const suit = cost_data[0][2];
      game.log(player, "声明了", `#y${get.translation(suit)}`);
      player.storage.hm_zongfu = suit.replace("lukai_", "");
      const targets = game.filterPlayer((p) => p.isMinHandcard());
      for (const i of targets) {
        const next = i.chooseCard("将一张牌置于牌堆顶，否则按“取消”从牌堆底摸一张牌", "he");
        next.set("ai", (card) => {
          if (get.attitude(i, player) < 0) {
            return 0;
          }
          if (get.suit(card) === suit.replace("lukai_", "")) {
            return 8 - get.value(card);
          }
          return 6 - get.value(card);
        });
        const result = await next.forResult();
        if (result.bool) {
          await i.lose(result.cards, ui.cardPile, "insert");
          game.log(i, "将一张牌置于牌堆顶");
          i.addTempSkill("hm_zongfu_lose", { global: "roundStart" });
        } else {
          await i.draw("bottom");
        }
      }
    },
    derivation: "hm_fudao",
    group: "hm_zongfu_useSkill",
    subSkill: {
      lose: {
        charlotte: true
      },
      useSkill: {
        trigger: {
          global: "damageSource"
        },
        charlotte: true,
        silent: true,
        filter(event, player) {
          return event.source?.hasSkill("hm_zongfu_lose");
        },
        async content(event, trigger, player) {
          event.trigger("useSkill_hm_fudao");
        }
      }
    }
  },
  hm_dangjing: {
    trigger: { player: ["hm_zongfuAfter", "hm_dangjing_callback"] },
    filter(event, player) {
      return player.isMaxEquip();
    },
    async cost(event, trigger, player) {
      const next = player.chooseTarget("令一名角色进行一次判定");
      next.set("ai", (target) => {
        const player2 = get.player();
        return get.damageEffect(target, player2, player2, "thunder");
      });
      const result = await next.forResult();
      event.result = result;
    },
    async content(event, trigger, player) {
      const { targets } = event;
      const next = targets[0].judge((card) => {
        const evt = get.event();
        if (get.suit(card) === evt.suitx) {
          return -4;
        }
        return 0;
      });
      next.judge2 = (result2) => result2.bool === false;
      next.set("suitx", player.storage.hm_zongfu);
      const result = await next.forResult();
      if (result.suit === player.storage.hm_zongfu) {
        targets[0].damage("thunder", player);
        event.trigger("hm_dangjing_callback");
      }
    },
    ai: { combo: "hm_zongfu" }
  },
  //神张宝
  hm_zhouyuan: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.countCards("h") > 0 || game.hasPlayer((current) => get.info("hm_zhouyuan").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      if (player === target) {
        return false;
      }
      return target.countCards("h") > 0;
    },
    async content(event, trigger, player) {
      const { targets } = event;
      const target = targets[0];
      if (!target.countCards("h", (card) => ["red", "black"].includes(get.color(card, false)))) {
        return;
      }
      const dialog = [
        "请选择一项",
        [
          [
            ["black", "将所有黑色手牌扣置于武将牌上"],
            ["red", "将所有红色手牌扣置于武将牌上"]
          ],
          "textbutton"
        ]
      ];
      const next = target.chooseButton(dialog, true);
      next.set("filterButton", (button) => {
        const evt = get.event();
        if (button.link === "black") {
          return evt.player.countCards("h", { color: "black" });
        }
        return evt.player.countCards("h", { color: "red" });
      });
      const result = await next.forResult();
      if (result?.links) {
        const color1 = result.links[0];
        const cards1 = target.getCards("h", { color: color1 });
        const cards2 = player.getCards("h", { color: color1 === "black" ? "red" : "black" });
        game.log(target, "选择了", color1);
        if (cards1.length) {
          await target.addToExpansion(cards1, "draw").set("gaintag", ["hm_zhouyuan_expansion"]);
          target.addTempSkill("hm_zhouyuan_expansion", ["phaseChange", "phaseAfter", "phaseBeforeStart"]);
        }
        if (cards2.length) {
          await player.addToExpansion(cards2, "draw").set("gaintag", ["hm_zhouyuan_expansion"]);
          player.addTempSkill("hm_zhouyuan_expansion", ["phaseChange", "phaseAfter", "phaseBeforeStart"]);
        }
      }
    },
    subSkill: {
      expansion: {
        trigger: {
          global: "phaseUseEnd"
        },
        forced: true,
        popup: false,
        charlotte: true,
        filter(event, player) {
          return player.countExpansions("hm_zhouyuan_expansion");
        },
        async content(event, trigger, player) {
          const cards2 = player.getExpansions("hm_zhouyuan_expansion");
          game.log(player, `收回了${get.cnNumber(cards2.length)}张“咒兵”牌`);
          await player.gain(cards2, "draw");
        },
        onremove: true,
        intro: {
          markcount: "expansion",
          mark(dialog, storage, player) {
            const cards2 = player.getExpansions("hm_zhouyuan_expansion");
            if (player.isUnderControl(true)) {
              dialog.addAuto(cards2);
            } else {
              return `共有${get.cnNumber(cards2.length)}张牌`;
            }
          }
        }
      }
    },
    ai: {
      order: 7,
      result: {
        target: -1
      },
      threaten: 1.5
    }
  },
  hm_zhaobing: {
    trigger: {
      player: ["loseEnd", "dying", "phaseBefore", "phaseAfter", "dyingAfter", "die"],
      global: ["equipEnd", "addJudgeEnd", "gainEnd", "loseAsyncEnd", "addToExpansionEnd"]
    },
    filter(event, player) {
      return (game.hasPlayer((target) => target.countExpansions("hm_zhouyuan_expansion")) && event.name !== "die") !== player.hasSkill("hm_zhaobing_in");
    },
    forced: true,
    firstDo: true,
    silent: true,
    forceDie: true,
    async content(event, trigger, player) {
      if (game.hasPlayer((target) => target.countExpansions("hm_zhouyuan_expansion")) && trigger.name !== "die") {
        const cards2 = game.filterPlayer().map((target) => target.getExpansions("hm_zhouyuan_expansion")).flat();
        const cardsx = cards2.map((card) => {
          const cardx = ui.create.card();
          cardx.init(get.cardInfo(card));
          cardx._cardid = card.cardid;
          return cardx;
        });
        player.directgains(cardsx, null, "hm_zhaobing_tag");
        player.addSkill("hm_zhaobing_in");
      } else {
        player.removeSkill("hm_zhaobing_in");
      }
    },
    ai: {
      combo: "hm_zhouyuan"
    },
    subSkill: {
      tag: {},
      in: {
        charlotte: true,
        trigger: {
          global: ["addToExpansionEnd", "gainEnd", "loseEnd", "equipEnd", "addJudgeEnd", "loseAsyncEnd"]
        },
        filter(event, player) {
          return event.gaintag?.includes("hm_zhouyuan_expansion") || Object.values(event.gaintag_map || {})?.flat().includes("hm_zhouyuan_expansion");
        },
        forced: true,
        locked: false,
        silent: true,
        async content(event, trigger, player) {
          const cards2 = player.getCards("s", (card) => card.hasGaintag("hm_zhaobing_tag"));
          if (player.isOnline2()) {
            player.send(
              (cards4, player2) => {
                cards4.forEach((i) => i.delete());
                if (player2 === game.me) {
                  ui.updatehl();
                }
              },
              cards2,
              player
            );
          }
          cards2.forEach((i) => i.delete());
          if (player === game.me) {
            ui.updatehl();
          }
          const cards3 = game.filterPlayer().map((target) => target.getExpansions("hm_zhouyuan_expansion")).flat();
          const cardsx = cards3.map((card) => {
            const cardx = ui.create.card();
            cardx.init(get.cardInfo(card));
            cardx._cardid = card.cardid;
            return cardx;
          });
          player.directgains(cardsx, null, "hm_zhaobing_tag");
        },
        onremove(player) {
          const cards2 = player.getCards("s", (card) => card.hasGaintag("hm_zhaobing_tag"));
          if (player.isOnline2()) {
            player.send(
              (cards3, player2) => {
                cards3.forEach((i) => i.delete());
                if (player2 === game.me) {
                  ui.updatehl();
                }
              },
              cards2,
              player
            );
          }
          cards2.forEach((i) => i.delete());
          if (player === game.me) {
            ui.updatehl();
          }
        },
        group: "hm_zhaobing_use"
      },
      use: {
        charlotte: true,
        trigger: {
          player: ["useCardBefore", "respondBefore"]
        },
        filter(event, player) {
          const cards2 = player.getCards("s", (card) => card.hasGaintag("hm_zhaobing_tag") && card._cardid);
          return event.cards && event.cards.some((card) => cards2.includes(card));
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player) {
          player.getCards("s", (card) => card.hasGaintag("hm_zhaobing_tag")).map((i) => i._cardid);
          const cards2 = game.filterPlayer().map((target) => target.getExpansions("hm_zhouyuan_expansion")).flat();
          const cards22 = [];
          for (const card of trigger.cards) {
            const cardx = cards2.find((cardx2) => cardx2.cardid === card._cardid);
            if (cardx) {
              cards22.push(cardx);
            }
          }
          const cards3 = trigger.cards.slice();
          trigger.cards = cards22;
          trigger.card.cards = cards22;
          if (player.isOnline2()) {
            player.send(
              (cards4, player2) => {
                cards4.forEach((i) => i.delete());
                if (player2 === game.me) {
                  ui.updatehl();
                }
              },
              cards3,
              player
            );
          }
          cards3.forEach((i) => i.delete());
          if (player === game.me) {
            ui.updatehl();
          }
        }
      }
    }
  },
  //神张梁
  hm_jijun: {
    trigger: { target: "useCardToPlayered" },
    frequent: true,
    filter(event, player) {
      if (!event.isFirstTarget) {
        return false;
      }
      return event.player === player;
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    intro: {
      markcount: "expansion",
      mark(dialog, storage, player) {
        const cards2 = player.getExpansions("hm_jijun");
        if (player.isUnderControl(true)) {
          dialog.addAuto(cards2);
        } else {
          return `共有${get.cnNumber(cards2.length)}张牌`;
        }
      }
    },
    async content(event, trigger, player) {
      const next = player.judge();
      const result = await next.forResult();
      const card = result?.card;
      if (!card || get.owner(card)) {
        return;
      }
      const next2 = player.chooseControl([`获得${get.translation(card)}`, `将${get.translation(card)}置于武将牌`]).set("ai", () => {
        const { player: player2, cardx } = get.event();
        if (player2.getUseValue(cardx) > 3) {
          return 0;
        }
        return player2.hasSkill("hm_fangtong") ? 1 : 0;
      }).set("cardx", card);
      const result2 = await next2.forResult();
      if (result2.index === 0) {
        await player.gain(card, "gain2");
      } else {
        const next3 = player.addToExpansion("giveAuto", card, player);
        next3.gaintag.add(event.name);
        await next3;
      }
    }
  },
  hm_fengtong: {
    trigger: { player: "phaseUseEnd" },
    getAuto(player) {
      const hs = player.getCards("h");
      const ss = player.getExpansions("xinfu_jijun");
      const max = Math.pow(2, ss.length);
      let index;
      let selectedCard;
      search: for (const card of hs) {
        for (let j = 1; j < max; j++) {
          let num = get.number(card);
          index = j.toString(2);
          while (index.length < ss.length) {
            index = `0${index}`;
          }
          for (const [k, expansion] of ss.entries()) {
            if (index[k] === "1") {
              num += get.number(expansion);
            }
          }
          if (num === 36) {
            selectedCard = card;
            break search;
          }
        }
      }
      if (!selectedCard) {
        return [];
      }
      const list = [selectedCard];
      for (const [k, expansion] of ss.entries()) {
        if (index[k] === "1") {
          list.push(expansion);
        }
      }
      return list;
    },
    filter(event, player) {
      return player.getExpansions("hm_jijun").length && player.hasCard(lib.filter.cardRecastable, "h");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard(get.prompt2(event.skill), "h", lib.filter.cardRecastable).forResult();
    },
    async content(event, trigger, player) {
      const { cards: cards2 } = event;
      await player.recast(cards2);
      const expansions = player.getExpansions("hm_jijun");
      if (!expansions.length) {
        return;
      }
      let result;
      const next = player.chooseCardButton(expansions, [1, Infinity]);
      next.set("num", get.number(cards2[0]));
      next.set("filterOk", () => {
        let sum = get.event().num;
        ui.selected.buttons.forEach((button) => {
          const num = get.number(button.link);
          if (typeof num === "number") {
            sum += num;
          }
        });
        return sum === 36;
      });
      next.set("autolist", lib.skill.xinfu_fangtong.getAuto(player));
      next.set("processAI", () => {
        if (_status.event.autolist?.length) {
          return {
            bool: true,
            links: _status.event.autolist
          };
        }
        return { bool: false };
      });
      next.set("complexSelect", true);
      result = await next.forResult();
      if (result?.bool && result?.links?.length) {
        await player.loseToDiscardpile(result.links);
        if (!game.hasPlayer((current) => player !== current)) {
          return;
        }
        const next2 = player.chooseTarget("【方统】：对一名其他角色造成3点雷电伤害", lib.filter.notMe, true);
        next2.set("ai", (target) => {
          const player2 = get.player();
          return get.damageEffect(player2, target, player2, "thunder");
        });
        result = await next2.forResult();
        if (result?.bool && result?.targets?.length) {
          await result.targets[0].damage("thunder", 3, player);
        }
      }
    },
    ai: { combo: "hm_jijun" }
  },
  //三兄弟都有的玩意
  hm_sanshou: {
    trigger: {
      player: "phaseChange"
    },
    filter(event, player) {
      if (event.phaseList[event.num].startsWith("phaseZhunbei") || event.phaseList[event.num].startsWith("phaseJieshu")) {
        return true;
      }
      return false;
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.phaseList[trigger.num] = `phaseUse|${event.name}`;
      const newPair = [];
      for (const i of [player.name1, player.name2]) {
        if (!i) {
          continue;
        }
        if (i === "hm_shen_zhangjiao") {
          const next = player.chooseButton(["请选择变身对象", [["hm_shen_zhangbao", "hm_shen_zhangliang"], "character"]], true);
          next.set("ai", (button) => Math.random() - 1);
          const result = await next.forResult();
          if (result.bool) {
            newPair.push(result.links[0]);
          }
        } else {
          newPair.push(i);
        }
      }
      await player.changeCharacter(newPair);
      player.addSkill("hm_sanshou_back");
    },
    subSkill: {
      back: {
        trigger: {
          player: "phaseUseAfter"
        },
        silent: true,
        charlotte: true,
        async content(event, trigger, player) {
          const newPair = [];
          for (const i of [player.name1, player.name2]) {
            if (!i) {
              continue;
            }
            if (["hm_shen_zhangbao", "hm_shen_zhangliang"].includes(i)) {
              newPair.push("hm_shen_zhangjiao");
            } else {
              newPair.push(i);
            }
          }
          await player.changeCharacter(newPair);
          player.removeSkill("hm_sanshou_back");
        }
      }
    }
  }
};
const skills$j = {
  //荆襄风云
  jxxiongzi: {
    audio: "reyingzi",
    trigger: {
      player: "phaseDrawBegin2"
    },
    forced: true,
    preHidden: true,
    filter(event, player) {
      return !event.numFixed;
    },
    async content(event, trigger, player) {
      trigger.num += player.hp;
    },
    ai: {
      threaten: 1.5
    },
    mod: {
      maxHandcard(player, num) {
        return num + player.hp;
      }
    }
  },
  jxzhanyan: {
    enable: "phaseUse",
    usable: 1,
    audio: "dcsbronghuo",
    filter(event, player) {
      return player.hasCards("h");
    },
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player) {
      const target = event.targets[0];
      const list = Array.from(Array(player.countCards("h") + 1)).map((i, p) => p);
      const dialog = [`猜测${get.translation(player)}拥有的红色手牌数量`];
      while (list.length) {
        const nums = list.slice(0, Math.min(10, list.length));
        list.removeArray(nums);
        dialog.push([nums, "tdnodes"]);
      }
      const result = await target.chooseButton(dialog, true).set("ai", () => Math.random()).forResult();
      if (result.bool) {
        target.chat(`我猜你有${result.links[0]}张红色牌！`);
        game.log(target, "猜测", player, "有红色牌", `#g${result.links[0]}张`);
        if (event.isMine() && !event.isOnline()) {
          await game.delay();
        }
        await player.showHandcards(`${get.translation(player)}发动了【绽焰】`);
        const num = Math.min(3, Math.abs(result.links[0] - player.countCards("h", (card) => get.color(card, player) === "red")));
        const redCards = player.getCards("he", (card) => get.color(card, player) === "red");
        if (redCards.length) {
          await player.give(redCards, target);
        }
        if (num > 0) {
          await target.damage("fire", num);
        }
      }
    },
    ai: {
      order: 3,
      result: {
        target(player, target) {
          return get.damageEffect(target, player, target) + player.countCards("he", { color: "red" });
        }
      }
    }
  },
  jxwusheng: {
    mod: {
      targetInRange(card) {
        if (get.suit(card) === "diamond" && card.name === "sha") {
          return true;
        }
      }
    },
    locked: false,
    audio: "wusheng",
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      return get.inpileVCardList((info) => {
        const name = info[2];
        if (info[3]) {
          return false;
        }
        if (name !== "sha" && name !== "jiu") {
          return false;
        }
        return get.type(name) === "basic";
      }).some((card) => player.hasCard((cardx) => get.color(cardx, player) === "red" && event.filterCard({ name: card[2], nature: card[3], cards: [cardx] }, player, event), "hes"));
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          const name = info[2];
          if (info[3]) {
            return false;
          }
          if (name !== "sha" && name !== "jiu") {
            return false;
          }
          return get.type(name) === "basic";
        }).filter((card) => player.hasCard((cardx) => get.color(cardx, player) === "red" && event.filterCard({ name: card[2], nature: card[3], cards: [cardx] }, player, event), "hes"));
        return ui.create.dialog("武圣", [list, "vcard"]);
      },
      filter(button, player) {
        return _status.event.getParent().filterCard({ name: button.link[2], nature: button.link[3] }, player, _status.event.getParent());
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = get.event().player;
        const value = player.getUseValue({ name: button.link[2], nature: button.link[3] });
        return value;
      },
      backup(links, player) {
        return {
          audio: "wusheng",
          filterCard(card, player2) {
            return get.color(card, player2) === "red";
          },
          popname: true,
          check(card) {
            return 6 - get.value(card);
          },
          position: "hse",
          viewAs: { name: links[0][2], nature: links[0][3] }
        };
      },
      prompt(links, player) {
        return `将一张牌当作${get.translation(links[0][3]) || ""}【${get.translation(links[0][2])}】使用或打出`;
      }
    },
    hiddenCard(player, name) {
      if (name !== "jiu") {
        return false;
      }
      return player.hasCards("hes", { color: "red" });
    },
    ai: {
      skillTagFilter(player) {
        if (!player.hasCards("hes", { color: "red" })) {
          return false;
        }
      },
      respondSha: true
    },
    subSkill: { backup: {} }
  },
  //神曹仁
  jxjushou: {
    trigger: {
      player: "phaseJieshuBegin"
    },
    check(event, player) {
      if (game.countPlayer() > 4) {
        return true;
      }
      return event.player.hp + player.countCards("h") < 4;
    },
    async content(event, trigger, player) {
      const num = game.countPlayer();
      await player.turnOver();
      await player.draw(num);
      let eff = num > 4 ? 4 * (4 - num) : 0;
      for (const current of game.players) {
        eff += get.sgnAttitude(player, current) * (current.countCards("e") + 3 + current.isTurnedOver() ? 5 : -5);
      }
      const result = await player.chooseBool("是否令所有角色翻面并摸三张牌？").set("choice", eff > 0).forResult();
      if (result.bool) {
        for (const current of game.players.sortBySeat(player)) {
          if (current.isIn()) {
            await current.turnOver();
            await current.draw(3);
          }
        }
        const lose_list = [];
        for (const current of game.players.sortBySeat(player)) {
          if (current.countCards("e") && current.isIn()) {
            lose_list.push([current, current.getCards("e")]);
          }
        }
        if (lose_list.length) {
          await game.loseAsync({
            lose_list,
            discarder: player
          }).setContent("discardMultiple");
        }
        await player.changeSkills(["jxtuwei"], ["jxjushou"]);
      }
    },
    ai: {
      effect: {
        target(card, player, target) {
          if (card.name === "guiyoujie") {
            return [0, 1];
          }
        }
      }
    },
    derivation: ["jxtuwei"]
  },
  jxtuwei: {
    enable: "phaseUse",
    intro: {
      content: "已对$发动过【突围】"
    },
    onremove: true,
    onChooseToUse(event) {
      if (!event.jxtuwei && !game.online) {
        get.player();
        const cards2 = Array.from(ui.discardPile.childNodes).filter((card) => get.type(card) === "equip");
        event.set("jxtuwei", cards2);
      }
    },
    filter(event, player) {
      if (!game.hasPlayer((current) => !player.getStorage("jxtuwei").includes(current))) {
        return false;
      }
      return event.jxtuwei && event.jxtuwei.length;
    },
    chooseButton: {
      dialog(event, player) {
        const list2 = event.jxtuwei;
        const dialog = ui.create.dialog('###突围###<div class="text center">请选择一张装备牌置入一名其他角色的装备区</div>');
        if (list2.length) {
          dialog.add(list2);
        }
        return dialog;
      },
      check(button) {
        const player = _status.event.player;
        const num = get.value(button.link);
        if (!game.hasPlayer((target) => !player.getStorage("jxtuwei").includes(target) && get.attitude(player, target) > 0)) {
          return num;
        }
        return 5 / num;
      },
      backup(links, player) {
        return {
          card: links[0],
          filterTarget(card, player2, target) {
            return !player2.getStorage("jxtuwei").includes(target) && target.canEquip(links[0], true);
          },
          check: () => 1,
          async content(event, trigger, player2) {
            const cardx = lib.skill.jxtuwei_backup.card;
            const target = event.targets[0];
            target.$gain2(cardx);
            await game.delayx();
            await target.equip(cardx);
            player2.markAuto("jxtuwei", target);
            if (target !== player2) {
              const result = await player2.chooseControl("令其摸一张牌", "对其造成1点伤害", "cancel2").set("ai", () => _status.event.choice).set(
                "choice",
                (() => {
                  if (get.damageEffect(target, player2, player2) > 0) {
                    return "对其造成1点伤害";
                  }
                  if (get.effect(target, { name: "draw" }, player2, player2) > 0) {
                    return "令其摸一张牌";
                  }
                  return "cancel2";
                })()
              ).forResult();
              if (result.index === 0) {
                await target.draw();
              }
              if (result.index === 1) {
                player2.line(target, "green");
                await target.damage();
              }
            }
          },
          ai: {
            result: {
              target(player2, target) {
                const att = get.attitude(player2, target);
                if (att > 0) {
                  return 3;
                }
                if (att < 0) {
                  return -1;
                }
                return 0;
              }
            }
          }
        };
      },
      prompt(links, player) {
        return `请选择置入${get.translation(links)}的角色`;
      }
    },
    subSkill: {
      backup: {}
    }
  },
  //神刘表
  jxxiongju: {
    trigger: {
      global: ["phaseBefore", "gameDrawBegin"],
      player: "enterGame"
    },
    forced: true,
    filter(event, player) {
      if (event.name === "gameDraw") {
        return true;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async content(event, trigger, player) {
      if (trigger.name === "gameDraw") {
        const me = player;
        const numx = trigger.num;
        trigger.num = (player2) => (player2 === me ? game.countGroup() : 0) + (typeof numx === "function" ? numx(player2) : numx);
        return;
      }
      const cards2 = [];
      while (cards2.length < 2) {
        const card = game.createCard2("jingxiangshengshi", "heart", 5);
        cards2.push(card);
      }
      if (cards2.length) {
        await player.gain(cards2, "gain2");
      }
      const num = game.countGroup();
      await player.gainMaxHp(num);
      await player.recover(num);
    },
    mod: {
      maxHandcard(player, num) {
        return num + game.countGroup();
      }
    }
  },
  jxfujing: {
    trigger: {
      player: "phaseDrawBefore"
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.cancel();
      const card = { name: "jingxiangshengshi", isCard: true };
      if (game.countPlayer((current) => player.canUse(card, current)) < game.countGroup()) {
        return;
      }
      await player.chooseUseTarget(card, true);
      for (const target of game.players) {
        if (target.getHistory("gain", (evt) => evt.getParent(event.name) === event).length && target !== player) {
          target.addTempSkill("jxfujing_effect", "roundStart");
          target.markAuto("jxfujing_effect", player);
        }
      }
    },
    subSkill: {
      effect: {
        mark: true,
        intro: {
          content: "本轮下一次对$使用牌时须弃置一张牌"
        },
        onremove: true,
        trigger: {
          player: "useCardToPlayer"
        },
        filter(event, player) {
          return player.getStorage("jxfujing_effect").includes(event.target);
        },
        forced: true,
        charlotte: true,
        async content(event, trigger, player) {
          const target = trigger.target;
          if (player.countCards("he")) {
            await player.chooseToDiscard("he", true);
          }
          player.unmarkAuto("jxfujing_effect", [target]);
          if (!player.getStorage("jxfujing_effect").length) {
            player.removeSkill(event.name);
          }
        }
      }
    }
  },
  jxyongrong: {
    trigger: {
      source: "damageBegin1",
      player: "damageBegin3"
    },
    usable: 1,
    filter(event, player, name) {
      const target = name === "damageBegin1" ? event.player : event.source;
      return target && target.isIn() && target.countCards("h") < player.countCards("h");
    },
    async cost(event, trigger, player) {
      const target = event.triggername === "damageBegin1" ? trigger.player : trigger.source;
      const prompt2 = `交给其一张牌并令此伤害${event.triggername === "damageBegin1" ? "+" : "-"}1`;
      const result = await player.chooseCard(get.prompt(event.skill, target), prompt2, "he").set("ai", (card) => {
        const eff = _status.event.eff;
        const isPlayer = _status.event.isPlayer;
        if (isPlayer && eff < 0 || !isPlayer && eff > 0) {
          return 6 - get.value(card);
        }
        return 0;
      }).set("eff", get.damageEffect(trigger.player, trigger.source, player)).set("isPlayer", player === trigger.player).forResult();
      event.result = {
        bool: result.bool,
        cards: result.cards,
        targets: [target]
      };
    },
    async content(event, trigger, player) {
      await player.give(event.cards, event.targets[0]);
      if (event.triggername === "damageBegin1") {
        trigger.num++;
      } else {
        trigger.num--;
      }
    }
  }
};
const skills$i = {
  //汉末神王允
  caanchao: {
    trigger: {
      global: "phaseAfter"
    },
    frequent: true,
    filter(event, player) {
      return game.hasPlayer2((current) => current.hasHistory("useCard", (evt) => get.is.convertedCard(evt.card) || get.is.virtualCard(evt.card)), true);
    },
    async content(event, trigger, player) {
      await player.draw();
      player.addCharge();
    }
  },
  cayurong: {
    trigger: { target: "useCardToTarget" },
    filter(event, player) {
      if (!get.is.damageCard(event.card)) {
        return false;
      }
      if (player.getStorage("cayurong_targeted").includes(event.card.name)) {
        return false;
      }
      return true;
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.getParent().excluded.add(player);
      player.addTempSkill("cayurong_targeted", "roundStart");
      player.markAuto("cayurong_targeted", [trigger.card.name]);
    },
    subSkill: {
      targeted: {
        onremove: true,
        charlotte: true
      }
    }
  },
  cadingxi: {
    beginMarkCount: 4,
    chargeSkill: Infinity,
    locked: false,
    enable: "chooseToUse",
    init(player, skill) {
      const num = lib.skill[skill].beginMarkCount;
      player.addCharge(num, false);
    },
    getCanUse(event, player) {
      return lib.inpile.filter((i) => event.filterCard(get.autoViewAs({ name: i }, "unsure"), player, event));
    },
    hiddenCard(player, name) {
      if (lib.inpile.includes(name)) {
        return player.countCharge();
      }
    },
    filter(event, player) {
      if (event.responded || !player.countCharge() || event.cadingxi_result) {
        return false;
      }
      return lib.skill["cadingxi"].getCanUse(event, player).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = lib.skill["cadingxi"].getCanUse(event, player).map((name) => get.type2(name)).unique();
        const dialog = ui.create.dialog("定西", [list.map((type) => ["", "", `caoying_${type}`]), "vcard"]);
        dialog.direct = true;
        return dialog;
      },
      check(event, player) {
        return Math.random();
      },
      backup(links, player) {
        const type = links[0][2].slice(8);
        return {
          type,
          filterCard: () => false,
          selectCard: -1,
          popname: true,
          async content(event, trigger, player2) {
            const type2 = lib.skill[event.name].type;
            const evt = event.getParent(2);
            const filterCard = evt.name === "_wuxie" ? (card2, player3, event2) => card2.name === "wuxie" : evt.filterCard;
            player2.removeCharge();
            const cards2 = get.cards(1, true);
            const card = cards2[0];
            await player2.showCards(cards2, `${get.translation(player2)}发动了【定西】`);
            let key;
            switch (evt.name) {
              case "_wuxie":
                key = "wuxieresult2";
                break;
              default:
                key = "result";
            }
            if (get.type2(card) === type2) {
              event.getParent().set("cadingxi_result", true);
              const history = player2.getAllHistory("useSkill", (evt2) => evt2.skill === event.name);
              const list = history.slice(-Math.min(3, history.length)).map((evt2) => evt2.event.cadingxi_result);
              if (list.slice(-2).length >= 2 && !list.slice(-2).some((i) => !i)) {
                await player2.recoverTo(player2.maxHp);
              }
              if (list.length >= 3 && !list.some((i) => !i)) {
                const damage = async (target) => {
                  await target.damage();
                };
                const targets = game.filterPlayer((target) => target !== player2);
                player2.line(targets);
                await game.doAsyncInOrder(targets, damage);
              }
              if (filterCard(get.autoViewAs(card), player2, evt)) {
                if (evt.name === "chooseToUse") {
                  game.broadcastAll(
                    (result, name) => {
                      lib.skill.cadingxi_backup2.viewAs = { name, cards: [result], isCard: true };
                    },
                    card,
                    card.name
                  );
                  evt.set("_backupevent", "cadingxi_backup2");
                  evt.set("openskilldialog", `请选择${get.translation(card)}的目标`);
                  evt.backup("cadingxi_backup2");
                } else {
                  delete evt[key].used;
                  evt[key].card = get.autoViewAs(card);
                  evt[key].cards = [card];
                  delete evt[key].skill;
                  evt.redo();
                  return;
                }
              }
            } else {
              event.getParent().set("cadingxi_result", false);
              await player2.draw("bottom");
            }
            evt.goto(0);
          }
        };
      }
    },
    mod: {
      aiOrder(player, card, num) {
        if (typeof card === "object" && player.isPhaseUsing()) {
          const evt = lib.skill.dcjianying.getLastUsed(player);
          if (evt && evt.card && get.type2(evt.card) && get.type2(evt.card) === get.type2(card)) {
            return num + 10;
          }
        }
      }
    },
    subSkill: {
      backup2: {
        async precontent(event, trigger, player) {
          const name = event.result?.card?.name;
          const cards2 = event.result.card?.cards.slice(0);
          event.result.cards = cards2;
          const rcard = cards2[0];
          let card;
          if (rcard.name === name) {
            card = get.autoViewAs(rcard);
          } else {
            card = get.autoViewAs({ name, isCard: true });
          }
          event.result.card = card;
        },
        filterCard: () => false,
        selectCard: -1,
        log: false
      },
      backup: {},
      init: {
        trigger: {
          player: "enterGame",
          global: "phaseBefore"
        },
        filter(event, player) {
          if (!player.countCharge(true)) {
            return false;
          }
          return event.name !== "phase" || game.phaseNumber === 0;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          player.addCharge(4);
        }
      }
    }
  },
  //汉末神曹
  cazhaoshao: {
    trigger: {
      player: "damageEnd",
      source: "damageSource"
    },
    getIndex(event) {
      return event.num;
    },
    frequent: true,
    async content(event, trigger, player) {
      await player.draw();
      const targets = [];
      let target;
      for (const current of [trigger.source, trigger.player]) {
        if (current?.isIn()) {
          targets.add(current);
        }
      }
      if (!targets.length) {
        return;
      }
      if (targets.length > 1) {
        const result = await player.chooseTarget("选择执行【诏绍】的目标", true, (card, player2, target2) => {
          const trigger2 = get.event().getTrigger();
          return target2 === trigger2.player || target2 === trigger2.source;
        }).set("ai", (target2) => {
          const player2 = get.player();
          return get.attitude(player2, target2) * (Math.random() - 0.5);
        }).forResult();
        if (!result.bool) {
          return;
        }
        target = result.targets[0];
      } else {
        target = targets[0];
      }
      player.line(target, "green");
      const choices = ["获得弃牌堆或场上一张装备牌并使用", "翻面并摸一张牌", "减少1点体力上限"];
      const { control } = await player.chooseControl().set("choiceList", choices).set("prompt", `令${get.translation(target)}执行一项`).set("target", target).set("ai", () => {
        const { player: player2, target: target2 } = get.event();
        let eff = 1;
        for (const current of game.players) {
          if (current === target2) {
            continue;
          }
          eff += get.effect(current, { name: "losehp" }, current, player2);
        }
        if (target2.hasSkill("caxiaoxiong") && eff > 0) {
          return "选项二";
        }
        if (get.attitude(player2, target2) > 0) {
          return target2.isTurnedOver() ? "选项二" : "选项一";
        }
        if (!target2.isTurnedOver() && Math.random() < 0.2) {
          return "选项二";
        }
        return "选项三";
      }).forResult();
      switch (control) {
        case "选项一": {
          let bool = false;
          let equip;
          if (game.hasPlayer((current) => current.countGainableCards(target, "e"))) {
            const result2 = await target.chooseTarget("获得场上的一张装备牌，或点取消从弃牌堆获得一张装备牌", (card, player2, target2) => target2.countGainableCards(player2, "e")).set("ai", (target2) => {
              const player2 = get.player();
              if (get.attitude(player2, target2) < 0) {
                if (target2.countCards("e", (card) => get.value(card, player2) >= 6)) {
                  return 12;
                }
                return 0;
              }
              return 0;
            }).forResult();
            if (result2.bool) {
              bool = result2.targets[0];
            }
          }
          if (bool) {
            const result3 = await target.gainPlayerCard(bool, "e", true).forResult();
            equip = result3.links[0];
          } else {
            equip = get.discardPile((card) => get.type(card) === "equip" && get.subtypes(card)?.length);
            if (equip) {
              await target.gain(equip, "gain2");
            } else {
              break;
            }
            game.updateRoundNumber();
          }
          target.addSkill("cazhaoshao_equip");
          if (target.getStorage("cazhaoshao_equip").length) {
            const list = target.getStorage("cazhaoshao_equip");
            const equips = target.getCards("e", (card) => list[1].includes(card));
            if (equips.length) {
              await target.loseToDiscardpile(equips);
            }
          }
          const subtypes = get.subtypes(equip);
          await target.expandEquip(subtypes);
          await target.equip(equip);
          target.setStorage("cazhaoshao_equip", [subtypes, [equip]]);
          break;
        }
        case "选项二": {
          await target.turnOver();
          await target.draw();
          break;
        }
        case "选项三": {
          await target.loseMaxHp();
          break;
        }
      }
    },
    closeEquip() {
      const next = game.createEvent("closenEquip");
      next.slots = [];
      for (const argument of arguments) {
        if (get.itemtype(argument) === "player") {
          if (!next.player) {
            next.player = argument;
          } else {
            next.source = argument;
          }
        } else if (Array.isArray(argument)) {
          for (const arg of argument) {
            if (typeof arg === "string") {
              if (arg.startsWith("equip") && parseInt(arg.slice(5)) > 0) {
                next.slots.push(arg);
              }
            } else if (typeof arg === "number") {
              next.slots.push(`equip${arg}`);
            }
          }
        } else if (typeof argument === "string") {
          if (argument.startsWith("equip") && parseInt(argument.slice(5)) > 0) {
            next.slots.push(argument);
          }
        } else if (typeof argument === "number") {
          next.slots.push(`equip${argument}`);
        }
      }
      if (!next.player) {
        next.player = get.player();
      }
      if (!next.source) {
        next.source = get.player();
      }
      if (!next.slots.length) {
        _status.event.next.remove(next);
        next.resolve();
      }
      next.setContent(lib.skill.cazhaoshao.closenEquip);
      return next;
    },
    async closenEquip(event, trigger, player) {
      const slotsx = [];
      if (get.is.mountCombined()) {
        event.slots.forEach((type) => {
          if (type === "equip3" || type === "equip4") {
            slotsx.add("equip3_4");
          } else {
            slotsx.add(type);
          }
        });
      } else {
        slotsx.addArray(event.slots);
      }
      slotsx.sort();
      for (const slot of slotsx) {
        let expand = get.numOf(event.slots, slot);
        let slot_key = slot;
        if (slot === "equip3_4") {
          expand = Math.max(get.numOf(event.slots, "equip3"), get.numOf(event.slots, "equip4"));
          slot_key = "equip3";
        }
        game.log(player, `失去了${get.cnNumber(expand)}个额外的`, `#g${get.translation(slot)}栏`);
        if (!player.expandedSlots) {
          player.expandedSlots = {};
        }
        if (!player.expandedSlots[slot_key]) {
          player.expandedSlots[slot_key] = 0;
        }
        player.expandedSlots[slot_key] -= expand;
      }
      player.$syncExpand();
    },
    subSkill: {
      equip: {
        charlotte: true,
        trigger: {
          player: "loseAfter",
          global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
        },
        filter(event, player) {
          if (!player.getStorage("cazhaoshao_equip").length) {
            return false;
          }
          const evt = event.getl(player);
          const list = player.getStorage("cazhaoshao_equip");
          if (evt?.player === player && evt.es) {
            return evt.es.some((card) => list[1].includes(card));
          }
        },
        direct: true,
        forced: true,
        async content(event, trigger, player) {
          await lib.skill.cazhaoshao.closeEquip(player, player.getStorage("cazhaoshao_equip")[0]);
          player.setStorage("cazhaoshao_equip", []);
        },
        mod: {
          canBeReplaced(card, player) {
            const list = player.getStorage("cazhaoshao_equip");
            if (!list.length) {
              return;
            }
            const cards2 = player.getVCards("e", (card2) => (card2?.cards || []).some((cardx) => list[1].includes(cardx)));
            if (cards2 && cards2.includes(card)) {
              return false;
            }
          }
        },
        sub: true
      }
    }
  },
  caxiaoxiong: {
    trigger: {
      player: "turnOverBegin"
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.cancel();
      const targets = game.players.sortBySeat().slice();
      for (const target of targets) {
        if (target === player) {
          continue;
        }
        await target.loseHp();
      }
    }
  },
  //神李郭
  caweijue: {
    forced: true,
    trigger: {
      player: "phaseZhunbeiBegin"
    },
    async content(event, trigger, player) {
      let target = player.getNext();
      while (target !== player) {
        if (!target.countCards("h")) {
          target = target.getNext();
          continue;
        }
        const result = await target.chooseCard("h", "将任意张手牌当作“威”置于武将牌上", [1, Infinity], true, "allowChooseAll").set("ai", () => -1).forResult();
        if (result.bool) {
          const cards2 = result.cards;
          const next = target.addToExpansion(cards2, "giveAuto", target);
          next.gaintag.add("caweijue_tag");
          await next;
          target.addSkill("caweijue_tag");
        }
        target = target.getNext();
      }
    },
    mod: {
      inRangeOf(from, to) {
        const num1 = from.countExpansions("caweijue_tag");
        const num2 = from.countCards("h");
        if (num1 <= num2) {
          return true;
        }
      },
      inRange(from, to) {
        const num1 = to.countExpansions("caweijue_tag");
        const num2 = to.countCards("h");
        if (num1 <= num2) {
          return true;
        }
      }
    },
    subSkill: {
      tag: {
        trigger: {
          global: "phaseEnd"
        },
        forced: true,
        popup: false,
        charlotte: true,
        filter(event, player) {
          return player.getExpansions("caweijue_tag").length > 0;
        },
        async content(event, trigger, player) {
          const cards2 = player.getExpansions("caweijue_tag");
          await player.gain(cards2, "draw");
          game.log(player, `收回了${get.cnNumber(cards2.length)}张“威”牌`);
          player.removeSkill("caweijue_tag");
        },
        marktext: "威",
        intro: {
          content: "expansion",
          markcount: "expansion"
        }
      }
    }
  },
  cachuxiong: {
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player) {
      return player.countCards("h");
    },
    check(event, player) {
      const colors = player.getCards("h").map((card) => get.color(card, player)).toUniqued();
      for (const color of colors) {
        if (lib.skill.cachuxiong.getVal(color, player) > 0) {
          return true;
        }
      }
      return false;
    },
    getVal(color, player) {
      let val = 0;
      const cards2 = player.getCards("h", { color });
      val -= cards2.length;
      if (color === "black") {
        val += Math.min(
          cards2.length,
          game.players.reduce((sum, current) => {
            if (get.attitude(player, current) > 0) {
              return sum;
            }
            return sum + current.countExpansions("caweijue_tag");
          }, 0)
        );
        val++;
      } else if (color === "red") {
        val += game.players.reduce((sum, current) => {
          if (!player.inRange(current) || player === current) {
            return sum;
          }
          return sum + get.damageEffect(current, player, player);
        }, 0);
      }
      return val;
    },
    async content(event, trigger, player) {
      await player.showHandcards(`${get.translation(player)}发动了【除凶】`);
      const colors = player.getCards("h").map((card) => get.color(card, player)).toUniqued();
      if (!colors.length) {
        return;
      }
      colors.sort((a, b) => lib.skill.cachuxiong.getVal(b, player) - lib.skill.cachuxiong.getVal(a, player));
      const result = await player.chooseControl(colors).set("prompt", "弃置一种颜色的所有手牌").set("resultC", colors[0]).set("ai", () => get.event().resultC).forResult();
      if (result.control) {
        const color = result.control;
        const cards2 = player.getDiscardableCards(player, "h").filter((card) => get.color(card, player) === color);
        if (cards2.length) {
          await player.discard(cards2);
        }
        if (color === "black") {
          let num = cards2.length;
          while (num > 0) {
            if (!game.hasPlayer((current) => current.countExpansions("caweijue_tag") > 0)) {
              break;
            }
            const result2 = await player.chooseTarget("获得一名角色的“威”", true, (card, player2, target2) => target2.countExpansions("caweijue_tag")).set("ai", (target2) => {
              const player2 = get.player();
              return -get.attitude(player2, target2);
            }).forResult();
            if (!result2.bool) {
              break;
            }
            const target = result2.targets[0];
            const result3 = await player.chooseCardButton(`选择获得至多${get.cnNumber(num)}张“威”`, [1, num], target.getExpansions("caweijue_tag"), true).forResult();
            if (!result3.bool) {
              break;
            }
            const cards3 = result3.links;
            player.line(target);
            await player.gain(cards3, "give", target, "bySelf");
            num -= cards3.length;
          }
        } else if (color === "red") {
          const targets = game.filterPlayer((current) => current !== player && player.inRange(current)).sortBySeat();
          if (targets.length) {
            for (const target of targets) {
              await target.damage();
            }
          }
        }
      }
    }
  },
  //长安樊稠
  caxingwei: {
    audio: "xinfu_xingluan",
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    frequent: true,
    filter(event, player) {
      return event.getg(player).some((i) => get.owner(i) === player && get.color(i, player) === "red");
    },
    async content(event, trigger, player) {
      const cards2 = trigger.getg(player).filter((i) => get.owner(i) === player && get.color(i, player) === "red");
      await player.showCards(cards2, `${get.translation(player)}发动了【兴威】`);
      await player.draw(cards2.length, "nodelay");
    },
    subfrequent: ["gain"],
    group: "caxingwei_gain",
    subSkill: {
      gain: {
        trigger: {
          player: ["phaseZhunbeiBegin", "damageEnd"]
        },
        prompt2: "获得弃牌堆里的一张红色牌",
        frequent: true,
        async content(event, trigger, player) {
          const card = get.discardPile((card2) => get.color(card2, false) === "red");
          if (card) {
            await player.gain(card, "gain2");
          }
        }
      }
    }
  },
  caqianmu: {
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      if (!player.countCards("hse")) {
        return false;
      }
      for (const i of lib.inpile) {
        const type = get.type2(i);
        if (!event.filterCard(get.autoViewAs({ name: i }, "unsure"), player, event)) {
          continue;
        }
        if (!player.countCards("hes", { suit: type === "basic" ? "diamond" : "heart" })) {
          continue;
        }
        if (["basic", "trick"].includes(type) && !player.getStorage("caqianmu_used").includes(type)) {
          return true;
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player) {
        const list = [];
        for (const name of lib.inpile) {
          if (!event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
            continue;
          }
          const type = get.type2(name);
          if (!["basic", "trick"].includes(type)) {
            continue;
          }
          if (!player.countCards("hes", { suit: type === "basic" ? "diamond" : "heart" })) {
            continue;
          }
          if (player.getStorage("caqianmu_used").includes(type)) {
            continue;
          }
          if (name === "sha") {
            list.push([type, "", "sha"]);
            for (const nature of lib.inpile_nature) {
              if (event.filterCard(get.autoViewAs({ name, nature }, "unsure"), player, event)) {
                list.push([type, "", "sha", nature]);
              }
            }
          } else {
            list.push([type, "", name]);
          }
        }
        return ui.create.dialog("浅目", [list, "vcard"]);
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = _status.event.player;
        if (["wugu", "zhulu_card", "yiyi", "lulitongxin", "lianjunshengyan", "diaohulishan"].includes(button.link[2])) {
          return 0;
        }
        return player.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard(card, player2) {
            return get.suit(card, player2) === (links[0][0] === "basic" ? "diamond" : "heart");
          },
          audio: "caqianmu",
          popname: true,
          check(card) {
            return 8 - get.value(card);
          },
          position: "hse",
          viewAs: { name: links[0][2], nature: links[0][3] },
          async precontent(event, trigger, player2) {
            player2.addTempSkill("caqianmu_used");
            player2.markAuto("caqianmu_used", get.type2(event.result.card));
            player2.showCards(event.result.cards);
          }
        };
      },
      prompt(links, player) {
        const suit = links[0][0] === "basic" ? "diamond" : "heart";
        return `将一张${get.translation(suit)}牌当做${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}使用或打出`;
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name)) {
        return false;
      }
      const type = get.type2(name);
      const suit = type === "basic" ? "diamond" : "heart";
      return (type === "basic" || type === "trick") && !player.getStorage("caqianmu_used").includes(type) && player.countCards("she", { suit });
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player) {
        if (player.getStorage("caqianmu_used").includes("basic") || !player.countCards("she", { suit: "diamond" })) {
          return false;
        }
      },
      order: 1,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      used: {
        onremove: true,
        charlotte: true
      },
      backup: {}
    }
  },
  //长安张济
  casilve: {
    audio: "xinfu_tunjun",
    trigger: { player: "phaseDrawBegin1" },
    filter(event, player) {
      return !event.numFixed;
    },
    async cost(event, trigger, player) {
      const result = await player.chooseTarget(
        get.prompt(event.skill),
        "改为获得任意名角色至多两张牌",
        [1, 2],
        (card, player2, target) => target.countCards("h") > 0,
        (target) => {
          const att = get.attitude(_status.event.player, target);
          if (target.hasSkill("tuntian")) {
            return att / 10;
          }
          return 1 - att;
        }
      ).forResult();
      event.result = result;
    },
    async content(event, trigger, player) {
      const targets = event.targets;
      if (targets.length > 1) {
        await player.gainMultiple(targets, "he");
      } else {
        await player.gainPlayerCard(targets[0], [1, 2], "he", true);
      }
      trigger.changeToZero();
      const num = Math.min(
        player.getHistory("gain", (evt) => evt.getParent(event.name) === event).reduce((sum, evt) => sum += evt.cards?.length || 0, 0),
        player.countCards("he")
      );
      const result = await player.chooseCard("he", `肆掠：将${get.cnNumber(num)}置于武将牌上`, num, true).set("ai", (card) => 7 - get.value(card)).forResult();
      if (result.bool) {
        const next = player.addToExpansion(result.cards, player, "giveAuto");
        next.gaintag.add(event.name);
        await next;
      }
    },
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    ai: {
      threaten: 2,
      expose: 0.3,
      ai: { combo: "casuibian" }
    }
  },
  casuibian: {
    audio: "xinfu_lveming",
    trigger: { global: "useCard" },
    filter(event, player) {
      const suit = get.suit(event.card, event.player);
      return player.getExpansions("casilve").some((card) => get.suit(card, false) === suit);
    },
    async cost(event, trigger, player) {
      const suit = get.suit(trigger.card, trigger.player);
      const result = await player.chooseButton([
        get.prompt(event.skill, trigger.player),
        [
          [
            ["damage", `弃置所有花色为${get.translation(suit)}的“掠”，对其造成1点伤害`],
            ["draw", "与其各摸一张牌"],
            ["losehp", `失去1点体力并令${get.translation(trigger.card)}失效，然后将此牌交给一名角色并摸一张牌`]
          ],
          "textbutton"
        ]
      ]).set("ai", (button) => {
        const { player: player2 } = get.event();
        const trigger2 = get.event().getTrigger();
        switch (button.link) {
          case "damage": {
            return get.damageEffect(trigger2.player, player2, player2) / 10;
          }
          case "draw": {
            return 2;
          }
          case "losehp": {
            let eff = 1 + get.effect(player2, { name: "losehp" }, player2, player2);
            if (trigger2.targets?.length) {
              for (const target of trigger2.targets) {
                eff -= get.effect(target, trigger2.card, player2, player2);
              }
            }
            return eff;
          }
        }
        return 0;
      }).forResult();
      event.result = {
        bool: result.bool,
        cost_data: result.bool ? result.links[0] : null
      };
    },
    async content(event, trigger, player) {
      const sel = event.cost_data;
      switch (sel) {
        case "damage": {
          const cards2 = player.getExpansions("casilve").filter((card) => get.suit(card, false) === get.suit(trigger.card, trigger.player));
          if (cards2.length) {
            await player.loseToDiscardpile(cards2);
          }
          player.line(trigger.player, "green");
          await trigger.player.damage();
          break;
        }
        case "draw": {
          await game.asyncDraw([player, trigger.player]);
          break;
        }
        case "losehp": {
          await player.loseHp();
          trigger.targets.length = 0;
          trigger.all_excluded = true;
          if (trigger.cards.length) {
            const result = await player.chooseTarget(`随变：令一名角色获得${get.translation(trigger.cards)}`, true).set("ai", (target) => {
              const player2 = get.player();
              return get.attitude(player2, target);
            }).forResult();
            if (result.bool) {
              await result.targets[0].gain(trigger.cards, "gain2");
            }
          }
          await player.draw();
          break;
        }
      }
    },
    ai: { combo: "casilve" }
  },
  //长安郭汜
  casixi: {
    audio: "xinfu_sidao",
    enable: "phaseUse",
    usable: 2,
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    filter(event, player) {
      return game.hasPlayer((current) => player.canCompare(current));
    },
    async content(event, trigger, player) {
      const { bool } = await player.chooseToCompare(event.target).forResult();
      if (bool) {
        const card = new lib.element.VCard({ name: "sha", nature: "stab", isCard: true });
        if (player.hasUseTarget(card)) {
          await player.chooseUseTarget(card, false);
        }
      } else {
        player.addTempSkill("casixi_distance", { player: "phaseBegin" });
        const list = player.getStorage("casixi_distance");
        player.setStorage("casixi_distance", list.concat([event.target]), true);
      }
    },
    ai: {
      order(name, player) {
        return get.order({ name: "sha" }) - 1;
      },
      result: {
        target(player, target) {
          if (target.countCards("h") === 1) {
            return -1;
          }
          return -0.5;
        }
      },
      threaten: 1.3
    },
    subfrequent: ["gain"],
    group: "casixi_gain",
    subSkill: {
      gain: {
        audio: "casixi",
        trigger: {
          global: ["chooseToCompareAfter", "compareMultipleAfter"]
        },
        frequent(event, player) {
          const bool = event.compareMeanwhile || event.compareMultiple;
          const cards2 = bool ? event.cards : [event.card1, event.card2];
          return !cards2.filterInD("od").some((card) => card.name === "du");
        },
        filter(event, player) {
          const bool = event.compareMeanwhile || event.compareMultiple;
          const cards2 = bool ? event.cards : [event.card1, event.card2];
          return cards2.someInD("od");
        },
        prompt2(event, player) {
          const bool = event.compareMeanwhile || event.compareMultiple;
          const cards2 = bool ? event.cards : [event.card1, event.card2];
          return `获得${get.translation(cards2.filterInD("od"))}`;
        },
        async content(event, trigger, player) {
          const bool = trigger.compareMeanwhile || trigger.compareMultiple;
          const cards2 = bool ? trigger.cards : [trigger.card1, trigger.card2];
          await player.gain(cards2.filterInD("od"), "gain2");
        }
      },
      distance: {
        intro: {
          markcount: () => null,
          content(storage, player) {
            if (!storage || !storage.length) {
              return "无记录";
            }
            let str = "直到你的下回合开始：";
            for (const target of game.players) {
              if (storage.includes(target)) {
                str += `<br>${get.translation(target)}计算与你的距离+${storage.filter((i) => i === target).length}`;
              }
            }
            return str;
          }
        },
        onremove: true,
        mod: {
          globalTo(from, to, distance) {
            return distance + to.getStorage("casixi_distance")?.filter((i) => i === from)?.length;
          }
        }
      }
    }
  },
  calvedao: {
    audio: "xinfu_tanbei",
    trigger: {
      source: "damageBegin1"
    },
    filter(event, player) {
      return event.card?.name === "sha";
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await player.gainPlayerCard(trigger.player, "he", true);
      await trigger.player.loseMaxHp();
      await trigger.player.addSkills("bixiong");
    },
    derivation: "bixiong"
  },
  //长安李傕
  cacuixi: {
    audio: "xinfu_langxi",
    enable: "phaseUse",
    usable: 2,
    filterCard: lib.filter.cardDiscardable,
    filter(event, player) {
      return player.countCards("h");
    },
    position: "h",
    selectCard: [1, Infinity],
    check(card) {
      if (ui.selected?.cards?.length >= 3) {
        return 0;
      }
      return 4 - get.value(card);
    },
    allowChooseAll: true,
    async content(event, trigger, player) {
      const num = event.cards.length;
      const targets = game.filterPlayer((current) => current.hp < player.hp);
      if (!targets.length) {
        return;
      }
      let result = targets.length > 2 ? await player.chooseTarget("摧袭：与两名体力值小于你的角色各展示牌堆顶一张牌", (card, player2, target) => target.hp < player2.hp, 2, true).set("ai", (target) => get.damageEffect(target, get.player(), get.player())).forResult() : { bool: true, targets };
      let list = [player];
      if (result?.targets?.length) {
        list.addArray(result.targets);
      }
      list = list.toUniqued().sortBySeat(player);
      let cardMap = [];
      for (const current of list) {
        const cards2 = get.cards();
        await game.cardsGotoOrdering(cards2);
        await current.showCards(cards2, `${get.translation(current)}发动了【摧袭】`);
        cardMap.push([cards2[0], get.number(cards2[0], false), current]);
        if (player === current && get.number(cards2[0], false) === 1) {
          player.chat("这河里吗？");
        }
      }
      result = await player.chooseBool().set("createDialog", [
        `摧袭：是否令你的点数+${num}？`,
        [
          cardMap,
          (item, type, position, noclick, node) => {
            node = ui.create.buttonPresets.card(item[0], type, position, noclick);
            game.createButtonCardsetion(item[2].getName(true) + item[1], node);
            return node;
          }
        ]
      ]).set(
        "choice",
        (() => {
          const max2 = cardMap.map((item) => item[1]).maxBy((i) => i);
          let bool = false;
          for (const [card, number, target] of cardMap) {
            if (player === target && number <= max2) {
              bool = true;
              break;
            }
          }
          if (bool) {
            return true;
          }
          const list2 = cardMap.map((item) => item[2]);
          if (list2.remove(player).every((current) => get.damageEffect(current, player, player) > 0)) {
            return true;
          }
          return false;
        })()
      ).forResult();
      if (result?.bool) {
        player.popup(num);
        game.log(player, "选择作弊使其展示牌点数+", `#g${num}`);
        cardMap = cardMap.map((item) => {
          const [card, number, target] = item;
          if (player === target) {
            return [card, number + num, target];
          }
          return item;
        });
      }
      const max = cardMap.map((item) => item[1]).maxBy((i) => i);
      for (const [card, number, target] of cardMap) {
        target.popup(number);
        game.log(target, "本次展示牌点数为", `#g${number}`);
        if (number < max) {
          player.line(target, "green");
          await target.damage(2);
        }
      }
    },
    ai: {
      order: 9,
      result: {
        player(player, target) {
          if (player.hp <= 2) {
            return 0;
          }
          return game.countPlayer((current) => get.damageEffect(current, player, player) > 0 && current.hp < player.hp);
        }
      }
    }
  },
  cajujun: {
    enable: "phaseUse",
    limited: true,
    skillAnimation: true,
    animationColor: "metal",
    manualConfirm: true,
    filter(event, player) {
      return Math.min(player.hp, player.countCards("h")) < player.maxHp;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.recoverTo(player.maxHp);
      await player.drawTo(player.maxHp);
      player.addTempSkill("cajujun_norecover", { source: "dieAfter" });
    },
    ai: {
      order: 2,
      result: {
        player(player, target) {
          if (Math.min(player.hp, player.countCards("h")) > 2) {
            return 0;
          }
          return 1;
        }
      }
    },
    subSkill: {
      norecover: {
        charlotte: true,
        mark: true,
        intro: { content: "不能回复体力" },
        trigger: { player: "recoverBefore" },
        forced: true,
        firstDo: true,
        async content(event, trigger, player) {
          trigger.cancel();
        },
        ai: {
          effect: {
            target(card, player, target) {
              if (get.tag(card, "recover")) {
                return "zeroplayertarget";
              }
            }
          }
        }
      }
    }
  },
  //长安吕布
  caliyu: {
    audio: "liyu",
    enable: "phaseUse",
    usable: 2,
    filter(event, player) {
      return game.hasPlayer((current) => current !== player && current.countGainableCards(player, "he"));
    },
    filterTarget(card, player, target) {
      return target !== player && target.countGainableCards(player, "he");
    },
    async content(event, trigger, player) {
      const target = event.target;
      await player.gainPlayerCard(target);
      player.addTempSkill("caliyu_damage");
      player.addMark("caliyu_damage", 1, false);
      const card = new lib.element.VCard({ name: "juedou", isCard: true });
      if (target.canUse(card, player)) {
        await target.useCard(card, player, "noai");
      }
    },
    ai: {
      order: 8,
      result: {
        target(player, target) {
          return get.effect(target, { name: "shunshou_copy2" }, player, target);
        },
        player(player, target) {
          return get.effect(player, { name: "juedou" }, target, player) / 2;
        }
      }
    },
    subSkill: {
      damage: {
        trigger: {
          source: "damageBegin1"
        },
        filter(event, player) {
          return player.countMark("caliyu_damage") && event.card && ["sha", "juedou"].includes(event.card.name);
        },
        intro: {
          content: "使用【杀】和【决斗】造成的伤害+$"
        },
        audio: "caliyu",
        onremove: true,
        forced: true,
        async content(event, trigger, player) {
          trigger.num += player.countMark(event.name);
        }
      }
    }
  },
  //长安王允
  calianji: {
    audio: "wylianji",
    enable: "phaseUse",
    usable: 1,
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player) {
      const target = event.target;
      await target.draw();
      const list = get.inpileVCardList((info) => {
        const card = new lib.element.VCard({ name: info[2], nature: info[3], isCard: true });
        if (!get.tag(card, "damage") || !["basic", "trick"].includes(get.type(card, false))) {
          return false;
        }
        return target.hasUseTarget(card);
      });
      if (!list.length) {
        return;
      }
      const result = await player.chooseButton([`连计：选择要令${get.translation(target)}使用的牌`, [list, "vcard"]], true).set("ai", (button) => {
        const card = new lib.element.VCard({ name: button.link[2], nature: button.link[3], isCard: true });
        const { player: player2, target: target2 } = get.event();
        return target2.getUseValue(card) * get.attitude(player2, target2);
      }).set("target", target).forResult();
      if (result.bool) {
        const card = new lib.element.VCard({ name: result.links[0][2], nature: result.links[0][3], isCard: true });
        if (target.hasUseTarget(card)) {
          await target.chooseUseTarget(card, true);
        }
      }
    },
    ai: {
      order: 6,
      result: {
        target: 1
      }
    }
  },
  camoucheng: {
    audio: "moucheng",
    trigger: {
      global: "damageSource"
    },
    filter(event, player) {
      let num = 0;
      const historys = _status.globalHistory;
      refrain: for (const history of historys) {
        const evts = history.everything;
        for (const evt of evts) {
          if (evt.name === "damage" && evt.card && evt.getParent(4)?.name === "calianji") {
            num += evt.num || 0;
          }
          if (num >= 3) {
            break refrain;
          }
        }
      }
      return num >= 3;
    },
    forced: true,
    juexingji: true,
    skillAnimation: true,
    animationColor: "gray",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.gainMaxHp();
      await player.removeSkills("calianji");
      await player.recover();
      await player.addSkills("cajingong");
    },
    derivation: "cajingong"
  },
  cajingong: {
    audio: "jingong",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.countCards("hes", (card) => card.name === "sha" || get.type(card) === "equip");
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpile("trick").concat(["wy_meirenji", "wy_xiaolicangdao"]).map((i) => ["锦囊", "", i]);
        return ui.create.dialog("矜功", [list, "vcard"]);
      },
      filter(button, player) {
        return lib.filter.filterCard({ name: button.link[2] }, player, _status.event.getParent());
      },
      check(button) {
        return _status.event.player.getUseValue({ name: button.link[2] });
      },
      backup(links, player) {
        return {
          audio: "cajingong",
          popname: true,
          position: "hes",
          viewAs: { name: links[0][2] },
          check(card) {
            return 6 - get.value(card);
          },
          filterCard(card) {
            return card.name === "sha" || get.type(card) === "equip";
          },
          async precontent(event, trigger, player2) {
            player2.addTempSkill("cajingong_benghuai");
          }
        };
      },
      prompt(links, player) {
        return `将一张【杀】或装备牌当做${get.translation(links[0][2])}使用`;
      }
    },
    onremove(player) {
      player.removeSkill("cajingong_benghuai");
    },
    ai: {
      order: 2,
      result: {
        player: 1
      }
    },
    subSkill: {
      benghuai: {
        trigger: {
          player: "phaseEnd"
        },
        charlotte: true,
        forced: true,
        locked: false,
        filter(event, player) {
          return !player.getHistory("sourceDamage").length;
        },
        async content(event, trigger, player) {
          await player.loseHp();
        }
      }
    }
  },
  //神贾诩
  zombiesangluan: {
    trigger: { player: "useCardAfter" },
    filter(event, player) {
      return get.is.damageCard(event.card) && game.hasPlayer((current) => current !== player);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => Boolean(ui.selected.targets.length) || target !== player2, 2).set("ai", (target) => {
        const player2 = get.player();
        const source = ui.selected.targets[0];
        if (!source) {
          return Math.max(
            ...game.filterPlayer((current) => current !== player2).map((current) => {
              const list2 = [get.effect(target, { name: "losehp" }, player2, player2) + get.recoverEffect(player2, player2, player2)];
              const cards3 = target.getCards("h", (card) => get.name(card) === "sha" && target.canUse(card, current));
              if (cards3.length) {
                cards3.sort((a, b) => get.effect(current, b, target, target) - get.effect(current, a, target, target));
                list2.push(get.effect(current, cards3[0], target, player2));
              }
              return Math.max(...list2);
            })
          );
        }
        const list = [get.effect(source, { name: "losehp" }, player2, player2) + get.recoverEffect(player2, player2, player2)];
        const cards2 = source.getCards("h", (card) => get.name(card) === "sha" && source.canUse(card, target));
        if (cards2.length) {
          cards2.sort((a, b) => get.effect(target, b, source, source) - get.effect(target, a, source, source));
          list.push(get.effect(target, cards2[0], source, player2));
        }
        return Math.max(...list);
      }).set("complexTarget", true).forResult();
    },
    line: false,
    async content(event, trigger, player) {
      player.line2(event.targets);
      await game.delayx();
      const [source, target] = event.targets;
      const result = await source.chooseToUse(
        function(card, player2, event2) {
          if (get.name(card) !== "sha") {
            return false;
          }
          return lib.filter.filterCard.apply(this, arguments);
        },
        `${get.translation(event.name)}：对${get.translation(target)}使用一张【杀】，或失去1点体力且${get.translation(player)}回复1点体力`
      ).set("filterTarget", function(card, player2, target2) {
        const source2 = get.event().sourcex;
        if (target2 !== source2 && !ui.selected.targets.includes(source2)) {
          return false;
        }
        return lib.filter.filterTarget.apply(this, arguments);
      }).set("sourcex", target).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).forResult();
      if (!result?.bool) {
        await source.loseHp();
        await player.recover();
      }
    }
  },
  zombieshibao: {
    enable: "phaseUse",
    filter(event, player) {
      return game.hasPlayer((target) => get.info("zombieshibao").filterTarget(null, player, target));
    },
    filterTarget(card, player, target) {
      return get.is.playerNames(target, "zombie_zombie") && target.getHp() > 0;
    },
    async content(event, trigger, player) {
      const target = event.target;
      if (target.getHp() > 0) {
        const targets = game.filterPlayer((current) => [current.getPrevious(), current.getNext()].includes(target));
        await target.loseHp(target.getHp());
        if (targets.length > 0) {
          player.line(targets);
          for (const i of targets) {
            await i.damage();
          }
        }
      }
    },
    ai: {
      order: 7,
      result: {
        player(player, target) {
          return game.countPlayer((current) => {
            if (![current.getPrevious(), current.getNext()].includes(target)) {
              return 0;
            }
            return get.damageEffect(current, player, player);
          }) + get.effect(target, { name: "losehp" }, player, player) * target.getHp();
        }
      }
    }
  },
  zombiechuce: {
    enable: "chooseToUse",
    filter(event, player) {
      return get.inpileVCardList((info) => ["basic", "trick", "delay"].includes(info[0])).some(
        (info) => player.hasCard((cardx) => {
          if (get.type2(cardx) !== "trick") {
            return false;
          }
          const card = get.autoViewAs({ name: info[2], nature: info[3], storage: { zombiechuce: true }, cards: [cardx] }, [cardx]);
          return event.filterCard(card, player, event);
        }, "hes")
      );
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => ["basic", "trick", "delay"].includes(info[0]));
        return ui.create.dialog("出策", [list, "vcard"]);
      },
      filter(button, player) {
        const event = get.event().getParent();
        return player.hasCard((cardx) => {
          if (get.type2(cardx) !== "trick") {
            return false;
          }
          const card = get.autoViewAs({ name: button.link[2], nature: button.link[3], storage: { zombiechuce: true }, cards: [cardx] }, [cardx]);
          return event.filterCard(card, player, event);
        }, "hes");
      },
      check(button) {
        if (get.event().getParent().type !== "phase") {
          return 1;
        }
        return get.player().getUseValue({ name: button.link[2], nature: button.link[3] }, false);
      },
      prompt(links, player) {
        return `将一张锦囊牌当作${get.translation(links[0][3]) || ""}【${get.translation(links[0][2])}】使用`;
      },
      backup(links, player) {
        return {
          filterCard(card, player2) {
            return get.type2(card) === "trick";
          },
          popname: true,
          check(card) {
            return 6 - get.value(card);
          },
          position: "hes",
          async precontent(event, trigger, player2) {
            event.getParent().addCount = false;
          },
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            storage: {
              zombiechuce: true
            }
          }
        };
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name) || !["basic", "trick", "delay"].includes(get.type(name))) {
        return false;
      }
      return player.hasCard((card) => {
        if (_status.connectMode && get.position(card) === "h") {
          return true;
        }
        return get.type2(card) === "trick";
      }, "hes");
    },
    locked: false,
    mod: {
      cardUsable(card, player) {
        if (card?.storage?.zombiechuce) {
          return Infinity;
        }
      },
      targetInRange(card, player) {
        if (card?.storage?.zombiechuce) {
          return true;
        }
      }
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      skillTagFilter(player, tag, arg) {
        if (arg === "respond") {
          return false;
        }
        if (!player.countCards("hes")) {
          return false;
        }
      },
      order(item, player) {
        if (player && _status.event.type === "phase" && player.hasValueTarget({ name: "sha" }, true, true)) {
          let max = 0;
          let names = get.inpileVCardList((info) => {
            const name = info[2];
            if (name !== "sha" && name !== "jiu") {
              return false;
            }
            return get.type(name) === "basic";
          });
          names = names.map((namex) => ({ name: namex[2], nature: namex[3] }));
          names.forEach((card) => {
            if (player.getUseValue(card) > 0) {
              let temp = get.order(card);
              if (card.name === "jiu") {
                const cards2 = player.getCards("hs", (cardx) => get.value(cardx) < 8);
                cards2.sort((a, b) => get.value(a) - get.value(b));
                if (!cards2.some((cardx) => get.name(cardx) === "sha" && !cards2.slice(0, 2).includes(cardx))) {
                  temp = 0;
                }
              }
              if (temp > max) {
                max = temp;
              }
            }
          });
          if (max > 0) {
            max += 15;
          }
          return max;
        }
        return 0.5;
      },
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    group: "zombiechuce_kanpo",
    subSkill: {
      backup: {},
      kanpo: {
        trigger: { global: "useCard" },
        filter(event, player) {
          return get.type2(event.card) === "trick" && event.player !== player;
        },
        usable: 1,
        check: (event, player) => get.info("sbkanpo").subSkill.kanpo.check(event, player),
        prompt2: (event) => `摸三张牌，令${get.translation(event.card)}无效，然后你可以视为使用此牌`,
        logTarget: "player",
        async content(event, trigger, player) {
          await player.draw(3);
          trigger.targets.length = 0;
          trigger.all_excluded = true;
          game.log(trigger.card, "被无效了");
          const card = new lib.element.VCard({ name: trigger.card.name, nature: trigger.card.nature, isCard: true });
          if (get.type(card) !== "delay" && player.hasUseTarget(card)) {
            await player.chooseUseTarget(card, null, false);
          }
        }
      }
    }
  },
  zombielongmu: {
    trigger: { global: ["die", "recoverBefore"] },
    filter(event, player) {
      const target = event.player;
      if (event.name === "recover") {
        return _status.currentPhase === player && target !== player;
      }
      if (get.is.playerNames(target, "zombie_zombie")) {
        return false;
      }
      if (event.reserveOut || player.maxHp <= 0) {
        return false;
      }
      return player.hasAllHistory("useSkill", (evt) => {
        if (evt.type !== "player") {
          return false;
        }
        if (!Array.isArray(evt.targets) || !evt.targets.includes(target)) {
          return false;
        }
        const skill = evt.skill;
        let info = get.info(skill);
        if (!info || info.charlotte) {
          return false;
        }
        if (skill === get.sourceSkillFor(skill)) {
          return true;
        }
        info = get.info(get.sourceSkillFor(skill));
        return info && !info.charlotte;
      });
    },
    forceDie: true,
    forced: true,
    logTarget: "player",
    async content(event, trigger, player) {
      const target = trigger.player;
      trigger.cancel();
      if (trigger.name === "die") {
        const names = get.nameList(target).filter((i) => i !== "zombie_zombie");
        const result = names.length > 1 ? await player.chooseControl(names).set("ai", () => {
          const { controls } = get.event();
          return controls.slice().sort((a, b) => get.rank(b, true) - get.rank(a, true))[0];
        }).set("prompt", "请选择替换的武将牌").forResult() : { control: names[0] };
        if (result.control) {
          game.broadcastAll((player2) => player2.revive(2), target);
          let doubleDraw = false;
          const num = (get.character("zombie_zombie").maxHp || get.character("zombie_zombie").hp) - (get.character(result.control).maxHp || get.character(result.control).hp);
          if (num !== 0) {
            if (typeof target.singleHp === "boolean") {
              if (num % 2 !== 0) {
                if (target.singleHp) {
                  target.maxHp += (num + 1) / 2;
                  target.singleHp = false;
                } else {
                  target.maxHp += (num - 1) / 2;
                  target.singleHp = true;
                  doubleDraw = true;
                }
              } else {
                target.maxHp += num / 2;
              }
            } else {
              target.maxHp += num;
            }
            target.update();
          }
          event.skills = get.character(result.control).skills || [];
          await target.reinitCharacter(result.control, "zombie_zombie");
          if (doubleDraw) {
            await target.doubleDraw();
          }
        }
      }
    },
    group: "zombielongmu_weimu",
    global: "zombielongmu_global",
    subSkill: {
      weimu: {
        trigger: { target: "useCardToTarget", player: "addJudgeBefore" },
        filter(event, player) {
          return event.name === "addJudge" || get.type2(event.card) === "trick";
        },
        forced: true,
        priority: 15,
        async content(event, trigger, player) {
          if (trigger.name === "addJudge") {
            trigger.cancel();
            game.log(trigger.card, "进入了弃牌堆");
            const owner = get.owner(trigger.card);
            if (owner?.getCards("hejxs").includes(trigger.card)) {
              owner.lose(trigger.card, ui.discardPile);
            } else {
              game.cardsDiscard(trigger.card);
            }
          } else {
            trigger.getParent().targets.remove(player);
            game.log(trigger.card, "对", player, "无效");
          }
        },
        ai: {
          effect: {
            target(card, player, target) {
              if (get.type2(card) === "trick") {
                return "zeroplayertarget";
              }
            }
          }
        }
      },
      global: {
        ai: {
          effect: {
            target(card, player, target2) {
              const target = _status.currentPhase;
              if (target?.hasSkill("zombielongmu") && target !== player && get.tag(card, "recover")) {
                return "zeroplayertarget";
              }
            }
          }
        }
      }
    }
  },
  zombieshibian: {
    trigger: { player: "changeCharacterAfter" },
    filter(event, player) {
      return event.getParent().name === "zombielongmu" || event.getParent().name === "zombieganran";
    },
    forced: true,
    async content(event, trigger, target) {
      const parent = trigger.getParent();
      let player = parent.player;
      const skills2 = parent.skills;
      player = player["zombieshibian"] || player;
      if (skills2.length) {
        await target.addSkills(skills2);
      }
      game.broadcastAll(
        (player2, target2) => {
          target2["zombieshibian"] = player2;
          const identity = target2.identity = ((identity2) => {
            switch (identity2) {
              case "zhu":
              case "mingzhong":
                return "zhong";
              case "zhu_false":
                return "zhong_false";
              case "bZhu":
                return "bZhong";
              case "rZhu":
                return "rZhong";
              default:
                return identity2;
            }
          })(player2.identity);
          if (!lib.translate[identity]) {
            lib.translate[identity] = "尸";
          }
          const goon = player2 !== game.me && target2 !== game.me && player2.node.identity.classList.contains("guessing") && !player2.identityShown;
          if (goon) {
            if (target2.identityShown) {
              delete target2.identityShown;
            }
            if (!target2.node.identity.classList.contains("guessing")) {
              target2.node.identity.classList.add("guessing");
            }
          }
          target2.setIdentity(goon ? "cai" : void 0);
          if (target2.node.dieidentity) {
            target2.node.dieidentity.innerHTML = get.translation(target2.identity + 2);
          }
          if (typeof player2.ai?.shown === "number" && target2.ai) {
            target2.ai.shown = player2.ai.shown;
          }
          if (typeof player2.side === "boolean") {
            target2.side = player2.side;
            target2.node.identity.firstChild.innerHTML = player2.node.identity.firstChild.innerHTML;
            target2.node.identity.dataset.color = player2.node.identity.dataset.color;
          }
          if (_status._zombieshibian) {
            return;
          }
          _status.zombieshibian = true;
          if (typeof game.checkResult === "function") {
            const origin_checkResult = game.checkResult;
            game.checkResult = function() {
              const player3 = game.me._trueMe || game.me;
              if (game.players.filter((i) => i !== player3).every((i) => i["zombieshibian"] === (player3["zombieshibian"] || player3))) {
                game.over(true);
              }
              return origin_checkResult.apply(this, arguments);
            };
          }
          if (typeof game.checkOnlineResult === "function") {
            const origin_checkOnlineResult = game.checkOnlineResult;
            game.checkOnlineResult = function(player3) {
              if (game.players.filter((i) => i !== player3).every((i) => i["zombieshibian"] === (player3["zombieshibian"] || player3))) {
                return true;
              }
              return origin_checkOnlineResult.apply(this, arguments);
            };
          }
          if (typeof lib.element.player.getFriends === "function") {
            const origin_getFriends = lib.element.player.getFriends;
            const getFriends = function(func, includeDie) {
              const player3 = this;
              return [...origin_getFriends.apply(this, arguments), ...game[includeDie ? "filterPlayer2" : "filterPlayer"]((target3) => (target3["zombieshibian"] || target3) === (player3["zombieshibian"] || player3))].filter((i) => i !== player3 || func === true).unique().sortBySeat(player3);
            };
            lib.element.player.getFriends = getFriends;
            [...game.players, ...game.dead].forEach((i) => i.getFriends = getFriends);
          }
          if (typeof lib.element.player.isFriendOf === "function") {
            const origin_isFriendOf = lib.element.player.isFriendOf;
            const isFriendOf = function(player3) {
              if ((this["zombieshibian"] || this) === (player3["zombieshibian"] || player3)) {
                return true;
              }
              return origin_isFriendOf.apply(this, arguments);
            };
            lib.element.player.isFriendOf = isFriendOf;
            [...game.players, ...game.dead].forEach((i) => i.isFriendOf = isFriendOf);
          }
          if (typeof lib.element.player.getEnemies === "function") {
            const origin_getEnemies = lib.element.player.getEnemies;
            const getEnemies = function(func, includeDie) {
              if (this["zombieshibian"]) {
                return this["zombieshibian"].getEnemies(func, includeDie);
              }
              const player3 = this;
              return [...origin_getEnemies.apply(this, arguments), ...game[includeDie ? "filterPlayer2" : "filterPlayer"]((target3) => origin_getEnemies.apply(this, arguments).includes(target3["zombieshibian"] || target3))].filter((i) => player3 !== (i["zombieshibian"] || i)).unique().sortBySeat(player3);
            };
            lib.element.player.getEnemies = getEnemies;
            [...game.players, ...game.dead].forEach((i) => i.getEnemies = getEnemies);
          }
        },
        player,
        target
      );
      target.ai.modAttitudeFrom = (from, to) => {
        if (to === from["zombieshibian"]) {
          return 114514;
        }
        return get.attitude(from["zombieshibian"] || from, to["zombieshibian"] || to);
      };
      target.ai.modAttitudeTo = (from, to, att) => {
        if (from === to["zombieshibian"]) {
          return 7;
        }
        return get.attitude(from["zombieshibian"] || from, to["zombieshibian"] || to);
      };
    },
    mark: true,
    intro: {
      content(_storage, player) {
        return `made in ${player?.["zombieshibian"] ? get.translation(player["zombieshibian"]) : "东汉"}`;
      }
    }
  },
  zombieganran: {
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return game.dead.some((target) => {
        if (target["zombieshibian"] || get.is.playerNames(target, "zombie_zombie")) {
          return false;
        }
        return game.getGlobalHistory("everything", (evt) => evt.name === "die" && evt.player === target && evt.source === player).length > 0;
      });
    },
    forced: true,
    logTarget(event, player) {
      return game.dead.filter((target) => {
        if (target["zombieshibian"] || get.is.playerNames(target, "zombie_zombie")) {
          return false;
        }
        return game.getGlobalHistory("everything", (evt) => evt.name === "die" && evt.player === target && evt.source === player).length > 0;
      }).sortBySeat();
    },
    async content(event, trigger, player) {
      for (const target of event.targets) {
        const names = get.nameList(target).filter((i) => i !== "zombie_zombie");
        const result = names.length > 1 ? await player.chooseControl(names).set("ai", () => {
          const { controls } = get.event();
          return controls.slice().sort((a, b) => get.rank(b, true) - get.rank(a, true));
        }).set("prompt", "请选择替换的武将牌").forResult() : { control: names[0] };
        if (result.control) {
          game.broadcastAll((player2) => player2.revive(2), target);
          let doubleDraw = false;
          const num = (get.character("zombie_zombie").maxHp || get.character("zombie_zombie").hp) - (get.character(result.control).maxHp || get.character(result.control).hp);
          if (num !== 0) {
            if (typeof target.singleHp === "boolean") {
              if (num % 2 !== 0) {
                if (target.singleHp) {
                  target.maxHp += (num + 1) / 2;
                  target.singleHp = false;
                } else {
                  target.maxHp += (num - 1) / 2;
                  target.singleHp = true;
                  doubleDraw = true;
                }
              } else {
                target.maxHp += num / 2;
              }
            } else {
              target.maxHp += num;
            }
            target.update();
          }
          event.skills = get.character(result.control).skills || [];
          await target.reinitCharacter(result.control, "zombie_zombie");
          if (doubleDraw) {
            await target.doubleDraw();
          }
        }
        if (player.isDamaged()) {
          const num = player.getDamagedHp();
          await player.recover(num);
          await player.draw(num);
        }
      }
    }
  }
};
const skills$h = {
  wn_zhanyi: {
    audio: "zhanyi",
    enable: "phaseUse",
    usable: 1,
    filterCard(card, player) {
      return ["basic", "trick"].includes(get.type2(card)) && lib.filter.cardDiscardable(card, player, "wn_zhanyi");
    },
    lose: false,
    discard: false,
    check(card) {
      const player = get.player();
      return 2.5 * player.countCards("hs", (cardx) => {
        if (get.type2(cardx) !== get.type2(card) || cardx === card) {
          return false;
        }
        return player.hasValueTarget(cardx);
      }) - get.value(card);
    },
    async content(event, trigger, player) {
      await player.loseHp();
      await player.modedDiscard(event.cards);
      player.addTempSkill("wn_zhanyi_effect");
    },
    subSkill: {
      effect: {
        trigger: {
          player: "useCard"
        },
        filter(event, player) {
          if (!event.targets?.length) {
            return false;
          }
          const type = get.type(event.card);
          if (type !== "basic" && type !== "trick") {
            return false;
          }
          return player.hasHistory("lose", (evt) => {
            if (evt.type !== "discard" || evt.getParent(2).name !== "wn_zhanyi") {
              return false;
            }
            return evt.cards.some((card) => get.type2(card) === type);
          });
        },
        charlotte: true,
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          const num = player.getHistory("lose", (evt) => {
            if (evt.type !== "discard" || evt.getParent(2).name !== "wn_zhanyi") {
              return false;
            }
            return evt.cards.some((card) => get.type2(card) === get.type(trigger.card));
          }).length;
          if (num > 0) {
            trigger.effectCount += num;
            game.log(player, "令", trigger.card, `额外结算了${num}次`);
          }
        }
      }
    },
    ai: {
      order: 8,
      result: {
        player(player) {
          const info = get.info("wn_zhanyi");
          if (player.countCards("h", (card) => {
            if (!info.filterCard(card, player)) {
              return false;
            }
            return info.check(card) > 0;
          })) {
            return 1;
          }
          return 0;
        }
      }
    }
  },
  //渭南风云
  //渭南神马超
  wn_qiangshu: {
    trigger: {
      source: "damageBegin1"
    },
    filter(event, player) {
      if (!event.card || !["sha", "juedou"].includes(event.card.name)) {
        return false;
      }
      const num = player.getAttackRange() - 1;
      return num > 0 && player.countCards("he") >= num;
    },
    async cost(event, trigger, player) {
      const num = player.getAttackRange() - 1;
      event.result = await player.chooseToDiscard("he", get.prompt2(event.skill), num).set("chooseonly", true).set("ai", (card) => {
        const trigger2 = get.event().getTrigger();
        const player2 = get.player();
        if (get.damageEffect(trigger2.player, trigger2.source, player2) <= 0) {
          return 0;
        }
        return 7 - get.value(card);
      }).forResult();
      event.result.targets = [trigger.player];
    },
    async content(event, trigger, player) {
      await player.discard(event.cards);
      trigger.num += event.cards.length;
    }
  },
  wn_yuma: {
    trigger: {
      global: ["loseAfter", "loseAsyncAfter", "cardsDiscardAfter", "equipAfter"]
    },
    usable: 1,
    filter(event, player) {
      if (!event.getd) {
        return false;
      }
      const cards2 = event.getd();
      return cards2.some((card) => {
        if (get.position(card) !== "d" || get.type(card) !== "equip") {
          return false;
        }
        if (card.willBeDestroyed("discardPile", get.owner(card), event)) {
          return false;
        }
        return game.hasPlayer((current) => current.canEquip(card, true));
      });
    },
    async cost(event, trigger, player) {
      const cards2 = trigger.getd().filter((card) => {
        if (get.position(card) !== "d" || get.type(card) !== "equip") {
          return false;
        }
        if (card.willBeDestroyed("discardPile", get.owner(card), trigger)) {
          return false;
        }
        return true;
      });
      const { bool, targets, links } = await player.chooseButtonTarget({
        createDialog: [get.prompt2(event.skill), cards2],
        filterTarget(card, player2, target) {
          const buttons = ui.selected.buttons;
          if (!buttons.length) {
            return false;
          }
          return target.canEquip(buttons[0].link, true);
        },
        ai1(button) {
          return 20 - get.value(button.link);
        },
        ai2(target) {
          const player2 = get.player();
          const card = ui.selected.buttons[0]?.link;
          if (!card) {
            return 0;
          }
          if (!target.countCards("h")) {
            return get.value(card, target) * get.attitude(player2, target);
          }
          return (get.value(card, target) - 2 * target.countCards("h")) * get.attitude(player2, target);
        }
      }).forResult();
      event.result = {
        bool,
        targets,
        cards: links
      };
    },
    async content(event, trigger, player) {
      const {
        targets: [target],
        cards: [card]
      } = event;
      target.$gain2(card);
      await game.delay();
      await target.equip(card);
      const num = target.countCards("h");
      if (num > 0 && target !== player) {
        await player.gainPlayerCard(target, true, "h", num);
      }
    }
  },
  //渭南神许褚
  wn_zhuanzhan: {
    trigger: {
      global: "phaseZhunbeiBegin"
    },
    filter(event, player) {
      if (event.player === player || !player.hasEnabledSlot()) {
        return false;
      }
      const card = new lib.element.VCard({ name: "juedou", isCard: true });
      return player.canUse(card, event.player);
    },
    async cost(event, trigger, player) {
      const list = [];
      for (let i = 1; i <= 5; i++) {
        const slot = `equip${i}`;
        if (player.hasEnabledSlot(slot)) {
          list.push(`equip${i}`);
        }
      }
      list.push("cancel2");
      let bool = "cancel2";
      const card = new lib.element.VCard({ name: "juedou", isCard: true });
      if (get.effect(trigger.player, card, player, player) > 0) {
        bool = list.filter((i) => i !== "cancel2").randomGet();
      }
      const result = await player.chooseControl(list).set("prompt", get.prompt2(event.skill)).set("ai", () => get.event().bool).set("bool", bool).forResult();
      event.result = {
        bool: result.control !== "cancel2",
        targets: [trigger.player],
        cost_data: result.control
      };
    },
    async content(event, trigger, player) {
      const slot = event.cost_data;
      await player.disableEquip([slot]);
      const card = new lib.element.VCard({ name: "juedou", isCard: true });
      if (player.canUse(card, trigger.player)) {
        await player.useCard(card, trigger.player);
      }
    }
  },
  wn_huwei: {
    trigger: {
      player: "phaseDrawBegin2"
    },
    forced: true,
    filter(event, player) {
      let list = Array.from({ length: 13 }).map((_, i) => `equip${parseFloat(i + 1)}`);
      list = list.filter((i) => player.hasDisabledSlot(i));
      return !event.numFixed && list.length;
    },
    async content(event, trigger, player) {
      const list = Array.from({ length: 13 }).map((_, i) => `equip${parseFloat(i + 1)}`);
      const num = list.reduce((sum, slot) => sum + player.countDisabledSlot(slot), 0);
      trigger.num += num;
    }
  },
  //张郃
  wn_qiaobian: {
    audio: "sbqiaobian",
    trigger: {
      global: "phaseZhunbeiBegin"
    },
    filter(event, player) {
      return player.countCards("he") && event.player !== player;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard(get.prompt2(event.skill, trigger.player), "he").set("ai", (card) => {
        const player2 = get.player();
        const event2 = get.event();
        if (get.attitude(player2, event2.getTrigger().player) > 0) {
          return 0;
        }
        return 7 - get.value(card);
      }).forResult();
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const {
        cards: cards2,
        targets: [target]
      } = event;
      const next = player.addToExpansion(cards2, "giveAuto", player);
      next.gaintag.add(event.name);
      await next;
      player.addTempSkill("wn_qiaobian_effect");
      player.markAuto("wn_qiaobian_effect", target);
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    marktext: "巧",
    intro: {
      name: "巧",
      mark(dialog, storage, player) {
        const cards2 = player.getExpansions("wn_qiaobian");
        if (player.isUnderControl(true)) {
          dialog.addAuto(cards2);
        } else {
          return `共有${get.cnNumber(cards2.length)}张牌`;
        }
      },
      markcount: "expansion"
    },
    subSkill: {
      effect: {
        audio: "wn_qiaobian",
        onremove: true,
        charlotte: true,
        trigger: {
          global: "useCard"
        },
        filter(event, player) {
          if (!player.getExpansions("wn_qiaobian")?.length || event.player !== _status.currentPhase) {
            return false;
          }
          return event.player?.isIn() && player.getStorage("wn_qiaobian_effect").includes(event.player);
        },
        async cost(event, trigger, player) {
          const { bool, links } = player.getExpansions("wn_qiaobian").length > 1 ? await player.chooseCardButton(player.getExpansions("wn_qiaobian"), true, "巧变：展示一张“巧”").set("ai", (button) => {
            const { cardType: type, player: player2 } = get.event();
            const trigger2 = get.event().getTrigger();
            const bool2 = get.type2(button.link) === type;
            const inphase = trigger2.getParent("phaseUse", true)?.player === trigger2.player;
            const att = get.attitude(player2, trigger2.player);
            if (att > 0) {
              return bool2 ? inphase ? -1 : 1 : 2;
            }
            return bool2 ? inphase ? trigger2.player.countCards("h") : -1 : 2;
          }).set("cardType", get.type2(trigger.card)).forResult() : {
            bool: true,
            links: player.getExpansions("wn_qiaobian")
          };
          event.result = {
            bool,
            targets: [trigger.player],
            cost_data: links
          };
        },
        async content(event, trigger, player) {
          const {
            cost_data: [card],
            targets: [target]
          } = event;
          await player.showCards(card);
          if (get.type2(card) === get.type2(trigger.card)) {
            await target.gain(card, "give", player);
            const evt = trigger.getParent("phaseUse", true);
            if (evt?.player === target) {
              game.log(player, "令", target, "结束了出牌阶段");
              evt.skipped = true;
            }
          } else {
            await player.draw();
            player.when({
              global: "phaseJieshuBegin"
            }).filter((evt, player2) => evt.getParent("phase", true) === trigger.getParent("phase", true) && player2.getExpansions("wn_qiaobian").length).step(async (event2, trigger2, player2) => {
              const cards2 = player2.getExpansions("wn_qiaobian");
              await player2.gain(cards2, "draw");
            });
          }
        }
      }
    }
  },
  //贾诩
  wn_jianshu: {
    audio: "jianshu",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.countCards("h");
    },
    filterTarget(card, player, target) {
      if (ui.selected.targets.length) {
        return ui.selected.targets[0] !== target && !ui.selected.targets[0].hasSkillTag("noCompareSource") && target.countCards("h") && !target.hasSkillTag("noCompareTarget");
      }
      return target !== player;
    },
    filterCard: true,
    position: "he",
    discard: false,
    lose: false,
    delay: false,
    check(card) {
      if (_status.event.player.hp === 1) {
        return 8 - get.value(card);
      }
      return 6 - get.value(card);
    },
    selectTarget: 2,
    multitarget: true,
    async content(event, trigger, player) {
      const [target1, target2] = event.targets;
      const cards2 = event.cards;
      await player.give(cards2, target1, "give");
      if (target1.canCompare(target2)) {
        const result = await target1.chooseToCompare(target2).forResult();
        if (result.tie) {
          await target1.loseHp();
          await target2.loseHp();
        } else {
          const winner = result.bool ? target1 : target2;
          const loser = result.bool ? target2 : target1;
          await winner.chooseToDiscard("he", 2, true);
          await loser.loseHp();
        }
      }
    },
    ai: {
      expose: 0.4,
      order: 4,
      result: {
        target(player, target) {
          if (ui.selected.targets.length) {
            return -1;
          }
          return -0.5;
        }
      }
    }
  },
  wn_zhenlve: {
    audio: 2,
    trigger: {
      global: "useCard"
    },
    filter(event, player) {
      return event.card.name === "wuxie" && player.countCards("he");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard(get.prompt2(event.skill, trigger.player), "he").set("ai", (card) => {
        const player2 = get.player();
        const trigger2 = get.event().getTrigger();
        if (get.attitude(player2, trigger2.player) >= 0) {
          return 0;
        }
        return 4 - get.value(card);
      }).set("chooseonly", true).forResult();
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const { cards: cards2 } = event;
      await player.discard(cards2);
      trigger.targets.length = 0;
      trigger.all_excluded = true;
      const cardx = trigger.cards?.filterInD();
      if (cardx?.length) {
        await player.gain(cardx, "gain2");
      }
    }
  },
  //徐晃
  wn_zhuying: {
    audio: 2,
    trigger: {
      global: "phaseJieshuBegin"
    },
    filter(event, player) {
      return !event.player.getHistory("useCard", (evt) => evt.targets?.includes(player)).length;
    },
    forced: true,
    marktext: "驻",
    intro: {
      name: "驻",
      content: "mark"
    },
    async content(event, trigger, player) {
      player.addMark(event.name, 1);
    },
    ai: { combo: "wn_chiyuan" }
  },
  wn_chiyuan: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player) {
      return player.hasMark("wn_zhuying") && game.hasPlayer((current) => current !== player);
    },
    filterTarget: lib.filter.notMe,
    selectTarget() {
      const player = get.player();
      return [1, player.countMark("wn_zhuying")];
    },
    async content(event, trigger, player) {
      const target = event.target;
      player.removeMark("wn_zhuying", 1, false);
      target.addMark("wn_zhuying", 1, false);
      game.log(player, "将一个“驻”标记交给了", target);
    },
    ai: {
      combo: "wn_zhuying",
      order: 9,
      result: {
        target(player, target) {
          if (ui.selected.targets?.length || player.countMark("wn_zhuying") <= 1) {
            return 0;
          }
          return Math.max(0, 3 - target.countMark("wn_zhuying"));
        }
      }
    },
    group: "wn_chiyuan_effect",
    subSkill: {
      effect: {
        audio: "wn_chiyuan",
        trigger: {
          global: "damageBegin3"
        },
        filter(event, player) {
          return event.player.hasMark("wn_zhuying");
        },
        async cost(event, trigger, player) {
          event.result = await player.chooseToDiscard("he", get.prompt(event.skill, trigger.player), "弃置一张牌并选择一项：<br>1.防止此伤害并移去一个“驻”；<br>2.对伤害来源造成1点伤害。").set("ai", (card) => {
            const { eff } = get.event();
            if (eff > 0) {
              return eff * 1.2 - get.value(card);
            }
            return 0;
          }).set(
            "eff",
            (() => {
              const source = trigger.source ?? trigger.player;
              let eff = -get.damageEffect(trigger.player, source, player, trigger.nature);
              if (trigger.source?.isIn()) {
                eff = Math.max(eff, get.damageEffect(trigger.source, player, player));
              }
              return eff;
            })()
          ).set("chooseonly", true).forResult();
          event.result.targets = [trigger.player];
        },
        async content(event, trigger, player) {
          const {
            cards: cards2,
            targets: [target]
          } = event;
          await player.discard(cards2);
          const result = trigger.source?.isIn() ? await player.chooseButton(
            [
              "驰援：请选择一项",
              [
                [
                  ["defend", `防止此伤害并移去${get.translation(target)}一个“驻”`],
                  ["attack", `对${get.translation(trigger.source)}造成1点伤害`]
                ],
                "textbutton"
              ]
            ],
            true
          ).set("ai", (button) => {
            const player2 = get.player();
            const trigger2 = get.event().getTrigger();
            if (button.link === "defend") {
              return -get.damageEffect(trigger2.player, trigger2.source, player2, trigger2.nature);
            }
            return get.damageEffect(trigger2.source, player2, player2);
          }).forResult() : {
            bool: true,
            links: ["defend"]
          };
          if (result?.bool && result?.links?.length) {
            if (result.links[0] === "defend") {
              trigger.cancel();
              target.removeMark("wn_zhuying", 1);
            } else if (trigger.source?.isIn()) {
              player.line(trigger.source);
              await trigger.source.damage(player);
            }
          }
        }
      }
    }
  },
  //曹操
  wn_dingluan: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player) {
      await player.loseHp();
      const card = new lib.element.VCard({ name: "dajunyajing", isCard: true });
      const target = event.target;
      const result = player.canUse(card, target) ? await target.chooseControl().set("choiceList", [`令${get.translation(player)}视为对你使用一张【大军压境】`, "令你武将牌上的所有技能失效直到你下个回合结束"]).set("prompt", "定乱：请选择一项").set("ai", () => {
        const player2 = get.player();
        const targets = game.countPlayer((current) => current !== player2 && get.attitude(player2, current) < 0);
        if (targets.length >= player2.hp) {
          return 1;
        }
        return Math.random() > 0.7 ? 1 : 0;
      }).forResult() : {
        index: 1
      };
      if (result.index === 0) {
        await player.useCard(card, target);
      } else {
        target.addTempSkill("wn_dingluan_blocker", { player: "phaseAfter" });
      }
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          if (player.hp <= 2) {
            return 0;
          }
          return -4;
        }
      }
    },
    subSkill: {
      blocker: {
        init(player, skill) {
          player.addSkillBlocker(skill);
        },
        onremove(player, skill) {
          player.removeSkillBlocker(skill);
        },
        charlotte: true,
        locked: true,
        skillBlocker(skill, player) {
          const skills2 = player.getStockSkills(true, true);
          const info = get.info(skill);
          return info && !info.charlotte && !info.persevereSkill && skills2.includes(skill);
        },
        mark: true,
        marktext: '<span style="text-decoration: line-through;">乱</span>',
        intro: {
          content(list, player, skill) {
            const storage = player.getSkills(null, false, false).filter((i) => lib.skill.wn_dingluan_blocker.skillBlocker(i, player));
            if (storage.length) {
              return `失效技能：${get.translation(storage)}`;
            }
            return "无失效技能";
          }
        }
      }
    }
  },
  wn_zhuijiang: {
    trigger: {
      global: "die"
    },
    filter(event, player) {
      return player.hasZhuSkill("wn_zhuijiang") && game.hasPlayer((current) => current.group === "wei");
    },
    zhuSkill: true,
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill, trigger.player), (card, player2, target) => target.group === "wei").set("ai", () => Math.random()).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      game.broadcastAll(
        (target1, target2) => {
          game.swapSeat(target1, target2);
        },
        target,
        trigger.player
      );
    }
  },
  //韩遂
  wn_jubing: {
    trigger: {
      global: "damageEnd"
    },
    usable: 1,
    filter(event, player) {
      if (!event.player?.isIn()) {
        return false;
      }
      return game.hasPlayer((current) => current.group === "qun" && current.countCards("he") && current.inRange(event.player));
    },
    logTarget: "player",
    prompt2(event, player) {
      const targets = game.filterPlayer((current) => current.group === "qun" && current.countCards("he") && current.inRange(event.player));
      return `弃置${get.translation(targets)}${targets.length > 1 ? "各" : ""}一张牌，视为对其使用等量张【杀】`;
    },
    check(event, player) {
      let eff = 0;
      game.filterPlayer((current) => {
        if (current.group !== "qun" || !current.countCards("he") || !current.inRange(event.player)) {
          return false;
        }
        eff += get.effect(current, { name: "guohe_copy2" }, player, player);
        const card = new lib.element.VCard({ name: "sha", isCard: true });
        if (player.canUse(card, event.player, false)) {
          eff += get.effect(event.player, card, player, player);
        }
      });
      return eff > 0;
    },
    async content(event, trigger, player) {
      const targets = game.filterPlayer((current) => current.group === "qun" && current.countCards("he") && current.inRange(trigger.player)).sortBySeat(_status.currentPhase);
      for (const target of targets) {
        player.line(target, "green");
        await player.discardPlayerCard(target, "he", true);
      }
      let num = 0;
      for (const target of targets) {
        num += target.getHistory("lose", (evt) => evt.type === "discard" && evt.getParent(3) === event && evt?.cards?.length).reduce((sum, evt) => sum + evt.cards.length, 0);
      }
      while (num > 0) {
        num--;
        const card = new lib.element.VCard({ name: "sha", isCard: true });
        if (player.canUse(card, trigger.player, false)) {
          await player.useCard(card, trigger.player);
        } else {
          break;
        }
      }
    }
  },
  wn_xiongju: {
    trigger: {
      global: ["phaseBefore", "changeGroupAfter"],
      player: "enterGame"
    },
    zhuSkill: true,
    filter(event, player) {
      return player.hasZhuSkill("wn_xiongju") && lib.skill.wn_xiongju.logTarget(event, player)?.length;
    },
    logTarget(event, player) {
      if (event.name === "changeGroup") {
        if ([event.originGroup, event.group].includes(player.group)) {
          return [event.player];
        }
        return [];
      }
      return game.filterPlayer((current) => current.group === player.group);
    },
    onremove(player, skill) {
      const skillName = `${skill}_${player.playerid}`;
      for (const target of game.players) {
        if (target?.additionalSkills?.[skillName]?.length) {
          target.removeAdditionalSkill(skillName);
        }
      }
    },
    firstDo: true,
    silent: true,
    async content(event, trigger, player) {
      const skillName = `${event.name}_${player.playerid}`;
      for (const target of event.targets) {
        if (target.group === player.group) {
          target.addAdditionalSkill(skillName, ["mashu"], true);
        } else {
          target.removeAdditionalSkill(skillName);
        }
      }
    },
    derivation: ["mashu"]
  },
  wn_zhongtao: {
    trigger: {
      global: "useCardAfter"
    },
    filter(event, player) {
      if (!event.player.isIn() || get.distance(event.player, player) > 1) {
        return false;
      }
      return event.card.name === "sha" && event.targets?.some((i) => {
        const card = new lib.element.VCard({ name: "sha", isCard: true });
        return i.isIn() && player.canUse(card, i, false);
      }) && player.countCards("hes");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        prompt: get.prompt2(event.skill, trigger.targets),
        filterCard(card, player2) {
          const targets = get.event().targetx;
          const cardx = new lib.element.VCard({ name: "sha", cards: [card] }, [card]);
          return targets.some((target) => player2.canUse(cardx, target, false));
        },
        targetx: trigger.targets,
        selectTarget: -1,
        position: "hes",
        filterTarget(card, player2, target) {
          const cards2 = ui.selected.cards;
          const targets = get.event().targetx;
          const cardx = new lib.element.VCard({ name: "sha", cards: cards2 }, cards2);
          return player2.canUse(cardx, target, false) && targets.includes(target);
        },
        ai1(card) {
          const { targetx: targets, player: player2 } = get.event();
          const cardx = new lib.element.VCard({ name: "sha", cards: [card] }, [card]);
          let num = 0;
          targets.filter((target) => {
            if (!player2.canUse(cardx, target, false)) {
              return false;
            }
            num += get.sgn(get.effect(target, cardx, player2, player2));
          });
          if (num <= 0) {
            return 0;
          }
          return 7 - get.value(card);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const { cards: cards2, targets } = event;
      const card = new lib.element.VCard({ name: "sha", cards: cards2 }, cards2);
      await player.useCard(card, cards2, targets, false);
    }
  },
  wn_dutan: {
    enable: "phaseUse",
    viewAs: {
      name: "juedou",
      isCard: true
    },
    usable: 1,
    filterCard: () => false,
    selectCard: -1,
    selectTarget: [1, Infinity]
  },
  wn_qifeng: {
    trigger: { global: "phaseEnd" },
    filter(event, player) {
      if (!player.getHistory("lose").length) {
        return false;
      }
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      return player.canUse(card, event.player, false);
    },
    check(event, player) {
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      return get.effect(event.player, card, player, player) > 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      const next = player.useCard(card, trigger.player);
      next.oncard = () => {
        const num = get.player().getHistory("lose", (evt) => evt?.cards2?.length).reduce((sum, evt) => sum + evt.cards2.length, 0);
        get.event().baseDamage = num;
      };
      await next;
    }
  }
};
const skills$g = {
  //欧陆风云
  //于禁张辽乐进
  eu_jieyue: {
    audio: "rejieyue",
    trigger: {
      player: "phaseJieshuBegin"
    },
    filter(event, player) {
      return player.getStorage("eu_jieyue_used").length < 2;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill)).set("ai", (target) => {
        const player2 = get.player();
        const used = player2.getStorage("eu_jieyue_used");
        const att = get.attitude(player2, target);
        if (used.includes("draw")) {
          return 10 - att;
        }
        if (target.hp === 1 && get.effect(target, { name: "losehp" }, player2, player2) <= 0) {
          return att;
        }
        return 10 + att;
      }).forResult();
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      const used = player.getStorage("eu_jieyue_used");
      await target.draw();
      const result = used.length ? {
        bool: true,
        links: ["draw", "phase"].removeArray(used)
      } : await target.chooseButton([
        "节钺：选择一项",
        [
          [
            ["draw", `令${get.translation(player)}摸三张牌`],
            ["phase", `失去1点体力，令${get.translation(player)}执行一个额外回合`]
          ],
          "textbutton"
        ]
      ]).set("ai", (button) => {
        const bool1 = button.link === "draw";
        const bool2 = get.event().eff;
        const player2 = get.player();
        if (bool2 && player2.hp > 1) {
          return bool1 ? 1 : 2;
        }
        return bool1 ? 2 : 1;
      }).set("eff", get.attitude(target, player) > 0).forResult();
      if (result.bool) {
        const link = result.links[0];
        player.addTempSkill("eu_jieyue_used", "roundStart");
        player.markAuto("eu_jieyue_used", link);
        if (link === "draw") {
          await player.draw(3);
        } else {
          await target.loseHp();
          player.insertPhase(event.name);
        }
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      }
    }
  },
  eu_tuxi: {
    audio: ["dcyuxi", "dcporong"],
    trigger: {
      source: "damageSource"
    },
    frequent: true,
    async content(event, trigger, player) {
      await player.draw();
      for (const current of [trigger.player, trigger.player?.getPrevious(), trigger.player?.getNext()]) {
        const pos = current === player ? "e" : "he";
        if (current?.isIn() && current.countGainableCards(player, pos)) {
          player.line(current);
          await player.gainPlayerCard(current, pos, true);
        }
      }
    }
  },
  eu_xiaoguo: {
    audio: "xiaoguo",
    enable: "phaseUse",
    filterCard(card) {
      return get.type(card) === "basic";
    },
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player) {
      const damage = event.target.damage("nocard");
      await damage;
      if (game.hasGlobalHistory("everything", (evt) => evt.name === "dying" && evt.reason === damage)) {
        player.tempBanSkill(event.name);
      }
    },
    check(card) {
      return 6 - get.value(card);
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          let eff = get.damageEffect(target, player, target);
          if (target.hp === 1 && player.countCards("h", (card) => get.type(card) === "basic") > 1) {
            eff /= 4;
          }
          return eff;
        },
        player: -1
      }
    }
  },
  //马抗
  eu_xiru: {
    trigger: {
      player: "phaseDrawBegin2"
    },
    filter(event, player) {
      if (event.numFixed) {
        return false;
      }
      return game.hasPlayer((current) => current.getEquips("equip3_4")?.length > 0);
    },
    forced: true,
    async content(event, trigger, player) {
      const cards2 = game.filterPlayer().reduce((cards3, current) => {
        const equips = current.getEquips("equip3_4");
        if (equips?.length) {
          return [...cards3, ...equips];
        }
        return cards3;
      }, []);
      trigger.num += Math.ceil(cards2.length / 2);
    }
  },
  eu_zongma: {
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player) {
      return player.countCards("he", (card) => {
        if (card.viewAs) {
          return false;
        }
        return ["zongma_attack", "zongma_defend"].some((name) => {
          const equip = get.autoViewAs({ name }, [card]);
          return game.hasPlayer((current) => current.canEquip(equip));
        });
      });
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        prompt: get.prompt2(event.skill),
        filterCard(card) {
          return !card.viewAs;
        },
        position: "he",
        filterTarget(card, player2, target) {
          const cards2 = ui.selected.cards;
          return cards2?.length && ["zongma_attack", "zongma_defend"].some((name) => {
            const equip = get.autoViewAs({ name }, cards2);
            return target.canEquip(equip);
          });
        },
        ai1(card) {
          return 7 - get.value(card);
        },
        ai2(target) {
          const cards2 = ui.selected.cards;
          const player2 = get.player();
          const attack = get.autoViewAs({ name: "zongma_attack" }, cards2);
          const defend = get.autoViewAs({ name: "zongma_defend" }, cards2);
          if (target.canEquip(attack) && get.attitude(player2, target) > 0) {
            return 2 + Math.random();
          }
          if (target.canEquip(defend) && get.attitude(player2, target) < 0) {
            return 1 + Math.random();
          }
          return 0;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const {
        cards: cards2,
        targets: [target]
      } = event;
      const list = ["zongma_attack", "zongma_defend"].filter((name) => {
        const equip = get.autoViewAs({ name }, cards2);
        return target.canEquip(equip);
      });
      if (!list?.length) {
        return;
      }
      const result = list.length > 1 ? await player.chooseButton(["选择要置入的装备栏", [list.map((i) => ["", "", i]), "vcard"]], true).set("ai", (button) => {
        const bool1 = button.link[2] === "zongma_attack";
        const bool2 = get.event().att;
        return bool1 === bool2 ? 2 : 1;
      }).set("att", get.attitude(player, target) > 0).forResult() : {
        bool: true,
        links: list.map((i) => ["", "", i])
      };
      if (result.bool) {
        const card = get.autoViewAs({ name: result.links[0][2] }, cards2);
        player.$give(cards2, target, false);
        await target.equip(card);
      }
    },
    subSkill: {
      attack: {
        charlotte: true,
        forced: true,
        locked: false,
        trigger: {
          source: "damageBegin1"
        },
        getIndex(event, player) {
          return player.getVEquips("zongma_attack");
        },
        filter(event, player, name, card) {
          return get.position(card) === "e";
        },
        async content(event, trigger, player) {
          const card = event.indexedData;
          const cards2 = player.getCards("e", (cardx) => cardx[cardx.cardSymbol] === card);
          await player.loseToDiscardpile(cards2);
          trigger.num++;
        }
      },
      defend: {
        charlotte: true,
        forced: true,
        locked: false,
        trigger: {
          player: "damageBegin3"
        },
        getIndex(event, player) {
          return player.getVEquips("zongma_defend");
        },
        filter(event, player, name, card) {
          return get.position(card) === "e";
        },
        async content(event, trigger, player) {
          const card = event.indexedData;
          const cards2 = player.getCards("e", (cardx) => cardx[cardx.cardSymbol] === card);
          await player.loseToDiscardpile(cards2);
          trigger.num++;
        }
      }
    }
  },
  //阿尔达希尔
  eu_wanwang: {
    trigger: {
      global: "phaseUseBegin"
    },
    logTarget: "player",
    round: 1,
    filter(event, player) {
      return event.player !== player && event.player.group === "western";
    },
    async content(event, trigger, player) {
      trigger.player = player;
    }
  },
  eu_sashan: {
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    forced: true,
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    logTarget: () => game.players,
    async content(event, trigger, player) {
      for (const target of event.targets) {
        if (target.group !== "western") {
          await target.changeGroup("western");
        }
      }
    }
  },
  eu_nagong: {
    global: "eu_nagong_global",
    subSkill: {
      global: {
        trigger: {
          player: "phaseZhunbeiBegin"
        },
        filter(event, player) {
          if (player.group === "qun" || !player.countCards("he")) {
            return false;
          }
          return game.hasPlayer((current) => current.hasSkill("eu_nagong") && current !== player);
        },
        async cost(event, trigger, player) {
          const targets = game.filterPlayer((current) => current.hasSkill("eu_nagong") && current !== player);
          if (targets.length > 1) {
            event.result = await player.chooseCardTarget({
              prompt: get.prompt(event.skill),
              prompt2: "上贡一名角色并将势力变更为群直到你的下回合开始",
              filterCard: true,
              position: "he",
              filterTarget(card, player2, target) {
                return player2 !== target && target.hasSkill("eu_nagong");
              },
              ai1(card) {
                return 6 - get.value(card);
              },
              ai2(target) {
                const player2 = get.player();
                if (get.attitude(player2, target) > 0) {
                  return 5;
                }
                if (!target.hasSkill("eu_wanwang") || target.storage.eu_wanwang_roundcount) {
                  return 0;
                }
                if (player2.needsToDiscard(2)) {
                  return 3;
                }
                return 0;
              }
            }).forResult();
          } else {
            event.result = await player.chooseCard("he", get.prompt(event.skill, targets)).set("prompt2", "上贡其一张牌，然后你将势力变更为群直到你的下回合开始").set("ai", (card) => {
              const { player: player2, targetx: target } = get.event();
              if (get.attitude(player2, target) > 0) {
                return 6 - get.value(card);
              }
              if (!target.hasSkill("eu_wanwang") || target.storage.eu_wanwang_roundcount) {
                return 0;
              }
              if (player2.needsToDiscard(2)) {
                return 6 - get.value(card);
              }
              return 0;
            }).set("targetx", targets[0]).forResult();
            event.result.targets = targets;
          }
          event.result.skill_popup = false;
        },
        async content(event, trigger, player) {
          const {
            cards: cards2,
            targets: [target]
          } = event;
          target.logSkill("eu_nagong", [player]);
          await player.give(cards2, target);
          const group = player.group;
          player.changeGroup("qun");
          player.when({
            player: "phaseBegin"
          }).step(async (event2, trigger2, player2) => {
            if (player2.group === group) {
              return;
            }
            await player2.changeGroup(group);
          });
        }
      }
    }
  },
  //马克里努斯
  eu_dengtian: {
    init(player, skill) {
      player.setStorage(skill, [0, 0, 0]);
      player.addTip(skill, `登天 ${player.getStorage(skill).join(" ")}`);
    },
    onremove(player, skill) {
      player.removeTip(skill);
      player.setStorage(skill, null);
    },
    trigger: {
      global: "roundStart"
    },
    forced: true,
    async content(event, trigger, player) {
      const list = player.getStorage(event.name, [0, 0, 0]);
      const choiceList = [`摸牌阶段摸牌数（当前+${list[0]}）`, `手牌上限（当前+${list[1]}）`, `每回合首次造成伤害值（当前+${list[2]}）`];
      const { index } = await player.chooseControl().set("prompt", "登天：令一项数值+1").set("choiceList", choiceList).set(
        "resultx",
        (() => {
          const val = (index2) => {
            let num = 3 - index2;
            if (player.hasSkill("eu_mingshu") && list[index2] === 2) {
              if (player.countCards("hs", (card) => ["tao", "jiu"].includes(card.name))) {
                num /= player.hp + 1;
              } else {
                num /= 114514;
              }
            }
            let rate = list[index2] + 1;
            if (index2 === 1) {
              rate = player.getHandcardLimit() / 3 + 1;
            }
            return num / rate;
          };
          return [0, 1, 2].maxBy(val);
        })()
      ).set("ai", () => get.event().resultx).forResult();
      list[index]++;
      const str = choiceList[index].slice(0, choiceList[index].indexOf("（"));
      game.log(player, "令", `#g${str}`, "+1");
      player.setStorage(event.name, list);
      player.addTip(event.name, `登天 ${player.getStorage(event.name).join(" ")}`);
      event.list = list;
      event.index = index;
    },
    mod: {
      maxHandcard(player, num) {
        const list = player.getStorage("eu_dengtian", [0, 0, 0]);
        return num + list[1];
      }
    },
    group: "eu_dengtian_effect",
    subSkill: {
      effect: {
        trigger: {
          source: "damageBegin1",
          player: "phaseDrawBegin2"
        },
        filter(event, player) {
          const list = player.getStorage("eu_dengtian", [0, 0, 0]);
          if (event.name === "damage") {
            const historys = game.getGlobalHistory("everything", (evt) => evt.name === "damage" && evt.source === player);
            return list[2] > 0 && historys.indexOf(event) === 0;
          }
          return list[0] > 0 && !event.numFixed;
        },
        forced: true,
        async content(event, trigger, player) {
          const list = player.getStorage("eu_dengtian", [0, 0, 0]);
          if (trigger.name === "damage") {
            trigger.num += list[2];
          } else {
            trigger.num += list[0];
          }
        }
      }
    }
  },
  eu_mingshu: {
    trigger: {
      player: "eu_dengtianAfter"
    },
    filter(event, player) {
      const { list, index } = event;
      return list[index] === 3 && player.getHp() > 0;
    },
    forced: true,
    async content(event, trigger, player) {
      await player.loseHp(player.getHp());
    },
    ai: {
      combo: "eu_dengtian",
      neg: true
    }
  },
  eu_juedou: {
    trigger: {
      global: "useCardAfter"
    },
    filter(event, player) {
      if (event.player === player || event.card?.name !== "sha" || !event.player?.isIn()) {
        return false;
      }
      if (!event.targets?.includes(player) || player.hasHistory("damage", (evt) => evt.card === event.card)) {
        return false;
      }
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      return player.canUse(card, event.player, false);
    },
    logTarget: "player",
    check(event, player) {
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      return get.effect(event.player, card, player, player) > 0;
    },
    async content(event, trigger, player) {
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      await player.useCard(card, event.targets);
    }
  },
  //欧陆凯撒
  eu_ducai: {
    init(player, skill) {
      if (_status?.currentPhase !== player) {
        return;
      }
      const targets = game.filterPlayer((current) => current !== player);
      for (const target of targets) {
        target.addTempSkill(`${skill}_block`);
      }
    },
    onremove(player, skill) {
      if (_status?.currentPhase !== player) {
        return;
      }
      const targets = game.filterPlayer((current) => current !== player);
      for (const target of targets) {
        target.removeSkill(`${skill}_block`);
      }
    },
    trigger: {
      player: "phaseBeginStart"
    },
    persevereSkill: true,
    forced: true,
    firstDo: true,
    priority: Infinity,
    async content(event, trigger, player) {
      get.info(event.name).init(player, event.name);
    },
    mod: {
      targetInRange(card, player) {
        if (player === _status.currentPhase) {
          return true;
        }
      },
      cardUsable(card, player) {
        if (player === _status.currentPhase) {
          return Infinity;
        }
      }
    },
    subSkill: {
      block: {
        inherit: "baiban",
        intro: {
          content(storage, player, skill) {
            let str = "<li>不能使用牌";
            const list = player.getSkills(null, false, false).filter((i) => lib.skill.baiban.skillBlocker(i, player));
            if (list.length) {
              str += `<br><li>${get.translation(list)}失效`;
            }
            return str;
          }
        },
        mod: {
          cardEnabled(card) {
            return false;
          },
          cardSavable(card) {
            return false;
          }
        }
      }
    }
  },
  eu_zhitong: {
    mark: true,
    zhuanhuanji: true,
    marktext: "☯",
    intro: {
      content(storage, player, skill) {
        if (storage) {
          return "转换技，当你使用牌时，若目标包含其他角色，你依次获得这些角色装备区的所有牌并对其造成1点伤害。";
        }
        return "转换技，当你使用牌时，若目标包含自己，摸两张牌且回复1点体力。";
      }
    },
    trigger: {
      player: "useCard"
    },
    filter(event, player) {
      if (!event?.targets?.length) {
        return false;
      }
      const bool = player.storage?.eu_zhitong;
      return bool && event.targets.some((current) => current !== player) || !bool && event.targets.includes(player);
    },
    check(event, player) {
      if (!player.storage?.eu_zhitong) {
        return true;
      }
      return event.targets.filter((target) => target !== player).reduce((eff, target) => eff + get.damageEffect(target, player, player), 0) > 0;
    },
    async content(event, trigger, player) {
      player.changeZhuanhuanji(event.name);
      if (player.storage?.eu_zhitong) {
        await player.draw(2);
        await player.recover();
      } else {
        const targets = trigger.targets.filter((current) => current !== player).sortBySeat();
        for (const target of targets) {
          const cards2 = target.getGainableCards(player, "e");
          if (cards2.length) {
            await player.gain(cards2, target, "give", "bySelf");
          }
          await target.damage();
        }
      }
    }
  },
  eu_jiquan: {
    trigger: {
      global: "phaseBegin"
    },
    zhuSkill: true,
    forced: true,
    filter(event, player) {
      return event.player?.group === "western" && event.player?.isIn();
    },
    async content(event, trigger, player) {
      await player.recover();
      await player.draw();
    }
  }
};
const skills$f = {
  //九鼎-徐晃
  jdsbduanliang: {
    audio: "sbduanliang",
    inherit: "sbduanliang",
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = await player.chooseToDuiben(target).set("title", "谋弈").set("namelist", ["固守城池", "突出重围", "围城断粮", "擂鼓进军"]).set("translationList", [`以防止${get.translation(player)}通过此技能对你使用【决斗】`, `以防止${get.translation(player)}通过此技能对你使用【兵粮寸断】`, `若成功，你摸一张牌，然后可以将一张黑色非锦囊牌当做【兵粮寸断】对${get.translation(target)}使用`, `若成功，视为对${get.translation(target)}使用【决斗】`]).set("ai", (button) => {
        const source = _status.event.getParent().player;
        const target2 = _status.event.getParent().target;
        if (get.effect(target2, { name: "juedou" }, source, source) >= 10 && button.link[2] === "db_def2" && Math.random() < 0.5) {
          return 10;
        }
        return 1 + Math.random();
      }).forResult();
      if (result.bool) {
        if (result.player === "db_def1") {
          await player.draw();
          if (target.hasJudge("bingliang") && target.countGainableCards(player, "he")) {
            await player.gainPlayerCard(target, "he", true);
          } else {
            const next = player.chooseToUse();
            next.set("openskilldialog", `断粮：是否将一张黑色非锦囊牌当作【兵粮寸断】对${get.translation(target)}使用？`);
            next.set("norestore", true);
            next.set("_backupevent", "jdsbduanliang_backup");
            next.set("custom", {
              add: {},
              replace: { window() {
              } }
            });
            next.backup("jdsbduanliang_backup");
            next.set("targetRequired", true);
            next.set("complexSelect", true);
            next.set("filterTarget", function(card, player2, target2) {
              if (target2 !== _status.event.sourcex) {
                return false;
              }
              return lib.filter.targetEnabled.apply(this, arguments);
            });
            next.set("sourcex", target);
            await next;
          }
        } else {
          const card = { name: "juedou", isCard: true };
          if (player.canUse(card, target)) {
            await player.useCard(card, target);
          }
        }
      }
    },
    subSkill: {
      backup: {
        viewAs: {
          name: "bingliang"
        },
        filterCard(card, player) {
          return get.itemtype(card) === "card" && get.color(card, player) === "black" && get.type2(card) !== "trick";
        },
        position: "hes",
        selectCard: 1,
        check(card) {
          return 6 - get.value(card);
        }
      }
    }
  },
  //九鼎--王元姬
  jdshiren: {
    audio: "shiren",
    trigger: { player: "showCharacterAfter" },
    filter(event, player) {
      if (!event.toShow?.some((i) => get.character(i).skills?.includes("jdshiren"))) {
        return false;
      }
      const target = _status.currentPhase;
      return target && target !== player && target.isAlive() && target.countCards("h") > 0;
    },
    logTarget: () => _status.currentPhase,
    hiddenSkill: true,
    async content(event, trigger, player) {
      const next = game.createEvent("jdyanxi", false);
      next.player = player;
      next.target = _status.currentPhase;
      next.setContent(lib.skill["jdyanxi"].content);
    }
  },
  jdyanxi: {
    audio: "yanxi",
    inherit: "yanxi",
    async content(event, trigger, player) {
      const target = event.target;
      const {
        cards: [card]
      } = await player.choosePlayerCard(target, "h", true).forResult();
      if (card) {
        const videoId = lib.status.videoId++;
        game.addVideo("showCards", player, [`${get.translation(player)}对${get.translation(target)}发动了【宴戏】`, get.cardsInfo([card])]);
        game.broadcastAll(
          (card2, id, player2, target2) => {
            let dialog;
            if (player2 === game.me) {
              dialog = ui.create.dialog(`${get.translation(target2)}手牌展示中...`);
            } else {
              dialog = ui.create.dialog(`${get.translation(player2)}对${get.translation(target2)}发动了【宴戏】`, [card2]);
            }
            dialog.forcebutton = true;
            dialog.videoId = id;
          },
          card,
          videoId,
          player,
          target
        );
        await game.delay(2);
        game.broadcastAll("closeDialog", videoId);
        let cards2 = [card].concat(get.cards(2)).randomSort();
        game.log(player, "展示了", cards2);
        const videoIdx = lib.status.videoId++;
        const str = `${get.translation(player)}对${get.translation(target)}发动了【宴戏】`;
        game.broadcastAll(
          (str2, id, cards3) => {
            const dialog = ui.create.dialog(str2, cards3);
            dialog.videoId = id;
          },
          str,
          videoIdx,
          cards2
        );
        game.addVideo("showCards", player, [str, get.cardsInfo(cards2)]);
        const func = (id, target2) => {
          const dialog = get.idDialog(id);
          if (dialog) {
            dialog.content.firstChild.innerHTML = `猜猜哪张是${get.translation(target2)}的手牌？`;
          }
        };
        if (player === game.me) {
          func(videoIdx, target);
        } else if (player.isOnline()) {
          player.send(func, videoIdx, target);
        }
        const next = player.chooseButton(true);
        next.set("dialog", videoIdx);
        next.set("ai", (button) => {
          const evt = get.event();
          if (evt.answer) {
            return button.link === evt.answer ? 1 : 0;
          }
          return get.value(button.link, evt.player);
        });
        if (player.hasSkillTag("viewHandcard", null, target, true)) {
          next.set("answer", card);
        }
        const result = await next.forResult();
        game.broadcastAll("closeDialog", videoIdx);
        if (result.bool) {
          const card2 = result.links[0];
          cards2.remove(card2);
          if (card2 === card) {
            player.popup("洗具");
            player.$gain2(cards2);
            await player.gain(cards2, "log");
            await player.gain(card, target, "bySelf", "give");
          } else {
            player.popup("杯具");
            await player.gain(card2, "gain2");
            const result2 = await player.chooseToMove("宴戏：将剩余的牌以任意顺序置于牌堆顶", true).set("list", [["牌堆顶", cards2]]).set("reverse", _status.currentPhase?.next && get.attitude(player, _status.currentPhase.next) > 0).set("processAI", (list) => {
              const cards3 = list[0][1].slice(0);
              cards3.sort((a, b) => (_status.event.reverse ? 1 : -1) * (get.value(b) - get.value(a)));
              return [cards3];
            }).forResult();
            if (!result2.bool) {
              return;
            }
            cards2 = result2.moved[0];
            cards2.reverse();
            if (cards2.includes(card)) {
              target.$throw(1, 1e3);
              await target.lose([card], ui.special);
            }
            await game.cardsGotoPile(cards2, "insert");
            game.log(player, "将", cards2, "置于了牌堆顶");
          }
        }
      }
    }
  },
  //九鼎-华歆
  jdcaozhao: {
    audio: "caozhao",
    trigger: { global: "phaseUseBegin" },
    filter(event, player) {
      if (lib.inpile.every((i) => player.getStorage("jdcaozhao").includes(i))) {
        return false;
      }
      return event.player.countCards("h") && event.player.getHp() <= player.getHp();
    },
    async cost(event, trigger, player) {
      const target = trigger.player;
      event.result = await player.choosePlayerCard(target, "h", get.prompt2(event.skill, target)).set("ai", () => {
        const player2 = get.player();
        const target2 = get.event().getTrigger().player;
        if (lib.inpile.some((i) => !player2.getStorage("jdcaozhao").includes(i) && target2.getUseValue(i) * get.attitude(player2, target2) > 0)) {
          return 1 + Math.random();
        }
        return 0;
      }).forResult();
    },
    logTarget: "player",
    round: 1,
    async content(event, trigger, player) {
      const target = trigger.player;
      await player.showCards(event.cards, `${get.translation(player)}对${get.translation(target)}发动了【草诏】`);
      const result = await player.chooseButton(
        [
          "草诏：请选择一个基本牌或锦囊牌",
          [
            lib.inpile.filter((i) => {
              if (!["basic", "trick"].includes(get.type(i))) {
                return false;
              }
              return !player.getStorage("jdcaozhao").includes(i);
            }),
            "vcard"
          ]
        ],
        true
      ).set("ai", (button) => {
        const player2 = get.player();
        const target2 = get.event().getTrigger().player;
        const sgn = get.sgn(get.attitude(player2, target2));
        const cards2 = get.event().getParent().cards;
        const card = get.autoViewAs({ name: button.link[2] }, cards2);
        if (!target2.hasUseTarget(card) || target2.getUseValue(card) * sgn <= 0) ;
        return 5 + target2.getUseValue(card) * sgn;
      }).forResult();
      if (result.bool) {
        const name = result.links[0][2];
        player.markAuto("jdcaozhao", [name]);
        player.popup(name, "thunder");
        game.log(player, "声明了", `#y${get.translation(name)}`);
        const card = get.autoViewAs({ name }, event.cards);
        let resultx;
        if (!target.hasUseTarget(card)) {
          resultx = { bool: false };
        } else {
          resultx = await target.chooseUseTarget(`###草诏###<div class="text center">使用${get.translation(card)}（${get.translation(event.cards)}），或失去1点体力</div>`, card, false).set("cards", event.cards).forResult();
        }
        if (!resultx.bool) {
          await target.loseHp();
        }
      }
    }
  },
  //九鼎-杨婉
  jdmingxuan: {
    audio: "spmingxuan",
    trigger: { player: "phaseUseBegin" },
    filter(event, player) {
      const num = Math.min(
        player.getCards("h").slice().map((i) => get.suit(i, player)).unique().length,
        game.countPlayer((current) => current !== player && !player.getStorage("jdmingxuan").includes(current))
      );
      return num > 0;
    },
    forced: true,
    async content(event, trigger, player) {
      const num = Math.min(
        player.getCards("h").slice().map((i) => get.suit(i, player)).unique().length,
        game.countPlayer((current) => current !== player && !player.getStorage("jdmingxuan").includes(current))
      );
      const result = await player.chooseCard("h", true, [1, num], `瞑昡：请选择至多${get.cnNumber(num)}张花色各不相同的手牌`, (card, player2) => {
        if (!ui.selected.cards.length) {
          return true;
        }
        return !ui.selected.cards.some((i) => get.suit(i, player2) === get.suit(card));
      }).set("complexCard", true).set("ai", (card) => 6 - get.value(card)).forResult();
      if (result?.bool && result.cards?.length > 0) {
        const cards2 = result.cards.slice().randomSort();
        const targets = game.filterPlayer((current) => current !== player && !player.getStorage("jdmingxuan").includes(current)).sortBySeat(player);
        const dialog = ui.create.dialog("瞑昡", cards2, true);
        _status.dieClose.push(dialog);
        dialog.videoId = lib.status.videoId++;
        event.dialogID = dialog.videoId;
        game.addVideo("cardDialog", null, ["瞑昡", get.cardsInfo(cards2), dialog.videoId]);
        game.broadcast(
          (cards3, id) => {
            const dialog2 = ui.create.dialog("瞑昡", cards3, true);
            _status.dieClose.push(dialog2);
            dialog2.videoId = id;
          },
          cards2,
          dialog.videoId
        );
        while (cards2.length && targets.length) {
          await game.delayx();
          const target = targets.shift();
          const resultx = await target.chooseButton(true, (button) => get.value(button.link, _status.event.player)).set("dialog", event.dialogID).set("closeDialog", false).set("dialogdisplay", true).set("cardFilter", cards2.slice()).set("filterButton", (button) => _status.event.cardFilter.includes(button.link)).forResult();
          if (resultx?.bool && resultx.links?.length) {
            const card = resultx.links[0];
            if (card) {
              cards2.remove(card);
              const capt = `${get.translation(target)}选择了${get.translation(card)}`;
              game.broadcastAll(
                (card2, id, name, capt2) => {
                  const dialog2 = get.idDialog(id);
                  if (dialog2) {
                    dialog2.content.firstChild.innerHTML = capt2;
                    for (const button of dialog2.buttons) {
                      if (button.link === card2) {
                        game.createButtonCardsetion(name, button);
                        break;
                      }
                    }
                    game.addVideo("dialogCapt", null, [dialog2.videoId, dialog2.content.firstChild.innerHTML]);
                  }
                },
                card,
                event.dialogID,
                target.getName(true),
                capt
              );
              await target.gain(card, player, "give");
            }
            const resulty = await target.chooseToUse(
              function(card2, player2, event2) {
                if (get.name(card2) !== "sha") {
                  return false;
                }
                return lib.filter.filterCard.apply(this, arguments);
              },
              `对${get.translation(player)}使用一张杀，否则交给其一张牌且其摸一张牌`
            ).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("filterTarget", function(card2, player2, target2) {
              if (target2 !== _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
                return false;
              }
              return lib.filter.filterTarget.apply(this, arguments);
            }).set("sourcex", player).set("addCount", false).forResult();
            if (resulty?.bool) {
              player.markAuto("jdmingxuan", [target]);
            } else {
              await target.chooseToGive("he", true, player, `交给${get.translation(player)}一张牌`);
              await player.draw();
            }
          }
        }
        for (const dialog2 of ui.dialogs) {
          if (dialog2.videoId === event.dialogID) {
            const dialogx = dialog2;
            dialogx.close();
            _status.dieClose.remove(dialogx);
            break;
          }
        }
        game.broadcast((id) => {
          const dialog2 = get.idDialog(id);
          if (dialog2) {
            dialog2.close();
            _status.dieClose.remove(dialog2);
          }
        }, event.dialogID);
        game.addVideo("cardDialog", null, event.dialogID);
      }
    },
    intro: { content: "已被$使用过杀" }
  },
  //九鼎-黄月英
  jdjizhi: {
    audio: "sbjizhi",
    trigger: { player: "useCard" },
    filter(event, player) {
      return get.type(event.card) === "trick";
    },
    forced: true,
    async content(event, trigger, player) {
      await player.draw("nodelay");
      player.addTempSkill("jdjizhi_mark");
      player.addMark("jdjizhi_mark", 1, false);
    },
    subSkill: {
      mark: {
        charlotte: true,
        onremove: true,
        intro: { content: "本回合手牌上限+#" },
        mod: {
          maxHandcard(player, num) {
            return num + player.countMark("jdjizhi_mark");
          }
        }
      }
    }
  },
  jdqicai: {
    audio: "sbqicai",
    enable: "phaseUse",
    filter(event, player) {
      return player.countCards("he", { type: "equip" });
    },
    filterCard(card) {
      return !ui.selected.cards.length && get.type(card) === "equip";
    },
    selectCard: [1, 2],
    filterTarget: lib.filter.notMe,
    position: "he",
    check(card) {
      return 8 - get.value(card);
    },
    complexCard: true,
    complexSelect: true,
    lose: false,
    discard: false,
    delay: false,
    usable: 1,
    get prompt() {
      return lib.translate.jdqicai_info.slice("①你使用锦囊牌无距离限制。②".length);
    },
    async content(event, trigger, player) {
      const target = event.target;
      const str = get.translation(player);
      await player.showCards(event.cards, `${get.translation(player)}发动了【奇才】`);
      await player.give(event.cards, target);
      const result = await target.chooseCard(2, "he", (card) => get.type(card) !== "equip", `奇才：交给${str}两张非装备牌，或令${str}获得两张普通锦囊牌`).set("ai", (card) => {
        if (get.event().att >= 0) {
          return -1;
        }
        return 7 - get.value(card);
      }).set("att", get.attitude(target, player)).forResult();
      if (!result.bool) {
        const gains = [];
        while (gains.length < 2) {
          const card = get.cardPile((i) => get.type(i) === "trick" && !gains.includes(i), false, "random");
          if (card) {
            gains.push(card);
          } else {
            break;
          }
        }
        if (gains.length) {
          await player.gain(gains, "gain2");
        } else {
          player.chat("无牌可得？！");
          game.log("但是牌堆和弃牌堆都没有普通锦囊牌了！");
        }
      } else {
        await target.showCards(result.cards);
        await target.give(result.cards, player);
      }
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          const att = get.attitude(player, target);
          return get.sgn(att) * (2 + get.sgn(att));
        }
      }
    },
    mod: {
      targetInRange(card) {
        if (get.type2(card) === "trick") {
          return true;
        }
      }
    },
    locked: false
  },
  //九鼎-赵云
  jdlongdan: {
    audio: "sblongdan",
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      if (event.type === "wuxie") {
        return false;
      }
      const marked = player.hasSkill("sblongdan_mark", null, null, false);
      for (const name of lib.inpile) {
        if (!marked && name !== "sha" && name !== "shan") {
          continue;
        }
        if (get.type(name) !== "basic") {
          continue;
        }
        if (player.hasCard(lib.skill.jdlongdan.getFilter(name, player), "hs")) {
          if (event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
            return true;
          }
          if (marked && name === "sha") {
            for (const nature of lib.inpile_nature) {
              if (event.filterCard(get.autoViewAs({ name, nature }, "unsure"), player, event)) {
                return true;
              }
            }
          }
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player) {
        const list = [];
        const marked = player.hasSkill("sblongdan_mark", null, null, false);
        for (const name of lib.inpile) {
          if (!marked && name !== "sha" && name !== "shan") {
            continue;
          }
          if (get.type(name) !== "basic") {
            continue;
          }
          if (player.hasCard(lib.skill.jdlongdan.getFilter(name, player), "hs")) {
            if (event.filterCard(get.autoViewAs({ name }, "unsure"), player, event)) {
              list.push(["基本", "", name]);
            }
            if (marked && name === "sha") {
              for (const nature of lib.inpile_nature) {
                if (event.filterCard(get.autoViewAs({ name, nature }, "unsure"), player, event)) {
                  list.push(["基本", "", name, nature]);
                }
              }
            }
          }
        }
        return ui.create.dialog("龙胆", [list, "vcard"], "hidden");
      },
      check(button) {
        if (_status.event.getParent().type !== "phase") {
          return 1;
        }
        const player = _status.event.player;
        const card = { name: button.link[2], nature: button.link[3] };
        if (card.name === "jiu" && player.countCards("h", { type: "basic" }) < 2) {
          return 0;
        }
        return player.getUseValue(card, null, true);
      },
      backup(links, player) {
        return {
          audio: "jdlongdan",
          viewAs: {
            name: links[0][2],
            nature: links[0][3]
          },
          filterCard: lib.skill.jdlongdan.getFilter(links[0][2], player),
          position: "he",
          popname: true,
          check(card) {
            return 6 / Math.max(1, get.value(card));
          },
          async precontent(event, trigger, player2) {
            player2.addTempSkill("jdlongdan_draw");
          }
        };
      },
      prompt(links, player) {
        const marked = player.hasSkill("sblongdan_mark", null, null, false);
        const card = {
          name: links[0][2],
          nature: links[0][3],
          isCard: true
        };
        if (marked) {
          return `将一张基本牌当作【${get.translation(card)}】使用`;
        }
        return `将一张【${card.name === "sha" ? "闪" : "杀"}】当作【${get.translation(card)}】使用`;
      }
    },
    hiddenCard(player, name) {
      if (get.type(name) !== "basic") {
        return false;
      }
      const marked = player.hasSkill("sblongdan_mark", null, null, false);
      if (!marked && name !== "sha" && name !== "shan") {
        return false;
      }
      return player.hasCard(lib.skill.jdlongdan.getFilter(name, player), "hs");
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player, tag) {
        return lib.skill.jdlongdan.hiddenCard(player, tag === "respondSha" ? "sha" : "shan");
      },
      order: 9,
      result: {
        player(player) {
          if (_status.event.dying) {
            return get.attitude(player, _status.event.dying);
          }
          return 1;
        }
      }
    },
    getFilter(name, player) {
      if (!player.hasSkill("sblongdan_mark", null, null, false)) {
        if (name === "sha") {
          return { name: "shan" };
        }
        if (name === "shan") {
          return { name: "sha" };
        }
        return () => false;
      }
      return { type: "basic" };
    },
    derivation: "jdlongdanx",
    onremove(player) {
      player.removeSkill("sblongdan_mark");
    },
    subSkill: {
      backup: { audio: "sblongdan" },
      mark: { charlotte: true },
      draw: {
        charlotte: true,
        trigger: { player: ["useCardAfter", "respondAfter"] },
        filter(event, player) {
          if (player.hasSkill("jdlongdan_mark")) {
            return false;
          }
          return event.skill === "jdlongdan_backup";
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          const result = (await player.draw(2).forResult()).cards;
          if (get.itemtype(result) === "cards") {
            player.addTempSkill("jdlongdan_mark", ["phaseChange", "phaseAfter"]);
          }
        }
      }
    }
  },
  jdjizhu: {
    inherit: "sbjizhu",
    audio: ["sbjizhu", 3],
    ai: {
      combo: "jdlongdan"
    }
  },
  //九鼎-甘宁
  jdqixi: {
    audio: "sbqixi",
    inherit: "sbqixi",
    filterCard(card) {
      return lib.suit.includes(get.suit(card));
    },
    check(card) {
      return 7 - get.value(card);
    },
    position: "h",
    lose: false,
    discard: false,
    async content(event, trigger, player) {
      const target = event.target;
      const suits = lib.suit.slice().reverse();
      let num = 0;
      while (suits.length > 0) {
        const { control } = await target.chooseControl(suits).set("prompt", `奇袭：猜测${get.translation(player)}选择的牌的花色`).set("ai", () => {
          const player2 = _status.event.getParent().player;
          const controls = _status.event.controls;
          if (player2.countCards("h") <= 3 && controls.includes("diamond") && Math.random() < 0.3) {
            return "diamond";
          }
          return controls.randomGet();
        }).forResult();
        if (control) {
          target.chat(`我猜是${get.translation(control)}！`);
          game.log(target, "猜测为", `#y${control}`);
          if (!event.isMine() && !event.isOnline()) {
            await game.delayx();
          }
          if (get.suit(event.cards[0]) !== control) {
            player.chat("猜错了！");
            game.log(target, "猜测", "#y错误");
            suits.remove(control);
            num++;
            continue;
          } else {
            player.chat(num === 0 ? "这么准？" : "猜对了！");
            game.log(target, "猜测", "#g正确");
            const card = event.cards[0];
            if (get.owner(card) === player && get.position(card) === "h") {
              await player.showCards([card], `${get.translation(player)}选择的手牌`);
              if (lib.filter.cardDiscardable(card, player)) {
                await player.discard([card]);
              }
            }
            if (num > 0 && target.countDiscardableCards(player, "hej")) {
              player.line(target);
              player.discardPlayerCard(target, num, true, "hej");
            }
            break;
          }
        } else {
          break;
        }
      }
    }
  },
  jdfenwei: {
    limited: true,
    audio: "sbfenwei",
    trigger: { global: "useCardToPlayered" },
    filter(event, player) {
      if (!event.isFirstTarget || get.type(event.card) !== "trick") {
        return false;
      }
      return event.targets.length >= 2;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt(event.skill), `令${get.translation(trigger.card)}对任意名角色无效`, [1, trigger.targets.length], (card, player2, target) => get.event().targets.includes(target)).set("ai", (target) => {
        const player2 = get.player();
        const trigger2 = get.event().getTrigger();
        return -get.effect(target, trigger2.card, trigger2.player, player2);
      }).set("targets", trigger.targets).forResult();
    },
    skillAnimation: true,
    animationColor: "wood",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      trigger.getParent().excluded.addArray(event.targets);
      if (event.targets.includes(player)) {
        player.addTempSkill("jdfenwei_qixi");
      }
    },
    ai: { expose: 0.2 },
    subSkill: {
      qixi: {
        charlotte: true,
        trigger: { global: "phaseEnd" },
        async cost(event, trigger, player) {
          const result = await player.chooseCardTarget({
            prompt: get.prompt("jdqixi"),
            prompt2: lib.translate.jdqixi_info.slice("出牌阶段限一次，你可以".length),
            filterCard: lib.skill.jdqixi.filterCard,
            filterTarget: lib.skill.jdqixi.filterTarget,
            position: lib.skill.jdqixi.position,
            ai1: lib.skill.jdqixi.check,
            ai2: (target) => {
              const player2 = get.player();
              return get.effect(target, "twyuanhu", player2, player2);
            }
          }).forResult();
          event.result = result;
          if (result.bool) {
            event.result.cost_data = result;
          }
        },
        popup: false,
        async content(event, trigger, player) {
          const result = event.cost_data;
          result.skill = "jdqixi";
          player.useResult(result, event);
        }
      }
    }
  },
  //九鼎-庞统
  jdlianhuan: {
    audio: "sblianhuan",
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      return event.card.name === "tiesuo" && !event.target.isLinked() && event.target.countCards("he");
    },
    direct: true,
    async content(event, trigger, player) {
      const target = trigger.target;
      player.discardPlayerCard(target, "he", get.prompt("jdlianhuan", target)).logSkill = ["jdlianhuan", target];
    },
    group: "jdlianhuan_lianhuan",
    subSkill: {
      lianhuan: {
        audio: "sblianhuan",
        inherit: "lianhuan",
        prompt: "将♣手牌当作【铁索连环】使用或重铸"
      }
    }
  },
  //九鼎-韩龙
  jdcibei: {
    audio: "cibei",
    inherit: "cibei",
    group: ["jdcibei_gain", "jdcibei_fullyReady"],
    subSkill: {
      fullyReady: {
        audio: "cibei",
        trigger: { global: "phaseEnd" },
        filter(event, player) {
          const storage = player.getExpansions("duwang");
          return storage.length > 0 && storage.every((i) => i.name === "sha");
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          await player.gain(player.getExpansions("duwang"), "gain2");
          player.addSkill("jdcibei_effect");
        }
      },
      effect: {
        mod: {
          cardUsable(card) {
            if (card.name === "sha") {
              return Infinity;
            }
          },
          targetInRange(card) {
            if (card.name === "sha") {
              return true;
            }
          }
        },
        charlotte: true,
        mark: true,
        marktext: "杀",
        intro: { content: "准备完毕！本局游戏使用【杀】无距离和次数限制" }
      },
      gain: {
        audio: "cibei",
        trigger: { global: "phaseEnd" },
        filter(event, player) {
          return player.hasHistory("lose", (evt) => evt.type === "discard" && evt.cards.filterInD("d").some((i) => i.name === "sha"));
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          player.gain(
            player.getHistory("lose", (evt) => evt.type === "discard" && evt.cards.filterInD("d").filter((i) => i.name === "sha")).slice().map((evt) => evt.cards.filterInD("d").filter((i) => i.name === "sha")).flat(),
            "gain2"
          );
        }
      }
    }
  },
  //九鼎-夏侯徽
  jdbaoqie: {
    audio: "baoqie",
    trigger: { player: "showCharacterAfter" },
    forced: true,
    hiddenSkill: true,
    filter(event, player) {
      return event.toShow?.some((i) => get.character(i).skills?.includes("jdbaoqie"));
    },
    async content(event, trigger, player) {
      const card = get.cardPile((card2) => get.subtype(card2, false) === "equip2" && !get.cardtag(card2, "gifts"));
      if (!card) {
        return;
      }
      await player.gain({
        cards: [card],
        animate: "gain2"
      });
      if (get.subtype(card) === "equip2" && player.hasCards("h", (cardx) => card === cardx)) {
        const next = player.chooseUseTarget({ card });
        next.nopopup = true;
        await next;
      }
    }
  },
  //九鼎-曹操
  jdjianxiong: {
    audio: "sbjianxiong",
    inherit: "sbjianxiong",
    filter(event, player) {
      return get.itemtype(event.cards) === "cards" && event.cards.some((i) => get.position(i, true) === "o") || 2 - player.countMark("sbjianxiong") > 0;
    },
    prompt2(event, player) {
      const gain = get.itemtype(event.cards) === "cards" && event.cards.some((i) => get.position(i, true) === "o");
      const draw = 2 - player.countMark("sbjianxiong");
      let str = "";
      if (gain) {
        str += `获得${get.translation(event.cards)}`;
      }
      if (gain && draw > 0) {
        str += "并";
      }
      if (draw > 0) {
        str += `摸${get.cnNumber(draw)}张牌`;
      }
      if (player.hasMark("sbjianxiong")) {
        str += "，然后可以弃1枚“治世”";
      }
      return str;
    },
    async content(event, trigger, player) {
      const nextEvents = [];
      if (get.itemtype(trigger.cards) === "cards" && trigger.cards.some((i) => get.position(i, true) === "o")) {
        nextEvents.push(player.gain(trigger.cards, "gain2"));
      }
      const num = player.countMark("sbjianxiong");
      if (2 - num > 0) {
        nextEvents.push(player.draw(2 - num, "nodelay"));
      }
      for (const next of nextEvents) {
        await next;
      }
      if (!num) {
        return;
      }
      const result = await player.chooseBool("是否弃1枚“治世”？").set("ai", () => {
        const player2 = _status.event.player;
        const current = _status.currentPhase;
        return get.distance(current, player2, "absolute") > 3 && player2.hp <= 2;
      }).forResult();
      if (result.bool) {
        player.removeMark("sbjianxiong", 1);
      }
    },
    ai: {
      maixie: true,
      maixie_hp: true,
      effect: {
        target(card, player, target) {
          if (player.hasSkillTag("jueqing", false, target)) {
            return [1, -1];
          }
          if (!get.tag(card, "damage") || player === target) {
            return;
          }
          let cards2 = card.cards;
          const evt = _status.event;
          if (evt.player === target && card.name === "damage" && evt.getParent().type === "card") {
            cards2 = evt.getParent().cards.filterInD();
          }
          if (target.hp <= 1) {
            return;
          }
          if (get.itemtype(cards2) !== "cards") {
            return;
          }
          for (const current of cards2) {
            if (get.name(current, target) === "tao") {
              return [1, 4.5];
            }
          }
          if (get.value(cards2, target) >= 7 + target.getDamagedHp()) {
            return [1, 2];
          }
          return [1, 0.55 + 0.05 * Math.max(0, 2 - target.countMark("sbjianxiong"))];
        }
      }
    },
    group: "jdjianxiong_mark"
  },
  //九鼎-诸葛亮
  jdhuoji: {
    audio: "sbhuoji",
    dutySkill: true,
    derivation: ["jdguanxing", "sbkongcheng"],
    group: ["jdhuoji_fire", "jdhuoji_achieve", "jdhuoji_fail", "jdhuoji_mark"],
    subSkill: {
      fire: {
        audio: "sbhuoji1.mp3",
        enable: "phaseUse",
        filterTarget: lib.filter.notMe,
        prompt: "选择一名其他角色，对其与其势力相同的所有其他角色各造成1点火属性伤害",
        usable: 1,
        line: "fire",
        async content(event, trigger, player) {
          const { target } = event;
          await target.damage({ nature: "fire" });
          const targets = game.filterPlayer((current) => current !== player && current !== target && current.group === target.group);
          if (targets.length) {
            player.line(targets, "fire");
            await game.delayx();
            await game.doAsyncInOrder(targets, (current) => current.damage({ nature: "fire" }));
          }
        },
        ai: {
          order: 7,
          fireAttack: true,
          result: {
            target(player, target) {
              const att = get.attitude(player, target);
              return get.sgn(att) * game.filterPlayer((current) => current !== player && current.group === target.group).reduce((num, current) => num + get.damageEffect(current, player, player, "fire"), 0);
            }
          }
        }
      },
      achieve: {
        audio: "jdhuoji2.mp3",
        trigger: { player: "phaseZhunbeiBegin" },
        filter(event, player) {
          return player.getAllHistory("sourceDamage", (evt) => evt.hasNature("fire") && evt.player !== player).reduce((num, evt) => num + evt.num, 0) >= game.players.length + game.dead.length;
        },
        forced: true,
        locked: false,
        skillAnimation: true,
        animationColor: "fire",
        async content(event, trigger, player) {
          player.awakenSkill("jdhuoji");
          game.log(player, "成功完成使命");
          player.changeSkin("jdhuoji", "sb_zhugeliang");
          player.changeSkills(["jdguanxing", "sbkongcheng"], ["jdhuoji", "jdkanpo"]);
        }
      },
      fail: {
        audio: "jdhuoji3.mp3",
        trigger: { player: "dying" },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          player.awakenSkill("jdhuoji");
          game.log(player, "使命失败");
        }
      },
      mark: {
        charlotte: true,
        trigger: { source: "damage" },
        filter(event, player) {
          return event.hasNature("fire");
        },
        firstDo: true,
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          player.addTempSkill("jdhuoji_count", {
            player: ["jdhuoji_achieveBegin", "jdhuoji_failBegin"]
          });
          player.storage.jdhuoji_count = player.getAllHistory("sourceDamage", (evt) => evt.hasNature("fire") && evt.player !== player).reduce((num, evt) => num + evt.num, 0);
          player.markSkill("jdhuoji_count");
        }
      },
      count: {
        charlotte: true,
        intro: { content: "本局游戏已造成过#点火属性伤害" }
      }
    }
  },
  jdkanpo: {
    audio: "sbkanpo",
    trigger: {
      global: ["phaseBefore", "useCard"],
      player: "enterGame"
    },
    filter(event, player) {
      if (event.name === "useCard") {
        return player.getExpansions("jdkanpo").map((i) => i.name).includes(event.card.name);
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    async cost(event, trigger, player) {
      if (trigger.name === "useCard") {
        event.result = await player.chooseButton([`###${get.prompt(event.skill)}###弃置一张同名牌，令${get.translation(trigger.card)}无效`, player.getExpansions(event.skill)]).set("filterButton", (button) => {
          const name = get.event().getTrigger().card.name;
          return button.link.name === name;
        }).set("ai", () => {
          const player2 = get.player();
          const trigger2 = get.event().getTrigger();
          return lib.skill.sbkanpo.subSkill.kanpo.check(trigger2, player2) ? 1 : 0;
        }).forResult();
        if (event.result.bool) {
          event.result.cards = event.result.links;
          event.result.targets = [trigger.player];
        }
      } else {
        event.result = { bool: true };
      }
    },
    async content(event, trigger, player) {
      if (trigger.name === "useCard") {
        await player.loseToDiscardpile(event.cards);
        trigger.targets.length = 0;
        trigger.all_excluded = true;
        game.log(trigger.card, "被无效了");
        await player.draw();
      } else {
        await player.draw(3);
        if (player.countCards("h")) {
          const result = await player.chooseCard("看破：是否将至多三张牌置于武将牌上？", [1, 3]).set("ai", (card) => {
            switch (card.name) {
              case "wuxie":
                return 5 + Math.random();
              case "sha":
                return 5 + Math.random();
              case "tao":
                return 4 + Math.random();
              case "jiu":
                return 3 + Math.random();
              case "lebu":
                return 3 + Math.random();
              case "shan":
                return 4.5 + Math.random();
              case "wuzhong":
                return 4 + Math.random();
              case "shunshou":
                return 2.7 + Math.random();
              case "nanman":
                return 2 + Math.random();
              case "wanjian":
                return 1.6 + Math.random();
              default:
                return 0;
            }
          }).forResult();
          if (result.bool) {
            await player.addToExpansion(result.cards, player, "giveAuto").set("gaintag", ["jdkanpo"]);
          }
        }
      }
    },
    marktext: "谋",
    intro: {
      mark(dialog, storage, player) {
        const cards2 = player.getExpansions("jdkanpo");
        if (player.isUnderControl(true)) {
          dialog.addAuto(cards2);
        } else {
          return `共有${get.cnNumber(cards2.length)}张牌`;
        }
      },
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    }
  },
  jdguanxing: {
    audio: "sbguanxing",
    inherit: "sbguanxing",
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player) {
      const bool = player.hasCard((card) => card.hasGaintag("sbguanxing"), "s");
      return bool || 7 - 2 * player.countMark("sbguanxingx") > 0;
    },
    async content(event, trigger, player) {
      player.addMark("sbguanxingx", 1, false);
      const cards2 = player.getCards("s", (card) => card.hasGaintag("sbguanxing"));
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
      const num = Math.max(0, 7 - 2 * (player.countMark("sbguanxingx") - 1));
      if (num) {
        const cards22 = get.cards(num);
        player.$gain2(cards22, false);
        game.log(player, "将", cards22, "置于了武将牌上");
        await player.loseToSpecial(cards22, "sbguanxing").set("visible", true);
        player.markSkill("sbguanxing");
      }
    },
    group: ["sbguanxing_unmark", "jdguanxing_put"],
    subSkill: {
      put: {
        audio: "sbguanxing",
        enable: "phaseUse",
        filter(event, player) {
          return player.hasCard((card) => card.hasGaintag("sbguanxing"), "s");
        },
        filterCard(card) {
          return card.hasGaintag("sbguanxing");
        },
        selectCard: [1, Infinity],
        position: "s",
        lose: false,
        discard: false,
        delay: 0,
        prompt: "将任意张“星”置于牌堆顶",
        allowChooseAll: true,
        async content(event, trigger, player) {
          player.loseToDiscardpile(event.cards, ui.cardPile, "insert").log = false;
          game.log(player, "将", event.cards, "置于了牌堆顶");
        }
      }
    }
  },
  //九鼎-司马师
  jdtairan: {
    audio: "tairan",
    inherit: "tairan",
    trigger: {
      player: "phaseJieshuBegin"
    },
    async content(event, trigger, player) {
      const maxHp = player.maxHp;
      const hp = maxHp - player.getHp();
      if (hp > 0) {
        await player.recoverTo(maxHp);
      }
      const num = maxHp - player.countCards("h");
      if (num > 0) {
        await player.drawTo(maxHp);
      }
      player.when("phaseUseBegin").step(async () => {
        if (hp > 0) {
          await player.loseHp(hp);
        }
        if (player.countCards("h") && num > 0) {
          await player.chooseToDiscard("h", num, true);
        }
      });
    }
  },
  //九鼎-张飞
  jdsbpaoxiao: {
    audio: "sbpaoxiao",
    inherit: "sbpaoxiao",
    async content(event, trigger, player) {
      if (!trigger.card.storage) {
        trigger.card.storage = {};
      }
      trigger.card.storage.jdsbpaoxiao = true;
      trigger.baseDamage++;
      trigger.directHit.addArray(game.players);
      player.addTempSkill(`${event.name}_effect`, "phaseUseAfter");
    },
    subSkill: {
      effect: {
        inherit: "sbpaoxiao_effect",
        filter(event, player) {
          return event.card.storage && event.card.storage.jdsbpaoxiao && event.target.isIn();
        },
        group: "jdsbpaoxiao_recoil"
      },
      recoil: {
        inherit: "sbpaoxiao_recoil",
        filter(event, player) {
          return event.card && event.card.storage && event.card.storage.jdsbpaoxiao && event.player.isIn();
        },
        async content(event, trigger, player) {
          await player.loseHp();
          if (player.countDiscardableCards(trigger.player, "h")) {
            await trigger.player.discardPlayerCard(player, "h", true);
          }
        }
      }
    }
  },
  //九鼎-法正
  jdsbxuanhuo: {
    audio: "sbxuanhuo",
    inherit: "sbxuanhuo",
    group: "jdsbxuanhuo_rob",
    filterTarget(card, player, target) {
      return !target.hasMark("jdsbxuanhuo_mark") && player !== target;
    },
    onremove(player) {
      delete player.storage.jdsbxuanhuo;
      player.unmarkSkill("jdsbxuanhuo");
    },
    subSkill: {
      mark: {
        marktext: "眩",
        intro: {
          name: "眩惑",
          name2: "眩",
          markcount: () => 0,
          content: "已获得“眩”标记"
        }
      },
      rob: {
        audio: "jdsbxuanhuo",
        inherit: "sbxuanhuo_rob",
        filter(event, player, name, target) {
          return target?.isIn();
        },
        getIndex(event, player) {
          const evt = event.getParent("phaseDraw");
          if (evt?.name === "phaseDraw") {
            return false;
          }
          return game.filterPlayer((current) => {
            if (!event.getg(current).length || !current.hasMark("jdsbxuanhuo_mark")) {
              return false;
            }
            if (evt?.player === current) {
              return false;
            }
            if (lib.skill.sbxuanhuo.getNum(current, "jdsbxuanhuo_rob", "jdsbxuanhuo_mark") >= 5) {
              return false;
            }
            return current.hasCard((card) => lib.filter.canBeGained(card, current, player), "he");
          }).sortBySeat();
        },
        async content(event, trigger, player) {
          const target = event.targets[0];
          const hs = target.getCards("h", (card) => lib.filter.canBeGained(card, target, player));
          if (hs.length) {
            await player.gainPlayerCard(target, "h", true);
            if (!player.storage.jdsbxuanhuo) {
              player.storage.jdsbxuanhuo = {};
            }
            player.storage.jdsbxuanhuo[target.playerid] = lib.skill.sbxuanhuo.getNum(target, "jdsbxuanhuo_rob", "jdsbxuanhuo_mark");
            player.markSkill("jdsbxuanhuo");
          }
        }
      }
    }
  },
  jdsbenyuan: {
    audio: "sbenyuan",
    inherit: "sbenyuan",
    filter(event, player, name, target) {
      return target?.isIn();
    },
    getIndex(event, player) {
      return game.filterPlayer((target) => target.hasMark("jdsbxuanhuo_mark")).sortBySeat();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      target.clearMark("jdsbxuanhuo_mark");
      for (const current of game.players) {
        const storage = current.storage.jdsbxuanhuo;
        if (storage && storage[target.playerid]) {
          delete storage[target.playerid];
        }
        if (storage && get.is.empty(storage)) {
          delete current.storage.jdsbxuanhuo;
          current.unmarkSkill("jdsbxuanhuo");
        }
      }
      const bool = target.countCards("h") < player.countCards("h");
      player.logSkill("jdsbenyuan", target, null, null, [bool ? 1 : 2]);
      if (bool) {
        const num = Math.min(player.countCards("he"), 2);
        if (num) {
          await player.chooseToGive(target, `恩怨：交给${get.translation(target)}${get.cnNumber(num)}张牌`, true, num, "he");
        }
      } else {
        await target.loseHp();
        await player.recover();
      }
    },
    ai: {
      combo: "jdsbxuanhuo"
    }
  },
  //九鼎-刘备
  jdsbzhangwu: {
    audio: "sbzhangwu",
    enable: "phaseUse",
    filter(event, player) {
      return player.countMark("sbrende");
    },
    limited: true,
    chooseButton: {
      dialog(event, player) {
        return ui.create.dialog(`###章武###${get.translation("jdsbzhangwu_info")}`);
      },
      chooseControl(event, player) {
        return Array.from({
          length: player.countMark("sbrende")
        }).map((_, i) => get.cnNumber(i + 1, true)).concat(["cancel2"]);
      },
      check(event, player) {
        const choices = Array.from({
          length: player.countMark("sbrende")
        }).map((_, i) => get.cnNumber(i + 1, true));
        return choices.length - 1;
      },
      backup(result, player) {
        return {
          num: result.index + 1,
          audio: "sbzhangwu",
          filterCard: () => false,
          selectCard: -1,
          skillAnimation: "epic",
          animationColor: "orange",
          async content(event, trigger, player2) {
            player2.awakenSkill("jdsbzhangwu");
            const num = lib.skill.jdsbzhangwu_backup.num;
            player2.removeMark("sbrende", num);
            await player2.draw(num);
            player2.tempBanSkill("sbrende", { player: "dying" });
            player2.addTempSkill("new_repaoxiao2");
          }
        };
      },
      prompt(result, player) {
        return `移去${result.index + 1}枚“仁望”并摸等量张牌`;
      }
    },
    ai: {
      order: 9,
      combo: "sbrende",
      result: {
        player(player, target) {
          return player.countMark("sbrende") > 3 ? 1 : 0;
        }
      }
    },
    subSkill: {
      backup: {}
    }
  },
  //九鼎-孙尚香
  jdsbjieyin: {
    audio: "sbjieyin",
    trigger: { player: "phaseUseBegin" },
    filter(event, player) {
      return game.hasPlayer((current) => current.countCards("h") <= player.countCards("h"));
    },
    locked: true,
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.name.slice(0, -5)), (card, player2, target) => target.countCards("h") <= player2.countCards("h"), true).set("ai", (target) => get.attitude(get.player(), target) * (target.countCards("h") + 1)).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const num = Math.min(2, Math.max(1, target.countCards("h")));
      let result;
      if (player == target) {
        result = !target.hasCards("h") ? { bool: false } : await player.chooseBool(`你发动了【结姻】`, "点击“确定”获得1点护甲，或点击“取消”回复1点体力并获得所有“妆”，然后减少1点体力上限，变更势力为吴").forResult();
      } else {
        result = await target.chooseToGive(player, `${get.translation(player)}对你发动了【结姻】`, `你可以交给${get.translation(player)}${get.cnNumber(num)}张手牌，然后获得1点护甲；或点击“取消”，其回复1点体力并获得所有“妆”，然后其减少1点体力上限，变更势力为吴`, num, "h").set("ai", (card) => {
          if (_status.event.goon) {
            return 100 - get.value(card);
          }
          return 0;
        }).set("goon", get.attitude(target, player) > 1).forResult();
      }
      if (result?.bool) {
        await target.changeHujia(1, null, true);
      } else {
        await player.recover();
        if (player.getExpansions("jdsbliangzhu").length) {
          await player.gain(player.getExpansions("jdsbliangzhu"), "gain2");
        }
        await player.loseMaxHp();
        if (player.group !== "wu") {
          await player.changeGroup("wu");
        }
      }
    }
  },
  jdsbliangzhu: {
    groupSkill: "shu",
    audio: "sbliangzhu",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.group == "shu" && game.hasPlayer((current) => get.info("jdsbliangzhu").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return player != target && target.hasCards("e");
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      let result = await player.choosePlayerCard(target, "e", true).forResult();
      if (!result?.cards?.length) {
        return;
      }
      const { cards: cards2 } = result;
      const next = player.addToExpansion(cards2, target, "give");
      next.gaintag.add(event.name);
      await next;
      const targets = game.filterPlayer((current) => current != player && current.isDamaged());
      if (!targets.length) {
        return;
      }
      result = targets.length == 1 ? { bool: true, targets } : await player.chooseTarget(`选择一名其他角色，令其回复1点体力`, (card, player2, target2) => {
        return target2 != player2 && target2.isDamaged();
      }).set("ai", (target2) => {
        const player2 = get.player();
        return get.recoverEffect(target2, player2, player2);
      }).forResult();
      if (result?.targets?.length) {
        await result.targets[0].recover();
      }
    },
    marktext: "妆",
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    ai: {
      order: 9,
      result: {
        player: 1,
        target: -1
      }
    }
  },
  //九鼎-种地的
  jdsbjieyue: {
    audio: "sbjieyue",
    trigger: {
      player: "phaseJieshuBegin"
    },
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(lib.filter.notMe, get.prompt2(event.name.slice(0, -5))).set("ai", (target) => get.attitude(get.player(), target) / Math.sqrt(Math.min(1, target.hp + target.hujia))).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.draw(2);
      await target.changeHujia(1, null, true);
      if (target.countCards("he")) {
        await target.chooseToGive(player, "he", Math.min(2, target.countCards("he")), true);
      }
    }
  },
  //九鼎-高贵名门
  jdsbluanji: {
    audio: "sbluanji",
    inherit: "sbluanji",
    filter(event, player) {
      if (event.name === "chooseToUse") {
        return player.countCards("hs") > 1 && !player.hasSkill("jdsbluanji_used");
      }
      const evt = event.getParent(2);
      return evt.name === "wanjian" && evt.getParent().player === player && event.player !== player && event.player.countCards("h") > player.countCards("h") && player.countCards("h") < player.getHp();
    },
    async precontent(event, trigger, player) {
      player.addTempSkill("jdsbluanji_used", "phaseUseAfter");
    },
    subSkill: {
      used: {
        charlotte: true
      }
    }
  },
  jdsbxueyi: {
    audio: "sbxueyi",
    trigger: {
      global: ["useCardAfter", "respondAfter"]
    },
    filter(event, player) {
      if (!event.respondTo) {
        return false;
      }
      if (player !== event.respondTo[0]) {
        return false;
      }
      return player.hasZhuSkill("jdsbxueyi") && event.player !== player && event.player.group === "qun";
    },
    zhuSkill: true,
    forced: true,
    logTarget: "player",
    async content(event, trigger, player) {
      for (const name of lib.phaseName) {
        const evt = _status.event.getParent(name);
        if (!evt || evt.name !== name) {
          continue;
        }
        trigger.player.addTempSkill(`${event.name}_ban`, `${name}After`);
        break;
      }
    },
    mod: {
      maxHandcard(player, num) {
        if (player.hasZhuSkill("jdsbxueyi")) {
          return num + 2 * game.countPlayer((current) => player !== current && current.group === "qun");
        }
      }
    },
    subSkill: {
      ban: {
        charlotte: true,
        mark: true,
        mod: {
          cardEnabled2(card) {
            if (get.position(card) === "h") {
              return false;
            }
          }
        },
        intro: {
          content: "不能使用或打出手牌"
        }
      }
    }
  },
  //九鼎-孟获
  jdsbhuoshou: {
    audio: "sbhuoshou",
    trigger: {
      player: "phaseUseEnd"
    },
    filter(event, player) {
      return player.countCards("h");
    },
    async content(event, trigger, player) {
      await player.discard(player.getCards("h"));
      const nanman = get.autoViewAs({ name: "nanman", isCard: true });
      if (player.hasUseTarget(nanman)) {
        await player.chooseUseTarget(nanman, true, false);
      }
    },
    forced: true,
    group: ["sbhuoshou_cancel", "sbhuoshou_source"]
  },
  jdsbzaiqi: {
    audio: "sbzaiqi",
    trigger: {
      player: "phaseDiscardEnd"
    },
    filter(event, player) {
      return player.getHistory("lose", (evt) => evt.type === "discard").length;
    },
    async cost(event, trigger, player) {
      const num = player.getHistory("lose", (evt) => evt.type === "discard").reduce((num2, evt) => num2 + evt.cards2.length, 0);
      event.result = await player.chooseTarget(get.prompt2(event.name.slice(0, -5)), [1, num]).set("ai", (target) => {
        const player2 = get.player();
        const att = get.attitude(player2, target);
        return 3 - get.sgn(att) + Math.abs(att / 1e3);
      }).forResult();
    },
    async content(event, trigger, player) {
      const targets = event.targets.sortBySeat();
      while (targets.length) {
        const target = targets.shift();
        const bool = !target.countCards("he") ? false : (await target.chooseToDiscard(`${get.translation(player)}对你发动了【再起】`, "是否弃置一张牌令其回复1点体力？或者点击“取消”，令该角色摸一张牌。", "he").set("ai", (card) => {
          const eff = _status.event.eff;
          const att = _status.event.att;
          if (eff > 0 && att > 0 || eff <= 0 && att < 0) {
            return 5.5 - get.value(card);
          }
          return 0;
        }).set("eff", get.recoverEffect(player, player, target)).set("att", get.attitude(target, player)).forResult()).bool;
        target.line(player);
        if (bool) {
          await player.recover(target);
        } else {
          await player.draw();
        }
      }
    }
  },
  //九鼎-大乔
  jdsbguose: {
    audio: "sbguose",
    inherit: "sbguose",
    usable: 1,
    filterTarget(card, player, target) {
      if (!ui.selected.cards.length) {
        if (!target.hasJudge("lebu")) {
          return false;
        }
        return game.hasPlayer((current) => current !== target && current.canAddJudge("lebu"));
      }
      if (player === target) {
        return false;
      }
      return player.canUse(get.autoViewAs({ name: "lebu" }, ui.selected.cards), target);
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      if (target.hasJudge("lebu")) {
        await player.moveCard(true, (card) => (card.viewAs || card.name) === "lebu").set("sourceTargets", [target]).set(
          "aimTargets",
          game.filterPlayer((current) => current !== target && current.canAddJudge("lebu"))
        ).set("prompt", `移动${get.translation(target)}的一张【乐不思蜀】`);
      } else {
        const next = player.useCard({ name: "lebu" }, target, event.cards);
        next.audio = false;
        await next;
      }
    }
  },
  jdsbliuli: {
    audio: "sbliuli",
    inherit: "liuli",
    group: "jdsbliuli_add",
    subSkill: {
      add: {
        trigger: { player: "logSkill" },
        filter(event, player) {
          if (event.skill !== "jdsbliuli") {
            return false;
          }
          return event.targets[0].isIn();
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          game.filterPlayer().forEach((current) => current.removeSkill("jdsbliuli_dangxian"));
          trigger.targets[0].addSkill("jdsbliuli_dangxian");
        }
      },
      dangxian: {
        trigger: { player: "phaseBegin" },
        forced: true,
        charlotte: true,
        mark: true,
        marktext: "流",
        intro: { content: "回合开始时，执行一个额外的出牌阶段" },
        async content(event, trigger, player) {
          player.removeSkill(event.name);
          trigger.phaseList.splice(trigger.num, 0, `phaseUse|${event.name}`);
        }
      }
    }
  },
  //九鼎-姜维
  jdsbtiaoxin: {
    audio: "sbtiaoxin",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    filterTarget: lib.filter.notMe,
    selectTarget() {
      return [1, get.player().getHp()];
    },
    multiline: true,
    async content(event, trigger, player) {
      const target = event.target;
      const { bool } = await target.chooseToUse(
        function(card, player2, event2) {
          if (get.name(card) !== "sha") {
            return false;
          }
          return lib.filter.filterCard.apply(this, arguments);
        },
        `挑衅：对${get.translation(player)}使用一张杀，或令其获得你一张牌`
      ).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("filterTarget", function(card, player2, target2) {
        if (target2 !== _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
          return false;
        }
        return lib.filter.targetEnabled.apply(this, arguments);
      }).set("sourcex", player).forResult();
      if (!target.countGainableCards(player, "he")) {
        return;
      }
      if (!bool || bool && !target.hasHistory("sourceDamage", (evt) => evt.getParent(4) === event)) {
        await player.gainPlayerCard(target, "he", true);
      }
    },
    ai: {
      threaten: 1.2,
      order: 4,
      expose: 0.2,
      result: {
        target(player, target) {
          if (target.countGainableCards(player, "he") === 0) {
            return 0;
          }
          return -1;
        },
        player(player, target) {
          if (!target.canUse("sha", player)) {
            return 0;
          }
          if (target.countCards("h") === 0) {
            return 0;
          }
          if (target.countCards("h") === 1) {
            return -0.1;
          }
          if (player.hp <= 2) {
            return -2;
          }
          if (player.countCards("h", "shan") === 0) {
            return -1;
          }
          return -0.5;
        }
      }
    }
  },
  jdsbzhiji: {
    audio: "sbzhiji",
    trigger: {
      player: "dying"
    },
    juexingji: true,
    forced: true,
    skillAnimation: true,
    animationColor: "fire",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await player.recoverTo(2);
      await player.loseMaxHp();
      await player.addSkills("jdsbbeifa");
      if (player.isMinHandcard()) {
        await player.draw(2);
      }
    },
    derivation: "jdsbbeifa"
  },
  jdsbbeifa: {
    enable: "phaseUse",
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    filterCard: lib.filter.cardDiscardable,
    selectCard: [1, Infinity],
    check(card) {
      if (ui.selected.cards.length > 2) {
        return 0;
      }
      const player = get.player();
      if (game.hasPlayer((current) => current !== player && get.attitude(player, current) > 0 && current.getCards("h").some((cardx) => get.name(cardx) === get.name(card)))) {
        return 1;
      }
      return 7.5 - get.value(card);
    },
    allowChooseAll: true,
    async content(event, trigger, player) {
      const cards2 = event.cards;
      const num = cards2.length;
      const names = cards2.map((card) => get.name(card)).toUniqued();
      if (!game.hasPlayer((current) => current !== player && current.countCards("h"))) {
        return;
      }
      const { targets } = await player.chooseTarget(`北伐：令一名其他角色展示${num}张手牌`, true, (card, player2, target2) => target2 !== player2 && target2.countCards("h")).set("ai", (target2) => {
        const player2 = get.player();
        return get.attitude(player2, target2) * (1 + target2.countCards("h"));
      }).forResult();
      if (!targets || !targets.length) {
        return;
      }
      const target = targets[0];
      const { cards: showCards } = await target.chooseCard("h", Math.min(num, target.countCards("h")), true, `选择${get.translation(num)}张手牌展示`).set("ai", (card) => {
        get.player();
        const goon = get.event().goon;
        const names2 = get.event().names;
        if (goon) {
          if (names2.includes(get.name(card))) {
            return 10;
          }
          return 7.5 - get.value(card);
        }
        if (names2.includes(get.name(card))) {
          return 0;
        }
        return 6 - get.value(card);
      }).set("goon", get.attitude(target, player) > 0).set("names", names).forResult();
      if (!showCards || !showCards.length) {
        return;
      }
      await target.showCards(showCards);
      while (showCards.some((card) => names.includes(get.name(card)) && player.hasUseTarget(get.autoViewAs({ name: "sha" }, [card]), false, false))) {
        const { links } = await player.chooseButton(["北伐：将其中一张牌当【杀】使用", showCards]).set("filterButton", (button) => {
          const player2 = get.player();
          const card2 = button.link;
          if (!get.event().names.includes(get.name(card2))) {
            return false;
          }
          return player2.hasUseTarget(get.autoViewAs({ name: "sha" }, [card2]), false, false);
        }).set("ai", (button) => get.value(button.link)).set("names", names).forResult();
        if (!links || !links.length) {
          break;
        }
        showCards.removeArray(links);
        const card = links[0];
        const cardx = {
          name: "sha",
          cards: [card]
        };
        await player.chooseUseTarget(cardx, [card], true, false);
      }
    },
    ai: {
      order: 10,
      result: {
        player(player, target) {
          if (!game.hasPlayer((current) => get.effect(current, { name: "sha" }, player, player) > 0)) {
            return 0;
          }
          const names = player.getCards("he").map((card) => get.name(card)).toUniqued();
          if (game.hasPlayer((current) => current !== player && get.attitude(player, current) > 0 && current.getCards("h").some((card) => names.includes(get.name(card))))) {
            return 1;
          }
          return 0;
        }
      }
    }
  },
  //九鼎-关羽
  jdsbwusheng: {
    audio: "sbwusheng",
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player) {
      return game.hasPlayer((target) => target !== player && target.countCards("h"));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt(event.name.slice(0, -5)), "令一名其他角色展示所有手牌，本阶段对其使用的前X张【杀】无距离和次数限制且结算后你摸一张牌（X为其以此法展示的红色手牌数）", (card, player2, target) => target !== player2 && target.countCards("h")).set("ai", (target) => {
        const player2 = get.player();
        return get.effect(target, { name: "sha" }, player2, player2) * (1 + target.countCards("h", { color: "red" }));
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.showHandcards();
      if (get.mode() !== "identity" || player.identity !== "nei") {
        player.addExpose(0.25);
      }
      const num = target.countCards("h", { color: "red" });
      if (num > 0) {
        player.addTempSkill("jdsbwusheng_effect", { player: "phaseUseAfter" });
        player.storage.jdsbwusheng_effect[target.playerid] = num;
      }
    },
    group: "sbwusheng_wusheng",
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        init(player, skill) {
          if (!player.storage[skill]) {
            player.storage[skill] = {};
          }
        },
        mod: {
          targetInRange(card, player, target) {
            if (card.name !== "sha" || typeof player.storage.jdsbwusheng_effect[target.playerid] !== "number") {
              return;
            }
            if (player.storage.jdsbwusheng_effect[target.playerid] > 0) {
              return true;
            }
          },
          cardUsableTarget(card, player, target) {
            if (card.name !== "sha" || typeof player.storage.jdsbwusheng_effect[target.playerid] !== "number") {
              return;
            }
            if (player.storage.jdsbwusheng_effect[target.playerid] > 0) {
              return true;
            }
          }
        },
        audio: "sbwusheng",
        trigger: {
          player: ["useCardAfter", "useCard1"]
        },
        filter(event, player, name) {
          if (event.card.name !== "sha" || name === "useCard1" && event.addCount === false) {
            return false;
          }
          return event.targets.some((target) => typeof player.storage.jdsbwusheng_effect[target.playerid] === "number" && player.storage.jdsbwusheng_effect[target.playerid] > 0);
        },
        forced: true,
        async content(event, trigger, player) {
          if (event.triggername === "useCard1") {
            trigger.addCount = false;
            const stat = player.getStat().card;
            const name = trigger.card.name;
            if (typeof stat[name] === "number") {
              stat[name]--;
            }
          } else {
            const targets = trigger.targets.filter((target) => typeof player.storage.jdsbwusheng_effect[target.playerid] === "number" && player.storage.jdsbwusheng_effect[target.playerid] > 0);
            player.line(targets);
            await player.draw(targets.length);
            for (const target of targets) {
              player.storage.jdsbwusheng_effect[target.playerid]--;
            }
          }
        }
      }
    }
  },
  jdsbyijue: {
    audio: "sbyijue",
    trigger: {
      player: "phaseZhunbeiBegin"
    },
    filter(event, player) {
      return game.hasPlayer((current) => current !== player);
    },
    forced: true,
    logTarget(event, player) {
      return game.filterPlayer((current) => current !== player).sortBySeat();
    },
    async content(event, trigger, player) {
      const givers = [];
      for (const target of event.targets) {
        const bool = !target.countCards("he") ? false : (await target.chooseToGive(player, "he", `交给${get.translation(player)}一张牌，本回合当你首次受到其的【杀】的造成的伤害时，防止之`).set("ai", (card) => {
          const player2 = get.event().player;
          const target2 = get.event().getParent().player;
          const att = get.attitude(player2, target2);
          if (att >= 0) {
            return 0;
          }
          if (player2.getHp() > 1 || !target2.canUse({ name: "sha" }, player2, true, true)) {
            return 0;
          }
          return 7.5 - get.value(card);
        }).forResult()).bool;
        if (bool) {
          givers.add(target);
        }
      }
      if (givers.length) {
        player.addTempSkill(`${event.name}_effect`);
        player.markAuto(`${event.name}_effect`, givers);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "本回合$首次受到你的【杀】的造成的伤害时，你防止之"
        },
        trigger: {
          global: "damageBegin4"
        },
        filter(event, player) {
          if (!player.getStorage("jdsbyijue_effect").includes(event.player)) {
            return false;
          }
          return event.card && event.card.name === "sha" && event.getParent().type === "card";
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          trigger.cancel();
          player.unmarkAuto(event.name, [trigger.player]);
        }
      }
    }
  },
  //九鼎-小乔
  jdsbtianxiang: {
    audio: "sbtianxiang",
    trigger: {
      player: "damageBegin4"
    },
    filter(event, player) {
      return player.countCards("h") > 1 && event.num > 0;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        filterCard: true,
        selectCard: 2,
        filterTarget: lib.filter.notMe,
        position: "h",
        ai1(card) {
          return 10 - get.value(card);
        },
        ai2(target) {
          const att = get.attitude(_status.event.player, target);
          const trigger2 = _status.event.getTrigger();
          let da = 0;
          if (_status.event.player.hp === 1) {
            da = 10;
          }
          const eff = get.damageEffect(target, trigger2.source, target);
          if (att === 0) {
            return 0.1 + da;
          }
          if (eff >= 0 && att > 0) {
            return att + da;
          }
          if (att > 0 && target.hp > 1) {
            if (target.maxHp - target.hp >= 3) {
              return att * 1.1 + da;
            }
            if (target.maxHp - target.hp >= 2) {
              return att * 0.9 + da;
            }
          }
          return -att + da;
        },
        prompt: get.prompt(event.skill),
        prompt2: lib.translate[`${event.skill}_info`]
      }).forResult();
    },
    async content(event, trigger, player) {
      const cards2 = event.cards;
      const target = event.targets[0];
      await player.showCards(cards2);
      const { links } = await target.chooseButton(["天香：获得其中一张牌", cards2], true).set("ai", (button) => {
        get.player();
        const card = button.link;
        return get.value(card);
      }).forResult();
      if (!links || !links.length) {
        return;
      }
      const suit = get.suit(links[0], player);
      await target.gain(links, "gain2");
      if (suit === "heart") {
        trigger.cancel();
        await target.damage(trigger.source || "nosource", trigger.nature, trigger.num).set("card", trigger.card).set("cards", trigger.cards);
      } else {
        target.addTempSkill(`${event.name}_effect`);
        target.markAuto(`${event.name}_effect`, [get.type2(links[0])]);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        mark: true,
        intro: {
          content: (storage) => `本回合不能使用${get.translation(storage)}牌`
        },
        mod: {
          cardEnabled(card, player) {
            const hs = player.getCards("h");
            const cards2 = [card];
            if (Array.isArray(card.cards)) {
              cards2.addArray(card.cards);
            }
            if (cards2.containsSome(...hs) && player.getStorage("jdsbtianxiang_effect").includes(get.type2(card))) {
              return false;
            }
          },
          cardSavable(card, player) {
            const hs = player.getCards("h");
            const cards2 = [card];
            if (Array.isArray(card.cards)) {
              cards2.addArray(card.cards);
            }
            if (cards2.containsSome(...hs) && player.getStorage("jdsbtianxiang_effect").includes(get.type2(card))) {
              return false;
            }
          }
        }
      }
    }
  },
  jdsbhongyan: {
    audio: "xinhongyan",
    mod: {
      suit(card, suit) {
        if (suit === "spade") {
          return "heart";
        }
      }
    },
    trigger: {
      player: "loseAfter",
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    forced: true,
    filter(event, player) {
      if (player.hasHistory("gain", (evt2) => evt2.getParent().name === "draw" && evt2.getParent(2).name === "jdsbhongyan")) {
        return false;
      }
      const evt = event.getl(player);
      return evt.cards2.some((i) => get.suit(i, player) === "heart");
    },
    async content(event, trigger, player) {
      if (!trigger.visible) {
        const cards2 = trigger.getl(player).hs.filter((i) => get.suit(i, player) === "heart");
        if (cards2.length > 0) {
          await player.showCards(cards2, `${get.translation(player)}发动了【红颜】`);
        }
      }
      await player.draw();
    }
  },
  //九鼎-孙权
  jdsbzhiheng: {
    audio: "sbzhiheng",
    locked: false,
    mod: {
      aiOrder(player, card, num) {
        if (num <= 0 || get.itemtype(card) !== "card" || get.type(card) !== "equip") {
          return num;
        }
        const eq = player.getEquip(get.subtype(card));
        if (eq && get.equipValue(card) - get.equipValue(eq) < Math.max(1.2, 6 - player.hp)) {
          return 0;
        }
      }
    },
    enable: "phaseUse",
    usable: 1,
    position: "he",
    filterCard: lib.filter.cardDiscardable,
    discard: false,
    lose: false,
    delay: false,
    selectCard: [1, Infinity],
    check(card) {
      const player = _status.event.player;
      if (get.position(card) === "e") {
        if (ui.selected.cards.some((i) => get.position(i) === "e")) {
          return 0;
        }
        const subs = get.subtypes(card);
        if (subs.includes("equip2") || subs.includes("equip3")) {
          return 2 * player.getHp() - get.value(card);
        }
        return 12 - get.value(card);
      }
      return 6 - get.value(card);
    },
    prompt() {
      return "出牌阶段限一次。你可以弃置任意张牌并摸等量的牌，若你以此法弃置的牌包括你装备区的牌，则你多摸一张牌";
    },
    allowChooseAll: true,
    async content(event, trigger, player) {
      const cards2 = event.cards;
      const num = cards2.some((card) => player.getCards("e").includes(card)) ? 1 : 0;
      await player.discard(cards2);
      await player.draw(cards2.length + num);
    },
    ai: {
      order(item, player) {
        if (player.hasCard((i) => get.value(i) > Math.max(6, 9 - player.hp), "he")) {
          return 1;
        }
        return 10;
      },
      result: {
        player: 1
      },
      nokeep: true,
      skillTagFilter(player, tag, arg) {
        if (tag === "nokeep") {
          return (!arg || arg && arg.card && get.name(arg.card) === "tao") && player.isPhaseUsing() && !player.getStat().skill.sbzhiheng && player.hasCard((card) => get.name(card) !== "tao", "h");
        }
      },
      threaten: 1.56
    }
  },
  jdsbtongye: {
    init(player) {
      if (game.shuffleNumber === 0) {
        player.addAdditionalSkill("jdsbtongye", get.info("jdsbtongye").derivation);
        lib.onwash.push(() => {
          player.removeAdditionalSkill("jdsbtongye");
        });
      }
    },
    onremove(player) {
      player.removeAdditionalSkill("jdsbtongye");
    },
    derivation: ["sbyingzi", "olguzheng"],
    locked: true
  },
  jdsbjiuyuan: {
    audio: "sbjiuyuan",
    enable: "phaseUse",
    usable: 1,
    zhuSkill: true,
    filter(event, player) {
      return game.hasPlayer((current) => get.info("jdsbjiuyuan").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return target !== player && target.group === "wu" && target.countGainableCards(player, "e") && player.hasZhuSkill("jdsbjiuyuan", target);
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await player.gain(target.getCards("e"), target, "giveAuto", "bySelf");
      await player.recover();
    },
    ai: {
      order: 10,
      result: {
        target(player, target) {
          return get.effect(target, { name: "shunshou_copy2" }, player, target) * target.countGainableCards(player, "e");
        }
      }
    }
  },
  //九鼎-司马炎
  jdfengtu: {
    mode: ["identity", "guozhan", "doudizhu", "versus"],
    available(mode) {
      if (mode === "versus" && _status.mode === "three") {
        return false;
      }
    },
    trigger: { global: "dieAfter" },
    filter(event, player) {
      return game.hasPlayer((target) => !game.getAllGlobalHistory("everything", (evt) => evt.name === "loseMaxHp" && evt.getParent().name === "jdfengtu" && evt.player === target).length);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => get.event().targets.includes(target)).set("ai", (target) => {
        const player2 = get.event().player;
        const att = get.attitude(player2, target);
        if (target.maxHp <= 1) {
          return 114514119810 * get.sgn(-att);
        }
        if (player2.identity === "nei" && target !== player2) {
          return 0;
        }
        return (target.maxHp - 1) * att;
      }).set(
        "targets",
        game.filterPlayer((target) => !game.getAllGlobalHistory("everything", (evt) => evt.name === "loseMaxHp" && evt.getParent().name === "jdfengtu" && evt.player === target).length)
      ).forResult();
    },
    async content(event, trigger, player) {
      if (!lib.onround.includes(lib.skill.jdfengtu.onRound)) {
        lib.onround.push(lib.skill.jdfengtu.onRound);
      }
      const target = event.targets[0];
      await target.loseMaxHp();
      target.addSkill("jdfengtu_phase");
      target.markAuto("jdfengtu_phase", [trigger.player]);
    },
    onRound(event) {
      return event.getParent().skill !== "jdfengtu_phase" && (event.relatedEvent || event.getParent(2)).name !== "jdfengtu_phase";
    },
    check(source, player) {
      const players = game.players.slice().concat(game.dead).sort((a, b) => parseInt(a.dataset.position) - parseInt(b.dataset.position));
      const num = players.indexOf(source);
      const num2 = players.indexOf(player);
      return num2 - num === 1 || num === players.length - 1 && num2 === 0;
    },
    subSkill: {
      phase: {
        charlotte: true,
        trigger: { global: "phaseOver" },
        filter(event, player) {
          return player.getStorage("jdfengtu_phase").some((target) => !game.players.includes(target) && lib.skill.jdfengtu.check(event.player, target));
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          const next = player.insertPhase();
          delete next.skill;
        },
        intro: { content: "获得$的额定回合" }
      }
    }
  },
  jdjuqi: {
    trigger: { global: "phaseZhunbeiBegin" },
    filter(event, player) {
      const storage = player.storage.jdjuqi;
      return event.player === player || event.player.countCards("h", (card) => _status.connectMode || (get.color(card, event.player) === storage ? "red" : "black"));
    },
    async cost(event, trigger, player) {
      const target = trigger.player;
      if (target === player) {
        event.result = { bool: true };
      } else {
        const color = player.storage.jdjuqi ? "red" : "black";
        event.result = await target.chooseCard((card, player2) => get.color(card, player2) === _status.event.color, `举棋：你可以交给${get.translation(player)}一张${get.translation(color)}手牌`).set("ai", (card) => {
          const player2 = get.player();
          const target2 = get.event().target;
          if (get.attitude(player2, target2) <= 0) {
            return 0;
          }
          return 6 - get.value(card);
        }).set("color", color).set("target", player).forResult();
      }
    },
    async content(event, trigger, player) {
      const target = trigger.player;
      player.changeZhuanhuanji(event.name);
      if (target === player) {
        if (player.storage[event.name]) {
          await player.draw(3);
        } else {
          player.addTempSkill(`${event.name}_effect`);
        }
      } else {
        await target.showCards(event.cards);
        await target.give(event.cards, player, true);
      }
    },
    mark: true,
    zhuanhuanji: true,
    marktext: "☯",
    intro: {
      content(storage) {
        if (storage) {
          return "<li>准备阶段，你令你本回合使用牌无次数限制且造成的伤害+1<br><li>其他角色的准备阶段，其可以展示并交给你一张红色手牌";
        }
        return "<li>准备阶段，你摸三张牌<br><li>其他角色的准备阶段，其可以展示并交给你一张黑色手牌";
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        mod: { cardUsable: () => Infinity },
        forced: true,
        popup: false,
        trigger: { source: "damageBegin1" },
        filter(event, player) {
          return event.card && event.getParent().type === "card";
        },
        async content(event, trigger, player) {
          trigger.num++;
        },
        mark: true,
        intro: { content: "本回合使用牌无次数限制且造成的伤害+1" }
      }
    }
  },
  jdtaishi: {
    zhuSkill: true,
    trigger: { global: "phaseBeginStart" },
    filter(event, player) {
      return game.hasPlayer((current) => current.isUnseen(2));
    },
    logTarget() {
      return game.filterPlayer((current) => current.isUnseen(2)).sortBySeat();
    },
    limited: true,
    skillAnimation: true,
    animationColor: "orange",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      for (const target of game.filterPlayer((current) => current.isUnseen(2)).sortBySeat()) {
        await target.showCharacter(2);
      }
    }
  }
};
const skills$e = {
  //燕幽烽火
  //全琮
  yyyaoming: {
    audio: "sbyaoming",
    enable: "phaseUse",
    trigger: { player: "damageEnd" },
    filter(event, player) {
      return game.hasPlayer((target) => {
        if (target.countCards("he") && target.countCards("h") >= player.countCards("h")) {
          if (target !== player && (event.name === "damage" || !player.getStorage("yyyaoming_used").includes("弃牌"))) {
            return true;
          }
        }
        if (target.countCards("h") <= player.countCards("h")) {
          if (event.name === "damage" || !player.getStorage("yyyaoming_used").includes("摸牌")) {
            return true;
          }
        }
        return false;
      });
    },
    filterTarget(card, player, target) {
      if (target !== player && target.countCards("he") && target.countCards("h") >= player.countCards("h") && !player.getStorage("yyyaoming_used").includes("弃牌")) {
        return true;
      }
      if (target.countCards("h") <= player.countCards("h") && !player.getStorage("yyyaoming_used").includes("摸牌")) {
        return true;
      }
      return false;
    },
    prompt() {
      const player = get.player(), storage = player.getStorage("yyyaoming_used");
      return ["弃牌", "摸牌"].filter((i) => !storage.includes(i)).map((i) => {
        return {
          弃牌: "弃置一名手牌数不小于你的其他角色的一张牌",
          摸牌: "令一名手牌数不大于你的角色摸一张牌"
        }[i];
      }).join("，或");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(
        get.prompt(event.skill),
        (card, player2, target) => {
          if (target !== player2 && target.countCards("he") && target.countCards("h") >= player2.countCards("h")) {
            return true;
          }
          if (target.countCards("h") <= player2.countCards("h")) {
            return true;
          }
          return false;
        },
        "弃置一名手牌数不小于你的其他角色的一张牌，或令一名手牌数不大于你的角色摸一张牌"
      ).set("ai", (target) => {
        const player2 = get.player();
        return get.effect(target, "yyyaoming", player2, player2);
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.target || event.targets[0];
      let choice = ["弃牌", "摸牌"].filter((choice2) => {
        if (!(trigger?.name === "damage" || !player.getStorage("yyyaoming_used").includes(choice2))) {
          return false;
        }
        if (choice2 === "弃牌") {
          return target !== player && target.countCards("he") && target.countCards("h") >= player.countCards("h");
        }
        return target.countCards("h") <= player.countCards("h");
      }), result;
      if (choice.length === 1) {
        result = { control: choice[0] };
      } else {
        result = await player.chooseControl(choice).set("choiceList", ["弃置" + get.translation(target) + "一张牌", "令" + get.translation(target) + "摸一张牌"]).set("prompt", "邀名：请选择一项").set("ai", () => {
          const player2 = get.player(), event2 = get.event().getParent(), target2 = event2.target || event2.targets[0];
          return get.effect(target2, { name: "guohe_copy2" }, player2, player2) > get.effect(target2, { name: "draw" }, player2, player2) ? 0 : 1;
        }).forResult();
      }
      if (result.control === "弃牌") {
        await player.discardPlayerCard(target, "he", true);
      } else {
        await target.draw();
      }
      if (!(trigger?.name === "damage")) {
        player.addTempSkill("yyyaoming_used", "phaseUseAfter");
        player.markAuto("yyyaoming_used", [result.control]);
      }
    },
    ai: {
      order: 7,
      result: {
        player(player, target) {
          let eff = [0, 0], hs = player.countCards("h"), ht = target.countCards("h");
          if (hs >= ht) {
            eff[0] = get.effect(target, { name: "draw" }, player, player);
          }
          if (hs <= ht) {
            eff[1] = get.effect(target, { name: "guohe_copy2" }, player, player);
          }
          return Math.max.apply(Math, eff);
        }
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      }
    }
  },
  //白虎骁骑
  yy_baimaxiaoqi_skill: {
    equipSkill: true,
    mod: {
      attackRange(player, num) {
        if (player.countVCards("e") > 0) {
          return num + player.countVCards("e");
        }
      },
      cardUsable(card, player, num) {
        if (card.name != "sha") {
          return;
        }
        if (player.countVCards("e") > 1) {
          return num + player.countVCards("e");
        }
      },
      globalFrom(player, target, num) {
        if (player.countVCards("e") > 2) {
          return num - player.countVCards("e");
        }
      }
    },
    trigger: { player: "phaseDrawBegin2" },
    filter(event, player) {
      return player.countVCards("e") > 3 && !event.numFixed;
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.num += player.countVCards("e");
    }
  },
  //麴义
  yyfuqi: {
    audio: "fuqi",
    trigger: { player: "useCardToPlayer" },
    filter(event, player) {
      return get.distance(player, event.target) > 1;
    },
    forced: true,
    logTarget: "target",
    async content(event, trigger, player) {
      await player.draw();
    },
    group: "yyfuqi_fuqi",
    subSkill: { fuqi: { audio: "fuqi", inherit: "fuqi" } }
  },
  //公孙瓒
  yyqizhen: {
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      return event.card.name == "sha";
    },
    check(event, player) {
      return get.attitude(player, event.target) < 0 || event.targets.some((i) => get.attitude(player, i) < 0);
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const skill = "yyqizhen_effect";
      player.addTempSkill(skill);
      if (!player.storage[skill]) {
        player.storage[skill] = {};
      }
      const id = trigger.target.playerid;
      if (!player.storage[skill][id]) {
        player.storage[skill][id] = [trigger.target, trigger.getParent()];
      } else {
        player.storage[skill][id].add(trigger.getParent());
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        trigger: { player: "useCardAfter" },
        filter(event, player) {
          return Object.keys(player.storage["yyqizhen_effect"]).some((id) => player.storage["yyqizhen_effect"][id].includes(event));
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          const storage = player.storage["yyqizhen_effect"];
          const targets = Object.keys(storage).filter((id) => {
            return storage[id].includes(trigger);
          }).slice().map((id) => storage[id][0]);
          const goon = game.getGlobalHistory("changeHp", (evt) => evt.getParent().name === "damage" && evt.getParent().card === trigger.card).reduce((sum, evt) => sum - evt.num, 0);
          for (const target of targets) {
            if (goon) {
              player.line(target);
              await player.draw(goon);
            } else {
              if (!target.isIn() || !target.countDiscardableCards(player, "e")) {
                continue;
              }
              player.line(target);
              await player.discardPlayerCard(target, "e", true);
            }
          }
        }
      }
    }
  },
  yymujun: {
    zhuSkill: true,
    limited: true,
    enable: "phaseUse",
    filterTarget(card, player, target) {
      return target.group == "qun" && player.hasZhuSkill("yymujun", target) && !target.hasSkill("yicong", null, false, false);
    },
    skillAnimation: true,
    animationColor: "metal",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      await event.target.addSkills("yicong");
    },
    ai: {
      order: 1,
      result: { target: 1 }
    },
    derivation: "yicong"
  },
  //文丑
  yyxuezhan: {
    trigger: { player: "useCard" },
    filter(event) {
      return event.card.name == "juedou";
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.nowuxie = true;
    },
    mod: {
      cardname(card, player) {
        if (get.type(card, "trick", false) == "trick") {
          return "juedou";
        }
      }
    }
  },
  yyyazhen: {
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      return player.countCards("e");
    },
    filterCard: true,
    position: "e",
    viewAs: { name: "sha" },
    check(card) {
      const val = get.value(card);
      if (get.event().name == "chooseToRespond") {
        return 1 / Math.max(0.1, val);
      }
      return 5 - val;
    },
    ai: {
      respondSha: true,
      skillTagFilter(player) {
        if (!player.countCards("e")) {
          return false;
        }
      }
    }
  },
  //公孙渊
  yyxuanshi: {
    enable: "phaseUse",
    filter(event, player) {
      return player.hasCards("h") && player.countCards("h", { color: "red" }) === player.countCards("h", { color: "black" });
    },
    filterTarget(card, player, target) {
      return target !== player && target.hasCards("hej");
    },
    usable: 2,
    delay: false,
    async content(event, trigger, player) {
      const target = event.target;
      await player.showHandcards(get.translation(player) + "对" + get.translation(target) + "发动了【旋势】");
      await player.gainPlayerCard({ target, position: "hej", forced: true });
    },
    ai: {
      order: 20,
      result: {
        player(player, target) {
          return get.effect(target, { name: "shunshou" }, player, player);
        }
      }
    }
  },
  yyxiongye: {
    zhuSkill: true,
    enable: "phaseUse",
    selectCard() {
      if (ui.selected.targets.length) {
        return [ui.selected.targets.length, Infinity];
      }
      return [1, Infinity];
    },
    selectTarget: () => ui.selected.cards.length,
    filterTarget(card, player, target) {
      return target !== player && target.group === "qun" && player.hasZhuSkill("yyxiongye", target);
    },
    filterCard: true,
    check(card) {
      if (get.tag(card, "recover")) {
        return 0;
      }
      return 7 - get.value(card);
    },
    position: "h",
    complexCard: true,
    discard: false,
    lose: false,
    delay: false,
    multitarget: true,
    multiline: true,
    usable: 1,
    async content(event, trigger, player) {
      await game.loseAsync({
        gain_list: Array.from({ length: event.targets.length }).map((_, i) => [event.targets[i], event.cards[i]]),
        giver: player,
        player,
        cards: event.cards,
        animate: "giveAuto"
      }).setContent("gaincardMultiple");
      for (const i of event.targets.sortBySeat()) {
        await i.damage();
      }
    },
    ai: {
      order: 5,
      result: {
        player(player, target) {
          return get.damageEffect(target, player, player);
        }
      }
    }
  },
  //袁绍
  yysudi: {
    trigger: { global: ["useCardAfter", "respondAfter"] },
    filter(event, player) {
      if (!Array.isArray(event.respondTo) || event.respondTo[0] !== player) {
        return false;
      }
      return event.player.inRange(player);
    },
    forced: true,
    logTarget: "player",
    async content(event, trigger, player) {
      await player.draw();
    }
  },
  yyqishe: {
    trigger: {
      global: "phaseBefore",
      player: ["enterGame", "phaseJieshuBegin"]
    },
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    frequent: true,
    async cost(event, trigger, player) {
      if (trigger.name !== "phaseJieshu") {
        event.result = { bool: true };
      } else {
        event.result = await player.chooseBool("是否发动【齐射】，从弃牌堆中获得一张【万箭齐发】？").set("frequentSkill", event.skill).forResult();
      }
    },
    async content(event, trigger, player) {
      const card = trigger.name === "phaseJieshu" ? get.discardPile("wanjian") : game.createCard2("wanjian", "heart", 1);
      if (card) {
        player.gain(card, "gain2");
      }
    }
  },
  yylinzhen: {
    locked: true,
    zhuSkill: true,
    global: "yylinzhen_global",
    subSkill: {
      global: {
        zhuSkill: true,
        mod: {
          inRange(from, to) {
            if (from === to || from.group !== "qun") {
              return;
            }
            if (to.hasZhuSkill("yylinzhen", from)) {
              return true;
            }
          }
        }
      }
    }
  },
  //司马懿
  yyyanggu: {
    enable: "chooseToUse",
    filter(event, player) {
      return player.storage.yyyanggu && player.countCards("h");
    },
    filterCard: true,
    position: "h",
    viewAs: { name: "shengdong" },
    prompt: "将一张手牌当作【声东击西】使用",
    check(card) {
      return 7 - get.value(card);
    },
    onuse(result, player) {
      player.changeZhuanhuanji("yyyanggu");
    },
    mark: true,
    zhuanhuanji: true,
    marktext: "☯",
    intro: {
      content(storage) {
        if (storage) {
          return "你可以将一张手牌当作【声东击西】使用";
        }
        return "当你受到伤害后，你可以回复1点体力";
      }
    },
    group: "yyyanggu_effect",
    subSkill: {
      effect: {
        trigger: { player: "damageEnd" },
        filter(event, player) {
          return !player.storage.yyyanggu && player.isDamaged();
        },
        check(event, player) {
          return get.recoverEffect(player, player, player) > 0;
        },
        prompt: "回复1点体力",
        async content(event, trigger, player) {
          player.changeZhuanhuanji("yyyanggu");
          player.recover();
        }
      }
    }
  },
  yyzuifu: {
    trigger: { global: ["gainAfter", "loseAsyncAfter"] },
    filter(event, player, name, target) {
      if (!event.getg || _status.dying.length) {
        return false;
      }
      return target?.isIn();
    },
    getIndex(event, player) {
      if (!event.getg) {
        return false;
      }
      return game.filterPlayer((current) => {
        const evt = event.getParent("phaseDraw");
        if (evt?.player == current) {
          return false;
        }
        return event.getg(current).length;
      }).sortBySeat();
    },
    usable: 1,
    logTarget: (event, player, name, target) => target,
    prompt2: (event, player, name, target) => "对" + get.translation(target) + "造成1点伤害",
    check: (event, player, name, target) => get.damageEffect(target, player, player) > 0,
    async content(event, trigger, player) {
      event.targets[0].damage();
    }
  },
  //曹叡
  yyhuituo: {
    audio: "huituo",
    trigger: { player: "damageEnd" },
    getIndex: (event) => event.num,
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill)).set("ai", (target) => {
        const player2 = get.player();
        if (get.attitude(player2, target) > 0) {
          return get.recoverEffect(target, player2, player2) + 1;
        }
        return 0;
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = await target.judge((card) => {
        if (get.color(card) == "red") {
          return target.isDamaged() ? 1 : -1;
        }
        return 0;
      }).forResult();
      if (result.color === "red") {
        await target.recover();
      }
      if (result.color === "black") {
        await target.draw();
      }
    }
  },
  yymingjian: {
    audio: "mingjian",
    trigger: { global: "phaseUseBegin" },
    filter(event, player) {
      return event.player !== player && player.countCards("h");
    },
    async cost(event, trigger, player) {
      const suits = player.getCards("h").slice().map((i) => get.suit(i, player)).unique();
      event.result = await player.chooseControl(suits, "cancel2").set("ai", () => {
        const player2 = get.player(), target = get.event().getTrigger().player;
        if (get.attitude(player2, target) < 2) {
          return "cancel2";
        }
        return get.event().controls.randomGet();
      }).set("prompt", get.prompt2(event.skill, trigger.player)).forResult();
      if (event.result.control !== "cancel2") {
        event.result.bool = true;
        event.result.cards = player.getCards("h", { suit: event.result.control });
      }
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = trigger.player;
      await player.showHandcards(get.translation(player) + "对" + get.translation(target) + "发动了【明鉴】");
      await player.give(event.cards, target, "visible");
      target.addTempSkill("yymingjian_effect");
      target.addMark("yymingjian_effect", 1, false);
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        intro: { content: "本回合使用的下一张牌额外结算#次" },
        trigger: { player: "useCard" },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          const num = player.countMark(event.name);
          player.removeSkill(event.name);
          if (lib.skill.dcshixian.filterx(trigger)) {
            trigger.effectCount += num;
            game.log(trigger.card, "额外结算" + num + "次");
          }
        },
        mod: {
          aiOrder(player, card, num) {
            if (typeof card == "object" && !get.tag(card, "norepeat")) {
              const type = get.type(card);
              if (type === "basic" || type === "trick") {
                return num + 20;
              }
            }
          }
        }
      }
    }
  }
};
const skills$d = {
  // 曹操
  jylijun: {
    trigger: { global: "damageEnd" },
    filter(event, player) {
      return event.player.isIn() && event.player.group === "wei";
    },
    check(event, player) {
      return get.effect(event.player, { name: "draw" }, player, player) > 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await trigger.player.draw();
    }
  },
  jytongbei: {
    trigger: { source: "damageBegin1" },
    filter(event, player) {
      return event.player.group !== "wei";
    },
    async cost(event, trigger, player) {
      const { player: target } = trigger;
      if (!target.countCards("he")) {
        event.result = await player.chooseBool(get.prompt2(event.skill, target)).set("choice", get.attitude(player, target) < 0).forResult();
      } else {
        const result = await player.chooseControl(lib.inpile.map((name) => get.type2(name)).unique(), "cancel2").set("prompt", get.prompt(event.skill, target)).set("ai", () => {
          const { player: player2, targetx, controls } = get.event();
          const att = get.attitude(player2, targetx);
          if (att > 0) {
            return "cancel2";
          }
          if (player2.hasSkillTag("viewHandcard", null, target, true)) {
            return controls.filter((type) => !targetx.countCards("he", { type })).randomGet();
          }
          const types = targetx.getCards("he", (card) => card.isKnownBy(player2)).map((name) => get.type2(name)).unique();
          if (types.length < controls.length) {
            return controls.removeArray(types).randomGet();
          }
          return controls.randomGet();
        }).set("targetx", target).forResult();
        event.result = {
          bool: result?.control !== "cancel2",
          cost_data: result?.control
        };
      }
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const { player: target } = trigger;
      const { cost_data } = event;
      if (cost_data) {
        player.popup(cost_data);
        game.log(player, "声明", `#g${get.translation(cost_data)}牌`);
      }
      let result;
      if (!cost_data || !target.countCards("he", { type: cost_data })) {
        result = { bool: false };
      } else {
        result = await target.chooseToGive(player, "he", { type: cost_data }, `交给${get.translation(player)}一张${get.translation(cost_data)}牌，否则此伤害+1`).set("ai", (card) => {
          const { player: player2, target: target2 } = get.event();
          get.attitude(player2, target2);
          const eff = get.damageEffect(player2, target2, player2);
          if (eff > 0) {
            return 0;
          }
          return 7 - get.value(card);
        }).forResult();
      }
      if (!result?.bool) {
        trigger.num++;
      }
    }
  },
  // 曹仁
  jybeirong: {
    enable: "phaseUse",
    usable: 1,
    filterCard: lib.filter.cardRecastable,
    selectCard: [1, Infinity],
    check(card) {
      return 6.5 - get.value(card);
    },
    discard: false,
    lose: false,
    delay: false,
    allowChooseAll: true,
    async content(event, trigger, player) {
      const { cards: cards2 } = event;
      const num = cards2.map((card) => get.suit(card)).toUniqued().length;
      await player.recast(cards2);
      if (num >= player.getHp()) {
        await player.link();
      }
    },
    ai: {
      order: 10,
      result: { player: 1 }
    }
  },
  jyyujun: {
    trigger: { global: "damageBegin4" },
    filter(event, player) {
      return event.player.isLinked() && event.hasNature();
    },
    check(event, player) {
      return get.damageEffect(event.player, event.source, player, event.nature) * event.num < get.effect(player, { name: "losehp" }, player, player) + get.effect(player, { name: "draw" }, player, player) * 3;
    },
    async content(event, trigger, player) {
      await player.turnOver();
      await player.loseHp();
      await player.draw(3);
      trigger.cancel();
    }
  },
  // 诸葛亮
  jyqibian: {
    trigger: { global: ["roundStart", "roundEnd"] },
    filter(event, player, name) {
      return name === "roundStart" || player.getExpansions("jyqibian").length;
    },
    forced: true,
    async content(event, trigger, player) {
      if (event.triggername === "roundEnd") {
        const cards2 = player.getExpansions(event.name);
        if (cards2.length) {
          await player.loseToDiscardpile(cards2);
        }
      } else {
        const next = player.addToExpansion(get.cards(7), "gain2");
        next.gaintag.add(event.name);
        await next;
      }
    },
    marktext: "才",
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    ai: { combo: "jycailve" }
  },
  jycailve: {
    hiddenCard(player, name) {
      return lib.inpile.includes(name) && player.getExpansions("jyqibian").some((card) => get.name(card) === name);
    },
    enable: "chooseToUse",
    onChooseToUse(event) {
      if (!game.online && !event.jycailve_cards) {
        event.set("jycailve_cards", event.player.getExpansions("jyqibian"));
      }
    },
    filter(event, player) {
      if (!Array.isArray(event.jycailve_cards) || event.responded || event.jycailve) {
        return false;
      }
      return player.getExpansions("jyqibian").some((card) => event.filterCard(card, player, event));
    },
    chooseButton: {
      dialog(event, player) {
        return ui.create.dialog("才辩", player.getExpansions("jyqibian"), "hidden");
      },
      filter(button, player) {
        const evt = get.event().getParent();
        return evt.filterCard(button.link, player, evt);
      },
      check(button) {
        const { link } = button;
        const player = get.player();
        if (get.event().getParent().type !== "phase") {
          return 1;
        }
        return player.getUseValue(link);
      },
      backup(links, player) {
        return {
          // filterCard: () => false,
          filterCard(card) {
            return card === lib.skill.jycailve_backup.card;
          },
          selectCard: -1,
          viewAs: links[0],
          card: links[0],
          position: "x",
          async precontent(event, trigger, player2) {
            player2.addTempSkill("jycailve_effect");
          }
        };
      },
      prompt(links, player) {
        return `才辩：是否使用${get.translation(links[0])}？`;
      }
    },
    ai: {
      combo: "jyqibian",
      order: 8,
      effect: {
        target(card, player, target, effect) {
          if (get.tag(card, "respondShan")) {
            return 0.7;
          }
          if (get.tag(card, "respondSha")) {
            return 0.7;
          }
        }
      },
      respondShan: true,
      respondSha: true,
      result: {
        player(player) {
          return get.event().dying ? get.attitude(player, get.event().dying) : 1;
        }
      }
    },
    subSkill: {
      backup: {},
      effect: {
        charlotte: true,
        trigger: { global: "useCardToBegin" },
        filter(event, player) {
          const { target } = event;
          if (!target?.isIn()) {
            return false;
          }
          return event.skill === "jycailve_backup" && player.countDiscardableCards(target, "he");
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          const { target } = trigger;
          if (player.countDiscardableCards(target, "he")) {
            await target.discardPlayerCard(player, "he");
          }
        }
      }
    }
  },
  // 庞统
  jylianhuan: {
    enable: "phaseUse",
    usable: 1,
    viewAs: { name: "tiesuo", isCard: true },
    filterCard: () => false,
    selectCard: -1,
    log: false,
    async precontent(event, trigger, player) {
      player.logSkill("jylianhuan");
      await player.loseHp();
    },
    ai: {
      order: 10,
      result: {
        target(player, target) {
          if (player.getHp() < 2) {
            return 0;
          }
          const att = get.attitude(player, target);
          if (player.hasSkill("jyyuhuo")) {
            return !target.isLinked() && att > 0 && player !== target ? 1 : -1;
          }
          return lib.card.tiesuo.ai.result.target(player, target);
        }
      }
    }
  },
  jysuozhou: {
    trigger: { player: "useCard", target: "useCardToTarget" },
    filter(event, player) {
      return get.suit(event.card) === "club" && game.hasPlayer((current) => current.isLinked());
    },
    usable: 1,
    logTarget: () => game.filterPlayer((current) => current.isLinked()).sortBySeat(),
    check(event, player) {
      return get.info("jysuozhou").logTarget().reduce((sum, current) => sum + get.effect(current, { name: "draw" }, player, player), 0) > 0;
    },
    async content(event, trigger, player) {
      await game.asyncDraw(event.targets);
    }
  },
  jyyuhuo: {
    trigger: { global: "damageBegin4" },
    filter(event, player) {
      return event.player !== player && event.player.isLinked();
    },
    forced: true,
    async content(event, trigger, player) {
      if (trigger.hasNature()) {
        trigger.num++;
      } else {
        trigger.num--;
      }
    }
  },
  // 鲁肃
  jydimeng: {
    getList(event) {
      const list = [event.player];
      if (event.targets?.length) {
        list.addArray(event.targets);
      } else {
        list.add(event.target);
      }
      return list.sortBySeat();
    },
    trigger: { global: "compareCardShowBefore" },
    filter(event, player) {
      return get.info("jydimeng").getList(event).length > 1;
    },
    async cost(event, trigger, player) {
      const list = get.info(event.skill).getList(trigger);
      event.result = await player.chooseTarget(2, get.prompt2(event.skill), (card, player2, target) => get.event().list.includes(target)).set("ai", (target) => {
        const player2 = get.player();
        const evt = get.event().getTrigger();
        const { player: source, small, lose_list } = evt;
        const att1 = get.attitude(player2, source);
        const att2 = get.attitude(player2, target);
        const [card] = lose_list.find((i) => i[0] === target)[1];
        if (!ui.selected.targets.length) {
          if (target === source) {
            if (player2 === source && get.number(evt.card1) <= 7 && !small || att1 <= 0 || small && get.number(evt.card1) > 6) {
              return 10;
            }
          }
          if (att2 > 0 && (get.number(card) <= 7 && !small || small && get.number(card) > 6)) {
            return 10;
          }
        }
        return -att2 * (small ? 14 - get.number(card) : get.number(card));
      }).set("list", list).forResult();
    },
    async content(event, trigger, player) {
      const { targets } = event;
      if (trigger.targets?.length) {
        const { cardlist, lose_list } = trigger;
        const [card1] = lose_list.find((i) => i[0] === targets[0])[1];
        const [card2] = lose_list.find((i) => i[0] === targets[1])[1];
        const index1 = cardlist.indexOf(card1);
        const index2 = cardlist.indexOf(card2);
        if (index1 >= 0 && index2 >= 0) {
          [cardlist[index1], cardlist[index2]] = [cardlist[index2], cardlist[index1]];
        } else {
          trigger.card1 = index1 < 0 ? card2 : card1;
          cardlist[index1 < 0 ? index2 : index1] = index1 < 0 ? card1 : card2;
        }
      } else {
        const list = [trigger.card1, trigger.card2];
        trigger.card1 = list[1];
        trigger.card2 = list[0];
      }
    }
  },
  jyzhouji: {
    enable: "phaseUse",
    usable(skill, player) {
      return player.getHp();
    },
    filter(event, player) {
      return game.hasPlayer((target) => get.info("jyzhouji").filterTarget(null, player, target));
    },
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    async content(event, trigger, player) {
      const { target } = event;
      const result = await player.chooseToCompare(target).set("small", get.attitude(player, target) > 0).forResult();
      if (target === result?.winner) {
        await target.draw(2);
      }
    },
    ai: {
      order: 10,
      expose: 0.2,
      result: {
        target(player, target) {
          let maxnum = 0;
          const cards2 = target.getCards("h");
          for (const card of cards2) {
            if (get.number(card) > maxnum) {
              maxnum = get.number(card);
            }
          }
          if (maxnum > 10) {
            maxnum = 10;
          }
          if (maxnum < 5 && cards2.length > 1) {
            maxnum = 5;
          }
          const cards3 = player.getCards("h");
          for (const card of cards3) {
            if (get.number(card) < maxnum) {
              return 1;
            }
          }
          return 0;
        }
      }
    }
  },
  // 张昭
  jyboyan: {
    enable: "phaseUse",
    usable: 2,
    filter(event, player) {
      return game.hasPlayer((target) => get.info("jyboyan").filterTarget(null, player, target));
    },
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    async content(event, trigger, player) {
      const { target } = event;
      const result = await player.chooseToCompare(target).forResult();
      const list = [player, target];
      if (result?.winner) {
        list.remove(result.winner);
      }
      for (const loser of list) {
        loser.addTempSkill(`${event.name}_effect`);
        await loser.draw(2);
      }
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          const hs = player.getCards("h").sort((a, b) => get.number(b) - get.number(a));
          const ts = target.getCards("h").sort((a, b) => get.number(b) - get.number(a));
          if (!hs.length || !ts.length) {
            return 0;
          }
          if (get.number(hs[0]) > get.number(ts[0]) || get.number(hs[0]) - ts.length >= 9 + Math.min(2, player.hp / 2)) {
            return get.sgnAttitude(player, target) * get.damageEffect(target, player, player);
          }
          return 0;
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        mod: {
          cardEnabled(card, player) {
            if ([card].concat(card.cards || []).some((cardx) => get.itemtype(cardx) === "card" && get.position(cardx) === "h")) {
              return false;
            }
          },
          cardSavable(card, player) {
            if ([card].concat(card.cards || []).some((cardx) => get.itemtype(cardx) === "card" && get.position(cardx) === "h")) {
              return false;
            }
          }
        },
        mark: true,
        intro: { content: "本回合不能使用手牌" }
      }
    }
  },
  jymushi: {
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      const targets = get.info("jymushi").logTarget(event, player);
      return targets.length && targets.every((current) => current.countCards("h") >= current.getHp());
    },
    async cost(event, trigger, player) {
      const targets = get.info(event.skill).logTarget(trigger, player);
      const choices = [];
      const choiceList = [`获得${get.translation(targets)}${targets.length > 1 ? "各" : ""}一张牌`, `令${get.translation(targets)}将手牌数调整至其体力值`];
      if (targets.some((current) => current.countGainableCards(player, "he"))) {
        choices.push("选项一");
      } else {
        choiceList[0] = `<span style="opacity:0.5">${choiceList[0]}</span>`;
      }
      if (targets.some((current) => current.countCards("h") !== current.getHp())) {
        choices.push("选项二");
      } else {
        choiceList[1] = `<span style="opacity:0.5">${choiceList[1]}</span>`;
      }
      const result = await player.chooseControl(choices, "cancel2").set("choiceList", choiceList).set("prompt", get.prompt(event.skill)).set("ai", () => {
        const { player: player2, targets: targets2, controls } = get.event();
        const eff1 = targets2.reduce((sum, current) => sum + get.effect(current, { name: "shunshou_copy2" }, player2, player2), 0);
        const eff2 = targets2.reduce((sum, current) => sum + 2 * get.effect(current, { name: "guohe_copy", position: "h" }, player2, player2) * (current.countCards("h") - current.getHp()), 0);
        if (eff1 <= 0 && eff2 <= 0) {
          return "cancel2";
        }
        if (controls.includes("选项一") && eff1 > 0 && eff1 >= eff2) {
          return "选项一";
        }
        if (controls.includes("选项二") && eff2 > 0) {
          return "选项二";
        }
        return "cancel2";
      }).set("targets", targets).forResult();
      event.result = {
        bool: result?.control !== "cancel2",
        cost_data: result?.control
      };
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => current !== player).sortBySeat();
    },
    async content(event, trigger, player) {
      const { cost_data, targets } = event;
      for (const target of targets) {
        if (!target.isIn()) {
          continue;
        }
        if (cost_data === "选项一" && target.countDiscardableCards(player, "he")) {
          await player.gainPlayerCard(target, "he", true);
        } else if (cost_data === "选项二" && target.countCards("h") !== target.getHp()) {
          const num = target.getHp() - target.countCards("h");
          if (num > 0) {
            await target.draw(num);
          } else if (target.countDiscardableCards(target, "h")) {
            await target.chooseToDiscard(target.countCards("h") - target.getHp(), true, "allowChooseAll");
          }
        }
      }
    }
  },
  // 周瑜
  jysashuang: {
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return get.discarded().someInD("d");
    },
    async cost(event, trigger, player) {
      const cards2 = get.discarded().filterInD("d");
      const result = await player.chooseButton([`飒爽：获得其中每种颜色的牌的各一张`, cards2], cards2.map((card) => get.color(card)).toUniqued().length).set("filterButton", (button) => {
        const { link } = button;
        return !ui.selected.buttons.reduce((list, card) => list.add(get.color(card.link)), []).includes(get.color(link));
      }).set("ai", (button) => get.value(button.link)).forResult();
      event.result = {
        bool: result?.bool,
        cost_data: result?.links
      };
    },
    async content(event, trigger, player) {
      await player.gain(event.cost_data, "gain2");
    }
  },
  jyhuoce: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.hasCard((card) => lib.filter.cardDiscardable(card, player, "jyhuoce"), "he") && game.hasPlayer((current) => get.info("jyhuoce").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return target !== player && target.countCards("he");
    },
    async content(event, trigger, player) {
      const { target } = event;
      const list = [player, target].sortBySeat();
      if (list.some((current) => !current.hasCard((card) => lib.filter.cardDiscardable(card, current, "jyhuoce"), "he"))) {
        return;
      }
      const result = await player.chooseCardOL(list, "he", true, "火策：选择弃置一张牌", (card, player2) => lib.filter.cardDiscardable(card, player2, "jyhuoce")).set("ai", get.unuseful).forResult();
      if (!result) {
        return;
      }
      const lose_list = [];
      const cards2 = [];
      for (const [index, currentResult] of result.entries()) {
        const current = list[index];
        const card = currentResult.cards[0];
        lose_list.push([current, currentResult.cards]);
        cards2.push(card);
      }
      if (lose_list.length) {
        await game.loseAsync({
          lose_list
        }).setContent("discardMultiple");
      }
      if (cards2.map((card) => get.color(card)).toUniqued().length === 1) {
        const result2 = await player.chooseTarget(true, `选择一名角色对其造成1点火焰伤害`).set("ai", (target2) => {
          const player2 = get.player();
          return get.damageEffect(target2, player2, player2, "fire");
        }).forResult();
        if (result2?.bool && result2?.targets?.length) {
          await result2.targets[0].damage("fire", player);
        }
      }
    },
    ai: {
      threaten: 1.2,
      order: 9.1,
      result: {
        target(player, target) {
          if (target.hasCard((card) => lib.filter.cardDiscardable(card, player, "jyhuoce"), "he")) {
            return -1;
          }
          return 0;
        }
      }
    }
  },
  // 黄盖
  jyliezhou: {
    trigger: { source: ["damageBegin1", "damageSource"] },
    filter(event, player, name) {
      return name === "damageBegin1" || event.checkJyliezhou && event.num > 0;
    },
    forced: true,
    async content(event, trigger, player) {
      if (event.triggername === "damageBegin1") {
        game.setNature(trigger, "fire");
      } else {
        await player.draw(trigger.num);
      }
    }
  },
  jyzhaxiang: {
    enable: "phaseUse",
    usable: 1,
    async content(event, trigger, player) {
      await player.loseMaxHp();
      player.addTempSkill(`${event.name}_effect`);
    },
    ai: {
      order: 8,
      result: {
        player(player) {
          if (!player.hasCard((card) => get.tag(card, "damage") && get.type(card) !== "delay" && player.hasValueTarget(card), "hs") || player.hasSkill("jyzhaxiang_effect") || player.maxHp <= 3) {
            return 0;
          }
          return player.isHealthy() ? 0 : 1;
        }
      },
      halfneg: true
    },
    subSkill: {
      effect: {
        charlotte: true,
        trigger: { player: "useCard" },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          trigger.directHit.addArray(game.filterPlayer());
        },
        ai: {
          threaten: 1.5,
          directHit_ai: true
        }
      }
    }
  }
};
const skills$c = {
  // 十常侍共用技能
  pschangshi: {
    initSkill(changshi, skill) {
      if (!lib.skill[skill]) {
        lib.skill[skill] = {
          charlotte: true,
          onremove: true,
          mark: true,
          intro: {
            name: `常侍（${get.translation(changshi)}）`,
            name2: "常侍",
            content: "mark"
          }
        };
        lib.translate[skill] = "常侍";
        lib.translate[skill + "_bg"] = "侍";
      }
    },
    changshi: ["ps_zhangrang", "ps_zhaozhong", "ps_sunzhang", "ps_bilan", "ps_xiayun", "ps_hankui", "ps_lisong", "ps_duangui", "ps_guosheng", "ps_gaowang"],
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    forced: true,
    locked: false,
    filter(event, player) {
      return event.name != "phase" || game.phaseNumber == 0;
    },
    async content(event, trigger, player) {
      const changshi = get.info(event.name).changshi.randomGet();
      const skill = `${changshi}_${player.playerid}`;
      game.broadcastAll(get.info(event.name).initSkill, changshi, skill);
      player.addSkill([skill, `${event.name}_effect`]);
      player.addMark(skill, 1);
      game.broadcastAll(
        (player2, changshi2, skill2) => {
          if (player2.marks[skill2]) {
            player2.marks[skill2].setBackground(changshi2, "character");
          }
        },
        player,
        changshi,
        skill
      );
    },
    group: "pschangshi_remove",
    subSkill: {
      remove: {
        trigger: { player: "removeMark" },
        forced: true,
        locked: false,
        filter(event, player) {
          return get.info("pschangshi").changshi.some((name) => event.markName == `${name}_${player.playerid}`);
        },
        async content(event, trigger, player) {
          await player.loseMaxHp();
        }
      },
      effect: {
        charlotte: true,
        trigger: {
          player: "damageBegin4",
          global: "phaseDiscardBegin"
        },
        filter(event, player) {
          if (get.info("pschangshi").changshi.every((name) => !player.hasMark(`${name}_${player.playerid}`))) {
            return false;
          }
          if (event.name == "phaseDiscard") {
            return get.info("jsrgzhenglve").isFirst(event.player);
          }
          if (event.player.hp + event.player.hujia > event.num) {
            return false;
          }
          return game.hasPlayer((current) => {
            return current != player && get.info("pschangshi").changshi.some((name) => current.hasMark(`${name}_${current.playerid}`));
          });
        },
        async cost(event, trigger, player) {
          if (trigger.name == "phaseDiscard") {
            const { player: target } = trigger;
            const result = await player.chooseBool(get.prompt(event.skill, target), `摸一张牌，令其本局游戏手牌上限+1`).set("choice", get.attitude(player, target) > 0).forResult();
            event.result = {
              bool: result?.bool,
              targets: [target]
            };
          } else {
            event.result = await player.chooseTarget(
              get.prompt(event.skill),
              (card, player2, target) => {
                return target != player2 && get.info("pschangshi").changshi.some((name) => target.hasMark(`${name}_${target.playerid}`));
              },
              `选择一名角色转移伤害`
            ).set("ai", (target) => {
              const player2 = get.player();
              return -get.attitude(player2, target);
            }).forResult();
          }
        },
        async content(event, trigger, player) {
          const {
            targets: [target]
          } = event;
          if (trigger.name == "phaseDiscard") {
            await player.draw();
            target.addSkill("pschangshi_hand");
            target.addMark("pschangshi_hand", 1, false);
          } else {
            trigger.cancel();
            await game.delay(0.5);
            const marks = get.info("pschangshi").changshi.filter((name) => player.hasMark(`${name}_${player.playerid}`));
            if (marks.length == 1) {
              const mark = `${marks[0]}_${player.playerid}`;
              player.removeMark(mark, 1);
              if (!player.hasMark(mark)) {
                player.removeSkill(mark);
              }
            } else if (marks.length) {
              const result = await player.chooseButton(["选择一个常侍标记移去", [marks, "character"]], true).forResult();
              if (result?.bool && result?.links?.length) {
                const mark = `${result.links[0]}_${player.playerid}`;
                player.removeMark(mark, 1);
                if (!player.hasMark(mark)) {
                  player.removeSkill(mark);
                }
              }
            }
            if (get.info("pschangshi").changshi.every((name) => !player.hasMark(`${name}_${player.playerid}`))) {
              player.removeSkill(event.name);
            }
            await target.damage(trigger.source ? trigger.source : "nosource", trigger.nature, trigger.num).set("card", trigger.card).set("cards", trigger.cards);
          }
        }
      },
      hand: {
        charlotte: true,
        onremove: true,
        markimage: "image/card/handcard.png",
        intro: { content: "手牌上限+#" },
        mod: {
          maxHandcard(player, num) {
            return num + player.countMark("pschangshi_hand");
          }
        }
      }
    }
  },
  // 张让
  pstaoluan: {
    audio: "scstaoluan",
    enable: "phaseUse",
    usable(skill, player) {
      return game.filterPlayer().reduce((num, current) => {
        const count = get.info("pschangshi").changshi.reduce((sum, name) => sum + current.countMark(`${name}_${current.playerid}`), 0);
        return num + count;
      }, 0);
    },
    filter(event, player) {
      if (!player.countCards("hes")) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (!["basic", "trick"].includes(info[0]) || player.getStorage("pstaoluan_record").includes(info[2])) {
          return false;
        }
        return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player, event);
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const vcards = get.inpileVCardList((info) => {
          if (!["basic", "trick"].includes(info[0]) || player.getStorage("pstaoluan_record").includes(info[2])) {
            return false;
          }
          return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player, event);
        });
        return ui.create.dialog("滔乱", [vcards, "vcard"]);
      },
      check(button) {
        return get.player().getUseValue({ name: button.link[2], nature: button.link[3] });
      },
      backup(links, player) {
        return {
          audio: "pstaoluan",
          popname: true,
          viewAs: { name: links[0][2], nature: links[0][3] },
          filterCard: true,
          position: "hes",
          async precontent(event, trigger, player2) {
            player2.addTempSkill("pstaoluan_record");
            player2.markAuto("pstaoluan_record", [event.result.card.name]);
          }
        };
      },
      prompt(links, player) {
        return "将一张牌当" + (get.translation(links[0][3]) || "") + get.translation(links[0][2]) + "使用";
      }
    },
    ai: {
      combo: "pschangshi",
      order: 7,
      result: { player: 1 },
      threaten: 1.9
    },
    subSkill: {
      record: {
        charlotte: true,
        onremove: true,
        intro: { content: "已记录牌名：$" }
      }
    }
  },
  // 赵忠
  pschiyan: {
    audio: "scschiyan",
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      return event.card.name == "sha" && event.target.countCards("he") && player.countCards("he");
    },
    logTarget: "target",
    check(event, player) {
      return get.attitude(player, event.target) <= 0;
    },
    async content(event, trigger, player) {
      for (const target2 of [player, trigger.target].sortBySeat()) {
        if (!target2.isIn() || !target2.countCards("he")) {
          continue;
        }
        const result = await target2.chooseCard("鸱咽：将任意张牌置于武将牌上直到回合结束", [1, Infinity], true, "he", "allowChooseAll").set("ai", (card) => {
          get.player();
          if (ui.selected.cards.length) {
            return 0;
          }
          return 6 - get.value(card);
        }).forResult();
        if (result?.bool && result?.cards?.length) {
          target2.addSkill(event.name + "_gain");
          const next = target2.addToExpansion("giveAuto", result.cards, target2);
          next.gaintag.add(event.name + "_gain");
          await next;
        }
      }
      const { target } = trigger;
      if (target.countCards("h") <= player.countCards("h")) {
        target.addTempSkill(event.name + "_damage");
      }
      if (target.countCards("h") >= player.countCards("h")) {
        target.addTempSkill(event.name + "_effect");
      }
    },
    subSkill: {
      gain: {
        trigger: { global: "phaseEnd" },
        forced: true,
        popup: false,
        charlotte: true,
        filter(event, player) {
          return player.countExpansions("pschiyan_gain");
        },
        async content(event, trigger, player) {
          const cards2 = player.getExpansions(event.name);
          await player.gain(cards2, "draw");
          game.log(player, "收回了" + get.cnNumber(cards2.length) + "张“鸱咽”牌");
          player.removeSkill(event.name);
        },
        intro: {
          markcount: "expansion",
          mark(dialog, storage, player) {
            var cards2 = player.getExpansions("pschiyan_gain");
            if (player.isUnderControl(true)) {
              dialog.addAuto(cards2);
            } else {
              return "共有" + get.cnNumber(cards2.length) + "张牌";
            }
          }
        }
      },
      damage: {
        charlotte: true,
        trigger: { player: "damageBegin3" },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          trigger.num++;
        },
        mark: true,
        intro: { content: "本回合受到的伤害+1" }
      },
      effect: {
        charlotte: true,
        mod: {
          cardEnabled(card, player) {
            const hs = player.getCards("h");
            if ([card].concat(card.cards || []).containsSome(...hs)) {
              return false;
            }
          },
          cardSavable(card, player) {
            return lib.skill.pschiyan_effect.mod.cardEnabled.apply(this, arguments);
          }
        },
        mark: true,
        intro: { content: "本回合不能使用手牌" }
      }
    }
  },
  // 孙璋
  pszimou: {
    audio: "scszimou",
    trigger: { player: "phaseUseBegin" },
    forced: true,
    logTarget: () => game.filterPlayer().sortBySeat(),
    async content(event, trigger, player) {
      for (const target of event.targets) {
        if (!target.isIn()) {
          continue;
        }
        if (target != player) {
          const result = !target.countCards("he") ? { bool: false } : await target.chooseToGive(player, "he", `交给${get.translation(player)}一张牌，或弃置其一张牌并受到其造成的1点伤害`).set("ai", (card) => {
            const { player: player2, target: target2 } = get.event();
            if (get.damageEffect(player2, target2, player2) + get.effect(target2, { name: "guohe_copy2" }, player2, player2) > 0) {
              return 0;
            }
            return 6 - get.value(card);
          }).forResult();
          if (!result?.bool) {
            if (player.countDiscardableCards(target, "he")) {
              await target.discardPlayerCard(player, "he", true);
              await target.damage();
            }
          }
        } else if (player.countDiscardableCards(player, "he")) {
          await player.chooseToDiscard("he", true);
          await player.damage();
        }
      }
    }
  },
  // 毕岚
  psbicai: {
    audio: "scspicai",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.countPlayer((current) => current.countCards("h")) >= player.getHp() && player.getHp() > 0;
    },
    filterTarget(card, player, target) {
      return target.countCards("h");
    },
    selectTarget() {
      return get.player().getHp();
    },
    multitarget: true,
    multiline: true,
    async content(event, trigger, player) {
      const num = event.targets.length;
      const list = [];
      for (const target of event.targets.sortBySeat()) {
        if (target.isIn() && target.countCards("h")) {
          const result = await target.chooseCard("选择一张手牌置于牌堆顶", "h", true).forResult();
          if (result?.bool && result?.cards?.length) {
            list.push(target);
            await target.lose(result.cards, ui.cardPile, "insert");
            game.broadcastAll((player2) => {
              const cardx = ui.create.card();
              cardx.classList.add("infohidden");
              cardx.classList.add("infoflip");
              player2.$throw(cardx, 1e3, "nobroadcast");
            }, target);
          }
          if (player == game.me) {
            await game.delay(0.5);
          }
        }
      }
      let cards2 = get.cards(num);
      await game.cardsGotoOrdering(cards2);
      await player.showCards(cards2, get.translation(player) + `发动了【${get.translation(event.name)}】`);
      const draw = cards2.map((card) => get.type2(card)).toUniqued().length;
      await player.draw(draw);
      if (draw == 3 && cards2.someInD()) {
        cards2 = cards2.filterInD();
        for (const target of list.sortBySeat()) {
          if (!target.isIn()) {
            continue;
          }
          const result = cards2.length == 1 ? { bool: true, links: cards2 } : await target.chooseButton([`${get.translation(event.name)}：获得其中一张牌`, cards2], true).forResult();
          if (result?.bool && result?.links?.length) {
            const { links } = result;
            await target.gain(links, "gain2");
            cards2.remove(links[0]);
          }
        }
      }
    },
    ai: {
      order: 10,
      result: { player: 1 }
    }
  },
  // 夏恽
  psyaozhuo: {
    audio: "scsyaozhuo",
    enable: "phaseUse",
    trigger: { player: "damageEnd" },
    filter(event, player) {
      if (!game.hasPlayer((current) => player.canCompare(current))) {
        return false;
      }
      return event.name == "damage" || !player.hasSkill("psyaozhuo_used", null, null, false);
    },
    filterTarget(card, player, target) {
      return player.canCompare(target);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => {
        return player2.canCompare(target);
      }).set("ai", (target) => {
        const player2 = get.player();
        return get.effect(target, "psyaozhuo", player2, player2);
      }).forResult();
    },
    async content(event, trigger, player) {
      if (!trigger) {
        player.addTempSkill(event.name + "_used", "phaseUseAfter");
      }
      const {
        targets: [target]
      } = event;
      const result = await player.chooseToCompare(target).forResult();
      if (result?.bool) {
        target.addTempSkill(event.name + "_effect");
        target.addMark(event.name + "_effect", 2, false);
      } else {
        await player.recover();
      }
    },
    ai: {
      order(item, player) {
        if (player.isDamaged()) {
          return 10;
        }
        return 1;
      },
      result: {
        target(player, target) {
          var hs = player.getCards("h").sort((a, b) => b.number - a.number);
          var ts = target.getCards("h").sort((a, b) => b.number - a.number);
          if (!hs.length || !ts.length) {
            return 0;
          }
          if (hs[0].number > ts[0].number - 2 && hs[0].number > 5 || player.isDamaged()) {
            return -1;
          }
          return 0;
        }
      }
    },
    group: "psyaozhuo_gain",
    subSkill: {
      used: { charlotte: true },
      effect: {
        // charlotte: true,
        charlotte: true,
        onremove: true,
        markimage: "image/card/handcard.png",
        intro: { content: "本回合手牌上限-#" },
        mod: {
          maxHandcard(player, num) {
            return num - player.countMark("psyaozhuo_effect");
          }
        }
      },
      gain: {
        audio: "scspsyaozhuo",
        getCards(event, player) {
          if (event.compareMultiple) {
            return [];
          }
          if (event.compareMeanwhile) {
            const index = [...event.targets, event.player].indexOf(player);
            event.winner || event.result.winner;
            if (index < 0) {
              return [];
            }
            return event.cards.filter((card, i) => {
              return i !== index;
            }).filterInD("od");
          }
          if (player != event.player && player != event.target) {
            return [];
          }
          const bool = player == event.player;
          return [event[bool ? "card2" : "card1"]].filterInD("od");
        },
        trigger: {
          global: ["chooseToCompareAfter", "compareMultipleAfter"]
        },
        filter(event, player) {
          const cards2 = get.info("psyaozhuo_gain").getCards(event, player);
          return cards2.length;
        },
        check(event, player) {
          const cards2 = get.info("psyaozhuo_gain").getCards(event, player);
          return cards2.every((card) => card.name != "du");
        },
        prompt2(event, player) {
          const cards2 = get.info("psyaozhuo_gain").getCards(event, player);
          return `获得${get.translation(cards2)}`;
        },
        async content(event, trigger, player) {
          const cards2 = get.info(event.name).getCards(trigger, player);
          await player.gain(cards2, "gain2", "log");
        }
      }
    }
  },
  // 韩悝
  psxiaolu: {
    audio: "scsxiaolu",
    global: "psxiaolu_give",
    subSkill: {
      give: {
        enable: "phaseUse",
        prompt() {
          const player = get.player();
          const list = game.filterPlayer((current) => current != player && current.hasSkill("psxiaolu"));
          let str = "将一张牌交给" + get.translation(list);
          if (list.length > 1) {
            str += "中的一人";
          }
          return str;
        },
        filter(event, player) {
          if (!player.countCards("he") || player.hasSkill("psxiaolu_used", null, null, false)) {
            return false;
          }
          return game.hasPlayer((current) => current != player && current.hasSkill("psxiaolu"));
        },
        filterTarget(card, player, target) {
          return target != player && target.hasSkill("psxiaolu");
        },
        selectTarget() {
          const player = get.player();
          const count = game.countPlayer((current) => current != player && current.hasSkill("psxiaolu"));
          return count > 1 ? 1 : -1;
        },
        chessForceAll: true,
        check(card) {
          const player = get.player();
          const hasFriend = game.hasPlayer((current) => {
            return current != player && current.hasSkill("psxiaolu") && get.attitude(player, current) > 0;
          });
          return (hasFriend ? 7 : 1) - get.value(card);
        },
        filterCard: true,
        position: "he",
        discard: false,
        lose: false,
        delay: false,
        line: true,
        log: false,
        async content(event, trigger, player) {
          const { target } = event;
          player.logSkill("psxiaolu", target);
          player.addTempSkill("psxiaolu_used", "phaseUseEnd");
          await player.give(event.cards, target);
          const targets = game.filterPlayer((current) => current != player && current != target);
          if (!targets.length) {
            return;
          }
          const result = targets.length == 1 ? { bool: true, targets } : await player.chooseTarget("请选择你要使用牌的目标", true, (card, player2, target2) => {
            return get.event().targetsx.includes(target2);
          }).set("ai", (target2) => {
            const player2 = get.player();
            return Math.max.apply(
              Math,
              lib.inpile.filter((name) => get.type(name) == "trick").map((name) => {
                const card = { name, isCard: true };
                if (!player2.canUse(card, target2, false)) {
                  return 0;
                }
                return get.effect(target2, card, player2, player2);
              })
            );
          }).set("targetsx", targets).forResult();
          if (result?.bool && result?.targets?.length) {
            const [target1] = result.targets;
            player.line(target1);
            game.log(player, "选择了", target1);
            const list = get.inpileVCardList((info) => {
              return info[0] == "trick" && player.canUse({ name: info[2], nature: info[3], isCard: true }, target1, false);
            });
            if (list.length) {
              const result2 = list.length == 1 ? { bool: true, links: list } : await player.chooseButton([`请选择你要${get.translation(target1)}使用的牌`, [list, "vcard"]], true).set("ai", (button) => {
                const { player: player2, target1: target12 } = get.event();
                return get.effect(target12, { name: button.link[2], nature: button.link[3], isCard: true }, player2, player2);
              }).set("target1", target1).forResult();
              if (result2?.bool && result2?.links?.length) {
                const card = get.autoViewAs({ name: result2.links[0][2], isCard: true, nature: result2.links[0][3] });
                await player.useCard(card, false, target1);
              }
            }
          }
        },
        ai: {
          expose: 0.3,
          order: 10,
          result: { target: 5 }
        }
      },
      used: { charlotte: true }
    }
  },
  // 栗嵩
  pskuiji: {
    audio: "scskuiji",
    inherit: "scskuiji",
    async content(event, trigger, player) {
      const { target } = event;
      if (!target.countCards("h")) {
        return;
      }
      event.list1 = [];
      event.list2 = [];
      const dialog = [];
      if (player.countCards("h")) {
        dialog.addArray(["你的手牌", player.getCards("h")]);
      }
      if (target.countCards("h")) {
        dialog.addArray([get.translation(target) + "的手牌", target.getCards("h")]);
      }
      const next = player.chooseButton(4, dialog);
      next.set("target", target);
      next.set("ai", (button) => {
        const { player: player2, target: target2 } = get.event();
        for (let i = 0; i < ui.selected.buttons.length; i++) {
          const card2 = ui.selected.buttons[i].link;
          if (target2.getCards("h").includes(card2)) ;
        }
        const card = button.link;
        const owner = get.owner(card);
        const val = get.value(card) || 1;
        if (owner == target2) {
          return 2 * val;
        }
        return 7 - val;
      });
      next.set("filterButton", (button) => {
        if (get.owner(button.link) && !lib.filter.canBeDiscarded(button.link, get.owner(button.link), get.player())) {
          return false;
        }
        for (let i = 0; i < ui.selected.buttons.length; i++) {
          if (get.suit(button.link) == get.suit(ui.selected.buttons[i].link)) {
            return false;
          }
        }
        return true;
      });
      const result = await next.forResult();
      if (result?.bool && result?.links?.length) {
        for (const link of result.links) {
          if (get.owner(link) == player) {
            event.list1.push(link);
          } else {
            event.list2.push(link);
          }
        }
        if (event.list1.length && event.list2.length) {
          await game.loseAsync({
            lose_list: [
              [player, event.list1],
              [target, event.list2]
            ],
            discarder: player
          }).setContent("discardMultiple");
        } else if (event.list2.length) {
          await target.discard(event.list2);
        } else {
          await player.discard(event.list1);
        }
        if (event.list1.length != event.list2.length) {
          const list = [player, target];
          if (event.list1.length < event.list2.length) {
            list.reverse();
          }
          await list[0].loseHp();
          await list[1].addTempSkills("rechouhai", "roundEnd");
        }
      }
    },
    derivation: "rechouhai"
  },
  // 段珪
  pschihe: {
    audio: "scschihe",
    trigger: {
      player: "useCardToPlayered",
      target: "useCardToTargeted"
    },
    filter(event, player) {
      return event.targets.length == 1 && event.card.name == "sha";
    },
    logTarget(event, player) {
      return player == event.player ? event.targets[0] : event.player;
    },
    check(event, player) {
      const target = get.info("pschihe").logTarget(event, player);
      return get.attitude(player, target) <= 0 || !player.canCompare(target);
    },
    async content(event, trigger, player) {
      await player.draw(2);
      if (!player.countCards("h")) {
        return;
      }
      const result = await player.chooseCard("h", true, 2, "选择两张手牌展示").forResult();
      if (result?.bool && result?.cards?.length) {
        await player.showCards(result.cards, get.translation(player) + "发动了【" + get.translation(event.name) + "】");
      }
      const target = get.info(event.name).logTarget(trigger, player);
      if (player.canCompare(target)) {
        const result2 = await player.chooseToCompare(target).forResult();
        if (result2?.bool) {
          const evt = trigger.getParent();
          if (typeof evt.baseDamage != "number") {
            evt.baseDamage = 1;
          }
          evt.baseDamage++;
        } else if (player.countDiscardableCards(player, "he")) {
          await player.chooseToDiscard("he", 2, true);
        }
      }
    }
  },
  // 郭胜
  psniqu: {
    audio: "scsniqu",
    trigger: { global: ["useCardAfter", "respondAfter"] },
    filter(event, player) {
      return event.card?.name == "shan";
    },
    check(event, player) {
      return get.attitude(player, event.player) <= 0 || player == event.player;
    },
    usable: 1,
    logTarget: "player",
    async content(event, trigger, player) {
      await player.draw();
      const { player: target } = trigger;
      const sha = get.autoViewAs({ name: "sha", isCard: true });
      if (player.canUse(sha, target, false)) {
        await player.useCard(sha, target, false);
      }
    }
  },
  // 高望
  psmiaoyu: {
    audio: "scsmiaoyu",
    trigger: { global: "recoverEnd" },
    filter(event, player) {
      return event.player.isIn();
    },
    check(event, player) {
      return get.attitude(player, event.player) <= 0;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const { player: target } = trigger;
      player.addTempSkill(event.name + "_effect");
      player.markAuto(event.name + "_effect", [target]);
      const gainEvent = target.gain(get.cards(), "draw");
      gainEvent.giver = player;
      await gainEvent;
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        trigger: { global: "phaseEnd" },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          for (const target of player.getStorage(event.name)) {
            if (!target.isIn()) {
              continue;
            }
            const num = game.getGlobalHistory("everything", (evt) => evt.name == "psmiaoyu").length;
            player.line(target, "green");
            await target.loseHp(num);
          }
        }
      }
    }
  }
};
const skills$b = {
  //赛马娘
  sm_dayuan: {
    audio: 2,
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player) {
      if (!player.countCards("h")) {
        return false;
      }
      return game.hasPlayer((current) => current !== player);
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        filterCard: lib.filter.cardDiscardable,
        position: "h",
        filterTarget: lib.filter.notMe,
        prompt: get.prompt2(event.skill),
        ai1(card) {
          return 7 - get.value(card);
        },
        ai2(target) {
          const player2 = get.player();
          const getE = (current) => get.recoverEffect(current, player2, player2) + get.sgnAttitude(player2, current);
          return getE(player2) + getE(target);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const {
        cards: cards2,
        targets: [target]
      } = event;
      await player.modedDiscard(cards2);
      const func = async (target2) => {
        await target2.draw();
        if (target2.isDamaged()) {
          await target2.recover();
        }
      };
      await game.doAsyncInOrder([player, target], func);
      const skill = `${event.name}_effect`;
      player.addTempSkill(skill, "roundStart");
      const map = player.getStorage(skill, /* @__PURE__ */ new Map());
      if (map.has(target)) {
        map.set(target, map.get(target) + 1);
      } else {
        map.set(target, 1);
      }
      player.setStorage(skill, map, true);
    },
    subSkill: {
      effect: {
        mod: {
          globalFrom(from, to, distance) {
            const map = from.getStorage("sm_dayuan_effect", /* @__PURE__ */ new Map());
            if (!map.has(to)) {
              return;
            }
            return distance - map.get(to);
          },
          globalTo(from, to, distance) {
            const map = to.getStorage("sm_dayuan_effect", /* @__PURE__ */ new Map());
            if (!map.has(from)) {
              return;
            }
            return distance - map.get(from);
          }
        },
        charlotte: true,
        onremove: true,
        intro: {
          content(map) {
            if (!map?.size) {
              return "无效果";
            }
            return Array.from(map.keys()).map((current) => `计算与${get.translation(current)}的距离互相-${map.get(current)}`).join("<br>");
          }
        }
      }
    }
  },
  sm_tianwu: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player) {
      return player.hasMark("chixia_jipao");
    },
    manualConfirm: true,
    async content(event, trigger, player) {
      player.removeMark("chixia_jipao", 1, false);
      game.log(player, "消耗了1点", "<span style='color: #42f0f6'>速度</span>");
      const skill = `${event.name}_effect`;
      player.addTempSkill(skill, { global: "phaseAnyAfter" });
    },
    ai: {
      order(item, player) {
        return get.order({ name: "sha" }, player) + 0.1;
      },
      result: {
        player(player) {
          if (player.hasSkill("sm_tianwu_effect")) {
            return 0;
          }
          return player.countCards("hs", (card) => {
            if (get.type(card) !== "basic") {
              return false;
            }
            const targets = game.filterPlayer((current) => player.canUse(card, current, true, true));
            if (!targets?.length) {
              return false;
            }
            const getE = (current) => get.effect(current, card, player, player);
            const getAE = (current) => {
              let eff = 0;
              const list = [current, current.getNext(), current.getPrevious].toUniqued();
              for (const target of list) {
                if (player.canUse(card, current, current === target, current === target)) {
                  eff += getE(current);
                }
              }
              return eff;
            };
            return getAE(targets.maxBy(getAE));
          });
        }
      }
    },
    subSkill: {
      effect: {
        trigger: {
          player: "useCard1"
        },
        mark: true,
        intro: {
          content: "本阶段使用的下一张基本牌额外指定目标角色的相邻角色为目标"
        },
        charlotte: true,
        onremove: true,
        filter(event, player) {
          return get.type(event.card) === "basic";
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          if (trigger.targets?.length) {
            const targets = game.filterPlayer((current) => {
              if (!trigger.targets.containsSome(current.getNext(), current.getPrevious())) {
                return false;
              }
              return lib.filter.targetEnabled2(trigger.card, trigger.player, current);
            });
            if (targets?.length) {
              game.log(targets, "成为了", trigger.card, "的额外目标");
              trigger.targets.addArray(targets);
            }
          }
          player.removeSkill(event.name);
        },
        ai: {
          effect: {
            player_use(card, player, target) {
              if (get.type(card) !== "basic" || _status._inTianwu) {
                return;
              }
              _status._inTianwu = true;
              let eff = 0;
              const targets = [target.getNext(), target.getPrevious()].toUniqued();
              if (targets?.length) {
                for (const current of targets) {
                  if (player.canUse(card, current, false)) {
                    eff += get.effect(current, card, player, player);
                  }
                }
              }
              delete _status._inTianwu;
              return [1, eff];
            }
          }
        }
      }
    }
  },
  chixia_jipao: {
    audio: 2,
    transformSkill: true,
    trigger: {
      player: ["damageEnd", "die"],
      source: "damageSource"
    },
    forceDie: true,
    forced: true,
    locked: false,
    async content(event, trigger, player) {
      const { name } = event;
      if (trigger.name !== "die") {
        player.addMark(name, 1, false);
        game.log(player, "获得了1点", "<span style='color: #42f0f6'>速度</span>");
        return;
      }
      const card = game.createCard2("sm_mabian", "heart", 13);
      const targets = game.filterPlayer((current) => {
        return current.canEquip(card, true);
      });
      if (!targets?.length) {
        return;
      }
      const result = targets.length > 1 ? await player.chooseTarget({
        prompt: `${get.translation(name)}：将${get.translation(card)}置入一名角色装备栏`,
        filterTarget: (card2, player2, target) => get.event().targetx.includes(target),
        forced: true,
        ai: (target) => {
          const player2 = get.player();
          if (target === player2) {
            return 0;
          }
          return get.attitude(player2, target);
        }
      }).set("targetx", targets).forResult() : {
        bool: true,
        targets
      };
      if (result?.bool && result.targets?.length) {
        const target = result.targets[0];
        player.line(target);
        if (target.canEquip(card, true)) {
          await target.equip(card);
        }
      }
    },
    marktext: "⚡",
    intro: {
      name: "速度·赤霞",
      content(storage, player) {
        return `当前速度：${storage}/5`;
      }
    },
    filter(event, player) {
      const num = player.countMark("chixia_jipao");
      if (event.name === "die") {
        return num >= 3;
      }
      return player?.isIn() && num < 5;
    }
  },
  sm_hualiu: {
    audio: 2,
    trigger: {
      player: "phaseDrawBegin2"
    },
    filter(event, player) {
      return !event.numFixed;
    },
    forced: true,
    async content(event, trigger, player) {
      trigger.num += Math.max(1, player.countMark("qiulin_jipao"));
    },
    mod: {
      globalTo(from, to, distance) {
        return distance + Math.max(1, to.countMark("qiulin_jipao"));
      },
      maxHandcard(player, num) {
        return num + Math.max(1, player.countMark("qiulin_jipao"));
      }
    }
  },
  sm_dianteng: {
    audio: 2,
    trigger: {
      global: "damageSource"
    },
    usable: 1,
    filter(event, player) {
      if (!player.countMark("qiulin_jipao")) {
        return false;
      }
      return event.source?.isIn() && event.source !== player;
    },
    check(event, player) {
      return get.attitude(player, event.source) > 0;
    },
    logTarget: "source",
    async content(event, trigger, player) {
      player.removeMark("qiulin_jipao", 1, false);
      game.log(player, "消耗了1点", "<span style='color: #42f0f6'>速度</span>");
      const selected = [];
      const target = trigger.source;
      const list = [
        ["draw", "摸两张牌"],
        ["recover", "回复1点体力"],
        ["useCard", "使用一张牌"],
        ["recast", "重铸两张牌"]
      ];
      let other = false;
      while (selected.length < list.length) {
        let type;
        if (other) {
          type = list.find((info) => !selected.includes(info[0]))?.[0];
        } else {
          const canSelect = list.map((info) => info[0]).filter((name) => {
            if (selected.includes(name)) {
              return false;
            }
            if (name === "recover") {
              return target.isDamaged();
            }
            if (name === "useCard") {
              return target.countCards("hs", (card) => target.hasUseTarget(card));
            }
            if (name === "recast") {
              return target.countCards("he") > 1;
            }
            return true;
          });
          const result = await target.chooseButton({
            createDialog: [`典滕：选择至多两项（${selected.length + 1}/2）`, [list, "textbutton"]],
            filterButton: (button) => get.event().canSelect.includes(button.link),
            ai: (button) => {
              const player2 = get.player();
              switch (button.link) {
                case "draw": {
                  return 2;
                }
                case "recover": {
                  return get.recoverEffect(player2);
                }
                case "useCard": {
                  const getE = (card) => player2.getUseValue(card);
                  return getE(player2.getCards("hs").maxBy(getE));
                }
              }
              return 0.6;
            }
          }).set("canSelect", canSelect).forResult();
          if (result?.bool && result.links?.length) {
            type = result.links[0];
          } else {
            other = true;
            continue;
          }
        }
        if (!type) {
          break;
        }
        selected.add(type);
        const user = other ? player : target;
        switch (type) {
          case "draw": {
            await user.draw(2);
            break;
          }
          case "recover": {
            await user.recover();
            break;
          }
          case "recast": {
            if (user.countCards("he", lib.filter.cardRecastable) > 1) {
              const result = await user.chooseCard({
                position: "he",
                selectCard: 2,
                filterCard: lib.filter.cardRecastable,
                forced: true,
                prompt: "典滕：重铸两张牌",
                ai: (card) => 7 - get.value(card)
              }).forResult();
              if (result?.bool && result.cards?.length) {
                await user.recast(result.cards);
              }
            }
            break;
          }
          default: {
            await user.chooseToUse({
              filterCard(card, player2, event2) {
                if (get.itemtype(card) !== "card") {
                  return false;
                }
                return lib.filter.filterCard.call(this, card, player2, event2);
              },
              prompt: "典滕：使用一张牌",
              addCount: false
            });
            break;
          }
        }
        if (selected.length > 1) {
          other = true;
        }
      }
    },
    ai: {
      expose: 0.3
    }
  },
  qiulin_jipao: {
    inherit: "chixia_jipao",
    intro: {
      name: "速度·璆琳",
      content(storage, player) {
        return `当前速度：${storage}/5`;
      }
    },
    filter(event, player) {
      const num = player.countMark("qiulin_jipao");
      if (event.name === "die") {
        return num >= 3;
      }
      return player?.isIn() && num < 5;
    }
  },
  sm_feidian: {
    audio: 2,
    forced: true,
    trigger: {
      player: "useCard"
    },
    filter(event, player) {
      return get.suit(event.card) === "heart";
    },
    async content(event, trigger, player) {
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        const stat = player.getStat("card");
        const name = trigger.card.name;
        if (typeof stat[name] === "number" && stat[name] > 0) {
          stat[name]--;
        }
      }
    },
    mod: {
      cardUsable(card, player) {
        if (["unsure", "heart"].includes(get.suit(card))) {
          return Infinity;
        }
      },
      targetInRange(card, player) {
        if (["unsure", "heart"].includes(get.suit(card))) {
          return true;
        }
      },
      cardnumber(card) {
        if (get.suit(card) === "heart") {
          return 13;
        }
      }
    },
    global: "sm_feidian_global",
    subSkill: {
      global: {
        mod: {
          cardEnabled(card, player) {
            let evt = get.event();
            if (evt.name !== "chooseToUse") {
              evt = evt.getParent("chooseToUse");
            }
            if (!evt || !evt.respondTo) {
              return;
            }
            if (!evt.respondTo[0].hasSkill("sm_feidian") || get.suit(evt.respondTo[1], evt.respondTo[0]) !== "heart") {
              return;
            }
            const num = get.number(card);
            if (num !== "unsure" && typeof num === "number" && num < 13) {
              return false;
            }
          },
          cardRespondable(card, player) {
            let evt = get.event();
            if (evt.name !== "chooseToRespond") {
              evt = evt.getParent("chooseToRespond");
            }
            if (!evt || !evt.respondTo) {
              return;
            }
            if (!evt.respondTo[0].hasSkill("sm_feidian") || get.suit(evt.respondTo[1], evt.respondTo[0]) !== "heart") {
              return;
            }
            const num = get.number(card);
            if (num !== "unsure" && typeof num === "number" && num < 13) {
              return false;
            }
          }
        }
      }
    }
  },
  sm_xianxing: {
    audio: 2,
    forced: true,
    trigger: {
      global: "roundStart"
    },
    filter(event, player) {
      return game.hasPlayer((current) => player.canCompare(current));
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => player.canCompare(current));
    },
    onRound(event) {
      return !event.xianxing_phase;
    },
    async content(event, trigger, player) {
      const result = await player.chooseToCompare(event.targets).setContent("chooseToCompareMeanwhile").forResult();
      if (!result?.winner || result.winner !== player) {
        await player.damage();
        const { player: card, targets: cards2 } = result;
        const list = cards2.filter((cardx) => get.position(cardx) === "d" && get.number(cardx) < get.number(card));
        if (list?.length) {
          await player.gain({
            cards: list,
            animate: "gain2"
          });
        }
        return;
      }
      const next = player.insertPhase();
      if (!trigger._finished) {
        trigger.finish();
        trigger._finished = true;
        trigger._triggered = 5;
        if (!lib.onround.includes(get.info("sm_xianxing").onRound)) {
          lib.onround.push(get.info("sm_xianxing").onRound);
        }
        const evt = trigger.player.insertPhase();
        evt.set("xianxing_phase", true);
        evt.relatedEvent = trigger.relatedEvent || trigger.getParent(2);
        evt.skill = trigger.skill;
        evt._noTurnOver = trigger._noTurnOver;
        evt.set("phaseList", trigger.phaseList);
        evt.pushHandler("xianxing_phase", (event2, option) => {
          if (event2.step === 0 && option.state === "begin") {
            event2.step = 2;
            _status.globalHistory.push({
              cardMove: [],
              custom: [],
              useCard: [],
              changeHp: [],
              everything: []
            });
            const players = game.players.slice(0).concat(game.dead);
            for (const current of players) {
              current.actionHistory.push({
                useCard: [],
                respond: [],
                skipped: [],
                lose: [],
                gain: [],
                sourceDamage: [],
                damage: [],
                custom: [],
                useSkill: []
              });
              current.stat.push({ card: {}, skill: {} });
            }
          }
        });
      }
      const nexts = trigger.getParent()?.next;
      if (nexts?.length) {
        for (const evt of nexts.slice(0)) {
          if (evt.finished) {
            continue;
          }
          if (evt === next) {
            break;
          }
          nexts.remove(evt);
          nexts.push(evt);
        }
      }
    }
  },
  zhuahuang_jipao: {
    inherit: "chixia_jipao",
    intro: {
      name: "速度·爪黄",
      content(storage, player) {
        return `当前速度：${storage}/5`;
      }
    },
    filter(event, player) {
      const num = player.countMark("zhuahuang_jipao");
      if (event.name === "die") {
        return num >= 3;
      }
      return player?.isIn() && num < 5;
    }
  },
  sm_zixin: {
    audio: 2,
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player) {
      if (player === _status.currentPhase) {
        return false;
      }
      const cards2 = event.getg(player);
      const hs = player.getCards("h");
      return cards2?.length && cards2.some((card) => hs.includes(card) && player.hasUseTarget(card));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard({
        prompt: get.prompt2(event.skill),
        filterCard: (card) => get.event().cardx.includes(card),
        ai: (card) => get.player().getUseValue(card)
      }).set(
        "cardx",
        trigger.getg(player).filter((card) => player.hasUseTarget(card))
      ).forResult();
    },
    async content(event, trigger, player) {
      const [card] = event.cards;
      await player.showCards(event.cards);
      if (player.hasUseTarget(card) && player.getCards("h").includes(card)) {
        await player.chooseUseTarget({
          card,
          forced: true,
          addCount: false
        });
      }
    }
  },
  sm_zhiyin: {
    audio: 2,
    trigger: {
      global: "phaseZhunbeiBegin"
    },
    filter(event, player) {
      return player.hasMark("zijian_jipao");
    },
    async cost(event, trigger, player) {
      const list = lib.suit.map((suit) => ["", "", `lukai_${suit}`]);
      const result = await player.chooseButton({
        createDialog: [get.prompt(event.skill, trigger.player), [list, "vcard"]],
        selectButton: [1, 4],
        ai: (button) => {
          const { player: player2, att } = get.event();
          if (att <= 0) {
            return 0;
          }
          return Math.random() > 0.3;
        }
      }).set("att", get.attitude(player, trigger.player)).forResult();
      if (result?.bool && result.links?.length) {
        event.result = {
          bool: true,
          targets: [trigger.player],
          cost_data: result.links.map((info) => info[2].slice(6))
        };
      }
    },
    async content(event, trigger, player) {
      const {
        name,
        targets: [target],
        cost_data: suits
      } = event;
      player.removeMark("zijian_jipao", 1, false);
      game.log(player, "消耗了1点", "<span style='color: #42f0f6'>速度</span>");
      game.log(player, "声明的花色：", suits);
      player.addTip(name, `知音${suits.map((suit) => get.translation(suit)).join("")}`);
      player.when({
        global: "phaseEnd"
      }).filter((evt) => evt === trigger.getParent()).step(async (event2, trigger2, player2) => {
        player2.removeTip(name);
        const used = game.getGlobalHistory("useCard").map((card) => get.suit(card)).toUniqued();
        const num1 = suits.filter((suit) => used.includes(suit)).length;
        const num2 = suits.length - num1;
        if (num1 > 0) {
          await game.asyncDraw([player2, target], num1);
        }
        if (num2 > 0) {
          await player2.loseHp(num2);
        }
      });
    }
  },
  zijian_jipao: {
    inherit: "chixia_jipao",
    intro: {
      name: "速度·子建",
      content(storage, player) {
        return `当前速度：${storage}/5`;
      }
    },
    filter(event, player) {
      const num = player.countMark("zijian_jipao");
      if (event.name === "die") {
        return num >= 3;
      }
      return player?.isIn() && num < 5;
    }
  },
  sm_jueying: {
    audio: 2,
    forced: true,
    init(player, skill) {
      player.addExtraEquip(skill, "jueying", true, (player2) => !player2.isTempBanned("sm_jueying"));
    },
    onremove(player, skill) {
      player.removeExtraEquip(skill);
    },
    trigger: {
      player: "damageEnd"
    },
    async content(event, trigger, player) {
      await player.draw();
      const list = get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        const card2 = get.autoViewAs({ name: info[2], nature: info[3], isCard: true });
        return player.hasUseTarget(card2);
      });
      if (!list.length) {
        return;
      }
      const result = await player.chooseButton({
        createDialog: ["珏颖：是否令此技能本回合失效并视为使用一张基本牌？", [list, "vcard"]],
        ai: (button) => {
          const card2 = get.autoViewAs({ name: button.link[2], nature: button.link[3], isCard: true });
          return get.player().getUseValue(card2);
        }
      }).forResult();
      if (!result?.bool || !result.links?.length) {
        return;
      }
      player.tempBanSkill(event.name);
      game.broadcastAll((player2) => {
        player2.$handleEquipChange();
      }, player);
      player.when({
        global: "phaseBeginStart"
      }).step(async (event2, trigger2, player2) => {
        game.broadcastAll((player3) => {
          player3.$handleEquipChange();
        }, player2);
      });
      const card = get.autoViewAs({ name: result.links[0][2], nature: result.links[0][3], isCard: true });
      if (player.hasUseTarget(card)) {
        await player.chooseUseTarget({
          card,
          forced: true,
          addCount: false
        });
      }
    },
    mod: {
      globalTo(from, to, distance) {
        if (to.isTempBanned("sm_jueying")) {
          return;
        }
        return distance + 1;
      }
    }
  },
  sm_xuneng: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player) {
      return player.hasMark("jieying_jipao");
    },
    manualConfirm: true,
    async content(event, trigger, player) {
      player.removeMark("jieying_jipao", 1, false);
      game.log(player, "消耗了1点", "<span style='color: #42f0f6'>速度</span>");
      const skill = `${event.name}_effect`;
      player.addTempSkill(skill);
      player.addMark(skill, 1, false);
    },
    ai: {
      order: 9,
      result: {
        player(player) {
          if (player.hasSkill("sm_xuneng_effect") || !player.getDamagedHp()) {
            return 0;
          }
          return player.countCards("hs", (card) => player.hasValueTarget(card) && ["basic", "trick"].includes(get.type(card)));
        }
      }
    },
    subSkill: {
      effect: {
        trigger: {
          player: "useCard"
        },
        charlotte: true,
        onremove: true,
        filter(event, player) {
          return event.targets?.length === 1;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          const num = player.countMark(event.name) * player.getDamagedHp();
          trigger.baseDamage ??= 1;
          trigger.baseDamage += num;
          player.removeSkill(event.name);
        },
        mod: {
          aiOrder(player, card, num) {
            if (num > 0 && ["trick", "basic"].includes(get.type(card))) {
              return num + 12;
            }
          }
        }
      }
    }
  },
  jieying_jipao: {
    inherit: "chixia_jipao",
    intro: {
      name: "速度·婕媖",
      content(storage, player) {
        return `当前速度：${storage}/5`;
      }
    },
    filter(event, player) {
      const num = player.countMark("jieying_jipao");
      if (event.name === "die") {
        return num >= 3;
      }
      return player?.isIn() && num < 5;
    }
  },
  sm_chitu: {
    audio: 2,
    forced: true,
    trigger: {
      source: "damageBegin1"
    },
    filter(event, player) {
      return event.card;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = event.targets[0];
      for (const key of ["useCard", "respond"]) {
        if (target.hasHistory(key, (evt) => {
          if (!evt.respondTo) {
            return false;
          }
          return evt.respondTo[1] === trigger.card;
        })) {
          trigger.num++;
          return;
        }
      }
      if (target.countGainableCards(player, "h")) {
        await player.gainPlayerCard({
          target,
          position: "h",
          forced: true
        });
      }
    }
  },
  sm_aozhan: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      if (!player.hasMark("liaoyuan_jipao")) {
        return false;
      }
      return get.info("sm_aozhan").derivation.some((skill) => !player.hasSkill(skill, null, null, false));
    },
    chooseButton: {
      dialog(event, player) {
        const skills2 = get.info("sm_aozhan").derivation.filter((skill) => !player.hasSkill(skill, null, null, false));
        return ui.create.dialog("傲战", [skills2, "skill"], "hidden");
      },
      check(button) {
        if (button.link === "jsrgguanjue") {
          return 1;
        }
        return Math.random();
      },
      prompt(links, player) {
        return `消耗1点速度，获得${get.poptip(links[0])}并摸一张牌`;
      },
      backup(links, player) {
        return {
          choiceSkill: links[0],
          manualConfirm: true,
          async content(event, trigger, player2) {
            player2.removeMark("liaoyuan_jipao", 1, false);
            game.log(player2, "消耗了1点", "<span style='color: #42f0f6'>速度</span>");
            const { choiceSkill: skill } = get.info(event.name);
            await player2.addSkills(skill);
            await player2.draw();
          }
        };
      }
    },
    ai: {
      order: 10,
      result: {
        player: 1
      }
    },
    derivation: ["xinshensu", "wushuang", "dclima", "jsrgguanjue"],
    subSkill: {
      backup: {}
    }
  },
  liaoyuan_jipao: {
    inherit: "chixia_jipao",
    intro: {
      name: "速度·燎原",
      content(storage, player) {
        return `当前速度：${storage}/5`;
      }
    },
    filter(event, player) {
      const num = player.countMark("liaoyuan_jipao");
      if (event.name === "die") {
        return num >= 3;
      }
      return player?.isIn() && num < 5;
    }
  },
  sm_dilu: {
    audio: 2,
    trigger: {
      player: "loseAfter",
      global: ["loseAsyncAfter", "gainAfter", "equipAfter", "addToExpansionAfter", "addJudgeAfter"]
    },
    filter(event, player) {
      if ((event.relatedEvent || event.getParent())?.name === "useCard") {
        return false;
      }
      const evt = event.getl(player);
      if (!evt?.cards2?.length) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        const card = get.autoViewAs({ name: info[2], nature: info[3], isCard: true });
        return player.hasUseTarget(card);
      }).length;
    },
    async cost(event, trigger, player) {
      const list = get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        const card = get.autoViewAs({ name: info[2], nature: info[3], isCard: true });
        return player.hasUseTarget(card);
      });
      const result = await player.chooseButton({
        createDialog: [get.prompt(event.skill), [list, "vcard"]],
        ai: (button) => {
          const card = get.autoViewAs({ name: button.link[2], nature: button.link[3], isCard: true });
          return get.player().getUseValue(card);
        }
      }).forResult();
      if (!result?.bool || !result.links?.length) {
        return;
      }
      event.result = {
        bool: true,
        cost_data: result.links[0]
      };
    },
    async content(event, trigger, player) {
      const { cost_data: link } = event;
      const card = get.autoViewAs({ name: link[2], nature: link[3], isCard: true });
      if (player.hasUseTarget(card)) {
        await player.chooseUseTarget({
          card,
          forced: true,
          addCount: false
        });
      }
    },
    locked: true,
    mod: {
      globalFrom(from, to, num) {
        return num + 1;
      },
      globalTo(from, to, num) {
        return num + 1;
      }
    }
  },
  sm_boxing: {
    audio: 2,
    forced: true,
    trigger: {
      target: "useCardToTarget"
    },
    filter(event, player) {
      return event.player !== player;
    },
    async content(event, trigger, player) {
      await player.judge({
        judge: (card) => {
          if (get.suit(card) === "heart") {
            return -1;
          }
          return 1;
        }
      }).set("callback", async (event2, trigger2, player2) => {
        if (event2.judgeResult.suit === "heart") {
          return;
        }
        const num = player2.countDiscardableCards(player2, "he");
        if (num > 0) {
          await player2.chooseToDiscard({
            position: "he",
            forced: true
          });
        }
        const targets = game.filterPlayer((current) => current !== player2);
        const gains = [];
        if (!targets.length) {
          return;
        }
        const map = await game.chooseAnyOL(
          game.filterPlayer((current) => current !== player2),
          get.info("sm_boxing").chooseCard,
          [player2]
        ).forResult();
        for (const current of targets) {
          const result = map.get(current);
          if (result?.bool) {
            current.popup("交给", "wood");
            gains.addArray(result.cards);
          }
        }
        await game.delay();
        if (gains.length) {
          await player2.gain({
            cards: gains,
            animate: "giveAuto"
          });
        }
      });
      if (player.getAllHistory("useSkill", (evt) => evt.skill === event.name).length > 1) {
        await player.changeSkills(["sm_chongji"], ["sm_boxing"]);
      }
    },
    chooseCard(current, boss) {
      const next = current.chooseCard({
        position: "he",
        prompt: `是否交给${get.translation(boss)}一张牌？`,
        ai: (card) => {
          if (get.event().att > 0) {
            return 6 - get.value(card);
          }
          return 1 - get.value(card);
        }
      });
      next.set("_global_waiting", true);
      next.set("att", get.attitude(current, boss));
      return next;
    },
    derivation: "sm_chongji"
  },
  sm_chongji: {
    audio: 2,
    trigger: { player: ["useCard", "recoverEnd"] },
    filter(event, player) {
      if (!player.countMark("dailu_jipao")) {
        return false;
      }
      if (event.name === "useCard") {
        return event.targets?.length === 1 && get.is.damageCard(event.card);
      }
      const evts = player.getAllHistory("gain");
      return game.hasPlayer((current) => {
        const evts2 = current.getAllHistory("lose").map((evt) => evt.getParent());
        return evts.containsSome(...evts2);
      });
    },
    async cost(event, trigger, player) {
      if (trigger.name === "useCard") {
        event.result = await player.chooseBool({
          prompt: get.prompt(event.skill),
          prompt2: `消耗1点速度令${get.translation(trigger.card)}额外结算一遍`,
          choice: (() => {
            let eff = 0;
            for (const target of trigger.targets) {
              eff += get.effect(target, trigger.card, trigger.player, player);
            }
            return eff > 0;
          })()
        }).forResult();
        return;
      }
      const evts = player.getAllHistory("gain");
      const targets = game.filterPlayer((current) => {
        const evts2 = current.getAllHistory("lose").map((evt) => evt.getParent());
        return evts.containsSome(...evts2);
      });
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "消耗1点速度，令一名交给过你牌的角色回复等量体力并摸一张牌",
        filterTarget: (card, player2, target) => get.event().targetx.includes(target),
        ai: (target) => {
          const player2 = get.player();
          return get.recoverEffect(target, player2, player2) + get.sgnAttitude(player2, target);
        }
      }).set("targetx", targets).forResult();
    },
    async content(event, trigger, player) {
      player.removeMark("dailu_jipao", 1, false);
      game.log(player, "消耗了1点", "<span style='color: #42f0f6'>速度</span>");
      if (trigger.name === "useCard") {
        trigger.effectCount++;
      } else {
        const {
          targets: [target]
        } = event;
        if (trigger.num > 0) {
          await target.recover(trigger.num);
          await target.draw();
        }
      }
    }
  },
  dailu_jipao: {
    inherit: "chixia_jipao",
    intro: {
      name: "速度·黛露",
      content(storage, player) {
        return `当前速度：${storage}/5`;
      }
    },
    filter(event, player) {
      const num = player.countMark("dailu_jipao");
      if (event.name === "die") {
        return num >= 3;
      }
      return player?.isIn() && num < 5;
    }
  }
};
const skills$a = {
  //青史刘璿
  hssifen: {
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return target.hasCard((cardx) => target.hasUseTarget(get.autoViewAs({ name: "juedou" }, [cardx]), false, false), "hs") && target !== player;
    },
    async content(event, trigger, player) {
      const { target } = event;
      if (!target.hasCard((cardx) => target.hasUseTarget(get.autoViewAs({ name: "juedou" }, [cardx]), false, false), "hs")) {
        return;
      }
      const next = target.chooseToUse({ forced: true });
      next.set("openskilldialog", `俟奋：将任意张手牌当作【决斗】使用`);
      next.set("norestore", true);
      next.set("_backupevent", "hssifen_backup");
      next.set("custom", {
        add: {},
        replace: { window() {
        } }
      });
      next.backup("hssifen_backup");
      next.set("targetRequired", true);
      next.set("complexSelect", true);
      next.set("addCount", false);
      const result = await next.forResult();
      await player.draw(2);
      player.addTempSkill("hssifen_viewAs", ["phaseChange", "phaseAfter", "phaseBeforeStart"]);
      player.storage.hssifen_viewAs[target.playerid] = result.cards.length;
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          const juedou = new lib.element.VCard({ name: "juedou", isCard: true });
          return target.getUseValue(juedou) * Math.sqrt(target.countCards("h"));
        }
      }
    },
    subSkill: {
      backup: {
        filterCard(card, player) {
          return get.itemtype(card) === "card";
        },
        position: "hs",
        viewAs: {
          name: "juedou"
        },
        selectCard: [1, Infinity],
        ai1(card) {
          if (ui.selected.cards.length) {
            return 0;
          }
          return 5 - get.value(card);
        },
        log: false
      },
      viewAs: {
        init(player, skill) {
          player.storage[skill] = {};
        },
        onremove: true,
        charlotte: true,
        enable: "phaseUse",
        viewAsFilter(player) {
          return player.countCards("hes", lib.skill.hssifen_viewAs.filterCard) > 0;
        },
        filterCard: {
          color: "red"
        },
        complexCard: true,
        selectCard() {
          const map = get.player()?.storage.hssifen_viewAs;
          if (map && Object.keys(map)?.length) {
            const maxCard = Object.keys(map).maxBy((i) => map[i]);
            const minCard = Object.keys(map).minBy((i) => map[i]);
            if (maxCard && minCard) {
              return [map[minCard], map[maxCard]];
            }
          }
          return [1, Infinity];
        },
        prompt() {
          let str = "将指定张红色牌当决斗对【俟奋】目标使用";
          const map = get.player()?.storage.hssifen_viewAs;
          if (map) {
            let list = [];
            for (const id in map) {
              const target = game.findPlayer((current) => current.playerid === id);
              if (target) {
                list.push(`${get.translation(target)}：${map[id]}`);
              }
            }
            if (list.length) {
              str += `<br><span class="text" style="font-family: yuanli">${list.join(" ")}</span>`;
            }
          }
          return str;
        },
        filterTarget(card, player, target) {
          const ids = Object.keys(player.storage.hssifen_viewAs);
          if (!ids.includes(target.playerid)) {
            return false;
          }
          if (ui.selected.cards.length !== player.storage.hssifen_viewAs[target.playerid]) {
            return false;
          }
          return lib.filter.targetEnabled.apply(this, arguments);
        },
        viewAs: {
          name: "juedou"
        },
        position: "hes",
        check(card) {
          return 6 - get.value(card);
        }
      }
    }
  },
  hsfunan: {
    zhuSkill: true,
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player) {
      if (player.hasSkill("hsfunan_used") || !player.hasZhuSkill("hsfunan") || !game.hasPlayer((current) => current !== player && current.group === "shu")) {
        return false;
      }
      return !event.hsfunan;
    },
    viewAs: { name: "sha" },
    filterCard: () => false,
    selectCard: -1,
    group: ["hsfunan_2"],
    derivation: "rejijiang",
    ai: {
      order(item, player) {
        const order = get.order({ name: "sha" });
        _status.debugger = _status.event;
        if (order <= 0) {
          return order;
        }
        const losehp = get.effect(player, { name: "losehp" }, player, player);
        if (losehp > 0) {
          return order + 0.3;
        }
        if (player.hp <= 1) {
          return 0;
        }
        const draw = 2 * get.effect(player, { name: "draw" }, player, player);
        if (losehp + draw > 0) {
          return order + 0.1;
        }
        const eff = player.getUseValue({ name: "sha" }, null, true);
        if (eff <= 0) {
          return 0;
        }
        const maySha = Math.max(
          ...game.filterPlayer((current) => {
            return player !== current && current.group === "shu" && get.attitude(player, current) > 0;
          }).map((current) => {
            return current.mayHaveSha(player, _status.event?.name === "chooseToUse" ? "use" : "respond", null, "count");
          })
        );
        if (maySha < 1 && (1 - maySha) * losehp + draw < 0) {
          return 0;
        }
        return order + 0.3;
      },
      respondSha: true,
      skillTagFilter(player) {
        if (!player.hasZhuSkill("hsfunan") || !game.hasPlayer((current) => current !== player && current.group === "shu")) {
          return false;
        }
      }
    },
    subSkill: {
      2: {
        trigger: { player: ["useCardBegin", "respondBegin"] },
        filter(event, player) {
          return event.skill === "hsfunan";
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          player.addTempSkill("hsfunan_used");
          const targets = game.filterPlayer((target) => target !== player && target.group === "shu");
          if (!targets.length) {
            return;
          }
          player.logSkill("rejijiang", targets);
          trigger.getParent().set("hsfunan", true);
          let responded = false;
          for (const target of targets.sortBySeat()) {
            const next = target.chooseToRespond({
              prompt: `是否替${get.translation(player)}打出一张杀？`,
              card: { name: "sha" },
              ai: () => {
                const event2 = _status.event;
                return get.attitude(event2.player, event2.source) - 2;
              }
            });
            next.set("source", player);
            next.set("hsfunan", true);
            next.set("skillwarn", `替${get.translation(player)}打出一张杀`);
            next.noOrdering = true;
            next.autochoose = lib.filter.autoRespondSha;
            const result = await next.forResult();
            if (result.bool) {
              trigger.card = result.card;
              trigger.cards = result.cards;
              trigger.throw = false;
              if (typeof target.ai.shown === "number" && target.ai.shown < 0.95) {
                target.ai.shown += 0.3;
                if (target.ai.shown > 0.95) {
                  target.ai.shown = 0.95;
                }
              }
              responded = true;
              break;
            }
          }
          if (!responded) {
            await player.loseHp();
            await player.draw(2);
            trigger.cancel();
            trigger.getParent().goto(0);
          }
        }
      },
      used: {
        charlotte: true
      }
    }
  },
  //青史曹奂
  hsjunwei: {
    enable: "chooseToUse",
    usable: 1,
    filterCard(card) {
      if (!ui.selected.cards.length) {
        return true;
      }
      return get.color(card) === get.color(ui.selected.cards[0]);
    },
    viewAsFilter(player) {
      return player.countCards("hes") > 1;
    },
    viewAs: {
      name: "wuxie",
      storage: {
        hsjunwei: true
      }
    },
    selectCard: 2,
    position: "hes",
    prompt: "将两张颜色相同的牌当无懈可击使用",
    check(card) {
      return 8 - get.value(card);
    },
    group: ["hsjunwei_effect"],
    subSkill: {
      effect: {
        trigger: { player: "useCardToEnd" },
        filter(event, player) {
          if (event.card.name !== "wuxie" || !event.card?.storage?.hsjunwei || event.getParent()._neutralized) {
            return false;
          }
          const card = event.getParent().respondTo?.[1];
          const evt = event.getParent(6);
          if (!card) {
            return false;
          }
          const info = get.info(card);
          const bool1 = evt.targets && info.allowMultiple !== false && !info.multitarget;
          const bool2 = game.hasPlayer((target) => !evt.targets?.includes(target) && lib.filter.targetEnabled2(card, evt.player, target));
          event.set("respondToEvent", evt);
          return bool1 && bool2;
        },
        async cost(event, trigger, player) {
          const evt = trigger.respondToEvent;
          const targets = evt.targets;
          const source = evt.player;
          const card = evt.card;
          event.result = await player.chooseTarget({
            prompt: `###${get.prompt("hsjunwei")}###令至多两名角色也成为${get.translation(card)}的目标`,
            selectTarget: [1, 2],
            filterTarget: (card2, player2, target) => !get.event().targets.includes(target) && lib.filter.targetEnabled2(get.event().card, get.event().sourcex, target),
            ai: (target) => get.effect(target, get.event().card, get.event().sourcex, get.player())
          }).set("targets", targets).set("sourcex", source).set("card", card).forResult();
        },
        async content(event, trigger, player) {
          const evt = trigger.respondToEvent;
          const targets = event.targets.sortBySeat(evt.player);
          evt.targets.addArray(targets);
          game.log(targets, "也成为", evt.card, "的目标");
        }
      }
    }
  },
  hsmoran: {
    trigger: { player: "damageEnd" },
    forced: true,
    filter(event, player) {
      return event.num > 0;
    },
    async content(event, trigger, player) {
      const list = [1, 2, 3];
      const result = await player.chooseControl({
        controls: list,
        prompt: "默然：你选择于一至三个回合结束后（包含此回合）摸两倍所选回合数量的牌，在此期间中你的所有技能失效。",
        ai: () => get.rand(0, 2)
      }).forResult();
      if (result?.control) {
        const skill = `${event.name}_draw`;
        const num = result.control;
        player.addMark(skill, num, false);
        player.storage[`${skill}_num`] = num * 2;
        player.addSkill(skill);
        player.addTempSkill("baiban", { player: "dieAfter" });
      }
    },
    subSkill: {
      draw: {
        onremove(player, skill) {
          delete player.storage[skill];
          delete player.storage[`${skill}_num`];
          player.removeSkill("baiban");
        },
        trigger: { global: "phaseEnd" },
        charlotte: true,
        direct: true,
        filter(event, player) {
          return player.hasMark("hsmoran_draw");
        },
        async content(event, trigger, player) {
          player.removeMark(event.name, 1, false);
          if (!player.hasMark(event.name)) {
            const num = player.storage[`${event.name}_num`];
            player.logSkill("hsmoran");
            player.removeSkill(event.name);
            player.draw(num);
          }
        },
        mark: true,
        intro: {
          content(storage, player, skill) {
            return `${storage}个回合结束后你摸${player.storage[`${skill}_num`]}张牌`;
          }
        }
      }
    }
  },
  //青史刘协
  hsjixu: {
    enable: "phaseUse",
    filterTarget(card, player, target) {
      const storage = player.getStorage("hsjixu_used");
      const selected = ui.selected.targets;
      if (!selected.length) {
        return true;
      }
      const targets = selected.concat([target]).sortBySeat();
      return !storage.some((targetsx) => targetsx.sortBySeat().every((targetx, index) => targetx === targets[index]));
    },
    selectTarget: 2,
    multiline: true,
    multitarget: true,
    complexTarget: true,
    async content(event, trigger, player) {
      const targets = event.targets.sortBySeat();
      const num1 = targets.reduce((sum, target) => sum += target.countCards("h"), 0);
      const others = game.filterPlayer().removeArray(targets);
      let goon = true;
      player.addTempSkill(`${event.name}_used`, ["phaseChange", "phaseAfter", "phaseBeforeStart"]);
      player.markAuto(`${event.name}_used`, [[...targets]]);
      if (!others.length) {
        goon = true;
      }
      if (others.length === 1 && others[0].countCards("h") <= num1) {
        goon = false;
      }
      if (others.length > 1) {
        for (let i = 0; i < others.length; i++) {
          for (let j = i + 1; j < others.length; j++) {
            if (others[i].countCards("h") + others[j].countCards("h") <= num1) {
              goon = false;
            }
          }
        }
      }
      if (!goon) {
        player.popup("杯具");
        return;
      }
      player.popup("洗具");
      if (_status.connectMode) {
        game.broadcastAll(() => _status.noclearcountdown = true);
      }
      const cards2 = get.cards(3, true);
      const map = {};
      while (cards2.length) {
        const result = await player.chooseButtonTarget({
          createDialog: [`济恤：请选择要分配的牌和目标`, cards2],
          forced: true,
          selectButton: [1, 2],
          cardsx: cards2,
          targets,
          map,
          filterTarget(card, player2, target) {
            return get.event().targets.includes(target);
          },
          ai1(button) {
            const { player: player2, map: map2 } = get.event();
            return Math.max(
              ...game.filterPlayer().map((target) => {
                return [...ui.selected.cards, button.link, ...map2[target.playerid] || []].reduce((sum, card) => {
                  return get.value(card, target) * get.attitude(player2, target);
                }, 0);
              })
            );
          },
          ai2(target) {
            const player2 = get.player();
            const card = ui.selected.buttons[0].link;
            if (card) {
              return get.value(card, target) * get.attitude(player2, target);
            }
            return 0;
          }
        }).forResult();
        if (result?.bool && result.targets?.length && result.links?.length) {
          cards2.removeArray(result.links);
          const id = result.targets[0].playerid;
          if (!map[id]) {
            map[id] = [];
          }
          map[id].addArray(result.links);
        } else {
          break;
        }
        const nogain = targets.filter((target) => !map[target.playerid]?.length);
        if (cards2.length === 1 && nogain.length > 0) {
          map[nogain[0].playerid] = [cards2.pop()];
        }
      }
      if (_status.connectMode) {
        game.broadcastAll(() => {
          delete _status.noclearcountdown;
          game.stopCountChoose();
        });
      }
      if (Object.keys(map).length) {
        const gain_list = [];
        for (const i in map) {
          const source = (_status.connectMode ? lib.playerOL : game.playerMap)[i];
          player.line(source, "green");
          gain_list.push([source, map[i]]);
          game.log(source, "获得了", map[i]);
        }
        await game.loseAsync({
          gain_list,
          giver: player,
          animate: "gain2"
        }).setContent("gaincardMultiple");
      }
    },
    ai: {
      order: 7,
      result: {
        target: 1
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true,
        intro: {
          content(storage, player, skill) {
            let str = "已触发过的组合：";
            if (!storage.length) {
              str += "无";
            } else {
              for (const targets of storage) {
                str += `<br><li>${get.translation(targets)}`;
              }
            }
            return str;
          }
        }
      }
    }
  },
  hsyouchong: {
    enable: "chooseToUse",
    usable: 1,
    filter(event, player) {
      if (event.responded || event.hsyouchong || !game.hasPlayer((target) => target.countCards("h") > player.countCards("h"))) {
        return false;
      }
      const list = get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player, event);
      });
      return list.length > 0;
    },
    hiddenCard(player, name) {
      if (get.type(name) === "basic" && lib.inpile.includes(name) && game.hasPlayer((target) => target.countCards("h") > player.countCards("h"))) {
        return true;
      }
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player, event);
        });
        return ui.create.dialog("优崇", [list, "vcard"], "hidden");
      },
      filter(button, player) {
        return get.event().getParent().filterCard({ name: button.link[2], nature: button.link[3] }, player, get.event().getParent());
      },
      backup(links, player) {
        return {
          selectCard: -1,
          filterCard: () => false,
          viewAs: {
            name: links[0][2],
            nature: links[0][3]
          },
          log: false,
          async precontent(event, trigger, player2) {
            if (!game.hasPlayer((target) => target.countCards("h") > player2.countCards("h"))) {
              return;
            }
            const skill = "hsyouchong";
            event.result.cards;
            const card = get.autoViewAs(event.result.card, "unsure");
            const result = await player2.chooseTarget({
              prompt: `优崇：令任意名手牌数多于你的角色选择是否将三张牌当作${get.translation(card)}替你使用`,
              selectTarget: [1, Infinity],
              forced: true,
              filterTarget: (card2, player3, target) => target.countCards("h") > player3.countCards("h"),
              ai: () => true
            }).forResult();
            if (!result?.targets) {
              return;
            }
            const targets = result.targets.sortBySeat();
            player2.logSkill(skill, targets);
            let goon = false;
            for (const target of targets) {
              const resultx = await target.chooseCard({
                prompt: `优崇：是否将三张牌当作${get.translation(card)}替${get.translation(player2)}使用`,
                selectCard: 3,
                ai: (card2) => get.event().att ? 7 - get.value(card2) : 0
              }).set("att", get.attitude(target, player2) > 0).forResult();
              if (resultx?.cards?.length) {
                target.popup("响应");
                goon = true;
                if (typeof target.ai.shown === "number" && target.ai.shown < 0.95) {
                  target.ai.shown += 0.3;
                  if (target.ai.shown > 0.95) {
                    target.ai.shown = 0.95;
                  }
                }
                event.result.cards = resultx.cards;
              }
            }
            if (!goon) {
              const evt = event.getParent();
              evt.set(skill, true);
              evt.goto(0);
              return;
            }
          }
        };
      },
      prompt(links, player) {
        const name = links[0][2];
        const nature = links[0][3];
        return `选择${get.translation(nature) || ""}${get.translation(name)}的目标`;
      }
    }
  },
  //青史孙皓
  hsshezuo: {
    trigger: { player: "phaseZhunbeiBegin" },
    async cost(event, trigger, player) {
      const list = lib.skill[event.skill].effectList.slice();
      const controls = [1, 2, 3].map((num) => `选项${get.cnNumber(num, true)}`);
      const result = await player.chooseControl({
        controls: [...controls, "cancel2"],
        choiceList: list,
        prompt: `###${get.prompt(event.skill)}###选择一项令本回合下次拼点结束后没赢的角色执行`,
        ai: () => get.rand(2)
      }).forResult();
      if (result?.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.index
        };
      }
    },
    async content(event, trigger, player) {
      const index = event.cost_data;
      player.addTempSkill(`${event.name}_effect`);
      player.markAuto(`${event.name}_effect`, index);
    },
    effectList: [`依次弃置两张牌，不足则失去等量体力；`, `横置并受到1点火焰伤害`, `将所有手牌当一张普通锦囊牌使用`],
    group: ["hsshezuo_compare"],
    subSkill: {
      compare: {
        prompt: "摸一张牌与一名其他角色进行拼点",
        enable: "phaseUse",
        usable: 1,
        filter(event, player) {
          return game.hasPlayer((target) => player.canCompare(target, true));
        },
        filterTarget(card, player, target) {
          return player.canCompare(target, true);
        },
        async content(event, trigger, player) {
          const target = event.target;
          await player.draw();
          if (player.canCompare(target)) {
            await player.chooseToCompare(target);
          }
        },
        ai: {
          order: 9,
          result: {
            target(player, target) {
              return -1;
            }
          }
        }
      },
      effect: {
        onremove: true,
        charlotte: true,
        forced: true,
        intro: {
          content(storage, player) {
            const list = lib.skill.hsshezuo.effectList.slice();
            let str = "下次拼点结束后没赢的角色须执行：";
            if (!storage.length) {
              str += "无";
            } else {
              storage.forEach((index) => str += `<br><li>${list[index]}`);
            }
            return str;
          }
        },
        trigger: {
          global: ["chooseToCompareAfter", "compareMultipleAfter"]
        },
        filter(event, player, name, target) {
          if (event.preserve || event.result?.cancelled) {
            return false;
          }
          if (!lib.skill.hsshezuo_effect.logTarget(event, player).length) {
            return false;
          }
          if (event.name === "compareMultiple") {
            return true;
          }
          return !event.compareMultiple;
        },
        logTarget(event, player) {
          let list = [];
          if (event.targets?.length) {
            list.addArray([event.player].concat(event.targets).filter((target) => target !== event.result?.winner));
          } else {
            list.addArray([event.player, event.target].filter((target) => target !== event.result?.winner));
          }
          return list.sortBySeat();
        },
        async content(event, trigger, player) {
          const { targets } = event;
          const storage = player.getStorage(event.name);
          player.removeSkill(event.name);
          for (const target of targets) {
            if (storage.includes(0)) {
              let count = 2;
              while (target.countDiscardableCards(target, "he") && count > 0) {
                await target.chooseToDiscard({ position: "he", forced: true });
                count--;
              }
              if (count > 0) {
                await target.loseHp(count);
              }
            }
            if (storage.includes(1)) {
              await target.link(true);
              await target.damage({ nature: "fire" });
            }
            if (storage.includes(2)) {
              if (!target.countCards("h")) {
                continue;
              }
              const list = get.inpileVCardList((info) => info[0] === "trick");
              const hs = target.getCards("h");
              if (!list.some((info) => target.hasUseTarget(get.autoViewAs({ name: info[2] }, hs), true, false))) {
                continue;
              }
              const result = await target.chooseButton({
                createDialog: ["设座：将所有手牌当作一张普通锦囊牌使用", [list, "vcard"]],
                forced: true,
                filterButton: (button) => get.player().hasUseTarget(get.event().viewAs(button), true, false),
                ai: (button) => get.player().getUseValue(get.event().viewAs(button))
              }).set("viewAs", (button) => get.autoViewAs({ name: button.link[2] }, get.player().getCards("h"))).forResult();
              if (result?.links?.length) {
                const name = result.links[0][2];
                const card = get.autoViewAs({ name }, hs);
                await target.chooseUseTarget({
                  card,
                  cards: hs,
                  forced: true,
                  addCount: false
                });
              }
            }
          }
        }
      }
    }
  },
  //陈寿
  //用扑克牌打牌神将
  poker_record: {
    mark: true,
    marktext: "🃏",
    intro: {
      name: "扑克牌堆",
      markcount(storage, player) {
        const pile = _status.pokerPile;
        const discarded = _status.pokerDiscarded;
        if (!pile || !discarded) {
          return 0;
        }
        return `${discarded.length || 0}/${pile.length || 0}`;
      },
      mark(dialog, storage, player) {
        const pile = _status.pokerPile;
        const discarded = _status.pokerDiscarded;
        if (pile.length) {
          dialog.addText("牌堆");
          dialog.addText(`共${pile.length}张牌`);
        }
        if (discarded.length) {
          dialog.addText("弃牌堆");
          dialog.addSmall(discarded);
        }
        if (!pile.length && !discarded.length) {
          dialog.addText("空空如也");
        }
      }
    },
    charlotte: true
  },
  hschenzhi: {
    //初始化扑克牌堆
    init(player, skill) {
      if (!lib.commonArea.has("pokerPile")) {
        lib.commonArea.set("pokerPile", {
          translate: "扑克牌堆",
          areaStatusName: "pokerPile",
          isUnseen: true,
          toName: "toPokerPile",
          fromName: "fromPokerPile",
          async addHandeler(event, trigger, player2) {
            const { cards: cards2 } = event;
            _status.pokerPile.addArray(cards2.filter((card) => !card.willBeDestroyed("pokerPile", null, event.relatedEvent)));
            lib.skill.hschenzhi.update();
          },
          /** 处理从相应区域中移出的卡牌*/
          async removeHandeler(event, trigger, player2) {
            _status.pokerPile.removeArray(event.cards);
            lib.skill.hschenzhi.update();
          }
        });
      }
      if (!lib.commonArea.has("pokerDiscarded")) {
        lib.commonArea.set("pokerDiscarded", {
          translate: "扑克弃牌堆",
          areaStatusName: "pokerDiscarded",
          toName: "toPokerDiscarded",
          fromName: "fromPokerDiscarded",
          async addHandeler(event, trigger, player2) {
            const { cards: cards2 } = event;
            _status.pokerDiscarded.addArray(cards2.filter((card) => !card.willBeDestroyed("pokerDiscarded", null, event.relatedEvent)));
            game.log(cards2, "被移入扑克弃牌堆");
            lib.skill.hschenzhi.update();
          },
          /** 处理从相应区域中移出的卡牌*/
          async removeHandeler(event, trigger, player2) {
            _status.pokerDiscarded.removeArray(event.cards);
            lib.skill.hschenzhi.update();
          }
        });
      }
      player.addSkill("poker_record");
      if (!_status.pokerPile) {
        lib.skill[skill].initPile();
      }
    },
    //初始化单张扑克
    initPoker(suit = "none", number = "none") {
      const card = game.createCard2("hschenzhi_poker", suit, number);
      game.broadcastAll(
        (card2, suit2) => {
          card2.destroyed = (card3, position, player, event) => {
            if (position === "discardPile") {
              lib.skill.hschenzhi.discard(card3, true);
            }
            return false;
          };
        },
        card,
        suit
      );
      return card;
    },
    //初始化牌堆
    initPile(nocardpile) {
      const suits = lib.suit.slice();
      const cards2 = [];
      if (nocardpile) {
        game.broadcastAll(() => {
          _status.pokerPile ??= [];
          _status.pokerDiscarded ??= [];
        });
      } else {
        for (const suit of suits) {
          for (let i = 1; i < 14; i++) {
            const card = lib.skill.hschenzhi.initPoker(suit, i);
            cards2.add(card);
          }
        }
        cards2.randomSort();
        _status.pokerPile ??= cards2;
        _status.pokerDiscarded ??= [];
        game.cardsGotoSpecial(cards2);
      }
      lib.skill.hschenzhi.update();
    },
    //扑克牌堆洗牌
    washCard() {
      if (!_status.pokerPile || !_status.pokerDiscarded) {
        lib.skill.hschenzhi.initPile(true);
      }
      if (!_status.pokerPile.length && !_status.pokerDiscarded.length) {
        return;
      }
      const cards2 = _status.pokerPile.concat(_status.pokerDiscarded);
      _status.pokerDiscarded = [];
      cards2.randomSort();
      _status.pokerPile = cards2;
      lib.skill.hschenzhi.update();
    },
    //更新扑克牌堆
    update() {
      game.broadcast(
        (pile, discard) => {
          _status.pokerPile = pile;
          _status.pokerDiscarded = discard;
        },
        _status.pokerPile,
        _status.pokerDiscarded
      );
      game.filterPlayer((target) => target.hasSkill("poker_record")).forEach((target) => target.markSkill("poker_record"));
    },
    //从扑克牌堆获得牌
    getCards(num) {
      if (typeof num !== "number") {
        num = 1;
      }
      if (num <= 0) {
        return [];
      }
      const list = [];
      while (num > 0) {
        if (!_status.pokerPile.length) {
          lib.skill.hschenzhi.washCard();
        }
        if (!_status.pokerPile.length) {
          break;
        }
        const cardx = _status.pokerPile.shift();
        if (!cardx) {
          break;
        }
        cardx.original = "s";
        list.push(cardx);
        num--;
      }
      list.slice().reverse().forEach((card) => _status.pokerPile.unshift());
      if (num > 0) {
        list.addArray(get.cards(num, true));
      }
      lib.skill.hschenzhi.update();
      return list;
    },
    //将扑克牌放回牌堆或弃置
    discard(card, noinsert) {
      if (card.name !== "hschenzhi_poker") {
        card.discard(false);
        return;
      }
      if (noinsert) {
        game.cardsGotoSpecial(card, "toPokerDiscarded");
      } else {
        ui.special.appendChild(card);
        _status.pokerPile.splice(get.rand(0, _status.pokerPile.length - 1), 0, card);
        lib.skill.hschenzhi.update();
      }
    },
    trigger: {
      player: "drawBegin",
      global: ["gameDrawBegin", "replaceHandcardsBegin"]
    },
    forced: true,
    lastDo: true,
    filter(event, player) {
      if (event.name === "draw") {
        return event.num > 0;
      }
      return true;
    },
    async content(event, trigger, player) {
      if (trigger.name === "draw") {
        trigger.set("otherGetCards", lib.skill.hschenzhi.getCards);
      } else {
        if (!trigger.otherPile) {
          trigger.set("otherPile", {});
        }
        trigger.otherPile[player.playerid] = {
          getCards: lib.skill.hschenzhi.getCards,
          discard: lib.skill.hschenzhi.discard
        };
      }
    },
    ai: {
      sortCardByNum: true,
      combo: "hsdianmo"
    }
  },
  hsdianmo: {
    init(player, skill) {
      lib.skill[skill].initList();
    },
    initList() {
      if (!_status.characterlist) {
        game.initCharacterList();
      }
      const characters2 = _status.characterlist.slice();
      const skills2 = characters2.map((i) => get.character(i, 3)).flat().unique();
      const list = [];
      game.expandSkills(skills2, true);
      for (let skill of skills2) {
        let info = get.info(skill);
        if (info.viewAs) {
          info = info.viewAs;
          if (typeof info === "function") {
            const str2 = info?.toString();
            if (!str2 || str2.includes("isCard: true")) {
              continue;
            }
          } else if (info.isCard === true) {
            continue;
          }
        } else if (info.chooseButton?.backup) {
          info = info.chooseButton?.backup;
          const str2 = info?.toString();
          if (!str2 || !str2.includes("viewAs: ") || str2.includes("isCard: true")) {
            continue;
          }
        } else {
          continue;
        }
        skill = get.sourceSkillFor(skill);
        info = get.info(skill);
        if (!info || info.silent || info.juexingji || info.hiddenSkill || info.groupSkill || info.zhuSkill) {
          continue;
        }
        if (info.ai && (info.ai.combo || info.ai.notemp || info.ai.neg)) {
          continue;
        }
        const str = get.skillInfoTranslation(skill);
        if (!["当", "当做", "当作"].some((s) => str.includes(s))) {
          continue;
        }
        list.add(skill);
      }
      _status.viewAsSkills = list;
    },
    keepSkill: true,
    trigger: { player: ["phaseZhunbeiBegin", "damageEnd"] },
    filter(event, player) {
      if (event.name === "damage") {
        return player.getHistory("damage", (evt) => evt.num > 0).indexOf(event) === 0;
      }
      return true;
    },
    async cost(event, trigger, player) {
      if (!_status.viewAsSkills) {
        lib.skill[event.skill].initList();
      }
      const list = _status.viewAsSkills.filter((skill) => !player.hasSkill(skill, null, null, false));
      if (!list.length) {
        return;
      }
      const skills2 = list.randomGets(2);
      const result = await player.chooseButton({
        createDialog: [
          `${get.prompt2(event.skill)}<span class=thundertext style="font-weight:bold;">当前拥有技能：${get.translation(player.additionalSkills["hsdianmo"])}</span>`,
          [
            [
              ["gain", "获得技能"],
              ["replace", "替换技能"],
              ["draw", "直接摸牌"]
            ],
            "tdnodes"
          ],
          [skills2, "skill"]
        ],
        selectButton: () => {
          if (ui.selected.buttons.length && ui.selected.buttons[0].link === "draw") {
            return 1;
          }
          return 2;
        },
        filterButton: (button) => {
          const actions = ["gain", "replace", "draw"];
          const len = get.player().additionalSkills["hsdianmo"]?.length || 0;
          if (!ui.selected.buttons.length) {
            if (button.link === "gain") {
              return len < 4;
            }
            if (button.link === "replace") {
              return len > 0;
            }
            if (button.link === "draw") {
              return len < 4;
            }
            return false;
          }
          return ui.selected.buttons[0].link === "draw" ? false : !actions.includes(button.link);
        },
        complexSelect: true,
        ai: () => Math.random()
      }).forResult();
      if (result?.links?.length) {
        event.result = {
          bool: true,
          cost_data: result.links
        };
      }
    },
    async content(event, trigger, player) {
      const skill = event.cost_data[1];
      const action = event.cost_data[0];
      const skills2 = player.additionalSkills[event.name]?.slice() || [];
      if (action === "replace") {
        if (!skills2.length) {
          return;
        }
        const result = await player.chooseButton({
          createDialog: [`###点墨：请选择一个要替换的技能###${get.translation(skill)}：${get.translation(`${skill}_info`)}`, [skills2, "skill"]],
          forced: true,
          ai: () => Math.random()
        }).forResult();
        if (!result?.links?.length) {
          return;
        }
        const replaced = result.links[0];
        skills2.remove(replaced);
      }
      if (action !== "draw") {
        skills2.add(skill);
        await player.addAdditionalSkills(event.name, skills2);
      }
      await player.draw(4 - (player.additionalSkills[event.name]?.length || 0));
    }
  },
  hszaibi: {
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return player.countCards("he", (card) => player.canRecast(card)) > 1;
    },
    filterCard(card, player) {
      const selected = ui.selected.cards.slice();
      if (!selected.length) {
        return player.canRecast(card);
      }
      selected.add(card);
      const nums = selected.map((card2) => get.number(card2, get.player())).unique().sort((a, b) => a - b);
      if (nums.length === selected.length && nums.length > 1) {
        if (nums[nums.length - 1] - nums[0] === nums.length - 1) {
          return player.canRecast(card);
        }
      }
      return false;
    },
    position: "he",
    selectCard: [2, Infinity],
    complexCard: true,
    lose: false,
    discard: false,
    delay: false,
    async content(event, trigger, player) {
      const cards2 = event.cards;
      await player.recast(cards2);
      const card = game.createCard2("chunqiubi", "heart", 5);
      if (player.canEquip(card, true)) {
        await player.equip(card);
      }
    },
    ai: {
      order: 7,
      result: {
        player: 1
      }
    }
  },
  chunqiubi_skill: {
    enable: "phaseUse",
    usable: 1,
    prompts: ["起：失去1点体力", "承：摸已损失体力值张牌", "转：回复1点体力", "合：弃置已损失体力值张手牌"],
    async content(event, trigger, player) {
      let funcs = [
        async (target) => {
          await target.loseHp();
        },
        async (target) => {
          if (!target.getDamagedHp()) {
            return;
          }
          await target.draw(target.getDamagedHp());
        },
        async (target) => {
          if (!target.isDamaged()) {
            return;
          }
          await target.recover();
        },
        async (target) => {
          if (!target.countDiscardableCards(target, "he") || !target.isDamaged()) {
            return;
          }
          await target.chooseToDiscard({
            position: "he",
            selectCard: target.getDamagedHp(),
            forced: true
          });
        }
      ];
      let prompts = lib.skill.chunqiubi_skill.prompts.slice();
      const selected = prompts.randomGet();
      const char = selected.slice(0, 1);
      const index = prompts.indexOf(selected);
      prompts = prompts.slice(index, 4).concat(prompts.slice(0, index));
      funcs = funcs.slice(index, 4).concat(funcs.slice(0, index));
      game.log(player, "抽中了", `#y${char}`);
      const result = await player.chooseButtonTarget({
        createDialog: [
          `###春秋笔：令一名角色正序或逆序执行以下项（上面为正序。下面为逆序）###${prompts.join("<br>")}<br><br>${[prompts[0]].concat(prompts.slice(1, 4).reverse()).join("<br>")}`,
          [
            [
              ["forward", "正序"],
              ["reverse", "逆序"]
            ],
            "tdnodes"
          ]
        ],
        forced: true,
        filterButton: true,
        filterTarget: true,
        ai1(button) {
        },
        ai2(target) {
        }
      }).forResult();
      if (result?.bool) {
        const target = result.targets[0];
        const link = result.links[0];
        player.line(target);
        if (link === "reverse") {
          funcs = [funcs[0]].concat(funcs.slice(1, 4).reverse());
        }
        for (const func of funcs) {
          if (!target.isIn()) {
            break;
          }
          await func(target);
        }
      }
    },
    ai: {
      order: 5,
      ai: {
        player: 1
      }
    }
  }
};
const skills$9 = {
  //张星彩
  ymhengren: {
    audio: 2,
    zhuanhuanji: true,
    marktext: "☯",
    mark: true,
    intro: {
      content(storage, player) {
        return `其他角色的出牌阶段${storage ? "结束时" : "开始时"}，若其本阶段有【杀】的剩余使用次数，你可以使用一张【杀】（将计入其使用次数且伤害+1）。`;
      }
    },
    trigger: { global: ["phaseUseBegin", "phaseUseEnd"] },
    filter(event, player, name) {
      const storage = player.storage.ymhengren;
      const num = event.player.getCardUsable("sha", false);
      if (name !== `phaseUse${storage ? "End" : "Begin"}`) {
        return false;
      }
      if (player === event.player) {
        return false;
      }
      return num > 0;
    },
    direct: true,
    clearTime: true,
    async content(event, trigger, player) {
      const target = trigger.player;
      const result = await player.chooseToUse({
        filterCard(card) {
          if (get.name(card) !== "sha") {
            return false;
          }
          return lib.filter.filterCard.apply(this, arguments);
        },
        prompt: "横刃：你可以使用一张【杀】",
        ai1(card) {
          return 10 - get.value(card);
        }
      }).set("logSkill", event.name).set("oncard", () => {
        const evt = _status.event;
        if (typeof evt.baseDamage !== "number") {
          evt.baseDamage = 1;
        }
        evt.baseDamage++;
      }).forResult();
      if (result?.bool) {
        player.changeZhuanhuanji(event.name);
        const stat = target.getStat("card");
        const name = "sha";
        if (typeof stat[name] !== "number") {
          stat[name] = 0;
        }
        stat[name]++;
      }
    }
  },
  ymdanjue: {
    audio: 2,
    trigger: { global: "useCardToTargeted" },
    filter(event, player) {
      return event.card.name === "sha" && get.distance(event.target, player) <= 1 && event.getParent().addCount !== false;
    },
    check(event, player) {
      if (get.attitude(player, event.player) > 0 && event.player.countCards("h") > 3) {
        return true;
      }
      if (get.attitude(player, event.target) > 0 && event.player.countCards("h") <= 3) {
        return true;
      }
      return false;
    },
    async content(event, trigger, player) {
      trigger.getParent().addCount = false;
      const stat = trigger.player.getStat("card");
      const name = trigger.card.name;
      if (typeof stat[name] === "number" && stat[name] > 0) {
        stat[name]--;
      }
      if (trigger.target?.isIn()) {
        await trigger.target.draw({ num: 1 });
        if (player === trigger.target) {
          return;
        }
        await trigger.target.chooseToGive({
          target: player,
          position: "he",
          prompt: `你可以交给${get.translation(player)}一张牌`,
          ai(card) {
            const { player: player2, target } = get.event();
            if (get.attitude(player2, target) > 0) {
              return 10 - get.value(card);
            }
            return 0;
          }
        }).set("target", player);
      }
    }
  },
  //关银屏
  ymsaxue: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player) {
      return game.countPlayer((current) => !current.isLinked()) > 1;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "横置两名角色",
        filterTarget(card, player2, target) {
          return !target.isLinked();
        },
        selectTarget: 2,
        ai(target) {
          const player2 = get.player();
          if (target === player2) {
            return 1;
          }
          return -get.attitude(player2, target);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const targets = event.targets.sortBySeat();
      await game.doAsyncInOrder(targets, async (target) => {
        await target.link(true);
      });
      let result;
      if (game.hasPlayer((current) => current !== player)) {
        result = await player.chooseTarget({
          prompt: "洒血：选择一名其他角色对你和其造成共计2点火焰伤害",
          filterTarget: lib.filter.notMe,
          forced: true,
          ai(target) {
            return -get.attitude(get.player(), target) * (target.isLinked() ? 2 : 0);
          }
        }).forResult();
        if (result?.bool && result.targets?.length) {
          const target = result.targets[0];
          player.line(target);
          const func = () => {
            const event2 = get.event();
            const controls = [
              (link) => {
                const targets2 = game.filterPlayer();
                if (targets2.length) {
                  for (const target2 of targets2) {
                    target2.classList.remove("selectable");
                    target2.classList.remove("selected");
                    const counterNode = target2.querySelector(".caption");
                    if (counterNode) {
                      counterNode.childNodes[0].innerHTML = ``;
                    }
                  }
                  ui.selected.targets.length = 0;
                  game.check();
                }
                return;
              }
            ];
            event2.controls = [ui.create.control(controls.concat(["清除选择", "stayleft"]))];
          };
          if (event.isMine()) {
            func();
          } else if (event.isOnline()) {
            event.player.send(func);
          }
          const num = 2;
          result = await target.chooseTarget({
            prompt: `${get.translation(player)}对你发动了洒血：请分配2点火焰伤害`,
            filterTarget(card, player2, target2) {
              return target2 === player2 || target2 === get.event().targetx;
            },
            selectTarget: 2,
            ai(target2) {
              const player2 = get.player();
              return get.damageEffect(target2, player2, player2, "fire");
            },
            forced: true,
            custom: {
              add: {
                confirm(bool) {
                  if (bool !== true) {
                    return;
                  }
                  const event2 = get.event().parent;
                  if (event2.controls) {
                    event2.controls.forEach((i) => {
                      if (i.innerText === "清除选择") {
                        i.custom();
                      }
                      i.close();
                    });
                  }
                  if (ui.confirm) {
                    ui.confirm.close();
                  }
                  game.uncheck();
                },
                target() {
                  if (ui.selected.targets.length) {
                    return;
                  }
                  const targets2 = game.filterPlayer();
                  if (targets2.length) {
                    for (const target2 of targets2) {
                      const counterNode = target2.querySelector(".caption");
                      if (counterNode) {
                        counterNode.childNodes[0].innerHTML = ``;
                      }
                    }
                  }
                  if (!ui.selected.targets.length) {
                    const evt = event.parent;
                    if (evt.controls) {
                      evt.controls[0].classList.add("disabled");
                    }
                  }
                }
              },
              replace: {
                target(target2) {
                  const event2 = get.event();
                  const sum = get.event().sum;
                  if (!event2.isMine()) {
                    return;
                  }
                  if (target2.classList.contains("selectable") === false) {
                    return;
                  }
                  if (ui.selected.targets.length >= sum) {
                    return false;
                  }
                  target2.classList.add("selected");
                  ui.selected.targets.push(target2);
                  let counterNode = target2.querySelector(".caption");
                  const count = ui.selected.targets.filter((i) => i === target2).length;
                  if (counterNode) {
                    counterNode = counterNode.childNodes[0];
                    counterNode.innerHTML = `×${count}`;
                  } else {
                    counterNode = ui.create.caption(`<span style="font-size:24px; font-family:xinwei; text-shadow:#FFF 0 0 4px, #FFF 0 0 4px, rgba(74,29,1,1) 0 0 3px;">×${count}</span>`, target2);
                    counterNode.style.right = "30px";
                    counterNode.style.bottom = "15px";
                  }
                  const evt = event2.parent;
                  if (evt.controls) {
                    evt.controls[0].classList.remove("disabled");
                  }
                  game.check();
                }
              }
            }
          }).set("targetx", player).set("sum", num).forResult();
          if (result?.bool && result.targets?.length) {
            if (!event.isMine()) {
              await game.delay();
            }
            const targets2 = result.targets.sortBySeat();
            for (const i of [player, target]) {
              if (targets2.includes(i)) {
                i.classList.remove("selectable");
                i.classList.remove("selected");
                const counterNode = i.querySelector(".caption");
                if (counterNode) {
                  counterNode.childNodes[0].innerHTML = ``;
                }
                await i.damage({ source: target, nature: "fire", num: targets2.filter((y) => y === i).length });
              }
            }
          }
        }
      }
    }
  },
  ymxianfeng: {
    audio: 2,
    trigger: { player: "damageEnd" },
    filter(event, player) {
      return event.source?.isIn() && event.source.hasDiscardableCards(player, "he");
    },
    getIndex: (event) => event.num,
    async cost(event, trigger, player) {
      const target = trigger.source;
      const list = [event.skill, target];
      event.result = await player.discardPlayerCard({
        target,
        prompt: get.prompt(...list),
        prompt2: "弃置其一张牌，若此牌花色为本回合首次进入弃牌堆，你获得之",
        position: "he",
        ai(button) {
          if (!get.event().att) {
            return 0;
          }
          const card = button.link;
          if (get.position(card) !== "e") {
            return 1;
          }
          return get.value(card) * (get.subtypes(card).includes("equip2") ? 5 : 1);
        }
      }).set("att", get.attitude(player, target) < 0).set("logSkill", list).forResult();
    },
    popup: false,
    logTarget: "source",
    async content(event, trigger, player) {
      const {
        cards: [card]
      } = event;
      const suit = get.suit(card);
      let num = 0;
      game.getGlobalHistory("cardMove", (evt) => {
        if (evt.name !== "lose" && evt.name !== "cardsDiscard") {
          return false;
        }
        if (evt.name === "lose" && evt.position !== ui.discardPile) {
          return false;
        }
        if (evt === trigger || evt.getParent() === trigger) {
          return false;
        }
        if (evt.cards?.some((card2) => get.suit(card2) === suit)) {
          num++;
        }
      });
      if (num === 1) {
        await player.gain({ cards: [card], animate: "gain2" });
      }
    }
  },
  //赵襄
  ymqianling: {
    audio: 2,
    enable: "phaseUse",
    usable: 2,
    filter(event, player) {
      return player.hasCards("h") && game.hasPlayer((current) => get.info("ymqianling").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return player !== target && target.hasCards("h");
    },
    selectTarget: [1, 3],
    multiline: true,
    multitarget: true,
    async content(event, trigger, player) {
      const targets = event.targets.concat(player).filter((i) => i.hasCards("h"));
      targets.sortBySeat();
      const cards2 = targets.map((target) => target.getCards("h")).flat();
      await player.showCards(cards2, `${get.translation(player)}发动了【潜聆】`).set("customButton", (button) => {
        const target = get.owner(button.link);
        if (target) {
          game.createButtonCardsetion(`${target.getName(true)}`, button);
        }
      }).set("delay_time", 4).set("multipleShow", true);
      if (targets.some((target) => target !== player && target.countCards("h", (card) => get.color(card) === "black") === player.countCards("h", (card) => get.color(card) === "black"))) {
        const result = await player.chooseTarget({
          prompt: "潜聆：你可以与一名黑色手牌数与你相同的其他角色交换手牌",
          filterTarget(card, player2, target) {
            return target !== player2 && get.event().targets.includes(target) && target.countCards("h", (card2) => get.color(card2) === "black") === player2.countCards("h", (card2) => get.color(card2) === "black");
          },
          ai(target) {
            const player2 = get.player();
            if (get.attitude(player2, target) < 0) {
              return target.getCards("h").reduce((a, b) => a + get.value(b), 0) > player2.getCards("h").reduce((a, b) => a + get.value(b), 0);
            }
            return 0;
          }
        }).set("targets", targets).forResult();
        if (result?.bool && result.targets?.length) {
          await player.swapHandcards(result.targets[0]);
        }
      }
    },
    ai: {
      order: 1,
      result: {
        player: 1,
        target(player, target) {
          return target.countCards("h");
        }
      }
    }
  },
  ymqueying: {
    audio: 2,
    enable: ["chooseToUse"],
    filter(event, player) {
      if (player.countCards("h") >= player.maxHp) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (!["sha", "shan"].includes(info[2])) {
          return false;
        }
        return event.filterCard(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3]
            },
            "unsure"
          ),
          player,
          event
        );
      }).length;
    },
    chooseButton: {
      dialog(event, player) {
        const list = get.inpileVCardList((info) => {
          if (!["sha", "shan"].includes(info[2])) {
            return false;
          }
          return event.filterCard(
            get.autoViewAs(
              {
                name: info[2],
                nature: info[3]
              },
              "unsure"
            ),
            player,
            event
          );
        });
        return ui.create.dialog("雀影", [list, "vcard"]);
      },
      filter(button, player) {
        return _status.event.getParent().filterCard(
          {
            name: button.link[2]
          },
          player,
          _status.event.getParent()
        );
      },
      check(button) {
        if (get.event().getParent()?.type !== "phase") {
          return 1;
        }
        return get.player().getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player) {
        return {
          filterCard: () => false,
          selectCard: -1,
          audio: "ymqueying",
          popname: true,
          viewAs: {
            name: links[0][2],
            nature: links[0][3]
          },
          log: false,
          async precontent(event, trigger, player2) {
            player2.logSkill("ymqueying");
            await player2.gain({ cards: lib.card.ying.getYing(1), animate: "gain2" });
          }
        };
      },
      prompt(links, player) {
        return `获得一张【影】然后视为使用一张${get.translation(links[0][3]) || ""}${get.translation(links[0][2])}`;
      }
    },
    hiddenCard(player, name) {
      if (!lib.inpile.includes(name)) {
        return false;
      }
      return ["sha", "shan"].includes(name) && player.countCards("h") < player.maxHp;
    },
    ai: {
      order: 7,
      fireAttack: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player) {
        if (player.countCards("h") >= player.maxHp) {
          return false;
        }
      },
      result: {
        player: 1
      }
    }
  },
  //梦貂蝉
  ymdiyu: {
    audio: 2,
    trigger: { global: "roundStart" },
    derivation: ["benghuai", "tongji", "olbihun", "dcyiju"],
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "令一名角色本轮伤害+1",
        ai(target) {
          const player2 = get.player();
          if (player2.getStorage("ymdiyu_gived").length > 3) {
            return get.attitude(player2, target) * Math.max(1, target.countCards("h"));
          }
          if (player2.hasCards("hs", (card) => get.name(card) === "shan")) {
            return -get.attitude(player2, target);
          }
          return 0;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      target.addTempSkill(`${event.name}_dam`, "roundStart");
      target.addMark(`${event.name}_dam`, 1, false);
      const skills2 = get.info(event.name).derivation.slice().removeArray(player.getStorage(`${event.name}_gived`));
      if (skills2.length) {
        const list = [];
        for (const skill of skills2) {
          list.push([skill, `<div class="popup text" style="width:calc(100% - 10px);display:inline-block"><div class="skill">【${get.translation(skill)}】</div><div>${lib.translate[`${skill}_info`]}</div></div>`]);
        }
        const result = skills2.length > 1 ? await player.chooseButton({
          createDialog: [`令${get.translation(target)}获得一个技能`, [list, "textbutton"]],
          forced: true,
          ai(button) {
            const { player: player2, skills: skills3, target: target2 } = get.event();
            if (get.attitude(player2, target2) > 0 && skills3.containsSome("benghuai", "tongji")) {
              return button.link === ["benghuai", "tongji"].removeArray(skills3).randomGet();
            }
            return button.link === skills3.randomGet();
          }
        }).set("skills", skills2).set("target", target).forResult() : { bool: true, links: skills2 };
        if (result?.bool && result.links?.length) {
          const skill = result.links[0];
          await target.addSkills(skill);
          player.addSkill(`${event.name}_gived`);
          player.markAuto(`${event.name}_gived`, [skill]);
        }
      }
    },
    subSkill: {
      gived: {
        charlotte: true,
        onremove: true,
        intro: {
          content(storage, player) {
            return `已选择过：${storage.map((skill) => get.poptip(skill)).join("、")}`;
          }
        }
      },
      dam: {
        charlotte: true,
        forced: true,
        mark: true,
        onremove: true,
        intro: { content: "本轮造成伤害+#" },
        trigger: { source: "damageBegin2" },
        filter(event, player) {
          return player.hasMark("ymdiyu_dam");
        },
        async content(event, trigger, player) {
          trigger.num += player.countMark(event.name);
        }
      }
    }
  },
  ymfuyi: {
    audio: 2,
    trigger: { target: "useCardToTarget" },
    filter(event, player) {
      return event.card.name === "sha" && game.hasPlayer((current) => lib.filter.targetEnabled2(event.card, event.player, current) && !event.targets.includes(current));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: `选择一名角色也成为${get.translation(trigger.card)}的目标`,
        filterTarget(crd, player2, target) {
          return lib.filter.targetEnabled2(get.event().card, get.event().targetx, target) && !get.event().targets.includes(target);
        },
        ai(target) {
          const { player: player2, targetx, card } = get.event();
          return get.effect(target, card, targetx, player2);
        }
      }).set("card", trigger.card).set("targetx", trigger.player).set("targets", trigger.targets).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      trigger.targets.add(target);
      await game.asyncDraw(trigger.targets);
      player.when({ global: "shaMiss" }).filter((evt) => evt.player === trigger.player && evt.card === trigger.card).step(async (event2, trigger2, player2) => {
        const targets = trigger.targets;
        const index = targets.indexOf(trigger2.target);
        if (index !== -1) {
          const targetx = targets.slice(index + 1, targets.length);
          trigger.getParent().excluded.addArray(targetx);
          game.log(trigger.card, "对", targetx, "无效");
        }
      });
    }
  },
  ymjiuji: {
    audio: 2,
    trigger: { global: "roundEnd" },
    filter(event, player) {
      return game.players.length > 1;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "选择两名角色依次执行一个仅能对对方使用牌且双方技能互换的额外回合",
        selectTarget: 2,
        ai(target) {
          return get.attitude(get.player(), target);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const targets = event.targets.sortBySeat();
      const target1 = targets[0];
      const target2 = targets[1];
      const skills1 = target1.getSkills(null, false, false).filter((skill) => {
        const info = get.info(skill);
        return info && !info.charlotte;
      });
      const skills2 = target2.getSkills(null, false, false).filter((skill) => {
        const info = get.info(skill);
        return info && !info.charlotte;
      });
      player.addTempSkill(`${event.name}_effect2`, "roundStart");
      player.setStorage(`${event.name}_effect2`, [target1, target2, skills1, skills2], true);
      target1.insertPhase();
      target2.insertPhase();
    },
    subSkill: {
      effect2: {
        charlotte: true,
        silent: true,
        lastDo: true,
        popup: false,
        onremove(player, skill) {
          const storage = player.storage[skill];
          if (storage?.length && player.storage[`${skill}2`]) {
            const [target1, target2, skills1, skills2] = storage;
            target1.changeSkills(skills1, skills2);
            target1.removeSkill("ymjiuji_effect");
            target2.changeSkills(skills2, skills1);
            target2.removeSkill("ymjiuji_effect");
          }
          delete player.storage[skill];
          delete player.storage[`${skill}2`];
        },
        trigger: {
          global: ["phaseBefore", "phaseAfter", "phaseCancelled"]
        },
        filter(event, player, name) {
          const storage = player.storage["ymjiuji_effect2"];
          if (storage?.length) {
            const [target1, target2, skills1, skills2] = storage;
            if (name === "phaseBefore") {
              return event.player === target1 && event.skill === "ymjiuji";
            } else if (name === "phaseCancelled") {
              return event.player === target2 && event.skill === "ymjiuji";
            }
            return event.player === target2 && event.skill === "ymjiuji" || !target2?.isIn();
          }
          return false;
        },
        async content(event, trigger, player) {
          const storage = player.storage["ymjiuji_effect2"];
          if (storage?.length) {
            const [target1, target2, skills1, skills2] = storage;
            const name = event.triggername;
            if (name === "phaseBefore") {
              player.setStorage(`${event.name}2`, true, true);
              target1.changeSkills(skills2, skills1);
              target1.addSkill("ymjiuji_effect");
              target1.markAuto("ymjiuji_effect", [target2]);
              if (target2?.isIn()) {
                target2.changeSkills(skills1, skills2);
                target2.addSkill("ymjiuji_effect");
                target2.markAuto("ymjiuji_effect", [target1]);
              }
            } else {
              player.removeSkill(event.name);
            }
          }
        }
      },
      effect: {
        charlotte: true,
        onremove: true,
        mark: true,
        intro: {
          content: "使用牌仅能指定$为目标"
        },
        mod: {
          playerEnabled(card, player, target) {
            if (!player.getStorage("ymjiuji_effect").includes(target)) {
              return false;
            }
          }
        }
      }
    }
  },
  //线下奶龙
  ymfriendzhugelianggongli: {
    audio: "friendzhugelianggongli",
    locked: true,
    ai: { combo: "ymfriendyance" }
  },
  ymfriendyance: {
    audio: ["friendyance1.mp3", "friendyance2.mp3", "friendyance3.mp3"],
    logAudio: () => ["friendyance1.mp3"],
    trigger: { global: "roundStart" },
    async content(event, trigger, player) {
      const bool = player.hasSkill("ymfriendzhugelianggongli") && get.info("friendgongli").isFriendOf(player, "friend_pangtong");
      let num = 5 + bool;
      await player.draw({ num });
      if (!player.hasCards("h")) {
        return;
      }
      num = Math.min(num, player.countCards("h"));
      const result = await player.chooseCard({
        prompt: `演策：将${get.cnNumber(num)}张手牌置于武将牌上（先选择的在上）`,
        selectCard: num,
        position: "h",
        ai(card) {
          return -player.getUseValue(card);
        },
        forced: true,
        filterOk() {
          return ui.selected.cards?.length === get.event().num;
        }
      }).set("num", num).forResult();
      if (result?.bool && result.cards?.length) {
        const cards2 = result.cards.reverse();
        player.addTempSkill(`${event.name}_yance`, "roundStart");
        await player.addToExpansion({ cards: cards2, source: player, animate: "give", gaintag: [`${event.name}_yance`] });
        await event.trigger("ymfriendyance_minigame");
      }
    },
    subSkill: {
      round: { charlotte: true },
      yance: {
        forced: true,
        charlotte: true,
        audio: "ymfriendyance",
        logAudio: () => ["friendyance2.mp3", "friendyance3.mp3"],
        onremove(player, skill) {
          delete player.storage[skill];
          const cards2 = player.getExpansions(skill);
          if (cards2.length) {
            player.loseToDiscardpile({ cards: cards2 });
          }
        },
        marktext: "策",
        intro: {
          name: "演策",
          markcount(storage, player) {
            return player.getExpansions("ymfriendyance_yance").length;
          },
          mark(dialog, content, player) {
            if (!player.storage.ymfriendyance_yance && !player.isUnderControl(true)) {
              return "天机可知却不可说...";
            }
            const cards2 = player.getExpansions("ymfriendyance_yance");
            if (cards2.length) {
              dialog.addAuto(cards2);
            } else {
              return "无演策牌";
            }
          }
        },
        trigger: { global: "useCard" },
        filter(event, player) {
          return player.hasExpansions("ymfriendyance_yance");
        },
        async content(event, trigger, player) {
          const cardx = player.getExpansions(event.name)[0];
          const card = trigger.card;
          await player.loseToDiscardpile({ cards: [cardx] });
          const bool = player.hasSkill("ymfriendzhugelianggongli") && get.info("friendgongli").isFriendOf(player, "friend_xushu") && !player.hasSkill("ymfriendyance_round");
          player.addTempSkill("ymfriendyance_round", "roundStart");
          let num = 0;
          if (get.suit(card) === get.suit(cardx)) {
            num++;
          }
          if (get.type2(card) === get.type2(cardx)) {
            num++;
          }
          if (num < 2 && bool) {
            num++;
          }
          if (player.storage[event.name]) {
            num *= 2;
          }
          if (num > 0) {
            player.popup("洗具");
            await player.draw({ num });
          } else {
            player.popup("杯具");
          }
          if (!player.hasExpansions("ymfriendyance_yance")) {
            player.removeSkill(event.name);
          }
        },
        mod: {
          aiOrder(player, card, num) {
            const cards2 = player.getExpansions("ymfriendyance_yance");
            if (!cards2.length || num <= 0) {
              return;
            }
            const bool = player.hasSkill("ymfriendzhugelianggongli") && get.info("friendgongli").isFriendOf(player, "friend_xushu") && !player.hasSkill("ymfriendyance_round");
            if (bool) {
              return;
            }
            const cardx = cards2[0];
            if (get.suit(card) === get.suit(cardx) || get.type2(card) === get.type2(cardx)) {
              return num += 100;
            }
          }
        }
      }
    }
  },
  ymfriendfangqiu: {
    audio: ["friendfangqiu1.mp3", "friendfangqiu2.mp3"],
    limited: true,
    skillAnimation: true,
    animationColor: "wood",
    trigger: { player: "ymfriendyance_minigame" },
    filter(event, player) {
      return player.hasExpansions("ymfriendyance_yance");
    },
    check(event, player) {
      const target = game.findPlayer((target2) => target2.getSeatNum() === 1);
      if (target?.isIn()) {
        return get.attitude(player, target) > 0;
      }
      return 1;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.storage.ymfriendyance_yance = true;
      const cards2 = player.getExpansions("ymfriendyance_yance");
      if (cards2.length) {
        await player.showCards(cards2, `${get.translation(player)}发动了【${get.translation(event.name)}】`);
      }
    },
    ai: { combo: "ymfriendyance" }
  }
};
const skills$8 = {
  //风云际会曹操
  psjuebing: {
    enable: "chooseToUse",
    filter(event, player) {
      return player.hasCard((card) => get.name(card) != "sha", "hs");
    },
    viewAs: { name: "sha", storage: { psjuebing: true } },
    filterCard(card, player) {
      return get.name(card) != "sha";
    },
    position: "hs",
    prompt: "将一张非【杀】手牌当做【杀】使用",
    check(card) {
      if (get.player().hasUseTarget(card)) {
        return 1;
      }
      return 6 - get.value(card);
    },
    async precontent(event, trigger, player) {
      player.addTempSkill("psjuebing_effect");
    },
    subSkill: {
      effect: {
        getList(event, target) {
          const list = event.cards.filterInD();
          const respondEvts = target.getHistory("useCard").filter((i) => i.respondTo?.[1] === event.card);
          if (respondEvts.length) {
            list.addArray(respondEvts.flatMap((evt) => evt.cards.filterInD("d")));
          }
          return list;
        },
        charlotte: true,
        trigger: {
          player: ["useCardAfter", "useCardToPlayered"],
          source: "damageSource"
        },
        filter(event, player, name) {
          if (!event.card?.storage?.psjuebing) {
            return false;
          }
          if (["damageSource", "useCardToPlayered"].includes(name)) {
            return true;
          }
          if (!event.targets?.length || event.targets.length != 1) {
            return false;
          }
          const [target] = event.targets;
          const list = get.info("psjuebing_effect").getList(event, target);
          return list.some((card) => [player, target].some((current) => current.hasUseTarget(card)));
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          if (event.triggername == "damageSource") {
            const evt = trigger.getParent(2);
            if (evt.addCount !== false) {
              evt.addCount = false;
              const stat = player.getStat().card, name = trigger.card.name;
              if (typeof stat[name] == "number") {
                stat[name]--;
              }
              game.log(trigger.card, "不计入次数限制");
            }
          } else if (event.triggername == "useCardAfter") {
            const [target] = trigger.targets;
            let list = get.info(event.name).getList(trigger, target);
            for (const current of [player, target].sortBySeat()) {
              if (!current.isIn() || !list.length) {
                continue;
              }
              const result = await current.chooseButton(["谲兵：你可以使用其中一张牌", list]).set("filterButton", (button) => {
                const player2 = get.player();
                return player2.hasUseTarget(button.link);
              }).set("ai", (button) => {
                const player2 = get.player();
                return player2.getUseValue(button.link);
              }).forResult();
              if (result?.links?.length) {
                list.removeArray(result.links);
                await current.chooseUseTarget(result.links[0], true, false);
              }
            }
          } else {
            const { target } = trigger;
            target.addTempSkill("psjuebing_block");
            target.markAuto("psjuebing_block", [trigger.card]);
          }
        }
      },
      block: {
        charlotte: true,
        onremove: true,
        mod: {
          cardEnabled(card, player) {
            if (!player.storage.psjuebing_block) {
              return;
            }
            const storage = player.getStorage("psjuebing_block");
            let evt = get.event();
            if (evt.name != "chooseToUse") {
              evt = evt.getParent("chooseToUse");
            }
            if (!evt?.respondTo || !storage.some((i) => i.cardid == evt.respondTo[1].cardid)) {
              return;
            }
            if (get.number(card) !== "unsure" && (!card.cards?.length || card.cards?.length != 1 || card.cards?.some((cardx) => {
              if (get.itemtype(cardx) != "card") {
                return false;
              }
              return get.name(cardx) == "shan" || !["h", "s"].includes(get.position(cardx));
            }))) {
              return false;
            }
          }
        },
        trigger: {
          player: ["damageBefore", "damageCancelled", "damageZero"],
          target: ["shaMiss", "useCardToExcluded", "useCardToEnd"],
          global: ["useCardEnd"]
        },
        filter(event, player) {
          const evt = event.getParent("useCard", true, true);
          if (evt && evt.effectedCount < evt.effectCount) {
            return false;
          }
          if (!event.card || !player.storage.psjuebing_block) {
            return false;
          }
          return player.getStorage("psjuebing_block").includes(event.card);
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player) {
          player.unmarkAuto(event.name, [trigger.card]);
          if (!player.getStorage(event.name).length) {
            player.removeSkill(event.name);
          }
        },
        group: "psjuebing_shan"
      },
      shan: {
        charlotte: true,
        enable: "chooseToUse",
        filter(event, player) {
          if (!player.hasCard((card) => get.name(card) != "shan", "hs")) {
            return false;
          }
          return event.respondTo && player.getStorage("psjuebing_block").some((i) => i.cardid == event.respondTo[1].cardid);
        },
        viewAs: { name: "shan" },
        filterCard(card, player) {
          return get.name(card) != "shan";
        },
        position: "hs",
        prompt: "将一张非【闪】手牌当做【闪】使用",
        check(card) {
          if (get.player().hasUseTarget(card)) {
            return 1;
          }
          return 6 - get.value(card);
        }
      }
    }
  },
  psfengxie: {
    enable: "phaseUse",
    filter(card, player) {
      return game.hasPlayer((current) => player != current);
    },
    filterTarget: lib.filter.notMe,
    limited: true,
    skillAnimation: true,
    animationColor: "gray",
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const { target } = event;
      for (const current of game.filterPlayer((current2) => target != current2).sortBySeat()) {
        const cards2 = current.getCards("e");
        if (!current.isIn() || !cards2.length) {
          continue;
        }
        player.line(current);
        const result = cards2.length == 1 ? { bool: true, links: cards2 } : await player.choosePlayerCard(current, "e", true, `选择${get.translation(current)}装备区的一张牌，如果${get.translation(target)}可以装备，则其装备之，否则你获得之`).set("ai", (button) => {
          const player2 = get.player();
          const { target: target2 } = get.event().getParent();
          const { link } = button;
          const att = get.attitude(player2, target2);
          let eff = get.effect(target2, link, player2, player2);
          if (eff < 0) {
            if (att > 0) {
              return 0;
            } else {
              eff += 4;
            }
          }
          if (att > 0 && target2.canEquip(link)) {
            eff += 4;
          }
          return eff;
        }).forResult();
        if (result?.bool && result?.links?.length) {
          const [card] = result.links;
          if (target.canEquip(card)) {
            const owner = get.owner(card);
            if (owner) {
              owner.$give(card, target, false);
            }
            await game.delay(0.5);
            await target.equip(card);
          } else {
            await player.gain(card, "gain2");
          }
        }
      }
      const targets = game.filterPlayer((current) => current.identity == "mingzhong" && ["dongcha", "sheshen"].some((skill) => current.hasSkill(skill)));
      if (targets.length) {
        for (const target2 of targets.sortBySeat()) {
          if (!target2.isIn()) {
            continue;
          }
          const skills2 = ["dongcha", "sheshen"].filter((skill) => target2.hasSkill(skill));
          await target2.removeSkills(skills2);
          await player.addSkills(skills2);
        }
      }
    },
    derivation: ["dongcha", "sheshen"],
    ai: {
      order(item, player) {
        const list = game.filterPlayer((current) => current != player);
        const friends = list.filter((current) => get.attitude(player, current) > 0);
        const enemies = list.filter((current) => get.attitude(player, current) < 0);
        if (friends.some((current) => {
          const num = enemies.filter((current2) => current2 != current && current2.hasCard((card) => current.canEquip(card) || get.value(card, player) > 2, "e")).length;
          return num > 1;
        })) {
          return 10;
        }
        if (enemies.some((current) => {
          const num = enemies.filter((current2) => current2 != current && current2.hasCard((card) => !current.canEquip(card) || get.value(card, player) > 2, "e")).length;
          return num > 1;
        })) {
          return 10;
        }
        return 0;
      },
      result: {
        target(player, target) {
          const enemies = game.filterPlayer((current) => current != player && get.attitude(player, current) < 0);
          const att = get.attitude(player, target);
          if (att > 0) {
            const num = enemies.filter((current) => current != target && current.hasCard((card) => target.canEquip(card) || get.value(card, player) > 2, "e")).length;
            return num;
          } else if (att < 0) {
            const num = enemies.filter((current) => current != target && current.hasCard((card) => !target.canEquip(card), "e")).length;
            return -num;
          } else {
            return 0;
          }
        }
      }
    }
  },
  //风云际会刘备
  pshuji: {
    trigger: { global: "roundStart" },
    filter(event, player) {
      return game.hasPlayer((current) => {
        if (current == player) {
          return false;
        }
        return !player.inRange(current) && !current.inRange(player);
      });
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => {
        if (target == player2) {
          return false;
        }
        return !player2.inRange(target) && !target.inRange(player2);
      }).set("ai", (target) => {
        const player2 = get.player();
        return -get.attitude(player2, target) * (1 + target.countCards("h"));
      }).forResult();
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      if (target.countCards("h")) {
        const result = target.countCards("h") == 1 ? { bool: true, cards: target.getCards("h") } : await target.chooseCard(true, "h", `选择一张手牌赠予${get.translation(player)}`).forResult();
        if (result?.bool && result?.cards?.length) {
          await target.gift(result.cards, player);
        }
      }
      const effect = `${event.name}_effect`;
      player.addTempSkill(effect, "roundStart");
      target.addTempSkill(effect, "roundStart");
      player.markAuto(effect, [target]);
      target.markAuto(effect, [player]);
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        trigger: { global: "phaseEnd" },
        filter(event, player) {
          return player.countDiscardableCards(player, "h") > 1 && game.hasPlayer((current) => player.inRange(current) && player.getStorage("pshuji_effect").includes(current));
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          for (const target of game.filterPlayer((current) => player.inRange(current) && player.getStorage(event.name).includes(current)).sortBySeat()) {
            if (!target.isIn() || player.countDiscardableCards(player, "h") < 2) {
              continue;
            }
            await player.chooseToDiscard("h", 2, true);
            await target.damage();
          }
        },
        intro: { content: "已和$陷入猜忌之中" }
      }
    }
  },
  pshoufa: {
    trigger: { source: "damageBegin1" },
    filter(event, player) {
      return player.countCards("h") < event.player.maxHp && player.getSeatNum() > event.player.getSeatNum();
    },
    usable: 1,
    async content(event, trigger, player) {
      await player.drawTo(trigger.player.maxHp);
    }
  },
  //风云际会孙权
  pszhanlun: {
    trigger: { player: "yingbian" },
    filter(event, player) {
      return event.card.name == "sha" && lib.yingbian.condition.complex.has("zhuzhan");
    },
    forced: true,
    async content(event, trigger, player) {
      const next = lib.yingbian.condition.complex.get("zhuzhan")(trigger);
      await next;
      const { zhuzhanresult2, result } = next;
      if (result?.bool) {
        if (trigger.addCount !== false) {
          trigger.addCount = false;
          const stat = player.getStat().card, name = trigger.card.name;
          if (typeof stat[name] == "number") {
            stat[name]--;
          }
          game.log(trigger.card, "不计入次数限制");
        }
        player.addTempSkill(event.name + "_effect");
        player.markAuto(event.name + "_effect", [[trigger.card, zhuzhanresult2.cards[0]]]);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        trigger: { player: "useCardAfter" },
        filter(event, player) {
          return player.getStorage("pszhanlun_effect").some(([card, cards2]) => card == event.card && ["red", "black"].includes(get.color(cards2)));
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          const card = player.getStorage(event.name).filter((list) => list[0] == trigger.card)[0][1];
          if (get.color(card) == "black") {
            player.addTempSkill("pszhanlun_damage");
            player.addMark("pszhanlun_damage", 1, false);
            game.log(player, "本回合下一张【杀】造成的伤害", "#g+1");
          } else {
            const targets = game.filterPlayer((current) => current.hasHistory("lose", (evt) => evt.getParent(2).name == "yingbianZhuzhan"));
            player.tempBanSkill("pszhanlun");
            await game.asyncDraw([player].addArray(targets).sortBySeat(), 2);
          }
        }
      },
      damage: {
        charlotte: true,
        onremove: true,
        trigger: { source: "damageBegin1" },
        filter(event, player) {
          return event.card?.name === "sha";
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          trigger.num += player.countMark(event.name);
          player.removeSkill(event.name);
        },
        intro: { content: "本回合下一张【杀】造成的伤害+#" }
      }
    }
  },
  psjueyi: {
    trigger: { player: "phaseUseBegin" },
    filter(event, player) {
      return player.hasCard(lib.filter.cardRecastable, "he");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCard("he", get.prompt2(event.skill), [1, 2], lib.filter.cardRecastable).set("ai", (card) => {
        const { player: player2, list } = get.event();
        if (!list.length) {
          return 6 - get.value(card);
        }
        const max = list.filter((i) => i[1] == list[0][1]).map((i) => i[0]);
        let val = 6;
        if (!ui.selected.cards.length) {
          if (get.suit(card) == max && player2.needsToDiscard()) {
            val += 4;
          }
          if (player2.hasSkill("pszhanlun") && player2.hasValueTarget({ name: "sha" }) && get.name(card) != "sha" && game.hasPlayer((current) => {
            return current != player2 && get.attitude(player2, current) > 0 && current.hasCard((cardx) => get.suit(cardx) !== get.suit(card) && get.type(cardx) == "basic", "h");
          })) {
            val += 6;
          }
          return val - get.value(card);
        }
        if (get.suit(card) !== get.suit(ui.selected.cards[0])) {
          val += 2;
        }
        return val - get.value(card);
      }).set(
        "list",
        (() => {
          const cards2 = player.getCards("h"), map = {};
          if (!cards2.length) {
            return [];
          }
          for (const card of cards2) {
            const type = get.suit(card);
            if (typeof map[type] != "number") {
              map[type] = 0;
            }
            map[type]++;
          }
          const list = [];
          for (let i in map) {
            if (map[i] > 0) {
              list.push([i, map[i]]);
            }
          }
          list.sort((a, b) => b[1] - a[1]);
          return list;
        })()
      ).forResult();
    },
    async content(event, trigger, player) {
      const { cards: cards2 } = event;
      await player.recast(cards2);
      const suits = cards2.map((card) => get.suit(card)).toUniqued();
      for (const target of game.filterPlayer()) {
        target.addTempSkill(event.name + "_effect");
        target.markAuto(event.name + "_effect", suits);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        mod: {
          cardDiscardable(card, player) {
            if (player.getStorage("psjueyi_effect").includes(get.suit(card))) {
              return false;
            }
          }
        },
        intro: { content: "本回合内不能弃置$的牌直到有角色进入濒死状态" },
        trigger: { global: "dying" },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          player.removeSkill(event.name);
        }
      }
    }
  }
};
const skills$7 = {
  //徐兖纵横
  //吕布
  xy_xiaoxi: {
    trigger: {
      global: "phaseBefore",
      player: ["enterGame", "phaseDrawBegin1"]
    },
    filter(event, player) {
      if (event.name === "phaseDraw") {
        return !event.numFixed;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    forced: true,
    locked: false,
    onremove: true,
    async content(event, trigger, player) {
      if (trigger.name === "phaseDraw") {
        trigger.num = player.countMark(event.name);
        trigger.numFixed = true;
        player.removeMark(event.name, 1);
      } else {
        player.addMark(event.name, 7);
      }
    },
    marktext: "势",
    intro: {
      name: "势",
      content: "mark"
    }
  },
  xy_fenqi: {
    enable: "phaseUse",
    filter(event, player) {
      return !player.hasSkill("xy_fenqi_used") && player.hasMark("xy_xiaoxi");
    },
    manualConfirm: true,
    async content(event, trigger, player) {
      player.addTempSkill("xy_fenqi_used");
      player.removeMark("xy_xiaoxi", 1);
      const args = [
        ["diamond", 13],
        ["club", 12],
        ["heart", 2],
        ["spade", 1]
      ].randomGet();
      const card = game.createCard2("yiguzuoqi", ...args);
      if (!lib.inpile.includes("yiguzuoqi")) {
        game.broadcastAll(() => {
          lib.inpile.add("yiguzuoqi");
        });
      }
      await player.gain(card, "gain2");
    },
    subSkill: {
      used: {
        charlotte: true
      }
    },
    ai: {
      combo: "xy_xiaoxi",
      order: 11,
      result: {
        player(player) {
          const cards2 = player.getCards("hs", (card) => {
            if (get.name(card) !== "sha") {
              return false;
            }
            return player.hasValueTarget(card, false);
          });
          if (cards2.length >= player.getCardUsable("sha", true)) {
            return 1;
          }
          return 0;
        }
      }
    }
  },
  yiguzuoqi_effect: {
    trigger: {
      player: "useCard"
    },
    charlotte: true,
    forced: true,
    popup: false,
    async content(event, trigger, player) {
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        const stat = player.getStat().card;
        const name = trigger.card.name;
        if (typeof stat[name] === "number" && stat[name] > 0) {
          stat[name]--;
        }
      }
    },
    mod: {
      cardUsable: () => Infinity,
      targetInRange: () => true
    }
  },
  //张邈
  xy_mouni: {
    audio: "mouni",
    trigger: {
      player: "phaseBegin"
    },
    filter(event, player) {
      return game.hasPlayer((current) => current !== player && current.countCards("h"));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), (card, player2, target) => {
        return player2 !== target && target.countCards("h");
      }).set("ai", (target) => {
        const player2 = get.player();
        return -get.attitude(player2, target) * target.countCards("h") / Math.max(1, target.getHp());
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const { cards: cards2 } = await target.showHandcards(`${get.translation(player)}发动了【谋逆】`).forResult();
      const shas = cards2.filter((card) => get.name(card) === "sha");
      const list = [];
      while (shas.length) {
        const card = shas.find((card2) => player.canUse(card2, target, false));
        if (!card) {
          break;
        }
        shas.remove(card);
        const next = player.useCard(card, target, false);
        list.push(next);
        await next;
        if (game.getGlobalHistory("everything", (evt) => {
          if (evt.name !== "dying" || evt.player !== target) {
            return false;
          }
          return evt.reason?.getParent(2) === next;
        }).length) {
          break;
        }
      }
      if (list.length && list.every((evt) => !player.hasHistory("sourceDamage", (evtx) => evtx.getParent(2) === evt))) {
        const evt = event.getParent("phase", true);
        if (evt) {
          game.log(player, "结束了回合");
          evt.num = evt.phaseList.length;
          evt.goto(11);
        }
      }
    }
  },
  xy_zongfan: {
    audio: "zongfan",
    juexingji: true,
    skillAnimation: true,
    animationColor: "metal",
    derivation: "xy_zhangu",
    trigger: {
      player: "phaseEnd"
    },
    filter(event, player) {
      if (!player.hasHistory("useSkill", (evt) => evt.skill === "xy_mouni")) {
        return false;
      }
      return game.hasGlobalHistory("everything", (evt) => evt.name === "dying");
    },
    forced: true,
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      let num = 0;
      if (player.countCards("he")) {
        const result = await player.chooseCardTarget({
          prompt: "纵反",
          prompt2: "将任意张牌交给一名其他角色，然后增加等量体力上限并恢复等量体力",
          forced: true,
          filterCard: true,
          selectCard: [1, Infinity],
          allowChooseAll: true,
          position: "he",
          filterTarget: lib.filter.notMe,
          ai1(card) {
            return 1;
          },
          ai2(target) {
            const player2 = get.player();
            return get.attitude(player2, target);
          }
        }).forResult();
        if (result.bool) {
          await player.give(result.cards, result.targets[0]);
          num = result.cards.length;
        }
      }
      if (num > 0) {
        await player.gainMaxHp(num);
      }
      await player.removeSkills("xy_mouni");
      if (num > 0) {
        await player.recover(num);
      }
      await player.addSkills("xy_zhangu");
    },
    ai: {
      combo: "xy_mouni"
    }
  },
  xy_zhangu: {
    audio: "zhangu",
    trigger: {
      player: "phaseBegin"
    },
    filter(event, player) {
      if (player.maxHp <= 1) {
        return false;
      }
      return ["h", "e"].some((pos) => !player.countCards(pos));
    },
    forced: true,
    async content(event, trigger, player) {
      await player.loseMaxHp();
      await player.draw(2);
    }
  },
  //曹嵩
  xy_lilu: {
    audio: "cslilu",
    trigger: {
      player: "phaseDrawBegin1"
    },
    filter(event, player) {
      return !event.numFixed;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard(get.prompt2(event.skill), [0, Infinity], "allowChooseAll", "he").set("ai", (card) => {
        const player2 = get.player();
        if (player2.countCards("h") > Math.min(5, player2.maxHp)) {
          return 0;
        }
        return 4 - get.value(card);
      }).set("chooseonly", true).forResult();
    },
    async content(event, trigger, player) {
      trigger.changeToZero();
      if (event.cards?.length) {
        await player.modedDiscard(event.cards);
      }
      const num = Math.min(5, player.maxHp);
      if (player.countCards("h") < num) {
        await player.drawTo(num);
      }
      if (!player.countCards("h")) {
        return;
      }
      let str = "将至少一张手牌交给一名其他角色";
      let numx = 0;
      let evts = player.getAllHistory("custom", (evt) => evt.skill === event.name);
      if (evts?.length) {
        numx = evts[evts.length - 1].num ?? 0;
      }
      if (numx < player.countCards("h")) {
        str = `${str}，${numx > 0 ? `若给出的牌数大于${get.cnNumber(numx)}张，你` : "然后"}增加1点体力上限并回复1点体力`;
      }
      const result = await player.chooseCardTarget({
        prompt: str,
        filterCard: true,
        filterTarget: lib.filter.notMe,
        selectCard: [1, Infinity],
        forced: true,
        ai1(card) {
          const { player: player2, goon } = get.event();
          if (ui.selected.cards.length < goon) {
            if (get.tag(card, "damage") && game.hasPlayer((current) => {
              current !== player2 && get.attitude(player2, current) > 0 && !current.hasSkillTag("nogain") && !current.hasJudge("lebu") && current.hasValueTarget(card);
            })) {
              return 1;
            }
            return 1 / Math.max(0.1, get.value(card));
          }
          return 0;
        },
        ai2(target2) {
          return Math.sqrt(5 - Math.min(4, target2.countCards("h"))) * get.attitude(get.player(), target2);
        },
        goon: (() => {
          if (!game.hasPlayer((current) => current !== player && get.attitude(player, current) > 0 && !current.hasSkillTag("nogain") && !current.hasJudge("lebu"))) {
            return 1;
          }
          if (num < player.countCards("h")) {
            return num + 1;
          }
          return 1;
        })(),
        allowChooseAll: true
      }).forResult();
      if (!result.bool) {
        return;
      }
      const {
        cards: cards2,
        targets: [target]
      } = result;
      await player.give(cards2, target);
      if (cards2.length > numx) {
        await player.gainMaxHp();
        await player.recover();
      }
      player.getHistory("custom").push({
        skill: event.name,
        num: cards2.length
      });
    }
  },
  xy_yizheng: {
    audio: "csyizheng",
    trigger: { player: "phaseJieshuBegin" },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill), lib.filter.notMe).set("ai", (target) => {
        if (target.isTurnedOver() || target.hasJudge("lebu")) {
          return 0;
        }
        return get.attitude(_status.event.player, target) * Math.max(0, target.countCards("h") - 2);
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const name = `${event.name}_effect`;
      player.markAuto(name, target);
      player.addTip(name, `翊正 ${get.translation(player.getStorage(name))}`);
      player.addTempSkill(name, { player: "phaseBegin" });
    },
    subSkill: {
      effect: {
        audio: "xy_yizheng",
        onremove(player, skill) {
          player.removeTip(skill);
          delete player.storage[skill];
        },
        charlotte: true,
        trigger: {
          global: ["recoverBegin", "damageBegin1"]
        },
        forced: true,
        logTarget(event) {
          return event.name === "damage" ? event.source : event.player;
        },
        filter(event, player) {
          const target = lib.skill.xy_yizheng_effect.logTarget(event);
          return player.getStorage("xy_yizheng_effect").includes(target);
        },
        async content(event, trigger, player) {
          await player.loseMaxHp();
          trigger.num++;
        }
      }
    }
  },
  //曹操
  xy_shengju: {
    forced: true,
    mod: {
      cardUsable(card, player, num) {
        if (get.name(card) === "sha") {
          return Math.min(
            5,
            game.countPlayer((current) => current.group === "wei")
          );
        }
      },
      attackRange(player, num) {
        return Math.min(
          5,
          game.countPlayer((current) => current.group === "wei")
        );
      }
    },
    trigger: { player: "phaseDrawBegin2" },
    filter(event, player) {
      return !event.numFixed;
    },
    async content(event, trigger, player) {
      trigger.num = Math.min(
        5,
        game.countPlayer((current) => current.group === "wei")
      );
    }
  },
  xy_mintong: {
    trigger: { global: "useCardAfter" },
    usable: 1,
    check(event, player) {
      return get.effect(event.player, { name: "sha" }, player, player) > 0;
    },
    filter(event, player) {
      if (!get.is.damageCard(event.card) || event.targets?.length !== 1) {
        return false;
      }
      if (event.targets[0].group !== "wei") {
        return false;
      }
      const card = get.autoViewAs({ name: "sha", isCard: true });
      return player.canUse(card, event.player, false);
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const sha = get.autoViewAs({ name: "sha", isCard: true });
      if (!player.canUse(sha, trigger.player, false)) {
        return;
      }
      player.useCard(sha, trigger.player, false);
    }
  },
  //程昱
  xy_liaofu: {
    enable: "phaseUse",
    trigger: { global: "useCard" },
    filterCard: (card, player, target) => {
      if (card.name !== "sha") {
        return false;
      }
      const cards2 = player.getExpansions("xy_liaofu");
      const natures = get.natureList(card);
      return cards2.every((cardx) => {
        const list = get.natureList(cardx);
        return list.length !== natures.length || !natures.containsAll(...list);
      });
    },
    position: "he",
    selectCard: 1,
    lose: false,
    discard: false,
    marktext: "伏",
    intro: {
      mark(dialog, storage, player) {
        const cards2 = player.getExpansions("xy_liaofu");
        if (player.isUnderControl(true)) {
          dialog.addAuto(cards2);
        } else {
          return `共有${get.cnNumber(cards2.length)}张牌`;
        }
      },
      markcount: "expansion"
    },
    onremove(player, skill) {
      const cards2 = player.getExpansions(skill);
      if (cards2.length) {
        player.loseToDiscardpile(cards2);
      }
    },
    filter(event, player) {
      if (event.name === "useCard") {
        if (player === _status.currentPhase || player === event.player || event.card.name !== "sha") {
          return false;
        }
        const natures = get.natureList(event.card);
        const cards2 = player.getExpansions("xy_liaofu");
        if (!cards2.length) {
          return false;
        }
        return cards2.some((cardx) => {
          const list = get.natureList(cardx);
          return list.length === natures.length && natures.containsAll(...list);
        });
      }
      return !player.hasSkill("xy_liaofu_used") && player.countCards("he", (card) => get.info("xy_liaofu").filterCard(card, player));
    },
    async cost(event, trigger, player) {
      const result = await player.chooseCardButton(`###${get.prompt(event.skill, trigger.player)}###弃置一张同属性杀并对其造成1点同属性伤害`, player.getExpansions(event.skill)).set("ai", (button) => {
        const player2 = get.player();
        const target = get.event().getTrigger().player;
        const natures = get.natureList(button.link).join(lib.natureSeparator);
        return get.damageEffect(target, player2, player2, natures);
      }).set("filterButton", (button) => {
        const event2 = get.event().getTrigger();
        const list = get.natureList(event2.card);
        const natures = get.natureList(button.link);
        return list.length === natures.length && list.containsAll(...natures);
      }).forResult();
      if (result?.bool) {
        event.result = {
          bool: true,
          targets: [trigger.player],
          cost_data: result.links
        };
      }
    },
    async content(event, trigger, player) {
      const { cards: cards2, name, cost_data } = event;
      if (cost_data?.length > 0) {
        await player.loseToDiscardpile(cost_data);
        await trigger.player.damage(1, get.natureList(cost_data[0]).join(lib.natureSeparator));
      } else {
        player.addTempSkill("xy_liaofu_used", { global: "phaseChange" });
        const next = player.addToExpansion(cards2, player, "giveAuto");
        next.gaintag.add(name);
        await next;
      }
    },
    ai: {
      order: 10,
      result: {
        player: 1,
        target: 1
      },
      fireAttack: true,
      skillTagFilter: (player, tag) => player.getExpansions("xy_liaofu").length > 0
    },
    subSkill: {
      used: {
        charlotte: true
      }
    }
  },
  xy_jinshou: {
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return game.getGlobalHistory("changeHp", (evt) => evt.player === player && evt.changedHp !== 0).length === 0;
    },
    check(event, player) {
      const cards2 = player.getCards("h");
      const num = cards2.reduce((num2, card) => {
        if (get.name(card) === "tao") {
          return num2 += 2;
        }
        {
          return num2 += 1;
        }
      }, 0);
      return num < player.getHandcardLimit();
    },
    prompt2: "是否弃置所有手牌并且直到你的下回合开始，其他角色不能对你使用单目标伤害牌",
    async content(event, trigger, player) {
      player.discard(player.getDiscardableCards(player, "h"));
      player.loseHp();
      player.addTempSkill("xy_jinshou_effect", { player: "phaseBegin" });
    },
    subSkill: {
      effect: {
        charlotte: true,
        mark: true,
        marktext: "烬守",
        intro: { content: "其他角色不能对你使用单目标伤害牌" },
        mod: {
          targetEnabled(card, player, target) {
            if (get.info(card).selectTarget === 1 && get.is.damageCard(card)) {
              return false;
            }
          }
        }
      }
    }
  },
  //荀彧
  xy_jianzu: {
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return target !== player && player.canCompare(target);
    },
    selectTarget: 1,
    filter(event, player) {
      return player.countCards("h") > 0 && game.hasPlayer((curr) => player.canCompare(curr));
    },
    async content(event, trigger, player) {
      const result = await player.chooseToCompare(event.targets[0]).forResult();
      if (result.tie) {
        return;
      }
      const win = result.bool ? player : event.targets[0];
      if (!game.hasPlayer((curr) => win.inRange(curr))) {
        win.chat("手短是会呼吸的痛");
        return;
      }
      const { targets } = await win.chooseTarget("请选择一名攻击范围内的角色对其造成1点伤害", true, 1).set("filterTarget", (card, player2, target) => {
        return player2.inRange(target);
      }).set("ai", (target) => {
        if (get.effect(target, { name: "damage" }, player, player) > 0) {
          return -get.attitude(player, target);
        }
      }).forResult();
      await targets[0].damage(win);
    },
    ai: {
      result: {
        player(player, target) {
          const list = player.getCards("h").map((card) => get.number(card));
          if (get.attitude(player, target) > 0) {
            return [1, 1];
          } else {
            let num = 0;
            for (const i of [7, 10, 13]) {
              if (Math.max(...list) >= i) {
                num++;
              }
            }
            if (num === 0) {
              return -10;
            }
            return [1, num];
          }
        },
        target(player, target) {
          const list = player.getCards("h").map((card) => get.number(card));
          if (get.attitude(player, target) > 0) {
            return [1, 1];
          } else {
            let num = 3;
            for (const i of [7, 10, 13]) {
              if (Math.max(...list) >= i) {
                num--;
              }
            }
            return [1, -num];
          }
        }
      }
    }
  },
  xy_dishou: {
    trigger: { player: "damageBegin4" },
    filter(event, player) {
      if (!event.source?.isIn()) {
        return false;
      }
      return event.source.countCards("h") !== event.source.getHp();
    },
    check(event, player) {
      if (get.attitude(player, event.source) > 0) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player) {
      const target = trigger.source;
      do {
        let result;
        const choiceList = ["弃置所有手牌", "失去1点体力"];
        if (!target.countDiscardableCards(target, "h")) {
          result = { index: 1 };
        } else {
          result = await target.chooseControl().set("choiceList", choiceList).set("ai", () => 1).forResult();
        }
        if (result?.index === 0) {
          await target.modedDiscard(target.getCards("h"));
        } else if (result?.index === 1) {
          await target.loseHp();
        }
      } while (target.countCards("h") !== target.getHp() && target.isIn() && !game.hasGlobalHistory("everything", (evt) => evt.name === "dying" && evt.player === target && evt.reason.getParent() === event));
    },
    ai: {
      effect: {
        target: (card, player, target) => {
          if (player.hasSkillTag("jueqing", false, target)) {
            return [1, -2];
          }
          if (card.cards?.length > 0) {
            return [1, -2];
          }
          if (get.tag(card, "damage") || card.name === "damage") {
            return [1, 10];
          }
        }
      }
    }
  },
  //陈宫
  xy_jizheng: {
    trigger: { player: "drawBegin" },
    usable: 1,
    filter(event, player) {
      return game.hasPlayer((curr) => curr.hasUseTarget({ name: "sha", isCard: true }, false));
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill)).set("ai", (target) => {
        const player2 = get.player();
        const card = new lib.element.VCard({ name: "sha", isCard: true });
        return target.getUseValue(card, false) * get.attitude(player2, target);
      }).forResult();
    },
    async content(event, trigger, player) {
      trigger.cancel();
      const target = event.targets[0];
      const card = new lib.element.VCard({ name: "sha", isCard: true });
      await target.chooseUseTarget(card, false, true, "nodistance");
    }
  },
  //张闿
  xy_luejin: {
    trigger: {
      global: "phaseDrawEnd"
    },
    check(event, player) {
      return get.effect(event.player, { name: "sha", isCard: true }, player, player) > 0;
    },
    filter(event, player) {
      let num = 0;
      event.player.getHistory("gain", (evt) => {
        if (evt.getParent()?.name !== "draw") {
          return false;
        }
        if (evt.getParent(event.name) !== event) {
          return false;
        }
        if (!evt?.cards?.length) {
          return false;
        }
        num += evt.cards.length;
      });
      return num > 2;
    },
    async content(event, trigger, player) {
      if (!player.canUse({ name: "sha", isCard: true }, trigger.player, false)) {
        return;
      }
      const next = player.useCard({ name: "sha", isCard: true }, trigger.player, false);
      await next;
      if (!trigger.player.hasHistory("damage", (evt) => evt.card === next.card)) {
        player.recover();
      }
    }
  }
};
const skills$6 = {
  //26版线下
  zc26_haoshi: {
    trigger: { player: "phaseDrawBegin2" },
    filter(event, player) {
      return !event.numFixed;
    },
    check(event, player) {
      const maxList = game.filterPlayer().map((current) => {
        let num = current.countCards("h");
        if (current === player) {
          num += event.num + 2;
        }
        return num;
      });
      const minList = game.filterPlayer((current) => current.isMinHandcard());
      let max = Math.max(...maxList);
      if (maxList.filter((i) => i === max).length > 1) {
        max = null;
      }
      if (!max) {
        return true;
      }
      if (minList.some((min) => get.attitude(player, min) > 0)) {
        return true;
      }
      return false;
    },
    async content(event, trigger, player) {
      trigger.num += 2;
      player.when({ player: "phaseDrawEnd" }).filter((evt) => evt === trigger).step(async (event2, trigger2, player2) => {
        const max = game.findPlayer((current) => current.isMaxHandcard(true));
        const minList = game.filterPlayer((current) => current.isMinHandcard());
        if (!max) {
          return;
        }
        let targets;
        if (minList.length === 1) {
          targets = minList;
        } else {
          targets = (await player2.chooseTarget(true, `好施：选择一名手牌最少的角色获得${get.translation(max)}的一半手牌（向下取整）`).set("filterTarget", (_, player3, target) => target.isMinHandcard()).set("ai", (target) => get.attitude(get.player(), target) * (target.getDamagedHp() + 1)).forResult()).targets;
        }
        if (!targets?.length) {
          return;
        }
        const min = targets[0];
        if (!max.countGainableCards(min, "h") || !Math.floor(max.countCards("h") / 2)) {
          return;
        }
        await max.chooseToGive(true, min, Math.floor(max.countCards("h") / 2));
      });
    }
  },
  zc26_dimeng: {
    usable: 1,
    enable: "phaseUse",
    filter(event, player) {
      return game.hasPlayer((current) => {
        if (current === player) {
          return false;
        }
        const num = current.countCards("h");
        return game.hasPlayer((current2) => {
          if (current2 === current || current2 === player) {
            return false;
          }
          return Math.abs(num - current2.countCards("h")) < 3;
        });
      });
    },
    selectTarget: 2,
    complexTarget: true,
    filterTarget(_, player, target) {
      if (target === player) {
        return false;
      }
      if (!ui.selected.targets.length) {
        return true;
      }
      return Math.abs(ui.selected.targets[0].countCards("h") - target.countCards("h")) < 3;
    },
    multitarget: true,
    multiline: true,
    async content(event, trigger, player) {
      const targets = event.targets.slice().sortBySeat(_status.currentPhase);
      while (targets.length) {
        let num = 3;
        const target = targets.shift();
        while (num > 0) {
          num--;
          if (!target.isIn()) {
            break;
          }
          const result = await target.chooseToUse().set("filterCard", (card, player2, event2) => {
            if (get.position(card) !== "h") {
              return false;
            }
            return lib.filter.filterCard.apply(this, [card, player2, event2]);
          }).forResult();
          if (!result?.bool) {
            break;
          }
        }
      }
      if (!event.targets.every((target) => target.isIn())) {
        return;
      }
      await event.targets[0].swapHandcards(event.targets[1]);
    },
    ai: {
      order: 10,
      threaten: 3,
      expose: 0.9,
      result: {
        target(player, target) {
          if (get.attitude(player, target) < 0) {
            return 0;
          }
          return (target.countCards("h") + 1) * get.sgnAttitude(player, target);
        }
      }
    }
  },
  zc26_qiaobian: {
    trigger: { global: "roundStart" },
    filter(event, player) {
      const lastTarget = get.info("zc26_qiaobian").getLastTarget(player);
      return game.hasPlayer((current) => current !== lastTarget);
    },
    async cost(event, trigger, player) {
      const lastTarget = get.info("zc26_qiaobian").getLastTarget(player);
      event.result = await player.chooseTarget(get.prompt2(event.skill), (_, player2, target) => target !== get.event().lastTarget).set("ai", (target) => get.attitude(get.player(), target) * target.countCards("h")).set("lastTarget", lastTarget).forResult();
    },
    async content(event, trigger, player) {
      await game.asyncDraw([player, ...event.targets].sortBySeat());
      player.setStorage("zc26_qiaobian_effect", event.targets[0], true);
      player.addTempSkill("zc26_qiaobian_effect", { global: "roundEnd" });
    },
    getLastTarget(player) {
      const historys = player.getRoundHistory("useSkill", (evt) => evt.skill === "zc26_qiaobian", 1);
      if (!historys.length) {
        return null;
      }
      return historys[0]?.targets?.[0];
    },
    subSkill: {
      effect: {
        charlotte: true,
        init(player, skill) {
          const target = player.getStorage(skill);
          if (target) {
            player.markSkillCharacter(skill, target, "巧变", `本轮指定${get.translation(target)}为目标`);
          }
        },
        onremove: true,
        trigger: { global: ["phaseJudgeBefore", "phaseDrawBefore", "phaseUseBefore", "phaseDiscardBefore"] },
        filter(event, player) {
          if (!player.countDiscardableCards(player, "he")) {
            return false;
          }
          return player.getStorage("zc26_qiaobian_effect") === event.player;
        },
        async cost(event, trigger, player) {
          let check;
          let str = `弃置一张手牌并跳过其${get.translation(trigger.name)}`;
          if (trigger.name === "phaseDraw") {
            str += "，然后其可以获得至多两名角色各一张手牌";
          }
          if (trigger.name === "phaseUse") {
            str += "，然后其可以移动场上的一张牌";
          }
          switch (trigger.name) {
            case "phaseJudge":
              check = trigger.player.countCards("j");
              break;
            case "phaseDraw": {
              let num = 0;
              let num2 = 0;
              const players = game.filterPlayer((current) => current !== trigger.player);
              for (const current of players) {
                let hs = current.countGainableCards(trigger.player, "h");
                if (current === player) {
                  hs--;
                }
                if (hs) {
                  const att = get.attitude(trigger.player, current);
                  if (att <= 0) {
                    num++;
                  }
                  if (att < 0) {
                    num2++;
                  }
                }
              }
              if (trigger.num < 2) {
                check = true;
              }
              check = num >= 2 && num2 > 0;
              break;
            }
            case "phaseUse":
              if (!trigger.player.canMoveCard(true)) {
                check = false;
              } else {
                check = game.hasPlayer((current) => get.attitude(trigger.player, current) > 0 && current.countCards("j"));
                if (!check) {
                  if (trigger.player.countCards("h") > trigger.player.hp + 1) {
                    check = false;
                  } else if (trigger.player.mayHaveSha() && trigger.player.getUseValue("sha") > 0) {
                    check = false;
                  } else {
                    check = true;
                  }
                }
              }
              break;
            case "phaseDiscard":
              check = trigger.player.needsToDiscard();
              break;
          }
          event.result = await player.chooseToDiscard(get.prompt(event.skill, trigger.player), str).set("ai", (card) => {
            if (!_status.event.check) {
              return -1;
            }
            return 7 - get.value(card);
          }).set("check", check).set("chooseonly", true).forResult();
        },
        async content(event, trigger, player) {
          await player.discard(event.cards);
          trigger.cancel();
          game.log(trigger.player, "跳过了", `#y${get.translation(trigger.name)}`);
          if (trigger.name === "phaseUse") {
            if (!trigger.player.canMoveCard()) {
              return;
            }
            await trigger.player.moveCard();
          } else if (trigger.name === "phaseDraw") {
            const result = await trigger.player.chooseTarget([1, 2], "获得至多两名角色各一张手牌", (card, player2, target) => target !== player2 && target.countGainableCards(player2, "h")).set("ai", (target) => get.effect(target, { name: "shunshou_copy2" }, get.player(), get.player())).forResult();
            if (!result?.bool || !result.targets?.length) {
              return;
            }
            result.targets.sortBySeat();
            trigger.player.line(result.targets, "green");
            await trigger.player.gainMultiple(result.targets);
            await game.delay();
          }
        },
        ai: {
          threaten: 3
        }
      }
    }
  },
  //SP徐氏
  zc26_longchen: {
    audio: "wengua",
    enable: "phaseUse",
    usable: 1,
    chooseButton: {
      dialog(event, player) {
        const list = [
          ["damage", "对一名角色造成1点雷电伤害"],
          ["draw", "令一名角色摸一张牌并回复1点体力"]
        ];
        return ui.create.dialog("龙谶", [list, "textbutton"], "hidden");
      },
      check(button) {
        const player = get.player();
        const getE = (current) => {
          if (button.link === "damage") {
            return get.damageEffect(current, player, player);
          }
          return get.effect(current, { name: "draw" }, player, player) + get.recoverEffect(current, player, player);
        };
        return getE(game.filterPlayer(() => true).maxBy(getE));
      },
      backup(links, player) {
        return {
          audio: "zc26_longchen",
          control: links[0],
          async content(event, trigger, player2) {
            const { control } = get.info(event.name);
            const { throwShengbei } = get.info("zc26_longchen");
            let num = await throwShengbei(player2);
            if (typeof num !== "number") {
              return;
            }
            let count = 1;
            if (num === 1) {
              count++;
            } else {
              player2.addMark("zc26_longchen", Math.max(1, 2 - num));
            }
            while (count > 0) {
              count--;
              const result2 = await player2.chooseTarget(control === "damage" ? "对一名角色造成1点雷电伤害" : "令一名角色摸一张牌并回复1点体力", true).set("control", control).set("ai", (target2) => {
                const { player: player3, control: control2 } = get.event();
                if (control2 === "draw") {
                  return get.effect(target2, { name: "draw" }, player3, player3) + get.recoverEffect(target2, player3, player3);
                }
                return get.damageEffect(target2, player3, player3, "thunder");
              }).forResult();
              if (!result2?.bool) {
                continue;
              }
              const target = result2.targets[0];
              player2.line(target, control === "draw" ? "wood" : "thunder");
              if (control === "draw") {
                await target.draw();
                await target.recover(1);
              } else {
                await target.damage("thunder");
              }
            }
          }
        };
      },
      prompt() {
        return "点击确定投掷龙鳞贝询问神明";
      }
    },
    async throwShengbei(player) {
      game.log(player, "掷出了圣杯向神明请示");
      let num1 = get.rand(0, 1);
      let num2 = get.rand(0, 1);
      const list = ["yang", "yin"];
      game.broadcastAll(() => {
        ui.arena.classList.add("thrownhighlight");
      });
      game.addVideo("thrownhighlight1");
      const shengBei1 = game.createCard(`shengbei_left_${list[num1]}`, "", "");
      const shengBei2 = game.createCard(`shengbei_right_${list[num2]}`, "", "");
      let cardsetions = {};
      if (lib.config.card_animation_info) {
        cardsetions[player.playerid] = get.cardsetion(player);
      }
      const throwc = (node) => {
        node.style.setProperty("transform-origin", "center", "important");
        if (lib.config.cardback_style !== "default") {
          node.style.transitionProperty = "none";
          ui.refresh(node);
          node.classList.add("infohidden");
          ui.refresh(node);
          node.style.transitionProperty = "";
        } else {
          node.classList.add("infohidden");
        }
        if (cardsetions) {
          const next = ui.create.div(".cardsetion", cardsetions[player.playerid] || "", node);
          next.style.setProperty("display", "block", "important");
          if (node.node) {
            if (node.node.cardsetion) {
              node.node.cardsetion.remove();
              delete node.node.cardsetion;
            }
            node.node.cardsetion = next;
          }
        }
        node.style.transform = "perspective(600px) rotateY(180deg) translateX(0)";
        const onEnd01 = () => {
          node.style.transition = "all ease-in 0.3s";
          node.style.transform = "perspective(600px) rotateY(270deg) translateX(0px)";
          const onEnd = () => {
            node.classList.remove("infohidden");
            node.style.transition = "all 0s";
            ui.refresh(node);
            node.style.transform = "perspective(600px) rotateY(-90deg) translateX(0px)";
            ui.refresh(node);
            node.style.transition = "";
            ui.refresh(node);
            node.style.transform = "";
          };
          node.listenTransition(onEnd);
        };
        onEnd01();
      };
      game.broadcastAll(
        (player2, throwc2, card1, card2, cardsetions2) => {
          const node1 = player2.$throwxy2(card1, "calc(50% - 114px)", "calc(50% - 52px)", "perspective(600px) rotateY(90deg) translateX(0px)", true);
          throwc2(node1);
          const node2 = player2.$throwxy2(card2, "50%", "calc(50% - 52px)", "perspective(600px) rotateY(90deg) translateX(0px)", true);
          throwc2(node2);
        },
        player,
        throwc,
        shengBei1,
        shengBei2,
        cardsetions
      );
      game.addVideo("compare", player, [get.cardInfo(shengBei1), player.dataset.position, get.cardInfo(shengBei2)]);
      await game.delay(0, 1500);
      const num = num1 + num2;
      const result = ["阴杯", "圣杯", "笑杯"][num];
      const map = {
        阴杯: ["thunder", "thundertext"],
        笑杯: ["soil", "firetext"],
        圣杯: ["metal", "yellowtext"]
      }, str = `${get.translation(player)}的掷杯结果为：<span class=${map[result][1]}>${result}</span>`;
      player.popup(result, map[result][0]);
      game.log("神明给", player, `的答复是<span class=${map[result][1]}>${result}</span>`);
      game.broadcastAll((str2) => {
        const dialog = ui.create.dialog(str2);
        dialog.classList.add("center");
        setTimeout(() => {
          dialog.close();
        }, 1e3);
      }, str);
      await game.delay(2);
      game.broadcastAll(() => {
        ui.arena.classList.remove("thrownhighlight");
      });
      game.addVideo("thrownhighlight2");
      game.broadcastAll(() => {
        ui.clear();
      });
      return num;
    },
    marktext: "怒",
    intro: {
      name: "龙怒(龙谶)",
      name2: "龙怒",
      content: "mark"
    },
    subSkill: {
      backup: {}
    },
    ai: {
      order: 10,
      result: {
        player: 1
      }
    }
  },
  zc26_tianqi: {
    audio: 2,
    trigger: {
      player: ["phaseZhunbeiBegin", "dying"]
    },
    juexingji: true,
    forced: true,
    skillAnimation: true,
    animationColor: "ice",
    filter(event, player, name) {
      return name === "dying" || player.countMark("zc26_longchen") >= 3;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.changeSkin({ characterName: "zc26_sp_xushi" }, "zc26_sp_xushi_shadow");
      await player.loseMaxHp();
      await player.recoverTo(player.maxHp);
      await player.addSkills("zc26_shouxin");
      const func = async (target) => {
        if (target.hasSex("male")) {
          player.line(target, "thunder");
          await target.damage("thunder");
        }
      };
      await game.doAsyncInOrder(
        game.filterPlayer(() => true),
        func
      );
    },
    derivation: "zc26_shouxin"
  },
  zc26_shouxin: {
    audio: 2,
    trigger: {
      target: "useCardToTarget",
      source: "damageSource"
    },
    filter(event, player) {
      if (event.name === "damage") {
        return event.hasNature();
      }
      return event.player !== player && player.hasMark("zc26_longchen");
    },
    async cost(event, trigger, player) {
      if (trigger.name === "damage") {
        event.result = {
          bool: true
        };
        return;
      }
      event.result = await player.chooseBool(get.prompt(event.skill), `移去1枚“龙怒”并令${get.translation(trigger.card)}对你无效`).set("choice", get.effect(player, trigger.card, trigger.player, player) < 0).forResult();
    },
    async content(event, trigger, player) {
      if (trigger.name === "damage") {
        await player.draw();
      } else {
        player.removeMark("zc26_longchen", 1);
        trigger.getParent().excluded.add(player);
      }
    }
  },
  //26珍藏太史慈
  zc26_tianyi: {
    enable: "phaseUse",
    usable: 1,
    filterTarget: (card, player, target) => player.canCompare(target),
    filter(event, player) {
      return game.hasPlayer((curr) => player.canCompare(curr));
    },
    async content(event, trigger, player) {
      const result = await player.chooseToCompare(event.targets[0]).forResult();
      if (result.bool) {
        player.addTempSkill("zc26_tianyi_effect");
      } else {
        player.addTempSkill("zc26_tianyi_diseffect");
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        mark: true,
        marktext: "天义",
        intro: {
          name: "天义",
          content: "本回合使用【杀】次数上限+1、目标上限+1、无距离限制"
        },
        mod: {
          cardUsable(card, player, num) {
            if (get.name(card) === "sha") {
              return num + 1;
            }
          },
          targetInRange(card, player, bool) {
            if (get.name(card) === "sha") {
              return true;
            }
          },
          selectTarget(card, player, range) {
            if (get.name(card) === "sha") {
              range[1]++;
            }
          }
        }
      },
      diseffect: {
        trigger: { player: "useCard" },
        charlotte: true,
        forced: true,
        mark: true,
        marktext: "天义",
        intro: {
          name: "天义",
          content: "本回合使用下一张牌时取消之并令唯一目标摸两张牌"
        },
        async content(event, trigger, player) {
          trigger.cancel();
          player.removeSkill(event.name);
          if (trigger.targets.length === 1) {
            await trigger.targets[0].draw(2);
          }
        },
        ai: {
          effect: {
            player_use(card, player, target) {
              return [0, 0, 0, 2];
            }
          }
        }
      }
    },
    ai: {
      order: 10,
      result: {
        player(player, target) {
          if (player.countCards("h") > 1) {
            return -get.attitude(player, target);
          }
          return 0;
        }
      }
    }
  },
  zc26_dangmo: {
    trigger: { player: "useCardAfter" },
    filter(event, player) {
      const evts = player.getHistory("useCard");
      if (evts.length < 2) {
        return false;
      }
      const targets = get.info("zc26_dangmo").logTarget(event, player);
      return targets?.length;
    },
    logTarget(event, player) {
      const evts = player.getHistory("useCard");
      if (evts.length < 2) {
        return [];
      }
      const index = evts.indexOf(event);
      const nows = event?.targets;
      const olds = evts[index - 1]?.targets;
      if (!olds?.length || !nows?.length || olds.containsAll(...nows) && nows.containsAll(...olds)) {
        return [];
      }
      return olds.filter((current) => current?.isIn() && nows.includes(current));
    },
    check(event, player) {
      const targets = get.info("zc26_dangmo").logTarget(event, player);
      return targets.reduce((total, target) => {
        return total + get.damageEffect(target, player, player);
      }, 0) > 0;
    },
    async content(event, trigger, player) {
      await game.doAsyncInOrder(event.targets, async (target) => await target.damage());
    },
    locked: false,
    mod: {
      aiOrder(player, card, num) {
        const num1 = get.info(card).selectTarget ?? 0;
        const num2 = game.countPlayer();
        if (typeof num1 === "number") {
          return Math.abs(num1 - num2);
        } else if (typeof num1 === "function") {
          return Math.abs(num1(card, player) - nmu2);
        } else {
          return Math.abs(num1[1] - num2);
        }
      }
    },
    ai: {
      effct: {
        target(card, player, target) {
          if (!player.getHistory("useCard", (evt) => evt.targets.length > 0).length && player.hasSkill("zc26_tianyi_effct") && ui.selected.targets.length > 0) {
            return 0;
          }
          return [1, 0];
        }
      }
    }
  },
  //26珍藏贾诩
  zc26_wansha: {
    trigger: { player: "phaseBegin" },
    forced: true,
    async content(event, trigger, player) {
      const targets = game.filterPlayer((curr) => curr !== player);
      targets.forEach((target) => target.addTempSkill("zc26_wansha_effect"));
    },
    group: "zc26_wansha_draw",
    subSkill: {
      draw: {
        audio: "zc26_wansha",
        trigger: { global: "useCard" },
        filter(event, player) {
          return event.modSkill?.cardname === "zc26_wansha_effect";
        },
        logTarget: "player",
        forced: true,
        async content(event, trigger, player) {
          await player.draw(2);
        }
      },
      effect: {
        mark: true,
        marktext: "完杀",
        intro: {
          name: "完杀",
          content: "本回合红色基本牌均视为杀"
        },
        charlotte: true,
        mod: {
          cardname(card, player, name) {
            if (get.color(card) === "red" && lib.card[card.name].type === "basic") {
              return "sha";
            }
          }
        }
      }
    }
  },
  zc26_weimu: {
    trigger: {
      target: "useCardToTarget",
      player: "addJudgeBefore"
    },
    forced: true,
    priority: 15,
    preHidden: true,
    check(event, player) {
      return event.name === "addJudge" || event.card.name !== "chiling" && get.effect(event.target, event.card, event.player, player) < 0;
    },
    filter(event, player) {
      if (event.name === "addJudge") {
        return get.color(event.card) === "black";
      }
      return get.type(event.card, null, false) === "trick" && get.color(event.card) === "black";
    },
    async content(event, trigger, player) {
      if (trigger.name === "addJudge") {
        trigger.cancel(void 0, void 0, void 0);
        const owner = get.owner(trigger.card);
        if (owner?.getCards("hej").includes(trigger.card)) {
          await owner.lose(trigger.card, ui.discardPile);
        } else {
          await game.cardsDiscard(trigger.card);
        }
        game.log(trigger.card, "进入了弃牌堆");
      } else {
        trigger.getParent()?.targets.remove(player);
      }
    },
    group: ["zc26_weimu_effect"],
    subSkill: {
      effect: {
        enable: "chooseToUse",
        viewAs: {
          name: "jiedao"
        },
        filterCard(card) {
          return get.color(card) === "black" && get.type(card) !== "trick" && get.type(card) !== "delay";
        },
        position: "hes",
        check(card) {
          return 4.5 - get.value(card);
        }
      }
    },
    ai: {
      effect: {
        target(card, player, target, current) {
          if (get.type(card, "trick") === "trick" && get.color(card) === "black") {
            return "zeroplayertarget";
          }
        }
      }
    }
  }
};
const skills$5 = {
  //曹丕
  wxdlyishi: {
    audio: 2,
    derivation: "wxdlfangzhu",
    enable: "phaseUse",
    manualConfirm: true,
    limited: true,
    skillAnimation: true,
    animationColor: "wood",
    filter(event, player) {
      return game.players.length > 1;
    },
    filterTarget: lib.filter.notMe,
    selectTarget: -1,
    multiline: true,
    multitarget: true,
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const targets = event.targets.sortBySeat();
      await player.useSkill({ skill: "wxdlfangzhu", targets });
      await game.doAsyncInOrder(targets, async (target) => {
        await target.addSkills("wxdlfangzhu");
        target.markAuto("wxdlfangzhu", [player]);
      });
    },
    ai: {
      order: 1,
      result: {
        player: 1
      }
    }
  },
  wxdlfangzhu: {
    audio: "fangzhu",
    inherit: "fangzhu",
    //防止useSkill重复触发
    multitarget: true,
    multiline: true,
    intro: {
      content: "只能对$发动"
    },
    async cost(event, trigger, player) {
      const draw = player.getDamagedHp();
      const storage = player.getStorage(event.skill);
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: `令一名其他角色翻面${draw > 0 ? `并摸${get.cnNumber(draw)}张牌` : ""}`,
        filterTarget(card, player2, target) {
          if (player2 === target) {
            return false;
          }
          const storage2 = get.event().storage;
          if (!storage2?.length) {
            return true;
          }
          return storage2.includes(target);
        },
        ai(target) {
          if (target.hasSkillTag("noturn")) {
            return 0;
          }
          const player2 = get.event().player;
          const current = _status.currentPhase;
          const dis = current ? get.distance(current, target, "absolute") : 1;
          const draw2 = player2.getDamagedHp();
          const att = get.attitude(player2, target);
          if (att === 0) {
            return target.hasJudge("lebu") ? Math.random() / 3 : Math.sqrt(get.threaten(target)) / 5 + Math.random() / 2;
          }
          if (att > 0) {
            if (target.isTurnedOver()) {
              return att + draw2;
            }
            if (draw2 < 4) {
              return -1;
            }
            if (current && target.getSeatNum() > current.getSeatNum()) {
              return att + draw2 / 3;
            }
            return 10 * Math.sqrt(Math.max(0.01, get.threaten(target))) / (3.5 - draw2) + dis / (2 * game.countPlayer());
          } else {
            if (target.isTurnedOver()) {
              return att - draw2;
            }
            if (draw2 >= 5) {
              return -1;
            }
            if (current && target.getSeatNum() <= current.getSeatNum()) {
              return -att + draw2 / 3;
            }
            return (4.25 - draw2) * 10 * Math.sqrt(Math.max(0.01, get.threaten(target))) + 2 * game.countPlayer() / dis;
          }
        }
      }).set("storage", storage).forResult();
    },
    async content(event, trigger, player) {
      const targets = event.targets;
      const num = player.getDamagedHp();
      if (num > 0) {
        await game.doAsyncInOrder(targets, async (target) => await target.draw({ num }));
      }
      await game.doAsyncInOrder(targets, async (target) => await target.turnOver());
    }
  },
  wxdlnianjun: {
    audio: 2,
    global: "wxdlnianjun_sha",
    group: "wxdlnianjun_loseHp",
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player) {
      return game.hasPlayer((current) => current.hasCards("h")) && player.hasCards("h");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "连接你与任意名其他角色各一张手牌",
        filterTarget(card, player2, target) {
          return player2 !== target && target.hasCards("h");
        },
        selectTarget: [1, Infinity],
        ai(target) {
          const player2 = get.player();
          const att = get.sgnAttitude(player2, target);
          return -att / (1 + target.countConnectedCards());
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const targets = event.targets.slice().concat([player]).sortBySeat();
      const connects = /* @__PURE__ */ new Map();
      for (const current of targets) {
        const cards2 = current.getCards("h");
        if (!current?.isIn() || !cards2.length) {
          continue;
        }
        const result = cards2.length === 1 ? { links: cards2 } : await player.choosePlayerCard({
          target: current,
          position: "h",
          forced: true,
          ai(button) {
            const { player: player2, target } = get.event();
            const { link } = button;
            const att = get.attitude(player2, target);
            let val = get.value(link, target);
            if (att > 0) {
              if (player2 === target && player2 === _status.currentPhase) {
                const num = target.countConnectedCards();
                if (num > 0) {
                  if (get.is.connectedCard(link)) {
                    val += 3;
                  }
                  return val;
                } else if (!get.is.connectedCard(link) && !get.tag(link, "damage")) {
                  return 6.5 - val;
                }
                return 6 - val;
              }
              if (get.is.connectedCard(link)) {
                val += 3;
              }
              return val;
            }
            if (!get.is.connectedCard(link)) {
              val += 3;
            }
            return val;
          }
        }).forResult();
        if (result?.links?.length) {
          connects.set(current, result.links);
        }
      }
      for (const [current, cards2] of connects) {
        await current.connectCards(cards2);
      }
    },
    subSkill: {
      loseHp: {
        audio: "wxdlnianjun",
        forced: true,
        trigger: {
          global: ["loseAfter", "loseAsyncAfter", "equipAfter", "addJudgeAfter", "addToExpansionAfter", "gainAfter"]
        },
        filter(event, player) {
          const evts = game.getGlobalHistory("everything", (evt) => game.hasPlayer2((current) => evt?.sxrmConnectCardsMap?.has?.(current)));
          return evts.indexOf(event) === 0;
        },
        getIndex(event, player) {
          return game.filterPlayer((current) => {
            const evt = event.getl(current);
            if (!evt || !evt.cards2 || !evt.cards2.length) {
              return false;
            }
            if (event.name === "lose") {
              for (const i in event.gaintag_map) {
                if (event.gaintag_map[i].includes("visible_sxrm_connect_tag")) {
                  return true;
                }
              }
              return false;
            }
            return current.hasHistory("lose", (evt2) => {
              if (event !== evt2.getParent()) {
                return false;
              }
              for (const i2 in evt2.gaintag_map) {
                if (evt2.gaintag_map[i2].includes("visible_sxrm_connect_tag")) {
                  return true;
                }
              }
              return false;
            });
          });
        },
        logTarget(event, player, name, index) {
          return index;
        },
        async content(event, trigger, player) {
          const target = event.indexedData;
          await target.loseHp();
        }
      },
      sha: {
        mod: {
          cardUsableTarget(card, player, target) {
            if (player.hasConnectedCards() && target.hasConnectedCards()) {
              return Infinity;
            }
          }
        }
      }
    }
  },
  wxdlqianqian: {
    audio: 2,
    locked: true,
    mod: {
      cardEnabled(card, player) {
        if (!player.hasHistory("useCard") && get.suit(card) === "heart") {
          return false;
        }
      },
      cardSavable(card, player) {
        if (!player.hasHistory("useCard") && get.suit(card) === "heart") {
          return false;
        }
      }
    }
  },
  //沙币文心雕龙曹植
  wxdl_huamao: {
    derivation: ["wushen", "liushi", "gongxin", "tianxiang", "guose", "limu", "fengpo", "jiexun", "leiji", "zuoding", "miehai", "jiyu", "luoying", "lianhuan", "zhujiu", "ninghan"].map((i) => `huamao_${i}`),
    map: {
      heart: ["wushen", "liushi", "gongxin", "tianxiang"].map((i) => `huamao_${i}`),
      diamond: ["guose", "limu", "fengpo", "jiexun"].map((i) => `huamao_${i}`),
      spade: ["leiji", "zuoding", "miehai", "jiyu"].map((i) => `huamao_${i}`),
      club: ["luoying", "lianhuan", "zhujiu", "ninghan"].map((i) => `huamao_${i}`)
    },
    getSuit(player, skill) {
      const { map } = get.info("wxdl_huamao");
      return player.getStorage(`${skill}_suit`, Object.keys(map).find((suit) => map[suit].includes(skill)) || null);
    },
    forced: true,
    locked: false,
    trigger: { global: "roundStart" },
    filter(event, player) {
      return game.roundNumber === 1;
    },
    async content(event, trigger, player) {
      const { map } = get.info(event.name);
      const suits = lib.suit.randomGets(3);
      const skills2 = suits.map((i) => map[i].randomGet());
      player.addSkill(`${event.name}_clear`);
      await player.addAdditionalSkills(event.name, skills2);
      const result = await player.chooseToMove_new({
        prompt: "华茂：你可以分配不同花色给以下技能",
        list: [
          [
            "要分配的花色",
            suits.map((suit) => {
              const node = ui.create.card();
              node.init(["", NaN, `lukai_${suit}`, ""]);
              return node;
            })
          ],
          skills2.map((i) => [get.translation(i)])
        ],
        filterOk(moved2) {
          const list = [moved2[1], moved2[2], moved2[3]];
          return list.every((i) => i.length === 1);
        },
        filterMove(from, to, moved2) {
          if (typeof to === "number") {
            if (to === 0) {
              return true;
            }
            return !moved2[to].length;
          }
          return true;
        },
        processAI(list) {
          const suits2 = list[0][1].slice();
          const moved2 = [[], [suits2.randomRemove()], [suits2.randomRemove()], [suits2.randomRemove()]];
          return moved2;
        }
      }).forResult();
      const { moved } = result;
      if (result.bool && [moved[1], moved[2], moved[3]]?.every((i) => i.length === 1)) {
        const suits2 = [moved[1], moved[2], moved[3]].map((i) => get.name(i[0])?.slice(6));
        skills2.forEach((skill, idx) => player.setStorage(`${skill}_suit`, suits2[idx]));
      }
    },
    subSkill: {
      clear: {
        charlotte: true,
        trigger: {
          global: "washCard"
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          player.removeSkill(event.name);
          await player.removeAdditionalSkills("wxdl_huamao");
        }
      }
    }
  },
  wxdl_qishen: {
    trigger: {
      player: "phaseEnd"
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt2(event.skill),
        selectTarget: [1, 3],
        filterTaraget: lib.filter.all,
        ai(target) {
          return 0;
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const { targets } = event;
      player.addTempSkill(`${event.name}_clear`, { player: "dieAfter" });
      player.when({ player: "phaseEnd" }).filter((evt) => evt !== trigger).then(async (event2, trigger2, player2) => player2.removeSkill("wxdl_qishen_clear"));
      targets.forEach((target) => target.addAdditionalSkill(`${event.name}_${player.playerid}`, `${event.name}_chanyuan`));
      let prevTargets = [];
      for (const history of player.actionHistory.slice(0, -1).reverse()) {
        if (!history.isMe || history.isSkipped) {
          continue;
        }
        prevTargets = history["useSkill"].find((evt) => evt.skill === event.name)?.targets || [];
        break;
      }
      if (targets.some((target) => prevTargets.includes(target))) {
        await game.asyncDraw(
          targets.filter((target) => prevTargets.includes(target)),
          3
        );
      }
    },
    subSkill: {
      clear: {
        charlotte: true,
        onremove(player, skill) {
          game.players.forEach((target) => target.removeAdditionalSkill(`wxdl_qishen_${player.playerid}`));
        }
      },
      chanyuan: {
        charlotte: true,
        init(player, skill) {
          player.addSkillBlocker(skill);
        },
        onremove(player, skill) {
          player.removeSkillBlocker(skill);
        },
        skillBlocker(skill, player) {
          return skill !== "wxdl_qishen" && !lib.skill[skill].charlotte && !lib.skill[skill].persevereSkill;
        },
        mark: true,
        intro: {
          content(storage, player, skill) {
            let str = "<li>你造成的伤害+1";
            const list = player.getSkills(null, false, false).filter((i) => lib.skill.wxdl_qishen_chanyuan.skillBlocker(i, player));
            if (list.length) {
              str += `<br><li>失效技能：${get.translation(list)}`;
            }
            return str;
          }
        },
        trigger: {
          source: "damageBegin1"
        },
        forced: true,
        popup: false,
        async content(event, trigger, player) {
          trigger.num++;
        }
      }
    }
  },
  huamao_wushen: {
    onremove: true,
    mod: {
      cardname(card, player, name) {
        if (get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_wushen")) {
          return "sha";
        }
      },
      cardnature(card, player) {
        if (get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_wushen")) {
          return false;
        }
      },
      targetInRange(card, player) {
        if (card.name === "sha") {
          const suit = get.suit(card);
          if (suit === get.info("wxdl_huamao").getSuit(player, "huamao_wushen") || suit === "unsure") {
            return true;
          }
        }
      },
      cardUsable(card, player) {
        if (card.name === "sha") {
          const suit = get.suit(card);
          if (suit === get.info("wxdl_huamao").getSuit(player, "huamao_wushen") || suit === "unsure") {
            return Infinity;
          }
        }
      }
    },
    trigger: { player: "useCard" },
    forced: true,
    filter(event, player) {
      return event.card.name === "sha" && get.suit(event.card) === get.info("wxdl_huamao").getSuit(player, "huamao_wushen");
    },
    async content(event, trigger, player) {
      trigger.directHit.addArray(game.players);
      if (trigger.addCount !== false) {
        trigger.addCount = false;
        if (player.stat[player.stat.length - 1].card.sha > 0) {
          player.stat[player.stat.length - 1].card.sha--;
        }
      }
    },
    ai: {
      effect: {
        target(card, player, target, current) {
          if (get.tag(card, "respondSha") && current < 0) {
            return 0.6;
          }
        }
      },
      directHit_ai: true,
      skillTagFilter(player, tag, arg) {
        return arg.card.name === "sha" && get.suit(arg.card) === get.info("wxdl_huamao").getSuit(player, "huamao_wushen");
      }
    }
  },
  huamao_liushi: {
    onremove: true,
    enable: "phaseUse",
    filter(event, player) {
      return player.countCards("he", { suit: get.info("wxdl_huamao").getSuit(player, "huamao_liushi") }) > 0;
    },
    filterCard(card, player) {
      return get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_liushi");
    },
    position: "he",
    filterTarget(card, player, target) {
      return player.canUse("sha", target, false);
    },
    check(card) {
      const player = _status.event.player;
      const next = player.getNext();
      const att = get.attitude(player, next);
      if (att > 0) {
        const js = next.getCards("j");
        if (js.length) {
          return get.judge(js[0]) + 10 - get.value(card);
        }
        return 9 - get.value(card);
      }
      return 6 - get.value(card);
    },
    discard: false,
    prepare: "throw",
    loseTo: "cardPile",
    visible: true,
    insert: true,
    async content(event, trigger, player) {
      const { cards: cards2, targets } = event;
      game.log(player, "将", cards2, "置于牌堆顶");
      await player.useCard({ name: "sha", isCard: true, storage: { [event.name]: true } }, false, targets);
    },
    group: "huamao_liushi_damage",
    subSkill: {
      damage: {
        trigger: { source: "damageSource" },
        forced: true,
        popup: false,
        filter(event, player) {
          return event.card?.storage?.huamao_liushi === true && event.player.isIn() && event.getParent(3).name === "huamao_liushi";
        },
        async content(event, trigger, player) {
          trigger.player.addMark("huamao_liushi_effect", 1);
          trigger.player.addSkill("huamao_liushi_effect");
        }
      },
      effect: {
        mod: {
          maxHandcard(player, num) {
            return num - player.countMark("huamao_liushi_effect");
          }
        },
        onremove: true,
        charlotte: true,
        intro: {
          name2: "流",
          content: "手牌上限-#"
        }
      }
    },
    ai: {
      order() {
        return get.order({ name: "sha" }) - 0.4;
      },
      result: {
        target(player, target) {
          const eff = get.effect(target, { name: "sha" }, player, target);
          const damageEff = get.damageEffect(target, player, player);
          if (eff > 0) {
            return damageEff > 0 ? 0 : eff;
          }
          if (target.hasSkill("bagua_skill") || target.hasSkill("rw_bagua_skill") || target.hasSkill("bazhen")) {
            return 0;
          }
          return eff;
        }
      }
    }
  },
  huamao_gongxin: {
    onremove: true,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card, player, target) {
      return target !== player && target.countCards("h");
    },
    async content(event, trigger, player) {
      const { target } = event;
      const cards2 = target.getCards("h");
      const result = await player.chooseToMove_new("攻心").set("list", [
        [`${get.translation(target)}的手牌`, cards2],
        [["弃置"], ["置于牌堆顶"]]
      ]).set("filterOk", (moved) => moved[1].slice().concat(moved[2]).filter((card) => get.suit(card) === get.info("wxdl_huamao").getSuit(get.player(), "huamao_gongxin")).length === 1).set("filterMove", (from, to, moved) => {
        if (moved[0].includes(from.link) && moved[1].length + moved[2].length >= 1 && [1, 2].includes(to)) {
          return false;
        }
        return get.suit(from) === get.info("wxdl_huamao").getSuit(get.player(), "huamao_gongxin");
      }).set("processAI", (list) => {
        let card = list[0][1].slice().filter((card2) => get.suit(card2) === get.info("wxdl_huamao").getSuit(get.player(), "huamao_gongxin")).sort((a, b) => get.value(b) - get.value(a))[0];
        if (!card) {
          return false;
        }
        return [list[0][1].slice().remove(card), [card], []];
      }).forResult();
      if (!result.bool) {
        return;
      }
      if (result.moved[1].length) {
        await target.discard(result.moved[1]);
      } else {
        await player.showCards(result.moved[2], `${get.translation(player)}对${get.translation(target)}发动了【攻心】`);
        await target.lose(result.moved[2], ui.cardPile, "visible", "insert");
      }
    },
    ai: {
      threaten: 1.5,
      result: {
        target(player, target) {
          return -target.countCards("h");
        }
      },
      order: 10,
      expose: 0.4
    }
  },
  huamao_tianxiang: {
    onremove: true,
    trigger: { player: "damageBegin4" },
    filter(event, player) {
      return player.countCards("h", (card) => _status.connectMode || get.suit(card, player) === get.info("wxdl_huamao").getSuit(player, "huamao_tianxiang")) > 0 && event.num > 0;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseCardTarget({
        filterCard(card, player2) {
          return get.suit(card) === get.info("wxdl_huamao").getSuit(player2, "huamao_tianxiang") && lib.filter.cardDiscardable(card, player2);
        },
        filterTarget(card, player2, target) {
          return player2 !== target;
        },
        ai1(card) {
          return 10 - get.value(card);
        },
        ai2(target) {
          const att = get.attitude(_status.event.player, target);
          const trigger2 = _status.event.getTrigger();
          let da = 0;
          if (_status.event.player.hp === 1) {
            da = 10;
          }
          const eff = get.damageEffect(target, trigger2.source, target);
          if (att === 0) {
            return 0.1 + da;
          }
          if (eff >= 0 && att > 0) {
            return att + da;
          }
          if (att > 0 && target.hp > 1) {
            if (target.maxHp - target.hp >= 3) {
              return att * 1.1 + da;
            }
            if (target.maxHp - target.hp >= 2) {
              return att * 0.9 + da;
            }
          }
          return -att + da;
        },
        prompt: get.prompt(event.skill),
        prompt2: get.skillInfoTranslation(event.skill, player, false)
      }).forResult();
    },
    async content(event, trigger, player) {
      const [target] = event.targets;
      const [card] = event.cards;
      trigger.cancel();
      await player.discard(event.cards);
      const result = await player.chooseControlList(
        true,
        (event2, player2) => {
          const target2 = _status.event.target;
          let att = get.attitude(player2, target2);
          if (target2.hasSkillTag("maixie")) {
            att = -att;
          }
          return att > 0 ? 0 : 1;
        },
        [`令${get.translation(target)}受到伤害来源对其造成的1点伤害，然后摸X张牌（X为其已损失体力值且至多为5）`, `令${get.translation(target)}失去1点体力，然后获得${get.translation(event.cards)}`]
      ).set("target", target).forResult();
      if (typeof result.index !== "number") {
        return;
      }
      if (result.index) {
        event.related = target.loseHp();
      } else {
        event.related = target.damage(trigger.source || "nosource", "nocard");
      }
      await event.related;
      if (result.index && card.isInPile()) {
        await target.gain(card, "gain2");
      } else if (target.getDamagedHp()) {
        await target.draw(Math.min(5, target.getDamagedHp()));
      }
    },
    ai: {
      maixie_defend: true,
      effect: {
        target(card, player, target) {
          if (player.hasSkillTag("jueqing", false, target)) {
            return;
          }
          if (get.tag(card, "damage") && target.countCards("he") > 1) {
            return 0.7;
          }
        }
      }
    }
  },
  huamao_guose: {
    onremove: true,
    enable: "phaseUse",
    usable: 1,
    discard: false,
    lose: false,
    delay: false,
    filter(event, player) {
      return player.countCards("hes", { suit: get.info("wxdl_huamao").getSuit(player, "huamao_guose") }) > 0;
    },
    position: "hes",
    filterCard(card, player) {
      return get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_guose");
    },
    filterTarget(card, player, target) {
      if (get.position(ui.selected.cards[0]) !== "s" && lib.filter.cardDiscardable(ui.selected.cards[0], player, "huamao_guose") && target.hasJudge("lebu")) {
        return true;
      }
      if (player === target) {
        return false;
      }
      if (!game.checkMod(ui.selected.cards[0], player, "unchanged", "cardEnabled2", player)) {
        return false;
      }
      return player.canUse({ name: "lebu", cards: ui.selected.cards }, target);
    },
    check(card) {
      return 7 - get.value(card);
    },
    async content(event, trigger, player) {
      const { target } = event;
      if (event.target.hasJudge("lebu")) {
        await player.discard(event.cards);
        await target.discard(event.target.getJudge("lebu"));
      } else {
        await player.useCard({ name: "lebu" }, event.target, event.cards).set("audio", false);
      }
      await player.draw();
    },
    ai: {
      result: {
        target(player, target) {
          if (target.hasJudge("lebu")) {
            return -get.effect(target, { name: "lebu" }, player, target);
          }
          return get.effect(target, { name: "lebu" }, player, target);
        }
      },
      order: 9
    }
  },
  huamao_limu: {
    onremove: true,
    mod: {
      targetInRange(card, player, target) {
        if (player.countCards("j") && player.inRange(target)) {
          return true;
        }
      },
      cardUsableTarget(card, player, target) {
        if (player.countCards("j") && player.inRange(target)) {
          return true;
        }
      },
      aiOrder(player, card, num) {
        if (get.type(card, null, player) === "trick" && player.canUse(card, player) && player.canAddJudge(card)) {
          return 15;
        }
      }
    },
    locked: false,
    audio: 2,
    enable: "phaseUse",
    discard: false,
    filter(event, player) {
      if (player.hasJudge("lebu")) {
        return false;
      }
      return player.countCards("hes", { suit: get.info("wxdl_huamao").getSuit(player, "huamao_limu") }) > 0;
    },
    viewAs: { name: "lebu" },
    //prepare:"throw",
    position: "hes",
    filterCard(card, player, event) {
      const lebu = get.autoViewAs({ name: "lebu", cards: [card] }, [card]);
      return get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_limu") && lib.filter.judge(lebu, player, player);
    },
    selectTarget: -1,
    filterTarget(card, player, target) {
      return player === target;
    },
    check(card) {
      const player = _status.event.player;
      if (!player.getEquip("zhangba")) {
        let damaged = player.maxHp - player.hp - 1;
        if (player.countCards("h", (cardx) => {
          if (cardx === card) {
            return false;
          }
          if (cardx.name === "tao") {
            if (damaged < 1) {
              return true;
            }
            damaged--;
          }
          return ["shan", "jiu"].includes(cardx.name);
        }) > 0) {
          return 0;
        }
      }
      if (card.name === "shan") {
        return 15;
      }
      if (card.name === "tao" || card.name === "jiu") {
        return 10;
      }
      return 9 - get.value(card);
    },
    onuse(links, player) {
      const next = game.createEvent("huamao_limu_recover", false, _status.event.getParent());
      next.player = player;
      next.setContent(() => {
        player.recover();
      });
    },
    ai: {
      result: {
        target(player, target) {
          if (player.countCards("hes", "zhangba")) {
            return player.countCards("h", { type: "basic" });
          }
          let res = lib.card.lebu.ai.result.target(player, target);
          if (player.countCards("hs", "sha") >= player.hp) {
            res++;
          }
          if (target.isDamaged()) {
            return res + 2 * Math.abs(get.recoverEffect(target, player, target));
          }
          return res;
        },
        ignoreStatus: true
      },
      order(item, player) {
        if (player.hp > 1 && player.countCards("j")) {
          return 0;
        }
        return 12;
      },
      effect: {
        target(card, player, target) {
          if (target.isPhaseUsing() && typeof card === "object" && get.type(card, null, target) === "delay" && !target.countCards("j")) {
            let shas = target.getCards("hs", (i) => {
              if (card === i || card.cards && card.cards.includes(i)) {
                return false;
              }
              return get.name(i, target) === "sha" && target.getUseValue(i) > 0;
            }) - target.getCardUsable("sha");
            if (shas > 0) {
              return [1, 1.5 * shas];
            }
          }
        }
      }
    }
  },
  huamao_fengpo: {
    onremove: true,
    trigger: {
      player: "useCardToPlayered"
    },
    filter(event, player) {
      if (event.targets.length !== 1 || !["sha", "juedou"].includes(event.card.name)) {
        return false;
      }
      const evtx = event.getParent();
      return player.getHistory("useCard", (evt) => evt.card.name === event.card.name).indexOf(evtx) === 0;
    },
    async cost(event, trigger, player) {
      const str1 = get.translation(trigger.card);
      const str2 = get.translation(trigger.target);
      const suit = get.translation(get.info("wxdl_huamao").getSuit(player, event.skill));
      const result = await player.chooseControl({
        controls: ["摸X加1伤", "摸1加X伤", "cancel2"],
        prompt: get.prompt(event.skill, trigger.target),
        prompt2: `你可以选择一项：1.摸X张牌，令${str1}的伤害+1；2.摸一张牌，令${str1}的伤害+X（X为${str2}的${suit}牌的数量）。`
      }).forResult();
      if (result.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.index
        };
      }
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const {
        cost_data: index,
        targets: [target]
      } = event;
      const nd = target.countCards("he", { suit: get.info("wxdl_huamao").getSuit(player, event.name) });
      let draw = 1;
      let damage = 1;
      if (index === 0) {
        draw = nd;
      } else {
        damage = nd;
      }
      await player.draw(draw);
      const evt = trigger.getParent();
      if (typeof evt.baseDamage !== "number") {
        evt.baseDamage = 1;
      }
      evt.baseDamage += damage;
    }
  },
  huamao_jiexun: {
    onremove: true,
    trigger: { player: "phaseJieshuBegin" },
    async cost(event, trigger, player) {
      const num1 = game.countPlayer((current) => current.countCards("ej", { suit: get.info("wxdl_huamao").getSuit(player, event.skill) }));
      const num2 = player.countMark(event.skill);
      let str = `令目标摸${get.cnNumber(num1)}张牌`;
      if (num2) {
        str += `，然后弃置${get.cnNumber(num2)}张牌`;
      }
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        filterTarget(card, player2, target) {
          return target !== player2;
        },
        ai(target) {
          return _status.event.coeff * get.attitude(_status.event.player, target);
        },
        prompt2: str
      }).set("coeff", num1 >= num2 ? 1 : -1).forResult();
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      const num1 = game.countPlayer((current) => current.countCards("ej", { suit: get.info("wxdl_huamao").getSuit(player, event.name) }));
      const num2 = player.countMark(event.name);
      player.addMark(event.name, 1, false);
      if (num1 > 0) {
        await target.draw({ num: num1 });
      }
      if (num2 > 0) {
        const result = await target.chooseToDiscard({ selectCard: num2, forced: true, position: "he" }).forResult;
        if (result.bool && result.autochoose && result.cards.length === result.rawcards.length) {
          player.clearMark(event.name, false);
          player.addSkill("funan_jiexun");
        }
      }
    },
    intro: { content: "已经发动过了#次" }
  },
  huamao_leiji: {
    onremove: true,
    trigger: { player: ["useCard", "respond"] },
    filter(event, player) {
      return event.card.name === "shan";
    },
    line: "thunder",
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget(get.prompt2(event.skill)).set("ai", (target) => {
        const player2 = get.player();
        if (target.hasSkill("hongyan")) {
          return 0;
        }
        return get.damageEffect(target, player2, player2, "thunder");
      }).forResult();
    },
    async content(event, trigger, player) {
      const [target] = event.targets;
      const next = target.judge((card) => {
        const evt = get.event().name === "judge" ? get.event() : get.event().getParent("judge");
        if (get.suit(card) === evt.suit) {
          return -4;
        }
        return 4;
      });
      next.set("suit", get.info("wxdl_huamao").getSuit(player, event.name));
      next.judge2 = (result2) => !result2.bool;
      const result = await next.forResult();
      if (!result?.bool) {
        await target.damage(2, "thunder");
      }
    },
    ai: {
      mingzhi: false,
      useShan: true,
      effect: {
        target_use(card, player, target, current) {
          if (get.tag(card, "respondShan") && !player.hasSkillTag(
            "directHit_ai",
            true,
            {
              target,
              card
            },
            true
          ) && game.hasPlayer((current2) => get.attitude(target, current2) < 0 && get.damageEffect(current2, target, target, "thunder") > 0)) {
            if (card.name === "sha") {
              if (!target.mayHaveShan(player, "use")) {
                return;
              }
            } else if (!target.mayHaveShan(player)) {
              return 1 - 0.1 * Math.min(5, target.countCards("hs"));
            }
            if (!target.hasSkillTag("rejudge")) {
              return [1, 1];
            }
            let pos = player.hasSkillTag("viewHandcard", null, target, true) ? "hes" : "e";
            if (target.hasCard((cardx) => get.suit(cardx) === get.info("wxdl_huamao").getSuit(player, "huamao_leiji"), pos)) {
              return [1, 4];
            }
            if (pos === "e") {
              return [1, Math.min(4, 1 + 0.75 * Math.max(1, target.countCards("hs")))];
            }
            return [1, 1];
          }
        }
      }
    }
  },
  huamao_zuoding: {
    onremove: true,
    trigger: { global: "useCardToPlayered" },
    filter(event, player) {
      if (event.getParent().triggeredTargets3.length > 1) {
        return false;
      }
      return get.suit(event.card) === get.info("wxdl_huamao").getSuit(player, "huamao_zuoding") && _status.currentPhase === event.player && event.targets && event.targets.length && event.player !== player && game.countPlayer2((current) => current.getHistory("damage").length > 0) === 0;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "令一名目标角色摸一张牌",
        filterTarget(card, player2, target) {
          return _status.event.targets.includes(target);
        },
        ai(target) {
          return get.attitude(_status.event.player, target);
        }
      }).set("targets", trigger.targets).forResult();
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      await target.draw();
    },
    ai: {
      expose: 0.2
    }
  },
  huamao_miehai: {
    onremove: true,
    enable: "chooseToUse",
    filterCard: true,
    selectCard: 2,
    position: "hes",
    viewAs: {
      name: "sha",
      nature: "stab",
      storage: {
        huamao_miehai: true
      }
    },
    complexCard: true,
    filter(event, player) {
      return player.countCards("hes") >= 2;
    },
    prompt: "将两张牌当刺【杀】使用或打出",
    async precontent(event, trigger, player) {
      player.when("useCardAfter").filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player2) => {
        const targets = game.filterPlayer((current) => current.getHistory("lose", (evt) => {
          const cards2 = evt.cards2;
          if (!evt.getParent((evt2) => evt2 === trigger2, true, true) || !cards2.some((card) => get.suit(card) === get.info("wxdl_huamao").getSuit(player2, "huamao_miehai"))) {
            return false;
          }
          return evt.visible;
        }).length);
        if (!targets?.length) {
          return;
        }
        for (const target of targets) {
          if (!target.isDamaged()) {
            continue;
          }
          await target.draw(2);
          await target.recover();
        }
      });
    },
    check(card) {
      let player = _status.event.player;
      let val = get.value(card);
      if (get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_miehai") && player.isDamaged()) {
        val *= 0.6;
      }
      return Math.max(5, 8 - 0.7 * player.hp) - val;
    },
    ai: {
      order(item, player) {
        return get.order({ name: "sha" }) + 0.1;
      }
    },
    locked: false,
    mod: {
      targetInRange(card) {
        if (card?.storage?.huamao_miehai) {
          return true;
        }
      },
      cardUsable(card, player, num) {
        if (card?.storage?.huamao_miehai) {
          return Infinity;
        }
      }
    }
  },
  huamao_jiyu: {
    onremove: true,
    enable: "phaseUse",
    locked: false,
    filter(event, player) {
      const hs = player.getCards("h");
      for (const card of hs) {
        if (!player.getStorage("huamao_jiyu_limit").includes(get.suit(card))) {
          return true;
        }
      }
      return false;
    },
    filterTarget(card, player, target) {
      return target.countCards("h") > 0 && !player.getStorage("huamao_jiyu").includes(target);
    },
    async content(event, trigger, player) {
      const { target } = event;
      player.addTempSkill(`${event.name}_limit`);
      player.markAuto(`${event.name}`, target);
      let spade = true;
      if (player.isTurnedOver() || get.attitude(target, player) > 0 || target.hp <= 2) {
        spade = false;
      }
      const result = await target.chooseToDiscard({
        position: "h",
        forced: true,
        ai(card2) {
          if (get.suit(card2) === get.event().suit) {
            if (_status.event.spade) {
              return 10 - get.value(card2);
            } else {
              return -10 - get.value(card2);
            }
          }
          if (_status.event.getParent().player.getStorage("huamao_jiyu_limit").includes(get.suit(card2))) {
            return -3 - get.value(card2);
          }
          return -get.value(card2);
        }
      }).set("spade", spade).set("suit", get.info("wxdl_huamao").getSuit(player, event.name)).forResult();
      if (!result.cards?.length) {
        return;
      }
      const card = result.cards[0];
      player.markAuto(`${event.name}_limit`, get.suit(card, target));
      if (get.suit(card, target) === get.info("wxdl_huamao").getSuit(player, event.name)) {
        await player.turnOver();
        await target.loseHp();
      }
    },
    ai: {
      order: 9,
      result: {
        target(player, target) {
          if (player.isTurnedOver() || target.countCards("h") <= 3) {
            return -1;
          }
          return 0;
        }
      }
    },
    subSkill: {
      limit: {
        charlotte: true,
        onremove: ["huamao_jiyu", "huamao_jiyu_limit"],
        mod: {
          cardEnabled(card, player) {
            if (player.getStorage("huamao_jiyu_limit").includes(get.suit(card))) {
              return false;
            }
          },
          cardSavable(card, player) {
            if (player.getStorage("huamao_jiyu_limit").includes(get.suit(card))) {
              return false;
            }
          }
        }
      }
    }
  },
  huamao_luoying: {
    onremove: true,
    group: ["huamao_luoying_discard", "huamao_luoying_judge"],
    subSkill: {
      discard: {
        trigger: { global: ["loseAfter", "loseAsyncAfter"] },
        filter(event, player) {
          if (event.type !== "discard" || event.getlx === false) {
            return false;
          }
          return game.filterPlayer2((target) => target !== player, void 0, true).some((target) => event.getl(target)?.cards2.some((card) => get.suit(card, target) === get.info("wxdl_huamao").getSuit(player, "huamao_luoying")));
        },
        async cost(event, trigger, player) {
          const cards2 = game.filterPlayer2((target) => target !== player, void 0, true).reduce((list, target) => [...list, ...trigger.getl(target)?.cards2.filter((card) => get.suit(card, target) === get.info("wxdl_huamao").getSuit(player, "huamao_luoying"))], []);
          const result = await player.chooseButton({
            createDialog: ["落英：选择要获得的牌", cards2],
            selectButton: [1, cards2.length],
            ai(button) {
              return get.value(button.link, _status.event.player, "raw");
            }
          }).forResult();
          if (result.bool !== false) {
            event.result = {
              bool: true,
              cost_data: result.links
            };
          }
        },
        async content(event, trigger, player) {
          const { cost_data: cards2 } = event;
          await player.gain({ cards: cards2, animate: "gain2" });
        }
      },
      judge: {
        trigger: { global: "cardsDiscardAfter" },
        filter(event, player) {
          const evt = event.getParent().relatedEvent;
          if (!evt || evt.name !== "judge") {
            return;
          }
          if (evt.player === player) {
            return false;
          }
          if (get.position(event.cards[0], true) !== "d") {
            return false;
          }
          return get.suit(event.cards[0]) === get.info("wxdl_huamao").getSuit(player, "huamao_luoying");
        },
        async cost(event, trigger, player) {
          const { cards: cards2 } = trigger;
          const result = await player.chooseButton({
            createDialog: ["落英：选择要获得的牌", cards2],
            selectButton: [1, cards2.length],
            ai(button) {
              return get.value(button.link, _status.event.player, "raw");
            }
          }).forResult();
          if (result.bool !== false) {
            event.result = {
              bool: true,
              cost_data: result.links
            };
          }
        },
        async content(event, trigger, player) {
          const { cost_data: cards2 } = event;
          await player.gain({ cards: cards2, animate: "gain2" });
        }
      }
    }
  },
  huamao_lianhuan: {
    onremove: true,
    hiddenCard(player, name) {
      return name === "tiesuo" && player.hasCard((card) => get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_lianhuan"), "sh");
    },
    enable: "chooseToUse",
    filter(event, player) {
      if (!player.hasCard((card) => get.suit(card) === get.info("wxdl_huamao").getSuit(player, "huamao_lianhuan"), "sh")) {
        return false;
      }
      return event.type === "phase" || event.filterCard(get.autoViewAs({ name: "tiesuo" }, "unsure"), player, event);
    },
    position: "hs",
    filterCard(card, player, event) {
      if (!event) {
        event = _status.event;
      }
      if (get.suit(card) !== get.info("wxdl_huamao").getSuit(player, "huamao_lianhuan")) {
        return false;
      }
      if (event.type === "phase" && get.position(card) !== "s" && player.canRecast(card)) {
        return true;
      }
      if (game.checkMod(card, player, "unchanged", "cardEnabled2", player) === false) {
        return false;
      }
      const cardx = get.autoViewAs({ name: "tiesuo" }, [card]);
      return event._backup.filterCard(cardx, player, event);
    },
    filterTarget(fuck, player, target) {
      const card = ui.selected.cards[0];
      const event = _status.event;
      const backup = event._backup;
      if (!card || game.checkMod(card, player, "unchanged", "cardEnabled2", player) === false) {
        return false;
      }
      const cardx = get.autoViewAs({ name: "tiesuo" }, [card]);
      return backup.filterCard(cardx, player, event) && backup.filterTarget(cardx, player, target);
    },
    selectTarget() {
      const card = ui.selected.cards[0];
      const event = _status.event;
      const player = event.player;
      const backup = event._backup;
      let recast = false;
      let use = false;
      const cardx = get.autoViewAs({ name: "tiesuo" }, [card]);
      if (event.type === "phase" && player.canRecast(card)) {
        recast = true;
      }
      if (card && game.checkMod(card, player, "unchanged", "cardEnabled2", player) !== false) {
        if (backup.filterCard(cardx, player, event)) {
          use = true;
        }
      }
      if (!use) {
        return [0, 0];
      }
      const select = backup.selectTarget(cardx, player);
      if (recast && select[0] > 0) {
        select[0] = 0;
      }
      return select;
    },
    filterOk() {
      const card = ui.selected.cards[0];
      const event = _status.event;
      const player = event.player;
      const backup = event._backup;
      const selected = ui.selected.targets.length;
      let recast = false;
      let use = false;
      const cardx = get.autoViewAs({ name: "tiesuo" }, [card]);
      if (event.type === "phase" && player.canRecast(card)) {
        recast = true;
      }
      if (card && game.checkMod(card, player, "unchanged", "cardEnabled2", player) !== false) {
        if (backup.filterCard(cardx, player, event)) {
          use = true;
        }
      }
      if (recast && selected === 0) {
        return true;
      }
      if (!use) {
        return;
      }
      const select = backup.selectTarget(cardx, player);
      if (select[0] <= -1) {
        return true;
      }
      return selected >= select[0] && selected <= select[1];
    },
    ai1(card) {
      return 6 - get.value(card);
    },
    ai2(target) {
      const player = get.player();
      return get.effect(target, { name: "tiesuo" }, player, player);
    },
    discard: false,
    lose: false,
    delay: false,
    viewAs(cards2, player) {
      return {
        name: "tiesuo"
      };
    },
    prepare: () => true,
    async precontent(event, trigger, player) {
      const result = event.result;
      if (!result?.targets?.length) {
        delete result.card;
      }
    },
    async content(event, trigger, player) {
      await player.recast(event.cards);
    },
    ai: {
      order(item, player) {
        if (game.hasPlayer((current) => get.effect(current, { name: "tiesuo" }, player, player) > 0) || player.hasCard((card) => get.suit(card) === "club" && player.canRecast(card), "h")) {
          return 8;
        }
        return 1;
      },
      result: { player: 1 }
    }
  },
  huamao_zhujiu: {
    onremove: true,
    onChooseToUse(event) {
      if (!game.online && !event.huamao_zhujiu) {
        event.set("huamao_zhujiu", event.player.getHistory("useCard", (evt) => get.name(evt.card) === "jiu").length + 1);
      }
    },
    enable: "chooseToUse",
    viewAs: {
      name: "jiu"
    },
    position: "hes",
    filter(event, player) {
      const num = event.huamao_zhujiu;
      return typeof num === "number" && player.countCards("hes") >= num;
    },
    filterCard: true,
    selectCard() {
      const num = get.event().huamao_zhujiu;
      return [num, Infinity];
    },
    log: false,
    allowChooseAll: true,
    async precontent(event, trigger, player) {
      player.logSkill("huamao_zhujiu");
      if (event.result.cards?.some((i) => get.suit(i) !== get.info("wxdl_huamao").getSuit(player, "huamao_zhujiu"))) {
        player.tempBanSkill("huamao_zhujiu");
      }
    },
    check(card) {
      const player = get.player();
      if (game.hasPlayer((current) => current.hasSkill("spoljinglei") && !current.storage.counttrigger?.spoljinglei && get.attitude(current, player) > 0 && current.getHp() > 1)) {
        if (get.position(card) !== "h") {
          return -get.value(card);
        }
        return 10 - ui.selected.cards.length - get.value(card);
      }
      const num = get.event().huamao_zhujiu;
      if (typeof num === "number" && ui.selected.cards.length >= num) {
        return 0;
      }
      return 6 - get.value(card);
    },
    ai: {
      save: true,
      order(item, player) {
        if (get.event().dying) {
          return 9;
        }
        if (game.hasPlayer((current) => current.hasSkill("spoljinglei") && !current.storage.counttrigger?.spoljinglei && get.attitude(current, player) > 0 && current.getHp() > 1)) {
          return get.order({ name: "sha" }) + 0.2;
        }
        return get.order({ name: "jiu" }) + 0.1;
      },
      nokeep: true,
      skillTagFilter(player, tag, arg) {
        if (tag === "nokeep") {
          return (!arg || arg?.card && get.name(arg.card) === "tao") && get.event()?.type === "phase" && game.hasPlayer((current) => current.hasSkill("spoljinglei") && !current.storage.counttrigger?.spoljinglei && get.attitude(current, player) > 0 && current.getHp() > 1) && player.hasCard((card) => get.name(card) !== "tao", "h");
        }
      }
    }
  },
  huamao_ninghan: {
    onremove: true,
    trigger: { global: "damageEnd" },
    filter(event, player) {
      return event.hasNature("ice") && event.cards?.someInD();
    },
    forced: true,
    async content(event, trigger, player) {
      const cards2 = trigger.cards.filterInD();
      const next = player.addToExpansion(cards2, "gain2");
      next.gaintag.add("jsrgshacheng");
      await next;
    },
    global: "huamao_ninghan_frozen",
    subSkill: {
      frozen: {
        mod: {
          cardnature(card, player) {
            const sources = game.filterPlayer((current) => current.hasSkill("huamao_ninghan"));
            if (!sources.length) {
              return;
            }
            sources.sortBySeat().reverse();
            if (card.name === "sha" && get.suit(card) === get.info("wxdl_huamao").getSuit(sources[0], "huamao_ninghan")) {
              return "ice";
            }
          },
          aiOrder(player, card, num) {
            if (!game.hasPlayer((current) => current.hasSkill("huamao_ninghan"))) {
              return;
            }
            if (num && card.name === "sha" && game.hasNature(card, "ice")) {
              return num + 0.15 * Math.sign(
                game.countPlayer((current) => {
                  if (!current.hasSkill("huamao_ninghan")) {
                    return 0;
                  }
                  return Math.sign(get.attitude(player, current));
                })
              );
            }
          }
        }
      }
    },
    ai: {
      combo: "jsrgshacheng"
    }
  },
  //文心雕龙
  wxdl_kejie: {
    audio: 2,
    trigger: {
      player: "phaseDrawEnd"
    },
    filter(event, player) {
      if (player.countCards("h") >= 5) {
        return false;
      }
      return game.hasPlayer((current) => current.isDamaged());
    },
    async cost(event, trigger, player) {
      const targets = game.filterPlayer((current) => current.isDamaged());
      let answer_ok = void 0;
      let humans = targets.filter((current) => current === game.me || current.isOnline());
      let locals = targets.slice(0).randomSort();
      locals.removeArray(humans);
      const eventId = get.id();
      const send = (current, player2, eventId2) => {
        lib.skill.wxdl_kejie.chooseBool(current, player2, eventId2);
        game.resume();
      };
      event._global_waiting = true;
      let time = 1e4;
      if (lib.configOL && lib.configOL.choose_timeout) {
        time = parseInt(lib.configOL.choose_timeout) * 1e3;
      }
      targets.forEach((current) => current.showTimer(time));
      if (humans.length > 0) {
        const solve = (resolve, reject) => (result, player2) => {
          if (result?.bool && !answer_ok) {
            answer_ok = player2;
            resolve();
          } else {
            reject();
          }
        };
        await Promise.any(
          humans.map(
            (current) => new Promise((resolve, reject) => {
              if (current.isOnline()) {
                current.send(send, current, player, eventId);
                current.wait(solve(resolve, reject));
              } else {
                const next = lib.skill.wxdl_kejie.chooseBool(current, player, eventId);
                const solver = solve(resolve, reject);
                if (_status.connectMode) {
                  game.me.wait(solver);
                }
                return next.forResult().then((result) => {
                  if (_status.connectMode && !answer_ok) {
                    game.me.unwait(result, current);
                  } else {
                    solver(result, current);
                  }
                });
              }
            })
          )
        ).catch(() => {
        });
        game.broadcastAll("cancel", eventId);
      }
      if (!answer_ok && locals.length > 0) {
        for (const current of locals) {
          const result = await lib.skill.wxdl_kejie.chooseBool(current, player).forResult();
          if (result?.bool) {
            answer_ok = current;
            break;
          }
        }
      }
      delete event._global_waiting;
      for (const i of targets) {
        i.hideTimer();
      }
      if (answer_ok && get.itemtype(answer_ok) === "player") {
        event.result = {
          bool: true,
          targets: [answer_ok]
        };
      }
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = {
        skill: "xiantu",
        targets: [player]
      };
      await target.useResult(result, event);
      const result2 = await player.chooseCard(
        "克捷：是否重铸任意张【闪】？",
        [1, Infinity],
        (card, player2, event2) => {
          if (get.name(card) !== "shan") {
            return false;
          }
          return lib.filter.cardRecastable(card, player2, event2);
        },
        "he",
        "allowChooseAll"
      ).set("ai", (card) => 6 - get.value(card)).forResult();
      if (result2?.bool) {
        await player.recast(result2.cards);
        player.addTempSkill("wxdl_kejie_sha");
        player.addMark("wxdl_kejie_sha", result2.cards.length, false);
      }
    },
    chooseBool(player, source, eventId) {
      const next = player.chooseBool(`###克捷###<div class="text center">是否对${get.translation(source)}发动${get.poptip("xiantu")}？</div>`);
      next.set("id", eventId);
      next.set("_global_waiting", true);
      next.set("choice", get.attitude(player, source) > 0);
      return next;
    },
    subSkill: {
      sha: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "出杀次数+#"
        },
        mod: {
          cardUsable(card, player, num) {
            if (card?.name === "sha") {
              return num + player.countMark("wxdl_kejie_sha");
            }
          }
        }
      }
    },
    derivation: "xiantu"
  },
  wxdl_hongqi: {
    audio: 2,
    trigger: {
      player: "phaseZhunbeiBegin"
    },
    limited: true,
    zhuSkill: true,
    skillAnimation: true,
    animationColor: "water",
    filter(event, player) {
      const card = new lib.element.VCard({ name: "wanjian", isCard: true });
      return player.hasUseTarget(card);
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const card = new lib.element.VCard({ name: "wanjian", isCard: true });
      await player.chooseUseTarget(card, true);
      const func = async (target) => {
        if (!target?.isIn() || target.group !== "wei" || target === player) {
          return;
        }
        await target.chooseToUse((card2, player2, event2) => {
          if (get.name(card2) !== "sha") {
            return false;
          }
          return lib.filter.filterCard(card2, player2, event2);
        }, "洪起：是否使用一张杀？");
      };
      await game.doAsyncInOrder(
        game.filterPlayer(() => true),
        func
      );
    }
  }
};
const skills$4 = {
  //KiraKira⭐DokiDoki
  //哥布林
  goblin_yibao: {
    forced: true,
    trigger: {
      player: "damageEnd",
      source: "damageSource"
    },
    filter(event, player) {
      return event.player?.isIn();
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.draw(3);
      if (trigger.source !== player) {
        return;
      }
      let list = [];
      for (let i = 1; i <= 5; i++) {
        const slot = `equip${i}`;
        if (target.hasEnabledSlot(slot)) {
          list.push(slot);
        }
      }
      if (get.is.mountCombined()) {
        if (list.includes("equip3") || list.includes("equip4")) {
          list.push("equip3_4");
        }
        list.remove("equip3", "equip4");
      }
      if (list.length) {
        const slot = list.randomGet();
        if (slot === "equip3_4") {
          await target.disableEquip(["equip3", "equip4"]);
        } else {
          await target.disableEquip([slot]);
        }
      }
      const num = Math.min(2, target.countGainableCards(player, "h"));
      if (num > 0 && target != player) {
        await player.gainPlayerCard(target, "h", num, "visible");
      }
    }
  },
  goblin_rulin: {
    forced: true,
    trigger: {
      source: "damageBegin4"
    },
    filter(event, player) {
      return event.num >= event.player.hp;
    },
    async content(event, trigger, player) {
      const num = trigger.num;
      trigger.cancel();
      await trigger.player.loseMaxHp();
      await player.gainMaxHp();
      await player.recover(num);
    }
  },
  goblin_kuangbao: {
    forced: true,
    trigger: {
      player: "damageBegin3",
      source: "damageBegin1"
    },
    filter(event, player) {
      if ([event.source, event.player].some((current) => !current?.isIn())) {
        return false;
      }
      return event.source.countCards("h") > event.player.countCards("h");
    },
    async content(event, trigger, player) {
      trigger.num++;
      if (trigger.card) {
        const evt = trigger.getParent((evtx) => evtx.name == "useCard" && evtx.card == trigger.card, true);
        if (evt && evt.addCount !== false) {
          evt.addCount = false;
          const stat = evt.player.getStat("card"), name = evt.card.name;
          if (typeof stat[name] == "number") {
            stat[name]--;
          }
        }
      }
    }
  },
  goblin_feisheng: {
    forced: true,
    trigger: {
      global: "phaseEnd"
    },
    filter(event, player) {
      let num = 0;
      player.getHistory("damage", (evt) => {
        num += evt.num;
      });
      return num > 1;
    },
    async content(event, trigger, player) {
      await player.loseMaxHp(2);
      player.insertPhase();
    }
  },
  goblin_shiqiang: {
    forced: true,
    trigger: {
      player: "damageBegin3",
      source: "damageBegin1"
    },
    filter(event, player, name) {
      const target = event[name == "damageBegin1" ? "player" : "source"];
      if (!target?.isIn()) {
        return false;
      }
      const num = player.countCards("h") - target.countCards("h");
      if (num == 0) {
        return false;
      }
      return num > 0 === (name == "damageBegin1");
    },
    async content(event, trigger, player) {
      trigger.num++;
    }
  },
  goblin_lingruo: {
    forced: true,
    trigger: {
      source: "damageSource"
    },
    filter(event, player) {
      return event.player?.isIn() && event.player.countGainableCards(player, "he");
    },
    logTarget: "player",
    async content(event, trigger, player) {
      await player.gainPlayerCard(trigger.player, "he", true);
    }
  },
  //董绾
  dongwan_moli: {
    transformSkill: true,
    trigger: {
      player: ["enterGame", "gainAfter"],
      global: ["phaseBefore", "loseAsyncAfter"]
    },
    markimage: "image/card/magic.png",
    intro: {
      name: "魔力·董绾",
      content(storage, player) {
        return `当前魔力：${storage}/5`;
      }
    },
    filter(event, player, name) {
      if (name == "enterGame") {
        return true;
      }
      if (event.name == "phase") {
        return game.phaseNumber == 0;
      }
      const evt = event.getParent("phaseDraw", true);
      if (evt?.player == player) {
        return false;
      }
      return event.getg?.(player)?.length;
    },
    frequent: true,
    async content(event, trigger, player) {
      const num = 1;
      player.addMark(event.name, num, false);
      game.log(player, "获得了", num, "点", "<span style='color: #d69dc8ff'>魔力</span>");
    },
    derivation: ["shinin_shengyan", "shinin_gongming"],
    group: "dongwan_moli_transform",
    subSkill: {
      transform: {
        trigger: {
          player: "phaseJieshuBegin"
        },
        filter(event, player) {
          if (get.nameList(player).every((name) => !get.character(name, 3).includes("dongwan_moli"))) {
            return false;
          }
          return player.countMark("dongwan_moli") >= 5;
        },
        skillAnimation: true,
        animationColor: "key",
        prompt2: "变身为闪耀战姬",
        async content(event, trigger, player) {
          const skill = "dongwan_moli";
          const cards2 = player.getDiscardableCards(player, "j");
          if (cards2.length) {
            await player.modedDiscard(cards2);
          }
          const name = get.nameList(player).find((name2) => get.character(name2, 3).includes(skill));
          if (!name) {
            return;
          }
          const str = lib.translate[`${skill}_append`] || "";
          if (str.length) {
            player.chat(str);
          }
          const infox = player.getStorage(event.name, {}), targetName = infox.targetName || "awaken_shinin_dongwan", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          await player.reinitCharacter(name, targetName, false);
          player.markSkill(skill);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.name, info);
          player.addSkill("dongwan_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "变身成了<span style='color: #d69dc8ff'>闪耀战姬</span>！");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      },
      transformBack: {
        trigger: {
          player: "removeMark"
        },
        filter(event, player) {
          const { markName: name, num } = event;
          return name == "dongwan_moli" && !player.countMark(name) && num > 0;
        },
        charlotte: true,
        async cost(event, trigger, player) {
          const infox = player.getStorage(event.skill.slice(0, -4), {}), targetName = infox.targetName || "shinin_dongwan", name = infox.name || "awaken_shinin_dongwan", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          if (!get.nameList(player).includes(name)) {
            return;
          }
          await player.reinitCharacter(name, targetName, false);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.skill.slice(0, -4), info);
          player.removeSkill("dongwan_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "解除了<span style='color: #d69dc8ff'>闪耀战姬</span>变身");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      }
    }
  },
  shinin_shengyan: {
    forced: true,
    trigger: {
      player: "useCard"
    },
    filter(event, player) {
      if (!event.targets?.length) {
        return false;
      }
      const type = get.type(event.card);
      if (type != "basic" && type != "trick") {
        return false;
      }
      return true;
    },
    async content(event, trigger, player) {
      trigger.effectCount++;
      game.log(trigger.card, "额外结算一次");
      if (!game.hasPlayer((current) => current != player)) {
        return;
      }
      const result = await player.chooseButtonTarget({
        createDialog: [
          "圣焰：选择一项并对一名其他角色造成1点火焰伤害",
          [
            [
              ["moli", "消耗1点魔力"],
              ["hp", "失去1点体力"]
            ],
            "tdnodes"
          ]
        ],
        filterTarget: lib.filter.notMe,
        ai1(button) {
          const player2 = get.player();
          if (player2.hp > 1 && player2.hasSkill("shinin_gongming")) {
            return button.link == "hp" ? 2 : 1;
          }
          return button.link == "hp" ? 1 : 2;
        },
        ai2(target) {
          const player2 = get.player();
          return get.damageEffect(target, player2, player2, "fire");
        },
        forced: true
      }).forResult();
      if (result?.links?.length && result.targets?.length) {
        if (result.links[0] == "hp") {
          await player.loseHp();
        } else {
          player.removeMark("dongwan_moli", 1, false);
          game.log(player, `消耗了1点`, "<span style='color: #d69dc8ff'>魔力</span>");
        }
        const target = result.targets[0];
        player.line(target, "fire");
        target.damage("fire");
      }
    }
  },
  shinin_gongming: {
    trigger: {
      player: ["removeMark", "loseHpAfter"]
    },
    logTarget(event, player) {
      const getTitles = (current) => {
        return get.nameList(current).map((name) => get.characterTitle(name)).filter((title) => title.length).toUniqued();
      };
      return game.filterPlayer((current) => getTitles(current).containsSome(...getTitles(player)));
    },
    filter(event, player) {
      if (event.name == "removeMark") {
        if (event.num <= 0 || event.markName != "dongwan_moli") {
          return false;
        }
      }
      const targets = get.info("shinin_gongming").logTarget(event, player);
      return targets?.length;
    },
    check(event, player) {
      const targets = get.info("shinin_gongming").logTarget(event, player);
      return targets.reduce((sum, current) => sum + get.effect(current, { name: "draw" }, player, player), 0) >= 0;
    },
    frequent(event, player) {
      const targets = get.info("shinin_gongming").logTarget(event, player);
      return player.getFriends(true).containsAll(...targets);
    },
    onremove: true,
    async content(event, trigger, player) {
      player.addMark(event.name, 1, false);
      await game.asyncDraw(event.targets, player.countMark(event.name));
    }
  },
  //吕玲绮
  lvlingqi_moli: {
    transformSkill: true,
    trigger: {
      player: "enterGame",
      global: "phaseBefore",
      source: "damageBegin1"
    },
    markimage: "image/card/magic.png",
    intro: {
      name: "魔力·吕玲绮",
      content(storage, player) {
        return `当前魔力：${storage}/5`;
      }
    },
    getIndex(event, player) {
      if (event.name == "damage") {
        return event.num;
      }
      return 1;
    },
    filter(event, player, name) {
      if (player.countMark("lvlingqi_moli") >= 5) {
        return false;
      }
      return event.name != "phase" || game.phaseNumber == 0;
    },
    frequent: true,
    async content(event, trigger, player) {
      const num = 1;
      {
        player.addMark(event.name, num, false);
        game.log(player, "获得了", num, "点", "<span style='color: #d69dc8ff'>魔力</span>");
      }
    },
    derivation: ["shinin_henghui", "shinin_moqi"],
    group: "lvlingqi_moli_transform",
    subSkill: {
      transform: {
        trigger: {
          player: "phaseZhunbeiBegin"
        },
        filter(event, player) {
          if (get.nameList(player).every((name) => !get.character(name, 3).includes("lvlingqi_moli"))) {
            return false;
          }
          if (player.countMark("lvlingqi_moli") < 5) {
            return false;
          }
          return true;
        },
        skillAnimation: true,
        animationColor: "key",
        prompt2: "变身为闪耀战姬",
        async content(event, trigger, player) {
          const skill = "lvlingqi_moli";
          const cards2 = player.getDiscardableCards(player, "j");
          if (cards2.length) {
            await player.modedDiscard(cards2);
          }
          const name = get.nameList(player).find((name2) => get.character(name2, 3).includes(skill));
          if (!name) {
            return;
          }
          const str = lib.translate[`${skill}_append`] || "";
          if (str.length) {
            player.chat(str);
          }
          const infox = player.getStorage(event.name, {}), targetName = infox.targetName || "awaken_shinin_lvlingqi", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          await player.reinitCharacter(name, targetName, false);
          player.markSkill(skill);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.name, info);
          player.addSkill("lvlingqi_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "变身成了<span style='color: #d69dc8ff'>闪耀战姬</span>！");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      },
      transformBack: {
        trigger: {
          player: "removeMark"
        },
        filter(event, player) {
          const { markName: name, num } = event;
          if (event.getParent().name == "shinin_moqi_backup") {
            return false;
          }
          return name == "lvlingqi_moli" && !player.countMark(name) && num > 0;
        },
        charlotte: true,
        async cost(event, trigger, player) {
          const infox = player.getStorage(event.skill.slice(0, -4), {}), targetName = infox.targetName || "shinin_lvlingqi", name = infox.name || "awaken_shinin_lvlingqi", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          if (!get.nameList(player).includes(name)) {
            return;
          }
          await player.reinitCharacter(name, targetName, false);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.skill.slice(0, -4), info);
          player.removeSkill("lvlingqi_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "解除了<span style='color: #d69dc8ff'>闪耀战姬</span>变身");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      }
    }
  },
  shinin_henghui: {
    trigger: {
      player: "transformCharacter"
    },
    filter(event, player) {
      if (!get.character(event.targetName, 3).includes("shinin_henghui")) {
        return false;
      }
      return player.countMark("lvlingqi_moli");
    },
    async cost(event, trigger, player) {
      const result = await player.chooseButton(
        [
          "恒辉：选择任意项并消耗等量魔力",
          [
            [
              ["limit", "使用【杀】的次数和造成的伤害+1"],
              ["range", "使用【杀】无距离限制且可选目标数+2"],
              ["draw", "摸五张牌并弃置装备区里的所有牌"]
            ],
            "textbutton"
          ]
        ],
        [1, Math.min(3, player.countMark("lvlingqi_moli"))]
      ).set("ai", () => 1).forResult();
      if (result.bool) {
        event.result = {
          bool: true,
          cost_data: result.links
        };
      }
    },
    locked: true,
    async content(event, trigger, player) {
      const { cost_data: list } = event;
      player.removeMark("lvlingqi_moli", list.length, false);
      game.log(player, `消耗了${list.length}点`, "<span style='color: #d69dc8ff'>魔力</span>");
      if (list.includes("limit")) {
        player.addSkill(`${event.name}_limit`);
      }
      if (list.includes("range")) {
        player.addSkill(`${event.name}_range`);
      }
      if (list.includes("draw")) {
        await player.draw(5);
        const cards2 = player.getDiscardableCards(player, "e");
        if (cards2.length) {
          await player.modedDiscard(cards2);
        }
      }
    },
    onremove(player, skill) {
      player.removeSkill(`${skill}_limit`);
      player.removeSkill(`${skill}_range`);
    },
    subSkill: {
      limit: {
        charlotte: true,
        trigger: {
          source: "damageBegin1"
        },
        filter(event, player) {
          return event.card?.name == "sha";
        },
        forced: true,
        async content(event, trigger, player) {
          let num = player.hasSkill("shinin_moqi_effect") ? 2 : 1;
          trigger.num += num;
        },
        mod: {
          cardUsable(card, player, num) {
            if (card.name == "sha") {
              let count = player.hasSkill("shinin_moqi_effect") ? 2 : 1;
              return num + count;
            }
          }
        }
      },
      range: {
        charlotte: true,
        mod: {
          targetInRange: () => true,
          selectTarget(card, player, range) {
            if (range[1] == -1 || get.name(card) != "sha") {
              return;
            }
            let info = lib.card[card.name];
            if (!info || info.notarget || info.selectTarget != 1) {
              return;
            }
            let num = player.hasSkill("shinin_moqi_effect") ? 4 : 2;
            range[1] += num;
          }
        }
      }
    }
  },
  shinin_moqi: {
    enable: "phaseUse",
    filter(event, player) {
      if (player.hasSkill("shinin_moqi_effect")) {
        return false;
      }
      return player.countMark("lvlingqi_moli") >= 3;
    },
    chooseButton: {
      dialog(event, player) {
        return ui.create.dialog("###魔契###" + get.skillInfoTranslation("shinin_moqi", null, false));
      },
      chooseControl(event, player) {
        let list = [], num = 3;
        while (num <= player.countMark("lvlingqi_moli")) {
          list.push(`${num}点`);
          num++;
        }
        return [...list, "cancel2"];
      },
      check() {
        return "cancel2";
      },
      backup(result, player) {
        return {
          audio: "shinin_moqi",
          index: result.index,
          skillAnimation: true,
          animationColor: "metal",
          async content(event, trigger, player2) {
            const index = get.info(event.name).index + 1;
            player2.removeMark("lvlingqi_moli", index, false);
            game.log(player2, `消耗了${index}点`, "<span style='color: #d69dc8ff'>魔力</span>");
            const hp = player2.getHp();
            if (hp > 0) {
              await player2.loseHp(hp);
            }
            player2.addSkill("shinin_moqi_effect");
            player2.when("phaseJieshuBegin").step(async (event2, trigger2, player3) => {
              const next = game.createEvent("transfromBack", false);
              next.player = player3;
              next.skill = "lvlingqi_moli_transformBack";
              next.setContent(get.info("lvlingqi_moli_transformBack").cost);
              await next;
            });
          }
        };
      }
    },
    subSkill: {
      backup: {},
      effect: {
        charlotte: true
      }
    },
    onremove(player, skill) {
      player.removeSkill(`${skill}_effect`);
    }
  },
  //芮姬
  ruiji_moli: {
    transformSkill: true,
    trigger: {
      player: ["enterGame", "damageBegin4"],
      global: "phaseBefore",
      source: "damageBegin1"
    },
    markimage: "image/card/magic.png",
    intro: {
      name: "魔力·芮姬",
      content(storage, player) {
        return `当前魔力：${storage}/5`;
      }
    },
    filter(event, player, name) {
      if (player.countMark("ruiji_moli") >= 5) {
        return false;
      }
      return event.name != "phase" || game.phaseNumber == 0;
    },
    frequent: true,
    async content(event, trigger, player) {
      const num = 1;
      {
        player.addMark(event.name, num, false);
        game.log(player, "获得了", num, "点", "<span style='color: #d69dc8ff'>魔力</span>");
      }
    },
    derivation: ["shinin_shengcai", "shinin_guanghui"],
    group: "ruiji_moli_transform",
    subSkill: {
      transform: {
        trigger: {
          player: ["dying", "useSkillAfter", "logSkill"]
        },
        filter(event, player) {
          if (get.nameList(player).every((name) => !get.character(name, 3).includes("ruiji_moli"))) {
            return false;
          }
          if (player.countMark("ruiji_moli") < 5) {
            return false;
          }
          if (event.name == "dying") {
            return player.hp <= 0;
          }
          if (event.type != "player") {
            return false;
          }
          const skill = get.sourceSkillFor(event);
          return skill == "dclingyin";
        },
        skillAnimation: true,
        animationColor: "key",
        prompt2: "变身为闪耀战姬",
        async content(event, trigger, player) {
          const skill = "ruiji_moli";
          const cards2 = player.getDiscardableCards(player, "j");
          if (cards2.length) {
            await player.modedDiscard(cards2);
          }
          const name = get.nameList(player).find((name2) => get.character(name2, 3).includes(skill));
          if (!name) {
            return;
          }
          const str = lib.translate[`${skill}_append`] || "";
          if (str.length) {
            player.chat(str);
          }
          const infox = player.getStorage(event.name, {}), targetName = infox.targetName || "awaken_shinin_ruiji", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          await player.reinitCharacter(name, targetName, false);
          player.markSkill(skill);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.name, info);
          player.addSkill("ruiji_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "变身成了<span style='color: #d69dc8ff'>闪耀战姬</span>！");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      },
      transformBack: {
        trigger: {
          player: "removeMark"
        },
        filter(event, player) {
          const { markName: name, num } = event;
          return name == "ruiji_moli" && !player.countMark(name) && num > 0;
        },
        charlotte: true,
        async cost(event, trigger, player) {
          const infox = player.getStorage(event.skill.slice(0, -4), {}), targetName = infox.targetName || "shinin_ruiji", name = infox.name || "awaken_shinin_ruiji", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          if (!get.nameList(player).includes(name)) {
            return;
          }
          await player.reinitCharacter(name, targetName, false);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.skill.slice(0, -4), info);
          player.removeSkill("ruiji_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "解除了<span style='color: #d69dc8ff'>闪耀战姬</span>变身");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      }
    }
  },
  shinin_shengcai: {
    enable: "phaseUse",
    filterCard: true,
    position: "hs",
    selectCard() {
      const player = get.player();
      return [1, player.countMark("ruiji_moli")];
    },
    filter(event, player) {
      return player.countMark("ruiji_moli") && player.countCards("hs");
    },
    viewAs: {
      name: "sha",
      storage: {
        shinin_shengcai: true
      }
    },
    check(card) {
      if (ui.selected.cards.length) {
        return 0;
      }
      return 15 - get.value(card);
    },
    async precontent(event, trigger, player) {
      const evt = event.getParent(), num = event.result.cards.length || 0;
      player.when("useCard").filter((event2) => event2.getParent() == evt).step(async (event2, trigger2, player2) => {
        trigger2.baseDamage ??= 1;
        trigger2.baseDamage += num;
        player2.removeMark("ruiji_moli", num, false);
        game.log(player2, `消耗了${num}点`, "<span style='color: #d69dc8ff'>魔力</span>");
      });
      evt.addCount = false;
    },
    locked: true,
    mod: {
      cardUsable(card, player) {
        if (card?.storage?.shinin_shengcai) {
          return Infinity;
        }
      },
      targetInRange(card, player) {
        if (card?.storage?.shinin_shengcai) {
          return true;
        }
      }
    }
  },
  shinin_guanghui: {
    forced: true,
    trigger: {
      global: ["useCard", "damageBegin1"]
    },
    filter(event, player) {
      if (event.name == "useCard") {
        return get.nameList(event.player).some((name) => {
          return name.startsWith("awaken_shinin_");
        });
      }
      if (!event.source?.isIn() || player.countMark("ruiji_moli") >= 5) {
        return false;
      }
      const getTitles = (current) => {
        return get.nameList(current).map((name) => get.characterTitle(name)).filter((title) => title.length).toUniqued();
      };
      return getTitles(event.source).containsSome(...getTitles(player));
    },
    logTarget(event, player) {
      if (event.name == "useCard") {
        return event?.player;
      }
      return event?.source;
    },
    async content(event, trigger, player) {
      if (trigger.name == "damage") {
        player.addMark("ruiji_moli", 1, false);
        game.log(player, "获得了", 1, "点", "<span style='color: #d69dc8ff'>魔力</span>");
        return;
      }
      const targets = game.filterPlayer((current) => {
        return get.nameList(current).every((name) => {
          return !name.startsWith("awaken_shinin_");
        });
      });
      if (targets.length) {
        trigger.directHit.addArray(targets);
      }
    }
  },
  //吴国太
  wuguotai_moli: {
    transformSkill: true,
    trigger: {
      player: "enterGame",
      global: ["phaseBefore", "loseAfter", "loseAsyncAfter", "gainAfter", "addToExpansionAfter", "addJudgeAfter", "equipAfter"]
    },
    markimage: "image/card/magic.png",
    intro: {
      name: "魔力·吴国太",
      content(storage, player) {
        return `当前魔力：${storage}/5`;
      }
    },
    filter(event, player, name) {
      if (player.countMark("wuguotai_moli") >= 5) {
        return false;
      }
      if (name == "enterGame" || event.name == "phase" && game.phaseNumber == 0) {
        return true;
      }
      if (["addJudge", "equip"].includes(event.name) && event.player == player) {
        return true;
      }
      const evt = event.getl?.(player);
      return evt && (evt.es?.length || evt.js?.length);
    },
    frequent: true,
    async content(event, trigger, player) {
      const num = 1;
      {
        player.addMark(event.name, num, false);
        game.log(player, "获得了", num, "点", "<span style='color: #d69dc8ff'>魔力</span>");
      }
    },
    derivation: ["shinin_fengshou", "shinin_susheng"],
    group: "wuguotai_moli_transform",
    subSkill: {
      transform: {
        trigger: {
          player: "loseAfter",
          global: ["loseAsyncAfter", "gainAfter", "addToExpansionAfter", "addJudgeAfter", "equipAfter"]
        },
        filter(event, player) {
          if (get.nameList(player).every((name) => !get.character(name, 3).includes("wuguotai_moli"))) {
            return false;
          }
          if (!event?.getl?.(player)?.cards2?.length) {
            return false;
          }
          return player.countMark("wuguotai_moli") >= 5;
        },
        skillAnimation: true,
        animationColor: "key",
        prompt2: "变身为闪耀战姬",
        async content(event, trigger, player) {
          const skill = "wuguotai_moli";
          const cards2 = player.getDiscardableCards(player, "j");
          if (cards2.length) {
            await player.modedDiscard(cards2);
          }
          const name = get.nameList(player).find((name2) => get.character(name2, 3).includes(skill));
          if (!name) {
            return;
          }
          const str = lib.translate[`${skill}_append`] || "";
          if (str.length) {
            player.chat(str);
          }
          const infox = player.getStorage(event.name, {}), targetName = infox.targetName || "awaken_shinin_wuguotai", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          await player.reinitCharacter(name, targetName, false);
          player.markSkill(skill);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.name, info);
          player.addSkill("wuguotai_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "变身成了<span style='color: #d69dc8ff'>闪耀战姬</span>！");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      },
      transformBack: {
        trigger: {
          player: "removeMark"
        },
        filter(event, player) {
          const { markName: name, num } = event;
          return name == "wuguotai_moli" && !player.countMark(name) && num > 0;
        },
        charlotte: true,
        async cost(event, trigger, player) {
          const infox = player.getStorage(event.skill.slice(0, -4), {}), targetName = infox.targetName || "shinin_wuguotai", name = infox.name || "awaken_shinin_wuguotai", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          if (!get.nameList(player).includes(name)) {
            return;
          }
          await player.reinitCharacter(name, targetName, false);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.skill.slice(0, -4), info);
          player.removeSkill("wuguotai_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "解除了<span style='color: #d69dc8ff'>闪耀战姬</span>变身");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      }
    }
  },
  shinin_fengshou: {
    trigger: {
      player: "transformCharacter"
    },
    filter(event, player) {
      if (!get.character(event.targetName, 3).includes("shinin_fengshou")) {
        return false;
      }
      return game.countPlayer((current) => get.distance(current, player) == 1) >= 2;
    },
    logTarget(event, player) {
      return game.filterPlayer((current) => get.distance(current, player) == 1);
    },
    frequent: true,
    async content(event, trigger, player) {
      const toSortPlayers = game.filterPlayer2((current) => true, null, true);
      toSortPlayers.sortBySeat(game.findPlayer2((current) => current.getSeatNum() >= 1 + toSortPlayers.length / 2, true));
      const next = player.chooseToMove("凤守：是否分配与你距离为1的所有角色的座次？");
      next.set("list", [
        [
          "座次",
          [
            toSortPlayers.map((i) => `${i.getSeatNum()}|${i.name}`),
            (item, type, position, noclick, node) => {
              node = get.info("tamo").$createButton(item, type, position, noclick, node);
              const seat = parseInt(item.split("|")), target = game.findPlayer((current) => current.getSeatNum() == seat);
              if (!target || get.distance(target, get.player()) !== 1) {
                node.classList.add("unselectable");
              }
              return node;
            }
          ]
        ]
      ]);
      next.set("toSortPlayers", toSortPlayers.slice(0));
      next.set("filterMove", (from, to, moved2) => {
        if (typeof to == "number") {
          return false;
        }
        const player2 = get.player(), filter = (info) => {
          const seat = parseInt(info.link.split("|")[0]);
          const target = game.findPlayer2((current) => current.getSeatNum() == seat, true);
          return target?.isIn() && get.distance(target, player2) == 1;
        };
        return filter(from) && filter(to);
      });
      next.set("processAI", () => {
        const players = get.event().toSortPlayers, player2 = get.player();
        let sortedTargets = players.filter((current) => current?.isIn() && get.distance(current, player2) == 1), sortedIndex = sortedTargets.map((current) => players.indexOf(current));
        sortedTargets.sort((a, b) => get.attitude(player2, a) - get.attitude(player2, b));
        sortedIndex.forEach((index) => {
          players[index] = sortedTargets.shift();
        });
        return [players.map((i) => `${i.getSeatNum()}|${i.name}`)];
      });
      const result = await next.forResult();
      const moved = result?.moved;
      const resultList = moved[0].map((info) => {
        return parseInt(info.split("|")[0]);
      });
      const toSwapList = [];
      const cmp = (a, b) => {
        return resultList.indexOf(a) - resultList.indexOf(b);
      };
      for (let i = 0; i < toSortPlayers.length; i++) {
        for (let j = 0; j < toSortPlayers.length; j++) {
          if (cmp(toSortPlayers[i].getSeatNum(), toSortPlayers[j].getSeatNum()) < 0) {
            toSwapList.push([toSortPlayers[i], toSortPlayers[j]]);
            [toSortPlayers[i], toSortPlayers[j]] = [toSortPlayers[j], toSortPlayers[i]];
          }
        }
      }
      game.broadcastAll((toSwapList2) => {
        for (const list of toSwapList2) {
          game.swapSeat(list[0], list[1], false);
        }
      }, toSwapList);
      if (!player.countMark("wuguotai_moli")) {
        return;
      }
      const result2 = await player.chooseTarget("消耗任意点魔力，令等量角色受到的火焰伤害+1", [1, player.countMark("wuguotai_moli")]).set("ai", (target) => {
        const player2 = get.player();
        return get.damageEffect(target, player2, player2, "fire") >= 3;
      }).forResult();
      if (!result2?.bool || !result2.targets?.length) {
        return;
      }
      const num = result2.targets.length;
      player.removeMark("wuguotai_moli", num, false);
      game.log(player, `消耗了${num}点`, "<span style='color: #d69dc8ff'>魔力</span>");
      player.line(result2.targets, "green");
      const func = async (target) => {
        target.addSkill("shinin_fengshou_fire");
        target.addMark("shinin_fengshou_fire", 1, false);
      };
      await game.doAsyncInOrder(result2.targets, func);
    },
    subSkill: {
      fire: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "受到的火焰伤害+#"
        },
        trigger: {
          player: "damageBegin3"
        },
        filter(event, player) {
          return game.hasNature(event, "fire") && player.countMark("shinin_fengshou_fire");
        },
        forced: true,
        async content(event, trigger, player) {
          trigger.num += player.countMark(event.name);
        }
      }
    }
  },
  shinin_susheng: {
    trigger: {
      global: "dying"
    },
    filter(event, player) {
      if (!player.countMark("wuguotai_moli") || event.player.hp > 0) {
        return false;
      }
      const getTitles = (current) => {
        return get.nameList(current).map((name) => get.characterTitle(name)).filter((title) => title.length).toUniqued();
      };
      return getTitles(event.player).containsSome(...getTitles(player));
    },
    async cost(event, trigger, player) {
      const result = await player.chooseNumbers(get.prompt(event.skill, trigger.player), [
        {
          prompt: "消耗任意点魔力，令其恢复等量体力和等量魔力，然后你摸等量的牌",
          min: 1,
          max: player.countMark("wuguotai_moli")
        }
      ]).set("processAI", () => {
        const player2 = get.player(), target = get.event().getTrigger().player;
        if (get.attitude(player2, target) <= 0) {
          return false;
        }
        if (player2 == target) {
          return [Math.min(player2.maxHp - player2.hp, player2.countMark("wuguotai_moli") - 1)];
        }
        return [Math.min(1 - target.hp, player2.countMark("wuguotai_moli"))];
      }).forResult();
      if (result.bool) {
        event.result = {
          bool: true,
          cost_data: result.numbers[0],
          targets: [trigger.player]
        };
      }
    },
    async content(event, trigger, player) {
      const {
        targets: [target],
        cost_data: num
      } = event;
      player.removeMark("wuguotai_moli", num, false);
      game.log(player, `消耗了${num}点`, "<span style='color: #d69dc8ff'>魔力</span>");
      await target.recover(num);
      const names = get.nameList(target).filter((name) => {
        if (name.startsWith("awaken_shinin_")) {
          return get.info(`${name.slice(14)}_moli`)?.transformSkill;
        }
        return name.startsWith("shinin_") && get.character(name, 3)?.includes(`${name.slice(7)}_moli`);
      });
      if (names.length > 0) {
        const result = names.length > 1 ? await player.chooseButton(["选择回复魔力的武将牌", [names, "character"]], true).set("ai", (button) => {
          if (button.link.endsWith("wuguotai")) {
            return 2;
          }
          return 1 + Math.random();
        }).forResult() : {
          bool: true,
          links: names
        };
        if (result?.bool) {
          const name = result.links?.map((name2) => {
            if (name2.startsWith("awaken_shinin_")) {
              return `${name2.slice(14)}_moli`;
            }
            return `${name2.slice(7)}_moli`;
          })[0];
          const numx = Math.min(5 - target.countMark(name), num);
          if (numx > 0) {
            player.addMark(name, numx, false);
            game.log(player, "获得了", numx, "点", "<span style='color: #d69dc8ff'>魔力</span>");
          }
        }
      }
      await player.draw(num);
    }
  },
  //甄姬
  shinin_luoshen: {
    audio: "luoshen",
    trigger: { player: "phaseZhunbeiBegin" },
    frequent: true,
    async content(event, trigger, player) {
      event.bool = true;
      while (event.bool) {
        await player.judge((card) => {
          return get.color(card) == "black" ? 1.5 : -1.5;
        }).set("judge2", (result) => result.bool).set("callback", async (event2, trigger2, player2) => {
          if (event2.judgeResult.color == "black" && get.position(event2.card, true) == "o") {
            await player2.gain(event2.card, "gain2");
          }
          const bool = event2.judgeResult.color == "black" && (await player2.chooseBool("是否继续发动【洛神】？").set("frequentSkill", "shinin_luoshen").forResult()).bool;
          if (!bool) {
            event2.getParent(2).bool = false;
          }
        });
      }
      const num = player.getHistory("gain", (evt) => evt.getParent(event.name) == event).length;
      if (num > 0) {
        const name = `${event.name}_effect`;
        player.addTempSkill(name);
        player.addMark(name, num, false);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "本回合手牌上限+#"
        },
        mod: {
          maxHandcard(player, num) {
            return num + player.countMark("shinin_luoshen_effect");
          }
        }
      }
    }
  },
  zhenji_moli: {
    transformSkill: true,
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    markimage: "image/card/magic.png",
    intro: {
      name: "魔力·甄宓",
      content(storage, player) {
        return `当前魔力：${storage}/5`;
      }
    },
    filter(event, player) {
      if (event.getParent("phaseDraw", true)) {
        return false;
      }
      return event.getg?.(player)?.length;
    },
    frequent: true,
    async content(event, trigger, player) {
      const cards2 = trigger.getg(player);
      await player.showCards(cards2, `${get.translation(player)}发动了【魔力】`);
      const num = Math.min(5 - player.countMark(event.name), cards2.filter((card) => get.color(card) == "black").length);
      if (num > 0) {
        player.addMark(event.name, num, false);
        game.log(player, "获得了", num, "点", "<span style='color: #d69dc8ff'>魔力</span>");
      }
    },
    derivation: ["shinin_jinghong", "shinin_youlong", "shinin_haiyou"],
    group: "zhenji_moli_transform",
    subSkill: {
      transform: {
        trigger: {
          player: "phaseJieshuBegin"
        },
        filter(event, player) {
          if (get.nameList(player).every((name) => !get.character(name, 3).includes("zhenji_moli"))) {
            return false;
          }
          return player.countMark("zhenji_moli") >= 5;
        },
        skillAnimation: true,
        animationColor: "key",
        prompt2: "变身为闪耀战姬",
        async content(event, trigger, player) {
          const skill = "zhenji_moli";
          const cards2 = player.getDiscardableCards(player, "j");
          if (cards2.length) {
            await player.modedDiscard(cards2);
          }
          const name = get.nameList(player).find((name2) => get.character(name2, 3).includes(skill));
          if (!name) {
            return;
          }
          const str = lib.translate[`${skill}_append`] || "";
          if (str.length) {
            player.chat(str);
          }
          const infox = player.getStorage(event.name, {}), targetName = infox.targetName || "awaken_shinin_zhenji", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          await player.reinitCharacter(name, targetName, false);
          player.markSkill(skill);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.name, info);
          player.addSkill("zhenji_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "变身成了<span style='color: #d69dc8ff'>闪耀战姬</span>！");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      },
      transformBack: {
        trigger: {
          player: "removeMark"
        },
        filter(event, player) {
          const { markName: name, num } = event;
          return name == "zhenji_moli" && !player.countMark(name) && num > 0;
        },
        charlotte: true,
        async cost(event, trigger, player) {
          const infox = player.getStorage(event.skill.slice(0, -4), {}), targetName = infox.targetName || "shinin_zhenji", name = infox.name || "awaken_shinin_zhenji", info = {
            name: targetName,
            targetName: name,
            hp: player.hp,
            maxHp: player.maxHp
          };
          if (!get.nameList(player).includes(name)) {
            return;
          }
          await player.reinitCharacter(name, targetName, false);
          if (!Object.keys(infox).length) {
            let { hp, maxHp } = get.character(targetName);
            if (get.mode() == "doudizhu" || game.players.length + game.dead.length > 4) {
              if (!player.isInitFilter("noZhuHp")) {
                maxHp++;
                hp++;
              }
            }
            infox.name = name;
            infox.targetName = targetName;
            infox.hp = hp;
            infox.maxHp = maxHp;
          }
          player.setStorage(event.skill.slice(0, -4), info);
          player.removeSkill("zhenji_moli_transformBack");
          player.maxHp = infox.maxHp;
          player.hp = infox.hp;
          player.update();
          game.log(player, "解除了<span style='color: #d69dc8ff'>闪耀战姬</span>变身");
          const next = game.createEvent("transformCharacter", false);
          next.player = player;
          next.targetName = targetName;
          next.setContent("emptyEvent");
          await next;
          if (player.getHp() <= 0) {
            await player.dying({});
          }
        }
      }
    }
  },
  shinin_jinghong: {
    trigger: {
      global: ["gainAfter", "loseAsyncAfter"]
    },
    getIndex(event, player) {
      return game.filterPlayer((current) => event.getg?.(current)?.length);
    },
    filter(event, player, name, target) {
      if (event.getParent("shinin_jinghong", true)) {
        return false;
      }
      if (!player.countMark("zhenji_moli")) {
        return false;
      }
      const getTitles = (current) => {
        return get.nameList(current).map((name2) => get.characterTitle(name2)).filter((title) => title.length).toUniqued();
      };
      return getTitles(target).containsSome(...getTitles(player));
    },
    check(event, player, name, target) {
      return get.attitude(player, target) > 0;
    },
    locked: false,
    logTarget: (_1, _2, _3, target) => target,
    async content(event, trigger, player) {
      player.removeMark("zhenji_moli", 1, false);
      game.log(player, "消耗了1点", "<span style='color: #d69dc8ff'>魔力</span>");
      const target = event.targets[0];
      event.bool = true;
      while (event.bool) {
        await target.judge((card) => {
          return get.color(card) == "black" ? 1.5 : -1.5;
        }).set("judge2", (result) => result.bool).set("callback", async (event2, trigger2, player2) => {
          if (event2.judgeResult.color == "black" && get.position(event2.card, true) == "o") {
            await player2.gain(event2.card, "gain2");
          }
          if (event2.judgeResult.color !== "black") {
            event2.getParent(2).bool = false;
          }
        });
      }
    },
    group: "shinin_jinghong_limit",
    subSkill: {
      limit: {
        trigger: { player: "useCard1" },
        filter(event, player) {
          return get.color(event.card) == "black" && event.addCount !== false;
        },
        firstDo: true,
        async cost(event, trigger, player) {
          trigger.addCount = false;
          const stat = player.getStat().card, name = trigger.card.name;
          if (typeof stat[name] == "number") {
            stat[name]--;
          }
        }
      }
    },
    mod: {
      cardUsable(card, player, num) {
        const color = get.color(card);
        if (color == "unsure" || color == "black") {
          return Infinity;
        }
      },
      ignoredHandcard(card, player) {
        if (get.color(card, player) == "black") {
          return true;
        }
      },
      cardDiscardable(card, player, name) {
        if (name == "phaseDiscard" && get.color(card, player) == "black") {
          return false;
        }
      }
    }
  },
  shinin_youlong: {
    trigger: {
      global: "damageEnd",
      source: "damageBegin"
    },
    filter(event, player, name) {
      if (name == "damageEnd") {
        return game.hasNature(event, "ice") && event.player?.isIn();
      }
      return event.nature !== "ice";
    },
    forced: true,
    logTarget(event, player, name) {
      if (name == "damageEnd") {
        return event.player;
      }
      return false;
    },
    async content(event, trigger, player) {
      if (event.triggername == "damageBegin") {
        game.setNature(trigger, "ice");
        return;
      }
      const map = player.getStorage(event.name, /* @__PURE__ */ new Map()), target = event.targets[0];
      const count = (map.has(target) ? map.get(target) : 0) + 1;
      map.set(target, count);
      player.setStorage(event.name, map, true);
      target.markSkill("shinin_youlong_effect");
      player.markSkill("shinin_youlong_effect");
    },
    init(player, skill) {
      game.addGlobalSkill(`${skill}_effect`);
      player.addSkill(`${skill}_directHit`);
    },
    subSkill: {
      directHit: {
        trigger: {
          player: "useCard"
        },
        filter(event, player) {
          const targets = get.info("shinin_youlong_directHit").logTarget(event, player);
          return targets?.length;
        },
        logTarget(event, player) {
          const map = player?.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
          return game.filterPlayer((current) => map.has(current));
        },
        forced: true,
        async content(event, trigger, player) {
          trigger.directHit.addArray(event.targets);
        }
      },
      effect: {
        trigger: {
          player: ["phaseDrawBegin2", "phaseEnd"]
        },
        filter(event, player) {
          if (event.name != "phase" && event.numFixed) {
            return false;
          }
          const playerMap = player.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
          return game.hasPlayer((current) => {
            if (current == player) {
              return false;
            }
            const map = current.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
            return map.has(player) || event.name != "phase" && playerMap.has(current);
          });
        },
        intro: {
          markcount(storage, player) {
            const playerMap = player.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
            let num = 0;
            game.countPlayer((current) => {
              if (current == player) {
                return false;
              }
              const map = current.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
              if (map.has(player)) {
                num -= map.get(player);
              }
              if (playerMap.has(current)) {
                num += playerMap.get(current);
              }
            });
            return num;
          },
          content(storage, player) {
            const playerMap = player.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
            let num = 0;
            game.countPlayer((current) => {
              if (current == player) {
                return false;
              }
              const map = current.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
              if (map.has(player)) {
                num -= map.get(player);
              }
              if (playerMap.has(current)) {
                num += playerMap.get(current);
              }
            });
            return `摸牌阶段摸牌数${num >= 0 ? "+" : ""}${num}，计算与其他角色距离${num <= 0 ? "+" : ""}${-num}`;
          }
        },
        locked: true,
        async cost(event, trigger, player) {
          const playerMap = player.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
          let num = 0;
          game.countPlayer((current) => {
            if (current == player) {
              return false;
            }
            const map = current.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
            if (map.has(player)) {
              num -= map.get(player);
              if (trigger.name == "phase") {
                map.delete(player);
                current.markSkill(event.skill);
              }
            }
            if (playerMap.has(current)) {
              num += playerMap.get(current);
            }
          });
          event.result = {
            bool: trigger.name != "phase" && num != 0,
            cost_data: num
          };
          if (trigger.name == "phase") {
            player.markSkill(event.skill);
            return;
          }
        },
        async content(event, trigger, player) {
          const { cost_data: num } = event;
          trigger.num = Math.max(0, trigger.num + num);
        },
        mod: {
          globalFrom(from, to, range) {
            const playerMap = from.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
            game.countPlayer((current) => {
              if (current == from) {
                return false;
              }
              const map = current.getStorage("shinin_youlong", /* @__PURE__ */ new Map());
              if (map.has(from)) {
                range += map.get(from);
              }
              if (playerMap.has(current)) {
                range -= playerMap.get(current);
              }
            });
            return range;
          }
        }
      }
    }
  },
  shinin_haiyou: {
    trigger: {
      global: "useCardToTarget"
    },
    filter(event, player) {
      if (!player.countMark("zhenji_moli")) {
        return false;
      }
      const getTitles = (current) => {
        return get.nameList(current).map((name) => get.characterTitle(name)).filter((title) => title.length).toUniqued();
      };
      return getTitles(event.target).containsSome(...getTitles(player));
    },
    logTarget: "target",
    check(event, player) {
      return get.effect(event.target, event.card, event.player, player) < 0;
    },
    async content(event, trigger, player) {
      player.removeMark("zhenji_moli", 1, false);
      game.log(player, "消耗了1点", "<span style='color: #d69dc8ff'>魔力</span>");
      trigger.getParent().excluded.add(trigger.target);
    }
  }
};
const skills$3 = {
  //雁翎徐晃
  ylyg_duanliang: {
    audio: "duanliang",
    enable: "chooseToUse",
    filterCard(card) {
      return get.type2(card) !== "trick" && get.color(card) === "black";
    },
    filter(event, player) {
      return player.hasCard((card) => get.type2(card) !== "trick" && get.color(card) === "black", "hes");
    },
    position: "hes",
    viewAs: {
      name: "bingliang"
    },
    filterTarget(card, player, target) {
      return lib.filter.targetEnabled.call(this, card, player, target);
    },
    prompt: "将一张黑色非锦囊牌当做无距离限制的兵粮寸断使用",
    check(card) {
      return 6 - get.value(card);
    },
    onuse(result, player) {
      player.addTempSkill(`${result.skill}_draw`);
    },
    subSkill: {
      draw: {
        charlotte: true,
        forced: true,
        trigger: {
          player: "useCardAfter"
        },
        filter(event, player) {
          return event.skill === "ylyg_duanliang" && event.targets[0].countCards("h") > player.countCards("h");
        },
        async content(event, trigger, player) {
          await player.draw();
        }
      }
    }
  },
  ylyg_zier: {
    audio: "jiezi",
    trigger: {
      global: "phaseBegin"
    },
    round: 1,
    filter(event, player) {
      return event.phaseList?.length && player.getStorage("ylyg_zier").length > 0;
    },
    async cost(event, trigger, player) {
      const target = trigger.player;
      const result = await player.chooseButton({
        createDialog: [get.prompt2(event.skill, target), `<div class="text center">${get.translation(target)}当前回合的阶段</div>`, [trigger.phaseList.map((name, index) => [index + 1, "", `lusu_${name.split("|")[0]}`]), "vcard"], `<div class="text center">你已记录的阶段</div>`, [player.getStorage(event.skill).map((name) => ["", "", `lusu_${name.split("|")[0]}`]), "vcard"]],
        filterButton(button) {
          if (!ui.selected.buttons?.length) {
            return typeof button.link[0] === "number";
          }
          return typeof button.link[0] !== "number";
        },
        selectButton: 2,
        att: get.attitude(player, target),
        target,
        ai(button) {
          const { att, target: target2 } = get.event();
          const { link } = button;
          if (!ui.selected.buttons?.length) {
            if (att > 0) {
              if (link[2].includes("phaseDiscard")) {
                return 5;
              }
              if (link[2].includes("phaseJudge") && target2.hasCards("j")) {
                return 4;
              }
            }
            if (att < 0) {
              if (link[2].includes("phaseUse")) {
                return 5;
              }
              if (link[2].includes("phaseDraw")) {
                return 4;
              }
            }
            return 0;
          } else {
            if (att > 0) {
              if (link[2].includes("phaseUse")) {
                return 5;
              }
              if (link[2].includes("phaseDraw")) {
                return 4 + (target2.countCards("h") < 3 ? 2 : 0);
              }
            }
            if (att < 0) {
              if (!["phaseUse", "phaseDraw"].some((i) => link[2].includes(i))) {
                return 5;
              }
            }
            return 0;
          }
        }
      }).forResult();
      if (result?.bool) {
        result.links?.forEach((i) => i[2] = i[2].slice(5));
        event.result = {
          bool: true,
          cost_data: result.links
        };
      }
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const {
        cost_data: [prev, cur]
      } = event;
      game.log(event.targets[0], "的", `#g${get.translation(prev[2])}`, "改为了", `#g${get.translation(cur[2])}`);
      trigger.phaseList[prev[0] - 1] = `${cur[2]}|${event.name}`;
    },
    intro: {
      content: "已记录：$"
    },
    group: "ylyg_zier_record",
    subSkill: {
      record: {
        trigger: {
          global: ["phaseAnyCancelled", "phaseAnySkipped"]
        },
        filter(event, player) {
          return !player.getStorage("ylyg_zier").includes(event.name);
        },
        prompt2(event, player) {
          return `记录${get.translation(event.name)}`;
        },
        async content(event, trigger, player) {
          player.markAuto("ylyg_zier", trigger.name);
          player.addTempSkill("ylyg_zier_clear", "roundStart");
        }
      },
      clear: {
        charlotte: true,
        onremove(player, skill) {
          player.setStorage("ylyg_zier", [], true);
        }
      }
    }
  },
  //雁翎祝融
  ylyg_lieren: {
    audio: "lieren",
    trigger: {
      player: "useCardToPlayered"
    },
    filter(event, player) {
      if (get.name(event.card) !== "sha" || event.targets?.length !== 1) {
        return false;
      }
      return player.getHistory("useCard", (evt) => get.name(evt.card) === "sha").indexOf(event.getParent()) === 0 && player.canCompare(event.target);
    },
    logTarget: "target",
    check(event, player) {
      const { target } = event;
      if (get.attitude(player, target) > 0) {
        return false;
      }
      let maxnum = 0;
      const cards2 = target.getCards("h");
      for (const card of cards2) {
        if (get.number(card) > maxnum) {
          maxnum = get.number(card);
        }
      }
      if (maxnum > 10) {
        maxnum = 10;
      }
      if (maxnum < 5 && cards2.length > 1) {
        maxnum = 5;
      }
      const cards3 = player.getCards("h");
      for (const card of cards3) {
        if (get.number(card) < maxnum) {
          return true;
        }
      }
      return false;
    },
    async content(event, trigger, player) {
      const {
        targets: [target]
      } = event;
      const result = await player.chooseToCompare(target).set("small", get.attitude(player, target) < 0).forResult();
      if (!result.winner?.isIn()) {
        return;
      }
      const { winner } = result;
      winner.when({ global: "useCardAfter" }).filter((evt) => evt.card === trigger.card).then(async (event2, trigger2, player2) => {
        const hs = player2.getCards("h");
        if (!hs.length) {
          return;
        }
        const vcard = get.autoViewAs({ name: "nanman" }, hs);
        if (player2.hasUseTarget(vcard)) {
          await player2.chooseUseTarget({
            card: vcard,
            cards: hs,
            forced: true
          });
        }
      });
    }
  },
  ylyg_juxiang: {
    audio: "juxiang",
    trigger: {
      global: "useCard"
    },
    filter(event, player) {
      return event.card.name === "nanman";
    },
    async cost(event, trigger, player) {
      if (trigger.player === player) {
        event.result = await player.chooseBool({
          prompt: get.prompt(event.skill),
          prompt2: `令${get.translation(trigger.card)}对体力值大于你的角色造成的伤害+1`,
          choice: (() => {
            const { targets, card } = trigger;
            let eff = 0;
            for (const target of targets) {
              eff += get.effect(target, card, player, player);
            }
            return eff > 0;
          })()
        }).forResult();
      } else {
        event.result = { bool: true };
      }
    },
    async content(event, trigger, player) {
      if (trigger.player === player) {
        player.addTempSkill(`${event.name}_damage`);
        player.markAuto(`${event.name}_damage`, trigger.card);
      } else {
        trigger.excluded.add(player);
        player.when({ global: "useCardAfter" }).filter((evt) => evt.card === trigger.card).then(async (event2, trigger2, player2) => {
          const { cards: cards2 } = trigger2;
          if (cards2.someInD()) {
            await player2.gain({ cards: cards2.filterInD(), animate: "gain2" });
          }
        });
      }
    },
    subSkill: {
      damage: {
        charlotte: true,
        onremove: true,
        forced: true,
        popup: false,
        trigger: {
          source: "damageBegin1"
        },
        filter(event, player) {
          return player.getStorage("ylyg_juxiang_damage").includes(event.card) && event.player.getHp() > player.getHp();
        },
        async content(event, trigger, player) {
          trigger.num++;
        }
      }
    }
  },
  //雁翎庞统
  ylygxiangxing: {
    locked: true,
    derivation: ["reyingzi", "rebiyue"],
    global: ["ylygxiangxing_yingzi", "ylygxiangxing_biyue"]
  },
  ylygxiangxing_yingzi: {
    inherit: "reyingzi",
    filter(event, player) {
      return game.hasPlayer((target) => target !== player && target.hasSkill("ylygxiangxing")) && !event.numFixed;
    },
    mod: {
      maxHandcardBase(player, num) {
        if (game.hasPlayer((target) => target !== player && target.hasSkill("ylygxiangxing"))) {
          return player.maxHp;
        }
      }
    }
  },
  ylygxiangxing_biyue: {
    inherit: "rebiyue",
    frequent: false,
    filter(event, player) {
      return game.hasPlayer((target) => target !== player && target.hasSkill("ylygxiangxing"));
    }
  },
  ylyglianhuan: {
    enable: "phaseUse",
    filterCard: true,
    viewAsFilter(player) {
      return player.countCards("hes") > 0;
    },
    usable(skill, player) {
      return game.shuffleNumber + 1;
    },
    viewAs: {
      name: "tiesuo"
    },
    position: "hes",
    onuse(result, player) {
      player.addTempSkill(`${result.skill}_effect`);
    },
    subSkill: {
      effect: {
        charlotte: true,
        forced: true,
        trigger: {
          player: "useCardAfter"
        },
        filter(event, player) {
          return event.skill === "ylyglianhuan" && event.targets?.length === 2 && event.targets.some((target) => target.countCards("h") > 0);
        },
        logTarget: "targets",
        async content(event, trigger, player) {
          const { targets } = event;
          const lose_list = [];
          let cards2 = [];
          targets.forEach((current) => {
            const hs = current.getCards("h");
            if (hs.length) {
              cards2.addArray(hs);
              current.$throw(hs.length, 500);
              game.log(current, "将", get.cnNumber(hs.length), "张牌置入了处理区");
              lose_list.push([current, hs]);
            }
          });
          if (lose_list.length) {
            await game.loseAsync({
              lose_list
            }).setContent("chooseToCompareLose");
          }
          await game.delay();
          cards2 = cards2.filterInD();
          const list = [];
          const num = Math.floor(cards2.length / 2);
          targets.forEach((target) => {
            const gain = cards2.randomRemove(num);
            if (gain.length) {
              list.push([target, gain]);
              game.log(target, "获得了", get.cnNumber(gain.length), "张牌");
            }
          });
          await game.loseAsync({
            gain_list: list,
            player,
            animate: "draw"
          }).setContent("gaincardMultiple");
          cards2 = cards2.filterInD();
          if (cards2.length) {
            game.log(player, "获得了", get.cnNumber(cards2.length), "张牌");
            await player.gain({ cards: cards2, animate: "draw" });
          }
        }
      }
    }
  },
  ylygniepan: {
    enable: "chooseToUse",
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    filter(event, player) {
      if (event.type === "dying") {
        if (player !== event.dying) {
          return false;
        }
        return true;
      }
      return false;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.addSkill(`${event.name}_restore`);
      await player.draw({ num: 3 });
      await player.recoverTo(3);
      const result = await player.chooseTarget({
        prompt: "涅槃：对一名角色造成2点火焰伤害",
        filterTarget: lib.filter.all,
        ai(target2) {
          return get.damageEffect(target2, get.player(), get.player(), "fire");
        },
        forced: true
      }).forResult();
      const { targets } = result;
      if (!targets.length) {
        return;
      }
      const [target] = targets;
      player.line(target, "fire");
      await target.damage({ num: 2, nature: "fire" });
    },
    ai: {
      order: 0.5,
      skillTagFilter(player, tag, target) {
        if (player !== target || player.storage.ylygniepan) {
          return false;
        }
      },
      save: true,
      result: {
        player: 10
      }
    },
    subSkill: {
      restore: {
        forced: true,
        charlotte: true,
        trigger: {
          global: "washCard"
        },
        async content(event, trigger, player) {
          player.removeSkill(event.name);
          player.refreshSkill("ylygniepan");
        }
      }
    }
  },
  //雁翎典韦
  ylygqiangxi: {
    enable: "phaseUse",
    onChooseToUse(event) {
      if (!game.online && !event.ylygqiangxi_targets && event.type === "phase") {
        event.set(
          "ylygqiangxi_targets",
          event.player.getHistory("useSkill", (evt) => evt.skill === "ylygqiangxi").flatMap((evt) => evt.targets)
        );
      }
    },
    filter(event, player) {
      return game.hasPlayer((target) => !event.ylygqiangxi_targets.includes(target) && player.canCompare(target));
    },
    filterTarget(card, player, target) {
      return !get.event().ylygqiangxi_targets.includes(target) && player.canCompare(target);
    },
    async content(event, trigger, player) {
      const { target } = event;
      const result = await player.chooseToCompare(target).forResult();
      event.getParent().set(event.name, !!result.bool);
      if (result.bool) {
        let result2;
        if (!target.countDiscardableCards(player, "he")) {
          result2 = { index: 0 };
        } else {
          result2 = await player.chooseControl({
            choiceList: [`摸一张牌`, `弃置${get.translation(target)}一张牌`],
            choice: get.effect(target, { name: "draw" }, player, player) > get.effect(target, { name: "guohe_copy2" }, player, player) ? 0 : 1
          }).forResult();
        }
        if (result2.index === 0) {
          await player.draw();
        } else {
          await player.discardPlayerCard({ target, position: "he", forced: true });
        }
      } else {
        const list = player.getHistory("useSkill", (evt) => evt.skill === event.name && evt.event[event.name] === false);
        if (list.length === 2) {
          await player.loseHp();
          let targets = list.flatMap((evt) => evt.targets).sortBySeat();
          let prevs = [targets[0]];
          let nexts = [targets[0]];
          while (prevs[0] !== targets[1] || nexts[0] !== targets[1]) {
            if (prevs[0] !== targets[1]) {
              const prev = prevs[0].previousSeat;
              prevs.unshift(prev);
            }
            if (nexts[0] !== targets[1]) {
              const next = nexts[0].nextSeat;
              nexts.unshift(next);
            }
          }
          prevs = prevs.filter((target2) => targets.includes(target2) || target2.isIn());
          nexts = nexts.filter((target2) => targets.includes(target2) || target2.isIn());
          if (prevs.length !== 0 && nexts.length !== 0) {
            if (prevs.length === nexts.length) {
              const getEff = (targets2) => targets2.slice().remove(player).reduce((sum, target2) => sum + get.damageEffect(target2, player, player), 0);
              const result2 = await player.chooseControl({
                prompt: `强袭：请选择从${get.translation(targets[0])}的哪个方向向${get.translation(targets[1])}逐个肘击`,
                controls: ["顺时针", "逆时针"],
                choice: getEff(prevs) >= getEff(nexts) ? 0 : 1
              }).forResult();
              if (result2.index === 0) {
                targets = prevs.slice();
              } else {
                targets = nexts.slice();
              }
            } else if (prevs.length < nexts.length) {
              targets = prevs.slice();
            } else if (prevs.length > nexts.length) {
              targets = nexts.slice();
            }
            targets = targets.filter((target2) => target2 !== player && target2.isIn());
            if (targets.length) {
              player.line(targets);
              player.chat("狠狠肘击！");
              await game.doAsyncInOrder(targets, async (target2) => target2.damage());
            }
          }
          player.tempBanSkill(event.name);
        }
      }
    },
    ai: {
      order: 7,
      result: {
        target(player, target) {
          let hs = player.getCards("h");
          if (hs.some((card) => get.value(card) <= 6 && card.number > 10) || player.getHp() < 2 && player.getHp() + player.countCards("h", { name: ["tao", "jiu"] }) > 2 || player.getHp() > 1 && player.getHp() + player.countCards("h", { name: "tao" }) > 2) {
            return -1;
          }
          return 0;
        }
      }
    }
  },
  //雁翎小乔
  ylygtianxiang: {
    audio: "retianxiang",
    trigger: {
      player: "damageBegin4"
    },
    filter(event, player) {
      return player.countDiscardableCards(player, "he", { suit: "heart" }) > 0;
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard(get.prompt(event.skill), `弃置一张红桃牌防止${trigger.source ? `${get.translation(trigger.source)}造成的` : ""}伤害`, { suit: "heart" }, "he", "chooseonly").set("ai", (card) => {
        const { source, player: player2, nature } = get.event().getTrigger();
        if (get.damageEffect(player2, source, player2, nature) < 0) {
          return 7 - get.value(card);
        }
        return 0;
      }).forResult();
    },
    async content(event, trigger, player) {
      const { cards: cards2 } = event;
      await player.modedDiscard(cards2);
      trigger.cancel();
    },
    group: "ylygtianxiang_lose",
    subSkill: {
      lose: {
        trigger: {
          player: ["loseAfter", "compare"],
          global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"],
          target: "compare"
        },
        filter(event, player) {
          if (!["chooseToCompare", "equip"].includes(event.name) && !event.visible) {
            return false;
          }
          return get.info("ylygtianxiang_lose").getCards(event, player).length > 0;
        },
        getCards(event, player) {
          let cards2;
          if (event.name === "chooseToCompare") {
            if (player === event.player) {
              if (event.iwhile > 0) {
                cards2 = [];
              }
              return cards2 = [event.card1];
            }
            return cards2 = [event.card2];
          } else {
            cards2 = event.getl?.(player)?.cards2 || [];
          }
          return cards2.filter((card) => get.suit(card, player) === "heart" && get.position(card) === "d");
        },
        async cost(event, trigger, player) {
          const cards2 = get.info(event.skill).getCards(trigger, player);
          event.result = await player.chooseTarget(lib.filter.notMe).set("createDialog", [`###${get.prompt(event.skill)}###将这些牌交给一名其他角色，其于此回合结束时失去1点体力（不叠加）`, cards2, [(dialog) => dialog.buttons.forEach((i) => i.style.opacity = 1), "handle"]]).set("ai", (target) => {
            const { cardsVal, targetsList, player: player2 } = get.event();
            const att = get.attitude(player2, target);
            const eff = get.effect(target, { name: "losehp" }, player2, player2);
            if (target.hasSkill("ylygtianxiang_loseHp") && att < 0) {
              return 0;
            }
            return att * (cardsVal - eff) * (targetsList.indexOf(target) / targetsList.length);
          }).set("cardsVal", get.value(cards2)).set(
            "targetsList",
            (() => game.filterPlayer((current) => current !== player).sort((a, b) => get.attitude(player, b) - get.attitude(player, a)))()
          ).forResult();
        },
        async content(event, trigger, player) {
          const {
            targets: [target]
          } = event;
          const cards2 = get.info(event.name).getCards(trigger, player);
          const next = target.gain(cards2, "gain2");
          next.giver = player;
          target.addTempSkill(`${event.name}Hp`);
          await next;
        }
      },
      loseHp: {
        charlotte: true,
        forced: true,
        popup: false,
        mark: true,
        intro: {
          content: "回合结束时失去1点体力"
        },
        trigger: { global: "phaseEnd" },
        async content(event, trigger, player) {
          await player.loseHp();
        }
      }
    }
  },
  ylyghongyan: {
    audio: "hongyan",
    mod: {
      suit(card, suit) {
        if (suit === "spade") {
          return "heart";
        }
      }
    },
    trigger: {
      player: "enterGame",
      global: "phaseBefore"
    },
    filter(event, player) {
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    forced: true,
    async content(event, trigger, player) {
      const keys = ["花色", "颜色", "红色", "黑色"].concat(lib.suit.flatMap((i) => [get.translation(i), get.translation(`${i}2`)]));
      const map = /* @__PURE__ */ new Map();
      game.filterPlayer((target) => target !== player && target.hasSex("male")).forEach((target) => {
        map.set(target, []);
        const skills2 = target.getSkills(null, false, false).filter((i) => !get.info(i).charlotte);
        if (skills2.length) {
          map.get(target).push(
            ...skills2.filter((i) => {
              const info = get.skillInfoTranslation(i, target, true);
              return keys.some((i2) => info.includes(i2));
            })
          );
        }
      });
      if (!Array.from(map.values()).flat().length) {
        player.chat("你走了，我们吃什么啊");
        return;
      }
      const next = player.chooseTarget("###红颜###选择获得其他男性角色的一个含颜色或花色的技能", true, (card, player2, target) => {
        return get.event().map.get(target)?.length > 0;
      }).set("map", map).set("ai", (target) => Math.random());
      next.targetprompt2.push((target) => {
        const { map: map2 } = get.event();
        if (!target.classList.contains("selectable") || !map2.get(target)?.length) {
          return;
        }
        return map2.get(target).map((i) => get.translation(i));
      });
      const result = await next.forResult();
      if (result.targets?.length) {
        const {
          targets: [target]
        } = result;
        player.line(target, "yellow");
        const skills2 = map.get(target);
        let result2;
        skills2.length === 1 ? result2 = { bool: true, links: skills2 } : result2 = await player.chooseButton([`红颜：选择获得一个技能`, [skills2, "skill"]], true).set("ai", (button) => Math.random()).forResult();
        if (result2.links?.length) {
          const { links } = result2;
          await player.addSkills(links);
        }
      }
    }
  },
  //雁翎于吉
  ylygguhuo: {
    audio: "reguhuo",
    derivation: ["ylyghuinu"],
    getList(event, player) {
      return get.inpileVCardList((info) => {
        if (!["basic", "trick"].removeArray(player.getStorage("ylygguhuo_used")).includes(info[0])) {
          return false;
        }
        return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player, event);
      });
    },
    hiddenCard(player, name) {
      return lib.inpile.includes(name) && player.countCards("hs") && !player.getStorage("ylygguhuo_used").includes(get.type(name));
    },
    enable: "chooseToUse",
    filter(event, player) {
      return player.countCards("hs") && get.info("ylygguhuo").getList(event, player).length;
    },
    chooseButton: {
      dialog(event, player) {
        return ui.create.dialog("蛊惑", [get.info("ylygguhuo").getList(event, player), "vcard"], "hidden");
      },
      check(button) {
      },
      backup(links, player) {
        return {
          filterCard(card) {
            let bool = true;
            const vcard = get.autoViewAs(card, [card]);
            vcard.suit = "none";
            vcard.number = null;
            const mod = game.checkMod(vcard, player, "unchanged", "cardEnabled2", player);
            if (mod !== "unchanged") {
              bool = mod;
            }
            return bool;
          },
          selectCard: 1,
          position: "hs",
          ignoreMod: true,
          aiUse: Math.random(),
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            suit: "none",
            number: null
          },
          ai1(card) {
            const player2 = _status.event.player;
            const enemyNum = game.countPlayer((current) => current !== player2 && !current.hasSkill("ylyghuinu") && (get.realAttitude || get.attitude)(current, player2) < 0);
            const cardx = lib.skill.ylygguhuo_backup.viewAs;
            if (enemyNum) {
              if (card.name === cardx.name && (card.name !== "sha" || get.is.sameNature(card, cardx))) {
                return 2 + Math.random() * 3;
              } else if (lib.skill.ylygguhuo_backup.aiUse < 0.5 && !player2.isDying()) {
                return 0;
              }
            }
            return 6 - get.value(card);
          },
          async precontent(event, trigger, player2) {
            player2.logSkill("ylygguhuo");
            player2.addTempSkill("ylygguhuo_guess");
            player2.addTempSkill("ylygguhuo_used");
            player2.markAuto("ylygguhuo_used", get.type(event.result.card.name));
            const [card] = event.result.cards;
            event.result.card.suit = get.suit(card);
            event.result.card.number = get.number(card);
          }
        };
      },
      prompt(links, player) {
        return `扣置一张手牌当作${get.translation(links[0][3]) || ""}【${get.translation(links[0][2])}】使用`;
      }
    },
    ai: {
      save: true,
      respondSha: true,
      respondShan: true,
      fireAttack: true,
      skillTagFilter(player) {
        if (!player.countCards("hs") || player.getStorage("ylygguhuo_used").includes("basic")) {
          return false;
        }
      },
      tag: {
        save: 1,
        recover: 1
      },
      threaten: 1.2,
      order: 8.1,
      result: {
        player: 1
      }
    },
    subSkill: {
      guess: {
        trigger: {
          player: ["useCardBefore"]
        },
        forced: true,
        silent: true,
        popup: false,
        firstDo: true,
        charlotte: true,
        filter(event, player) {
          return event.skill === "ylygguhuo_backup";
        },
        async content(event, trigger, player) {
          let isFake = false;
          const [card] = trigger.cards;
          if (card.name !== trigger.card.name || card.name === "sha" && !get.is.sameNature(trigger.card, card)) {
            isFake = true;
          }
          player.popup(trigger.card.name, "metal");
          const next = player.lose(card, ui.ordering).set("relatedEvent", trigger);
          await next;
          trigger.throw = false;
          trigger.skill = "ylygguhuo_backup";
          game.log(player, "声明", trigger.targets?.length ? "对" : "", trigger.targets || "", "使用", trigger.card);
          const targets = game.filterPlayer((target) => target.countCards("h") > player.countCards("h"));
          const doubter = targets.filter((target) => target.hasSkill("ylyghuinu"));
          const acceptor = [];
          game.broadcastAll(
            (card2, player2) => {
              _status.guhuoNode = card2.copy("thrown");
              if (lib.config.cardback_style !== "default") {
                _status.guhuoNode.style.transitionProperty = "none";
                ui.refresh(_status.guhuoNode);
                _status.guhuoNode.classList.add("infohidden");
                ui.refresh(_status.guhuoNode);
                _status.guhuoNode.style.transitionProperty = "";
              } else {
                _status.guhuoNode.classList.add("infohidden");
              }
              _status.guhuoNode.style.transform = "perspective(600px) rotateY(180deg) translateX(0)";
              player2.$throwordered2(_status.guhuoNode);
            },
            trigger.cards[0],
            player
          );
          event.onEnd01 = () => {
            _status.guhuoNode.removeEventListener("webkitTransitionEnd", _status.event.onEnd01);
            setTimeout(() => {
              _status.guhuoNode.style.transition = "all ease-in 0.3s";
              _status.guhuoNode.style.transform = "perspective(600px) rotateY(270deg)";
              const onEnd = () => {
                _status.guhuoNode.classList.remove("infohidden");
                _status.guhuoNode.style.transition = "all 0s";
                ui.refresh(_status.guhuoNode);
                _status.guhuoNode.style.transform = "perspective(600px) rotateY(-90deg)";
                ui.refresh(_status.guhuoNode);
                _status.guhuoNode.style.transition = "";
                ui.refresh(_status.guhuoNode);
                _status.guhuoNode.style.transform = "";
                _status.guhuoNode.removeEventListener("webkitTransitionEnd", onEnd);
              };
              _status.guhuoNode.listenTransition(onEnd);
            }, 300);
          };
          const prompt = `${get.translation(player)}声明${trigger.targets?.length ? `对${get.translation(trigger.targets)}` : ""}使用${get.translation(trigger.card)}，是否质疑？`;
          for (const target of targets) {
            if (doubter.includes(target)) {
              game.log(target, "#y质疑");
              target.popup("质疑！", "fire");
              continue;
            }
            const { links } = await target.chooseButton([prompt, [["reguhuo_ally", "reguhuo_betray"], "vcard"]], true).set("ai", (button) => {
              const player2 = _status.event.player;
              const evt = _status.event.getParent("ylygguhuo_guess");
              const evtx = evt.getTrigger();
              if (!evt) {
                return Math.random();
              }
              const card2 = { name: evtx.card.name, nature: evtx.card.nature, isCard: true };
              const ally = button.link[2] === "reguhuo_ally";
              if (ally && (player2.hp <= 1 || get.attitude(player2, evt.player) >= 0)) {
                return 1.1;
              }
              if (!ally && get.attitude(player2, evt.player) < 0 && evtx.name === "useCard") {
                let eff = 0;
                const targetsx = evtx.targets || [];
                for (const target2 of targetsx) {
                  const isMe = target2 === evt.player;
                  eff += get.effect(target2, card2, evt.player, player2) / (isMe ? 1.5 : 1);
                }
                eff /= 1.5 * targetsx.length || 1;
                if (eff > 0) {
                  return 0;
                }
                if (eff < -7) {
                  return Math.random() + Math.pow(-(eff + 7) / 8, 2);
                }
                return Math.pow((get.value(card2, evt.player, "raw") - 4) / (eff === 0 ? 5 : 10), 2);
              }
              return Math.random();
            }).forResult();
            if (links[0][2] === "reguhuo_betray") {
              target.addExpose(0.2);
              game.log(target, "#y质疑");
              target.popup("质疑！", "fire");
              doubter.add(target);
            } else {
              game.log(target, "#g不质疑");
              target.popup("不质疑", "wood");
              acceptor.add(target);
            }
          }
          await game.delayx();
          game.broadcastAll((onEnd) => {
            _status.event.onEnd01 = onEnd;
            if (_status.guhuoNode) {
              _status.guhuoNode.listenTransition(onEnd, 300);
            }
          }, event.onEnd01);
          await game.delay(2);
          if (isFake) {
            if (doubter.length) {
              doubter.forEach((target) => target.popup("质疑正确", "wood"));
              game.log(player, "声明的", trigger.card, "作废了");
              trigger.cancel();
              trigger.getParent().goto(0);
              trigger.line = false;
              const giver = acceptor.concat(game.filterPlayer((target) => target !== player && !targets.includes(target)));
              if (giver.length) {
                player.line(giver, "yellow");
                await game.doAsyncInOrder(giver, async (target) => target.chooseToGive(player, "h", true));
              }
            }
          } else if (doubter.length) {
            doubter.forEach((target) => target.popup("质疑错误", "fire"));
            player.line(doubter, "fire");
            await game.doAsyncInOrder(doubter, async (target) => {
              await target.loseHp();
              await target.addSkills("ylyghuinu");
            });
          }
          await game.delay(2);
          if (isFake) {
            game.broadcastAll(() => ui.clear());
          }
        }
      },
      used: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "已转化过<span class=thundertext>$牌</span>"
        }
      }
    }
  },
  ylyghuinu: {
    locked: true,
    mark: true,
    intro: {
      content: (storage, player, skill) => get.skillInfoTranslation(skill, player)
    }
  }
};
const skills$2 = {
  //神张角
  zhyuanlin: {
    forced: true,
    trigger: {
      global: ["roundStart", "phaseJudgeBegin", "damageEnd"]
    },
    filter(event, player) {
      if (event.name === "phaseJudge") {
        return player !== event.player;
      } else if (event.name === "damage") {
        return event.hasNature("thunder");
      }
      return game.players.length > 1;
    },
    logTarget(event, player) {
      if (event.name === "phaseJudge") {
        return event.player;
      } else if (event.name === "damage") {
        return player;
      }
      return game.filterPlayer((current) => current !== player).sortBySeat();
    },
    async content(event, trigger, player) {
      const targets = event.targets;
      if (trigger.name === "damage") {
        await player.chooseDrawRecover({ forced: true });
      } else {
        await game.doAsyncInOrder(targets, async (target) => {
          await target.executeDelayCardEffect("shandian");
        });
      }
    },
    ai: {
      nothunder: true,
      effect: {
        target(card, player, target, current) {
          if (get.tag(card, "thunderDamage")) {
            return "zeroplayertarget";
          }
        }
      }
    }
  },
  zhkuanglei: {
    forced: true,
    trigger: { target: "useCardToTarget" },
    filter(event, player) {
      return player !== event.player && get.is.damageCard(event.card);
    },
    logTarget: "player",
    derivation: "zhkuanglei_leiji",
    async content(event, trigger, player) {
      const target = event.targets[0];
      const result = await target.chooseToGive({
        prompt: `狂雷：请交给${get.translation(player)}一张牌，否则你进行一次〖雷击〗判定`,
        target: player,
        position: "he",
        ai(card) {
          const { player: player2, target: target2 } = get.event();
          if (get.attitude(player2, target2) > 0 || player2.hp <= 2) {
            return 114514 - get.value(card);
          }
          return 6 - get.value(card);
        }
      }).set("target", player).forResult();
      if (!result?.bool || !result.cards?.length) {
        await player.useSkill({ skill: `${event.name}_leiji`, targets: [target] });
      }
    },
    subSkill: {
      leiji: {
        inherit: "releiji"
      }
    }
  },
  zhqiongshan: {
    round: 1,
    trigger: { global: "judge" },
    filter(event, player) {
      return player !== event.player;
    },
    check(event, player) {
      const target = event.player;
      const card = target.judging[0];
      const suits = ["spade", "club", "diamond", "heart"];
      const numbers = Array.from({ length: 13 }).map((val, idx) => idx + 1);
      for (const suit of suits) {
        for (const number of numbers) {
          const cardx = get.autoViewAs({ name: card.name, nature: card.nature, suit, number }, [card]);
          const num = get.sgnAttitude(player, target) * (event.judge(cardx) - event.judge(card));
          if (num > 0) return true;
        }
      }
      return false;
    },
    logTarget: "player",
    async content(event, trigger, player) {
      const target = event.targets[0];
      const card = target.judging[0];
      if ([card].filterInD("od").length) {
        await player.gain({ cards: [card], animate: "gain2" });
      }
      const str = `穹闪：${get.translation(target)}的${trigger.judgestr || ""}判定为${get.translation(target.judging[0])}，请修改判定结果`;
      const suits = ["spade", "club", "diamond", "heart"];
      const suitx = suits.map((suit2) => get.translation(suit2));
      const numbers = Array.from({ length: 13 }).map((val, idx) => idx + 1);
      const numberx = ["A"];
      numberx.addArray(Array.from({ length: 9 }).map((val, idx) => idx + 2));
      numberx.addArray("J", "Q", "K");
      const result = await player.chooseButton({
        createDialog: [str, [suitx, "tdnodes"], [numberx, "tdnodes"]],
        filterButton(button) {
          const { numberx: numberx2, suitx: suitx2 } = get.event();
          if (ui.selected.buttons?.some((buttonx) => numberx2.includes(buttonx.link))) {
            return !numberx2.includes(button.link);
          }
          if (ui.selected.buttons?.some((buttonx) => suitx2.includes(buttonx.link))) {
            return !suitx2.includes(button.link);
          }
          return true;
        },
        forced: true,
        selectButton: 2,
        ai(button) {
          const { card: card2, suitx: suitx2, numberx: numberx2, suits: suits2, numbers: numbers2, player: player2 } = get.event();
          const list = [];
          const trigger2 = get.event().getTrigger();
          for (const suit2 of suits2) {
            for (const number2 of numbers2) {
              const cardx = get.autoViewAs({ name: card2.name, nature: card2.nature, suit: suit2, number: number2 }, [card2]);
              const num = get.sgnAttitude(player2, trigger2.player) * (trigger2.judge(cardx) - trigger2.judge(card2));
              list.push([suit2, number2, num]);
            }
          }
          if (list.length) {
            list.sort((a, b) => b[2] - a[2]);
            return [suitx2[suits2.indexOf(list[0][0])], numberx2[numbers2.indexOf(list[0][1])]].includes(button.link);
          }
          return 1 + Math.random();
        }
      }).set("card", card).set("numberx", numberx).set("suitx", suitx).set("numbers", numbers).set("suits", suits).forResult();
      if (!result?.bool || !result.links?.length) {
        return;
      }
      const suit = suits[result.links.map((link) => suitx.indexOf(link)).filter((i) => i !== -1)[0]];
      const number = numbers[result.links.map((link) => numberx.indexOf(link)).filter((i) => i !== -1)[0]];
      game.log(player, "将判定结果修改为了", `#g${get.translation(suit + 2)}${get.strNumber(number)}`);
      trigger.fixedResult = {
        suit,
        color: get.color({ suit }),
        number
      };
      player.popup(`${get.translation(suit + 2)}${get.strNumber(number)}`, "thunder");
    }
  }
};
const skills$1 = {
  //甲虫
  lxbeini: {
    audio: "beini",
    enable: "phaseUse",
    usable: 1,
    filter(event, player) {
      return game.players.length > 1;
    },
    filterTarget(card, player, target) {
      if (!ui.selected.targets?.length) {
        return target != player;
      }
      return true;
    },
    selectTarget: 2,
    targetprompt: ["使用牌", "目标"],
    multiline: true,
    multitarget: true,
    complexTarget: true,
    async content(event, trigger, player) {
      const targets = event.targets;
      const result = await targets[0].chooseToUse({
        filterCard(card) {
          return lib.filter.filterCard.apply(this, arguments) && get.is.damageCard(card);
        },
        prompt: `对${get.translation(targets[1])}使用一张伤害牌或点“取消”交给${get.translation(player)}一张非伤害牌`,
        targetRequired: true,
        complexSelect: true,
        complexTarget: true,
        filterTarget(card, player2, target) {
          const { targetx } = get.event();
          if (target != targetx && !ui.selected.targets.includes(targetx)) {
            return false;
          }
          return lib.filter.filterTarget.apply(this, arguments);
        }
      }).set("targetx", targets[1]).forResult();
      if (!result?.bool && targets[0].hasGainableCards(player, "he", (card) => !get.is.damageCard(card))) {
        await targets[0].chooseToGive({
          forced: true,
          filterCard(card) {
            return !get.is.damageCard(card);
          },
          prompt: `悖逆：请交给${get.translation(player)}一张非伤害牌`,
          position: "he",
          target: player
        });
      }
    },
    ai: {
      order: 5,
      result: {
        player: 1,
        target(player, target) {
          if (!ui.selected.targets?.length || game.countPlayer((current) => get.attitude(player, current) < 0) > 1) {
            return -1;
          }
          return 0;
        }
      }
    }
  },
  lxdingfa: {
    audio: "dingfa",
    forced: true,
    locked: false,
    trigger: { player: "phaseDiscardEnd" },
    filter(event, player) {
      return get.discarded().some((card) => get.is.damageCard(card)) && game.hasPlayer((current) => current != player && current.hasCards("he"));
    },
    async content(event, trigger, player) {
      const result = await player.chooseTarget({
        prompt: "定法：弃置一名其他角色一张牌",
        filterTarget(card, player2, target) {
          return player2 != target && target.hasCards("he");
        },
        forced: true,
        ai(target) {
          const player2 = get.player();
          return get.effect(target, { name: "guohe_copy", position: "he" }, player2, player2);
        }
      }).forResult();
      if (result?.bool && result.targets?.length) {
        const target = result.targets[0];
        player.line(target);
        if (target.hasCards("he")) {
          await player.discardPlayerCard({ target, forced: true, position: "he" });
        }
      }
    }
  },
  //成济
  lxkuangli: {
    audio: "mbkuangli",
    forced: true,
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      return get.is.damageCard(event.card) && player != event.target && (player.hasCards("he") || event.target.hasCards("he"));
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const target = event.targets[0];
      let result;
      const cards2 = [];
      if (player.hasDiscardableCards(player, "he")) {
        result = await player.chooseToDiscard({ forced: true, position: "he" }).forResult();
        if (result?.bool && result.cards?.length) {
          cards2.addArray(result.cards);
        }
      }
      let num = player == _status.currentPhase ? 1 : 2;
      if (target.hasCards("he")) {
        result = await player.discardPlayerCard({ forced: true, target, position: "he", selectButton: num }).forResult();
        if (result?.bool && result.links?.length) {
          cards2.addArray(result.links);
        }
      }
      if (cards2.length) {
        const types = cards2.map((card) => get.type2(card));
        if (types.length == 1 || types[0] == types[1]) {
          trigger.getParent().baseDamage ??= 1;
          trigger.getParent().baseDamage++;
        } else {
          await player.draw(2);
        }
      }
    }
  },
  lxxiongsi: {
    audio: "mbxiongsi",
    limited: true,
    skillAnimation: true,
    animationColor: "wood",
    enable: "phaseUse",
    manualConfirm: true,
    filter(event, player) {
      return player.getAllHistory("sourceDamage").reduce((a, b) => a + b.num, 0) >= 3 && player.hasDiscardableCards(player, "he");
    },
    filterCard: lib.filter.cardDiscardable,
    selectCard: -1,
    position: "h",
    filterTarget: lib.filter.notMe,
    selectTarget: -1,
    multiline: true,
    multitarget: true,
    lose: false,
    discard: false,
    delay: 0,
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const { cards: cards2, targets } = event;
      await player.modedDiscard({ cards: cards2 });
      await game.doAsyncInOrder(targets, async (target) => {
        await target.loseHp();
      });
    }
  },
  //李昭焦伯
  lxzuoyou: {
    audio: "mbzuoyou",
    enable: "phaseUse",
    usable: 1,
    zhuanhuanji: true,
    marktext: "☯",
    mark: true,
    intro: {
      content(storage, player) {
        if (!storage) {
          return `转换技，出牌阶段限一次，你可受到1点无来源伤害，令一名角色获得1点护甲。`;
        }
        return `转换技，出牌阶段限一次，你可失去1点护甲，对一名角色造成1点伤害。`;
      }
    },
    filter(event, player) {
      const storage = player.storage.lxzuoyou;
      return !storage || player.hujia > 0;
    },
    filterTarget(card, player, target) {
      const storage = player.storage.lxzuoyou;
      return storage || target.hujia < 5;
    },
    prompt(event, player) {
      const storage = player.storage.lxzuoyou;
      return get.info("lxzuoyou").intro.content(storage, player);
    },
    async content(event, trigger, player) {
      const target = event.target, storage = player.storage.lxzuoyou;
      player.changeZhuanhuanji(event.name);
      if (!storage) {
        await player.damage({ num: 1, nosource: true });
        await target.changeHujia(1, null, true);
      } else {
        await player.changeHujia(-1);
        await target.damage({ num: 1, source: player });
      }
    },
    ai: {
      order: 1,
      result: {
        player(player, target) {
          const storage = player.storage.lxzuoyou;
          if (!storage && player.hp <= 1) {
            return -1;
          }
          return 1;
        },
        target(player, target) {
          const storage = player.storage.lxzuoyou;
          if (!storage) {
            if (target == player && player.hujia < 1) {
              return 6;
            }
            if (target.hp === 1) {
              return 5;
            }
            return 2;
          } else {
            return get.sgnAttitude(player, target) * get.damageEffect(target, player, player);
          }
        }
      }
    }
  },
  lxshishou: {
    audio: "mbshishou",
    trigger: { global: "phaseJieshuBegin" },
    filter(event, player) {
      return player.hasHistory("damage") || player.hasHistory("sourceDamage");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseTarget({
        prompt: get.prompt2(event.skill),
        filterTarget(card, player2, target) {
          return player2.group == target.group && target.hujia < 5;
        },
        ai(target) {
          return get.attitude(get.player(), target);
        }
      }).forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.changeHujia(1, null, true);
    }
  },
  //文钦
  lxbeiming: {
    audio: "mbbeiming",
    enable: "phaseUse",
    usable: 1,
    manualConfirm: true,
    filter(event, player) {
      return Array.from(ui.discardPile.childNodes).some((card) => get.type(card) == "equip");
    },
    async content(event, trigger, player) {
      const cards2 = Array.from(ui.discardPile.childNodes).filter((card2) => get.type(card2) == "equip");
      const card = cards2[cards2.length - 1];
      let result;
      if (card) {
        player.addSkill("lxbeiming_use");
        await player.gain({ cards: [card], animate: "gain2" });
        player.addGaintag(card, "lxbeiming_use");
        result = await player.chooseTarget({
          prompt: `孛明：你可以将${get.translation(card)}置于一名其他角色的装备区内`,
          filterTarget(card2, player2, target) {
            const { cardx } = get.event();
            return target.canEquip(cardx, true) && target != player2;
          },
          ai(target) {
            return get.attitude(get.player(), target);
          }
        }).set("cardx", card).forResult();
        if (result?.bool && result.targets?.length) {
          const target = result.targets[0];
          target.addSkill("lxbeiming_use");
          player.line(target);
          player.$give(card, target);
          await target.equip(card);
          game.broadcastAll((card2) => {
            card2.addGaintag("lxbeiming_use");
          }, card);
          result = await player.chooseBool({
            prompt: `孛明：你可再从弃牌堆获得一张装备牌`,
            ai: () => 1
          }).forResult();
          if (result?.bool) {
            const cards3 = Array.from(ui.discardPile.childNodes).filter((card3) => get.type(card3) == "equip");
            const card2 = cards3[cards3.length - 1];
            if (card2) {
              await player.gain({ cards: [card2], animate: "gain2" });
              player.addGaintag(card2, "lxbeiming_use");
            } else {
              player.popup("杯具");
            }
          }
        }
      }
    },
    ai: {
      order: 5,
      result: {
        player: 1
      }
    },
    subSkill: {
      use: {
        charlotte: true,
        onremove(player, skill) {
          player.removeGaintag(skill);
        },
        enable: "chooseToUse",
        mod: {
          targetInRange(card) {
            if (card?.storage?.lxbeiming_use) {
              return true;
            }
          }
        },
        filter(event, player) {
          return player.hasCards("he", (card) => {
            if (!card.hasGaintag("lxbeiming_use")) {
              return false;
            }
            return get.inpileVCardList((info) => {
              if (!["sha", "jiu"].includes(info[2]) || info[3]) {
                return false;
              }
              const cardx = get.autoViewAs({ name: info[2], storage: { lxbeiming_use: true } }, [card]);
              return event.filterCard(cardx, player, event);
            }).length;
          });
        },
        chooseButton: {
          dialog(event, player) {
            const list = get.inpileVCardList((info) => {
              if (!["sha", "jiu"].includes(info[2]) || info[3]) {
                return false;
              }
              return player.hasCards("he", (card) => {
                if (!card.hasGaintag("lxbeiming_use")) {
                  return false;
                }
                const cardx = get.autoViewAs({ name: info[2], storage: { lxbeiming_use: true } }, [card]);
                return event.filterCard(cardx, player, event);
              });
            });
            return ui.create.dialog("孛明", [list, "vcard"]);
          },
          check(button) {
            if (_status.event.getParent().type != "phase") {
              return 1;
            }
            const player = get.player();
            return player.getUseValue({
              name: button.link[2]
            });
          },
          backup(links, player) {
            return {
              filterCard(card) {
                return card.hasGaintag("lxbeiming_use");
              },
              audio: "lxbeiming",
              popname: true,
              check(card) {
                return 10 - get.value(card);
              },
              position: "he",
              viewAs: {
                name: links[0][2],
                storage: { lxbeiming_use: true }
              }
            };
          },
          prompt(links, player) {
            return "将一张“孛明”牌当做" + get.translation(links[0][2]) + "使用";
          }
        },
        hiddenCard(player, name) {
          if (!lib.inpile.includes(name)) {
            return false;
          }
          return player.hasCards("he", (card) => card.hasGaintag("lxbeiming_use")) && ["jiu", "sha"].includes(name);
        },
        ai: {
          respondSha: true,
          unequip: true,
          skillTagFilter(player, tag, arg) {
            if (tag == "unequip") {
              if (!arg?.card?.storage?.lxbeiming_use) {
                return false;
              }
              return true;
            }
            if (arg == "respond") {
              return false;
            }
            return lib.skill["lxbeiming_use"].hiddenCard(player, "sha");
          },
          order: 7,
          result: {
            player(player) {
              if (_status.event.dying) {
                return get.attitude(player, _status.event.dying);
              }
              return 1;
            }
          }
        }
      }
    }
  },
  lxchoumang: {
    audio: "mbchoumang",
    trigger: { player: "useCardToPlayered" },
    filter(event, player) {
      return event.card.name == "sha" && event.isFirstTarget;
    },
    async cost(event, trigger, player) {
      const targets = trigger.targets, card = trigger.card;
      const controls = ["选项一"], choiceList = ["令此【杀】伤害+1", "令一名其他角色成为此【杀】的额外目标", "执行上述全部效果并废除一个装备栏"];
      if (game.hasPlayer((current) => !targets.includes(current) && player != current && player.canUse(card, current, true, false))) {
        controls.push("选项二");
      } else {
        choiceList[1] = `<span style="opacity:0.5">` + choiceList[1] + "</span>";
      }
      if (player.hasEnabledSlot()) {
        controls.push("背水！");
      } else {
        choiceList[2] = `<span style="opacity:0.5">` + choiceList[2] + "</span>";
      }
      controls.push("cancel2");
      const result = await player.chooseControl({
        controls,
        choiceList,
        prompt: "仇铓：你可以选择一项",
        ai() {
          const { player: player2, controls: controls2, card: card2, targets: targets2 } = get.event();
          if (game.hasPlayer((current) => !targets2.includes(current) && current != player2 && player2.canUse(card2, current, true, false))) {
            if (controls2.length >= 4 && controls2.includes("背水！")) {
              return "背水！";
            }
            if (controls2.includes("选项二")) {
              return "选项二";
            }
          }
          if (!targets2.includes((target) => get.attitude(player2, target) > 0)) {
            return "选项一";
          }
          return "cancel2";
        }
      }).set("card", card).set("targets", targets).forResult();
      if (typeof result?.control == "string" && result.control != "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.control
        };
      }
    },
    async content(event, trigger, player) {
      const link = event.cost_data;
      const targets = trigger.targets, card = trigger.card;
      if (["背水！", "选项一"].includes(link)) {
        trigger.getParent().baseDamage ??= 1;
        trigger.getParent().baseDamage++;
      }
      if (["背水！", "选项二"].includes(link) && game.hasPlayer((current) => !targets.includes(current) && player != current && player.canUse(card, current, true, false))) {
        const result = await player.chooseTarget({
          prompt: `仇铓：选择一名其他角色成为${get.translation(card)}的额外目标`,
          forced: true,
          filterTarget(card2, player2, target) {
            const { targets: targets2, cardx } = get.event();
            return target != player2 && !targets2.includes(target) && player2.canUse(cardx, target, true, false);
          },
          ai(target) {
            const { player: player2, cardx } = get.event();
            return get.effect(target, cardx, player2, player2);
          }
        }).set("cardx", card).set("targets", targets).forResult();
        if (result?.bool && result.targets?.length) {
          const target = result.targets[0];
          player.line(target);
          trigger.targets.push(target);
        }
      }
      if (link == "背水！") {
        await player.chooseToDisable();
      }
    }
  },
  //毌丘俭
  lxcuizhen: {
    audio: "mbcuizhen",
    trigger: { player: "phaseBegin" },
    filter(event, player) {
      return player.hasCards("he", (card) => get.type(card) == "equip");
    },
    async cost(event, trigger, player) {
      event.result = await player.chooseToDiscard({
        prompt: get.prompt2(event.skill),
        filterCard(card) {
          return get.type(card) == "equip";
        },
        selectCard: [1, Infinity],
        position: "he",
        check(card) {
          return 6 - get.value(card);
        },
        chooseonly: true
      }).forResult();
    },
    async content(event, trigger, player) {
      const { cards: cards2 } = event;
      await player.modedDiscard({ cards: cards2 });
      const num = cards2.length, subtypes = cards2.map((card) => get.subtype(card)).unique();
      if (!game.hasPlayer((current) => subtypes.concat(["equip1"]).some((subtype) => current.hasEnabledSlot(subtype)))) {
        return;
      }
      const result = await player.chooseButtonTarget({
        createDialog: [
          "摧阵：请选择一项",
          [
            [
              ["one", `废除一名角色的${get.translation(subtypes)}栏`],
              ["two", `废除至多${get.cnNumber(num)}名角色的武器栏`]
            ],
            "textbutton"
          ]
        ],
        complexSelect: true,
        forced: true,
        filterTarget(card, player2, target) {
          const link = ui.selected.buttons?.[0]?.link;
          if (link == "one") {
            return get.event().subtypes.some((subtype) => target.hasEnabledSlot(subtype));
          }
          return target.hasEnabledSlot("equip1");
        },
        selectTarget() {
          const link = ui.selected.buttons?.[0]?.link;
          if (link == "one") {
            return 1;
          }
          return [1, get.event().num];
        },
        ai1(button) {
          return 1 + Math.random();
        },
        ai2(target) {
          return -get.attitude(get.player(), target);
        }
      }).set("subtypes", subtypes).set("num", num).forResult();
      if (result?.bool && result.targets?.length && result.links?.length) {
        const targets = result.targets.sortBySeat(), link = result.links[0];
        player.line(targets);
        if (link == "one") {
          await targets[0].disableEquip(subtypes);
        } else {
          await game.doAsyncInOrder(targets, async (target) => {
            await target.disableEquip(["equip1"]);
          });
        }
      }
    },
    group: "lxcuizhen_draw",
    subSkill: {
      draw: {
        audio: "lxcuizhen",
        forced: true,
        locked: false,
        trigger: { player: "phaseDrawBegin2" },
        filter(event, player) {
          return !event.numFixed && game.hasPlayer((current) => current.hasDisabledSlot());
        },
        async content(event, trigger, player) {
          const num = Math.min(
            3,
            game.countPlayer((current) => current.hasDisabledSlot())
          );
          trigger.num += num;
        }
      }
    }
  },
  lxkuili: {
    audio: "mbkuili",
    forced: true,
    trigger: { player: "damageEnd" },
    filter(event, player) {
      return event.source?.isIn() && event.source.hasDisabledSlot();
    },
    logTarget: "source",
    async content(event, trigger, player) {
      const target = event.targets[0];
      await target.chooseToEnable();
    }
  },
  //司马伷
  lxbifeng: {
    audio: "mbbifeng",
    trigger: { target: "useCardToTarget" },
    filter(event, player) {
      return event.card.name == "sha" || get.type(event.card) == "trick";
    },
    check(event, player) {
      let cancel = get.effect(player, event.card, event.player, player), name = event.card.name;
      if (get.effect(player, { name: "losehp" }, player, player) - cancel > 0) {
        return true;
      }
      if (2 * get.effect(player, { name: "draw" }, player, player) - cancel <= 0) {
        return false;
      }
      const targets = event.targets.filter((current) => {
        return player !== current && get.effect(current, event.card, event.player, current) < 0;
      });
      if (name === "sha") {
        return targets.some((target) => {
          return target.mayHaveShan(player, "use");
        });
      }
      if (name === "juedou" || name === "nanman") {
        return targets.some((target) => {
          return target.mayHaveSha(player, "respond");
        });
      }
      if (name === "wanjian") {
        return targets.some((target) => {
          return target.mayHaveShan(player, "respond");
        });
      }
      if (name === "qizhengxiangsheng") {
        return targets.some((target) => {
          return target.mayHaveSha(player, "respond") || target.mayHaveShan(player, "respond");
        });
      }
      return false;
    },
    async content(event, trigger, player) {
      const bool = player.isTurnedOver();
      await player.turnOver();
      trigger.targets.remove(player);
      trigger.getParent().triggeredTargets2.remove(player);
      trigger.untrigger();
      if (bool && !player.isTurnedOver()) {
        await player.loseHp();
      }
    }
  },
  lxsuwang: {
    audio: "mbsuwang",
    trigger: { player: "turnOverEnd" },
    filter(event, player) {
      return player.countCards("h") != 4;
    },
    async content(event, trigger, player) {
      if (player.countCards("h") < 4) {
        await player.drawTo(4);
      } else if (player.countCards("h") > 4) {
        await player.chooseToDiscard({ forced: true, position: "h", selectCard: player.countCards("h") - 4 });
      }
      if (!player.isTurnedOver() && player.hasCards("h")) {
        const num = Math.min(player.countCards("h"), game.players.length);
        const result = await player.chooseCardTarget({
          prompt: "宿望：将所有手牌分配给等量名角色各一张",
          forced: true,
          selectCard: [1, num],
          selectTarget: [1, num],
          complexSelect: true,
          complexCard: true,
          complexTarget: true,
          position: "h",
          filterOk() {
            const { num: num2 } = get.event();
            return ui.selected.cards.length == ui.selected.targets.length && num2 == ui.selected.cards.length;
          },
          ai1(card) {
            return -get.value(card);
          },
          ai2(target) {
            const card = ui.selected.cards[ui.selected.cards.length - 1];
            if (get.value(card) <= 0) {
              return -get.attitude(get.player(), target);
            }
            return get.attitude(get.player(), target);
          },
          targetprompt() {
            const links = ui.selected.cards;
            return ["获得", get.translation(links[ui.selected.targets.length - 1])].join("<br>");
          }
        }).set("num", num).forResult();
        if (result?.bool && result.targets?.length && result.cards?.length) {
          const cards2 = result.cards, targets = result.targets;
          const gain_list = targets.map((target, i) => [target, [cards2[i]]]);
          await game.loseAsync({
            gain_list,
            player,
            cards: cards2,
            giver: player,
            animate: "give"
          }).setContent("gaincardMultiple");
        }
      }
    }
  },
  //司马孚
  lxpanxiang: {
    audio: "mbpanxiang",
    trigger: { global: ["damageBegin1", "damageBegin3"] },
    filter(event, player, name) {
      const target = name == "damageBegin1" ? event.source : event.player;
      if (!target?.isIn()) {
        return false;
      }
      return target.group == player.group && (!player.getStorage("lxpanxiang").includes("add") && target.hasCards("he") || !player.getStorage("lxpanxiang").includes("reduce") && player.hasDiscardableCards(player, "he"));
    },
    logTarget(event, player, name) {
      return name == "damageBegin1" ? event.source : event.player;
    },
    async cost(event, trigger, player) {
      const name = event.triggername;
      const target = name == "damageBegin1" ? trigger.source : trigger.player;
      const target2 = name != "damageBegin1" ? trigger.source : trigger.player;
      const controls = [], choiceList = [`令${get.translation(target)}交给你一张牌，此伤害+1`, "你弃置一张牌，此伤害-1"];
      const pos = target == player ? "e" : "he";
      if (!player.getStorage("lxpanxiang").includes("add") && target.hasCards(pos)) {
        controls.push("选项一");
      } else {
        choiceList[0] = `<span style="opacity:0.5">` + choiceList[0] + "</span>";
      }
      if (!player.getStorage("lxpanxiang").includes("reduce") && player.hasDiscardableCards(player, "he")) {
        controls.push("选项二");
      } else {
        choiceList[1] = `<span style="opacity:0.5">` + choiceList[1] + "</span>";
      }
      if (!controls.length) {
        return;
      }
      controls.push("cancel2");
      const result = await player.chooseControl({
        controls,
        choiceList,
        prompt: "蹒襄：你可以选择一项",
        ai() {
          const { player: player2, target: target3, controls: controls2, name: name2, target2: target22 } = get.event();
          if (name2 == "damageBegin1") {
            if (controls2.includes("选项一") && get.damageEffect(target22, target3, player2) > 0) {
              return "选项一";
            }
            if (controls2.includes("选项二") && get.damageEffect(target22, target3, player2) < 0) {
              return "选项二";
            }
          } else {
            if (controls2.includes("选项一") && get.damageEffect(target3, target22, player2) > 0) {
              return "选项一";
            }
            if (controls2.includes("选项二") && get.damageEffect(target3, target22, player2) < 0) {
              return "选项二";
            }
          }
          return "cancel2";
        }
      }).set("target", target).set("target2", target2).set("name", name).forResult();
      if (typeof result?.control == "string" && result.control != "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.control
        };
      }
    },
    async content(event, trigger, player) {
      const link = event.cost_data, target = event.targets[0];
      player.markAuto(event.name, [link == "选项一" ? "add" : "reduce"]);
      player.markAuto(event.name + "_targets", [target]);
      if (link == "选项一") {
        const pos = target == player ? "e" : "he";
        if (target.hasCards(pos)) {
          await target.chooseToGive({ target: player, forced: true, position: pos });
        }
        trigger.num++;
      } else {
        await player.chooseToDiscard({ forced: true, position: "he" });
        trigger.num--;
      }
      if (player.getStorage("lxpanxiang").length >= 2) {
        player.setStorage("lxpanxiang", [], true);
      }
    },
    intro: {
      content(storage, player) {
        const s = player.getStorage("lxpanxiang");
        if (!s.length) {
          return "当前没触发任何选项";
        }
        let str = "";
        if (s.includes("add")) {
          str += "当前触发了加伤选项";
        } else {
          str += "当前触发了减伤选项";
        }
        return str;
      }
    },
    subSkill: {
      targets: {
        intro: { content: "$成为过【蹒襄】的目标" }
      }
    }
  },
  lxchenjie: {
    audio: "mbchenjie",
    forced: true,
    trigger: { global: "die" },
    filter(event, player) {
      return player.getStorage("lxpanxiang_targets").includes(event.player) && event.source?.isIn();
    },
    logTarget(event, player) {
      return event.source;
    },
    async content(event, trigger, player) {
      const target = event.targets[0], cards2 = target.getCards("h");
      if (cards2.length && target != player) {
        await player.gain({ cards: cards2, animate: "giveAuto", target });
      }
      player.addTempSkill(event.name + "_effect", "roundStart");
      player.markAuto(event.name + "_effect", [target]);
    },
    subSkill: {
      effect: {
        charlotte: true,
        intro: { content: "$本轮使用牌不能指定你为目标" },
        mod: {
          targetEnabled(card, player, target) {
            if (target.getStorage("lxchenjie_effect").includes(player)) {
              return false;
            }
          }
        }
      }
    }
  },
  //神马超 孩子们这次我不是区了
  lxyuli: {
    audio: "yuli",
    forced: true,
    trigger: {
      source: "damageBegin1",
      player: "damageBegin4",
      global: "damageEnd"
    },
    logAudio(event, player, name) {
      switch (name) {
        case "damageBegin1":
          return ["yuli1.mp3", "yuli2.mp3"];
        case "damageEnd":
          return ["yuli3.mp3", "yuli4.mp3"];
        case "damageBegin4":
          return ["yuli5.mp3", "yuli6.mp3"];
      }
    },
    filter(event, player, name) {
      if (name == "damageBegin1") {
        return !event.hasNature("thunder");
      }
      return event.hasNature("thunder");
    },
    async content(event, trigger, player) {
      switch (event.triggername) {
        case "damageBegin1":
          game.setNature(trigger, "thunder");
          break;
        case "damageBegin4":
          trigger.cancel();
          break;
        case "damageEnd":
          await player.draw({ num: trigger.num });
          break;
      }
    },
    ai: {
      nothunder: true,
      effect: {
        target(card, player, target, current) {
          if (get.tag(card, "thunderDamage")) {
            return "zeroplayertarget";
          }
        }
      }
    }
  },
  lxtingwei: {
    audio: "tingwei",
    logAudio: (event) => typeof event == "number" ? `tingwei${event}.mp3` : 2,
    trigger: { player: "useCardToPlayer" },
    filter(event, player) {
      return event.card.name == "sha";
    },
    logTarget: "target",
    async content(event, trigger, player) {
      const target = event.targets[0];
      target.addTempSkill(event.name + "_baiban", { player: "damageAfter" });
      target.when({ player: "damageBegin2" }).filter((evt) => evt.getParent(2) == event.getParent(4)).step(async (event2, trigger2, playerx) => {
        const cards2 = playerx.getCards("e");
        player.logSkill("lxtingwei", playerx, null, null, [get.rand(3, 4)]);
        if (cards2.length) {
          await playerx.modedDiscard({ cards: cards2, discarder: player });
        }
        trigger2.num++;
      });
    },
    subSkill: {
      baiban: {
        charlotte: true,
        inherit: "baiban"
      }
    }
  },
  lxjimie: {
    audio: "jimie",
    trigger: { player: "phaseJieshuBegin" },
    skillAnimation: true,
    animationColor: "wood",
    filter(event, player) {
      return game.hasPlayer((current) => current.hasHistory("damage"));
    },
    async cost(event, trigger, player) {
      const next = player.chooseTarget({
        prompt: get.prompt2(event.skill),
        filterTarget(card, player2, target) {
          return target.hasHistory("damage");
        },
        ai(target) {
          const player2 = get.player();
          const list = [];
          game.countPlayer((current) => {
            const num = current.getHistory("damage").reduce((sum, evt) => sum + evt.num, 0);
            if (num > 0) {
              list.push([target, get.damageEffect(target, player2, player2) * num]);
            }
          });
          if (!list.length) {
            return false;
          }
          list.sort((a, b) => b[1] - a[1]);
          return target == list[0][0];
        }
      });
      next.set(
        "targetprompt2",
        next.targetprompt2.concat([
          (target) => {
            if (!target.isIn()) {
              return;
            }
            return `寂灭${target.getHistory("damage").reduce((sum, evt) => sum + evt.num, 0)}`;
          }
        ])
      );
      event.result = await next.forResult();
    },
    async content(event, trigger, player) {
      const target = event.targets[0];
      const num = target.getHistory("damage").reduce((sum, evt) => sum + evt.num, 0);
      await target.damage({ num, source: player });
      player.addSkill(event.name + "_use");
      player.markAuto(event.name + "_use", [target]);
    },
    subSkill: {
      use: {
        charlotte: true,
        intro: { content: "对$使用牌无次数限制" },
        mod: {
          cardUsableTarget(card, player, target) {
            if (player.getStorage("lxjimie_use").includes(target)) {
              return Infinity;
            }
          }
        }
      }
    }
  },
  //曹髦
  lxqianlong: {
    audio: "mbqianlong",
    persevereSkill: true,
    forced: true,
    locked: false,
    keepSkill: true,
    derivation: ["lxjuejin", "lxwuhui"],
    trigger: {
      player: "enterGame",
      global: "phaseBefore"
    },
    filter(event, player) {
      return event.name != "phase" || game.phaseNumber == 0;
    },
    async content(event, trigger, player) {
      let num = game.hasPlayer((current) => {
        return current !== player && current.group === "wei" && player.hasZhuSkill("lxweitong", current);
      }) ? 8 : 5;
      if (!_status.characterlist) {
        game.initCharacterList();
      }
      const alllist = _status.characterlist.slice();
      game.countPlayer2((current) => {
        alllist.removeArray([current.name, current.name1, current.name2]);
      });
      const characters2 = alllist.filter((name) => get.characterSurname(name).some((j) => j[0] == "曹") && get.character(name, 1) == "wei").randomGets(num);
      const skills2 = characters2.flatMap((name) => get.character(name).skills);
      if (!characters2.length || !skills2.length) {
        player.popup("孩子你都禁了什么将");
        return;
      }
      const list = [];
      for (const skill of skills2) {
        list.push([skill, `<div class="popup text" style="width:calc(100% - 10px);display:inline-block"><div class="skill">【` + get.translation(skill) + "】</div><div>" + lib.translate[skill + "_info"] + "</div></div>"]);
      }
      num = Math.min(5, skills2.length);
      const result = skills2.length > num ? await player.chooseButton({
        createDialog: [`潜龙：获得其中任意${num}个技能`, [list, "textbutton"]],
        selectButton: num,
        forced: true,
        ai(button) {
          const skill = button.link;
          const info = get.info(skill);
          if (!info || info.ai?.neg || info.ai?.combo) {
            return 0;
          }
          return 1 + Math.random();
        }
      }).forResult() : { bool: true, links: skills2 };
      if (result?.bool && result.links?.length) {
        const skills3 = result.links;
        player.markAuto(event.name, skills3);
        await player.addAdditionalSkills(event.name, skills3, true);
      }
    },
    intro: {
      content(storage, player) {
        let str = "";
        const skills2 = player.getStorage("lxqianlong");
        for (const skill of skills2) {
          str += `${get.poptip(skill)}`;
        }
        return str;
      }
    },
    group: ["lxqianlong_remove", "lxqianlong_juexing"],
    subSkill: {
      remove: {
        audio: "lxqianlong",
        trigger: { global: "roundEnd" },
        filter(event, player) {
          return player.getStorage("lxqianlong").containsSome(...player.getSkills(null, false, false));
        },
        forced: true,
        locked: false,
        async content(event, trigger, player) {
          const skills2 = player.getStorage("lxqianlong").filter((skill) => player.getSkills(null, false, false).includes(skill));
          const list = [];
          for (const skill of skills2) {
            list.push([skill, `<div class="popup text" style="width:calc(100% - 10px);display:inline-block"><div class="skill">【` + get.translation(skill) + "】</div><div>" + lib.translate[skill + "_info"] + "</div></div>"]);
          }
          if (skills2.length) {
            const result = skills2.length > 1 ? await player.chooseButton({
              createDialog: ["潜龙：请失去一个技能", [list, "textbutton"]],
              forced: true,
              ai(button) {
                return 1 + Math.random();
              }
            }).forResult() : { bool: true, links: skills2 };
            if (result?.bool && result.links?.length) {
              const skill = result.links[0];
              await player.removeSkills(skill);
              get.info("potyinhui").refreshSkill(player, skill);
            }
          }
        }
      },
      juexing2: { charlotte: true },
      juexing: {
        audio: "lxqianlong",
        forced: true,
        locked: false,
        skillAnimation: true,
        animationColor: "wood",
        trigger: {
          player: ["dying", "changeSkillsAfter"]
        },
        filter(event, player) {
          return event.name == "dying" && !player.hasSkill("lxqianlong_juexing2") || !player.getStorage("lxqianlong").some((skill) => player.hasSkill(skill, null, false, false)) && event.removeSkill?.length;
        },
        async content(event, trigger, player) {
          if (trigger.name == "dying") {
            player.addSkill(event.name + "2");
          }
          await player.recoverTo(player.maxHp);
          player.insertPhase();
          await player.addSkills(["lxjuejin", "lxwuhui"]);
        }
      }
    }
  },
  lxweitong: {
    audio: "mbweitong",
    persevereSkill: true,
    zhuSkill: true,
    trigger: {
      player: "lxqianlongBegin"
    },
    forced: true,
    locked: false,
    async content(event, trigger, player) {
    },
    ai: {
      combo: "mbqianlong"
    }
  },
  lxjuejin: {
    audio: "mbjuejin",
    persevereSkill: true,
    limited: true,
    skillAnimation: true,
    animationColor: "wood",
    manualConfirm: true,
    enable: "phaseUse",
    filterTarget: true,
    selectTarget: -1,
    multiline: true,
    multitarget: true,
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.changeSkin({ characterName: "lx_caomao" }, "lx_caomao_shadow");
      const targets = event.targets.sortBySeat();
      await game.doAsyncInOrder(targets, async (target) => {
        let delt = target.getHp() - 1, num = Math.abs(delt);
        if (delt != 0) {
          if (delt > 0) {
            const next = target.changeHp(-delt);
            next._triggered = null;
            await next;
          } else {
            num = 1 - target.getHp(true);
            await target.recover(num);
          }
        }
        if (delt > 0) {
          await target.changeHujia(num + (player == target ? 2 : 0), null, true);
        } else if (player == target) {
          await target.changeHujia(2, null, true);
        }
      });
      game.addGlobalSkill("lxjuejin_xiangsicunwei");
      player.$fullscreenpop("向死存魏！", "thunder");
      const cards2 = ["cardPile", "discardPile"].map((pos) => Array.from(ui[pos].childNodes)).flat();
      const filter = (card) => ["shan", "tao", "jiu"].includes(card.name);
      const cardx = cards2.filter(filter);
      if (cardx.length) {
        await game.cardsGotoSpecial(cardx);
        game.log(cardx, "被移出了游戏");
      }
      for (const target of game.filterPlayer()) {
        const sishis = target.getCards("hej", filter);
        if (sishis.length) {
          target.$throw(sishis);
          game.log(sishis, "被移出了游戏");
          await target.lose(sishis, ui.special);
        }
      }
    },
    ai: {
      order: 0.1,
      result: {
        player(player) {
          let eff = 1;
          game.countPlayer((current) => {
            const att = get.attitude(player, current), num = Math.abs(current.getHp(true) - 1);
            const delt = Math.max(0, num + current.hujia - 5);
            eff -= att * delt;
          });
          return eff > 0 ? 1 : 0;
        }
      }
    },
    subSkill: {
      xiangsicunwei: {
        forced: true,
        silent: true,
        firstDo: true,
        trigger: {
          player: "gainAfter",
          global: "loseAsyncAfter"
        },
        getIndex(event, player) {
          return game.filterPlayer((current) => {
            const cards2 = event.getg?.(current);
            if (!cards2?.length) {
              return false;
            }
            return cards2.some((card) => ["shan", "tao", "jiu"].includes(card.name));
          }).sortBySeat();
        },
        async content(event, trigger, player) {
          const target = event.indexedData;
          const cards2 = trigger.getg(target).filter((card) => ["shan", "tao", "jiu"].includes(card.name));
          if (cards2.length) {
            await target.recast(cards2);
          }
        }
      }
    }
  },
  lxwuhui: {
    audio: "mbcmqingzheng",
    persevereSkill: true,
    limited: true,
    skillAnimation: true,
    animationColor: "wood",
    manualConfirm: true,
    enable: "phaseUse",
    keepSkill: true,
    filter(event, player) {
      return player.getStorage("lxqianlong").length && player.getStorage("lxqianlong").some((skill) => !player.hasSkill(skill, null, false, false));
    },
    changeLimit(skill) {
      game.broadcastAll((skill2) => {
        const info = get.info(skill2);
        info.limited = true;
        info.skillAnimation = true;
        info.animationColor = "wood";
        if (lib.dynamicTranslate[skill2]) {
          lib.dynamicTranslate[skill2] = `限定技，${lib.dynamicTranslate[skill2]}`;
        } else {
          lib.translate[skill2 + "_info"] = `限定技，${lib.translate[skill2 + "_info"]}`;
        }
        game.finishSkill(skill2);
      }, skill);
      return skill;
    },
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      player.addSkill(event.name + "_limit");
      player.addTempSkill(event.name + "_use");
      const skills2 = player.getStorage("lxqianlong").filter((skill) => !player.hasSkill(skill, null, false, false));
      const skillx = [];
      if (skills2.length) {
        for (let skill of skills2) {
          const info = get.info(skill);
          if (!info) {
            continue;
          }
          if (info?.limited) {
            skillx.push(skill);
          } else {
            skill = get.info(event.name).changeLimit(skill);
            skillx.push(skill);
          }
        }
      }
      await player.addAdditionalSkills("lxqianlong", skillx, true);
      player.markAuto(event.name, skillx);
    },
    ai: {
      order: 114514,
      result: {
        player: 1
      },
      combo: "lxqianlong"
    },
    subSkill: {
      use: {
        charlotte: true,
        mark: true,
        intro: { content: "本回合使用牌无次数限制" },
        mod: {
          cardUsable: () => Infinity
        }
      },
      limit: {
        charlotte: true,
        silent: true,
        popup: false,
        firstDo: true,
        trigger: {
          player: ["useSkill", "logSkillBegin", "changeSkillsAfter"]
        },
        filter(event, player) {
          if (event.name == "changeSkills") {
            return event.removeSkill?.length && player.getStorage("lxwuhui").containsSome(...event.removeSkill);
          }
          if (["global", "equip"].includes(event.type)) {
            return false;
          }
          const skill = get.sourceSkillFor(event);
          if (!skill || !player.getStorage("lxwuhui").includes(skill)) {
            return false;
          }
          return true;
        },
        async content(event, trigger, player) {
          if (trigger.name == "changeSkills") {
            const skills2 = trigger.removeSkill;
            player.unmarkAuto("lxwuhui", skills2);
            if (!player.getStorage("lxwuhui").length) {
              await player.die();
            }
          } else {
            const skill = get.sourceSkillFor(trigger);
            player.awakenSkill(skill);
          }
        }
      }
    }
  },
  //司马昭
  lxqiantun: {
    audio: ["jsrgqiantun1.mp3", "jsrgqiantun2.mp3"],
    trigger: { source: "damageSource" },
    forced: true,
    locked: false,
    getIndex: (event) => event.num,
    async content(event, trigger, player) {
      const targets = game.filterPlayer((current) => lib.skill.jsrgjuxia.countSkill(current) >= lib.skill.jsrgjuxia.countSkill(player)).sortBySeat();
      const result = targets.length > 1 ? await player.chooseTarget({
        prompt: `令${get.translation(targets)}中的一名角色摸一张牌并展示所有手牌`,
        filterTarget(card, player2, target) {
          return get.event().targets.includes(target);
        },
        ai(target) {
          const player2 = get.player();
          return get.effect(target, { name: "draw" }, player2, player2) + get.effect(target, { name: "shunshou" }, player2, player2) > 0;
        }
      }).set("targets", targets).forResult() : { bool: true, targets };
      if (result?.bool && result.targets?.length) {
        const target = result.targets[0];
        player.line(target);
        await target.draw();
        await target.showCards(target.getCards("h"), `${get.translation(player)}发动了〖${get.translation(event.name)}〗`);
        const position = player == target ? "ej" : "hej";
        if (target.hasCards(position)) {
          await player.gainPlayerCard({ target, position, forced: true });
        }
      }
    }
  },
  lxxiezheng: {
    audio: "jsrgxiezheng",
    enable: "phaseUse",
    usable: 1,
    manualConfirm: true,
    filter(event, player) {
      return game.hasPlayer((current) => get.info("lxxiezheng").filterTarget(null, player, current));
    },
    filterTarget(card, player, target) {
      return target.getSeatNum() == 1 && target.hasCards("he");
    },
    async content(event, trigger, player) {
      const target = event.target;
      if (target.hasCards("he")) {
        await player.discardPlayerCard({ forced: true, target, position: "he" });
        const card = get.autoViewAs({ name: "juedou", isCard: true }, "unsure");
        if (player.hasUseTarget(card)) {
          const next = player.chooseUseTarget({ card, forced: true });
          player.when({ player: "damageEnd" }).filter((evt) => evt.getParent(3) == next).step(async (event2, trigger2, player2) => {
            const source = trigger2.source;
            if (source?.isIn() && source.hasCards("he")) {
              await player2.gainPlayerCard({ target: source, forced: true, promisify: "he" });
            }
          });
          await next;
        }
      }
    }
  },
  lxweisi: {
    audio: "jsrgweisi",
    logAudio: () => ["jsrgweisi3.mp3"],
    derivation: "lxzhaoxiong",
    dutySkill: true,
    group: ["lxweisi_achieve", "lxweisi_fail"],
    forced: true,
    locked: false,
    trigger: {
      global: ["roundStart", "loseAsyncEnd"],
      player: "gainEnd"
    },
    getIndex(event, player, name) {
      if (name == "roundStart") {
        return 1;
      }
      const cards2 = event.getg?.(player);
      if (!cards2?.length) {
        return 0;
      }
      return game.countPlayer((current) => {
        if (current == player) {
          return false;
        }
        const cardx = event.getl?.(current)?.cards2;
        if (!cardx?.length) {
          return false;
        }
        return cards2.containsSome(...cardx);
      });
    },
    jiuxiList: ["gongshi", "fuyue", "zhuhu", "chema", "nabi", "juchang", "yifu", "huben", "lexuan"],
    jiuxiLength(player) {
      return get.info("lxweisi").jiuxiList.filter((skill) => player.hasSkill(`lxweisi_${skill}`)).length;
    },
    init(player, skill) {
      player.addSkill(skill + "_mark");
    },
    onremove(player, skill) {
      const list = get.info(skill).jiuxiList;
      for (const skillx of list) {
        player.removeSkill(`${skill}_${skillx}`);
      }
      player.removeSkill(skill + "_mark");
    },
    filter(event, player) {
      return get.info("lxweisi").jiuxiList.some((skill) => !player.hasSkill(`lxweisi_${skill}`));
    },
    async content(event, trigger, player) {
      const skill = get.info("lxweisi").jiuxiList.slice().filter((skill2) => !player.hasSkill(`${event.name}_${skill2}`)).randomGet();
      player.addSkill(`${event.name}_${skill}`);
      game.log(player, "获得了“九锡”中的", `#g${get.translation(`${event.name}_${skill}`)}`);
      player.markAuto("lxweisi_mark", [`${event.name}_${skill}`]);
      await event.trigger("gainJiuxi");
    },
    subSkill: {
      achieve: {
        audio: "jsrgweisi1.mp3",
        forced: true,
        locked: false,
        skillAnimation: true,
        animationColor: "wood",
        trigger: { player: "gainJiuxi" },
        filter(event, player) {
          return !get.info("lxweisi").jiuxiList.some((skill) => !player.hasSkill(`lxweisi_${skill}`));
        },
        async content(event, trigger, player) {
          game.log(player, "成功完成使命");
          player.awakenSkill("lxweisi");
          player.changeSkin({ characterName: "lx_simazhao" }, "lx_simazhao_shadow");
          await player.addSkills("lxzhaoxiong");
        }
      },
      fail: {
        audio: "jsrgweisi2.mp3",
        forced: true,
        locked: false,
        trigger: { player: "dying" },
        async content(event, trigger, player) {
          game.log(player, "使命失败");
          player.awakenSkill("lxweisi");
          await player.recoverTo(1);
          const list = get.info("lxweisi").jiuxiList.filter((skill) => !player.hasSkill(`lxweisi_${skill}`));
          if (!list.length) {
            return;
          }
          for (const skillx of list) {
            player.addSkill(`lxweisi_${skillx}`);
            player.markAuto("lxweisi_mark", [`lxweisi_${skillx}`]);
            game.log(player, "获得了“九锡”中的", `#g${get.translation(`lxweisi_${skillx}`)}`);
          }
        }
      },
      mark: {
        charlotte: true,
        onremove: true,
        marktext: "九锡",
        intro: {
          name: "九锡",
          content(storage, player) {
            const jiuxi = player.getStorage("lxweisi_mark");
            if (!jiuxi.length) {
              return "当前没有“九锡”";
            }
            let str = "";
            for (const skill of jiuxi) {
              str += `<li>${get.info(skill).name}：${get.info(skill).info}`;
            }
            return str;
          }
        }
      },
      gongshi: {
        charlotte: true,
        name: "弓矢",
        info: "你的攻击范围+X（X为你获得的“九锡”牌的数量）。",
        mod: {
          attackRange(player, num) {
            return num + get.info("lxweisi").jiuxiLength(player);
          }
        }
      },
      fuyue: {
        charlotte: true,
        name: "斧钺",
        info: "你造成的伤害值+X（X为你获得的“九锡”牌的数量）。",
        forced: true,
        trigger: { source: "damageBegin2" },
        async content(event, trigger, player) {
          trigger.num += get.info("lxweisi").jiuxiLength(player);
        }
      },
      zhuhu: {
        charlotte: true,
        name: "朱户",
        info: "摸牌阶段，你将手牌摸至场上最多。",
        forced: true,
        trigger: { player: "phaseDrawBegin2" },
        filter(event, player) {
          return !event.numFixed && !player.isMaxHandcard();
        },
        async content(event, trigger, player) {
          const num = game.filterPlayer((current) => current.isMaxHandcard())[0].countCards("h");
          if (player.countCards("h") < num) {
            await player.drawTo(num);
          }
        }
      },
      chema: {
        charlotte: true,
        name: "车马",
        info: "其他角色计算与你的距离+1，你计算与其他角色的距离-1。",
        mod: {
          globalFrom(from, to, distance) {
            return distance - 1;
          },
          globalTo(from, to, distance) {
            return distance + 1;
          }
        }
      },
      nabi: {
        charlotte: true,
        name: "纳陛",
        info: "每轮限一次，一号位的回合结束后，你获得一个额外的回合。",
        round: 1,
        forced: true,
        trigger: { global: "phaseAfter" },
        async content(event, trigger, player) {
          player.insertPhase();
        }
      },
      juchang: {
        charlotte: true,
        name: "秬鬯",
        info: "当你的判定牌生效前，你可此打出一张牌代替之。",
        trigger: { player: "judge" },
        filter(event, player) {
          return player.hasCards("hes");
        },
        async cost(event, trigger, player) {
          event.result = await player.chooseCard({
            prompt: get.prompt(event.skill),
            prompt2: `${get.translation(player)}的${trigger.judgestr || ""}判定为${get.translation(player.judging[0])}`,
            position: "hes",
            filterCard(card, player2) {
              const mod2 = game.checkMod(card, player2, "unchanged", "cardEnabled2", player2);
              if (mod2 != "unchanged") {
                return mod2;
              }
              const mod = game.checkMod(card, player2, "unchanged", "cardRespondable", player2);
              if (mod != "unchanged") {
                return mod;
              }
              return true;
            },
            ai(card) {
              const trigger2 = get.event().getTrigger();
              const { player: player2, judging } = get.event();
              const result = trigger2.judge(card) - trigger2.judge(judging);
              let val = get.value(card);
              if (get.subtype(card) == "equip2") {
                val /= 2;
              } else {
                val /= 4;
              }
              if (result == 0) {
                return 0;
              }
              return result - val;
            }
          }).set("judging", player.judging[0]).forResult();
        },
        async content(event, trigger, player) {
          const next = player.respond(event.cards, event.name, "highlight", "noOrdering");
          await next;
          const { cards: cards2 } = next;
          if (cards2?.length) {
            if (player.judging[0].clone) {
              player.judging[0].clone.classList.remove("thrownhighlight");
              game.broadcast(function(card) {
                if (card.clone) {
                  card.clone.classList.remove("thrownhighlight");
                }
              }, player.judging[0]);
              game.addVideo("deletenode", player, get.cardsInfo([player.judging[0].clone]));
            }
            await game.cardsDiscard(player.judging[0]);
            player.judging[0] = cards2[0];
            trigger.orderingCards.addArray(cards2);
            game.log(player, "的判定牌改为", cards2);
            await game.delay(2);
          }
        }
      },
      yifu: {
        charlotte: true,
        name: "衣服",
        info: "当你成为【杀】的目标时，你可进行判定，若判定结果与此【杀】花色相同，则你令此【杀】无效。",
        trigger: { target: "useCardToTarget" },
        filter(event, player) {
          return event.card.name == "sha";
        },
        prompt2: "你可进行判定，若判定结果与此【杀】花色相同，则你令此【杀】无效",
        async content(event, trigger, player) {
          const suit = get.suit(trigger.card);
          const result = await player.judge((card) => {
            const suit2 = get.event().suit;
            if (get.suit(card) == suit2) {
              return 1;
            }
            return 0;
          }).set("suit", suit).set("judge2", (result2) => result2.bool == true ? true : false).forResult();
          if (result.bool) {
            trigger.getParent().excluded.add(player);
          }
        }
      },
      huben2: { charlotte: true },
      huben: {
        charlotte: true,
        name: "虎贲",
        info: "出牌阶段限一次，你可将一张牌当做【杀】使用或打出，若此【杀】造成伤害，则此【杀】不计入次数。",
        enable: ["chooseToUse", "chooseToRespond"],
        filter(event, player) {
          if (player.hasSkill("lxweisi_huben2") || !player.isPhaseUsing()) {
            return false;
          }
          return player.hasCards("hes", (card) => {
            const cardx = get.autoViewAs({ name: "sha" }, [card]);
            return event.filterCard(cardx, player, event);
          });
        },
        filterCard: true,
        position: "hes",
        viewAs: {
          name: "sha"
        },
        async precontent(event, trigger, player) {
          player.addTempSkill("lxweisi_huben2", "phaseAnyAfter");
          player.when({ source: "damageSource" }).filter((evt) => {
            return evt.getParent(3) == event.getParent() && evt.getParent(3).addCount != false;
          }).step(async (event2, trigger2, player2) => {
            trigger2.getParent(3).addCount = false;
            const stat = player2.getStat("card"), name = trigger2.card.name;
            if (typeof stat[name] == "number" && stat[name] > 0) {
              stat[name]--;
            }
          });
        },
        hiddenCard(player, name) {
          return name == "sha" && !player.hasSkill("lxweisi_huben2") && player.isPhaseUsing() && player.hasCards("hes");
        },
        prompt: "将一张牌当做【杀】使用或打出",
        ai: {
          order(item, player) {
            player ??= get.player();
            return get.order({ name: "sha" }, player) + 0.1;
          },
          result: {
            player: 1
          }
        }
      },
      lexuan: {
        charlotte: true,
        name: "乐縣",
        info: "当一张多目标牌生效前，你可令此牌对其中任意名目标角色无效。",
        trigger: { global: "useCard" },
        filter(event, player) {
          return get.info("lxweisi").lexuanCard(event.card.name) && event.targets?.length;
        },
        async cost(event, trigger, player) {
          const card = trigger.card, targets = trigger.targets;
          event.result = await player.chooseTarget({
            prompt: get.prompt(event.skill),
            prompt2: `令任意名${get.translation(card)}对任意名目标角色无效`,
            filterTarget(card2, player2, target) {
              return get.event().targets.includes(target);
            },
            selectTarget: [1, Infinity],
            ai(target) {
              const { card: card2, player: player2, targetx } = get.event();
              return -get.effect(target, card2, targetx, player2);
            }
          }).set("card", card).set("targets", targets).set("targetx", trigger.player).forResult();
        },
        async content(event, trigger, player) {
          const targets = event.targets;
          trigger.targets.removeArray(targets);
        }
      }
    },
    lexuanCard(name) {
      const info = lib.card[name];
      if (!info || info.notarget) {
        return false;
      }
      if (info.selectTarget != void 0) {
        if (Array.isArray(info.selectTarget)) {
          if (info.selectTarget[0] < 0) {
            return !info.toself;
          }
          return info.selectTarget[0] != 1 || info.selectTarget[1] != 1;
        } else {
          if (info.selectTarget < 0) {
            return !info.toself;
          }
          return info.selectTarget != 1;
        }
      }
      return false;
    }
  },
  lxzhaoxiong: {
    audio: "jsrgzhaoxiong",
    limited: true,
    skillAnimation: true,
    animationColor: "wood",
    enable: "phaseUse",
    manualConfirm: true,
    filter(event, player) {
      return game.players.length > 1;
    },
    filterTarget: lib.filter.notMe,
    selectTarget: -1,
    multiline: true,
    multitarget: true,
    async content(event, trigger, player) {
      player.awakenSkill(event.name);
      const targets = event.targets.sortBySeat();
      const targetx = [];
      await game.doAsyncInOrder(targets, async (target) => {
        const cards2 = target.getCards("h").filter((card) => card.name == "sha");
        if (cards2.length) {
          await target.showCards(cards2);
          targetx.push(target);
        } else {
          await target.damage();
        }
      });
      if (targetx.length) {
        const result = await player.chooseTarget({
          prompt: "昭凶：选择一名角色令展示过【杀】的角色对其使用所有【杀】",
          ai(target) {
            return -get.attitude(get.player(), target);
          },
          forced: true
        }).forResult();
        if (result?.bool && result.targets?.length) {
          const target = result.targets[0];
          player.line(target);
          player.popup("给我杀！");
          await game.doAsyncInOrder(targetx, async (i) => {
            while (true) {
              const cards2 = i.getCards("h").filter((card) => card.name == "sha" && i.canUse(card, target, false, false));
              if (cards2.length) {
                await i.useCard({ card: cards2[0], targets: [target], addCount: false });
              } else {
                break;
              }
            }
          });
        }
      }
    },
    ai: {
      order: 1,
      result: {
        player(player, target) {
          if (!game.hasPlayer((current) => get.attitude(player, current) < 0)) {
            return -1;
          }
          if (player.getEnemies().length > player.getFriends(true).length) {
            return 1;
          }
          return -1;
        }
      }
    }
  }
};
const skills = {
  ...skills$E,
  ...skills$D,
  ...skills$C,
  ...skills$B,
  ...skills$A,
  ...skills$z,
  ...skills$y,
  ...skills$x,
  ...skills$w,
  ...skills$v,
  ...skills$u,
  ...skills$t,
  ...skills$s,
  ...skills$r,
  ...skills$q,
  ...skills$p,
  ...skills$o,
  ...skills$n,
  ...skills$m,
  ...skills$l,
  ...skills$k,
  ...skills$j,
  ...skills$i,
  ...skills$h,
  ...skills$g,
  ...skills$f,
  ...skills$e,
  ...skills$d,
  ...skills$c,
  ...skills$b,
  ...skills$a,
  ...skills$9,
  ...skills$8,
  ...skills$7,
  ...skills$6,
  ...skills$5,
  ...skills$4,
  ...skills$3,
  ...skills$2,
  ...skills$1
};
const translates = {
  lx_simazhao: "PE司马昭",
  lx_simazhao_prefix: "PE",
  lxqiantun: "谦吞",
  lxqiantun_info: "当你造成1点伤害后，你令技能数不小于你的角色摸一张牌并展示所有手牌，然后你获得其区域内的一张牌。",
  lxxiezheng: "挟征",
  lxxiezheng_info: "出牌阶段限一次，你可弃置一号位的一张牌，并视为使用一张【决斗】；若你因此受到伤害，你获得伤害来源的一张牌。",
  lxweisi: "威肆",
  lxweisi_info: `使命技，每轮开始时或当你获得其他角色的牌时，你随机将一张${get.poptip({
    id: "lx_jiuxi",
    name: "“九锡”",
    info: "<br>弓矢：你的攻击范围+X（X为你获得的“九锡”牌的数量）。<br>斧钺：你造成的伤害值+X（X为你获得的“九锡”牌的数量）。<br>朱户：摸牌阶段，你将手牌摸至场上最多。<br>车马：其他角色计算与你的距离+1，你计算与其他角色的距离-1。<br>纳陛：每轮限一次，①号位的回合结束后，你获得一个额外的回合。<br>秬鬯：当你的判定牌生效前，你可此打出一张牌代替之。<br>衣服：当你成为【杀】的目标时，你可进行判定，若判定结果与此【杀】花色相同，则你令此【杀】无效。<br>虎贲：出牌阶段限一次，你可将一张牌当做【杀】使用或打出，若此【杀】造成伤害，则此【杀】不计入次数。<br>乐縣：当一张多目标牌生效前，你可令此牌对其中任意名目标角色无效。",
    type: "character"
  })}置于你的武将牌上并拥有对应效果。成功：你获得所有${get.poptip("lx_jiuxi")}后，你获得技能${get.poptip("lxzhaoxiong")}。失败：你进入濒死状态时，你将体力回复至1点并获得其余${get.poptip("lx_jiuxi")}。`,
  lxzhaoxiong: "昭凶",
  lxzhaoxiong_info: "限定技，出牌阶段，你可令所有其他角色展示手牌中的所有【杀】，未展示的角色受到你造成的1点伤害；展示【杀】的角色依次对你选择的目标角色使用其所有【杀】。",
  lx_caomao: "PE曹髦",
  lx_caomao_prefix: "PE",
  lxqianlong: "潜龙",
  lxqianlong_info: `持恒技，游戏开始时，你随机获得五个魏势力曹姓武将牌并选择其中的任意五个技能获得；每轮结束时，你失去一个以此法获得的技能。当你失去所有因“潜龙”获得的技能时或首次进入濒死状态，你回复体力至上限并于本回合结束后执行一个额外回合，然后你获得${get.poptip("lxjuejin")}和${get.poptip("lxwuhui")}。`,
  lxweitong: "卫统",
  lxweitong_info: `持恒技，主公技，游戏开始时，若场上有其他魏势力角色，则你因${get.poptip("lxqianlong")}获得的武将牌改为8张。`,
  lxjuejin: "决进",
  lxjuejin_info: `持恒技，限定技，出牌阶段，你可令所有角色依次调整体力至1点并获得X点护甲（X为其因调整减少的体力值，你以此获得的护甲值额外+2）。然后你将场上、弃牌堆、所有角色手牌中的【闪】，【桃】，【酒】移出游戏且增加全局技能“${get.poptip({
    id: "lxjuejin_xiangsi",
    name: "向死存魏",
    info: "当有角色获得【闪】【桃】【酒】时立即重铸之。",
    type: "character"
  })}”。`,
  lxwuhui: "无回",
  lxwuhui_info: `持恒技，限定技，出牌阶段，你可重新获得游戏开始时因${get.poptip("lxqianlong")}获得的所有技能（以此法获得的技能标签改为限定技）且你本回合使用牌无次数限制。当你再次失去这些技能后，你死亡。`,
  lx_mb_shen_machao: "PE手杀神马超",
  lx_mb_shen_machao_prefix: "PE|手杀神",
  lxyuli: "驭雳",
  lxyuli_info: "锁定技，你造成的伤害改为雷电伤害且防止你受到的雷电伤害。有角色受到雷电伤害后，你摸此伤害数张牌。",
  lxtingwei: "霆威",
  lxtingwei_info: "你使用【杀】指定目标时，你可令目标角色所有技能失效直到其下次受到伤害结算后。若其因此受到伤害，则弃置其装备区所有牌且此伤害+1。",
  lxjimie: "寂灭",
  lxjimie_info: "结束阶段，你可对本回合受到过伤害的一名角色造成X点伤害（X为其本回合受到的伤害数且至少为1），然后你对其使用牌无次数限制直到游戏结束。",
  lx_simafu: "PE司马孚",
  lx_simafu_prefix: "PE",
  lxpanxiang: "蹒襄",
  lxpanxiang_info: "一名与你同势力的角色造成或受到伤害时，你可选择一项：1.令其交给你一张牌，此伤害+1；2.你弃置一张牌，此伤害-1（每项限一次，当两项均选择后，此技能视为未发动过）。",
  lxchenjie: "臣节",
  lxchenjie_info: `锁定技，一名成为过${get.poptip("lxpanxiang")}目标的角色死亡后，你获得伤害来源所有手牌，且其本轮不能使用牌指定你为目标。`,
  lx_simazhou: "PE司马伷",
  lx_simazhou_prefix: "PE",
  lxbifeng: "避锋",
  lxbifeng_info: "当你成为【杀】或普通锦囊牌的目标时，你可翻面并取消之。若你因此将武将牌翻至正面时，你失去1点体力。",
  lxsuwang: "宿望",
  lxsuwang_info: "当你翻面后，你可将手牌调整至4张，若此时你的武将牌正面朝上，你将所有手牌交给等量名角色各一张。",
  lx_guanqiujian: "PE毌丘俭",
  lx_guanqiujian_prefix: "PE",
  lxcuizhen: "摧阵",
  lxcuizhen_info: "回合开始时，你可弃置任意张装备牌，然后选择一项：1.废除一名角色对应副类别的装备栏；2.废除至多等量名角色的武器栏。摸牌阶段，你额外摸X张牌（X为场上被废除装备栏的角色数且至多为3）。",
  lxkuili: "溃离",
  lxkuili_info: "锁定技，当你受到伤害后，若伤害来源有装备栏被废除，其选择一个恢复。",
  lx_wenqin: "PE文钦",
  lx_wenqin_prefix: "PE",
  lxbeiming: "孛明",
  lxbeiming_info: "出牌阶段限一次，你可以获得上一张进入弃牌堆中的装备牌，然后你可将此牌置入一名其他角色的装备区内。若如此做，你可再以此法获得一张装备牌。因此获得装备牌的角色，可以将此牌当做【酒】或无距离限制且无视防具的【杀】使用。",
  lxchoumang: "仇铓",
  lxchoumang_info: `你使用【杀】指定目标后，你可选择一项：1.令此【杀】伤害+1；2.选择一名其他角色成为此【杀】的额外目标。${get.poptip("rule_beishui")}：你废除一个装备栏。`,
  lx_lizhaojiaobo: "PE李昭焦伯",
  lx_lizhaojiaobo_prefix: "PE",
  lxzuoyou: "佐佑",
  lxzuoyou_info: "转换技，出牌阶段限一次，①你可受到1点无来源伤害，令一名角色获得1点护甲；②你可失去1点护甲，对一名角色造成1点伤害。",
  lxshishou: "侍守",
  lxshishou_info: "：一名角色的结束阶段，若你于此回合造成或受到过伤害，你可以令一名与你势力相同的角色获得1点护甲。",
  lx_chengji: "PE成济",
  lx_chengji_prefix: "PE",
  lxkuangli: "狂戾",
  lxkuangli_info: "锁定技，当你使用伤害牌指定其他角色为目标后，你弃置你和目标角色各一张牌，若这两张牌的类别：相同，此牌伤害+1；不同，你摸两张牌。你于回合外使用伤害牌时，额外弃置目标角色一张牌。",
  lxxiongsi: "凶肆",
  lxxiongsi_info: "限定技，出牌阶段，若你已累计造成3点伤害，你可弃置所有手牌，然后令所有其他角色依次失去1点体力。",
  lx_jiachong: "PE贾充",
  lx_jiachong_prefix: "PE",
  lxbeini: "悖逆",
  lxbeini_info: "出牌阶段限一次，你可选择一名其他角色，令其选择一项：1.对你选择的另一名角色使用一张伤害牌；2.交给你一张非伤害牌。",
  lxdingfa: "定法",
  lxdingfa_info: "弃牌阶段结束时，若此回合有伤害牌进入弃牌堆，你弃置一名其他角色一张牌。",
  pe_xiangyu: "项羽",
  pegaishi: "盖世",
  pegaishi_info: "锁定技，座位数小于/大于你的角色不能响应你的牌/受到你造成的伤害+1。当你的体力为1时，此技能不受座位数限制。",
  pefuqin: "覆秦",
  pefuqin_info: `使命技，出牌阶段开始时，你可以摸X张牌并令此阶段前X张牌无次数限制（X为场上和弃牌堆中点数为K的牌数+1）。成功：有角色死亡后，你获得${get.poptip("dcfencheng")}和${get.poptip("jsrgyansha")}。若伤害来源不为你，此技能不会失效。`,
  peyinghao: "英豪",
  peyinghao_info: "主公技，场上每有一名与你势力相同的角色，你的攻击范围便+1。",
  ps_shen_jiangwei: "★神姜维",
  ps_shen_jiangwei_prefix: "★|神",
  psfenzhi: "焚志",
  psfenzhi_info: "锁定技，你造成的伤害改为火焰伤害；有角色受到火焰伤害后，你摸一张牌或令一号位回复1点体力。",
  psjiucheng: "九乘",
  psjiucheng_info: "你可以视为使用一张无距离次数限制的基本牌或普通锦囊牌并摸三张牌，然后此技能失效直到你使用了九张牌。",
  psranjin: "燃尽",
  psranjin_info: "限定技，回合结束时，你可执行一个使用牌无次数限制的额外回合，此回合结束时，若你此回合杀死了其他角色，你须重新发动此技能，否则你死亡。",
  ps_devil_jiangwei: "★魔姜维",
  ps_devil_jiangwei_prefix: "★|魔",
  psmoyan: "魔魇",
  psmoyan_info: "锁定技，你造成和受到的伤害改为体力流失；有角色失去体力后，你摸一张牌。",
  psjiusheng: "九牲",
  psjiusheng_info: "出牌阶段限一次，你可以选择任意名其他角色，你摸这些角色数张牌，令这些角色视为对你使用一张无距离限制的【杀】，然后你弃置其中任意名角色各一张牌，此技能失效直到所有角色失去了9点体力。",
  pshunjin: "魂尽",
  pshunjin_info: "限定技，回合结束时，你可以减少任意点体力上限并进行等量使用牌没有次数限制的额外回合，所有额外回合结束后，你死亡。",
  scl_zhangxiu: "SCL张绣",
  scl_zhangxiu_prefix: "SCL",
  sclfudi: "附敌",
  sclfudi_info: "当你受到伤害后，你可以交给伤害来源一张手牌。若如此做，你对其势力中体力值最大且不小于你的一名角色造成1点伤害。",
  sclcongjian: "从谏",
  sclcongjian_info: "锁定技，当你于回合外造成伤害，或于回合内受到伤害时，此伤害+1。",
  shangguanwaner: "上官婉儿",
  pecaishi: "才诗",
  pecaishi_info: "每回合每种字数限一次，你可以将一张牌当做任意字数相同的基本牌或普通锦囊牌使用。",
  peculv: "促律",
  peculv_info: "锁定技，每名角色于其回合内使用牌时，若与上一张被使用的牌名字数或类型相同，你摸一张牌；其回合结束时，若此技能发动过至少三次，其回复一点体力并摸两张牌。",
  zh_shen_zhangjiao: "纵横神张角",
  zh_shen_zhangjiao_prefix: "纵横|神",
  zhyuanlin: "渊临",
  zhyuanlin_info: "锁定技，每轮开始时/其他角色的判定阶段，令所有其他角色依次/其判定【闪电】；每有一名角色受到雷电伤害，你摸一张牌或回复1点体力。",
  zhkuanglei: "狂雷",
  zhkuanglei_info: `锁定技，你成为其他角色使用伤害牌的目标时，除非其交给你一张牌，否则其判定一次${get.poptip("zhkuanglei_leiji")}。`,
  zhkuanglei_leiji: "雷击",
  zhkuanglei_leiji_info: "当你使用或打出一张【闪】时，你可令一名角色进行一次判定。若结果为：梅花，你回复1点体力，其受到1点雷电伤害；黑桃，其受到2点雷电伤害。",
  zhqiongshan: "穹闪",
  zhqiongshan_info: "每轮限一次，其他角色的判定牌生效前，你可获得此判定牌并声明此判定牌的花色，点数。",
  ps_shen_caocao: "★神曹操",
  ps_shen_caocao_prefix: "★|神",
  psguixin: "归心",
  psguixin_info: "每轮开始时或当你受到伤害后，你可以获得所有其他角色各一张牌。",
  psshenzun: "神尊",
  psshenzun_info: "当你造成伤害时，若受伤角色的势力与你不同，你可以弃置一张牌令此伤害+1且不计入次数。",
  psfeiying: "飞影",
  psfeiying_info: "锁定技，你使用牌无距离限制。",
  wxdl_caopi: "文心雕龙曹丕",
  wxdl_caopi_prefix: "文心雕龙",
  wxdlyishi: "遗世",
  wxdlyishi_info: `限定技，出牌阶段，你可对所有其他角色发动一次${get.poptip("wxdlfangzhu")}。然后所有其他角色本局游戏获得仅能对你发动的${get.poptip("wxdlfangzhu")}。`,
  wxdlfangzhu: "放逐",
  wxdlfangzhu_info: "当你受到伤害后，你可令一名其他角色摸X张牌（X为你已损失体力值），然后该角色翻面。",
  wxdlnianjun: "念君",
  wxdlnianjun_info: `结束阶段，你可${get.poptip("sxrm_connect")}你和任意名其他角色各一张手牌；有${get.poptip("sxrm_connect")}牌的角色对彼此使用牌无次数限制。每回合最先失去${get.poptip("sxrm_connect")}牌的角色失去1点体力。`,
  wxdlqianqian: "慊慊",
  wxdlqianqian_info: "锁定技，你每回合使用的首张牌不能为♥️。",
  pe_wuzetian: "武曌",
  peersheng: "二圣",
  peersheng_info: "一号位不以此法获得牌时，你摸等量的牌；一号位回合结束时，你可以视为使用其本回合使用过的任意张不同名的基本或普通锦囊牌。",
  pelinchao: "临朝",
  pelinchao_info: `限定技，一号位死去时，你可以回复所有体力并结束当前回合，然后你执行一个额外回合，并获得其身份牌（只生效身份场、斗地主）和${get.poptip("penvdi")}。`,
  penvdi: "女帝",
  penvdi_info: "主公技，锁定技，女性角色摸牌数+1，每回合首次受到伤害时防止之；你对对你使用过伤害牌的角色使用牌无次数限制，且你用牌每次指定其时，其减X点体力上限（X为你与其互相指定为目标的次数之和）。",
  ym_zhaoxiang: "春秋赵襄",
  ym_zhaoxiang_prefix: "春秋",
  ymqianling: "潜聆",
  ymqianling_info: "出牌阶段限两次，你可以令至多三名角色与你展示手牌，然后你可以与其中一名黑色手牌数与你相同的角色交换手牌。",
  ymqueying: "雀影",
  ymqueying_info: "若你的手牌数小于体力上限，你可以获得一张【影】，然后视为使用一张【杀】或【闪】。",
  ym_guanyinping: "春秋关银屏",
  ym_guanyinping_prefix: "春秋",
  ymsaxue: "洒血",
  ymsaxue_info: "准备阶段，你可以横置两名角色，然后令一名其他角色对你与其造成共计2点火焰伤害（其先为每名目标分配造成伤害的点数，然后由你先进行结算）。",
  ymxianfeng: "衔锋",
  ymxianfeng_info: "当你受到1点伤害后，你可以弃置伤害来源一张牌，若此牌花色为本回合首次进入弃牌堆，你获得之。",
  ym_zhangxingcai: "春秋张星彩",
  ym_zhangxingcai_prefix: "春秋",
  ymhengren: "横刃",
  ymhengren_info: "转换技，其他角色的出牌阶段：阳，开始时；阴，结束时，若其本阶段有【杀】的剩余使用次数，你可以使用一张【杀】（将计入其使用次数且伤害+1）。",
  ymdanjue: "胆决",
  ymdanjue_info: "当与你距离1以内的角色成为【杀】的目标后，你可以令此【杀】不计入次数，然后其摸一张牌并可以交给你一张牌。",
  ym_jsrg_diaochan: "春秋梦貂蝉",
  ym_jsrg_diaochan_prefix: "春秋|梦",
  ymdiyu: "低语",
  ymdiyu_info: `每轮开始时，你可以令一名角色本轮造成的伤害+1，然后你选择${get.poptip("benghuai")}、${get.poptip("tongji")}、${get.poptip("olbihun")}、${get.poptip("dcyiju")}中一个未选择过的技能令其获得。`,
  ymfuyi: "拂衣",
  ymfuyi_info: "当你成为【杀】的目标时，你可以令一名角色也成为此【杀】的目标并令所有目标角色各摸一张牌；当此【杀】被一名目标角色抵消后，此【杀】对其他目标角色无效。",
  ymjiuji: "就计",
  ymjiuji_info: "每轮结束后，你可以令两名角色依次执行一个仅能对对方使用牌且双方技能互换的额外回合。",
  ym_friend_zhugeliang: "春秋友诸葛亮",
  ym_friend_zhugeliang_prefix: "春秋|友",
  ymfriendyance: "演策",
  ymfriendyance_info: "每轮开始时，你可以摸五张牌并将等量张手牌以任意顺序置于武将牌上直到本轮结束。当一张牌被使用时，你移去武将牌上首张牌，每满足一项你便摸一张牌：1.两张牌花色相同；2.两张牌类型相同。",
  ymfriendfangqiu: "方遒",
  ymfriendfangqiu_info: `限定技，当你因${get.poptip("ymfriendyance")}放置牌后，你可以展示武将牌上的所有“演策”牌，直到本轮结束你因${get.poptip("ymfriendyance")}获得的牌翻倍。`,
  ymfriendzhugelianggongli: "共励",
  ymfriendzhugelianggongli_info: `锁定技，若友庞统/友徐庶在场且与你阵营相同，${get.poptip("ymfriendyance")}摸五张牌改为摸六张牌/${get.poptip("ymfriendyance")}每轮首张牌视为满足任意一项条件。`,
  pe_shen_zhonghui: "PE神钟会",
  pe_shen_zhonghui_prefix: "PE|神",
  pelinjie: "凛界",
  pelinjie_info: "每轮开始时，你可对一名角色造成1点伤害并令其获得1个“凛”。有“凛”的其他角色受到伤害后须弃置一张手牌。若其因此弃置了最后一张手牌，你对其造成1点伤害。",
  peduzhang: "独仗",
  peduzhang_info: "当你成为黑色牌的唯一目标后，你摸一张牌并获得一个“凛”，你的手牌上限+X（X为你拥有的“凛”的数量）。",
  pejianghuo: "降祸",
  pejianghuo_info: `觉醒技，回合开始时，若“凛”大于场上角色数，你将所有“凛”置于你的武将牌上，然后你摸X张牌并增加1点体力上限（X为你“凛”的数量），失去${get.poptip("pelinjie")}获得${get.poptip("pelishi")}。`,
  pelishi: "立世",
  pelishi_info: "结束阶段，若你没有“凛”，你受到1点雷电伤害。否则你可移去任意个“凛”并选择等量名角色，这些角色下回合开始前，你可选择一个阶段令其跳过。",
  ps_shen_weiyan: "★神魏延",
  ps_shen_weiyan_prefix: "★|神",
  ps_devil_weiyan: "★魔魏延",
  ps_devil_weiyan_prefix: "★|魔",
  psjimou: "极谋",
  psjimou_info: "限定技，准备阶段，你可失去任意点体力上限并选择一名其他角色，除你与其之外的角色须进入“休整”状态直到有角色死亡或此后你与其进行了等量个回合。",
  psyuxue: "浴血",
  psyuxue_info: "每回合限一次，你可将任意张非伤害牌当做一张可指定等量目标且无距离限制的【杀】使用，你可“吸取”因此牌受伤的目标角色的1点体力上限。",
  pskuifa: "馈伐",
  pskuifa_info: `你使用多目标伤害牌时，选择一项：1.若此牌指定目标数小于存活人数时，你可令你本回合的出【杀】次数+1；2.若此牌指定目标数不小于你的体力值时，你可摸不为此牌目标的其他角色数张牌。<br>${get.poptip("rule_chengshi")}：此牌不可被响应。<br>${get.poptip("rule_beishui")}：你失去X点体力（X为你的背水次数）。`,
  psnimou: "逆谋",
  psnimou_info: "限定技，准备阶段，你可失去任意点体力上限并选择等量名其他角色，其进入“休整”状态直到有角色死亡或此后其他角色均进行了回合。",
  pskexue: "渴血",
  pskexue_info: "每回合限一次，你可以将任意张非伤害牌当做一张须等量【闪】抵消的【杀】使用，若此【杀】造成伤害且目标角色未打出【闪】，你可“吸取”目标角色1点体力上限。",
  psjiefa: "竭伐",
  psjiefa_info: `你使用伤害牌指定唯一目标时，选择一项：1.若目标体力值大于游戏内角色数，此牌可额外指定一个目标；2.若你的体力上限小于其，你可令此牌额外执行一次。<br>${get.poptip("rule_chengshi")}：你将体力回复至上限或将手牌摸至上限。<br>${get.poptip("rule_beishui")}：你失去X点体力（X为你背水的次数）。`,
  sm_shenmo_sunquan: "PE神魔孙权",
  sm_shenmo_sunquan_prefix: "PE|神|魔",
  smsibian: "俟变",
  smsibian_info: `觉醒技，回合开始时，若你的体力值为奇数且“神格”多于“魔心”，你${get.poptip({
    id: "smsibian_huashen",
    name: "化神",
    info: `<br>失去技能${get.poptip("smsibian")}、${get.poptip("smqihua")}和${get.poptip("smdue")}<br>获得技能${get.poptip("smshenjiang")}、${get.poptip("smshengshou")}和${get.poptip("smshifeng")}<br>调整体力上限为10`,
    type: "character"
  })}；若你的体力值为偶数且“魔心”多于”神格”，你${get.poptip({
    id: "smsibian_rumo",
    name: "入魔",
    info: `<br>失去技能${get.poptip("smsibian")}、${get.poptip("smqihua")}和${get.poptip("smdue")}<br>获得技能${get.poptip("smmobian")}、${get.poptip("smyanshi")}和${get.poptip("smpoyu")}<br>调整体力上限为3`,
    type: "character"
  })}。`,
  smqihua: "启化",
  smqihua_info: "每回合每项限一次。当其他角色受到伤害时，你可以失去等量体力防止之并获得等量“神格”；当你受到伤害时，你可以获得等量“魔心”并令一名其他角色失去等量体力。",
  smdue: "渡厄",
  smdue_info: "限定技，当你的体力值变化后，你可以交换你的“神格”和“魔心”的数量。",
  sm_shen_sunquan: "PE神孙权",
  sm_shen_sunquan_prefix: "PE|神",
  smshenjiang: "神降",
  smshenjiang_info: "锁定技，你化神时，将你的体力调整为化神前的两倍，并弃置装备区所有武器和进攻坐骑牌。",
  smshengshou: "圣授",
  smshengshou_info: "你的回合限X次，你可以令一名角色视为使用一张非伤害牌（X为你的“神格”数）。",
  smshifeng: "释锋",
  smshifeng_info: `锁定技，当武器和进攻坐骑进入你的装备区时，改为弃置此牌，并随机获得一个“${get.poptip({
    id: "smtianfashenchen",
    name: "天发神谶",
    info: ["yinghun", "hongde", "bingyi", "smshifeng_guanwei", "bizheng", "anguo", "shelie", "wengua", "rebotu", "rezhiheng", "mbjiexun", "xiashu", "rejieyin", "oldimeng", "xinfu_guanchao", "drlt_jueyan", "lanjiang", "anxu"].map((skill) => `${get.poptip(skill)}`).join("、"),
    type: "character"
  })}”技能。`,
  smshifeng_guanwei: "观微",
  smshifeng_guanwei_info: "每回合限一次。一名角色的出牌阶段结束时，若其本回合使用过两张以上的牌且这些牌花色均相同或均没有花色，则你可以弃置一张牌，令其摸两张牌并进行一个额外的出牌阶段。",
  sm_mo_sunquan: "PE魔孙权",
  sm_mo_sunquan_prefix: "PE|魔",
  smmobian: "魔变",
  smmobian_info: "锁定技，你入魔时，将你的体力值和体力上限调整为入魔前体力值的一半，并弃置装备区所有防具、防御坐骑和判定区的牌。",
  smyanshi: "魇噬",
  smyanshi_info: "你的回合限X次，你可以视为使用一张无距离次数限制且本回合未使用过的伤害牌（X为你的“魔心”数-体力上限且至少为1）。",
  smpoyu: "破御",
  smpoyu_info: `锁定技，当防具和防御坐骑牌进入你的装备区时，改为弃置此牌，并令你的伤害牌永久获得一个“${get.poptip({
    id: "smwuliujian",
    name: "吴六剑",
    info: "<li>白虹：伤害基数改为2；<li>青冥：可以额外指定一个目标；<li>辟邪：无视防具；<li>紫电：不可响应；<li>百里：额外结算一次；<li>流星：无次数限制。",
    type: "character"
  })}”效果`,
  pe_baiqi: "白起",
  pewuan: "武安",
  pewuan_info: "锁定技，你造成伤害后，你造成的伤害永久+1，且受到你伤害的角色本回合所有非锁定技失效，你对其使用牌无距离次数限制。",
  peshashen: "杀神",
  peshashen_info: "锁定技，当你令一名其他角色进入濒死状态时，其直接死亡然后你于本回合结束后执行一个额外的回合。",
  pe_liuhui: "PE刘徽",
  pe_liuhui_prefix: "PE",
  pejieshu: "解术",
  pejieshu_info: "当你使用牌指定目标结算后，你可以获得之且此牌本轮不能指定相同目标，然后你本回合使用此牌无距离限制且不能被【解术】获得。",
  pegeyuan: "割圆",
  pegeyuan_info: "锁定技，若你使用“解术”牌指定的连续两个目标与你的距离：1.不同：你弃两张牌；2.相同，你摸两张牌。然后重置“割圆”的目标。",
  pegusuan: "股算",
  pegusuan_info: "觉醒技，当“割圆”指定过三组角色完全不同的目标且你因此弃置的牌少于四张时，你摸五张牌并选择两名其他角色与你的距离互相为1。若如此做，你对距离1以内的角色使用牌无次数限制。",
  pe_huamulan: "花木兰",
  perongbian: "戎弁",
  perongbian_info: "锁定技，当你使用装备牌后，你随机获得一张女将牌，获得其一个技能直到你失去此牌。",
  peliezhan: "烈瞻",
  peliezhan_info: "你可以将一张女将牌当无距离和次数限制的基本牌或普通锦囊牌使用，然后摸两张牌。",
  petijun: "替君",
  petijun_info: "限定技，出牌阶段，你可以获得一名男性角色装备区里的所有牌，你与其回复所有体力；当你杀死其以外的角色后，此技能视为未发动。",
  pe_yangyuhuan: "杨玉环",
  peyichuan: "驿传",
  peyichuan_info: "出牌阶段限一次，你可以声明一种牌类型，所有其他角色同时选择是否交给你任意张此类型的牌，不执行的角色不能响应一号位使用牌直到游戏结束。",
  pexiuhua: "羞花",
  pexiuhua_info: `锁定技，当你获得其他角色的牌后，其摸等量的牌；${get.poptip("peyichuan")}结算后，你和交给你牌最多的角色各回复1点体力并摸三张牌。`,
  pe_xiahoulan: "夏侯岚",
  pexunji: "迅击",
  pexunji_info: "每阶段你首次获得牌后，你可以视为使用一张无距离限制且不可被响应的基本牌。",
  peshuangfeng: "双锋",
  peshuangfeng_info: `回合开始时，你可以令你本回合所有阶段改为出牌/摸牌阶段并获得${get.poptip("peguiren")}/${get.poptip("peyichi")}直到回合结束。`,
  peguiren: "归刃",
  peguiren_info: "你造成伤害后本阶段使用牌无次数限制，然后你和一名角色各摸一张牌。",
  peyichi: "义驰",
  peyichi_info: "你执行摸牌阶段后，你可以令一名其他角色回复1点体力。",
  pe_lishimin: "李世民",
  pepozhen: "破阵",
  pepozhen_info: `${get.poptip("rule_chihengji")}，你对其他角色造成伤害后，令其选择一项其本回合未选择的：1.你对其使用牌无次数距离限制；2.其交给你一个区域内的所有牌；3.其体力上限调整至1且失去所有技能。`,
  petaoge: "韬戈",
  petaoge_info: "每回合每种类别限一次，你可以将一张非装备牌当做同类别的牌使用。",
  pezhenguan: "贞观",
  pezhenguan_info: "主公技，每轮开始时或结束时，你令每种势力的一名其他角色摸一张牌，然后你摸X张牌（X为场上势力数）。",
  ylyg_zhurong: "雁翎祝融",
  ylyg_zhurong_prefix: "雁翎",
  ylyg_lieren: "烈刃",
  ylyg_lieren_info: "当你每回合首次使用【杀】指定唯一目标后，你可以与其拼点，赢的角色于此【杀】结算后将所有手牌当【南蛮入侵】使用。",
  ylyg_juxiang: "巨象",
  ylyg_juxiang_info: "当一名角色使用【南蛮入侵】时，若使用者不为你，此牌对你无效且你于结算后获得之；否则你可令此牌对体力值大于你的角色造成的伤害+1。",
  ylyg_xuhuang: "雁翎徐晃",
  ylyg_xuhuang_prefix: "雁翎",
  ylyg_duanliang: "断粮",
  ylyg_duanliang_info: "你可以将一张黑色非锦囊牌当无距离限制的【兵粮寸断】使用；若目标数手牌大于你，你摸一张牌。",
  ylyg_zier: "辎饵",
  ylyg_zier_info: "当一名角色跳过一个阶段后，你可以记录此阶段。每轮限一次，一名角色回合开始时，你可以将其本回合的一个阶段改为你本轮记录的阶段。",
  wxdl_caozhi: "文心雕龙曹植",
  wxdl_caozhi_prefix: "文心雕龙",
  wxdl_huamao: "华茂",
  wxdl_huamao_info: `首轮开始时，你随机获得三个描述中仅含一种花色且花色各不相同的${get.poptip({
    id: "wxdl_huamao_skills",
    name: "技能",
    type: "character",
    info: `
			<li>♥️：${["wushen", "liushi", "gongxin", "tianxiang"].map((i) => get.poptip("huamao_" + i)).join("、")}<br>
			<li>♦️：${["guose", "limu", "fengpo", "jiexun"].map((i) => get.poptip("huamao_" + i)).join("、")}<br>
			<li>♠️：${["leiji", "zuoding", "miehai", "jiyu"].map((i) => get.poptip("huamao_" + i)).join("、")}<br>
			<li>♣️：${["luoying", "lianhuan", "zhujiu", "ninghan"].map((i) => get.poptip("huamao_" + i)).join("、")}
		`
  })}直到洗牌，然后你可以重新分配这些技能描述中的花色。`,
  wxdl_qishen: "弃身",
  wxdl_qishen_info: "回合结束时，你可以选择至多三名角色，直到你的下回合结束：这些角色其他技能失效且造成的伤害+1，然后其中上回合也选择过的角色各摸三张牌。",
  huamao_wushen: "武神",
  huamao_liushi: "流矢",
  huamao_gongxin: "攻心",
  huamao_tianxiang: "天香",
  huamao_guose: "国色",
  huamao_limu: "立牧",
  huamao_fengpo: "凤魄",
  huamao_jiexun: "诫训",
  huamao_leiji: "雷击",
  huamao_zuoding: "佐定",
  huamao_miehai: "灭害",
  huamao_jiyu: "讥谀",
  huamao_luoying: "落英",
  huamao_lianhuan: "连环",
  huamao_zhujiu: "煮酒",
  huamao_ninghan: "凝寒",
  huamao_wushen_info: "锁定技。①你的♥手牌均视为【杀】。②你使用♥【杀】无距离和次数限制且不可被响应。",
  huamao_liushi_info: "出牌阶段，你可以将一张♥牌置于牌堆顶，视为对一名角色使用一张【杀】（无距离限制且不计入使用次数）。当此【杀】造成伤害后，受到伤害的角色获得一个“流”。有“流”的角色手牌上限-X（X为其“流”数）。",
  huamao_gongxin_info: "出牌阶段限一次，你可以观看一名其他角色的手牌，并可以展示其中一张♥牌，然后将其弃置或置于牌堆顶。",
  huamao_tianxiang_info: "当你受到伤害时，你可以弃置一张♥手牌，防止此次伤害并选择一名其他角色，然后你选择一项：1.令其受到伤害来源对其造成的1点伤害，然后摸X张牌（X为其已损失体力值且至多为5）；2.令其失去1点体力，然后获得你弃置的牌。",
  huamao_guose_info: "出牌阶段限一次，你可以选择一项：将一张♦牌当做【乐不思蜀】使用；或弃置一张♦牌并弃置场上的一张【乐不思蜀】。选择完成后，你摸一张牌。",
  huamao_limu_info: "出牌阶段，你可以将一张♦牌当做【乐不思蜀】对自己使用，然后回复1点体力。只要你的判定区内有牌，你对攻击范围内的其他角色使用牌便没有次数和距离限制。",
  huamao_fengpo_info: "当你每回合首次使用【杀】或【决斗】指定唯一目标后，你可以选择一项：1.摸X张牌，令此牌的伤害值基数+1；2.摸一张牌，令此牌的伤害值基数+X（X为其♦牌的数量）。",
  huamao_jiexun_info: "结束阶段，你可令一名其他角色摸等同于场上♦牌数的牌，然后弃置X张牌（X为此前该技能发动过的次数）。若有角色因此法弃置了所有牌，则你将X归零，然后你发动〖复难〗时，无须令对方获得你使用的牌。",
  huamao_leiji_info: "当你使用或打出一张【闪】时，你可令任意一名角色进行一次判定。若结果为♠，其受到2点雷电伤害。",
  huamao_zuoding_info: "当其他角色于其回合内使用♠牌指定目标后，若本回合内没有角色受到过伤害，则你可以令其中一名目标角色摸一张牌。",
  huamao_miehai_info: "你可以将两张牌当作无距离次数限制的刺【杀】使用。此【杀】结算完成后，此过程中正面失去♠牌且已受伤的角色摸两张牌并回复1点体力。",
  huamao_jiyu_info: "出牌阶段限一次，你可以令一名角色弃置一张手牌。若如此做，你不能使用与之相同花色的牌，直到回合结束。若其以此法弃置的牌为♠，你翻面并令其失去1点体力。若你有未被〖讥谀〗限制的手牌，则你可以继续发动此技能，但不能选择本回合已经选择过的目标。",
  huamao_luoying_info: "当其他角色的♣牌因弃置或判定而进入弃牌堆后，你可以获得之。",
  huamao_lianhuan_info: "你可以将♣手牌当作【铁索连环】使用或重铸。",
  huamao_zhujiu_info: "你可以将至少X+1张牌当【酒】使用（X为你本回合已使用过【酒】的次数），然后若这些牌存在非♣牌，此技能失效直到回合结束。",
  huamao_ninghan_info: "锁定技。①所有角色手牌中的♣【杀】均视为冰【杀】。②当一名角色受到冰冻伤害后，你将造成此伤害的牌对应的实体牌置入“城”。",
  hl_shen_guanyu: "虎牢神关羽",
  hl_shen_guanyu_prefix: "虎牢|神",
  pewushen: "武神",
  pewushen_info: "锁定技，你的♥️手牌均视为【杀】，你使用的♥️【杀】无距离次数限制且不可被响应；其他角色的♥️牌因弃置而置入弃牌堆时，你可获得之。",
  hl_shen_zhugeliang: "虎牢神诸葛亮",
  hl_shen_zhugeliang_prefix: "虎牢|神",
  pekuangfeng: "狂风",
  pekuangfeng_info: "回合结束时，你可移去任意张“星”并选择等量名角色，直到你的下回合开始前，这些角色受到火焰伤害时，此伤害+1。若如此做，你可令所有角色依次视为使用一张无距离限制的火【杀】 。",
  petianlin: "天霖",
  petianlin_info: "回合结束时，你可移去任意张“星”并选择等量名角色，直到你的下回合开始前，这些角色受到非雷电伤害时，你防止此伤害并摸X张牌（X为你武将牌上“星”牌的数量）。",
  hl_shen_lvmeng: "虎牢神吕蒙",
  hl_shen_lvmeng_prefix: "虎牢|神",
  peshelie: "涉猎",
  peshelie_info: "①摸牌阶段，你可改为展示牌堆顶的五张牌，然后你获得其中每种花色的牌各一张。②当你造成伤害时，你可发动一次〖涉猎①〗。",
  pegongxin: "攻心",
  pegongxin_info: "①出牌阶段限一次，你可观看一名其他角色的手牌并展示其中一张♥️牌，然后将此牌置于牌堆顶或弃置此牌。②当你受到伤害时，你可对伤害来源发动一次〖攻心①〗。",
  hl_shen_zhouyu: "虎牢神周瑜",
  hl_shen_zhouyu_prefix: "虎牢|神",
  peqinyin: "琴音",
  peqinyin_info: "弃牌阶段结束时，你可令所有角色失去1点体力或回复1点体力。",
  peyeyan: "业炎",
  peyeyan_info: "限定技，出牌阶段，你可将至多X点火焰伤害分配给至多三名角色，若有角色因此技能死亡，你重置此技能并令X-1（X初始为3）。",
  hl_shen_lvbu: "虎牢神吕布",
  hl_shen_lvbu_prefix: "虎牢|神",
  peshenwei: "神威",
  pexiuluo: "修罗",
  peduomo: "堕魔",
  pedaojue: "道绝",
  peshenwei_info: "出牌阶段开始时，你可以失去任意点体力(至多为4)，然后令等量名角色依次弃两张牌，若其未弃置【闪】，你将武将牌翻面并获得其一张手牌。",
  pexiuluo_info: "锁定技，你的【桃】/【酒】均视为无次数限制的火/雷【杀】；你造成和受到的伤害均+1。",
  peduomo_info: `限定技，当你进入濒死状态时，你可以将体力值回复至手牌数，然后获得技能${get.poptip("pedaojue")}。`,
  pedaojue_info: "当你使用【杀】指定唯一目标时，你可以与其交换武将牌上的一个技能直到本回合结束。",
  ylyg_pangtong: "雁翎庞统",
  ylyg_pangtong_prefix: "雁翎",
  ylygxiangxing: "相形",
  ylygxiangxing_info: `锁定技，其他角色视为拥有${get.poptip("reyingzi")}${get.poptip("rebiyue")}。`,
  ylygxiangxing_yingzi: "英姿",
  ylygxiangxing_yingzi_info: "锁定技，摸牌阶段摸牌时，你额外摸一张牌；你的手牌上限为你的体力上限。",
  ylygxiangxing_biyue: "闭月",
  ylygxiangxing_biyue_info: "结束阶段，你可以摸一张牌，若你没有手牌，则改为摸两张牌。",
  ylyglianhuan: "连环",
  ylyglianhuan_info: "出牌阶段限X次，你可以将一张牌当【铁索连环】使用，结算后两名目标随机均分手牌（无法均分的手牌交给你，X为洗牌的次数+1）。",
  ylygniepan: "涅槃",
  ylygniepan_info: "限定技，当你处于濒死状态时，你可以摸三张牌，回复至3点体力，然后造成2点火焰伤害；洗牌时，此技能重置。",
  ylyg_dianwei: "雁翎典韦",
  ylyg_dianwei_prefix: "雁翎",
  ylygqiangxi: "强袭",
  ylygqiangxi_info: "出牌阶段，你可以与一名本回合未选择过的角色拼点：若你赢，你摸一张牌或弃置对方一张牌；本回合第二次没赢时，你失去1点体力对这两次拼点目标及其之间的所有其他角色各造成1点伤害，然后此技能本回合失效。",
  ylyg_xiaoqiao: "雁翎小乔",
  ylyg_xiaoqiao_prefix: "雁翎",
  ylygtianxiang: "天香",
  ylygtianxiang_info: "当你受到伤害时，你可以弃置一张♥牌防止之；当你正面失去♥牌时，你可以将之交给一名其他角色，其于此回合结束时失去1点体力（不叠加）。",
  ylyghongyan: "红颜",
  ylyghongyan_info: "锁定技，你的♠牌视为♥；游戏开始时，你选择获得其他男性角色的一个含颜色或花色的技能。",
  ylyg_yuji: "雁翎于吉",
  ylyg_yuji_prefix: "雁翎",
  ylygguhuo: "蛊惑",
  ylygguhuo_info: `每回合每种类别限一次，你可以扣置一张手牌当任意基本牌或普通锦囊牌使用，手牌数大于你的角色依次选择是否质疑，然后翻开此牌：若为假，且有角色质疑则此牌作废，选择不质疑的角色各交给你一张手牌；若为真，质疑的角色依次失去1点体力并获得${get.poptip("ylyghuinu")}`,
  ylyghuinu: "恚怒",
  ylyghuinu_info: `锁定技，你必须质疑${get.poptip("ylygguhuo")}`,
  zc26_sp_xushi: "26SP徐氏",
  zc26_sp_xushi_prefix: "26|SP",
  zc26_longchen: "龙谶",
  zc26_longchen_info: `出牌阶段限一次，你可以选择一项：1.对一名角色造成1点雷电伤害；2.令一名角色摸一张牌并回复1点体力。在执行效果前，你投掷${get.poptip({
    id: "longchen_shengbei",
    name: "龙鳞贝",
    type: "character",
    info: "投出两个龙鳞贝，若结果为：<br><li>圣杯：一阴一阳，额外执行一次所选效果。<br><li>笑杯：双阳，获得一枚“龙怒”。<br><li>阴杯：双阴，获得两枚“龙怒”。"
  })}询问神明。`,
  shengbei_left_yin: "龙鳞贝",
  shengbei_left_yang: "龙鳞贝",
  shengbei_right_yin: "龙鳞贝",
  shengbei_right_yang: "龙鳞贝",
  zc26_tianqi: "天泣",
  zc26_tianqi_info: `觉醒技，准备阶段若你的“龙怒”标记不小于3枚，或当你进入濒死状态时，你减1点体力上限并回复所有体力，然后你获得${get.poptip("zc26_shouxin")}，对所有男性角色造成1点雷电伤害。`,
  zc26_shouxin: "守心",
  zc26_shouxin_info: "当你造成属性伤害后，你摸一张牌；当你成为其他角色使用牌的目标时，你可以移去1枚“龙怒”取消之。",
  zc26_lusu: "26界鲁肃",
  zc26_lusu_prefix: "26|界",
  zc26_haoshi: "好施",
  zc26_haoshi_info: "摸牌阶段，你可以多摸两张牌，然后手牌数唯一最多的角色将其一半手牌（向下取整）交给一名由你指定的手牌数最少的角色。",
  zc26_dimeng: "缔盟",
  zc26_dimeng_info: "出牌阶段限一次，你可以选择两名手牌数之差小于3的其他角色，这些角色依次可以使用至多三张手牌，然后双方交换手牌。",
  zc26_zhanghe: "26界张郃",
  zc26_zhanghe_prefix: "26|界",
  zc26_qiaobian: "巧变",
  zc26_qiaobian_info: "每轮开始时，你可以与一名上轮未选择过的角色各摸一张牌；本轮其回合内：你可以弃置一张牌跳过其一个主要阶段，若为：摸牌阶段，其获得至多两名角色各一张手牌；出牌阶段，其可以移动场上一张牌。",
  zc26_taishici_prefix: "26|界",
  zc26_taishici: "26界太史慈",
  zc26_tianyi: "天义",
  zc26_tianyi_info: "出牌阶段限一次，你可以拼点：若你赢，你本回合使用【杀】次数上限+1、目标上限+1、无距离限制。若你没赢，你本回合使用下一张牌时取消之并令唯一目标摸两张牌。",
  zc26_dangmo: "荡魔",
  zc26_dangmo_info: "当你使用牌结算后，若此牌目标与你本回合使用的上一张牌目标不完全相同，你可以对两张牌的重合目标各造成1点伤害。",
  zc26_jiaxu_prefix: "26|界",
  zc26_jiaxu: "26界贾诩",
  zc26_wansha: "完杀",
  zc26_wansha_info: "锁定技，你的回合内：除濒死角色外，所有其他角色的红色基本牌均视为【杀】，且使用时你摸两张牌。",
  zc26_weimu: "帷幕",
  zc26_weimu_info: "黑色锦囊牌对你无效：你可以将黑色非锦囊牌当【借刀杀人】使用。",
  scl_jiaxu: "SCL贾诩",
  scl_jiaxu_prefix: "SCL",
  sclwansha: "完杀",
  sclwansha_info: "锁定技，当其他角色于你的回合内进入濒死状态时，你令其死亡。",
  pe_guozhao: "PE郭照",
  pe_guozhao_prefix: "PE",
  pepianchong: "偏宠",
  pepianchong_info: "摸牌阶段，你可以改为获得牌堆底的一张牌，此牌倒置（标记为“偏宠”），然后你摸一张牌，直到你下回合开始：你每失去一张倒置牌后，摸一张牌；你每失去一张未倒置的手牌后，获得牌堆底的一张牌并倒置。",
  pezunwei: "尊位",
  pezunwei_info: "出牌阶段限一次，你可以选择一项，然后移除该选项：1.将手牌数摸至全场最多；2.随机使用牌堆中的装备牌，直到你装备区牌数为全场最多；3.将体力回复至全场最多。",
  jun_lvbu: "君吕布",
  jun_lvbu_prefix: "君",
  xia_caopi: "侠曹丕",
  xia_caopi_prefix: "侠",
  peqinyi: "勤艺",
  peqinyi_info: "当你每回合首次造成或受到伤害后，你可以视为使用一张未以此法使用过的基本牌或普通锦囊牌。",
  pejixin: "技新",
  pejixin_info: "当你于本局游戏首次使用一种牌名的牌后，你可以摸X张牌并展示之，令这些牌无距离和任何次数且不计入手牌上限（X为本轮发动〖技新〗的次数）。",
  pejiwei: "继魏",
  pejiwei_info: "觉醒技，每回合结束时，若本轮〖技新〗发动次数不小于一号位的体力值，则你增加1点体力上限并回复所有体力，然后你可以从武将牌堆中选择获得五个魏势力主公技。",
  xy_caocao_prefix: "徐兖",
  xy_caocao: "徐兖曹操",
  xy_shengju: "笙聚",
  xy_shengju_info: "锁定技，你的摸牌阶段摸牌数，攻击范围，每回合使用【杀】的次数改为X（X为场上魏势力角色数且至多为5）。",
  xy_mintong: "悯恸",
  xy_mintong_info: "每回合限一次，当一名角色对另一名魏势力角色使用仅指定一名角色的伤害牌结算结束后，你可视为对其使用一张【杀】。",
  xy_chengyu_prefix: "徐兖",
  xy_chengyu: "徐兖程昱",
  xy_liaofu: "燎伏",
  xy_liaofu_info: "每回合限一次，出牌阶段，你可以将一张与“燎伏”牌属性均不同的【杀】置于武将牌上；其他角色于你回合外使用【杀】时，你可以移去一张与此【杀】属性相同的“燎伏”牌并对其造成1点对应属性的伤害。",
  xy_jinshou: "烬守",
  xy_jinshou_info: "结束阶段，若你本回合体力值未发生过变化，你可以弃置所有手牌并失去1点体力，若如此做，直到你的下个回合开始，其他角色不能对你使用仅指定一名角色的伤害牌。",
  xy_xunyu_prefix: "徐兖",
  xy_xunyu: "徐兖荀彧",
  xy_jianzu: "谏族",
  xy_jianzu_info: "出牌阶段限一次，你可以与一名角色拼点，赢的角色对其攻击范围内的一名角色造成1点伤害。",
  xy_dishou: "砥守",
  xy_dishou_info: "当你受到伤害时，若伤害来源手牌数和体力值不等，伤害来源选择一项：1.弃置所有手牌；2.失去1点体力，然后重复此过程直到手牌数等于体力值或进入濒死状态。",
  xy_chengong_prefix: "徐兖",
  xy_chengong: "徐兖陈宫",
  xy_jizheng: "积诤",
  xy_jizheng_info: "每回合限一次，当你摸牌时，你可以改为令一名角色视为使用一张无视距离的【杀】。",
  xy_zhangkai_prefix: "徐兖",
  xy_zhangkai: "徐兖张闿",
  xy_luejin: "掠金",
  xy_luejin_info: "一名角色摸牌阶段结束时，若其此阶段因摸牌获得了超过两张牌，你可以视为对该角色使用一张【杀】，若该【杀】未造成伤害，你回复1点体力。",
  xy_lvbu: "徐兖吕布",
  xy_lvbu_prefix: "徐兖",
  xy_xiaoxi: "虓袭",
  xy_xiaoxi_info: "游戏开始时，你获得七枚“势”标记。摸牌阶段，你改为摸“势”标记数张牌，然后弃置1枚“势”标记。",
  xy_fenqi: "焚骑",
  xy_fenqi_info: "每回合限一次，出牌阶段，你可以弃置1枚“势”标记，获得一张【一鼓作气】。",
  yiguzuoqi: "一鼓作气",
  yiguzuoqi_info: "出牌阶段，对你使用。你本回合使用牌无距离次数限制。",
  xy_zhangmiao: "徐兖张邈",
  xy_zhangmiao_prefix: "徐兖",
  xy_mouni: "谋逆",
  xy_mouni_info: "回合开始时，你可以展示一名其他角色的所有手牌并对其使用其中的所有【杀】直到其进入濒死。若这些【杀】均未造成过伤害，你跳过本回合。",
  xy_zongfan: "纵反",
  xy_zongfan_info: "觉醒技，回合结束时，若本回合你发动过〖谋逆〗且有角色进入过濒死，你将任意张牌交给一名其他角色，增加X点体力上限并失去〖谋逆〗，然后回复X点体力并获得〖战孤〗（X为你以此法给出的牌数）。",
  xy_zhangu: "战孤",
  xy_zhangu_info: "锁定技，回合开始时，若你的体力上限大于1且你的装备区或手牌区没有牌，你减少1点体力上限并摸两张牌。",
  xy_caosong: "徐兖曹嵩",
  xy_caosong_prefix: "徐兖",
  xy_lilu: "礼赂",
  xy_lilu_info: "摸牌阶段，你可以改为弃置任意张牌并摸至体力上限（至多摸至五张），然后将至少一张手牌交给一名其他角色，若你以此法交出的手牌数大于你上一次因〖礼赂〗交出的手牌数，你增加1点体力上限，然后回复1点体力。",
  xy_yizheng: "翊正",
  xy_yizheng_info: "回合结束时，你可以选择一名其他角色，直到你的下个回合开始，其造成伤害或回复体力时，你减少1点体力上限并令伤害值/回复值+1。",
  eu_yujin: "欧陆于禁",
  eu_yujin_prefix: "欧陆",
  eu_zhangliao: "欧陆张辽",
  eu_zhangliao_prefix: "欧陆",
  eu_yuejin: "欧陆乐进",
  eu_yuejin_prefix: "欧陆",
  eu_jieyue: "节钺",
  eu_jieyue_info: "每轮各限一次，结束阶段，你可以令一名角色摸一张牌，然后其选择一项：1.令你摸三张牌；2.失去1点体力，令你执行一个额外回合。",
  eu_tuxi: "突袭",
  eu_tuxi_info: "你造成伤害后，可以摸一张牌，然后获得受伤角色和其上下家各一张牌。",
  eu_xiaoguo: "骁果",
  eu_xiaoguo_info: "出牌阶段，你可以弃置一张基本牌并对一名其他角色造成1点伤害。若其因此进入濒死，此技能本回合失效。",
  eu_makang: "马抗",
  eu_xiru: "西入",
  eu_xiru_info: "锁定技，摸牌阶段，你额外摸X张牌（X为场上坐骑牌数量的一半，向上取整）。",
  eu_zongma: "纵马",
  eu_zongma_info: "出牌阶段开始时，你可以将一张牌当作【进攻马】/【防御马】置入一名角色的装备区，令其造成/受到伤害时，将此牌置入弃牌堆并令此伤害+1。",
  zongma_attack: "进攻马",
  zongma_attack_info: "你造成伤害时，将此牌置入弃牌堆并令此伤害+1。",
  zongma_defend: "防御马",
  zongma_defend_info: "你受到伤害时，将此牌置入弃牌堆并令此伤害+1。",
  eu_Ardashir: "阿尔达希尔",
  eu_wanwang: "万王",
  eu_wanwang_info: "每轮限一次，其他西势力角色的出牌阶段开始时，你可以代替其执行此阶段。",
  eu_sashan: "萨珊",
  eu_sashan_info: "锁定技，游戏开始时，你令所有角色变更势力为西。",
  eu_nagong: "纳贡",
  eu_nagong_info: "其他角色的准备阶段，其可以交给你一张牌，将其势力变更为群直到其下回合开始。",
  eu_Macrinus: "马克里努斯",
  eu_dengtian: "登天",
  eu_dengtian_info: "锁定技，每轮开始时，你须选择一项数值+1：1.摸牌阶段摸牌数；2.手牌上限；3.每回合首次造成的伤害值。",
  eu_mingshu: "命数",
  eu_mingshu_info: "锁定技，你发动〖登天〗令任意一项增加的数值达到3后，失去所有体力。",
  eu_juedou: "角斗",
  eu_juedou_info: "其他角色对你使用的【杀】结算后，若你未受到此【杀】造成的伤害，你可以视为对其使用一张无距离限制的【杀】。",
  eu_kaisa: "欧陆凯撒",
  eu_kaisa_prefix: "欧陆",
  eu_ducai: "独裁",
  eu_ducai_info: `${get.poptip("rule_chihengji")}，你的回合内使用牌无距离次数限制，其他角色不能使用牌且所有技能失效。`,
  eu_zhitong: "治统",
  eu_zhitong_info: "转换技，当你使用牌时，若目标包含，阳：自己，摸两张牌且回复1点体力；阴：其他角色，你获得其装备区所有牌并对其造成1点伤害。",
  eu_jiquan: "集权",
  eu_jiquan_info: "主公技，锁定技，西势力角色的回合开始时，你回复1点体力并摸一张牌。",
  pe_xiahouxuan: "PE夏侯玄",
  pe_xiahouxuan_prefix: "PE",
  pehuanfu: "宦浮",
  pehuanfu_info: "当你使用【杀】指定目标后或成为【杀】的目标后，你可以弃置至多体力上限张牌，然后摸2X张牌，若如此做，此【杀】对目标角色造成的伤害改为X(X为你弃置的牌数)。",
  drag_huaci: "龙华雌",
  drag_huaci_prefix: "龙",
  dragshiyao: "试药",
  dragshiyao_info: "每名角色的出牌阶段限一次，其可以摸一张牌并令你获得一张【毒】，然后进行判定。若结果为：黑色，你减少1点体力上限并令其获得一张【毒】；红色，其可以将一张红色牌当【桃】使用。",
  dragzuoyu: "佐愈",
  dragzuoyu_info: "每回合限一次，其他角色于其回合外回复/失去体力时，你可以增加1点体力上限/对其发动一次〖毒医〗。",
  dragduyi: "毒医",
  dragduyi_info: "每回合每种牌名限一次，你可以将一张【毒】当作【杀】/【刮骨疗毒】/【以毒攻毒】使用。",
  dragjuliao: "拒疗",
  dragjuliao_info: "锁定技，【毒】对你无效。当其他角色成为你使用的转化牌目标后，其选择是否减少1点体力上限令此牌对其无效。",
  drag_guansuo: "龙关索",
  drag_guansuo_prefix: "龙",
  dragqianfu: "搴芙",
  dragqianfu_info: "每回合限一次，你可以视为使用一张基本牌，然后摸三张牌。若你以此法指定了女性角色，你令其增加1点体力上限。",
  dragchengyuan: "承缘",
  dragchengyuan_info: "限定技，你或一名女性角色进入濒死时，你可令其回复体力至体力上限。",
  dragyuxian: "羽现",
  dragyuxian_info: `使命技，①每轮限一次，一名角色的回合结束时，若本回合你或其回复过体力，你可以执行一个额外回合。②成功：准备阶段，若你已执行过四个回合，你获得${get.poptip("dragyinglong")}和${get.poptip("dragxiefang")}。③失败：一名女性角色令你进入濒死。`,
  dragyinglong: "应龙",
  dragyinglong_info: "每回合限一次，一名角色使用虚拟牌或转化牌时，你可以令其本回合：1.使用同类型牌不可被响应；2.使用同颜色牌无次数限制。",
  dragxiefang: "撷芳",
  dragxiefang_info: "你计算与其他角色的距离-X，你的基本牌或普通锦囊牌可以额外指定X名角色为目标（X为场上女性角色数且至少为1）。",
  drag_guanping: "龙关平",
  drag_guanping_prefix: "龙",
  draglongyin: "龙吟",
  draglongyin_info: "每回合限一次，一名角色于其出牌阶段内使用红色【杀】时，你可以摸一张牌并展示之，令此【杀】不计入使用次数；若你展示的牌与其使用的【杀】颜色相同，你令此杀额外结算一次。",
  dragjiezhong: "竭忠",
  dragjiezhong_info: "限定技，一名角色的准备阶段，你可以令其摸你体力值张牌；若该角色不为你，下一轮开始时此技能视为未发动过。",
  drag_guanyinping: "龙关银屏",
  drag_guanyinping_prefix: "龙",
  dragxueji: "血祭",
  dragxueji_info: "出牌阶段限一次，你可以对至多X名角色各造成1点火焰伤害（X为你已损失的体力值且至少为1）.",
  draghuxiao: "虎啸",
  draghuxiao_info: "锁定技，你每回合首次造成火焰伤害后，摸一张牌并令你本回合对受伤角色使用牌无次数限制。",
  drag_zhoucang: "龙周仓",
  drag_zhoucang_prefix: "龙",
  dragzhongyong: "忠勇",
  dragzhongyong_info: "每回合限X+1次，与你势力相同的角色成为另一名角色使用牌的目标时，若此牌为：基本牌，你可令其摸一张牌；锦囊牌：你可失去1点体力，然后你代替其成为此牌目标（X为你已损失的体力值）。",
  drag_liaohua: "龙廖化",
  drag_liaohua_prefix: "龙",
  dragzhawang: "诈亡",
  dragzhawang_info: `回合开始时，你可选择一项：1.本回合获得〖诈降〗；2.失去1点体力，将本回合准备阶段和结束阶段改为摸牌阶段；3.${get.poptip("rule_beishui")}：减少1点体力上限。`,
  dragxigui: "西归",
  dragxigui_info: "限定技，你进入濒死或每轮结束时，你可以将体力值回复至体力上限。若如此做，下一个轮次结束时，你失去〖诈亡〗并获得〖当先〗。",
  drag_shendan: "龙申耽",
  drag_shendan_prefix: "龙",
  drag_shenyi: "龙申仪",
  drag_shenyi_prefix: "龙",
  draglianxiang: "连降",
  draglianxiang_info: "你成为其他角色使用牌的目标时，可以弃置一张牌，然后令一名手牌数全场最多的角色摸一张牌。",
  dragpingmeng: "平孟",
  dragpingmeng_info: "你使用【杀】或失去最后的手牌时，可以变更势力并将手牌补至体力上限，然后当前回合结束阶段，你可视为使用一张【杀】。",
  dragpanfeng: "叛封",
  dragpanfeng_info: "你使用【杀】或失去最后的手牌时，可以变更势力并将手牌补至体力上限，然后对一名与你势力相同的角色造成1点伤害。",
  drag_luxun: "龙陆逊",
  drag_luxun_prefix: "龙",
  dragqianxun: "谦逊",
  dragqianxun_info: "判定阶段开始时/出牌阶段，你可以弃置两张手牌，然后弃置判定区一张牌/摸一张牌。",
  draglianying: "连营",
  draglianying_info: "当你不因你持有的技能而失去最后的手牌时，你可以摸两张牌并选择一项：1.弃置一张牌；2.弃置两张牌，令一种牌名的牌对你无效直到你的下回合开始。",
  drag_xusheng: "龙徐盛",
  drag_xusheng_prefix: "龙",
  dragpojun: "破军",
  dragpojun_info: "当你使用【杀】指定目标后，你可以观看其至多X张牌并将之移出游戏直到回合结束(X为其体力值）：若其中有装备牌，你弃置其中一张；若其中有锦囊牌，你摸一张牌。你使用的【杀】对手牌区与装备区内牌数皆不大于你的角色造成的伤害+1。",
  she_lvmeng: "龙吕蒙",
  she_lvmeng_prefix: "龙",
  shefujing: "蝜荆",
  shefujing_info: "锁定技，首轮开始时，你翻面并摸三张牌，然后令一名其他角色获得“伏”标记。",
  shefujing_fu: "伏",
  shefujing_fu_info: `你每回合首次造成/受到伤害后，令你获得此标记的角色可以：1.摸一张牌；2.获得1点护甲；3.${get.poptip("rule_beishui")}：失去1点体力并对你发动一次〖攻心〗。`,
  shefujiang: "浮江",
  shefujiang_info: "每回合限一次，一名角色受到伤害后，你可以令其上家和下家依次选择是否对其使用一张【杀】。",
  shetonglu: "通陆",
  shetonglu_info: "觉醒技，每轮结束时，若你的手牌数大于体力值，你减少1点体力上限并获得〖攻心〗〖博图〗和〖夺荆〗。若如此做，你可令拥有“伏”的角色选择移动至你的上家或下家，然后你执行一个额外回合。",
  hm_zhong_heart: "众",
  hm_zhong_diamond: "众",
  hm_zhong_club: "众",
  hm_zhong_spade: "众",
  hm_zhong_heart_info: "锁定技，当你受到伤害后，你出牌阶段使用【杀】次数上限+1，攻击范围+1。",
  hm_zhong_diamond_info: "锁定技，当你受到伤害后，你摸两张牌。",
  hm_zhong_club_info: "锁定技，当你受到伤害后，你的手牌上限+1。",
  hm_zhong_spade_info: "锁定技，当你受到伤害后，其他角色计算到你的距离+1，你计算到其他角色的距离-1。",
  hm_shen_huangfusong: "汉末神皇甫嵩",
  hm_shen_huangfusong_prefix: "汉末神",
  hm_shen_zhujun: "汉末神朱儁",
  hm_shen_zhujun_prefix: "汉末神",
  hm_shen_yl_luzhi: "汉末神卢植",
  hm_shen_yl_luzhi_prefix: "汉末神",
  hm_shen_zhangjiao: "汉末神张角",
  hm_shen_zhangjiao_prefix: "汉末神",
  hm_shen_zhangbao: "汉末神张宝",
  hm_shen_zhangbao_prefix: "汉末神",
  hm_shen_zhangliang: "汉末神张梁",
  hm_shen_zhangliang_prefix: "汉末神",
  hm_nanhualaoxian: "汉末南华老仙",
  hm_nanhualaoxian_prefix: "汉末",
  hm_guigu: "眭固",
  hm_bosi: "卜巳",
  hm_yudu: "于毒",
  hm_taosheng: "陶升",
  hm_fuyun: "浮云",
  hm_tangzhou: "唐周",
  hm_chengyuanzhi: "程远志",
  hm_gaosheng: "高升",
  hm_heman: "何曼",
  hm_yanzheng: "严政",
  hm_bocai: "波才",
  hm_dengmao: "邓茂",
  hm_bairao: "白绕",
  hm_huoyin: "祸引",
  hm_huoyin_info: "锁定技，你对攻击距离内包含你且你攻击距离包含其的其他角色：使用【杀】无次数限制；当你对这些角色造成伤害后，你摸一张牌，然后其选择是否使用一张牌",
  hm_jigan: "积干",
  hm_jigan_info: "每个回合结束后，若有角色交给过其他角色手牌/计算距离发生变化，你可以令其中两名角色分别视为使用一张基本牌。",
  hm_weizhu: "围铸",
  hm_weizhu_info: "出牌阶段限一次，你可以重铸任意张手牌并获得弃牌堆中等量张装备牌，然后你交给等量名其他角色各一张牌，依此法获得牌的角色本轮计算距离-1。",
  hm_guiquan: "贵权",
  hm_guiquan_info: "你可以将一张装备牌当未以此法使用过的锦囊牌对体力不小于你的其他角色使用。",
  hm_paoxi: "咆袭",
  hm_paoxi_info: "锁定技，每回合限一次，当你连续成为牌/使用牌指定目标后，你本回合下次受到/造成的伤害+1。",
  hm_houying: "后应",
  hm_houying_info: "出牌阶段，你可以弃置两张黑色牌并视为使用一张无次数限制的【杀】，若你造成了伤害，你摸一张牌。",
  hm_sanshou: "三首",
  hm_sanshou_info: `锁定技，你的准备阶段和结束阶段改为出牌阶段，并在此阶段将武将牌改为${get.poptip({
    id: "character_hm_shen_zhangbao",
    name: "张宝",
    type: "character",
    dialog: "characterDialog"
  })}或${get.poptip({
    id: "character_hm_shen_zhangliang",
    name: "张梁",
    type: "character",
    dialog: "characterDialog"
  })}。此阶段结束后把武将牌替换回${get.poptip({
    id: "character_hm_shen_zhangjiao",
    name: "张角",
    type: "character",
    dialog: "characterDialog"
  })}。`,
  hm_jijun: "集军",
  hm_jijun_info: "当你使用牌指定首个目标后，若你为此牌的目标，你可以进行判定，然后选择一项：1.获得此牌；2.将判定牌置于武将牌上，称为“方”。",
  hm_fengtong: "方统",
  hm_fengtong_info: "出牌阶段结束时，若你有“方”，你可以重铸一张手牌，若你重铸的牌与任意张“方”的点数之和为36，你可以将对应的“方”置入弃牌堆，然后对一名其他角色造成3点雷电伤害。",
  hm_zhouyuan: "咒怨",
  hm_zhouyuan_info: "出牌阶段限一次，你可以选择一名其他角色，然后其将所有黑色/红色手牌扣置于其武将牌上，你将所有红色/黑色手牌置于武将牌上，这些牌称为“咒兵”，出牌阶段结束时，你与其收回“咒兵”。",
  hm_zhaobing: "诏兵",
  hm_zhaobing_info: "当需要使用或打出手牌时，你可以将“咒兵”如手牌般使用或打出。",
  hm_kunjun: "困军",
  hm_kunjun_info: "锁定技，你的初始手牌数+4，手牌数小于你的角色不能响应你使用的牌；你不能响应手牌数大于你的角色使用的牌。",
  hm_yingzhan: "营战",
  hm_yingzhan_info: "锁定技，你造成和受到的属性伤害+1。",
  hm_cuiji: "摧击",
  hm_cuiji_info: "其他角色的出牌阶段开始时，若你手牌数大于其，你可以将任意张手牌当一张雷【杀】对其使用，若你以此法造成了伤害，你摸等量的牌。",
  hm_fudao: "覆道",
  hm_fudao_info: "游戏开始时，你可以将一张【众】置入你的装备区。",
  hm_zongfu: "众附",
  hm_zongfu_info: `每轮开始时，你可以声明一种花色，然后令手牌最少的角色依次选择一项：1.将一张牌置于牌堆顶；2.从牌堆底摸一张牌，当以此法失去牌的角色造成伤害，你发动一次${get.poptip("hm_fudao")}。`,
  hm_dangjing: "荡京",
  hm_dangjing_info: `当你发动${get.poptip("hm_zongfu")}后，若你伤害/回复值为全场最高，你可以令一名角色进行一次判定，若结果为你${get.poptip("hm_zongfu")}声明的花色，你对其造成1点雷电伤害并重复此流程。`,
  hm_didao: "地道",
  hm_didao_info: "限定技，出牌阶段开始时，若你已受伤，你可以将所有手牌当一张无距离限制的伤害为X的【杀】使用(X为此【杀】的实体牌数）。",
  hm_xianxiang: "献降",
  hm_xianxiang_info: "锁定技，当你杀死一名角色时，你令另一名其他角色获得死亡角色区域内的所有牌。",
  hm_juedian: "决巅",
  hm_juedian_info: `锁定技。当你每回合首次使用目标唯一的牌造成伤害后，你选择一项并视为对受伤角色使用一张【决斗】：①失去1点体力。②减1点体力上限。③${get.poptip("rule_beishui")}：你令此牌的伤害值基数+1。`,
  hm_nitian: "逆天",
  hm_nitian_info: "限定技，出牌阶段，你可以获得以下效果直到回合结束：1.你使用的牌不能被抵消；2.结束阶段，若你本回合未杀死角色，你死亡。",
  hm_xiongshi: "凶势",
  hm_xiongshi_info: "每名角色的出牌阶段限一次，其可以将一张手牌置于你的武将牌上。",
  hm_difeng: "地锋",
  hm_difeng_info: "锁定技，当一名角色将牌置于武将牌上后，你与其各摸一张牌；你造成或受到伤害时，伤害来源可以弃置你武将牌上一张牌，令此伤害+1。",
  hm_wuxiao: "武嚣",
  hm_wuxiao_info: "锁定技，当每回合首次有红色牌进入弃牌堆时，你本回合下次造成和受到的伤害+1。",
  hm_qianhu: "前呼",
  hm_qianhu_info: "出牌阶段，你可以弃置两张红色牌并视为使用一张【决斗】，若你造成了伤害，你摸一张牌。",
  hm_jukou: "举寇",
  hm_jukou_info: "出牌阶段限一次，你可以令一名其他角色摸一张牌/获得武将牌上所有的牌，然后其本回合不能使用【杀】/手牌。",
  hm_weichenn: "违谶",
  hm_weichenn_info: "限定技，出牌阶段，你可以依次选择两名其他角色，然后你展示前者的手牌，并与后者各摸三张牌。若如此做，前者对后者依次使用手牌中的所有伤害牌，且本回合这两名角色互相使用牌无次数限制。",
  hm_shuiqu: "随去",
  hm_shuiqu_info: "锁定技，一名角色的弃牌阶段开始时，你弃置所有手牌，然后你选择增加1点体力上限或回复1点体力。",
  hm_yure: "余热",
  hm_yure_info: "限定技，当你弃置牌后，你可以将你此次所有弃置的牌交给一名其他角色。",
  hm_zhannei: "战内",
  hm_zhannei_info: "限定技，出牌阶段，你可以选择一名其他角色，令你与其的距离视为1，直到你进入濒死状态。",
  hm_qianwei: "扦卫",
  hm_qianwei_info: "出牌阶段限一次，你可以展示并交给一名与你距离为1的其他角色任意张非伤害牌并摸等量张牌，然后其可以使用你交给其的任意张牌。",
  hm_dafu: "打富",
  hm_dafu_info: "你使用伤害牌指定目标后，你可令目标角色摸一张牌，然后其不能响应此牌。",
  hm_jipin: "济贫",
  hm_jipin_info: "你对手牌数大于你的角色造成伤害后，你可以获得其一张手牌，然后可以将之交给一名其他角色。",
  hm_tianshu: "天书",
  hm_tianshu_info: "出牌阶段开始时，若场上没有太平要术，你可以弃置一张牌，然后将太平要术置入一名角色的装备区。",
  hm_yufeng: "御风",
  hm_yufeng_info: "出牌阶段限一次，你可以进行判定，若结果为：黑桃，你令一名其他角色跳过其下个回合的出牌阶段和弃牌阶段；红桃，你令一名其他角色跳过其下个回合的摸牌阶段；梅花或方块，你摸一张牌并重置〖御风〗。",
  hm_weiluan: "为乱",
  hm_weiluan_info: "锁定技，准备阶段/摸牌阶段/出牌阶段开始时，你进行判定，若结果为黑桃，你的攻击范围/摸牌阶段摸牌数/使用杀的限制次数+1。",
  hm_tianpan: "天判",
  hm_tianpan_info: "锁定技，当你的判定牌生效时，若结果为黑桃，你获得此牌，然后你回复1点体力或1点体力上限；不为黑桃，你失去1点体力或减1体力上限。",
  hm_gaiming: "改命",
  hm_gaiming_info: "每回合限一次，当你的判定牌生效前，若结果不为黑桃，你可以亮出牌堆顶的一张牌代替之。",
  hm_tuntian: "屯犬",
  hm_tuntian_info: "锁定技，准备阶段，你令你本局游戏的摸牌阶段摸牌数、手牌上限、每回合首次受到的伤害+1直到你发动〖迁军〗。",
  hm_qianjun: "迁军",
  hm_qianjun_info: "限定技，出牌阶段，你可以交给一名其他角色装备区内的所有牌并与其交换座次，然后你回复1点体力并获得〖乱击〗。",
  hm_cheji: "撤击",
  hm_cheji_info: "出牌阶段限一次，你可以重铸任意张牌，然后令一名其他角色重铸等量张手牌，若其重铸的牌含：【杀】，你对其造成1点火焰伤害；【闪】，其对你指定的角色视为使用一张【杀】；【桃】，你与其各摸两张牌。",
  hm_daicui: "怠摧",
  hm_daicui_info: "锁定技，你的回合内，一名角色使用属性【杀】指定目标后，目标角色需将X张牌置于武将牌上直到回合结束，若如此做，此【杀】伤害+1（X为本回合进入过弃牌堆的牌）。",
  hm_kuixiang: "溃降",
  hm_kuixiang_info: "每名角色限一次，其他角色脱离濒死状态时，你可以对其造成1点伤害，若你杀死了该角色，你可以摸三张牌。",
  hm_shice: "势策",
  hm_shice_info: "转换技，阳：当你受到属性伤害时，若你的技能数不大于伤害来源，你可以防止此伤害并视为使用一张【火攻】；阴：当你不因此技能使用牌指定唯一目标后，你可以令其弃置装备区任意张牌，然后此牌额外结算X次（X为其装备区的牌数）。",
  hm_podai: "破怠",
  hm_podai_info: "每轮各限一次，一名角色的回合开始或结束时，你可以选择一项：1.令其描述中含有基本牌名或数字的一个技能失效；2.令其摸三张牌，然后对其造成1点火焰伤害。",
  hm_podai_faq: "关于破怠",
  hm_podai_faq_info: "<br>技能能被破怠失效需要满足以下其中至少一个条件：<br>①技能描述中含有阿拉伯数字；<br>②技能描述中含有中文数字；<br>③含有基本牌名（包含未开启卡包）。",
  xk_qiyijun: "起义军",
  xk_qiyijun_info: "锁定技，①你的出【杀】次数+1。②回合结束时，若你本回合未对没有“起义军”的角色使用过【杀】且未对这些角色造成过伤害，你选择一项：1.失去“起义军”并弃置所有手牌；2.失去1点体力。③没有“起义军”的角色对“起义军”使用【杀】的次数限制+1。",
  xk_luoli: "罗厉",
  xk_cuilian: "崔廉",
  xk_penghu: "彭虎",
  xk_shanfu: "单福",
  xk_pengqi: "彭绮",
  xk_zulang: "祖郎",
  xkjuluan: "聚乱",
  xkjuluan_info: "锁定技，①游戏开始时你获得“起义军”，然后你令至多两名不为1号位且没有“起义军”的其他角色依次选择一项：1.获得“起义军”；2.你弃置其一张手牌。②你每回合造成或受到第二次伤害时，此伤害+1。",
  xkxianxing: "险行",
  xkxianxing_info: "出牌阶段，你使用伤害牌指定其他角色为唯一目标后，可以摸X张牌，若如此做，此牌结算完成后，若此牌未造成伤害且X大于1，你选择一项：1.失去X-1点体力；2.本回合此技能失效（X为此技能本回合发动次数）。",
  xktanlu: "贪赂",
  xktanlu_info: "其他角色的回合开始时，你可令其选择一项：1.交给你X张手牌（X为你与其体力值之差的绝对值）；2.你对其造成1点伤害，然后其弃置你一张手牌。",
  xkjubian: "惧鞭",
  xkjubian_info: "锁定技，你受到其他角色造成的伤害时，若你的手牌数大于体力值，你将手牌弃至体力值并防止此伤害。",
  xkjuqian: "聚黔",
  xkjuqian_info: "锁定技，①游戏开始时你获得“起义军”，然后你令至多两名不为1号位且没有“起义军”的其他角色依次选择一项：1.获得“起义军”；2.你对其造成1点伤害。",
  xkkanpo: "勘破",
  xkkanpo_info: "锁定技，每回合限一次，你对体力值不大于你的角色造成伤害后，你摸X张牌（X为场上“起义军”的数量）。",
  xkyizhong: "倚众",
  xkyizhong_info: "锁定技，一名角色获得“起义军”后，你令其获得1点护甲。",
  xkbimeng: "弊蒙",
  xkbimeng_info: "出牌阶段限一次，你可以将体力值张手牌当任意基本牌或普通锦囊牌使用。",
  xkzhue: "诛恶",
  xkzhue_info: "群势力技，每回合限一次，一名群势力角色使用非装备牌时，你可以令其摸一张牌且此牌不可被响应。此牌结算完成后，若此牌造成过伤害，你变更势力至蜀。",
  xkfuzhu: "辅主",
  xkfuzhu_info: "蜀势力技，每回合限一次，一名角色使用转化牌结算完成后，你可以将一张牌置于牌堆顶，然后展示牌堆顶的四张牌。若如此做，其使用展示牌中的所有锦囊牌，然后你将剩余牌以任意顺序置于牌堆顶或牌堆底。",
  xkjushou: "聚首",
  xkjushou_info: "锁定技，①游戏开始时你获得“起义军”，然后你令至多两名不为1号位且没有“起义军”的其他角色依次选择一项：1.获得“起义军”；2.你获得其一张手牌。",
  xkliaoluan: "缭乱",
  xkliaoluan_info: "每名拥有“起义军”的角色限一次，其可以于出牌阶段翻面，然后视为对攻击范围内一名没有“起义军”的角色造成1点伤害。",
  xkhuaying: "花影",
  xkhuaying_info: "一名有“起义军”的角色因受到其以外的角色造成的伤害而死亡后，你可以令一名有“起义军”的角色重置武将牌且其视为未发动过〖缭乱〗。",
  xkjizhong: "集众",
  xkjizhong_info: "锁定技，有“起义军”的角色摸牌阶段额外摸一张牌，其计算与其以外的角色距离-1。",
  xkxijun: "袭军",
  xkxijun_info: "每回合限两次，出牌阶段或你受到伤害后，你可以将一张黑色牌当【杀】或【决斗】使用或打出；一名角色受到此牌造成的伤害后，其本回合不能回复体力。",
  xkhaokou: "豪寇",
  xkhaokou_info: "群势力技，锁定技，①游戏开始时，你获得“起义军”。②你失去“起义军”后，变更势力至吴。",
  xkronggui: "荣归",
  xkronggui_info: "吴势力技，一名吴势力角色使用【决斗】或红色【杀】指定首个目标时，你可以弃置一张基本牌并为此牌额外指定一个目标。",
  yj_jiling: "PE纪灵",
  yj_jiling_prefix: "PE",
  yjshuangren: "双刃",
  yjshuangren_info: "出牌阶段，你可以与一名角色拼点。若你赢，你可以依次视为使用X张【杀】；若你没赢，你的【杀】本回合均视为K点的【闪】（X为你本回合〖双刃〗拼赢的次数）。",
  scl_lvbu: "SCL吕布",
  scl_lvbu_prefix: "SCL",
  scls_zhanshen: "战神",
  scls_zhanshen_info: "锁定技，准备阶段，你获得以下一项未获得的效果：1.摸牌阶段额外摸一张牌；2.使用【杀】造成伤害+1；3.使用【杀】可额外指定一个目标。",
  scl_pangdegong: "SCL庞德公",
  scl_pangdegong_prefix: "SCL",
  scl_sunhanhua: "SCL孙寒华",
  scl_sunhanhua_prefix: "SCL",
  scl_miheng: "SCL祢衡",
  scl_miheng_prefix: "SCL",
  scl_peixiu: "SCL裴秀",
  scl_peixiu_prefix: "SCL",
  scl_caoying: "SCL曹婴",
  scl_caoying_prefix: "SCL",
  scl_luotong: "SCL骆统",
  scl_luotong_prefix: "SCL",
  scls_yinshi: "隐士",
  scls_yinshi_info: "锁定技，你跳过判定阶段。",
  scls_yinshi_info_doudizhu: "锁定技，你跳过判定阶段；你不能成为延时锦囊牌的目标。",
  scls_pingcai: "评才",
  scls_pingcai_info: "出牌阶段限一次，你可以展示一张手牌，根据此牌花色执行对应的宝物效果：方片，卧龙；梅花，凤雏；黑桃，水镜；红桃，玄剑。",
  scls_pingcai_info_doudizhu: "出牌阶段限一次，你可以选择执行一种宝物的效果。",
  sclc_wolong: "卧龙",
  sclc_wolong_info: "对一名角色造成1点火焰伤害。",
  sclc_wolong_info_doudizhu: "对一名角色造成1点火焰伤害。若场上有诸葛亮，改为对至多两名角色各造成1点火焰伤害。",
  sclc_fengchu: "凤雏",
  sclc_fengchu_info: "横置至多三名角色。",
  sclc_fengchu_info_doudizhu: "横置至多三名角色。若场上有庞统，改为横置至多四名角色。",
  sclc_shuijing: "水镜",
  sclc_shuijing_info: "移动场上一张装备牌。",
  sclc_shuijing_info_doudizhu: "移动场上一张防具牌，若场上有司马徽，改为一张装备牌。",
  sclc_xuanjian: "玄剑",
  sclc_xuanjian_info: "令一名角色回复1点体力并摸一张牌。",
  sclc_xuanjian_info_doudizhu: "令一名角色回复1点体力并摸一张牌。若场上有徐庶，你摸一张牌。",
  scls_chongxu: "冲虚",
  scls_chongxu_info: "出牌阶段限一次，你可以展示牌堆顶三张牌，然后获得其中一张，若此牌为：黑色，本回合你修改〖妙剑〗；红色，直到你下回合开始前，你修改〖莲华〗。",
  scls_miaojian: "妙剑",
  scls_miaojian_info: "出牌阶段限一次，你可将一张基本牌当做刺【杀】使用，或将一张非基本牌当做【无中生有】使用。若你修改〖妙剑〗，改为你可视为使用一张刺【杀】或【无中生有】。",
  scls_lianhua: "莲华",
  scls_lianhua_info: "当你成为【杀】的目标后，你摸一张牌，然后若你修改〖莲华〗，使用者需弃置一张牌，否则此【杀】对你无效。",
  scls_kuangcai: "狂才",
  scls_kuangcai_info: "出牌阶段，你使用牌无距离和次数限制；当你于出牌阶段阶段内使用牌时，你可以摸一张牌，若如此做，本回合你至多使用五张牌。",
  scls_shejian: "舌剑",
  scls_shejian_info: "弃牌阶段结束时，若你于此阶段弃置的牌花色均不相同，你可以弃置一名其他角色的一张牌。",
  scls_juezhi: "爵制",
  scls_juezhi_info: "出牌阶段，你可以弃置至少两张牌，然后展示牌堆顶等量的牌并获得其中一张。",
  scls_lingren: "凌人",
  scls_lingren_info: "出牌阶段限一次，当你使用【杀】或伤害类普通锦囊牌指定目标后，你可以猜测其中一个目标的手牌中是否包含基本牌、锦囊牌、装备牌，然后其展示所有手牌。若你猜对至少：一种，你摸两张牌；两种，此牌对其造成的伤害+1；三种，你获得〖奸雄〗和〖行殇〗直到你的下个回合开始。",
  scls_lingren_jianxiong: "奸雄",
  scls_lingren_jianxiong_info: "当你受到伤害后，你可以获得造成伤害的牌。",
  scls_lingren_xingshang: "行殇",
  scls_lingren_xingshang_info: "其他角色死亡时，你可以获得其所有牌。",
  scls_qinzheng: "勤政",
  scls_qinzheng_info: "锁定技，当你使用或打出牌时，你摸X张牌（X为你符合的条件数：本局游戏你使用或打出过的牌数可以整除3/5/8）。",
  yj_tianchuan: "田钏",
  pshuying: "狐影",
  pshuying_info: "锁定技，游戏开始时/其他角色死亡后，你从游戏外获得两/一张【刑鞭】；【刑鞭】不计入你的手牌上限，其他角色计算与你的距离+X（X为场上的【刑鞭】数）。",
  psqianjing: "潜荆",
  psqianjing_info: "当你造成或受到伤害后，你可以将手牌中的一张【刑鞭】置入一名角色的任意装备栏，若为你则摸一张牌。你可以将场上或你手牌中的一张【刑鞭】当作不计入次数限制的【杀】使用。",
  psbianchi: "鞭笞",
  psbianchi_info: "限定技，结束阶段，你可以弃置场上所有【刑鞭】，并令以此法失去牌的所有其他角色依次选择一项：1.令你操控其执行一个额外出牌阶段，此阶段至多使用两张牌；2.失去2点体力。",
  xingbian: "刑鞭",
  xingbian_info: "锁定技，你装备区每有一张【刑鞭】，你的攻击范围便+1；此牌可置入任意装备栏；你的出牌阶段开始时，若场上存在“田钏”，其为你指定一名其他角色，结束阶段若你本回合未对其使用【杀】或造成伤害，你对自己造成1点伤害。",
  xingbian_skill: "刑鞭",
  xingbian_skill_info: "锁定技，你的出牌阶段开始时，若场上存在“田钏”，其为你指定一名其他角色，结束阶段若你本回合未对其使用【杀】或造成伤害，你对自己造成1点伤害。",
  sp_gongsunzan: "SP公孙瓒",
  sp_gongsunzan_prefix: "SP",
  sp_simazhao: "SP司马昭",
  sp_simazhao_prefix: "SP",
  sp_wangyuanji: "SP王元姬",
  sp_wangyuanji_prefix: "SP",
  sp_xinxianying: "SP辛宪英",
  sp_xinxianying_prefix: "SP",
  sp_liuxie: "SP刘协",
  sp_liuxie_prefix: "SP",
  spyicong_info: "弃牌阶段结束时，你可以将任意张牌置于你的武将牌上，称为「扈」。每有一张「扈」，其他角色与你计算距离时便+1。",
  spyicong: "义从",
  sptuji: "突骑",
  sptuji_info: "准备开始时，你将所有「扈」置于弃牌堆，然后你本回合内计算与其他角色的距离时-X。若X不大于1，你摸一张牌。（X为以此法进入弃牌堆的「扈」的数量）",
  sphuangen: "皇恩",
  sphuangen_info: "一名角色使用锦囊牌指定目标时，若此牌的目标数大于1，则你可以令此牌对其中的至多X个目标无效，然后摸一张牌。（X为你的体力值）",
  sphantong: "汉统",
  sphantong_gain: "汉统",
  sphantong_info: "当你的牌因弃牌阶段的游戏规则要求而进入弃牌堆后，你可以将这些牌置于你的武将牌上，称为「诏」。一名角色的回合开始时，你可以弃置一张「诏」并获得〖护驾〗/〖激将〗/〖救援〗/〖血裔〗中的一个技能直至当前回合结束。",
  spzhaoxin: "昭心",
  spzhaoxin_info: "摸牌阶段结束时，你可以展示所有手牌，然后视为使用一张【杀】。",
  splanggu: "狼顾",
  splanggu_rewrite: "狼顾",
  splanggu_info: "当你受到有来源的伤害后，你可以进行判定（此判定结果生效前，你可以打出一张手牌代替判定牌）。然后你可以观看伤害来源的手牌并弃置其中的任意张与判定结果花色相同的牌。",
  spfuluan: "扶乱",
  spfuluan_info: "出牌阶段限一次，你可以弃置三张花色相同的牌并选择攻击范围内的一名角色。若如此做，该角色翻面且你不能使用【杀】直到回合结束。",
  spshude: "淑德",
  spshude_info: "结束阶段开始时，你可以将手牌补至体力上限。",
  spmingjian: "明鉴",
  spmingjian_info: "一名角色的回合开始时，你可以选择一项：①弃置一张牌，然后其跳过本回合的判定阶段。②将一张手牌置于其武将牌上，然后其本回合内进行判定时不触发「判定结果生效前」的时机，且其回合结束时将此牌置入弃牌堆。",
  spyinzhi: "隐智",
  spyinzhi_info: "当你受到1点伤害后，你可以亮出牌堆顶的两张牌。若其中有黑桃牌，则你可以进行至多X次「令一名角色获得伤害来源的一张手牌」的步骤。然后获得其余的牌。（X为其中黑桃牌的数量）",
  yj_caoang: "用间曹昂",
  yj_caoang_prefix: "用间",
  yjxuepin: "血拼",
  yjxuepin_info: "出牌阶段限一次，你可以选择攻击范围内的一名角色并失去1点体力。你弃置其两张牌。若这两张牌类型相同，你回复1点体力。",
  ns_chendao: "用间陈到",
  ns_chendao_prefix: "用间",
  nsjianglie: "将烈",
  nsjianglie_info: "当你使用【杀】指定目标后，你可以令其展示所有手牌，然后弃置其中一种颜色的牌。",
  ns_jiaxu: "★贾诩",
  ns_jiaxu_prefix: "★",
  nsyice: "遗策",
  nsyice_info: "锁定技，当你使用/打出/弃置的牌进入弃牌堆后，你将这些牌以任意顺序置于你的武将牌上，称为“策”。若这些“策”中有点数相同的牌，则你获得这两张牌中的所有牌，将这两张牌置于牌堆两端。若场上没有处于濒死状态的角色，则你对一名角色造成1点伤害。",
  ns_lijue: "SP李傕",
  ns_lijue_prefix: "SP",
  ns_zhangji: "SP张济",
  ns_zhangji_prefix: "SP",
  nsfeixiong: "飞熊",
  nsfeixiong_info: "出牌阶段开始时，你可以和一名其他角色拼点。赢的角色对没赢的角色造成1点伤害。",
  nscesuan: "策算",
  nscesuan_info: "锁定技，当你受到伤害时，你防止此伤害并失去1点体力上限。若你因以此法失去体力上限导致体力值减少，则你摸一张牌。",
  nslulve: "掳掠",
  nslulve_info: "出牌阶段限一次，你可以弃置X张牌并选择一名装备区内有牌的其他角色，然后对其造成1点伤害（X为其装备区内的牌数）。",
  ns_fanchou: "SP樊稠",
  ns_fanchou_prefix: "SP",
  nsyangwu: "扬武",
  nsyangwu_info: "出牌阶段限一次，你可以弃置一张♥手牌并选择一名手牌数大于你的其他角色。你观看其手牌并获得其中的X张牌（X为其与你手牌数之差的一半且向上取整）。",
  jsp_liubei: "★刘备",
  jsp_liubei_prefix: "★",
  jsprende: "仁德",
  jsprende_info: "出牌阶段，你可以将至少一张手牌交给其他角色；若你于此阶段内给出的牌首次达到两张，你可以视为使用一张基本牌。",
  ns_caoanmin: "战役篇曹安民",
  ns_caoanmin_prefix: "战役篇",
  nskuishe: "窥舍",
  nskuishe_info: "出牌阶段限一次，你可以选择一名其他角色A的一张牌，并将此牌交给不为A的一名角色。然后A可以对你使用一张【杀】。",
  sp_xiahoushi: "SP夏侯氏",
  sp_xiahoushi_prefix: "SP",
  xinfu_yanyu: "燕语",
  xinfu_yanyu_info: "一名角色的出牌阶段开始时，你可以弃置一张牌。若如此做，则该出牌阶段内限三次，当一张与你弃置的牌类别相同的其他牌进入弃牌堆后，你可令任意一名角色获得此牌。",
  xinfu_yanyu2: "燕语",
  xinfu_xiaode: "孝德",
  xinfu_xiaode_info: "其他角色死亡后，你可以声明该角色武将牌上的一个技能（主公技、觉醒技、隐匿技、使命技除外）。若如此做，你获得此技能且不能再发动〖孝德〗直到你的回合结束。",
  jsp_zhaoyun: "J.SP赵云",
  jsp_zhaoyun_prefix: "J.SP",
  chixin: "赤心",
  chixin1: "赤心",
  chixin2: "赤心",
  chixin_info: "你可以将♦牌当作【杀】或【闪】使用或打出。出牌阶段，你对在你攻击范围内且本回合内未成为过你使用的【杀】的目标的角色使用的【杀】没有次数限制。",
  suiren: "随仁",
  suiren_info: "限定技，准备阶段开始时，你可以失去技能〖义从〗，然后加1点体力上限并回复1点体力，然后令一名角色摸三张牌。",
  huangjinleishi: "黄巾雷使",
  fulu: "符箓",
  fulu_info: "当你声明使用普通【杀】后，你可以将此【杀】改为雷【杀】。",
  fuji: "助祭",
  fuji_info: "当一名角色造成雷属性伤害时，你可以令其进行判定，若结果为黑色，此伤害+1；若结果为红色，该角色获得判定牌。",
  sp_pangtong: "☆SP庞统",
  sp_pangtong_prefix: "☆SP",
  manjuan: "漫卷",
  manjuan_info: "其他角色的牌因弃置而进入弃牌堆后，你可以弃置一张花色与之不同的牌，然后获得此牌。",
  xinmanjuan: "漫卷",
  xinmanjuan_info: "锁定技，当你不因〖漫卷〗或〖醉乡〗而得到牌时，你将此牌置入弃牌堆。然后若此时处于你的回合内，则你可以从弃牌堆中选择获得一张与此牌点数相同的其他牌。",
  zuixiang: "醉乡",
  zuixiang2: "醉乡",
  zuixiang_info: "限定技。准备阶段开始时，你可以亮出牌堆顶的三张牌并置于你的武将牌上。你不能使用或打出与该些牌同类的牌，所有同类牌对你无效。之后的每个准备阶段，你须重复展示一次，直到这些牌中任意两张点数相同。然后，你获得这些牌。",
  sp_daqiao: "☆SP大乔",
  sp_daqiao_prefix: "☆SP",
  yanxiao: "言笑",
  yanxiao_info: "出牌阶段，你可以将一张♦牌置于一名角色的判定区内。判定区内有〖言笑〗牌的角色下个判定阶段开始时，其获得判定区里的所有牌。",
  anxian: "安娴",
  anxian_info: "当你使用【杀】对目标角色造成伤害时，你可以防止此伤害，令其弃置一张手牌，然后你摸一张牌；当你成为【杀】的目标后，你可以弃置一张手牌，令此【杀】对你无效，然后此【杀】的使用者摸一张牌。",
  sp_ganning: "☆SP甘宁",
  sp_ganning_prefix: "☆SP",
  yinling: "银铃",
  yinling_bg: "锦",
  yinling_info: "出牌阶段，若你的“锦”小于四张，你可以弃置一张黑色牌并指定一名其他角色。若如此做，你将其的一张牌置于你的武将牌上，称为“锦”。",
  junwei: "军威",
  junwei2: "军威",
  junwei_info: "结束阶段开始时，你可以移去三张“锦”。若如此做，你须指定一名角色并令其选择一项：1.展示一张【闪】，然后你将此【闪】交给一名其他角色。2.该角色失去1点体力，然后你将其装备区内的一张牌移出游戏。该角色的回合结束后，将以此法移出游戏的装备牌移回原处。",
  sp_xiahoudun: "☆SP夏侯惇",
  sp_xiahoudun_prefix: "☆SP",
  fenyong: "愤勇",
  fenyong2: "愤勇",
  fenyong2_bg: "勇",
  fenyong_info: "你受到伤害后，若你没有「愤勇」，你可以获得一枚「愤勇」标记；当你拥有「愤勇」标记时，防止你受到的所有伤害。",
  xuehen: "雪恨",
  xuehen_info: "每个角色的结束阶段开始时，若你有「愤勇」标记，你弃置之，然后选择一项：1.弃置当前回合角色X张牌（X为你已损失的体力值）；2.视为对一名任意角色使用一张【杀】。",
  sp_lvmeng: "☆SP吕蒙",
  sp_lvmeng_prefix: "☆SP",
  tanhu: "探虎",
  tanhu2: "探虎",
  tanhu3: "探虎",
  tanhu_info: "出牌阶段限一次，你可以与一名其他角色拼点。若你赢，你获得以下效果直到回合结束：你与该角色的距离为1，你对该角色使用的普通锦囊牌不能被【无懈可击】响应。",
  mouduan: "谋断",
  mouduan_info: "游戏开始时，你获得标记“武”并获得技能〖激昂〗和〖谦逊〗。当你失去手牌后，若手牌数不大于2，你须将你的标记变为“文”，将这两项技能改为〖英姿〗和〖克己〗。一名角色的回合开始前，你可弃一张牌将标记翻回。",
  sp_zhangfei: "☆SP张飞",
  sp_zhangfei_prefix: "☆SP",
  jie: "嫉恶",
  jie_info: "锁定技，当你使用红色【杀】造成伤害时，此伤害+1。",
  dahe: "大喝",
  dahe2: "大喝",
  dahe2_bg: "喝",
  dahe_info: "出牌阶段限一次，你可以与一名其他角色拼点。若你赢，该角色不能使用或打出不为♥花色的【闪】直到回合结束，且你可将该角色拼点的牌交给场上一名体力不多于你的角色。若你没赢，你须展示手牌并弃置其中的一张。",
  sp_liubei: "☆SP刘备",
  sp_liubei_prefix: "☆SP",
  zhaolie: "昭烈",
  zhaolie_info: "摸牌阶段摸牌时，你可以少摸一张牌并指定攻击范围内的一名角色。你亮出牌堆顶的三张牌，将其中的非基本牌和【桃】置于弃牌堆，然后该角色选择一项：1.你对其造成X点伤害，然后其获得这些基本牌；2.其弃置X张牌，然后你获得这些基本牌。（X为其中非基本牌的数量）",
  shichou: "誓仇",
  shichou2: "誓仇",
  shichou_info: "主公技，限定技，准备阶段，你可指定一名蜀势力角色并交给其两张牌。本局游戏中，当你受到伤害时，改为该角色受到等量的伤害并摸等量的牌，直至该角色第一次进入濒死状态。",
  longyufei: "龙羽飞",
  longyi: "龙裔",
  longyi_info: "你可将所有手牌当做任意基本牌使用或打出。若此牌对应的实体牌中：有锦囊牌，你摸一张牌；有装备牌，此牌不可被响应。",
  zhenjue: "阵绝",
  zhenjue_info: "一名角色的结束阶段开始时，若你没有手牌，则你可以令其选择一项：①弃置一张牌。②令你摸一张牌。",
  //用间
  yj_caocao: "用间曹操",
  yj_caocao_prefix: "用间",
  yjxiandao: "献刀",
  yjxiandao_info: "每回合限一次。当你赠予其他角色一张牌后，你令其不能使用或打出与本次赠予移动的牌A花色相同的牌直到回合结束。然后若牌A：为锦囊牌，你摸两张牌。为装备牌，你获得其一张不为A的牌。为武器牌，你对其造成1点伤害。",
  yjsancai: "散财",
  yjsancai_info: "出牌阶段限一次，你可以展示所有手牌。若这些牌的类别均相同，则你可以赠予一名其他角色一张手牌。",
  yjyibing: "义兵",
  yjyibing_info: "当你不因赠予且不因〖义兵〗的嵌套结算而于摸牌阶段外得到牌时，你可以将此次得到的所有牌当做【杀】使用（无距离限制且不计入使用次数）。",
  yj_caohong: "用间曹洪",
  yj_caohong_prefix: "用间",
  yjlifeng: "厉锋",
  yjlifeng_info: "①出牌阶段限一次。你可以获得弃牌堆里的一张装备牌。②你发动〖赠予〗可以选择手牌区里的装备牌或装备区里的牌。",
  yj_zhangfei: "用间张飞",
  yj_zhangfei_prefix: "用间",
  yjmangji: "莽击",
  yjmangji_info: "锁定技。当你装备区里的牌数或体力值变化后，若你的体力值不小于1，你弃置一张手牌并视为使用一张【杀】。",
  yongjian_ganning: "用间甘宁",
  yongjian_ganning_prefix: "用间",
  yjjielve: "劫掠",
  yjjielve_info: "你可以将两张颜色相同的牌当【趁火打劫】使用，若你以此法造成伤害，此技能于本回合失效。",
  //什么？孙鲁班？谁会做这种离谱的东西
  yj_dongzhuo: "用间董卓",
  yj_dongzhuo_prefix: "用间",
  yjtuicheng: "推诚",
  yjtuicheng_info: "你可以失去1点体力并视为使用一张【推心置腹】。",
  yjyaoling: "耀令",
  yjyaoling_info: "出牌阶段结束时，你可以减1点体力上限并选择一名其他角色A和一名角色B，你令A选择一项：1.对B使用一张【杀】；2.你弃置其一张牌。",
  yjshicha: "失察",
  yjshicha_info: "锁定技。弃牌阶段开始时，若你本回合未发动过〖推诚〗或〖耀令〗之一，你本回合的手牌上限为1。",
  yjyongquan: "拥权",
  yjyongquan_info: "主公技。结束阶段，其他群势力角色依次可以交给你一张牌。",
  yj_liru: "用间李儒",
  yj_liru_prefix: "用间",
  yjdumou: "毒谋",
  yjdumou_info: "锁定技。你的回合内，其他角色的黑色手牌均视为【毒】，你的【毒】均视为【过河拆桥】。",
  yjweiquan: "威权",
  yjweiquan_info: "限定技。出牌阶段，你可以选择至多X名角色（X为游戏轮数），然后选择一名角色A，这些角色依次将一张手牌交给A。然后若A的手牌数大于体力值，其插入执行一个仅有弃牌阶段的回合。",
  yjrenwang: "人望",
  yjrenwang_info: "出牌阶段限一次。你可以选择弃牌堆中的一张黑色基本牌，令一名角色获得之。",
  yj_xuyou: "用间许攸",
  yj_xuyou_prefix: "用间",
  yjshicai: "恃才",
  yjshicai_info: "①回合内，牌堆顶的一张牌对你可见。②出牌阶段限一次。你可以弃置一张牌，展示并获得牌堆顶的一张牌。当此牌离开你的手牌区后，重置〖恃才②〗。",
  yjchenggong: "逞功",
  yjchenggong_info: "当一名角色使用牌指定第一个目标后，若此牌目标数大于1，你可以令其摸一张牌。",
  yjzezhu: "择主",
  yjzezhu_info: "出牌阶段限一次。你可以获得主公区域内的一张牌，然后交给其一张牌。",
  yj_jiaxu: "用间贾诩",
  yj_jiaxu_prefix: "用间",
  yjzhenlve: "缜略",
  yjzhenlve_info: "锁定技。①你使用的普通锦囊牌不能被响应。②你不能成为延时锦囊牌的目标。",
  yjjianshu: "间书",
  yjjianshu_info: "出牌阶段限一次。你可以将一张手牌交给一名其他角色，令其与你选择的另一名其他角色拼点，没赢的角色失去1点体力。",
  yjyongdi: "拥嫡",
  yjyongdi_info: "限定技。准备阶段，你可以令一名男性角色加1点体力上限并回复1点体力，然后若其武将牌上有主公技且其不为主公，其获得此主公技。",
  yj_zhugedan: "用间诸葛诞",
  yj_zhugedan_prefix: "用间",
  yj_zhenji: "用间甄宓",
  yj_zhenji_prefix: "用间",
  yjluoshen: "洛神",
  yjluoshen_info: "准备阶段，你可以判定并获得判定牌，且可重复此流程直到结果的颜色不同。",
  //线下E系列 一战成名 战役篇官盗
  shen_jiaxu: "战役篇神贾诩",
  shen_jiaxu_prefix: "战役篇神",
  zybishi: "避世",
  zybishi_info: "当你成为【杀】的目标后，你可以令使用者摸一张牌，然后令此【杀】无效。",
  zyjianbing: "谏兵",
  zyjianbing_info: "当一名其他角色受到执行【杀】的效果而受到伤害时，你可以获得其一张牌。若此牌花色为♥，其回复1点体力。",
  pe_wangyun: "战役篇王允",
  pe_wangyun_prefix: "战役篇",
  zylianji: "连计",
  zylianji_info: "出牌阶段结束时，若你于此阶段使用牌的类别数达到：1，你可以令一名角色摸一张牌；2，你可以回复1点体力；3，你可以跳过本回合剩余阶段，然后令一名其他角色执行一个仅有你于此回合未执行过的阶段的回合。",
  zymoucheng: "谋逞",
  zymoucheng_info: "每回合限一次。你可以将一张黑色牌当【借刀杀人】使用。",
  pe_zhonghui: "战役篇钟会",
  pe_zhonghui_prefix: "战役篇",
  zyquanji: "权计",
  zyquanji_info: "①当你受到伤害后或使用牌对唯一目标造成伤害后，你可以摸一张牌并将一张牌置于武将上，称为“权”。②你的手牌上限+X（X为“权”数）。",
  zypaiyi: "排异",
  zypaiyi_backup: "排异",
  zypaiyi_info: "出牌阶段限一次。你可以移去一张“权”并令一名角色摸X张牌（X为“权”数，且至多为7），然后若其手牌数大于你，你对其造成1点伤害。",
  pe_mengda: "战役篇孟达",
  pe_mengda_prefix: "战役篇",
  qiuan: "求安",
  qiuan_info: "当你受到伤害时，若此伤害的渠道有对应的实体牌且你的武将牌上没有“函”，则你可以防止此伤害并将这些牌置于你的武将牌上，称为“函”。",
  liangfan: "量反",
  liangfan2: "量反",
  liangfan_info: "锁定技，准备阶段开始时，若你的武将牌上有“函”，则你获得这些牌，然后失去1点体力。当你于此回合内因使用实体牌中包含“函”的牌且执行这些牌的效果而造成伤害后，你可以获得目标角色的一张牌。",
  pe_sunchen: "战役篇孙綝",
  pe_sunchen_prefix: "战役篇",
  zyshilu: "嗜戮",
  zyshilu_info: "①一名角色死亡后，你可以将其武将牌置于你的武将牌上，称为“戮”，若杀死其的角色是你，你将一张武将牌堆里的牌置为“戮”。②回合开始时，你可以弃置至多X张牌，然后摸等量的牌（X为“戮”数）。",
  zyxiongnve: "凶虐",
  zyxiongnve_info: "①出牌阶段开始时，你可以将一张“戮”置入武将牌堆并选择一项直到回合结束：1.当你造成伤害时，此伤害+1；2.当你对其他角色造成伤害时，获得其一张牌；3.你使用牌无次数限制。②出牌阶段结束时，你可以将两张“戮”置入武将牌堆，然后当你于下回合开始前受到其他角色造成的伤害时，此伤害-1。",
  pe_wenqin: "战役篇文钦",
  pe_wenqin_prefix: "战役篇",
  gzjinfa: "矜伐",
  gzjinfa_info: "出牌阶段限一次。你可以弃置一张牌，令一名其他角色选择一项：1.令你获得其一张牌；2.交给你一张装备牌，若此牌花色为♠，其视为对你使用一张【杀】。",
  zyshangyi: "尚义",
  zyshangyi_info: "出牌阶段限一次。你可以令一名其他角色观看你的手牌，然后你观看其手牌并可以弃置其中一张牌。",
  zymingshi: "名士",
  zymingshi_info: "锁定技。若你有空置的防具栏，属性【杀】对你无效。",
  gzsuishi: "随势",
  gzsuishi2: "随势",
  gzsuishi_info: "锁定技，其他角色进入濒死状态时，若伤害来源与你势力相同，你摸一张牌；其他角色死亡时，若其与你势力相同，你失去1点体力。",
  //线下S系列
  ps_guanyu: "★关羽",
  ps_guanyu_prefix: "★",
  pszhonghun: "忠魂",
  pszhonghun_info: "当你使用或打出红色牌时，你可以亮出牌堆顶的一张牌。若此牌为红色，你获得之。",
  ps2070_guojia: "★郭嘉",
  ps2070_guojia_prefix: "★",
  psquanmou: "全谋",
  psquanmou_info: "当其他角色使用锦囊牌结算结束后，若你是此牌目标，你可以弃置一张与此牌颜色相同的手牌并获得之。",
  ps1059_guojia: "★郭嘉",
  ps1059_guojia_prefix: "★",
  psqizuo: "奇佐",
  psqizuo_info: "当你攻击范围内的角色造成或受到伤害时，你可以弃置一张牌并判定，若此牌颜色与结果相同，你可以令此伤害+1或-1。",
  ps2063_zhaoyun: "★赵云",
  ps2063_zhaoyun_prefix: "★",
  psqijin: "七进",
  psqijin_info: "摸牌阶段，你可以改为亮出牌堆顶的七张牌，并获得其中一种颜色的所有牌。",
  psqichu: "七出",
  psqichu_info: "每回合限一次。当你于回合外需要使用或打出一张基本牌时，你可以观看牌堆顶的两张牌。若其中有此牌，你可以使用或打出之。",
  pslongxin: "龙心",
  pslongxin_info: "判定阶段开始时，你可以弃置一张装备牌，然后弃置你判定区里的一张牌。",
  ps2080_zhouyu: "★周瑜",
  ps2080_zhouyu_prefix: "★",
  psshiyin: "识音",
  psshiyin_info: "①游戏开始时，你可以将一张手牌置于武将牌上，称为“杂音”牌。②出牌阶段开始时，你可以用一张手牌替换“杂音”牌。",
  psquwu: "曲误",
  psquwu_info: "锁定技。你不能使用或打出与“杂音”牌花色相同的牌，且这些牌对你无效。",
  psliaozou: "聊奏",
  psliaozou_info: "出牌阶段，你可以展示所有手牌，若其中没有与“杂音”牌花色相同的牌，你摸一张牌。",
  ps1062_zhouyu: "★周瑜",
  ps1062_zhouyu_prefix: "★",
  psoldshiyin: "识音",
  psoldshiyin_info: "当你于回合内得到牌后，你可以展示之，然后根据你展示的牌包含的花色数令你本回合使用的下一张牌获得对应效果：不小于1，不能被响应；不小于2，造成的伤害+1；不小于3，使用时摸一张牌。",
  ps_caozhi: "★曹植",
  ps_caozhi_prefix: "★",
  psliushang: "流殇",
  psliushang_info: "锁定技。①摸牌阶段，你改为摸X+1张牌，然后依次将一张手牌置于所有其他角色的武将牌上，称为“流殇”牌（X为场上角色数且至少为3）。②其他角色的准备阶段，其选择一项：1.获得其“流殇”牌，且当其于本回合对你造成伤害时，防止此伤害；2.将其“流殇”牌置入弃牌堆。",
  psqibu: "七步",
  psqibu_info: "限定技。当你进入濒死状态时，你可以亮出牌堆顶的七张牌，回复等同于其中♥牌数的体力，并获得所有♣牌。",
  ps_jin_simayi: "★司马懿",
  ps_jin_simayi_prefix: "★",
  psquanyi: "权奕",
  psquanyi_info: "①出牌阶段限一次。你可以与一名角色拼点，赢的角色根据所有拼点牌的花色执行以下效果：♥，其获得没赢的角色区域里的一张牌；♦其对没赢的角色造成1点伤害；♠，其失去1点体力；♣，其弃置两张牌。②当你拼点时，你可以选择牌堆顶的牌作为拼点牌。",
  ps2067_zhaoyun: "武将传赵云",
  ps2067_zhaoyun_prefix: "武将传",
  pshuiqiang: "回枪",
  pshuiqiang_info: "当你使用的【杀】被【闪】抵消后，你可以对其使用一张【杀】。",
  pshuntu: "魂突",
  pshuntu_info: "出牌阶段限一次。当你使用【杀】对目标角色造成伤害后，你可以对其使用一张【杀】。",
  ps_caopi: "★曹丕",
  ps_caopi_prefix: "★",
  psjianwei: "僭位",
  psjianwei_info: "限定技。回合开始时，你可以失去1点体力，然后与一名其他角色交换区域里的所有牌。",
  ps2068_simayi: "★司马懿",
  ps2068_simayi_prefix: "★",
  pszhonghu: "冢虎",
  pszhonghu_info: "当一名角色于你的回合外死亡后，你可以结束此回合，然后令所有角色于其回合开始前跳过此回合直到你的回合开始前。",
  ps_simayi: "★司马懿",
  ps_simayi_prefix: "★",
  pshuxiao: "虎啸",
  pshuxiao_info: "回合开始时，你可以判定。若结果为基本牌或普通锦囊牌，你于本回合内获得如下效果：你可以将与结果点数或花色相同的手牌当与判定牌牌名和属性相同的牌使用。",
  ps_zhugeliang: "★诸葛亮",
  ps_zhugeliang_prefix: "★",
  psguanxing: "观星",
  psguanxing_info: "准备阶段，你可以观看牌堆顶的五张牌，并将其以任意顺序置于牌堆顶或牌堆底。",
  pslongyin: "龙吟",
  pslongyin_info: "每回合限一次。你可以将任意张点数和为13的牌当做任意一张基本牌或普通锦囊牌使用或打出。",
  ps2066_zhugeliang: "武将传诸葛亮",
  ps2066_zhugeliang_prefix: "武将传",
  pszhiji: "智激",
  pszhiji_info: "出牌阶段限一次。你可以弃置两张手牌并选择两名势力不同的角色，视为这两名角色依次视为对对方使用一张【杀】。",
  psjiefeng: "借风",
  psjiefeng_info: "出牌阶段，你可以弃置两张手牌，然后亮出牌堆顶五张牌。若其中有至少两张红色牌，你视为使用一张【万箭齐发】。",
  ps_machao: "★马超",
  ps_machao_prefix: "★",
  psweihou: "威侯",
  psweihou_info: "当你判定前，你可以亮出牌堆顶的两张牌，选择其中一张作为你的本次判定结果，然后将另一张置入弃牌堆。",
  ps_lvbu: "★吕布",
  ps_lvbu_prefix: "★",
  pssheji: "射戟",
  pssheji_info: "出牌阶段限一次。你可以将所有手牌当一张无距离限制的【杀】使用，然后当此【杀】对目标角色造成伤害后，你获得其装备区里的所有武器牌和坐骑牌。",
  ps_jiaxu: "★贾诩",
  ps_jiaxu_prefix: "★",
  psqupo: "驱魄",
  psqupo_info: "一名角色A的回合开始时，你可以将一张牌交给另一名其他角色B。若此牌为：黑色，当A使用【杀】指定不为B的角色为目标时，A失去1点体力；红色，当B于本回合下一次受到伤害时，B失去1点体力。",
  psbaoquan: "保全",
  psbaoquan_info: "当你受到伤害时，你可以弃置一张锦囊牌并防止此伤害。",
  //S特
  ps_shen_machao: "S特神马超",
  ps_shen_machao_prefix: "S特神",
  psshouli: "狩骊",
  psshouli_info: "锁定技。①游戏开始时，所有角色依次选择一项：1.使用一张坐骑牌，然后摸一张牌；2.随机从游戏外的八张坐骑牌指示物中使用一张。②你可以将场上一张进攻坐骑当【杀】，防御坐骑当【闪】使用或打出，若此坐骑牌的拥有者不为你，则其非锁定技于本回合内失效。且当你或其于本回合内受到伤害时，此伤害+1且改为雷属性。",
  pshengwu: "横骛",
  pshengwu_info: "当你使用或打出牌时，若场上有该花色的装备牌，你可以弃置任意张该花色的手牌，然后摸X张牌（X为你弃置的牌数与场上与此牌花色相同的装备牌数之和）。",
  //线下K系列木盒
  pk_sp_duyu: "K系列杜预",
  pk_sp_duyu_prefix: "K系列",
  pkwuku: "武库",
  pkwuku_info: "锁定技。当有角色使用装备牌时，若你的“武库”数小于3，则你获得1枚“武库”。",
  pksanchen: "三陈",
  pksanchen_info: `觉醒技。结束阶段，若你的“武库”数大于2，则你加1点体力上限并回复1点体力，然后获得${get.poptip("pkmiewu")}。`,
  pkmiewu: "灭吴",
  pkmiewu_info: "每回合限一次。你可移去1枚“武库”，视为使用或打出任意一张基本牌或普通锦囊牌，然后摸一张牌。",
  //天书乱斗虚拟偶像
  vtb_xiaosha: "小杀",
  vtbguisha: "瑰杀",
  vtbguisha_info: "当其他角色使用【杀】时，你可以弃置一张牌令此【杀】不计入次数，且此【杀】对目标角色造成的伤害+1。",
  vtbshuli: "淑丽",
  vtbshuli_info: "每回合限两次。当其他角色使用【杀】造成伤害后，你可以与其各摸一张牌。",
  vtb_xiaoshan: "小闪",
  vtbshanwu: "闪舞",
  vtbshanwu_info: "当其他角色成为【杀】的第一个目标时，你可以弃置一张【闪】，然后取消此【杀】的所有目标。",
  vtbxianli: "娴丽",
  vtbxianli_info: "每回合限两次。当你失去牌后，若其中有【闪】，你可以获得当前回合角色的一张牌。",
  vtb_xiaotao: "小桃",
  vtbtaoyan: "桃宴",
  vtbtaoyan_info: "回合开始时，你可以令至多两名其他角色摸一张牌并于游戏外获得一张【桃】指示物（共六张）。",
  vtbyanli: "妍丽",
  vtbyanli_info: "每轮限一次。一名角色于你的回合外进入濒死状态时，你可以令其回复至1点体力，然后其摸一张牌。",
  vtb_xiaole: "小乐",
  vtbleyu: "乐虞",
  vtbleyu_info: "一名角色的回合开始时，你可以弃置三张牌令其判定。若结果不为♥，其跳过本回合的出牌阶段。",
  vtbyuanli: "媛丽",
  vtbyuanli_info: "一名角色跳过出牌阶段后，你可以与一名其他角色各摸一张牌。",
  vtb_xiaojiu: "小酒",
  vtbmeiniang: "美酿",
  vtbmeiniang_info: "其他角色的出牌阶段开始时，你可以令其视为使用一张无次数限制且不计入次数的【酒】。",
  vtbyaoli: "媱丽",
  vtbyaoli_info: "其他角色于其出牌阶段内使用【酒】后，你可以令其于本回合内使用的下一张【杀】不能被响应且可以额外指定一个目标。",
  old_machao: "J.SP马超",
  old_machao_prefix: "J.SP",
  jsp_caoren: "☆SP曹仁",
  jsp_caoren_prefix: "☆SP",
  jsp_ganfuren: "SP甘夫人",
  jsp_ganfuren_prefix: "SP",
  zhangliang: "SP张梁",
  zhangliang_prefix: "SP",
  ol_xinxianying: "将辛宪英",
  ol_xinxianying_prefix: "将",
  ol_liuyu: "将刘虞",
  ol_liuyu_prefix: "将",
  ol_zhangrang: "将张让",
  ol_zhangrang_prefix: "将",
  jiangqing: "战役篇蒋钦",
  jiangqing_prefix: "战役篇",
  tianfeng: "战役篇田丰",
  tianfeng_prefix: "战役篇",
  jiling: "战役篇纪灵",
  jiling_prefix: "战役篇",
  kongrong: "战役篇孔融",
  kongrong_prefix: "战役篇",
  mateng: "战役篇马腾",
  mateng_prefix: "战役篇",
  drag_pangde: "龙庞德",
  drag_pangde_prefix: "龙",
  dragtaiguan: "抬棺",
  dragtaiguan_info: "出牌阶段限X次，你可以弃置一张牌并令攻击范围内的一名其他角色弃置一张牌，若其弃置的不为【杀】且你的体力不大于其，你视为对其使用一张【决斗】（X为你损失的体力且至少为1）",
  drag_guanyu: "龙关羽",
  drag_guanyu_prefix: "龙",
  drag_caoren: "龙曹仁",
  drag_caoren_prefix: "龙",
  drag_lvchang: "吕常",
  dragchaojue: "超绝",
  dragchaojue_info: "准备阶段，你可以摸一张牌并展示一张手牌，然后令所有其他角色本回合不能使用或打出此花色的牌，然后这些角色依次选择一项：①正面朝上交给你一张与此牌花色相同的手牌；②本回合非锁定技失效。",
  dragjunshen: "军神",
  dragjunshen_info: "①你可以将一张红色牌当作【杀】使用或打出。②当你使用〖军神①〗转化的【杀】造成伤害时，你可以令受伤角色弃置装备区的所有牌，然后你选择X次（X为其因此弃置的装备牌数且至少为1）：1.弃置其一张手牌，2.令此伤害+1。③你使用方片【杀】无距离限制，使用红桃【杀】可以额外选择一个目标。",
  draglizhong: "厉众",
  draglizhong_info: "结束阶段，你可以以任意顺序选择执行任意项：①将任意张装备牌置入任意名角色的装备区；②令你或任意名装备区里有牌的角色各摸一张牌。然后本次成为〖厉众②〗的角色于本轮手牌上限+2且可以将装备区的牌当作【无懈可击】使用。",
  dragjuesui: "玦碎",
  dragjuesui_info: "每名角色限一次，一名角色进入濒死状态时，你可以令其选择是否将体力值回复至1点并废除装备栏。若其选择是，则其本局游戏获得以下效果：你可以将一张黑色非基本牌当作无次数限制的【杀】使用或打出。",
  dragjuwu: "拒武",
  dragjuwu_info: "锁定技，攻击范围内至少包含三名角色的角色使用的无属性【杀】对你无效。",
  dragshouxiang: "守襄",
  dragshouxiang_info: "摸牌阶段，你可以额外摸X张牌并选择至多X名角色。此阶段结束时，你依次交给这些角色一张手牌或将一张【杀】当【桃】对其使用。（X为攻击范围内包含你的角色且至多为3）",
  yj_zhouji: "周姬",
  //肘击（bushi
  psyanmou: "炎谋",
  psyanmou_info: "①其他角色的【火攻】或火【杀】因弃置或判定进入弃牌堆后，你可以获得之。②当你得到牌后，你展示得到的牌，然后你使用其中的一张【火攻】或火【杀】。",
  pszhanyan: "绽焰",
  pszhanyan_info: "出牌阶段限一次，你可以令你攻击范围内的所有角色依次选择一项：①受到你对其造成的1点火属性伤害；②将手牌或弃牌堆中的一张【火攻】或火【杀】置于牌堆顶。然后你摸X张牌（X为本次选择次数较小的选项的被选择次数）。",
  psyuhuo: "驭火",
  psyuhuo_info: "锁定技。①防止你受到的火属性伤害。②你的【火攻】和火【杀】不计入手牌上限。",
  yj_ehuan: "鄂焕",
  psdiwan: "敌万",
  psdiwan_info: "每回合限一次，当你使用【杀】指定第一个目标后，你可以摸X张牌（X为此牌指定的目标数）。",
  pssuiluan: "随乱",
  pssuiluan_info: "群势力技。你使用【杀】可以额外指定两个目标，若如此做，此牌结算完毕后，所有目标角色可依次对你使用一张【杀】，你以此法受到伤害后，将势力变更至蜀。",
  psconghan: "从汉",
  psconghan_info: "蜀势力技。一号位造成伤害后，你可以对受伤角色使用一张【杀】。",
  yj_zhonghui: "PE钟会",
  yj_zhonghui_prefix: "PE",
  psmouchuan: "谋川",
  psmouchuan_info: `每轮开始时，你可以摸两张牌并交给一名其他角色一张牌，然后你与其依次展示一张手牌，若这两张牌颜色相同/不同，你获得${get.poptip("psdaohe")}/${get.poptip("pszhiyi")}直到本轮结束。`,
  pszizhong: "自重",
  pszizhong_info: "锁定技，当你使用或打出一张你本轮未使用过的非装备牌时，你摸X-2张牌；你的手牌上限+X（X为你的技能数）。",
  psjizun: "极尊",
  psjizun_info: "觉醒技。当你脱离濒死状态时，若你没有〖清算〗，你获得之；否则你将体力回复至上限。",
  psqingsuan: "清算",
  psqingsuan_info: "主公技，锁定技。你对本局游戏对你造成过伤害且势力与你不同的角色使用牌无距离和次数限制。",
  psdaohe: "道合",
  psdaohe_info: "出牌阶段限一次，你可以令一名其他角色交给你至少一张手牌，然后你令其回复1点体力。",
  pszhiyi: "志异",
  pszhiyi_info: "出牌阶段限一次，你可以令一名角色摸一张牌并对其造成1点伤害。",
  jx_shen_caoren: "荆神曹仁",
  jx_shen_caoren_prefix: "荆神",
  jx_shen_liubiao: "荆神刘表",
  jx_shen_liubiao_prefix: "荆神",
  jingxiangshengshi: "荆襄盛世",
  jingxiangshengshi_info: "出牌阶段，对X名其他角色使用，亮出牌堆顶存活角色数张牌，令这些角色依次获得其中一张，然后你获得剩余的牌（X为全场势力数）。",
  jxjushou: "据守",
  jxjushou_info: `结束阶段，你可以翻面并摸X张牌（X为场上存活人数），然后你可令全场角色翻面并各摸三张牌，若如此做你弃置场上所有装备牌，失去〖据守〗并获得${get.poptip("jxtuwei")}。`,
  jxtuwei: "突围",
  jxtuwei_info: "出牌阶段，你可将弃牌堆的一张装备牌置入一名角色对应的装备栏内，然后若其不为你，你可以令其摸一张牌或对其造成1点伤害。（每名角色限一次）",
  jxxiongju: "雄踞",
  jxxiongju_info: `锁定技，游戏开始时，你从游戏外获得两张${get.poptip("jingxiangshengshi")}，然后增加X点体力上限并恢复等量体力；你的手牌上限和起始手牌+X（X为场上势力数）。`,
  jxfujing: "富荆",
  jxfujing_info: `锁定技，摸牌阶段开始前，你跳过此阶段并视为使用一张${get.poptip("jingxiangshengshi")}；以此法获得牌的其他角色本轮首次对你使用牌时需弃置一张牌。`,
  jxyongrong: "雍容",
  jxyongrong_info: "每回合限一次，你造成/受到伤害时，若受伤角色/伤害来源的手牌数小于你，你可以交给其一张牌令此伤害+1/-1。",
  jx_zhouyu: "荆周瑜",
  jx_zhouyu_prefix: "荆",
  jx_guanyu: "荆关羽",
  jx_guanyu_prefix: "荆",
  jxxiongzi: "雄姿",
  jxxiongzi_info: "锁定技，摸牌阶段你额外摸X张牌，你的手牌上限+X（X为你的体力值）。",
  jxzhanyan: "绽焰",
  jxzhanyan_info: "出牌阶段限一次，你可以令一名其他角色猜测你的红色手牌数量，然后你展示手牌并将所有红色牌交给其，对其造成X点火焰伤害（X为其猜测值与红色手牌数之差，至多为3）。",
  jxwusheng: "武圣",
  jxwusheng_info: "你可以将一张红色牌当【杀】或【酒】使用或打出；你使用♦【杀】无距离限制。",
  jd_simayan: "司马炎",
  jdfengtu: "封土",
  jdfengtu_info: "一名其他角色死亡后，你可以选择一名未因此法失去过体力上限的角色，令其减1点体力上限，然后其获得死亡角色座次数的额定回合。",
  jdjuqi: "举棋",
  jdjuqi_info: "转换技。阳：准备阶段，你摸三张牌；其他角色的准备阶段，其可以展示并交给你一张黑色手牌。阴：准备阶段，你令你本回合使用牌无次数限制且造成的伤害+1；其他角色的准备阶段，其可以展示并交给你一张红色手牌。",
  jdtaishi: "泰始",
  jdtaishi_info: "主公技，限定技。一名角色的回合开始前，你可以令场上所有隐匿角色依次登场。",
  jdtaishi_info_guozhan: "主公技，限定技。一名角色的回合开始前，你可以令场上所有存在未明置武将牌的角色依次明置所有武将牌。",
  jd_sb_sunquan: "九鼎孙权",
  jd_sb_sunquan_prefix: "九鼎",
  jdsbzhiheng: "制衡",
  jdsbzhiheng_info: "出牌阶段限一次。你可以弃置任意张牌并摸等量的牌，若你以此法弃置的牌包括你装备区的牌，则你多摸一张牌。",
  jdsbtongye: "统业",
  jdsbtongye_info: `锁定技，若牌堆未洗过牌，你视为拥有${get.poptip("sbyingzi")}和${get.poptip("olguzheng")}。`,
  jdsbjiuyuan: "救援",
  jdsbjiuyuan_info: "主公技，出牌阶段限一次，你可以获得一名其他吴势力角色装备区的所有牌，然后你回复1点体力。",
  jd_sb_xiaoqiao: "九鼎小乔",
  jd_sb_xiaoqiao_prefix: "九鼎",
  jdsbtianxiang: "天香",
  jdsbtianxiang_info: "当你受到伤害时，你可以展示两张手牌，令一名其他角色选择并获得其中一张，若其以此法获得♥牌，你将此伤害转移给其；否则其本回合不能使用同类型的手牌。",
  jdsbhongyan: "红颜",
  jdsbhongyan_info: "锁定技。①你的♠牌和♠判定牌的花色视为♥。②当你失去牌后，若你本回合未以此法获得过牌，你展示这些牌中背面向上移动的牌，若这些牌中有花色为♥的牌，你摸一张牌。",
  jd_sb_guanyu: "九鼎关羽",
  jd_sb_guanyu_prefix: "九鼎",
  jdsbwusheng: "武圣",
  jdsbwusheng_info: "你可以将一张手牌当任意【杀】使用或打出。出牌阶段开始时，你可以令一名其他角色展示所有手牌，本阶段你对其使用的前X张【杀】无距离和次数限制且结算后你摸一张牌（X为其以此法展示的红色手牌数）。",
  jdsbyijue: "义绝",
  jdsbyijue_info: "锁定技，准备阶段，所有其他角色依次选择是否交给你一张牌，以此法交给你牌的角色本回合首次受到你的【杀】的造成的伤害时，你防止之。",
  jd_sb_jiangwei: "九鼎姜维",
  jd_sb_jiangwei_prefix: "九鼎",
  jdsbtiaoxin: "挑衅",
  jdsbtiaoxin_info: "出牌阶段限一次。你可以选择至多X名角色（X为你的体力值），令这些角色选择一项：1.对你使用一张无距离限制的【杀】，此【杀】结算结束后，若此【杀】未造成伤害，你获得其一张牌；2.你获得其一张牌。",
  jdsbzhiji: "志继",
  jdsbzhiji_info: "觉醒技，当你进入濒死状态时，你回复体力至2点，减少1点体力上限，获得〖北伐〗，然后若你的手牌数为全场最少，你摸两张牌。",
  jdsbbeifa: "北伐",
  jdsbbeifa_info: "出牌阶段，你可以弃置任意张手牌，然后令一名其他角色展示等量张手牌。若如此做，你可以将其展示牌中与你弃置牌中牌名相同的牌当无次数限制的【杀】使用，然后你可以重复此流程。",
  jd_sb_daqiao: "九鼎大乔",
  jd_sb_daqiao_prefix: "九鼎",
  jdsbguose: "国色",
  jdsbguose_info: "出牌阶段限一次，你可以将一张♦牌当【乐不思蜀】使用或移动场上一张【乐不思蜀】。",
  jdsbliuli: "流离",
  jdsbliuli_info: "当你成为【杀】的目标时，你可以弃置一张牌并选择你攻击范围内的一名不为此【杀】使用者的角色，将此【杀】转移给该角色。然后其获得“流离”标记，且移去场上所有其他的“流离”。有“流离”的角色回合开始时，其移去其“流离”并执行一个额外的出牌阶段。",
  jd_sb_menghuo: "九鼎孟获",
  jd_sb_menghuo_prefix: "九鼎",
  jdsbhuoshou: "祸首",
  jdsbhuoshou_info: "锁定技。①【南蛮入侵】对你无效。②其他角色使用【南蛮入侵】指定目标后，你代替其成为此牌的伤害来源。③出牌阶段结束时，你弃置所有手牌视为使用一张【南蛮入侵】。",
  jdsbzaiqi: "再起",
  jdsbzaiqi_info: "弃牌阶段结束时，你可以令至多X名角色各选择一项（X为你本回合弃置的牌数）：1.你摸一张牌；2.弃置一张牌，然后你回复1点体力。",
  jd_sb_yuanshao: "九鼎袁绍",
  jd_sb_yuanshao_prefix: "九鼎",
  jdsbluanji: "乱击",
  jdsbluanji_info: "出牌阶段限一次，你可以将两张手牌当【万箭齐发】使用。手牌数大于你的其他角色打出【闪】响应你的【万箭齐发】后，若你的手牌数小于体力值，你摸一张牌。",
  jdsbxueyi: "血裔",
  jdsbxueyi_info: "主公技，锁定技，你的手牌上限+2X（X为其他群势力角色数）。当其他群势力角色使用或打出牌响应你使用的牌结算结束后，你令其此阶段不能使用或打出手牌。",
  jd_sb_yujin: "九鼎于禁",
  jd_sb_yujin_prefix: "九鼎",
  jdsbjieyue: "节钺",
  jdsbjieyue_info: "结束阶段，你可以令一名其他角色摸两张牌并获得1点护甲，然后其须交给你两张牌。",
  jd_sb_sunshangxiang: "九鼎孙尚香",
  jd_sb_sunshangxiang_prefix: "九鼎",
  jdsbjieyin: "结姻",
  jdsbjieyin_info: "锁定技，出牌阶段开始时，你令一名手牌数不大于你的角色选择一项：1.交给你X张手牌（X=min(2,其手牌数)且至少为1），然后获得1点护甲；2.你回复1点体力并获得所有“妆”，然后减少1点体力上限，变更势力为吴。",
  jdsbliangzhu: "良助",
  jdsbliangzhu_info: "蜀势力技。出牌阶段限一次，你可以将一名其他角色装备区的一张牌置于你的武将牌上，称为“妆”，然后令一名其他角色回复1点体力。",
  jd_sb_liubei: "九鼎刘备",
  jd_sb_liubei_prefix: "九鼎",
  jdsbzhangwu: "章武",
  jdsbzhangwu_info: "限定技，出牌阶段，你可以移去任意的“仁望”标记并摸等量张牌，若如此做，本回合你使用【杀】无距离限制且〖仁德〗失效直到你进入濒死状态。",
  jd_sb_fazheng: "九鼎法正",
  jd_sb_fazheng_prefix: "九鼎",
  jdsbxuanhuo: "眩惑",
  jdsbxuanhuo_info: "①出牌阶段限一次。你可以将一张牌交给一名没有“眩”标记的其他角色，然后令其获得“眩”标记。②当有“眩”的其他角色于摸牌阶段外得到牌后，若你以此法于其本次获得“眩”的期间内得到其的牌数小于5，你获得其一张手牌。",
  jdsbenyuan: "恩怨",
  jdsbenyuan_info: "锁定技。准备阶段，若场上存在有“眩”的角色，你移去该角色的“眩”，且若其手牌数小于你，你交给其两张牌；否则其失去1点体力，你回复1点体力。",
  jd_sb_zhangfei: "九鼎张飞",
  jd_sb_zhangfei_prefix: "九鼎",
  jdsbpaoxiao: "咆哮",
  jdsbpaoxiao_info: "锁定技。①你使用【杀】无次数限制。②若你的装备区内有武器牌，则你使用【杀】无距离限制。③当你于出牌阶段内使用第二张及以后【杀】时，你获得如下效果：{此【杀】不可被响应且伤害值基数+1；此【杀】指定目标后，目标角色的非锁定技于本回合内失效；此【杀】造成伤害后，若目标角色存活，则你失去1点体力且其弃置你一张手牌。}",
  jd_jin_simashi: "九鼎司马师",
  jd_jin_simashi_prefix: "九鼎",
  jdtairan: "泰然",
  jdtairan_info: "锁定技，结束阶段，你将体力回复至体力上限，并将手牌摸至体力上限。然后你的下一个出牌阶段开始时，你失去上一次以此法回复的体力值的体力，弃置X张手牌（X为你上次以此法获得的牌数）。",
  jd_sb_sp_zhugeliang: "九鼎诸葛亮",
  jd_sb_sp_zhugeliang_prefix: "九鼎",
  jdhuoji: "火计",
  jdhuoji_info: "使命技。①使命：出牌阶段限一次，你可以对一名其他角色造成1点火焰伤害，然后你对所有与其势力相同的不为其的其他角色各造成1点火焰伤害。②成功：准备阶段，若你本局游戏已对其他角色造成的火焰伤害不小于本局游戏总角色数，则你失去〖火计〗和〖看破〗，然后获得〖观星〗和〖空城〗。③失败：使命成功前进入濒死状态。",
  jdkanpo: "看破",
  jdkanpo_info: "①游戏开始时，你摸三张牌，然后可以将至多三张牌扣置于武将牌上。②一名角色使用牌时，你可以移去武将牌上的一张与此牌牌名相同的“看破”牌，然后取消之并摸一张牌。",
  jdguanxing: "观星",
  jdguanxing_info: "①准备阶段，你将所有“星”置入弃牌堆，然后将牌堆顶的X张牌置于你的武将牌上，称为“星”（X为7-此前发动〖观星①〗次数的2倍，且X至少为0）。②出牌阶段，你可以将任意张“星”置于牌堆顶。③你可以如手牌般使用或打出“星”。",
  jd_sb_caocao: "九鼎曹操",
  jd_sb_caocao_prefix: "九鼎",
  jdjianxiong: "奸雄",
  jdjianxiong_info: "①游戏开始时，你可获得至多2枚“治世”标记。②当你受到伤害后，你可获得伤害牌，摸2-X张牌（X为“治世”数），然后你可以移去1枚“治世”。",
  jd_jin_simazhao: "九鼎司马昭",
  jd_jin_simazhao_prefix: "九鼎",
  jd_jin_xiahouhui: "九鼎夏侯徽",
  jd_jin_xiahouhui_prefix: "九鼎",
  jdbaoqie: "宝箧",
  jdbaoqie_info: "隐匿技，锁定技。你登场后，从牌堆或弃牌堆中获得一张不为赠物的防具牌，然后你可以使用此牌。",
  jd_hanlong: "九鼎韩龙",
  jd_hanlong_prefix: "九鼎",
  jdcibei: "刺北",
  jdcibei_info: "①当一名角色使用【杀】造成伤害且此牌对应的实体牌进入弃牌堆后，你可以将一张不为【杀】的“刺”置入弃牌堆，并将这些牌置入“刺”，然后弃置一名角色区域里的一张牌。②一名角色的回合结束时，若你的“刺”均为【杀】，你获得所有“刺”，然后你本局游戏使用【杀】无距离和次数限制。③一名角色的回合结束时，你获得弃牌堆中你本回合弃置的所有【杀】。",
  jd_sb_pangtong: "九鼎庞统",
  jd_sb_pangtong_prefix: "九鼎",
  jdlianhuan: "连环",
  jdlianhuan_info: "①你可以将♣手牌当作【铁索连环】使用或重铸。②当你使用【铁索连环】指定一名未横置角色为目标后，你可以弃置其一张牌。",
  jd_sb_ganning: "九鼎甘宁",
  jd_sb_ganning_prefix: "九鼎",
  jdqixi: "奇袭",
  jdqixi_info: "出牌阶段限一次，你可以选择一张手牌，然后令一名其他角色声明一个此次未以此法声明过的花色，若此牌的花色与其声明的花色：不同，你令其重复此流程；相同，你展示并弃置选择的牌，然后弃置其区域内的X-1张牌（X为其于本次〖奇袭〗中选择花色的次数）。",
  jdfenwei: "奋威",
  jdfenwei_info: `限定技，当一名角色使用普通锦囊牌指定第一个目标后，若此牌目标数不小于2，则你可以令此牌对任意名角色无效，若你选择了自己，则你可以于本回合结束时发动${get.poptip("jdqixi")}。`,
  jd_sb_zhaoyun: "九鼎赵云",
  jd_sb_zhaoyun_prefix: "九鼎",
  jdlongdan: "龙胆",
  jdlongdan_info: "你可以将一张【杀】/【闪】当作【闪】/【杀】使用或打出。每阶段你首次以此法使用或打出牌结算结束后，摸两张牌。",
  jdlongdanx: "龙胆·改",
  jdlongdanx_info: "你可以将一张基本牌当作任意基本牌使用或打出。每阶段你首次以此法使用或打出牌结算结束后，摸两张牌。",
  jdjizhu: "积著",
  jdjizhu_info: `准备阶段开始时，你可以与一名其他角色“${get.poptip("rule_xieli")}”。其的下个结束阶段开始时，若你与其“协力”成功，则你修改〖龙胆〗直到你的下个回合结束后。`,
  jd_sb_huangyueying: "九鼎黄月英",
  jd_sb_huangyueying_prefix: "九鼎",
  jdjizhi: "集智",
  jdjizhi_info: "锁定技，当你使用一张普通锦囊牌时，你摸一张牌，且你本回合手牌上限+1。",
  jdqicai: "奇才",
  jdqicai_info: "①你使用锦囊牌无距离限制。②出牌阶段限一次，你可以展示一张装备牌并交给一名其他角色，然后其选择一项：1.展示并交给你两张非装备牌；2.你从牌堆或弃牌堆随机获得两张普通锦囊牌。",
  jd_sp_yangwan: "九鼎杨婉",
  jd_sp_yangwan_prefix: "九鼎",
  jdmingxuan: "瞑昡",
  jdmingxuan_info: "锁定技。出牌阶段开始时，你须选择至多X张花色各不相同的手牌（X为未选择过选项一的角色）展示之。然后从你的下家开始，未选择过选项一的角色依次选择获得其中的一张牌，然后选择一项：⒈对你使用一张【杀】。⒉交给你一张牌，然后你摸一张牌。",
  jd_ol_huaxin: "九鼎华歆",
  jd_ol_huaxin_prefix: "九鼎",
  jdcaozhao: "草诏",
  jdcaozhao_info: "每轮限一次，体力值小于等于你的角色的出牌阶段开始时，你可以展示其一张手牌，选择一个你本局游戏未以此法声明过的基本牌或普通锦囊牌，然后令其选择一项：1.将此牌当作你声明的牌使用；2.失去1点体力。",
  jd_jin_wangyuanji: "九鼎王元姬",
  jd_jin_wangyuanji_prefix: "九鼎",
  jdshiren: "识人",
  jdshiren_info: "隐匿技，当你于其他角色的回合内登场时，若其有手牌，则你可对其发动〖宴戏〗。",
  jdyanxi: "宴戏",
  jdyanxi_info: "出牌阶段，你可以选择一名有手牌的其他角色，其将此牌给场上其他角色展示，然后你将此牌与牌堆顶的两张牌混合后展示，然后你选择其中一张。若你以此法选择的是该角色展示的手牌，则你获得这三张牌；否则你获得选择的牌，然后将剩余的牌以任意顺序置于牌堆顶。",
  jd_sb_xuhuang: "九鼎徐晃",
  jd_sb_xuhuang_prefix: "九鼎",
  jdsbduanliang: "断粮",
  jdsbduanliang_info: "出牌阶段限一次。你可以与一名其他角色进行谋弈。若你赢，且你选择的选项为：“围城断粮”，你摸一张牌，然后若其判定区没有【兵粮寸断】，你可以将一张黑色非锦囊牌当【兵粮寸断】对其使用，否则你获得其一张牌；“擂鼓进军”，你视为对其使用一张【决斗】。",
  ty_guanyu: "神秘将军",
  ty_guanyu_ab: "牢关羽",
  ty_guanyu_prefix: "牢",
  tywusheng: "武圣",
  tywusheng_info: "你可以将一张红色牌当【杀】使用或打出。你以此法使用的【杀】仅能被与此牌花色相同的【闪】抵消。",
  tychengshi: "乘势",
  tychengshi_info: "锁定技，每回合限一次，当你于回合内/回合外使用红色【杀】造成伤害后，你令此牌不计入次数/令受伤角色此阶段不能使用伤害类牌指定除你以外的角色为目标。",
  tyfuwei: "抚危",
  tyfuwei_info: "每回合限一次，当一号位或「刘备」受到伤害后，你可以交给其至多X张牌，然后可以对伤害来源依次使用至多X张【杀】（X为受到伤害点数）。",
  ty_sunquan: "桃孙权",
  ty_sunquan_prefix: "桃",
  tyfuhan: "辅汉",
  tyfuhan_info: "当你获得其他角色的手牌后/其他角色获得你的手牌后，该角色可以废除/恢复你的一个装备栏。一名角色的回合结束时，若你本回合发动过【辅汉】，你令当前回合角色将手牌摸至体力上限。",
  tychende: "臣德",
  tychende_info: "出牌阶段，你可以展示并交给其他角色至少两张手牌，然后你可以视为使用其中一张基本牌或普通锦囊牌。",
  tywansu: "完夙",
  tywansu_info: "锁定技，有装备栏被废除的角色不能响应虚拟牌；一张虚拟牌即将造成的伤害视为体力流失。",
  ty_liubei: "桃刘备",
  ty_liubei_prefix: "桃",
  tyqingshi: "倾师",
  tyqingshi_info: "准备阶段，你可令至多你体力值名角色进行议事，若议事结果为：红色，本轮意见为红色的角色各与其以外的角色互相计算距离+1；黑色，你摸意见为黑色的角色数张牌，然后你可以交给任意名意见为黑色的角色各一张牌。",
  tyyilin: "夷临",
  tyyilin_info: "每回合每名角色限一次，当你获得其他角色的牌/其他角色获得你的牌后，你可令获得牌的角色选择是否使用其中一张牌。",
  tychengming: "承命",
  tychengming_info: "主公技，限定技，当你进入濒死时，你可令一名其他蜀势力角色获得你区域里的所有牌，然后你将体力值恢复至1点。若其拥有锁定技，其获得【仁德】。",
  ty_chenshi: "桃陈式",
  ty_chenshi_prefix: "桃",
  tyzhuan: "驻岸",
  tyzhuan_info: "出牌阶段，你可以弃置一张【杀】并获得一名其他角色场上的一张装备牌；一名角色使用装备牌后，你可以摸一张牌。",
  ty_chengjix: "程畿",
  tyzhongen: "忠恩",
  tyzhongen_info: "一名角色的结束阶段，若你的手牌本回合发生过变化，你可以将一张【杀】当【无中生有】对其使用，或使用一张无距离限制的【杀】。",
  tyliebao: "烈报",
  tyliebao_info: "手牌数最少的角色成为【杀】的目标后，你可以摸一张牌并代替其成为目标，若你未因此【杀】受到伤害，其回复1点体力。",
  ty_zhaorong: "赵融",
  tyyuantao: "援讨",
  tyyuantao_info: "每回合限一次，一名角色使用基本牌时，你可以令此牌额外结算一次，然后你于当前回合结束时失去1点体力。",
  ty_zhangnan: "桃张南",
  ty_zhangnan_prefix: "桃",
  tyfenwu: "奋武",
  tyfenwu_info: "准备阶段，你可以摸一张牌并展示之，然后你可以将此牌当牌名字数与之相同的基本牌或【决斗】使用。",
  ty_fengxí: "桃冯习",
  ty_fengxí_prefix: "桃",
  tyqingkou: "轻寇",
  tyqingkou_info: "结束阶段，你可以从牌堆底摸一张牌并展示之，然后你可以将此牌当牌名字数与你的体力值相同的普通锦囊牌或【杀】使用。",
  ty_liaohua: "桃廖化",
  ty_liaohua_prefix: "桃",
  tydangxian: "当先",
  tydangxian_info: "锁定技，回合开始时，你执行一个额外的出牌阶段并从弃牌堆中选择一张【杀】获得。",
  tyfuli: "伏枥",
  tyfuli_info: "限定技，当你处于濒死状态时，你可以回复体力至2点并将手牌摸至两张。",
  ty_huangzhong: "桃黄忠",
  ty_huangzhong_prefix: "桃",
  tyyizhuang: "益壮",
  tyyizhuang_info: "准备阶段，若你的判定区里有牌，你可以对自己造成1点伤害，然后弃置判定区里的所有牌。",
  ty_wuban: "桃吴班",
  ty_wuban_prefix: "桃",
  tyyoujun: "诱军",
  tyyoujun_info: "出牌阶段限一次，你可以获得一名其他角色的一张牌。若如此做，其可以令其本回合所有手牌视为【杀】并视为对你使用一张【决斗】。",
  tyjicheng: "计成",
  tyjicheng_info: "限定技，当你受到普通锦囊牌的伤害后，若你的体力值不大于2，你可以选择回复1点体力或摸两张牌。",
  ty_shicong: "侍从",
  tyjinzhong: "尽忠",
  tyjinzhong_info: "出牌阶段开始时或你受到伤害后，你可以选择一项：1：失去1点体力并令一号位或「刘备」回复1点体力；2：交给一名角色至多两张手牌。",
  ty_guanxing: "桃关兴",
  ty_guanxing_prefix: "桃",
  tyconglong: "从龙",
  tyconglong_info: "一名角色使用红色【杀】时，你可以弃置一张锦囊牌，令此【杀】不可被响应；一名角色受到红色【杀】造成的伤害时，你可以弃置一张装备牌，令此伤害+1；一名角色的回合结束时，若你本回合弃置过至少两张牌，你可以摸一张牌。",
  tyzhaowu: "昭武",
  tyzhaowu_info: "当你受到其他角色造成的伤害后，你可以弃置一张牌。若如此做，直到本轮结束，你可以将一张红色牌当【杀】对其使用，且你对其使用牌无距离限制。",
  ty_shamoke: "桃沙摩柯",
  ty_shamoke_prefix: "桃",
  tymanyong: "蛮勇",
  tymanyong_info: "回合开始时，若你的装备区里没有【铁蒺藜骨朵】，你可以从游戏外搜寻并使用之；回合结束时，你可以弃置装备区里的【铁蒺藜骨朵】。",
  ty_guanyinping: "桃关银屏",
  ty_guanyinping_prefix: "桃",
  tywuji: "武继",
  tywuji_info: "觉醒技，结束阶段，若你本回合造成了3点或更多伤害，你加1点体力上限并回复1点体力，失去【虎啸】，然后选择一项：1.从游戏外搜寻并获得【青龙偃月刀】；2.摸两张牌。",
  ty_shen_liubei: "桃神刘备",
  ty_shen_liubei_prefix: "桃神",
  tylongnu: "龙怒",
  tylongnu_info: "转换技，游戏开始时，你可以改变此转换技的状态。出牌阶段开始时，你可以摸一张牌并：阳：失去1点体力，然后此阶段内你可以将红色手牌当无距离限制的火【杀】使用或打出；阴：减少1点体力上限，然后此阶段内你可以将锦囊牌当无次数限制的雷【杀】使用或打出。",
  tytaoyuan: "桃园",
  tytaoyuan_info: "出牌阶段限一次，你可以弃置两张牌，然后令一名角色从游戏外获得一张【桃园结义】。",
  ty_luxun: "桃陆逊",
  ty_luxun_prefix: "桃",
  tyqianshou: "谦守",
  tyqianshou_info: "转换技，其他角色的回合开始时，若其体力值大于你，或其未处于横置状态，阳：你可展示并交给其一张红色牌，本回合你不能使用手牌且你与其不能成为牌的目标；阴：你可令其展示并交给你一张牌，若此牌不为黑色，你失去1点体力。",
  tytanlong: "探龙",
  tytanlong_info: "出牌阶段限x次，你可以与一名角色进行拼点。若如此做，拼点赢的角色可以获得另一名拼点角色的拼点牌，然后其视为对自己使用一张【铁索连环】（x为横置角色数+1）。",
  tyxibei: "袭惫",
  tyxibei_info: "当其他角色从牌堆外获得牌后，你可以摸一张牌；若此时为你的出牌阶段，你可展示一张锦囊牌并令此牌本回合视为【火烧连营】直到离开你的手牌区。",
  ty_ganning: "桃甘宁",
  ty_ganning_prefix: "桃",
  tyqixi: "奇袭",
  tyqixi_info: "你可以将一张黑色牌当【过河拆桥】使用：你使用非基本牌转化的【过河拆桥】不能被响应。",
  tyfenwei: "奋威",
  tyfenwei_info: "当一张锦囊牌指定两个或更多目标后，你可以失去X点体力或【奋威】，令此牌对其中任意名目标角色无效(X为“奋威”发动的次数且至少为1)。",
  ty_buzhi: "桃步骘",
  ty_buzhi_prefix: "桃",
  tyhongde: "弘德",
  tyhongde_info: "当你一次性获得或失去至少两张牌后，你可以令一名角色摸一张牌或弃一张牌。",
  tydingpan: "定叛",
  tydingpan_info: "出牌阶段限X次，你可以令一名装备区有牌的角色摸一张牌并选择一项：1.令你弃置其两张牌：2.获得其装备区里的所有牌，然后你对其造成1点伤害(X为你本回合使用过的牌的类型数）。",
  ty_tanxiong: "谭雄",
  tylengjian: "冷箭",
  tylengjian_info: "锁定技，你对攻击范围内/外的角色每回合首次使用【杀】的伤害+1/无距离限制且不能被响应。",
  tysheju: "射驹",
  tysheju_info: "当你使用【杀】结算结束后，你可以弃置其中一名目标角色的一张牌，若此牌不为坐骑牌，其本回合攻击范围+1，然后若其攻击范围内含有你，其可以对你使用一张【杀】。",
  ty_liue: "刘阿",
  tyxiyu: "西御",
  tyxiyu_info: "一名角色使用转化牌或虚拟牌指定目标后，你可以摸一张牌。",
  ty_zhangda: "桃张达",
  ty_zhangda_prefix: "桃",
  ty_fanjiang: "桃范疆",
  ty_fanjiang_prefix: "桃",
  tybianta: "鞭挞",
  tybianta_info: "每回合限一次，你成为伤害类牌的目标后，你可以将此牌置于你的武将牌上，称为“怨”。结束阶段，你可以依次使用所有“怨”。",
  tybenxiang: "奔降",
  tybenxiang_info: "锁定技，你杀死一名角色后，你令一名其他角色摸三张牌。",
  tyxiezhan: "协战",
  tyxiezhan_info: `锁定技，①游戏开始时，你选择${get.poptip({
    id: "character_ty_fanjiang",
    name: "范疆",
    type: "character",
    dialog: "characterDialog"
  })}或${get.poptip({
    id: "character_ty_zhangda",
    name: "张达",
    type: "character",
    dialog: "characterDialog"
  })}并变更武将牌。②出牌阶段开始时，你变更武将牌。`,
  tyxingsha: "刑杀",
  tyxingsha_info: "每回合限一次，出牌阶段，你可以将至多两张牌置于你的武将牌上，称为“怨”。结束阶段，你可以将两张“怨”当做无距离限制的普通【杀】使用。",
  tyxianshou: "献首",
  tyxianshou_info: "锁定技，你杀死一名角色后，你令一名其他角色回复2点体力。",
  ty_shen_zhangfei: "桃神张飞",
  ty_shen_zhangfei_prefix: "桃神",
  tyshencai: "神裁",
  tyshencai_info: "①你可以将一张无色牌当作【杀】使用或打出。②出牌阶段限一次，你可以令一名其他角色进行判定。你获得此判定牌，然后若此判定牌：包含以下要素中的任意一个，则其失去已有的下列效果，并获得对应的效果：{⒈体力：当其受到伤害后，其失去等量的体力、⒉武器：其不能使用牌响应【杀】、⒊打出：当其失去手牌后，其再随机弃置一张手牌（不嵌套触发）、⒋距离：其的结束阶段开始时，其翻面}；若均不包含，你获得其区域里的一张牌，其获得一枚“死”并获得如下效果：其的角色手牌上限-X、其的回合结束时，若X大于场上存活人数，则其死亡（X为其“死”标记数）。",
  tyshencai_wusheng: "神裁·杀",
  tyxunshi: "巡使",
  tyxunshi_info: "锁定技。①你手牌中的多目标锦囊牌花色视为none。②你使用颜色为none的牌无距离和次数限制。③当你使用无颜色的牌选择目标后，你令你的〖神裁〗的发动次数上限+1（至多为5），然后可以为此牌增加任意个目标。",
  tyxunshi_tag: "无色牌",
  ty_shen_guanyu: "桃神关羽",
  ty_shen_guanyu_prefix: "桃神",
  tywushen: "武神",
  tywushen_info: "你可以将红桃牌当作无距离和次数限制且不可被响应的【杀】使用或打出。",
  tywuhun: "武魂",
  tywuhun_info: "锁定技，你受到1点伤害后，令伤害来源获得1枚“梦魇”标记；当你死亡时，你令杀死你的角色或拥有“梦魇”标记最多的一名其他角色进行判定，若结果不为【桃】，其死亡。",
  ty_anying: "暗影",
  ty_anyingx: "暗影",
  ty_wuque: "乌鹊",
  ty_yanque: "阎鹊",
  ty_wangque: "亡鹊",
  tyliupo: "流魄",
  tyliupo_info: "转换技，回合开始时，你令本轮：阳：所有角色不能使用【桃】；阴：所有即将造成的伤害均视为体力流失。",
  tyzhuiling: "追灵",
  tyzhuiling_info: "锁定技，当一名角色失去体力后，你获得等量的“魂”（你至多拥有3枚“魂”）；你对没有手牌的角色使用牌无距离和次数限制。",
  tyxihun: "吸魂",
  tyxihun_info: "锁定技，每轮结束时，所有其他角色失去1点体力或弃置两张手牌。然后你弃置至多3枚“魂”并回复等量体力。",
  tyxianqi: "献气",
  tyxianqi_info: "其他角色的出牌阶段限一次，其可以对自身造成1点伤害或弃置两张手牌，若如此做，你受到1点伤害。",
  tyfansheng: "返生",
  tyfansheng_info: "锁定技，你首次进入濒死状态时，你回复体力至1点，然后令所有其他角色依次选择一项：1：弃置所有手牌；2：弃置装备区里的所有牌。",
  tyansha: "暗杀",
  tyansha_info: "其他角色的回合开始时，你可以将一张牌当刺【杀】对其使用，此牌结算后，其对你的距离视为1直到本轮结束。",
  tycangshen: "藏身",
  tycangshen_info: "锁定技，所有其他角色计算与你的距离+1；当你使用【杀】后，【藏身】失效直到本轮结束。",
  tyxiongren: "凶刃",
  tyxiongren_info: "锁定技，你对计算与你距离大于1/不大于1的角色使用【杀】造成的伤害+1/无距离和次数限制。",
  tysiji: "伺机",
  tysiji_info: "其他角色回合结束时，若其本回合不因使用或打出而失去过牌，你可以将一张牌当无距离限制的刺【杀】对其使用。",
  tydaifa: "待发",
  tydaifa_info: "其他角色回合结束时，若其本回合获得过除其外的角色的牌，你可以将一张牌当无距离限制的刺【杀】对其使用。",
  yy_quyi: "燕幽麴义",
  yy_quyi_prefix: "燕幽",
  yyfuqi: "伏骑",
  yyfuqi_info: "锁定技。①当你使用牌时，与你距离不大于1的角色无法响应此牌。②当你使用牌指定距离大于1的角色时，摸一张牌。",
  yy_gongsunzan: "燕幽公孙瓒",
  yy_gongsunzan_prefix: "燕幽",
  yyqizhen: "骑阵",
  yyqizhen_info: "当你使用【杀】指定目标后，你可以对其发动此技能。若此【杀】结算完毕后，此【杀】：造成过伤害，你摸造成伤害数的牌；未造成过伤害，你弃置其装备区的一张牌。",
  yymujun: "募军",
  yymujun_info: "主公技，限定技。出牌阶段，你可以令一名群势力角色获得〖义从〗。",
  yy_wenchou: "燕幽文丑",
  yy_wenchou_prefix: "燕幽",
  yyxuezhan: "血战",
  yyxuezhan_info: "锁定技。①你的锦囊牌视为【决斗】。②你使用【决斗】不可被【无懈可击】响应。",
  yyyazhen: "压阵",
  yyyazhen_info: "你可以将装备区的牌当作【杀】使用或打出。",
  yy_gongsunyuan: "燕幽公孙渊",
  yy_gongsunyuan_prefix: "燕幽",
  yyxuanshi: "旋势",
  yyxuanshi_info: "出牌阶段限两次，若你的红色手牌和黑色手牌数相同，则你可以展示所有手牌并获得一名其他角色区域里的一张牌。",
  yyxiongye: "凶业",
  yyxiongye_info: "主公技，出牌阶段限一次，你可以交给任意名其他群势力角色各一张手牌，然后依次对这些角色各造成1点伤害。",
  yy_yuanshao: "燕幽袁绍",
  yy_yuanshao_prefix: "燕幽",
  yysudi: "肃敌",
  yysudi_info: "锁定技，攻击范围包含你的角色响应你使用的牌后，你摸一张牌。",
  yyqishe: "齐射",
  yyqishe_info: "①游戏开始时，你从游戏外获得一张【万箭齐发】。②结束阶段，你可以获得弃牌堆中的一张【万箭齐发】。",
  yylinzhen: "临阵",
  yylinzhen_info: "主公技，锁定技。你视为在其他群势力角色的攻击范围内。",
  yy_simayi: "燕幽司马懿",
  yy_simayi_prefix: "燕幽",
  yyyanggu: "佯固",
  yyyanggu_info: "转换技。阳：当你受到伤害后，你可以回复1点体力；阴：你可以将一张手牌当作【声东击西】使用。",
  yyzuifu: "罪缚",
  yyzuifu_info: "每回合限一次，一名角色于摸牌阶段外获得牌后，若场上没有角色处于濒死状态，则你可以对其造成1点伤害。",
  yy_caorui: "燕幽曹叡",
  yy_caorui_prefix: "燕幽",
  yyhuituo: "恢拓",
  yyhuituo_info: "当你受到1点伤害后，你可以令一名角色进行一次判定，若结果为红色/黑色，其回复1点体力/摸一张牌。",
  yymingjian: "明鉴",
  yymingjian_info: "其他角色的出牌阶段开始时，你可以展示所有手牌并交给其一种花色的所有牌，然后其本回合使用的下一张牌额外结算一次。",
  yy_zhaoyun: "燕幽赵云",
  yy_zhaoyun_prefix: "燕幽",
  yy_baimaxiaoqi: "白马骁骑",
  yy_baimaxiaoqi_skill: "白马骁骑",
  yy_baimaxiaoqi_info: "锁定技，若你的装备区牌数X不少于：一张，你的攻击范围+X；两张，你使用【杀】的额定次数+X；三张，你计算与其他角色的距离-X；四张，摸牌阶段，你多摸X张牌。",
  yy_quancong: "燕幽全琮",
  yy_quancong_prefix: "燕幽",
  yyyaoming: "邀名",
  yyyaoming_info: "出牌阶段各限一次，或当你受到伤害后，你可以：①弃置一名手牌数大于等于你的其他角色的一张牌；②令一名手牌数小于等于你的角色摸一张牌。",
  yjqingjiao: "轻狡",
  yjqingjiao_info: "主公技，锁定技。结束阶段，若你本回合对其他群势力角色造成过伤害，你摸一张牌。",
  yj_yongkai: "雍闿",
  psxiaofan: "嚣反",
  psxiaofan_info: "一名蜀/吴/群势力角色不因〖嚣反〗受到伤害后/失去装备后/造成伤害后，你可以对其造成1点伤害/与其各摸一张牌/获得造成此伤害的牌，然后你变更势力至吴/群/蜀。",
  psjiaohu: "骄扈",
  psjiaohu_info: "蜀势力技，锁定技。摸牌阶段，你多摸X张牌（X为一号位的已损失体力值+1）。",
  psquanpan: "劝叛",
  psquanpan_info: "吴势力技，当你获得装备牌后，你可以展示其中一张牌并交给一名其他角色。",
  pshuoluan: "惑乱",
  pshuoluan_info: "群势力技，锁定技。你与蜀势力角色互相对对方造成的伤害+1。",
  yj_chezhou: "车胄",
  psanmou: "暗谋",
  psanmou_info: "锁定技，游戏开始时，你暗中指定一名其他角色，你与其互相对对方使用牌无次数限制。",
  pstousuan: "偷算",
  pstousuan_info: "锁定技，若你/“暗谋”角色于本轮游戏中首次对其/对你造成伤害时，你令此伤害+1并摸三张牌，然后失去〖偷算〗。",
  jy_caocao: "荆扬曹操",
  jy_caocao_prefix: "荆扬",
  jylijun: "砺军",
  jylijun_info: "当魏势力角色受到伤害后，你可以令其摸一张牌。",
  jytongbei: "统北",
  jytongbei_info: "当你对非魏势力角色造成伤害时，你可以令其选择一项：1.此伤害+1；2.交给你一张你声明类型的牌。",
  jy_caoren: "荆扬曹仁",
  jy_caoren_prefix: "荆扬",
  jybeirong: "备戎",
  jybeirong_info: "出牌阶段限一次。你可以重铸任意张手牌，若你以此法重铸的花色数不小于你的体力值，你横置。",
  jyyujun: "御军",
  jyyujun_info: "一名横置角色受到属性伤害时，你可以翻面并失去1点体力，然后摸三张牌，若如此做，你防止此伤害。",
  jy_zhugeliang: "荆扬诸葛亮",
  jy_zhugeliang_prefix: "荆扬",
  jyqibian: "七辩",
  jyqibian_info: "锁定技。①每轮开始时，你将牌堆顶的七张牌置于武将牌上，称为“才”。②每轮结束时，你将所有“才”置入弃牌堆。",
  jycailve: "才略",
  jycailve_info: "当你需要使用或打出牌时，若“才”中有此牌，你可以使用或打出之，然后成为此牌目标的角色可以弃置你一张牌。",
  jy_pangtong: "荆扬庞统",
  jy_pangtong_prefix: "荆扬",
  jylianhuan: "连环",
  jylianhuan_info: "出牌阶段限一次。你可以失去1点体力并视为使用一张【铁索连环】。",
  jysuozhou: "索舟",
  jysuozhou_info: "每回合限一次，当你成为梅花牌的目标或使用梅花牌时，你可以令所有已横置的角色摸一张牌。",
  jyyuhuo: "浴火",
  jyyuhuo_info: "锁定技，已横置的其他角色受到的属性伤害+1，非属性伤害-1。",
  jy_lusu: "荆扬鲁肃",
  jy_lusu_prefix: "荆扬",
  jydimeng: "缔盟",
  jydimeng_info: "当有角色拼点时，你可以交换双方的拼点牌。",
  jyzhouji: "周济",
  jyzhouji_info: "出牌阶段限X次，你可以与一名角色拼点，若其赢，其摸两张牌（X为你的体力值）。",
  jy_zhangzhao: "荆扬张昭",
  jy_zhangzhao_prefix: "荆扬",
  jyboyan: "驳言",
  jyboyan_info: "出牌阶段限两次，你可以与一名角色拼点，没赢的角色本回合不能使用手牌并摸两张牌。",
  jymushi: "慕势",
  jymushi_info: "结束阶段，若所有其他角色的手牌数均不小于其体力值，你可以选择一项：1.获得所有其他角色各一张牌；2.令所有其他角色将手牌数调整至其体力值。",
  jy_zhouyu: "荆扬周瑜",
  jy_zhouyu_prefix: "荆扬",
  jysashuang: "飒爽",
  jysashuang_info: "结束阶段，你可以获得本回合进入弃牌堆的每种颜色的牌各一张。",
  jyhuoce: "火策",
  jyhuoce_info: "出牌阶段限一次，你可以与一名其他角色同时弃置一张手牌，若这些牌颜色相同，你对一名角色造成1点火焰伤害。",
  jy_huanggai: "荆扬黄盖",
  jy_huanggai_prefix: "荆扬",
  jyliezhou: "烈舟",
  jyliezhou_info: "锁定技，你造成的伤害均视为火焰伤害；当你对一名角色造成伤害后，若其受到此伤害时已横置，你摸X张牌（X为伤害值）。",
  jyzhaxiang: "诈降",
  jyzhaxiang_info: "出牌阶段限一次，你可以减少1点体力上限，然后你本回合使用牌不能被响应。",
  ps_zhangrang: "蛇张让",
  ps_zhangrang_prefix: "蛇",
  pstaoluan: "滔乱",
  pstaoluan_info: "出牌阶段限X次，你可以将一张牌当本回合你未以此法使用过的任意一种基本牌或普通锦囊牌使用（X为场上的“十常侍”标记数）。",
  pschangshi: "常侍",
  pschangshi_info: "①游戏开始时，你获得1枚“十常侍”标记并获得以下效果直到你失去所有“十常侍”标记：{1.一号位的弃牌阶段开始时，你可以摸一张牌，然后令其本局游戏的手牌上限+1；2.当你受到致命伤害时，你可以弃置1枚“十常侍”标记，将此伤害转移给一名拥有“十常侍”标记的其他角色}。②当你失去“十常侍”标记时，你减少1点体力上限。",
  ps_zhaozhong: "蛇赵忠",
  ps_zhaozhong_prefix: "蛇",
  pschiyan: "鸱咽",
  pschiyan_info: "当你使用【杀】指定目标后，你可以依次令目标角色与你将任意张牌置于自己的武将牌上直到当前回合结束。若如此做，手牌数不大于/不小于你的目标角色本回合受到的伤害+1/不能使用手牌。",
  ps_sunzhang: "蛇孙璋",
  ps_sunzhang_prefix: "蛇",
  pszimou: "自谋",
  pszimou_info: "锁定技。出牌阶段开始时，你令所有角色选择一项：1.交给你一张牌；2.弃置你一张牌，然后受到你造成的1点伤害。",
  ps_bilan: "蛇毕岚",
  ps_bilan_prefix: "蛇",
  psbicai: "庀材",
  psbicai_info: "出牌阶段限一次。你可令X名角色依次将一张手牌置于牌堆顶，然后你亮出牌堆顶X张牌。其中每有一种类别，你便摸一张牌。若你因此获得三张牌，则因此失去牌的角色依次从亮出的牌中选择一张获得（X为你的体力值）。",
  ps_xiayun: "蛇夏恽",
  ps_xiayun_prefix: "蛇",
  psyaozhuo: "谣诼",
  psyaozhuo_info: "①出牌阶段限一次或当你受到伤害后，你可以与一名角色拼点，若你赢，其本回合手牌上限-2；若你没赢，你回复1点体力。②当你拼点结算完成后，你可以获得对方的拼点牌。",
  ps_hankui: "蛇韩悝",
  ps_hankui_prefix: "蛇",
  psxiaolu: "宵赂",
  psxiaolu_info: "其他角色的出牌阶段限一次。其可以交给你一张牌，然后其视为对另一名角色使用一张仅指定其为目标的普通锦囊牌。",
  ps_lisong: "蛇栗嵩",
  ps_lisong_prefix: "蛇",
  pskuiji: "窥机",
  pskuiji_info: "出牌阶段限一次。你可以观看一名其他角色的手牌，然后你可以弃置你与其的共计四张花色各不相同的手牌。若如此做，因此弃置牌数较多/较少的角色失去1点体力/获得〖仇海〗直到本轮结束。",
  ps_duangui: "蛇段珪",
  ps_duangui_prefix: "蛇",
  pschihe: "叱吓",
  pschihe_info: "当你使用【杀】指定唯一目标后或成为其他角色【杀】的唯一目标后，你可以摸两张牌并展示等量张手牌，然后你与目标/使用者拼点，若你赢，此【杀】的伤害值基数+1；否则你弃置两张牌。",
  ps_guosheng: "蛇郭胜",
  ps_guosheng_prefix: "蛇",
  psniqu: "逆取",
  psniqu_info: "每回合限一次。一名角色使用或打出【闪】后，你可以摸一张牌，然后视为对其使用一张【杀】。",
  ps_gaowang: "蛇高望",
  ps_gaowang_prefix: "蛇",
  psmiaoyu: "妙语",
  psmiaoyu_info: "一名角色回复体力后，你可以将牌堆顶的一张牌交给其，若如此做，当前回合结束时，其失去X点体力（X为此技能本回合发动的次数）。",
  zombie_jiaxu: "长安神贾诩",
  zombie_jiaxu_prefix: "长安神",
  zombiesangluan: "丧乱",
  zombiesangluan_info: "当你使用伤害牌结算完毕后，你可以选择一名其他角色A和另一名角色B，A选择一项：①对B使用一张【杀】；②失去1点体力，你回复1点体力。",
  zombieshibao: "尸爆",
  zombieshibao_info: "出牌阶段，你可以令一名拥有“丧尸”武将牌的角色失去所有体力，然后你对其上下家各造成1点伤害。",
  zombiechuce: "出策",
  zombiechuce_info: "①你可以将一张锦囊牌当作无距离次数限制的任意基本牌或锦囊牌使用。②每回合限一次，其他角色使用锦囊牌时，你可以摸三张牌，令此牌无效，然后你可以视为使用此牌。",
  zombielongmu: "笼墓",
  zombielongmu_info: "锁定技。①你的回合内，其他角色不能回复体力。②当你成为锦囊牌的目标时，取消之。③一名角色死亡后，若你本局游戏对其发动过技能且其武将牌不为“丧尸”，则你取消之，然后使用“丧尸”替换其武将牌。",
  zombie_zombie: "丧尸",
  zombieshibian: "尸变",
  zombieshibian_info: "锁定技。①你保留被替换武将牌的技能。②你的身份和胜利条件改为和神贾诩一致。",
  zombieganran: "感染",
  zombieganran_info: "锁定技，结束阶段，你令本回合被你杀死且未拥有“丧尸”武将牌的角色复活，然后你将体力值回复至上限并摸等量张牌。",
  chenshou: "青史陈寿",
  chenshou_prefix: "青史",
  //eternal_poker: "invisible",
  hschenzhi_poker: "扑克",
  hschenzhi_poker_info: "扑克牌，牌名字数为0、无效果且类型为poker的牌",
  hschenzhi: "沉滞",
  hschenzhi_info: "锁定技，你摸牌改为从一副扑克牌中摸牌（包含初始手牌）；扑克牌拥有独立的弃牌堆。",
  hsdianmo: "点墨",
  hsdianmo_info: "准备阶段或当你每回合首次受到伤害后，你可以观看两个转化牌类的技能，选择是否获得其中一个（至多4个）或用其中一个替换另一个以此法获得的技能，然后摸空置的技能数张牌。",
  hszaibi: "载笔",
  hszaibi_info: "出牌阶段限一次，你可以重铸至少两张点数连续的牌，然后将游戏外的【春秋笔】置入你的装备区。",
  chunqiubi: "春秋笔",
  chunqiubi_skill: "春秋笔",
  chunqiubi_info: "出牌阶段限一次，你可以随机选择一项，然后选择一名角色从此项开始正序或逆序依次执行所有项。起：失去1点体力。承：摸已损失体力值张牌。转：回复1点体力。合：弃置已损失体力值张手牌（不足则全弃）。此牌离开你的装备区后销毁。",
  hs_liuxuan: "青史刘璿",
  hs_liuxuan_prefix: "青史",
  hssifen: "俟奋",
  hssifen_info: "出牌阶段限一次，你可令一名其他角色将任意张手牌当作一张【决斗】使用，然后你摸两张牌且此阶段内：你可以无次数限制的将等量张红色牌当作一张【决斗】对其使用。",
  hsfunan: "赴难",
  hsfunan_info: "主公技，每回合限一次，你可以发动一次〖激将〗，若没有角色响应，你失去1点体力并摸两张牌。",
  hs_caohuan: "青史曹奂",
  hs_caohuan_prefix: "青史",
  hsjunwei: "君威",
  hsjunwei_info: "每回合限一次，你可以将两张颜色相同的牌当作【无懈可击】使用，生效后你可以为目标普通锦囊牌选择另外至多两名额外目标角色。",
  hsmoran: "默然",
  hsmoran_info: "锁定技，当你受到伤害后，你选择于一至三个回合结束后（包含此回合）摸两倍所选回合数量的牌，在此期间中你的所有技能失效。",
  hs_liuxie: "青史刘协",
  hs_liuxie_prefix: "青史",
  hsjixu: "济恤",
  hsjixu_info: "出牌阶段每个角色组合限一次，你可以选择两名角色，若他们手牌数之和小于任意两名除其外的角色，则你观看牌堆顶的三张牌并分配给选择角色（每名角色至少一张）。",
  hsyouchong: "优崇",
  hsyouchong_info: "每回合限一次，当你需要使用基本牌时，你可以令任意名手牌数多于你的角色选择是否将三张牌当此牌替你使用。",
  hs_sunhao: "青史孙皓",
  hs_sunhao_prefix: "青史",
  hsshezuo: "设座",
  hsshezuo_info: "准备阶段，你可以选择一项令本回合下次拼点结束后没赢的角色执行：1.依次弃置两张牌，不足则失去等量体力；2.横置并受到1点火焰伤害；3.将所有手牌当一张普通锦囊牌使用。出牌阶段限一次，你可以摸一张牌并拼点。",
  ca_shen_lijueguosi: "长安神李傕郭汜",
  ca_shen_lijueguosi_prefix: "长安神",
  caweijue: "威傕",
  caweijue_info: "锁定技，准备阶段，从你的下家开始所有其他角色依次将任意张手牌置于武将牌上直到回合结束，称为“威”；若一名角色的“威”不大于其手牌数，你与其视为在彼此攻击范围内。",
  cachuxiong: "除凶",
  cachuxiong_info: "出牌阶段开始时，你可以展示所有手牌并弃置其中一种颜色的所有牌，若你以此法弃置的牌颜色为：黑色，你获得等量张“威”；红色，你对攻击范围内的所有其他角色依次造成1点伤害。",
  ca_shen_caocao: "长安神曹操",
  ca_shen_caocao_prefix: "长安神",
  cazhaoshao: "诏绍",
  cazhaoshao_info: "当你造成或受到1点伤害后，你可以摸一张牌，然后令受伤角色或伤害来源：1.获得弃牌堆或场上的一张装备牌并使用之（此装备占用独立装备栏）；2.翻面并摸一张牌；3.减少1点体力上限。",
  caxiaoxiong: "嚣凶",
  caxiaoxiong_info: "锁定技，当你翻面时，取消之，然后令所有其他角色失去1点体力。",
  ca_shen_wangyun: "长安神王允",
  ca_shen_wangyun_prefix: "长安神",
  caanchao: "安朝",
  caanchao_info: "一名角色的回合结束时，若本回合有角色使用过虚拟牌或转化牌，你可摸一张牌并获得1点蓄力值。",
  cayurong: "御戎",
  cayurong_info: "锁定技，你于一轮内首次成为一种伤害牌的目标时，取消之。",
  cadingxi: "定西",
  cadingxi_info: "蓄力技（4/∞）。当你需要使用一种类型的牌时，你可以消耗1点蓄力值并展示牌堆顶的一张牌，若类型相同，你使用之，否则你从牌堆底摸一张牌；若你连续相同2/3次，你回复全部体力/对所有其他角色各造成1点伤害。",
  ca_wangyun: "长安王允",
  ca_wangyun_prefix: "长安",
  calianji: "连计",
  calianji_info: "出牌阶段限一次，你可令一名其他角色摸一张牌，然后令其视为使用一张你指定的伤害牌。",
  camoucheng: "谋逞",
  camoucheng_info: "觉醒技，一名角色造成伤害后，若本局游戏已通过〖连计〗造成了3点伤害，你增加1点体力上限并失去〖连计〗，然后回复1点体力并获得〖矜功〗。",
  cajingong: "矜功",
  cajingong_info: "出牌阶段限一次，你可以将一张【杀】或装备牌当作锦囊牌使用；若如此做，回合结束时若你本回合未造成伤害，你失去1点体力。",
  ca_lvbu: "长安吕布",
  ca_lvbu_prefix: "长安",
  caliyu: "利驭",
  caliyu_info: "出牌阶段限两次，你可以获得一名其他角色的一张牌，令你本回合使用【杀】或【决斗】造成的伤害+1，且其视为对你使用一张【决斗】。",
  ca_lijue: "长安李傕",
  ca_lijue_prefix: "长安",
  cacuixi: "摧袭",
  cacuixi_info: "出牌阶段限两次，你可以弃置任意张手牌，然后依次与两名体力值小于你的角色展示牌堆顶的一张牌，对展示牌点数不为最大的角色造成2点伤害。你可以令你的点数+X（X为你弃置的牌数）。",
  cajujun: "聚军",
  cajujun_info: "限定技，出牌阶段，你可以将手牌和体力值补至体力上限，若如此做，你不能回复体力直到杀死一名角色。",
  ca_guosi: "长安郭汜",
  ca_guosi_prefix: "长安",
  casixi: "伺袭",
  casixi_info: "出牌阶段限两次，你可以与一名角色拼点。若你赢，你可视为使用一张刺【杀】；否则其计算与你的距离+1。一次拼点结束后，你获得所有拼点牌。",
  calvedao: "掠盗",
  calvedao_info: "你使用【杀】对一名角色造成伤害时，可以获得其一张牌并令其减少1点体力上限，然后其获得〖避凶〗。",
  ca_zhangji: "长安张济",
  ca_zhangji_prefix: "长安",
  casilve: "肆掠",
  casilve_info: "摸牌阶段开始时，你可以改为获得任意名角色至多两张牌，然后将等量的牌置于武将牌上，称为“掠”。",
  casuibian: "随变",
  casuibian_info: "一名角色使用与“掠”花色相同的牌时，你可选择一项：1.弃置所有此花色“掠”并对其造成1点伤害；2.与其各摸一张牌；3.失去1点体力令此牌无效，然后将此牌交给一名角色并摸一张牌。",
  ca_fanchou: "长安樊稠",
  ca_fanchou_prefix: "长安",
  caxingwei: "兴威",
  caxingwei_info: "你获得红色牌时，可以展示此牌并摸一张牌；准备阶段或你受到伤害后，你可以获得弃牌堆里的一张红色牌。",
  caqianmu: "浅目",
  caqianmu_info: "每回合每项各限一次，你可以展示并将一张♦/♥牌当作一张基本牌/锦囊牌使用或打出。",
  ps_caocao: "风云曹操",
  ps_caocao_prefix: "风云",
  psjuebing: "谲兵",
  psjuebing_info: "你可以将一张非【杀】手牌当【杀】使用。且你以此法使用的【杀】获得以下效果：1.目标角色仅能将一张非【闪】手牌当【闪】来响应此牌。2.此牌造成伤害后不计入次数限制。3.此牌结算后，你和唯一目标可以依次使用双方的一张被转换牌。",
  psfengxie: "奉挟",
  psfengxie_info: "限定技。出牌阶段，你可以选择一名其他角色A，你依次选择所有除其以外的角色的装备区内的一张装备牌，若此牌A可装备则其装备之，否则你获得之。然后所有场上身份为明忠的角色失去〖舍身〗和〖明察〗，你获得之。",
  ps_liubei: "风云刘备",
  ps_liubei_prefix: "风云",
  pshuji: "互忌",
  pshuji_info: "每轮开始时，你可以选择一名攻击范围内互不包含对方的其他角色，其赠予你一张手牌并与你获得以下效果直到本轮结束：本轮每名角色回合结束时，你与其攻击范围内包含对方的角色须弃置两张手牌对对方造成1点伤害。",
  pshoufa: "后发",
  pshoufa_info: "每回合限一次。当你对一名座次比你靠前的角色造成伤害时，你可以将手牌摸至其体力上限。",
  ps_sunquan: "风云孙权",
  ps_sunquan_prefix: "风云",
  pszhanlun: "战论",
  pszhanlun_info: "你使用的【杀】可以通过助战获得以下效果：1.此【杀】不计入次数限制；2.此【杀】结算结束后，你根据此次助战牌的颜色获得对应效果：黑色：你本回合使用的下一张【杀】伤害+1；红色，你与本回合进行过助战的角色各摸两张牌，此技能本回合失效。",
  psjueyi: "决意",
  psjueyi_info: "出牌阶段开始时，你可以重铸至多两张牌，然后所有角色本回合不能弃置与重铸牌花色相同的牌，直到有角色进入濒死状态。",
  yj_caocaoyuanshao: "曹操袁绍",
  yjguibei: "贵卑",
  yjguibei_info: "锁定技，游戏开始时，你摸四张牌，然后你与一号位的上家交换座次。",
  yjjiechu: "劫出",
  yjjiechu_info: "转换技，阳：出牌阶段，你可以视为使用一张【顺手牵羊】，结算结束后成为目标的角色可以对你使用一张【杀】；阴：当你成为【杀】的目标时，你可以弃置一张手牌改变【杀】的花色和属性。",
  yjdaojue: "道抉",
  yjdaojue_info: "使命技，当你首次受到一种花色的牌造成的伤害时，你防止此伤害，然后选择一项：1.获得造成伤害的牌；2.使用一张指定所有其他角色的【杀】。成功：当你因技能〖道抉〗而获得至少三张牌后，你失去技能〖劫出〗，变更势力至魏，然后获得技能〖清正〗〖治暗〗和〖护驾〗。失败：当你因技能〖道抉〗而使用的第三张【杀】结算结束后，你失去技能〖劫出〗，变更势力至群，然后获得技能〖神离〗〖诛逆〗和〖士首〗。",
  yjtuonan: "脱难",
  yjtuonan_info: "限定技，你进入濒死状态时，你可以回复1点体力，然后失去一个带有标签的技能。",
  yjzhuni: "诛逆",
  yjzhuni_info: "出牌阶段限一次，你可以令所有角色同时选择一名其他角色，若有以此法被选择次数唯一最多的角色，你本回合对其使用牌无距离和次数限制，然后你摸等同于其以此法被选择次数的牌。",
  yjqingzheng: "清正",
  yjqingzheng_info: "出牌阶段开始时，你可以展示所有手牌并弃置其中一种花色的所有牌，然后展示一名其他角色的所有手牌井弃置其中一种花色的所有牌，若你以此法被弃置的牌数大于其以此法被弃置的牌数，你对其造成1点伤害。",
  yjzhian: "冶暗",
  yjzhian_info: "每回合限X次，当一名角色使用的非基本牌结算结束后，你可以选择一项：①弃置一张手牌，获得场上或弃牌堆中的此牌；②回复1点体力，此技能本轮失效；③对其造成X点伤害，然后你获得〖飞影〗井删去此选项(X为你已损失体力值且至少为1)。",
  pe_que: "曲阿小将",
  peyingzhen: "应阵",
  peyingzhen_info: "游戏开始时，你与一名其他角色的上家或下家交换位置，然后你与其依次执行一个额外回合。",
  peyuanjue: "援绝",
  peyuanjue_info: "转换技，摸牌阶段开始时，你可以跳过摸牌阶段，阳：令所有角色的基本牌视为无次数限制的【杀】；阴：令所有角色与你互相计算距离为1，且你视为拥有〖同忾〗。",
  peaoyong: "鏊勇",
  peaoyong_info: `${get.poptip("rule_chihengji")}，你不因此技能获得牌时，可以选择一项：1.摸一张牌；2.回复1点体力；3.使用一张牌；${get.poptip("rule_beishui")}：减少1点体力上限。`,
  petongkai: "同忾",
  petongkai_info: "一名角色成为伤害牌的目标时，若你与其的距离不大于1，你可以摸一张牌，然后展示并交给其一张牌；若此牌为装备牌，其可使用之。",
  pe_jun_caopi: "君曹丕",
  pe_jun_caopi_prefix: "君",
  jun_cuanzun: "篡尊",
  jun_cuanzun_info: "其他角色死亡后，你可以获得其所有牌并回复1点体力。",
  jun_liufang: "流放",
  jun_liufang_info: "出牌阶段限一次或你受到伤害后，你可以令一名其他角色翻面并摸X张牌，若X大于1，其进行一次【闪电】判定（X为你的已损失体力值）。",
  pe_jun_liushan: "君刘禅",
  pe_jun_liushan_prefix: "君",
  jun_fuxiang: "付相",
  jun_fuxiang_info: "出牌阶段开始时，你可以跳过此阶段并选择一名其他角色，若如此做，你于弃牌阶段结束时将本阶段进入弃牌堆的牌交给其，并令其于此回合结束后执行一个额外回合。",
  jun_leling: "乐陵",
  jun_leling_info: "锁定技，你成为【杀】或延时锦囊牌的目标时，使用者须交给你一张同类型的手牌，否则此牌无效。",
  pe_jun_sunquan: "君孙权",
  pe_jun_sunquan_prefix: "君",
  jun_henglv: "衡虑",
  jun_henglv_info: "出牌阶段，你可以失去X点体力并弃置任意张手牌，然后摸弃牌数+X张牌；你可以将以此法弃置的【桃】交给任意角色（X为此技能发动次数-1）。",
  pe_jun_liuxie: "君刘协",
  pe_jun_liuxie_prefix: "君",
  jun_tianze: "天择",
  jun_tianze_info: "你成为【杀】的目标时，可以弃置两张牌并观看牌堆顶四张牌，然后获得其中两张牌。若如此做，你令一名手牌数或体力值最大的其他角色执行相同操作。",
  jun_zhaoshou: "诏授",
  jun_zhaoshou_info: "出牌阶段，你可以将所有手牌交给一名其他角色，并令其与另一名其他角色拼点，赢的角色视为使用一张无距离限制的【杀】。",
  pe_jun_liuhong: "君刘宏",
  pe_jun_liuhong_prefix: "君",
  jun_gezhi: "革制",
  jun_gezhi_info: "你使用牌时，可以重铸三种类型的牌各一张，然后选择一项：1.回复1点体力，2.使用的下一张牌无次数限制；3.对一名其他角色造成1点伤害（每名角色限一次）。",
  jun_julian: "聚敛",
  jun_julian_info: "主公技，结束阶段，其他群势力角色可依次摸两张牌；若如此做，你获得其一张牌。",
  pe_jun_liubei: "君刘备",
  pe_jun_liubei_prefix: "君",
  jun_renwang: "仁望",
  jun_renwang_info: "出牌阶段，你可以将至少两张牌交给一名其他角色，然后回复1点体力且本回合出【杀】次数+1。",
  pe_jun_caocao: "君曹操",
  pe_jun_caocao_prefix: "君",
  jun_xiongtu: "雄图",
  jun_xiongtu_info: "你的体力值变化后，你可以从场上、牌堆、弃牌堆、处理区各获得一张伤害牌。",
  pe_jun_sunce: "君孙策",
  pe_jun_sunce_prefix: "君",
  jun_jiang: "激昂",
  jun_jiang_info: "你使用红色牌时，摸一张牌。",
  jun_zhiyang: "志扬",
  jun_zhiyang_info: "你的红色拼点牌视为K；你受到伤害后，可以令伤害来源对你发动两次〖制霸〗。",
  pe_jun_yuanshao: "君袁绍",
  pe_jun_yuanshao_prefix: "君",
  jun_hefa: "合伐",
  jun_hefa_info: "出牌阶段限一次，你可以将任意张手牌当无距离次数限制的【杀】对等量名角色使用；此【杀】造成伤害后，你将手牌补至体力上限。",
  pe_jun_zhangjiao: "君张角",
  pe_jun_zhangjiao_prefix: "君",
  jun_huanlei: "唤雷",
  jun_huanlei_info: "当你使用或打出【闪】或【闪电】后，或你不因此技能进行的判定结算后，你可以令一名其他角色判定：若结果为♠，对其造成2点雷电伤害；否则获得其一张手牌。",
  jun_xiandao: "显道",
  jun_xiandao_info: "一名角色的判定牌生效前，你可以打出一张黑色牌替换之，然后摸一张牌。",
  wn_shen_machao: "渭南神马超",
  wn_shen_machao_prefix: "渭南神",
  wn_qiangshu: "枪术",
  wn_qiangshu_info: "你使用【杀】或【决斗】造成伤害时，可以弃置X张牌，令此伤害+X（X为你的攻击范围-1）。",
  wn_yuma: "御马",
  wn_yuma_info: "每回合限一次，一张装备牌进入弃牌堆后，你可以将此牌置入一名角色装备区，然后获得其所有手牌。",
  wn_shen_xuzhu: "渭南神许褚",
  wn_shen_xuzhu_prefix: "渭南神",
  wn_zhuanzhan: "转战",
  wn_zhuanzhan_info: "其他角色的准备阶段，你可以废除一个装备栏并视为对其使用一张【决斗】。",
  wn_huwei: "虎威",
  wn_huwei_info: "锁定技，摸牌阶段你多摸X张牌（X为你已废除的装备栏数）。",
  wn_caocao: "渭南曹操",
  wn_caocao_prefix: "渭南",
  wn_dingluan: "定乱",
  wn_dingluan_info: "出牌阶段限一次，你可以失去1点体力，令一名其他角色选择一项：1.你视为对其使用一张【大军压境】；2.其武将牌上的技能失效直到其回合结束。",
  wn_zhuijiang: "遣将",
  wn_zhuijiang_info: "主公技，一名角色死亡后，你可以令一名魏势力角色与其交换座次。",
  wn_hansui: "渭南韩遂",
  wn_hansui_prefix: "渭南",
  wn_jubing: "举兵",
  wn_jubing_info: "每回合限一次，一名角色受到伤害后，你可以弃置攻击范围含有其的群势力角色各一张牌，视为对其使用X张【杀】（X为以此法弃置的牌数）。",
  wn_xiongju: "雄踞",
  wn_xiongju_info: "主公技，与你势力相同的角色视为拥有〖马术〗。",
  wn_houxuan: "候选",
  wn_zhongtao: "众讨",
  wn_zhongtao_info: "与你距离为1的角色使用【杀】结算完成后，你可以将一张牌当【杀】对相同目标角色使用。",
  wn_chengyi: "成宜",
  wn_dutan: "独探",
  wn_dutan_info: "出牌阶段限一次，你可以视为使用一张指定任意名角色的【决斗】。",
  wn_yangqiú: "杨秋",
  wn_qifeng: "齐锋",
  wn_qifeng_info: "一名角色的回合结束时，若你本回合失去过牌，你可以视为对其使用一张伤害为X的【杀】（X为你本回合失去的牌数）。",
  wn_zhanghe: "渭南张郃",
  wn_zhanghe_prefix: "渭南",
  wn_qiaobian: "巧变",
  wn_qiaobian_info: "其他角色的准备阶段，你可以将一张牌置于武将牌上，称为“巧”。本回合其使用牌时，你展示一张“巧”，若这两张牌类型：相同，其获得“巧”，然后若此时在其出牌阶段内，结束此阶段；不同，你摸一张牌，其的结束阶段，你获得所有“巧”。",
  wn_jiaxu: "渭南贾诩",
  wn_jiaxu_prefix: "渭南",
  wn_jianshu: "间书",
  wn_jianshu_info: "出牌阶段限一次，你可以交给一名其他角色一张牌，令其与你指定的另一名角色拼点，赢的角色弃置两张牌，没赢的角色失去1点体力。",
  wn_zhenlve: "缜略",
  wn_zhenlve_info: "一名角色使用【无懈可击】时，你可以弃置一张牌，令此牌无效并获得之。",
  wn_xuhuang: "渭南徐晃",
  wn_xuhuang_prefix: "渭南",
  wn_zhuying: "驻营",
  wn_zhuying_info: "一名角色的结束阶段，若其本回合未使用牌指定你为目标，你获得一个“驻”。",
  wn_chiyuan: "驰援",
  wn_chiyuan_info: "①出牌阶段，你可以交给任意名角色各一个“驻”。②有“驻”的角色受到伤害时，你可以弃置一张牌并选择一项：1.防止此伤害，移去其一个“驻”；2.对伤害来源造成1点伤害。",
  zj_zhugeliang: "魂诸葛亮",
  zj_zhugeliang_prefix: "魂",
  zj_jindao: "尽道",
  zj_jindao_info: "锁定技，摸牌阶段，你改为将手牌数摸至七张，展示任意张点数为7的手牌并回复等量体力，然后你观看牌堆顶的七张牌并以任意顺序置于牌堆顶或牌堆底。",
  zj_hanshi: "汉势",
  zj_hanshi_info: "出牌阶段每种牌名限一次，你使用牌时，可以展示一张同名牌，令此牌不计入使用次数且造成伤害+1。",
  zj_wuzhe: "武哲",
  zj_wuzhe_info: "限定技，出牌阶段，你可以声明至多两个锦囊牌名，若如此做，其他角色使用同名锦囊时，你可令此牌无效，然后于此牌置入弃牌堆后获得之。",
  zj_liuyuan: "刘渊",
  zj_jianxi: "兼习",
  zj_jianxi_info: "当你受到伤害后，你可以摸一张牌并展示之，然后你须声明一个描述中含有此牌牌名且不在场上的技能并获得之，否则令你使用基本牌的数值+1。",
  zj_zhaofu: "朝缚",
  zj_zhaofu_info: "锁定技，①若你的技能数小于一号位的技能数，你使用伤害牌后减少1点体力上限。②你受到大于1的伤害时，将此伤害改为1。",
  zj_tuicheng: "推诚",
  zj_tuicheng_info: "出牌阶段限一次，你可以将一张伤害牌当作无次数限制的基本牌对两名角色使用，然后令其中一名角色获得此牌。",
  zj_jiangwei: "智将姜维",
  zj_jiangwei_prefix: "智将",
  zj_juta: "据沓",
  zj_juta_info: "锁定技，①其他角色计算与你的距离+1。②其他角色使用牌指定你为目标时，须弃置你与其距离数张牌，否则此牌对你无效。③你使用【杀】结算完成后，失去〖据沓〗并获得〖不戢〗。",
  zj_buji: "不戢",
  zj_buji_info: "你获得或弃置牌后，可以展示其中一张牌并使用之；此牌结算完成后若你未因此牌造成伤害，你失去1点体力。",
  zj_linze: "麟择",
  zj_linze_info: "锁定技，若你的体力值与已损失体力值之差：不小于0，你视为拥有〖挑衅〗；不大于0，你视为拥有〖困奋〗。",
  zj_fuji: "扶稷",
  zj_fuji_info: "限定技，结束阶段，若一号位于其回合内因弃置失去过牌，你可以执行一个额外回合；你于此回合内杀死角色后，摸三张牌，恢复体力至体力上限并重置〖扶稷〗。",
  zj_zhonghui: "智将钟会",
  zj_zhonghui_prefix: "智将",
  zj_quanwei: "权威",
  zj_quanwei_info: "准备阶段，你可展示一张手牌，然后和一名其他角色议事。若结果与此牌相同，你可以依次跳过本回合剩下的任意个阶段，令至多两名角色恢复等量体力；否则你可以减少1点体力上限，获得与你意见不同角色的所有手牌。",
  zj_quanshu: "权术",
  zj_quanshu_info: "你议事结束或受到伤害后，可以摸X张牌并蓄谋。你的手牌上限+X（X为你场上的牌数）。",
  zj_quanqing: "权倾",
  zj_quanqing_info: "限定技，结束阶段，你可以对攻击范围内的所有角色发动〖权威〗，然后你可以变更势力并获得〖伐异〗。",
  zj_qiangyi: "戕异",
  zj_qiangyi_info: "主公技，你蓄谋后，获得一张【影】。你可以将【影】当作【玉石皆碎】对同势力角色使用。",
  xiahouhan: "夏侯含",
  zj_zhuhui: "烛晦",
  zj_zhuhui_info: "每轮限一次，一名男性角色的回合开始时，你可以令其选择一项：1.你本轮获得〖清俭〗，其选择交给你至少一张手牌；2.你本轮获得〖急救〗，其选择受到至少1点伤害。",
  zj_hanci: "寒慈",
  zj_hanci_info: "锁定技，一名角色获得技能后，你与其依次摸一张牌。",
  zj_xiahouhan: "结依夏侯含",
  zj_xiahouhan_ab: "夏侯含",
  zj_jieyi: "结依",
  zj_jieyi_info: "每轮开始时，你可以令一名男性角色交给你至少一张牌并成为你的“结依”角色，然后若其手牌数大于其以此法失去的牌数，你可失去1点体力，令〖理内〗本轮发动次数+1。",
  zj_linei: "理内",
  zj_linei_info: "每轮限一次，你的“结依”角色获得牌后，若其手牌数大于体力值，你可以获得其X张牌并令其恢复1点体力（X为其手牌数与体力值之差且至少为3）。",
  zj_tongxin: "同心",
  zj_tongxin_info: "锁定技，①你的摸牌阶段结束时，你的“结依”角色摸你此阶段摸牌数张牌。②你的“结依”角色的摸牌阶段结束时，你摸其此阶段摸牌数张牌。",
  zj_yaokehui: "姚柯回",
  zj_qiangdu: "羌督",
  zj_qiangdu_info: "其他角色的出牌阶段开始时，你可以摸一张牌并交给其一张牌，然后当其本回合首次使用指定唯一目标的【杀】或普通锦囊牌结算结算后，你可以无距离限制的视为使用此牌，然后若你未指定其指定的目标，你失去1点体力。",
  zj_huoyi: "霍弋",
  zj_zhongjue: "忠绝",
  zj_zhongjue_info: "锁定技，游戏开始时，你令一名其他角色使用牌无次数限制，获得其武将牌上一个的主公技。",
  zj_qingming: "请命",
  zj_qingming_info: "出牌阶段开始时，你可与“忠绝”角色议事，且你改为将上一张被使用或打出的牌作为议事牌。若你与其意见相同，你摸两张牌并获得〖烈伐〗，跳过本回合弃牌阶段。",
  zj_liefa: "烈伐",
  zj_liefa_info: "你可以视为使用一张不能指定你为目标的基本牌，然后选择一项：1.失去1点体力；2.弃置两张牌；3.失去〖烈伐〗。",
  zj_liuyin: "柳隐",
  zj_guguo: "固国",
  zj_guguo_info: "当你成为其他角色使用【杀】或锦囊牌的目标后，你可以摸一张牌；此牌结算完成后，若未造成伤害，你可弃置此牌使用者一张牌。",
  zj_zhugezhan: "智将诸葛瞻",
  zj_zhugezhan_prefix: "智将",
  zj_zhongwang: "众望",
  zj_zhongwang_info: "锁定技，准备阶段，你令所有其他角色选择是否将至少一张牌置于牌堆顶，然后你摸五张牌；回合结束时，若你本回合满足以下至少两种条件，本回合以此法盖牌的角色各摸两张牌，否则你与这些角色各失去1点体力：造成过伤害、未弃置过牌、手牌数全场最少。",
  zj_fuyin: "负荫",
  zj_fuyin_info: "锁定技，你的手牌上限+X（X为蜀势力角色数）；你成为【杀】的目标时，若为你本回合首次成为【杀】的目标，取消之，否则你本回合不能回复体力。",
  zj_zhaotong: "赵统",
  zj_zhaoguang: "赵广",
  zj_yizan: "翊赞",
  zj_yizan_info: "你可以将两张牌当作一张基本牌使用或打出。",
  zj_shuge: "戍阁",
  zj_shuge_info: "一名角色的回合结束时，你可以令X名蜀势力角色各摸一张牌（X为你本回合发动〖翊赞〗的次数）。",
  zj_zhengui: "镇归",
  zj_zhengui_info: "一名角色的回合结束时，若蜀势力角色本回合受到过伤害，你可以依次视为对伤害来源使用一张【决斗】。",
  zj_lukang: "智将陆抗",
  zj_lukang_prefix: "智将",
  zj_jueyan: "决堰",
  zj_jueyan_info: "出牌阶段限一次，你可以废除一种装备栏，并执行对应效果：武器栏，本回合使用牌无次数限制；防具栏：摸三张牌并跳过本回合弃牌阶段；坐骑栏：本回合使用牌无距离限制且不可被响应；宝物栏：本回合获得〖集智〗。",
  zj_huairou: "怀柔",
  zj_huairou_info: "出牌阶段，你可以重铸一张装备牌，若为本回合首次以此法重铸牌，你可以视为使用一张普通锦囊牌。",
  zj_zhugexu: "诸葛绪",
  zj_tuizhi: "退制",
  zj_tuizhi_info: "你可以展示并弃置一种颜色的所有手牌，然后视为使用一张基本牌；若此牌造成了伤害，你摸一张牌。",
  zj_qianjun: "牵军",
  zj_qianjun_info: "其他角色使用【杀】结算结束后，你可对其中一个目标使用一张【杀】；若其未因此受到伤害，你弃置其一张牌。",
  zj_guluo: "孤落",
  zj_guluo_info: "锁定技，你进入濒死时，若没有手牌，减少1点体力上限并回复体力至1点。",
  zj_hulie: "智将胡烈",
  zj_hulie_prefix: "智将",
  zj_chengxi: "乘袭",
  zj_chengxi_info: "你使用牌指定唯一目标时，可以令其展示所有手牌，然后其重铸至少一张牌。若如此做，你令此牌额外结算X次（X为其展示牌中未重铸的花色数）。",
  zj_zhaoe: "昭恶",
  zj_zhaoe_info: "限定技，你失去过牌的回合结束时，可以展示当前回合角色所有手牌，令至多X名角色对其使用至多X张【杀】（X为其展示的伤害牌数）。",
  zj_zhanghu: "智将张虎",
  zj_zhanghu_prefix: "智将",
  zj_cuijian: "摧坚",
  zj_cuijian_info: "出牌阶段限一次，你可以令一名其他角色展示所有手牌，交给你其中所有的防具牌和【闪】；若你未以此法得到牌，你摸两张牌。",
  zj_yuechen: "智将乐綝",
  zj_yuechen_prefix: "智将",
  zj_porui: "破锐",
  zj_porui_info: "每轮限两次，其他角色的结束阶段，你可以弃置两张牌并选择一名本回合失去过牌的角色，视为对其使用X+1张【杀】（X为其本回合失去过的牌数且至多为5）.",
  shinin_zhenji: "闪耀甄宓",
  shinin_zhenji_prefix: "闪耀",
  awaken_shinin_zhenji: "闪耀战姬甄宓",
  awaken_shinin_zhenji_prefix: "闪耀战姬",
  shinin_luoshen: "洛神",
  shinin_luoshen_info: "准备阶段，你可以进行判定，若结果为黑色，你获得该判定牌，然后你可重复此流程；你的手牌上限+X（X为你本回合因〖洛神〗获得的牌数）。",
  zhenji_moli: "魔力",
  zhenji_moli_info: `${get.poptip("rule_bianshenji")}（0/5），你于摸牌阶段外获得牌时，可以展示之，若为黑色，你获得1点魔力。结束阶段，你可${get.poptip({
    id: "shinin_yinchang",
    name: "吟唱",
    type: "character",
    info: "念出此技能对应的引文，方可执行后续效果"
  })}并${get.poptip("rule_bianshen")}。`,
  zhenji_moli_append: "<span style='font-family: yuanli'>冻住，不许走！</span>",
  shinin_jinghong: "惊鸿",
  shinin_jinghong_info: "称号与你相同的角色不因此技能获得牌后，你可消耗1点魔力令其判定，若结果为黑色，其获得此牌并重复此流程。你的黑色牌不计入手牌上限且无次数限制。",
  shinin_youlong: "游龙",
  shinin_youlong_info: "锁定技，你造成的伤害改为冰属性伤害。一名角色受到冰属性伤害后，直到其下个回合结束：其摸牌阶段少摸一张牌，计算与其他角色的距离+1，你摸牌阶段多摸一张牌，计算与其他角色的距离-1，且其不能响应你使用的牌。",
  shinin_haiyou: "海佑",
  shinin_haiyou_info: "称号与你相同的角色成为牌的目标时，你可以消耗1点魔力令此牌对其无效。",
  shinin_wuguotai: "闪耀吴国太",
  shinin_wuguotai_prefix: "闪耀",
  awaken_shinin_wuguotai: "闪耀战姬吴国太",
  awaken_shinin_wuguotai_prefix: "闪耀战姬",
  wuguotai_moli: "魔力",
  wuguotai_moli_info: `${get.poptip("rule_bianshenji")}（0/5）游戏开始时或你场上的牌变化时，你获得1点魔力。你失去牌时，你可${get.poptip("shinin_yinchang")}并${get.poptip("rule_bianshen")}。`,
  wuguotai_moli_append: "<span style='font-family: yuanli'>降临吧，属于我的真实姿态——</span>",
  shinin_fengshou: "凤守",
  shinin_fengshou_info: "你变身后，可以重新分配与你距离为1的角色的座次，然后消耗任意点魔力，令等量角色受到的火焰伤害+1。",
  shinin_susheng: "苏生",
  shinin_susheng_info: "称号与你相同的角色进入濒死时，你可以消耗任意点魔力，令其恢复等量体力和魔力，然后你摸等量张牌。",
  shinin_ruiji: "闪耀芮姬",
  shinin_ruiji_prefix: "闪耀",
  awaken_shinin_ruiji: "闪耀战姬芮姬",
  awaken_shinin_ruiji_prefix: "闪耀战姬",
  ruiji_moli: "魔力",
  ruiji_moli_info: `${get.poptip("rule_bianshenji")}（0/5）游戏开始时或你造成或受到伤害时，你获得1点魔力。你进入濒死或发动${get.poptip("dclingyin")}后，你可${get.poptip("shinin_yinchang")}并${get.poptip("rule_bianshen")}。`,
  ruiji_moli_append: "<span style='font-family: yuanli'>一天是哥布林，一辈子都是哥布林！</span>",
  shinin_shengcai: "圣裁",
  shinin_shengcai_info: "出牌阶段，你可消耗任意点魔力，将等量张手牌当无距离次数限制的【杀】使用，你以此法使用【杀】造成的伤害+X（X为消耗的魔力数）。",
  shinin_guanghui: "光辉",
  shinin_guanghui_info: "锁定技，所有变身状态的角色使用牌不能被非变身状态的角色响应；称号与你相同的角色造成伤害时，你获得1点魔力。",
  shinin_lvlingqi: "闪耀吕玲绮",
  shinin_lvlingqi_prefix: "闪耀",
  awaken_shinin_lvlingqi: "闪耀战姬吕玲绮",
  awaken_shinin_lvlingqi_prefix: "闪耀战姬",
  lvlingqi_moli: "魔力",
  lvlingqi_moli_info: `${get.poptip("rule_bianshenji")}（0/5）游戏开始时或你造成1点伤害时，你获得1点魔力。准备阶段，你可${get.poptip("shinin_yinchang")}并${get.poptip("rule_bianshen")}。`,
  lvlingqi_moli_append: "<span style='font-family: yuanli'>Starlight Ascension！</span>",
  shinin_henghui: "恒辉",
  shinin_henghui_info: "锁定技，你变身后，获得任意个效果并消耗等量魔力：1.使用【杀】的次数和造成的伤害+1；2.使用【杀】无距离限制且可选目标数+2；3.摸五张牌并弃置装备区里的所有牌。",
  shinin_moqi: "魔契",
  shinin_moqi_info: `每次变身限一次，出牌阶段，你可以消耗至少3点魔力并失去所有体力，然后令${get.poptip("shinin_henghui")}的所有数值翻倍。发动此技能后你保持变身状态直到结束阶段。`,
  shinin_dongwan: "闪耀董绾",
  shinin_dongwan_prefix: "闪耀",
  awaken_shinin_dongwan: "闪耀战姬董绾",
  awaken_shinin_dongwan_prefix: "闪耀战姬",
  dongwan_moli: "魔力",
  dongwan_moli_info: `${get.poptip("rule_bianshenji")}（0/5）游戏开始时或你于你的摸牌阶段外获得牌时，你获得1点魔力。结束阶段，你可${get.poptip("shinin_yinchang")}并${get.poptip("rule_bianshen")}。`,
  dongwan_moli_append: "<span style='font-family: yuanli'>马猴烧酒，很行！</span>",
  shinin_shengyan: "圣焰",
  shinin_shengyan_info: "锁定技，你使用牌额外结算一次，然后你消耗1点魔力或失去1点体力，对一名其他角色造成1点火焰伤害。",
  shinin_gongming: "共鸣",
  shinin_gongming_info: "你消耗1点魔力或失去1点体力后，你可以令与你称号相同的所有角色各摸X张牌（X为你此次变身期间发动此技能的次数）。",
  lord_goblin: "领主哥布林",
  lord_goblin_prefix: "领主",
  goblin: "哥布林",
  goblin_yibao: "疑暴",
  goblin_yibao_info: "锁定技，你造成或受到伤害后，令受伤角色摸三张牌；若你为伤害来源，随机废除受伤角色的一个装备栏，然后观看其手牌并获得其中两张牌。",
  goblin_rulin: "如麟",
  goblin_rulin_info: "锁定技，你对一名角色造成伤害时，若此伤害值不小于其体力值，防止之并减少其1点体力上限，然后你增加1点体力上限并回复1点体力。",
  goblin_kuangbao: "狂暴",
  goblin_kuangbao_info: "锁定技，你造成或受到伤害时，若伤害来源手牌数大于受伤角色此伤害+1，令造成伤害的牌不计入使用次数。",
  goblin_feisheng: "飞升",
  goblin_feisheng_info: "锁定技，一名角色的回合结束时，若你本回合受到了至少2点伤害，你减少2点体力上限并执行一个额外回合。",
  goblin_shiqiang: "恃强",
  goblin_shiqiang_info: "锁定技，你造成/受到伤害时，若受伤角色/伤害来源手牌数小于/大于你，此伤害+1。",
  goblin_lingruo: "凌弱",
  goblin_lingruo_info: "锁定技，你造成伤害后，获得受伤角色的一张牌。",
  zj_huangchong: "智将黄崇",
  zj_huangchong_prefix: "智将",
  zj_juxian: "据险",
  zj_juxian_info: "每回合限二十次，你失去牌时，可以失去1点体力防止之；你受到伤害时，若你手牌数为1，你可以防止之。",
  zj_juxian_append: "<span style='font-family: yuanli'>原无发动限制，为避免无意义的无限loop增加限制。</span>",
  zj_lijun: "励军",
  zj_lijun_info: "准备阶段，你可令任意名已受伤的角色各摸一张牌；结束阶段，若你本回合未弃置过牌，你可以回复1点体力。",
  zj_dingfeng: "智将丁奉",
  zj_dingfeng_prefix: "智将",
  zj_duanbing: "短兵",
  zj_duanbing_info: "你使用【杀】可以额外指定一名距离为1的角色为目标。你对距离为1的角色：使用的【杀】需要两张【闪】才能抵消；每回合首次造成伤害后摸一张牌。",
  zj_fenxun: "奋迅",
  zj_fenxun_info: "出牌阶段，你可选择一名其他角色，令你本回合计算与其的距离视为1。",
  zj_liaohua: "智将廖化",
  zj_liaohua_prefix: "智将",
  zj_dangxian: "当先",
  zj_dangxian_info: "锁定技，回合开始时，你执行一个出牌阶段并从弃牌堆中选择一张【杀】获得；你于非额定的出牌阶段使用牌无距离限制。",
  zj_fuli: "伏枥",
  zj_fuli_info: "限定技，你处于濒死状态时，可以恢复体力至2点并摸两张牌，然后于当前回合结束后执行一个仅有出牌阶段的回合。",
  wn_zhuling: "渭南朱灵",
  wn_zhuling_prefix: "渭南",
  wn_zhanyi: "战意",
  wn_zhanyi_info: "出牌阶段限一次，你可以失去1点体力并弃置一张基本或锦囊牌，令你本回合使用此类型的牌额外结算一次。",
  wxdl_caocao: "文心雕龙曹操",
  wxdl_caocao_prefix: "文心雕龙",
  wxdl_kejie: "克捷",
  wxdl_kejie_info: `每当你的摸牌阶段结束，若手牌数小于5，受伤角色可以对你发动一次${get.poptip("xiantu")}，然后你可将任意张【闪】重铸，令你本回合增加等量出杀次数。`,
  wxdl_hongqi: "洪起",
  wxdl_hongqi_info: "主公技，限定技，准备阶段，你可以视为使用一张【万箭齐发】，然后其他魏势力角色可以使用一张【杀】。",
  sm_chixia: "菲尔瓦娜·赤霞",
  sm_qiulin: "骅骝·璆琳",
  sm_zhuahuang: "爪黄·飞电",
  sm_zijian: "紫骍·子建",
  sm_jieying: "绝影·婕媖",
  sm_liaoyuan: "赤兔·燎原",
  sm_dailu: "的卢·黛露",
  sm_dayuan: "妲媛",
  sm_dayuan_info: "出牌阶段开始时，你可选择一名其他角色并弃置一张手牌，然后你与其各摸一张牌并回复1点体力，且本轮计算与彼此距离-1。",
  sm_tianwu: "天舞",
  sm_tianwu_info: "出牌阶段，你可消耗1点速度，令你本阶段使用的下一张基本牌额外指定目标角色的相邻角色为目标。",
  chixia_jipao: "疾跑",
  chixia_jipao_info: `${get.poptip("rule_bianshenji")}（0/5），你造成或受到伤害后，获得1点速度；你死亡时，若有至少3点速度，你从游戏外获得一件${get.poptip("sm_mabian")}并令一名角色装备之。`,
  chixia_jipao_append: "<span style='font-family: yuanli'>好好看着吧，这来自西域的———天之舞步！</span>",
  sm_hualiu: "华琉",
  sm_hualiu_info: "锁定技，其他角色计算与你的距离+X，你摸牌阶段摸牌数和你的手牌上限+X（X为你的速度且至少为1）。",
  sm_dianteng: "典滕",
  sm_dianteng_info: "每回合限一次，一名其他角色造成伤害后，你可消耗1点速度，令其选择至多两项：1.摸两张牌；2.回复1点体力；3.使用一张牌；4.重铸两张牌。然后你执行剩余项。",
  qiulin_jipao_append: "<span style='font-family: yuanli'>你的步伐有些乱了，请调整呼吸。</span>",
  sm_feidian: "妃殿",
  sm_feidian_info: "锁定技，①你的♥牌无距离次数限制限制且点数视为K。②你使用的♥牌不能被点数小于K的牌响应。",
  sm_xianxing: "先行",
  sm_xianxing_info: "锁定技，每轮开始时，你可与所有角色进行一次共同拼点。若你赢，你执行一个额外回合；若你没赢，你受到1点伤害并获得拼点牌中所有点数小于你的牌。",
  zhuahuang_jipao_append: "<span style='font-family: yuanli'>以魏武之名，驱驰吧！爪黄飞电！</span>",
  sm_zixin: "资馨",
  sm_zixin_info: "你于回合外获得牌后，可以展示并使用其中一张牌。",
  sm_zhiyin: "知音",
  sm_zhiyin_info: "一名角色的准备阶段，你可消耗1点速度并声明至多四种花色。若如此做，此回合结束时，每有一种被声明的花色：被使用过，你与当前回合角色各摸一张牌；未被使用，你失去1点体力。",
  zijian_jipao_append: "<span style='font-family: yuanli'>风的气息，today，似乎孕育着一首绝句。</span>",
  sm_jueying: "珏颖",
  sm_jueying_info: "锁定技，①你视为装备着【绝影】。②你受到伤害后，可以摸一张牌，然后你可令此技能本回合失效并视为使用一张基本牌。",
  sm_xuneng: "蓄能",
  sm_xuneng_info: "出牌阶段，你可消耗1点速度，令你本回合使用的下一张单目标牌数值+X（X为你已损失体力值）。",
  jieying_jipao_append: "<span style='font-family: yuanli'>任务完成。</span>",
  sm_chitu: "炽突",
  sm_chitu_info: "锁定技，你使用牌造成伤害时，若受伤角色：响应过此牌，此伤害+1；未响应过此牌，你获得其一张手牌。",
  sm_aozhan: "傲战",
  sm_aozhan_info: `出牌阶段限一次，你可消耗1点速度，然后选择一个技能获得并摸一张牌：${get.poptip("xinshensu")}${get.poptip("wushuang")}${get.poptip("dclima")}${get.poptip("jsrgguanjue")}。`,
  liaoyuan_jipao_append: "<span style='font-family: yuanli'>连我的尘烟都尝不到吗？</span>",
  sm_dilu: "滴露",
  sm_dilu_info: "锁定技，①你与其他角色互相计算距离时+1。②你不因使用失去牌后，可以视为使用一张无次数限制的基本牌。",
  sm_boxing: "薄幸",
  sm_boxing_info: `锁定技，①你成为其他角色使用牌的目标时，你进行判定：若结果不为♥，你弃置一张牌，然后其他角色可交给你一张牌。②若此技能发动次数不小于2，你失去此技能并获得${get.poptip("sm_chongji")}。`,
  sm_chongji: "冲迹",
  sm_chongji_info: "你使用单目标伤害牌/回复体力时，可以消耗1点速度，令此牌额外结算一次/令一名交给过你牌的角色回复等量体力并摸一张牌。",
  dailu_jipao_append: "<span style='font-family: yuanli'>……离我远点，会变得不幸的。</span>",
  ol_manchong: "将满宠",
  ol_manchong_prefix: "将",
  shen_diaochan: "神貂蝉",
  shen_diaochan_prefix: "神",
  meihun: "魅魂",
  meihun_info: "结束阶段或当你成为【杀】的目标后，你可以令一名其他角色交给你一张你声明的花色的手牌，若其没有则你观看其手牌然后弃置其中一张。",
  huoxin_control: "惑心",
  huoxin: "惑心",
  huoxin_info: "出牌阶段限一次，你可以展示两张花色相同的手牌并分别交给两名其他角色，然后令这两名角色拼点，没赢的角色获得1个“魅惑”标记。拥有2个或更多“魅惑”的角色回合即将开始时，该角色移去其所有“魅惑”，此回合改为由你操控。",
  boss_zhaoyun: "高达一号",
  boss_zhaoyun_ab: "神赵云",
  boss_zhaoyun_prefix: "神",
  boss_juejing: "绝境",
  boss_juejing2: "绝境",
  boss_juejing_info: "锁定技，摸牌阶段开始前，你跳过此阶段。当你得到牌/失去手牌后，若你的手牌数大于四/小于四，则你将手牌摸至四张/弃置至四张。",
  zhanjiang: "斩将",
  zhanjiang_info: "准备阶段开始时，如果其他角色的装备区内有【青釭剑】，你可以获得之。",
  zc26_shen_huangyueying: "26神黄月英",
  zc26_shen_huangyueying_prefix: "26|神",
  zc26_cangqiao: "藏巧",
  zc26_cangqiao_info: "每轮开始时，你可以获得游戏外或弃牌堆中的【断剑】、【水手服】、【庸驴】各至多一张；你使用上述牌时可以将手牌摸至体力上限。",
  zc26_shenxie: "神械",
  zc26_shenxie_info: "每回合限一次，以你为唯一目标的黑色牌结算后，你可以将场上一张装备牌当未以此法使用过的延时锦囊牌使用（均使用过后重置）；此类锦囊牌在判定区内同时有被转化的装备牌的效果。",
  zc26_huaxiu: "化朽",
  zc26_huaxiu_info: `出牌阶段限一次，你可以将一种“藏巧”装备牌效果修改为下述对应顺序的牌直到下回合开始：${get.poptip("zc26_zhuge")}、${get.poptip("zc26_bagua")}、${get.poptip("zc26_lingling")}。`,
  zc26_zhuge: "魂·诸葛连弩",
  zc26_zhuge_info: "你使用【杀】无次数限制且指定目标后，你可以令任意名死亡角色依次观看目标手牌并可以重铸其中一张牌。",
  zc26_zhuge_skill: "魂·诸葛连弩",
  zc26_zhuge_skill_info: "你使用【杀】无次数限制且指定目标后，你可以令任意名死亡角色依次观看目标手牌并可以重铸其中一张牌。",
  zc26_bagua: "魂·八卦阵",
  zc26_bagua_info: "当你需要使用或打出【闪】时，你可以进行一次判定，若结果为红色，视为使用或打出之；判定前你可以令一名死亡角色卜算3。",
  zc26_bagua_skill: "魂·八卦阵",
  zc26_bagua_skill_info: "当你需要使用或打出【闪】时，你可以进行一次判定，若结果为红色，视为使用或打出之；判定前你可以令一名死亡角色卜算3。",
  zc26_lingling: "軨軨",
  zc26_lingling_info: "准备阶段，你须对一名角色造成1点雷电伤害；每轮结束时，所有死亡角色同时秘密选择上家或下家，然后按顺序（死亡由前到后）依次移动此牌至选择的角色对应区域内。",
  zc26_lingling_skill: "軨軨",
  zc26_lingling_skill_info: "准备阶段，你须对一名角色造成1点雷电伤害；每轮结束时，所有死亡角色同时秘密选择上家或下家，然后按顺序（死亡由前到后）依次移动此牌至选择的角色对应区域内。",
  le_shen_jiaxu: "神贾诩",
  le_shen_jiaxu_prefix: "神",
  jxlianpo: "炼魄",
  jxlianpo_info: "锁定技。①若场上最大阵营为：反贼，其他角色的手牌上限-1，所有角色使用【杀】的次数上限和攻击范围+1；主忠，其他角色不能对其以外的角色使用【桃】。若有多个最大阵营，其他角色死亡后，来源摸两张牌或回复1点体力。②每轮开始时，你展示一张未加入游戏或已死亡角色的身份牌，本轮视为该身份对应阵营的角色数+1。",
  jxzhaoluan: "兆乱",
  jxzhaoluan_info: "限定技。一名角色死亡前，若其此次进入过濒死状态，你可以取消之，令其加3点体力上限并失去所有非锁定技，回复体力至3点，摸四张牌。然后你获得如下效果：出牌阶段，你可以令一名成为过你〖兆乱〗目标的角色减1点体力上限，然后对一名此阶段未以此法选择过的角色造成1点伤害。",
  mark_shen_machao: "骏骊神马超",
  mark_shen_machao_prefix: "骏骊|神",
  mark_shouli: "狩骊",
  mark_shouli_info: `①游戏开始时，所有其他角色随机获得1枚“狩骊”（包含3枚“${get.poptip({
    id: "shouli_jun",
    name: "骏",
    type: "character",
    info: `①若你持有的“骏”数量大于：
			<br><li>0，你计算与其他角色的距离-1；
			<br><li>1，摸牌阶段你额外摸一张牌；
			<br><li>2，你使用【杀】指定目标时，令其本回合非锁定技失效。
			<br>②当你受到属性伤害或【南蛮入侵】、【万箭齐发】造成的伤害时，你将所有“骏”移动至你上家。
			`
  })}”和4枚“${get.poptip({
    id: "shouli_li",
    name: "骊",
    type: "character",
    info: `①若你持有的“骊”数量大于：
			<br><li>0，其他角色计算与你的距离+1；
			<br><li>1，摸牌阶段你额外摸一张牌；
			<br><li>2，你造成或受到的伤害视为雷电伤害；
			<br><li>3，你造成或受到的伤害+1。
			<br>②当你受到属性伤害或【南蛮入侵】、【万箭齐发】造成的伤害时，你将所有“骊”移动至你下家。
			`
  })}”）②每回合各限一次，你可以选择一项：1.移动一名其他角色的所有“${get.poptip("shouli_li")}”至其的上家或下家，视为使用或打出一张【闪】；2.移动一名其他角色的所有“${get.poptip("shouli_jun")}”至其的上家或下家，视为使用或打出一张无距离次数限制的【杀】。`,
  mark_shouli_append: "<span style='font-family: yuanli'>狩猎开始……</span>",
  mark_shouli_jun: "骏",
  get mark_shouli_jun_info() {
    return lib.poptip.getInfo("shouli_jun");
  },
  mark_shouli_li: "骊",
  get mark_shouli_li_info() {
    return lib.poptip.getInfo("shouli_li");
  },
  mark_hengwu: "横骛",
  mark_hengwu_info: "锁定技，有“骏”/“骊”的角色获得“骏”/“骊”后，你摸X张牌（X为其拥有的“骏”/“骊”数）。",
  sp_sm_shen_machao: "SP赛马神马超",
  sp_sm_shen_machao_prefix: "SP|赛马|神",
  sm_mabian: "马鞭",
  sm_mabian_info: `若称号为“赛马娘”的角色因${get.poptip("rule_bianshenji")}将此牌置入你的装备区，你视为拥有其武将牌上的第一个技能直到失去此牌。`,
  sm_kulian: "酷练",
  sm_kulian_info: `锁定技，游戏开始时，你令所有角色依次装备牌堆中的一张坐骑牌，并从游戏外将一张${get.poptip("sm_mabian")}置入装备区，然后开始${get.poptip({
    name: "比赛",
    id: "sm_PrettyDerby",
    info: `
			1.每轮开始时，随机亮出两张${get.poptip("sm_PerttyDerby_reward")}，于本轮结束时执行效果<br>
			2.全部${get.poptip("sm_PerttyDerby_reward")}亮出后，重新洗切奖励牌堆<br>
			3.装备有${get.poptip("sm_mabian")}的角色称为“选手”<br>
			4.装备坐骑的“选手”使用牌无距离限制，每回合首次对其他“选手”造成伤害后摸一张牌<br>
			5.失去${get.poptip("sm_mabian")}的“选手”进行“退赛”，不再视为“选手”<br>
			6.快乐第一，比赛第二
		`,
    type: "character"
  })}。`,
  sm_kulian_reward: "赛马奖励",
  get sm_kulian_reward_info() {
    return lib.poptip.getInfo("sm_PerttyDerby_reward");
  },
  sm_lema: "乐马",
  sm_lema_info: "每回合限一次，你可以视为使用一张基本牌，然后摸X张牌（X为场上坐骑牌数量且至少为1）。",
  sm_chaoxuan: "潮炫",
  sm_chaoxuan_info: `锁定技，一次${get.poptip("sm_PrettyDerby")}结束时，你执行一次${get.poptip({
    id: "sm_PerttyDerby_reward",
    name: "赛马奖励",
    info: `
			1.受到伤害唯一最多<br>
			<li><span style='font-family: yuanli'>奖励：回复全部体力</span><br>
			2.手牌数唯一最多<br>
			<li><span style='font-family: yuanli'>奖励：手牌上限改为体力上限</span><br>
			3.体力值唯一最高<br>
			<li><span style='font-family: yuanli'>奖励：增加1点体力上限</span><br>
			4.装备区牌数唯一最多<br>
			<li><span style='font-family: yuanli'>奖励：获得一张其他角色的装备牌</span><br>
			5.击杀数唯一最多<br>
			<li><span style='font-family: yuanli'>奖励：执行一个仅有出牌阶段的额外回合</span><br>
			6.使用牌数唯一最多<br>
			<li><span style='font-family: yuanli'>奖励：摸五张牌</span><br>
			7.造成伤害唯一最多<br>
			<li><span style='font-family: yuanli'>奖励：使用【杀】造成伤害+1</span>
		`,
    type: "character"
  })}。`,
  sm_wandou: "玩斗",
  sm_wandou_info: "一名角色“退赛”时，你可令其将体力值调整至1点。",
  sm_shen_machao: "赛马神马超",
  sm_shen_machao_prefix: "赛马|神",
  sm_tuanlian: "团练",
  sm_tuanlian_info: "锁定技，游戏开始时/你每回合首次造成或受到伤害后，你随机获得五/一张名字或称号包含“马”的武将牌，称为“赛马”。",
  sm_jingji: "竞激",
  sm_jingji_info: `①你可弃置一张“赛马”，发动其牌面上的第一个技能。②你可移去一张“赛马”，视为使用以下牌：${get.poptip("sm_prettyDerby")}；任意基本牌，数值+1；任意普通锦囊牌，摸一张牌。`,
  sm_prettyDerby: "赛马",
  sm_prettyDerby_info: "锁定技，你计算与其他角色的距离-1，其他角色计算与你的距离+1。",
  sm_kuangchi: "狂驰",
  sm_kuangchi_info: "你杀死一名角色后，可以交给其一张“赛马”代替其一张武将牌并令其复活，其胜利条件改为与你一致。"
};
const characterIntro = {
  pe_xiangyu: "项羽（公元前232年—公元前202年），名籍，字羽，泗水下相（今江苏宿迁）人。秦末汉初著名军事家、政治家，西楚霸王。项羽出身楚国贵族，世代为楚将。少时学书不成，学剑又不成，后学兵法，略知其意。秦二世元年（公元前209年），随叔父项梁于吴中起兵反秦，屡建战功。巨鹿之战中，项羽率军破釜沉舟，大败秦军主力，威震诸侯，被推为诸侯上将军。秦亡后，自立为西楚霸王，定都彭城，分封诸侯。此后与刘邦展开长达四年的楚汉战争，前期屡胜，但因刚愎自用、不善用人，渐失优势。汉高祖五年（公元前202年），被刘邦联军围于垓下，夜闻楚歌四起，以为楚地尽失，遂率八百骑突围至乌江。乌江亭长劝其渡江东山再起，项羽以‘无颜见江东父老’为由拒渡，自刎而死，年仅三十岁。司马迁在《史记》中将其列入本纪，赞其‘力拔山兮气盖世’，亦叹其‘自矜功伐，奋其私智而不师古’，为千古悲情英雄。",
  shangguanwaner: "上官婉儿（664年－710年7月21日），复姓上官，小字婉儿，陕州陕县（今河南三门峡）人。 唐代著名女官、诗人、政治家，有“巾帼宰相”之名。上官婉儿出身官宦世家，祖父上官仪为唐初宰相，因替高宗起草废后诏书得罪武则天，全家获罪，尚在襁褓中的婉儿随母郑氏没入掖庭为奴。婉儿天性聪慧，善文章，明习吏事，十四岁时为武则天召见，当场命题作文，须臾而成，且文意通畅、词藻优美，武则天大悦，遂免其奴婢身份，命其掌管宫中诏命，自此参与机要，政务表奏多出其手。中宗复位后，上官婉儿深得信任，专掌制诏，进拜婕妤，其母郑氏亦封沛国夫人。景龙年间，婉儿开府置属官，品秩同三品，朝中文士竞相投其门下，她也以诗文引领当时文坛风气，与李峤、崔融、苏味道等并称“文章四友”，曾代朝臣赋诗和作，传世名篇颇多。然婉儿身处宫廷权力漩涡，先后周旋于武氏、韦氏之间。景龙四年（710年），唐中宗驾崩，韦后欲效武则天称帝，婉儿草拟遗诏时，虽暗引相王李旦辅政以自保，然六月二十日，临淄王李隆基发动唐隆政变，率军入宫诛杀韦后及其党羽，婉儿执烛率宫人迎接，并出示其与太平公主所拟遗诏以示投诚，然李隆基以其“此婢妖淫，渎乱宫闱”为由，下令斩于旗下，时年四十七岁。开元初年，唐玄宗虽杀婉儿，却惜其才华，令宰相张说编录其诗文，成《上官昭容集》二十卷传世。两《唐书》有传，称其“聪明善属文，明习吏事，则天时每进文章，则天称善”。",
  wuzetian: "武则天（624年—705年12月16日），并州文水（今山西文水）人，中国历史上唯一的正统女皇帝（690年—705年在位）。唐高宗李治皇后，武周开国君主。<br>武则天出身官宦之家，父武士彟为唐朝开国功臣。十四岁入宫，为唐太宗才人，赐号“武媚”。太宗驾崩后，依制入感业寺为尼。高宗即位后，召其还宫，先封昭仪，永徽六年（655年）立为皇后。高宗晚年多病，武则天逐渐掌握朝政，与高宗并称“二圣”。弘道元年（683年）高宗驾崩，中宗李显继位，武则天以太后身份临朝称制。嗣圣元年（684年），废中宗为庐陵王，改立睿宗李旦，实则大权独揽。天授元年（690年），武则天正式称帝，改国号为“周”，定都洛阳（称神都），成为中国历史上唯一的女皇帝。<br>在位期间，武则天打击关陇门阀，提拔庶族人才，首创殿试制度，完善科举，任用狄仁杰、姚崇等名臣；重视农业生产，推行均田制，轻徭薄赋，使社会经济持续发展；军事上设立北庭都护府，巩固西域边防。但晚年宠信张易之、张昌宗兄弟，朝政渐趋腐败；任用酷吏周兴、来俊臣，大行告密之风，造成一定政治恐怖。<br>神龙元年（705年），宰相张柬之等发动“神龙政变”，逼其退位，复唐国号。同年十一月，武则天病逝于上阳宫，享年八十二岁。遗诏去帝号，称“则天大圣皇后”，与高宗合葬于乾陵，立无字碑，功过任由后人评说。<br>武则天是中国古代少有的女性政治强人，其治国才能不逊于明君，但统治手段严苛且争议极大。她既是唐代盛世的承继者与破坏者，又是开创者，其一生在史书、戏曲、影视作品中反复演绎，成为中国文化中兼具权力、智慧与悲剧色彩的传奇符号。",
  ym_jsrg_diaochan: "话说董卓伏诛后，貂蝉暗忖:“吕布犹在，必为祸端。遂仍留其侧，以柔情惑其心志。及吕布败亡，貂蝉救国之志犹然，叹汉室危颓，便随关羽千里投刘。刘备敬其志节，使居甘、糜二夫人处，名为协理内务。实蓄忠坚少女、暗织情报罗网。耳目渐布四方。赤壁战后。孙权进妹联姻，貂蝉闻之笑日:“此美人计也。”后东吴欲谋荆州，遣周善乔装潜入，欲以密函诱孙尚香归吴。貂蝉于荆州城内早布眼线，围杀周善，截获密函呈与刘备。览毕、貂蝉曰:“孙权欲袭主公，又恐其妹为害，遂以探母为由，召夫人回乡。”刘备深以为然，遂依貂蝉之计留孙尚香为质。孙权闻讯掷剑长叹。至关羽北伐襄樊、中原震动之时，江东诸将屡请袭荆，权终叹:“吾妹在彼，投鼠须忌器。欲知后事如何，且听下回分解。",
  baiqi: "白起（？—公元前257年），秦国白氏，名起，郿邑（今陕西眉县常兴镇白家村）人。战国时期杰出的军事家、“兵家”代表人物。熟知兵法，善于用兵，交好秦宣太后，和穰侯魏冉的关系很好。辅佐秦昭王，屡立战功。伊阕之战，大破魏韩联军；伐楚之战，攻陷楚都郢城。长平之战，重创赵国主力。担任秦军主将30多年，攻城70余座，为秦国统一六国做出了巨大的贡献，受封为武安君。功高震主，得罪应侯，接连贬官。秦昭襄王五十年（前257年），赐死于杜邮。作为中国历史上继孙武、吴起之后又一个杰出的军事家、统帅，白起与廉颇、李牧、王翦并称为战国四大名将，并且被列为战国四大名将之首，名列武庙十哲。",
  huamulan: "花木兰（生卒年不详），中国古代著名巾帼英雄，其故事源自北朝民歌《木兰辞》，因替父从军的传奇经历而家喻户晓，被后世尊为忠孝两全的典范，唐代追封为“孝烈将军”。花木兰的故事最早见于南北朝时期叙事诗《木兰辞》，最初收录于南朝陈的《古今乐录》。诗中讲述北魏时期（一说隋朝），北方游牧民族柔然南下侵扰，朝廷征兵，每户出一丁。木兰之父年迈，家中无长兄，她毅然女扮男装，代父从军，隐瞒身份征战十余载，历经“将军百战死，壮士十年归”的艰苦岁月，最终凯旋。因战功卓著，天子欲封其为尚书郎，木兰辞官不受，只求“愿驰千里足，送儿还故乡”，回归故里与亲人团聚。花木兰其人其事在正史中并无明确记载，其姓氏、籍贯、生卒年历来众说纷纭。明代徐渭在杂剧《雌木兰替父从军》中首次赋予其“花”姓，此后“花木兰”之名流传最广。另有姓魏、朱、韩、任等说法；故里亦有河南商丘虞城县、湖北黄陂、安徽亳州、陕西延安等不同说法。元代碑文出现了木兰被召入后宫、以死相拒、追谥“孝烈”的结局，明清以来戏曲、小说等不断丰富其故事内容。花木兰的故事被改编为京剧、豫剧、电影、电视剧、动画片等各类文艺作品，影响力遍及海内外，成为中国巾帼英雄的文化符号之一。",
  yangyuhuan: "杨玉环（719年—756年7月15日），号太真，蒲州永乐（今山西永济）人，唐朝宫廷音乐家、舞蹈家，中国古代四大美女之一（“羞花”之典即源于她）。杨玉环出身弘农杨氏，高祖父杨汪为隋朝上柱国，父杨玄琰曾任蜀州司户。她性格婉顺，姿质丰艳，擅长歌舞，通晓音律。开元二十二年（734年），嫁与唐玄宗第十八子寿王李瑁为妃。开元二十八年（740年），唐玄宗命其出家为女道士，道号“太真”，天宝四载（745年）正式册封为贵妃，史称杨贵妃。杨贵妃得宠后，其兄杨国忠及三位姐姐皆获显贵，杨氏一门权倾天下。安史之乱爆发后，天宝十五载（756年），唐玄宗携杨贵妃逃往蜀中，途经马嵬驿（今陕西兴平西），随行禁军哗变，杀死杨国忠，并逼唐玄宗赐死杨贵妃。杨贵妃被缢杀于佛堂，时年三十八岁（一说死于乱军之中，或流落民间）。白居易作《长恨歌》，将其与唐玄宗的爱情故事传颂千古。杨贵妃生平无显著政治作为，但因“一骑红尘妃子笑，无人知是荔枝来”等典故，成为后世文学、艺术作品中盛唐气象与帝王悲剧的经典象征。",
  xiahoulan: "夏侯岚，游卡桌游《三国杀阵面对决》中虚构的人物。原为刘备之女，在长坂坡之战中被曹纯掳走，然后将其交给夏侯渊抚养并改名为夏侯岚。长大后嫁给了曹纯的儿子曹演，后来夏侯子弟被迫害时，跟随夏侯霸入蜀。",
  lishimin: "李世民（599年1月23日—649年7月10日），即唐太宗，陇西成纪（今甘肃秦安）人，唐朝第二位皇帝，杰出的政治家、战略家、军事家、书法家、诗人。名字取“济世安民”之意。李世民出身关陇贵族，父为唐高祖李渊，母为窦皇后。少年从军，曾于雁门关解救被突厥围困的隋炀帝。隋末天下大乱，他力劝并辅佐李渊于晋阳起兵反隋，率军攻克长安，为唐朝建立立下首功。唐建后封秦王，先后指挥平定薛仁杲、刘武周、王世充、窦建德等割据势力，为统一全国立下赫赫战功，被封前所未有的天策上将，地位在王公之上。武德九年（626年）六月初四，李世民发动玄武门之变，射杀太子李建成、齐王李元吉，李渊被迫立其为太子，旋传位于他。次年改元贞观。李世民在位二十三年，开创“贞观之治”，对内以隋亡为戒，虚心纳谏，重用魏徵、房玄龄、杜如晦等贤臣；推行均田制、租庸调制，轻徭薄赋，完善三省六部制和科举制。对外开疆拓土，攻灭东突厥与薛延陀，征服高昌、龟兹、吐谷浑，设立安西四镇，被尊为“天可汗”。晚年著有《帝范》十二篇教诫太子李治，并自评一生功过。贞观二十三年（649年）五月，病逝于终南山翠微宫，享年五十二岁，庙号太宗，谥号文皇帝，葬于昭陵。",
  sm_chixia: "传承自异世界的光辉姓名与竞跑能力，自久以来与人类共存。她们继承名留青史的异世界灵魂，身上长着“尾巴和耳朵”，拥有超越人类的奔跑能力，以奔跑为命运。",
  sm_qiulin: "传承自异世界的光辉姓名与竞跑能力，自久以来与人类共存。她们继承名留青史的异世界灵魂，身上长着“尾巴和耳朵”，拥有超越人类的奔跑能力，以奔跑为命运。",
  sm_zhuahuang: "传承自异世界的光辉姓名与竞跑能力，自久以来与人类共存。她们继承名留青史的异世界灵魂，身上长着“尾巴和耳朵”，拥有超越人类的奔跑能力，以奔跑为命运。",
  sm_zijian: "传承自异世界的光辉姓名与竞跑能力，自久以来与人类共存。她们继承名留青史的异世界灵魂，身上长着“尾巴和耳朵”，拥有超越人类的奔跑能力，以奔跑为命运。",
  sm_jieying: "传承自异世界的光辉姓名与竞跑能力，自久以来与人类共存。她们继承名留青史的异世界灵魂，身上长着“尾巴和耳朵”，拥有超越人类的奔跑能力，以奔跑为命运。",
  sm_liaoyuan: "传承自异世界的光辉姓名与竞跑能力，自久以来与人类共存。她们继承名留青史的异世界灵魂，身上长着“尾巴和耳朵”，拥有超越人类的奔跑能力，以奔跑为命运。",
  sm_dailu: "传承自异世界的光辉姓名与竞跑能力，自久以来与人类共存。她们继承名留青史的异世界灵魂，身上长着“尾巴和耳朵”，拥有超越人类的奔跑能力，以奔跑为命运。",
  shinin_wuguotai: "孙坚的妻子，武烈皇后。在一次偶然的机会下成为闪耀战姬，排行老大。",
  awaken_shinin_wuguotai: "吴国太，被选中成为草元素勇者，拥有控制森林和风之呼吸的力量，可以通过融合草元素对敌人进行攻击，定位为辅助。",
  shinin_lvlingqi: "吕布与严夫人所生的女儿。在一次偶然的机会下成为闪耀战姬，排行老二。",
  awaken_shinin_lvlingqi: "吕玲绮的外表是五个姐妹中最成熟的一位，战斗时最为飒爽，为了获得胜利不择手段。其手持的魔法方天戟。可以链接来自于恶魔迪鲁达的力量，每一次使用。其心智都会受到侵蚀。",
  shinin_zhenji: "甄姬，曹丕之妻，曹叡之母。在一次偶然的机会下成为闪耀战姬，排行老三。",
  awaken_shinin_zhenji: "甄姬化身为海的女儿，学会了冰霜魔法，最喜欢走下路打辅助，可以帮队友洛神和加盾。",
  shinin_dongwan: "董绾，原为袁术妾室，生性善妒。在一次偶然的机会下成为闪耀战姬，排行老四。",
  awaken_shinin_dongwan: "董绾在五个姐妹中排行老四，外热内冷，个性活泼，大开大合，年纪偏小。且带有一点疯批属性，使用红色火属性能量。",
  shinin_ruiji: "原为孙权长子孙登之妾，芮玄之女芮氏。在一次偶然的机会下成为闪耀战姬，排行老五。",
  awaken_shinin_ruiji: "在闪耀战姬中。芮姬为五姊妹之五为年纪最小者，掌控光之魔法之力，性格活泼、坚韧、可爱。",
  goblin: "哥布林是西方奇幻传说中的一种类人生物。哥布林通常的设定为：暗绿色皮肤的小矮人，四肢不强壮，长着红眼睛和尖耳朵，毛发较少或无毛发，身着肮脏的褐色破衣烂衫，手持武器通常为木棍或普通刀剑。个性贪婪又卑劣，性格倾向邪恶、狡猾而善于诈欺。它们主要生活在黑暗深处的地下世界，有着独特的行动秩序与体系。虽然文化发展程度称不上高，但在采矿与建造地下建筑物上有着独到的见解，个体战斗能力弱小，但会以大批群体行动补其不足。一般被各游戏当作初级小怪的素材。 ",
  lord_goblin: "领主哥布林（哥布林大王），哥布林族的最高统治者，拥有无上的权威。通常从普通哥布林聚落中进化而来，个体体型较大，身体力量较强，使用更为强大的武器，还有一定领袖能力。",
  xiahouhan: "夏侯含，三国杀线下产品《山河煮酒：绝代智将》虚构人物，设定上为夏侯渊侄孙女。",
  makang: "马抗，马翼的儿子，马超是其堂兄弟，马岱是其兄弟，马米科尼扬家族的始祖。马抗等人在马翼的带领下，由凉州出发，到达波斯。后大秦国皇帝的使节劝告波斯国王交出马抗一干人等，阿尔达希尔一世于是将马抗等人送至属国亚美尼亚，并要求国王特尔达特二世妥为收留，于是马抗便在亚美尼亚扎下根来，逐渐凭借战功赢得国王的欢心，并被赐姓为马米科尼扬。",
  Ardashir: "阿尔达希尔一世（Ardashir I ，? -241年），古代波斯萨珊帝国的创建者，自称“众王之王”。阿尔达希尔一世约于208年继位为伊朗法尔斯省内的小邦君主后，逐渐击灭各邦，扩大地盘。224年，阿尔达希尔一世打败并杀死帕提亚王，推翻了帕提亚帝国。226年，阿尔达希尔一世在泰西封加冕为“众王之王”，建立了中央集权的帝国。此时，罗马与贵霜两帝国都处于衰乱时期，232年，阿尔达希尔一世逼迫罗马皇帝乞和后，吞灭了亚美尼亚。不久，又大举东征，几年之内夺占花剌子模、粟特、吐火罗等大片土地，贵霜皇帝遣使称降。",
  Macrinus: "马尔库斯·欧佩里乌斯·马克里努斯（拉丁文：Marcus Opellius Macrinus），（约165年——218年6月8日），罗马帝国第二十四任皇帝，217年－218年在位。",
  kaisa: "盖乌斯·尤利乌斯·恺撒（Gaius Julius Caesar，公元前100年7月13日—公元前44年3月15日），史称恺撒大帝，又译盖厄斯·儒略·凯撒、加伊乌斯·朱利叶斯·凯撒等，罗马共和国末期杰出的军事统帅、政治家、军事家、历史学家，并且以其优越的才能成为了罗马帝国的奠基者。",
  zhugexu: "诸葛绪（生卒年不详），字佑明，琅邪阳都（今山东省沂南县）人。西晋大臣。以门荫入仕曹魏，历任泰山太守，打退东吴孙峻进攻。后迁雍州刺史，参与魏灭蜀之战，负责牵制姜维退路，之后其部队被时任镇西将军的钟会收编。西晋建立后，册封乐安亭侯（辟雍碑），历任太常卿、卫尉卿等官。",
  zhaotong: "赵统，赵云长子，生卒年不详。常山真定（今为河北正定）人，陈寿在正史《三国志》中记载赵云去世后，赵统袭爵永昌亭侯，官至蜀汉虎贲中郎督，加行领军。",
  zhaoguang: "赵广（？—263年），三国时期蜀汉牙门将，赵云的次子，赵统之弟。随姜维前往沓中，官拜牙门将。曹魏司马氏派五路大军伐蜀时，随大将军姜维与魏兵战于疆川口，姜维败绩还守剑阁，赵广于沓中战死。",
  liuyin: "柳隐（生卒年不详），字休然，蜀郡成都（今四川成都）人，三国时蜀汉武将，年轻时与同乡杜祯、柳伸并知名。多次跟随大将军姜维北伐，既能参赞军事，又能冲阵杀敌，勇略冠军。钟会伐蜀时，柳隐驻守黄金围，钟会包围柳隐却不能攻下，后邓艾袭取成都，刘禅投降，柳隐接到命令，方才归降钟会。西晋时，柳隐担任西河太守，后以年老辞职，求回蜀地。后卒于家中，享年八十岁。",
  huoyi: "霍弋，字绍先，南郡枝江（今湖北省枝江市）人，三国时期蜀汉至西晋初时将领，蜀汉将领霍峻之子。刘备时为太子舍人。后主登基为谒者。诸葛亮北驻汉中时用为丞相府记室，诸葛亮死后为黄门侍郎，刘禅立太子后为中庶子。尽言规谏太子，甚为得体。后永昌郡蛮夷作乱，刘禅以霍弋领永昌太守，率军讨伐，斩其豪帅，郡界宁静之后迁监军翊军将军，领建宁太守，统南中诸郡。景耀六年（263年），进号安南将军。邓艾偷袭阴平，霍弋想率军救援成都，但刘禅以成都已有准备，不准，后刘禅投降，霍弋在得知司马氏善待刘禅后，方才率领南中六郡投降。降晋后仍为南中都督，平定交阯、日南、九真三郡，功封列侯。",
  yaokehui: "姚柯回为汉代烧当羌首领姚迁郍后裔，世袭统领赤亭羌部。东汉建安年间率部活跃于陇西地区，曾助魏军掎制蜀汉姜维于沓中。据文献记载，其在曹魏景元四年（263年）魏灭蜀战役中助魏将绊姜维于沓中，以功假绥戎校尉、西羌都督，奠定姚氏家族在西北羌族中的统治地位。",
  liuyuan: "刘渊（252年/253年－310年8月29日），字元海，新兴郡（今山西忻州）人，一说西河隰城（今山西汾阳）人。匈奴族。东晋十六国时期汉国开国皇帝（304年－310年8月29日在位），军事统帅。匈奴冒顿单于之后（一说为刘氏伪冒之称），南匈奴左贤王刘豹之子。刘渊曾师从汉儒习经，尤好《春秋左氏传》、孙吴兵法。咸熙年间，作为侍子留居洛阳。刘豹去世后，继任左部帅。历任北部都尉、离石将兵都尉、五部大都督等职，颇能团结匈奴五部部众。后被成都王司马颖召往麾下供职，眼见晋室内斗、局势混乱，遂于建武元年（304年）在离石起兵反晋，称大单于。后迁居左国城（今山西离石东北），改称汉王，建国号“汉”，改元元熙。期间屡败晋军进讨，但败于晋并州刺史刘琨。后进据河东，攻占多地，吸引王弥、石勒、汲桑及鲜卑陆逐延等归降，声势大增。元熙五年（308年），称汉帝，次年徙都平阳。此后一方面健全统治机构，加强集权，并设单于台管理“六夷”事务；另一方面派兵大举进攻洛阳、宜阳等地，四处扩张势力。河瑞二年（310年），刘渊病逝，在位七年。谥号光文皇帝，庙号高祖，葬于永光陵。其侄刘曜即位后，改国号为赵，史称前赵。",
  houxuan: "侯选，河东人，东汉末年凉州军阀之一。建安十六年，同韩遂、马超联合，起兵反抗曹操，失败后入汉中依附张鲁，曹操平定汉中后，侯选投降，被封官赐爵。",
  chengyi: "成宜（？—211年），东汉末年凉州军阀之一。建安十六年，同韩遂、马超联合，起兵反抗曹操。同年九月，曹操发动渭南大决战，成宜战死。",
  yangqiú: "杨秋（生卒年不详），汉末三国时期魏国将领。建安十六年，同韩遂、马超联合，起兵反抗曹操。参加潼关之战，兵败于渭南，投降丞相曹操，成为魏国名将。魏文帝曹丕称帝后，出任冠军将军、畤乡侯。参与征讨郑甘、卢水、平定关中，官至讨寇将军，册封临泾侯，后以寿终。",
  pe_que: "曲阿小将，《三国演义》虚构人物。太史慈神亭酣战孙策时，独自抵挡程普等十二将。",
  yj_caocaoyuanshao: "请分别查看「曹操」和「袁绍」的武将介绍。",
  hs_caohuan: "曹奂（246年～302年），原名曹璜，以名讳难避，改名奂，字景明，三国时期曹魏末代皇帝。魏武帝曹操之孙，燕王曹宇之子，奉魏明帝曹叡之祀。曹奂出生于燕王宫。甘露三年（258年），被封为常道乡公。甘露五年（260年）五月，魏帝曹髦被杀，司马昭与众臣商议，立曹奂为帝，同年六月初二曹奂即皇帝位，改年号为景元。咸熙元年（264年），以曹奂为皇帝的魏国朝廷，宣布废除屯田官，以均政役。次年，司马炎篡夺魏国政权，建立西晋，史称晋武帝，魏国自此灭亡。十一月十七日，晋武帝封曹奂为陈留王。太安元年（302年），曹奂去世，谥号为元。",
  hs_liuxuan: "刘璿（224年―264年），字文衡，涿郡涿县（今河北涿州）人，三国时期蜀汉宗室，蜀汉昭烈帝刘备长孙，后主刘禅长子，北地王刘谌长兄，母王贵人。刘璿生于建兴二年（224年），延熙元年（238年），立为太子。蜀汉灭亡后，魏将邓艾拜刘璿为奉车都尉。魏咸熙元年（264年），邓艾被收，钟会举蜀地作乱，姜维打算借钟会之手复国，但不久后钟会起事之事泄漏，钟会、姜维同时被杀，刘璿也被乱兵杀害，时年四十一岁。",
  hm_bairao: "白绕（生卒年不详），东汉末年黑山军首领之一。献帝初平二年（191年），与于毒、眭固合兵攻略魏郡、东郡，东郡太守王肱无法抵御，于是曹操领兵进入东郡，在濮阳击破白绕。",
  hm_bocai: "波才，东汉末年黄巾军将领，统领东方黄巾军部队。曾在黄巾起义初期击败朱隽，取得一定战果，但在长社之战为皇甫嵩击败。",
  hm_bosi: "卜巳是东汉末年黄巾起义中的重要将领，作为张角麾下渠帅参与兖州地区的军事行动。他曾在兖州扫荡周边县城，导致莘县、阳谷县、东阿县等地县令弃城而逃并计划攻取范县和濮阳以扩大势力。其结局存在不同记载：部分史料称其在东郡仓亭之战中被皇甫嵩俘虏，另有记载称其于东郡仓亭之战中被傅燮俘虏。卜巳的活动对东汉朝廷震动较大，间接推动了汉灵帝的求贤诏令。",
  hm_chengyuanzhi: "程远志是小说《三国演义》中的人物，黄巾军将领，不见于正史记载。于小说第一回登场，被关羽斩于马下。",
  hm_dengmao: "邓茂是小说《三国演义》中的人物，黄巾军将领，为程远志副将，随之引兵五万进犯涿郡。幽州刺史刘焉令校尉邹靖引刘备、关羽、张飞三人，统兵五百，前去破敌。在大兴山下，两军交战，邓茂被张飞一回合刺于马下。",
  hm_fuyun: "浮云，三国杀集换式卡牌游戏《阵面对决》中的卡牌。三国杀虚构武将。",
  hm_gaosheng: "高升，黄巾军将领，张宝部将，于《三国演义》第2回登场。在朱儁等官军的攻势下，被张飞于阵前斩杀。",
  hm_guigu: "眭固（？－199年），一作畦固，字白兔(一说本名白兔，又一说本名兔)，东汉末年人物。原为黑山贼，与于毒、白绕齐名，攻打东郡，反为曹操所败，后成为张杨部下。建安三年（198年），曹操攻击吕布时张杨欲迎救，张杨部下杨丑杀张杨响应曹操。眭固又杀死杨丑，带领张杨部下屯军射犬（河南省武陟县西北），欲投奔袁绍。曹操亲征河内，派遣史涣、曹仁、乐进、于禁、徐晃等人渡河攻击眭固。眭固自领亲兵出奔袁绍，于犬城遭史涣等人截击被斩。",
  hm_heman: "何曼，东汉末年颍川、汝南一带的黄巾军将领，与刘辟、黄邵、何仪等人各拥有数万军队，先响应袁术，后来依靠孙坚，但最终被曹操击败。在小说《三国演义》中，何曼自称“截天夜叉”，被曹洪用拖刀计所杀。",
  hm_tangzhou: "唐周（生卒年不详），一作唐客，济南人（一作济阴人），东汉末年黄巾军首领张角的弟子。中平元年（184年），唐周向朝廷告发了张角要发动起义，使张角失去了在朝廷的内应，让朝廷有了准备的时间，张角不得不仓促起事。",
  hm_taosheng: "原为内黄小吏，有善心，黑山贼于毒攻陷邺城时，独自率领部众逾西城入，闭守州门，不让其他人进来，用车载着袁绍的家人及诸衣冠在州内者，身自扞卫，送到斥丘还。袁绍到，遂屯斥丘，以陶升为建义中郎将。",
  hm_yanzheng: "严政是罗贯中所著历史小说《三国演义》中虚构的黄巾军将领角色。在第二回“张翼德怒鞭督邮 何国舅谋诛宦竖”中，其作为黄巾军首领张宝的部下，因刺杀张宝并献首级投降而遭史料记载。该人物被塑造为衬托名将华雄武力的功能性角色，属于文学创作中的“经验包”设定。后严政刺杀张宝，献首投降",
  hm_yudu: "于毒（？—193年），朝歌（今河南省鹤壁市淇县）人，东汉末年黄巾农民起义军黑山分军首领。先是与白绕、眭固等攻略魏郡、东郡，被曹操击败。后与大陆各地的其他叛乱军联手，对袁绍的本据地——邺城展开袭击，虽然造成一时威胁，但仍不敌袁绍军队的猛烈反击，最终败北。随后袁绍乘胜追击，攻入其位于朝歌鹿场山苍岩谷的大本营，斩杀于毒。",
  huaci: "华雌，华佗之子，三国杀虚构武将。",
  chenshou: "陈寿（233年—297年），字承祚，巴西郡安汉县（现四川省南充市）人，蜀汉至西晋时期的历史学者。年轻时好学，曾师事于同郡的谯周，在蜀汉曾任观阁令史，屡被谴黜。蜀汉亡国后则仕于西晋，当时晋国司空张华爱其才，便举为孝廉，除佐著作郎，补阳平令。时人称其善叙事，有良史之才，当时夏侯湛也著有《魏书》，见陈寿所作的《三国志》的魏书部分，便销毁自己的著作。著有《蜀相诸葛亮集》、《三国志》六十五篇、《古国志》五十篇、《益部耆旧传》十篇，余文章传于世。",
  yj_yongkai: "雍闿（?～225年），益州郡人。 西汉什邡侯雍齿的后人（吕凯称其“先君雍侯”）， 在地方上很有权势，平日里就不服管制，嚣张跋扈。刘备病逝永安的消息传回后，雍闿觉得机会来了，心生反意，联合朱褒、高定，拉拢孟获发动叛乱，后被高定部下所杀。",
  simayan: "晋武帝司马炎（236年—290年5月16日），字安世，河内郡温县（今河南省温县）人。中国西晋开国皇帝（266年2月8日—290年5月16日在位），晋宣帝司马懿之孙，晋文帝司马昭嫡长子，母为文明皇后王元姬。司马炎出身河内司马氏。曹魏末年，其祖司马懿、伯父司马师、父司马昭相继控制朝政。咸熙元年（264年），司马炎被立为晋王世子，翌年司马昭去世，他继任为相国，袭封晋王。同年十二月（266年2月），逼迫魏元帝曹奂禅让，即位为帝，建立西晋，改元泰始。即位之初，鉴于曹魏宗室屏藩无力以致孤立而亡，大封同姓诸王，又委任几位宗王统领重兵，出镇要地；重视法律的修订，其颁行的《晋律》是中国古代重要的法典。咸宁五年（279年），发兵南下，次年灭亡吴国，实现全国统一。其间颁行占田制、户调式，促进人口增长，使得经济社会呈现繁荣景象，史称“太康之治”。但随着天下安定，司马炎逐渐“怠于政术，耽于游宴”，使统治阶层中奢侈荒淫之风广泛蔓延。他执意立智力低下的司马衷为继承人，又在统一前后命诸王就国、废除州郡武备，为西晋短暂统治的覆亡埋下了深刻隐患。太熙元年（290年），司马炎在洛阳含章殿病逝，时年五十五岁，谥号武皇帝，庙号世祖，安葬于峻阳陵。司马炎在父祖开创的政治基础上，建立晋朝，渡江灭吴，完成了三国统一的历史伟业。由他制定的有晋一代的政治、经济、律法制度，还深刻影响了后世王朝的政治体制。",
  ehuan: "鄂焕，古典文学名著《三国演义》人物，为蜀将高定部将，身长九尺，面目狰狞，使一只方天戟，有万夫不当之勇。于孔明征朱褒、雍闿时粉墨登场，与魏延大战不分胜负，后中计被魏延、王平、张翼联手擒获，孔明以礼相待，成功离间高定与朱、雍二人。后高定派鄂焕斩朱褒、平雍闿，二人一起归蜀，鄂焕遂因其功而被封为牙门将。",
  zhouji: "三国杀集换式卡牌游戏《阵面对决》中的权倾系列卡牌。游卡桌游官方的三国时期女性角色，原型是周妃（又名周彻）。周瑜之女。",
  lvchang: "吕常（161—221年），荆州南阳博望（今河南省南阳市方城县博望镇）人，汉末至三国时期曹魏将领。吕常曾担任曹魏横海将军、章陵太守，为武猛都尉厉节中郎将裨将军，封关内侯。常以中勇，显名州司，试守雉长，执戈秉戎，慎守易，兵不顿于敌国，坠不侵于四邻，拜武猛都尉厉节中郎将裨将军，封关内侯。王师南征，与充军从，奄有江汉，舍爵册勋，封阴德亭侯，领郡。鸠集荒散，为民统纪，三考有成，转拜平狄将军，改封卢亭侯，莅国赋政，十有三年。会蜀将关羽猖獗为寇，常御之，羽不能克。文帝加其庸，转拜横海将军，徙封西鄂都乡侯，食邑并七百户。年六十一，黄初二年正月卒。",
  huangjinleishi: "黄巾军中负责施法的女祭司二人组。",
  shendan: "申耽（生卒年不详），字义举，三国时期人物，申仪之兄。起初在西平、上庸间聚众数千家。后与张鲁联系，又遣使拜见曹操，被封为将军，任上庸都尉。建安二十四年（219年），遭刘备将领刘封、孟达攻打，举众投降，任征北将军，封员乡侯（一作郧乡侯），领上庸太守如故。刘封败走后，申耽降魏，担任怀集将军，徙居南阳。",
  shenyi: "申仪（生卒年不详），汉末三国时期人物，申耽之弟。申耽投降刘备后，申仪被刘备任命为建信将军、西城太守。曹丕派夏侯尚攻打上庸时，申仪背叛刘封归魏，被拜为魏兴太守，封真乡侯。孟达谋叛魏归蜀后，申仪密表孟达勾结蜀汉。孟达死后，申仪因屡次承制刻印而被司马懿送到京师，转拜为楼船将军，在礼请中。",
  longyufei: "《三国杀·阵面对决》中的虚构角色，设定是由刘备之女夏侯岚、关羽之女关银屏、张飞之女张星彩三人在与吕布之魔魂战斗时，释放雅典娜的惊叹而召唤出来的精元化神。",
  pk_sp_duyu: "杜预（222年－285年），字元凯，京兆郡杜陵县（今陕西西安）人，中国魏晋时期军事家、经学家、律学家，曹魏散骑常侍杜恕之子。杜预初仕曹魏，任尚书郎，后成为权臣司马昭的幕僚，封丰乐亭侯。西晋建立后，历任河南尹、安西军司、秦州刺史、度支尚书等职。咸宁四年（278年）接替羊祜出任镇南大将军，镇守荆州。他积极备战，支持晋武帝司马炎对孙吴作战，并在咸宁五年（279年）成为晋灭吴之战的统帅之一。战后因功进封当阳县侯，仍镇荆州。太康五年（285年），杜预被征入朝，拜司隶校尉，途中于邓县逝世，终年六十三岁。获赠征南大将军、开府仪同三司，谥号为成。杜预耽思经籍，博学多通，多有建树，时誉为“杜武库”。著有《春秋左氏传集解》及《春秋释例》等。为明朝之前唯一一个同时进入文庙和武庙之人。",
  ps_shen_machao: "字孟起，扶风茂陵人。面如冠玉，目如流星，虎体猿臂，彪腹狼腰，声雄力猛。因衣着讲究，举止非凡，故人称“锦马超”。麾铁骑，捻金枪。",
  xiaosha: '2021年「虚拟天团」Vtuber企划中推出的虚拟偶像之一，该企划原定培养12位虚拟偶像，然而企划由于各方面原因暴死，现虚拟偶像仅存在于各武将的皮肤中。<br>设定上为三国杀新手菜鸡，误打误撞参加了三国杀组织的《三国之星》选秀大赛，没想到一路靠着颜值和风风火火的爽快性格过关斩将吸粉无数，获得了冠军，成为官方签约的形象代言人。<br>小杀现于B站还存在<style type="text/css">#slash_bilibili:link, #slash_bilibili:visited {color:white;}</style><a id="slash_bilibili" href="https://space.bilibili.com/686179867" target="_blank">对应账号</a>，但早已荒废。',
  xiaoshan: "2021年「虚拟天团」Vtuber企划中推出的虚拟偶像之一，该企划原定培养12位虚拟偶像，然而企划由于各方面原因暴死，现虚拟偶像仅存在于各武将的皮肤中。<br>设定上为小杀同系不同班的同学，由于本身性格比较安静，所以和风风火火的小杀有些不对盘。知道小杀也玩三国杀后，外表高冷、内心傲娇的她偷偷也去玩了游戏，立志要在游戏里碾压她。<br>小闪对应B站账号已销号。",
  xiaotao: "2021年「虚拟天团」Vtuber企划中推出的虚拟偶像之一，该企划原定培养12位虚拟偶像，然而企划由于各方面原因暴死，现虚拟偶像仅存在于各武将的皮肤中。<br>设定上为小杀的亲妹妹，陪姐姐去拍摄代言宣传照的时候，被导演看中，觉得三国美貌姐妹花是非常好的噱头，于是一并签下。正所谓“三国姐妹花，永远不分家！”。<br>小桃对应B站账号已销号。",
  xiaole: "2021年「虚拟天团」Vtuber企划中推出的虚拟偶像之一，该企划原定培养12位虚拟偶像，然而企划由于各方面原因暴死，现虚拟偶像仅存在于各武将的皮肤中。<br>设定上小乐家与小桃小杀家是世交，三人从小就认识。和小闪是同班同学，小乐很喜欢安静的小闪。和小酒是小杀介绍认识的，大家都是打三国杀的网友，在打游戏时候比较依赖小酒——躺赢人躺赢魂，躺赢都是人上人。<br>小乐对应B站账号已销号。",
  xiaojiu: "2021年「虚拟天团」Vtuber企划中推出的虚拟偶像之一，该企划原定培养12位虚拟偶像，然而企划由于各方面原因暴死，现虚拟偶像仅存在于各武将的皮肤中。<br>设定上为非人类，是一只年龄成迷的神兽，真身是白泽。是小杀网上玩游戏认识的网友，爱好是喝酒，和小杀也有好酒友的关系。<br>小酒对应B站账号已销号。",
  tianchuan: "田钏是《阵面对决》中的角色，原型为一个出现在江东的神秘女刺客，她犹如一只狡猾的狐狸，穿梭于人群之中，执行着不为人知的猎杀任务。她能潜伏在目标身边数月，只为了逐渐消磨目标的意志，慢慢折磨他直到死亡。她不知道自己的生父是谁，只知道自己名叫田钏，出生名门，却从来没见过自己的父母。她来自暗影，遁于暗影，在她的手中，再具有权势的人，也终将成为亡魂。在之后的故事里，吕蒙病逝被怀疑就是田钏所为。",
  chengjix: "程畿（？～222年），巴西阆中（今四川省阆中市）人 。三国时期蜀汉官员。刘璋割据益州时，程畿为汉昌长。后来，巴西太守庞羲因刘璋猜疑而怀有叛离之心，命令程畿的儿子征召士兵自保，程畿不仅制止儿子的行为，还说服了庞羲，因此受到刘璋的赏识，被刘璋任命为江阳太守。后来刘备取代刘璋担任益州牧，程畿担任从事祭酒。蜀汉建国后，程畿随刘备伐吴，刘备兵败夷陵，程畿不肯撤退，最终奋力战死。",
  zhaorong: "赵融（生卒年不详），三国时期蜀汉将领，曾随刘备参加夷陵之战，为帐下别督。",
  shicong: "是个侍从，大概。",
  tanxiong: "谭雄，小说《三国演义》中的虚构人物，不见于正史记载。东吴将领，在夷陵之战时随孙桓迎战蜀军，曾暗放冷箭射倒张苞的坐骑；后为关兴所擒，被张苞斩杀祭马。",
  liue: "刘阿（生卒年不详）三国时期孙吴将领。章武元年（221）七月，刘备因关关羽被杀准备东征孙吴，孙权请求和解遭拒绝后，令陆议、李异、刘阿等人屯兵于巫县和秭归。后被吴班、冯习打败了，刘备军进驻秭归。黄初七年（226年），魏明帝曹叡即位，派张郃与司马懿伐吴，击败吴将刘阿。",
  zhangda: "张达，请查看「范疆张达」的武将介绍。",
  fanjiang: "范疆，请查看「范疆张达」的武将介绍。",
  ty_anyingx: "刘备率蜀汉雄狮伐吴，途经诸乡。沿途黎庶咸感其仁德之召，箪食壶浆，夹道以迎。刘备屯军修整之际，亦不缀微服私访，察民情、恤疾苦，每以温语慰问，布施恩泽于四方。一日夜阑人静，玄德公自外回营，行至幽深之处，忽逢数名刺客，皆头戴乌鹊之冠，身披轻捷之裳。骤然暴起，意图行刺。虽侍卫忠勇奋激，终因寡不敌众，难以抵挡。玄德公亦年岁已高，此刻难以匹敌。",
  ty_anying: "刘备率蜀汉雄狮伐吴，途经诸乡。沿途黎庶咸感其仁德之召，箪食壶浆，夹道以迎。刘备屯军修整之际，亦不缀微服私访，察民情、恤疾苦，每以温语慰问，布施恩泽于四方。一日夜阑人静，玄德公自外回营，行至幽深之处，忽逢数名刺客，皆头戴乌鹊之冠，身披轻捷之裳。骤然暴起，意图行刺。虽侍卫忠勇奋激，终因寡不敌众，难以抵挡。玄德公亦年岁已高，此刻难以匹敌。",
  ty_wuque: "刘备率蜀汉雄狮伐吴，途经诸乡。沿途黎庶咸感其仁德之召，箪食壶浆，夹道以迎。刘备屯军修整之际，亦不缀微服私访，察民情、恤疾苦，每以温语慰问，布施恩泽于四方。一日夜阑人静，玄德公自外回营，行至幽深之处，忽逢数名刺客，皆头戴乌鹊之冠，身披轻捷之裳。骤然暴起，意图行刺。虽侍卫忠勇奋激，终因寡不敌众，难以抵挡。玄德公亦年岁已高，此刻难以匹敌。",
  ty_yanque: "刘备率蜀汉雄狮伐吴，途经诸乡。沿途黎庶咸感其仁德之召，箪食壶浆，夹道以迎。刘备屯军修整之际，亦不缀微服私访，察民情、恤疾苦，每以温语慰问，布施恩泽于四方。一日夜阑人静，玄德公自外回营，行至幽深之处，忽逢数名刺客，皆头戴乌鹊之冠，身披轻捷之裳。骤然暴起，意图行刺。虽侍卫忠勇奋激，终因寡不敌众，难以抵挡。玄德公亦年岁已高，此刻难以匹敌。",
  ty_wangque: "刘备率蜀汉雄狮伐吴，途经诸乡。沿途黎庶咸感其仁德之召，箪食壶浆，夹道以迎。刘备屯军修整之际，亦不缀微服私访，察民情、恤疾苦，每以温语慰问，布施恩泽于四方。一日夜阑人静，玄德公自外回营，行至幽深之处，忽逢数名刺客，皆头戴乌鹊之冠，身披轻捷之裳。骤然暴起，意图行刺。虽侍卫忠勇奋激，终因寡不敌众，难以抵挡。玄德公亦年岁已高，此刻难以匹敌。",
  ty_sunquan: "公元二百二十四年，先主挟夷陵之余威，挥师东进，直捣吴都建邺，吴主孙权并大都督陆逊，见事不可为，率群臣出降。江东膏腴之地，自此尽入炎汉彀中。先主既克建邺，颁诏大赦，广开言路，招揽贤才。更约法三章，施以宽政，留其旧制，以安民心。<br>先主此举，旨在抚慰江南，彰大汉仁德。吴地上下，无不感激涕零。自此六郡归心，昔日之敌，融为一炉。上下一心，共图国泰民安，传为一时佳话。",
  ty_guanyu: "危难之间，一神秘将军挺身而出，与刘备协力御敌，其人面目刚毅，铁甲覆身，临危不惧。二人同进共退，配合无间，见久攻不下，刺客突施冷箭，直指刘备要害。神秘将军惊觉异常，以及身为盾，挡下夺命之矢。箭铁透甲入骨，其而持人巍然不动，手持青龙刀：“吾兄，大汉之未来，望汝承载矣！”继而持刀冲阵，化为清风。",
  xk_luoli: "罗厉（？—235年），三国时期南海（今广东广州）人，因作乱而被吴国将领吕岱斩杀。",
  xk_cuilian: "刘备当安喜县尉不久，朝廷诏令各州郡，淘汰县级以军功为长吏的人，刘备也在被淘汰之列。负责裁汰工作的督邮来到刘备的地盘，刘备前去求见，可督邮装病不予接见。刘备大怒，带人冲进去将督邮绑出，将自己的官印挂在他脖子上，并狠狠的抽了他二百鞭，弃官而去。<br>崔廉是为这位督邮虚构的名字。",
  xk_penghu: "彭虎，生卒年不详，东汉末年鄱阳起义军首领，后为孙权部将董袭所败。",
  xk_shanfu: "字元直，与司马徽、诸葛亮等人为友。先化名单福仕官于新野的刘备，后因曹操囚禁其母而不得不弃备投操，临行前向刘备推荐诸葛亮之才。入曹营后，一言不发，不曾为曹操进献过一计半策。后人形容徐庶“身在曹营心在汉”。",
  xk_pengqi: "彭绮，是鄱阳郡宗贼大帅。吴黄武四年（225年）十二月，彭绮自称将军，攻破鄱阳诸县，其部众有数万人。东吴以周鲂为鄱阳太守，与胡综并力征讨彭绮。",
  xk_zulang: "祖郎，生卒年不详。东汉末年活跃于扬州丹阳郡泾县（今属安徽宣城）一带的山越势力头领，被称为“泾县大帅”、“丹阳宗帅”。在孙策下江东期间，多次率军与孙策交锋，互有胜败。曾两次击败孙策：先是逼得孙策“几至危殆”；后来，又在防御战中击溃孙策。最后，孙策纠合大军与之交战，大破之，祖郎遂投降孙策。"
};
const characterFilters = {
  shen_diaochan(mode) {
    return mode == "identity" || mode == "doudizhu" || mode == "single" || mode == "versus" && _status.mode != "standard" && _status.mode != "three";
  },
  le_shen_jiaxu(mode) {
    return mode == "identity" && _status.mode != "purple";
  }
};
const characterTitles = {
  lx_jiachong: "凶凶踽行",
  lx_chengji: "劣犬良弓",
  lx_lizhaojiaobo: "竭诚尽节",
  lx_wenqin: "淮山骄腕",
  lx_guanqiujian: "才识拔干",
  lx_simazhou: "恭温克己",
  lx_simafu: "徒难夷惠",
  lx_mb_shen_machao: "势震九天",
  lx_caomao: "向死存魏",
  lx_simazhao: "独袄吞天",
  pe_xiangyu: "西楚霸王",
  ps_shen_jiangwei: "残阳泣血",
  ps_devil_jiangwei: "残魂噬血",
  shangguanwaner: "天性韶警",
  scl_zhangxiu: "星汉灿烂",
  zh_shen_zhangjiao: "神临天劫",
  ps_shen_caocao: "晷月皓冕",
  wxdl_caopi: "秋风萧瑟",
  pe_wuzetian: "助天承圣皇帝",
  ym_zhaoxiang: "暗香吟雪",
  ym_guanyinping: "铁衣蔷薇",
  ym_zhangxingcai: "兰芷锋镝",
  ym_jsrg_diaochan: "群芳之魁",
  ym_friend_zhugeliang: "龙骧九天",
  pe_shen_zhonghui: "荡徊的蜚螭",
  ps_shen_weiyan: "子午创世神",
  ps_devil_weiyan: "戾骨噬汉魔",
  sm_shenmo_sunquan: "一念神魔",
  pe_liuhui: "周天古率",
  pe_baiqi: "大良造",
  pe_huamulan: "孝烈巾帼",
  pe_yangyuhuan: "贵妃醉酒",
  pe_xiahoulan: "不驯巾帼",
  pe_lishimin: "天策上将",
  ylyg_xuhuang: "公明大义",
  ylyg_zhurong: "刺美人",
  wxdl_caozhi: "白马饰金羁",
  hl_shen_guanyu: "刀炽炼狱",
  hl_shen_zhugeliang: "赤壁唤风",
  hl_shen_lvmeng: "白衣渡江",
  hl_shen_zhouyu: "红莲业火",
  hl_shen_lvbu: "魔神再临",
  ylyg_pangtong: "南州冠英",
  ylyg_dianwei: "戟贯数人",
  jx_shen_caoren: "征南将军",
  jx_shen_liubiao: "称雄荆襄",
  ylyg_xiaoqiao: "秋水并蒂",
  ylyg_yuji: "江东的福神",
  pe_guozhao: "碧海青天",
  scl_lvbu: "武德化身",
  jun_lvbu: "最强神话",
  sm_chixia: "赛马娘",
  sm_qiulin: "赛马娘",
  sm_zhuahuang: "赛马娘",
  sm_zijian: "赛马娘",
  sm_jieying: "赛马娘",
  sm_liaoyuan: "赛马娘",
  sm_dailu: "赛马娘",
  zc26_sp_xushi: "红魂龙谶",
  wxdl_caocao: "东临碣石",
  shinin_zhenji: "薄幸的美人",
  awaken_shinin_zhenji: "闪耀战姬",
  shinin_wuguotai: "武烈皇后",
  awaken_shinin_wuguotai: "闪耀战姬",
  shinin_ruiji: "柔荑弄钺",
  awaken_shinin_ruiji: "闪耀战姬",
  shinin_lvlingqi: "无双虓姬",
  awaken_shinin_lvlingqi: "闪耀战姬",
  shinin_dongwan: "蜜言如鸩",
  awaken_shinin_dongwan: "闪耀战姬",
  goblin: "残暴掳掠",
  lord_goblin: "疑暴如麟",
  zc26_lusu: "荷檐吐奇",
  zc26_zhanghe: "无定的水虺",
  zc26_taishici: "剑鸣太平",
  zc26_jiaxu: "毒士无双",
  scl_jiaxu: "陈筹画策",
  xia_caopi: "弃旧学新",
  eu_kaisa: "无冕之皇",
  eu_makang: "马米科尼扬",
  eu_Macrinus: "传奇的角斗士",
  eu_Ardashir: "萨珊的创始人",
  eu_luomalishi: "国王的精锐",
  //罗马力士等着打复活赛吧
  eu_yujin: "驰援襄樊",
  eu_zhangliao: "镇斩蹋顿",
  //但是在欧洲
  eu_yuejin: "凯歌高进",
  pe_xiahouxuan: "明皎月影",
  drag_huaci: "献躯验方",
  drag_guansuo: "蜃龙傲骨",
  drag_guanping: "青龙出水",
  drag_guanyinping: "兔年春节",
  drag_zhoucang: "忠烈舍身",
  drag_liaohua: "御敌屏障",
  drag_shendan: "败阵降曹",
  drag_shenyi: "背汉顺曹",
  drag_luxun: "淑人君子",
  drag_xusheng: "破军杀敌",
  she_lvmeng: "青龙入命",
  hm_shen_huangfusong: "厥功至伟",
  hm_shen_zhujun: "围师必阙",
  hm_shen_yl_luzhi: "鏖战广宗",
  hm_shen_zhangjiao: "庇佑万千",
  hm_shen_zhangbao: "庇佑万千",
  hm_shen_zhangliang: "庇佑万千",
  hm_nanhualaoxian: "冠绝天下",
  hm_guigu: "兔入犬城",
  hm_bosi: "黄巾渠帅",
  hm_yudu: "劫富济贫",
  hm_taosheng: "平汉将军",
  hm_fuyun: "黄天末伐",
  hm_tangzhou: "叛门高足",
  hm_chengyuanzhi: "逆流而动",
  hm_gaosheng: "地公之锋",
  hm_heman: "截天夜叉",
  hm_yanzheng: "献首投降",
  hm_bocai: "黄巾执首",
  hm_dengmao: "逆势而行",
  hm_bairao: "黑山寇首",
  xk_luoli: "庐江义寇",
  xk_cuilian: "缚树行鞭",
  xk_penghu: "鄱阳风浪",
  xk_shanfu: "忠义万全",
  xk_pengqi: "百花缭乱",
  xk_zulang: "抵力坚存",
  scl_pangdegong: "超脱于世",
  scl_sunhanhua: "威灵尽显",
  scl_miheng: "完赋鹦飞",
  scl_peixiu: "登高勘形",
  scl_caoying: "魏武遗风",
  scl_luotong: "仁政爱民",
  sp_gongsunzan: "白马将军",
  sp_simazhao: "狼子野心",
  sp_wangyuanji: "文明皇后",
  sp_xinxianying: "明鉴业才",
  sp_liuxie: "汉献帝",
  yj_caoang: "捍不畏死",
  ns_chendao: "白毦出征",
  ns_jiaxu: "乱武天下",
  ns_lijue: "狼噬火踪",
  ns_zhangji: "文和乱武",
  ns_fanchou: "文和乱武",
  ns_caoanmin: "瓢取祸水",
  sp_xiahoushi: "疾冲之恋",
  jsp_zhaoyun: "常山的游龙",
  huangjinleishi: "雷祭之姝",
  sp_pangtong: "荆楚之高俊",
  sp_daqiao: "韶光易逝",
  sp_ganning: "怀铃的乌羽",
  sp_xiahoudun: "啖睛的苍狼",
  sp_lvmeng: "国士之风",
  sp_zhangfei: "横矛立马",
  sp_liubei: "汉昭烈帝",
  yj_caocao: "谯水击蛟",
  yj_caohong: "转战四方",
  yj_zhangfei: "勇力绝伦",
  yongjian_ganning: "披星踏浪",
  yj_dongzhuo: "专擅朝政",
  yj_liru: "魔王之脑",
  yj_xuyou: "恃功矜能",
  yj_jiaxu: "良平之才",
  yj_zhenji: "皎月莲香",
  bol_sunluban: "为虎作伥",
  pe_wangyun: "忠魂不泯",
  //储备皮肤，暂未命名，先用别的
  pe_zhonghui: "桀骜的野心家",
  pe_mengda: "怠军反复",
  pe_sunchen: "凶竖盈溢",
  pe_wenqin: "勇而无算",
  ps_guanyu: "飞龙在天",
  ps2070_guojia: "谋定天下",
  ps1059_guojia: "破晓逐辰",
  ps2063_zhaoyun: "寒光凛然",
  ps1062_zhouyu: "盖世之才",
  ps2080_zhouyu: "舞剑助兴",
  ps_caozhi: "洛阳感怀",
  ps_jin_simayi: "通达权变",
  //储备皮肤没有定称号，暂且用晋司马懿原皮作称号
  ps2067_zhaoyun: "战场游龙",
  ps_caopi: "帝业霸途",
  ps2068_simayi: "谋定天下",
  ps_simayi: "狼顾鬼才",
  ps_zhugeliang: "三分天下",
  ps2066_zhugeliang: "谋定天下",
  ps_machao: "一马当先",
  ps_lvbu: "虓虎之勇",
  ps_jiaxu: "控魂驱魄",
  pk_sp_duyu: "文成武德",
  vtb_xiaosha: "虚拟偶像",
  vtb_xiaoshan: "虚拟偶像",
  vtb_xiaotao: "虚拟偶像",
  vtb_xiaole: "虚拟偶像",
  vtb_xiaojiu: "虚拟偶像",
  old_machao: "西凉的猛狮",
  bolx_jsp_guanyu: "汉寿亭侯",
  jsp_liubei: "乱世的枭雄",
  jsp_caoren: "坚不可摧",
  jsp_ganfuren: "昭烈皇后",
  zhangliang: "黄昏的斗蛾",
  ol_xinxianying: "才涌花娇",
  ol_liuyu: "察民农事",
  ol_zhangrang: "祸乱朝纲",
  jiangqing: "祁奚之器",
  tianfeng: "河北瑰杰",
  jiling: "仲家的主将",
  kongrong: "凛然重义",
  mateng: "驰骋西陲",
  longyufei: "将魂之星",
  drag_pangde: "义怒气壮",
  drag_guanyu: "国士无双",
  drag_caoren: "玉钤奉国",
  drag_lvchang: "险守襄阳",
  yj_zhouji: "江东的红莲",
  yj_ehuan: "牙门勇将",
  yj_zhonghui: "龙生九子",
  jx_zhouyu: "雄姿英发",
  jx_guanyu: "美髯公",
  jd_simayan: "晋武帝",
  jd_sb_sunquan: "永开吴祚",
  jd_sb_xiaoqiao: "战场绝版",
  jd_sb_guanyu: "以身正道",
  jd_sb_jiangwei: "妙算神谟",
  jd_sb_daqiao: "清萧清丽",
  jd_sb_menghuo: "何惧生死",
  jd_sb_yuanshao: "令出将行",
  jd_sb_yujin: "威严毅重",
  jd_sb_sunshangxiang: "花曳心牵",
  jd_sb_liubei: "计澜锦绣",
  jd_sb_fazheng: "陈筹画策",
  jd_sb_zhangfei: "武动乾坤",
  jd_jin_simashi: "桀骜睥睨",
  jd_sb_sp_zhugeliang: "契若金兰",
  jd_sb_caocao: "临渊观海",
  jd_jin_simazhao: "权倾天下",
  jd_jin_xiahouhui: "熠熠珠玉",
  jd_hanlong: "白虹贯日",
  jd_sb_pangtong: "计澜锦绣",
  jd_sb_ganning: "龙生九子",
  jd_sb_zhaoyun: "截江救主",
  jd_sb_huangyueying: "青云鸢飞",
  jd_sp_yangwan: "融沫之鲡",
  jd_ol_huaxin: "渊清玉洁",
  jd_jin_wangyuanji: "越女采薇",
  jd_sb_xuhuang: "无往不胜",
  ty_shen_liubei: "龙兴海内",
  ty_shen_guanyu: "佑子伐吴",
  ty_shen_zhangfei: "傲睨山河",
  ty_guanyu: "卷土重来",
  ty_sunquan: "大汉吴王",
  ty_liubei: "见龙献渊",
  ty_chenshi: "夹岸屯军",
  ty_chengjix: "大义之诚",
  ty_zhaorong: "从龙别督",
  ty_zhangnan: "澄辉的义烈",
  ty_fengxí: "赤胆的忠魂",
  ty_liaohua: "果敢刚直",
  ty_huangzhong: "炎汉后将军 ",
  ty_wuban: "奉命诱贼",
  ty_shicong: "尽职尽忠",
  ty_anying: "黑影笼罩",
  ty_anyingx: "黑影射沙",
  ty_wuque: "密执生死",
  ty_yanque: "神出鬼没",
  ty_wangque: "神鬼莫测",
  ty_guanxing: "少有令闻",
  ty_shamoke: "狂喜胜战",
  ty_guanyinping: "步步生花",
  ty_luxun: "社稷心膂",
  ty_ganning: "锦帆英豪 ",
  ty_buzhi: "积跬靖边",
  ty_tanxiong: "暗箭难防",
  ty_liue: "西抵怒龙",
  ty_zhangda: "有死无生",
  ty_fanjiang: "有死无生",
  yy_quyi: "界桥先登",
  yy_gongsunzan: "金簇雕弓",
  yy_wenchou: "狼咄虎攫",
  yy_gongsunyuan: "逐鹿天下",
  yy_yuanshao: "一往无前",
  yy_simayi: "隐忍对阵",
  yy_caorui: "大权独揽",
  yy_zhaoyun: "冰甲龙魄",
  yy_quancong: "利国利民",
  yj_yongkai: "惑豩南中",
  yj_chezhou: "反受其害",
  jy_caocao: "鲸吞江东",
  jy_caoren: "星流霆击",
  jy_zhugeliang: "隆中观局",
  jy_pangtong: "玉帛雕翎",
  jy_lusu: "周济万民",
  jy_zhangzhao: "锦运绵长",
  jy_zhouyu: "兔年清明",
  jy_huanggai: "以身证道",
  ps_zhangrang: "玉翎琢雉",
  ps_zhaozhong: "奉天司鸣",
  ps_sunzhang: "半身执礼",
  ps_bilan: "竭荒净尘",
  ps_xiayun: "截谋囚鸟",
  ps_hankui: "嘲嬉弄权",
  ps_lisong: "令指天元",
  ps_duangui: "逐书交困",
  ps_guosheng: "毋同岁凤",
  ps_gaowang: "龙凤交欢",
  zombie_zombie: "丧尸围城",
  chenshou: "青史陈寿",
  hs_liuxuan: "暗渊龙吟",
  hs_caohuan: "陈留王",
  hs_liuxie: "山阳公",
  hs_sunhao: "归命侯",
  ca_shen_lijueguosi: "大破长安",
  ca_wangyun: "忠魂不泯",
  ca_shen_wangyun: "幽狱冰心",
  //看来这里有些缺漏的武将是因为旧武将ID被隐藏或没人完整删掉导致的，为了以防万一，两个ID都写了
  ca_shen_caocao: "四方归心",
  zombie_jiaxu: "丧尸出笼",
  ca_lvbu: "傲睨万物",
  ca_lijue: "筹谋长安",
  ca_guosi: "党豺为虐",
  ca_zhangji: "悍威狠决",
  ca_fanchou: "攻占长安",
  ps_caocao: "奉天徙入望",
  ps_liubei: "仁兵伐无道",
  ps_sunquan: "年少万兜鍪",
  yj_caocaoyuanshao: "总角之交",
  yj_jiling: "全军冲锋",
  yj_tianchuan: "潜行之狐",
  pe_que: "神人何惧",
  pe_jun_caopi: "魏王称帝",
  pe_jun_liushan: "大智若愚",
  pe_jun_sunquan: "吴王六剑",
  pe_jun_liuxie: "汉献帝",
  pe_jun_liuhong: "长乐未央",
  pe_jun_liubei: "龙御天下",
  pe_jun_caocao: "逐鹿天下",
  pe_jun_sunce: "虎踞江东",
  pe_jun_yuanshao: "讨伐奸佞",
  pe_jun_zhangjiao: "天公策雷",
  wn_shen_machao: "麾骑撚抢",
  wn_shen_xuzhu: "咆虎熔兵",
  //谁拿的普通许褚的皮肤来当做神许褚的插画？现在神许褚已经有新的皮肤了 渭南版称号猛虎战骊
  wn_caocao: "乱世的奸雄",
  wn_hansui: "征西将军",
  wn_houxuan: "合聚複相",
  wn_chengyi: "气高志远",
  wn_yangqiú: "似若无敌",
  wn_zhanghe: "料敌机先",
  wn_jiaxu: "巧施间策",
  wn_xuhuang: "乘虚渡江",
  wn_zhuling: "良将之亚",
  xy_caocao: "与兵血仇",
  xy_chengyu: "腹蕴千军",
  xy_xunyu: "令君劝战",
  xy_chengong: "刚直壮烈",
  xy_zhangkai: "财靡欲壑",
  xy_lvbu: "虓烬飞将",
  xy_zhangmiao: "据兖以观",
  xy_caosong: "醉梦折冲",
  zj_zhanghu: "晋阳侯",
  zj_yuechen: "广昌亭侯",
  zj_hulie: "玄武定澜",
  zj_zhugexu: "疑途独落",
  zj_lukang: "社稷之瑰宝",
  zj_zhaotong: "戍边驻阁",
  zj_zhaoguang: "沓中独断",
  zj_huangchong: "星陨绵竹",
  zj_liaohua: "果敢刚直",
  zj_dingfeng: "寿春解围",
  zj_zhugezhan: "绵竹之殇",
  zj_liuyin: "御军定守",
  zj_huoyi: "三世忠烈",
  zj_yaokehui: "绥戎校尉",
  xiahouhan: "绮梦年华",
  zj_xiahouhan: "飘萍半生",
  zj_zhonghui: "荡涤山河",
  //这是主公形态的钟会，一般形态下的称号叫权昭忠义
  zj_jiangwei: "残薪续志",
  zj_liuyuan: "留质洛阳",
  zj_zhugeliang: "武魂",
  sm_shen_machao: "赛马娘",
  sp_sm_shen_machao: "赛马神",
  mark_shen_machao: "迅鹭惊雷",
  shen_diaochan: "欲界非天",
  boss_zhaoyun: "天龙乘云",
  zc26_shen_huangyueying: "卧龙的点睛人",
  shen_jiaxu: "文和乱武",
  le_shen_jiaxu: "倒悬云衢",
  ps_shen_machao: "迅骛惊雷"
};
const dynamicTranslates = {
  lxzuoyou(player, skill) {
    const storage = player.storage[skill];
    let yang = "你可受到1点无来源伤害，令一名角色获得1点护甲", yin = "你可失去1点护甲，对一名角色造成1点伤害";
    if (storage) {
      yin = `<span class="bluetext">${yin}</span>`;
    } else {
      yang = `<span class="firetext">${yang}</span>`;
    }
    const start = "转换技，出牌阶段限一次，";
    return `${start}阳：${yang}；阴：${yin}。`;
  },
  ymhengren(player, skill) {
    const storage = player.storage[skill];
    let yang = "开始时", yin = "结束时";
    if (storage) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    const start = "转换技，其他角色的出牌阶段：", end = "，若其本阶段有【杀】的剩余使用次数，你可以使用一张【杀】（将计入其使用次数且伤害+1）。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  huamao_wushen(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `锁定技。①你的${suit}手牌均视为【杀】。②你使用${suit}【杀】无距离和次数限制且不可被响应。`;
  },
  huamao_liushi(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `出牌阶段，你可以将一张${suit}牌置于牌堆顶，视为对一名角色使用一张【杀】（无距离限制且不计入使用次数）。当此【杀】造成伤害后，受到伤害的角色获得一个“流”。有“流”的角色手牌上限-X（X为其“流”数）。`;
  },
  huamao_gongxin(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `出牌阶段限一次，你可以观看一名其他角色的手牌，并可以展示其中一张${suit}牌，然后将其弃置或置于牌堆顶。`;
  },
  huamao_tianxiang(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `当你受到伤害时，你可以弃置一张${suit}手牌，防止此次伤害并选择一名其他角色，然后你选择一项：1.令其受到伤害来源对其造成的1点伤害，然后摸X张牌（X为其已损失体力值且至多为5）；2.令其失去1点体力，然后获得你弃置的牌。`;
  },
  huamao_guose(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `出牌阶段限一次，你可以选择一项：将一张${suit}牌当做【乐不思蜀】使用；或弃置一张${suit}牌并弃置场上的一张【乐不思蜀】。选择完成后，你摸一张牌。`;
  },
  huamao_limu(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `出牌阶段，你可以将一张${suit}牌当做【乐不思蜀】对自己使用，然后回复1点体力。只要你的判定区内有牌，你对攻击范围内的其他角色使用牌便没有次数和距离限制。`;
  },
  huamao_fengpo(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `每种牌名各限一次。当你每回合使用的第一张【杀】或【决斗】指定目标后，若目标角色数为1，你可以选择一项：1.摸X张牌，令此牌的伤害值基数+1；2.摸一张牌，令此牌的伤害值基数+X（X为其${suit}牌的数量）。`;
  },
  huamao_jiexun(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `结束阶段，你可令一名其他角色摸等同于场上${suit}牌数的牌，然后弃置X张牌（X为此前该技能发动过的次数）。若有角色因此法弃置了所有牌，则你将X归零，然后你发动〖复难〗时，无须令对方获得你使用的牌。`;
  },
  huamao_leiji(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `当你使用或打出一张【闪】时，你可令任意一名角色进行一次判定。若结果为${suit}，其受到2点雷电伤害。`;
  },
  huamao_zuoding(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `当其他角色于其回合内使用${suit}牌指定目标后，若本回合内没有角色受到过伤害，则你可以令其中一名目标角色摸一张牌。`;
  },
  huamao_miehai(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `你可以将两张牌当作无距离次数限制的刺【杀】使用。此【杀】结算完成后，此过程中正面失去${suit}牌且已受伤的角色摸两张牌并回复1点体力。`;
  },
  huamao_jiyu(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `出牌阶段限一次，你可以令一名角色弃置一张手牌。若如此做，你不能使用与之相同花色的牌，直到回合结束。若其以此法弃置的牌为${suit}，你翻面并令其失去1点体力。若你有未被〖讥谀〗限制的手牌，则你可以继续发动此技能，但不能选择本回合已经选择过的目标。`;
  },
  huamao_luoying(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `当其他角色的${suit}牌因弃置或判定而进入弃牌堆后，你可以获得之。`;
  },
  huamao_lianhuan(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `你可以将${suit}手牌当作【铁索连环】使用或重铸。`;
  },
  huamao_zhujiu(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `你可以将至少X+1张牌当【酒】使用（X为你本回合已使用过【酒】的次数），然后若这些牌存在非${suit}牌，此技能失效直到回合结束。`;
  },
  huamao_ninghan(player, skill) {
    const suit = get.translation(get.info("wxdl_huamao").getSuit(player, skill));
    return `锁定技。①所有角色手牌中的${suit}【杀】均视为冰【杀】。②当一名角色受到冰冻伤害后，你将造成此伤害的牌对应的实体牌置入“城”。`;
  },
  eu_zhitong(player) {
    const bool = player.storage.eu_zhitong;
    let yang = "自己，摸两张牌且回复1点体力", yin = "其他角色，你获得其装备区所有牌并对其造成1点伤害";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技，当你使用牌时，若目标包含，", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  peyuanjue(player) {
    const bool = player.storage.peyuanjue;
    let yang = "令所有角色的基本牌视为无次数限制的【杀】", yin = "令所有角色与你互相计算距离为1，且你视为拥有〖同忾〗";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。摸牌阶段开始时，你可以跳过摸牌阶段，", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  yjjiechu(player) {
    const bool = player.getStorage("yjjiechu", false);
    let yang = "出牌阶段，你可以视为使用一张【顺手牵羊】，结算结束后成为目标的角色可以对你使用一张【杀】", yin = "当你成为【杀】的目标时，你可以弃置一张手牌改变【杀】的花色和属性";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  scls_miaojian(player) {
    if (player.hasMark("scls_miaojian")) {
      return "出牌阶段限一次，你可视为使用一张刺【杀】或【无中生有】。";
    }
    return "出牌阶段限一次，你可将一张基本牌当做刺【杀】使用，或将一张非基本牌当做【无中生有】使用。";
  },
  scls_lianhua(player) {
    if (player.hasMark("scls_lianhua")) {
      return "当你成为【杀】的目标后，你摸一张牌。然后此【杀】的使用者需弃置一张牌，否则此【杀】对你无效。";
    }
    return "当你成为【杀】的目标后，你摸一张牌。";
  },
  jdjuqi(player) {
    const bool = player.storage.jdjuqi;
    let yang = "你摸三张牌；其他角色的准备阶段，其可以展示并交给你一张黑色手牌", yin = "你令你本回合使用牌无次数限制且造成的伤害+1；其他角色的准备阶段，其可以展示并交给你一张红色手牌";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。准备阶段，", end = "。";
    return `${start}阳：${yang}。阴：${yin}${end}`;
  },
  jdlongdan(player) {
    return lib.translate["jdlongdan" + (player.hasSkill("sblongdan_mark", null, null, false) ? "x" : "") + "_info"];
  },
  tylongnu(player) {
    const bool = player.hasSkill("tylongnu_yang") || player.storage.tylongnu && !player.hasSkill("tylongnu_yin");
    let yang = "失去1点体力，然后此阶段内你可以将红色手牌当无距离限制的火【杀】使用或打出", yin = "减少1点体力上限，然后此阶段内你可以将锦囊牌当无次数限制的雷【杀】使用或打出";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。游戏开始时，你可以改变此转换技的状态。出牌阶段开始时，你可以摸一张牌并：", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  tyqianshou(player) {
    const bool = player.storage.tyqianshou;
    let yang = "你可展示并交给其一张红色牌，本回合你不能使用手牌且你与其不能成为牌的目标", yin = "你可令其展示并交给你一张牌，若此牌不为黑色，你失去1点体力";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。其他角色的回合开始时，若其体力值大于你，或其未处于横置状态，", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  tyliupo(player) {
    const bool = player.storage.tyliupo;
    let yang = "所有角色不能使用【桃】", yin = "所有即将造成的伤害均视为体力流失";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。回合开始时，你令本轮：", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  yyyanggu(player) {
    const bool = player.storage.yyyanggu;
    let yang = "当你受到伤害后，你可以回复1点体力", yin = "你可以将一张手牌当作【声东击西】使用";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  hm_shice(player) {
    const bool = player.storage.hm_shice;
    let yang = "当你受到属性伤害时，若你的技能数不大于伤害来源，你可以防止此伤害并视为使用一张【火攻】", yin = "当你不因此技能使用牌指定唯一目标后，你可以令其弃置装备区任意张牌，然后此牌额外结算X次（X为其装备区的牌数）";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  }
};
const voices = {
  "#sclfudi1": "弃暗投明，为明公计！",
  "#sclfudi2": "绣虽有降心，奈何贵营难容。",
  "#juejing": "龙战于野，其血玄黄。",
  "#longhun1": "潜龙于渊，涉灵愈伤。",
  "#longhun2": "千里一怒，红莲灿世。",
  "#longhun3": "金甲映日，驱邪祛秽。",
  "#longhun4": "腾龙行云，首尾不见。",
  "#meihun1": "这个和这个不要，其他全给我吧~",
  "#meihun2": "嗯~~妾身就是喜欢这些，给我嘛~",
  "#huoxin1": "一笑倾城，一眼惑心~",
  "#huoxin2": "哎呀~妾身~能有什么坏心思呢？",
  "#shen_diaochan:die": "纵使消逝，妾影长存……",
  "#kuiwei1": "休整片刻，且随我杀出一条血路。",
  "#kuiwei2": "骑兵列队，准备突围。",
  "#yanzheng1": "任你横行霸道，我自岿然不动。",
  "#yanzheng2": "行伍严整，百战不殆。",
  "#jsp_caoren:die": "城在人在，城破人亡……",
  "#vtbguisha1": "瑰姿媚丽，沙场来战。",
  "#vtbshuli1": "姿颜姝丽，决意于众。",
  "#vtb_xiaosha:die": "哼！你欺负人家！",
  "#vtbshanwu1": "见风起舞，胡步轻盈。",
  "#vtbxianli1": "娴丽曼瑶，宜室宜家。",
  "#vtb_xiaoshan:die": "……你欺负人家！",
  "#vtbtaoyan1": "灿若桃花，琼华宴婉！",
  "#vtbyanli1": "妍姿秀丽，翩然如花。",
  "#vtb_xiaotao:die": "呜呜……你欺负人家……",
  "#vtbleyu1": "虞歌爱晚，此间为乐。",
  "#vtbyuanli1": "媛媛相携，心有灵犀。",
  "#vtb_xiaole:die": "呜呜……你欺负人家……",
  "#vtbmeiniang1": "桨醇轻启，酒香富裕。",
  "#vtbyaoli1": "酾酒临江,慷慨而旋。",
  "#vtb_xiaojiu:die": "呜呜……你欺负人家！",
  "#xiongyi1": "此时不战，更待何时！",
  "#xiongyi2": "弟兄们，我们的机会来啦！",
  "#mateng:die": "儿子，为爹报仇啊……",
  "#sijian1": "且听我最后一言！",
  "#sijian2": "忠言逆耳啊！！",
  "#suishi1": "一荣俱荣！",
  "#suishi2": "一损俱损……",
  "#tianfeng:die": "不纳吾言而反诛吾心，奈何奈何！！",
  "#jiling:die": "额，将军为何咆哮不断……",
  "#kongrong:die": "覆巢之下，岂有完卵……",
  "#zhongyi1": "忠义是为将之本。",
  "#zhongyi2": "为国尽忠，天经地义。",
  "#gzjinfa1": "居功者，当自矜，为将者，当善伐。",
  "#gzjinfa2": "此战伐敌所获，皆我之功！",
  "#pe_wenqin:die": "公休，汝这是何意，呃……",
  "#qiuan1": "明公神文圣武，吾自当举城来降。",
  "#qiuan2": "臣心不自安，乃君之过也。",
  "#liangfan1": "今举兵投魏，必可封王拜相，一展宏图。",
  "#liangfan2": "今举义军事若成，吾为复汉元勋也。",
  "#pe_mengda:die": "吾一生寡信，今报应果然来矣……",
  "#gzquanji1": "不露圭角，择时而发！",
  "#gzquanji2": "晦养厚积，乘势而起！",
  "#gzpaiyi1": "排斥异己，为王者必由之路！",
  "#gzpaiyi2": "非吾友，则必敌也！",
  "#chenggong1": "吾与主公患难之交也！",
  "#chenggong2": "我豫州人才济济，元皓之辈，不堪大用。",
  "#zhaolie1": "不灭东吴，誓不归蜀！",
  "#zhaolie2": "汝等勿劝，此战势在必行！",
  "#shichou": "尔等叛贼，害我兄弟，饶不得汝！",
  "#dahe": "燕人张飞在此！",
  "#fenyong1": "放手一搏吧！",
  "#fenyong2": "壮情豪胆，小伤何惧！",
  "#sp_xiahoudun:die": "血染沙场，马革裹尸，快哉，快哉！",
  "#yanxiao1": "倾心一笑，愿君驻足。",
  "#yanxiao2": "英雄壮志，红颜无怨。",
  "#anxian1": "大恩不言谢哦~",
  "#anxian2": "伯符，不用担心我！",
  "#manjuan": "漫卷纵酒，白首狂歌。",
  "#zuixiang": "懵懵醉乡中，天下心中藏。",
  "#sp_pangtong:die": "纵有治世才，难遇治世主……",
  "#zyshilu1": "以杀立威，谁敢反我？",
  "#zyshilu2": "将这些乱臣贼子，尽皆诛之！",
  "#zyxiongnve1": "当今天子乃我所立，他敢怎样？",
  "#zyxiongnve2": "我兄弟三人同掌禁军，有何所惧？",
  "#pe_sunchen:die": "愿陛下念臣昔日之功，陛下？陛下！！",
  "#ns_lijue:die": "若无内讧，也不至如此……",
  "#ns_fanchou:die": "唉，稚然疑心，甚重……"
};
const characterSort = {
  extra_offline: ["shen_jiaxu", "shen_diaochan", "boss_zhaoyun", "le_shen_jiaxu", "ps_shen_machao", "mark_shen_machao", "zc26_shen_huangyueying", "sm_shen_machao", "sp_sm_shen_machao", "pe_shen_zhonghui", "ps_shen_caocao"],
  offline_scl: ["scl_lvbu", "scl_pangdegong", "scl_sunhanhua", "scl_miheng", "scl_peixiu", "scl_caoying", "scl_luotong", "scl_jiaxu", "scl_zhangxiu"],
  offline_star: ["jsp_ganfuren", "sp_xiahoushi", "jsp_zhaoyun", "huangjinleishi", "sp_pangtong", "sp_daqiao", "sp_ganning", "sp_xiahoudun", "sp_lvmeng", "sp_zhangfei", "sp_liubei", "old_machao", "zhangliang", "jsp_caoren"],
  offline_sticker: ["sp_gongsunzan", "sp_simazhao", "sp_wangyuanji", "sp_xinxianying", "sp_liuxie"],
  offline_yijiang: ["ol_manchong", "ol_xinxianying", "ol_liuyu", "ol_zhangrang"],
  offline_luanwu: ["ns_lijue", "ns_zhangji", "ns_fanchou"],
  offline_yongjian: ["ns_chendao", "yj_caoang", "yj_caocao", "yj_liru", "yj_caohong", "yj_zhangfei", "yongjian_ganning", "yj_dongzhuo", "yj_xuyou", "yj_jiaxu", "yj_zhenji"],
  offline_piracyE_zy: ["pe_wangyun", "pe_zhonghui", "pe_sunchen", "pe_mengda", "pe_wenqin", "ns_caoanmin", "jiangqing", "kongrong", "jiling", "tianfeng", "mateng"],
  offline_piracyE_xk: ["xk_luoli", "xk_cuilian", "xk_penghu", "xk_shanfu", "xk_pengqi", "xk_zulang"],
  offline_piracyE_jb: ["pe_jun_caopi", "pe_jun_liushan", "pe_jun_sunquan", "pe_jun_liuxie", "pe_jun_liuhong", "pe_jun_liubei", "pe_jun_caocao", "pe_jun_sunce", "pe_jun_yuanshao", "pe_jun_zhangjiao"],
  offline_piracyE_hl: ["hl_shen_lvbu", "hl_shen_zhouyu", "hl_shen_lvmeng", "hl_shen_zhugeliang", "hl_shen_guanyu"],
  offline_piracyE: ["xia_caopi", "pe_xiahouxuan", "pe_guozhao", "pe_que", "yj_zhouji", "yj_ehuan", "yj_tianchuan", "yj_zhonghui", "yj_jiling", "yj_yongkai", "yj_chezhou", "yj_caocaoyuanshao", "pe_lishimin", "pe_xiahoulan", "pe_yangyuhuan", "pe_huamulan", "pe_liuhui", "pe_baiqi", "pe_wuzetian", "shangguanwaner", "pe_xiangyu"],
  offline_piracyE_shenmo: ["sm_shenmo_sunquan"],
  offline_piracyS: ["ns_jiaxu", "longyufei", "ps_guanyu", "ps1059_guojia", "ps2070_guojia", "ps2063_zhaoyun", "ps2067_zhaoyun", "ps1062_zhouyu", "ps2080_zhouyu", "ps_caozhi", "ps_jin_simayi", "ps_caopi", "ps_simayi", "ps2068_simayi", "ps_machao", "ps_zhugeliang", "ps2066_zhugeliang", "ps_jiaxu", "ps_lvbu", "jsp_liubei"],
  offline_piracyS_shenmo: ["ps_shen_weiyan", "ps_devil_weiyan", "ps_shen_jiangwei", "ps_devil_jiangwei"],
  offline_piracyK: ["pk_sp_duyu"],
  offline_vtuber: ["vtb_xiaosha", "vtb_xiaoshan", "vtb_xiaotao", "vtb_xiaole", "vtb_xiaojiu"],
  offline_huan: ["jun_lvbu"],
  offline_juedai: ["zj_huangchong", "zj_dingfeng", "zj_liaohua", "zj_zhugeliang", "zj_liuyuan", "zj_jiangwei", "zj_zhonghui", "xiahouhan", "zj_xiahouhan", "zj_yaokehui", "zj_huoyi", "zj_liuyin", "zj_zhugezhan", "zj_zhaotong", "zj_zhaoguang", "zj_lukang", "zj_zhugexu", "zj_hulie", "zj_zhanghu", "zj_yuechen"],
  offline_longyutao: ["drag_pangde", "drag_guanyu", "drag_caoren", "drag_lvchang"],
  offline_guansuo: ["drag_huaci", "drag_guansuo", "drag_guanyinping", "drag_guanping", "drag_zhoucang", "drag_liaohua", "drag_shendan", "drag_shenyi", "drag_luxun", "drag_xusheng", "she_lvmeng"],
  offline_taoyuan: ["ty_guanyu", "ty_sunquan", "ty_liubei", "ty_chenshi", "ty_chengjix", "ty_zhaorong", "ty_zhangnan", "ty_fengxí", "ty_liaohua", "ty_huangzhong", "ty_wuban", "ty_shicong", "ty_guanxing", "ty_shamoke", "ty_guanyinping", "ty_luxun", "ty_ganning", "ty_buzhi", "ty_tanxiong", "ty_liue", "ty_zhangda", "ty_fanjiang", "ty_anying", "ty_anyingx", "ty_wuque", "ty_yanque", "ty_wangque", "ty_shen_zhangfei", "ty_shen_guanyu", "ty_shen_liubei"],
  offline_hanmo: ["hm_bairao", "hm_guigu", "hm_bosi", "hm_nanhualaoxian", "hm_yudu", "hm_taosheng", "hm_fuyun", "hm_tangzhou", "hm_chengyuanzhi", "hm_gaosheng", "hm_heman", "hm_yanzheng", "hm_bocai", "hm_dengmao", "hm_shen_yl_luzhi", "hm_shen_huangfusong", "hm_shen_zhangjiao", "hm_shen_zhangbao", "hm_shen_zhangliang", "hm_shen_zhujun"],
  offline_jingxiang: ["jx_zhouyu", "jx_guanyu", "jx_shen_caoren", "jx_shen_liubiao"],
  offline_changan: ["ca_wangyun", "ca_lvbu", "ca_lijue", "ca_guosi", "ca_zhangji", "ca_fanchou", "ca_shen_wangyun", "ca_shen_caocao", "ca_shen_lijueguosi", "zombie_jiaxu", "zombie_zombie"],
  offline_weinan: ["wn_caocao", "wn_hansui", "wn_houxuan", "wn_chengyi", "wn_yangqiú", "wn_zhanghe", "wn_jiaxu", "wn_xuhuang", "wn_zhuling", "wn_shen_machao", "wn_shen_xuzhu"],
  offline_Europe: ["eu_kaisa", "eu_Macrinus", "eu_Ardashir", "eu_makang", "eu_yujin", "eu_zhangliao", "eu_yuejin"],
  offline_jiudin: ["jd_jin_wangyuanji", "jd_ol_huaxin", "jd_sp_yangwan", "jd_sb_huangyueying", "jd_sb_zhaoyun", "jd_sb_ganning", "jd_sb_pangtong", "jd_hanlong", "jd_jin_xiahouhui", "jd_jin_simazhao", "jd_sb_caocao", "jd_sb_sp_zhugeliang", "jd_simayan", "jd_sb_sunquan", "jd_sb_xiaoqiao", "jd_sb_guanyu", "jd_sb_jiangwei", "jd_sb_daqiao", "jd_sb_menghuo", "jd_sb_yuanshao", "jd_sb_yujin", "jd_sb_sunshangxiang", "jd_sb_liubei", "jd_sb_fazheng", "jd_sb_zhangfei", "jd_jin_simashi", "jd_sb_xuhuang"],
  offline_yanyou: ["yy_quyi", "yy_gongsunzan", "yy_wenchou", "yy_gongsunyuan", "yy_yuanshao", "yy_simayi", "yy_caorui", "yy_zhaoyun", "yy_quancong"],
  offline_jingyang: ["jy_caocao", "jy_caoren", "jy_zhugeliang", "jy_pangtong", "jy_lusu", "jy_zhangzhao", "jy_zhouyu", "jy_huanggai"],
  offline_shichangshi: ["ps_zhangrang", "ps_zhaozhong", "ps_sunzhang", "ps_bilan", "ps_xiayun", "ps_hankui", "ps_lisong", "ps_duangui", "ps_guosheng", "ps_gaowang"],
  offline_prettyDerby: ["sm_chixia", "sm_qiulin", "sm_zhuahuang", "sm_zijian", "sm_jieying", "sm_liaoyuan", "sm_dailu"],
  offline_qingshi: ["chenshou", "hs_liuxuan", "hs_caohuan", "hs_liuxie", "hs_sunhao"],
  offline_chunqiu: ["ym_friend_zhugeliang", "ym_jsrg_diaochan", "ym_zhangxingcai", "ym_guanyinping", "ym_zhaoxiang"],
  offline_fengyun: ["ps_caocao", "ps_liubei", "ps_sunquan"],
  offline_xy: ["xy_caocao", "xy_chengyu", "xy_xunyu", "xy_chengong", "xy_zhangkai", "xy_lvbu", "xy_zhangmiao", "xy_caosong"],
  offline_zc26: ["zc26_sp_xushi", "zc26_lusu", "zc26_jiaxu", "zc26_zhanghe", "zc26_taishici"],
  offline_wenxindiaolong: ["wxdl_caozhi", "wxdl_caocao", "wxdl_caopi"],
  offline_shiningGrils: ["shinin_zhenji", "shinin_wuguotai", "shinin_ruiji", "shinin_lvlingqi", "shinin_dongwan", "goblin", "lord_goblin"],
  offline_yanling: ["ylyg_xiaoqiao", "ylyg_yuji", "ylyg_pangtong", "ylyg_dianwei", "ylyg_xuhuang", "ylyg_zhurong"],
  offline_zongheng: ["zh_shen_zhangjiao"],
  offline_longxuexuanhuang: ["lx_simazhao", "lx_caomao", "lx_mb_shen_machao", "lx_simafu", "lx_simazhou", "lx_guanqiujian", "lx_wenqin", "lx_lizhaojiaobo", "lx_chengji", "lx_jiachong"]
  //offline_others: [],
};
const characterSortTranslate = {
  offline_hanmo: "风云志·汉末风云",
  offline_scl: "王者之战",
  offline_star: "桌游志·SP",
  offline_sticker: "桌游志·贴纸",
  offline_yijiang: "一将之魂",
  offline_luanwu: "文和乱武",
  offline_yongjian: "用间篇",
  offline_yongjian_prefix: "用间",
  offline_feihongyingxue: "飞鸿映雪",
  offline_piracyE_zy: "官盗E系列·战役篇",
  offline_piracyE_xk: "官盗E系列·侠客行",
  offline_piracyE_jb: "官盗E系列·君霸天下",
  offline_piracyE_hl: "官盗E系列·神霸虎牢",
  offline_piracyE_shenmo: "官盗E系列·一念神魔",
  offline_piracyE: "官盗E系列",
  offline_piracyS_shenmo: "官盗S系列·神魔传",
  offline_piracyS: "官盗S系列",
  offline_vtuber: "天书乱斗·虚拟偶像",
  offline_huan: "三国杀·幻",
  offline_piracyK: "官盗K系列",
  offline_others: "线下其他系列",
  offline_juedai: "绝代智将",
  offline_longyutao: "山河煮酒·龙起襄樊",
  offline_jingxiang: "风云志·荆襄风云",
  offline_changan: "风云志·长安风云",
  offline_weinan: "风云志·渭南风云",
  offline_taoyuan: "山河煮酒·桃园挽歌",
  offline_guansuo: "山河煮酒·关索传",
  offline_jiudin: "九鼎系列",
  offline_yanyou: "燕幽烽火",
  offline_jingyang: "荆扬对垒",
  offline_qingshi: "特典·青史翰墨",
  offline_chunqiu: "春秋演墨",
  offline_shichangshi: "蛇年限定·十常侍",
  offline_prettyDerby: "马年限定·赛马娘",
  offline_fengyun: "风云际会",
  offline_Europe: "风云志·欧陆风云",
  offline_xy: "徐兖纵横",
  offline_zc26: "26珍藏",
  offline_wenxindiaolong: "星汉灿烂·文心雕龙",
  offline_shiningGrils: "闪耀战姬",
  offline_yanling: "雁翎耀光",
  extra_offline: "神话再临·线下",
  offline_zongheng: "纵横九州",
  offline_longxuexuanhuang: "龙血玄黄·线下"
};
game.import("character", function() {
  return {
    name: "offline",
    connect: true,
    connectBanned: ["zhangliang", "yj_tianchuan"],
    character: { ...characters },
    characterSort: {
      offline: characterSort
    },
    characterFilter: { ...characterFilters },
    characterTitle: { ...characterTitles },
    characterSubstitute: {
      jd_sb_sp_zhugeliang: [["sb_zhugeliang", []]],
      shinin_zhenji: [["awaken_shinin_zhenji", []]],
      shinin_wuguotai: [["awaken_shinin_wuguotai", []]],
      shinin_ruiji: [["awaken_shinin_ruiji", []]],
      shinin_lvlingqi: [["awaken_shinin_lvlingqi", []]],
      shinin_dongwan: [["awaken_shinin_dongwan", []]],
      zc26_sp_xushi: [["zc26_sp_xushi_shadow", ["die:xushi"]]],
      sm_shenmo_sunquan: [
        ["sm_shen_sunquan", []],
        ["sm_mo_sunquan", []]
      ],
      lx_simazhao: [["lx_simazhao_shadow", ["die:jin_jsrg_simazhao"]]],
      lx_caomao: [["lx_caomao_shadow", []]]
    },
    dynamicTranslate: { ...dynamicTranslates },
    characterIntro: { ...characterIntro },
    card: { ...cards },
    skill: { ...skills },
    translate: { ...translates, ...voices, ...characterSortTranslate },
    pinyins: { ...pinyins }
  };
});
