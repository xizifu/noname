import { lib, get, game, _status, ui } from "noname";
const characters = {
  mb_shen_caocao: {
    sex: "male",
    group: "shen",
    hp: 3,
    skills: ["guixin", "feiying"],
    groupInGuozhan: "wei",
    dieAudios: ["shen_caocao"]
  },
  zhuji: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["potjiezhu", "pothuanshi"]
  },
  mb_cheliji: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mbqucheng", "mbqiangyong"],
    names: "null|null"
  },
  mb_dingshangwan: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["mbzhaofu", "mbqiliu"]
  },
  re_wangji: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["reqizhi", "rejinqu"]
  },
  re_zhoufei: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["reliangyin", "rekongsheng"],
    names: "周|null"
  },
  re_yanyan: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["rejuzhan"],
    doubleGroup: ["shu", "qun"]
  },
  mb_gaoxiang: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["mbgxchiyuan", "mberdi"]
  },
  mb_cuimao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbzhengbi", "mbfengying"],
    names: "崔|琰-毛|玠"
  },
  mb_zhongyao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["rehuomo", "mbzuoding"],
    clans: ["颍川钟氏"],
    dieAudios: ["zhongyao"]
  },
  mb_wutugu: {
    sex: "male",
    group: "qun",
    hp: 15,
    skills: ["mbranshang", "mbhanyong"],
    names: "null|null",
    dieAudios: ["wutugu"]
  },
  mb_chengyu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbshefu", "mbbenyu"],
    dieAudios: ["chengyu"]
  },
  mb_dongbai: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["mblianzhu", "mbxiahui"],
    dieAudios: ["dongbai"]
  },
  mb_zhugeguo: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["mbqirang", "mbyuhua"],
    names: "诸葛|果",
    dieAudios: ["zhugeguo"],
    clans: ["琅琊诸葛氏"]
  },
  mb_caochun: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbshanjia"],
    dieAudios: ["caochun"]
  },
  mb_xiahoumao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbtongwei", "mbcuguo"],
    names: "夏侯|楙"
  },
  o_tadun: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["youlve", "lianxi"]
  },
  o_chenlan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["jujun"]
  },
  o_meicheng: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["bixian"]
  },
  o_guanyu: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["mbwusheng", "mbyijue"]
  },
  o_sunquan: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["mbshizhong", "mbcaowei"]
  },
  mb_tw_zhangji: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbdingzhen", "mbyouye"],
    dieAudios: ["tw_zhangji"]
  },
  xin_simayi: {
    sex: "male",
    group: "shen",
    hp: 4,
    skills: ["xinrenjie", "xinbaiyin", "xinlianpo"],
    tempname: ["new_simayi"],
    groupInGuozhan: "wei",
    names: "司马|懿",
    dieAudios: ["new_simayi"]
  },
  o_tanshihuai: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mblianzhan", "mbkoulve"],
    names: "null|檀石槐"
  },
  o_lvbu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mblvezhen", "mbhengwei"]
  },
  o_gaoshun: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mbjiren", "mbjuezhi"]
  },
  hanxuan: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["mbweizhan", "mbyilv"]
  },
  hefei_zhangliao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["hefeichonglei", "hefeidangshi", "hefeiheyuzhangliao"]
  },
  hefei_lidian: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["hefeiduanjin", "hefeigaigong", "hefeiheyulidian"]
  },
  hefei_yuejin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["hefeixianjian", "hefeizherui", "hefeiheyuyuejin"]
  },
  mb_liuhui: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["mbgeyuan", "mbchongcha"]
  },
  mb_caozhi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbchongsi", "mbpeidong"]
  },
  mb_zhouyu: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["mbshouyue", "mbdieyin"]
  },
  mb_zhangzhi: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["mbshiju"]
  },
  mb_cuilingyi: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["mbcaiqiu", "mbxishang"],
    names: "崔|null"
  },
  mb_luyu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbbingfa", "mbshuxing"]
  },
  mb_caocao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mblingfa"],
    dieAudios: ["2"]
  },
  mb_caohong: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbyuanhu"],
    dieAudios: ["caohong"]
  },
  re_baosanniang: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["mbshuyong", "mbxushen", "mbzhennan"],
    dieAudios: ["baosanniang"]
  },
  mb_zerong: {
    sex: "male",
    hp: 4,
    group: "qun",
    skills: ["mbfutu", "mbjingtu", "mbjiebian"]
  },
  mb_mengda: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mbjili", "mbshishu"]
  },
  mb_caoxing: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mbjinzu", "mbanxian"]
  },
  mb_guanyinping: {
    sex: "female",
    group: "shu",
    hp: 3,
    maxHp: 4,
    skills: ["mbxuehen", "mbhuxiao", "mbwuji"],
    dieAudios: ["guanyinping"]
  },
  mb_xingdaorong: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mbkuangwu"]
  },
  wuke: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["mbanda", "mbzhuguo"],
    names: "吴|null"
  },
  mb_simazhao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbxiezheng", "mbqiantun", "mbweisi", "mbzhaoxiong"],
    img: "image/character/jsrg_simazhao.jpg",
    names: "司马|昭",
    dieAudios: ["jsrg_simazhao"],
    groupBorder: "jin",
    doubleGroup: ["wei", "qun"]
  },
  friend_cuijun: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["friendshunyi", "friendbiwei", "friendgongli_cuijun"]
  },
  friend_shitao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["friendqinying", "friendlunxiong", "friendgongli_shitao"]
  },
  mb_qinghegongzhu: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["mbzengou", "mbfeili"],
    names: "曹|null"
  },
  friend_pangtong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["friendmanjuan", "friendyangming", "friendpangtonggongli"]
  },
  friend_xushu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["friendxiaxing", "friendqihui", "friendxushugongli"]
  },
  friend_zhugeliang: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["friendyance", "friendfangqiu", "friendzhugelianggongli"],
    names: "诸葛|亮",
    clans: ["琅琊诸葛氏"]
  },
  mb_xuezong: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["mbfunan", "mbjiexun"],
    dieAudios: ["xuezong"]
  },
  mb_ganfuren: {
    sex: "female",
    group: "shu",
    hp: 3,
    skills: ["mbzhijie", "mbshushen"],
    names: "甘|null"
  },
  mb_sp_zhenji: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["mbbojian", "mbjiwei"]
  },
  mb_zhangfen: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["mbquchong", "mbxunjie"]
  },
  mb_jiachong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["mbbeini", "mbdingfa"],
    groupBorder: "jin",
    dieAudios: ["jiachong"]
  },
  mb_simazhou: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbbifeng", "mbsuwang"],
    names: "司马|伷",
    groupBorder: "jin"
  },
  mb_wenqin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbbeiming", "mbchoumang"]
  },
  mb_simafu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbpanxiang", "mbchenjie"],
    names: "司马|孚",
    groupBorder: "jin"
  },
  mb_sp_guanqiujian: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbcuizhen", "mbkuili"],
    names: "毌丘|俭"
  },
  mb_caomao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbqianlong", "mbweitong"],
    isZhugong: true
  },
  chengji: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbkuangli", "mbxiongsi"],
    groupBorder: "jin"
  },
  lizhaojiaobo: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbzuoyou", "mbshishou"],
    names: "李|昭-焦|伯"
  },
  yangfeng: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mbxuetu", "mbweiming"]
  },
  xin_huojun: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["sidai", "jieyu"],
    dieAudios: ["tw_huojun"]
  },
  muludawang: {
    sex: "male",
    group: "qun",
    hp: 3,
    maxHp: 3,
    hujia: 1,
    skills: ["shoufa", "zhoulin", "yuxiang"],
    names: "null|null"
  },
  mb_chengui: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["guimou", "zhouxian"]
  },
  mb_huban: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbyilie"]
  },
  mb_xianglang: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["naxue", "yijie"]
  },
  yanxiang: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["kujian", "ruilian"],
    dieAudios: ["tw_yanxiang"]
  },
  mb_sunluyu: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["mbmeibu", "mbmumu"],
    dieAudios: ["sunluyu"]
  },
  xin_wuban: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["xinjintao"],
    clans: ["陈留吴氏"],
    img: "image/character/wuban.jpg",
    dieAudios: ["wuban"]
  },
  baoxin: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["mutao", "yimou"],
    dieAudios: ["tw_baoxin"]
  },
  jiangji: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["twjichou", "jilun"],
    img: "image/character/tw_jiangji.jpg",
    dieAudios: ["tw_jiangji"]
  },
  liwei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jiaohua"],
    dieAudios: ["tw_liwei"]
  },
  laimin: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["laishou", "luanqun"]
  },
  xin_guozhao: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["yichong", "wufei"]
  },
  xin_zhangyi: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["xinwurong", "shizhi"]
  },
  xin_sunliang: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["xinzhizheng", "xinkuizhu", "xinlijun"],
    isZhugong: true,
    dieAudios: ["sunliang"]
  },
  re_xiaoqiao: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["retianxiang", "xinhongyan"],
    names: "桥|null"
  },
  shichangshi: {
    sex: "male",
    group: "qun",
    hp: 1,
    skills: ["mbdanggu", "mbmowang"],
    trashBin: ["sex:male_castrated"],
    names: "张|让-赵|忠-孙|璋-毕|岚-夏|恽-韩|悝-栗|嵩-段|珪-郭|胜-高|望"
  },
  re_zhangzhang: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["rezhijian", "guzheng"],
    names: "张|昭-张|纮"
  },
  qianzhao: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbshihe", "mbzhenfu"]
  },
  re_yanwen: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["reshuangxiong"],
    names: "颜|良-文|丑"
  },
  xin_zhoutai: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["buqu", "new_fenji"],
    dieAudios: ["zhoutai"]
  },
  re_caozhi: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["reluoying", "rejiushi", "chengzhang"]
  },
  re_chenqun: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["redingpin", "refaen"],
    clans: ["颍川陈氏"]
  },
  xin_caoxiu: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["qianju", "xinqingxi"]
  },
  xin_zhuhuan: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["fenli", "xinpingkou"]
  },
  sp_pengyang: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["spdaming", "spxiaoni"]
  },
  wangjun: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["zhujian", "duansuo"],
    groupBorder: "jin"
  },
  xin_zhuzhi: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["sbanguo"]
  },
  xin_wuyi: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["sbbenxi"],
    clans: ["陈留吴氏"]
  },
  yangfu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["jiebing", "hannan"]
  },
  sp_caosong: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["yijin", "guanzong"]
  },
  re_liru: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["rejuece", "remieji", "xinfencheng"]
  },
  re_dianwei: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["reqiangxi"]
  },
  xin_mamidi: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["chengye", "buxu"]
  },
  ruanhui: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["mingcha", "jingzhong"]
  },
  xin_quancong: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["sbyaoming"]
  },
  re_xunyu: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["quhu", "rejieming"],
    clans: ["颍川荀氏"]
  },
  xin_jushou: {
    sex: "male",
    group: "qun",
    hp: 2,
    maxHp: 3,
    hujia: 3,
    skills: ["xinjianying", "shibei"]
  },
  re_bulianshi: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["reanxu", "zhuiyi"]
  },
  re_caiwenji: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["rebeige", "duanchang"]
  },
  sp_jianggan: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbdaoshu", "spdaizui"]
  },
  peixiu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xingtu", "juezhi"],
    groupBorder: "jin"
  },
  re_gaoshun: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["rexianzhen", "rejinjiu"]
  },
  re_wuguotai: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["reganlu", "buyi"],
    names: "丁|null"
  },
  xin_sunxiu: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["mobileyanzhu", "mobilexingxue", "zhaofu"],
    isZhugong: true
  },
  sp_maojie: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["bingqing", "yingfeng"]
  },
  re_yufan: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["zhiyan", "rezongxuan"]
  },
  sunhanhua: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["chongxu", "miaojian", "shhlianhua"]
  },
  yanpu: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["twhuantu", "twbihuo"]
  },
  mayuanyi: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["jibing", "wangjing", "moucuan"]
  },
  fuqian: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["jueyong", "poxiang"]
  },
  xin_caozhen: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mbsidi"]
  },
  xin_sunluban: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["xinzenhui", "xinjiaojin"]
  },
  qiaozhou: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["zhiming", "xingbu"]
  },
  xin_guyong: {
    sex: "male",
    group: "wu",
    hp: 3,
    skills: ["xinshenxing", "xinbingyi"]
  },
  re_zhonghui: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["requanji", "zili"],
    clans: ["颍川钟氏"]
  },
  xin_caifuren: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["xinqieting", "xianzhou"],
    names: "蔡|null"
  },
  xin_zhoucang: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["mobilezhongyong"]
  },
  ol_yujin: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["rejieyue"],
    dieAudios: ["yujin_yujin.mp3"]
  },
  zhouqun: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["tiansuan"]
  },
  re_zhurong: {
    sex: "female",
    group: "shu",
    hp: 4,
    skills: ["juxiang", "relieren"],
    doubleGroup: ["shu", "qun"],
    names: "null|null"
  },
  re_jiangwei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["retiaoxin", "zhiji"]
  },
  nanhualaoxian: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["yufeng", "tianshu"],
    names: "庄|周"
  },
  re_handang: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["regongji", "jiefan"]
  },
  re_dengai: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["retuntian", "zaoxian"]
  },
  xin_fuhuanghou: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["xinzhuikong", "xinqiuyuan"]
  },
  xin_panzhangmazhong: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["xinduodao", "xinanjian"],
    names: "潘|璋-马|忠"
  },
  xin_guohuai: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["mobilejingce"]
  },
  gongsunkang: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["juliao", "taomie"],
    names: "公孙|康"
  },
  xin_zhangfei: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["new_repaoxiao", "liyong"],
    dieAudios: ["re_zhangfei"]
  },
  xin_hansui: {
    sex: "male",
    group: "qun",
    hp: 4,
    maxHp: 4,
    hujia: 1,
    skills: ["xinniluan", "xiaoxi_hansui"],
    dieAudios: ["hansui"]
  },
  hucheer: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["daoji"]
  },
  re_lingtong: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["rexuanfeng", "reyongjin"]
  },
  re_liubiao: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zishou", "rezongshi"]
  },
  simashi: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["baiyi", "jinglve", "shanli"],
    names: "司马|师",
    groupBorder: "jin"
  },
  yanghuiyu: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["hongyi", "requanfeng"],
    groupBorder: "jin"
  },
  xin_zhuran: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["mobiledanshou"]
  },
  xin_gongsunzan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["xinyicong", "qiaomeng"],
    dieAudios: ["gongsunzan"],
    names: "公孙|瓒"
  },
  dingyuan: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["beizhu"]
  },
  xin_jianyong: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["xinqiaoshui", "xinjyzongshi"]
  },
  xin_caozhang: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["rejiangchi"]
  },
  xin_liaohua: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["redangxian", "refuli"]
  },
  furong: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["xuewei", "liechi"]
  },
  re_dongzhuo: {
    sex: "male",
    group: "qun",
    hp: 8,
    skills: ["rejiuchi", "roulin", "benghuai", "baonue"],
    isZhugong: true
  },
  re_xusheng: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["repojun"]
  },
  sp_sufei: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["zhengjian", "gaoyuan"]
  },
  re_sunjian: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["gzyinghun", "repolu"]
  },
  zhengxuan: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zhengjing"]
  },
  dengzhi: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["jimeng", "shuaiyan"]
  },
  xin_chengpu: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["relihuo", "chunlao"]
  },
  yangyi: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["duoduan", "gongsun"]
  },
  dongcheng: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["chengzhao"]
  },
  re_pangtong: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["xinlianhuan", "niepan"]
  },
  re_guanqiujian: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["rezhengrong", "rehongju"],
    names: "毌丘|俭",
    dieAudios: ["guanqiujian"]
  },
  chendeng: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zhouxuan", "fengji"]
  },
  re_heqi: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["reqizhou", "reshanxi"],
    tempname: ["heqi"],
    dieAudios: ["heqi"]
  },
  yangbiao: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zhaohan", "rangjie", "yizheng"],
    clans: ["弘农杨氏"]
  },
  re_sp_zhugeliang: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["bazhen", "rehuoji", "rekanpo"],
    names: "诸葛|亮",
    clans: ["琅琊诸葛氏"]
  },
  xin_xiahoudun: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["reganglie", "xinqingjian"],
    names: "夏侯|惇",
    dieAudios: ["re_xiahoudun"]
  },
  zhangyì: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["rezhiyi"]
  },
  jiakui: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["tongqu", "xinwanlan"]
  },
  re_jikang: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["new_qingxian", "new_juexiang"],
    dieAudios: ["jikang"]
  },
  miheng: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["kuangcai", "shejian"]
  },
  taoqian: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["zhaohuo", "yixiang", "yirang"]
  },
  liuzan: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["fenyin"]
  },
  lingcao: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["dujin"]
  },
  sunru: {
    sex: "female",
    group: "wu",
    hp: 3,
    skills: ["yingjian", "shixin"]
  },
  lifeng: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["tunchu", "shuliang"]
  },
  zhuling: {
    sex: "male",
    group: "wei",
    hp: 4,
    skills: ["xinzhanyi"]
  },
  liuye: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["polu", "choulve"]
  },
  zhaotongzhaoguang: {
    sex: "male",
    group: "shu",
    hp: 4,
    skills: ["yizan_use", "xinfu_longyuan"],
    names: "赵|统-赵|广"
  },
  majun: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["xinfu_jingxie", "qiaosi"]
  },
  simazhao: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["xinfu_daigong", "xinfu_zhaoxin"],
    names: "司马|昭",
    groupBorder: "jin"
  },
  wangyuanji: {
    sex: "female",
    group: "wei",
    hp: 3,
    skills: ["xinfu_qianchong", "xinfu_shangjian"],
    groupBorder: "jin"
  },
  pangdegong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["xinfu_pingcai", "xinfu_pdgyingshi"]
  },
  old_yuanshu: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["xinyongsi", "yjixi"],
    dieAudios: ["yuanshu"]
  },
  zhangbu: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["mbchengxiong", "mbwangzhuang"]
  },
  mb_wangjing: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["mbzujin", "mbjiejian"]
  },
  shenpei: {
    sex: "male",
    group: "qun",
    hp: 2,
    maxHp: 3,
    skills: ["shouye", "liezhi"]
  },
  re_wangyun: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["relianji", "remoucheng"],
    clans: ["太原王氏"],
    dieAudios: ["wangyun"]
  },
  hujinding: {
    sex: "female",
    group: "shu",
    hp: 2,
    maxHp: 6,
    skills: ["renshi", "wuyuan", "huaizi"]
  },
  re_zhanggong: {
    sex: "male",
    group: "wei",
    hp: 3,
    skills: ["reqianxin", "rezhenxing"],
    dieAudios: ["zhanggong"]
  },
  re_xugong: {
    sex: "male",
    group: "qun",
    hp: 3,
    skills: ["rebiaozhao", "yechou"],
    doubleGroup: ["wu", "qun"],
    dieAudios: ["xugong"]
  },
  re_weiwenzhugezhi: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["refuhai"],
    names: "卫|温-诸葛|直",
    dieAudios: ["weiwenzhugezhi"]
  },
  xin_yuanshao: {
    sex: "male",
    group: "qun",
    hp: 4,
    skills: ["reluanji", "xueyi"],
    isZhugong: true
  },
  re_liushan: {
    sex: "male",
    group: "shu",
    hp: 3,
    skills: ["xiangle", "refangquan", "ruoyu"],
    isZhugong: true
  },
  re_sunben: {
    sex: "male",
    group: "wu",
    hp: 4,
    skills: ["jiang", "rehunzi", "zhiba"],
    isZhugong: true
  },
  scs_zhangrang: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scstaoluan"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_zhaozhong: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scschiyan"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_sunzhang: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scszimou"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_bilan: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scspicai"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_xiayun: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scsyaozhuo"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_hankui: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scsxiaolu"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_lisong: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scskuiji"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_duangui: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scschihe"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_guosheng: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scsniqu"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  scs_gaowang: {
    sex: "male",
    group: "qun",
    hp: 0,
    skills: ["scsmiaoyu"],
    isUnseen: true,
    trashBin: ["sex:male_castrated"]
  },
  mb_lingju: {
    sex: "female",
    group: "qun",
    hp: 3,
    skills: ["mbjieyuan", "mbfenxin"],
    names: "吕|null",
    dieAudios: ["lingju"]
  },
  mb_shen_lvbu: {
    sex: "male",
    group: "shen",
    hp: 5,
    skills: ["baonu", "mbwumou", "mbwuqian", "ol_shenfen"],
    groupInGuozhan: "qun",
    dieAudios: ["shen_lvbu"]
  }
};
const cards$1 = {
  //神肘不疑的五灵卡牌
  //何意味，不是手刹神华佗吗
  wuqinxi_hu: {
    fullskin: true,
    noname: true
  },
  wuqinxi_lu: {
    fullskin: true,
    noname: true
  },
  wuqinxi_xiong: {
    fullskin: true,
    noname: true
  },
  wuqinxi_yuan: {
    fullskin: true,
    noname: true
  },
  wuqinxi_he: {
    fullskin: true,
    noname: true
  },
  hefei_xianjian: {
    derivation: "hefei_yuejin",
    fullskin: true,
    image: "image/card/hefei_xianjian.png",
    type: "equip"
  },
  cz_liulongcanjia: {
    fullskin: true,
    cardimage: "liulongcanjia",
    derivation: "mb_caozhi",
    cardcolor: "heart",
    type: "equip",
    subtype: "equip5",
    skills: ["cz_liulongcanjia_skill"]
  },
  ly_piliche: {
    fullskin: true,
    //vanish:true,
    derivation: "liuye",
    cardcolor: "diamond",
    type: "equip",
    subtype: "equip1",
    distance: { attackFrom: -8 },
    skills: ["ly_piliche"]
    //destroy:'polu'
  },
  wolong_card: {
    type: "takaramono",
    fullskin: true
    //derivation:"pangdegong",
  },
  fengchu_card: {
    type: "takaramono",
    fullskin: true
    //derivation:"pangdegong",
  },
  xuanjian_card: {
    fullskin: true,
    type: "takaramono"
    //derivation:"pangdegong",
  },
  shuijing_card: {
    fullskin: true,
    type: "takaramono"
    //derivation:"pangdegong",
  },
  rewrite_bagua: {
    derivation: "majun",
    //cardimage:"bagua",
    fullskin: true,
    type: "equip",
    subtype: "equip2",
    ai: {
      basic: {
        equipValue: 7.5
      }
    },
    skills: ["rw_bagua_skill"]
  },
  rewrite_baiyin: {
    derivation: "majun",
    fullskin: true,
    //cardimage:"baiyin",
    type: "equip",
    subtype: "equip2",
    loseDelay: false,
    onLose() {
      player.addTempSkill("rw_baiyin_skill_lose");
    },
    skills: ["rw_baiyin_skill"],
    tag: {
      recover: 1
    },
    ai: {
      order: 9.5,
      equipValue(card2, player2) {
        if (player2.hp == player2.maxHp) {
          return 5;
        }
        if (player2.countCards("h", "rewrite_baiyin")) {
          return 6;
        }
        return 0;
      },
      basic: {
        equipValue: 5
      }
    }
  },
  rewrite_lanyinjia: {
    derivation: "majun",
    //cardimage:"lanyinjia",
    fullskin: true,
    type: "equip",
    subtype: "equip2",
    skills: ["rw_lanyinjia", "lanyinjia2"],
    ai: {
      equipValue: 6,
      basic: {
        equipValue: 1
      }
    }
  },
  rewrite_renwang: {
    derivation: "majun",
    //cardimage:"renwang",
    fullskin: true,
    type: "equip",
    subtype: "equip2",
    skills: ["rw_renwang_skill"],
    ai: {
      basic: {
        equipValue: 7.5
      }
    }
  },
  rewrite_tengjia: {
    derivation: "majun",
    //cardimage:"tengjia",
    fullskin: true,
    type: "equip",
    subtype: "equip2",
    ai: {
      equipValue(card2, player2) {
        if (player2.hasSkillTag("maixie") && player2.hp > 1) {
          return 0;
        }
        if (player2.hasSkillTag("noDirectDamage")) {
          return 10;
        }
        if (get.damageEffect(player2, player2, player2, "fire") >= 0) {
          return 10;
        }
        var num = 3 - game.countPlayer(function(current) {
          return get.attitude(current, player2) < 0;
        });
        if (player2.hp == 1) {
          num += 4;
        }
        if (player2.hp == 2) {
          num += 1;
        }
        if (player2.hp == 3) {
          num--;
        }
        if (player2.hp > 3) {
          num -= 4;
        }
        return num;
      },
      basic: {
        equipValue: 3
      }
    },
    skills: ["rw_tengjia1", "rw_tengjia2", "rw_tengjia3", "rw_tengjia4"]
  },
  rewrite_zhuge: {
    derivation: "majun",
    //cardimage:"zhuge",
    distance: {
      attackFrom: -2
    },
    fullskin: true,
    type: "equip",
    subtype: "equip1",
    ai: {
      equipValue(card2, player2) {
        if (player2._rewrite_zhuge_temp) {
          return 1;
        }
        player2._rewrite_zhuge_temp = true;
        const result = (function() {
          if (!game.hasPlayer(function(current) {
            return player2.canUse("sha", current) && get.effect(current, { name: "sha" }, player2, player2) > 0;
          })) {
            return 1;
          }
          if (player2.hasSha() && _status.currentPhase == player2) {
            if (player2.getEquip("zhuge") || player2.getCardUsable("sha") == 0) {
              return 10;
            }
          }
          var num = player2.countCards("h", "sha");
          if (num > 1) {
            return 6 + num;
          }
          return 3 + num;
        })();
        delete player2._rewrite_zhuge_temp;
        return result;
      },
      basic: {
        equipValue: 5
      },
      tag: {
        valueswap: 1
      }
    },
    skills: ["rw_zhuge_skill"]
  },
  //大攻车
  dagongche_attack: {
    cardimage: "dagongche",
    fullskin: true,
    derivation: "mb_zhangfen",
    type: "equip",
    subtype: "equip1",
    destroyLog: false,
    distance: { attackFrom: -8 },
    ai: {
      equipValue: 10,
      basic: {
        equipValue: 10
      }
    },
    cardPrompt(card2) {
      if (!card2.storage || typeof card2.storage.mbquchong != "number") {
        return lib.translate["dagongche_attack_info"];
      }
      let str = "②此牌剩余" + parseFloat(card2.storage.mbquchong) + "点耐久度，耐久度为0时销毁此牌。";
      return str + "①当此牌进入你的装备区时，弃置你装备区里的其他牌。②其他装备区进入你的装备区前，改为将这些牌置于弃牌堆。③当你对一名角色造成伤害时，你可减少1点此牌的耐久度，令此伤害+X（X为游戏轮数且至多为3）。④当此牌不因〖渠冲〗离开装备区时，减少1点此牌的耐久度并防止之。";
    },
    skills: ["dagongche_attack_skill", "mbquchong_effect"]
  },
  dagongche_defend: {
    cardimage: "dagongche",
    fullskin: true,
    derivation: "mb_zhangfen",
    type: "equip",
    subtype: "equip1",
    destroyLog: false,
    distance: { attackFrom: -8 },
    ai: {
      equipValue: 10,
      basic: {
        equipValue: 10
      }
    },
    cardPrompt(card2) {
      if (!card2.storage || typeof card2.storage.mbquchong != "number") {
        return lib.translate["dagongche_defend_info"];
      }
      let str = "②此牌剩余" + parseFloat(card2.storage.mbquchong) + "点耐久度，耐久度为0时销毁此牌。";
      return str + "①当此牌进入你的装备区时，弃置你装备区里的其他牌。②其他装备区进入你的装备区前，改为将这些牌置于弃牌堆。③当你受到伤害时，减少X点此牌的耐久度，令此伤害-X（X为伤害值且至多为此牌耐久度）。④当此牌不因〖渠冲〗离开装备区时，减少1点此牌的耐久度并防止之。";
    },
    skills: ["dagongche_defend_skill", "mbquchong_effect"]
  },
  xuanjian: {
    derivation: "friend_xushu",
    distance: { attackFrom: -2 },
    fullskin: true,
    type: "equip",
    subtype: "equip1",
    ai: {
      equipValue(card2, player2) {
        return 4;
      },
      basic: { equipValue: 5 },
      tag: { valueswap: 1 }
    },
    cardPrompt(card2, player2) {
      if (!player2?.hasSkill("friendxushugongli")) {
        return lib.translate.xuanjian_info;
      }
      return (() => {
        if (!get.info("friendgongli").isFriendOf(player2, "friend_zhugeliang")) {
          return lib.translate.xuanjian_info;
        }
        return "你可以将一张手牌当作【杀】使用。";
      })().slice(0, -1) + (get.info("friendgongli").isFriendOf(player2, "friend_pangtong") ? "（无距离限制）" : "") + "。";
    },
    skills: ["xuanjian_skill"]
  },
  mbjianji_card: {
    ai: {
      basic: {
        value: 1
      }
    }
    //image: 'image/card/mbjianji_card.png',
    //fullimage: true,
  }
};
const pinyins = {};
const skills = {
  //神吕布
  mbwumou: {
    audio: "wumou",
    getfilter: (card2) => card2.name !== "sha" || get.number(card2) === "unsure" || !card2?.cards?.length || card2?.cards?.length > 1 || lib.card[card2.cards[0].name].type !== "trick",
    mod: {
      cardname(card2) {
        const event = get.event();
        if (!["chooseToUse", "chooseToRespond"].includes(event.name)) return;
        if (lib.card[card2.name].type === "trick" && !_status._mbwumou_check) return "sha";
      },
      selectTarget(card2, player2, range) {
        const event = get.event();
        if (!["chooseToUse", "chooseToRespond"].includes(event.name)) return;
        if (get.info("mbwumou").getfilter(card2)) return;
        let cardx = card2.cards[0];
        let select = lib.card[cardx.name]?.selectTarget;
        let res = [1, 1];
        if (typeof select === "number") {
          res = [select, select];
        } else if (get.itemtype(select) === "select") {
          res = select;
        } else if (typeof select === "function") {
          let num = select(cardx, player2);
          res = typeof num === "number" ? [num, num] : num;
        }
        if (res) {
          [range[0], range[1]] = res;
        }
        game.checkMod(cardx, player2, range, "selectTarget", player2);
      },
      cardEnabled(card2, player2, event) {
        event = event || get.event();
        if (!["chooseToUse", "chooseToRespond"].includes(event?.name)) return;
        if (get.info("mbwumou").getfilter(card2) || _status._mbwumou_check) return;
        _status._mbwumou_check = true;
        const sha = get.autoViewAs({ name: "sha", cards: card2.cards }, card2.cards);
        const bool = game.hasPlayer((target) => lib.filter.filterTarget(card2.cards[0], player2, target) && lib.filter.targetEnabledx(sha, player2, target));
        delete _status._mbwumou_check;
        return bool;
      },
      playerEnabled(card2, player2, target) {
        const event = get.event();
        if (!["chooseToUse", "chooseToRespond"].includes(event.name)) return;
        if (get.info("mbwumou").getfilter(card2) || _status._mbwumou_check) return;
        _status._mbwumou_check = true;
        const sha = get.autoViewAs({ name: "sha", cards: card2.cards }, card2.cards);
        const bool = lib.filter.filterTarget(card2.cards[0], player2, target) && lib.filter.targetEnabledx(sha, player2, target);
        delete _status._mbwumou_check;
        return bool;
      },
      targetInRange(card2, player2, target) {
        const event = get.event();
        if (!["chooseToUse", "chooseToRespond"].includes(event.name)) return;
        if (get.info("mbwumou").getfilter(card2) || _status._mbwumou_check) return;
        _status._mbwumou_check = true;
        const bool = lib.filter.filterTarget(card2.cards[0], player2, target);
        delete _status._mbwumou_check;
        return bool;
      }
    },
    trigger: { player: "useCard1" },
    filter(event, player2) {
      return !get.info("mbwumou").getfilter(event.card);
    },
    forced: true,
    async content(event, trigger, player2) {
    },
    ai: { halfneg: true }
  },
  mbwuqian: {
    audio: "ol_wuqian",
    enable: "phaseUse",
    filter(event, player2) {
      return player2.countMark("baonu") >= 2 && game.hasPlayer((target) => lib.skill.mbwuqian.filterTarget(null, player2, target));
    },
    filterTarget(card2, player2, target) {
      return !target.getStorage("mbwuqian_targeted").includes(player2);
    },
    async content(event, trigger, player2) {
      const { target } = event;
      player2.removeMark("baonu", 2);
      target.addSkill("mbwuqian_targeted");
      target.markAuto("mbwuqian_targeted", [player2]);
      player2.addSkill("mbwuqian_wushuang");
      await player2.addAdditionalSkills("mbwuqian_wushuang", "wushuang");
    },
    ai: {
      order(item, player2) {
        return get.order({ name: "sha" }, player2) + 0.1;
      },
      result: { target: -1 },
      combo: "baonu"
    },
    locked: false,
    mod: {
      cardUsable(card2, player2, num) {
        if (card2.name === "sha") return num + game.countPlayer((target) => target.getStorage("mbwuqian_targeted").includes(player2));
      }
    },
    derivation: "wushuang",
    group: "mbwuqian_phaseEnd",
    subSkill: {
      targeted: {
        charlotte: true,
        init(player2, skill) {
          player2.addTip(skill, `${get.translation(skill)} 防具失效`);
        },
        intro: {
          nocount: true,
          onunmark(storage, player2, skill) {
            player2.removeTip(skill);
            player2.removeSkill(skill);
          },
          content: "防具牌失效"
        },
        ai: { unequip2: true }
      },
      wushuang: {
        charlotte: true,
        trigger: { player: ["useCardAfter", "die"] },
        filter(event, player2) {
          if (event.name === "die") return true;
          return get.is.damageCard(event.card) && !player2.hasHistory("sourceDamage", (evt) => evt.card === event.card);
        },
        silent: true,
        forceDie: true,
        async content(event, trigger, player2) {
          for (const target of game.filterPlayer()) target.unmarkAuto("mbwuqian_targeted", [player2]);
          player2.removeSkill(event.name);
        }
      },
      phaseEnd: {
        audio: "ol_wuqian",
        trigger: { player: "phaseEnd" },
        filter(event, player2) {
          return !player2.hasCard((card2) => get.is.damageCard(card2), "h");
        },
        forced: true,
        async content(event, trigger, player2) {
          let card2 = get.cardPile((card3) => get.is.damageCard(card3));
          if (card2) await player2.gain(card2, "draw");
        }
      }
    }
  },
  //手杀小车
  mbqucheng: {
    audio: 2,
    forced: true,
    mod: {
      targetInRange(card2) {
        if (card2.name == "sha") {
          return true;
        }
      },
      playerEnabled(card2, player2, target) {
        const targets2 = [player2.next, player2.previous];
        if (!card2?.storage?.mbqucheng_next && !card2?.storage?.mbqucheng_previous && !targets2.includes(target) && card2.name == "sha") {
          return false;
        }
      }
    },
    trigger: { player: "useCardAfter" },
    filter(event, player2) {
      const targets2 = event.targets;
      if (!targets2 || targets2.length != 1) {
        return false;
      }
      const target = targets2[0], card2 = event.card;
      if (card2.name != "sha" || player2.hasHistory("sourceDamage", (evt) => evt.card == card2)) {
        return false;
      }
      if (!card2.storage?.mbqucheng_next && !card2.storage?.mbqucheng_previous) {
        return [target.next, target.previous].some((t) => t != player2);
      }
      let targetx, cardx;
      if (card2.storage?.mbqucheng_next) {
        targetx = target.next;
        cardx = get.autoViewAs({ name: "sha", isCard: true, storage: { mbqucheng_next: true } });
      }
      if (card2.storage?.mbqucheng_previous) {
        targetx = target.previous;
        cardx = get.autoViewAs({ name: "sha", isCard: true, storage: { mbqucheng_previous: true } });
      }
      return player2 != targetx && player2.canUse(cardx, targetx, false);
    },
    async content(event, trigger, player2) {
      const card2 = trigger.card, target = trigger.targets[0];
      let targetx, cardx;
      if (!card2.storage?.mbqucheng_next && !card2.storage?.mbqucheng_previous) {
        let result;
        if (player2 != target.next && player2 != target.previous) {
          result = await player2.chooseControl({
            controls: ["下家", "上家"],
            prompt: "请选择驱乘的方向",
            ai() {
              return get.event().controls.slice(0).randomGet();
            }
          }).forResult();
        }
        if (player2 == target.next || typeof result?.control == "string" && result.control == "上家") {
          targetx = target.previous;
          cardx = get.autoViewAs({ name: "sha", isCard: true, storage: { mbqucheng_previous: true } });
        } else {
          targetx = target.next;
          cardx = get.autoViewAs({ name: "sha", isCard: true, storage: { mbqucheng_next: true } });
        }
      } else {
        if (card2.storage.mbqucheng_next) {
          targetx = target.next;
          cardx = get.autoViewAs({ name: "sha", isCard: true, storage: { mbqucheng_next: true } });
        } else {
          targetx = target.previous;
          cardx = get.autoViewAs({ name: "sha", isCard: true, storage: { mbqucheng_previous: true } });
        }
      }
      if (player2.canUse(cardx, targetx, false)) {
        await player2.useCard(cardx, targetx);
      }
      if (trigger.addCount != false) {
        trigger.addCount = false;
        const stat = player2.getStat("card"), name = trigger.card.name;
        if (typeof stat[name] == "number" && stat[name] > 0) {
          stat[name]--;
        }
      }
    }
  },
  mbqiangyong: {
    audio: 4,
    logAudio: (index) => typeof index == "number" ? "mbqiangyong" + index + ".mp3" : 2,
    forced: true,
    trigger: { source: "damageBegin2" },
    filter(event, player2) {
      return event.card?.name == "sha";
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      const num = player2.countHistory("useCard", (evt) => evt.card.name == "sha");
      if (trigger.player.countDiscardableCards(player2, "he")) {
        await player2.discardPlayerCard({
          target: trigger.player,
          selectButton: num,
          forced: true,
          position: "he",
          allowChooseAll: true
        });
      }
      if (!trigger.player.countCards("h")) {
        player2.logSkill(event.name, trigger.player, null, null, [get.rand(3, 4)]);
        trigger.num++;
      }
    }
  },
  //手杀丁尚涴
  mbzhaofu: {
    audio: 2,
    enable: "chooseToUse",
    usable: 1,
    basicList: ["sha", "shan", "jiu", "tao"],
    derivation: ["sha", "shan", "jiu", "tao"].map((i) => "mbzhaofu_" + i),
    filter(event, player2) {
      if (player2.countCards("h") != player2.countDiscardableCards(player2, "h") || !player2.countCards("h")) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (!get.info("mbzhaofu").basicList.includes(info[2])) {
          return false;
        }
        return event.filterCard(
          get.autoViewAs(
            {
              name: info[2],
              nature: info[3]
            },
            "unsure"
          ),
          player2,
          event
        );
      }).length;
    },
    chooseButton: {
      dialog(event, player2) {
        const list = get.inpileVCardList((info) => {
          if (!get.info("mbzhaofu").basicList.includes(info[2])) {
            return false;
          }
          return event.filterCard(
            get.autoViewAs(
              {
                name: info[2],
                nature: info[3]
              },
              "unsure"
            ),
            player2,
            event
          );
        });
        return ui.create.dialog("照抚", [list, "vcard"]);
      },
      filter(button, player2) {
        return _status.event.getParent().filterCard(
          {
            name: button.link[2]
          },
          player2,
          _status.event.getParent()
        );
      },
      check(button) {
        if (_status.event.getParent().type != "phase") {
          return 1;
        }
        const player2 = get.player();
        return player2.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player2) {
        return {
          filterCard: () => false,
          selectCard: -1,
          audio: "mbzhaofu",
          popname: true,
          viewAs: {
            name: links[0][2],
            nature: links[0][3],
            isCard: true
          },
          log: false,
          async precontent(event, trigger, player3) {
            player3.logSkill("mbzhaofu");
            event.result.cards = [];
            const cards2 = player3.getCards("h"), name = event.result.card.name;
            await player3.modedDiscard({ cards: cards2 });
            if (cards2.length == get.info("mbzhaofu").basicList.indexOf(name) + 1) {
              const result = await player3.chooseTarget({
                prompt: get.prompt("mbzhaofu"),
                prompt2: "你可以令一名其他角色获得照抚·" + get.translation(name),
                filterTarget: lib.filter.notMe,
                ai(target) {
                  return get.attitude(get.player(), target);
                }
              }).forResult();
              if (result?.bool && result.targets?.length) {
                const target = result.targets[0];
                player3.line(target);
                const skill = "mbzhaofu_" + name;
                await target.addTempSkills(skill, { player: "phaseEnd" });
              }
            }
          }
        };
      },
      prompt(links, player2) {
        return "弃置所有手牌并视为使用一张" + (get.translation(links[0][3]) || "") + get.translation(links[0][2]);
      }
    },
    hiddenCard(player2, name) {
      if (!lib.inpile.includes(name) || !get.info("mbzhaofu").basicList.includes(name)) {
        return false;
      }
      return player2.countCards("h") == player2.countDiscardableCards(player2, "h") && player2.countCards("h");
    },
    ai: {
      fireAttack: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2) {
        if (!player2.countCards("h")) {
          return false;
        }
      },
      order: 1,
      result: {
        player(player2) {
          if (_status.event.dying) {
            return get.attitude(player2, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: {
      backup: {}
    }
  },
  mbzhaofu_sha: {
    enable: "chooseToUse",
    usable: 1,
    filterCard: () => false,
    selectCard: -1,
    manualConfirm: true,
    viewAsFilter(player2) {
      return player2.countCards("h") == player2.countDiscardableCards(player2, "h") && player2.countCards("h");
    },
    viewAs: {
      name: "sha",
      isCard: true
    },
    log: false,
    prompt: "弃置所有手牌并视为使用一张【杀】",
    async precontent(event, trigger, player2) {
      player2.logSkill(event.name.slice(4));
      await player2.removeSkills(event.name.slice(4));
      const cards2 = player2.getCards("h");
      await player2.modedDiscard({ cards: cards2 });
    }
  },
  mbzhaofu_shan: {
    inherit: "mbzhaofu_sha",
    viewAs: {
      name: "shan",
      isCard: true
    },
    prompt: "弃置所有手牌并视为使用一张【闪】"
  },
  mbzhaofu_jiu: {
    inherit: "mbzhaofu_sha",
    viewAs: {
      name: "jiu",
      isCard: true
    },
    prompt: "弃置所有手牌并视为使用一张【酒】"
  },
  mbzhaofu_tao: {
    inherit: "mbzhaofu_sha",
    viewAs: {
      name: "tao",
      isCard: true
    },
    prompt: "弃置所有手牌并视为使用一张【桃】"
  },
  mbqiliu: {
    audio: 2,
    forced: true,
    trigger: {
      player: ["loseAfter", "useCardAfter"],
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    filter(event, player2, name) {
      if (event.name == "useCard") {
        return event.card.name == "tao" && player2 != _status.currentPhase && event.targets?.some((target) => target != player2);
      }
      if (player2.countCards("h") || player2.hasSkill("mbqiliu_round")) {
        return false;
      }
      if (event.getParent(2).name != "pre_mbzhaofu_backup") {
        return false;
      }
      const evt = event.getl(player2), num = player2.actionHistory.filter((evt2) => {
        return evt2.isMe && !evt2.isSkipped;
      }).length - (player2 == _status.currentPhase ? 1 : 0);
      if (!evt?.hs?.length || evt.player != player2) {
        return false;
      }
      return num > 0;
    },
    logTarget(event, player2) {
      if (event.name == "useCard") {
        return event.targets.filter((target) => target != player2).sortBySeat();
      }
      return player2;
    },
    async content(event, trigger, player2) {
      if (trigger.name == "useCard") {
        player2.addTempSkill(event.name + "_effect");
        player2.markAuto(event.name + "_effect", event.targets);
        event.targets.forEach((target) => target.addTempSkill(event.name + "_mark"));
      } else {
        const num = player2.actionHistory.filter((evt) => {
          return evt.isMe && !evt.isSkipped;
        }).length - (player2 == _status.currentPhase ? 1 : 0);
        await player2.draw({ num });
        player2.addTempSkill(event.name + "_round", "roundStart");
      }
    },
    subSkill: {
      round: { charlotte: true },
      mark: {
        charlotte: true,
        mark: true,
        intro: { content: "防止你本回合下次受到的伤害" }
      },
      effect: {
        audio: "mbqiliu",
        charlotte: true,
        forced: true,
        onremove: true,
        trigger: { global: "damageBegin4" },
        filter(event, player2) {
          return event.player?.isIn() && player2.getStorage("mbqiliu_effect").includes(event.player);
        },
        logTarget: "player",
        async content(event, trigger, player2) {
          const {
            targets: [target]
          } = event;
          trigger.cancel();
          player2.unmarkAuto(event.name, [target]);
          target.removeSkill("mbqiliu_mark");
          player2.tempBanSkill("mbqiliu");
        }
      }
    }
  },
  //界王基
  reqizhi: {
    audio: 4,
    logAudio: () => 2,
    chargeSkill: 3,
    beginMarkCount: 1,
    init(player2, skill) {
      const num = lib.skill[skill].beginMarkCount;
      player2.addCharge(num, false);
    },
    enable: "phaseUse",
    filterTarget: true,
    filter(event, player2) {
      return player2.countCharge();
    },
    async content(event, trigger, player2) {
      const target = event.target;
      player2.removeCharge(1);
      player2.addTempSkill(event.name + "_effect");
      player2.markAuto(event.name + "_effect", [target]);
      await target.draw({ num: 3 });
    },
    ai: {
      order: 1,
      result: {
        player(player2) {
          if (player2.countCharge() > 2 || player2.getCardUsable("sha", true)) {
            return 1;
          }
          return 0;
        },
        target: 1
      }
    },
    subSkill: {
      effect: {
        audio: ["reqizhi3.mp3", "reqizhi4.mp3"],
        charlotte: true,
        onremove: true,
        forced: true,
        intro: {
          content: "本回合使用基本牌或普通锦囊牌指定目标后，依次观看$的手牌并弃置其中一张"
        },
        trigger: {
          player: "useCardToPlayered"
        },
        filter(event, player2) {
          return event.isFirstTarget && event.targets?.length && ["basic", "trick"].includes(get.type(event.card)) && player2.getStorage("reqizhi_effect").some((target) => target.countCards("h"));
        },
        logTarget(event, player2) {
          return player2.getStorage("reqizhi_effect").filter((target) => target.countCards("h")).sortBySeat();
        },
        async content(event, trigger, player2) {
          const targets2 = event.targets;
          await game.doAsyncInOrder(targets2, async (target) => player2.discardPlayerCard({ target, forced: true, position: "h", visible: true }));
        }
      }
    }
  },
  rejinqu: {
    audio: 4,
    logAudio: (event) => event.name == "useCard" ? 2 : ["rejinqu3.mp3", "rejinqu4.mp3"],
    forced: true,
    trigger: {
      player: ["useCardAfter", "phaseJieshuBegin"]
    },
    filter(event, player2) {
      const storage = player2.getStorage("rejinqu", [0, 0]);
      return event.name == "useCard" ? storage[0] >= 3 && game.hasPlayer((target) => target.countCharge(true)) : storage[1] > 0;
    },
    init(player2, skill) {
      if (!player2.storage[skill]) {
        player2.setStorage(skill, [0, 0], true);
      }
      player2.addSkill(skill + "_mark");
    },
    onremove(player2, skill) {
      delete player2.storage[skill];
      player2.removeSkill(skill + "_mark");
    },
    marktext: "趋",
    intro: {
      name: "趋",
      markcount(storage) {
        storage = storage ?? [0, 0];
        let str = storage[0].toString();
        if (storage[1] > 0) {
          str += `/${storage[1]}`;
        }
        return str;
      },
      content(storage) {
        storage = storage ?? [0, 0];
        let str = `<li>使用牌数：${storage[0]}/3`;
        if (storage[1] > 0) {
          str += `<li>本回合蓄力点最大值：${storage[1]}`;
        }
        return str;
      }
    },
    async content(event, trigger, player2) {
      const storage = player2.getStorage(event.name, [0, 0]);
      if (trigger.name == "useCard") {
        player2.setStorage(event.name, [0, storage[1]], true);
        const targets2 = game.filterPlayer((current) => current.countCharge(true));
        if (targets2.length) {
          const result = targets2.length > 1 ? await player2.chooseTarget({
            prompt: "进趋：令一名有蓄力技的角色获得1点蓄力点",
            forced: true,
            filterTarget(card2, player3, target) {
              return get.event().targets.includes(target);
            },
            ai(target) {
              return get.attitude(get.player(), target);
            }
          }).set("targets", targets2).forResult() : { targets: targets2 };
          result.targets[0].addCharge(1);
        }
      } else {
        const num = storage[1];
        await player2.draw({ num });
      }
    },
    subSkill: {
      mark: {
        silent: true,
        trigger: {
          player: ["phaseBeforeStart", "addMark", "removeMark", "phaseAfter", "useCardAfter"]
        },
        filter(event, player2) {
          return ["phase", "useCard"].includes(event.name) || event.markName == "charge" && player2 == _status.currentPhase;
        },
        async content(event, trigger, player2) {
          if (!player2.storage.rejinqu) {
            player2.setStorage("rejinqu", [0, 0], true);
          }
          const storage = player2.getStorage("rejinqu");
          if (trigger.name == "useCard") {
            player2.setStorage("rejinqu", [storage[0] + 1, storage[1]], true);
          } else if (event.triggername == "phaseAfter") {
            player2.setStorage("rejinqu", [storage[0], 0], true);
          } else {
            const num = player2.countCharge();
            if (num > storage[1]) {
              player2.setStorage("rejinqu", [storage[0], num], true);
            }
          }
        }
      }
    }
  },
  //朱绩
  potjiezhu: {
    audio: 2,
    audioname: ["zhuji_shadow"],
    enable: "chooseToUse",
    getOnlyNum(player2, isDiscard) {
      let num = player2.countCards("h");
      if (isDiscard) {
        const list = game.filterPlayer((target) => target != player2 && target.countCards("h") < num).map((target) => target.countCards("h"));
        while (true) {
          num--;
          if (!list.includes(num)) {
            return num;
          }
          if (num <= 0) {
            break;
          }
        }
      } else {
        const list = game.filterPlayer((target) => target != player2 && target.countCards("h") > num).map((target) => target.countCards("h"));
        while (true) {
          num++;
          if (!list.includes(num)) {
            return num;
          }
        }
      }
      return null;
    },
    viewAsFilter(player2) {
      return get.info("potjiezhu").getOnlyNum(player2, true) >= 0 && player2.countDiscardableCards(player2, "h") > 0;
    },
    selectTarget() {
      return [1, ui.selected.cards.length];
    },
    selectCard() {
      const player2 = get.player();
      return player2.countCards("h") - get.info("potjiezhu").getOnlyNum(player2, true);
    },
    filterCard: lib.filter.cardDiscardable,
    usable: 1,
    log: false,
    viewAs: {
      name: "sha",
      isCard: true,
      suit: "none",
      number: void 0,
      color: "none",
      cards: [],
      storage: {
        potjiezhu: true
      }
    },
    ignoreMod: true,
    async precontent(event, trigger, player2) {
      const skill = "potjiezhu";
      player2.logSkill(skill);
      const { cards: cards2 } = event.result;
      const num = cards2.length;
      await player2.modedDiscard({ cards: cards2 });
      event.getParent().oncard = () => {
        const { player: player3, card: card2 } = get.event();
        player3.when({ global: "useCardAfter" }).filter((evt) => evt.card == card2).then(async (event2, trigger2, player4) => {
          const { targets: targets2, card: card3 } = trigger2;
          if (targets2.length == num && targets2.every((target) => target.hasHistory("damage", (evt) => evt.card == card3))) {
            const num2 = get.info("potjiezhu").getOnlyNum(player4);
            if (num2 > 0) {
              await player4.drawTo(num2);
            }
          }
        });
      };
      event.result.cards = [];
    },
    locked: false,
    mod: {
      targetInRange(card2, player2, target) {
        if (card2.storage?.potjiezhu) {
          return true;
        }
      }
    }
  },
  pothuanshi: {
    audio: 3,
    dutySkill: true,
    locked: false,
    group: ["pothuanshi_achieve", "pothuanshi_damage"],
    mod: {
      cardEnabled(card2, player2) {
        if (get.name(card2) == "jiu" && !player2.isDying()) {
          return false;
        }
      },
      cardSavable(card2, player2) {
        if (get.name(card2) == "jiu" && !player2.isDying()) {
          return false;
        }
      }
    },
    enable: "phaseUse",
    filter(event, player2) {
      return player2.hasCard((card2) => get.name(card2) == "jiu" && player2.canRecast(card2), "h");
    },
    prompt: "你可以重铸一张【酒】",
    filterCard(card2, player2) {
      return get.name(card2) == "jiu" && player2.canRecast(card2);
    },
    lose: false,
    discard: false,
    delay: false,
    logAudio: () => "pothuanshi2.mp3",
    derivation: ["potjianlv"],
    async content(event, trigger, player2) {
      await player2.recast(event.cards);
    },
    subSkill: {
      damage: {
        audio: "pothuanshi1.mp3",
        forced: true,
        inherit: "zf_cardDamage",
        filter(event, player2) {
          return event.card.name == "sha" && player2.getHistory("useCard", (evt) => evt.card.name == "sha").indexOf(event) == 0;
        }
      },
      achieve: {
        audio: "pothuanshi3.mp3",
        forced: true,
        locked: false,
        trigger: {
          player: "damageEnd",
          source: "damageSource"
        },
        filter(event, player2) {
          return event.num == player2.hp;
        },
        skillAnimation: true,
        animationColor: "wood",
        async content(event, trigger, player2) {
          player2.awakenSkill("pothuanshi");
          game.log(player2, "成功完成使命");
          player2.changeSkin({ characterName: "zhuji" }, "zhuji_shadow");
          game.broadcastAll(
            (player3, name) => {
              if (player3.name == "zhuji" || player3.name1 == "zhuji") {
                player3.node.name.innerHTML = name;
              }
              if (player3.name2 == "zhuji") {
                player3.node.name2.innerHTML = name;
              }
            },
            player2,
            "施绩"
          );
          await player2.addSkills("potjianlv");
        }
      }
    }
  },
  potjianlv: {
    audio: 2,
    onremove: true,
    intro: {
      content: "已发动#次"
    },
    trigger: {
      player: "loseAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player2) {
      return event.type == "discard" && event.getl(player2)?.cards2?.length >= player2.countMark("potjianlv") + 1;
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget({
        prompt: get.prompt2(event.skill),
        filterTarget: lib.filter.notMe,
        ai(target) {
          return get.damageEffect(target, get.player(), get.player());
        }
      }).forResult();
    },
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      player2.addMark(event.name, 1, false);
      const next = target.damage();
      await next;
      if (game.hasGlobalHistory("everything", (evt) => {
        return evt.name == "die" && evt.player == target && evt.reason == next;
      })) {
        const result = await player2.chooseTarget({
          prompt: "兼虑：你对一名其他角色造成1点伤害，或者取消并重置此技能的X",
          filterTarget: lib.filter.notMe,
          ai(target2) {
            const player3 = get.player();
            if (player3.countMark("potjianlv") > 3) {
              return 0;
            }
            return get.damageEffect(target2, player3, player3);
          }
        }).forResult();
        if (result.bool) {
          const { targets: targets2 } = result;
          player2.line(targets2, "yellow");
          await targets2[0].damage();
        } else {
          game.log(player2, "重置了技能", `#g${get.translation(event.name)}`, "的X");
          player2.clearMark(event.name, false);
        }
      }
    }
  },
  //界周妃
  reliangyin: {
    audio: 4,
    logAudio: () => 2,
    trigger: {
      global: ["addToExpansionAfter", "loseAsyncAfter", "gainAfter"]
    },
    getIndex(event, player2) {
      if (event.name == "loseAsync") {
        if (event.type == "addToExpansion") {
          return 1;
        }
        if (event.type == "gain") {
          const xs = game.filterPlayer2(() => true, void 0, true).flatMap((target) => (target2) => event.getl(target2)?.xs || []);
          return game.filterPlayer2(
            (target) => {
              return event.getg(target)?.containsSome(...xs);
            },
            void 0,
            true
          ).sortBySeat();
        }
        return 0;
      }
      if (event.name == "gain") {
        return game.hasPlayer2((target) => event.getl(target)?.xs?.length, true) ? 1 : 0;
      }
      return 1;
    },
    filter(event, player2) {
      const funcName = event.name == "gain" || event.type == "gain" ? "discard" : "draw";
      return funcName != "discard" || game.hasPlayer((current) => current.hasCards("he"));
    },
    async cost(event, trigger, player2) {
      const funcName = trigger.name == "gain" || trigger.type == "gain" ? "discard" : "draw";
      event.result = await player2.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: `令一名角色${funcName == "discard" ? "弃置" : "摸"}一张牌`,
        filterTarget(card2, player3, target) {
          return get.event().funcName == "discard" ? target.hasDiscardableCards(target, "he") : true;
        },
        ai(target) {
          const { funcName: funcName2, player: player3 } = get.event();
          return get.effect(target, { name: funcName2 == "discard" ? "guohe_copy2" : "draw" }, player3, player3);
        }
      }).set("funcName", funcName).forResult();
      event.result.cost_data ??= {};
      event.result.cost_data.funcName = funcName;
    },
    async content(event, trigger, player2) {
      const {
        cost_data: { funcName },
        targets: [target]
      } = event;
      target.addTip(event.name, get.translation(event.name), "roundStart", { width: "fit-content" });
      if (funcName == "discard") {
        await target.chooseToDiscard({ forced: true, position: "he" });
      } else {
        await target.draw();
      }
    },
    group: "reliangyin_end",
    subSkill: {
      end: {
        audio: "reliangyin",
        logAudio: () => ["reliangyin3.mp3", "reliangyin4.mp3"],
        trigger: { global: "roundEnd" },
        filter(event, player2) {
          const targets2 = player2.getRoundHistory("useSkill", (evt) => evt.skill == "reliangyin").flatMap((evt) => evt.targets).unique();
          return game.countPlayer((target) => !targets2.includes(target)) == 1 || targets2.length == 1 && targets2[0].isDamaged();
        },
        async cost(event, trigger, player2) {
          const targeted = player2.getRoundHistory("useSkill", (evt) => evt.skill == "reliangyin").flatMap((evt) => evt.targets).unique();
          const untargeted = game.filterPlayer((target) => !targeted.includes(target));
          const next = player2.chooseTarget({
            prompt: get.prompt(event.skill),
            prompt2: "令本轮唯一成为“良姻”目标的角色回复1点体力，或令本轮唯一未成为“良姻”目标的角色失去1点体力",
            filterTarget(card2, player3, target) {
              const { targeted: targeted2, untargeted: untargeted2 } = get.event();
              return targeted2.includes(target) && targeted2.length == 1 && target.isDamaged() || untargeted2.includes(target) && untargeted2.length == 1;
            },
            ai(target) {
              const { targeted: targeted2, untargeted: untargeted2, player: player3 } = get.event();
              if (targeted2.includes(target)) {
                return get.recoverEffect(target, player3, player3);
              }
              if (untargeted2.includes(target)) {
                return get.effect(target, { name: "losehp" }, player3, player3);
              }
              return 0;
            }
          }).set("targeted", targeted).set("untargeted", untargeted);
          next.set(
            "targetprompt2",
            next.targetprompt2.concat((target) => {
              if (!target.classList.contains("selectable")) {
                return;
              }
              const { targeted: targeted2, untargeted: untargeted2 } = get.event();
              if (targeted2.includes(target)) {
                return "回复体力";
              }
              if (untargeted2.includes(target)) {
                return "失去体力";
              }
            })
          );
          event.result = await next.forResult();
          if (event.result?.bool && event.result.targets?.length) {
            event.result.cost_data ??= {};
            event.result.cost_data.funcName = targeted.includes(event.result.targets[0]) ? "recover" : "loseHp";
          }
        },
        async content(event, trigger, player2) {
          const {
            cost_data: { funcName },
            targets: [target]
          } = event;
          await target[funcName]();
        }
      }
    }
  },
  rekongsheng: {
    audio: 2,
    trigger: { player: ["phaseZhunbeiBegin", "recoverEnd", "loseHpEnd"] },
    filter(event, player2) {
      const current = get.info("rekongsheng").getFakeCurrent();
      return current?.isIn() && current.hasCards("he");
    },
    getFakeCurrent() {
      let current = _status.currentPhase;
      if (!current) {
        const history = _status.globalHistory;
        for (let i = history.length - 1; i >= 0; i--) {
          for (const current2 of game.filterPlayer2()) {
            const action = current2.actionHistory[i];
            if (action.isMe && !action.isSkipped) {
              current = current2;
            }
          }
          if (current) break;
          if (history[i].isRound) {
            break;
          }
        }
      }
      return current;
    },
    logTarget: () => get.info("rekongsheng").getFakeCurrent(),
    async cost(event, trigger, player2) {
      const target = get.info("rekongsheng").getFakeCurrent();
      event.result = await player2.choosePlayerCard({
        prompt: get.prompt2(event.skill, target),
        position: "he",
        selectButton: [1, Infinity],
        allowChooseAll: true,
        target,
        ai(button) {
          const { player: player3, target: target2 } = get.event();
          const att = get.attitude(get.player(), get.event().target);
          if (att < 0) {
            return get.buttonValue(button);
          }
          if (player3 === target2) {
            const card2 = button.link;
            if (get.position(card2) == "e") {
              return 1 - get.value(card2);
            }
            if (card2.name == "shan" || card2.name == "du" || !player3.hasValueTarget(card2)) {
              return 1;
            }
            return 4 - get.value(card2);
          } else {
            if (ui.selected.buttons.length) {
              return 0;
            }
            return 6 - get.value(button.link);
          }
        }
      }).forResult();
    },
    intro: {
      markcount: "expansion",
      mark(dialog, content, player2) {
        const cards2 = player2.getExpansions("rekongsheng");
        if (cards2.length) {
          if (player2.isUnderControl(true)) {
            dialog.addAuto(cards2);
          } else {
            return "共有" + get.cnNumber(cards2.length) + "张“箜声”";
          }
        }
      },
      content(content, player2) {
        const cards2 = player2.getExpansions("rekongsheng");
        if (cards2.length) {
          if (player2.isUnderControl(true)) {
            return get.translation(cards2);
          }
          return "共有" + get.cnNumber(cards2.length) + "张“箜声”";
        }
      }
    },
    async content(event, trigger, player2) {
      const {
        targets: [target],
        cards: cards2
      } = event;
      await target.addToExpansion({ cards: cards2, animate: "give", source: target, gaintag: [event.name] });
      player2.when({ global: "phaseJieshuBegin" }).filter((evt) => {
        return evt.player == target;
      }).then(async (event2, trigger2, player3) => {
        if (!target.hasExpansions("rekongsheng")) {
          return;
        }
        const cards3 = target.getExpansions("rekongsheng");
        if (cards3.length) {
          const result = await player3.chooseButton({
            createDialog: [`箜声：请选择要使用的牌`, cards3],
            filterButton(button, player4) {
              const card2 = button.link;
              return player4.hasUseTarget(card2, void 0, false) || get.info(card2).notarget && lib.filter.cardEnabled(card2, player4);
            },
            ai(button) {
              return get.player().getUseValue(button.link);
            }
          }).forResult();
          if (result?.bool && result.links?.length) {
            const { links } = result;
            await player3.chooseUseTarget({ card: links[0], addCount: false, forced: true });
          }
        }
        await target.gain({ cards: target.getExpansions("rekongsheng"), animate: "gain2" });
      });
    },
    group: "rekongsheng_remove",
    subSkill: {
      remove: {
        forceDie: true,
        direct: true,
        trigger: { player: "die" },
        async content(event, trigger, player2) {
          for (const current of game.filterPlayer()) {
            const cards2 = current.getExpansions("rekongsheng");
            if (cards2.length) {
              if (!event.logged) {
                event.logged = true;
                player2.logSkill(event.name);
              }
              await current.gain(cards2, "gain2");
            }
          }
        }
      }
    }
  },
  //界严颜
  rejuzhan: {
    audio: 4,
    addtip(player2, skill) {
      player2.addTip(skill, `${get.translation(skill)} ${player2.storage[skill] ? `阴${player2.countMark("rejuzhan_yin")}` : `阳${player2.countMark("rejuzhan_yang")}`}`);
    },
    logAudio: (index) => typeof index == "number" ? "rejuzhan" + index + ".mp3" : 2,
    init(player2, skill) {
      get.info(skill).addtip(player2, skill);
      player2.addSkill(skill + "_mark");
    },
    onremove(player2, skill) {
      player2.removeTip(skill);
      player2.removeSkill(skill + "_mark");
    },
    zhuanhuanji(player2, skill) {
      player2.storage[skill] = !player2.storage[skill];
      get.info(skill).addtip(player2, skill);
    },
    mark: true,
    marktext: "☯",
    intro: {
      markcount(storage, player2, skill) {
        return player2.countMark(skill + "_yang") + "/" + player2.countMark(skill + "_yin");
      },
      content(storage, player2) {
        return "转换技，" + (!storage ? "当你或者此状态下第三次有角色成为【杀】的目标后，你可以与此【杀】的使用者各摸一张牌，然后其本回合不能对你使用牌" : "当你或者此状态下第三次有角色使用【杀】指定目标后，你可以获得此【杀】的目标角色一张牌，然后你本回合不能对其使用牌") + "。乘势：你使用【杀】的次数上限+1，且此【杀】结算完毕后，你获得之。";
      }
    },
    trigger: { global: ["useCardToTargeted", "useCardToPlayered"] },
    filter(event, player2, name) {
      if (event.card.name !== "sha") {
        return false;
      }
      if (name == "useCardToPlayered") {
        return player2.storage.rejuzhan && (event.player == player2 || player2.countMark("rejuzhan_yin") == 3) && event.target.hasGainableCards(player2, "he") && event.target.isIn();
      }
      return !player2.storage.rejuzhan && (event.target == player2 || player2.countMark("rejuzhan_yang") == 3) && event.player.isIn();
    },
    logTarget: (event, player2, name) => name == "useCardToTargeted" ? event.player : event.target,
    prompt2(event, player2, name) {
      if (name == "useCardToPlayered") {
        let str = `获得${get.translation(event.target)}一张牌，然后本回合不能对其使用牌`;
        if (event.player == player2 && player2.countMark("rejuzhan_yin") == 3) {
          str += `，你使用【杀】的次数上限+1`;
          if (event.cards.filterInD("od")) {
            str += `并获得${get.translation(event.cards.filterInD("od"))}`;
          }
        }
        str += "。";
        return str;
      } else {
        let str = `与${get.translation(event.player)}各摸一张牌，然后其本回合不能对你使用牌`;
        if (event.target == player2 && player2.countMark("rejuzhan_yang") == 3) {
          str += `，你使用【杀】的次数上限+1`;
          if (event.cards.filterInD("od")) {
            str += `并获得${get.translation(event.cards.filterInD("od"))}`;
          }
        }
        str += "。";
        return str;
      }
    },
    check(event, player2, name) {
      const target = name == "useCardToPlayered" ? event.target : event.player;
      if (name == "useCardToPlayered") {
        return get.effect(target, { name: "shunshou_copy2" }, player2, player2) > 0;
      }
      return true;
    },
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      const suffix = !player2.storage[event.name] ? "yang" : "yin";
      player2.changeZhuanhuanji(event.name);
      let isChengshi = false;
      if (event.triggername == "useCardToTargeted") {
        await game.asyncDraw([player2, target]);
        target.addTempSkill(event.name + "_debuff");
        target.markAuto(event.name + "_debuff", player2);
        if (trigger.target == player2 && player2.countMark(`${event.name}_${suffix}`) == 3) {
          isChengshi = true;
        }
      } else {
        await player2.gainPlayerCard({ target, position: "he", forced: true });
        player2.addTempSkill(event.name + "_debuff");
        player2.markAuto(event.name + "_debuff", target);
        if (trigger.player == player2 && player2.countMark(`${event.name}_${suffix}`) == 3) {
          isChengshi = true;
        }
      }
      player2.clearMark(`${event.name}_${suffix}`, false);
      if (isChengshi) {
        player2.logSkill(event.name, null, null, null, [get.rand(3, 4)]);
        player2.popup("乘势！");
        player2.addSkill(event.name + "_sha");
        player2.addMark(event.name + "_sha", 1, false);
        player2.when({ global: "useCardAfter" }).filter((evt) => evt.card == trigger.card).then(async (event2, trigger2, player3) => {
          const cards2 = trigger2.cards?.filterInD("od");
          if (cards2?.length) {
            await player3.gain({ cards: cards2, animate: "gain2" });
          }
        });
      }
    },
    group: "rejuzhan_init",
    subSkill: {
      init: {
        audio: "rejuzhan",
        trigger: {
          player: "enterGame",
          global: "phaseBefore"
        },
        filter(event, player2) {
          return (event.name != "phase" || game.phaseNumber == 0) && player2.group == "shu" && !player2.storage.rejuzhan;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          player2.changeZhuanhuanji("rejuzhan");
        }
      },
      sha: {
        charlotte: true,
        marktext: "战",
        onremove: true,
        intro: { content: "使用【杀】的次数上限+#" },
        mod: {
          cardUsable(card2, player2, num) {
            if (get.name(card2) == "sha") {
              return num + player2.countMark("rejuzhan_sha");
            }
          }
        }
      },
      debuff: {
        charlotte: true,
        marktext: "拒",
        onremove: true,
        intro: { content: "本回合你不能对$使用牌" },
        mod: {
          playerEnabled(card2, player2, target) {
            if (player2.getStorage("rejuzhan_debuff").includes(target)) {
              return false;
            }
          }
        }
      },
      mark: {
        charlotte: true,
        trigger: { global: ["useCardToTargeted", "useCardToPlayered"] },
        filter(event, player2, name) {
          if (event.card.name !== "sha") {
            return false;
          }
          if (name == "useCardToPlayered") {
            return player2.storage.rejuzhan;
          }
          return !player2.storage.rejuzhan;
        },
        silent: true,
        firstDo: true,
        async content(event, trigger, player2) {
          if (event.triggername == "useCardToPlayered") {
            player2.addMark("rejuzhan_yin", 1, false);
          } else {
            player2.addMark("rejuzhan_yang", 1, false);
          }
          get.info("rejuzhan").addtip(player2, "rejuzhan");
        }
      }
    }
  },
  //高翔
  mbgxchiyuan: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filterCard: true,
    selectCard: 1,
    filterTarget(card2, player2, target) {
      return target != player2 && target.hp < player2.hp;
    },
    selectTarget: 1,
    filter(event, player2) {
      return game.hasPlayer((target) => get.info("mbgxchiyuan")?.filterTarget?.(void 0, player2, target));
    },
    lose: false,
    discard: false,
    delay: false,
    async content(event, trigger, player2) {
      const { cards: cards2, target } = event;
      await player2.give(cards2, target);
      const virtualSha = get.autoViewAs({ name: "sha", isCard: true });
      const targets2 = game.filterPlayer((targetx) => {
        return targetx.inRange(target) && player2.canUse(virtualSha, targetx, false, false);
      });
      if (!targets2.length) {
        return;
      }
      const result = await player2.chooseTarget({
        prompt: `驰援：请选择攻击范围内包含${get.translation(target)}的一名角色，视为对其使用一张【杀】`,
        filterTarget(card2, player3, target2) {
          return get.event().targets.includes(target2);
        },
        forced: true
      }).set("targets", targets2).forResult();
      if (!result?.targets?.length) {
        return;
      }
      const useEvent = player2.useCard({ card: virtualSha, targets: result.targets, addCount: false });
      await useEvent;
      const damaged = game.hasPlayer2((target2) => target2.hasHistory("damage", (evt) => evt.card === useEvent.card), true);
      if (damaged) {
        await target.recover();
      } else {
        await game.doAsyncInOrder(useEvent.targets, async (shaUser) => {
          if (shaUser.canUse(virtualSha, player2, false, false)) {
            return shaUser.useCard({ card: virtualSha, targets: [player2], addCount: false });
          }
        });
      }
    },
    ai: {
      order: 8,
      result: {
        target(player2, target) {
          if (target.hp < player2.hp) {
            const att = get.attitude(player2, target);
            if (att > 0) {
              const shaTargets = game.filterPlayer((t) => t != player2 && t.inRange(target) && get.effect(t, { name: "sha", isCard: true }, player2, player2) > 0);
              if (shaTargets.length) {
                return att;
              }
            }
          }
          return 0;
        }
      }
    }
  },
  mberdi: {
    audio: 2,
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player2) {
      return game.hasPlayer((target) => target != player2 && target.countCards("h") > player2.countCards("h"));
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget({
        prompt: get.prompt2(event.skill),
        filterTarget(card2, player3, target) {
          return target != player3 && target.countCards("h") > player3.countCards("h");
        },
        ai(target) {
          return -get.attitude(get.player(), target) * (114514 - target.countCards("h"));
        }
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      await player2.loseHp();
      await player2.draw();
      const discardMap = await game.chooseAnyOL([player2, target], get.info(event.name).chooseToDiscard, []).forResult();
      const playerResult = discardMap.get(player2);
      const targetResult = discardMap.get(target);
      const playerCount = playerResult?.cards?.length || 0;
      const targetCount = targetResult?.cards?.length || 0;
      for (const targetx of [player2, target]) {
        const result = discardMap.get(targetx);
        if (result?.bool && result.cards?.length) {
          await targetx.modedDiscard(result.cards);
        }
      }
      if (playerCount > targetCount) {
        await player2.recover();
        await target.damage();
      } else if (playerCount < targetCount) {
        await player2.draw(targetCount);
      }
    },
    /**
     *
     * @param { Player } player
     * @param { number } eventId
     * @returns { GameEvent }
     */
    chooseToDiscard(player2, eventId) {
      const next = player2.chooseToDiscard({
        prompt: "饵敌：请弃置任意张手牌",
        selectCard: [1, player2.countCards("h")],
        position: "h",
        chooseonly: true,
        forced: true
      });
      next.set("id", eventId);
      next.set("_global_waiting", true);
      return next;
    }
  },
  //崔琰毛玠
  mbzhengbi: {
    audio: 4,
    onremove: ["mbzhengbi", "mbzhengbi_used"],
    logAudio: (index) => typeof index == "number" ? "mbzhengbi" + index + ".mp3" : 2,
    intro: { content: "可选择的角色+#" },
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player2) {
      return game.hasPlayer((target) => target != player2);
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget({
        prompt: get.prompt2(event.skill),
        filterTarget(card2, player3, target) {
          return target != player3 && target.hasCards("hej");
        },
        selectTarget: [1, 1 + player2.countMark(event.skill)],
        ai(target) {
          const player3 = get.player();
          const storage = player3.getStorage("mbzhengbi_used");
          const att = get.attitude(player3, target);
          if (att > 0) {
            if (!storage.includes(target) || target.hasCards("j")) {
              return 114515 - target.countCards("hej");
            }
            return 0;
          }
          return 114514 - target.countCards("hej");
        }
      }).forResult();
    },
    async content(event, trigger, player2) {
      const { targets: targets2 } = event;
      await game.doAsyncInOrder(targets2, async (target) => {
        const result = await target.chooseControl({
          prompt: `征辟：请选择令${get.translation(player2)}获得的牌数`,
          controls: [1, 2, 3].map((i) => i.toString()),
          choice: get.attitude(player2, target) > 0 ? 2 : 0
        }).forResult();
        const { index } = result;
        if (typeof index == "number") {
          if (index + 1 == 3 && !player2.getStorage(event.name + "_used").includes(target)) {
            player2.logSkill(event.name, null, null, null, [get.rand(3, 4)]);
            player2.addMark(event.name, 1, false);
            player2.markAuto(event.name + "_used", target);
          }
          await player2.gainPlayerCard({ target, position: "hej", selectButton: index + 1, forced: true, allowChooseAll: true });
          await player2.chooseToGive({ target, position: "he", forced: true, selectCard: index + 1, allowChooseAll: true });
        }
      });
    }
  },
  mbfengying: {
    audio: 4,
    logAudio: () => 2,
    limited: true,
    skillAnimation: true,
    animationColor: "thunder",
    enable: "phaseUse",
    filterTarget: true,
    async content(event, trigger, player2) {
      const { target, name } = event;
      player2.awakenSkill(name);
      const targets2 = game.filterPlayer((i) => target.inRange(i));
      await game.doAsyncInOrder(targets2, async (source) => {
        if (source.hasCards("hs", (card2) => lib.filter.targetEnabled2(get.autoViewAs({ name: "wuzhong" }, [card2]), source, target))) {
          await source.chooseToUse({ forced: true }).set("openskilldialog", `奉迎：将一张手牌当做【无中生有】对${get.translation(target)}使用`).set("norestore", true).set("_backupevent", `${event.name}_backup`).set("custom", {
            add: {},
            replace: { window() {
            } }
          }).backup(`${event.name}_backup`).set("targetRequired", true).set("complexTarget", true).set("complexSelect", true).set("pre_target", target);
        }
      });
      if (target.isMaxMaxHp()) {
        player2.addTempSkill(event.name + "_effect");
      }
    },
    ai: {
      order: 10,
      result: {
        target(player2, target) {
          if (target.isMaxMaxHp()) {
            return 2 + game.countPlayer((i) => target.inRange(i));
          }
          return game.countPlayer((i) => target.inRange(i));
        }
      }
    },
    subSkill: {
      effect: {
        audio: "mbfengying",
        logAudio: () => ["mbfengying3.mp3", "mbfengying4.mp3"],
        charlotte: true,
        forced: true,
        trigger: { global: "phaseEnd" },
        async content(event, trigger, player2) {
          player2.removeSkill(event.name);
          player2.insertPhase();
        },
        mark: true,
        intro: { content: "当前回合结束时获得一个额外回合" }
      },
      backup: {
        viewAs: { name: "wuzhong" },
        filterCard(card2) {
          return get.itemtype(card2) == "card";
        },
        position: "hs",
        selectCard: 1,
        check(card2) {
          return 6 - get.value(card2);
        },
        filterTarget(card2, player2, target) {
          if (target != get.event().pre_target) {
            return false;
          }
          return lib.filter.targetEnabled2.call(this, card2, player2, target);
        },
        log: false
      }
    }
  },
  //钟繇
  mbzuoding: {
    audio: "zuoding",
    trigger: { global: "useCardToPlayered" },
    filter(event, player2) {
      if (event.getParent()?.triggeredTargets3.length > 1) {
        return false;
      }
      return get.suit(event.card) == "spade" && event.targets?.length > 0 && event.player != player2 && event.player.isPhaseUsing() && [player2, ...event.targets].some((target) => !target.hasHistory("damage", (evt) => evt.num > 0 && evt.getParent("phaseUse") == event.getParent("phaseUse")));
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget({
        prompt: get.prompt(event.skill),
        prompt2: "令一名目标角色摸一张牌",
        filterTarget(card2, player3, target) {
          return get.event().targets.includes(target);
        },
        ai(target) {
          return get.attitude(get.event().player, target);
        }
      }).set("targets", trigger.targets).forResult();
    },
    logTarget: "targets",
    async content(event, trigger, player2) {
      await event.targets[0].draw();
    },
    ai: {
      expose: 0.2
    }
  },
  //兀突骨
  mbranshang: {
    audio: "ranshang",
    trigger: { player: "damageEnd" },
    filter(event, player2) {
      return event.hasNature("fire");
    },
    forced: true,
    async content(event, trigger, player2) {
      player2.addMark(event.name, trigger.num);
    },
    intro: {
      name2: "燃",
      content: "mark"
    },
    ai: {
      neg: true,
      effect: {
        target(card2, player2, target, current) {
          if (card2.name == "sha") {
            if (game.hasNature(card2, "fire") || player2.hasSkill("zhuque_skill")) {
              return 2;
            }
          }
          if (get.tag(card2, "fireDamage") && current < 0) {
            return 2;
          }
        }
      }
    },
    group: "mbranshang_loseHp",
    subSkill: {
      loseHp: {
        audio: "ranshang",
        trigger: { player: "phaseJieshuBegin" },
        forced: true,
        filter(event, player2) {
          return player2.hasMark("mbranshang");
        },
        async content(event, trigger, player2) {
          await player2.loseHp(player2.countMark("mbranshang"));
          if (player2.countMark("mbranshang") > 1) {
            await player2.loseMaxHp(2);
            await player2.draw(2);
          }
        }
      }
    }
  },
  mbhanyong: {
    audio: "hanyong",
    trigger: {
      player: "useCard"
    },
    filter(event, player2) {
      return event.targets?.length > 1 && player2.isDamaged();
    },
    async cost(event, trigger, player2) {
      const { card: card2, targets: targets2 } = trigger;
      const result = await player2.chooseControl({
        prompt: get.prompt2(event.skill),
        choiceList: [`摸两张牌`, `令${get.translation(card2)}造成的伤害+1`, `背水！获得一个“燃”`],
        controls: ["选项一", "选项二", "背水！", "cancel2"],
        choice: (() => {
          if (!get.is.damageCard(card2)) {
            return 0;
          }
          if (targets2.reduce((sum, target) => sum + get.effect(target, card2, player2, player2), 0) > 0) {
            return 1;
          }
          return 0;
        })()
      }).forResult();
      if (result.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: { index: result.index }
        };
      }
    },
    async content(event, trigger, player2) {
      const {
        cost_data: { index }
      } = event;
      if (index % 2 == 0) {
        await player2.draw(2);
      }
      if (index > 0) {
        game.log(trigger.card, "的基础伤害+1");
        trigger.baseDamage++;
      }
      if (index == 2) {
        player2.addMark("mbranshang");
      }
    }
  },
  //程昱
  mbshefu: {
    audio: "shefu",
    onremove(player2, skill) {
      delete player2.storage[skill];
      const cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile({ cards: cards2 });
      }
    },
    intro: {
      content: "cards",
      mark(dialog, storage = [], player2) {
        const cards2 = storage.map((arr) => arr[1]);
        const names = storage.map((arr) => arr[0]);
        if (cards2.length) {
          if (player2.isUnderControl(true)) {
            dialog.addAuto(cards2);
            const { buttons } = dialog;
            buttons.forEach((button, index) => ui.create.cardTempName(get.autoViewAs({ name: names[index] }), button));
          } else {
            return "共有" + get.cnNumber(cards2.length) + "张“伏兵”";
          }
        }
      }
    },
    trigger: { player: "phaseJieshuBegin" },
    getList(player2, exclude) {
      return get.inpileVCardList((info) => {
        if (!["basic", "trick", "delay"].includes(info[0]) || exclude && exclude == info[2]) {
          return false;
        }
        if (info[2] == "sha" && info[3]) {
          return false;
        }
        return !player2.getStorage("mbshefu").some((arr) => arr[0] == info[2]);
      });
    },
    filter(event, player2) {
      return get.info("mbshefu").getList(player2).length > 0 && player2.hasCards("he");
    },
    async cost(event, trigger, player2) {
      const result = await player2.chooseButton({
        createDialog: [get.prompt2(event.skill), [get.info(event.skill).getList(player2), "vcard"]],
        ai(button) {
          const { rand } = get.event();
          switch (button.link[2]) {
            case "sha":
              return 5 + rand[1];
            case "tao":
              return 4 + rand[2];
            case "shan":
              return 4.5 + rand[4];
            case "wuzhong":
              return 4 + rand[5];
            case "shunshou":
              return 3 + rand[6];
            case "nanman":
              return 2 + rand[7];
            case "wanjian":
              return 2 + rand[8];
            default:
              return rand[0];
          }
        }
      }).set("rand", [Math.random(), Math.random(), Math.random(), Math.random(), Math.random(), Math.random(), Math.random(), Math.random()]).forResult();
      if (result?.bool && result.links?.length) {
        const {
          links: [link]
        } = result;
        event.result = {
          bool: true,
          cost_data: link[2]
        };
      }
    },
    async content(event, trigger, player2) {
      const { cost_data: name } = event;
      if (!player2.hasCards("he")) {
        return;
      }
      const result = await player2.chooseCard({ position: "he", prompt: "设伏：选择一张牌作为“伏兵”", forced: true }).forResult();
      const { cards: cards2 } = result;
      if (cards2?.length) {
        await player2.addToExpansion({ cards: cards2, source: player2, animate: "give", gaintag: [event.name] });
        if (player2.getExpansions(event.name).includes(cards2[0])) {
          player2.markAuto(event.name, [[name, cards2[0]]]);
        }
      }
    },
    group: "mbshefu_exclude",
    subSkill: {
      exclude: {
        audio: "shefu",
        trigger: { global: "useCard" },
        filter(event, player2) {
          if (_status.currentPhase == player2 || event.player == player2 || event.all_excluded) {
            return false;
          }
          return player2.getStorage("mbshefu").some((arr) => arr[0] == get.name(event.card));
        },
        logTarget: "player",
        prompt2(event, player2) {
          return `移去“伏兵”令其${event.targets?.length ? `对${get.translation(event.targets)}` : ""}使用的${get.translation(event.card)}无效并摸一张牌`;
        },
        check(event, player2) {
          const { card: card2, player: target, targets: targets2 } = event;
          const name = get.name(card2);
          let effect = 0;
          if (name == "wuxie" || name == "shan") {
            if (get.attitude(player2, target) < -1) {
              effect = -1;
            }
          } else if (targets2?.length) {
            for (let i = 0; i < targets2.length; i++) {
              effect += get.effect(targets2[i], card2, target, player2);
            }
          }
          if (effect < 0) {
            if (name == "sha") {
              if (targets2[0] == player2) {
                return !player2.countCards("h", "shan");
              } else {
                return targets2[0].hp == 1 || targets2[0].countCards("h") <= 2 && targets2[0].hp <= 2;
              }
            } else {
              return true;
            }
          }
          return false;
        },
        async content(event, trigger, player2) {
          const { card: card2 } = trigger;
          const {
            targets: [target]
          } = event;
          const name = get.name(card2);
          const skill = "mbshefu";
          const storage = player2.getStorage(skill);
          const index = storage.findIndex((arr) => arr[0] == name);
          if (index !== -1) {
            const list = storage[index].slice();
            storage.splice(index, 1);
            player2.setStorage(skill, storage, true);
            await player2.loseToDiscardpile({ cards: [list[1]] });
            game.log(card2, "被无效了");
            trigger.targets.length = 0;
            trigger.all_excluded = true;
            await player2.draw();
          }
        }
      }
    }
  },
  mbbenyu: {
    audio: "benyu",
    trigger: { player: "damageEnd" },
    filter(event, player2) {
      if (!event.source?.isIn()) {
        return false;
      }
      const nh1 = player2.countCards("h");
      const nh2 = event.source.countCards("h");
      return nh1 < nh2 || nh1 > nh2 && player2.countDiscardableCards(player2, "h") > nh2;
    },
    logTarget: "source",
    async cost(event, trigger, player2) {
      const num1 = player2.countCards("h");
      const num2 = trigger.source.countCards("h");
      if (num1 > num2) {
        event.result = await player2.chooseToDiscard({
          prompt: get.prompt(event.skill, trigger.source),
          prompt2: `你可以弃置至少${get.cnNumber(num2 + 1)}张手牌并对其造成1点伤害`,
          selectCard: [num2 + 1, Infinity],
          position: "h",
          allowChooseAll: true,
          chooseonly: true,
          ai(card2) {
            const trigger2 = get.event().getTrigger();
            const { player: player3, num } = get.event();
            if (ui.selected.cards.length >= num) {
              return -1;
            }
            if (get.damageEffect(trigger2.source, player3, player3) > 0 && (get.value(card2, player3) < 0 || num <= 2)) {
              return 8 - get.value(card2);
            }
            return -1;
          }
        }).set("num", num2 + 1).forResult();
      } else {
        event.result = await player2.chooseBool({
          prompt: get.prompt(event.skill, trigger.source),
          prompt2: `你可以摸${get.cnNumber(Math.min(5, num2 - num1))}张牌`
        }).forResult();
      }
    },
    async content(event, trigger, player2) {
      const num1 = player2.countCards("h");
      const num2 = trigger.source.countCards("h");
      if (event.cards?.length) {
        await player2.discard(event.cards);
        await trigger.source.damage();
      } else {
        await player2.draw(Math.min(5, num2 - num1));
      }
    }
  },
  //董白
  mblianzhu: {
    audio: "lianzhu",
    enable: "phaseUse",
    usable: 1,
    filterCard: true,
    filterTarget: lib.filter.notMe,
    lose: false,
    discard: false,
    delay: false,
    async content(event, trigger, player2) {
      const {
        cards: [card2],
        target
      } = event;
      await player2.showCards(card2);
      player2.addTempSkill(event.name + "_effect", "phaseChange");
      target.addTempSkill(event.name + "_mark", "phaseChange");
      const next = player2.give(card2, target);
      next.gaintag.add(event.name);
      await next;
      let targetx = target;
      while (targetx && targetx != player2) {
        const prevPlayer = targetx.getPrevious();
        let result;
        if (!targetx.hasCards("he", (cardx) => cardx != card2)) {
          result = { bool: false };
        } else {
          result = await targetx.chooseToGive({
            prompt: `连诛：交给${get.translation(player2)}一张不为${get.translation(card2)}的牌${prevPlayer ? `然后将连诛牌交给${get.translation(prevPlayer)}` : ""}，否则其摸两张牌`,
            target: player2,
            filterCard(card3, player3) {
              return card3 !== get.event().card;
            },
            position: "he",
            ai(card3) {
              return 5.5 - get.value(card3);
            }
          }).set("card", card2).forResult();
        }
        if (result?.bool) {
          if (prevPlayer) {
            prevPlayer.addTempSkill(event.name + "_mark", "phaseChange");
            const next2 = targetx.give(card2, prevPlayer);
            next2.gaintag.add(event.name);
            await next2;
          }
          targetx = prevPlayer;
        } else {
          await player2.draw(2);
          break;
        }
      }
    },
    ai: {
      order: 8,
      result: {
        target(player2, target) {
          if (target == player2.getPrevious()) {
            return 1;
          }
        }
      }
    },
    subSkill: {
      mark: {
        init(player2, skill) {
          player2.addTip(skill, "连诛");
        },
        onremove(player2, skill) {
          player2.removeGaintag("mblianzhu");
          player2.removeTip(skill);
        },
        charlotte: true,
        firstDo: true,
        silent: true,
        trigger: {
          player: "loseEnd",
          global: ["loseAsyncEnd", "equipEnd", "gainEnd", "addToExpansionEnd", "addJudgeEnd"]
        },
        filter(event, player2) {
          const cards2 = event.getl?.(player2)?.hs;
          const map = event.getl?.(player2)?.gaintag_map;
          return cards2.some((i) => map[i.cardid]?.includes("mblianzhu")) && !player2.hasCards("h", (card2) => card2.hasGaintag("mblianzhu"));
        },
        async content(event, trigger, player2) {
          player2.removeSkill(event.name);
        }
      },
      effect: {
        charlotte: true,
        audio: "mblianzhu",
        mod: {
          cardUsableTarget(card2, player2, target) {
            if (target.hasCards("h", (card3) => card3.hasGaintag("mblianzhu"))) {
              return true;
            }
          },
          targetInRange(card2, player2, target) {
            if (target.hasCards("h", (card3) => card3.hasGaintag("mblianzhu"))) {
              return true;
            }
          }
        },
        trigger: {
          source: "damageSource"
        },
        filter(event, player2) {
          return event.player.isIn() && event.player.hasCards("h", (card2) => card2.hasGaintag("mblianzhu"));
        },
        prompt2: "获得其连诛牌",
        check: () => true,
        logTarget: "player",
        async content(event, trigger, player2) {
          const cards2 = event.targets[0].getCards("h", (card2) => card2.hasGaintag("mblianzhu"));
          player2.addTempSkill("mblianzhu_mark", "phaseChange");
          await player2.gain({ cards: cards2, animate: "giveAuto", source: event.targets[0], gaintag: ["mblianzhu"] });
        }
      }
    }
  },
  mbxiahui: {
    audio: "xiahui",
    mod: {
      ignoredHandcard(card2, player2) {
        if (get.color(card2) == "black") {
          return true;
        }
      },
      cardDiscardable(card2, player2, name) {
        if (name == "phaseDiscard" && get.color(card2) == "black") {
          return false;
        }
      }
    },
    trigger: {
      global: "gainAfter",
      player: "loseAsyncAfter"
    },
    forced: true,
    popup: false,
    filter(event, player2) {
      if (event.name == "loseAsync") {
        if (event.type != "gain") {
          return false;
        }
        return game.hasPlayer(function(current) {
          if (current == player2) {
            return false;
          }
          const hs = current.getCards("h"), cards2 = event.getl(player2).cards2;
          const cardsx = event.getg(current);
          for (const i of cardsx) {
            if (i.hasGaintag("mblianzhu")) {
              return true;
            }
            if (hs.includes(i) && cards2.includes(i) && get.color(i, player2) == "black") {
              return true;
            }
          }
          return false;
        });
      }
      if (event.player != player2) {
        const hs = event.player.getCards("h");
        const evt = event.getl(player2);
        return evt?.cards2?.filter(function(card2) {
          return card2.hasGaintag("mblianzhu") || hs.includes(card2) && get.color(card2, player2) == "black";
        }).length > 0;
      }
      return false;
    },
    async content(event, trigger, player2) {
      const cards2 = trigger.getl(player2).cards2;
      game.filterPlayer().forEach(function(current) {
        if (current == player2) {
          return;
        }
        const hs = current.getCards("h"), cardsx = trigger.getg(current).filter(function(card2) {
          return hs.includes(card2) && (card2.hasGaintag("mblianzhu") || cards2.includes(card2) && get.color(card2, player2) == "black");
        });
        if (cardsx.length > 0) {
          current.addSkill("mbxiahui_effect");
          current.addGaintag(cardsx, "mbxiahui_effect");
        }
      });
    },
    subSkill: {
      effect: {
        mark: true,
        intro: {
          content: "不能使用、打出或弃置标记牌"
        },
        mod: {
          cardDiscardable(card2, player2) {
            if (card2.hasGaintag("mbxiahui_effect")) {
              return false;
            }
          },
          cardEnabled2(card2, player2) {
            if (get.itemtype(card2) == "card" && card2.hasGaintag("mbxiahui_effect")) {
              return false;
            }
          }
        },
        trigger: {
          player: "changeHp"
        },
        forced: true,
        popup: false,
        charlotte: true,
        filter(event) {
          return event.changedHp < 0;
        },
        async content(event, trigger, player2) {
          player2.removeSkill(event.name);
        },
        onremove(player2, skill) {
          player2.removeGaintag(skill);
        }
      }
    }
  },
  // 诸葛果
  mbqirang: {
    audio: "qirang",
    trigger: { player: "equipEnd" },
    frequent: true,
    async content(event, trigger, player2) {
      const card2 = get.cardPile2((card3) => get.type2(card3) === "trick");
      if (card2) {
        const next = player2.gain(card2, "gain2");
        next.gaintag.add("mbqirang");
        await next;
      }
    },
    ai: {
      effect: {
        target(card2, player2, target) {
          if (get.type(card2) === "equip" && !get.cardtag(card2, "gifts")) {
            return [1, 3];
          }
        }
      },
      threaten: 1.3
    },
    group: ["mbqirang_use"],
    subSkill: {
      use: {
        mod: {
          targetInRange(card2, player2, target) {
            if (!card2.cards) {
              return;
            }
            for (const cardx of card2.cards) {
              if (get.itemtype(cardx) == "card" && cardx.hasGaintag("mbqirang")) {
                return true;
              }
            }
          }
        },
        audio: "qirang",
        trigger: { player: "useCard" },
        filter(event, player2) {
          if (get.type2(event.card) != "trick") {
            return false;
          }
          return player2.hasHistory("lose", (evt) => {
            if ((evt.relatedEvent || evt.getParent()) !== event) {
              return false;
            }
            return Object.values(evt.gaintag_map).flat().includes("mbqirang");
          });
        },
        forced: true,
        async content(event, trigger, player2) {
          await player2.draw();
        }
      }
    }
  },
  mbyuhua: {
    audio: "yuhua",
    trigger: { player: "phaseJieshuBegin" },
    forced: true,
    filter(event, player2) {
      return player2.countCards("h") > player2.getHp();
    },
    mod: {
      ignoredHandcard(card2, player2) {
        if (get.type(card2) !== "basic") {
          return true;
        }
      },
      cardDiscardable(card2, player2, name) {
        if (name === "phaseDiscard" && get.type(card2) !== "basic") {
          return false;
        }
      }
    },
    async content(event, trigger, player2) {
      const types = player2.getCards("h").map((card2) => get.type2(card2)).toUniqued();
      const num = Math.min(5, types.length);
      if (num > 0) {
        await player2.chooseToGuanxing(num);
      }
    },
    ai: { guanxing: true }
  },
  // 曹纯
  mbshanjia: {
    audio: "shanjia",
    enable: "phaseUse",
    usable: 1,
    init(player2) {
      player2.addSkill("mbshanjia_count");
    },
    onremove(player2) {
      player2.removeSkill("mbshanjia_count");
    },
    locked: false,
    mod: {
      aiValue(player2, card2, num) {
        if (player2.countMark("mbshanjia") < 3 && get.type(card2) === "equip" && !get.cardtag(card2, "gifts")) {
          return num / player2.hp;
        }
      }
    },
    intro: {
      content: "已从装备区失去过#张装备牌"
    },
    sync(player2) {
      const history = player2.actionHistory;
      let num = 0;
      for (let i = 0; i < history.length; i++) {
        for (let j = 0; j < history[i].lose.length; j++) {
          const loseEvent = history[i].lose[j];
          const es = loseEvent.es;
          if (es?.length) {
            num += es.filter((card2) => get.type(card2) === "equip").length;
          }
        }
      }
      player2.setStorage("mbshanjia", num, true);
    },
    async content(event, trigger, player2) {
      await player2.draw(3);
      lib.skill.mbshanjia.sync(player2);
      const num = 3 - player2.countMark("mbshanjia");
      let result;
      if (num > 0) {
        result = await player2.chooseToDiscard({
          position: "he",
          forced: true,
          selectCard: num,
          ai: (card2) => -get.value(card2)
        }).forResult();
      }
      lib.skill.mbshanjia.sync(player2);
      let bool1 = true;
      let bool2 = true;
      if (result?.cards?.length) {
        const cards2 = result.cards;
        for (const card2 of cards2) {
          const type = get.type(card2, "trick", card2.original === "h" ? player2 : false);
          if (type === "basic") {
            bool1 = false;
          }
          if (type === "trick") {
            bool2 = false;
          }
        }
      }
      if (bool2) {
        player2.addTempSkill("mbshanjia_nodis", "phaseChange");
      }
      if (bool1) {
        await player2.chooseUseTarget({
          card: new lib.element.VCard({ name: "sha", isCard: true }),
          addCount: false,
          prompt: "缮甲：是否视为使用一张无任何次数限制的【杀】？"
        });
      }
    },
    ai: {
      order: 7,
      result: {
        player(player2) {
          const lostEquip = player2.countMark("mbshanjia");
          const discardNum = 3 - lostEquip;
          if (discardNum <= 0) {
            return 3;
          }
          const cards2 = player2.getCards("he");
          if (cards2.length >= discardNum) {
            return 2;
          }
          return 0;
        }
      },
      threaten: 3,
      noe: true
    },
    subSkill: {
      count: {
        forced: true,
        silent: true,
        popup: false,
        trigger: {
          player: "loseEnd"
        },
        filter(event, player2) {
          const es = event.es;
          return es?.some((card2) => get.type(card2) === "equip");
        },
        async content(event, trigger, player2) {
          lib.skill.mbshanjia.sync(player2);
        }
      },
      nodis: {
        mark: true,
        charlotte: true,
        intro: { content: "使用牌无距离限制" },
        mod: {
          targetInRange: () => true
        }
      }
    }
  },
  // 夏侯楙
  mbtongwei: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.hasCards("h", (card2) => get.info("mbtongwei").filterCard(card2, player2));
    },
    filterCard(card2, player2) {
      return get.type(card2) == "basic" && player2.canRecast(card2);
    },
    selectCard: [1, Infinity],
    position: "h",
    check(card2) {
      return 6.5 - get.value(card2);
    },
    discard: false,
    lose: false,
    delay: false,
    allowChooseAll: true,
    async content(event, trigger, player2) {
      const cards2 = event.cards;
      await player2.recast(cards2);
      const cardNames = [...new Set(cards2.map((card2) => card2.name))];
      const nameStr = cardNames.map((name) => "【" + get.translation(name) + "】").join("、");
      const list = [];
      game.getGlobalHistory("cardMove", (evt) => {
        if (evt.name != "lose" || evt.type != "discard") {
          return false;
        }
        for (const card2 of evt.cards) {
          if (get.type(card2) === "basic" && cardNames.includes(card2.name) && get.position(card2, true) == "d") {
            list.push(card2);
          }
        }
      });
      const choices = [];
      const choiceList = [`令所有手牌中有${nameStr}的角色各弃置一张基本牌`, `获得${list.length > 0 ? get.translation(list) : "空气"}`, "背水！移除一个可触发【蹙国】的牌名"];
      const targets2 = game.filterPlayer((current) => current.hasCards("h", (card2) => cardNames.includes(card2.name)));
      const bool1 = targets2.length > 0;
      if (bool1) {
        choices.push("选项一");
      } else {
        choiceList[0] = '<span style="opacity:0.5">' + choiceList[0] + "</span>";
      }
      const bool2 = list.length > 0;
      if (bool2) {
        choices.push("选项二");
      } else {
        choiceList[1] = '<span style="opacity:0.5">' + choiceList[1] + "</span>";
      }
      choices.push("背水！");
      const { control } = await player2.chooseControl(choices).set("choiceList", choiceList).set("prompt", "统围：请选择一项").set("ai", () => {
        const controls = get.event().controls.slice();
        if (["背水！", "选项一", "选项二"].every((choice) => controls.includes(choice))) return "背水！";
        if (controls.includes("选项一")) return "选项一";
        if (controls.includes("选项二")) return "选项二";
        return controls.randomGet();
      }).forResult();
      if (!control) {
        return;
      }
      if (control === "背水！") {
        const skipNames = player2.getStorage("mbtongwei");
        const vcards = get.inpileVCardList((info) => {
          if (skipNames.includes(info[2]) || info[3]) {
            return false;
          }
          return get.is.damageCard({ name: info[2] });
        });
        if (vcards.length > 0) {
          const result = await player2.chooseButton({
            createDialog: [`统围·背水：选择要移除的牌名`, [vcards, "vcard"]],
            forced: true,
            ai() {
              return Math.random();
            }
          }).forResult();
          if (result?.bool) {
            const name = result.links[0][2];
            player2.markAuto("mbtongwei", [name]);
            game.log(player2, "移除了牌名", "#y" + get.translation(name));
          }
        }
      }
      if (["选项一", "背水！"].includes(control) && bool1) {
        for (const target of targets2.sortBySeat()) {
          await target.chooseToDiscard({
            prompt: `统围：请弃置一张基本牌`,
            position: "h",
            forced: true,
            filterCard(card2) {
              return get.type(card2) === "basic";
            },
            ai(card2) {
              return -get.value(card2);
            }
          });
        }
      }
      if (["选项二", "背水！"].includes(control) && list.someInD("d")) {
        const gainCards = list.filterInD("d");
        if (gainCards.length > 0) {
          await player2.gain(gainCards, "gain2");
        }
      }
    },
    intro: { content: "已移除牌名：$" },
    oneremove: true,
    derivation: "mbcuguo",
    ai: {
      order: 7,
      result: { player: 1 }
    }
  },
  mbcuguo: {
    audio: 2,
    trigger: { player: "useCardToPlayered" },
    filter(event, player2) {
      if (!get.is.damageCard(event.card) || !event.isFirstTarget) return false;
      return !player2.getStorage("mbtongwei").includes(event.card.name);
    },
    forced: true,
    logTarget: "target",
    async content(event, trigger, player2) {
      const card2 = trigger.card;
      const cardNumber = get.number(card2);
      const targets2 = event.targets;
      const allTargets = [player2, ...targets2];
      const discardMap = /* @__PURE__ */ new Map();
      for (const target of allTargets) {
        const hs = target.getCards("h");
        if (hs.length > 0) {
          const maxNum = Math.max(...hs.map((card3) => get.number(card3)));
          const maxCards = hs.filter((card3) => get.number(card3) === maxNum);
          discardMap.set(target, maxCards);
        }
      }
      if (discardMap.size === 0) {
        return;
      }
      let bool = false;
      for (const [target, cards2] of discardMap) {
        const result = await target.modedDiscard(cards2).forResult();
        if (typeof cardNumber == "number" && result?.cards?.some((card3) => get.number(card3) < cardNumber)) {
          bool = true;
        }
      }
      if (bool) {
        trigger.getParent().effectCount++;
      }
    }
  },
  // OP蹋顿
  youlve: {
    audio: 2,
    trigger: {
      player: "loseAfter",
      global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
    },
    getIndex(event, player2, triggername) {
      return event.getl?.(player2)?.cards2?.length;
    },
    filter(event, player2) {
      if (_status.currentPhase == player2 || !player2.countDiscardableCards(player2, "he")) {
        return false;
      }
      return event.getParent(3).name != "youlve";
    },
    forced: true,
    async content(event, trigger, player2) {
      await player2.chooseToDiscard("he", true, "游掠：请弃置一张牌").set("ai", (card2) => {
        const player3 = get.player();
        if (get.name(card2) == "sha" && player3.hasValueTarget({ name: "sha" }, false) && player3.hasSkill("lianxi")) {
          return 10;
        }
        return 6 - get.value(card2);
      });
    },
    group: "youlve_draw",
    subSkill: {
      draw: {
        audio: "youlve",
        trigger: {
          player: "gainAfter",
          global: "loseAsyncAfter"
        },
        getIndex(event, player2, triggername) {
          return event.getg?.(player2)?.length;
        },
        filter(event, player2) {
          if (_status.currentPhase != player2) {
            return false;
          }
          return event.getParent(2).name != "youlve_draw";
        },
        forced: true,
        async content(event, trigger, player2) {
          await player2.draw("nodelay");
        }
      }
    }
  },
  lianxi: {
    audio: 2,
    mod: {
      ignoredHandcard(card2, player2) {
        if (get.name(card2) == "sha") {
          return true;
        }
      },
      cardDiscardable(card2, player2, name) {
        if (name === "phaseDiscard" && get.name(card2) == "sha") {
          return false;
        }
      }
    },
    trigger: {
      player: "loseAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player2) {
      if (event.type != "discard" || !player2.hasUseTarget({ name: "sha", isCard: true }, false)) {
        return false;
      }
      return event.getl?.(player2)?.cards2?.some((card2) => get.name(card2) == "sha");
    },
    direct: true,
    locked: false,
    async content(event, trigger, player2) {
      await player2.chooseUseTarget({ name: "sha", isCard: true }, false, "nodistance", get.prompt(event.name), "你可以视为使用一张无距离和任何次数限制的【杀】").set("logSkill", event.name);
    }
  },
  // OP陈兰
  jujun: {
    audio: 1,
    trigger: { global: "phaseAnyEnd" },
    filter(event, player2) {
      if (!player2.hasUseTarget({ name: "wanjian", isCard: true })) {
        return false;
      }
      return game.getGlobalHistory("changeHp", (evt) => evt.player == player2 && evt.getParent(event.name) == event && evt.changedHp !== 0).length > 0 && game.getGlobalHistory("useCard", (evt) => evt.targets?.includes(player2) && evt.getParent(event.name) == event).length > 0;
    },
    forced: true,
    async content(event, trigger, player2) {
      await player2.chooseUseTarget({ name: "wanjian", isCard: true }, true, "据峻：请选择【万箭齐发】的目标");
    }
  },
  // OP梅成
  bixian: {
    audio: 1,
    trigger: { global: "phaseAnyEnd" },
    filter(event, player2) {
      if (!player2.hasUseTarget({ name: "juedou", isCard: true })) {
        return false;
      }
      return !game.getGlobalHistory("changeHp", (evt) => evt.player == player2 && evt.getParent(event.name) == event && evt.changedHp !== 0).length && game.getGlobalHistory("useCard", (evt) => evt.targets?.includes(player2) && evt.getParent(event.name) == event).length > 0;
    },
    forced: true,
    async content(event, trigger, player2) {
      await player2.chooseUseTarget({ name: "juedou", isCard: true }, true, "壁险：请选择【决斗】的目标");
    }
  },
  // OP关羽
  mbwusheng: {
    locked: false,
    mod: {
      targetInRange(card2) {
        if (card2.storage?.mbwusheng) {
          return true;
        }
      }
    },
    audio: 4,
    hiddenCard(player2, name) {
      return name == "sha" && player2.countCards("hes");
    },
    enable: ["chooseToRespond", "chooseToUse"],
    filter(event, player2) {
      if (!player2.countCards("hes")) {
        return false;
      }
      return get.inpileVCardList((info) => {
        if (info[2] != "sha") {
          return false;
        }
        return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player2, event);
      }).length > 0;
    },
    chooseButton: {
      dialog(event, player2) {
        const vcards = get.inpileVCardList((info) => {
          if (info[2] != "sha") {
            return false;
          }
          return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player2, event);
        });
        const dialog = ui.create.dialog("武圣", [vcards, "vcard"], "hidden");
        dialog.direct = true;
        return dialog;
      },
      check(button) {
        const player2 = get.player();
        const card2 = { name: button.link[2], nature: button.link[3] };
        if (_status.event.getParent().type == "phase" && game.hasPlayer((current) => player2.canUse(card2, current) && get.effect(current, card2, player2, player2) > 0)) {
          switch (button.link[2]) {
            case "sha":
              if (button.link[3] == "fire") {
                return 2.95;
              } else if (button.link[3] == "thunder" || button.link[3] == "ice") {
                return 2.92;
              } else {
                return 2.9;
              }
          }
        }
        return 1 + Math.random();
      },
      backup(links, player2) {
        return {
          audio: "mbwusheng",
          filterCard: true,
          selectCard: [1, Infinity],
          position: "hes",
          viewAs: { name: links[0][2], nature: links[0][3], storage: { mbwusheng: true } },
          popname: true,
          ai1(card2) {
            const player3 = get.player();
            const storage = player3.getStorage("mbwusheng_effect");
            const index = [1, 2, 3, 4].find((num) => !storage.map((i) => i?.[1]).includes(num));
            if (index) {
              if (ui.selected.cards.length > index - 1) {
                return 0;
              }
              return 7.5 - get.value(card2);
            }
            if (ui.selected.cards.length) {
              return 0;
            }
            return 6 - get.value(card2);
          },
          async precontent(event, trigger, player3) {
            const effect = "mbwusheng_effect";
            player3.addTempSkill(effect);
            player3.markAuto(effect, [[event, event.result.cards.length]]);
            player3.addTip(effect, [get.translation(effect), ...player3.storage[effect].map((i) => i[1]).toUniqued()].join(" "));
          }
        };
      },
      prompt(links, player2) {
        return "将任意张牌当作" + get.translation(links[0][3] || "") + "【" + get.translation(links[0][2]) + "】" + (_status.event.name == "chooseToUse" ? "使用" : "打出");
      }
    },
    ai: {
      respondSha: true,
      fireAttack: true,
      skillTagFilter(player2, tag) {
        if (!player2.countCards("hes")) {
          return false;
        }
      },
      order(item, player2) {
        if (player2 && _status.event.type == "phase") {
          let max = 0;
          if (lib.inpile_nature.some((nature) => player2.getUseValue({ name: "sha", nature }) > 0)) {
            var temp = get.order({ name: "sha" });
            if (temp > max) {
              max = temp;
            }
          }
          if (max > 0) {
            max += 4;
          }
          return max;
        }
        return 4;
      },
      result: { player: 1 }
    },
    subSkill: {
      backup: {},
      effect: {
        charlotte: true,
        onremove(player2, skill) {
          delete player2.storage[skill];
          player2.removeTip(skill);
        },
        intro: { content: (storage = []) => `本回合因【武圣】已经转化牌数：${storage.map((i) => i[1]).join("、")}` },
        trigger: { player: ["useCardAfter", "respondAfter"] },
        filter(event, player2) {
          const storage = player2.getStorage("mbwusheng_effect");
          if (event.skill !== "mbwusheng_backup" || !storage.length) {
            return false;
          }
          const index = storage.findIndex((i) => i[0].getParent() === event.getParent());
          if (index == -1) {
            return false;
          }
          const nums = storage.map((i) => i[1]);
          return !nums.some((item, i) => item == event.cards.length && i != index);
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          await player2.draw(2);
          if (trigger.addCount !== false) {
            trigger.addCount = false;
            const stat = trigger.player.getStat().card, name = trigger.card.name;
            if (typeof stat[name] === "number") {
              stat[name]--;
            }
          }
        }
      }
    }
  },
  mbyijue: {
    audio: 4,
    trigger: { source: "damageBegin4" },
    filter(event, player2) {
      return event.num >= event.player.hp;
    },
    forced: true,
    logTarget: "player",
    async content(event, trigger, player2) {
      const target = event.targets[0];
      if (!target.countCards("he")) {
        return;
      }
      let result = !target.countCards("he") ? {} : await target.chooseToGive("he", player2, get.translation(player2) + "对你发动了【义绝】", "是否交给其任意张牌", [1, Infinity], true).set("ai", (card2) => {
        const { player: player3, target: target2 } = get.event();
        const att = get.attitude(player3, target2);
        if (att > 0) {
          return 6 - get.value(card2, target2);
        }
        if (ui.selected.cards.some((cardx) => get.suit(cardx) == get.suit(card2)) || ui.selected.cards.length > 1) {
          return 0;
        }
        return -get.value(card2);
      }).forResult();
      const cards2 = result?.cards || [];
      const suits = cards2.map((card2) => get.suit(card2)).toUniqued();
      result = await player2.chooseControl().set("choiceList", [`本回合${get.translation(target)}不能使用或打出牌${cards2.length ? `，你弃置所有花色为${get.translation(suits)}的手牌` : ""}`, `防止此伤害，本轮你与${get.translation(target)}使用牌指定对方为目标时，取消之`]).set("ai", () => {
        const player3 = get.event().player, target2 = get.event().getTrigger().player;
        const att = get.attitude(player3, target2);
        return att <= 0 ? 0 : 1;
      }).forResult();
      if (result?.index === 0) {
        const effect = event.name + "_ban";
        target.addTempSkill(effect);
        const cards3 = player2.getCards("h", (card2) => suits.includes(get.suit(card2, player2)));
        await player2.modedDiscard(cards3);
      } else if (result?.index === 1) {
        trigger.cancel();
        const effect = event.name + "_effect";
        player2.addTempSkill(effect, "roundStart");
        player2.markAuto(effect, [target]);
      }
    },
    subSkill: {
      ban: {
        charlotte: true,
        mod: {
          cardEnabled2(card2) {
            return false;
          }
        },
        mark: true,
        marktext: "绝",
        intro: { content: "本回合不能使用或打出牌" }
      },
      effect: {
        charlotte: true,
        onremove: true,
        trigger: { global: "useCardToPlayer" },
        filter(event, player2) {
          if (player2 == event.player) return player2.getStorage("mbyijue_effect").includes(event.target);
          return player2 == event.target && player2.getStorage("mbyijue_effect").includes(event.player);
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          trigger.getParent().excluded.add(player2 == trigger.player ? trigger.target : player2);
        },
        ai: {
          effect: {
            player(card2, player2, target) {
              if (player2.getStorage("mbyijue_effect").includes(target)) {
                if (typeof card2 == "object" && card2.name == "sha" && player2.hasSkill("mbwusheng")) {
                  const storage = player2.getStorage("mbwusheng_effect");
                  const index = [1, 2, 3].find((num) => !storage.map((i) => i?.[1]).includes(num));
                  if (index) {
                    return;
                  }
                }
                return "zeroplayertarget";
              }
            },
            target(card2, player2, target) {
              if (target.getStorage("mbyijue_effect").includes(player2)) {
                return "zeroplayertarget";
              }
            }
          }
        },
        marktext: "义",
        intro: { content: "本轮与$互放一马" }
      }
    }
  },
  //OP孙权
  mbshizhong: {
    audio: 3,
    logAudio: () => 2,
    trigger: { player: ["phaseJieshuBegin", "phaseZhunbeiBegin"] },
    filter(event, player2) {
      return event.name === "phaseJieshu" || player2.hasCard((card2) => card2.hasGaintag("mbshizhong"), "h");
    },
    forced: true,
    async content(event, trigger, player2) {
      if (trigger.name === "phaseJieshu") {
        await player2.draw();
        const hs = player2.getCards("h");
        if (hs.length > 0) {
          player2.addGaintag(hs, event.name);
          player2.storage[event.name] = hs.length;
          player2.markSkill(event.name);
          player2.addTip(event.name, `${get.translation(event.name)} ${hs.length}张`);
          await player2.showCards(hs, `${get.translation(player2)}发动了【${get.translation(event.name)}】`);
        }
      } else {
        const num = player2.countCards("h", (card2) => card2.hasGaintag(event.name));
        await player2.draw(num);
        const goon = typeof player2.storage[event.name] === "number" && num === player2.storage[event.name];
        player2.unmarkSkill(event.name);
        if (goon) {
          player2.addTempSkill(`${event.name}_oht`);
        }
      }
    },
    intro: {
      content: "上一次因此展示了#张牌",
      onunmark(storage, player2, skill) {
        lib.skill[skill].onremove(player2, skill);
      }
    },
    onremove(player2, skill) {
      player2.removeTip(skill);
      delete player2.storage[skill];
    },
    subSkill: {
      oht: {
        charlotte: true,
        mark: true,
        intro: { content: "十万！十万！十万！" },
        audio: "mbshizhong3.mp3",
        trigger: { source: "damageBegin1" },
        forced: true,
        async content(event, trigger, player2) {
          trigger.num = 114514;
        }
      }
    }
  },
  mbcaowei: {
    audio: 2,
    trigger: { player: "damageEnd" },
    filter(event, player2) {
      const types = player2.getCards("he").map((i) => get.type2(i)).unique();
      return types.some((type) => {
        const cards2 = player2.getCards("he", (card2) => get.type2(card2) === type);
        return cards2.every((card2) => player2.canRecast(card2));
      });
    },
    forced: true,
    async content(event, trigger, player2) {
      let types = player2.getCards("he").map((i) => get.type2(i)).unique();
      types = types.filter((type) => {
        const cards2 = player2.getCards("he", (card2) => get.type2(card2) === type);
        return cards2.every((card2) => player2.canRecast(card2));
      });
      const result = types.length > 1 ? await player2.chooseButton([`###${get.translation(event.name)}###<div class="text center">选择重铸至少一个类别的所有牌</div>`, [types.map((type) => [type, `${get.translation(type)}牌`]), "tdnodes"]], [1, types.length], true).set("ai", (button) => {
        const player3 = get.player();
        return player3.getCards("he", (card2) => get.type2(card2) === button.link).reduce((sum, card2) => sum + lib.skill.zhiheng.check(card2), 0);
      }).forResult() : { bool: true, links: types };
      if (result?.bool && result.links?.length) {
        await player2.recast(player2.getCards("he", (card2) => result.links.includes(get.type2(card2))));
        await player2.draw();
      }
    }
  },
  //手杀张既
  mbdingzhen: {
    audio: "twdingzhen",
    trigger: { global: "roundStart" },
    filter(event, player2) {
      return game.hasPlayer((current) => get.distance(current, player2) <= player2.getHp() && current != player2);
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget({
        prompt: get.prompt2(event.skill),
        selectTarget() {
          return [1, get.player().getHp()];
        },
        filterTarget(card2, player3, target) {
          return get.distance(target, player3) <= player3.getHp() && target != player3;
        },
        ai(target) {
          const player3 = get.player();
          if (target == player3) {
            return 0;
          }
          return Math.max(-get.attitude(player3, target), 1);
        }
      }).forResult();
    },
    async content(event, trigger, player2) {
      const { targets: targets2 } = event;
      await game.doAsyncInOrder(targets2, async (target) => {
        const result = await target.chooseToDiscard({
          position: "h",
          filterCard(card2, player3) {
            return get.name(card2) == "sha";
          },
          prompt: "定镇：弃置一张【杀】，或本轮你于回合内使用的锦囊牌不能指定" + get.translation(player2) + "为目标",
          ai(card2) {
            if (_status.event.goon) {
              return 1;
            }
            return 0;
          }
        }).set("goon", get.attitude(target, player2) < 0 && player2.countCards("hs") <= 3 && target.countCards("hs", (card2) => target.hasValueTarget(card2)) > 1).forResult();
        if (result?.bool) {
          target.addExpose(0.1);
        } else {
          target.addTempSkill(`${event.name}_target`, "roundStart");
          target.markAuto(`${event.name}_target`, [player2]);
        }
      });
    },
    subSkill: {
      target: {
        charlotte: true,
        onremove: true,
        intro: {
          markcount: () => 0,
          content: "回合内使用的锦囊牌不能指定$为目标"
        },
        mod: {
          playerEnabled(card2, player2, target) {
            if (_status.currentPhase == player2 && get.type2(card2) == "trick" && player2.getStorage("mbdingzhen_target").includes(target)) {
              return false;
            }
          }
        }
      }
    }
  },
  mbyouye: {
    audio: "twyouye",
    trigger: { global: "phaseJieshuBegin" },
    filter(event, player2) {
      return event.player != player2 && !event.player.hasHistory("sourceDamage", (evt) => evt.player == player2) && player2.getExpansions("mbyouye").length < 5;
    },
    forced: true,
    group: "mbyouye_give",
    async content(event, trigger, player2) {
      await player2.addToExpansion({
        cards: get.cards(1, true),
        animate: "gain2",
        gaintag: [event.name]
      });
    },
    marktext: "蓄",
    intro: {
      name: "蓄(攸业)",
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player2, skill) {
      const cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    subSkill: {
      give: {
        audio: "twyouye",
        trigger: { source: "damageSource", player: "damageEnd" },
        filter(event, player2) {
          return player2.getExpansions("mbyouye").length;
        },
        forced: true,
        async content(event, trigger, player2) {
          const cards2 = player2.getExpansions("mbyouye");
          if (_status.connectMode) {
            game.broadcastAll(function() {
              _status.noclearcountdown = true;
            });
          }
          const given_map = /* @__PURE__ */ new Map();
          while (cards2.length > 0) {
            const result = cards2.length > 1 ? await player2.chooseButtonTarget({
              createDialog: [`攸业：请选择要分配的牌`, cards2],
              selectButton: [1, Infinity],
              forced: true,
              filterTarget: true,
              ai1(button) {
                return get.value(button.link);
              },
              canHidden: true,
              ai2(target) {
                const player3 = get.player();
                const card2 = ui.selected.buttons[0].link;
                if (card2) {
                  return get.value(card2, target) * get.attitude(player3, target);
                }
                return 1;
              }
            }).set("allowChooseAll", true).forResult() : await player2.chooseTarget(`攸业：令一名角色获得${get.translation(cards2)}`, true).set("ai", (target) => {
              const { player: player3, enemy } = get.event();
              const att = get.attitude(player3, target);
              if (enemy) {
                return -att;
              } else if (att > 0) {
                return att / (1 + target.countCards("h"));
              } else {
                return att / 100;
              }
            }).set("enemy", get.value(cards2[0], player2, "raw") < 0).forResult();
            if (result?.bool) {
              let links;
              if (!result.links?.length) {
                links = cards2.slice();
              } else {
                links = result.links;
              }
              cards2.removeArray(links);
              const [target] = result.targets;
              if (!given_map.has(target)) {
                given_map.set(target, links);
              } else {
                given_map.get(target).addArray(links);
              }
            } else {
              break;
            }
          }
          if (_status.connectMode) {
            game.broadcastAll(() => {
              delete _status.noclearcountdown;
              game.stopCountChoose();
            });
          }
          if (given_map.size) {
            await game.loseAsync({
              gain_list: Array.from(given_map),
              player: player2,
              cards: Object.values(given_map).slice().flat(),
              giver: player2,
              animate: "gain2"
            }).setContent("gaincardMultiple");
          }
        }
      }
    }
  },
  //手杀神司马？
  //极略神司马！
  xinrenjie: {
    audio: 2,
    trigger: {
      player: ["chooseToUseAfter", "chooseToRespondAfter"],
      global: "_wuxieAfter"
    },
    filter(event, player2) {
      if (player2.countMark("xinrenjie_used") >= 4) {
        return false;
      }
      if (event.name == "chooseToUse" && event.type == "wuxie") {
        return false;
      }
      if (event.name == "_wuxie") {
        const directHit = event._trigger?.getParent()?.directHit;
        if (directHit?.length && directHit.includes(player2)) {
          return false;
        }
        if (event.wuxieresult && event.wuxieresult == player2) {
          return false;
        }
        if (event._info_map.player == player2) {
          return false;
        }
        return true;
      }
      return event.respondTo && event.respondTo[0] !== player2 && !event.result.bool;
    },
    forced: true,
    async content(event, trigger, player2) {
      player2.addMark("xinrenjie", 1);
      player2.addTempSkill("xinrenjie_used", "roundStart");
      player2.addMark("xinrenjie_used", 1, false);
    },
    intro: {
      name2: "忍",
      content: "mark"
    },
    marktext: "忍",
    hiddenCard: (player2) => player2.countMark("xinrenjie_used") < 4,
    ai: {
      combo: "xinjilve",
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2) {
        if (player2.countMark("xinrenjie_used") >= 4) {
          return false;
        }
      }
    },
    group: "xinrenjie_change",
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      },
      change: {
        audio: "xinrenjie",
        trigger: {
          global: "phaseBefore",
          player: "enterGame"
        },
        filter(event, player2) {
          if (event.name === "phase" && game.phaseNumber > 0) {
            return false;
          }
          if (!lib.group.some((group) => group !== "shen")) {
            return false;
          }
          return player2.group === "shen" && player2._groupChosen !== "kami";
        },
        async cost(event, trigger, player2) {
          const groups = lib.group.filter((group) => group !== "shen");
          const result = event.result = await player2.chooseControl(groups, "cancel2").set("ai", () => {
            const groups2 = get.event().controls.filter((group) => !["wei", "shu", "wu", "qun"].includes(group));
            return groups2.length ? groups2.randomGet() : "cancel2";
          }).set("prompt", get.translation("xinrenjie") + "：是否变更势力？").forResult();
          event.result.bool = typeof result.control === "string" && result.control !== "cancel2";
          event.result.cost_data = result.control;
        },
        async content(event, trigger, player2) {
          await player2.changeGroup(event.cost_data);
        }
      }
    }
  },
  xinbaiyin: {
    audio: 2,
    inherit: "sbaiyin",
    filter(event, player2) {
      return player2.countMark("xinrenjie") >= 4;
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      await player2.loseMaxHp();
      await player2.addSkills("xinjilve");
    },
    derivation: ["xinjilve", "guicai", "fangzhu", "jizhi", "zhiheng", "wansha"],
    ai: { combo: "xinrenjie" }
  },
  xinlianpo: {
    audio: "lianpo",
    audioname: ["new_simayi"],
    trigger: { source: "dieAfter" },
    filter(event, player2) {
      return !player2.hasSkill("xinlianpo_mark") || get.info("xinbaiyin").derivation.some((skill) => !["xinjilve", "guicai"].includes(skill) && !player2.hasSkill(skill, null, null, false));
    },
    async cost(event, trigger, player2) {
      const skills2 = get.info("xinbaiyin").derivation.removeArray(["xinjilve", "guicai"]).filter((skill) => !player2.hasSkill(skill, null, null, false));
      if (skills2.length && player2.hasSkill("xinjilve", null, null, false)) {
        const next = player2.chooseButton(["连破：请选择一项", [skills2.map((i) => [i, `获得【${get.translation(i)}】`]).concat(["于此回合结束后获得一个额外回合"]), "textbutton"]]);
        next.set("ai", (button) => {
          const link = button.link, skills3 = get.event().skills, player3 = get.player();
          if ((skills3.length <= 2 || game.countPlayer() <= 2) && !player3.hasSkill("xinlianpo_mark", null, null, false) && link == "于此回合结束后获得一个额外回合") {
            return 6;
          }
          if (link == "zhiheng" && player3.countCards("h") > 0) {
            return 5;
          }
          if (link == "jizhi" && (!skills3.includes("zhiheng") || player3.countCards("hs", { type: "trick" }))) {
            return 3;
          }
          if (link == "wansha" && game.hasPlayer((current) => get.attitude(player3, current) < 0 && current.getHp() < 2 && (player3 == _status.currentPhase || player3.hasSkill("xinlianpo_mark", null, null, false)))) {
            return 2;
          }
          return 1;
        });
        next.set("skills", skills2);
        next.set("filterButton", (button) => {
          return lib.skill[button.link] || !get.player().hasSkill("xinlianpo_mark");
        });
        const { bool, links } = await next.forResult();
        event.result = {
          bool,
          cost_data: links
        };
      } else {
        const { bool } = await player2.chooseBool("连破：于此回合结束后获得一个额外回合？").forResult();
        event.result = {
          bool
        };
      }
    },
    async content(event, trigger, player2) {
      const links = event.cost_data;
      if (links && get.info("xinbaiyin").derivation.includes(links[0])) {
        await player2.addSkills(links[0]);
      } else {
        player2.addTempSkill("xinlianpo_mark");
        player2.insertPhase();
      }
    },
    subSkill: {
      mark: {
        charlotte: true,
        mark: true,
        intro: {
          content: "本回合结束后执行一个额外回合"
        }
      }
    }
  },
  xinjilve: {
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return player2.hasMark("xinrenjie");
    },
    async cost(event, trigger, player2) {
      const limit = Math.min(3, player2.countMark("xinrenjie"));
      const choices = Array.from({
        length: limit
      }).map((_, i) => [i, get.cnNumber(i + 1, true)]);
      const history = game.getAllGlobalHistory("everything", (evt) => evt.name == "xinjilve" && evt.player == player2 && Array.isArray(evt.cost_data) && get.info("xinbaiyin").derivation.includes(evt.cost_data[0]));
      const num = Math.max(2, history.length + 1);
      const skills2 = get.info("xinbaiyin").derivation.removeArray(["xinjilve", "guicai"]).filter((skill) => !player2.hasSkill(skill, null, null, false));
      if (skills2.length && limit >= num) {
        const next = player2.chooseButton(2, ["极略：请选择你要移去的“忍”标记数和相应操作", '<div class="text center">移去“忍”标记数</div>', [choices, "tdnodes"], '<div class="text center">执行的操作</div>', [skills2.map((i) => [i, `获得【${get.translation(i)}】`]).concat(["摸牌"]), "tdnodes"]]);
        next.set("filterButton", (button) => {
          const link = button.link;
          if (Boolean(ui.selected.buttons.length) !== (typeof link == "number")) {
            return false;
          }
          if (ui.selected.buttons.length) {
            if (ui.selected.buttons[0].link == "摸牌") {
              return link <= 1;
            }
            return link == get.event().num - 1;
          }
          return true;
        });
        next.set("ai", (button) => {
          const link = button.link, { num: num2, skills: skills3, player: player3 } = get.event();
          if (!ui.selected.buttons.length) {
            if (num2 > 0 && link == "摸牌") {
              return 2;
            }
            if (link == "zhiheng" && player3.hasCards("h")) {
              return 10;
            }
            if (link == "jizhi" && (!skills3.includes("zhiheng") || player3.countCards("hs", { type: "trick" }))) {
              return 8;
            }
            if (link == "fangzhu") {
              return 6;
            }
            return 0;
          }
          return link == (ui.selected.buttons.length && ui.selected.buttons[0].link != "摸牌" ? num2 - 1 : 1);
        });
        next.set("num", num);
        next.set("skills", skills2);
        const { bool, links } = await next.forResult();
        event.result = {
          bool,
          cost_data: links
        };
      } else {
        const draw = Array.from({
          length: Math.min(2, limit)
        }).map((_, i) => get.cnNumber(i + 1, true));
        const result = await player2.chooseControl(draw, "cancel2").set("prompt", get.prompt("xinrenjie")).set("prompt2", `你可以移去至多${get.cnNumber(draw.length)}枚“忍”标记并摸等量张牌`).set("ai", () => {
          return get.event().choice;
        }).set(
          "choice",
          (function() {
            if (!player2.hasSkill("jizhi", null, null, false)) {
              return "cancel2";
            }
            return draw[draw.length - 1];
          })()
        ).forResult();
        event.result = {
          bool: result.control != "cancel2",
          cost_data: result.index
        };
      }
    },
    async content(event, trigger, player2) {
      const choice = event.cost_data;
      if (typeof choice == "number") {
        player2.removeMark("xinrenjie", choice + 1);
        await player2.draw(choice + 1);
      } else if (get.info("xinbaiyin").derivation.includes(choice[0])) {
        const history = game.getAllGlobalHistory("everything", (evt) => evt.name == "xinjilve" && evt.player == player2 && Array.isArray(evt.cost_data) && get.info("xinbaiyin").derivation.includes(evt.cost_data[0]));
        const num = Math.max(2, history.length);
        player2.removeMark("xinrenjie", num);
        await player2.addSkills(choice[0]);
      } else {
        player2.removeMark("xinrenjie", choice[1] + 1);
        await player2.draw(choice[1] + 1);
      }
    },
    group: "xinjilve_gain",
    subSkill: {
      gain: {
        audio: "xinjilve",
        trigger: {
          player: "changeSkillsAfter"
        },
        filter(event, player2) {
          return event.addSkill.includes("xinjilve");
        },
        forced: true,
        async content(event, trigger, player2) {
          let skills2 = ["guicai"];
          const groupList = /* @__PURE__ */ new Map([
            ["wei", "fangzhu"],
            ["shu", "jizhi"],
            ["wu", "zhiheng"],
            ["qun", "wansha"],
            ["key", "hiroto_zonglve"]
          ]);
          if (Array.from(groupList.keys()).includes(player2.group)) {
            skills2.push(groupList.get(player2.group));
          }
          skills2 = skills2.filter((skill) => !player2.hasSkill(skill, null, null, false));
          if (skills2.length) {
            await player2.addSkills(skills2);
          }
        }
      }
    },
    ai: {
      notemp: true
    }
  },
  //缘檀石槐
  mblianzhan: {
    audio: 2,
    trigger: {
      player: "useCardAfter"
    },
    filter(event, player2) {
      return event.card.name == "sha" && typeof get.number(event.card) == "number" && event.targets?.length == 1 && event.targets[0]?.isIn();
    },
    direct: true,
    async content(event, trigger, player2) {
      const {
        targets: [target],
        card: card2
      } = trigger;
      player2.addTempSkill(`${event.name}_draw`);
      await player2.chooseToUse(get.prompt2(event.name, target).replace("更大", `大于${get.number(card2)}`)).set("num", get.number(card2)).set("targetx", target).set("filterCard", (card3, player3, event2) => {
        return get.number(card3) > get.event().num && get.name(card3) == "sha" && lib.filter.filterCard(card3, player3, event2);
      }).set("filterTarget", function(card3, player3, target2) {
        const { targetx } = get.event();
        if (!ui.selected.targets?.includes(targetx) && target2 != targetx) {
          return false;
        }
        return lib.filter.targetEnabled.call(this, card3, player3, target2);
      }).set("addCount", false).set("logSkill", event.name).forResult();
    },
    locked: false,
    mod: {
      aiOrder(player2, card2, num) {
        if (get.event().name == "chooseToUse" && get.name(card2) == "sha") {
          const numx = get.number(card2) || 13;
          return num + 13 - numx;
        }
      }
    },
    subSkill: {
      draw: {
        charlotte: true,
        forced: true,
        popup: false,
        trigger: {
          player: "useCard"
        },
        filter(event, player2) {
          const evt = event.getParent(2);
          return evt?.name == "mblianzhan" && evt?.player == player2;
        },
        async content(event, trigger, player2) {
          await player2.draw();
        }
      }
    }
  },
  mbkoulve: {
    audio: 2,
    forced: true,
    trigger: {
      player: "gainBegin"
    },
    filter(event, player2) {
      return event.getParent("phaseDraw")?.player != player2;
    },
    async content(event, trigger, player2) {
      trigger.gaintag.add(event.name);
    },
    mod: {
      ignoredHandcard(card2, player2) {
        if (card2.hasGaintag("mbkoulve")) {
          return true;
        }
      },
      cardDiscardable(card2, player2, name) {
        if (card2.hasGaintag("mbkoulve") && name == "phaseDiscard") {
          return false;
        }
      },
      cardname(card2, player2) {
        const event = get.event();
        if (!["chooseToUse", "chooseToRespond"].includes(event.name)) {
          return;
        }
        if (get.itemtype(card2) === "card" && card2.hasGaintag("mbkoulve")) {
          return "sha";
        }
      },
      cardnature(card2, player2) {
        const event = get.event();
        if (!["chooseToUse", "chooseToRespond"].includes(event.name)) {
          return;
        }
        if (get.itemtype(card2) === "card" && card2.hasGaintag("mbkoulve")) {
          return false;
        }
      }
    }
  },
  //缘吕布
  mblvezhen: {
    audio: 2,
    trigger: {
      player: "phaseUseBegin"
    },
    filter(event, player2) {
      return game.hasPlayer((target) => target != player2);
    },
    logTarget: (event, player2) => game.filterPlayer((target) => target != player2),
    check: () => true,
    async content(event, trigger, player2) {
      const { targets: targets2 } = event;
      await game.doAsyncInOrder(targets2, async (target) => {
        const num = target.countMark("mblvezhen_show") + 1;
        const hs = target.getCards("h");
        let result;
        if (!hs.length) {
          result = { bool: false };
        } else if (hs.length <= num) {
          result = await target.chooseBool({
            prompt: `掠阵：是否展示所有手牌，或者取消令${get.translation(player2)}视为对你使用一张无距离次数限制的【杀】`,
            choice: Math.random() > 0.5
          }).forResult();
          result.cards = hs;
        } else {
          result = await target.chooseCard({
            prompt: `掠阵：是否展示${num}张手牌，或者取消令${get.translation(player2)}视为对你使用一张无距离次数限制的【杀】`,
            selectCard: num,
            position: "h",
            ai(card2) {
              const { sourcex, player: player3, cardx, num: num2 } = get.event();
              if (!sourcex.canUse(cardx, player3, false, false) || !sourcex.hasSkill("mbhengwei") || get.effect(player3, cardx, sourcex, player3) > 0) {
                return 0;
              }
              const att = get.attitude(player3, sourcex);
              if (att > 0 || player3.countCards("h", (card3) => get.color(card3) != "red") >= num2) {
                return get.color(card2) == "red" ? Math.random() : Math.random() + 1;
              }
              return Math.random() - 0.5;
            }
          }).set("sourcex", player2).set("num", num).set("cardx", get.autoViewAs({ name: "sha", isCard: true })).forResult();
        }
        if (result?.bool) {
          target.addMark("mblvezhen_show", 1, false);
          return target.showCards(result.cards);
        } else {
          const card2 = get.autoViewAs({ name: "sha", isCard: true });
          if (player2.canUse(card2, target, false, false)) {
            return player2.useCard({ card: card2, targets: [target], addCount: false });
          }
        }
      });
    }
  },
  mbhengwei: {
    audio: 2,
    forced: true,
    trigger: {
      source: "damageBegin1"
    },
    filter(event, player2) {
      return event.player != player2;
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      if (!target.countGainableCards(player2, "h")) {
        trigger.num++;
      } else {
        const result = await target.chooseCard(`横威：是否${get.translation(player2)}交给一张手牌， 否则此次伤害+1`, "h").set("ai", (card2) => {
          const trigger2 = get.event().getTrigger();
          const { player: player3 } = get.event();
          if (trigger2.num + 1 >= player3.getHp()) {
            return 7.5 - get.value(card2);
          }
          return 6 - get.value(card2);
        }).forResult();
        if (result?.bool) {
          const { cards: cards2 } = result;
          await target.showCards(cards2);
          await target.give(cards2, player2);
        } else {
          trigger.num++;
        }
      }
    },
    group: ["mbhengwei_show"],
    subSkill: {
      show: {
        audio: "mbhengwei",
        forced: true,
        trigger: {
          global: ["showCardsAfter"]
        },
        filter(event, player2, name, target) {
          return _status.currentPhase == player2 && target?.isIn();
        },
        getIndex(event, player2) {
          if (_status.currentPhase != player2) {
            return [];
          }
          return game.filterPlayer((target) => target != player2 && event.getShown(target)?.cards2?.length);
        },
        logTarget(event, player2, name, target) {
          return target;
        },
        async content(event, trigger, player2) {
          const {
            targets: [target]
          } = event;
          const cards2 = trigger.getShown(target).cards2;
          target.addTempSkill("mbhengwei_debuff");
          target.markAuto("mbhengwei_debuff", cards2.map((i) => get.color(i)).unique());
        }
      },
      debuff: {
        charlotte: true,
        onremove: true,
        intro: {
          content: "不能使用$的手牌"
        },
        mod: {
          cardEnabled(card2, player2, result) {
            if (!card2.cards?.length) {
              return;
            }
            const hs = player2.getCards("h");
            const colors = player2.getStorage("mbhengwei_debuff");
            if (card2.cards?.some((i) => hs.includes(i) && colors.includes(get.color(i)))) {
              return false;
            }
          },
          cardSavable(card2, player2, target, result) {
            return get.info("mbhengwei_debuff").mod?.cardEnabled?.call(this, card2, player2, result);
          }
        }
      }
    }
  },
  //缘高顺
  mbjiren: {
    audio: 2,
    limited: true,
    skillAnimation: true,
    animationColor: "soil",
    enable: "phaseUse",
    manualConfirm: true,
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      game.addGlobalSkill(`${event.name}_global`);
    },
    subSkill: {
      global: {
        mod: {
          playerEnabled(card2, player2, target) {
            if (get.subtype(card2) != "equip1" && player2 == target) {
              return false;
            }
          }
        }
      }
    },
    ai: {
      order: 1,
      result: {
        player: 1
      }
    }
  },
  mbjuezhi: {
    audio: 4,
    enable: "phaseUse",
    filter(event, player2) {
      return player2.getStorage("mbjuezhi_used").length < 2;
    },
    chooseButton: {
      dialog(event, player2) {
        const list = [
          [`nodamage`, `摸两张牌，视为使用一张【决斗】，然后弃置手牌中所有非伤害牌`],
          ["damage", "摸两张牌，然后回复1点体力并弃置手牌中所有伤害牌"]
        ];
        return ui.create.dialog("决止", [list, "textbutton"], "hidden");
      },
      check(button) {
        return Math.random() + (button.link == "damage" && get.player().isDamaged() ? 1 : 0);
      },
      filter(button) {
        return !get.player().getStorage("mbjuezhi_used").includes(button.link);
      },
      backup(links, player2) {
        return {
          audio: "mbjuezhi",
          choice: links[0],
          logAudio: (event, player3) => {
            const control = get.info("mbjuezhi_backup")?.choice;
            return control == "damage" ? ["mbjuezhi3.mp3", "mbjuezhi4.mp3"] : 2;
          },
          async content(event, trigger, player3) {
            const { choice } = get.info(event.name);
            player3.addTempSkill("mbjuezhi_used", "phaseChange");
            player3.markAuto("mbjuezhi_used", choice);
            await player3.draw(2);
            if (choice == "nodamage") {
              const card2 = get.autoViewAs({ name: "juedou", isCard: true });
              if (player3.hasUseTarget(card2, true, false)) {
                await player3.chooseUseTarget(card2, false);
              }
              await player3.modedDiscard(player3.getCards("h", (card3) => !get.is.damageCard(card3, true)));
            } else if (choice == "damage") {
              await player3.recover();
              await player3.modedDiscard(player3.getCards("h", (card2) => get.is.damageCard(card2, true)));
            }
          }
        };
      }
    },
    ai: {
      order: 1,
      result: {
        player: 1
      }
    },
    subSkill: {
      backup: {},
      used: {
        charlotte: true,
        onremove: true
      }
    }
  },
  //韩玄
  mbweizhan: {
    audio: 2,
    trigger: {
      global: "phaseUseBegin"
    },
    filter(event, player2) {
      return event.player != player2 && get.distance(player2, event.player) <= 1;
    },
    logTarget: "player",
    check(event, player2) {
      return get.attitude(player2, event.player) > 0 || event.player.countCards("h") > 6;
    },
    async content(event, trigger, player2) {
      await player2.draw();
      const target = trigger.player;
      const num = player2.countGainableCards(target, "he");
      if (num <= 0) {
        return;
      }
      const result = await player2.chooseToGive(target, "he", [1, Math.min(3, num)], true).set("ai", (card2) => {
        if (ui.selected.cards.length) {
          return 0;
        }
        return 6 - get.value(card2);
      }).set("complexCard", true).forResult();
      if (!result?.bool || !result.cards?.length) {
        return;
      }
      const numx = result.cards.length;
      player2.when({
        global: "phaseEnd"
      }).filter((evt) => evt == trigger.getParent("phase", true, true)).step(async (event2, trigger2, player3) => {
        let damage = 0;
        target.getHistory("sourceDamage", (evt) => {
          damage += evt.num;
        });
        if (damage >= numx) {
          await player3.draw(2);
        } else {
          const num2 = Math.min(2, target.countGainableCards(player3, "he"));
          if (num2 > 0) {
            await player3.gainPlayerCard(target, "he", num2, true);
          }
        }
      });
    }
  },
  mbyilv: {
    audio: 4,
    usable: 1,
    trigger: {
      target: "useCardToPlayered"
    },
    logTarget: "player",
    logAudio: () => 1,
    filter(event, player2) {
      return player2 != event.player && event.card.name == "sha" && event.player.isIn();
    },
    check(event, player2) {
      return get.effect(player2, event.card, event.player, player2) <= 0;
    },
    async content(event, trigger, player2) {
      const target = trigger.player;
      const result = await player2.chooseToDuiben(target).set("title", "谋弈").set("namelist", ["克敌先机", "洞若观火", "金蝉脱壳", "弃履狂奔"]).set("translationList", [`以防止${get.translation(player2)}令此杀伤害-1`, `以防止${get.translation(player2)}令你随机弃置一张手牌`, `若成功，你令${get.translation(target)}随机弃置一张手牌`, `若成功，你令此杀伤害-1`]).set("ai", (button) => {
        return 1 + Math.random();
      }).forResult();
      if (result.bool) {
        if (result.player == "db_def1") {
          const cards2 = target.getDiscardableCards(player2, "he");
          if (cards2.length) {
            const discards = cards2.randomGets(1);
            await target.modedDiscard(discards);
            if (get.type(discards[0]) == "basic") {
              await player2.gain(discards, "gain2");
            }
          }
        } else {
          const skill = "mbyilv_effect";
          target.addTempSkill(skill);
          target.markAuto(skill, trigger.card);
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        trigger: {
          source: "damageBegin1"
        },
        filter(event, player2) {
          return event.card && player2.getStorage("mbyilv_effect").includes(event.card);
        },
        direct: true,
        firstDo: true,
        async content(event, trigger, player2) {
          game.log(trigger.card, "造成的伤害-1");
          trigger.num--;
        }
      },
      true1: {
        audio: "mbyilv2.mp3"
      },
      true2: {
        audio: "mbyilv3.mp3"
      },
      false: {
        audio: "mbyilv4.mp3"
      }
    }
  },
  //老友记dlc-合肥
  hefeichonglei: {
    audio: 2,
    forced: true,
    trigger: {
      global: ["useCard", "respond"]
    },
    filter(event, player2) {
      if (!player2.isPhaseUsing()) {
        return false;
      }
      if (player2.countMark("hefeichonglei_used") >= game.countPlayer((target2) => target2 != player2)) {
        return false;
      }
      if (!event.respondTo || !Array.isArray(event.respondTo)) {
        return false;
      }
      if (player2 != event.player && player2 != event.respondTo[0]) {
        return false;
      }
      if (event.player == event.respondTo[0]) {
        return false;
      }
      const target = event.player == player2 ? event.respondTo[0] : event.player;
      return target.countGainableCards(player2, "h");
    },
    logTarget(event, player2) {
      if (event.player == player2) {
        return event.respondTo[0];
      }
      return event.player;
    },
    async content(event, trigger, player2) {
      player2.addTempSkill(`${event.name}_used`, { global: ["phaseChange", "phaseBeforeStart", "phaseAfter"] });
      player2.addMark(`${event.name}_used`, 1, false);
      const {
        targets: [target]
      } = event;
      await player2.gainPlayerCard({ target, position: "h", forced: true });
    },
    global: "hefeichonglei_wansha",
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      },
      wansha: {
        enable: ["chooseToUse", "chooseToRespond"],
        filterCard(card2, player2) {
          const target = get.event()?.getParent("phaseUse", true, true)?.player;
          if (!target || !target.hasSkill("hefeichonglei") || target == player2 || player2.isDying()) {
            return false;
          }
          if (target.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(target, "hefei_yuejin")) {
            return true;
          }
          return get.type(card2) != "basic";
        },
        position: "hs",
        viewAs: {
          name: "shan"
        },
        viewAsFilter(player2) {
          const target = get.event()?.getParent("phaseUse", true, true)?.player;
          if (!target || !target.hasSkill("hefeichonglei") || target == player2 || player2.isDying()) {
            return false;
          }
          if (player2.countCards("hs", (card2) => {
            if (target.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(target, "hefei_yuejin")) {
              return true;
            }
            return get.type(card2) != "basic";
          })) {
            return true;
          }
          return false;
        },
        prompt() {
          const target = get.event()?.getParent("phaseUse", true, true)?.player;
          if (!target || !target.hasSkill("hefeichonglei")) {
            return "";
          }
          if (target.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(target, "hefei_yuejin")) {
            return "将一张手牌当作闪使用或打出";
          }
          return "将一张非基本手牌当作闪使用或打出";
        },
        check(card2) {
          const val = get.value(card2);
          if (_status.event.name == "chooseToRespond") {
            return 1 / Math.max(0.1, val);
          }
          return 5 - val;
        },
        locked: false,
        ai: {
          respondShan: true,
          skillTagFilter(player2) {
            const target = get.event()?.getParent("phaseUse", true, true)?.player;
            if (!target || !target.hasSkill("hefeichonglei") || target == player2 || player2.isDying()) {
              return false;
            }
            if (!player2.countCards("hs", (card2) => {
              if (target.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(target, "hefei_yuejin")) {
                return true;
              }
              return get.type(card2) != "basic";
            })) {
              return false;
            }
          }
        },
        mod: {
          cardEnabled(card2, player2) {
            if (get.name(card2) == "shan" || player2.isDying()) {
              return;
            }
            const target = get.event()?.getParent("phaseUse", true, true)?.player;
            if (!target || !target.hasSkill("hefeichonglei") || target == player2) {
              return;
            }
            const hs = player2.getCards("hs", (card3) => {
              if (target.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(target, "hefei_yuejin")) {
                return true;
              }
              return get.type(card3) != "basic";
            });
            if ("cards" in card2 && Array.isArray(card2.cards) && card2.cards.containsSome(...hs)) {
              return false;
            }
          },
          cardRespondable(card2, player2) {
            if (get.name(card2) == "shan" || player2.isDying() || get.event().skill == "hefeichonglei_wansha") {
              return;
            }
            const target = get.event()?.getParent("phaseUse", true, true)?.player;
            if (!target || !target.hasSkill("hefeichonglei") || target == player2) {
              return;
            }
            const hs = player2.getCards("hs", (card3) => {
              if (target.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(target, "hefei_yuejin")) {
                return true;
              }
              return get.type(card3) != "basic";
            });
            if ("cards" in card2 && Array.isArray(card2.cards) && card2.cards.containsSome(...hs)) {
              return false;
            }
          },
          cardSavable(card2, player2) {
            if (get.name(card2) == "shan" || player2.isDying()) {
              return;
            }
            const target = get.event()?.getParent("phaseUse", true, true)?.player;
            if (!target || !target.hasSkill("hefeichonglei") || target == player2) {
              return;
            }
            const hs = player2.getCards("hs", (card3) => {
              if (target.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(target, "hefei_yuejin")) {
                return true;
              }
              return get.type(card3) != "basic";
            });
            if ("cards" in card2 && Array.isArray(card2.cards) && card2.cards.containsSome(...hs)) {
              return false;
            }
          }
        }
      }
    }
  },
  hefeidangshi: {
    audio: 4,
    trigger: { player: "useCardAfter" },
    filter(event, player2) {
      return get.is.damageCard(event.card) && event.targets?.some((target) => target !== player2);
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget({
        prompt: get.prompt2(event.skill),
        filterTarget(card2, player3, target) {
          return get.event().targets.includes(target);
        },
        ai(target) {
          const player3 = get.player();
          if (get.attitude(player3, target) > 0) {
            return 0;
          }
          if (!target.countCards("he")) {
            return get.damageEffect(target, player3, player3);
          }
          return 10 / target.countCards("he");
        }
      }).set(
        "targets",
        trigger.targets.filter((target) => target !== player2)
      ).forResult();
    },
    async content(event, trigger, player2) {
      const {
        targets: [target],
        name
      } = event;
      const getNum = (player3) => {
        let num = Math.max(1, player3.countMark(`hefeidangshi_count`));
        if (player3.hasSkill("hefeiheyuzhangliao") && get.info("friendgongli").isFriendOf(player3, "hefei_lidian")) {
          num = 3;
        }
        return num;
      };
      const list = [
        ["useCard", `对${get.translation(player2)}使用一张非转化且非虚拟的【${get.translation(trigger.card.name)}】`],
        ["discard", `弃置${get.cnNumber(getNum(player2))}张牌`],
        ["damage", `${get.translation(player2)}对你造成1点伤害`]
      ];
      const canChoose = list.map((info) => info[0]).filter((info) => {
        switch (info) {
          case "useCard": {
            return target.countCards("hs", (card2) => {
              if (get.name(card2) != trigger.card.name) {
                return false;
              }
              return target.canUse(card2, player2);
            }) > 0;
          }
          case "discard": {
            const num = getNum(player2);
            return target.countDiscardableCards(target, "he") >= num;
          }
          default: {
            return true;
          }
        }
      });
      const result = canChoose.length > 1 ? await target.chooseButton({
        createDialog: ["荡势：请选择一项", [list, "textbutton"]],
        forced: true,
        filterButton(button) {
          return get.event().canChoose?.includes(button.link);
        },
        ai(button) {
          const { player: player3, getNum: getNum2 } = get.event(), trigger2 = get.event().getTrigger();
          if (button.link == "useCard") {
            const cards2 = player3.getCards("hs", (card2) => {
              if (get.name(card2) != trigger2.card.name) {
                return false;
              }
              return player3.canUse(card2, trigger2.player);
            });
            const check = (card2) => get.effect(trigger2.player, card2, player3, player3);
            return cards2.length ? check(cards2.maxBy(check)) : 0;
          }
          if (button.link == "discard") {
            return get.effect(player3, { name: "guohe_copy2" }, player3, player3) / getNum2;
          }
          return get.damageEffect(player3, trigger2.player, player3);
        }
      }).set("getNum", getNum(player2)).set("canChoose", canChoose).forResult() : {
        bool: true,
        links: canChoose
      };
      if (!result?.bool || !result.links?.length) {
        return;
      }
      const type = result.links[0];
      const index = ["useCard", "discard", "damage"].indexOf(type);
      game.log(player2, "选择了", "#g【荡势】", "的", "#y选项" + get.cnNumber(index + 1, true));
      switch (type) {
        case "useCard": {
          await target.chooseToUse({
            filterCard(card2, player3, event2) {
              if (get.itemtype(card2) != "card" || get.name(card2) != get.event().cardx) {
                return false;
              }
              return lib.filter.filterCard.apply(this, arguments);
            },
            prompt: `荡势：对${get.translation(player2)}使用一张非转化且非虚拟的【${get.translation(trigger.card.name)}】`,
            addCount: false,
            forced: true,
            filterTarget(card2, player3, target2) {
              if (target2 != get.event().sourcex) {
                return false;
              }
              return lib.filter.filterTarget.apply(this, arguments);
            }
          }).set("targetRequired", true).set("complexTarget", true).set("cardx", trigger.card.name).set("sourcex", player2);
          break;
        }
        case "discard": {
          const num = Math.min(target.countDiscardableCards(target, "he"), getNum(player2));
          player2.addMark(`${name}_count`, 1, false);
          player2.addTempSkill(`${name}_count`, "roundStart");
          if (num > 0) {
            await target.chooseToDiscard({ position: "he", forced: true, selectCard: num, allowChooseAll: true });
          }
          break;
        }
        default: {
          await target.damage();
        }
      }
      const bool = !player2.getStorage("hefeidangshi_choices").includes(type);
      if (bool) {
        for (const name2 of lib.phaseName) {
          const evt = event.getParent(name2);
          if (!evt || evt.name != name2) {
            continue;
          }
          player2.addTempSkill("hefeidangshi_choices", name2 + "After");
          player2.markAuto("hefeidangshi_choices", [type]);
          await player2.draw();
          player2.addTempSkill("hefeidangshi_effect", name2 + "After");
          player2.addMark("hefeidangshi_effect", 1, false);
          break;
        }
      }
    },
    subSkill: {
      count: { charlotte: true, onremove: true },
      effect: {
        charlotte: true,
        onremove: true,
        intro: { content: "本阶段出杀次数+#" },
        mod: {
          cardUsable(card2, player2, num) {
            if (card2.name == "sha") {
              return num + player2.countMark("hefeidangshi_effect");
            }
          }
        }
      },
      choices: {
        charlotte: true,
        onremove: true,
        marktext: "势",
        intro: {
          content: (storage, player2) => `本阶段【荡势】已执行选项：${storage.map((item) => {
            const index = ["useCard", "discard", "damage"].indexOf(item);
            return `选项${get.cnNumber(index + 1, true)}`;
          }).join("、")}`
        }
      }
    }
  },
  hefeiheyuzhangliao: {
    audio: 2,
    locked: true,
    ai: {
      combo: ["hefeichonglei", "hefeidangshi"]
    }
  },
  hefeiduanjin: {
    audio: 2,
    trigger: {
      player: "useCardAfter"
    },
    filter(event, player2) {
      if (get.type(event.card) != "basic") {
        return false;
      }
      return game.hasPlayer((current) => {
        if (current == player2) {
          return false;
        }
        return current.hasDiscardableCards(player2, "he");
      });
    },
    async cost(event, trigger, player2) {
      const targets2 = game.filterPlayer((current) => {
        if (current == player2) {
          return false;
        }
        return current.hasDiscardableCards(player2, "he");
      });
      if (!targets2.length) {
        return;
      }
      event.result = await player2.chooseTarget(get.prompt2(event.skill), (card2, player3, target) => {
        return get.event().targetx.includes(target);
      }).set("targetx", targets2).set("ai", (target) => {
        const player3 = get.player();
        return get.effect(target, { name: "guohe_copy2" }, player3, player3);
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      await player2.discardPlayerCard(target, "he", true);
    }
  },
  hefeigaigong: {
    audio: 4,
    trigger: {
      source: "damageSource",
      player: "damageEnd"
    },
    filter(event, player2) {
      if (event.player == event.source) {
        return false;
      }
      return [event.player, event.source].some((current) => {
        if (!current?.isIn()) {
          return false;
        }
        return current.hasCards("h");
      });
    },
    usable: 1,
    async cost(event, trigger, player2) {
      const targets2 = [trigger.player, trigger.source].filter((current) => {
        if (!current?.isIn()) {
          return false;
        }
        return current.hasCards("h");
      });
      player2 == trigger.player ? trigger.source : trigger.player;
      event.result = await player2.chooseTarget(get.prompt2(event.skill), (card2, player3, target) => {
        return get.event().targetx.includes(target);
      }).set("targetx", targets2).set("ai", (target) => {
        const { player: player3, bottomCards } = get.event();
        const cards2 = bottomCards.filter((card2) => card2.isKnownBy(player3));
        const suits = cards2.map((card2) => get.suit(card2)).toUniqued();
        const att = get.attitude(player3, target);
        const hs = player3.getCards("h");
        const ts = target.getCards("h");
        if (att < 0) {
          if (cards2.length) {
            if (suits.length > 1) {
              return 10;
            }
          }
          if (player3.hasSkillTag("viewHandcard", null, target, true)) {
            return 1.2 * Math.min(2, ts);
          }
          if (target.hasCards("h", (card2) => get.is.shownCard(card2))) {
            return 1.1 * Math.min(2, ts);
          }
          if (hs.length <= ts && get.value(hs) <= 7) {
            return 10;
          }
          return Math.max(0.1, 3 - ts);
        } else {
          if (cards2.length) {
            if (get.value(cards2) >= 7) {
              return 11;
            }
            if (suits.length > 1) {
              return 9;
            }
          }
          return Math.min(2, ts);
        }
      }).set("bottomCards", Array.from(ui.cardPile.childNodes).slice(-2)).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      if (!target.hasCards("h")) {
        return;
      }
      let result = await player2.choosePlayerCard(target, [1, 2], true, "h", `展示${get.translation(target)}的至多两张手牌并令其与牌堆底的等量牌交换`).set("ai", (button) => {
        const { player: player3, target: target2 } = get.event();
        const card3 = button.link;
        const att = get.attitude(player3, target2);
        if (player3 == target2) {
          if (!ui.selected.buttons.length) {
            if (player3.hasCards("h", (cardx) => get.suit(card3) !== get.suit(cardx))) {
              if (card3.name == "sha") {
                return 10;
              }
              return 7 - get.value(card3);
            }
            return 6 - get.value(card3);
          } else {
            const card1 = ui.selected.buttons[0].link;
            if (get.suit(card3) !== get.suit(card1) && [card1, card3].some((cardx) => player3.hasValueTarget(cardx, false))) {
              return 10;
            }
            return -get.value(card3);
          }
        } else {
          if (player3.hasSkillTag("viewHandcard", null, target2, true)) {
            if (!ui.selected.buttons.length) {
              if (target2.hasCards("h", (cardx) => get.suit(card3) !== get.suit(cardx))) {
                return att > 0 ? 6.5 - get.value(card3) : get.value(card3);
              }
              if (get.is.shownCard(card3) && player3.hasValueTarget(card3, false)) {
                return 10;
              }
              return att > 0 ? 6 - get.value(card3) : get.value(card3);
            } else {
              const card1 = ui.selected.buttons[0].link;
              if (get.suit(card3) !== get.suit(card1) && [card1, card3].some((cardx) => player3.hasValueTarget(cardx, false))) {
                return 10;
              }
              return att > 0 ? 6 - get.value(card3) : get.value(card3);
            }
          }
        }
      }).forResult();
      if (!result?.bool || !result.links?.length) {
        return;
      }
      const puts = result.links;
      await player2.showCards(puts, `${get.translation(player2)}发动了【慨公】`);
      const gains = get.bottomCards(puts.length, true);
      if (puts.length && gains.length) {
        target.$throw(puts.length, 1e3);
        await target.lose(puts, ui.cardPile);
        await target.gain(gains, "draw");
      }
      game.addCardKnower(puts, player2);
      const allCards = [puts, gains].flat(), suits = allCards.map((card3) => get.suit(card3)).toUniqued();
      if (suits.length < 2) {
        return;
      }
      result = await player2.chooseButton([
        "慨公：你可以使用其中一张牌",
        [
          allCards.map((card3) => [
            card3,
            (() => {
              return gains.includes(card3) ? "手牌" : "牌堆底";
            })()
          ]),
          (item, type, position, noclick, node) => {
            node = ui.create.buttonPresets.card(item[0], type, position, noclick);
            game.createButtonCardsetion(item[1], node);
            return node;
          }
        ]
      ]).set("filterButton", (button) => {
        return get.player().hasUseTarget(button.link, false);
      }).set("ai", (button) => {
        const card3 = button.link;
        let eff = get.player().getUseValue(button.link, false);
        if (get.owner(card3)) {
          const att = get.sgnAttitude(player2, get.owner(card3));
          eff += -1.5 * att;
        }
        return eff;
      }).forResult();
      if (!result?.bool || !result?.links?.length) {
        return;
      }
      const card2 = result.links[0];
      if (player2.hasUseTarget(card2)) {
        if (puts.includes(card2)) {
          game.clearCardKnowers(card2);
        }
        await player2.chooseUseTarget(card2, true, false, "nodistance");
      }
    }
  },
  hefeiheyulidian: {
    audio: 2,
    trigger: {
      player: "useCard",
      global: "discardAfter"
    },
    filter(event, player2) {
      const name = event.name == "useCard" ? "hefei_zhangliao" : "hefei_yuejin";
      if (!get.info("friendgongli").isFriendOf(player2, name)) {
        return false;
      }
      const evt = event.getParent(2);
      if (event.name == "useCard") {
        return evt?.name == "hefeigaigong";
      }
      return evt?.name == "hefeiduanjin" && evt.player == player2 && event.cards?.someInD("od");
    },
    forced: true,
    async content(event, trigger, player2) {
      if (trigger.name == "useCard") {
        trigger.directHit.addArray(game.players);
        return;
      }
      const cards2 = trigger.cards?.filterInD("od");
      if (cards2?.length) {
        await player2.gain(cards2, "gain2");
      }
    },
    ai: {
      combo: ["hefeiduanjin", "hefeigaigong"]
    }
  },
  hefeixianjian: {
    audio: 2,
    trigger: { player: "useCardToPlayered" },
    filter(event, player2) {
      return event.card.name == "sha" && event.targets?.length == 1 && !get.is.convertedCard(event.card) && !get.is.virtualCard(event.card);
    },
    async cost(event, trigger, player2) {
      const target = trigger.target;
      const result = await player2.chooseButton([
        get.prompt(event.skill, target),
        [
          [
            ["draw", `摸一张牌，然后其需弃置${get.cnNumber(Math.max(1, target.countCards("ej")))}张牌`],
            ["equip", `此杀结算结束后将对应实体牌置入其一个空置装备栏中，称为“陷坚”牌`]
          ],
          "textbutton"
        ]
      ]).set("filterButton", (button) => {
        if (button.link == "draw") {
          return true;
        }
        const trigger2 = get.event().getTrigger();
        if (!trigger2.cards?.length) {
          return false;
        }
        for (let i = 1; i < 6; i++) {
          if (trigger2.target.hasEmptySlot(i)) {
            return true;
          }
        }
        return false;
      }).set("ai", (button) => {
        const trigger2 = get.event().getTrigger();
        if (get.attitude(get.player(), trigger2.target) > 0) {
          return 0;
        }
        if (button.link == "draw") {
          return 2;
        }
        if (trigger2.target.countCards("e", (card2) => card2.name == "hefei_xianjian")) {
          return 1;
        }
        return 3;
      }).forResult();
      if (result?.bool && result.links?.length) {
        event.result = {
          bool: true,
          targets: [target],
          cost_data: result.links[0]
        };
      }
    },
    async content(event, trigger, player2) {
      const {
        targets: [target],
        cost_data: link
      } = event;
      const card2 = trigger.getParent().card;
      card2.storage ??= {};
      card2.storage[event.name] ??= link;
      game.broadcastAll(
        (card3, storage) => {
          card3.storage = storage;
        },
        card2,
        card2.storage
      );
      if (link == "draw") {
        await player2.draw();
        const num = Math.min(target.countDiscardableCards(target, "he"), Math.max(1, target.countCards("ej")));
        if (num > 0) {
          await target.chooseToDiscard(num, "he", true, "allowChooseAll");
        }
      } else {
        player2.when({
          global: "useCardAfter"
        }).filter((evt) => evt == trigger.getParent("useCard", true, true)).step(async (event2, trigger2, player3) => {
          const cards2 = trigger2.cards.filterInD("od");
          if (!cards2?.length) {
            return;
          }
          const list = [];
          for (let i = 1; i < 6; i++) {
            const slot = `equip${i}`;
            if (target.hasEmptySlot(slot)) {
              list.add(slot);
            }
          }
          if (!list.length) {
            return;
          }
          const result = await player3.chooseControl(list).set("prompt", `陷坚：将${get.translation(cards2)}置入${get.translation(target)}的一个空置装备栏中`).set("ai", () => {
            return get.event().resultx;
          }).set("resultx", list.randomGet()).forResult();
          if (result?.control) {
            const card3 = get.autoViewAs({ name: `hefei_xianjian` }, cards2);
            card3.subtypes = [result.control];
            target.$gain2(cards2, false);
            await target.equip(card3);
          }
        });
      }
    }
  },
  hefeizherui: {
    derivation: "hefei_xianjian",
    audio: 4,
    trigger: { global: "useCardToPlayered" },
    filter(event, player2) {
      if (event.card.name != "sha" || get.is.convertedCard(event.card) || get.is.virtualCard(event.card)) {
        return false;
      }
      return event.player.hasCards("e", (card2) => card2.name == "hefei_xianjian");
    },
    logTarget: "player",
    prompt2(event, player2) {
      return `对其发动一次其选择选项的${get.poptip("hefeixianjian")}`;
    },
    check(event, player2) {
      return get.attitude(player2, event.player) < 0;
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const result = await target.chooseButton(
        [
          `${get.translation(player2)}对你发动了【陷坚】，选择一项：`,
          [
            [
              ["draw", `其摸一张牌，然后你需弃置${get.cnNumber(Math.max(1, target.countCards("ej")))}张牌`],
              ["equip", `此杀结算结束后其将对应实体牌置入你一个空置装备栏中，称为“陷坚”牌`]
            ],
            "textbutton"
          ]
        ],
        true
      ).set("filterButton", (button) => {
        if (button.link == "draw") {
          return true;
        }
        const trigger2 = get.event().getTrigger();
        if (!trigger2.cards?.length) {
          return false;
        }
        for (let i = 1; i < 6; i++) {
          if (trigger2.player.hasEmptySlot(i)) {
            return true;
          }
        }
        return false;
      }).set("ai", (button) => {
        if (button.link == "draw") {
          return 2;
        }
        return 1.3 + Math.random();
      }).forResult();
      if (!result?.bool || !result.links?.length) {
        return;
      }
      const skill = "hefeixianjian";
      player2.logSkill(skill, target);
      const next = game.createEvent(skill);
      next.player = player2;
      next._trigger = trigger;
      next.targets = [target];
      next.cost_data = result.links[0];
      next.triggername = event.triggername;
      next.setContent(get.info(skill).content);
      await next;
    },
    group: "hefeizherui_damage",
    subSkill: {
      damage: {
        audio: "hefeizherui",
        trigger: { global: ["loseAfter", "loseAsyncAfter", "equipAfter", "addJudgeAfter", "addToExpansionAfter", "gainAfter"] },
        getIndex(event, player2) {
          let list = [];
          game.countPlayer((current) => {
            const evt = event.getl(current);
            evt.es.forEach((card2) => {
              const VEquip = evt.vcard_map.get(card2);
              if (VEquip?.name === "hefei_xianjian") {
                list.add([current, VEquip]);
              }
            });
            return false;
          });
          return list;
        },
        filter(event, player2, name, list) {
          const [target, card2] = list;
          if (!card2 || card2.name != "hefei_xianjian") {
            return false;
          }
          return true;
        },
        forced: true,
        locked: false,
        logTarget(_1, _2, _3, list) {
          return list[0];
        },
        async content(event, trigger, player2) {
          const target = event.indexedData[0];
          await target.damage(player2);
        }
      }
    },
    ai: { combo: "hefeixianjian" }
  },
  hefeiheyuyuejin: {
    audio: 2,
    trigger: {
      source: "damageSource",
      global: ["loseAfter", "loseAsyncAfter", "equipAfter", "addJudgeAfter", "addToExpansionAfter", "gainAfter"]
    },
    filter(event, player2) {
      const name = event.name == "damage" ? "hefei_zhangliao" : "hefei_lidian";
      if (!get.info("friendgongli").isFriendOf(player2, name)) {
        return false;
      }
      if (event.name == "damage") {
        return event.card?.storage?.hefeixianjian;
      }
      const evts = game.getGlobalHistory("everything", (evt) => {
        if (!["lose", "gain", "loseAsync", "equip", "addJudge", "addToExpansion"].includes(evt.name)) {
          return false;
        }
        return true;
      });
      for (const evt of evts) {
        for (const current of game.filterPlayer(() => true)) {
          const evtx = evt.getl(current);
          if (evtx?.vcard_map?.size && Array.from(evtx.vcard_map.values()).some((card2) => card2.name == "hefei_xianjian")) {
            return evt == event;
          }
        }
      }
      return false;
    },
    async cost(event, trigger, player2) {
      if (trigger.name !== "damage") {
        event.result = {
          bool: true
        };
        return;
      }
      const link = trigger.card?.storage?.hefeixianjian, target = trigger.player;
      const prompt = link !== "draw" ? `你摸一张牌，其弃置${get.cnNumber(Math.max(1, target.countCards("ej")))}张牌` : `此杀结算后将对应实体牌置入其一个空置装备栏`;
      event.result = await player2.chooseBool(get.prompt(event.skill, target), prompt).set("choice", get.attitude(player2, target) <= 0).forResult();
      event.result.targets = [target];
    },
    async content(event, trigger, player2) {
      if (trigger.name != "damage") {
        await player2.draw();
        return;
      }
      const link = trigger.card?.storage?.hefeixianjian == "draw" ? "equip" : "draw";
      const next = game.createEvent("hefeixianjian_effect", false);
      next.player = player2;
      next._trigger = trigger;
      next.targets = [trigger.player];
      next.cost_data = link;
      next.triggername = event.triggername;
      next.setContent(get.info("hefeixianjian").content);
      await next;
    },
    ai: {
      combo: "hefeixianjian"
    }
  },
  //数刘徽
  mbgeyuan: {
    audio: 2,
    init(player2, skill) {
      let index = 0;
      player2.setStorage(skill, index, true);
      player2.addTip(skill, `${get.translation(skill)} ${get.info(skill).getNumList(index, true)}`);
    },
    onremove(player2, skill) {
      player2.setStorage(skill, void 0, true);
      player2.removeTip(skill);
    },
    mark: true,
    marktext: "◯",
    //⚪
    intro: {
      markcount(storage) {
        storage = storage || 0;
        return Number(get.info("mbgeyuan").PI[storage]);
      },
      mark(dialog, storage, player2, evt, skill) {
        let str = get.info(skill).getNumList(storage);
        dialog.addText(str);
      }
    },
    mod: {
      aiOrder(player2, card2, order) {
        let number = get.number(card2, player2), index = player2.getStorage("mbgeyuan", 0);
        let num = Number(get.info("mbgeyuan").PI[index]);
        if (typeof number !== "number") {
          return;
        }
        if (num == number || player2.hasSkill("mbchongcha") && num == 0 && number > 9) {
          return order + 10;
        }
      },
      aiUseful(player2, card2, useful) {
        let number = get.number(card2, player2), index = player2.getStorage("mbgeyuan", 0);
        let num = Number(get.info("mbgeyuan").PI[index]);
        if (typeof number !== "number") {
          return;
        }
        if (num == number || player2.hasSkill("mbchongcha") && num == 0 && number > 9) {
          return useful + 10;
        }
      }
    },
    trigger: { player: "useCard" },
    filter(event, player2) {
      let index = player2.getStorage("mbgeyuan", 0);
      let num = Number(get.info("mbgeyuan").PI[index]), number = get.number(event.card);
      if (num == number) {
        return true;
      }
      if (typeof number == "number" && number > 9) {
        return player2.hasSkill("mbchongcha") && num == 0;
      }
      return false;
    },
    prompt2(event, player2) {
      let draw = Math.min(3, player2.getHistory("useSkill", (evt) => evt.skill === "mbgeyuan").length + 1);
      return `摸${get.cnNumber(draw)}张牌并调整“割圆”中X的值`;
    },
    frequent: "check",
    check(event, player2) {
      return get.effect(player2, { name: "draw" }, player2, player2) > 0;
    },
    async content(event, trigger, player2) {
      await player2.draw(Math.min(3, player2.getHistory("useSkill", (evt) => evt.skill === event.name).length));
      let index = player2.getStorage(event.name, 0);
      index++;
      const { PI, getNumList } = get.info(event.name);
      if (index >= PI.length) {
        index -= PI.length;
      }
      player2.setStorage(event.name, index, true);
      player2.addTip(event.name, `${get.translation(event.name)} ${getNumList(index, true)}`);
    },
    getNumList(index, isTip) {
      const { PI } = get.info("mbgeyuan");
      index = index || 0;
      function getNexts(index2) {
        let result = "", cnt = 0;
        while (cnt++ < 3) {
          let index22 = index2 + cnt - 1;
          if (index22 >= PI.length) {
            index22 - PI.length;
          }
          result += PI[index22];
        }
        return result;
      }
      let first = isTip ? PI[index] : `<span data-nature="fire">${PI[index]}</span>`, nextNums = getNexts(index + 1);
      return first + nextNums;
    },
    PI: "1415926535897932384626433832795028841971693993751058209749445923078164062862089986280348253421170679"
  },
  mbchongcha: {
    audio: 2,
    mod: {
      ignoredHandcard(card2, player2) {
        let number = get.number(card2, player2);
        if (typeof number == "number" && number > 9) {
          return true;
        }
      },
      cardDiscardable(card2, player2, name) {
        if (name == "phaseDiscard") {
          let number = get.number(card2, player2);
          if (typeof number == "number" && number > 9) {
            return false;
          }
        }
      }
    },
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      if (!player2.hasSkill("mbgeyuan", null, false, false)) {
        return false;
      }
      return player2.countDiscardableCards(player2, "he") > 0;
    },
    position: "he",
    filterCard: lib.filter.cardDiscardable,
    check(card2) {
      const player2 = get.player();
      let index = player2.getStorage("mbgeyuan", 0);
      const { getNumList } = get.info("mbgeyuan");
      let next = Number(getNumList(index, true).slice(1, 2));
      if (get.number(card2, player2) == next) {
        return 0;
      }
      return 10 - get.value(card2);
    },
    selectTarget: 0,
    prompt(event) {
      const player2 = get.player();
      let index = player2.getStorage("mbgeyuan", 0);
      const { PI, getNumList } = get.info("mbgeyuan");
      let num = PI[index], next = getNumList(index, true).slice(1, 2);
      return `弃一张牌并调整“割圆”中X的值（当前为${num}，调整后为${next}）`;
    },
    async content(event, trigger, player2) {
      let index = player2.getStorage("mbgeyuan", 0);
      index++;
      const { PI, getNumList } = get.info("mbgeyuan");
      if (index >= PI.length) {
        index -= PI.length;
      }
      player2.setStorage("mbgeyuan", index, true);
      player2.addTip("mbgeyuan", `${get.translation("mbgeyuan")} ${getNumList(index, true)}`);
    },
    ai: {
      order(item, player2) {
        player2 = player2 || get.player();
        let index = player2.getStorage("mbgeyuan", 0), hs = player2.getCards("hs", (card2) => player2.getUseValue(card2));
        if (!hs.length) {
          return 0;
        }
        const { getNumList } = get.info("mbgeyuan");
        const numList = getNumList(index, true);
        let first = Number(numList.slice(0, 1)), next = Number(numList.slice(1, 2));
        if (hs.some((card2) => get.number(card2, player2) == first)) {
          return 0;
        } else if (!hs.some((card2) => get.number(card2, player2) == next)) {
          return 0;
        }
        return 10;
      },
      result: {
        player: 1
      }
    }
  },
  //乐周瑜
  mbshouyue: {
    audio: 2,
    trigger: { player: ["phaseDrawBegin", "changeHpAfter"] },
    filter(event, player2) {
      return event.name != "changeHp" || event.changedHp < 0;
    },
    async cost(event, trigger, player2) {
      const next = player2.chooseButtonTarget({
        createDialog: [
          get.prompt(event.skill),
          [
            [
              ["draw", `摸一张牌，令一名角色获得${get.poptip("qinyin")}（已有则改为摸一张牌）`],
              ["reset", "令一名角色复原武将牌"]
            ],
            "textbutton"
          ]
        ],
        filterTarget: true,
        ai1(button) {
          const player3 = get.player();
          if (button.link == "draw") {
            return 2;
          }
          if (game.hasPlayer((current) => {
            return get.attitude(player3, current) > 0 && current.isTurnedOver();
          })) {
            return 3;
          }
          return 1;
        },
        ai2(target) {
          const player3 = get.player();
          if (ui.selected.buttons[0]?.link == "draw") {
            let eff = get.effect(target, { name: "draw" }, player3, player3);
            if (!target.hasSkill("qinyin", null, null, false)) {
              eff *= 1.3;
            }
            return eff;
          }
          if (get.attitude(player3, target) <= 0) {
            return 0;
          }
          if (target.isTurnedOver()) {
            return 2;
          }
          if (target.isLinked()) {
            return 1.5;
          }
          return 1;
        }
      });
      next.set(
        "targetprompt2",
        next.targetprompt2.concat([
          (target) => {
            if (!target.isIn() || ui.selected.buttons[0]?.link !== "draw") {
              return false;
            }
            if (target.hasSkill("qinyin", null, null, false)) {
              return "摸一张牌";
            }
            return "获得【琴音】";
          }
        ])
      );
      const result = await next.forResult();
      if (result?.bool && result.links?.length) {
        event.result = {
          bool: true,
          targets: result.targets,
          cost_data: result.links[0]
        };
      }
    },
    async content(event, trigger, player2) {
      const {
        targets: [target],
        cost_data: link
      } = event;
      if (link == "draw") {
        await player2.draw();
        if (target.hasSkill("qinyin", null, null, false)) {
          await target.draw();
        } else {
          await target.addSkills("qinyin");
        }
      } else {
        await target.turnOver(false);
        await target.link(false);
      }
    },
    derivation: "qinyin"
  },
  mbdieyin: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return !player2.isTurnedOver();
    },
    chooseButton: {
      dialog(event, player2) {
        const list = lib.phaseName.map((name) => ["", "", `lusu_${name}`]);
        return ui.create.dialog("叠音", [list, "vcard"], "hidden");
      },
      check(button) {
        if (button.link[2].slice(5) == "phaseDraw") {
          return 2;
        }
        return Math.random();
      },
      prompt(links, player2) {
        return `翻面并于本阶段结束后执行一个${get.translation(links[0][2])}`;
      },
      backup(links, player2) {
        return {
          audio: "mbdieyin",
          phase: links[0][2].slice(5),
          async content(event, trigger, player3) {
            const { phase } = get.info(event.name);
            await player3.turnOver(true);
            const evt = event.getParent("phase", true, true);
            if (evt?.name == "phase") {
              evt.phaseList.splice(evt.num + 1, 0, `${phase}|mbdieyin`);
            }
          }
        };
      }
    },
    ai: {
      order: 1,
      result: {
        player(player2) {
          if (player2.hasSkill("mbshouyue")) {
            return 1;
          }
          return 0;
        }
      }
    },
    subSkill: {
      backup: {}
    }
  },
  //御曹植
  mbchongsi: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player2) {
      if (player2.hasSkill("mbchongsi_damage")) {
        return false;
      }
      return game.hasPlayer((current) => current != player2);
    },
    chooseButton: {
      dialog(event, player2) {
        return ui.create.dialog(
          "冲司",
          [
            [
              ["sha", "使用一张【杀】"],
              ["discard", "弃置两张手牌"],
              ["damage", "对自己或装备【六龙骖驾】的角色造成1点伤害"]
            ],
            "textbutton"
          ],
          "hidden"
        );
      },
      filter(button, player2) {
        switch (button.link) {
          case "sha": {
            return player2.hasUsableCard("sha", "use");
          }
          case "discard": {
            return player2.countDiscardableCards(player2, "h") > 1;
          }
          default: {
            return true;
          }
        }
      },
      check(button) {
        const player2 = get.player();
        switch (button.link) {
          case "sha": {
            const card2 = get.autoViewAs({ name: "sha" }, "unsure");
            return player2.getUseValue(card2);
          }
          case "discard": {
            return 2 * get.effect(player2, { name: "guohe_copy", position: "h" }, player2, player2);
          }
          default: {
            return 0;
          }
        }
      },
      prompt(links, player2) {
        return "执行你选择的项，并选择一名其他角色，令其也选择一项执行";
      },
      backup(links, player2) {
        return {
          audio: "mbchongsi",
          filterTarget: lib.filter.notMe,
          ai1: () => 1,
          ai2(target) {
            const player3 = get.player();
            const targets2 = game.filterPlayer((current) => {
              return current == target || Boolean(current.getEquip("cz_liulongcanjia"));
            }), getE = (current) => get.damageEffect(current, target, target);
            if (get.damageEffect(targets2.maxBy(getE), target, player3) > 0) {
              return Math.max(0.1, get.attitude(player3, target));
            }
            return -get.attitude(player3, target);
          },
          choice: links[0],
          async content(event, trigger, player3) {
            const {
              targets: [target]
            } = event;
            const { choice: link } = get.info(event.name);
            const func = async (current, link2) => {
              let mustDamage;
              if (link2 == "sha") {
                const result2 = await current.chooseToUse(function(card2, player4, event2) {
                  if (get.name(card2) != "sha") {
                    return false;
                  }
                  return lib.filter.filterCard.apply(this, arguments);
                }, "冲司：使用一张杀").forResult();
                if (!result2?.bool) {
                  const result22 = await current.chooseToDiscard("h", 2, "冲司：弃置两张手牌，否则你须对你或装备有【六龙骖驾】的角色造成1点伤害").set("ai", (card2) => {
                    if (get.event().eff) {
                      return 0;
                    }
                    return 6 - get.value(card2);
                  }).set(
                    "eff",
                    (() => {
                      const targets2 = game.filterPlayer((currentx) => {
                        return currentx == current || Boolean(currentx.getEquip("cz_liulongcanjia"));
                      }), getE = (currentx) => get.damageEffect(currentx, current, current);
                      return getE(targets2.maxBy(getE)) >= 0;
                    })()
                  ).forResult();
                  if (!result22?.bool) {
                    mustDamage = true;
                  }
                }
              }
              if (link2 == "discard") {
                const num = Math.min(2, current.countDiscardableCards(current, "h"));
                if (num > 0) {
                  await current.chooseToDiscard("h", true, num);
                }
              }
              if (link2 == "damage" || mustDamage) {
                current.addTempSkill("mbchongsi_damage", { global: "roundStart" });
                const targets2 = game.filterPlayer((currentx) => {
                  return currentx == current || Boolean(currentx.getEquip("cz_liulongcanjia"));
                });
                if (targets2.length) {
                  const result2 = targets2.length > 1 ? await current.chooseTarget(
                    "冲司：对你或装备有【六龙骖驾】的角色造成1点伤害",
                    (card2, player4, target2) => {
                      return get.event().targetx.includes(target2);
                    },
                    true
                  ).set("targetx", targets2).set("ai", (target2) => {
                    const player4 = get.player();
                    return get.damageEffect(target2, player4, player4);
                  }).forResult() : {
                    bool: true,
                    targets: targets2
                  };
                  if (result2?.bool && result2.targets?.length) {
                    current.line(result2.targets);
                    const target2 = result2.targets[0];
                    await target2.damage(current);
                  }
                }
              }
            };
            await func(player3, link);
            const result = await target.chooseButton(
              [
                "冲司：选择一项执行",
                [
                  [
                    ["sha", "使用一张【杀】"],
                    ["discard", "弃置两张手牌"],
                    ["damage", "对自己或装备【六龙骖驾】的角色造成1点伤害"]
                  ],
                  "textbutton"
                ]
              ],
              true
            ).set("filterButton", (button) => {
              const player4 = get.player();
              switch (button.link) {
                case "sha": {
                  return player4.hasUsableCard("sha", "use");
                }
                case "discard": {
                  return player4.countDiscardableCards(player4, "h") > 1;
                }
                default: {
                  return true;
                }
              }
            }).set("ai", (button) => {
              const player4 = get.player();
              switch (button.link) {
                case "sha": {
                  const card2 = get.autoViewAs({ name: "sha" }, "unsure");
                  return player4.getUseValue(card2);
                }
                case "discard": {
                  return 2 * get.effect(player4, { name: "guohe_copy", position: "h" }, player4, player4);
                }
                default: {
                  const targets2 = game.filterPlayer((current) => {
                    return current == player4 || Boolean(current.getEquip("cz_liulongcanjia"));
                  }), getE = (current) => get.damageEffect(current, player4, player4);
                  return getE(targets2.maxBy(getE));
                }
              }
            }).forResult();
            if (result?.bool && result.links?.length) {
              await func(target, result.links[0]);
            }
          }
        };
      }
    },
    ai: {
      order(item, player2) {
        return get.order({ name: "sha" }, player2) + 0.1;
      },
      result: {
        player: 1
      }
    },
    subSkill: {
      backup: {},
      damage: {
        charlotte: true
      }
    }
  },
  mbpeidong: {
    audio: 4,
    logAudio(event) {
      if (typeof event == "number") {
        return `mbpeidong${event}.mp3`;
      }
      return 4;
    },
    enable: "chooseToUse",
    filter(event, player2) {
      const filterCard = (name) => {
        const card2 = new lib.element.VCard({ name, isCard: true });
        return event.filterCard(card2, player2, event);
      }, filter = (card2) => card2.name == "cz_liulongcanjia";
      if (filterCard("sha") && player2.canMoveCard(false, true, player2, player2.getNext(), filter, false)) {
        return true;
      }
      if (filterCard("shan") && game.hasPlayer((current) => {
        return current != player2 && current.getGainableCards(player2, "ej", filter).length > 0;
      })) {
        return true;
      }
      if (filterCard("tao") && (() => {
        if (player2.countCards("h", filter)) {
          return true;
        }
        return get.cardPile2(filter);
      })()) {
        return true;
      }
      if (filterCard("jiu") && (() => {
        if ("addedLiulong" in _status) {
          return false;
        }
        return player2.canEquip("cz_liulongcanjia", true);
      })()) {
        return true;
      }
      return false;
    },
    chooseButton: {
      dialog(event, player2) {
        const list = [];
        const filterCard = (name) => {
          const card2 = new lib.element.VCard({ name, isCard: true });
          return event.filterCard(card2, player2, event);
        }, filter = (card2) => card2.name == "cz_liulongcanjia";
        if (filterCard("sha") && player2.canMoveCard(false, true, player2, player2.getNext(), filter, false)) {
          list.add(["basic", "", "sha"]);
        }
        if (filterCard("shan") && game.hasPlayer((current) => {
          return current != player2 && current.getGainableCards(player2, "ej", filter).length > 0;
        })) {
          list.add(["basic", "", "shan"]);
        }
        if (filterCard("tao") && (() => {
          if (player2.countCards("h", filter)) {
            return true;
          }
          return get.cardPile2(filter);
        })()) {
          list.add(["basic", "", "tao"]);
        }
        if (filterCard("jiu") && (() => {
          if ("addedLiulong" in _status) {
            return false;
          }
          return player2.canEquip("cz_liulongcanjia", true);
        })()) {
          list.add(["basic", "", "jiu"]);
        }
        const dialog = ui.create.dialog("辔东", [list, "vcard"], "hidden");
        dialog.direct = true;
        return dialog;
      },
      check(button) {
        const card2 = new lib.element.VCard({ name: button.link[2], isCard: true });
        return get.player().getUseValue(card2);
      },
      prompt(links, player2) {
        const name = links[0][2];
        switch (name) {
          case "sha": {
            return `将【六龙骖驾】从装备区移至下家，视为使用【杀】`;
          }
          case "shan": {
            return `从其他角色场上获得【六龙骖驾】，视为使用【闪】`;
          }
          case "tao": {
            return `将【六龙骖驾】从手牌或牌堆亮出，视为使用【桃】`;
          }
          default: {
            return `将【六龙骖驾】从游戏外置入宝物，视为使用【酒】`;
          }
        }
      },
      backup(links, player2) {
        const name = links[0][2];
        return get.copy(get.info(`mbpeidong_${name}`));
      }
    },
    hiddenCard(player2, name) {
      const filterCard = (card2) => card2 == name, filter = (card2) => card2.name == "cz_liulongcanjia";
      if (filterCard("sha") && player2.canMoveCard(false, true, player2, player2.getNext(), filter, false)) {
        return true;
      }
      if (filterCard("shan") && game.hasPlayer((current) => {
        return current != player2 && current.getGainableCards(player2, "ej", filter).length > 0;
      })) {
        return true;
      }
      if (filterCard("tao") && (() => {
        if (player2.countCards("h", filter)) {
          return true;
        }
        return get.cardPile2(filter);
      })()) {
        return true;
      }
      if (filterCard("jiu") && (() => {
        if ("addedLiulong" in _status) {
          return false;
        }
        return player2.canEquip("cz_liulongcanjia", true);
      })()) {
        return true;
      }
    },
    ai: {
      order: 6,
      result: {
        player(player2) {
          if (_status.event.dying) {
            return get.attitude(player2, _status.event.dying);
          }
          return 1;
        }
      },
      save: true,
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2, tag, arg) {
        const filter = (card2) => card2.name == "cz_liulongcanjia";
        switch (tag) {
          case "respondSha": {
            return player2.canMoveCard(false, true, player2, player2.getNext(), filter, false);
          }
          case "respondShan": {
            return game.hasPlayer((current) => {
              return current != player2 && current.getGainableCards(player2, "ej", filter).length > 0;
            });
          }
          default: {
            if (player2.countCards("h", filter) || get.cardPile2(filter)) {
              return true;
            }
            if ("addedLiulong" in _status) {
              return false;
            }
            return player2.canEquip("cz_liulongcanjia", true);
          }
        }
      }
    },
    subSkill: {
      backup: {},
      use: {
        async precontent(event, trigger, player2) {
          event.result._apply_args = { addSkillCount: false };
          player2.popup(event.result.card.name, "metal");
          await game.delayx();
        },
        filterCard: () => false,
        prompt: "请选择【杀】的目标",
        selectCard: -1,
        log: false
      },
      sha: {
        viewAs: {
          name: "sha",
          isCard: true
        },
        popname: true,
        filterCard: () => false,
        selectCard: -1,
        log: false,
        async precontent(event, trigger, player2) {
          player2.logSkill("mbpeidong", null, null, null, [1]);
          const target = player2.getNext();
          if (!target?.isIn()) {
            return;
          }
          const cards2 = player2.getCards("e", (card2) => card2.name == "cz_liulongcanjia" && target.canEquip(card2));
          if (cards2.length) {
            const result = cards2.length > 1 ? await player2.chooseButton(["将一张【六龙骖驾】移动至下家", cards2], true).set("ai", (button) => {
              return Math.random();
            }).forResult() : {
              bool: true,
              links: cards2
            };
            if (result?.bool && result.links?.length) {
              const next = game.createEvent("moveCard");
              next.player = player2;
              next.targets = [player2, target];
              next.card = result.links[0];
              next.setContent(async (event2, trigger2, playerx) => {
                const {
                  card: card2,
                  targets: [player3, target2]
                } = event2;
                game.log(player3, "的", card2, "被移动给了", target2);
                if (player3.getCards("e").includes(card2)) {
                  if (!card2.cards?.length) {
                    target2.removeVirtualEquip(card2);
                  } else {
                    player3.$give(card2.cards, target2, false);
                  }
                  await target2.equip(card2);
                }
                event2.result = {
                  bool: true,
                  card: card2,
                  position: "e",
                  targets: [player3, target2]
                };
                await game.delay();
              });
              await next;
            }
          }
        }
      },
      shan: {
        filterTarget(_1, player2, target) {
          if (target == player2) {
            return false;
          }
          if (!target.countCards("ej", (card3) => card3.name == "cz_liulongcanjia")) {
            return false;
          }
          let event = _status.event, evt = event;
          if (event._backup) {
            evt = event._backup;
          }
          const card2 = new lib.element.VCard({ name: "shan", isCard: true });
          return evt.filterCard(card2, player2, event);
        },
        log: false,
        async content(event, trigger, player2) {
          player2.logSkill("mbpeidong", null, null, null, [2]);
          const { target } = event;
          const cards2 = target.getGainableCards(player2, "ej", (card3) => card3.name == "cz_liulongcanjia");
          if (cards2.length) {
            if (cards2.length > 1) {
              await player2.gainPlayerCard("ej", true, target).set("filterButton", (button) => button.link.name == "cz_liulongcanjia");
            } else {
              await player2.gain(cards2, target, "giveAuto", "bySelf");
            }
          }
          const evt = event.getParent(2), card2 = { name: "shan", isCard: true };
          if (!evt) {
            return;
          }
          game.broadcastAll((card3) => {
            lib.skill.mbpeidong_use.viewAs = card3;
            lib.skill.mbpeidong_use.prompt = `选择${get.translation(card3)}的目标`;
          }, card2);
          evt.set("_backupevent", "mbpeidong_use");
          evt.backup("mbpeidong_use");
          evt.set("openskilldialog", `选择${get.translation(card2)}的目标`);
          evt.set("norestore", true);
          evt.set("custom", {
            add: {},
            replace: { window() {
            } }
          });
          evt.goto(0);
        },
        ai: {
          result: {
            player: 2,
            target: -1
          }
        }
      },
      tao: {
        filterCard(card2, player2, target) {
          return card2.name == "cz_liulongcanjia";
        },
        selectCard() {
          const player2 = get.player(), cards2 = player2.getCards("h", (card2) => card2.name == "cz_liulongcanjia");
          let event = _status.event, evt = event;
          if (event._backup) {
            evt = event._backup;
          }
          const tao = new lib.element.VCard({ name: "tao", isCard: true });
          if (evt.filterCard(tao, player2, event) && !cards2.length) {
            return -1;
          }
          return 1;
        },
        log: false,
        lose: false,
        discard: false,
        check(card2) {
          return 1;
        },
        async content(event, trigger, player2) {
          player2.logSkill("mbpeidong", null, null, null, [3]);
          const filter = (card3) => card3.name == "cz_liulongcanjia";
          if (event.cards?.length) {
            await player2.lose(event.cards, ui.ordering);
            player2.$throw(event.cards, 1e3);
            await player2.showCards(event.cards, get.translation(player2) + "发动了【辔东】", true);
          } else {
            const card3 = get.cardPile2(filter);
            if (card3) {
              await game.cardsGotoOrdering(card3);
              await player2.showCards([card3], get.translation(player2) + "发动了【辔东】", true);
            }
          }
          const evt = event.getParent(2), card2 = { name: "tao", isCard: true };
          if (!evt) {
            return;
          }
          game.broadcastAll((card3) => {
            lib.skill.mbpeidong_use.viewAs = card3;
            lib.skill.mbpeidong_use.prompt = `选择${get.translation(card3)}的目标`;
          }, card2);
          evt.set("_backupevent", "mbpeidong_use");
          evt.backup("mbpeidong_use");
          evt.set("openskilldialog", `选择${get.translation(card2)}的目标`);
          evt.set("norestore", true);
          evt.set("custom", {
            add: {},
            replace: { window() {
            } }
          });
          evt.goto(0);
        }
      },
      jiu: {
        viewAs: {
          name: "jiu",
          isCard: true
        },
        popname: true,
        filterCard: () => false,
        selectCard: -1,
        log: false,
        manualConfirm: true,
        async precontent(event, trigger, player2) {
          player2.logSkill("mbpeidong", null, null, null, [4]);
          game.broadcastAll(() => {
            _status.addedLiulong = true;
          });
          const card2 = game.createCard2("cz_liulongcanjia", "heart", 13);
          if (player2.canEquip(card2, true)) {
            player2.$gain2(card2, false);
            await player2.equip(card2);
          }
        }
      }
    }
  },
  cz_liulongcanjia_skill: {
    equipSkill: true,
    mod: {
      globalFrom(from, to, distance) {
        const num = game.countPlayer((current) => current.countCards("ej", (card2) => get.number(card2) == 13));
        return distance - num;
      },
      globalTo(from, to, distance) {
        const num = game.countPlayer((current) => current.countCards("ej", (card2) => get.number(card2) == 13));
        return distance + num;
      }
    }
  },
  //书张芝
  mbshiju: {
    audio: 3,
    trigger: {
      player: "useCardAfter"
    },
    filter(event, player2) {
      const history = game.getAllGlobalHistory("useCard"), index = history.indexOf(event);
      if (index <= 0) {
        return false;
      }
      const evt = history[index - 1];
      return get.type2(evt.card) == get.type2(event.card) || get.suit(evt.card) == get.suit(event.card);
    },
    forced: true,
    logAudio(event) {
      const history = game.getAllGlobalHistory("useCard"), index = history.indexOf(event);
      const evt = history[index - 1];
      const bool1 = get.type2(evt.card) == get.type2(event.card), bool2 = get.suit(evt.card) == get.suit(event.card), bool3 = get.name(evt.card) == get.name(event.card);
      return bool1 && bool2 && bool3 ? "mbshiju3.mp3" : 2;
    },
    async content(event, trigger, player2) {
      const history = game.getAllGlobalHistory("useCard"), index = history.indexOf(trigger);
      if (index <= 0) {
        return;
      }
      const evt = history[index - 1];
      const bool1 = get.type2(evt.card) == get.type2(trigger.card), bool2 = get.suit(evt.card) == get.suit(trigger.card), bool3 = get.name(evt.card) == get.name(trigger.card);
      if (bool1) {
        await player2.gain(get.cards(1, true), "gain2", false);
      }
      if (bool2) {
        await player2.gain(get.bottomCards(1, true), "gain2", false);
      }
      if (bool1 && bool2) {
        player2.popup("乘势", "fire");
        if (bool3) {
          if (!player2.hasSkill("mbkubai", null, null, false)) {
            await player2.addSkills("mbkubai");
          } else if (player2.countMark("mbkubai") < 2) {
            game.log(player2, "升级了", "#g【枯白】");
            player2.addMark("mbkubai", 1, false);
            get.info("mbkubai").init(player2, "mbkubai");
          }
        }
      }
    },
    init(player2, skill) {
      player2.addSkill(`${skill}_record`);
    },
    onremove(player2, skill) {
      player2.removeSkill(`${skill}_record`);
    },
    mod: {
      aiOrder(player2, card2, num) {
        if (typeof card2 == "object") {
          const evts = game.getAllGlobalHistory("useCard");
          if (evts.length) {
            let evt = evts[evts.length - 1];
            const bool1 = get.type2(evt.card) == get.type2(card2), bool2 = get.suit(evt.card) == get.suit(card2), bool3 = get.name(evt.card) == get.name(card2);
            if (bool1) {
              num += 10;
            }
            if (bool2) {
              num += 10;
            }
            if (bool1 && bool2 && bool3) {
              num += 30;
            }
          }
          return num;
        }
      }
    },
    derivation: ["mbkubai"],
    subSkill: {
      record: {
        charlotte: true,
        trigger: {
          global: "useCard1"
        },
        silent: true,
        async content(event, trigger, player2) {
          get.info(event.name).init(player2, event.name);
        },
        intro: {
          markcount() {
            const history = game.getAllGlobalHistory("useCard");
            if (history.length) {
              const evt = history.at(-1);
              if (evt) {
                return get.translation(get.suit(evt.card));
              }
            }
            return 0;
          },
          content() {
            const history = game.getAllGlobalHistory("useCard");
            if (history.length) {
              const evt = history.at(-1);
              if (evt) {
                return `
									上一张被使用的牌：${get.translation(evt.card.name)}<br>
									花色：${get.translation(get.suit(evt.card))}<br>
									类型：${get.translation(get.type2(evt.card))}
								`;
              }
            }
            return "无效果";
          }
        },
        init(player2, skill) {
          const history = game.getAllGlobalHistory("useCard");
          if (history.length) {
            const evt = history.at(-1);
            if (!evt) {
              return;
            }
            player2.addTip(skill, `势举 ${get.translation(evt.card.name)}${get.translation(get.suit(evt.card))}`);
            player2.markSkill(skill);
            game.broadcastAll(
              (evt2, player3) => {
                const mark = player3.marks.mbshiju_record;
                if (mark) {
                  mark.firstChild.innerHTML = get.translation(get.type2(evt2.card));
                }
              },
              evt,
              player2
            );
          }
        },
        onremove(player2, skill) {
          player2.removeTip(skill);
        }
      }
    }
  },
  mbkubai: {
    audio: 6,
    trigger: { player: "useCard" },
    filter(event, player2) {
      if (player2 !== _status.currentPhase) {
        return false;
      }
      const key = ["color", "suit", "number"][Math.min(2, player2.countMark("mbkubai"))], evts = player2.getHistory("useCard");
      for (let i = 0; i < evts.length; i++) {
        const evt = evts[i];
        if (evt == event) {
          break;
        }
        if (get[key](evt.card) == get[key](event.card)) {
          return false;
        }
      }
      return true;
    },
    forced: true,
    locked: false,
    logAudio(event, player2, name) {
      const num = Math.min(2, player2.countMark("mbkubai"));
      const index = num * 2 + 1;
      return [`mbkubai${index}.mp3`, `mbkubai${index + 1}.mp3`];
    },
    async content(event, trigger, player2) {
      await player2.draw();
    },
    init(player2, skill) {
      const level = Math.min(2, player2.countMark(skill)), key = ["color", "suit", "number"][level];
      let list = player2.getHistory("useCard").map((evt) => get[key](evt.card)).toUniqued();
      if (_status.currentPhase == player2) {
        if (key == "number") {
          list = list.filter((i) => typeof i == "number").sort((a, b) => a - b);
        }
        for (const target of game.filterPlayer((current) => current != player2).sortBySeat()) {
          const name = "mbkubai_guanjued";
          target.addTempSkill(name);
          target.storage[name] = list;
          target.markSkill(name);
        }
      }
    },
    derivation: ["mbkubai_suit", "mbkubai_number"],
    group: "mbkubai_guanjue",
    subSkill: {
      guanjue: {
        trigger: {
          player: ["useCard1", "phaseBeginStart"]
        },
        popup: false,
        forced: true,
        locked: false,
        filter(event, player2) {
          return player2 == _status.currentPhase;
        },
        async content(event, trigger, player2) {
          const name = "mbkubai";
          get.info(name).init(player2, name);
        }
      },
      guanjued: {
        charlotte: true,
        onremove: true,
        mark: true,
        marktext: "白",
        intro: {
          markcount: (storage) => storage?.length || 0,
          content(_1, player2) {
            const list = player2.getStorage("mbkubai_guanjued"), target = _status.currentPhase;
            if (target.hasSkill("mbkubai")) {
              if (!list.length) {
                return "不能使用牌";
              }
              const level = Math.min(2, target.countMark("mbkubai")), key = ["颜色", "花色", "点数"][level];
              return `仅能使用${key}为${get.translation(list)}的牌`;
            }
            return "无效果";
          }
        },
        mod: {
          cardEnabled(card2, player2) {
            const list = player2.getStorage("mbkubai_guanjued"), target = _status.currentPhase;
            if (target.hasSkill("mbkubai") && target != player2) {
              const level = Math.min(2, target.countMark("mbkubai")), key = get[["color", "suit", "number"][level]](card2);
              if (key != "unsure" && !list.includes(key)) {
                return false;
              }
            }
          },
          cardSavable(card2, player2) {
            const list = player2.getStorage("mbkubai_guanjued"), target = _status.currentPhase;
            if (target.hasSkill("mbkubai") && target != player2) {
              const level = Math.min(2, target.countMark("mbkubai")), key = get[["color", "suit", "number"][level]](card2);
              if (key != "unsure" && !list.includes(key)) {
                return false;
              }
            }
          }
        }
      },
      suit: { nopop: true },
      number: { nopop: true }
    }
  },
  //手杀崔令仪
  mbcaiqiu: {
    audio: 4,
    logAudio: () => 2,
    trigger: { global: "roundStart" },
    filter(event, player2) {
      return game.countPlayer2(() => true, true) > 0;
    },
    forced: true,
    locked: false,
    async content(event, trigger, player2) {
      const cards2 = get.cards(
        game.countPlayer2(() => true, true),
        true
      );
      const result = await player2.chooseButton(["裁裘：是否获得其中任意张牌？", cards2], [1, Infinity], "allowChooseAll").set("ai", (button) => {
        get.player();
        return 1;
      }).forResult();
      if (result?.bool && result.links?.length) {
        await player2.gain(result.links, "draw");
      }
    },
    group: "mbcaiqiu_effect",
    subSkill: {
      effect: {
        audio: "mbcaiqiu",
        logAudio: () => ["mbcaiqiu3.mp3", "mbcaiqiu4.mp3"],
        trigger: { global: "useCardAfter" },
        filter(event, player2) {
          return event.player != player2 && player2.hasRoundHistory("gain", (evt) => {
            if (evt.getParent().name != "mbcaiqiu") {
              return false;
            }
            return evt.cards?.length && evt.cards.some((card2) => card2.name == event.card.name);
          });
        },
        round: 1,
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          await player2.loseHp();
        }
      }
    }
  },
  mbxishang: {
    audio: 8,
    logAudio(event, player2) {
      if (!get.nameList(player2).includes("mb_cuilingyi")) {
        return 6;
      }
      const skin = player2.skin[player2.name2 === "mb_cuilingyi" ? "name2" : "name"];
      if (skin?.indexOf("guidian") !== -1) {
        return 2;
      }
      if (skin?.indexOf("dongjiao") !== -1) {
        return ["mbxishang3.mp3", "mbxishang4.mp3"];
      }
      if (skin?.indexOf("xiuge") !== -1) {
        return ["mbxishang5.mp3", "mbxishang6.mp3"];
      }
      return 6;
    },
    derivation: ["mbweizhuang", "mbweizhuang_guidianx", "mbweizhuang_dongjiaox", "mbweizhuang_xiugex"],
    trigger: {
      player: "enterGame",
      global: "phaseBefore"
    },
    filter(event, player2) {
      if (!get.nameList(player2).includes("mb_cuilingyi")) {
        return false;
      }
      return event.name != "phase" || game.phaseNumber == 0;
    },
    locked: true,
    async cost(event, trigger, player2) {
      const list = lib.characterSubstitute["mb_cuilingyi"];
      if (!list.length) {
        return;
      }
      const createButton = (item, type, position, noclick, node) => {
        const [name, info] = item, skill = `mbweizhuang_${name.slice(13, -1)}x`;
        let isTemp = false;
        if (!lib.character[name]) {
          isTemp = true;
          lib.character[name] = get.convertedCharacter(["female", "", 0, [], info || []]);
        }
        lib.translate[name] ??= lib.translate[skill];
        node = ui.create.buttonPresets.character(name, type, position, noclick);
        if (isTemp) {
          delete lib.character[name];
        }
        node._link = node.link = [null, null, name];
        node.skinSkill = skill;
        node._customintro = [(node2) => `形象：${lib.translate[node2.skinSkill]}`, (node2) => get.skillInfoTranslation(node2.skinSkill, null, false)];
        return node;
      };
      const result = await player2.chooseButton(["袭裳：选择你本局的形象", [list.slice(0, 3), createButton], [list.slice(3, 6), createButton], [list.slice(6), createButton]], true).forResult();
      if (result?.bool && result.links?.length) {
        player2.changeSkin(event.skill, result.links[0][2]);
        event.result = {
          bool: true
        };
      }
    },
    async content(event, trigger, player2) {
      await player2.addSkills("mbweizhuang");
    },
    mark: true,
    marktext: "裳",
    intro: {
      markcount(storage, player2) {
        return player2.countCards("h", (card2) => card2.hasGaintag("faceup_tag"));
      },
      mark(dialog, content, player2) {
        const cards2 = player2.getCards("h", (card2) => card2.hasGaintag("faceup_tag"));
        if (cards2.length) {
          dialog.addAuto(cards2);
        } else {
          return "无明置牌";
        }
      }
    },
    group: "mbxishang_show",
    subSkill: {
      show: {
        audio: "mbxishang",
        logAudio() {
          return ["mbxishang7.mp3", "mbxishang8.mp3"];
        },
        trigger: {
          player: "gainAfter",
          global: "loseAsyncAfter"
        },
        filter(event, player2) {
          if (event.getParent().name == "draw") {
            return false;
          }
          const cards2 = event.getg(player2);
          return cards2?.length && player2.getCards("h").containsSome(...cards2);
        },
        forced: true,
        async content(event, trigger, player2) {
          const gains = trigger.getg(player2);
          const cards2 = player2.getCards("h", (card2) => gains.includes(card2));
          if (!cards2.length) {
            return;
          }
          const next = game.createEvent("faceUpCard");
          next.player = player2;
          next.cards = cards2;
          next.skill = "mbxishang";
          next.setContent(async (event2, trigger2, player3) => {
            const { cards: cards3 } = event2;
            game.log(player3, "明置了", cards3);
            game.addCardKnower(
              cards3,
              game.filterPlayer(() => true)
            );
            game.broadcastAll((cards4) => {
              cards4.forEach((card2) => card2.addGaintag("faceup_tag"));
            }, cards3);
          });
          player2.markSkill("mbxishang");
        }
      }
    }
  },
  mbweizhuang: {
    // @ts-ignore audio的类型注释不够全
    audio: ["guidian", "dongjiao", "xiuge"].map((key) => `mbweizhuang_${key}`),
    // 手杀：一名角色装备区和判定区的牌都是明置牌，但是一名角色的明置牌不包括其判定区的牌
    getFaceupCards(player2, judge = false) {
      const cards2 = player2.getCards("h", (card2) => card2.hasGaintag("faceup_tag"));
      if (player2.countCards("e")) {
        cards2.addArray(player2.getCards("e"));
      }
      if (judge && player2.countCards("j")) {
        cards2.addArray(player2.getCards("j"));
      }
      return cards2;
    },
    derivation: ["mbweizhuang_guidianx", "mbweizhuang_dongjiaox", "mbweizhuang_xiugex"],
    group: ["mbweizhuang_guidian", "mbweizhuang_dongjiao", "mbweizhuang_xiuge"],
    subSkill: {
      guidianx: {
        audio: "mbweizhuang_guidian"
      },
      dongjiaox: {
        audio: "mbweizhuang_dongjiao"
      },
      xiugex: {
        audio: "mbweizhuang_xiuge"
      },
      guidian: {
        audio: 4,
        logAudio(event, player2) {
          if (event.name == "faceUpCard") {
            return ["mbweizhuang_guidian1.mp3", "mbweizhuang_guidian4.mp3"];
          }
          return ["mbweizhuang_guidian2.mp3", "mbweizhuang_guidian3.mp3"];
        },
        trigger: {
          global: ["faceUpCardAfter", "phaseJieshuBegin", "equipAfter", "addJudgeAfter"],
          player: "phaseDrawBegin2"
        },
        filter(event, player2) {
          if (!get.nameList(player2).includes("mb_cuilingyi")) {
            return false;
          }
          const skin = player2.skin[player2.name2 === "mb_cuilingyi" ? "name2" : "name"];
          if (!skin || skin.indexOf("guidian") === -1) {
            return false;
          }
          if (event.name == "phaseDraw") {
            return !event.numFixed && player2.getStorage("mbweizhuang_guidian", [0, 0, 0])[0] !== 0;
          }
          if (event.name == "phaseJieshu") {
            if (!get.info("mbweizhuang").getFaceupCards(event.player, true).length) {
              return false;
            }
            return true;
          }
          if (player2.countMark("mbweizhuang_used") > game.countPlayer2(() => true, true)) {
            return false;
          }
          let num = 0, evts = game.getAllGlobalHistory("everything", (evt) => ["faceUpCard", "equip", "addJudge"].includes(evt.name));
          for (let i = evts.indexOf(event); i >= 0; i--) {
            const evt = evts[i];
            if (evt?.mbweizhuang_count) {
              break;
            }
            if (evt.name == "faceUpCard" && evt.cards?.length) {
              num += evt.cards.length;
            } else if (["equip", "addJudge"].includes(evt.name)) {
              num++;
            }
          }
          return num > game.countPlayer2(() => true, true);
        },
        intro: {
          nocount: true,
          content(storage, player2, skill) {
            const list = player2.getStorage("mbweizhuang_guidian", [0, 0, 0]);
            const getStr = (num) => {
              if (num >= 0) {
                return `+${num}`;
              }
              return num;
            };
            return `摸牌阶段摸牌数${getStr(list[0])}<br>出杀次数${getStr(list[1])}<br>手牌上限${getStr(list[2])}`;
          }
        },
        onremove: true,
        locked: false,
        mod: {
          cardUsable(card2, player2, num) {
            if (card2.name == "sha") {
              const list = player2.getStorage("mbweizhuang_guidian", [0, 0, 0]);
              return num + list[1];
            }
          },
          maxHandcard(player2, num) {
            const list = player2.getStorage("mbweizhuang_guidian", [0, 0, 0]);
            return num + list[2];
          }
        },
        async cost(event, trigger, player2) {
          let record = player2.getStorage(event.skill, [0, 0, 0]);
          if (trigger.name == "phaseDraw") {
            trigger.num = Math.max(0, trigger.num + record[0]);
            return;
          }
          const list = [2 + record[0], player2.getCardUsable("sha", true), player2.getHandcardLimit(), player2.getHp()];
          const prompt = trigger.name == "phaseJieshu" ? `是否令一项数值-1并发动一次${get.poptip("mbcaiqiu")}？` : `令一项数值+1`;
          const choiceList = [
            ["draw", `摸牌阶段摸牌数(${list[0]})`],
            ["sha", `出杀次数(${list[1]})`],
            ["limit", `手牌上限(${list[2]})`],
            ["hp", `体力值(${list[3]})`]
          ];
          const next = player2.chooseButton([
            `褽装：${prompt}`,
            [choiceList.slice(0, 2), "tdnodes"],
            [choiceList.slice(2), "tdnodes"],
            [
              (dialog) => {
                dialog.buttons.forEach((i) => {
                  i.style.setProperty("width", "200px", "important");
                  i.style.setProperty("text-align", "left", "important");
                });
              },
              "handle"
            ]
          ]).set("numList", list);
          if (trigger.name == "phaseJieshu") {
            next.set("filterButton", (button) => {
              const { player: player3, numList } = get.event(), index = ["draw", "sha", "limit", "hp"].indexOf(button.link);
              return numList[index] > 0;
            });
            next.set("ai", (button) => {
              const { player: player3, numList } = get.event();
              if (numList[1] > 1 && button.link == "sha") {
                return 3;
              }
              if (numList[2] > 2 && button.link == "limit") {
                return 2;
              }
              if (button.link == "draw") {
                return 1;
              }
              return 0;
            });
          } else {
            next.set("filterButton", (button) => {
              const { player: player3, numList } = get.event();
              return button.link != "hp" || player3.isDamaged();
            });
            next.set("ai", (button) => {
              const { player: player3, numList } = get.event();
              if (button.link == "hp") {
                return 3;
              }
              if (numList[1] < 3 && button.link == "sha") {
                return 2;
              }
              return Math.random();
            });
            next.set("forced", true);
          }
          const result = await next.forResult();
          if (result?.bool && result.links?.length) {
            event.result = {
              bool: true,
              cost_data: result.links[0]
            };
          }
        },
        async content(event, trigger, player2) {
          const choice = event.cost_data, index = ["draw", "sha", "limit", "hp"].indexOf(choice), list = player2.getStorage(event.name, [0, 0, 0]);
          if (trigger.name != "phaseJieshu") {
            trigger.set("mbweizhuang_count", true);
            player2.addSkill("mbweizhuang_used");
            player2.addMark("mbweizhuang_used", 1, false);
          }
          if (index > 2) {
            if (trigger.name == "phaseJieshu") {
              await player2.loseHp();
            } else {
              await player2.recover();
            }
          } else {
            if (trigger.name == "phaseJieshu") {
              list[index]--;
            } else {
              list[index]++;
            }
            player2.setStorage(event.name, list, true);
          }
          if (trigger.name == "phaseJieshu") {
            await player2.useResult({ skill: "mbcaiqiu" }, event);
          }
        }
      },
      used: {
        charlotte: true,
        onremove: true
      },
      dongjiao: {
        audio: 6,
        trigger: {
          player: ["useCard", "useCardToPlayered", "useCardAfter"],
          source: "damageBegin2"
        },
        logAudio(event, player2, name) {
          if (name == "useCardAfter") {
            return ["mbweizhuang_dongjiao3.mp3", "mbweizhuang_dongjiao6.mp3"];
          }
          if (name == "useCardToPlayered") {
            return ["mbweizhuang_dongjiao4.mp3", "mbweizhuang_dongjiao5.mp3"];
          }
          return 2;
        },
        filter(event, player2, name) {
          if (!get.nameList(player2).includes("mb_cuilingyi")) {
            return false;
          }
          const skin = player2.skin[player2.name2 === "mb_cuilingyi" ? "name2" : "name"];
          if (!skin || skin.indexOf("dongjiao") === -1) {
            return false;
          }
          const num = get.info("mbweizhuang").getFaceupCards(player2)?.map((card2) => get.type2(card2))?.toUniqued()?.length;
          const type = get.type2(event.card), list = player2.getStorage("mbweizhuang_block");
          if (list.includes(type)) {
            return false;
          }
          if (name == "useCard" || event.name == "damage") {
            if (num < 1 || type !== "basic") {
              return false;
            }
            return event.card?.name != "sha" || event.name == "damage";
          }
          if (name == "useCardAfter") {
            return num >= 3 && type == "equip" && game.hasPlayer((current) => {
              return get.info("mbweizhuang").getFaceupCards(current).length;
            });
          }
          return event.isFirstTarget && num >= 2 && type == "trick" && event.targets?.length && event.targets.some((target) => {
            const pos = target == player2 ? "e" : "he";
            return target.countGainableCards(player2, pos);
          });
        },
        async cost(event, trigger, player2) {
          switch (event.triggername) {
            case "damageBegin2":
            case "useCard": {
              event.result = {
                bool: true
              };
              return;
            }
            case "useCardAfter": {
              const targets2 = game.filterPlayer((current) => {
                return get.info("mbweizhuang").getFaceupCards(current).length;
              });
              if (!targets2?.length) {
                return;
              }
              event.result = await player2.chooseTarget(`###是否发动【褽装】？###令一名有明置牌的角色摸两张牌`, (card2, player3, target) => {
                return get.event().targetx.includes(target);
              }).set("targetx", targets2).set("ai", (target) => {
                const player3 = get.player();
                return get.effect(target, { name: "draw" }, player3, player3);
              }).forResult();
              return;
            }
            default: {
              const targets2 = trigger.targets.filter((target) => {
                const pos = target == player2 ? "e" : "he";
                return target.countGainableCards(player2, pos);
              });
              if (!targets2?.length) {
                return;
              }
              event.result = await player2.chooseTarget(`###是否发动【褽装】？###获得一名目标角色一张牌`, (card2, player3, target) => {
                return get.event().targetx.includes(target);
              }).set("targetx", targets2).set("ai", (target) => {
                const player3 = get.player();
                return get.effect(target, { name: "shunshou_copy2" }, player3, player3);
              }).forResult();
              return;
            }
          }
        },
        async content(event, trigger, player2) {
          const { targets: targets2, triggername: name } = event;
          player2.addTempSkill("mbweizhuang_block");
          player2.markAuto("mbweizhuang_block", get.type2(trigger.card));
          switch (name) {
            case "damageBegin2": {
              trigger.num++;
              break;
            }
            case "useCard": {
              trigger.baseDamage ??= 1;
              trigger.baseDamage++;
              break;
            }
            case "useCardAfter": {
              await game.doAsyncInOrder(targets2, async (target) => await target.draw(2));
              break;
            }
            default: {
              await game.doAsyncInOrder(targets2, async (target) => {
                const pos = target == player2 ? "e" : "he";
                if (target.countGainableCards(player2, pos)) {
                  await player2.gainPlayerCard(target, pos, true);
                }
              });
            }
          }
        }
      },
      block: {
        charlotte: true,
        onremove: true
      },
      xiuge: {
        audio: 6,
        logAudio(event) {
          if (typeof event == "number") {
            return `mbweizhuang_xiuge${event}.mp3`;
          }
          return 6;
        },
        enable: "chooseToUse",
        hiddenCard(player2, name) {
          if (!get.nameList(player2).includes("mb_cuilingyi")) {
            return false;
          }
          const skin = player2.skin[player2.name2 === "mb_cuilingyi" ? "name2" : "name"];
          if (!skin || skin.indexOf("xiuge") === -1) {
            return false;
          }
          const list = ["sha", "shan", "tao", "jiu"];
          if (!list.includes(name) || player2.getStorage("mbweizhuang_blocker").includes(name)) {
            return false;
          }
          const subtype = `equip${list.indexOf(name) + 1}`, count = get.info("mbweizhuang").getFaceupCards(player2)?.map((card2) => get.suit(card2))?.toUniqued()?.length;
          return player2.countCards("he", (card2) => {
            if (get.subtype(card2) != subtype) {
              return false;
            }
            return count >= 4 || lib.filter.cardDiscardable(card2, player2, "mbweizhuang");
          }) > 0;
        },
        filter(event, player2) {
          if (!get.nameList(player2).includes("mb_cuilingyi")) {
            return false;
          }
          const skin = player2.skin[player2.name2 === "mb_cuilingyi" ? "name2" : "name"];
          if (!skin || skin.indexOf("xiuge") === -1) {
            return false;
          }
          const list = ["sha", "shan", "tao", "jiu"], count = get.info("mbweizhuang").getFaceupCards(player2)?.map((card2) => get.suit(card2))?.toUniqued()?.length;
          return list.some((name) => {
            if (player2.getStorage("mbweizhuang_blocker").includes(name)) {
              return false;
            }
            const vcard = new lib.element.VCard({ name, isCard: true, storage: { wzxiuge: true } });
            if (!event.filterCard(vcard, player2, event)) {
              return false;
            }
            const subtype = `equip${list.indexOf(name) + 1}`;
            return player2.countCards("he", (card2) => {
              if (get.subtype(card2) != subtype) {
                return false;
              }
              return count >= 4 || lib.filter.cardDiscardable(card2, player2, "mbweizhuang");
            }) > 0;
          });
        },
        viewAs(cards2, player2) {
          if (cards2.length) {
            let name;
            const subtype = get.subtype(cards2[0], player2);
            if (typeof subtype == "string") {
              name = ["sha", "shan", "tao", "jiu"][subtype.slice(5) - 1];
            }
            if (name) {
              return {
                name,
                isCard: true,
                suit: "none",
                number: null,
                storage: {
                  wzxiuge: true
                }
              };
            }
          }
          return null;
        },
        filterCard(card2, player2, event) {
          event ??= _status.event;
          const filter = event._backup.filterCard;
          const list = ["sha", "shan", "tao", "jiu"], count = get.info("mbweizhuang").getFaceupCards(player2)?.map((card3) => get.suit(card3))?.toUniqued()?.length;
          if (count < 4 && !lib.filter.cardDiscardable(card2, player2, "mbweizhuang")) {
            return false;
          }
          for (const name of list) {
            if (player2.getStorage("mbweizhuang_blocker").includes(name)) {
              continue;
            }
            const vcard = new lib.element.VCard({ name, isCard: true, storage: { wzxiuge: true } });
            if (!filter(vcard, player2, event)) {
              continue;
            }
            const subtype = `equip${list.indexOf(name) + 1}`;
            if (subtype == get.subtype(card2, player2)) {
              return true;
            }
          }
          return false;
        },
        popname: true,
        ignoreMod: true,
        position: "he",
        log: false,
        check(card2) {
          const player2 = get.player();
          if (_status.event.type === "phase") {
            const name = ["sha", "shan", "tao", "jiu"][get.subtype(card2, player2)?.slice(5) - 1];
            if (name) {
              const vcard = get.autoViewAs({ name, isCard: true, storage: { wzxiuge: true } });
              if (player2.getUseValue(vcard) > 0) {
                return 14 - get.value(card2);
              }
            }
            return 0;
          }
          return 1;
        },
        prompt(event, player2) {
          return get.skillInfoTranslation("mbweizhuang_xiugex", player2);
        },
        async precontent(event, trigger, player2) {
          const name = event.result.card?.name;
          let nature = event.result.card?.nature;
          const cards2 = event.result.cards;
          const parent = event.getParent();
          if (parent == null) {
            return;
          }
          if (name === "sha" && get.subtype(cards2[0], player2) === "equip1") {
            const list = [null].concat(lib.inpile_nature).filter((nature2) => {
              const vcard2 = { name: "sha", isCard: true, storage: { wzxiuge: true } };
              if (nature2) {
                vcard2.nature = nature2;
              }
              return parent.filterCard(get.autoViewAs(vcard2), player2, parent);
            });
            if (list.length > 1) {
              const result = await player2.chooseControl({
                prompt: "绣阁：请选择视为使用的【杀】",
                controls: list.map((nature2) => nature2 || "普通杀")
              }).forResult();
              nature = result.control === "普通杀" ? null : result.control;
            } else {
              nature = list[0];
            }
          }
          player2.addTempSkill("mbweizhuang_blocker");
          player2.markAuto("mbweizhuang_blocker", name);
          parent.addCount = false;
          const count = get.info("mbweizhuang").getFaceupCards(player2)?.map((card2) => get.suit(card2))?.toUniqued()?.length;
          if (count >= 4) {
            player2.logSkill("mbweizhuang_xiuge", null, null, null, [get.rand(1, 2)]);
            await player2.showCards(cards2, `${get.translation(player2)}发动了【褽装】`);
          } else {
            const index = ["sha", "jiu", "tao", "shan"].indexOf(name) + 3;
            player2.logSkill("mbweizhuang_xiuge", null, null, null, [index]);
            await player2.modedDiscard({ cards: cards2 });
          }
          const vcard = { name, isCard: true, storage: { wzxiuge: true } };
          if (nature) {
            vcard.nature = nature;
          }
          event.result.card = get.autoViewAs(vcard);
          event.result.cards = [];
          player2.when("useCardAfter").filter((evt) => evt.getParent() === event.getParent()).step(async (event2, trigger2, player3) => {
            const card2 = get.cardPile((card3) => get.suit(card3) == get.suit(cards2[0]));
            if (card2) {
              await player3.gain({
                cards: [card2],
                animate: "gain2"
              });
            }
          });
        },
        locked: false,
        mod: {
          cardUsable(card2, player2) {
            if (card2?.storage?.wzxiuge) {
              return Infinity;
            }
          }
        },
        ai: {
          order: 3,
          result: {
            player(player2) {
              if (_status.event.dying) {
                return get.attitude(player2, _status.event.dying);
              }
              return 1;
            }
          }
        }
      },
      blocker: {
        charlotte: true,
        onremove: true
      }
    }
  },
  //君子六艺
  //礼·卢毓
  mbbingfa: {
    audio: 4,
    logAudio(event) {
      if (typeof event == "number") {
        return `mbbingfa${event}.mp3`;
      }
      return 2;
    },
    trigger: {
      global: "roundStart"
    },
    filter(event, player2) {
      return game.hasPlayer((current) => !current.isTurnedOver());
    },
    async cost(event, trigger, player2) {
      const falvs = lib.poptip.getInfo("bingfa_lvfa").slice(4).split("<br><li>");
      const result = await player2.chooseButton([`###${get.prompt(event.skill)}###选择两条律法发布`, [falvs.map((value, index) => [index, value]), "textbutton"]], 2).set("ai", (button) => {
        return Math.random();
      }).forResult();
      if (result?.bool && result.links?.length) {
        event.result = {
          bool: true,
          cost_data: result.links.map((index) => [index, falvs[index]])
        };
      }
    },
    async content(event, trigger, player2) {
      const { name, cost_data: lvfas } = event, targets2 = game.filterPlayer((current) => !current.isTurnedOver()).sortBySeat();
      const map = await game.chooseAnyOL(targets2, get.info(name).chooseLvfa, [targets2, player2, lvfas]).forResult();
      const tickets = [0, 0, 0, 0];
      for (const target of targets2) {
        if (target != player2 && target.hasSkill("mbbingfa_control")) {
          target.removeSkill("mbbingfa_control");
        }
        const result = map.get(target);
        if (result?.bool && result.links?.length) {
          lvfas.forEach((lvfa) => {
            const count = result.links.filter((count2) => count2 === lvfa[0]).length;
            if (count > 0) {
              game.log(target, "为", `#r${lvfa[1]}`, "投出了", `#y${count}`, "票");
              tickets[lvfa[0]] += count;
            }
          });
        }
      }
      const maxTicket = tickets.maxBy();
      const enact = async (lvfa) => {
        const [index, prompt] = lvfa;
        if (tickets[index] !== maxTicket) {
          return;
        }
        game.log(`#r${prompt}`, "得票最多");
        player2.addTip(`${name}_${index}`, `秉法 ${prompt.slice(0, 4)}`);
        player2.when({
          global: "roundEnd"
        }).step(get.info(name).effect[index]);
      };
      await game.doAsyncInOrder(lvfas, enact, () => 1);
    },
    effect: [
      async (event, trigger, player2) => {
        player2.removeTip("mbbingfa_0");
        const players = game.filterPlayer(() => true);
        let max = 0, targets2 = [];
        for (const target of players) {
          const num = target.getRoundHistory("sourceDamage", (evt) => evt.num).reduce((sum, evt) => {
            return sum + evt.num;
          }, 0);
          if (num > max) {
            targets2 = [target];
            max = num;
          }
          if (num === max) {
            targets2.add(target);
          }
        }
        const func = async (target) => {
          await target.damage(2, "nosource");
        };
        if (targets2.length) {
          player2.logSkill("mbbingfa", targets2, null, null, [get.rand(3, 4)]);
          await game.doAsyncInOrder(targets2, func);
        }
      },
      async (event, trigger, player2) => {
        player2.removeTip("mbbingfa_1");
        const players = game.filterPlayer(() => true);
        let max = 0, targets2 = [];
        for (const target of players) {
          const num = target.getRoundHistory("gain", (evt) => evt.cards?.length).reduce((sum, evt) => {
            return sum + evt.cards?.length;
          }, 0);
          if (num > max) {
            targets2 = [target];
            max = num;
          }
          if (num === max) {
            targets2.add(target);
          }
        }
        const func = async (target) => {
          target.addSkill("mbbingfa_limit");
          target.addMark("mbbingfa_limit", 2, false);
        };
        if (targets2.length) {
          player2.logSkill("mbbingfa", targets2, null, null, [get.rand(3, 4)]);
          await game.doAsyncInOrder(targets2, func);
        }
      },
      async (event, trigger, player2) => {
        player2.removeTip("mbbingfa_2");
        const players = game.filterPlayer(() => true);
        let max = 0, targets2 = [];
        for (const target of players) {
          const num = game.getRoundHistory("changeHp", (evt) => evt.player == target && evt.num !== 0).length;
          if (num > max) {
            targets2 = [target];
            max = num;
          }
          if (num === max) {
            targets2.add(target);
          }
        }
        const func = async (target) => {
          await target.loseMaxHp();
        };
        if (targets2.length) {
          player2.logSkill("mbbingfa", targets2, null, null, [get.rand(3, 4)]);
          await game.doAsyncInOrder(targets2, func);
        }
      },
      async (event, trigger, player2) => {
        player2.removeTip("mbbingfa_3");
        const players = game.filterPlayer(() => true);
        let max = 0, targets2 = [];
        for (const target of players) {
          const num = target.countCards("hej");
          if (num > max) {
            targets2 = [target];
            max = num;
          }
          if (num === max) {
            targets2.add(target);
          }
        }
        const func = async (target) => {
          const targetx = game.filterPlayer((current) => current != target);
          if (targetx?.length) {
            const func2 = async (current) => {
              if (target.countGainableCards(current, "he")) {
                await target.chooseToGive(current, "he", true);
              }
            };
            await game.doAsyncInOrder(targetx, func2);
          }
        };
        if (targets2.length) {
          player2.logSkill("mbbingfa", targets2, null, null, [get.rand(3, 4)]);
          await game.doAsyncInOrder(targets2, func);
        }
      }
    ],
    chooseLvfa(player2, targets2, source, lvfas, eventId) {
      const num = game.countPlayer((current) => {
        if (!targets2.includes(current)) {
          return false;
        }
        if (current == player2) {
          if (current.getStorage("mbbingfa_control").length) {
            return false;
          }
          return true;
        }
        return current.getStorage("mbbingfa_control").includes(player2);
      });
      if (num > 1) {
        const event = get.event();
        const controls = [
          (link) => {
            const evt = get.event();
            if (evt.dialog && evt.dialog.buttons) {
              for (let i = 0; i < evt.dialog.buttons.length; i++) {
                const button = evt.dialog.buttons[i];
                button.classList.remove("selectable");
                button.classList.remove("selected");
                const counterNode = button.querySelector(".caption");
                if (counterNode) {
                  counterNode.childNodes[0].innerHTML = ``;
                }
              }
              ui.selected.buttons.length = 0;
              game.check();
            }
            return;
          }
        ];
        event.controls = [ui.create.control(controls.concat(["清除选择", "stayleft"]))];
      }
      const prompt = num > 0 ? `秉法：为本轮要执行的律法投票（你持有票数：${num}）` : "秉法：你没有投票权";
      const next = player2.chooseButton([prompt, [lvfas, "textbutton"]]).set("ai", (button) => {
        return Math.random();
      }).set("maxNum", num).set("filterButton", (button) => {
        const { maxNum } = get.event();
        const selected = ui.selected.buttons.slice();
        if (selected.length >= maxNum) {
          return false;
        }
        return true;
      }).set("custom", {
        add: {
          confirm(bool) {
            if (bool != true) {
              return;
            }
            const event = get.event().parent;
            if (event.controls) {
              event.controls.forEach((i) => i.close());
            }
            if (ui.confirm) {
              ui.confirm.close();
            }
            game.uncheck();
          },
          button() {
            if (ui.selected.buttons.length) {
              return;
            }
            const event = get.event();
            if (event.dialog && event.dialog.buttons) {
              for (let i = 0; i < event.dialog.buttons.length; i++) {
                const button = event.dialog.buttons[i];
                const counterNode = button.querySelector(".caption");
                if (counterNode) {
                  counterNode.childNodes[0].innerHTML = ``;
                }
              }
            }
            if (!ui.selected.buttons.length) {
              const evt = event.parent;
              if (evt.controls) {
                evt.controls[0].classList.add("disabled");
              }
            }
          }
        },
        replace: {
          button(button) {
            const event = get.event();
            if (!event.isMine() || !event.filterButton(button)) {
              return;
            }
            if (button.classList.contains("selectable") == false) {
              return;
            }
            button.classList.add("selected");
            ui.selected.buttons.push(button);
            let counterNode = button.querySelector(".caption");
            const count = ui.selected.buttons.filter((i) => i == button).length;
            if (counterNode) {
              counterNode = counterNode.childNodes[0];
              counterNode.innerHTML = `×${count}`;
            } else {
              counterNode = ui.create.caption(`<span style="font-family:xinwei; text-shadow:#FFF 0 0 4px, #FFF 0 0 4px, rgba(74,29,1,1) 0 0 3px;">×${count}</span>`, button);
            }
            const evt = event.parent;
            if (evt.controls) {
              evt.controls[0].classList.remove("disabled");
            }
            game.check();
          }
        }
      }).set("id", eventId).set("_global_waiting", true);
      if (num > 0) {
        next.set("selectButton", [1, num]);
      }
      return next;
    },
    subSkill: {
      limit: {
        markimage: "image/card/handcard.png",
        intro: {
          markcount(storage) {
            if (typeof storage !== "number" || storage == 0) {
              return null;
            }
            return `-${storage}`;
          },
          content: "手牌上限-#"
        },
        onremove: true,
        charlotte: true,
        mod: {
          maxHandcard(player2, num) {
            return num - player2.countMark("mbbingfa_limit");
          }
        }
      },
      control: {
        charlotte: true,
        onremove: true,
        mark: true,
        intro: {
          content: "你的投票权已被交给$"
        }
      }
    }
  },
  mbshuxing: {
    audio: 2,
    trigger: {
      global: "useCardToTarget"
    },
    filter(event, player2) {
      if (player2 != _status.currentPhase) {
        return false;
      }
      if (event.target == player2 || event.card.name != "sha") {
        return false;
      }
      return !player2.getStorage("mbshuxing_used").includes(event.target);
    },
    logTarget: "target",
    check(event, player2) {
      const bool = get.effect(event.target, event.card, event.player, player2) > 0, hasShan = (target) => {
        if (event.player.hasSkillTag("directHit_ai", true, {
          target,
          card: event.card
        })) {
          return false;
        }
        if (target.hasKnownCards(player2, (card2) => get.name(card2) == "shan")) {
          return true;
        }
        return target.countCards("h") >= Math.random() * 7;
      };
      if (!bool) {
        return true;
      }
      return hasShan(event.target);
    },
    async content(event, trigger, player2) {
      player2.addTempSkill("mbshuxing_used");
      const evt = trigger.getParent(), target = trigger.target;
      player2.markAuto("mbshuxing_used", target);
      if (evt) {
        evt.excluded.add(target);
      }
      if (!target.countCards("h")) {
        return;
      }
      const { cards: cards2 } = await target.showHandcards(`${get.translation(player2)}发动了【束刑】`).forResult();
      if (cards2.every((card2) => get.name(card2) !== "shan")) {
        return;
      }
      const result = await target.chooseButton(
        [
          "束刑：选择一项",
          [
            [
              ["losehp", "失去1点体力"],
              ["give", `交给${get.translation(player2)}你手牌中的【闪】和你下次【秉法】的投票权`]
            ],
            "textbutton"
          ]
        ],
        true
      ).set("canGive", !target.hasSkill("mbbingfa_control")).set("source", player2).set("filterButton", (button) => {
        const { player: player3, canGive, source } = get.event();
        if (button.link == "losehp") {
          return true;
        }
        return canGive && player3 != source;
      }).set("ai", (button) => {
        return button.link == "give" ? 2 : 1;
      }).forResult();
      if (result?.bool && result.links?.length) {
        if (result.links.includes("losehp")) {
          await target.loseHp();
        }
        if (result.links.includes("give")) {
          const cards3 = target.getGainableCards(player2, "he", (card2) => get.name(card2) == "shan");
          if (cards3?.length) {
            await target.give(cards3, player2);
          }
          if (!target.hasSkill("mbbingfa_control")) {
            target.addSkill("mbbingfa_control");
            target.markAuto("mbbingfa_control", player2);
          }
        }
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      }
    }
  },
  //手杀SP曹操
  mblingfa: {
    audio: 4,
    trigger: { global: "roundStart" },
    filter(event, player2) {
      return game.roundNumber < 3 || player2.hasSkill("mblingfa", null, false, false);
    },
    async cost(event, trigger, player2) {
      switch (game.roundNumber) {
        case 1: {
          event.result = await player2.chooseBool(get.prompt(event.skill), "本轮其他角色使用【杀】时，其需弃置一张牌，否则你对其造成1点伤害").forResult();
          break;
        }
        case 2: {
          event.result = await player2.chooseBool(get.prompt(event.skill), "本轮其他角色使用【桃】结算结束后，其需交给你一张牌，否则你对其造成1点伤害").forResult();
          break;
        }
        default: {
          event.result = { bool: true };
        }
      }
    },
    async content(event, trigger, player2) {
      switch (game.roundNumber) {
        case 1:
          player2.line(game.filterPlayer((current) => current != player2).sortBySeat());
          player2.addTempSkill(`${event.name}_sha`, "roundStart");
          break;
        case 2:
          player2.line(game.filterPlayer((current) => current != player2).sortBySeat());
          player2.addTempSkill(`${event.name}_tao`, "roundStart");
          break;
        default:
          await player2.changeSkills(lib.skill[event.name].derivation, [event.name]);
          break;
      }
    },
    derivation: ["twzhian", "new_rejianxiong"],
    subSkill: {
      sha: {
        charlotte: true,
        audio: "mblingfa",
        trigger: { global: "useCard" },
        filter(event, player2) {
          return player2 != event.player && event.card.name == "sha";
        },
        forced: true,
        logTarget: "player",
        async content(event, trigger, player2) {
          await game.delayx();
          const target = event.targets[0];
          const result = await target.chooseToDiscard("he", "令法：弃置一张牌，或受到来自" + get.translation(player2) + "的1点伤害").set("goon", get.damageEffect(target, player2, target) < 0).set("ai", function(card2) {
            if (!_status.event.goon) {
              return 0;
            }
            return 8 - get.value(card2);
          }).forResult();
          if (!result?.bool) {
            await target.damage();
          }
        },
        mark: true,
        marktext: '<span style="text-decoration: line-through;">杀</span>',
        intro: { content: "其他角色使用【杀】时，其需弃置一张牌，否则你对其造成1点伤害。" }
      },
      tao: {
        charlotte: true,
        audio: "mblingfa",
        trigger: { global: "useCardAfter" },
        filter(event, player2) {
          return player2 != event.player && event.card.name == "tao";
        },
        forced: true,
        logTarget: "player",
        async content(event, trigger, player2) {
          await game.delayx();
          const target = event.targets[0];
          const result = await target.chooseToGive("he", player2, "令法：交给" + get.translation(player2) + "一张牌，否则受到来自其的1点伤害").set("goon", get.damageEffect(target, player2, target) < 0).set("ai", function(card2) {
            if (!_status.event.goon) {
              return 0;
            }
            return 8 - get.value(card2);
          }).forResult();
          if (!result?.bool) {
            await target.damage();
          }
        },
        mark: true,
        marktext: '<span style="text-decoration: line-through;">桃</span>',
        intro: { content: "其他角色使用【桃】结算结束后，其需交给你一张牌，否则你对其造成1点伤害。" }
      }
    }
  },
  //手杀曹洪
  mbyuanhu: {
    audio: "yuanhu",
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.hasCard({ type: "equip" }, "eh");
    },
    filterCard: { type: "equip" },
    filterTarget(card2, player2, target) {
      var card2 = ui.selected.cards[0];
      return target.canEquip(card2);
    },
    discard: false,
    lose: false,
    prepare: "give",
    position: "he",
    check(card2) {
      if (get.position(card2) == "h") {
        return 9 - get.value(card2);
      }
      return 7 - get.value(card2);
    },
    logAudio(event, player2) {
      const num = Math.min(get.equipNum(event.cards[0]), 3);
      return "yuanhu" + num + ".mp3";
    },
    async content(event, trigger, player2) {
      const {
        target,
        cards: [card2]
      } = event;
      await target.equip(card2);
      switch (get.subtype(card2)) {
        case "equip1":
          if (game.hasPlayer(function(current) {
            return current != target && get.distance(target, current) == 1 && current.countCards("hej") > 0;
          })) {
            const result = await player2.chooseTarget(true, "弃置一名距离" + get.translation(target) + "为1的角色区域内的一张牌", function(card3, player3, target2) {
              var current = _status.event.current;
              return current != target2 && get.distance(current, target2) == 1 && current.countCards("hej") > 0;
            }).set("current", target).set("ai", function(target2) {
              var player3 = _status.event.player;
              return get.effect(target2, { name: "guohe_copy" }, player3, player3);
            }).forResult();
            if (result?.bool) {
              const targetx = result.targets[0];
              player2.line(targetx);
              await player2.discardPlayerCard(targetx, true, "hej");
            }
          }
          break;
        case "equip2":
          await target.draw();
          break;
        case "equip3":
        case "equip4":
        case "equip6":
          await target.recover();
          break;
        case "equip5": {
          const result = await player2.chooseButton(["获得一种类型的牌", [["basic", "trick"].map((i) => ["", "", `caoying_${i}`]), "vcard"]], true).set("ai", () => Math.random()).forResult();
          if (result.bool) {
            const type = result.links[0][2].slice(8), type2 = ["basic", "trick"].find((i) => i != type);
            const card1 = get.cardPile2((card3) => get.type(card3) == type);
            if (card1) {
              await player2.gain(card1, "gain2");
            }
            const card22 = get.cardPile2((card3) => get.type(card3) == type2);
            if (card22) {
              await target.gain(card22, "gain2");
            }
          }
          break;
        }
      }
      if (target.hp <= player2.hp || target.countCards("h") <= player2.countCards("h")) {
        const { bool } = await player2.chooseBool("援护：是否摸一张牌？").forResult();
        if (!bool) {
          return;
        }
        await player2.draw();
        player2.addTempSkill("mbyuanhu_end");
      }
    },
    ai: {
      order: 10,
      result: {
        player(player2, target) {
          if (get.attitude(player2, target) == 0) {
            return 0;
          }
          if (!ui.selected.cards.length) {
            return;
          }
          var eff = get.effect(target, ui.selected.cards[0], player2, player2), sub = get.subtype(ui.selected.cards[0], false);
          if (target == player2) {
            eff += 4;
          } else {
            var hp = player2.hp, hs = player2.countCards("h", (card2) => card2 != ui.selected.cards[0]);
            var tp = target.hp, ts = target.countCards("h");
            if (sub == "equip2") {
              ts++;
            }
            if (tp < target.maxHp && (sub == "equip3" || sub == "equip4" || sub == "equip5" || sub == "equip6")) {
              tp++;
            }
            if (tp <= hp || ts <= hs) {
              eff += 2;
            }
          }
          if (sub == "equip1") {
            var list = game.filterPlayer(function(current) {
              return current != target && get.distance(target, current) == 1 && current.countCards("hej") < 0;
            }).map(function(i) {
              return get.effect(i, { name: "guohe_copy" }, player2, player2);
            }).sort((a, b) => b - a);
            if (list.length) {
              eff += list[0];
            }
          }
          return eff;
        },
        target(player2, target) {
          if (!ui.selected.cards.length) {
            return 0;
          }
          var sub = get.subtype(ui.selected.cards[0], false);
          var eff = get.effect(target, ui.selected.cards[0], player2, target);
          if (sub == "equip2") {
            eff += get.effect(target, { name: "draw" }, target, target);
          }
          if (target.isDamaged() && (sub == "equip3" || sub == "equip4" || sub == "equip5" || sub == "equip6")) {
            eff += get.recoverEffect(target, player2, player2);
          }
          return eff;
        }
      }
    },
    group: "mbyuanhu_init",
    derivation: ["twjuezhu", "feiying"],
    subSkill: {
      init: {
        audio: ["mbyuanhu", 1],
        trigger: {
          global: "phaseBefore",
          player: "enterGame"
        },
        filter(event, player2) {
          return event.name != "phase" || game.phaseNumber == 0;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          await player2.addSkills("twjuezhu");
        }
      },
      end: {
        trigger: { player: "phaseJieshuBegin" },
        charlotte: true,
        filter(event, player2) {
          return player2.hasSkill("mbyuanhu") && player2.hasCard({ type: "equip" }, "eh");
        },
        async cost(event, trigger, player2) {
          event.result = await player2.chooseCardTarget({
            prompt: get.prompt("mbyuanhu"),
            prompt2: "将一张装备牌置入一名角色的装备区内。若此牌为：武器牌，你弃置与其距离为1的另一名角色区域的一张牌；防具牌，其摸一张牌；坐骑牌，其回复1点体力；宝物牌，你选择基本牌或普通锦囊牌从牌堆中获得一张，其获得另一类型的一张牌。然后若其体力值或手牌数不大于你，则你可摸一张牌。",
            filterCard: lib.skill.mbyuanhu.filterCard,
            filterTarget: lib.skill.mbyuanhu.filterTarget,
            position: "he",
            ai1: lib.skill.mbyuanhu.check,
            ai2(target) {
              var player3 = _status.event.player;
              return get.effect(target, "mbyuanhu", player3, player3);
            }
          }).forResult();
          event.result.skill_popup = false;
        },
        async content(event, trigger, player2) {
          const { cards: cards2, targets: targets2 } = event;
          const result = {
            cards: cards2,
            targets: targets2,
            skill: "mbyuanhu"
          };
          await player2.useResult(result, event);
        }
      }
    }
  },
  //三娘
  mbshuyong: {
    audio: "meiyong",
    trigger: {
      player: ["useCard", "respond"]
    },
    filter(event, player2) {
      return event.card.name == "sha";
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt2(event.skill), (card2, player3, target) => {
        return target.countGainableCards(player3, "hej") && target != player3;
      }).set("ai", (target) => get.effect(target, { name: "shunshou_copy" }, get.player(), get.player())).forResult();
    },
    async content(event, trigger, player2) {
      const [target] = event.targets;
      await player2.gainPlayerCard(target, "hej", true);
      if (player2.getRoundHistory("gain", (evt) => evt.getParent(2).name == event.name && evt.getParent(2).targets.includes(target)).length > 1) {
        await target.draw();
      }
    }
  },
  mbxushen: {
    limited: true,
    audio: "xinfu_xushen",
    enable: "phaseUse",
    chooseButton: {
      dialog(event, player2) {
        return ui.create.dialog("###许身###" + get.skillInfoTranslation("mbxushen", null, false));
      },
      chooseControl(event, player2) {
        const choices = [...[1, 2, 3].map((i) => get.cnNumber(i) + "点"), "cancel2"];
        return choices;
      },
      check() {
        const player2 = get.player();
        const num = game.filterPlayer(
          (target) => get.attitude(target, player2) > 0 && target.hasCard((card2) => {
            return lib.filter.cardSavable(card2, player2);
          }, "hs")
        ).reduce((sum, target) => {
          const cards2 = target.getCards("hs", (card2) => lib.filter.cardSavable(card2, player2));
          return sum + cards2.reduce((sum2, card2) => sum2 + (get.tag(card2, "recover") || 0), 0);
        }, 0);
        const minHp = player2.getHp() + num;
        return minHp <= 1 ? "cancel2" : Math.min(2, Math.max(0, minHp - 1));
      },
      backup(result, player2) {
        return {
          audio: "xinfu_xushen",
          index: result.index,
          skillAnimation: true,
          animationColor: "orange",
          async content(event, trigger, player3) {
            const index = get.info(event.name).index;
            player3.awakenSkill(event.name.slice(0, -7));
            player3.addTempSkill(event.name.slice(0, -7) + "_effect");
            await player3.draw(index + 1);
            await player3.loseHp(index + 1);
          }
        };
      },
      prompt(result, player2) {
        const num = result.index + 1;
        return `摸${get.cnNumber(num)}张牌并失去${num}点体力`;
      }
    },
    ai: {
      order: 10,
      result: {
        player(player2) {
          if (player2.hasUnknown() || player2.getHp() > 3) {
            return 0;
          }
          const num = game.filterPlayer(
            (target) => get.attitude(target, player2) > 0 && target.hasCard((card2) => {
              return lib.filter.cardSavable(card2, player2);
            }, "hs")
          ).reduce((sum, target) => {
            const cards2 = target.getCards("hs", (card2) => lib.filter.cardSavable(card2, player2));
            return sum + cards2.reduce((sum2, card2) => sum2 + (get.tag(card2, "recover") || 0), 0);
          }, 0);
          const minHp = player2.getHp() + num;
          return num > 0 && minHp > 1 ? 1 : 0;
        }
      }
    },
    derivation: ["new_rewusheng", "redangxian", "rezhiman"],
    subSkill: {
      backup: {},
      effect: {
        charlotte: true,
        forced: true,
        trigger: { player: "dyingAfter" },
        filter(event, player2) {
          const evt2 = event.getParent(2);
          if (!(evt2.name === "mbxushen_backup" && evt2.player === player2)) {
            return false;
          }
          lib.skill.mbxushen.derivation;
          return game.hasGlobalHistory("changeHp", (evt) => {
            if (evt.player === player2) {
              const evt3 = evt.getParent();
              if (evt3.name === "recover") {
                return evt3.getParent("dying") === event && evt3.source?.isIn();
              }
            }
            return false;
          });
        },
        async content(event, trigger, player2) {
          let skills2 = lib.skill.mbxushen.derivation.slice();
          let targets2 = [];
          game.getGlobalHistory("changeHp", (evt) => {
            if (evt.player === player2) {
              const evt3 = evt.getParent();
              if (evt3.name === "recover" && evt3.getParent("dying") === trigger && evt3.source?.isIn()) {
                targets2.add(evt3.source);
              }
            }
          });
          targets2.sortBySeat();
          while (skills2.length) {
            const skill = skills2.shift();
            const result = await player2.chooseTarget().set("createDialog", [`###许身###令一名令你回复过体力的角色获得【${get.translation(skill)}】`, [[skill], "skill"]]).set("filterTarget", (card2, player3, target) => {
              const { targetx } = get.event();
              return targetx?.includes(target);
            }).set("ai", (target) => {
              const { gainSkill: skill2, player: player3 } = get.event();
              _status.event.skillRankPlayer = target;
              const num = get.skillRank(skill2, "inout") * Math.sign(Math.sign(get.attitude(player3, target)) - 0.5);
              delete _status.event.skillRankPlayer;
              return num;
            }).set("targetx", targets2).set("gainSkill", skill).forResult();
            if (result?.bool && result.targets?.length) {
              const [target] = result.targets;
              player2.line(target);
              if (target.hasSkill(skill, null, false, false)) {
                await target.draw(3);
              } else {
                await target.addSkills(skill);
              }
            }
          }
        }
      }
    }
  },
  wusheng_re_baosanniang: { audio: 1 },
  zhiman_re_baosanniang: { audio: 1 },
  dangxian_re_baosanniang: { audio: 1 },
  mbzhennan: {
    audio: "xinfu_zhennan",
    trigger: { global: "useCardToPlayered" },
    filter(event, player2) {
      if (!event.isFirstTarget || !event.targets.includes(player2) || get.type2(event.card) !== "trick") {
        return false;
      }
      return event.targets.length > Math.max(1, event.player.getHp());
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseCardTarget({
        prompt: get.prompt(event.skill),
        prompt2: "弃置一张牌并对一名角色造成1点伤害",
        filterCard(card2, player3) {
          return lib.filter.cardDiscardable(card2, player3, "mbzhennan");
        },
        position: "he",
        filterTarget: true,
        ai1(card2) {
          return 5 - get.value(card2);
        },
        ai2(target) {
          const player3 = get.player();
          return get.damageEffect(target, player3, player3);
        }
      }).forResult();
    },
    async content(event, trigger, player2) {
      const {
        cards: cards2,
        targets: [target]
      } = event;
      await player2.modedDiscard(cards2);
      await target.damage("nocard");
    }
  },
  mbfangxu: {
    audio: 4,
    onChooseToUse(event) {
      const player2 = event.player;
      if (!game.online && (player2.getStat().skill.mbfangxu || 0) < lib.skill.mbfangxu.usable) {
        event.set(
          "mbfangxu",
          (() => {
            event.mbfangxu ??= {};
            event.mbfangxu[player2.playerid] = player2.getHistory("gain").reduce((cards2, evt) => cards2.addArray(evt.cards), []);
            return event.mbfangxu;
          })()
        );
      }
    },
    enable: "chooseToUse",
    filter(event, player2) {
      const cards2 = player2.getCards("he", (card2) => event.mbfangxu?.[player2.playerid]?.includes(card2));
      return ["sha", "shan"].some((name) => cards2.some((i) => event.filterCard(get.autoViewAs({ name }, [i]), player2, event)));
    },
    usable: 2,
    chooseButton: {
      dialog(event, player2) {
        let vcards = [], cards2 = player2.getCards("he", (card2) => event.mbfangxu[player2.playerid].includes(card2));
        for (const name of ["sha", "shan"]) {
          cards2.some((i) => event.filterCard(get.autoViewAs({ name }, [i]), player2, event)) && vcards.push(["基本", "", name]);
        }
        const dialog = ui.create.dialog("芳许", [vcards, "vcard"], "hidden");
        dialog.direct = true;
        return dialog;
      },
      ai: () => 1,
      prompt(links) {
        return `###芳许###<div class="text center">将一张本回合获得的牌当作【${get.translation(links[0][2])}】使用</div>`;
      },
      backup(links, player2) {
        return {
          audio: "mbfangxu",
          logAudio: () => 1,
          filterCard(card2, player3) {
            return get.event().mbfangxu?.[player3.playerid]?.includes(card2);
          },
          position: "he",
          check(card2) {
            return 7 - get.value(card2);
          },
          viewAs: { name: links[0][2] },
          async precontent(event, trigger, player3) {
            await player3.addTempSkill("mbfangxu_effect");
          }
        };
      }
    },
    ai: {
      order(item, player2) {
        return get.event().type === "phase" ? get.order({ name: "sha" }, player2) + 0.1 : 1;
      },
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2, tag, arg) {
        const cards2 = player2.getHistory("gain").reduce((cards3, evt) => cards3.addArray(evt.cards), []);
        return arg !== "respond" && (player2.getStat().skill.mbfangxu || 0) < lib.skill.mbfangxu.usable && cards2.containsSome(...player2.getCards("he"));
      },
      result: { player: 1 }
    },
    subSkill: {
      backup: {},
      effect: {
        audio: "mbfangxu",
        logAudio: (index) => typeof index === "number" ? "mbfangxu" + index + ".mp3" : "",
        charlotte: true,
        trigger: { player: ["useCard", "useCardAfter"] },
        filter(event, player2, name) {
          return event.skill === "mbfangxu_backup" && (name === "useCard" || typeof event.mbfangxu_guess?.[player2.playerid] === "number");
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const card2 = trigger.card, target = _status.currentPhase;
          if (event.triggername === "useCard") {
            const str = get.translation(card2);
            const result = await player2.chooseControl().set("choiceList", [`若${str}造成伤害，你弃置受伤角色至多两张牌`, `若${str}未造成伤害${target?.isIn() ? `，${get.translation(target)}摸两张牌` : ""}`]).set("prompt", "芳许：请进行你的选择（不公开）").set("ai", () => {
              const player3 = get.player(), target2 = _status.currentPhase;
              const trigger2 = get.event().getTrigger();
              let guess = 1;
              trigger2.card.name === "sha" && trigger2.targets.some((i) => i.mayHaveShan(player3, "use")) && guess--;
              guess === 1 ? target2?.isIn() && get.effect(target2, { name: "draw" }, player3, player3) < 0 && guess-- : trigger2.targets.some((i) => i.mayHaveShan(player3, "use") && get.effect(target2, { name: "guohe_copy2" }, player3, player3) < 0) && guess++;
              return guess;
            }).forResult();
            player2.logSkill(event.name, null, null, null, [2]);
            trigger.set(
              "mbfangxu_guess",
              (() => {
                trigger.mbfangxu_guess ??= {};
                trigger.mbfangxu_guess[player2.playerid] = result.index;
                return trigger.mbfangxu_guess;
              })()
            );
          } else {
            const guess = trigger.mbfangxu_guess[player2.playerid];
            const goon = game.hasPlayer2((i) => i.hasHistory("damage", (evt) => evt.card === card2));
            if (goon === Boolean(guess)) {
              return;
            }
            switch (guess) {
              case 0: {
                const targets2 = game.filterPlayer((i) => i.hasHistory("damage", (evt) => evt.card === card2)).sortBySeat();
                if (targets2.length) {
                  player2.logSkill(event.name, targets2, null, null, [3]);
                  for (const i of targets2) {
                    await player2.discardPlayerCard(i, [1, 2], "he", true);
                  }
                }
                break;
              }
              case 1: {
                if (target?.isIn()) {
                  player2.logSkill(event.name, [target], null, null, [4]);
                  await target.draw(2);
                }
                break;
              }
            }
          }
        }
      }
    }
  },
  mbzhuguan: {
    audio: 4,
    logAudio: (event) => event.name == "phase" ? ["mbzhuguan3.mp3", "mbzhuguan4.mp3"] : 2,
    trigger: { player: ["useCard", "phaseBegin"] },
    filter(event, player2) {
      if (event.name === "phase") {
        return player2.hasCard((card2) => card2.hasGaintag("mbzhuguan") && lib.filter.cardDiscardable(card2, player2), "h");
      }
      return get.type(event.card) === "basic" && get.color(event.card) === "red";
    },
    forced: true,
    async content(event, trigger, player2) {
      if (trigger.name === "phase") {
        await player2.discard(player2.getCards("h", (card2) => card2.hasGaintag(event.name) && lib.filter.cardDiscardable(card2, player2)));
      } else {
        const next = player2.draw();
        next.gaintag.add(event.name);
        await next;
      }
    },
    onremove(player2, skill) {
      player2.removeGaintag(skill);
    },
    mod: {
      ignoredHandcard(card2, player2) {
        if (card2.hasGaintag("mbzhuguan")) {
          return true;
        }
      },
      cardDiscardable(card2, player2, name) {
        if (name === "phaseDiscard" && card2.hasGaintag("mbzhuguan")) {
          return false;
        }
      }
    }
  },
  mblisuo: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player2) {
      return player2.countCards("h") > 0 && game.hasPlayer((target) => lib.skill.mblisuo.filterTarget(null, player2, target));
    },
    filterTarget(card2, player2, target) {
      return target !== player2 && target.countCards("h") > 0;
    },
    usable: 1,
    async content(event, trigger, player2) {
      const target = event.target;
      const next = player2.chooseCardOL([player2, target], "h", true, "栗索：请展示任意张手牌", [1, Infinity], "allowChooseAll").set("ai", () => -0.5 + Math.random());
      next._args.remove("glow_result");
      const result2 = await next.forResult();
      const [playerCards, targetCards] = result2.map((i) => i.cards);
      const cards2 = [...playerCards, ...targetCards];
      await player2.showCards(cards2, get.translation(player2) + "发动了【栗索】").set("customButton", (button) => {
        const target2 = get.owner(button.link);
        if (target2) {
          game.createButtonCardsetion(`${target2.getName(true)}`, button);
        }
      }).set("delay_time", 4).set("multipleShow", true);
      let sgn = playerCards.length - targetCards.length;
      if (sgn > 0) {
        target.addTempSkill(event.name + "_zhixi", { player: "phaseUseAfter" });
        target.addMark(event.name + "_zhixi", targetCards.length, false);
      } else if (sgn < 0) {
        sgn = playerCards.length;
        const recastCards = playerCards.filter((i) => player2.canRecast(i));
        recastCards.length > 0 && await player2.recast(recastCards);
        while (sgn > 0) {
          const result = await player2.chooseToUse(
            function(card2, player3, event2) {
              return get.name(card2) === "sha" && lib.filter.filterCard.apply(this, arguments);
            },
            `栗索（剩余${sgn}张）<div class="text center">你可以对${get.translation(target)}使用无距离和任何次数限制的【杀】</div></div>`
          ).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("filterTarget", function(card2, player3, target2) {
            const source = get.event().sourcex;
            return (target2 === source || ui.selected.targets.includes(source)) && lib.filter.targetEnabled.apply(this, arguments);
          }).set("sourcex", target).forResult();
          if (result?.bool) {
            sgn--;
          } else {
            break;
          }
        }
      }
    },
    ai: {
      order: 10,
      result: { target: -1 }
    },
    subSkill: {
      zhixi: {
        charlotte: true,
        onremove: true,
        markimage: "image/character/sunluyu.jpg",
        mark: true,
        intro: {
          markcount: (storage) => (storage || 0).toString(),
          content: (storage) => `下个出牌阶段还可使用${storage || 0}张牌`
        },
        trigger: { player: "useCard0" },
        filter(event, player2) {
          return player2.isPhaseUsing() && player2.countMark("mblisuo_zhixi") > 0;
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player2) {
          player2.removeMark(event.name, 1, false);
        },
        mod: {
          cardUsable(card2, player2) {
            if (player2.isPhaseUsing() && !player2.hasMark("mblisuo_zhixi")) {
              return false;
            }
          },
          cardSavable(card2, player2) {
            if (player2.isPhaseUsing() && !player2.hasMark("mblisuo_zhixi")) {
              return false;
            }
          }
        }
      }
    }
  },
  //笮融
  mbfutu: {
    audio: 8,
    logAudio: (event, player2, name, target, costResult) => {
      if (costResult.cost_data.length > 1) {
        return ["mbfutu5.mp3", "mbfutu6.mp3"];
      }
      return (costResult.cost_data.includes("black") ? [1, 2] : [3, 4]).map((i) => `mbfutu${i}.mp3`);
    },
    trigger: {
      global: "phaseEnd"
    },
    filter(event, player2) {
      return ["damage", "recover"].some((name) => get.info("mbfutu")?.isMax(player2, name));
    },
    isMax(player2, name) {
      let count = (current) => {
        let history = _status.globalHistory?.[_status.globalHistory.length - 1]?.everything, count2 = 0;
        if (!history?.length) {
          return count2;
        }
        for (let evt of history) {
          if (evt._cancelled || evt.name != name) {
            continue;
          }
          if (evt?.source != current || typeof evt.num != "number") {
            continue;
          }
          count2 += evt.num;
        }
        return count2;
      };
      return count(player2) >= 1 && !game.hasPlayer((current) => count(current) > count(player2));
    },
    marktext: "业",
    intro: {
      name: "业",
      name2: "业",
      content: "expansion",
      markcount: "expansion"
    },
    async cost(event, trigger, player2) {
      let list = ["damage", "recover"].filter((name) => get.info(event.skill)?.isMax(player2, name)), map = {
        damage: "black",
        recover: "red"
      };
      list = list.map((i) => map[i]);
      event.result = {
        bool: true,
        cost_data: list
      };
    },
    async content(event, trigger, player2) {
      const colors = event.cost_data, cards2 = [];
      for (let color of colors) {
        const card2 = get.cardPile2((card3) => get.color(card3) == color);
        if (card2) {
          cards2.push(card2);
        }
      }
      if (cards2?.length) {
        const next = player2.addToExpansion(cards2, "gain2");
        next.gaintag.add(event.name);
        await next;
      }
    },
    group: "mbfutu_defend",
    subSkill: {
      defend: {
        trigger: {
          player: "damageBegin3"
        },
        audio: "mbfutu",
        logAudio: () => [7, 8].map((i) => `mbfutu${i}.mp3`),
        filter(event, player2) {
          return player2.hasExpansions("mbfutu");
        },
        async cost(event, trigger, player2) {
          const { bool, links } = await player2.chooseButton([`###${get.prompt("mbfutu")}###弃置一张业并防止此伤害`, player2.getExpansions("mbfutu")]).set("eff", get.damageEffect(player2, trigger.source, player2)).set("ai", (button) => {
            const { player: player3, eff } = get.event();
            if (eff >= 0) {
              return 0;
            }
            return player3.getExpansions("mbfutu")?.filter((card2) => {
              return get.color(card2) == get.color(button.link);
            })?.length;
          }).forResult();
          event.result = {
            bool,
            cards: links
          };
        },
        async content(event, trigger, player2) {
          await player2.loseToDiscardpile(event.cards);
          trigger.cancel();
        }
      }
    }
  },
  mbjingtu: {
    audio: 6,
    enable: "phaseUse",
    limited: true,
    skillAnimation: true,
    animationColor: "gray",
    filter(event, player2) {
      return player2.countExpansions("mbfutu") > 0;
    },
    chooseButton: {
      dialog(event, player2) {
        const list = [
          ["red", "获得所有黑色“业”，然后对一名角色造成等量伤害"],
          ["black", "获得所有红色“业”，然后令一名角色增加等量点体力上限并回复等量体力"],
          ["all", "背水！同时执行两项"]
        ];
        return ui.create.dialog("净土：选择一项", [list, "textbutton"], "hidden");
      },
      filter(button) {
        const player2 = get.player(), { link } = button, count = (color) => player2.countCards("x", (card2) => card2.hasGaintag("mbfutu") && get.color(card2) != color);
        if (link != "all") {
          return count(link) > 0;
        }
        return count("red") > 0 && count("red") == count("black");
      },
      check(button) {
        switch (button.link) {
          case "black":
            return 5;
          case "red":
            return 10;
          case "all":
            return 15;
        }
      },
      backup(links, player2) {
        return {
          audio: "mbjingtu",
          logAudio: (event, player3) => {
            const choice = get.info("mbjingtu_backup")?.choice;
            switch (choice) {
              case "red":
                return ["mbjingtu1.mp3", "mbjingtu2.mp3"];
              case "black":
                return ["mbjingtu3.mp3", "mbjingtu4.mp3"];
              default:
                return ["mbjingtu5.mp3", "mbjingtu6.mp3"];
            }
          },
          choice: links[0],
          skillAnimation: true,
          animationColor: "gray",
          async content(event, trigger, player3) {
            player3.awakenSkill("mbjingtu");
            const choice = get.info(event.name)?.choice;
            const cards2 = player3.getCards("x", (card2) => card2.hasGaintag("mbfutu") && get.color(card2) != choice);
            if (!cards2?.length) {
              return;
            }
            await player3.gain(cards2, "gain2");
            player3.changeSkin({ characterName: "mb_zerong" }, `mb_zerong_${choice == "all" ? choice : choice == "red" ? "black" : "red"}`);
            const count = (color) => cards2?.filter((card2) => get.color(card2) == color)?.length, black = count("black"), red = count("red");
            if (choice != "black" && black > 0) {
              const result = await player3.chooseTarget(`净土：对一名角色造成${black}点伤害`, true).set("ai", (target) => {
                const player4 = get.player();
                return get.damageEffect(target, player4, player4);
              }).forResult();
              if (result.bool) {
                const target = result.targets[0];
                player3.line(target, "green");
                await target.damage(player3, black);
              }
            }
            if (choice != "red" && red > 0) {
              const result = await player3.chooseTarget(`净土：令一名角色增加${red}点体力上限并恢复${red}点体力`, true).set("ai", (target) => {
                const player4 = get.player();
                return get.recoverEffect(target, player4, player4);
              }).forResult();
              if (result.bool) {
                const target = result.targets[0];
                player3.line(target, "green");
                await target.gainMaxHp(red);
                await target.recover(player3, red);
              }
            }
            await player3.changeSkills(["mbfozong"], ["mbfutu"]);
            const colors = cards2.slice(0).map((i) => get.color(i)).toUniqued();
            player3.markAuto("mbfozong", colors);
          }
        };
      },
      prompt(links, player2) {
        const map = {
          red: "获得所有黑色“业”，然后对一名角色造成等量伤害",
          black: "获得所有红色“业”，然后令一名角色增加等量点体力上限并回复等量体力",
          all: "获得所有“业”，然后依次执行：1.对一名角色造成黑色“业”数点伤害；2.令一名角色增加红色“业”数点体力上限并回复等量体力"
        };
        return map[links[0]];
      }
    },
    derivation: "mbfozong",
    ai: {
      combo: "mbfutu",
      order: 3,
      result: {
        player(player2) {
          const count = (color) => player2.countCards("x", (card2) => card2.hasGaintag("mbfutu") && get.color(card2) == color);
          if (count("red") > 1 && count("red") == count("black")) {
            return 1;
          }
          if (player2.hp < 2 && count("red") > 1) {
            return 1;
          }
          return 0;
        }
      }
    },
    subSkill: {
      backup: {}
    }
  },
  mbjiebian: {
    audio: 2,
    trigger: {
      global: "phaseUseEnd"
    },
    filter(event, player2) {
      if (game.hasPlayer2(
        (current) => current.getHistory("damage", (evt) => {
          return evt.getParent(event.name) == event;
        }).length > 0,
        true
      )) {
        return false;
      }
      return game.hasPlayer((current) => {
        if (current != _status.currentPhase && !current.isMinHp()) {
          return false;
        }
        return player2.canCompare(current, player2.hasExpansions("mbfutu"));
      });
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt2(event.skill)).set("filterTarget", (card2, player3, target) => {
        if (target != _status.currentPhase && !target.isMinHp()) {
          return false;
        }
        return player3.canCompare(target, player3.hasExpansions("mbfutu"));
      }).set("ai", (target) => {
        const player3 = get.player(), eff1 = get.damageEffect(target, player3, player3), eff2 = get.recoverEffect(target, player3, player3);
        if (target.countCards("h") > 2) {
          return Math.max(eff1, eff2);
        }
        return eff1;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      player2.addTempSkill("mbjiebian_fake");
      const cards2 = game.createFakeCards(player2.getExpansions("mbfutu"));
      player2.directgains(cards2, null, "mbjiebian");
      const result = await player2.chooseToCompare(target).set("mbjiebian", true).set("position", "hs").forResult();
      if (result.tie || result.winner != player2) {
        return;
      }
      const winner = result.winner, loser = winner == player2 ? target : player2;
      const result2 = await winner.chooseButton(
        [
          "劫辩：选择一项",
          [
            [
              ["damage", `对${get.translation(loser)}造成1点伤害`],
              ["recover", `令${get.translation(loser)}恢复1点体力并摸一张牌，然后获得其至多两张牌`]
            ],
            "textbutton"
          ]
        ],
        true
      ).set("ai", (button) => {
        const { player: player3, loser: loser2 } = get.event(), { link } = button;
        return get[`${link}Effect`](loser2, player3, player3);
      }).set("loser", loser).forResult();
      if (result2?.bool) {
        if (result2?.links[0] == "damage") {
          await loser.damage(winner);
        } else {
          if (loser.isDamaged()) {
            await loser.recover(winner);
          }
          await loser.draw();
          await winner.gainPlayerCard(loser, "he", [1, 2], true);
        }
      }
    },
    subSkill: {
      fake: {
        charlotte: true,
        trigger: {
          global: ["chooseCardOLBegin", "chooseCardOLEnd"]
        },
        filter(event, player2) {
          return event.type == "compare" && !event.directresult && event.getParent().mbjiebian;
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player2) {
          if (event.triggername == "chooseCardOLBegin") {
            trigger._set.push(["position", "hs"]);
            const originalFilter = trigger.filterCard;
            trigger._set.push([
              "filterCard",
              function(card2) {
                if (typeof originalFilter === "function" && !originalFilter(card2)) {
                  return false;
                }
                if (get.position(card2) == "s") {
                  return card2.hasGaintag("mbjiebian");
                }
                return true;
              }
            ]);
          } else {
            const cards2 = player2.getCards("s", (card2) => card2.hasGaintag("mbjiebian"));
            if (cards2?.length) {
              game.deleteFakeCards(cards2);
            }
            if (!trigger.result[trigger.targets.indexOf(player2)].skill) {
              const card2 = trigger.result[trigger.targets.indexOf(player2)].cards[0], precard = player2.getExpansions("mbfutu").find((cardx) => cardx.cardid == card2._cardid);
              if (precard) {
                trigger.result[trigger.targets.indexOf(player2)].cards = [precard];
              }
            }
          }
        }
      }
    }
  },
  mbfozong: {
    audio: 6,
    logAudio: (event, player2) => {
      const list = player2.getStorage("mbfozong");
      let audios = list.length > 1 ? [5, 6] : list.includes("red") ? [3, 4] : [1, 2];
      return `mbfozong${audios[["recover", "damage"].indexOf(event.name)]}.mp3`;
    },
    forced: true,
    onremove: true,
    trigger: {
      global: ["damageBegin1", "recoverBegin"]
    },
    filter(event, player2) {
      const list = player2.getStorage("mbfozong");
      let evt = event.getParent("useCard", true), card2 = event.card;
      if (evt?.player != player2 || !card2 || evt.card != card2) {
        return false;
      }
      return list?.includes(get.color(card2, player2)) && player2.hasHistory("lose", (evtx) => {
        const evt2 = evtx.relatedEvent || evtx.getParent();
        return evtx.hs?.length && evt2 == evt;
      });
    },
    async content(event, trigger, player2) {
      trigger.num++;
    },
    mod: {
      ignoredHandcard(card2, player2) {
        const list = player2.getStorage("mbfozong");
        if (list?.includes(get.color(card2, player2))) {
          return true;
        }
      },
      cardDiscardable(card2, player2, name) {
        const list = player2.getStorage("mbfozong");
        if (name == "phaseDiscard" && list?.includes(get.color(card2, player2))) {
          return false;
        }
      }
    },
    ai: {
      combo: "mbjingtu"
    }
  },
  //手杀孟达
  mbjili: {
    audio: 9,
    logAudio: () => ["mbjili1.mp3", "mbjili2.mp3"],
    trigger: {
      global: "phaseBegin"
    },
    filter(event, player2) {
      return player2.inRange(event.player) && player2.getStorage("mbjili").length < 3;
    },
    async cost(event, trigger, player2) {
      const list = [0, 1, 2].filter((num) => !player2.getStorage("mbjili").includes(num));
      list.add("cancel2");
      const result = await player2.chooseControl(list).set("prompt", get.prompt2(event.skill, trigger.player)).set("ai", () => {
        const player3 = get.player(), target = get.event().getTrigger().player;
        if (get.attitude(player3, target) > 0) {
          return "cancel2";
        }
        return [0, 1, 2].filter((num) => !player3.getStorage("mbjili").includes(num)).randomGet();
      }).forResult();
      event.result = {
        bool: result.control != "cancel2",
        cost_data: result.control,
        targets: [trigger.player]
      };
    },
    async content(event, trigger, player2) {
      const num = event.cost_data;
      player2.markAuto(event.name, num);
      player2.addTempSkill("mbjili_used", "roundStart");
      if (!trigger.mbjili) {
        trigger.set("mbjili", {});
      }
      player2.addTempSkill("mbjili_effect");
      player2.setStorage("mbjili_effect", [num, event.targets[0]]);
    },
    subSkill: {
      effect: {
        audio: "mbjili",
        trigger: {
          global: "phaseJieshuBegin"
        },
        charlotte: true,
        onremove: true,
        forced: true,
        locked: false,
        filter(event, player2) {
          const storage = player2.getStorage("mbjili_effect");
          return typeof storage[0] == "number" && storage[1] == event.player;
        },
        logAudio(event, player2) {
          const storage = player2.getStorage("mbjili_effect");
          const target = storage[1], num = storage[0], count = target.getHistory("useCard", (evt) => evt?.targets?.includes(player2))?.length;
          if (count < num) {
            return ["mbjili7.mp3", "mbjili8.mp3", "mbjili9.mp3"];
          }
          if (count > num) {
            return ["mbjili5.mp3", "mbjili6.mp3"];
          }
          return ["mbjili3.mp3", "mbjili4.mp3"];
        },
        logTarget: "player",
        async content(event, trigger, player2) {
          const storage = player2.getStorage(event.name);
          const target = storage[1], num = storage[0], count = target.getHistory("useCard", (evt) => evt?.targets?.includes(player2)).length;
          if (count < num) {
            if (num < 4) {
              await player2.draw(4 - num);
            }
          } else if (count == num) {
            if (num > 0 && player2.countCards("he")) {
              await player2.chooseToGive(target, num, true, "he");
            }
          } else {
            const card2 = { name: "sha", isCard: true };
            if (player2.canUse(card2, target, false)) {
              const { bool } = await player2.chooseBool("积戾", `是否对${get.translation(target)}视为使用一张杀？`).set("choice", get.effect(target, card2, player2, player2) > 0).forResult();
              if (bool) {
                await player2.useCard(card2, target, false);
              }
            }
          }
        }
      },
      used: {
        charlotte: true,
        onremove(player2) {
          player2.setStorage("mbjili", []);
        }
      }
    }
  },
  mbshishu: {
    audio: 2,
    trigger: {
      global: ["gainAfter", "loseAsyncAfter"]
    },
    filter(event, player2) {
      if (event.name != "gain" && event.type != "gain") {
        return false;
      }
      const cards2 = event.getl(player2)?.cards2;
      if (!cards2.length) {
        return false;
      }
      return game.hasPlayer((current) => {
        if (current == player2 || current != _status.currentPhase) {
          return false;
        }
        const cardsx = event.getg(current);
        return cardsx.some((card2) => cards2.includes(card2));
      });
    },
    logTarget(event, player2) {
      const cards2 = event.getl(player2)?.cards2;
      return game.filterPlayer((current) => {
        if (current == player2 || current != _status.currentPhase) {
          return false;
        }
        const cardsx = event.getg(current);
        return cardsx.some((card2) => cards2.includes(card2));
      });
    },
    forced: true,
    async content(event, trigger, player2) {
      for (const target of event.targets) {
        const cards2 = trigger.getg(target).filter((card2) => trigger.getl(player2).cards2.includes(card2));
        const result = await target.chooseToGive(player2, "he").set("prompt", "恃术").set("prompt2", `交给${get.translation(player2)}一张与${get.translation(cards2)}类型均不同的牌，或点取消弃置这些牌`).set(
          "types",
          cards2.map((i) => get.type2(i, target))
        ).set("filterCard", (card2) => {
          const { player: player3, types } = get.event();
          return !types.includes(get.type2(card2, player3));
        }).forResult();
        if (!result.bool) {
          await target.modedDiscard(cards2);
        }
      }
    }
  },
  mbjinzu: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.countCards("h") && game.hasPlayer((current) => current != player2);
    },
    filterTarget(card2, player2, target) {
      return target != player2;
    },
    position: "h",
    filterCard: true,
    check(card2) {
      const player2 = get.player();
      let num = player2.countCards("h"), sha = player2.countCards("h", "sha");
      if (sha && num - sha > 0 && player2.hasUseTarget("sha", void 0, true)) {
        return get.name(card2, player2) != "sha";
      }
      return 20 - get.value(card2);
    },
    discard: false,
    lose: false,
    delay: 0,
    async content(event, trigger, player2) {
      const card2 = event.cards[0], target = event.target;
      if (!target.countCards("h")) {
        player2.$throw(card2);
        await player2.showCards(card2, get.translation(player2) + "发动了【劲镞】");
        await player2.modedDiscard(card2);
        const stat = player2.getStat().skill;
        if (stat.mbjinzu) {
          delete stat.mbjinzu;
        }
        const skill = "mbjinzu_effect";
        player2.storage[skill] ??= /* @__PURE__ */ new Map();
        player2.storage[skill].set(target, !player2.storage[skill].has(target) ? 1 : player2.storage[skill].get(target) + 1);
        player2.addTempSkill(skill);
        player2.markSkill(skill);
        return;
      }
      const result = target.countCards("h") > 2 ? await target.chooseCard("劲镞：展示两张手牌", 2, "h", true).forResult() : {
        bool: true,
        cards: target.getCards("h")
      };
      if (!result?.bool) {
        return;
      }
      const cards2 = [card2].concat(result.cards);
      player2.$throw(card2);
      target.$throw(result.cards);
      game.log(player2, "展示了", player2, "的", card2, "和", target, "的", result.cards);
      await player2.showCards(cards2, get.translation(player2) + "发动了【劲镞】").set("log", false);
      const numbers = cards2.map((card3) => get.number(card3)).toUniqued(), min = Math.min(...numbers), max = Math.max(...numbers), number = get.number(card2);
      if (number == min || number == max) {
        const lose_list = [
          [player2, [card2]],
          [target, result.cards]
        ];
        await game.loseAsync({
          lose_list
        }).setContent("discardMultiple");
        const stat = player2.getStat().skill;
        if (stat.mbjinzu) {
          delete stat.mbjinzu;
        }
      }
      if (result.cards.some((cardx) => get.number(cardx) <= number) && result.cards.some((cardx) => get.number(cardx) >= number)) {
        const skill = "mbjinzu_effect";
        player2.storage[skill] ??= /* @__PURE__ */ new Map();
        player2.storage[skill].set(target, !player2.storage[skill].has(target) ? 1 : player2.storage[skill].get(target) + 1);
        player2.addTempSkill(skill);
        player2.markSkill(skill);
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        audio: "mbjinzu",
        trigger: { player: "useCard" },
        filter(event, player2) {
          return event.card?.name == "sha";
        },
        forced: true,
        async content(event, trigger, player2) {
          const targets2 = Array.from(player2.storage[event.name].keys()).sortBySeat();
          trigger.directHit.addArray(targets2);
          for (const [target, num] of player2.storage[event.name]) {
            const id = target.playerid;
            const map = trigger.customArgs;
            map[id] ??= {};
            if (typeof map[id].extraDamage !== "number") {
              map[id].extraDamage = 0;
            }
            map[id].extraDamage += num;
          }
          player2.removeSkill(event.name);
        },
        ai: {
          directHit_ai: true,
          skillTagFilter(player2, tag, arg) {
            if (tag == "directHit_ai" && (arg?.card?.name !== "sha" || !player2.getStorage("mbjinzu_effect", /* @__PURE__ */ new Map())?.has(arg?.target))) {
              return false;
            }
          }
        },
        intro: {
          content(storage, player2) {
            if (!storage?.size) {
              return "";
            }
            let str = "下一张杀不可响应的角色和对应伤害增加值";
            for (const [target, num] of storage) {
              str += `<li>${get.translation(target)}：${num}`;
            }
            return str;
          }
        },
        targetprompt2: (target) => {
          const player2 = get.player(), card2 = get.card();
          if (card2?.name == "sha") {
            if (player2.getStorage("mbjinzu_effect", /* @__PURE__ */ new Map())?.has(target)) {
              return `强命+${player2.getStorage("mbjinzu_effect", /* @__PURE__ */ new Map()).get(target)}`;
            }
          }
        },
        onChooseToUse(event) {
          event.targetprompt2.add(lib.skill.mbjinzu_effect.targetprompt2);
        },
        onChooseTarget(event) {
          event.targetprompt2.add(lib.skill.mbjinzu_effect.targetprompt2);
        }
      }
    },
    ai: {
      order(item, player2) {
        let sha = player2.countCards("hs", (card2) => card2.name == "sha" && player2.hasUseTarget("sha", true, true));
        if (!sha.length) {
          return 0.5;
        } else if (player2.countCards("hs", (card2) => get.tag(card2, "draw"))) {
          return 0;
        } else if (!game.hasPlayer((target) => target != player2 && get.attitude(player2, target) < 0 && lib.skill.mbjinzu.ai.result.target(player2, target) < 0)) {
          return 0;
        }
        return 9;
      },
      result: {
        target(player2, target) {
          if (!target.hasCards("h")) {
            let sha = player2.countCards("hs", (card2) => card2.name == "sha" && player2.hasUseTarget("sha", true, true));
            if (!sha.length) {
              return 0;
            } else if (target.hasSkillTag("filterDamage", null, { player: player2 })) {
              return 0;
            } else if (!player2.hasCards("h", "sha")) {
              return 0;
            } else if (!player2.canUse("sha", target, void 0, true)) {
              return 0;
            } else if (get.effect(target, { name: "sha" }, player2, player2) < 0) {
              return 0;
            } else if (player2.hasHistory("useSkill", (evt) => evt.skill == "mbanxian") && !player2.canUse("sha", target, true, true)) {
              return 0;
            }
          }
          return -1;
        }
      }
    }
  },
  mbanxian: {
    audio: 2,
    trigger: {
      player: "loseAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player2) {
      if (event.type != "discard") {
        return false;
      }
      const evt = event.getl(player2);
      if (!evt?.hs?.some((i) => get.position(i, true) == "d" && i.name == "sha")) {
        return false;
      }
      const history = player2.getHistory("lose", (evtx) => {
        if (evtx.type != "discard") {
          return false;
        }
        return evtx?.hs?.length;
      }).map((evtx) => event.name == "lose" ? evtx : evtx.getParent());
      return history.indexOf(event) == 0;
    },
    async cost(event, trigger, player2) {
      const result = await player2.chooseButton([get.prompt2(event.skill), trigger.getl(player2).hs]).set("filterButton", (button) => {
        return get.event().canGain.includes(button.link);
      }).set(
        "canGain",
        trigger.getl(player2).hs.filter((card2) => get.position(card2, true) == "d" && card2.name == "sha")
      ).forResult();
      event.result = {
        bool: result.bool,
        cards: result?.links
      };
    },
    async content(event, trigger, player2) {
      const cards2 = event.cards;
      await player2.gain(cards2, "gain2");
      if (player2.hasUseTarget(cards2[0], false, false)) {
        await player2.chooseUseTarget(cards2[0], true, false, "nodistance");
      }
    },
    //group: "mbanxian_draw",
    subSkill: {
      draw: {
        trigger: {
          source: "damageSource"
        },
        filter(event, player2) {
          return event?.card?.name == "sha" && event.getParent("mbanxian", true);
        },
        popup: false,
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          await player2.draw(2);
        }
      }
    }
  },
  //手杀关银屏
  mbxuehen: {
    audio: "xueji",
    trigger: {
      source: "damageSource",
      player: "damageEnd"
    },
    filter(event, player2, name) {
      if (!player2.isDamaged() || !player2.countCards("h")) {
        return false;
      }
      if (name == "damageSource") {
        return player2.getHistory("sourceDamage", (evt) => evt.num > 0).indexOf(event) == 0;
      }
      if (name == "damageEnd") {
        return player2.getHistory("damage", (evt) => evt.num > 0).indexOf(event) == 0;
      }
      return false;
    },
    async cost(event, trigger, player2) {
      const str = `###${get.prompt(event.skill)}###${lib.dynamicTranslate[event.skill](player2)}`;
      event.result = await player2.chooseCard(str, [1, player2.getDamagedHp()]).set("ai", (card2) => card2.hasGaintag("mbxuehen_sha") ? 0 : 7 - get.value(card2)).forResult();
    },
    async content(event, trigger, player2) {
      const cards2 = event.cards;
      await player2.showCards(cards2, `${get.translation(player2)}发动了〖雪恨〗`);
      player2.addGaintag(cards2, "mbxuehen_sha");
    },
    group: ["mbxuehen_sha"],
    subSkill: {
      rewrite: { nopop: true },
      sha: {
        mod: {
          cardname(card2) {
            const evt = get.event();
            if (!["chooseToUse", "chooseToRespond"].includes(evt.name)) {
              return;
            }
            if (get.itemtype(card2) == "card" && card2.hasGaintag("mbxuehen_sha")) {
              return "sha";
            }
          },
          cardnature(card2) {
            const evt = get.event();
            if (!["chooseToUse", "chooseToRespond"].includes(evt.name)) {
              return;
            }
            if (get.itemtype(card2) == "card" && card2.hasGaintag("mbxuehen_sha")) {
              return false;
            }
          },
          cardUsable(card2, player2, num) {
            if (!card2.cards || card2.cards.length != 1) {
              return;
            }
            if (get.itemtype(card2.cards[0]) === "card" && card2.cards[0].hasGaintag("mbxuehen_sha")) {
              return Infinity;
            }
          }
        },
        charlotte: true,
        onremove(player2, skill) {
          player2.removeGaintag(skill);
        },
        silent: true,
        trigger: {
          source: "damageSource",
          player: ["useCard1", "useCardAfter"]
        },
        filter(event, player2, name) {
          if (event.name == "useCard") {
            if (name == "useCard1" && event.addCount === false) {
              return false;
            }
            if (name == "useCardAfter" && !player2.storage.mbxuehen) {
              return false;
            }
            return player2.hasHistory("lose", (evt) => {
              const evtx = evt.relatedEvent || evt.getParent();
              return evtx == event && Object.values(evt.gaintag_map).flat().includes("mbxuehen_sha");
            });
          }
          return event.card && player2.hasHistory("lose", (evt) => {
            const evtx = evt.relatedEvent || evt.getParent();
            return evtx.card == event.card && Object.values(evt.gaintag_map).flat().includes("mbxuehen_sha");
          });
        },
        async content(event, trigger, player2) {
          if (trigger.name == "useCard") {
            const name = event.triggername;
            if (name == "useCard1") {
              trigger.addCount = false;
              const stat = player2.getStat().card, name2 = trigger.card.name;
              if (typeof stat[name2] === "number") {
                stat[name2]--;
              }
            }
            if (name == "useCardAfter") {
              await player2.draw();
            }
          } else {
            player2.removeGaintag(event.name);
          }
        }
      }
    }
  },
  mbhuxiao: {
    audio: "huxiao",
    enable: "phaseUse",
    usable: 1,
    chooseButton: {
      dialog(event, player2) {
        var dialog = ui.create.dialog("虎啸：选择一项", "hidden");
        dialog.add([
          [
            ["damage", "对一名体力值大于等于你的角色造成1点火焰伤害"],
            ["nodistance", "本回合使用牌无距离限制"],
            ["both", "背水！弃置一张红色牌，然后依次执行以上所有选项"]
          ],
          "textbutton"
        ]);
        return dialog;
      },
      filter(button) {
        const player2 = get.player();
        const { link } = button;
        return link !== "both" || player2.countDiscardableCards(player2, "h", (card2, player3) => get.color(card2, player3) == "red");
      },
      check(button) {
        switch (button.link) {
          case "damage":
            return 10;
          case "nodistance":
            return 5;
          case "both":
            return 15;
        }
      },
      backup(links) {
        return {
          audio: "huxiao",
          choice: links[0],
          async content(event, trigger, player2) {
            const choice = lib.skill.mbhuxiao_backup.choice;
            if (choice != "nodistance" && game.hasPlayer((target) => target.hp >= player2.hp)) {
              const result = await player2.chooseTarget(`虎啸：对一名体力值大于等于你的角色造成1点火焰伤害`, true, (card2, player3, target) => {
                return player3.hp <= target.hp;
              }).set("ai", (target) => get.damageEffect(target, get.player(), get.player(), "fire")).forResult();
              if (result?.targets) {
                const target = result.targets[0];
                player2.line(target, "fire");
                await target.damage("fire");
              }
            }
            if (choice != "damage") {
              player2.addTempSkill("mbhuxiao_effect");
            }
            if (choice == "both" && player2.countDiscardableCards(player2, "h", (card2, player3) => get.color(card2, player3) == "red")) {
              await player2.chooseToDiscard(`虎啸：请弃置一张红色牌`, true, (card2, player3) => get.color(card2, player3) == "red");
            }
          }
        };
      },
      prompt(links) {
        switch (links[0]) {
          case "damage":
            return "对一名体力值大于等于你的角色造成1点火焰伤害";
          case "discard":
            return "本回合使用牌无距离限制";
          case "both":
            return "背水！对一名体力值大于等于你的角色造成1点火焰伤害且本回合使用牌无距离限制，然后弃置一张红色牌";
        }
      }
    },
    ai: {
      order: 9,
      result: { player: 1 }
    },
    subSkill: {
      backup: {},
      effect: {
        charlotte: true,
        mod: { targetInRange: () => true },
        intro: { content: "本回合使用牌无距离限制" },
        mark: true
      }
    }
  },
  mbwuji: {
    audio: "wuji",
    enable: "phaseUse",
    filter(event, player2, name) {
      return player2.hasSkill("mbhuxiao");
    },
    skillAnimation: true,
    animationColor: "orange",
    limited: true,
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      if (player2.hasSkill("mbxuehen")) {
        player2.setStorage("mbxuehen", true, true);
        player2.when({ player: "phaseUseEnd" }).filter((evt) => event.getParent("phaseUse") == evt).step(async () => {
          const num = player2.getHistory("gain", (evt) => {
            return evt.getParent(2)?.name == "mbxuehen_sha" && evt.cards?.length;
          }).reduce((sum, evt) => sum + evt.cards.length, 0);
          if (num < 2) {
            player2.removeStorage("mbxuehen", true);
          }
        });
      }
    },
    ai: {
      combo: "mbhuxiao",
      order: 10,
      result: { player: 1 }
    },
    derivation: "mbxuehen_rewrite"
  },
  //手杀邢道荣
  mbkuangwu: {
    audio: 4,
    trigger: { global: "phaseUseBegin" },
    filter(event, player2) {
      return player2 !== event.player && player2.countCards("h") !== event.player.countCards("h");
    },
    logAudio: (index) => typeof index === "number" ? "mbkuangwu" + index + ".mp3" : 2,
    async cost(event, trigger, player2) {
      const target = trigger.player, num = Math.min(5, Math.max(1, target.countCards("h"))), hs = player2.countCards("h"), prompt = get.prompt(event.skill, target);
      const effect = (() => {
        const juedou = new lib.element.VCard({ name: "juedou" });
        const juedouEff = get.effect(target, juedou, player2, player2);
        const loseEff = get.effect(player2, { name: "losehp" }, player2, player2);
        if (!player2.canUse(juedou, target, false)) {
          return loseEff;
        }
        return juedouEff + (juedouEff >= 0 ? 0 : loseEff);
      })();
      if (hs > target.countCards("h") && hs > num) {
        event.result = await player2.chooseToDiscard(prompt, player2.countCards("h") - num, "allowChooseAll").set("effect", effect).set("ai", (card2) => {
          const { player: player3, selectCard, effect: effect2 } = get.event();
          let cards2 = player3.getDiscardableCards(player3, "h");
          const select = selectCard?.[1] ?? 1;
          if (cards2.length < select) {
            return 0;
          }
          cards2.sort((a, b) => get.value(a) - get.value(b));
          return cards2.slice(0, select).reduce((s, c) => s + get.value(c), 0) + effect2 > 0 && cards2.includes(card2) ? 1 : 0;
        }).set("prompt2", "将手牌数弃至" + get.cnNumber(num) + "张，视为对" + get.translation(target) + "使用【决斗】").set("chooseonly", true).forResult();
      } else {
        let str = `视为对${get.translation(target)}使用【决斗】`;
        if (hs < num) {
          str = `将手牌摸至${get.cnNumber(num)}张，然后${str}`;
        }
        event.result = await player2.chooseBool(prompt, str).set(
          "choice",
          (() => {
            const count = Math.max(0, num - hs);
            return get.effect(player2, { name: "draw" }, player2, player2) * count + effect >= 0;
          })()
        ).forResult();
      }
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      const target = trigger.player;
      if (event.cards?.length) {
        await player2.discard(event.cards);
      } else {
        await player2.drawTo(Math.min(5, target.countCards("h")));
      }
      player2.when({ global: "useCardAfter" }, false).filter((evt, player3) => player3.getStorage("mbkuangwu").includes(evt.card)).assign({ firstDo: true }).step(async (event2, trigger2, player3) => {
        player3.unmarkAuto("mbkuangwu", [trigger2.card]);
        if (target.hasHistory("damage", (evtx) => trigger2 === evtx?.getParent(2))) {
          return;
        }
        player3.logSkill("mbkuangwu", null, null, null, [get.rand(3, 4)]);
        await player3.loseHp();
        player3.tempBanSkill("mbkuangwu", "roundStart");
      }).finish();
      const juedou = new lib.element.VCard({ name: "juedou", isCard: true });
      if (player2.canUse(juedou, target)) {
        await player2.useCard(juedou, target).set("oncard", () => {
          const event2 = get.event(), { card: card2, player: player3 } = event2;
          player3.markAuto("mbkuangwu", [card2]);
        });
      }
    }
  },
  //吴珂
  mbzhuguo: {
    audio: 3,
    logAudio: (index) => typeof index === "number" ? "mbzhuguo" + index + ".mp3" : 2,
    usable: 1,
    enable: "phaseUse",
    filterTarget: true,
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const num = Math.min(5, target.maxHp) - target.countCards("h");
      if (num > 0) {
        const next = target.draw(num);
        await next;
        const targets2 = game.filterPlayer((current) => current != player2 && current != target);
        if (target.isMaxHandcard() && targets2.length) {
          const result = await player2.chooseTarget(`助国：你可以选择一名角色，令${get.translation(target)}选择是否对其使用一张无距离和次数限制的【杀】`, (card2, player3, target2) => {
            return get.event().targets.includes(target2);
          }).set("ai", (target2) => {
            const { player: player3, targetx } = get.event();
            return get.effect(target2, { name: "sha" }, targetx, player3);
          }).set("targets", targets2).set("targetx", target).forResult();
          if (result?.bool) {
            player2.logSkill(event.name, [result.targets[0]], null, null, [3]);
            await target.chooseToUse(
              function(card2, player3, event2) {
                return get.name(card2, player3) === "sha" && lib.filter.filterCard.apply(this, arguments);
              },
              `助国：是否对${get.translation(result.targets[0])}使用【杀】？`
            ).set("filterTarget", function(card2, player3, target2) {
              const sourcex = get.event().sourcex;
              if (target2 != sourcex && !ui.selected.targets.includes(sourcex)) {
                return false;
              }
              return lib.filter.targetEnabled.apply(this, arguments);
            }).set("addCount", false).set("sourcex", result.targets[0]);
          }
        }
      } else {
        if (num < 0 && target.hasDiscardableCards(target, "h")) {
          await target.chooseToDiscard("h", -num, true, "allowChooseAll");
        }
        await target.recover();
      }
    },
    ai: {
      order: 8,
      result: {
        target(player2, target) {
          const att = get.sgnAttitude(player2, target);
          const num = target.maxHp - target.countCards("h");
          if (att > 0) {
            if (num < 0 && target.isDamaged()) {
              if (target.maxHp >= 3 && num >= -2) {
                return 1.1;
              }
              return 0;
            }
            return num;
          } else {
            if (num >= 0) {
              return 0;
            }
            return num;
          }
        }
      }
    }
  },
  mbanda: {
    audio: 2,
    trigger: { global: "dying" },
    round: 1,
    check: (event, player2) => get.attitude(player2, event.player) > 0,
    filter: (event) => event.getParent().name == "damage" && event.getParent().source?.isIn(),
    logTarget: "player",
    async content(event, trigger, player2) {
      const source = trigger.getParent().source;
      trigger.player.line(source);
      const result = await source.chooseToGive(
        "谙达：交给" + get.translation(trigger.player) + "两张不同颜色牌，否则其回复1点体力",
        (card2, source2) => {
          const selected = ui.selected.cards;
          if (!selected.length) {
            return true;
          }
          const targetColor = get.color(card2, source2);
          return !selected.some((selectedCard) => get.color(selectedCard, source2) === targetColor);
        },
        "he",
        2,
        trigger.player
      ).set("complexCard", true).set("ai", (card2) => {
        const player3 = get.player(), source2 = get.event().source;
        if (["tao", "jiu"].includes(get.name(card2, source2))) {
          return 0;
        }
        if (get.attitude(player3, source2) > 0) {
          return 11 - get.value(card2);
        }
        return 7 - get.value(card2);
      }).set("source", source).forResult();
      if (!result.bool) {
        await trigger.player.recover();
      }
    }
  },
  //玄司马昭
  //若为？若为！若为~
  mbxiezheng: {
    audio: "jsrgxiezheng",
    inherit: "jsrgxiezheng",
    initGroup: "wei",
    async cost(event, trigger, player2) {
      const mode = get.mode();
      event.result = await player2.chooseTarget(
        get.prompt2(event.skill),
        (card2, player3, target) => {
          return target.countCards("h");
        },
        mode === "doudizhu" ? [1, 2] : 1
      ).set("ai", (target) => {
        const player3 = get.player();
        if (!player3.hasValueTarget({ name: "binglinchengxiax", isCard: true, mbxiezheng: true })) {
          return 0;
        }
        let val = 0;
        if (ui.selected.targets.length) {
          val -= get.sgnAttitude(player3, target);
        }
        val += get.sgnAttitude(player3, target);
        if (target.mayHaveSha(player3, null, null, "odds") > 0.5) {
          val *= 2;
        }
        return val;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const mode = get.mode();
      if (mode === "doudizhu") {
        player2.tempBanSkill(event.name, "forever", false);
      }
      for (const target of event.targets.sortBySeat()) {
        if (target.countCards("h")) {
          const cards2 = target.getCards("h").randomGets(1);
          target.$throw(1, 1e3);
          game.log(target, "将", "#y一张手牌", "置于了牌堆顶");
          await target.lose(cards2, ui.cardPile, "insert");
          game.updateRoundNumber();
        }
      }
      const card2 = { name: "binglinchengxiax", isCard: true, mbxiezheng: true };
      if (player2.hasUseTarget(card2)) {
        await player2.chooseUseTarget(card2, true);
      }
      if (!game.hasPlayer2((current) => {
        return current.hasHistory("damage", (evt) => evt.getParent(card2.name)?.card?.mbxiezheng);
      }, true)) {
        await player2.loseHp();
      }
    },
    locked: false,
    mod: {
      playerEnabled(card2, player2, target) {
        const mode = get.mode();
        if (mode !== "identity" || !card2.mbxiezheng || player2.storage.mbzhaoxiong || target.group === player2.group) {
          return;
        }
        if (game.hasPlayer((current) => current.group === player2.group && player2.canUse(card2, current))) {
          return false;
        }
      }
    }
  },
  mbqiantun: {
    audio: "jsrgqiantun",
    logAudio: (index) => `jsrgqiantun${typeof index == "number" ? index : get.rand(1, 2)}.mp3`,
    inherit: "jsrgqiantun",
    filter(event, player2) {
      return player2.group === "wei" && game.hasPlayer((target) => get.info("mbqiantun").filterTarget(null, player2, target));
    },
    groupSkill: "wei",
    async content(event, trigger, player2) {
      const target = event.target, cards2 = target.getCards("h").sort((a, b) => get.number(a, target) - get.number(b, target));
      const result = await target.chooseCard("展示任意张手牌，只能用这些牌拼点", [1, Infinity], "h", true, "allowChooseAll").set("maxNum", get.number(cards2[cards2.length - 1], target)).set("minNum", get.number(cards2[0], target)).set("ai", (card2) => {
        const { player: player3, maxNum, minNum } = get.event();
        if (maxNum > 12) {
          return 2;
        }
        if (minNum < 2) {
          if (get.number(card2, player3) == minNum) {
            return 2;
          }
          return 0;
        }
        if ([minNum, maxNum].some((num) => get.number(card2, player3) == num)) {
          return 1;
        }
        return Math.random() - 0.5;
      }).forResult();
      if (!result.bool) {
        return;
      }
      await target.showCards(result.cards);
      target.addGaintag(result.cards, "mbqiantun_tag");
      const next = player2.chooseToCompare(target);
      next.set("filterCard", (card2, player3) => {
        const bool = (cardx) => cardx.hasGaintag("mbqiantun_tag");
        return !player3?.countCards("h", bool) || bool(card2);
      });
      if (target.countCards("h") + 1 > result.cards.length * 2) {
        next.set("small", true);
      }
      const result3 = await next.forResult();
      target.removeGaintag("mbqiantun_tag");
      const mode = get.mode();
      if (result3.winner == player2) {
        player2.logSkill("mbqiantun", [target], null, null, [3]);
        const cards3 = target.getCards("h", (card2) => result.cards.includes(card2));
        if (cards3.length) {
          if (mode !== "doudizhu") {
            await target.give(cards3, player2);
          } else {
            const result2 = cards3.length > 2 ? await player2.chooseButton([get.translation(event.name) + "：请选择你要获得的牌", cards3], 2, true).set("ai", (button) => get.value(button.link)).forResult() : { bool: true, links: cards3 };
            if (result2?.bool && result2.links?.length) {
              await target.give(result2.links, player2);
            }
          }
        }
      } else {
        player2.logSkill("mbqiantun", [target], null, null, [4]);
        const cards3 = target.getCards("h", (card2) => !result.cards.includes(card2));
        if (cards3.length) {
          if (mode !== "doudizhu") {
            await target.give(cards3, player2);
          } else {
            const result2 = cards3.length > 2 ? await player2.chooseButton(
              [
                get.translation(event.name) + "：请选择你要获得的牌",
                (() => {
                  if (player2.hasSkillTag("viewHandcard", null, target, true)) {
                    return cards3;
                  }
                  return [cards3.slice().randomSort(), "blank"];
                })()
              ],
              2,
              true
            ).set("ai", (button) => get.value(button.link)).forResult() : { bool: true, links: cards3 };
            if (result2?.bool && result2.links?.length) {
              await target.give(result2.links, player2);
            }
          }
        }
      }
      await player2.showHandcards(get.translation(player2) + "发动了【谦吞】");
    }
  },
  mbzhaoxiong: {
    audio: "jsrgzhaoxiong",
    inherit: "jsrgzhaoxiong",
    filter(event, player2) {
      return player2.isDamaged();
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      player2.changeSkin({ characterName: "mb_simazhao" }, "jin_jsrg_simazhao");
      await player2.changeGroup("qun");
      await player2.addSkills("mbdangyi");
    },
    persevereSkill: true,
    derivation: "mbdangyi"
  },
  mbweisi: {
    audio: "jsrgweisi",
    logAudio: (index) => `jsrgweisi${typeof index == "number" ? index : get.rand(1, 2)}.mp3`,
    inherit: "jsrgweisi",
    filter(event, player2) {
      return player2.group === "qun" && game.hasPlayer((target) => get.info("mbweisi").filterTarget(null, player2, target));
    },
    groupSkill: "qun",
    async content(event, trigger, player2) {
      const { target } = event;
      if (target.countCards("h")) {
        const result = await target.chooseCard("将任意张手牌移出游戏直到本回合结束", [1, Infinity], "h", "allowChooseAll").set("ai", (card3) => {
          const { numx, player: player3 } = get.event();
          if (player3.countCards("h", "sha") <= numx) {
            return 9;
          }
          if (get.name(card3, player3) == "sha") {
            return 0;
          }
          return 5;
        }).set("numx", player2.countCards("h") / 4).forResult();
        if (result.bool) {
          const next = target.addToExpansion(result.cards, "giveAuto", target);
          next.gaintag.add("mbweisi");
          await next;
          target.when({
            global: ["phaseBefore", "phaseAfter"]
          }).step(async (event2, trigger2, player3) => {
            const cards2 = player3.getExpansions("mbweisi");
            if (cards2.length) {
              await player3.gain(cards2, "draw");
              game.log(player3, "收回了" + get.cnNumber(cards2.length) + "张“威肆”牌");
            }
          });
        }
      }
      const card2 = { name: "juedou", isCard: true };
      player2.when({
        source: "damageSource"
      }).filter((evt) => evt.getParent(event.name) == event).step(async (event2, trigger2, player3) => {
        const cards2 = trigger2.player.getCards("h");
        if (cards2.length) {
          player3.logSkill("mbweisi", [trigger2.player], null, null, [3]);
          const mode = get.mode();
          if (mode !== "doudizhu") {
            await trigger2.player.give(cards2, player3);
          } else {
            await player3.gainPlayerCard(trigger2.player, "h", true);
          }
        }
      });
      if (player2.canUse(card2, target)) {
        await player2.useCard(card2, target);
      }
    }
  },
  mbdangyi: {
    audio: "jsrgdangyi",
    inherit: "jsrgdangyi",
    filter(event, player2) {
      return player2.countMark("mbdangyi_used") < player2.countMark("mbdangyi");
    },
    usable: 1,
    persevereSkill: true
  },
  //牢又寄双雄
  //友崔均	子右：这玩意真有人玩吗
  friendshunyi: {
    audio: 2,
    trigger: { player: "useCard" },
    getIndex(event, player2) {
      const evt = player2.getHistory("lose", (evt2) => (evt2.relatedEvent || evt2.getParent()) === event)[0];
      return evt?.hs ?? [];
    },
    filter(event, player2, name, card2) {
      const hs = player2.getHistory("lose", (evt) => (evt.relatedEvent || evt.getParent()) === event)[0].hs;
      const suit = get.suit(card2, player2), number = get.number(card2, player2);
      if (!["heart"].concat(player2.getStorage("friendgongli_cuijun_shunyi")).includes(suit)) {
        return false;
      }
      if (typeof number !== "number" || number <= player2.countHistory("useSkill", (evt) => evt.skill == "friendshunyi")) {
        return false;
      }
      const cards2 = [...hs, ...player2.getCards("h")].unique().filter((i) => {
        return i !== card2 && typeof get.number(i, player2) === "number";
      });
      return !cards2.length || number <= Math.min(...cards2.map((i) => get.number(i, player2)));
    },
    prompt2(event, player2, name, card2) {
      return "将所有" + get.translation(get.suit(card2, player2)) + "的牌扣置于武将牌上直到回合结束，然后摸一张牌";
    },
    check(event, player2, name, card2) {
      const suit = get.suit(card2, player2), names = player2.getCards("h", (i) => get.suit(i, player2) === suit).map((i) => get.name(i, player2)).unique();
      let used = [];
      for (const name2 of names) {
        let cards2 = player2.getCards("h", { name: name2 });
        cards2.sort((a, b) => player2.getUseValue(b) - player2.getUseValue(a));
        used.addArray(cards2.slice(0, player2.getCardUsable(name2)));
      }
      return get.effect(player2, { name: "draw" }, player2, player2) >= used.reduce((sum, i) => sum + player2.getUseValue(i), 0);
    },
    async content(event, trigger, player2) {
      player2.addTempSkill("friendshunyi_effect");
      const cards2 = player2.getCards("h", { suit: get.suit(event.indexedData) });
      if (cards2.length) {
        const next = player2.addToExpansion(cards2, player2, "giveAuto");
        next.gaintag.add("friendshunyi_effect");
        await next;
      }
      await player2.draw();
    },
    subSkill: {
      effect: {
        charlotte: true,
        trigger: { global: "phaseEnd" },
        filter(event, player2) {
          return player2.getExpansions("friendshunyi_effect").length > 0;
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const cards2 = player2.getExpansions(event.name);
          await player2.gain(cards2, "draw");
          game.log(player2, "收回了" + get.cnNumber(cards2.length) + "张牌");
          player2.removeSkill(event.name);
        },
        intro: {
          markcount: "expansion",
          mark(dialog, storage, player2) {
            const cards2 = player2.getExpansions("friendshunyi_effect");
            if (player2.isUnderControl(true)) {
              dialog.addAuto(cards2);
            } else {
              return "共有" + get.cnNumber(cards2.length) + "张牌";
            }
          }
        }
      }
    }
  },
  friendbiwei: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player2) {
      const info = get.info("friendbiwei");
      return player2.hasCard((card2) => info.filterCard(card2, player2), "h") && game.hasPlayer((target) => info.filterTarget(null, player2, target));
    },
    filterCard(card2, player2) {
      const number = get.number(card2, player2);
      if (!lib.filter.cardDiscardable(card2, player2) || typeof number !== "number") {
        return false;
      }
      const cards2 = player2.getCards("h", (i) => i !== card2 && typeof get.number(i, player2) === "number");
      return !cards2.length || number >= Math.max(...cards2.map((i) => get.number(i, player2)));
    },
    position: "h",
    filterTarget(card2, player2, target) {
      return target !== player2 && target.countCards("h");
    },
    usable: 1,
    check: (card2) => 10 - get.value(card2),
    async content(event, trigger, player2) {
      const {
        cards: [card2],
        target
      } = event, number = get.number(card2, player2);
      const cards2 = target.getDiscardableCards(target, "h", (i) => typeof get.number(i) === "number" && get.number(i) >= number);
      if (cards2.length) {
        await target.discard(cards2);
      } else {
        delete player2.getStat("skill")[event.name];
      }
    },
    ai: {
      order: 10,
      result: {
        target(player2, target) {
          return get.effect(target, { name: "guohe_copy", position: "h" }, player2, target);
        }
      }
    }
  },
  friendgongli_cuijun: {
    audio: 2,
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player2) {
      if (lib.suit.every((suit) => suit === "heart" || player2.getStorage("friendgongli_cuijun_shunyi").includes(suit))) {
        return false;
      }
      if (!game.hasPlayer((target) => lib.characterSort?.mobile?.mobile_laoyouji?.some((name) => get.is.playerNames(target, name)))) {
        return false;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    forced: true,
    async content(event, trigger, player2) {
      const num = game.countPlayer((target) => lib.characterSort?.mobile?.mobile_laoyouji?.some((name) => get.is.playerNames(target, name)));
      const suits = lib.suit.filter((suit) => !(suit === "heart" || player2.getStorage("friendgongli_cuijun_shunyi").includes(suit))).reverse().map((suit) => "lukai_" + suit);
      const choices = suits.length > num ? (await player2.chooseButton(["共砺：请选择可令〖顺逸〗触发的额外花色", [suits, "vcard"]], true, [1, num]).set("ai", () => 1 + Math.random()).forResult()).links : suits.map((suit) => ["", "", suit]);
      if (choices?.length) {
        player2.addSkill("friendgongli_cuijun_shunyi");
        player2.markAuto(
          "friendgongli_cuijun_shunyi",
          choices.map((i) => i[2].slice("lukai_".length))
        );
      }
    },
    subSkill: {
      shunyi: {
        charlotte: true,
        onremove: true,
        intro: { content: "可因$花色触发〖顺逸〗" }
      }
    },
    ai: { combo: "friendshunyu" }
  },
  //友石韬
  friendqinying: {
    audio: 4,
    logAudio: () => 2,
    inherit: "dcctjiuxian",
    selectCard: [1, Infinity],
    allowChooseAll: true,
    position: "he",
    async content(event, trigger, player2) {
      await player2.recast(event.cards);
      player2.addTempSkill("friendqinying_effect");
      const card2 = new lib.element.VCard({ name: "juedou", isCard: true, storage: { friendqinying: event.cards.length } });
      await player2.chooseUseTarget(card2, true);
    },
    ai: {
      order(item, player2) {
        return 0.9 * get.order({ name: "juedou" }, player2);
      },
      tag: {
        respond: 2,
        respondSha: 2,
        damage: 1
      },
      result: {
        player(player2) {
          const card2 = new lib.element.VCard({ name: "juedou", isCard: true, storage: { friendqinying: true } });
          let target = null, maxval = 0;
          for (let i of game.filterPlayer()) {
            if (!player2.canUse(card2, i)) {
              continue;
            }
            let jdeff = get.effect(i, card2, player2, player2);
            if (jdeff < 0) {
              continue;
            }
            if (jdeff / 5 > maxval) {
              target = i;
              maxval = jdeff / 5;
            }
          }
          if (target) {
            return maxval / 80;
          }
          return 0;
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        global: "friendqinying_global"
      },
      global: {
        charlotte: true,
        enable: "chooseToRespond",
        filter(event, player2) {
          if (event.friendqinying || !(Array.isArray(event.respondTo) && (event.respondTo[1]?.storage?.friendqinying ?? 0) > 0)) {
            return false;
          }
          const source = event.respondTo[0], types = source.getStorage("friendgongli_shitao_qinying");
          return player2.hasCard((card2) => lib.filter.cardDiscardable(card2, player2) && !types.includes(get.type2(card2)), "hej");
        },
        filterCard: () => false,
        selectCard: [-2, -1],
        prompt() {
          const event = get.event();
          const source = event.respondTo[0], types = source.getStorage("friendgongli_shitao_qinying");
          return '<span class="text center">' + ["此流程还可发动" + event.respondTo[1].storage.friendqinying + "次本效果", "弃置区域里的一张" + (types.length > 0 ? "非" + get.translation(types) : "") + "牌，视为打出【杀】"].map((str) => "※" + str).join("<br>") + "</span>";
        },
        log: false,
        viewAs: { name: "sha" },
        async precontent(evt, trigger, player2) {
          const event = evt.getParent(), types = event.respondTo[0].getStorage("friendgongli_shitao_qinying");
          const { bool } = await player2.discardPlayerCard(player2, "hej", true).set("types", types).set(
            "prompt",
            (() => {
              return '###钦英###<div class="text center">弃置区域里的一张' + (types.length > 0 ? "非" + get.translation(types) : "") + "牌，视为打出【杀】</div>";
            })()
          ).set("logSkill", ["friendqinying", null, null, null, [get.rand(3, 4)]]).set("filterButton", (button) => !get.event().types.includes(get.type2(button.link))).forResult();
          if (bool) {
            event.respondTo[1].storage.friendqinying--;
            game.broadcastAll(
              (card2, storage) => {
                card2.storage = storage;
              },
              event.respondTo[1],
              event.respondTo[1].storage
            );
          } else {
            event.set("friendqinying", true);
            event.goto(0);
          }
        },
        ai: {
          order(item, player2) {
            const card2 = new lib.element.VCard({ name: "shunshou" });
            return get.effect(player2, card2, player2, player2) > 0 ? get.order(card2, player2) : 0.1;
          },
          respondSha: true,
          skillTagFilter(player2, tag, arg) {
            if (arg === "use") {
              return false;
            }
            const event = get.event();
            if (event.friendqinying || !(Array.isArray(event?.respondTo) && (event.respondTo[1]?.storage?.friendqinying ?? 0) > 0)) {
              return false;
            }
            const source = event.respondTo[0], types = source.getStorage("friendgongli_shitao_qinying");
            return player2.hasCard((card2) => lib.filter.cardDiscardable(card2, player2) && !types.includes(get.type2(card2)), "hej");
          }
        }
      }
    }
  },
  friendlunxiong: {
    audio: 2,
    trigger: {
      player: "damageEnd",
      source: "damageSource"
    },
    filter(event, player2) {
      const cardx = player2.getCards("h", (card2) => typeof get.number(card2, player2) === "number");
      if (!cardx.length) {
        return false;
      }
      return cardx.some((card2) => {
        if (!lib.filter.cardDiscardable(card2, player2)) {
          return false;
        }
        const number = get.number(card2, player2);
        if (number < (player2.storage?.friendlunxiong ?? 0)) {
          return false;
        }
        const cards2 = cardx.slice().remove(card2);
        return !cards2.length || number >= Math.max(...cards2.map((i) => get.number(i, player2)));
      });
    },
    prompt2(event, player2) {
      const cardx = player2.getCards("h", (card3) => typeof get.number(card3, player2) === "number");
      const card2 = cardx.find((card3) => {
        if (!lib.filter.cardDiscardable(card3, player2)) {
          return false;
        }
        const number2 = get.number(card3, player2);
        if (number2 < (player2.storage?.friendlunxiong ?? 0)) {
          return false;
        }
        const cards2 = cardx.slice().remove(card3);
        return !cards2.length || number2 >= Math.max(...cards2.map((i) => get.number(i, player2)));
      }), number = get.number(card2, player2);
      return "弃置" + get.translation(card2) + "并摸三张牌，本局游戏发动此技能弃置牌的点数须大于等于" + number;
    },
    check(event, player2) {
      const cardx = player2.getCards("h", (card3) => typeof get.number(card3, player2) === "number");
      const card2 = cardx.find((card3) => {
        if (!lib.filter.cardDiscardable(card3, player2)) {
          return false;
        }
        const number = get.number(card3, player2);
        if (number < (player2.storage?.friendlunxiong ?? 0)) {
          return false;
        }
        const cards2 = cardx.slice().remove(card3);
        return !cards2.length || number >= Math.max(...cards2.map((i) => get.number(i, player2)));
      });
      return get.effect(player2, { name: "draw" }, player2, player2) * 3 > get.value(card2, player2);
    },
    async content(event, trigger, player2) {
      const cardx = player2.getCards("h", (card3) => typeof get.number(card3, player2) === "number");
      if (!cardx.length) {
        return;
      }
      const card2 = cardx.find((card3) => {
        if (!lib.filter.cardDiscardable(card3, player2)) {
          return false;
        }
        const number = get.number(card3, player2);
        if (number < (player2.storage?.friendlunxiong ?? 0)) {
          return false;
        }
        const cards2 = cardx.slice().remove(card3);
        return !cards2.length || number >= Math.max(...cards2.map((i) => get.number(i, player2)));
      });
      if (card2) {
        const number = get.number(card2, player2);
        await player2.discard(card2);
        await player2.draw(3);
        player2.storage[event.name] = number;
        player2.markSkill(event.name);
      }
    },
    intro: { content: "发动〖论雄〗弃置牌的点数须大于等于#" }
  },
  friendgongli_shitao: {
    audio: 2,
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player2) {
      if (lib.inpile.map((i) => get.type2(i)).every((type) => player2.getStorage("friendgongli_shitao_qinying").includes(type))) {
        return false;
      }
      if (!game.hasPlayer((target) => lib.characterSort?.mobile?.mobile_laoyouji?.some((name) => get.is.playerNames(target, name)))) {
        return false;
      }
      return event.name !== "phase" || game.phaseNumber === 0;
    },
    forced: true,
    async content(event, trigger, player2) {
      const num = game.countPlayer((target) => lib.characterSort?.mobile?.mobile_laoyouji?.some((name) => get.is.playerNames(target, name)));
      const types = lib.inpile.map((i) => get.type2(i)).unique().filter((type) => !player2.getStorage("friendgongli_shitao_qinying").includes(type));
      const choices = types.length > num ? (await player2.chooseButton(["共砺：请选择不可令〖钦英〗弃置的类别", [types.map((type) => [type, get.translation(type)]), "tdnodes"]], true, [1, num]).set("ai", () => 1 + Math.random()).forResult()).links : types;
      if (choices?.length) {
        player2.addSkill("friendgongli_shitao_qinying");
        player2.markAuto("friendgongli_shitao_qinying", choices);
      }
    },
    subSkill: {
      qinying: {
        charlotte: true,
        onremove: true,
        intro: { content: "不可令〖钦英〗弃置$类别的牌" }
      }
    },
    ai: { combo: "friendqinying" }
  },
  //孩子们，我来上班了
  //清河公主
  mbzengou: {
    audio: 3,
    enable: "phaseUse",
    filter(event, player2) {
      return game.hasPlayer((current) => get.info("mbzengou").filterTarget(null, player2, current));
    },
    filterTarget(card2, player2, target) {
      if (player2.getStorage("mbfeili_effect").includes(target)) {
        return false;
      }
      return target !== player2 && target.countCards("h");
    },
    usable: 1,
    async content(event, trigger, player2) {
      const target = event.target;
      player2.chat(get.translation(target) + "也干了");
      await game.delayx();
      target.chat("孩子我" + (["xizhicai", "xiahoumao"].some((i) => get.is.playerNames(target, i)) ? "也干了" : "没干"));
      await game.delayx();
      await player2.viewHandcards(target);
      let names = lib.inpile.filter((name) => get.type(name) === "basic");
      const allNames = player2.getCards("h", (card2) => target.hasCards("h", { name: get.name(card2) })).map((card2) => get.name(card2)).unique();
      const list = get.inpileVCardList((info) => {
        if (info[0] !== "basic") {
          return false;
        }
        if (info[3]) {
          return false;
        }
        return !target.hasCards("h", { name: info[2] });
      });
      const goon = list.length > 0;
      const listx = names.filter((name) => !target.hasCards("h", { name }));
      const choiceList = [];
      choiceList.push(goon ? `视为使用两张${listx.map((name) => `【${get.translation(name)}】`).join("、")}${listx.length > 1 ? "中的牌" : ""}（不计入次数且无次数限制）` : "视为使用空气");
      choiceList.push(allNames.length ? `将你与其手牌中的${allNames.map((name) => `【${get.translation(name)}】`).join("、")}替换为牌堆中等量的【杀】且你的这些牌不计入手牌上限直到你的结束阶段` : "替换空气");
      let result = await player2.chooseControl().set("choiceList", choiceList).set("ai", () => {
        const {
          player: player3,
          goon: goon2,
          list: [target2, names2, allNames2]
        } = get.event();
        const list2 = get.inpileVCardList((info) => {
          if (info[0] !== "basic") {
            return false;
          }
          if (info[3]) {
            return false;
          }
          return !target2.hasCards("h", { name: info[2] });
        }).filter((info) => player3.hasUseTarget(new lib.element.VCard({ name: info[2], nature: info[3], isCard: true }), true, false));
        return Math.max(...list2.map((info) => player3.getUseValue(new lib.element.VCard({ name: info[2], nature: info[3], isCard: true }), false))) > (() => {
          let sum = 0;
          sum += target2.getCards("h", { name: allNames2 }).reduce((num, card2) => num + get.value(card2, target2), 0);
          sum -= player3.getCards("h", { name: allNames2 }).reduce((num, card2) => num + get.value(card2, player3), 0);
          return sum;
        })() * Math.sign(get.attitude(player3, target2)) ? 0 : 1;
      }).set("list", [target, names, allNames]).set("goon", goon).forResult();
      if (result?.index === 0) {
        const used = [];
        for (let i = 0; i < 2; i++) {
          let list2 = get.inpileVCardList((info) => {
            if (info[0] !== "basic" || used.includes(info[2])) {
              return false;
            }
            if (info[3]) {
              return false;
            }
            return !target.hasCards("h", { name: info[2] });
          }).filter((info) => player2.hasUseTarget(new lib.element.VCard({ name: info[2], nature: info[3], isCard: true }), true, false));
          if (!list2.length) {
            break;
          }
          const result2 = await player2.chooseButton([get.translation(event.name) + "：请选择你要视为使用的基本牌", [list2, "vcard"]], true).set("direct", true).set("ai", (button) => get.player().getUseValue(new lib.element.VCard({ name: button.link[2], nature: button.link[3], isCard: true }), false, false)).forResult();
          if (result2?.bool) {
            used.add(result2.links[0][2]);
            await player2.chooseUseTarget(new lib.element.VCard({ name: result2.links[0][2], nature: result2.links[0][3], isCard: true }), true, false);
          }
        }
      } else if (result?.index === 1) {
        const cards2 = [player2.getCards("h", { name: allNames }), target.getCards("h", { name: allNames })];
        await game.loseAsync({
          lose_list: [
            [player2, cards2[0]],
            [target, cards2[1]]
          ],
          position: ui.cardPile
        }).setContent("loseToDiscardpileMultiple");
        let gains = [[], []];
        for (let i = 0; i < cards2.length; i++) {
          while (gains[i].length < cards2[i].length) {
            const card2 = get.cardPile2((card3) => {
              if (gains.flat().includes(card3)) {
                return false;
              }
              return card3.name === "sha";
            });
            if (card2) {
              gains[i].push(card2);
            } else {
              break;
            }
          }
        }
        if (gains.flat().length) {
          player2.addTempSkill("mbzengou_effect", { player: "phaseJieshuBegin" });
          if (gains[1].length) {
            await game.loseAsync({
              gain_list: [
                [player2, gains[0]],
                [target, gains[1]]
              ],
              animate: "gain2",
              gaintag: ["mbzengou_effect"]
            }).setContent("gaincardMultiple");
          } else {
            await player2.gain(gains[0], "gain2");
          }
        }
      }
      names = names.filter((name) => !target.storage?.["mbzengou_debuff"]?.[name]);
      if (names.length && target.isIn()) {
        const choose = names.length > 1 ? (await player2.chooseControl(names).set("ai", () => {
          const { player: player3, target: target2, controls } = get.event();
          if (get.attitude(player3, target2) < 0) {
            const cards2 = target2.getCards("h", (card2) => get.type(card2) === "basic" && target2.hasUseTarget(card2));
            const names2 = cards2.map((card2) => get.name(card2));
            if (names2.includes("sha") && controls.includes("sha")) {
              return "sha";
            }
            if (names2.includes("tao") && controls.includes("tao")) {
              return "tao";
            }
          }
          return controls.randomGet();
        }).set("target", target).set("prompt", "请选择令" + get.translation(target) + "获得的“诬”标记名称").forResult()).control : names[0];
        if (choose) {
          player2.line(target);
          player2.popup(choose);
          target.addSkill("mbzengou_debuff");
          target.storage["mbzengou_debuff"][choose] = 1 + (target.storage["mbzengou_debuff"]?.[choose] || 0);
          target.addTip("mbzengou_debuff", `谮构 ${get.translation(Object.keys(target.storage["mbzengou_debuff"]))}`);
          target.markSkill("mbzengou_debuff");
        }
      }
    },
    ai: {
      order: 10,
      result: {
        target(player2, target) {
          return -target.countCards("h") - 0.5;
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove(player2, skill) {
          player2.removeGaintag(skill);
        },
        mod: {
          ignoredHandcard(card2, player2) {
            if (card2.hasGaintag("mbzengou_effect")) {
              return true;
            }
          },
          cardDiscardable(card2, player2, name) {
            if (name === "phaseDiscard" && card2.hasGaintag("mbzengou_effect")) {
              return false;
            }
          }
        }
      },
      debuff: {
        charlotte: true,
        init(player2, skill) {
          player2.storage[skill] = {};
        },
        onremove(player2, skill) {
          delete player2.storage[skill];
          player2.removeTip(skill);
        },
        mark: true,
        marktext: "诬",
        intro: {
          markcount(storage = {}) {
            return Object.keys(storage).reduce((sum, item) => sum + storage[item], 0);
          },
          content(storage) {
            if (!storage) {
              return "无效果";
            }
            return [
              `你每回合使用的前三张牌结算完毕后，若你拥有此牌名的“诬”标记，则你失去1点体力并移去“诬”标记。`,
              "“诬”标记：<br>" + Object.keys(storage).map((item) => {
                return get.translation(item) + "：" + storage[item] + "枚";
              }).join("<br>")
            ].map((str) => "<li>" + str).join("<br>");
          }
        },
        audio: "mbzengou",
        trigger: { player: "useCardAfter" },
        filter(event, player2) {
          if (player2.getHistory("useCard").indexOf(event) > 2) {
            return false;
          }
          return player2.storage["mbzengou_debuff"]?.[event.card.name] ?? 0 > 0;
        },
        forced: true,
        async content(event, trigger, player2) {
          await player2.loseHp();
          if (typeof player2.storage?.[event.name]?.[trigger.card.name] == "number" && player2.storage[event.name][trigger.card.name] > 0) {
            player2.storage[event.name][trigger.card.name]--;
            if (get.info(event.name).intro.markcount(player2.storage[event.name]) === 0) {
              player2.removeSkill(event.name);
              return;
            }
            if (player2.storage[event.name][trigger.card.name] === 0) {
              delete player2.storage[event.name][trigger.card.name];
            }
          }
          if (player2.storage[event.name]) {
            player2.markSkill(event.name);
            player2.addTip(event.name, `谮构 ${get.translation(Object.keys(player2.storage[event.name]))}`);
          }
        },
        mod: {
          aiOrder(player2, card2, num) {
            if (player2.getHistory("useCard").length > 0 || player2.storage["mbzengou_debuff"][card2.name] > 0) {
              return;
            }
            const effect = get.effect(player2, { name: "losehp" }, player2, player2);
            if (effect < 0) {
              return num / 1145141919810;
            }
            return num + 10 * Math.sign(effect);
          }
        }
      }
    },
    async loseToDiscardpileMultiple(event, trigger, player2) {
      event.visible = true;
      if (!event.position) {
        event.position = ui.discardPile;
      }
      var cards2 = [];
      event.cards = cards2;
      for (var i = 0; i < event.lose_list.length; i++) {
        var next = event.lose_list[i][0].lose(event.lose_list[i][1], event.position);
        game.log(event.lose_list[i][0], "将", event.lose_list[i][1], "置入了弃牌堆");
        next.animate = false;
        next.delay = false;
        cards2.addArray(event.lose_list[i][1]);
        next.getlx = false;
      }
      var evt = event;
      if (evt.animate != false) {
        evt.discardid = lib.status.videoId++;
        game.broadcastAll(
          function(list, id, cards3) {
            for (var i2 of list) {
              for (var j of i2[1]) {
                j.classList.remove("glow");
                j.classList.remove("glows");
              }
              i2[0].$throw(i2[1], null, "nobroadcast");
            }
            var cardnodes = [];
            cardnodes._discardtime = get.time();
            for (var ix of list) {
              ix[1];
              for (var i2 = 0; i2 < cards3.length; i2++) {
                if (cards3[i2].clone) {
                  cardnodes.push(cards3[i2].clone);
                }
              }
            }
            ui.todiscard[id] = cardnodes;
          },
          event.lose_list,
          evt.discardid,
          cards2
        );
        if (lib.config.sync_speed && cards2[0] && cards2[0].clone) {
          if (evt.delay != false) {
            var waitingForTransition = get.time();
            evt.waitingForTransition = waitingForTransition;
            cards2[0].clone.listenTransition(function() {
              if (_status.waitingForTransition == waitingForTransition && _status.paused) {
                game.resume();
              }
              delete evt.waitingForTransition;
            });
          } else if (evt.getParent().discardTransition) {
            delete evt.getParent().discardTransition;
            var waitingForTransition = get.time();
            evt.getParent().waitingForTransition = waitingForTransition;
            cards2[0].clone.listenTransition(function() {
              if (_status.waitingForTransition == waitingForTransition && _status.paused) {
                game.resume();
              }
              delete evt.getParent().waitingForTransition;
            });
          }
        }
      }
      if (event.delay != false) {
        if (event.waitingForTransition) {
          _status.waitingForTransition = event.waitingForTransition;
          game.pause();
        } else {
          await game.delayx();
        }
      }
    }
  },
  mbfeili: {
    audio: 2,
    trigger: { player: "damageBegin4" },
    filter(event, player2) {
      if (!player2.hasSkill("mbzengou", null, false, false)) {
        return false;
      }
      const source = event.source;
      return player2.countCards("he", (card2) => lib.filter.cardDiscardable(card2, player2)) >= 2 || source?.isIn() && source.hasSkill("mbzengou_debuff");
    },
    async cost(event, trigger, player2) {
      const source = trigger.source, str = get.translation(source || "");
      const goon1 = player2.countCards("he", (card2) => lib.filter.cardDiscardable(card2, player2)) >= 2;
      const goon2 = source?.isIn() && source.hasSkill("mbzengou_debuff");
      const result = await player2.chooseControl(
        ["弃牌", "移标记", "cancel2"].filter((item) => {
          if (item === "cancel2") {
            return true;
          }
          return item === "弃牌" && goon1 || item === "移标记" && goon2;
        })
      ).set("choiceList", ["弃置两张牌并防止此伤害", "移去" + str + "的“诬”标记并防止此伤害，然后你摸四张牌，本局游戏你不能再对其发动〖谮构〗"]).set("ai", () => {
        const { player: player3, controls } = get.event();
        const trigger2 = get.event().getTrigger();
        if (get.damageEffect(player3, trigger2.source, player3, trigger2.nature) * trigger2.num >= 0) {
          return "cancel2";
        }
        return controls[0];
      }).set("prompt", get.prompt2(event.skill)).forResult();
      switch (result.control) {
        case "弃牌":
          event.result = await player2.chooseToDiscard("弃置两张牌并防止此伤害", "he", 2, true).set("chooseonly", true).forResult();
          break;
        case "移标记":
          event.result = { bool: true, targets: [source] };
          break;
        default:
          event.result = { bool: false };
          break;
      }
    },
    async content(event, trigger, player2) {
      trigger.cancel();
      if (event.cards?.length) {
        await player2.discard(event.cards);
      }
      if (event.targets?.length) {
        const [target] = event.targets;
        target.removeSkill("mbzengou_debuff");
        await player2.draw(4);
        player2.addSkill("mbfeili_effect");
        player2.markAuto("mbfeili_effect", [target]);
      }
    },
    ai: { combo: "mbzengou" },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        intro: { content: "和$彻底闹掰，想谮构也没得谮构" }
      }
    }
  },
  //牢又寄 —— 庞统
  friendmanjuan: {
    audio: 2,
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    filter(event, player2) {
      if (player2.countMark("friendmanjuan_used") >= 5) {
        return false;
      }
      const cards2 = event.getg?.(player2) || [];
      if (cards2.length < 2 || !cards2.some((i) => get.owner(i) === player2 && ["h", "e"].includes(get.position(i)))) {
        return false;
      }
      return event.getParent().name != "friendmanjuan";
    },
    async cost(event, trigger, player2) {
      const cards2 = trigger.getg(player2).filter((i) => get.owner(i) === player2 && ["h", "e"].includes(get.position(i)));
      const { moved } = await player2.chooseToMove(get.prompt2(event.skill), "allowChooseAll").set("list", [
        ["牌堆顶", []],
        [["获得的牌"], cards2]
      ]).set("processAI", (list) => {
        let listx = list.map((i) => i[1]);
        let cards3 = listx[1].filter((card2) => {
          if (!get.discardPile((c) => get.type2(c) !== get.type2(card2))) {
            return false;
          }
          return get.info("zhiheng").check(card2) > 0;
        });
        cards3 = cards3.sort((a, b) => get.info("zhiheng").check(b) - get.info("zhiheng").check(a)).slice(0, 5);
        return [cards3, listx[0].removeArray(cards3)];
      }).forResult();
      event.result = {
        bool: Boolean(moved?.[0]?.length),
        cost_data: moved
      };
    },
    async content(event, trigger, player2) {
      player2.addTempSkill(event.name + "_used", "roundStart");
      player2.addMark(event.name + "_used", 1, false);
      const cards2 = event.cost_data[0];
      await player2.lose(cards2, ui.cardPile, "insert");
      const list = [];
      for (const i of cards2) {
        const card2 = get.cardPile((c) => {
          if (list.includes(c)) {
            return false;
          }
          return get.type2(c) != get.type2(i);
        }, "discardPile");
        if (card2) {
          list.add(card2);
        }
        if (list.length >= 5) {
          break;
        }
      }
      if (list.length) {
        await player2.gain(list, "gain2");
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true,
        intro: { content: "本轮已发动【漫卷】#次" }
      }
    }
  },
  friendyangming: {
    audio: 2,
    trigger: { player: "phaseUseEnd" },
    filter(event, player2) {
      return player2.getHistory("lose", (evt) => {
        return evt.getParent("phaseUse") === event;
      }).reduce((sum, evt) => sum + (evt.getl?.(player2)?.hs?.length ?? 0), 0) >= 2;
    },
    frequent: true,
    async content(event, trigger, player2) {
      let num = player2.hasSkill("friendpangtonggongli") && get.info("friendgongli").isFriendOf(player2, "friend_zhugeliang") ? 1 : 0;
      num += 4;
      const cards2 = get.cards(num, true);
      const next = player2.addToExpansion(cards2, "gain2");
      next.gaintag.add(event.name);
      await next;
      while (cards2.some((card2) => player2.hasUseTarget(card2, true, false))) {
        const result2 = await player2.chooseCardButton(cards2, "养名：请选择要使用的牌").set("filterButton", (button) => {
          const card2 = button.link;
          return get.player().hasUseTarget(card2, true, false);
        }).set("ai", (button) => {
          return get.player().getUseValue(button.link, true, false);
        }).forResult();
        if (result2.bool) {
          const card2 = result2.links[0];
          player2.$gain2(card2, false);
          await game.delayx();
          const result3 = await player2.chooseUseTarget(card2, true, false).forResult();
          if (result3.bool) {
            cards2.removeArray(cards2.filter((cardx) => get.suit(cardx) === get.suit(card2)));
            continue;
          } else {
            break;
          }
        } else {
          break;
        }
      }
      if (player2.hasSkill("friendpangtonggongli") && get.info("friendgongli").isFriendOf(player2, "friend_xushu") && cards2.length) {
        const result = await player2.chooseCardButton(cards2, "养名：是否获得其中一张牌？").set("ai", (button) => get.value(button.link)).forResult();
        if (result.bool) {
          await player2.gain(result.links, "gain2");
        }
      }
      await player2.loseToDiscardpile(player2.getExpansions(event.name));
    },
    intro: {
      markcount: "expansion",
      content: "expansion"
    },
    //group: "friendyangming_check",
    subSkill: {
      mark: {
        charlotte: true,
        onremove: true
      },
      check: {
        charlotte: true,
        trigger: {
          player: "loseAfter",
          global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
        },
        filter(event, player2) {
          if (!player2.isPhaseUsing() || player2.countCards("h")) {
            return false;
          }
          const storage = player2.getStorage("friendyangming_mark");
          return !storage.includes(event.getParent("phaseUse")) && event.getl(player2)?.cards2?.length > 0;
        },
        silent: true,
        async content(event, trigger, player2) {
          player2.addTempSkill("friendyangming_mark");
          player2.markAuto("friendyangming_mark", [trigger.getParent("phaseUse")]);
        }
      }
    }
  },
  friendpangtonggongli: {
    audio: 2,
    locked: true,
    ai: { combo: "friendyangming" }
  },
  //牢又寄 —— 徐庶
  friendxiaxing: {
    audio: 2,
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player2) {
      if (event.name === "phase") {
        return game.phaseNumber === 0;
      }
      return true;
    },
    forced: true,
    locked: true,
    async content(event, trigger, player2) {
      const card2 = game.createCard2("xuanjian", "spade", 9);
      await player2.gain(card2, "gain2");
      if (player2.hasUseTarget(card2) && player2.getCards("h").includes(card2) && get.name(card2, player2) == "xuanjian") {
        await player2.chooseUseTarget(card2, true, false);
      }
    },
    group: "friendxiaxing_gain",
    subSkill: {
      gain: {
        audio: "friendxiaxing",
        trigger: { global: ["loseEnd", "equipEnd", "addJudgeEnd", "gainEnd", "loseAsyncEnd", "addToExpansionEnd"] },
        filter(event, player2) {
          if (player2.getStorage("friendqihui").length < 2) {
            return false;
          }
          return event.getd()?.some((i) => i.name == "xuanjian");
        },
        async cost(event, trigger, player2) {
          const storage = player2.getStorage("friendqihui").sort((a, b) => b.indexOf("c") - a.indexOf("c")).map((i) => `caoying_${i}`);
          const gains = trigger.getd().filter((i) => i.name == "xuanjian");
          const { links, bool } = await player2.chooseButton(["###" + get.prompt("friendxiaxing") + '###<div class="text center">移去2枚“启诲”标记，获得' + get.translation(gains) + "</div>", [storage, "vcard"]], 2).set("ai", (button) => {
            const player3 = get.player(), type = button.link[2].slice(8);
            if (player3.getVEquip("xuanjian")) {
              return 0;
            }
            return 1 + Math.random() + player3.countCards("he", (card2) => {
              return get.type2(card2) === type && player3.hasValueTarget(card2);
            });
          }).forResult();
          if (bool && links?.length) {
            event.result = {
              bool,
              cost_data: links.map((i) => i[2].slice(8))
            };
          }
        },
        async content(event, trigger, player2) {
          const skillName = "friendqihui";
          const storage = player2.storage[skillName];
          player2.unmarkAuto(skillName, event.cost_data);
          if (storage.length) {
            player2.addTip(skillName, `${get.translation(skillName)} ${storage.map((i) => get.translation(i)[0]).join("")}`);
          } else {
            player2.removeTip(skillName);
          }
          await player2.gain(
            trigger.getd().filter((i) => i.name == "xuanjian"),
            "gain2"
          );
        }
      }
    }
  },
  friendqihui: {
    audio: 3,
    trigger: { player: "useCard" },
    filter(event, player2) {
      const storage = player2.getStorage("friendqihui");
      const type = get.type2(event.card);
      return get.numOf(storage, type) < (player2.countMark("friendqihui_limit") || 1);
    },
    forced: true,
    async content(event, trigger, player2) {
      const { name: skillName } = event;
      const type = get.type2(trigger.card);
      const storage = player2.storage[skillName];
      storage.push(type);
      player2.markSkill(skillName);
      player2.addTip(skillName, `${get.translation(skillName)} ${storage.map((i) => get.translation(i)[0]).join("")}`);
      if (storage.length >= 3) {
        const types = storage.sort((a, b) => b.indexOf("c") - a.indexOf("c")).map((i) => `caoying_${i}`);
        const { links } = await player2.chooseButton(["选择你要移去的“启诲”标记（移去3个标记上限+1）", [types, "vcard"]], [2, 3], true).set("ai", (button) => {
          const player3 = get.player(), type2 = button.link[2].slice(8);
          return 1 + Math.random() + player3.countCards("he", (card2) => {
            return get.type2(card2) === type2 && player3.hasValueTarget(card2);
          });
        }).forResult();
        if (!links?.length) {
          return;
        }
        player2.unmarkAuto(
          skillName,
          links.map((link) => link[2].slice(8))
        );
        if (!storage.length) {
          player2.removeTip(skillName);
        } else {
          player2.addTip(skillName, `${get.translation(skillName)} ${storage.map((i) => get.translation(i)[0]).join("")}`);
        }
        const result = await player2.chooseButton(
          [
            "启诲：请执行一项",
            [
              [
                ["recover", "回复1点体力"],
                ["draw", "摸两张牌"],
                ["use", "使用的下一张牌无任何次数限制"],
                ["drawx", "令一名其他角色摸三张牌"]
              ],
              "textbutton"
            ]
          ],
          true
        ).set("ai", (button) => {
          const player3 = get.player();
          if (button.link === "recover") {
            return get.recoverEffect(player3, player3, player3);
          }
          if (button.link === "draw") {
            return get.effect(player3, { name: "draw" }, player3, player3) * 2;
          }
          if (button.link === "drawx") {
            return Math.max(...game.filterPlayer((target) => target != player3).map((target) => get.effect(target, { name: "draw" }, player3, player3) * 2));
          }
          return Math.max(
            ...[0].concat(
              player3.getCards("he", (card2) => {
                return player3.hasValueTarget(card2, false);
              }).map((card2) => player3.getUseValue(card2, false))
            )
          );
        }).set("filterButton", (button) => {
          const player3 = get.player();
          if (button.link == "drawx") {
            return game.hasPlayer((target) => target != player3);
          }
          return button.link !== "recover" || player3.isDamaged();
        }).forResult();
        if (result.bool) {
          switch (result.links[0]) {
            case "recover":
              await player2.recover();
              break;
            case "draw":
              await player2.draw(2);
              break;
            case "drawx": {
              const result2 = await player2.chooseTarget(`启诲：令一名其他角色摸三张牌`, true, lib.filter.notMe).set("ai", (target2) => get.effect(target2, { name: "draw" }, get.player(), get.player())).forResult();
              const {
                targets: [target]
              } = result2;
              if (target) {
                player2.line(target);
                await target.draw(3);
              }
              break;
            }
            default:
              player2.addSkill(skillName + "_unlimit");
          }
        }
        if (links.length == 3) {
          player2.addMark(`${skillName}_limit`, 1, false);
        }
      }
    },
    init(player2, skill) {
      if (!player2.hasMark(`${skill}_limit`)) {
        player2.addMark(`${skill}_limit`, 1, false);
      }
      player2.setStorage(skill, [], true);
    },
    onremove(player2, skill) {
      delete player2.storage[skill];
      player2.removeTip(skill);
    },
    intro: {
      content(storage, player2, skill) {
        let str = `<li>标记上限：${player2.countMark(`${skill}_limit`) || 1}`;
        str += `<br><li>标记：${get.translation(storage)}`;
        return str;
      }
    },
    subSkill: {
      unlimit: {
        charlotte: true,
        mod: { cardUsable: () => Infinity },
        trigger: { player: "useCard1" },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player2) {
          player2.removeSkill(event.name);
          if (trigger.addCount !== false) {
            trigger.addCount = false;
            const stat = player2.getStat().card, name = trigger.card.name;
            if (typeof stat[name] == "number") {
              stat[name]--;
            }
          }
        },
        mark: true,
        intro: { content: "使用的下一张牌无任何次数限制" }
      }
    }
  },
  friendxushugongli: {
    audio: 2,
    locked: true,
    ai: { combo: "friendxiaxing" }
  },
  xuanjian_skill: {
    equipSkill: true,
    enable: "chooseToUse",
    mod: {
      targetInRange(card2, player2) {
        const evt = get.event();
        if (player2.hasSkill("friendxushugongli") && get.info("friendgongli").isFriendOf(player2, "friend_pangtong") && evt.skill === "xuanjian_skill") {
          return true;
        }
      }
    },
    viewAs: {
      name: "sha"
    },
    filterCard(card2) {
      if (ui.selected.cards.length) {
        return get.suit(card2) === get.suit(ui.selected.cards[0]);
      }
      return true;
    },
    prompt() {
      return get.info({ name: "xuanjian" }).cardPrompt(null, get.player());
    },
    selectCard() {
      const player2 = get.player();
      if (ui.selected.cards.length) {
        if (!player2.hasSkill("friendxushugongli") || !get.info("friendgongli").isFriendOf(player2, "friend_zhugeliang")) {
          return -1;
        }
      }
      return 1;
    },
    ai: {
      respondSha: true,
      skillTagFilter(player2, tag, arg) {
        return arg !== "respond" && player2.countCards("hs");
      }
    }
  },
  //牢又寄 —— 诸葛亮
  friendyance: {
    audio: 7,
    trigger: {
      global: "roundStart",
      player: "phaseZhunbeiBegin"
    },
    filter(event, player2, name) {
      if (name == "roundStart") {
        return game.roundNumber === 1;
      }
      return true;
    },
    check(event, player2, name) {
      return name !== "roundStart";
    },
    round: 1,
    popup: false,
    logAudio: (index) => typeof index === "number" ? "friendyance" + index + ".mp3" : 1,
    async cost(event, trigger, player2) {
      const result = await player2.chooseButton([
        get.prompt2(event.skill),
        [
          [
            ["trick", `从牌堆中随机获得一张锦囊牌`],
            ["minigame", `执行“卧龙演策”`]
          ],
          "textbutton"
        ]
      ]).set("filterButton", (button) => {
        if (button.link !== "minigame") {
          return true;
        }
        const player3 = get.player();
        return typeof player3.storage.friendyance !== "number" || player3.hasMark("friendyance");
      }).set("ai", (button) => {
        const { triggername } = get.event();
        if (triggername === "roundStart") {
          return 0;
        }
        return button.link === "minigame" ? 2 : 1;
      }).set("triggername", event.triggername).forResult();
      event.result = {
        bool: result.bool,
        cost_data: result
      };
    },
    async content(event, trigger, player2) {
      const {
        cost_data: {
          links: [choice]
        }
      } = event;
      player2.logSkill("friendyance", null, null, null, [choice === "trick" ? null : get.rand(2, 3)]);
      if (choice === "trick") {
        const card2 = get.cardPile2((c) => get.type2(c) === "trick");
        if (card2) {
          await player2.gain(card2, "draw");
        } else {
          player2.chat("一无所获");
        }
      } else {
        await lib.skill.friendyance.minigame(event, trigger, player2);
      }
    },
    marktext: "策",
    intro: { content: "初始可预测#次" },
    async minigame(event, trigger, player2) {
      await event.trigger("friendyance_minigameBegin");
      if (typeof player2.storage.friendyance !== "number") {
        let bool = player2.hasSkill("friendzhugelianggongli") && get.info("friendgongli").isFriendOf(player2, "friend_pangtong");
        player2.addMark("friendyance", 3 + bool, false);
      }
      const num = player2.countMark("friendyance");
      if (!num) {
        return;
      }
      player2.addSkill("friendyance_record");
      const storage = player2.storage["friendyance_record"];
      const { control } = await player2.chooseControl("颜色预测", "类型预测").set("prompt", "卧龙演策：请选择预测方式").set("ai", () => get.rand(0, 1)).forResult();
      const type = lib.inpile.map((c) => get.type2(c)).unique();
      const color = Object.keys(lib.color).filter((color2) => color2 !== "none");
      const list = [];
      if (control === "颜色预测") {
        list.addArray(color);
        storage[2] = "color";
      } else {
        list.addArray(type);
        storage[2] = "type2";
      }
      const dialog = ["卧龙演策：请进行你的预测"];
      for (const i of Array.from({ length: num }, (_, k) => k)) {
        const button = list.map((c) => [`${c}_${i}`, get.translation(c)]);
        dialog.push(`<div class="text center">第${get.cnNumber(i + 1, true)}张牌的预测</div>`);
        dialog.push([button, "tdnodes"]);
      }
      const { links } = await player2.chooseButton(dialog, get.select(num)).set("forced", true).set("filterButton", (button) => {
        return parseInt(button.link.at(-1)) === ui.selected.buttons.length;
      }).set("ai", () => 1 + Math.random()).forResult();
      if (!links?.length) {
        return;
      }
      for (const i of links) {
        storage[1].push(i.replace(`_${i.at(-1)}`, ""));
      }
      storage[3] = links.length;
      player2.markSkill("friendyance_record");
      await event.trigger("friendyance_minigame");
    },
    subSkill: {
      record: {
        charlotte: true,
        init(player2, skill) {
          player2.storage[skill] = [[], [], null, null, null];
        },
        onremove: true,
        mark: true,
        marktext: "阵",
        intro: {
          name: "卧龙演策",
          markcount: () => 0,
          content([trrList, gussList, type, num, show], player2) {
            if (!show && !player2.isUnderControl(true)) {
              return "天机可知却不可说...";
            }
            return [
              "剩余猜测：" + get.translation(gussList),
              "猜测进度：" + trrList.map((i) => i ? "正确" : "错误").concat(Array.from({ length: num - trrList.length }, () => "未知")).join("、")
            ].map((str) => "<li>" + str).join("<br>");
          }
        },
        mod: {
          aiOrder(player2, card2, num) {
            if (num > 0) {
              const storage = player2.getStorage("friendyance_record");
              if (storage[0]?.length === storage[3]) {
                return;
              }
              if (player2.hasSkill("friendzhugelianggongli") && get.info("friendgongli").isFriendOf(player2, "friend_xushu") && storage[0].length === 0) {
                return;
              }
              return get[storage[2]](card2) === storage[1][storage[0].length] ? num + 1145141919810 : num * 1e-5;
            }
          }
        },
        group: "friendyance_check"
      },
      check: {
        charlotte: true,
        trigger: {
          global: "useCard",
          player: "friendyance_minigameBegin"
        },
        filter(event, player2) {
          const storage = player2.getStorage("friendyance_record");
          return event.name !== "useCard" || storage[0].length < storage[3];
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const storage = player2.getStorage("friendyance_record");
          const num = trigger.name === "useCard" && storage[4] ? 1 : 0;
          if (trigger.name === "useCard") {
            const i = storage[1][storage[0].length];
            if (get[storage[2]](trigger.card) === i || player2.hasSkill("friendzhugelianggongli") && get.info("friendgongli").isFriendOf(player2, "friend_xushu") && storage[0].length === 0) {
              player2.popup("预测正确", "wood");
              game.log(player2, "预测", "#y正确");
              storage[0].push(true);
              if (storage[0].filter((b) => b === true).length <= 5) {
                await player2.draw("nodelay");
              }
            } else {
              player2.popup("预测错误", "fire");
              game.log(player2, "预测", "#r错误");
              storage[0].push(false);
            }
            player2.markSkill("friendyance_record");
          }
          if (trigger.name !== "useCard" || storage[0].length === storage[3]) {
            const trueArr = storage[0].filter((b) => b === true);
            if (trueArr.length === 0) {
              player2.logSkill("friendyance", null, null, null, [4]);
              await player2.loseHp(1 + num);
              player2.removeMark("friendyance", 1 + num, false);
            }
            if (trueArr.length * 2 < storage[3]) {
              if (trueArr.length !== 0) {
                player2.logSkill("friendyance", null, null, null, [5]);
              }
              if (player2.hasCard((card2) => lib.filter.cardDiscardable(card2, player2), "he")) {
                await player2.chooseToDiscard(1 + num, "he", true);
              }
            } else {
              player2.logSkill("friendyance", null, null, null, [trueArr.length === storage[3] ? 7 : 6]);
              const choice = storage[2] == "color" ? Object.keys(lib.color).filter((color) => color !== "none") : lib.inpile.map((name) => get.type2(name)).unique();
              const control = choice.length > 1 ? (await player2.chooseControl(choice).set("ai", () => {
                return get.event().controls.randomGet();
              }).set("prompt", `请选择获得牌的条件`).forResult()).control : choice[0];
              let gains = [];
              while (gains.length < 1 + num) {
                const card2 = get.cardPile2((card3) => {
                  if (gains.includes(card3)) {
                    return false;
                  }
                  return get[storage[2]](card3) === control;
                });
                if (card2) {
                  gains.push(card2);
                } else {
                  break;
                }
              }
              if (gains.length) {
                await player2.gain(gains, "draw");
              } else {
                player2.chat("一无所获");
              }
              if (trueArr.length === storage[3]) {
                await player2.draw(2 + num);
                if (player2.countMark("friendyance") < 7) {
                  player2.addMark("friendyance", Math.min(7 - player2.countMark("friendyance"), 1 + num), false);
                }
                if (storage[4] && storage[3] >= 3) {
                  player2.logSkill("friendfangqiu", null, null, null, [3]);
                  player2.restoreSkill("friendfangqiu");
                }
              }
            }
            player2.removeSkill("friendyance_record");
          }
        }
      }
    }
  },
  friendfangqiu: {
    audio: 3,
    logAudio: (index) => typeof index === "number" ? "friendfangqiu" + index + ".mp3" : 2,
    limited: true,
    trigger: { player: "friendyance_minigame" },
    check(event, player2) {
      return _status.currentPhase === player2;
    },
    skillAnimation: true,
    animationColor: "metal",
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      const storage = player2.getStorage("friendyance_record");
      storage[4] = true;
      player2.popup(storage[1]);
      game.log(player2, "的预测为", "#g" + get.translation(storage[1]));
    },
    ai: { combo: "friendyance" }
  },
  friendzhugelianggongli: {
    audio: 2,
    locked: true,
    ai: { combo: "friendyance" }
  },
  //共励
  friendgongli: {
    audio: 2,
    isFriendOf(player2, name) {
      return player2.getFriends(true).some((target) => {
        if (target.identityShown === false) {
          return false;
        }
        return get.nameList(target).some((namex) => namex.endsWith(name));
      });
    }
  },
  //手杀薛综
  mbfunan: {
    audio: "funan",
    trigger: { global: ["respond", "useCard"] },
    filter(event, player2) {
      if (!event.respondTo) {
        return false;
      }
      if (event.player == player2) {
        return false;
      }
      if (player2 != event.respondTo[0]) {
        return false;
      }
      return event.cards.filterInD("od").length > 0;
    },
    prompt2(event, player2) {
      return `获得${get.translation(event.cards.filterInD("od"))}`;
    },
    check(event, player2) {
      return get.value(event.cards.filterInD("od")) > 0;
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      const cards2 = trigger.cards.filterInD("od");
      const next = player2.gain(cards2, "log", "gain2");
      next.gaintag.add(event.name);
      await next;
    },
    group: "mbfunan_draw",
    subSkill: {
      rewrite: {
        charlotte: true,
        init: (player2, skill) => player2.storage[skill] = true
      },
      draw: {
        mod: {
          aiValue(player2, card2, num) {
            if (get.itemtype(card2) == "card" && card2.hasGaintag("mbfunan")) {
              return num + 2;
            }
          },
          aiUseful() {
            return lib.skill.mbfunan_draw.mod.aiValue.apply(this, arguments);
          }
        },
        audio: "funan",
        trigger: { player: "useCardAfter" },
        filter(event, player2) {
          if (!player2.storage.mbfunan_rewrite) {
            return false;
          }
          if (!player2.hasHistory("lose", (evt) => {
            const evtx = evt.relatedEvent || evt.getParent();
            return evtx == event && Object.values(evt.gaintag_map).flat().includes("mbfunan");
          })) {
            return false;
          }
          return !game.hasPlayer2((current) => {
            if (current == player2) {
              return false;
            }
            return ["useCard", "respond"].some((key) => {
              return current.hasHistory(key, (evt) => evt.respondTo?.[1] == event.card);
            });
          });
        },
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          await player2.draw();
        }
      }
    }
  },
  mbjiexun: {
    getSuitsMap() {
      const suits = {};
      game.countPlayer((current) => {
        for (const card2 of current.getCards("ej")) {
          if (typeof suits[get.suit(card2)] != "number") {
            suits[get.suit(card2)] = 0;
          }
          suits[get.suit(card2)]++;
        }
      });
      return suits;
    },
    audio: "jiexun",
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player2) {
      return game.hasPlayer((current) => current != player2);
    },
    async cost(event, trigger, player2) {
      const suits = get.info(event.skill).getSuitsMap();
      const num = player2.countMark("mbjiexun_used") + 1;
      const str = lib.suit.toReversed().map((suit) => {
        return `${get.translation(suit)}：${get.cnNumber(suits[suit] || 0)}张`;
      }).join("；");
      const result = await player2.chooseButtonTarget({
        createDialog: [
          `###${get.prompt(event.skill)}###<div class="text center">选择一个花色并令一名其他角色摸等同于场上此花色牌数张牌（至多为5），然后其弃置${get.cnNumber(num)}张牌<br>当前花色情况：${str}</div>`,
          [
            lib.suit.slice().reverse().map((i) => [i, get.translation(i)]),
            "tdnodes"
          ]
        ],
        filterButton: true,
        filterTarget: lib.filter.notMe,
        ai1(button) {
          const { player: player3, numx } = get.event();
          const map = get.info("mbjiexun").getSuitsMap();
          const bool = game.hasPlayer((current) => get.attitude(player3, current) > 0 && player3 != current);
          const list = lib.suit.slice().sort((a, b) => (bool ? 1 : -1) * (Math.min(5, map[b] || 0) - Math.min(5, map[a] || 0)));
          if (bool && map[list[0]] > 0 || !bool) {
            return list[0];
          }
          return 0;
        },
        ai2(target) {
          const { player: player3, numx } = get.event();
          const suit = ui.selected.buttons[0].link;
          const map = get.info("mbjiexun").getSuitsMap();
          const num2 = Math.min(5, map[suit] || 0);
          const eff = num2 >= numx && num2 > 0 ? 1 : -1;
          const att = get.attitude(player3, target);
          return eff * get.sgn(att) + att / 114514;
        }
      }).set("numx", num).forResult();
      event.result = {
        bool: result?.bool,
        cost_data: result?.links,
        targets: result?.targets
      };
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const suit = event.cost_data[0];
      player2.addSkill(event.name + "_used");
      player2.addMark(event.name + "_used", 1, false);
      player2.chat(get.translation(suit + 2));
      game.log(player2, "选择了", "#y" + get.translation(suit + 2));
      const drawNum = Math.min(5, get.info(event.name).getSuitsMap()[suit] || 0);
      const discardNum = player2.countMark(event.name + "_used");
      await target.draw(drawNum);
      const handCardsBefore = target.countCards("h");
      await target.chooseToDiscard({
        selectCard: discardNum,
        position: "he",
        forced: true
      }).forResult();
      if (handCardsBefore > 0 && !target.hasCards("h")) {
        game.log(player2, "修改了", "#g【复难】");
        player2.addSkill("mbfunan_rewrite");
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true,
        intro: { content: "已发动#次【诫训】" }
      }
    }
  },
  // SP甘夫人
  mbzhijie: {
    audio: 2,
    trigger: { global: "phaseUseBegin" },
    filter(event, player2) {
      return event.player.countCards("h");
    },
    async cost(event, trigger, player2) {
      event.result = await player2.choosePlayerCard(trigger.player, "h", get.prompt2(event.skill)).set("ai", (button) => {
        const { player: player3, target } = get.event(), att = get.attitude(player3, target), type = get.type2(button.link);
        if (att === 0) {
          return 0;
        }
        const cards2 = target.getCards("hs", (card2) => get.type2(card2) === type && target.hasValueTarget(card2));
        return cards2.length > 0 ^ att < 0 ? (() => {
          if (att < 0) {
            return 1 + Math.random();
          }
          return Math.max(...cards2.map((card2) => target.getUseValue(card2)));
        })() : -1;
      }).forResult();
    },
    round: 1,
    logTarget: "player",
    async content(event, trigger, player2) {
      const { cards: cards2, name } = event, { player: target } = trigger;
      await player2.showCards(cards2, get.translation(player2) + "对" + get.translation(target) + "发动了【智诫】");
      target.addTempSkill(name + "_effect", "phaseUseAfter");
      target.markAuto(name + "_effect", [[player2, get.type2(cards2[0])]]);
    },
    subSkill: {
      effect: {
        mod: {
          aiOrder(player2, card2, num) {
            if (num > 0) {
              return num + 1.5 * (player2.getStorage("mbzhijie_effect").some((list) => list[1] == get.type2(card2)) ? 1 : -1);
            }
          }
        },
        charlotte: true,
        onremove: true,
        intro: {
          content(storage, player2) {
            const infos = [];
            for (let i = 0; i < storage.length; i++) {
              const list = storage[i];
              infos.add(`本阶段使用${get.translation(list[1])}牌后摸一张牌并弃置本回合使用此牌类型牌的次数-1张牌；本阶段结束时，若因此获得的牌数大于因此弃置的牌数，则与${get.translation(list[0])}各摸一张牌`);
            }
            return infos.join("<br>");
          }
        },
        audio: "mbzhijie",
        trigger: { player: ["useCardAfter", "phaseUseEnd"] },
        filter(event, player2) {
          const skillName = "mbzhijie_effect", storage = player2.getStorage(skillName);
          if (event.name == "useCard") {
            return storage.some((list) => list[1] == get.type2(event.card));
          }
          const num1 = player2.getHistory("gain", (evt) => evt.getParent(2).name == skillName && evt.getParent(event.name) == event).reduce((sum, evt) => sum + evt.cards.length, 0), num2 = player2.getHistory("lose", (evt) => evt.getParent(3).name == skillName && evt.getParent(event.name) == event).reduce((sum, evt) => sum + evt.cards2.length, 0);
          return num1 > num2 && storage.some((list) => list[0].isIn());
        },
        forced: true,
        async content(event, trigger, player2) {
          const { name, card: card2 } = trigger;
          if (name == "useCard") {
            await player2.draw();
            const num = player2.getHistory(name, (evt) => get.type2(evt.card) == get.type2(card2)).length - 1;
            if (player2.countCards("he") && num) {
              await player2.chooseToDiscard("he", true, num);
            }
          } else {
            const targets2 = player2.getStorage(event.name).map((list) => list[0]).filter((i) => i.isIn()).sortBySeat();
            await game.asyncDraw([player2].concat(targets2));
          }
        }
      }
    }
  },
  mbshushen: {
    audio: 2,
    trigger: {
      player: ["gainAfter", "recoverBegin"],
      global: "loseAsyncAfter"
    },
    filter(event, player2) {
      const name = event.name != "recover" ? "gain" : "recover";
      if (player2.getStorage("mbshushen_used").includes(name)) {
        return false;
      }
      if (event.name == "recover") {
        return game.hasPlayer((current) => player2 != current);
      }
      return event.getg(player2).length >= 2 && game.hasPlayer((current) => player2 != current && current.isDamaged());
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt(event.name.slice(0, -5)), `令一名其他角色${trigger.name == "recover" ? `摸两张牌` : `回复1点体力`}`, (card2, player3, target) => {
        if (player3 == target) {
          return false;
        }
        return get.event().getTrigger().name == "recover" || target.isDamaged();
      }).set("ai", (target) => {
        const player3 = get.player();
        if (get.event().getTrigger().name == "recover") {
          return get.effect(target, { name: "draw" }, player3, player3) * 2;
        }
        return get.recoverEffect(target, player3, player3);
      }).forResult();
    },
    async content(event, trigger, player2) {
      const name = trigger.name != "recover" ? "gain" : "recover";
      player2.addTempSkill(event.name + "_used");
      player2.markAuto(event.name + "_used", [name]);
      const target = event.targets[0];
      if (trigger.name != "recover") {
        await target.recover();
      } else {
        await target.draw(2);
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      }
    }
  },
  //SP甄宓
  mbbojian: {
    audio: 2,
    init(player2, skill) {
      player2.addSkill(skill + "_record");
    },
    onremove(player2, skill) {
      player2.removeTip(skill + "_record");
    },
    trigger: { player: "phaseUseEnd" },
    filter(event, player2) {
      _status.mbbojian ??= {};
      _status.mbbojian[player2.playerid] ??= [0, 0];
      const record = _status.mbbojian;
      const history = player2.getHistory("useCard", (evt) => evt.getParent(event.name) == event);
      const num1 = history.length, num2 = history.map((evt) => get.suit(evt.card)).toUniqued().length, cards2 = history.reduce((list, evt) => list.addArray(evt.cards.filterInD("d")), []);
      return num1 != record[player2.playerid][0] && num2 != record[player2.playerid][1] || cards2.length;
    },
    forced: true,
    async content(event, trigger, player2) {
      _status.mbbojian ??= {};
      _status.mbbojian[player2.playerid] ??= [0, 0];
      const record = _status.mbbojian;
      const history = player2.getHistory("useCard", (evt) => evt.getParent(trigger.name) == trigger);
      const num1 = history.length, num2 = history.map((evt) => get.suit(evt.card)).toUniqued().length, cards2 = history.reduce((list, evt) => list.addArray(evt.cards.filterInD("d")), []);
      if (num1 != record[player2.playerid][0] && num2 != record[player2.playerid][1]) {
        await player2.draw();
      } else {
        if (!cards2.length) {
          return;
        }
        const result = await player2.chooseButtonTarget({
          createDialog: [`博鉴：请将其中一张牌交给一名角色`, cards2],
          forced: true,
          filterTarget: true,
          ai1(button) {
            return get.value(button.link);
          },
          canHidden: true,
          ai2(target) {
            const player3 = get.player();
            const card2 = ui.selected.buttons[0].link;
            if (card2) {
              return get.value(card2, target) * get.attitude(player3, target);
            }
            return 1;
          }
        }).forResult();
        if (result?.bool) {
          await result.targets[0].gain(result.links, "gain2");
        }
      }
    },
    subSkill: {
      record: {
        init(player2, skill) {
          _status.mbbojian ??= {};
          _status.mbbojian[player2.playerid] ??= [0, 0];
          player2.addTip(skill, `${get.translation(skill)} 上次 ${_status.mbbojian[player2.playerid][0]}/${_status.mbbojian[player2.playerid][1]}`);
          player2.markSkill(skill);
        },
        onremove(player2, skill) {
          player2.removeTip(skill);
        },
        trigger: { player: "phaseUseAfter" },
        firstDo: true,
        charlotte: true,
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const history = player2.getHistory("useCard", (evt) => evt.getParent(trigger.name) == trigger);
          const num1 = history.length, num2 = history.map((evt) => get.suit(evt.card)).toUniqued().length;
          _status.mbbojian ??= {};
          _status.mbbojian[player2.playerid] = [num1, num2];
          player2.addTip(event.name, `${get.translation(event.name)} 上次 ${_status.mbbojian[player2.playerid][0]}/${_status.mbbojian[player2.playerid][1]}`);
          player2.markSkill(event.name);
        },
        intro: {
          markcount: () => 0,
          content(storage, player2) {
            const record = _status.mbbojian || {};
            return "上个出牌阶段使用牌情况：①牌数：" + (record[player2.playerid]?.[0] || 0) + "；②花色数：" + (record[player2.playerid]?.[1] || 0);
          }
        }
      }
    }
  },
  mbjiwei: {
    audio: 4,
    getNum(event, player2) {
      let num = 0;
      if (game.countPlayer2((current) => current.hasHistory("lose")) >= 1) {
        num++;
      }
      if (game.countPlayer2((current) => current.hasHistory("damage")) >= 1) {
        switch (get.mode()) {
          case "doudizhu": {
            num += 2;
            break;
          }
          case "identity":
            break;
          default: {
            num++;
            break;
          }
        }
      }
      if (event.name == "phase") {
        return num;
      }
      if (get.mode() == "identity") {
        return Math.max(game.countPlayer(), player2.getHp());
      }
      if (game.hasPlayer2((current) => !current.isAlive())) {
        return 114514;
      }
      return 5;
    },
    trigger: {
      player: "phaseZhunbeiBegin",
      global: "phaseEnd"
    },
    filter(event, player2) {
      const num = get.info("mbjiwei").getNum(event, player2);
      if (event.name == "phaseZhunbei") {
        return player2.countCards("h") >= num && game.hasPlayer((current) => current != player2);
      }
      return event.player != player2 && num > 0;
    },
    logAudio(event, player2) {
      if (event.name == "phaseZhunbei") {
        return ["mbjiwei3.mp3", "mbjiwei4.mp3"];
      }
      return ["mbjiwei1.mp3", "mbjiwei2.mp3"];
    },
    forced: true,
    async content(event, trigger, player2) {
      const num = get.info(event.name).getNum(trigger, player2);
      if (trigger.name == "phase") {
        await player2.draw(num);
      } else {
        const cards2 = player2.getCards("h"), map = {};
        for (let color of ["red", "black", "none"]) {
          if (typeof map[color] != "number") {
            map[color] = 0;
          }
          map[color] += cards2.filter((card2) => get.color(card2) == color).length;
        }
        const list = [];
        for (var i in map) {
          if (map[i] > 0) {
            list.push([`${i}2`, map[i]]);
          }
        }
        list.sort((a, b) => b[1] - a[1]);
        let colors = list.filter((i2) => i2[1] == list[0][1]).map((i2) => i2[0]);
        const control = colors.length == 1 ? colors[0] : (await player2.chooseControl(colors).set("prompt", "济危：请选择一个颜色").forResult()).control;
        let togive = player2.getCards("h").filter((card2) => get.color(card2) == control.slice(0, -1));
        if (_status.connectMode) {
          game.broadcastAll(() => _status.noclearcountdown = true);
        }
        let given_map = [];
        while (togive.length && game.hasPlayer((current) => current != player2) && player2.hasCard((card2) => !card2.hasGaintag("olsujian_given"), "h")) {
          const result = await player2.chooseCardTarget({
            forced: true,
            filterCard(card2, player3) {
              return get.event().togive.includes(card2) && !card2.hasGaintag("olsujian_given");
            },
            selectCard: [1, Infinity],
            position: "h",
            filterTarget: lib.filter.notMe,
            prompt: "济危：请选择要分配的卡牌和目标",
            ai1(card2) {
              return !ui.selected.cards.length && card2.name == "du" ? 1 : 0;
            },
            ai2(target) {
              const player3 = get.event().player;
              const card2 = ui.selected.cards[0];
              if (card2) {
                return get.value(card2, target) * get.attitude(player3, target);
              }
              return 0;
            },
            togive,
            allowChooseAll: true
          }).forResult();
          if (result?.cards?.length && result.targets?.length) {
            const {
              cards: cards3,
              targets: [target]
            } = result;
            togive.removeArray(cards3);
            if (given_map.some((i2) => i2[0] == target)) {
              given_map[given_map.indexOf(given_map.find((i2) => i2[0] == target))][1].addArray(cards3);
            } else {
              given_map.push([target, cards3]);
            }
            player2.addGaintag(cards3, "olsujian_given");
          } else {
            break;
          }
        }
        if (_status.connectMode) {
          game.broadcastAll(() => {
            delete _status.noclearcountdown;
            game.stopCountChoose();
          });
        }
        if (given_map.length) {
          await game.loseAsync({
            gain_list: given_map,
            player: player2,
            cards: given_map.slice().flatMap((list2) => list2[1]),
            giver: player2,
            animate: "giveAuto"
          }).setContent("gaincardMultiple");
        }
      }
    }
  },
  //张奋
  mbquchong: {
    audio: 4,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      if (!game.hasPlayer((target) => {
        return target.hasCards("e", (card2) => card2.name.startsWith("dagongche_"));
      })) {
        const num = player2.getAllHistory("custom", (evt) => evt.name == "mbquchong").length;
        const list = (
          /*get.mode() == "identity" ? [0, 5, 10, 10] : */
          [0, 2, 5, 5]
        );
        return num < 4 && player2.countMark("mbquchong") >= list[num];
      }
      return game.hasPlayer((target) => {
        return target.hasCards("e", (card2) => card2.name.startsWith("dagongche_")) && game.hasPlayer(
          (current) => current != target
          /* && !current.hasCards("e", card => card.name.startsWith("dagongche_")) */
        );
      });
    },
    mod: {
      aiValue(player2, card2, num) {
        if (!player2.hasCards("e", (cardx) => cardx.name.startsWith("dagongche_"))) {
          return num;
        }
        if (card2.name.startsWith("dagongche_")) {
          return num;
        }
        if (get.type(card2) == "equip" && num > 0) {
          return 0.3;
        }
      }
    },
    locked: false,
    direct: true,
    logAudio: (index) => typeof index === "number" ? "mbquchong" + index + ".mp3" : 4,
    async content(event, trigger, player2) {
      if (game.hasPlayer((target) => {
        return target.hasCards("e", (card2) => card2.name.startsWith("dagongche_"));
      })) {
        let result = await player2.chooseTarget(2, (card2, player3, target) => {
          if (!ui.selected.targets.length) {
            return target.hasCards("e", (card3) => card3.name.startsWith("dagongche_"));
          } else {
            const from = ui.selected.targets[0];
            return target != from;
          }
        }).set("multitarget", true).set("cards", event.cards).set("targetprompt", ["被移走", "获得目标"]).set("prompt", get.prompt(event.name)).set("prompt2", `将场上的一张【大攻车】交给另一名角色并令其使用之`).set("ai", (target) => {
          const player3 = get.player();
          const att = get.attitude(player3, target);
          if (!ui.selected.targets.length) {
            if (att > 0) {
              return 0;
            }
            if (target.hasSkillTag("noe")) {
              att /= 4;
            }
            return -att;
          } else {
            const from = ui.selected.targets[0];
            if (from.hasCards("e", (card2) => card2.name.startsWith("dagongche_") && target.canEquip(card2))) {
              return att * (2.5 - target.countCards("e"));
            }
            return att;
          }
        }).forResult();
        if (result?.bool) {
          const { targets: targets2 } = result;
          const [target1, target2] = targets2;
          player2.logSkill(event.name, targets2, null, null, [4]);
          const cards2 = target1.getCards("e", (card2) => card2.name.startsWith("dagongche_"));
          if (cards2.length) {
            result = await target1.chooseButton([`###渠冲###<div class='text center'>请选择其中一张牌将之交给${get.translation(target2)}并令其使用</div>`, cards2], true).set("direct", true).forResult();
            if (result?.bool) {
              const card2 = result.links[0];
              await target2.gain(card2, target1, "giveAuto").set("giver", player2);
              if (get.position(card2) == "h" && get.owner(card2) == target2 && target2.hasUseTarget(card2)) {
                await target2.chooseUseTarget(card2, "nopopup", false, true);
              }
            }
          }
        }
      } else {
        const numbers = Array.from({ length: 13 }).map((_, i) => get.strNumber(i + 1));
        const list = (
          /*get.mode() == "identity" ? [0, 5, 10, 10] : */
          [0, 2, 5, 5]
        );
        const costMark = list[player2.getAllHistory("custom", (evt) => evt.name == "mbquchong").length];
        const result = await player2.chooseButton(
          [
            "###" + get.prompt("mbquchong") + '###<div class="text center">消耗' + parseFloat(costMark) + "点铸造值，制造任意花色和点数的【大攻车·攻】或【大攻车·守】</div>",
            [["dagongche_attack", "dagongche_defend"].map((i) => [i, get.translation(i)]), "tdnodes"],
            [
              lib.suit.slice().reverse().map((i) => [i, get.translation(i)]),
              "tdnodes"
            ],
            [numbers, "tdnodes"]
          ],
          3
        ).set("filterButton", (button) => {
          return !ui.selected.buttons.some((but) => {
            return [["dagongche_attack", "dagongche_defend"], lib.suit, get.event().numbers].some((list2) => list2.includes(but.link) && list2.includes(button.link));
          });
        }).set("numbers", numbers).set("ai", () => 1 + Math.random()).forResult();
        if (result.bool) {
          const equips = result.links.sort((a, b) => {
            return lib.suit.includes(a) + (numbers.includes(a) ? 2 : 0) - (lib.suit.includes(b) + (numbers.includes(b) ? 2 : 0));
          });
          const card2 = game.createCard(equips[0], equips[1], get.numString(equips[2]));
          if (!card2.storage) {
            card2.storage = {};
          }
          if (typeof card2.storage.mbquchong != "number") {
            card2.storage.mbquchong = card2.name == "dagongche_attack" ? 2 : 3;
          }
          lib.skill.mbquchong.broadcast(card2);
          const resultx = await player2.chooseTarget("令一名角色获得" + get.translation(card2) + "并使用之", true).set("ai", (target) => {
            const player3 = get.event().player, att = get.attitude(player3, target);
            if (!target.canEquip(get.event().card)) {
              return att;
            }
            return att * (2.5 - target.countCards("e"));
          }).set("card", card2).forResult();
          if (resultx?.bool) {
            const target = resultx.targets[0];
            player2.logSkill("mbquchong", target, null, null, [card2.name == "dagongche_attack" ? 3 : 2]);
            if (costMark > 0) {
              player2.removeMark("mbquchong", costMark);
            }
            player2.getHistory("custom").push({ name: "mbquchong" });
            await target.gain(card2, "gain2");
            if (get.position(card2) == "h" && get.owner(card2) == target && target.hasUseTarget(card2)) {
              await target.chooseUseTarget(card2, "nopopup", false, true);
            }
          }
        }
      }
    },
    broadcast(card2) {
      game.broadcast(
        (card3, storage) => {
          card3.storage = storage;
        },
        card2,
        card2.storage
      );
    },
    marktext: "铸",
    intro: {
      name: "铸造点",
      content: "当前拥有#铸造点"
    },
    group: ["mbquchong_recast", "mbquchong_remove"],
    derivation: ["dagongche_attack", "dagongche_defend"],
    subSkill: {
      recast: {
        audio: "mbquchong1.mp3",
        inherit: "drlt_huairou"
      },
      remove: {
        audio: "mbquchong1.mp3",
        trigger: { global: "phaseEnd" },
        filter(event, player2) {
          return get.discardPile((card2) => get.type(card2, false) == "equip");
        },
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          const cards2 = Array.from(ui.discardPile.childNodes).filter((card2) => get.type(card2, false) == "equip");
          await game.cardsGotoSpecial(cards2);
          await player2.showCards(cards2, get.translation(player2) + "发动了【渠冲】");
          game.log(cards2, "被移出了游戏");
          player2.addMark("mbquchong", cards2.length);
        }
      },
      effect: {
        equipSkill: true,
        trigger: { player: ["loseBefore", "mbquchongOnRemove", "equipBefore", "equipAfter"] },
        filter(event, player2, name) {
          if (name == "mbquchongOnRemove") {
            return player2.hasCards("e", (card2) => card2.name.startsWith("dagongche_") && card2.storage?.mbquchong <= 0);
          }
          if (event.name == "equip") {
            if (name == "equipBefore") {
              return true;
            }
            if (!event.card.name.startsWith("dagongche_")) {
              return false;
            }
            return player2.hasCards("e", (card2) => {
              return !event.cards.includes(card2) && lib.filter.cardDiscardable(card2, player2);
            });
          }
          if (event.getParent(2).name == "disableEquip") {
            return false;
          }
          if (event.getParent(2).name == "mbquchong" || event.getParent(3).name == "mbquchong_recast") {
            return false;
          }
          return player2.hasCards("e", (card2) => {
            if (!event.cards.includes(card2)) {
              return false;
            }
            return card2.name.startsWith("dagongche_") && card2.storage?.mbquchong > 0;
          });
        },
        forced: true,
        async content(event, trigger, player2) {
          if (event.triggername == "mbquchongOnRemove") {
            const cards2 = player2.getCards("e", (card2) => card2.name.startsWith("dagongche_") && card2.storage?.mbquchong <= 0);
            await player2.lose(cards2, ui.special);
            for (const card2 of cards2) {
              card2.fix();
              card2.remove();
              card2.destroyed = true;
            }
            game.log(cards2, "被移出了游戏");
          } else if (trigger.name == "equip") {
            if (event.triggername == "equipBefore") {
              trigger.cancel();
            } else {
              await player2.discard(
                player2.getCards("e", (card2) => {
                  return !trigger.cards.includes(card2) && lib.filter.cardDiscardable(card2, player2);
                })
              );
            }
          } else {
            const cards2 = player2.getCards("e", (card2) => {
              if (!trigger.cards.includes(card2)) {
                return false;
              }
              return card2.name.startsWith("dagongche_") && card2.storage?.mbquchong > 0;
            });
            trigger.cards.removeArray(cards2);
            for (const card2 of cards2) {
              card2.storage.mbquchong--;
              game.log(card2, "减少了", "#y1点", "#g耐久值");
              lib.skill.mbquchong.broadcast(card2);
            }
            await event.trigger("mbquchongOnRemove");
          }
        },
        ai: {
          effect: {
            target(card2, player2, target) {
              if (!target.hasCards("e", (card3) => card3.name.startsWith("dagongche_"))) {
                return;
              }
              if (player2 == target && get.type(card2) == "equip") {
                return 0;
              }
            }
          }
        }
      }
    }
  },
  dagongche_attack_skill: {
    equipSkill: true,
    trigger: { source: "damageBegin3" },
    filter(event, player2) {
      if (!player2.hasCard((card2) => {
        return card2.name == "dagongche_attack" && card2.storage?.mbquchong > 0;
      }, "e")) {
        return false;
      }
      return game.roundNumber > 0;
    },
    logTarget: "player",
    prompt2(event, player2) {
      return "令对" + get.translation(event.player) + "造成的伤害+" + parseFloat(Math.min(3, game.roundNumber));
    },
    check(event, player2) {
      return get.damageEffect(event.player, player2, player2) > 0;
    },
    async content(event, trigger, player2) {
      trigger.num += Math.min(3, game.roundNumber);
      const cards2 = player2.getCards("e", (card2) => {
        return card2.name == "dagongche_attack" && card2.storage?.mbquchong > 0;
      });
      for (const card2 of cards2) {
        card2.storage.mbquchong--;
        game.log(card2, "减少了", "#y1点", "#g耐久值");
        lib.skill.mbquchong.broadcast(card2);
      }
      await event.trigger("mbquchongOnRemove");
    }
  },
  dagongche_defend_skill: {
    equipSkill: true,
    trigger: { player: "damageBegin3" },
    filter(event, player2) {
      if (!player2.hasCard((card2) => {
        return card2.name == "dagongche_defend" && card2.storage?.mbquchong > 0;
      }, "e")) {
        return false;
      }
      return game.roundNumber > 0;
    },
    forced: true,
    locked: false,
    async content(event, trigger, player2) {
      const cards2 = player2.getCards("e", (card2) => {
        return card2.name == "dagongche_defend" && card2.storage?.mbquchong > 0;
      });
      for (const card2 of cards2) {
        const num = Math.min(trigger.num, card2.storage.mbquchong);
        trigger.num -= num;
        card2.storage.mbquchong -= num;
        game.log(card2, "减少了", "#y" + num + "点", "#g耐久值");
        lib.skill.mbquchong.broadcast(card2);
        if (trigger.num <= 0) {
          break;
        }
      }
      await event.trigger("mbquchongOnRemove");
    }
  },
  mbxunjie: {
    audio: 2,
    trigger: { player: "damageBegin4" },
    filter(event, player2) {
      if (!event.source || event.source.getHp() <= player2.getHp()) {
        return false;
      }
      return !game.hasPlayer((target) => {
        return target.hasCard((card2) => card2.name.startsWith("dagongche_"), "e");
      });
    },
    forced: true,
    logTarget: "source",
    async content(event, trigger, player2) {
      const result = await player2.judge((card2) => {
        return get.color(card2) == "red" ? 2 : -2;
      }).set("judge2", (result2) => Boolean(result2.bool)).forResult();
      if (result.color == "red") {
        trigger.num--;
      }
    },
    ai: {
      combo: "mbquchong",
      effect: {
        target(card2, player2, target) {
          if (player2.getHp() <= target.getHp() || game.hasPlayer((current) => {
            return current.hasCard((card3) => card3.name.startsWith("dagongche_"), "e");
          })) {
            return;
          }
          if (player2.hasSkillTag("jueqing", false, target)) {
            return;
          }
          const num = get.tag(card2, "damage");
          if (num) {
            if (num > 1) {
              return 0.55;
            }
            return 0.05;
          }
        }
      }
    }
  },
  //贾充
  mbbeini: {
    audio: "beini",
    inherit: "beini",
    filterTarget(card2, player2, target) {
      return target.hp >= player2.hp && player2 != target;
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const str = get.translation(target);
      const { index } = await player2.chooseControl().set("choiceList", [`摸两张牌，然后令${str}视为对自己使用【杀】或获得自己场上一张牌`, `令${str}摸两张牌，然后视为对其使用【杀】或获得其场上一张牌`]).set("ai", () => {
        const evt = _status.event.getParent(), player3 = evt.player, target2 = evt.target;
        const card2 = { name: "sha", isCard: true }, att = get.attitude(player3, target2) > 0;
        if (!target2.canUse(card2, player3, false) || get.effect(player3, card2, target2, player3) >= 0) {
          return 0;
        }
        if (att && (!player3.canUse(card2, target2, false) || get.effect(target2, card2, player3, player3) >= 0)) {
          return 1;
        }
        if (target2.hasSkill("nogain") && player3.canUse(card2, target2, false) && get.effect(target2, card2, player3, player3) > 0) {
          return 1;
        }
        if (player3.hasShan()) {
          return 0;
        }
        if (att && target2.hasShan()) {
          return 1;
        }
        return 0;
      }).forResult();
      const list = [player2, target];
      if (index == 1) {
        list.reverse();
      }
      await list[0].draw(2);
      const sha = get.autoViewAs({ name: "sha", isCard: true });
      const choices = [];
      const choiceList = [`视为对${get.translation(list[0])}使用一张【杀】`, `弃置${get.translation(list[0])}场上一张牌`];
      if (list[1].canUse("sha", list[0], false)) {
        choices.push("选项一");
      } else {
        choiceList[0] = '<span style="opacity:0.5">' + choiceList[0] + "</span>";
      }
      if (list[0].countGainableCards(list[1], "ej")) {
        choices.push("选项二");
      } else {
        choiceList[1] = '<span style="opacity:0.5">' + choiceList[1] + "</span>";
      }
      if (!choices.length) {
        return;
      }
      const control = choices.length == 1 ? choices[0] : (await list[1].chooseControl(choices).set("choiceList", choiceList).set("prompt", "悖逆：请选择一项").set("ai", () => {
        const player3 = get.player(), target2 = get.event().target;
        const eff2 = get.effect(target2, { name: "sha" }, player3, player3), eff1 = get.effect(target2, { name: "guohe_copy2" }, player3, player3);
        return eff1 > eff2 ? "选项一" : "选项二";
      }).set("target", list[0]).forResult()).control;
      if (control == "选项一") {
        await list[1].useCard(sha, list[0], false, "noai");
      } else {
        await list[1].gainPlayerCard(list[0], "ej", true);
      }
    }
  },
  mbdingfa: {
    audio: "dingfa",
    trigger: { player: "phaseDiscardAfter" },
    filter(event, player2) {
      let num = 0;
      player2.getHistory("lose", (evt) => {
        num += evt.cards2.length;
      });
      return num >= 4 && (player2.isDamaged() || game.hasPlayer((current) => current.hasDiscardableCards(player2, "he")));
    },
    async cost(event, trigger, player2) {
      const choices = [];
      const choiceList = ["回复1点体力", "弃置一名角色至多两张牌"];
      if (player2.isDamaged()) {
        choices.push("选项一");
      } else {
        choiceList[0] = '<span style="opacity:0.5">' + choiceList[0] + "</span>";
      }
      if (game.hasPlayer((current) => current.hasDiscardableCards(player2, "he"))) {
        choices.push("选项二");
      } else {
        choiceList[1] = '<span style="opacity:0.5">' + choiceList[1] + "</span>";
      }
      const { control } = await player2.chooseControl(choices, "cancel2").set("choiceList", choiceList).set("prompt", get.prompt(event.name.slice(0, -5))).set("ai", () => {
        const player3 = get.player();
        const choices2 = get.event().controls.slice().remove("cancel2");
        const eff = get.recoverEffect(player3, player3, player3);
        if (!game.hasPlayer((current) => get.effect(current, { name: "guohe_copy2" }, player3, player3) > eff)) {
          choices2.remove("选项二");
        } else if (choices2.includes("选项二")) {
          return "选项二";
        }
        if (eff <= 0) {
          choices2.remove("选项一");
        }
        if (!choices2.length) {
          return "cancel2";
        }
        return choices2.randomGet();
      }).forResult();
      event.result = {
        bool: control != "cancel2",
        cost_data: control
      };
    },
    async content(event, trigger, player2) {
      if (event.cost_data == "选项一") {
        await player2.recover();
      } else {
        const targets2 = game.filterPlayer((current) => current.hasDiscardableCards(player2, "he"));
        if (!targets2.length) {
          return;
        }
        const result = await player2.chooseTarget(
          "选择一名角色弃置其至多两张牌",
          (card2, player3, target2) => {
            return get.event().targets?.includes(target2);
          },
          true
        ).set("targets", targets2).set("ai", (target2) => {
          const player3 = get.player();
          return get.effect(target2, { name: "guohe_copy2" }, player3, player3);
        }).forResult();
        if (!result?.targets?.length) {
          return;
        }
        const target = result.targets[0];
        await player2.discardPlayerCard(target, "he", true, [1, 2]);
      }
    }
  },
  //司马伷
  mbbifeng: {
    audio: 3,
    trigger: { target: "useCardToTarget" },
    filter(event, player2, name) {
      if (event.targets?.length > 4) {
        return false;
      }
      return ["trick", "basic"].includes(get.type(event.card));
    },
    prompt2(event, player2) {
      return `取消你为${get.translation(event.card)}的目标。若如此做，此牌结算完成后若没有其他角色响应此牌，你失去1点体力，否则你摸两张牌。`;
    },
    check(event, player2) {
      let cancel = get.effect(player2, event.card, event.player, player2), name = event.card.name;
      if (get.effect(player2, { name: "losehp" }, player2, player2) - cancel > 0) {
        return true;
      }
      if (2 * get.effect(player2, { name: "draw" }, player2, player2) - cancel <= 0) {
        return false;
      }
      const targets2 = event.targets.filter((current) => {
        return player2 !== current && get.effect(current, event.card, event.player, current) < 0;
      });
      if (name === "sha") {
        return targets2.some((target) => {
          return target.mayHaveShan(player2, "use");
        });
      }
      if (name === "juedou" || name === "nanman") {
        return targets2.some((target) => {
          return target.mayHaveSha(player2, "respond");
        });
      }
      if (name === "wanjian") {
        return targets2.some((target) => {
          return target.mayHaveShan(player2, "respond");
        });
      }
      if (name === "qizhengxiangsheng") {
        return targets2.some((target) => {
          return target.mayHaveSha(player2, "respond") || target.mayHaveShan(player2, "respond");
        });
      }
      return false;
    },
    logAudio: () => 1,
    async content(event, trigger, player2) {
      player2.addTempSkill(event.name + "_effect");
      player2.markAuto(event.name + "_effect", [trigger.card]);
      trigger.targets.remove(player2);
      trigger.getParent().triggeredTargets2.remove(player2);
      trigger.untrigger();
    },
    subSkill: {
      effect: {
        audio: "mbbifeng",
        trigger: { global: "useCardAfter" },
        filter(event, player2, name) {
          return player2.getStorage("mbbifeng_effect").includes(event.card);
        },
        forced: true,
        popup: false,
        logAudio: () => ["mbbifeng2.mp3", "mbbifeng3.mp3"],
        async content(event, trigger, player2) {
          player2.unmarkAuto(event.name, [trigger.card]);
          if (!player2.getStorage(event.name).length) {
            player2.removeSkill(event.name);
          }
          if (game.hasPlayer((current) => {
            if (current == player2) {
              return false;
            }
            let respondEvts = [];
            respondEvts.addArray(current.getHistory("useCard")).addArray(current.getHistory("respond"));
            respondEvts = respondEvts.filter((evt) => evt.respondTo).map((evt) => evt.respondTo);
            return respondEvts.some((list) => {
              return list[1] == trigger.card;
            });
          })) {
            player2.logSkill(event.name, null, null, null, [3]);
            await player2.draw(2);
          } else {
            player2.logSkill(event.name, null, null, null, [2]);
            await player2.loseHp();
          }
        }
      }
    }
  },
  mbsuwang: {
    audio: 2,
    trigger: { global: "phaseEnd" },
    filter(event, player2) {
      if (!event.player.hasHistory("useCard", (evt) => {
        return (evt.stocktargets || []).includes(player2) || (evt.targets || []).includes(player2);
      })) {
        return false;
      }
      if (get.mode() == "versus" && _status.mode == "two") {
        return player2.getHistory("damage").reduce((numx, evt) => numx + evt.num, 0) <= 1;
      }
      return !player2.hasHistory("damage");
    },
    forced: true,
    locked: false,
    async content(event, trigger, player2) {
      const next = player2.addToExpansion(get.cards(1), "draw");
      next.gaintag.add(event.name);
      await next;
    },
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player2, skill) {
      const cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    group: "mbsuwang_draw",
    subSkill: {
      draw: {
        audio: "mbsuwang",
        trigger: { player: "phaseDrawBegin1" },
        filter(event, player2) {
          return !event.numFixed && player2.hasExpansions("mbsuwang");
        },
        prompt2(event, player2) {
          const cards2 = player2.getExpansions("mbsuwang");
          return `改为获得${get.translation(cards2)}，然后你可以令一名其他角色摸两张牌`;
        },
        check(event, player2) {
          const cards2 = player2.getExpansions("mbsuwang");
          return event.num <= cards2.length;
        },
        async content(event, trigger, player2) {
          const cards2 = player2.getExpansions("mbsuwang");
          trigger.changeToZero();
          if (cards2.length) {
            await player2.gain(cards2, "gain2");
          }
          if (game.hasPlayer((current) => current != player2)) {
            const result = await player2.chooseTarget("宿望：你可以令一名其他角色摸两张牌", lib.filter.notMe).set("ai", (target) => {
              const player3 = get.player();
              return get.effect(target, { name: "draw" }, player3, player3);
            }).forResult();
            if (result?.bool) {
              player2.line(result.targets[0], "green");
              await result.targets[0].draw(2);
            }
          }
        }
      }
    }
  },
  //文钦
  mbbeiming: {
    audio: 2,
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player2) {
      return event.name != "phase" || game.phaseNumber == 0;
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt(event.name.slice(0, -5)), "令至多两名角色获得武器牌", [1, 2]).set("ai", (target) => {
        return get.attitude(get.player(), target);
      }).forResult();
    },
    async content(event, trigger, player2) {
      const targets2 = event.targets;
      for (const target of targets2) {
        const suits = [];
        for (const card2 of target.getCards("h")) {
          suits.add(get.suit(card2));
        }
        const equip = get.cardPile2((card2) => {
          if (get.subtype(card2) != "equip1") {
            return false;
          }
          const info = get.info(card2, false);
          if (!info) {
            return false;
          }
          if (!info.distance || typeof info.distance.attackFrom != "number") {
            return suits.length == 1;
          }
          return 1 - info.distance.attackFrom == suits.length;
        });
        if (equip) {
          await target.gain(equip, "gain2");
        }
      }
    }
  },
  mbchoumang: {
    audio: 2,
    trigger: {
      player: "useCardToPlayered",
      target: "useCardToTargeted"
    },
    usable: 1,
    filter(event, player2) {
      return event.card.name == "sha" && event.targets.length == 1;
    },
    async cost(event, trigger, player2) {
      const list = ["选项一", "选项二"], target = event.triggername == "useCardToPlayered" ? trigger.target : trigger.player;
      if (player2.getEquip(1) || target.getEquip(1)) {
        list.push("背水！");
      }
      list.push("cancel2");
      const result = await player2.chooseControl(list).set("choiceList", ["令此【杀】伤害+1", "若此【杀】被【闪】抵消，你可以获得你距离1以内的一名其他角色区域里的一张牌", "背水！弃置你与其装备区的武器牌并执行所有选项"]).set("prompt", get.prompt(event.skill)).set(
        "resultx",
        (function() {
          let eff = 0;
          for (const targetx of trigger.targets) {
            eff += get.effect(targetx, trigger.card, trigger.player, player2);
          }
          const bool = game.hasPlayer((current) => player2 != current && get.distance(player2, current) <= 1 && get.effect(current, { name: "shunshou_copy2" }, player2, player2) > 0);
          if (list.includes("背水！") && eff > 0 && bool) {
            return "背水！";
          }
          if (bool) {
            return "选项二";
          }
          if (eff > 0) {
            return "选项一";
          }
          return "cancel2";
        })()
      ).set("ai", function() {
        return _status.event.resultx;
      }).forResult();
      event.result = {
        bool: result.control != "cancel2",
        targets: [target],
        cost_data: result.control
      };
    },
    async content(event, trigger, player2) {
      const result = event.cost_data, target = event.targets[0];
      if (result == "背水！") {
        const list = [];
        if (player2.getEquips(1).length) {
          list.push([player2, player2.getEquips(1)]);
        }
        if (target.getEquips(1).length) {
          list.push([target, target.getEquips(1)]);
        }
        await game.loseAsync({
          lose_list: list,
          discarder: player2
        }).setContent("discardMultiple");
      }
      if (result != "选项二") {
        trigger.getParent().baseDamage++;
        await game.delay();
      }
      if (result != "选项一") {
        player2.addTempSkill("mbchoumang_effect");
        player2.markAuto("mbchoumang_effect", trigger.card);
      }
    },
    subSkill: {
      effect: {
        audio: "mbchoumang",
        trigger: { global: "shaMiss" },
        filter(event, player2) {
          if (!player2.getStorage("mbchoumang_effect").includes(event.card)) {
            return false;
          }
          return game.hasPlayer((current) => player2 != current && get.distance(player2, current) <= 1 && current.hasGainableCards(player2, "hej"));
        },
        charlotte: true,
        onremove: true,
        async cost(event, trigger, player2) {
          event.result = await player2.chooseTarget("仇铓：是否获得你距离1以内的一名其他角色区域里的一张牌？", function(card2, player3, target) {
            return player3 != target && get.distance(player3, target) <= 1 && target.hasGainableCards(player3, "hej");
          }).set("ai", function(target) {
            const player3 = _status.event.player;
            return get.effect(target, { name: "shunshou_copy2" }, player3, player3);
          }).forResult();
        },
        async content(event, trigger, player2) {
          const target = event.targets[0];
          await player2.gainPlayerCard(target, "hej", true);
          player2.unmarkAuto(event.name, [trigger.card]);
          if (!player2.getStorage(event.name).length) {
            player2.removeSkill(event.name);
          }
        }
      }
    }
  },
  //张布
  mbchengxiong: {
    audio: 2,
    trigger: { player: "useCardToTargeted" },
    filter(event, player2) {
      if (get.type2(event.card) !== "trick" || !event.isFirstTarget || event.targets.includes(player2)) {
        return false;
      }
      const num = lib.skill.mbchengxiong.phaseUsed(event, player2);
      return game.hasPlayer((current) => current !== player2 && current.countCards("he") >= num);
    },
    phaseUsed(event, player2) {
      let phase = null;
      for (let i of lib.phaseName) {
        if (event.getParent(i, true)) {
          phase = i;
          break;
        }
      }
      if (!phase) {
        return 0;
      }
      return player2.getHistory("useCard", (evt) => evt.getParent(phase) == event.getParent(phase)).length;
    },
    async cost(event, trigger, player2) {
      const num = lib.skill.mbchengxiong.phaseUsed(trigger, player2);
      event.result = await player2.chooseTarget(get.prompt2(event.skill), function(card2, player3, target) {
        const num2 = get.event().num;
        return target !== player3 && target.countCards("he") >= num2;
      }).set("num", num).set("color", get.color(trigger.card)).set("ai", function(target) {
        let player3 = get.player(), eff = get.effect(target, { name: "guohe_copy2" }, player3, player3);
        const color = get.event().color;
        if (target.getCards("e").some((card2) => get.color(card2) == color)) {
          eff += get.damageEffect(target, player3, player3) / 2;
        }
        return eff;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const result = await player2.discardPlayerCard("he", target, true).set("ai", function(button) {
        let val = get.buttonValue(button);
        if (get.attitude(_status.event.player, get.owner(button.link)) > 0) {
          val *= -1;
        }
        if (get.position(button.link) == "e" && get.color(button.link) == get.event().color) {
          return val *= 2;
        }
        return val;
      }).set("color", get.color(trigger.card)).forResult();
      if (result?.bool && get.color(result.links[0]) == get.color(trigger.card)) {
        await target.damage();
      }
    },
    locked: false,
    mod: {
      aiOrder(player2, card2, num) {
        if (get.type2(card2) == "trick") {
          return num + 10;
        }
      }
    }
  },
  mbwangzhuang: {
    audio: 2,
    trigger: { global: "damageEnd" },
    filter(event, player2) {
      if (event.card) {
        return false;
      }
      return [event.source, event.player].includes(player2);
    },
    logTarget(event, player2) {
      return _status.currentPhase || player2;
    },
    async content(event, trigger, player2) {
      await player2.draw();
      if (_status.currentPhase) {
        _status.currentPhase.addTempSkill("fengyin");
      }
    }
  },
  //王经
  mbzujin: {
    audio: 3,
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player2) {
      if (!player2.countCards("hse", (card2) => get.type(card2) == "basic")) {
        return false;
      }
      if (player2.isDamaged()) {
        if (event.filterCard(get.autoViewAs({ name: "shan" }, "unsure"), player2, event) && !player2.getStorage("mbzujin").includes("shan")) {
          return true;
        }
        if (event.filterCard(get.autoViewAs({ name: "wuxie" }, "unsure"), player2, event) && !player2.getStorage("mbzujin").includes("wuxie")) {
          return true;
        }
      }
      if (!player2.isDamaged() || !player2.isMinHp()) {
        if (event.filterCard(get.autoViewAs({ name: "sha" }, "unsure"), player2, event) && !player2.getStorage("mbzujin").includes("sha")) {
          return true;
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player2) {
        var list = [];
        if (player2.isDamaged()) {
          if (event.filterCard(get.autoViewAs({ name: "shan" }, "unsure"), player2, event) && !player2.getStorage("mbzujin").includes("shan")) {
            list.push(["基本", "", "shan"]);
          }
          if (event.filterCard(get.autoViewAs({ name: "wuxie" }, "unsure"), player2, event) && !player2.getStorage("mbzujin").includes("wuxie")) {
            list.push(["锦囊", "", "wuxie"]);
          }
        }
        if (!player2.isDamaged() || !player2.isMinHp() && !player2.getStorage("mbzujin").includes("sha")) {
          if (event.filterCard(get.autoViewAs({ name: "sha" }, "unsure"), player2, event)) {
            list.push(["基本", "", "sha"]);
          }
        }
        return ui.create.dialog("阻进", [list, "vcard"]);
      },
      check(button) {
        if (_status.event.getParent().type != "phase") {
          return 1;
        }
        var player2 = _status.event.player;
        return player2.getUseValue({
          name: button.link[2],
          nature: button.link[3]
        });
      },
      backup(links, player2) {
        return {
          audio: "mbzujin",
          filterCard: (card2) => get.type(card2) == "basic",
          popname: true,
          check(card2) {
            return 8 - get.value(card2);
          },
          logAudio(event, player3) {
            return "mbzujin" + (["sha", "shan", "wuxie"].indexOf(event.card.name) + 1) + ".mp3";
          },
          position: "hse",
          viewAs: { name: links[0][2], nature: links[0][3] },
          async precontent(event, trigger, player3) {
            if (!player3.getStorage("mbzujin").length) {
              player3.when({ global: "phaseEnd" }).step(async () => {
                player3.removeStorage("mbzujin", true);
              });
            }
            player3.markAuto("mbzujin", [event.result.card.name]);
          }
        };
      },
      prompt(links, player2) {
        return "将一张基本牌当做" + get.translation(links[0][2]) + "使用";
      }
    },
    hiddenCard(player2, name) {
      if (!player2.countCards("she", (card2) => get.type(card2) == "basic")) {
        return false;
      }
      if (player2.getStorage("mbzujin").includes(name)) {
        return false;
      }
      if (["shan", "wuxie"].includes(name)) {
        return player2.isDamaged();
      }
      if (name == "sha") {
        return !player2.isDamaged() || !player2.isMinHp();
      }
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2, tag) {
        if (!player2.countCards("hse", (card2) => get.type(card2) == "basic")) {
          return false;
        }
        if (tag == "respondSha") {
          return (!player2.isDamaged() || !player2.isMinHp()) && !player2.getStorage("mbzujin").includes("sha");
        }
        return player2.isDamaged() && !player2.getStorage("mbzujin").includes("shan");
      },
      order: 1,
      result: {
        player(player2) {
          if (_status.event.dying) {
            return get.attitude(player2, _status.event.dying);
          }
          return 1;
        }
      }
    },
    subSkill: { backup: {} }
  },
  mbjiejian: {
    audio: 3,
    trigger: {
      player: "phaseZhunbeiBegin"
    },
    filter(event, player2) {
      return player2.countCards("h");
    },
    async cost(event, trigger, player2) {
      if (_status.connectMode) {
        game.broadcastAll(function() {
          _status.noclearcountdown = true;
        });
      }
      const give_map = {};
      let used = [];
      do {
        const result = await player2.chooseCardTarget({
          filterCard(card2) {
            return get.itemtype(card2) == "card" && !card2.hasGaintag("mbjiejian_tag");
          },
          filterTarget: lib.filter.notMe,
          selectCard: [1, Infinity],
          prompt: used.length ? "是否继续分配手牌？" : get.prompt(event.skill),
          prompt2: "将任意张手牌交给一名其他角色",
          allowChooseAll: true,
          ai1(card2) {
            if (!ui.selected.cards.length) {
              return 8 - get.value(card2);
            }
            return 0;
          },
          ai2(target) {
            let player3 = _status.event.player, card2 = ui.selected.cards[0];
            let val = get.value(card2), att = get.attitude(player3, target);
            if (val <= 4) {
              if (get.event().used.includes(target)) {
                return 0;
              }
              return 1 / target.getUseValue(card2);
            }
            return att * (target.getUseValue(card2) + 4);
          }
        }).set("used", used).forResult();
        if (result?.bool && result.targets?.length) {
          const id = result.targets[0].playerid, map = give_map;
          if (!map[id]) {
            map[id] = [];
          }
          map[id].addArray(result.cards);
          player2.addGaintag(result.cards, "mbjiejian_tag");
          used.addArray(result.targets);
        }
        break;
      } while (player2.countCards("h"));
      if (_status.connectMode) {
        game.broadcastAll(function() {
          delete _status.noclearcountdown;
          game.stopCountChoose();
        });
      }
      const list = [], targets2 = [];
      for (const i in give_map) {
        const source = (_status.connectMode ? lib.playerOL : game.playerMap)[i];
        player2.line(source, "green");
        if (player2 !== source && (get.mode() !== "identity" || player2.identity !== "nei")) {
          player2.addExpose(0.2);
        }
        targets2.push(source);
        list.push([source, give_map[i]]);
      }
      event.result = {
        bool: list.length > 0,
        targets: targets2,
        cost_data: list
      };
    },
    logAudio: () => 1,
    async content(event, trigger, player2) {
      const list = event.cost_data;
      await game.loseAsync({
        gain_list: list,
        player: player2,
        cards: list.map((i) => i[1]).flat(),
        giver: player2,
        animate: "giveAuto"
      }).setContent("gaincardMultiple");
      for (let target of event.targets) {
        let num = target.hp - target.countMark("mbjiejian_mark");
        target.addMark("mbjiejian_mark", num, false);
      }
    },
    group: ["mbjiejian_liuli", "mbjiejian_remove"],
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      },
      liuli: {
        audio: "mbjiejian2.mp3",
        trigger: { global: "useCardToTarget" },
        filter(event, player2) {
          if (event.player == player2 || get.type(event.card) == "equip") {
            return false;
          }
          if (!event.targets || event.targets.length != 1) {
            return false;
          }
          if (!event.targets[0].hasMark("mbjiejian_mark")) {
            return false;
          }
          return !player2.getStorage("mbjiejian_used").includes(event.target);
        },
        prompt2(event, player2) {
          return `将${get.translation(event.card)}转移给自己`;
        },
        check(event, player2) {
          const target = event.targets[0];
          const card2 = event.card;
          const source = event.player;
          const eff1 = get.effect(target, card2, source, player2);
          if (eff1 > 0) {
            return false;
          }
          const eff2 = get.effect(player2, card2, source, player2) + get.effect(player2, { name: "draw" }, player2, player2);
          if (eff2 >= eff1) {
            return true;
          }
          let save = false;
          if (get.attitude(player2, target) > 2) {
            if (card2.name == "sha") {
              if (player2.hasShan() || player2.getEquip(2) || target.hp == 1 || player2.hp > target.hp + 1) {
                if (!target.hasShan() || target.countCards("h") < player2.countCards("h")) {
                  save = true;
                }
              }
            } else if (card2.name == "juedou" && target.hp == 1) {
              save = true;
            } else if (card2.name == "shunshou" && get.attitude(player2, source) < 0 && get.attitude(source, target) < 0) {
              save = true;
            }
          }
          return save;
        },
        logTarget: "target",
        async content(event, trigger, player2) {
          player2.addTempSkill("mbjiejian_used");
          player2.markAuto("mbjiejian_used", event.targets);
          const evt = trigger.getParent();
          evt.triggeredTargets2.removeArray(event.targets);
          evt.targets.removeArray(event.targets);
          if (lib.filter.targetEnabled2(trigger.card, trigger.player, player2)) {
            evt.targets.push(player2);
          }
          await player2.draw();
        }
      },
      remove: {
        audio: "mbjiejian3.mp3",
        trigger: { global: "phaseEnd" },
        forced: true,
        filter(event, player2) {
          return event.player.hasMark("mbjiejian_mark");
        },
        logTarget: "player",
        async content(event, trigger, player2) {
          const target = event.targets[0], num = target.countMark("mbjiejian_mark");
          target.removeMark("mbjiejian_mark", num, false);
          if (target.hp >= num) {
            await player2.draw(2);
          }
        }
      },
      mark: { intro: { content: "获得“节谏”时的体力值：$" } }
    }
  },
  //新司马孚
  mbpanxiang: {
    initSkill(skill) {
      if (!lib.skill[skill]) {
        lib.skill[skill] = {
          charlotte: true,
          onremove(player2, skill2) {
            delete player2.storage[skill2];
            player2.removeTip(skill2);
          },
          mark: true,
          marktext: "襄",
          intro: {
            markcount: () => 0,
            content: (storage) => `上次${get.translation(storage[1])}对你发动【蹒襄】选择的选项是：${storage[0]}`
          }
        };
        lib.translate[skill] = "蹒襄";
      }
    },
    init(player2, skill) {
      game.countPlayer((current) => {
        const history = game.getAllGlobalHistory("everything", (evt) => evt.name == skill && evt.player == player2 && evt.targets?.[0] == current);
        if (history.length) {
          const { control } = history.at(-1).cost_data;
          const mark = `${skill}_${player2.playerid}`;
          game.broadcastAll(lib.skill.mbpanxiang.initSkill, mark);
          const storage = [control, player2];
          current.storage[mark] = storage;
          current.addSkill(mark);
          current.markSkill(mark);
          current.addTip(mark, `${get.translation(mark)}(${get.translation(storage[1])}) ${storage[0]}`);
        }
      });
    },
    onremove(player2, skill) {
      delete player2.storage[skill];
      game.countPlayer((current) => {
        current.removeSkill(`${skill}_${player2.playerid}`);
      });
    },
    audio: 4,
    trigger: { global: "damageBegin3" },
    async cost(event, trigger, player2) {
      const { player: target, source, card: card2 } = trigger;
      const history = game.getAllGlobalHistory("everything", (evt) => evt.name == event.skill && evt.player == player2 && evt.targets?.[0] == target);
      const [SUB, ADD] = ["减伤", "加伤"];
      const list = ["减伤", "加伤"].filter((text) => {
        if (!history.length) {
          return true;
        }
        const { control } = history.at(-1).cost_data;
        return text !== control;
      });
      list.push("cancel2");
      let prompt = `${get.translation(target)}即将受到${source ? "来自" + get.translation(source) : "无来源"}的${trigger.num}点伤害，你可以选择一项：`;
      const choiceTexts = [`⒈令此伤害-1${source && source.isIn() ? "，" + get.translation(source) + "摸两张牌" : ""}；`, `⒉令此伤害+1，${get.translation(target)}摸三张牌。`];
      if (!list.includes(SUB)) {
        choiceTexts[0] = `<span style="text-decoration: line-through;">${choiceTexts[0]}（上次选过）</span>`;
      }
      if (!list.includes(ADD)) {
        choiceTexts[1] = `<span style="text-decoration: line-through;">${choiceTexts[1]}（上次选过）</span>`;
      }
      choiceTexts.forEach((text) => prompt += text);
      const result = await player2.chooseControl(list).set("prompt", get.prompt(event.skill, target)).set("prompt2", prompt).set("ai", () => {
        return get.event().choice;
      }).set(
        "choice",
        (() => {
          const damageEff = get.damageEffect(target, source, player2);
          const att = get.attitude(player2, target), attSource = get.attitude(player2, source);
          const canFilterDamage = target.hasSkillTag("filterDamage", null, {
            player: source,
            card: card2
          });
          if (list.includes(ADD)) {
            if (damageEff > 0) {
              if (!canFilterDamage && target.getHp() <= trigger.num + 1) {
                return ADD;
              }
            } else {
              if (att > 0 && (damageEff === 0 || canFilterDamage)) {
                return ADD;
              }
              if (target.getHp() + target.countCards("hs", (card3) => {
                return target.canSaveCard(card3, target);
              }) > trigger.num + 1 && !list.includes(SUB)) {
                return ADD;
              }
            }
          }
          if (list.includes(SUB)) {
            if (att > 0 && attSource >= 0) {
              return SUB;
            }
            if (canFilterDamage && att > 0) {
              return "cancel2";
            }
            if (damageEff > 0) {
              if (target.getHp() > trigger.num && attSource > 0 && source.countCards("h") + source.getHp() <= 4) {
                return SUB;
              }
            } else {
              if (att > 0) {
                if (trigger.num >= target.getHp()) {
                  return SUB;
                }
                if (source && !source.countCards("hs", (card3) => {
                  return source.canUse(card3, target, true) && get.effect(target, card3, source, player2) > 0;
                })) {
                  return Math.random() < 0.7 && list.includes(ADD) ? ADD : "cancel2";
                }
              } else {
                if (attSource >= 0) {
                  return SUB;
                }
                if (target.hasSkillTag("maixie") && trigger.num === 1 && damageEff < -20) {
                  return SUB;
                }
              }
            }
          }
          return "cancel2";
        })()
      ).forResult();
      if (result.control !== "cancel2") {
        event.result = {
          bool: true,
          cost_data: {
            control: result.control
          }
        };
      }
    },
    logTarget: "player",
    logAudio(event, player2, name, indexedData, evt) {
      const { control } = evt.cost_data;
      return control == "减伤" ? ["mbpanxiang1.mp3", "mbpanxiang2.mp3"] : ["mbpanxiang3.mp3", "mbpanxiang4.mp3"];
    },
    async content(event, trigger, player2) {
      const { control } = event.cost_data;
      const { player: target, source } = trigger;
      player2.markAuto("mbpanxiang_mark", [trigger.player]);
      const skill = `mbpanxiang_${player2.playerid}`;
      game.broadcastAll(lib.skill.mbpanxiang.initSkill, skill);
      const storage = [control, player2];
      target.storage[skill] = storage;
      target.addSkill(skill);
      target.markSkill(skill);
      target.addTip(skill, `${get.translation(skill)}(${get.translation(storage[1])}) ${storage[0]}`);
      if (control === "减伤") {
        trigger.num--;
        game.log(player2, "令此伤害", "#y-1");
        if (source && source.isIn()) {
          await source.draw(2);
        }
      } else {
        trigger.num++;
        game.log(player2, "令此伤害", "#y+1");
        await target.draw(3);
      }
    },
    subSkill: {
      mark: {
        mark: true,
        marktext: "襄",
        charlotte: true,
        intro: {
          content: "已对$发动过〖蹒襄〗"
        }
      }
    }
  },
  mbchenjie: {
    audio: 2,
    trigger: { global: "dieAfter" },
    filter(event, player2) {
      return player2.hasSkill("mbpanxiang", null, false, false) && player2.hasAllHistory("useSkill", (evt) => {
        return evt.skill === "mbpanxiang" && evt.targets.includes(event.player);
      });
    },
    forced: true,
    async content(event, trigger, player2) {
      const cards2 = player2.getCards("hej", (card2) => lib.filter.cardDiscardable(card2, player2, "mbchenjie"));
      if (cards2.length) {
        await player2.discard(cards2);
      }
      await player2.draw(4);
    },
    ai: {
      combo: "mbpanxiang"
    }
  },
  //李昭焦伯
  mbzuoyou: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    zhuanhuanji: true,
    filterTarget: true,
    prompt() {
      return get.info("mbzuoyou").intro.content(get.player().storage["mbzuoyou"]);
    },
    async content(event, trigger, player2) {
      const storage = player2.storage.mbzuoyou, target = event.target;
      if (event.name === "mbzuoyou") {
        player2.changeZhuanhuanji("mbzuoyou");
      }
      if (!storage) {
        await target.draw(3);
        await target.chooseToDiscard(2, true, "h");
      } else {
        await target.changeHujia(1, null, true);
      }
    },
    mark: true,
    marktext: "☯",
    intro: {
      content(storage) {
        get.mode() !== "versus" || _status.mode !== "two";
        if (!storage) {
          return "转换技。出牌阶段限一次，你可以令一名角色摸三张牌，然后其弃置两张手牌。";
        }
        return "转换技。出牌阶段限一次，你可以令一名角色获得1点护甲。";
      }
    },
    ai: {
      order(item, player2) {
        if (player2.storage.mbzuoyou && game.hasPlayer((current) => {
          return current !== player2 && get.effect(current, "mbzuoyou", player2, player2) > 0;
        })) {
          return get.order({ name: "zengbin" }) + 0.1;
        }
        return 2;
      },
      result: {
        target(player2, target) {
          let eff = 0;
          if (player2.storage.mbzuoyou) {
            eff = target.hujia < 5 ? 1 : 0;
          } else {
            eff = 1;
          }
          if (target === player2 && player2.hasSkill("mbshishou")) {
            eff /= 10;
          }
          return eff;
        }
      }
    }
  },
  mbshishou: {
    audio: 2,
    trigger: { player: "useSkillAfter" },
    filter(event, player2) {
      return event.skill === "mbzuoyou" && !event.targets.includes(player2);
    },
    forced: true,
    async content(event, trigger, player2) {
      await lib.skill.mbzuoyou.content(
        {
          target: player2
        },
        {},
        player2
      );
    },
    ai: {
      combo: "mbzuoyou"
    }
  },
  //成济
  mbkuangli: {
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return game.hasPlayer((current) => current !== player2);
    },
    forced: true,
    group: ["mbkuangli_target", "mbkuangli_remove"],
    async content(event, trigger, player2) {
      let targets2 = game.filterPlayer((current) => current !== player2).randomSort();
      targets2 = targets2.slice(0, Math.ceil(Math.random() * targets2.length));
      targets2.sortBySeat();
      player2.line(targets2, "thunder");
      targets2.forEach((current) => {
        current.addSkill("mbkuangli_mark");
      });
      await game.delayx();
    },
    subSkill: {
      target: {
        audio: "mbkuangli",
        trigger: { player: "useCardToPlayered" },
        filter(event, player2) {
          return event.target.hasSkill("mbkuangli_mark") && [player2, event.target].some((current) => current.countCards("he"));
        },
        forced: true,
        logTarget: "target",
        /*
        get usable() {
        	return get.mode() == "doudizhu" ? 1 : 2;
        },
        */
        usable: 2,
        async content(event, trigger, player2) {
          const target = trigger.target, list = [];
          const playerCards = player2.getCards("he", (card2) => {
            return lib.filter.cardDiscardable(card2, player2, "mbkuangli");
          });
          if (playerCards.length > 0) {
            list.push([player2, playerCards.randomGets(1)]);
          }
          const targetCards = target.getCards("he", (card2) => {
            return lib.filter.cardDiscardable(card2, target, "mbkuangli");
          });
          if (targetCards.length > 0) {
            list.push([target, targetCards.randomGets(1)]);
          }
          await game.loseAsync({
            lose_list: list,
            discarder: player2
          }).setContent("discardMultiple");
          await game.delayx();
          await player2.draw(2);
          await game.delayx();
        },
        ai: {
          effect: {
            player_use(card2, player2, target, current) {
              if (!target) {
                return;
              }
              const counttrigger = player2.storage.counttrigger;
              if (counttrigger && counttrigger.mbkuangli_target && counttrigger.mbkuangli_target >= lib.skill.mbkuangli_target.usable) {
                return;
              }
              if (target.hasSkill("mbkuangli_mark")) {
                if (get.attitude(player2, target) > 0) {
                  return 0.75;
                }
                return 1.25;
              }
            }
          }
        }
      },
      remove: {
        audio: "mbkuangli",
        trigger: { player: "phaseEnd" },
        filter(event, player2) {
          return game.hasPlayer((current) => current.hasSkill("mbkuangli_mark"));
        },
        forced: true,
        async content(event, trigger, player2) {
          game.countPlayer((current) => {
            if (current.hasSkill("mbkuangli_mark")) {
              player2.line(current);
              current.removeSkill("mbkuangli_mark");
            }
          });
        }
      },
      mark: {
        mark: true,
        marktext: "戾",
        charlotte: true,
        intro: {
          name: "狂戾",
          name2: "狂戾",
          content: "已拥有“狂戾”标记"
        }
      }
    }
  },
  mbxiongsi: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player2) {
      return player2.countCards("h") >= 3;
    },
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    filterCard: true,
    selectCard: [-1, -2],
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      const targets2 = game.filterPlayer((current) => current !== player2).sortBySeat();
      for (const target of targets2) {
        player2.line(target, "thunder");
        await target.loseHp();
      }
    },
    ai: {
      order(item, player2) {
        if (get.effect(player2, "mbxiongsi", player2) <= 0) {
          return 1;
        }
        if (player2.countCards("h") > 3 && player2.countCards("h", (card2) => {
          return player2.hasValueTarget(card2);
        }) > 0) {
          return 0.1;
        }
        return 8;
      },
      result: {
        player(player2) {
          let eff = 0;
          game.countPlayer((current) => {
            let effx = get.effect(current, { name: "losehp" }, player2, player2);
            if (get.attitude(player2, current) < -6 && current.getHp() <= 1) {
              effx *= 1.3;
            }
            eff += effx;
          });
          eff *= player2.getHp() + player2.countCards("hs", (card2) => player2.canSaveCard(card2, player2)) <= 2 ? 1.5 : 0.35;
          eff -= player2.getCards("h").map((card2) => {
            if (lib.filter.cardDiscardable(card2, player2, "mbxiongsi")) {
              return get.value(card2);
            }
            return 0;
          }).reduce((p, c) => p + c, 0);
          if (eff > 0) {
            return 2;
          }
          return -1;
        }
      }
    }
  },
  //SP母兵脸
  mbcuizhen: {
    audio: 2,
    trigger: {
      global: "phaseBefore",
      player: "enterGame"
    },
    filter(event, player2) {
      return (event.name != "phase" || game.phaseNumber == 0) && game.hasPlayer((current) => {
        return current !== player2 && current.hasEnabledSlot(1);
      });
    },
    async cost(event, trigger, player2) {
      const num = 3;
      event.result = await player2.chooseTarget(get.prompt(event.skill), "废除至多" + get.cnNumber(num) + "名其他角色的武器栏", [1, num], (card2, player3, target) => {
        return target !== player3 && target.hasEnabledSlot(1);
      }).set("ai", (target) => {
        const player3 = get.event().player;
        return (1 - get.attitude(player3, target)) * Math.sqrt(get.distance(player3, target));
      }).forResult();
    },
    group: ["mbcuizhen_inphase", "mbcuizhen_draw"],
    async content(event, trigger, player2) {
      const targets2 = event.targets.slice().sortBySeat();
      for (const target of targets2) {
        if (target.identityShown) {
          if (get.mode() != "identity" || player2.identity != "nei") {
            player2.addExpose(0.3);
          }
        }
        await target.disableEquip(1);
      }
      await game.delay();
    },
    subSkill: {
      inphase: {
        audio: "mbcuizhen",
        trigger: { player: "useCardToPlayered" },
        filter(event, player2) {
          if (!player2.isPhaseUsing()) {
            return false;
          }
          if (!get.is.damageCard(event.card)) {
            return false;
          }
          const target = event.target;
          return target !== player2 && target.countCards("h") >= target.getHp() && target.hasEnabledSlot(1);
        },
        prompt2: "废除其的武器栏",
        logTarget: "target",
        check(event, player2) {
          return get.attitude(player2, event.target) <= 0;
        },
        async content(event, trigger, player2) {
          await trigger.target.disableEquip(1);
          await game.delayx();
        }
      },
      draw: {
        audio: "mbcuizhen",
        trigger: { player: "phaseDrawBegin2" },
        forced: true,
        locked: false,
        filter(event, player2) {
          return !event.numFixed;
        },
        async content(event, trigger, player2) {
          trigger.num += Math.min(
            3,
            game.countPlayer((current) => {
              return current.countDisabledSlot(1);
            }) + 1
          );
        }
      }
    }
  },
  mbkuili: {
    audio: 2,
    trigger: {
      player: "damageEnd"
    },
    filter(event, player2) {
      return event.source && event.source.isIn() && event.source.hasDisabledSlot(1);
    },
    forced: true,
    async content(event, trigger, player2) {
      const source = trigger.source;
      player2.line(source, "green");
      await source.enableEquip(1, player2);
    },
    ai: {
      neg: true,
      effect: {
        target(card2, player2, target) {
          if (player2 && player2.isIn() && get.tag(card2, "damage") && player2.hasDisabledSlot(1)) {
            return [1, 0, 1, 1.5];
          }
        }
      }
    }
  },
  //曹髦  史?! 我求你别改了
  mbqianlong: {
    audio: 6,
    persevereSkill: true,
    trigger: {
      player: ["mbqianlong_beginAfter", "mbqianlong_addAfter", "mbweitongAfter"]
    },
    filter(event, player2) {
      let current = player2.additionalSkills?.mbqianlong?.length ?? 0;
      let target = player2.countMark("mbqianlong") == lib.skill.mbqianlong.maxMarkCount ? lib.skill.mbqianlong.derivation.length : Math.floor(player2.countMark("mbqianlong") / 25);
      return target > current;
    },
    forced: true,
    popup: false,
    locked: false,
    beginMarkCount: 20,
    maxMarkCount: 99,
    derivation: ["mbcmqingzheng", "mbcmjiushi", "mbcmfangzhu", "mbjuejin"],
    addMark(player2, num) {
      num = Math.min(num, lib.skill.mbqianlong.maxMarkCount - player2.countMark("mbqianlong"));
      player2.addMark("mbqianlong", num);
    },
    group: ["mbqianlong_begin", "mbqianlong_add", "mbqianlong_die"],
    async content(event, trigger, player2) {
      const derivation = lib.skill.mbqianlong.derivation, skills2 = player2.countMark("mbqianlong") == lib.skill.mbqianlong.maxMarkCount ? derivation : derivation.slice(0, Math.floor(player2.countMark("mbqianlong") / 25));
      player2.addAdditionalSkill("mbqianlong", skills2);
    },
    marktext: "道",
    intro: {
      name: "道心(潜龙)",
      name2: "道心",
      content: "当前道心数为#"
    },
    subSkill: {
      begin: {
        audio: "mbqianlong",
        persevereSkill: true,
        trigger: {
          global: "phaseBefore",
          player: "enterGame"
        },
        filter(event, player2) {
          return event.name != "phase" || game.phaseNumber == 0;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          const num = game.hasPlayer((current) => {
            return current !== player2 && current.group === "wei" && player2.hasZhuSkill("mbweitong", current);
          }) ? 60 : lib.skill.mbqianlong.beginMarkCount;
          lib.skill.mbqianlong.addMark(player2, num);
        }
      },
      add: {
        audio: "mbqianlong",
        persevereSkill: true,
        trigger: {
          player: ["gainAfter", "damageEnd"],
          source: "damageSource",
          global: "loseAsyncAfter"
        },
        filter(event, player2) {
          if (player2.countMark("mbqianlong") >= lib.skill.mbqianlong.maxMarkCount) {
            return false;
          }
          if (event.name === "damage") {
            return event.num > 0;
          }
          return event.getg(player2).length > 0;
        },
        getIndex(event, player2, triggername) {
          if (event.name === "damage") {
            return event.num;
          }
          return 1;
        },
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          let toAdd = 5 * (1 + (trigger.name === "damage") + (event.triggername === "damageSource"));
          lib.skill.mbqianlong.addMark(player2, toAdd);
        }
      },
      die: {
        trigger: {
          player: "dieBefore"
        },
        charlotte: true,
        firstDo: true,
        forced: true,
        popup: false,
        forceDie: true,
        async content(event, trigger, player2) {
          player2.changeSkin({ characterName: "mb_caomao" }, "mb_caomao_dead");
        }
      }
    }
  },
  mbweitong: {
    audio: 1,
    persevereSkill: true,
    zhuSkill: true,
    trigger: {
      player: "mbqianlong_beginBegin"
    },
    forced: true,
    locked: false,
    async content(event, trigger, player2) {
    },
    ai: {
      combo: "mbqianlong"
    }
  },
  old_mbcmqingzheng: {
    audio: "mbcmqingzheng",
    persevereSkill: true,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return player2.countCards("h") > 0;
    },
    direct: true,
    async content(event, trigger, player2) {
      var num = 1;
      var prompt = "###" + get.prompt("sbqingzheng") + "###弃置" + get.cnNumber(num) + "种花色的所有牌";
      var next = player2.chooseButton([prompt, [lib.suit.map((i) => ["", "", "lukai_" + i]), "vcard"]], [num, num + 1]);
      next.set("filterButton", (button) => {
        var player3 = _status.event.player;
        if (ui.selected.buttons.length >= get.event().num) {
          return false;
        }
        var cards3 = player3.getCards("h", { suit: button.link[2].slice(6) });
        return cards3.length > 0 && cards3.filter((card2) => lib.filter.cardDiscardable(card2, player3, "sbqingzheng")).length == cards3.length;
      });
      next.set("num", num);
      next.set("ai", (button) => {
        var player3 = _status.event.player;
        return 15 - player3.getCards("h", { suit: button.link[2].slice(6) }).map((i) => get.value(i)).reduce((p, c) => p + c, 0);
      });
      next.set("custom", {
        replace: {
          button(button) {
            if (!_status.event.isMine()) {
              return;
            }
            if (button.classList.contains("selectable") == false) {
              return;
            }
            var cards3 = _status.event.player.getCards("h", {
              suit: button.link[2].slice(6)
            });
            if (cards3.length) {
              var chosen = cards3.filter((i) => ui.selected.cards.includes(i)).length == cards3.length;
              if (chosen) {
                ui.selected.cards.removeArray(cards3);
                cards3.forEach((card2) => {
                  card2.classList.remove("selected");
                  card2.updateTransform(false);
                });
              } else {
                ui.selected.cards.addArray(cards3);
                cards3.forEach((card2) => {
                  card2.classList.add("selected");
                  card2.updateTransform(true);
                });
              }
            }
            if (button.classList.contains("selected")) {
              ui.selected.buttons.remove(button);
              button.classList.remove("selected");
              if (_status.multitarget || _status.event.complexSelect) {
                game.uncheck();
                game.check();
              }
            } else {
              button.classList.add("selected");
              ui.selected.buttons.add(button);
            }
            var custom = _status.event.custom;
            if (custom && custom.add && custom.add.button) {
              custom.add.button();
            }
            game.check();
          }
        },
        add: next.custom.add
      });
      const result = await next.forResult();
      if (!result.bool) {
        return;
      }
      let cards2 = result.cards;
      if (!cards2.length) {
        const suits = result.links.map((i) => i[2].slice(6));
        cards2 = player2.getCards("h", (card2) => suits.includes(get.suit(card2, player2)));
      }
      if (!cards2.length) {
        return;
      }
      const result2 = await player2.chooseTarget("清正：观看一名其他角色的手牌并弃置其中一种花色的所有牌", (card2, player3, target2) => {
        return target2 != player3 && target2.countCards("h");
      }).set("ai", (target2) => {
        var player3 = _status.event.player, att = get.attitude(player3, target2);
        if (att >= 0) {
          return 0;
        }
        return 1 - att / 2 + Math.sqrt(target2.countCards("h"));
      }).forResult();
      if (!result2.bool) {
        return;
      }
      const target = result2.targets[0];
      player2.logSkill("mbcmqingzheng", target);
      await player2.discard(cards2);
      const list = lib.suit.slice().reverse().concat("none").filter((i) => target.hasCard({ suit: i }, "h"));
      event.videoId = lib.status.videoId++;
      function createDialog(target2, id) {
        var dialog = ui.create.dialog("清正：弃置" + get.translation(target2) + "一种花色的所有牌");
        dialog.addNewRow({ item: get.translation("heart"), retio: 1 }, { item: target2.getCards("h", { suit: "heart" }), ratio: 3 }, { item: get.translation("diamond"), retio: 1 }, { item: target2.getCards("h", { suit: "diamond" }), ratio: 3 });
        dialog.addNewRow({ item: get.translation("spade"), retio: 1 }, { item: target2.getCards("h", { suit: "spade" }), ratio: 3 }, { item: get.translation("club"), retio: 1 }, { item: target2.getCards("h", { suit: "club" }), ratio: 3 });
        if (target2.hasCard({ suit: "none" }, "h")) {
          dialog.classList.add("fullheight");
          dialog.addNewRow({ item: get.translation("none"), retio: 1 }, { item: target2.getCards("h", { suit: "none" }), ratio: 8 });
        }
        dialog.css({ height: "60%" });
        dialog.videoId = id;
      }
      if (event.isMine()) {
        createDialog(target, event.videoId);
      } else if (player2.isOnline2()) {
        player2.send(createDialog, target, event.videoId);
      }
      let control;
      if (list.length) {
        control = (await player2.chooseControl(list).set("dialog", get.idDialog(event.videoId)).set("ai", () => {
          return _status.event.control;
        }).set(
          "control",
          (() => {
            var getv = (cards3) => cards3.map((i) => get.value(i)).reduce((p, c) => p + c, 0);
            return list.sort((a, b) => {
              return getv(target.getCards("h", { suit: b })) - getv(target.getCards("h", { suit: a }));
            })[0];
          })()
        ).forResult()).control;
      }
      game.broadcastAll("closeDialog", event.videoId);
      const cards22 = target.getCards("h", { suit: control });
      await target.modedDiscard(cards22, player2);
      if (cards22.length < cards2.length) {
        await target.damage();
      }
    }
  },
  mbcmqingzheng: {
    audio: 2,
    persevereSkill: true,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return player2.countCards("h") > 0 && game.hasPlayer((current) => player2 != current && current.countCards("h") > 0);
    },
    /**
     * player选择target的一种花色的牌
     * @param {Player} player
     * @param {Player} target
     */
    chooseOneSuitCard(player2, target, force = false, limit, str = "请选择一个花色的牌", ai = { bool: false }) {
      const { promise, resolve } = Promise.withResolvers();
      const event = _status.event;
      event.selectedCards = [];
      event.selectedButtons = [];
      let suitCards = Object.groupBy(target.getCards("h"), (c) => get.suit(c, target));
      suitCards.heart ??= [];
      suitCards.diamond ??= [];
      suitCards.spade ??= [];
      suitCards.club ??= [];
      let dialog = event.dialog = ui.create.dialog();
      dialog.classList.add("fullheight");
      event.control_ok = ui.create.control("ok", (link) => {
        _status.imchoosing = false;
        event.dialog.close();
        event.control_ok?.close();
        event.control_cancel?.close();
        event._result = {
          bool: true,
          cards: event.selectedCards
        };
        resolve(event._result);
        game.resume();
      });
      event.control_ok.classList.add("disabled");
      if (!force) {
        event.control_cancel = ui.create.control("cancel", (link) => {
          _status.imchoosing = false;
          event.dialog.close();
          event.control_ok?.close();
          event.control_cancel?.close();
          event._result = {
            bool: false
          };
          resolve(event._result);
          game.resume();
        });
      }
      event.switchToAuto = function() {
        _status.imchoosing = false;
        event.dialog?.close();
        event.control_ok?.close();
        event.control_cancel?.close();
        event._result = ai();
        resolve(event._result);
        game.resume();
      };
      dialog.addNewRow(str);
      let keys = Object.keys(suitCards).sort((a, b) => {
        let arr = ["spade", "heart", "club", "diamond", "none"];
        return arr.indexOf(a) - arr.indexOf(b);
      });
      while (keys.length) {
        let createCustom = function(suit, count) {
          return function(itemContainer) {
            function formatStr(str2) {
              return str2.replace(/(?:♥︎|♦︎)/g, '<span style="color: red; ">$&</span>');
            }
            let div = ui.create.div(itemContainer);
            if (count) {
              div.innerHTML = formatStr(`${get.translation(suit)}牌${count}张`);
            } else {
              div.innerHTML = formatStr(`没有${get.translation(suit)}牌`);
            }
            div.css({
              position: "absolute",
              width: "100%",
              bottom: "1%",
              height: "35%",
              background: "#352929bf",
              display: "flex",
              justifyContent: "center",
              alignItems: "center",
              fontSize: "1.2em",
              zIndex: "2"
            });
          };
        };
        let key1 = keys.shift();
        let cards1 = suitCards[key1];
        let key2 = keys.shift();
        let cards2 = suitCards[key2];
        const clickItemContainer = function(container, item, allContainer) {
          if (!item?.length || item.some((card2) => !lib.filter.cardDiscardable(card2, player2, event.name))) {
            return;
          }
          if (event.selectedButtons.includes(container)) {
            container.classList.remove("selected");
            event.selectedButtons.remove(container);
            event.selectedCards.removeArray(item);
          } else {
            if (event.selectedButtons.length >= limit) {
              let precontainer = event.selectedButtons[0];
              precontainer.classList.remove("selected");
              event.selectedButtons.remove(precontainer);
              let suit = get.suit(event.selectedCards[0], target), cards3 = target.getCards("h", { suit });
              event.selectedCards.removeArray(cards3);
            }
            container.classList.add("selected");
            event.selectedButtons.add(container);
            event.selectedCards.addArray(item);
          }
          event.control_ok.classList[event.selectedButtons.length === limit ? "remove" : "add"]("disabled");
        };
        let itemContainerCss = {
          border: "solid #c6b3b3 2px",
          minHeight: "100px"
        };
        if (key2) {
          dialog.addNewRow(
            {
              item: cards1,
              ItemNoclick: true,
              //卡牌不需要被点击
              clickItemContainer,
              custom: createCustom(key1, cards1.length),
              //添加封条
              itemContainerCss
            },
            {
              item: cards2,
              ItemNoclick: true,
              //卡牌不需要被点击
              clickItemContainer,
              custom: createCustom(key2, cards2.length),
              itemContainerCss
            }
          );
        } else {
          dialog.addNewRow({
            item: cards1,
            ItemNoclick: true,
            //卡牌不需要被点击
            clickItemContainer,
            custom: createCustom(key1, cards1.length),
            itemContainerCss
          });
        }
      }
      game.pause();
      dialog.open();
      _status.imchoosing = true;
      return promise;
    },
    async cost(event, trigger, player2) {
      const list = get.addNewRowList(player2.getCards("h"), "suit", player2);
      let limit = event.skill === "sbqingzheng" ? 3 - player2.countMark("sbjianxiong") : 1;
      const result = await player2.chooseButtonTarget({
        createDialog: [
          [
            [[`${get.prompt(event.skill)}<div class="text center">${get.translation(event.skill, "info")}</div>`], "addNewRow"],
            [
              (dialog) => {
                dialog.classList.add("fullheight");
                dialog.forcebutton = false;
                dialog._scrollset = false;
              },
              "handle"
            ],
            list.map((item) => [Array.isArray(item) ? item : [item], "addNewRow"])
          ]
        ],
        filterButton(button) {
          const player3 = get.player();
          if (!button.links.length || button.links.some((card2) => !lib.filter.cardDiscardable(card2, player3, get.event().getParent().skill))) {
            return false;
          }
          return true;
        },
        selectButton: limit,
        limit,
        filterTarget(card2, player3, target) {
          return target != player3 && target.countCards("h");
        },
        ai1(button) {
          const player3 = get.player();
          if (!game.hasPlayer((current) => player3 != current && current.countDiscardableCards(player3, "h") > 0 && get.attitude(player3, current) < 0)) {
            return 0;
          }
          let values = button.links.map((i) => get.value(i)).reduce((p, c) => p + c, 0) / button.links.length;
          if (button.links.length > 4 || values > 6) {
            return 0;
          }
          return (13 - button.links.length) / values;
        },
        ai2(target) {
          const player3 = get.player(), att = get.attitude(player3, target);
          if (att >= 0) {
            return 0;
          }
          return 1 - att / 2 + Math.sqrt(target.countCards("h"));
        }
      }).forResult();
      event.result = {
        bool: result?.bool,
        cost_data: result?.links,
        targets: result?.targets
      };
      if (event.result.bool && result?.links?.length) {
        event.result.cards = player2.getCards("h").filter((card2) => result.links.includes(get.suit(card2, player2)));
      }
    },
    async content(event, trigger, player2) {
      const {
        targets: [target],
        cards: cards1
      } = event;
      await player2.discard(cards1);
      if (!target.countCards("h") || lib.suits.slice().filter((suit) => target.hasCard((card2, playerx) => get.suit(card2, playerx) === suit, "h")).every((suit) => target.hasCard((card2, playerx) => get.suit(card2, playerx) === suit && !lib.filter.cardDiscardable(card2, player2), "h"))) {
        if (target.countCards("h")) {
          const content = [`###清正###<div class="text center">${get.translation(target)}的手牌</div>`, target.getCards("h")];
          await player2.chooseControl("ok").set("dialog", content);
        }
        return;
      }
      const list = get.addNewRowList(target.getCards("h"), "suit", target);
      let result = await player2.chooseButton(
        [
          [
            [[`清正：弃置${get.translation(target)}一种花色的所有牌`], "addNewRow"],
            [
              (dialog) => {
                dialog.classList.add("fullheight");
                dialog.forcebutton = false;
                dialog._scrollset = false;
              },
              "handle"
            ],
            list.map((item) => [Array.isArray(item) ? item : [item], "addNewRow"])
          ]
        ],
        true
      ).set("filterButton", (button) => {
        const player3 = get.player();
        if (!button.links.length || button.links.some((card2) => !lib.filter.cardDiscardable(card2, player3, get.event().getParent().name))) {
          return false;
        }
        return true;
      }).set("ai", (button) => {
        get.player();
        return button.links.length;
      }).forResult();
      if (!result?.links?.length) {
        return;
      }
      let cards2 = target.getCards("h", (card2) => result.links.includes(get.suit(card2, target)));
      if (cards2.length) {
        cards2 = (await target.modedDiscard(cards2, player2).forResult()).cards;
      }
      if (cards1.length > cards2.length) {
        await target.damage(player2);
      }
      if (event.name !== "sbqingzheng" || player2.countMark("sbjianxiong") >= 2) {
        return;
      }
      if (["sbjianxiong", "jdjianxiong"].some((skill) => player2.hasSkill(skill, null, null, false))) {
        result = await player2.chooseBool("是否获得1枚“治世”？").set("choice", Math.random() >= 0.5).forResult();
        if (result?.bool) {
          player2.addMark("sbjianxiong", 1);
        }
      }
    }
  },
  mbcmjiushi: {
    audio: 2,
    inherit: "rejiushi",
    persevereSkill: true,
    group: ["mbcmjiushi_use", "mbcmjiushi_turnback", "mbcmjiushi_gain"],
    subSkill: {
      use: {
        hiddenCard(player2, name) {
          if (name == "jiu") {
            return !player2.isTurnedOver();
          }
          return false;
        },
        audio: "mbcmjiushi",
        enable: "chooseToUse",
        filter(event, player2) {
          if (player2.classList.contains("turnedover")) {
            return false;
          }
          return event.filterCard({ name: "jiu", isCard: true }, player2, event);
        },
        async content(event, trigger, player2) {
          if (_status.event.getParent(2).type == "dying") {
            event.dying = player2;
            event.type = "dying";
          }
          await player2.turnOver();
          await player2.useCard({ name: "jiu", isCard: true }, player2);
        },
        ai: {
          save: true,
          skillTagFilter(player2, tag, arg) {
            return !player2.isTurnedOver() && _status.event?.dying == player2;
          },
          order: 5,
          result: {
            player(player2) {
              if (_status.event.parent.name == "phaseUse") {
                if (player2.countCards("h", "jiu") > 0) {
                  return 0;
                }
                if (player2.getEquip("zhuge") && player2.countCards("h", "sha") > 1) {
                  return 0;
                }
                if (!player2.countCards("h", "sha")) {
                  return 0;
                }
                var targets2 = [];
                var target;
                var players = game.filterPlayer();
                for (var i = 0; i < players.length; i++) {
                  if (get.attitude(player2, players[i]) < 0) {
                    if (player2.canUse("sha", players[i], true, true)) {
                      targets2.push(players[i]);
                    }
                  }
                }
                if (targets2.length) {
                  target = targets2[0];
                } else {
                  return 0;
                }
                var num = get.effect(target, { name: "sha" }, player2, player2);
                for (var i = 1; i < targets2.length; i++) {
                  var num2 = get.effect(targets2[i], { name: "sha" }, player2, player2);
                  if (num2 > num) {
                    target = targets2[i];
                    num = num2;
                  }
                }
                if (num <= 0) {
                  return 0;
                }
                var e2 = target.getEquip(2);
                if (e2) {
                  if (e2.name == "tengjia") {
                    if (!player2.countCards("h", { name: "sha", nature: "fire" }) && !player2.getEquip("zhuque")) {
                      return 0;
                    }
                  }
                  if (e2.name == "renwang") {
                    if (!player2.countCards("h", { name: "sha", color: "red" })) {
                      return 0;
                    }
                  }
                  if (e2.name == "baiyin") {
                    return 0;
                  }
                }
                if (player2.getEquip("guanshi") && player2.countCards("he") > 2) {
                  return 1;
                }
                return target.countCards("h") > 3 ? 0 : 1;
              }
              if (player2 == _status.event.dying || player2.isTurnedOver()) {
                return 3;
              }
            }
          },
          effect: {
            target(card2, player2, target) {
              if (target.isTurnedOver()) {
                if (get.tag(card2, "damage")) {
                  if (player2.hasSkillTag("jueqing", false, target)) {
                    return [1, -2];
                  }
                  if (target.hp == 1) {
                    return;
                  }
                  return [1, target.countCards("h") / 2];
                }
              }
            }
          }
        }
      },
      turnback: {
        audio: "mbcmjiushi",
        persevereSkill: true,
        trigger: { player: "damageEnd" },
        check(event, player2) {
          return player2.isTurnedOver();
        },
        filter(event, player2) {
          if (player2.hasHistory("useCard", (evt) => {
            if (evt.card.name != "jiu" || evt.getParent().name != "mbcmjiushi_use") {
              return false;
            }
            return evt.getParent("damage", true) == event;
          })) {
            return false;
          }
          return player2.isTurnedOver();
        },
        prompt(event, player2) {
          return "是否发动【酒诗】，将武将牌翻面？";
        },
        async content(event, trigger, player2) {
          await player2.turnOver();
        }
      },
      gain: {
        audio: "mbcmjiushi",
        persevereSkill: true,
        trigger: { player: "turnOverAfter" },
        frequent: true,
        prompt: "是否发动【酒诗】，获得牌堆中的一张锦囊牌？",
        async content(event, trigger, player2) {
          var card2 = get.cardPile2(function(card3) {
            return get.type2(card3) == "trick";
          });
          if (card2) {
            await player2.gain(card2, "draw");
          }
        }
      }
    }
  },
  mbcmfangzhu: {
    audio: 2,
    persevereSkill: true,
    inherit: "sbfangzhu",
    filter(event, player2) {
      const target = player2.storage.mbcmfangzhu;
      return game.hasPlayer((current) => current !== player2 && (target ? target != current : true));
    },
    usable: 1,
    chooseButton: {
      dialog() {
        const dialog = ui.create.dialog("放逐：令一名其他角色...", "hidden");
        dialog.add([
          [
            [1, "不能使用手牌中的非锦囊牌直到其回合结束"],
            [2, "非Charlotte技能失效直到其回合结束"]
          ],
          "textbutton"
        ]);
        return dialog;
      },
      check(button) {
        const player2 = get.player();
        if (button.link === 2) {
          if (game.hasPlayer((target) => {
            if (target.hasSkill("mbcmfangzhu_ban") || target.hasSkill("fengyin") || target.hasSkill("baiban")) {
              return false;
            }
            return get.attitude(player2, target) < 0 && ["name", "name1", "name2"].map((sum, name) => {
              if (target[name] && (name != "name1" || target.name != target.name1)) {
                if (get.character(target[name])) {
                  return get.rank(target[name], true);
                }
              }
              return 0;
            }).reduce((p, c) => {
              return p + c;
            }, 0) > 5;
          })) {
            return 6;
          }
        }
        return button.link === 1 ? 1 : 0;
      },
      backup(links, player2) {
        return {
          num: links[0],
          audio: "mbcmfangzhu",
          filterCard: () => false,
          selectCard: -1,
          filterTarget(card2, player3, target) {
            if (target == player3) {
              return false;
            }
            const num = lib.skill.mbcmfangzhu_backup.num, storage = target.getStorage("mbcmfangzhu_ban"), targetx = player3.storage.mbcmfangzhu;
            if (target == targetx) {
              return false;
            }
            return num != 1 || !storage.length;
          },
          async content(event, trigger, player3) {
            const target = event.target;
            const num = lib.skill.mbcmfangzhu_backup.num;
            player3.storage.mbcmfangzhu = target;
            let evt = event.getParent("phaseUse", true);
            if (evt) {
              evt.fangzhuUsed = true;
            }
            player3.when("phaseUseEnd").filter((evtx) => !evtx.fangzhuUsed).step(async () => {
              player3.storage.mbcmfangzhu = player3;
            });
            switch (num) {
              case 1:
                target.addTempSkill("mbcmfangzhu_ban", { player: "phaseEnd" });
                target.markAuto("mbcmfangzhu_ban", ["trick"]);
                lib.skill.mbcmfangzhu_ban.init(target, "mbcmfangzhu_ban");
                break;
              case 2:
                target.addTempSkill("mbcmfangzhu_baiban", { player: "phaseEnd" });
                break;
            }
          },
          ai: {
            result: {
              target(player3, target) {
                switch (lib.skill.mbcmfangzhu_backup.num) {
                  case 1:
                    return -target.countCards("h", (card2) => get.type(card2) != "trick") - 1;
                  case 2:
                    return -target.getSkills(null, null, false).reduce((sum, skill) => {
                      return sum + Math.max(get.skillRank(skill, "out"), get.skillRank(skill, "in"));
                    }, 0);
                }
              }
            }
          }
        };
      },
      prompt(links, player2) {
        const str = "###放逐###";
        switch (links[0]) {
          case 1:
            return str + "令一名其他角色不能使用手牌中的非锦囊牌直到其回合结束";
          case 2:
            return str + "令一名其他角色的非Charlotte技能失效直到其回合结束";
        }
      }
    },
    ai: {
      order: 10,
      result: {
        player(player2) {
          return game.hasPlayer((current) => get.attitude(player2, current) < 0) ? 1 : 0;
        }
      }
    },
    subSkill: {
      backup: {},
      baiban: {
        init(player2, skill) {
          player2.addSkillBlocker(skill);
          player2.addTip(skill, "放逐 技能失效");
        },
        onremove(player2, skill) {
          player2.removeSkillBlocker(skill);
          player2.removeTip(skill);
        },
        inherit: "baiban",
        marktext: "逐"
      },
      ban: {
        init(player2, skill) {
          let storage = player2.getStorage(skill);
          if (storage.length) {
            player2.addTip(skill, "放逐 限" + (storage.length === 1 ? get.translation(storage[0])[0] : "手牌"));
          }
        },
        onremove(player2, skill) {
          player2.removeTip(skill);
          delete player2.storage[skill];
        },
        charlotte: true,
        mark: true,
        marktext: "禁",
        intro: {
          markcount: () => 0,
          content(storage) {
            if (storage.length > 1) {
              return "不能使用手牌";
            }
            return "不能使用手牌中的非" + get.translation(storage[0]) + "牌";
          }
        },
        mod: {
          cardEnabled(card2, player2) {
            const storage = player2.getStorage("mbcmfangzhu_ban");
            const hs = player2.getCards("h"), cards2 = [card2];
            if (Array.isArray(card2.cards)) {
              cards2.addArray(card2.cards);
            }
            if (cards2.containsSome(...hs) && !storage.includes(get.type2(card2))) {
              return false;
            }
          },
          cardSavable(card2, player2) {
            const storage = player2.getStorage("mbcmfangzhu_ban");
            const hs = player2.getCards("h"), cards2 = [card2];
            if (Array.isArray(card2.cards)) {
              cards2.addArray(card2.cards);
            }
            if (cards2.containsSome(...hs) && !storage.includes(get.type2(card2))) {
              return false;
            }
          }
        }
      }
    }
  },
  mbjuejin: {
    audio: 2,
    persevereSkill: true,
    enable: "phaseUse",
    limited: true,
    skillAnimation: true,
    animationColor: "thunder",
    filterCard: () => false,
    selectCard: [-1, -2],
    filterTarget: true,
    selectTarget: -1,
    multiline: true,
    async contentBefore(event, trigger, player2) {
      game.broadcastAll(() => {
        _status.tempMusic = "effect_caomaoBJM";
        game.playBackgroundMusic();
      });
      player2.changeSkin({ characterName: "mb_caomao" }, "mb_caomao_shadow");
      player2.awakenSkill(event.skill);
    },
    async content(event, trigger, player2) {
      const target = event.target;
      let delt = target.getHp() - 1, num = Math.abs(delt);
      if (delt != 0) {
        if (delt > 0) {
          const next = target.changeHp(-delt);
          next._triggered = null;
          await next;
        } else {
          num = 1 - target.getHp(true);
          await target.recover(num);
        }
      }
      if (delt > 0) {
        await target.changeHujia(num + (player2 == target ? 2 : 0), null, true);
      } else if (player2 == target) {
        await target.changeHujia(2, null, true);
      }
    },
    async contentAfter(event, trigger, player2) {
      game.addGlobalSkill("mbjuejin_xiangsicunwei");
      player2.$fullscreenpop("向死存魏！", "thunder");
      const cards2 = ["cardPile", "discardPile"].map((pos) => Array.from(ui[pos].childNodes)).flat();
      const filter = (card2) => ["shan", "tao", "jiu"].includes(card2.name);
      const cardx = cards2.filter(filter);
      if (cardx.length) {
        await game.cardsGotoSpecial(cardx);
        game.log(cardx, "被移出了游戏");
      }
      for (const target of game.filterPlayer()) {
        const sishis = target.getCards("hej", filter);
        if (sishis.length) {
          target.$throw(sishis);
          game.log(sishis, "被移出了游戏");
          await target.lose(sishis, ui.special);
        }
      }
    },
    ai: {
      order: 0.1,
      result: {
        player(player2) {
          let eff = 1;
          game.countPlayer((current) => {
            const att = get.attitude(player2, current), num = Math.abs(current.getHp(true) - 1);
            const delt = Math.max(0, num + current.hujia - 5);
            eff -= att * delt;
          });
          return eff > 0 ? 1 : 0;
        }
      }
    },
    subSkill: {
      xiangsicunwei: {
        trigger: { global: ["loseAfter", "equipAfter", "loseAsyncAfter", "cardsDiscardAfter"] },
        forced: true,
        silent: true,
        firstDo: true,
        filter(event, player2) {
          const nameList = ["shan", "tao", "jiu"];
          return event.getd().some((card2) => {
            return nameList.includes(get.name(card2, false)) && get.position(card2, true) === "d";
          });
        },
        async content(event, trigger, player2) {
          const nameList = ["shan", "tao", "jiu"];
          const cards2 = trigger.getd().filter((card2) => {
            return nameList.includes(get.name(card2, false)) && get.position(card2, true) === "d";
          });
          await game.cardsGotoSpecial(cards2);
          game.log(cards2, "被移出了游戏");
        }
      }
    }
  },
  //杨奉
  mbxuetu: {
    audio: 4,
    enable: "phaseUse",
    usable(skill, player2) {
      if (player2.countMark("mbxuetu_status") !== 1) {
        return 1;
      }
      return 2;
    },
    filter(event, player2) {
      if (player2.countMark("mbxuetu_status") == 2 && !game.hasPlayer((current) => current != player2)) {
        return false;
      }
      if (!game.hasPlayer((current) => current.isDamaged())) {
        if (player2.countMark("mbxuetu_status") == 1 && player2.getStorage("mbxuetu_used").includes(1)) {
          return false;
        }
        if (player2.countMark("mbxuetu_status") == 0 && !player2.storage.mbxuetu) {
          return false;
        }
      }
      return true;
    },
    zhuanhuanji2(skill, player2) {
      return !player2 || player2.countMark("mbxuetu_status") !== 1;
    },
    position: "he",
    onremove: ["mbxuetu", "mbxuetu_status"],
    derivation: ["mbxuetu_achieve", "mbxuetu_fail"],
    chooseButton: {
      dialog() {
        const dialog = ui.create.dialog("###血途###请选择要执行的项");
        dialog.direct = true;
        return dialog;
      },
      chooseControl(event, player2) {
        let list = ["令一名角色回复1点体力", "令一名角色摸两张牌"];
        if (player2.countMark("mbxuetu_status") !== 1) {
          list[player2.storage.mbxuetu ? "shift" : "pop"]();
        } else {
          list = list.filter((choice, index) => {
            if (index == 0 && !game.hasPlayer((current) => current.isDamaged())) {
              return false;
            }
            return !player2.getStorage("mbxuetu_used").includes(index);
          });
        }
        list.push("cancel2");
        return list;
      },
      check() {
        return get.event().controls[0];
      },
      backup(result, player2) {
        return {
          audio: "mbxuetu",
          logAudio(event, player3) {
            return player3.countMark("mbxuetu_status") == 2 ? ["mbxuetu3.mp3", "mbxuetu4.mp3"] : ["mbxuetu1.mp3", "mbxuetu2.mp3"];
          },
          choice: result.control.includes("回复") ? 0 : 1,
          filterCard: () => false,
          selectCard: -1,
          filterTarget(card2, player3, target) {
            const { choice } = get.info("mbxuetu_backup");
            if (player3.countMark("mbxuetu_status") !== 2 && choice == 0) {
              return target.isDamaged();
            }
            if (player3.countMark("mbxuetu_status") == 2) {
              return target != player3;
            }
            return true;
          },
          async content(event, trigger, player3) {
            const { choice } = get.info("mbxuetu_backup");
            const target = event.targets[0];
            const status = player3.countMark("mbxuetu_status");
            player3.changeZhuanhuanji("mbxuetu");
            if (status < 2) {
              player3.addTempSkill("mbxuetu_used", "phaseUseAfter");
              player3.markAuto("mbxuetu_used", [choice]);
              if (!choice) {
                await target.recover();
              } else {
                await target.draw(2);
              }
            } else {
              if (!choice) {
                await player3.recover();
                await target.chooseToDiscard(2, true, "he");
              } else {
                await player3.draw();
                await target.damage();
              }
            }
          },
          ai: {
            result: {
              target(player3, target) {
                const { choice } = get.info("mbxuetu_backup");
                const status = player3.countMark("mbxuetu_status");
                if (status > 1) {
                  if (player3.storage.mbxuetu) {
                    return get.damageEffect(target, player3, target) / 10;
                  }
                  return -2;
                }
                if (choice === 1) {
                  return 2;
                }
                const eff = get.recoverEffect(target, player3, player3);
                return eff > 0 ? 2 : eff < 0 ? -get.sgnAttitude(player3, target) : 0;
              },
              player(player3, target) {
                const status = player3.countMark("mbxuetu_status");
                if (status > 1) {
                  if (player3.storage.mbxuetu) {
                    return 1;
                  }
                  return get.recoverEffect(player3, player3) / 6;
                }
                return 0;
              }
            }
          }
        };
      },
      prompt(result, player2) {
        const { choice } = get.info("mbxuetu_backup");
        const status = player2.countMark("mbxuetu_status");
        let str = "";
        if (status < 2) {
          str += "令一名角色" + (choice ? "摸两张牌" : "回复1点体力");
        } else {
          str += choice ? "摸一张牌，然后对一名其他角色造成1点伤害" : "回复1点体力，然后令一名其他角色弃置两张牌";
        }
        return `###血途###<div class="text center">${str}</div>`;
      }
    },
    mark: true,
    marktext: "☯",
    intro: {
      content: (storage, player2) => {
        if (!player2.countMark("mbxuetu_status")) {
          if (storage) {
            return "转换技。出牌阶段限一次，你可以令一名角色摸两张牌。";
          }
          return "转换技。出牌阶段限一次，你可以令一名角色回复1点体力。";
        } else {
          if (storage) {
            return "转换技。出牌阶段限一次，你可以摸一张牌，然后对一名其他角色造成1点伤害。";
          }
          return "转换技。出牌阶段限一次，你可以回复1点体力，然后令一名其他角色弃置两张牌。";
        }
      }
    },
    ai: {
      order(item, player2) {
        const status = player2.countMark("mbxuetu_status");
        if (status > 1) {
          return Math.max(get.order({ name: "guohe" }), get.order({ name: "chuqibuyi" }));
        }
        if (status === 1 || player2.storage.mbxuetu) {
          return 9;
        }
        return 2;
      },
      result: { player: 1 }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true
      },
      backup: {}
    }
  },
  mbweiming: {
    audio: 3,
    dutySkill: true,
    locked: true,
    group: ["mbweiming_achieve", "mbweiming_fail", "mbweiming_effect"],
    intro: { content: "已记录$" },
    subSkill: {
      effect: {
        audio: "mbweiming1.mp3",
        trigger: {
          player: "phaseUseBegin"
        },
        filter(event, player2) {
          return game.hasPlayer((current) => {
            return !player2.getStorage("mbweiming").includes(current) && current != player2;
          });
        },
        locked: true,
        async cost(event, trigger, player2) {
          const targets2 = game.filterPlayer((current) => !player2.getStorage("mbweiming").includes(current) && current != player2);
          if (targets2.length == 1) {
            event.result = { bool: true, targets: targets2 };
          } else {
            event.result = await player2.chooseTarget("威命：记录一名未记录过的其他角色", "当你杀死没有被记录过的角色后，则〖威命〗使命成功；如果在你杀死这些角色中的一名之前，有被记录过的角色死亡，则你〖威命〗使命失败。", true).set("filterTarget", (card2, player3, target) => {
              return !player3.getStorage("mbweiming").includes(target) && target != player3;
            }).set("ai", (target) => {
              if (target === player2) {
                return 1;
              }
              return 1 + Math.sqrt(Math.abs(get.attitude(player2, target))) * Math.abs(get.threaten(target)) / Math.sqrt(target.getHp() + 1) / Math.sqrt(target.countCards("hes") + 1);
            }).forResult();
          }
        },
        async content(event, trigger, player2) {
          const targets2 = event.targets;
          if (targets2?.length) {
            player2.markAuto("mbweiming", targets2[0]);
          }
        }
      },
      achieve: {
        audio: "mbweiming2.mp3",
        trigger: {
          source: "dieAfter"
        },
        filter(event, player2) {
          return !player2.getStorage("mbweiming").includes(event.player);
        },
        dutySkill: true,
        forced: true,
        skillAnimation: true,
        animationColor: "fire",
        async content(event, trigger, player2) {
          game.log(player2, "成功完成使命");
          player2.awakenSkill("mbweiming");
          player2.storage.mbxuetu_status = 1;
          player2.unmarkSkill("mbxuetu");
          await game.delayx();
        }
      },
      fail: {
        audio: "mbweiming3.mp3",
        trigger: {
          global: "dieAfter"
        },
        filter(event, player2) {
          return player2.getStorage("mbweiming").includes(event.player);
        },
        dutySkill: true,
        forced: true,
        async content(event, trigger, player2) {
          game.log(player2, "使命失败");
          player2.awakenSkill("mbweiming");
          player2.storage.mbxuetu_status = 2;
          if (player2.getStat("skill").mbxuetu) {
            delete player2.getStat("skill").mbxuetu;
          }
          await game.delayx();
        }
      }
    },
    ai: {
      combo: "mbxuetu"
    }
  },
  //霍骏
  sidai: {
    audio: ["twsidai1.mp3", "sidai.mp3"],
    enable: "phaseUse",
    usable: 1,
    locked: false,
    limited: true,
    skillAnimation: true,
    animationColor: "fire",
    filter(event, player2) {
      var cards2 = player2.getCards("h", { type: "basic" });
      if (!cards2.length) {
        return false;
      }
      for (var i of cards2) {
        if (!game.checkMod(i, player2, "unchanged", "cardEnabled2", player2)) {
          return false;
        }
      }
      return event.filterCard(get.autoViewAs({ name: "sha", storage: { sidai: true } }, cards2), player2, event);
    },
    viewAs: { name: "sha", storage: { sidai: true } },
    filterCard: { type: "basic" },
    selectCard: -1,
    check: () => 1,
    onuse(result, player2) {
      player2.awakenSkill("sidai");
      player2.addTempSkill("sidai_tao");
      player2.addTempSkill("sidai_shan");
    },
    ai: {
      order(item, player2) {
        return get.order({ name: "sha" }, player2) + 0.1;
      },
      result: {
        target(player2, target) {
          var cards2 = ui.selected.cards.slice(0);
          var names = [];
          for (var i of cards2) {
            names.add(i.name);
          }
          if (names.length < player2.hp) {
            return 0;
          }
          if (player2.hasUnknown() && (player2.identity != "fan" || !target.isZhu)) {
            return 0;
          }
          if (get.attitude(player2, target) >= 0) {
            return -20;
          }
          return lib.card.sha.ai.result.target.apply(this, arguments);
        }
      }
    },
    subSkill: {
      tao: {
        trigger: { source: "damageSource" },
        filter(event, player2) {
          if (!event.card || !event.card.storage || !event.card.storage.sidai || !event.player.isIn()) {
            return false;
          }
          for (var i of event.cards) {
            if (i.name == "tao") {
              return true;
            }
          }
          return false;
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          await trigger.player.loseMaxHp();
        }
      },
      shan: {
        trigger: { player: "useCardToPlayered" },
        filter(event, player2) {
          if (!event.card || !event.card.storage || !event.card.storage.sidai || !event.target.isIn()) {
            return false;
          }
          for (var i of event.cards) {
            if (i.name == "shan") {
              return true;
            }
          }
          return false;
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const result = await trigger.target.chooseToDiscard("h", { type: "basic" }, "弃置一张基本牌，否则不能响应" + get.translation(trigger.card)).set("ai", function(card2) {
            var player3 = _status.event.player;
            if (player3.hasCard("hs", function(cardx) {
              return cardx != card2 && get.name(cardx, player3) == "shan";
            })) {
              return 12 - get.value(card2);
            }
            return 0;
          }).forResult();
          if (!result.bool) {
            trigger.directHit.add(trigger.target);
          }
        }
      }
    }
  },
  jieyu: {
    audio: ["twjieyu1.mp3", "jieyu.mp3"],
    frequent: true,
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player2) {
      for (let i = 0; i < ui.discardPile.childElementCount; i++) {
        if (get.type(ui.discardPile.childNodes[i], false) == "basic") {
          return true;
        }
      }
      return false;
    },
    prompt2(event, player2) {
      const num = lib.skill.jieyu.getNum(player2);
      return "获得弃牌堆中" + get.cnNumber(num) + "张" + (num > 1 ? "牌名各不相同的" : "") + "基本牌";
    },
    async content(event, trigger, player2) {
      const num = lib.skill.jieyu.getNum(player2, event);
      let gains = [], names = [];
      for (let i = 0; i < ui.discardPile.childElementCount; i++) {
        let card2 = ui.discardPile.childNodes[i];
        if (get.type(card2, null, false) == "basic" && !names.includes(card2.name)) {
          gains.push(card2);
          names.push(card2.name);
        }
      }
      if (gains.length) {
        await player2.gain(gains.randomGets(Math.min(gains.length, num)), "gain2");
      }
    },
    getNum(player2, event) {
      let num = 3;
      const history = game.getAllGlobalHistory("everything");
      let index;
      for (let i = history.length - 1; i >= 0; i--) {
        const evt = history[i];
        if (evt.name == "jieyu" && evt.player == player2) {
          if (!event || evt != event) {
            index = i;
            break;
          }
        }
      }
      if (index) {
        for (let i = index + 1; i < history.length; i++) {
          const evt = history[i];
          if (evt.name == "useCard" && evt.player != player2 && evt.targets?.includes(player2) && get.is.damageCard(evt.card)) {
            num--;
            if (num == 1) {
              break;
            }
          }
        }
      }
      return num;
    }
  },
  //木鹿大王
  shoufa: {
    filterTargetx(name, player2, target) {
      const num = get.mode() == "doudizhu" ? 1 : 2;
      if (name == "damageEnd" && get.distance(target, player2) < num) {
        return false;
      }
      if (name == "damageSource" && get.distance(player2, target) > num) {
        return false;
      }
      const zhoufa = player2.storage.zhoulin_zhoufa;
      if (!zhoufa) {
        return true;
      }
      if (zhoufa == "豹" || zhoufa == "兔") {
        return true;
      }
      if (zhoufa == "鹰") {
        return target.hasCards("he");
      }
      return target.hasDiscardableCards(player2, "e");
    },
    getPrompt2(event, player2) {
      const zhoufa = player2.storage.zhoulin_zhoufa;
      const bool = get.mode() == "versus" && _status.mode == "two" || get.mode() == "doudizhu";
      let str;
      if (zhoufa) {
        str = ["令其受到1点无来源伤害", "你随机获得其一张牌", "你随机弃置其装备区的一张牌", "令其摸一张牌"][["豹", "鹰", "熊", "兔"].indexOf(zhoufa)];
      } else if (bool) {
        str = "令其随机执行一个效果（若该角色为你或你的队友，则其必定摸牌）";
      } else {
        str = "令其随机执行一个效果";
      }
      const nodoudizhu = (event.triggername === "damageEnd" ? "与你距离不小于" : "距离不大于") + (1 + (get.mode() !== "doudizhu")) + "的";
      return `选择一名${nodoudizhu}角色，${str}`;
    },
    // 是的孩子们，斗地主和22我就是开了
    zhishiRabbit(player2, target) {
      if (get.mode() == "versus" && _status.mode == "two" || get.mode() == "doudizhu") {
        return player2 == target || player2.getFriends().includes(target);
      }
      return false;
    },
    filterList(player2, target) {
      return ["豹", "鹰", "熊", "兔"].filter((animal) => {
        if (animal == "豹" || animal == "兔") {
          return true;
        }
        if (animal == "鹰") {
          return target.hasCards("he");
        }
        return target.hasDiscardableCards(player2, "e");
      });
    },
    audio: 2,
    trigger: {
      player: "damageEnd",
      source: "damageSource"
    },
    filter(event, player2, name) {
      if (name == "damageSource" && player2.getHistory("sourceDamage").indexOf(event) != 0) {
        return false;
      }
      if (name == "damageEnd" && player2.countMark("shoufa_used") > 4) {
        return false;
      }
      return game.hasPlayer((target) => get.info("shoufa").filterTargetx(name, player2, target));
    },
    async cost(event, trigger, player2) {
      const prompt2 = get.info(event.skill).getPrompt2(event, player2);
      const targets2 = game.filterPlayer((current) => get.info(event.skill).filterTargetx(event.triggername, player2, current));
      event.result = await player2.chooseTarget(get.prompt(event.skill), prompt2, (cards2, player3, target) => {
        return get.event().targets.includes(target);
      }).set("targets", targets2).set("ai", (target) => {
        const player3 = get.player();
        const zhoufa = player3.storage.zhoulin_zhoufa;
        if (!zhoufa) {
          const att = get.attitude(player3, target);
          if (get.info("shoufa").zhishiRabbit(player3, target) || get.isLuckyStar(player3)) {
            return att * 10;
          }
          return -att;
        }
        switch (zhoufa) {
          case "豹": {
            return get.damageEffect(target, player3, player3);
          }
          case "鹰": {
            return get.effect(target, { name: "guohe_copy2" }, player3, player3);
          }
          case "熊": {
            let att = get.attitude(player3, target), eff = 0;
            target.getCards("e", (card2) => {
              var val = get.value(card2, target);
              eff = Math.max(eff, -val * att);
            });
            return eff;
          }
          case "兔": {
            return get.effect(target, { name: "draw" }, player3, player3);
          }
        }
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      if (event.triggername == "damageEnd") {
        player2.addTempSkill(event.name + "_used");
        player2.addMark(event.name + "_used", 1, false);
      }
      let shoufa;
      const zhoufa = player2.storage.zhoulin_zhoufa;
      if (zhoufa) {
        shoufa = zhoufa;
      } else if (get.info(event.name).zhishiRabbit(player2, target) || get.isLuckyStar(player2)) {
        player2.chat("是的孩子们，我开了");
        shoufa = "兔";
      } else {
        shoufa = get.info(event.name).filterList(player2, target).randomGet();
      }
      player2.popup(shoufa);
      game.log(target, "执行", "#g" + shoufa, "效果");
      switch (shoufa) {
        case "豹": {
          await target.damage("nosource");
          break;
        }
        case "鹰": {
          const cards2 = target.getGainableCards(player2, "he");
          if (cards2.length) {
            await player2.gain(cards2.randomGet(), target, "giveAuto");
          }
          break;
        }
        case "熊": {
          await target.randomDiscard("e", player2);
          break;
        }
        case "兔": {
          await target.draw();
          break;
        }
      }
    },
    subSkill: {
      used: {
        charlotte: true,
        onremove: true,
        intro: { content: "本回合因受伤触发【兽法】#次" }
      }
    }
  },
  yuxiang: {
    mod: {
      globalFrom(from, to, distance) {
        if (from.hujia > 0) {
          return distance - 1;
        }
      },
      globalTo(from, to, distance) {
        if (to.hujia > 0) {
          return distance + 1;
        }
      }
    },
    audio: true,
    trigger: { player: "damageBegin2" },
    filter(event, player2) {
      return player2.hujia > 0 && event.hasNature("fire");
    },
    forced: true,
    async content(event, trigger, player2) {
      trigger.num++;
    },
    ai: {
      combo: "zhoulin"
    }
  },
  zhoulin: {
    audio: 2,
    limited: true,
    enable: "phaseUse",
    skillAnimation: true,
    animationColor: "fire",
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      player2.changeHujia(2, null, true);
      const { control } = await player2.chooseControl("豹", "鹰", "熊", "兔").set("ai", () => "豹").set("prompt", "选择一个固定效果").forResult();
      if (control) {
        player2.popup(control);
        game.log(player2, "选择了", "#g" + control, "效果");
        player2.addTempSkill("zhoulin_zhoufa", { player: "phaseBegin" });
        player2.storage.zhoulin_zhoufa = control;
        player2.markSkill("zhoulin_zhoufa");
        game.broadcastAll(
          function(player3, zhoufa) {
            if (player3.marks.zhoulin_zhoufa) {
              player3.marks.zhoulin_zhoufa.firstChild.innerHTML = zhoufa;
            }
          },
          player2,
          control
        );
      }
    },
    ai: {
      order: 12,
      result: { player: 1 }
    },
    derivation: "shoufa",
    subSkill: {
      zhoufa: {
        charlotte: true,
        onremove: true,
        intro: { content: "已选择$效果" }
      }
    }
  },
  //陈珪
  guimou: {
    audio: 2,
    trigger: {
      global: "phaseBefore",
      player: ["enterGame", "phaseEnd", "phaseZhunbeiBegin"]
    },
    filter(event, player2, name) {
      if (event.name == "phaseZhunbei" || name == "phaseEnd") {
        return true;
      }
      return event.name != "phase" || game.phaseNumber == 0;
    },
    direct: true,
    locked: true,
    async content(event, trigger, player2) {
      if (trigger.name != "phaseZhunbei") {
        player2.logSkill("guimou");
        var result, choiceList = ["惩罚期间使用牌最少的角色", "惩罚期间弃置牌最少的角色", "惩罚期间得到牌最少的角色"];
        if (trigger.name != "phase" || game.phaseNumber == 0) {
          result = { index: get.rand(0, 2) };
        } else {
          result = await player2.chooseControl().set("choiceList", choiceList).set("ai", () => get.rand(0, 2)).forResult();
        }
        var str = choiceList[result.index];
        game.log(player2, "选择", "#g" + str);
        player2.addSkill("guimou_" + result.index);
        return;
      }
      var targets2 = [];
      for (var i = 0; i <= 2; i++) {
        var skill = "guimou_" + i;
        if (player2.hasSkill(skill)) {
          var storage = player2.storage[skill], nums = storage[0].slice();
          var targetx = nums.sort((a, b) => storage[1][storage[0].indexOf(a)] - storage[1][storage[0].indexOf(b)]);
          targetx = targetx.filter((target2) => storage[1][storage[0].indexOf(target2)] == storage[1][storage[0].indexOf(targetx[0])]);
          targets2.addArray(targetx);
          player2.removeSkill(skill);
        }
      }
      targets2 = targets2.filter((target2) => target2 != player2 && target2.countCards("h"));
      if (targets2.length) {
        var result = await player2.chooseTarget(
          "请选择【诡谋】的目标",
          "观看一名可选择的角色的手牌并选择其中一张牌，然后你可以此牌交给另一名其他角色或弃置此牌",
          (card2, player3, target2) => {
            return _status.event.targets.includes(target2) && target2.countCards("h");
          },
          true
        ).set("ai", (target2) => {
          return Math.sqrt(Math.min(3, target2.countCards("h"))) * get.effect(target2, { name: "guohe_copy2" }, player2, player2);
        }).set("targets", targets2).forResult();
        if (result.bool) {
          var target = result.targets[0];
          player2.logSkill("guimou", target);
          player2.addExpose(0.3);
          var result2 = await player2.choosePlayerCard(target, "h", "visible", true).set("ai", (button) => {
            return get.value(button.link);
          }).set("prompt", "诡谋：请选择" + get.translation(target) + "的一张手牌").set("prompt2", '<div class="text center">将选择的牌交给另一名其他角色或弃置此牌</div>').forResult();
          if (result2.bool) {
            var cards2 = result2.links.slice(), result3;
            if (!game.hasPlayer((targetx2) => targetx2 != player2 && targetx2 != target)) {
              result3 = { bool: false };
            } else {
              result3 = await player2.chooseTarget("是否令另一名其他角色获得" + get.translation(cards2) + "？", (card2, player3, target2) => {
                return target2 != player3 && target2 != _status.event.target;
              }).set("ai", (target2) => get.attitude(_status.event.player, target2)).set("target", target).forResult();
            }
            if (result3.bool) {
              var targetx = result3.targets[0];
              player2.line(targetx);
              await targetx.gain(cards2, target, "give");
            } else {
              await target.modedDiscard(cards2, player2);
            }
          }
        }
      }
    },
    subSkill: {
      0: {
        charlotte: true,
        onremove: true,
        init(player2, skill) {
          if (!player2.storage[skill]) {
            player2.storage[skill] = [[], []];
            var targets2 = game.filterPlayer((i) => i !== player2).sortBySeat(player2);
            targets2.forEach((target) => {
              player2.storage[skill][0].push(target);
              player2.storage[skill][1].push(0);
            });
          }
        },
        mark: true,
        intro: {
          markcount: (storage) => 0,
          content(storage, player2) {
            var str = "当前使用牌数排行榜";
            var lose = storage[1].slice().sort((a, b) => a - b)[0];
            storage[0].forEach((target) => {
              str += "<br><li>";
              var score = storage[1][storage[0].indexOf(target)];
              if (score == lose) {
                str += "<span class='texiaotext' style='color:#FF0000'>";
              }
              str += " " + get.translation(target) + " ";
              str += score + "张";
              if (score == lose) {
                str += "</span>";
              }
            });
            return str;
          }
        },
        trigger: { global: "useCard1" },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const storage = player2.getStorage("guimou_0", [[], []]);
          if (!storage[0].includes(trigger.player)) {
            storage[0].push(trigger.player);
            storage[1].push(0);
          }
          storage[1][storage[0].indexOf(trigger.player)]++;
          player2.markSkill("guimou_0");
        }
      },
      1: {
        charlotte: true,
        onremove: true,
        init(player2, skill) {
          if (!player2.storage[skill]) {
            player2.storage[skill] = [[], []];
            var targets2 = game.filterPlayer((i) => i !== player2).sortBySeat(player2);
            targets2.forEach((target) => {
              player2.storage[skill][0].push(target);
              player2.storage[skill][1].push(0);
            });
          }
        },
        mark: true,
        intro: {
          markcount: (storage) => 0,
          content(storage, player2) {
            var str = "当前弃置牌数排行榜";
            var lose = storage[1].slice().sort((a, b) => a - b)[0];
            storage[0].forEach((target) => {
              str += "<br><li>";
              var score = storage[1][storage[0].indexOf(target)];
              if (score == lose) {
                str += "<span class='texiaotext' style='color:#FF0000'>";
              }
              str += " " + get.translation(target) + " ";
              str += score + "张";
              if (score == lose) {
                str += "</span>";
              }
            });
            return str;
          }
        },
        trigger: { global: ["loseAfter", "loseAsyncAfter"] },
        filter(event, player2) {
          return event.type == "discard" && game.hasPlayer((target) => event.getl(target).cards2.length);
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const storage = player2.getStorage("guimou_1", [[], []]);
          const targets2 = game.filterPlayer((target) => trigger.getl(target).cards2.length);
          targets2.forEach((target) => {
            if (!storage[0].includes(target)) {
              storage[0].push(target);
              storage[1].push(0);
            }
            storage[1][storage[0].indexOf(target)] += trigger.getl(target).cards2.length;
          });
          player2.markSkill("guimou_1");
        }
      },
      2: {
        charlotte: true,
        onremove: true,
        init(player2, skill) {
          if (!player2.storage[skill]) {
            player2.storage[skill] = [[], []];
            var targets2 = game.filterPlayer((i) => i !== player2).sortBySeat(player2);
            targets2.forEach((target) => {
              player2.storage[skill][0].push(target);
              player2.storage[skill][1].push(0);
            });
          }
        },
        mark: true,
        intro: {
          markcount: (storage) => 0,
          content(storage, player2) {
            var str = "当前得到牌数排行榜";
            var lose = storage[1].slice().sort((a, b) => a - b)[0];
            storage[0].forEach((target) => {
              str += "<br><li>";
              var score = storage[1][storage[0].indexOf(target)];
              if (score == lose) {
                str += "<span class='texiaotext' style='color:#FF0000'>";
              }
              str += " " + get.translation(target) + " ";
              str += score + "张";
              if (score == lose) {
                str += "</span>";
              }
            });
            return str;
          }
        },
        trigger: { global: ["gainAfter", "loseAsyncAfter"] },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const storage = player2.getStorage("guimou_2", [[], []]);
          const targets2 = game.filterPlayer((target) => trigger.getg(target).length);
          targets2.forEach((target) => {
            if (!storage[0].includes(target)) {
              storage[0].push(target);
              storage[1].push(0);
            }
            storage[1][storage[0].indexOf(target)] += trigger.getg(target).length;
          });
          player2.markSkill("guimou_2");
        }
      }
    }
  },
  zhouxian: {
    audio: 2,
    trigger: { target: "useCardToTarget" },
    filter(event, player2) {
      return event.player != player2 && get.is.damageCard(event.card);
    },
    forced: true,
    logTarget: "player",
    async content(event, trigger, player2) {
      var target = trigger.player;
      var cards2 = get.cards(3);
      await game.cardsDiscard(cards2);
      player2.showCards(cards2, get.translation(player2) + "发动了【州贤】");
      var result = await target.chooseToDiscard("he", "州贤：弃置一张其中有的类别的牌，或令此牌对" + get.translation(player2) + "无效", (card2, player3) => {
        return _status.event.cards.some((cardx) => get.type2(cardx) == get.type2(card2));
      }).set("cards", cards2).set("ai", (card2) => {
        if (!_status.event.goon) {
          return 0;
        }
        return 7.5 - get.value(card2);
      }).set("goon", get.effect(player2, trigger.card, target, target) > 0).forResult();
      if (!result?.bool) {
        trigger.targets.remove(player2);
        trigger.getParent().triggeredTargets2.remove(player2);
        trigger.untrigger();
      }
    },
    ai: {
      effect: {
        target(card2, player2, target, current) {
          if (get.tag(card2, "damage") && get.attitude(player2, target) < 0 && target != player2) {
            if (_status.event.name == "zhouxian") {
              return;
            }
            if (get.attitude(player2, target) > 0 && current < 0) {
              return "zeroplayertarget";
            }
            var bs = player2.getDiscardableCards(player2, "he");
            bs.remove(card2);
            if (card2.cards) {
              bs.removeArray(card2.cards);
            } else {
              bs.removeArray(ui.selected.cards);
            }
            var cardx = Array.from(ui.cardPile.childNodes).slice(0, 3);
            bs = bs.filter((i) => cardx.some((j) => get.type2(j) == get.type2(i)));
            if (!bs.length) {
              return "zerotarget";
            }
            if (bs.length <= 2) {
              if (bs.some((bsi) => get.value(bsi) < 7)) {
                return [1, 0, 1, -0.5];
              }
              return [1, 0, 0.3, 0];
            }
            return [1, 0, 1, -0.5];
          }
        }
      }
    }
  },
  //胡班
  mbyilie: {
    audio: 3,
    trigger: { global: "phaseBefore", player: "enterGame" },
    filter(event, player2) {
      return !player2.storage.mbyilie2 && (event.name != "phase" || game.phaseNumber == 0);
    },
    forced: true,
    async content(event, trigger, player2) {
      const { targets: targets2 } = await player2.chooseTarget(get.prompt2("mbyilie"), lib.filter.notMe, true).set("ai", function(target) {
        let player3 = _status.event.player;
        return Math.max(1 + get.attitude(player3, target) * get.threaten(target), Math.random());
      }).set("animate", false).forResult();
      if (targets2) {
        const target = targets2[0];
        player2.line(target, "green");
        player2.storage.mbyilie2 = target;
        player2.addSkill("mbyilie2");
        const func = (player3, target2) => {
          target2.markSkillCharacter("mbyilie2", player3, "义烈", `${get.translation(player3)}决定追随于你`, true);
        };
        if (event.isMine()) {
          func(player2, target);
        } else if (player2.isOnline2()) {
          player2.send(func, player2, target);
        }
      }
    },
    marktext: "烈",
    intro: {
      name2: "烈",
      content: "mark"
    },
    group: "mbyilie3"
  },
  mbyilie2: {
    charlotte: true,
    audio: "mbyilie",
    trigger: { global: ["damageBegin4", "damageSource"] },
    sourceSkill: "mbyilie",
    filter(event, player2, name) {
      var target = player2.storage.mbyilie2;
      if (name == "damageSource") {
        return event.source == target && event.player != player2 && player2.isDamaged();
      }
      return event.player == target && !player2.countMark("mbyilie");
    },
    forced: true,
    logTarget(event, player2) {
      return player2.storage.mbyilie2;
    },
    async content(event, trigger, player2) {
      if (event.triggername == "damageSource") {
        await player2.recover();
      } else {
        event.targets[0].markSkillCharacter("mbyilie2", player2, "义烈", `${get.translation(player2)}决定追随于你`);
        player2.addMark("mbyilie", trigger.num);
        trigger.cancel();
      }
    }
  },
  mbyilie3: {
    audio: "mbyilie",
    trigger: { player: "phaseEnd" },
    sourceSkill: "mbyilie",
    filter(event, player2) {
      return player2.hasMark("mbyilie");
    },
    forced: true,
    async content(event, trigger, player2) {
      await player2.draw();
      const num = player2.countMark("mbyilie");
      if (num) {
        await player2.loseHp(num);
        player2.removeMark("mbyilie", num);
      }
    }
  },
  //向朗
  naxue: {
    audio: 2,
    trigger: { player: "phaseUseBefore" },
    check(event, player2) {
      var cards2 = player2.getCards("h", (card2) => player2.hasValueTarget(card2));
      if (!cards2.length) {
        return true;
      }
      if (!(player2.hp >= 2 && player2.countCards("h") <= player2.hp + 1)) {
        return false;
      }
      return game.hasPlayer(function(target) {
        if (target.hasJudge("lebu") || target == player2) {
          return false;
        }
        if (get.attitude(player2, target) > 4) {
          return get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards("h") + 1) > 0;
        }
        return false;
      });
    },
    async content(event, trigger, player2) {
      trigger.cancel();
      var num = player2.countDiscardableCards(player2, "he");
      if (num) {
        var result = await player2.chooseToDiscard("纳学：是否弃置任意张牌并摸等量的牌？", "he", [1, num], "allowChooseAll").set("ai", lib.skill.zhiheng.check).forResult();
        if (result.bool) {
          await player2.draw(result.cards.length);
        }
      }
      if (player2.countCards("h")) {
        var result2 = await player2.chooseCardTarget({
          prompt: "是否交给至多两名其他角色各一张手牌？",
          prompt2: "先按顺序选中所有要给出的牌，然后再按顺序选择等量的目标角色。",
          selectCard: [1, 2],
          filterCard: true,
          filterTarget: lib.filter.notMe,
          selectTarget() {
            return ui.selected.cards.length;
          },
          filterOk: () => {
            return ui.selected.cards.length == ui.selected.targets.length;
          },
          position: "h",
          allowChooseAll: true,
          ai1(card2) {
            if (card2.name == "du") {
              return 10;
            } else if (ui.selected.cards.length && ui.selected.cards[0].name == "du") {
              return 0;
            }
            var player3 = _status.event.player;
            if (ui.selected.cards.length > 4 || !game.hasPlayer(function(current) {
              return get.attitude(player3, current) > 0 && !current.hasSkillTag("nogain");
            })) {
              return 0;
            }
            return 1 / Math.max(0.1, get.value(card2));
          },
          ai2(target) {
            var player3 = _status.event.player, att = get.attitude(player3, target);
            if (ui.selected.cards[0].name == "du") {
              return -att;
            }
            if (target.hasSkillTag("nogain")) {
              att /= 6;
            }
            return att;
          }
        }).forResult();
        if (result2.bool) {
          const list = [];
          for (let i = 0; i < result2.targets.length; i++) {
            list.push([result2.targets[i], result2.cards[i]]);
            player2.line(result2.targets[i]);
          }
          await game.loseAsync({
            gain_list: list,
            player: player2,
            cards: result2.cards,
            giver: player2,
            animate: "giveAuto"
          }).setContent("gaincardMultiple");
        }
      }
    }
  },
  yijie: {
    audio: 2,
    trigger: { player: "die" },
    filter(event, player2) {
      return game.hasPlayer((target) => target != player2);
    },
    forced: true,
    forceDie: true,
    skillAnimation: true,
    animationColor: "orange",
    logTarget(event, player2) {
      return game.filterPlayer((target) => target != player2);
    },
    async content(event, trigger, player2) {
      var targets2 = game.filterPlayer((target2) => target2 != player2);
      var sum = Math.max(1, Math.floor(targets2.reduce((num, target2) => num += target2.hp, 0) / targets2.length));
      for (var target of targets2) {
        var delta = target.hp - sum;
        if (delta != 0) {
          await target[delta > 0 ? "loseHp" : "recover"](Math.abs(delta));
        }
      }
    }
  },
  //阎象
  kujian: {
    audio: "twkujian",
    inherit: "twkujian",
    selectCard: [1, 2],
    logAudio: () => "twkujian1.mp3",
    async content(event, trigger, player2) {
      const { cards: cards2, target } = event;
      player2.addSkill("kujian_discard");
      const next = player2.give(cards2, target);
      next.gaintag.add("twkujianx");
      await next;
    },
    subSkill: {
      discard: {
        trigger: {
          global: ["loseAfter", "equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
        },
        forced: true,
        getIndex(event, player2) {
          let list = [], players = game.filterPlayer().sortBySeat();
          for (const current of players) {
            let bool = ["useCard", "respond"].includes(event.getParent().name);
            if (current == player2) {
              continue;
            }
            const evt = event.getl(current);
            if (!evt || !evt.hs || !evt.hs.length) {
              continue;
            }
            if (event.name == "lose") {
              for (const i in event.gaintag_map) {
                if (event.gaintag_map[i].includes("twkujianx")) {
                  list.push([current, bool]);
                }
              }
              continue;
            }
            current.getHistory("lose", (evt2) => {
              if (event != evt2.getParent()) {
                return false;
              }
              for (const i in evt2.gaintag_map) {
                if (evt2.gaintag_map[i].includes("twkujianx")) {
                  list.push([current, bool]);
                }
              }
            });
          }
          return list;
        },
        charlotte: true,
        logTarget(event, player2, name, data) {
          return data[0];
        },
        logAudio(event, player2, name, data) {
          let type = data[1];
          if (type) {
            return "twkujian2.mp3";
          }
          return "twkujian3.mp3";
        },
        async content(event, trigger, player2) {
          const target = event.targets[0];
          const type = event.indexedData[1];
          if (type) {
            await game.asyncDraw([player2, target], 2);
          } else {
            if (player2.countCards("h")) {
              await player2.chooseToDiscard(1, true, "h");
            }
            if (target.countCards("h")) {
              await target.chooseToDiscard(1, true, "h");
            }
          }
        }
      }
    }
  },
  ruilian: {
    audio: "twruilian",
    trigger: { global: "roundStart" },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt2(event.skill)).set("ai", (target) => {
        let player3 = _status.event.player, att = get.attitude(player3, target), eff = att / (player3 == target ? 2 : 1) + 1;
        if (att >= 0) {
          if (target.hasSkill("yongsi")) {
            return eff * 5;
          }
          if (target.hasSkill("zhiheng") || target.hasSkill("rezhiheng")) {
            return eff * 4;
          }
          if (target.hasSkill("rekurou")) {
            return eff * 3;
          }
          if (target.hasSkill("xinlianji") || target.hasSkill("dclianji")) {
            return eff * 2;
          }
          if (target.needsToDiscard()) {
            return eff * 1.5;
          }
          return eff;
        }
        return 0;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      player2.addSkill("ruilian_target");
      player2.markAuto("ruilian_target", [target]);
    },
    subSkill: {
      target: {
        audio: "twruilian",
        onremove: true,
        intro: { content: "已选择$" },
        trigger: { global: "phaseEnd" },
        filter(event, player2) {
          return player2.getStorage("ruilian_target").includes(event.player);
        },
        direct: true,
        charlotte: true,
        async content(event, trigger, player2) {
          const target = trigger.player;
          let cards2 = [];
          player2.removeSkill("ruilian_target");
          target.getHistory("lose", (evt) => {
            if (evt.type == "discard") {
              cards2.addArray(evt.cards2);
            }
          });
          if (!cards2.length) {
            return;
          }
          let list = [];
          for (let type of ["basic", "trick", "equip"]) {
            for (let card2 of cards2) {
              if (get.type2(card2) == type) {
                list.push(type);
                break;
              }
            }
          }
          list.push("cancel2");
          const result = await player2.chooseControl(list).set("prompt", "睿敛：是否与" + get.translation(target) + "各获得一种类型的牌？").set("ai", function() {
            let player3 = _status.event.player, list2 = _status.event.controls;
            if (player3.hp <= 3 && !player3.countCards("h", { name: ["shan", "tao"] }) && list2.includes("basic")) {
              return "basic";
            }
            if (player3.countCards("he", { type: "equip" }) < 2 && list2.includes("equip")) {
              return "equip";
            }
            if (list2.includes("trick")) {
              return "trick";
            }
            return list2.remove("cancel2").randomGet();
          }).forResult();
          if (result.control != "cancel2") {
            player2.logSkill("ruilian_target", target);
            let type = result.control;
            list = [target, player2].sortBySeat(_status.currentPhase);
            cards2 = [];
            for (let current of list) {
              let card2 = get.discardPile(function(card3) {
                return get.type2(card3) == type && !cards2.includes(card3);
              });
              if (card2) {
                cards2.push(card2);
                await current.gain(card2, "gain2");
              }
            }
          }
        }
      }
    }
  },
  //手杀差异化孙鲁育
  mbmumu: {
    audio: "mumu",
    inherit: "new_mumu",
    filter(event, player2) {
      return game.hasPlayer((current) => {
        return current.countCards("e") > 0;
      });
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt("mbmumu"), "弃置场上的一张装备牌，或者获得场上的一张防具牌。", function(card2, player3, target2) {
        return target2.countCards("e") > 0;
      }).set("ai", function(target2) {
        var player3 = _status.event.player;
        var att = get.attitude(player3, target2);
        if (target2.getEquip(2) && player3.hasEmptySlot(2)) {
          return -2 * att;
        }
        return -att;
      }).forResult();
      if (!result.bool || !result.targets?.length) {
        return;
      }
      const target = result.targets[0];
      player2.logSkill("mbmumu", target);
      player2.line(target, "green");
      const e = target.getEquips(2);
      let choice = "弃置一张装备牌";
      if (e.length > 0) {
        const control = await player2.chooseControl("弃置一张装备牌", "获得一张防具牌").set("ai", function() {
          if (_status.event.player.getEquips(2).length > 0) {
            return "弃置一张装备牌";
          }
          return "获得一张防具牌";
        }).forResult();
        choice = control.control;
      }
      if (choice == "弃置一张装备牌") {
        await player2.discardPlayerCard(target, "e", true);
      } else if (e.length) {
        await player2.gain(e, target, "give", "bySelf");
        player2.addTempSkill("new_mumu_notsha");
      }
    }
  },
  mbmeibu: {
    inherit: "new_meibu",
    derivation: ["mbzhixi"],
    async content(event, trigger, player2) {
      const check = lib.skill.new_meibu.checkx(trigger, player2);
      const result = await player2.chooseToDiscard(get.prompt2("mbmeibu", trigger.player), "he").set("ai", function(card3) {
        if (_status.event.check) {
          return 6 - get.value(card3);
        }
        return 0;
      }).set("check", check).set("logSkill", ["mbmeibu", trigger.player]).forResult();
      if (!result.bool) {
        return;
      }
      var target = trigger.player;
      var card2 = result.cards[0];
      player2.line(target, "green");
      target.addTempSkills("mbzhixi", "phaseEnd");
      if (card2.name != "sha" && !(get.type(card2, "trick") == "trick" && get.color(card2) == "black")) {
        target.addTempSkill("new_meibu_range", "phaseEnd");
        target.markAuto("new_meibu_range", player2);
      }
      target.markSkillCharacter("mbmeibu", player2, "魅步", "锁定技。出牌阶段，若你于此阶段使用过的牌数不小于X，你不能使用牌（X为你的体力值）；当你使用锦囊牌时，你结束此阶段。");
    }
  },
  mbzhixi: {
    mod: {
      cardEnabled(card2, player2) {
        if (player2.countMark("mbzhixi") >= player2.hp) {
          return false;
        }
      },
      cardUsable(card2, player2) {
        if (player2.countMark("mbzhixi") >= player2.hp) {
          return false;
        }
      },
      cardSavable(card2, player2) {
        if (player2.countMark("mbzhixi") >= player2.hp) {
          return false;
        }
      }
    },
    trigger: {
      player: "useCard1"
    },
    forced: true,
    popup: false,
    firstDo: true,
    init(player2, skill) {
      player2.storage[skill] = 0;
      var evt = _status.event.getParent("phaseUse");
      if (evt && evt.player == player2) {
        player2.getHistory("useCard", function(evtx) {
          if (evtx.getParent("phaseUse") == evt) {
            player2.storage[skill]++;
          }
        });
      }
    },
    onremove(player2) {
      player2.unmarkSkill("mbmeibu");
      delete player2.storage.mbzhixi;
    },
    async content(event, trigger, player2) {
      player2.addMark("mbzhixi", 1, false);
      player2.addTempSkill("mbzhixi_clear", "phaseChange");
      if (get.type2(trigger.card) == "trick") {
        var evt = trigger.getParent("phaseUse");
        if (evt && evt.player == player2) {
          evt.skipped = true;
          game.log(player2, "结束了出牌阶段");
        }
      }
    },
    subSkill: {
      clear: {
        charlotte: true,
        onremove(player2) {
          player2.clearMark("mbzhixi", false);
        }
      }
    },
    ai: {
      presha: true,
      pretao: true,
      neg: true,
      nokeep: true
    }
  },
  //庞统
  xinlianhuan: {
    audio: 2,
    audioname: ["ol_pangtong"],
    inherit: "lianhuan",
    group: "xinlianhuan_add",
    subSkill: {
      add: {
        audio: "xinlianhuan",
        audioname: ["ol_pangtong"],
        trigger: { player: "useCard2" },
        filter(event, player2) {
          if (event.card.name != "tiesuo") {
            return false;
          }
          var info = get.info(event.card);
          if (info.allowMultiple == false) {
            return false;
          }
          if (event.targets && !info.multitarget) {
            if (game.hasPlayer((current) => {
              return !event.targets.includes(current) && lib.filter.targetEnabled2(event.card, player2, current);
            })) {
              return true;
            }
          }
          return false;
        },
        charlotte: true,
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const result = await player2.chooseTarget(get.prompt("xinlianhuan"), "为" + get.translation(trigger.card) + "额外指定一个目标", (card2, player3, target) => {
            return !_status.event.sourcex.includes(target) && lib.filter.targetEnabled2(_status.event.card, player3, target);
          }).set("sourcex", trigger.targets).set("ai", function(target) {
            var player3 = _status.event.player;
            return get.effect(target, _status.event.card, player3, player3);
          }).set("card", trigger.card).forResult();
          if (!result.bool) {
            return;
          }
          if (!event.isMine() && !event.isOnline()) {
            await game.delayex();
          }
          var targets2 = result.targets;
          player2.logSkill("xinlianhuan_add", targets2);
          trigger.targets.addArray(targets2);
          game.log(targets2, "也成为了", trigger.card, "的目标");
        }
      }
    }
  },
  //吴班
  xinjintao: {
    audio: "jintao",
    inherit: "jintao",
    async content(event, trigger, player2) {
      var evt = trigger.getParent("phaseUse");
      var index = player2.getHistory("useCard", function(evtx) {
        return evtx.card.name == "sha" && evtx.getParent("phaseUse") == evt;
      }).indexOf(trigger);
      if (index == 0) {
        game.log(trigger.card, "不可被响应");
        trigger.directHit.addArray(game.players);
      } else {
        game.log(trigger.card, "伤害+1");
        if (typeof trigger.baseDamage != "number") {
          trigger.baseDamage = 1;
        }
        trigger.baseDamage++;
      }
    }
  },
  //鲍信
  mutao: {
    audio: "twmutao",
    inherit: "twmutao",
    filterTarget(card2, player2, target) {
      return target.countCards("h");
    },
    async content(event, trigger, player2) {
      const source = event.target;
      const cards2 = source.getCards("h", { name: "sha" });
      if (!cards2.length) {
        game.log("但", source, "没有", "#y杀", "！");
        return;
      }
      const next = source.addToExpansion(cards2, source, "give");
      next.gaintag.add("mutao");
      await next;
      let togive = source;
      while (source.getExpansions("mutao").length) {
        togive = togive.getNext();
        await source.give(source.getExpansions("mutao").randomGet(), togive);
      }
      source.line(togive);
      let num = togive.countCards("h", { name: "sha" });
      if (num) {
        await togive.damage(Math.min(2, num), source);
      }
    },
    intro: {
      content: "expansion",
      markcount: "expansion"
    }
  },
  yimou: {
    audio: ["twyimou1.mp3", "yimou.mp3"],
    filter(event, player2) {
      return event.player.isIn() && get.distance(event.player, player2) <= 1;
    },
    inherit: "twyimou",
    async content(event, trigger, player2) {
      if (trigger.player != player2) {
        player2.addExpose(0.3);
      }
      var target = get.translation(trigger.player);
      var choiceList = ["令" + target + "获得牌堆里的一张【杀】", "令" + target + "将一张手牌交给另一名角色，然后" + target + "摸一张牌"];
      var list = ["选项一"];
      if (trigger.player.countCards("h") && game.hasPlayer((t) => t !== trigger.player)) {
        list.push("选项二");
      } else {
        choiceList[1] = '<span style="opacity:0.5">' + choiceList[1] + "</span>";
      }
      var choice = (await player2.chooseControl(list).set("prompt", "毅谋：请选择一项").set("choiceList", choiceList).set("ai", function() {
        var evt = _status.event.getTrigger(), list2 = _status.event.list;
        _status.event.player;
        var target2 = evt.player;
        if (target2.countCards("h") && list2.includes("选项二")) {
          return "选项二";
        }
        return "选项一";
      }).set("list", list).forResult()).control;
      if (choice != "选项二") {
        var card2 = get.cardPile2(function(card3) {
          return card3.name == "sha";
        });
        if (card2) {
          await trigger.player.gain(card2, "gain2");
        } else {
          game.log("但牌堆里已经没有", "#y杀", "了！");
        }
        if (choice == "选项一") {
          return;
        }
      }
      if (trigger.player.countCards("h") && game.hasPlayer((t) => t !== trigger.player)) {
        const result = await trigger.player.chooseCardTarget({
          prompt: "毅谋：将一张手牌交给另一名其他角色",
          filterCard: true,
          forced: true,
          filterTarget: lib.filter.notMe,
          ai1(card3) {
            return 1 / Math.max(0.1, get.value(card3));
          },
          ai2(target2) {
            var player3 = _status.event.player, att = get.attitude(player3, target2);
            if (target2.hasSkillTag("nogain")) {
              att /= 9;
            }
            return 4 + att;
          }
        }).forResult();
        if (!result?.bool || !result.cards?.length || !result.targets?.length) {
          return;
        }
        var target = result.targets[0];
        trigger.player.line(target);
        await trigger.player.give(result.cards, target);
        await trigger.player.draw();
      }
    }
  },
  //蒋济
  jilun: {
    audio: 2,
    trigger: { player: "damageEnd" },
    filter(event, player2) {
      return player2.hasSkill("twjichou", null, false, false);
    },
    async cost(event, trigger, player2) {
      const num = Math.min(Math.max(1, player2.getStorage("twjichou").length), 3);
      const list = get.inpileVCardList((info) => {
        return info[0] == "basic" && !player2.getStorage(event.skill).includes(info[2]);
      }).addArray(
        player2.getStorage("twjichou").filter((name) => !player2.getStorage(event.skill).includes(name)).map((name) => ["锦囊", "", name])
      );
      const result = await player2.chooseButton([`###${get.prompt(event.skill)}###摸${get.cnNumber(num)}张牌或者视为使用一张牌`, [[[num, `摸${get.cnNumber(num)}张牌`]], "tdnodes"], [list, "vcard"]]).set("filterButton", (button) => {
        const { player: player3, numx } = get.event();
        const { link } = button;
        if (!Array.isArray(link)) {
          return true;
        }
        return player3.hasUseTarget({ name: link[2], nature: link[3] }) && (get.type(link[2]) == "basic" || game.countPlayer((current) => {
          return player3.canUse({ name: link[2], nature: link[3] }, current);
        }) <= numx);
      }).set("ai", (button) => {
        const { player: player3, numx } = get.event();
        const { link } = button;
        const val = numx > 2 ? Math.min(1.5, 1 + (numx - 2) * 0.1) : 1;
        if (Array.isArray(link)) {
          if (player3.getUseValue({ name: link[2], nature: link[3] }) > 4 * val) {
            return 1;
          }
        }
        if (typeof link == "number") {
          return 1;
        }
        return 0;
      }).set("numx", num).forResult();
      event.result = {
        bool: result?.bool,
        cost_data: result?.links
      };
    },
    async content(event, trigger, player2) {
      const { cost_data: links } = event;
      if (typeof links[0] == "number") {
        await player2.draw(links[0]);
      } else {
        const card2 = get.autoViewAs({ name: links[0][2], nature: links[0][3], isCard: true });
        player2.markAuto(event.name, [card2.name]);
        await player2.chooseUseTarget(card2, true);
      }
    },
    marktext: "论",
    intro: { content: "已记录牌名：$" },
    onremove: true,
    ai: { combo: "twjichou" }
  },
  //李遗
  jiaohua: {
    onremove: true,
    audio: "twjiaohua",
    enable: "phaseUse",
    usable: 2,
    chooseButton: {
      dialog(event, player2) {
        return ui.create.dialog("###教化###选择一种牌的类型，令一名角色从牌堆获得此类型的一张牌");
      },
      chooseControl(event, player2) {
        var list = ["basic", "trick", "equip"].filter((type) => !player2.getStorage("jiaohua").includes(type));
        list.push("cancel2");
        return list;
      },
      check(event, player2) {
        var list = ["trick", "equip", "basic"].filter((type) => !player2.getStorage("jiaohua").includes(type));
        return list[0];
      },
      backup(result, player2) {
        return {
          type: result.control,
          audio: "twjiaohua",
          filterCard: () => false,
          selectCard: -1,
          filterTarget: true,
          async content(event, trigger, player3) {
            var type = lib.skill.jiaohua_backup.type;
            var card2 = get.cardPile2((card3) => get.type2(card3) == type);
            if (card2) {
              await event.target.gain(card2, "gain2");
            } else {
              game.log("但牌堆里已经没有", "#y" + get.translation(type) + "牌", "了！");
            }
            player3.markAuto("jiaohua", [lib.skill.jiaohua_backup.type]);
            if (!["basic", "trick", "equip"].some((type2) => !player3.getStorage("jiaohua").includes(type2))) {
              player3.popup("教化");
              player3.unmarkAuto("jiaohua", player3.getStorage("jiaohua"));
              game.log(player3, "清空了", "#g【教化】", "记录");
            }
          },
          ai: {
            result: { target: 1 }
          }
        };
      },
      prompt(result, player2) {
        return "令一名角色从牌堆中获得一张" + get.translation(result.control) + "牌";
      }
    },
    ai: {
      order: 7,
      result: { player: 1 }
    },
    intro: { content: "已记录$牌" }
  },
  //来敏
  laishou: {
    audio: 3,
    trigger: { player: ["damageBegin4", "phaseZhunbeiBegin"] },
    filter(event, player2) {
      var num = 9;
      if (event.name == "damage") {
        return event.num >= player2.getHp() && player2.maxHp < num;
      }
      return player2.maxHp >= num;
    },
    forced: true,
    logAudio(event, player2) {
      if (event.name == "damage") {
        return 2;
      }
      return "laishou3.mp3";
    },
    async content(event, trigger, player2) {
      if (trigger.name == "damage") {
        await player2.gainMaxHp(trigger.num);
        trigger.cancel();
      } else {
        await player2.die();
      }
    }
  },
  luanqun: {
    audio: 2,
    enable: "phaseUse",
    manualConfirm: true,
    filter(event, player2) {
      return player2.countCards("h");
    },
    usable: 1,
    async contentBefore(event, trigger, player2) {
      player2.line(game.filterPlayer((current) => current.countCards("h")));
    },
    async content(event, trigger, player2) {
      const targets2 = game.filterPlayer((current) => current.countCards("h")).sortBySeat();
      const next = player2.chooseCardOL(targets2, "乱群：请选择要展示的牌", true).set("ai", function(card3) {
        return -get.value(card3);
      }).set("source", player2);
      next.aiCard = function(target) {
        var hs = target.getCards("h");
        return { bool: true, cards: [hs.randomGet()] };
      };
      next._args.remove("glow_result");
      const result = await next.forResult();
      const cards2 = result.map((i) => i.cards[0]);
      await player2.showCards(cards2, get.translation(player2) + "发动了【乱群】").set("customButton", (button) => {
        const target = get.owner(button.link);
        if (target) {
          button.node.gaintag.innerHTML = target.getName();
        }
      }).set("delay_time", 4).set("multipleShow", true);
      const card2 = cards2[targets2.indexOf(player2)];
      const cardx = cards2.filter((cardy) => cardy != card2 && get.color(cardy, targets2[cards2.indexOf(cardy)]) == get.color(card2, player2));
      if (cardx.length) {
        const num = get.mode() == "identity" ? 4 : 2;
        const result2 = await player2.chooseButton(["乱群：是否获得其中至多" + get.cnNumber(num) + "张牌", cardx]).set("forceAuto", true).set("ai", function(button) {
          var cards3 = _status.event.list[0];
          var targets3 = _status.event.list[1];
          var player3 = _status.event.player;
          if (get.attitude(player3, targets3[cards3.indexOf(button.link)]) > 0) {
            return 0;
          }
          return get.value(button.link, player3);
        }).set("selectButton", [1, num]).set("list", [cards2, targets2]).forResult();
        if (result2?.links?.length) {
          await player2.gain(result2.links, "give");
        }
      }
      const targetsx = targets2.filter((target) => get.color(cards2[targets2.indexOf(target)], target) != get.color(card2, player2));
      if (targetsx.length) {
        player2.line(targetsx);
        targetsx.forEach((target) => {
          target.addTempSkill("luanqun_effect", { player: "phaseUseAfter" });
          target.markAuto("luanqun_effect", [player2]);
          target.addTempSkill("luanqun_directHit", { player: "phaseEnd" });
          target.markAuto("luanqun_directHit", [player2]);
        });
      }
    },
    ai: {
      order: 9,
      result: {
        player(player2, target) {
          if (player2.hasSkill("laishou")) {
            return 1;
          }
          return player2.hp >= 2 ? 1 : 0;
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        mod: {
          playerEnabled(card2, player2, target) {
            if (!player2.isPhaseUsing()) {
              return;
            }
            if (card2.name == "sha" && !player2.getStorage("luanqun_effect").includes(target)) {
              return false;
            }
          }
        },
        trigger: { player: "useCard1" },
        filter(event, player2) {
          return player2.isPhaseUsing() && event.card.name == "sha";
        },
        firstDo: true,
        forced: true,
        async content(event, trigger, player2) {
          player2.removeSkill("luanqun_effect");
        }
      },
      directHit: {
        charlotte: true,
        onremove: true,
        intro: { content: "出牌阶段第一张【杀】只能指定$为目标，且其不可响应你回合内使用的【杀】" },
        trigger: { player: "useCardToPlayered" },
        filter(event, player2) {
          return player2 === _status.currentPhase && event.card.name == "sha" && player2.getStorage("luanqun_directHit").some((tar) => {
            return event.targets.includes(tar);
          });
        },
        forced: true,
        logTarget(event, player2) {
          return player2.getStorage("luanqun_directHit");
        },
        async content(event, trigger, player2) {
          trigger.directHit.addArray(player2.getStorage("luanqun_directHit"));
        },
        ai: {
          directHit_ai: true,
          skillTagFilter(player2, tag, arg) {
            if (tag === "directHit_ai") {
              return player2.getStorage("luanqun_directHit").includes(arg.target);
            }
          }
        }
      }
    }
  },
  //郭照
  yichong: {
    initSkill(skill) {
      if (!lib.skill[skill]) {
        lib.skill[skill] = {
          charlotte: true,
          onremove: true,
          mark: true,
          marktext: "雀",
          intro: {
            markcount(storage) {
              return (storage || 0).toString();
            },
            content(storage) {
              return "已被掠夺" + (storage || 0) + "张牌";
            }
          }
        };
        lib.translate[skill] = "易宠";
        lib.translate[skill + "_bg"] = "雀";
      }
    },
    getLimit: 1,
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player2) {
      return game.hasPlayer((current) => current != player2);
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt("yichong"), "选择一名其他角色并选择一个花色，获得其此花色的所有牌并令其获得“雀”标记", lib.filter.notMe).set("ai", (target) => {
        const player3 = get.player();
        const att = get.attitude(player3, target);
        if (att > 0) {
          return 0;
        }
        const getNum = function(player4) {
          const list = [];
          for (const i of lib.suit) {
            list.push(player4.countCards("he", { suit: i }) + 3);
          }
          return list.sort((a, b) => b - a)[0];
        };
        return getNum(target) + target.countCards("h") / 10;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const result = await player2.chooseControl(lib.suit.slice(0).reverse()).set("prompt", "请声明一个花色").set("ai", () => {
        const target2 = _status.event.target, cards3 = target2.getCards("he");
        const suits = lib.suit.slice(0);
        suits.sort(function(a, b) {
          var num = function(suit2) {
            return cards3.filter(function(card2) {
              return get.suit(card2) == suit2;
            }).length;
          };
          return num(b) - num(a);
        });
        return suits[0];
      }).set("target", target).forResult();
      if (!result?.control) {
        return;
      }
      const suit = result.control;
      event.suit = suit;
      player2.chat(get.translation(suit + 2));
      game.log(player2, "选择了", "#y" + get.translation(suit + 2));
      const cards2 = target.getCards("he", (card2) => {
        return lib.filter.canBeGained(card2, target, player2) && get.suit(card2) == suit;
      });
      if (cards2.length) {
        await player2.gain(cards2, target, "giveAuto", "bySelf");
      }
      player2.storage.yichong = suit;
      player2.markSkill("yichong");
      const skill = `yichong_${player2.playerid}`;
      game.broadcastAll(lib.skill.yichong.initSkill, skill);
      game.broadcastAll(
        function(player3, suit2) {
          if (player3.marks.yichong) {
            player3.marks.yichong.firstChild.innerHTML = get.translation(suit2);
          }
        },
        player2,
        suit
      );
      game.countPlayer(function(current) {
        current.removeSkill(`yichong_${player2.playerid}`);
        if (current == target) {
          target.addSkill(`yichong_${player2.playerid}`);
        }
      });
      player2.addTempSkill("yichong_clear", { player: "phaseBegin" });
    },
    onremove: true,
    intro: { content: "拥有“雀”标记的角色得到$牌后，你获得之" },
    group: "yichong_gain",
    subSkill: {
      gain: {
        audio: "yichong",
        trigger: { global: ["gainAfter", "loseAsyncAfter"] },
        filter(event, player2) {
          if (!player2.storage.yichong) {
            return false;
          }
          return game.hasPlayer(function(current) {
            if (!event.getg(current).length || !current.hasSkill(`yichong_${player2.playerid}`)) {
              return false;
            }
            if (current.countMark(`yichong_${player2.playerid}`) >= lib.skill.yichong.getLimit) {
              return false;
            }
            return event.getg(current).some((card2) => get.suit(card2, current) == player2.storage.yichong && lib.filter.canBeGained(card2, current, player2));
          });
        },
        forced: true,
        async content(event, trigger, player2) {
          const target = game.findPlayer(function(current) {
            if (!trigger.getg(current).length || !current.hasSkill(`yichong_${player2.playerid}`)) {
              return false;
            }
            if (current.countMark(`yichong_${player2.playerid}`) >= lib.skill.yichong.getLimit) {
              return false;
            }
            return trigger.getg(current).some((card2) => get.suit(card2, current) == player2.storage.yichong && lib.filter.canBeGained(card2, current, player2));
          });
          let cards2 = trigger.getg(target).filter((card2) => get.suit(card2, target) == player2.storage.yichong && lib.filter.canBeGained(card2, target, player2));
          const num = lib.skill.yichong.getLimit - target.countMark(`yichong_${player2.playerid}`);
          cards2 = cards2.randomGets(num);
          if (cards2.length) {
            await player2.gain(cards2, target, "giveAuto");
            target.addMark(`yichong_${player2.playerid}`, cards2.length, false);
          }
        }
      },
      clear: {
        charlotte: true,
        onremove(player2) {
          game.countPlayer(function(current) {
            current.removeSkill(`yichong_${player2.playerid}`);
          });
        }
      }
    }
  },
  wufei: {
    audio: 2,
    trigger: { source: "damageBefore" },
    filter(event, player2, name) {
      const target = game.findPlayer((current) => current.hasSkill(`yichong_${player2.playerid}`));
      if (!target) {
        return false;
      }
      return event.card && (event.card.name == "sha" || get.type(event.card) == "trick" && get.is.damageCard(event.card));
    },
    forced: true,
    locked: false,
    logTarget(event, player2) {
      return game.findPlayer((current) => current.hasSkill(`yichong_${player2.playerid}`));
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      trigger.source = target;
      game.log(target, "成为了", trigger.card, "的伤害来源");
    },
    ai: { combo: "yichong" },
    group: "wufei_damage",
    subSkill: {
      damage: {
        audio: "wufei",
        trigger: { player: "damageEnd" },
        filter(event, player2, name) {
          const target = game.findPlayer((current) => current.hasSkill(`yichong_${player2.playerid}`));
          if (!target) {
            return false;
          }
          const num = target.getHp();
          return num > 3 && num > player2.getHp();
        },
        async cost(event, trigger, player2) {
          const target = game.findPlayer((current) => current.hasSkill(`yichong_${player2.playerid}`));
          event.result = await player2.chooseBool(get.prompt(event.skill, target), `令${get.translation(target)}受到1点无来源伤害`).set("choice", get.damageEffect(target, player2, player2) > 0).forResult();
        },
        logTarget(event, player2) {
          return game.findPlayer((current) => current.hasSkill(`yichong_${player2.playerid}`));
        },
        async content(event, trigger, player2) {
          const target = event.targets[0];
          await target.damage("nosource");
        }
      }
    }
  },
  //张嶷
  xinwurong: {
    audio: 3,
    enable: "phaseUse",
    usable: 1,
    filterTarget: lib.filter.notMe,
    logAudio: (index) => typeof index === "number" ? "xinwurong" + index + ".mp3" : 1,
    async content(event, trigger, player2) {
      const target = event.target;
      const result = await player2.chooseToDuiben(target).set("title", "谋弈").set("namelist", ["反抗", "归顺", "镇压", "安抚"]).set("translationList", [`对方选择镇压：${get.translation(player2)}对你造成1点伤害，然后其摸一张牌<br>对方选择安抚：${get.translation(player2)}受到1点伤害，然后其摸一张牌`, `对方选择镇压：${get.translation(player2)}获得你一张牌，然后其交给你两张牌<br>对方选择安抚：你须交给${get.translation(player2)}两张牌（若你牌数不足两张，则改为其令你跳过你下个摸牌阶段）`, `对方选择反抗：你对${get.translation(target)}造成1点伤害，然后你摸一张牌<br>对方选择归顺：你获得${get.translation(target)}一张牌，然后你交给其两张牌`, `对方选择反抗：你受到1点伤害，然后你摸一张牌<br>对方选择归顺：${get.translation(target)}须交给你两张牌（若其牌数不足两张，则改为令其跳过其下个摸牌阶段）`]).set("ai", (button) => 1 + Math.random()).forResult();
      if (result.bool) {
        player2.logSkill("xinwurong", target, null, null, [result.player == "db_def1" ? 3 : 2]);
        if (result.player == "db_def1") {
          await target.damage();
          await player2.draw();
          return;
        }
        var cards2 = target.getCards("he");
        if (cards2.length < 2) {
          target.skip("phaseDraw");
          target.addTempSkill("xinwurong_skip", { player: "phaseDrawSkipped" });
          return;
        }
        if (cards2.length == 2) {
          await player2.gain(cards2, target, "giveAuto");
          return;
        }
        const result2 = await target.chooseCard("怃戎：交给" + get.translation(player2) + "两张牌", 2, true, "he").forResult();
        if (result2.bool) {
          await player2.gain(result2.cards, target, "giveAuto");
        }
        return;
      }
      if (result.player == "db_def1") {
        await player2.gainPlayerCard(target, "he", true);
        var cards2 = player2.getCards("he");
        if (!cards2.length) {
          return;
        }
        if (cards2.length <= 2) {
          await target.gain(cards2, player2, "giveAuto");
          return;
        }
        const result3 = await player2.chooseCard("怃戎：交给" + get.translation(target) + "两张牌", 2, true, "he").forResult();
        if (result3.bool) {
          await target.gain(result3.cards, player2, "giveAuto");
        }
        return;
      }
      await player2.damage();
      await player2.draw();
    },
    ai: {
      order: 7,
      result: {
        player: 1,
        target: -1
      }
    },
    subSkill: {
      skip: {
        charlotte: true,
        mark: true,
        intro: { content: "跳过下个摸牌阶段" }
      }
    }
  },
  //孙亮
  xinkuizhu: {
    audio: "nzry_kuizhu",
    trigger: { player: "phaseDiscardAfter" },
    filter(event, player2) {
      return player2.getHistory("lose", function(evt) {
        return evt.type == "discard" && evt.getParent("phaseDiscard") == event;
      }).length;
    },
    direct: true,
    async content(event, trigger, player2) {
      var cards2 = [];
      player2.getHistory("lose", function(evt) {
        if (evt.type == "discard" && evt.getParent("phaseDiscard") == trigger) {
          cards2.addArray(evt.cards2);
        }
      });
      var num = cards2.length;
      var str1 = "令至多" + num + "名角色摸一张牌";
      var str2 = "对任意名体力值之和为" + num + "的角色造成1点伤害";
      var result = await player2.chooseControl("cancel2").set("ai", function() {
        if (game.countPlayer(function(current) {
          return get.attitude(player2, current) < 0 && current.hp == event.num;
        }) > 0 && event.num <= 3) {
          return 1;
        }
        return 0;
      }).set("choiceList", [str1, str2]).set("prompt", "是否发动【溃诛】？").forResult();
      if (result.control == "cancel2") {
        return;
      }
      var control = [str1, str2][result.index];
      var str = "请选择〖溃诛〗的目标";
      while (true) {
        if (control == str2) {
          var result2 = await player2.chooseTarget(str, function(card2, player3, target) {
            var targets3 = ui.selected.targets;
            var num2 = 0;
            for (var i2 = 0; i2 < targets3.length; i2++) {
              num2 += targets3[i2].hp;
            }
            return num2 + target.hp <= _status.event.num;
          }).set("ai", function(target) {
            if (ui.selected.targets[0] != void 0) {
              return -1;
            }
            return get.attitude(player2, target) < 0;
          }).set("promptbar", "none").set("num", num).set("selectTarget", function() {
            var targets3 = ui.selected.targets;
            var num2 = 0;
            for (var i2 = 0; i2 < targets3.length; i2++) {
              num2 += targets3[i2].hp;
            }
            if (num2 == _status.event.num) {
              return ui.selected.targets.length;
            }
            return ui.selected.targets.length + 1;
          }).forResult();
          if (!result2.bool) {
            return;
          }
          var targets2 = result2.targets.sortBySeat();
          var sum = 0;
          for (var i = 0; i < targets2.length; i++) {
            sum += targets2[i].hp;
          }
          if (sum < num) {
            str = "<br>所选目标体力之和不足" + num + "，请重选";
            continue;
          }
          player2.logSkill("xinkuizhu", targets2);
          for (var i of targets2) {
            await i.damage();
          }
          if (targets2.length >= 2) {
            await player2.loseHp();
          }
          return;
        }
        var result2 = await player2.chooseTarget("请选择〖溃诛〗的目标", "令至多" + get.cnNumber(num) + "名角色各摸一张牌", [1, num]).set("ai", function(target) {
          return get.attitude(_status.event.player, target);
        }).forResult();
        if (!result2.bool) {
          return;
        }
        var targets2 = result2.targets.sortBySeat();
        player2.logSkill("xinkuizhu", targets2);
        await game.asyncDraw(targets2);
        return;
      }
    }
  },
  xinzhizheng: {
    audio: "nzry_zhizheng",
    mod: {
      playerEnabled(card2, player2, target) {
        var info = get.info(card2);
        if (target != player2 && (!info || !info.singleCard || !ui.selected.targets.length) && player2.isPhaseUsing() && !target.inRange(player2)) {
          return false;
        }
      }
    },
    trigger: { player: "phaseUseEnd" },
    filter(event, player2) {
      return player2.getHistory("useCard", function(evt) {
        return evt.getParent("phaseUse") == event;
      }).length < game.countPlayer(function(current) {
        return current != player2 && !current.inRange(player2);
      }) && game.hasPlayer(function(target) {
        return target != player2 && !target.inRange(player2) && target.countDiscardableCards(player2, "he");
      });
    },
    forced: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget("请选择〖掣政〗的目标", "弃置一名攻击范围内不包含你的角色的一张牌", true, function(card2, player3, target) {
        return target != player3 && !target.inRange(player3) && target.countDiscardableCards(player3, "he");
      }).set("ai", function(target) {
        var player3 = _status.event.player;
        return get.effect(target, { name: "guohe_copy2" }, player3, player3);
      }).forResult();
      if (result.bool) {
        player2.line(result.targets);
        await player2.discardPlayerCard(result.targets[0], "he", true);
      }
    }
  },
  xinlijun: {
    audio: "nzry_lijun1",
    trigger: { global: "useCardAfter" },
    filter(event, player2) {
      if (event.card.name != "sha") {
        return false;
      }
      if (_status.currentPhase != event.player || event.player.group != "wu") {
        return false;
      }
      if (!player2.hasZhuSkill("xinlijun", event.player) || player2 == event.player) {
        return false;
      }
      return event.cards.filterInD().length;
    },
    zhuSkill: true,
    direct: true,
    async content(event, trigger, player2) {
      const bool = await trigger.player.chooseBool(get.prompt("xinlijun"), "将" + get.translation(trigger.cards) + "交给" + get.translation(player2)).set("choice", get.attitude(trigger.player, player2) > 0).forResult();
      if (!bool.bool) {
        return;
      }
      player2.logSkill("xinlijun", trigger.player);
      await player2.gain(trigger.cards.filterInD(), "gain2");
      const bool2 = await player2.chooseBool().set("prompt", "是否令" + get.translation(trigger.player) + "摸一张牌？").set("choice", get.attitude(player2, trigger.player) > 0).forResult();
      if (bool2.bool) {
        await trigger.player.draw();
      }
    }
  },
  //十常侍
  mbdanggu: {
    trigger: {
      player: "enterGame",
      global: "phaseBefore"
    },
    filter(event, player2) {
      return event.name != "phase" || game.phaseNumber == 0;
    },
    derivation: ["mbdanggu_faq", "mbdanggu_faq2", "scstaoluan", "scschiyan", "scszimou", "scspicai", "scsyaozhuo", "scsxiaolu", "scskuiji", "scschihe", "scsniqu", "scsmiaoyu"],
    forced: true,
    unique: true,
    onremove(player2) {
      delete player2.storage.mbdanggu;
      delete player2.storage.mbdanggu_current;
      if (lib.skill.mbdanggu.isSingleShichangshi(player2)) {
        game.broadcastAll(function(player3) {
          player3.name1 = player3.name;
          player3.skin.name = player3.name;
          player3.smoothAvatar(false);
          player3.node.avatar.setBackground(player3.name, "character");
          player3.node.name.innerHTML = get.slimName(player3.name);
          delete player3.name2;
          delete player3.skin.name2;
          player3.classList.remove("fullskin2");
          player3.node.avatar2.classList.add("hidden");
          player3.node.name2.innerHTML = "";
          if (player3 == game.me && ui.fakeme) {
            ui.fakeme.style.backgroundImage = player3.node.avatar.style.backgroundImage;
          }
        }, player2);
      }
    },
    changshi: [
      ["scs_zhangrang", "scstaoluan"],
      ["scs_zhaozhong", "scschiyan"],
      ["scs_sunzhang", "scszimou"],
      ["scs_bilan", "scspicai"],
      ["scs_xiayun", "scsyaozhuo"],
      ["scs_hankui", "scsxiaolu"],
      ["scs_lisong", "scskuiji"],
      ["scs_duangui", "scschihe"],
      ["scs_guosheng", "scsniqu"],
      ["scs_gaowang", "scsmiaoyu"]
    ],
    conflictMap(player2) {
      if (!_status.changshiMap) {
        _status.changshiMap = {
          scs_zhangrang: [],
          scs_zhaozhong: [],
          scs_sunzhang: [],
          scs_bilan: ["scs_hankui"],
          scs_xiayun: [],
          scs_hankui: ["scs_bilan"],
          scs_lisong: [],
          scs_duangui: ["scs_guosheng"],
          scs_guosheng: ["scs_duangui"],
          scs_gaowang: []
        };
        if (!get.isLuckyStar(player2)) {
          var list = lib.skill.mbdanggu.changshi.map((i2) => i2[0]);
          for (var i of list) {
            var select = list.filter((scs) => scs != i && !_status.changshiMap[i].includes(i));
            _status.changshiMap[i].addArray(select.randomGets(get.rand(0, select.length)));
          }
        }
      }
      return _status.changshiMap;
    },
    async content(event, trigger, player2) {
      const list = lib.skill.mbdanggu.changshi.map((i) => i[0]);
      player2.markAuto("mbdanggu", list);
      game.broadcastAll(
        function(player3, list2) {
          const cards2 = [];
          for (let i = 0; i < list2.length; i++) {
            const cardname = "huashen_card_" + list2[i];
            lib.card[cardname] = {
              fullimage: true,
              image: "character:" + list2[i]
            };
            lib.translate[cardname] = get.rawName2(list2[i]);
            cards2.push(game.createCard(cardname, "", ""));
          }
          player3.$draw(cards2, "nobroadcast");
        },
        player2,
        list
      );
      const next = game.createEvent("mbdanggu_clique");
      next.player = player2;
      next.setContent(lib.skill.mbdanggu.contentx);
      await next;
    },
    async contentx(event, trigger, player2) {
      let list = player2.getStorage("mbdanggu").slice();
      const first = list.randomRemove();
      const others = list.randomGets(4);
      let result;
      if (others.length == 1) {
        result = { bool: true, links: others };
      } else {
        const map = {
          scs_bilan: "scs_hankui",
          scs_hankui: "scs_bilan",
          scs_duangui: "scs_guosheng",
          scs_guosheng: "scs_duangui"
        }, map2 = lib.skill.mbdanggu.conflictMap(player2);
        const conflictList = others.filter((changshi) => {
          if (map[first] && others.some((changshi2) => map[first] == changshi2)) {
            return map[first] == changshi;
          } else {
            return map2[first].includes(changshi);
          }
        });
        list = others.slice();
        if (conflictList.length) {
          const conflict = conflictList.randomGet();
          list.remove(conflict);
          game.broadcastAll(
            function(changshi, player3) {
              if (lib.config.background_speak) {
                if (player3.isUnderControl(true)) {
                  game.playAudio("skill", changshi + "_enter");
                }
              }
            },
            conflict,
            player2
          );
        }
        result = await player2.chooseButton(["党锢：请选择结党对象", [[first], "character"], '<div class="text center">可选常侍</div>', [others, "character"]], true).set("filterButton", (button) => {
          return _status.event.canChoose.includes(button.link);
        }).set("canChoose", list).set("ai", (button) => Math.random() * 10).forResult();
      }
      if (result?.bool) {
        const chosen = result.links[0];
        const skills2 = [];
        list = lib.skill.mbdanggu.changshi;
        const changshis = [first, chosen];
        player2.unmarkAuto("mbdanggu", changshis);
        player2.storage.mbdanggu_current = changshis;
        player2.markSkill("mbdanggu_current");
        for (const changshi of changshis) {
          for (const cs of list) {
            if (changshi == cs[0]) {
              skills2.push(cs[1]);
            }
          }
        }
        if (lib.skill.mbdanggu.isSingleShichangshi(player2)) {
          game.broadcastAll(
            function(player3, first2, chosen2) {
              player3.name1 = first2;
              player3.node.avatar.setBackground(first2, "character");
              player3.node.name.innerHTML = get.slimName(first2);
              player3.name2 = chosen2;
              player3.skin.name = first2;
              player3.skin.name2 = chosen2;
              player3.classList.add("fullskin2");
              player3.node.avatar2.classList.remove("hidden");
              player3.node.avatar2.setBackground(chosen2, "character");
              player3.node.name2.innerHTML = get.slimName(chosen2);
              if (player3 == game.me && ui.fakeme) {
                ui.fakeme.style.backgroundImage = player3.node.avatar.style.backgroundImage;
              }
            },
            player2,
            first,
            chosen
          );
        }
        game.log(player2, "选择了常侍", "#y" + get.translation(changshis));
        if (skills2.length) {
          player2.addAdditionalSkill("mbdanggu", skills2);
          let str = "";
          for (const i of skills2) {
            str += "【" + get.translation(i) + "】、";
            player2.popup(i);
          }
          str = str.slice(0, -1);
          game.log(player2, "获得了技能", "#g" + str);
        }
      }
    },
    isSingleShichangshi(player2) {
      var map = lib.skill.mbdanggu.conflictMap(player2);
      return player2.name == "shichangshi" && (map[player2.name1] && map[player2.name2] || map[player2.name1] && !player2.name2 || !player2.name1 && !player2.name2 || player2.name == player2.name1 && !player2.name2);
    },
    mod: {
      aiValue(player2, card2, num) {
        if (["shan", "tao", "wuxie", "caochuan"].includes(card2.name)) {
          return num / 10;
        }
      },
      aiUseful() {
        return lib.skill.mbdanggu.mod.aiValue.apply(this, arguments);
      }
    },
    ai: {
      combo: "mbmowang",
      nokeep: true
    },
    intro: {
      mark(dialog, storage, player2) {
        dialog.addText("剩余常侍");
        dialog.addSmall([storage, "character"]);
        if (player2.storage.mbdanggu_current && player2.isIn()) {
          dialog.addText("当前常侍");
          dialog.addSmall([player2.storage.mbdanggu_current, "character"]);
        }
      }
    }
  },
  mbmowang: {
    trigger: {
      player: ["dieBefore", "rest", "dieAfter"]
    },
    filter(event, player2, name) {
      if (name == "rest") {
        return true;
      }
      if (name == "dieAfter") {
        return event.reserveOut;
      }
      return event.getParent().name != "giveup" && player2.maxHp > 0;
    },
    derivation: "mbmowang_faq",
    forced: true,
    forceDie: true,
    forceOut: true,
    direct: true,
    priority: 15,
    group: ["mbmowang_die", "mbmowang_return"],
    async content(event, trigger, player2) {
      if (event.triggername == "rest") {
        game.broadcastAll(
          function(player3, list) {
            if (list.includes(player3.name1) || player3.name1 == "shichangshi") {
              player3.smoothAvatar(false);
              player3.skin.name = player3.name1 + "_dead";
              player3.node.avatar.setBackground(player3.name1 + "_dead", "character");
            }
            if (list.includes(player3.name2) || player3.name2 == "shichangshi") {
              player3.smoothAvatar(true);
              player3.skin.name2 = player3.name2 + "_dead";
              player3.node.avatar2.setBackground(player3.name2 + "_dead", "character");
            }
          },
          player2,
          lib.skill.mbdanggu.changshi.map((i) => i[0])
        );
      } else if (event.triggername == "dieAfter") {
        if (player2.getStorage("mbdanggu").length) {
          game.broadcastAll(function() {
            if (lib.config.background_speak) {
              game.playAudio("die", "shichangshiRest");
            }
          });
          await player2.rest({ type: "round", count: 1 });
        }
      } else {
        if (player2.isRest()) {
          trigger.cancel();
        } else {
          if (player2.getStorage("mbdanggu").length) {
            player2.logSkill("mbmowang");
            trigger.excludeMark.add("mbdanggu");
            trigger.noDieAudio = true;
            trigger.reserveOut = true;
          } else {
            game.broadcastAll(function(player3) {
              player3.name1 = player3.name;
              player3.skin.name = player3.name + "_dead";
              player3.smoothAvatar(false);
              player3.node.avatar.setBackground(player3.name + "_dead", "character");
              player3.node.name.innerHTML = get.slimName(player3.name);
              delete player3.name2;
              delete player3.skin.name2;
              player3.classList.remove("fullskin2");
              player3.node.avatar2.classList.add("hidden");
              player3.node.name2.innerHTML = "";
              if (player3 == game.me && ui.fakeme) {
                ui.fakeme.style.backgroundImage = player3.node.avatar.style.backgroundImage;
              }
            }, player2);
          }
        }
      }
    },
    ai: {
      combo: "mbdanggu",
      neg: true
    },
    subSkill: {
      die: {
        audio: "mbmowang",
        trigger: { player: "phaseAfter" },
        forced: true,
        //forceDie: true,
        async content(event, trigger, player2) {
          if (lib.skill.mbdanggu.isSingleShichangshi(player2)) {
            if (!player2.getStorage("mbdanggu").length) {
              game.broadcastAll(function(player3) {
                player3.name1 = player3.name;
                player3.skin.name = player3.name + "_dead";
                player3.smoothAvatar(false);
                player3.node.avatar.setBackground(player3.name + "_dead", "character");
                player3.node.name.innerHTML = get.slimName(player3.name);
                delete player3.name2;
                delete player3.skin.name2;
                player3.classList.remove("fullskin2");
                player3.node.avatar2.classList.add("hidden");
                player3.node.name2.innerHTML = "";
                if (player3 == game.me && ui.fakeme) {
                  ui.fakeme.style.backgroundImage = player3.node.avatar.style.backgroundImage;
                }
              }, player2);
            }
          }
          if (!player2.getStorage("mbdanggu").length) {
            await game.delay();
          }
          await player2.die();
        }
      },
      return: {
        trigger: { player: "restEnd" },
        forced: true,
        charlotte: true,
        silent: true,
        forceDie: true,
        forceOut: true,
        filter(event, player2) {
          return event.player == player2 && player2.hasSkill("mbdanggu", null, null, false);
        },
        async content(event, trigger, player2) {
          game.broadcastAll(function(player3) {
            if (player3.name1 == "shichangshi") {
              player3.smoothAvatar(false);
              player3.node.avatar.setBackground(player3.name1, "character");
              if (!lib.skill.mbdanggu.isSingleShichangshi(player3)) {
                player3.skin.name = player3.name1;
              }
            }
            if (player3.name2 == "shichangshi") {
              player3.smoothAvatar(true);
              player3.node.avatar2.setBackground(player3.name2, "character");
              if (!lib.skill.mbdanggu.isSingleShichangshi(player3)) {
                player3.skin.name2 = player3.name2;
              }
            }
          }, player2);
          delete player2.storage.mbdanggu_current;
          player2.markSkill("mbdanggu_current");
          if (lib.skill.mbdanggu.isSingleShichangshi(player2)) {
            game.broadcastAll(function(player3) {
              player3.name1 = player3.name;
              player3.skin.name = player3.name;
              player3.smoothAvatar(false);
              player3.node.avatar.setBackground(player3.name, "character");
              player3.node.name.innerHTML = get.slimName(player3.name);
              delete player3.name2;
              delete player3.skin.name2;
              player3.classList.remove("fullskin2");
              player3.node.avatar2.classList.add("hidden");
              player3.node.name2.innerHTML = "";
              if (player3 == game.me && ui.fakeme) {
                ui.fakeme.style.backgroundImage = player3.node.avatar.style.backgroundImage;
              }
            }, player2);
          }
          const next = game.createEvent("mbdanggu_clique");
          next.player = player2;
          next.setContent(lib.skill.mbdanggu.contentx);
          await next;
          await player2.draw();
        }
      }
    }
  },
  //张让
  scstaoluan: {
    audio: 1,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.countCards("hes") > 0;
    },
    chooseButton: {
      dialog(event, player2) {
        var list = [];
        for (var i = 0; i < lib.inpile.length; i++) {
          var name = lib.inpile[i];
          if (name == "sha") {
            list.push(["基本", "", "sha"]);
            for (var j of lib.inpile_nature) {
              list.push(["基本", "", "sha", j]);
            }
          } else if (get.type(name) == "trick") {
            list.push(["锦囊", "", name]);
          } else if (get.type(name) == "basic") {
            list.push(["基本", "", name]);
          }
        }
        return ui.create.dialog("滔乱", [list, "vcard"]);
      },
      filter(button, player2) {
        return _status.event.getParent().filterCard({ name: button.link[2] }, player2, _status.event.getParent());
      },
      check(button) {
        var player2 = _status.event.player;
        if (player2.countCards("hs", button.link[2]) > 0) {
          return 0;
        }
        if (button.link[2] == "wugu") {
          return;
        }
        var effect = player2.getUseValue(button.link[2]);
        if (effect > 0) {
          return effect;
        }
        return 0;
      },
      backup(links, player2) {
        return {
          filterCard: true,
          audio: "scstaoluan",
          selectCard: 1,
          popname: true,
          check(card2) {
            return 6 - get.value(card2);
          },
          position: "hes",
          viewAs: { name: links[0][2], nature: links[0][3] }
        };
      },
      prompt(links, player2) {
        return "将一张牌当做" + (get.translation(links[0][3]) || "") + get.translation(links[0][2]) + "使用";
      }
    },
    ai: {
      order: 4,
      result: {
        player: 1
      },
      threaten: 1.9
    },
    subSkill: { backup: {} }
  },
  //赵忠
  scschiyan: {
    audio: 1,
    trigger: { player: "useCardToPlayered" },
    direct: true,
    filter(event, player2) {
      return event.card.name == "sha" && event.target.hp > 0 && event.target.countCards("he") > 0;
    },
    async content(event, trigger, player2) {
      var next = player2.choosePlayerCard(trigger.target, "he", get.prompt("scschiyan", trigger.target));
      next.set("ai", function(button) {
        if (!_status.event.goon) {
          return 0;
        }
        var val = get.value(button.link);
        if (button.link == _status.event.target.getEquip(2)) {
          return 2 * (val + 3);
        }
        return val;
      });
      next.set("goon", get.attitude(player2, trigger.target) <= 0);
      next.set("forceAuto", true);
      const result = await next.forResult();
      if (result.bool) {
        var target = trigger.target;
        player2.logSkill("scschiyan", target);
        target.addSkill("scschiyan_get");
        const nextx = target.addToExpansion("giveAuto", result.cards, target);
        nextx.gaintag.add("scschiyan_get");
        await nextx;
      }
    },
    ai: {
      unequip_ai: true,
      directHit_ai: true,
      skillTagFilter(player2, tag, arg) {
        if (get.attitude(player2, arg.target) > 0) {
          return false;
        }
        if (tag == "directHit_ai") {
          return arg.target.hp >= Math.max(1, arg.target.countCards("h") - 1);
        }
        if (arg && arg.name == "sha" && arg.target.getEquip(2)) {
          return true;
        }
        return false;
      }
    },
    group: "scschiyan_damage",
    subSkill: {
      get: {
        trigger: { global: "phaseEnd" },
        forced: true,
        popup: false,
        charlotte: true,
        filter(event, player2) {
          return player2.getExpansions("scschiyan_get").length > 0;
        },
        async content(event, trigger, player2) {
          var cards2 = player2.getExpansions("scschiyan_get");
          await player2.gain(cards2, "draw");
          game.log(player2, "收回了" + get.cnNumber(cards2.length) + "张“鸱咽”牌");
          player2.removeSkill("scschiyan_get");
        },
        intro: {
          markcount: "expansion",
          mark(dialog, storage, player2) {
            var cards2 = player2.getExpansions("scschiyan_get");
            if (player2.isUnderControl(true)) {
              dialog.addAuto(cards2);
            } else {
              return "共有" + get.cnNumber(cards2.length) + "张牌";
            }
          }
        }
      },
      damage: {
        audio: "scschiyan",
        trigger: { source: "damageBegin1" },
        forced: true,
        locked: false,
        logTarget: "player",
        filter(event, player2) {
          var target = event.player;
          return event.getParent().name == "sha" && player2.countCards("h") >= target.countCards("h") && player2.countCards("e") >= target.countCards("e");
        },
        async content(event, trigger, player2) {
          trigger.num++;
        }
      }
    }
  },
  //孙璋
  scszimou: {
    audio: 1,
    trigger: { player: "useCard" },
    forced: true,
    filter(event, player2) {
      var evt = event.getParent("phaseUse");
      if (!evt || evt.player != player2) {
        return false;
      }
      var num = player2.getHistory("useCard", (evtx) => evtx.getParent("phaseUse") == evt).length;
      return num == 2 || num == 4 || num == 6;
    },
    async content(event, trigger, player2) {
      var evt = trigger.getParent("phaseUse");
      var num = player2.getHistory("useCard", (evtx) => evtx.getParent("phaseUse") == evt).length;
      var cards2 = [];
      if (num == 2) {
        var card2 = get.cardPile2((card3) => {
          return ["jiu", "xionghuangjiu"].includes(card3.name);
        });
        if (card2) {
          cards2.push(card2);
        }
      } else if (num == 4) {
        var card2 = get.cardPile2((card3) => {
          return card3.name == "sha";
        });
        if (card2) {
          cards2.push(card2);
        }
      } else if (num == 6) {
        var card2 = get.cardPile2((card3) => {
          return card3.name == "juedou";
        });
        if (card2) {
          cards2.push(card2);
        }
      }
      if (cards2.length) {
        await player2.gain(cards2, "gain2");
      }
    }
  },
  //毕岚
  scspicai: {
    audio: 1,
    enable: "phaseUse",
    usable: 1,
    manualConfirm: true,
    frequent: true,
    async content(event, trigger, player2) {
      event.cards = [];
      event.suits = [];
      event.again = true;
      while (event.again) {
        event.again = false;
        await player2.judge((result2) => {
          let evt = _status.event.getParent("scspicai");
          if (evt?.suits?.includes(get.suit(result2))) {
            return 0;
          }
          return 1;
        }).set("callback", lib.skill.scspicai.callback).set("judge2", (result2) => result2.bool);
      }
      const cards2 = event.cards.filterInD();
      if (!cards2.length) {
        return;
      }
      const result = await player2.chooseTarget("将" + get.translation(cards2) + "交给一名角色", true).set("ai", function(target2) {
        var player3 = _status.event.player;
        var att = get.attitude(player3, target2) / Math.sqrt(1 + target2.countCards("h"));
        if (target2.hasSkillTag("nogain")) {
          att /= 10;
        }
        return att;
      }).forResult();
      if (!result.bool) {
        return;
      }
      const target = result.targets[0];
      player2.line(target, "green");
      const next = target.gain(cards2, "gain2");
      next.giver = player2;
      await next;
    },
    async callback(event, trigger, player2) {
      const evt = event.getParent(2);
      event.getParent().orderingCards.remove(event.judgeResult.card);
      evt.cards.push(event.judgeResult.card);
      if (event.getParent().result.bool) {
        evt.suits.push(event.getParent().result.suit);
        const result = await player2.chooseBool("是否继续发动【庀材】？").set("frequentSkill", "scspicai").forResult();
        if (result.bool) {
          event.getParent(2).again = true;
        }
      } else {
        return;
      }
    },
    ai: {
      order: 9,
      result: {
        player: 1
      }
    }
  },
  //夏恽
  scsyaozhuo: {
    audio: 1,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.hasPlayer(function(current) {
        return player2.canCompare(current);
      });
    },
    filterTarget(card2, player2, current) {
      return player2.canCompare(current);
    },
    async content(event, trigger, player2) {
      const target = event.target;
      const result = await player2.chooseToCompare(target).forResult();
      if (result.bool) {
        target.skip("phaseDraw");
        target.addTempSkill("scsyaozhuo_skip", { player: "phaseDrawSkipped" });
      } else {
        await player2.chooseToDiscard(2, true, "he");
      }
    },
    subSkill: {
      skip: {
        mark: true,
        intro: { content: "跳过下一个摸牌阶段" }
      }
    },
    ai: {
      order: 1,
      result: {
        target(player2, target) {
          if (target.skipList.includes("phaseDraw") || target.hasSkill("pingkou")) {
            return 0;
          }
          var hs = player2.getCards("h").sort(function(a, b) {
            return b.number - a.number;
          });
          var ts = target.getCards("h").sort(function(a, b) {
            return b.number - a.number;
          });
          if (!hs.length || !ts.length) {
            return 0;
          }
          if (hs[0].number > ts[0].number - 2 && hs[0].number > 5) {
            return -1;
          }
          return 0;
        }
      }
    }
  },
  //韩悝
  scsxiaolu: {
    audio: 1,
    enable: "phaseUse",
    usable: 1,
    async content(event, trigger, player2) {
      await player2.draw(2);
      var num = player2.countCards("h");
      if (!num) {
        return;
      }
      if (num < 2) {
        await player2.chooseToDiscard(2, true, "h");
        return;
      }
      const result = await player2.chooseControl().set("choiceList", ["将两张手牌交给一名其他角色", "弃置两张手牌"]).set("ai", function() {
        if (game.hasPlayer(function(current) {
          return current != player2 && get.attitude(player2, current) > 0;
        })) {
          return 0;
        }
        return 1;
      }).forResult();
      if (result.index == 0) {
        const result2 = await player2.chooseCardTarget({
          position: "h",
          filterCard: true,
          selectCard: 2,
          filterTarget(card2, player3, target2) {
            return player3 != target2;
          },
          ai1(card2) {
            return get.unuseful(card2);
          },
          ai2(target2) {
            var att = get.attitude(_status.event.player, target2);
            if (target2.hasSkillTag("nogain")) {
              att /= 10;
            }
            if (target2.hasJudge("lebu")) {
              att /= 5;
            }
            return att;
          },
          prompt: "选择两张手牌，交给一名其他角色",
          forced: true
        }).forResult();
        if (result2.bool) {
          var target = result2.targets[0];
          await player2.give(result2.cards, target);
        }
      } else {
        await player2.chooseToDiscard(2, true, "h");
      }
    },
    ai: {
      order: 9,
      result: { player: 2 }
    }
  },
  //栗嵩
  scskuiji: {
    audio: 1,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card2, player2, target) {
      return target != player2 && target.countCards("h") > 0;
    },
    async content(event, trigger, player2) {
      const target = event.target;
      var list1 = [];
      var list2 = [];
      var chooseButton;
      if (player2.countCards("h") > 0) {
        chooseButton = player2.chooseButton(4, ["你的手牌", player2.getCards("h"), get.translation(target.name) + "的手牌", target.getCards("h")]);
      } else {
        chooseButton = player2.chooseButton(4, [get.translation(target.name) + "的手牌", target.getCards("h")]);
      }
      chooseButton.set("target", target);
      chooseButton.set("ai", function(button) {
        _status.event.player;
        var target2 = _status.event.target;
        for (var i2 = 0; i2 < ui.selected.buttons.length; i2++) {
          var card2 = ui.selected.buttons[i2].link;
          if (target2.getCards("h").includes(card2)) ;
        }
        var card2 = button.link;
        var owner = get.owner(card2);
        var val = get.value(card2) || 1;
        if (owner == target2) {
          return 2 * val;
        }
        return 7 - val;
      });
      chooseButton.set("filterButton", function(button) {
        if (get.owner(button.link) && !lib.filter.canBeDiscarded(button.link, get.owner(button.link), get.player())) {
          return false;
        }
        for (var i2 = 0; i2 < ui.selected.buttons.length; i2++) {
          if (get.suit(button.link) == get.suit(ui.selected.buttons[i2].link)) {
            return false;
          }
        }
        return true;
      });
      const result = await chooseButton.forResult();
      if (result.bool) {
        var list = result.links;
        for (var i = 0; i < list.length; i++) {
          if (get.owner(list[i]) == player2) {
            list1.push(list[i]);
          } else {
            list2.push(list[i]);
          }
        }
        if (list1.length && list2.length) {
          await game.loseAsync({
            lose_list: [
              [player2, list1],
              [target, list2]
            ],
            discarder: player2
          }).setContent("discardMultiple");
        } else if (list2.length) {
          await target.discard(list2);
        } else {
          await player2.discard(list1);
        }
      }
    },
    ai: {
      order: 6,
      result: {
        target: -1
      }
    }
  },
  //段珪
  scschihe: {
    audio: 1,
    trigger: { player: "useCardToPlayered" },
    filter(event, player2) {
      return event.targets.length == 1 && event.card.name == "sha";
    },
    prompt2(event, player2) {
      var str = "亮出牌堆顶的两张牌并增加伤害；且";
      str += "令" + get.translation(event.target) + "不能使用";
      str += "这两张牌所包含的花色";
      str += "的牌响应" + get.translation(event.card);
      return str;
    },
    logTarget: "target",
    locked: false,
    check(event, player2) {
      var target = event.target;
      if (get.attitude(player2, target) > 0) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player2) {
      var num = 2;
      var evt = trigger.getParent();
      var suit = get.suit(trigger.card);
      var suits = [];
      {
        if (typeof evt.baseDamage != "number") {
          evt.baseDamage = 1;
        }
        var cards2 = get.cards(num);
        await player2.showCards(cards2.slice(0), get.translation(player2) + "发动了【叱吓】");
        while (cards2.length > 0) {
          var card2 = cards2.pop();
          var suitx = get.suit(card2, false);
          suits.add(suitx);
          if (suit == suitx) {
            evt.baseDamage++;
          }
        }
        game.updateRoundNumber();
      }
      evt._scschihe_player = player2;
      var target = trigger.target;
      target.addTempSkill("scschihe_block");
      if (!target.storage.scschihe_block) {
        target.storage.scschihe_block = [];
      }
      target.storage.scschihe_block.push([evt.card, suits]);
      lib.skill.scschihe.updateBlocker(target);
    },
    updateBlocker(player2) {
      var list = [], storage = player2.storage.scschihe_block;
      if (storage && storage.length) {
        for (var i of storage) {
          list.addArray(i[1]);
        }
      }
      player2.storage.scschihe_blocker = list;
    },
    ai: {
      threaten: 2.5
    },
    subSkill: {
      block: {
        mod: {
          cardEnabled(card2, player2) {
            if (!player2.storage.scschihe_blocker) {
              return;
            }
            var suit = get.suit(card2);
            if (suit == "none" || suit == "unsure") {
              return;
            }
            var evt = _status.event;
            if (evt.name != "chooseToUse") {
              evt = evt.getParent("chooseToUse");
            }
            if (!evt || !evt.respondTo || evt.respondTo[1].name != "sha") {
              return;
            }
            if (player2.storage.scschihe_blocker.includes(suit)) {
              return false;
            }
          }
        },
        trigger: {
          player: ["damageBefore", "damageCancelled", "damageZero"],
          target: ["shaMiss", "useCardToExcluded", "useCardToEnd"],
          global: ["useCardEnd"]
        },
        forced: true,
        firstDo: true,
        charlotte: true,
        popup: false,
        onremove(player2) {
          delete player2.storage.scschihe_block;
          delete player2.storage.scschihe_blocker;
        },
        filter(event, player2) {
          const evt = event.getParent("useCard", true, true);
          if (evt && evt.effectedCount < evt.effectCount) {
            return false;
          }
          if (!event.card || !player2.storage.scschihe_block) {
            return false;
          }
          for (var i of player2.storage.scschihe_block) {
            if (i[0] == event.card) {
              return true;
            }
          }
          return false;
        },
        async content(event, trigger, player2) {
          var storage = player2.storage.scschihe_block;
          for (var i = 0; i < storage.length; i++) {
            if (storage[i][0] == trigger.card) {
              storage.splice(i--, 1);
            }
          }
          if (!storage.length) {
            player2.removeSkill("scschihe_block");
          } else {
            lib.skill.scschihe.updateBlocker(event.target);
          }
        }
      }
    }
  },
  //郭胜
  scsniqu: {
    audio: 1,
    enable: "phaseUse",
    usable: 1,
    filterTarget: true,
    selectTarget: 1,
    async content(event, trigger, player2) {
      const target = event.target;
      await target.damage("fire");
    },
    ai: {
      expose: 0.2,
      order: 5,
      result: {
        target(player2, target) {
          return get.damageEffect(target, player2, target, "fire") / 10;
        }
      }
    }
  },
  //高望
  scsanruo: {
    audio: 1,
    enable: ["chooseToUse", "chooseToRespond"],
    prompt: "将一张♥牌当做桃，♦牌当做火杀，♣牌当做闪，♠牌当做无懈可击使用或打出",
    viewAs(cards2, player2) {
      var name = false;
      var nature = null;
      switch (get.suit(cards2[0], player2)) {
        case "club":
          name = "shan";
          break;
        case "diamond":
          name = "sha";
          nature = "fire";
          break;
        case "spade":
          name = "wuxie";
          break;
        case "heart":
          name = "tao";
          break;
      }
      if (name) {
        return { name, nature };
      }
      return null;
    },
    check(card2) {
      var player2 = _status.event.player;
      if (_status.event.type == "phase") {
        var max = 0;
        var name2;
        var list = ["sha", "tao"];
        var map = { sha: "diamond", tao: "heart" };
        for (var i = 0; i < list.length; i++) {
          var name = list[i];
          if (player2.countCards("hes", function(card3) {
            return (name != "sha" || get.value(card3) < 5) && get.suit(card3, player2) == map[name];
          }) > 0 && player2.getUseValue({ name, nature: name == "sha" ? "fire" : null }) > 0) {
            var temp = get.order({ name, nature: name == "sha" ? "fire" : null });
            if (temp > max) {
              max = temp;
              name2 = map[name];
            }
          }
        }
        if (name2 == get.suit(card2, player2)) {
          return name2 == "diamond" ? 5 - get.value(card2) : 20 - get.value(card2);
        }
        return 0;
      }
      return 1;
    },
    position: "hes",
    filterCard(card2, player2, event) {
      event = event || _status.event;
      var filter = event._backup.filterCard;
      var name = get.suit(card2, player2);
      if (name == "club" && filter({ name: "shan", cards: [card2] }, player2, event)) {
        return true;
      }
      if (name == "diamond" && filter({ name: "sha", cards: [card2], nature: "fire" }, player2, event)) {
        return true;
      }
      if (name == "spade" && filter({ name: "wuxie", cards: [card2] }, player2, event)) {
        return true;
      }
      if (name == "heart" && filter({ name: "tao", cards: [card2] }, player2, event)) {
        return true;
      }
      return false;
    },
    filter(event, player2) {
      var filter = event.filterCard;
      if (filter(get.autoViewAs({ name: "sha", nature: "fire" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "diamond" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "shan" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "club" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "tao" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "heart" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "wuxie" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "spade" })) {
        return true;
      }
      return false;
    },
    async precontent(event, trigger, player2) {
      player2.addTempSkill("scsanruo_effect");
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2, tag) {
        var name;
        switch (tag) {
          case "respondSha":
            name = "diamond";
            break;
          case "respondShan":
            name = "club";
            break;
          case "save":
            name = "heart";
            break;
        }
        if (!player2.countCards("hes", { suit: name })) {
          return false;
        }
      },
      order(item, player2) {
        if (player2 && _status.event.type == "phase") {
          var max = 0;
          var list = ["sha", "tao"];
          var map = { sha: "diamond", tao: "heart" };
          for (var i = 0; i < list.length; i++) {
            var name = list[i];
            if (player2.countCards("hes", function(card2) {
              return (name != "sha" || get.value(card2) < 5) && get.suit(card2, player2) == map[name];
            }) > 0 && player2.getUseValue({
              name,
              nature: name == "sha" ? "fire" : null
            }) > 0) {
              var temp = get.order({
                name,
                nature: name == "sha" ? "fire" : null
              });
              if (temp > max) {
                max = temp;
              }
            }
          }
          max /= 1.1;
          return max;
        }
        return 2;
      }
    },
    hiddenCard(player2, name) {
      if (name == "wuxie" && _status.connectMode && player2.countCards("hes") > 0) {
        return true;
      }
      if (name == "wuxie") {
        return player2.countCards("hes", { suit: "spade" }) > 0;
      }
      if (name == "tao") {
        return player2.countCards("hes", { suit: "heart" }) > 0;
      }
    },
    subSkill: {
      effect: {
        audio: "scsanruo",
        trigger: {
          player: ["useCard", "respond"]
        },
        filter(event, player2) {
          return event.skill == "scsanruo";
        },
        direct: true,
        forced: true,
        charlotte: true,
        async content(event, trigger, player2) {
          var name = trigger.card.name;
          var next = game.createEvent("scsanruo_" + name);
          next.player = player2;
          await next.setContent(lib.skill.scsanruo_effect[name == "shan" ? "sha" : name] || function() {
          });
        },
        async sha(event, trigger, player2) {
          var trigger = event.getParent().getTrigger();
          var target;
          if (trigger.name == "useCard") {
            target = lib.skill.chongzhen.logTarget(trigger, player2);
          } else {
            target = trigger.source;
          }
          if (!target || !target.countGainableCards(player2, "he")) {
            return;
          }
          const result = await player2.chooseBool(get.prompt("scsanruo_effect", target), "获得该角色的一张牌").set("ai", () => {
            return _status.event.goon;
          }).set("goon", get.attitude(player2, target) < 1).forResult();
          if (result.bool) {
            player2.logSkill("scsanruo_effect", target);
            await player2.gainPlayerCard(target, "he", true);
          }
        },
        async tao(event, trigger, player2) {
          const result = await player2.chooseTarget(get.prompt("scsanruo"), "获得一名其他角色的一张牌", (card2, player3, target2) => {
            return target2.countGainableCards(player3, "he") && target2 != player3;
          }).set("ai", (target2) => {
            return 1 - get.attitude(_status.event.player, target2);
          }).forResult();
          if (result.bool) {
            var target = result.targets[0];
            player2.logSkill("scsanruo_effect", target);
            await player2.gainPlayerCard(target, "he", true);
          }
        },
        async wuxie(event, trigger, player2) {
          var trigger = event.getParent().getTrigger();
          if (!trigger.respondTo) {
            return;
          }
          var target = trigger.respondTo[0];
          if (!target || !target.countGainableCards(player2, player2 == target ? "e" : "he")) {
            return;
          }
          const result = await player2.chooseBool(get.prompt("scsanruo_effect", target), "获得该角色的一张牌").set("ai", () => {
            return _status.event.goon;
          }).set("goon", get.attitude(player2, target) < 1).forResult();
          if (result.bool) {
            player2.logSkill("scsanruo_effect", target);
            await player2.gainPlayerCard(target, player2 == target ? "e" : "he", true);
          }
        }
      }
    }
  },
  scsmiaoyu: {
    audio: "scsanruo",
    enable: ["chooseToUse", "chooseToRespond"],
    prompt: "将至多两张♦牌当作火【杀】，♥牌当作【桃】，♣牌当作【闪】，♠牌当作【无懈可击】使用或打出",
    viewAs(cards2, player2) {
      var name = false;
      var nature = null;
      switch (get.suit(cards2[0], player2)) {
        case "club":
          name = "shan";
          break;
        case "diamond":
          name = "sha";
          nature = "fire";
          break;
        case "spade":
          name = "wuxie";
          break;
        case "heart":
          name = "tao";
          break;
      }
      if (name) {
        return { name, nature };
      }
      return null;
    },
    check(card2) {
      if (ui.selected.cards.length) {
        return 0;
      }
      var player2 = _status.event.player;
      if (_status.event.type == "phase") {
        var max = 0;
        var name2;
        var list = ["sha", "tao"];
        var map = { sha: "diamond", tao: "heart" };
        for (var i = 0; i < list.length; i++) {
          var name = list[i];
          if (player2.countCards("hes", function(card3) {
            return (name != "sha" || get.value(card3) < 5) && get.suit(card3, player2) == map[name];
          }) > 0 && player2.getUseValue({ name, nature: name == "sha" ? "fire" : null }) > 0) {
            var temp = get.order({ name, nature: name == "sha" ? "fire" : null });
            if (temp > max) {
              max = temp;
              name2 = map[name];
            }
          }
        }
        if (name2 == get.suit(card2, player2)) {
          return name2 == "diamond" ? 5 - get.value(card2) : 20 - get.value(card2);
        }
        return 0;
      }
      return 1;
    },
    selectCard: [1, 2],
    complexCard: true,
    position: "hes",
    filterCard(card2, player2, event) {
      if (ui.selected.cards.length) {
        return get.suit(card2, player2) == get.suit(ui.selected.cards[0], player2);
      }
      event = event || _status.event;
      var filter = event._backup.filterCard;
      var name = get.suit(card2, player2);
      if (name == "club" && filter({ name: "shan", cards: [card2] }, player2, event)) {
        return true;
      }
      if (name == "diamond" && filter({ name: "sha", cards: [card2], nature: "fire" }, player2, event)) {
        return true;
      }
      if (name == "spade" && filter({ name: "wuxie", cards: [card2] }, player2, event)) {
        return true;
      }
      if (name == "heart" && filter({ name: "tao", cards: [card2] }, player2, event)) {
        return true;
      }
      return false;
    },
    filter(event, player2) {
      var filter = event.filterCard;
      if (filter(get.autoViewAs({ name: "sha", nature: "fire" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "diamond" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "shan" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "club" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "tao" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "heart" })) {
        return true;
      }
      if (filter(get.autoViewAs({ name: "wuxie" }, "unsure"), player2, event) && player2.countCards("hes", { suit: "spade" })) {
        return true;
      }
      return false;
    },
    async precontent(event, trigger, player2) {
      player2.addTempSkill("scsmiaoyu_num");
      player2.addTempSkill("scsmiaoyu_discard");
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2, tag) {
        var name;
        switch (tag) {
          case "respondSha":
            name = "diamond";
            break;
          case "respondShan":
            name = "club";
            break;
          case "save":
            name = "heart";
            break;
        }
        if (!player2.countCards("hes", { suit: name })) {
          return false;
        }
      },
      order(item, player2) {
        if (player2 && _status.event.type == "phase") {
          var max = 0;
          var list = ["sha", "tao"];
          var map = { sha: "diamond", tao: "heart" };
          for (var i = 0; i < list.length; i++) {
            var name = list[i];
            if (player2.countCards("hes", function(card2) {
              return (name != "sha" || get.value(card2) < 5) && get.suit(card2, player2) == map[name];
            }) > 0 && player2.getUseValue({
              name,
              nature: name == "sha" ? "fire" : null
            }) > 0) {
              var temp = get.order({
                name,
                nature: name == "sha" ? "fire" : null
              });
              if (temp > max) {
                max = temp;
              }
            }
          }
          max /= 1.1;
          return max;
        }
        return 2;
      }
    },
    hiddenCard(player2, name) {
      if (name == "wuxie" && _status.connectMode && player2.countCards("hs") > 0) {
        return true;
      }
      if (name == "wuxie") {
        return player2.countCards("hes", { suit: "spade" }) > 0;
      }
      if (name == "tao") {
        return player2.countCards("hes", { suit: "heart" }) > 0;
      }
    },
    subSkill: {
      num: {
        charlotte: true,
        trigger: { player: "useCard" },
        filter(event) {
          return ["sha", "tao"].includes(event.card.name) && event.skill == "scsmiaoyu" && event.cards && event.cards.length == 2;
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          trigger.baseDamage++;
        }
      },
      discard: {
        charlotte: true,
        trigger: { player: ["useCardAfter", "respondAfter"] },
        autodelay(event) {
          return event.name == "respond" ? 0.5 : false;
        },
        filter(event, player2) {
          return ["shan", "wuxie"].includes(event.card.name) && event.skill == "scsmiaoyu" && event.cards && event.cards.length == 2 && _status.currentPhase && _status.currentPhase != player2 && _status.currentPhase.countDiscardableCards(player2, "he");
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          player2.line(_status.currentPhase, "green");
          await player2.discardPlayerCard(_status.currentPhase, "he", true);
        }
      }
    }
  },
  //牵招
  mbshihe: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.hasPlayer((current) => player2.canCompare(current));
    },
    filterTarget(card2, player2, target) {
      return player2.canCompare(target);
    },
    async content(event, trigger, player2) {
      const target = event.target;
      const result = await player2.chooseToCompare(target).forResult();
      if (result.bool) {
        target.addTempSkill("mbshihe_prevent", { player: "phaseAfter" });
        target.markAuto("mbshihe_prevent", [player2]);
      } else {
        var cards2 = player2.getCards("he", (card2) => {
          return lib.filter.cardDiscardable(card2, player2, "mbshihe");
        });
        if (cards2.length > 0) {
          await player2.discard(cards2.randomGet());
        }
      }
    },
    ai: {
      order: 6,
      result: {
        player(player2, target) {
          if ((get.realAttitude || get.attitude)(target, player2) >= 0 || get.damageEffect(player2, target, player2) >= 0) {
            return 0;
          }
          var card2 = player2.getCards("h").sort(function(a, b) {
            return get.number(b) - get.number(a);
          })[0];
          return get.number(card2) >= 10 || get.number(card2) >= 7 && target.countCards("h") <= 2 ? 1 : -1;
        }
      }
    },
    subSkill: {
      prevent: {
        trigger: { source: "damageBegin2" },
        filter(event, player2) {
          if (get.mode() == "identity") {
            return player2.getStorage("mbshihe_prevent").includes(event.player);
          }
          return player2.getStorage("mbshihe_prevent").some((target) => event.player.isFriendOf(target));
        },
        onremove: true,
        forced: true,
        charlotte: true,
        async content(event, trigger, player2) {
          trigger.cancel();
        },
        mark: true,
        marktext: "吓",
        intro: {
          content(storage, player2) {
            var targets2 = storage.filter((i) => i.isIn());
            return "被" + get.translation(targets2) + "吓到了，对他" + (targets2.length > 1 ? "们" : "") + (get.mode() != "identity" ? "和他的友方角色" : "") + "打不出伤害";
          }
        },
        ai: {
          effect: {
            player(card2, player2, target, current) {
              if (get.tag(card2, "damage")) {
                var bool = false;
                if (get.mode() == "identity" && player2.getStorage("mbshihe_prevent").includes(target)) {
                  bool = true;
                }
                if (get.mode() != "identity" && player2.getStorage("mbshihe_prevent").some((targetx) => target.isFriendOf(targetx))) {
                  bool = true;
                }
                if (bool) {
                  return "zeroplayertarget";
                }
              }
            }
          }
        }
      }
    }
  },
  mbzhenfu: {
    audio: 2,
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player2) {
      return player2.hasHistory("lose", (evt) => {
        return evt.type == "discard";
      });
    },
    direct: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt("mbzhenfu"), "令一名其他角色获得1点护甲", (card2, player3, target2) => {
        return target2 != player3 && target2.hujia < 5;
      }).set("ai", (target2) => {
        return Math.max(0, get.threaten(target2)) * get.attitude(_status.event.player, target2);
      }).forResult();
      if (result.bool) {
        var target = result.targets[0];
        player2.logSkill("mbzhenfu", target);
        await target.changeHujia(1, null, true);
      }
    },
    ai: {
      expose: 0.2
    }
  },
  // 界曹休
  xinqingxi: {
    audio: 2,
    usable: 1,
    trigger: { source: "damageBegin1" },
    check(event, player2) {
      return get.attitude(player2, event.player) < 0;
    },
    filter(event, player2) {
      return event.player != player2;
    },
    async content(event, trigger, player2) {
      var num = Math.max(1, 4 - get.distance(player2, trigger.player));
      if (trigger.player.countCards("h") < num) {
        trigger.num++;
        return;
      }
      const result = await trigger.player.chooseToDiscard(num, "弃置" + get.cnNumber(num) + "张手牌，或令" + get.translation(player2) + "对你造成的此伤害+1").set("ai", function(card2) {
        var player3 = _status.event.player;
        if (player3.hp == 1) {
          if (get.type(card2) == "basic") {
            return 8 - get.value(card2);
          } else {
            return 10 - get.value(card2);
          }
        } else {
          if (num > 2) {
            return 0;
          }
          return 8 - get.value(card2);
        }
      }).forResult();
      if (!result.bool) {
        trigger.num++;
      }
    }
  },
  // 界朱桓
  xinpingkou: {
    audio: 2,
    trigger: { player: "phaseEnd" },
    direct: true,
    filter(event, player2) {
      return player2.getHistory("skipped").length > 0;
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget([1, player2.getHistory("skipped").length], get.prompt2("xinpingkou"), function(card3, player3, target) {
        return target != player3;
      }).set("ai", function(target) {
        var player3 = _status.event.player;
        return get.damageEffect(target, player3, player3);
      }).forResult();
      if (!result.bool) {
        return;
      }
      player2.logSkill("xinpingkou", result.targets);
      const targets2 = result.targets.slice(0).sortBySeat();
      for (const target of targets2) {
        await target.damage();
      }
      var card2 = get.cardPile2((card3) => get.type(card3, null, false) == "equip");
      if (card2) {
        await player2.gain(card2, "gain2");
      }
    },
    ai: {
      effect: {
        target(card2) {
          if (card2.name == "lebu" || card2.name == "bingliang") {
            return 0.5;
          }
        }
      },
      combo: "fenli"
    }
  },
  // 彭羕
  spdaming: {
    audio: 3,
    trigger: { global: "phaseBefore", player: "enterGame" },
    global: "spdaming_give",
    filter(event, player2) {
      return event.name != "phase" || game.phaseNumber == 0;
    },
    change(player2, num) {
      if (!player2.storage.spdaming) {
        player2.storage.spdaming = 0;
      }
      if (!num) {
        return;
      }
      player2.storage.spdaming += num;
      player2.markSkill("spdaming");
      game.log(player2, (num > 0 ? "获得了" : "减少了") + get.cnNumber(Math.abs(num)) + "点“达命”值");
    },
    forced: true,
    locked: false,
    logAudio: () => 2,
    async content(event, trigger, player2) {
      lib.skill.spdaming.change(player2, 3);
    },
    intro: {
      name: "达命值",
      markcount(storage, player2) {
        return (storage || 0).toString();
      },
      content: "当前有#点“达命”值"
    },
    subSkill: {
      used: { charlotte: true },
      give: {
        audio: ["spdaming", 2],
        enable: "phaseUse",
        forceaudio: true,
        nopop: true,
        filter(event, player2) {
          if (!player2.countCards("he")) {
            return false;
          }
          return game.hasPlayer((current) => {
            return current != player2 && current.hasSkill("spdaming") && !current.hasSkill("spdaming_used");
          });
        },
        selectCard: 1,
        filterCard: true,
        filterTarget(card2, player2, target) {
          return target.hasSkill("spdaming") && !target.hasSkill("spdaming_used");
        },
        selectTarget() {
          const player2 = get.player();
          const targets2 = game.filterPlayer((current) => {
            return current != player2 && current.hasSkill("spdaming") && !current.hasSkill("spdaming_used");
          });
          return targets2.length > 1 ? 1 : -1;
        },
        complexSelect: true,
        prompt() {
          const player2 = get.player();
          const targets2 = game.filterPlayer((current) => {
            return current != player2 && current.hasSkill("spdaming") && !current.hasSkill("spdaming_used");
          });
          return "将一张牌交给" + get.translation(targets2) + (targets2.length > 1 ? "中的一人" : "");
        },
        position: "he",
        discard: false,
        lose: false,
        delay: false,
        check(card2) {
          const player2 = get.player();
          if (game.hasPlayer((current) => {
            return lib.skill.spdaming_give.filterTarget(null, player2, current) && get.attitude(player2, current) > 0;
          })) {
            return 6 + Math.random() - get.value(card2) / 15;
          }
          return 0;
        },
        async content(event, trigger, player2) {
          const { cards: cards2, target } = event;
          await player2.give(cards2, target);
          target.addTempSkill("spdaming_used", "phaseUseAfter");
          if (!game.hasPlayer((current) => current != player2 && current != target && get.owner(cards2[0]) == target)) {
            await target.give(cards2, player2);
            return;
          }
          const type = get.type(cards2[0], "trick", target);
          const str = get.translation(type), user = get.translation(player2);
          const result = await target.chooseTarget(
            "达命：选择另一名其他角色",
            `若该角色有${str}牌，其将一张该类型的牌交给${user}，你获得1点“达命”值；否则你将${get.translation(cards2)}交给${user}`,
            (card2, player3, target2) => {
              return target2 != player3 && target2 != get.event().getParent().player;
            },
            true
          ).set("ai", (target2) => 1 - get.attitude(get.player(), target2)).forResult();
          if (!result?.bool || !result?.targets?.length) {
            return;
          }
          const [targetx] = result.targets;
          target.line(targetx);
          if (targetx.countCards("he", (card2) => get.type2(card2) == type)) {
            await targetx.chooseToGive(player2, `交给${get.translation(player2)}一张${get.translation(type)}牌`, "he", true, (card2) => {
              return get.type2(card2) == get.event().cardtype;
            }).set("ai", (card2) => 10 - get.value(card2)).set("cardtype", type);
            targetx.line(player2);
            lib.skill.spdaming.change(target, 1);
            await game.delayx();
          } else if (get.owner(cards2[0]) == target) {
            await target.give(cards2, player2);
            game.broadcastAll(() => {
              if (lib.config.background_speak) {
                game.playAudio("skill", "spdaming3");
              }
            });
          }
        },
        ai: {
          expose: 0.2,
          order: 10,
          result: { target: 1 }
        }
      }
    }
  },
  spxiaoni: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    locked: false,
    filter(event, player2) {
      return player2.hasMark("spdaming") && player2.countCards("hes") && get.inpileVCardList((info) => info[0] == "trick" && get.tag({ name: info[2] }, "damage") || info[2] == "sha");
    },
    chooseButton: {
      dialog(event, player2) {
        const list = get.inpileVCardList((info) => info[0] == "trick" && get.tag({ name: info[2] }, "damage") || info[2] == "sha");
        return ui.create.dialog("嚣逆", [list, "vcard"]);
      },
      filter(button, player2) {
        return lib.filter.filterCard({ name: button.link[2] }, player2, get.event().getParent());
      },
      check(button) {
        const player2 = get.player();
        if (player2.countCards("hs", button.link[2]) > 0) {
          return 0;
        }
        const effect = player2.getUseValue({ name: button.link[2], nature: button.link[3] });
        if (effect > 0) {
          return effect;
        }
        return 0;
      },
      backup(links, player2) {
        return {
          filterCard: true,
          audio: "spxiaoni",
          selectCard: 1,
          popname: true,
          check(card2) {
            return 6 - get.value(card2);
          },
          position: "hes",
          viewAs: { name: links[0][2], nature: links[0][3] },
          onuse(result, player3) {
            lib.skill.spdaming.change(player3, -result.targets.length);
          }
        };
      },
      prompt(links, player2) {
        return "将一张牌当" + (get.translation(links[0][3]) || "") + "【" + get.translation(links[0][2]) + "】使用";
      }
    },
    mod: {
      maxHandcardBase(player2, num) {
        return Math.min(Math.max(0, player2.countMark("spdaming")), player2.hp);
      },
      cardUsable(card2, player2, num) {
        if (card2.name == "sha") {
          const numx = Math.max(player2.countMark("spdaming"), player2.hp) - num;
          return num + numx;
        }
      }
    },
    ai: {
      order: 4,
      result: { player: 1 },
      threaten: 1.4,
      combo: "spdaming"
    },
    subSkill: { backup: {} }
  },
  // 灭霸
  zhujian: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card2, player2, target) {
      return target.countCards("e") > 0;
    },
    selectTarget: [2, Infinity],
    multiline: true,
    multitarget: true,
    filter(event, player2) {
      return game.countPlayer((current) => current.countCards("e") > 0) >= 2;
    },
    async content(event, trigger, player2) {
      const targets2 = event.targets;
      await game.asyncDraw(targets2);
    },
    ai: {
      order: 8,
      result: { target: 1 }
    }
  },
  duansuo: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card2, player2, target) {
      return target.isLinked();
    },
    selectTarget: [1, Infinity],
    multiline: true,
    multitarget: true,
    filter(event, player2) {
      return game.countPlayer((current) => current.isLinked());
    },
    async content(event, trigger, player2) {
      var targets2 = event.targets.sortBySeat();
      for (var i of targets2) {
        await i.link(false);
      }
      for (var i of targets2) {
        await i.damage("fire");
      }
    },
    ai: {
      order: 2,
      result: { target: -1 }
    }
  },
  // 界朱治
  sbanguo: {
    audio: 3,
    trigger: { global: "phaseBefore", player: "enterGame" },
    group: ["sbanguo_move", "sbanguo_damage", "sbanguo_dying"],
    logAudio: () => 2,
    filter(event, player2) {
      return game.hasPlayer((current) => current != player2) && (event.name != "phase" || game.phaseNumber == 0);
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget("安国：令一名其他角色获得“安国”标记", lib.filter.notMe, true).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      target.addMark("sbanguo_mark", 1, false);
      target.addAdditionalSkill("sbanguo_" + player2.playerid, "sbanguo_mark");
      target.addMark("sbanguo_marked", 1, false);
    },
    subSkill: {
      mark: {
        onremove: true,
        marktext: "安",
        charlotte: true,
        intro: {
          name: "安国",
          name2: "安国",
          content: "已拥有“安国”标记"
        },
        mod: {
          maxHandcardBase(player2, num) {
            return player2.maxHp;
          }
        }
      },
      move: {
        audio: ["sbanguo1.mp3", "sbanguo2.mp3"],
        trigger: { player: "phaseUseBegin" },
        filter(event, player2) {
          return game.hasPlayer((current) => current.hasSkill("sbanguo_mark")) && game.hasPlayer((current) => !current.hasMark("sbanguo_marked") && current != player2);
        },
        async cost(event, trigger, player2) {
          const targets2 = game.filterPlayer((current) => current.hasSkill("sbanguo_mark"));
          const prompt2 = targets2.length == 1 ? "将" + get.translation(targets2[0]) + "的“安国”交给一名未获得过“安国”的其他角色" : "选择一名有“安国”的角色，将该标记交给一名未获得过“安国”的其他角色";
          event.result = await player2.chooseTarget(get.prompt("sbanguo"), prompt2, targets2.length == 1 ? 1 : 2, (card2, player3, target) => {
            if (ui.selected.targets.length == 0 && _status.event.targets.length > 1) {
              return target.hasSkill("sbanguo_mark");
            }
            return !target.hasMark("sbanguo_marked") && target != player3;
          }).set("ai", (target) => {
            var player3 = _status.event.player;
            if (ui.selected.targets.length == 0 && _status.event.targets.length > 1) {
              return -get.attitude(player3, target);
            }
            return get.attitude(player3, _status.event.targets[0]) < get.attitude(player3, target);
          }).set("targets", targets2).set("line", false).forResult();
        },
        async content(event, trigger, player2) {
          const { targets: targets2 } = event;
          let target1, target2;
          if (targets2.length == 1) {
            target1 = game.filterPlayer((current) => current.hasSkill("sbanguo_mark"))[0];
            target2 = targets2[0];
          } else {
            target1 = targets2[0];
            target2 = targets2[1];
          }
          player2.line2([target1, target2], "green");
          const map = target1.additionalSkills;
          for (const key in map) {
            if (key.indexOf("sbanguo_") != 0) {
              continue;
            }
            const id = parseInt(key.slice(8));
            target1.removeAdditionalSkill("sbanguo_" + id);
            target2.addMark("sbanguo_mark", 1, false);
            target2.addAdditionalSkill("sbanguo_" + id, "sbanguo_mark");
            target2.addMark("sbanguo_marked", 1, false);
          }
        }
      },
      damage: {
        audio: ["sbanguo1.mp3", "sbanguo2.mp3"],
        forced: true,
        locked: false,
        trigger: { player: "damageBegin4" },
        filter(event, player2) {
          if (!game.hasPlayer((current) => current.hasSkill("sbanguo_mark"))) {
            return false;
          }
          if (event.source && event.source.isIn() && event.source.hasSkill("sbanguo_mark")) {
            return false;
          }
          return event.num >= player2.hp;
        },
        async content(event, trigger, player2) {
          trigger.cancel();
        },
        ai: {
          nofire: true,
          nothunder: true,
          nodamage: true,
          effect: {
            target(card2, player2, target, current) {
              if (!game.hasPlayer((current2) => current2.hasSkill("sbanguo_mark"))) {
                return;
              }
              if (player2.hasSkill("sbanguo_mark")) {
                return;
              }
              if (get.tag(card2, "damage")) {
                if (target.hp <= 1) {
                  return [0, 0];
                }
                return 0.5;
              }
            }
          }
        }
      },
      dying: {
        audio: "sbanguo3.mp3",
        forced: true,
        locked: false,
        trigger: { global: "dying" },
        filter(event, player2) {
          var skills2 = event.player.additionalSkills["sbanguo_" + player2.playerid];
          return skills2 && skills2.length;
        },
        logTarget: "player",
        async content(event, trigger, player2) {
          const target = trigger.player;
          target.removeAdditionalSkill("sbanguo_" + player2.playerid);
          await target.recoverTo(1);
          const hp = player2.hp - 1, maxhp = player2.maxHp - 1;
          let result;
          if (hp > 0 && maxhp > 0) {
            result = await player2.chooseControl().set("prompt", "安国：请选择一项").set("choiceList", ["失去" + hp + "点体力，令" + get.translation(target) + "获得1点护甲", "减" + maxhp + "点体力上限，令" + get.translation(target) + "获得1点护甲"]).set("ai", () => "选项一").forResult();
          } else if (hp > 0) {
            result = { control: "选项一" };
          } else if (maxhp > 0) {
            result = { control: "选项二" };
          } else {
            return;
          }
          if (result?.control == "选项一") {
            var num = player2.hp - 1;
            if (num > 0) {
              await player2.loseHp(num);
            }
          } else if (result?.control == "选项二") {
            var num = player2.maxHp - 1;
            if (num > 0) {
              await player2.loseMaxHp(num);
            }
          }
          await target.changeHujia(1, null, true);
        }
      }
    }
  },
  // 界吴懿
  sbbenxi: {
    audio: 3,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return player2.countDiscardableCards(player2, "he") > 0;
    },
    logAudio: () => 1,
    async cost(event, trigger, player2) {
      event.result = await player2.chooseToDiscard(get.prompt2(event.skill), [1, Infinity], "he", "allowChooseAll", "chooseonly").set("ai", (card2) => {
        var player3 = _status.event.player;
        if (ui.selected.cards.length < _status.event.num) {
          return 100 - (get.useful(card2, player3) + player3.getUseValue(card2) / 3);
        }
        return 0;
      }).set(
        "num",
        (function() {
          var count = 0;
          var list = [], list2 = [];
          var targets2 = game.filterPlayer((current) => get.distance(player2, current) >= 1);
          var cards2 = player2.getCards("hs", (card3) => {
            return player2.hasUseTarget(card3, false) && ["basic", "trick"].includes(get.type(card3, false, player2)) && get.info(card3).allowMultiple != false;
          });
          var cards22 = player2.getCards("he").filter((i2) => lib.filter.cardDiscardable(i2, player2, "sbbenxi")).sort((a, b) => {
            return get.useful(a, player2) + player2.getUseValue(a) / 3 - (get.useful(b, player2) + player2.getUseValue(b) / 3);
          });
          for (var i = 0; i < cards22.length; i++) {
            count = 0;
            list = [];
            for (var card2 of cards2) {
              var num = i + 1;
              if (cards22.slice(0, num).includes(card2)) {
                continue;
              }
              if (get.tag(card2, "damage") && i > 0) {
                count += get.effect(player2, { name: "draw" }, player2);
              }
              var targets22 = targets2.filter((current) => {
                return player2.canUse(card2, current, false) && get.distance(player2, current) <= num && get.effect(current, card2, player2, player2) > 0;
              });
              targets22 = targets22.map((target) => get.effect(target, card2, player2, player2)).sort((a, b) => b - a);
              targets22.slice(0, num).forEach((eff) => count += eff);
              list.push(count - 1.2 * get.value(cards22[i]));
            }
            var val = list.sort((a, b) => b - a)[0];
            if (!isNaN(val)) {
              list2.push([val, i]);
            }
          }
          list2 = list2.filter((i2) => i2[0] > 0);
          if (!list2.length) {
            return 0;
          }
          return list2.sort((a, b) => b[0] - a[0])[0][1];
        })()
      ).forResult();
    },
    async content(event, trigger, player2) {
      await player2.discard(event.cards);
      const num = event.cards.length;
      player2.addTempSkill("sbbenxi_effect", "phaseUseAfter");
      player2.addTempSkill("sbbenxi_effect2", "phaseUseAfter");
      player2.addMark("sbbenxi_effect2", num, false);
    },
    subSkill: {
      effect: {
        audio: "sbbenxi2.mp3",
        trigger: { player: "useCard2" },
        forced: true,
        charlotte: true,
        direct: true,
        onremove: true,
        filter(event, player2) {
          var type = get.type(event.card, null, false);
          return type == "basic" || type == "trick";
        },
        async content(event, trigger, player2) {
          var num = player2.countMark("sbbenxi_effect2");
          player2.removeSkill("sbbenxi_effect");
          player2.addTempSkill("sbbenxi_effect3", "phaseUseAfter");
          player2.markAuto("sbbenxi_effect3", [trigger.card]);
          var filter = function(event2, player3) {
            var card2 = event2.card, info = get.info(card2);
            if (info.allowMultiple == false) {
              return false;
            }
            if (event2.targets && !info.multitarget) {
              if (game.hasPlayer(function(current) {
                return !event2.targets.includes(current) && lib.filter.targetEnabled2(card2, player3, current) && get.distance(player3, current) == 1;
              })) {
                return true;
              }
            }
            return false;
          };
          if (!filter(trigger, player2)) {
            return;
          }
          var prompt = "为" + get.translation(trigger.card) + "增加至多" + get.cnNumber(num) + "个距离为1的目标？";
          const result = await trigger.player.chooseTarget(get.prompt("sbbenxi_effect"), prompt, [1, num], function(card2, player3, target) {
            var player3 = _status.event.player;
            return !_status.event.targets.includes(target) && lib.filter.targetEnabled2(_status.event.card, player3, target) && get.distance(player3, target) == 1;
          }).set("ai", function(target) {
            var trigger2 = _status.event.getTrigger();
            var player3 = _status.event.player;
            return get.effect(target, trigger2.card, player3, player3);
          }).set("card", trigger.card).set("targets", trigger.targets).forResult();
          if (!result.bool) {
            return;
          }
          if (!event.isMine() && !event.isOnline()) {
            await game.delayex();
          }
          player2.logSkill("sbbenxi_effect", result.targets);
          game.log(result.targets, "也成为了", trigger.card, "的目标");
          trigger.targets.addArray(result.targets);
        },
        ai: {
          effect: {
            target_use(card2, player2, target) {
              if (player2.canUse(card2, target) && get.distance(player2, target) != 1) {
                return 1.2;
              }
            }
          }
        }
      },
      effect2: {
        audio: "sbbenxi3.mp3",
        trigger: {
          global: "useCardAfter"
        },
        forced: true,
        charlotte: true,
        onremove: true,
        filter(event, player2) {
          return player2.getStorage("sbbenxi_effect3").includes(event.card) && game.hasPlayer2((current) => {
            return current.hasHistory("damage", (evt) => {
              return event.card == evt.card;
            });
          });
        },
        async content(event, trigger, player2) {
          await player2.draw(5);
        },
        mod: {
          aiOrder(player2, card2, num) {
            var evt = _status.event.getParent("phaseUse");
            if (!evt || evt.player != player2) {
              return;
            }
            if (player2.hasHistory("useCard", (evtx) => {
              return evtx.getParent("phaseUse") == evt && ["basic", "trick"].includes(get.type(evtx.card));
            })) {
              return;
            }
            if (get.tag(card2, "damage") || get.type(card2) == "equip") {
              return num + 10;
            }
          },
          globalFrom(from, to, distance) {
            return distance - from.countMark("sbbenxi_effect2");
          }
        },
        marktext: "奔",
        intro: {
          content(storage, player2) {
            var str = "于此阶段至其他角色的距离-" + storage;
            if (player2.hasSkill("sbbenxi_effect")) {
              str += "；使用下一张基本牌或普通锦囊牌选择目标后，可以增加" + get.cnNumber(storage) + "个目标";
            }
            return str;
          }
        }
      },
      effect3: {
        forced: true,
        charlotte: true,
        popup: false,
        onremove: true
      }
    }
  },
  // 杨阜
  jiebing: {
    audio: 2,
    trigger: {
      player: "damageEnd"
    },
    direct: true,
    forced: true,
    filter(event, player2) {
      return game.hasPlayer((current) => {
        return current != event.source && current != player2 && current.countGainableCards(player2, "he");
      });
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget("借兵：选择一名其他角色", get.skillInfoTranslation("jiebing", null, false), true, (card3, player3, target2) => {
        return player3 != target2 && target2 != _status.event.getTrigger().source && target2.countGainableCards(player3, "he");
      }).set(
        "ai",
        (target2) => get.effect(target2, { name: "shunshou_copy2" }, player2, player2)
        /** (target.countCards('he')>1?1.5:1)*/
      ).forResult();
      if (!result.bool) {
        return;
      }
      var target = result.targets[0];
      player2.logSkill("jiebing", target);
      if (target.ai.shown > 0) {
        player2.addExpose(0.15);
      }
      var cards2 = target.getGainableCards(player2, "he").randomGets(1);
      await player2.gain(target, cards2, "give", "bySelf");
      await player2.showCards(cards2, "借兵");
      for (var card2 of cards2) {
        if (get.type(card2) == "equip" && player2.hasUseTarget(card2) && get.owner(card2) == player2) {
          await player2.chooseUseTarget(card2, true);
        }
      }
    },
    ai: {
      maixie: true,
      maixie_hp: true,
      effect: {
        target(card2, player2, target) {
          if (get.tag(card2, "damage")) {
            if (player2.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            if (player2 != target && !player2.getFriends().length) {
              return;
            }
            if (game.hasPlayer((current) => {
              return current != player2 && get.attitude(player2, current) > 0 && current.countGainableCards(target, "he") > 0;
            })) {
              return [1, 1];
            }
          }
        }
      }
    }
  },
  hannan: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return !player2.hasSkillTag("noCompareSource");
    },
    filterTarget(card2, player2, target) {
      return player2.canCompare(target);
    },
    async content(event, trigger, player2) {
      const target = event.target;
      const result = await player2.chooseToCompare(target).forResult();
      if (!result.tie) {
        var players = [player2, target];
        if (result.bool) {
          players.reverse();
        }
        players[1].line(players[0], "thunder");
        await players[0].damage(players[1], 1);
      }
    },
    ai: {
      order: 7,
      result: {
        target(player2, target) {
          var hs = player2.getCards("h").sort(function(a, b) {
            return get.number(b) - get.number(a);
          });
          var ts = target.getCards("h").sort(function(a, b) {
            return get.number(b) - get.number(a);
          });
          if (!hs.length || !ts.length) {
            return 0;
          }
          if (get.number(hs[0]) > get.number(ts[0]) || get.number(hs[0]) - ts.length >= 9 + Math.min(2, player2.hp / 2)) {
            return get.sgnAttitude(player2, target) * get.damageEffect(target, player2, player2);
          }
          return 0;
        }
      }
    }
  },
  // 曹嵩
  yijin: {
    audio: 3,
    trigger: { player: "phaseUseBegin" },
    locked: true,
    logAudio(_1, _2, _3, _4, result) {
      return "yijin" + (["yijin_jinmi", "yijin_guxiong", "yijin_yongbi"].includes(result.cost_data) ? 2 : 1) + ".mp3";
    },
    group: ["yijin_upstart", "yijin_die"],
    filter(event, player2) {
      if (!game.hasPlayer((current) => current != player2 && !lib.skill.yijin.getKane(current).length)) {
        return false;
      }
      return lib.skill.yijin.getKane(player2).length;
    },
    getKane(player2) {
      var list = lib.skill.yijin.derivation;
      return list.filter((mark) => player2.hasMark(mark));
    },
    derivation: ["yijin_wushi", "yijin_jinmi", "yijin_guxiong", "yijin_tongshen", "yijin_yongbi", "yijin_houren"],
    getValue(player2, mark, target) {
      let dis = Math.sqrt(get.distance(player2, target, "absolute"));
      if (target.isTurnedOver()) {
        dis++;
      }
      let draw = get.effect(target, { name: "draw" }, target, target);
      switch (mark.slice(6)) {
        case "wushi":
          if (target.hasJudge("bingliang")) {
            return 12 / (1 + target.getCardUsable("sha", true));
          }
          return 5 * draw / dis + 12 / (1 + target.getCardUsable("sha", true));
        case "jinmi":
          if (target.hasJudge("lebu") && !target.hasCard({ name: "wuxie" }, "hs")) {
            return draw * target.needsToDiscard(2.2) / dis;
          }
          return get.effect(target, { name: "lebu" }, player2, target) + draw * target.needsToDiscard(2.2) / dis;
        case "guxiong":
          if (target.hasJudge("lebu")) {
            return -draw * target.needsToDiscard(3) / dis;
          }
          return get.effect(target, { name: "losehp" }, target, target) * 2 / dis - draw * target.needsToDiscard(3) / dis;
        case "tongshen":
          if (target.isMin()) {
            return 0;
          }
          var eff = -get.damageEffect(target, player2, target);
          if (eff <= 0) {
            return 0;
          }
          if (target.hp < 2) {
            return eff * dis * 2;
          }
          if (target.hp < 3 && target.countCards("he") < 3) {
            return eff * dis * 1.5;
          }
          if (target.hp > 3) {
            return eff * dis / target.hp;
          }
          return eff * dis;
        case "yongbi":
          if (target.hasJudge("bingliang") && !target.hasCard({ name: "wuxie" }, "hs")) {
            return 0;
          }
          return get.effect(target, { name: "bingliang" }, player2, target) * 2 / dis;
        case "houren":
          return Math.min(5, 2 + target.getDamagedHp()) * get.recoverEffect(target, player2, target) / dis;
      }
    },
    async cost(event, trigger, player2) {
      const { targets: targets2 } = await player2.chooseTarget("亿金：令一名其他角色获得1枚“金”", true, (card2, player3, target2) => {
        return player3 != target2 && !lib.skill.yijin.getKane(target2).length;
      }).set("ai", (target2) => {
        let player3 = _status.event.player, att = get.attitude(player3, target2), kane2 = lib.skill.yijin.getKane(player3);
        if (Math.abs(att) > 1) {
          att = Math.sign(att) * Math.sqrt(Math.abs(att));
        }
        return Math.max.apply(
          Math.max,
          kane2.map((i) => {
            return att * lib.skill.yijin.getValue(player3, i, target2);
          })
        );
      }).forResult();
      if (!targets2.length) {
        event.result = { bool: false };
        return;
      }
      const target = targets2[0];
      event.target = target;
      const kane = lib.skill.yijin.getKane(player2);
      const { control } = await player2.chooseControl(kane).set(
        "choiceList",
        kane.map((i) => {
          return '<div class="skill">【' + get.translation(lib.translate[i + "_ab"] || get.translation(i).slice(0, 2)) + "】</div><div>" + get.skillInfoTranslation(i, player2, false) + "</div>";
        })
      ).set("displayIndex", false).set("prompt", "选择令" + get.translation(target) + "获得的“金”").set("ai", () => {
        let controls = _status.event.controls, player3 = _status.event.player, target2 = _status.event.getParent().target, att = get.attitude(player3, target2);
        if (Math.abs(att) > 1) {
          att = Math.sign(att) * Math.sqrt(Math.abs(att));
        }
        let list = controls.map((i) => {
          return [i, att * lib.skill.yijin.getValue(player3, i, target2)];
        });
        list.sort((a, b) => b[1] - a[1]);
        if (list.length) {
          return list[0][0];
        }
        return controls.randomGet();
      }).forResult();
      event.result = {
        bool: true,
        targets: targets2,
        cost_data: control
      };
    },
    async content(event, trigger, player2) {
      const kane = event.cost_data;
      const target = event.targets[0];
      player2.removeMark(kane, 1);
      player2.popup(kane, "metal");
      player2.addSkill("yijin_clear");
      target.addMark(kane, 1);
      target.addAdditionalSkill("yijin_" + player2.playerid, kane);
      await game.delayx();
    },
    ai: { neg: true },
    subSkill: {
      mark: {
        mark: true,
        marktext: "金",
        intro: {
          name: "亿金",
          name2: "亿金",
          markcount(storage, player2) {
            return lib.skill.yijin.getKane(player2).length;
          },
          content(storage, player2) {
            return "剩余金：" + get.translation(lib.skill.yijin.getKane(player2));
          }
        }
      },
      upstart: {
        audio: "yijin1.mp3",
        trigger: {
          global: "phaseBefore",
          player: "enterGame"
        },
        forced: true,
        filter(event, player2) {
          return event.name != "phase" || game.phaseNumber == 0;
        },
        async content(event, trigger, player2) {
          var kane = lib.skill.yijin.derivation;
          for (var mark of kane) {
            player2.addMark(mark, 1, false);
            player2.unmarkSkill(mark);
          }
          player2.addSkill("yijin_mark");
        }
      },
      die: {
        audio: "yijin3.mp3",
        trigger: { player: "phaseBegin" },
        forced: true,
        check: () => false,
        filter(event, player2) {
          return !lib.skill.yijin.getKane(player2).length;
        },
        async content(event, trigger, player2) {
          await player2.die();
        }
      },
      clear: {
        trigger: {
          global: "phaseAfter",
          player: "die"
        },
        charlotte: true,
        forced: true,
        popup: false,
        forceDie: true,
        filter(event, player2) {
          if (event.name == "die") {
            return true;
          }
          return lib.skill.yijin.getKane(event.player).length && event.player.additionalSkills["yijin_" + player2.playerid];
        },
        async content(event, trigger, player2) {
          if (trigger.name == "die") {
            game.countPlayer((current) => {
              var skills3 = current.additionalSkills["yijin_" + player2.playerid];
              if (skills3 && skills3.length) {
                current.removeAdditionalSkill("yijin_" + player2.playerid);
                for (var i of skills3) {
                  trigger.player.removeSkill(i);
                }
              }
            });
            return;
          }
          const skills2 = trigger.player.additionalSkills["yijin_" + player2.playerid];
          for (const mark of skills2) {
            trigger.player.removeMark(mark, 1);
          }
          trigger.player.removeAdditionalSkill("yijin_" + player2.playerid);
        }
      },
      wushi: {
        charlotte: true,
        forced: true,
        trigger: { player: "phaseDrawBegin2" },
        async content(event, trigger, player2) {
          trigger.num += 4;
        },
        mod: {
          cardUsable(card2, player2, num) {
            if (card2.name == "sha") {
              return num + 1;
            }
          }
        },
        nopop: true,
        marktext: "金",
        intro: {
          name: "金(膴仕)",
          name2: "金(膴仕)",
          content: "摸牌阶段多摸四张牌；使用【杀】的次数上限+1"
        }
      },
      jinmi: {
        charlotte: true,
        forced: true,
        trigger: { player: "phaseBegin" },
        async content(event, trigger, player2) {
          player2.skip("phaseUse");
          player2.skip("phaseDiscard");
        },
        nopop: true,
        marktext: "金",
        intro: {
          name: "金(金迷)",
          name2: "金(金迷)",
          content: "回合开始时，跳过下一个出牌阶段和弃牌阶段"
        }
      },
      guxiong: {
        charlotte: true,
        forced: true,
        trigger: { player: "phaseUseBegin" },
        async content(event, trigger, player2) {
          await player2.loseHp();
        },
        ai: {
          neg: true,
          nokeep: true
        },
        mod: {
          maxHandcard(player2, num) {
            return num - 3;
          }
        },
        nopop: true,
        marktext: "金",
        intro: {
          name: "金(贾凶)",
          name2: "金(贾凶)",
          content: "出牌阶段开始时，失去1点体力；手牌上限-3"
        }
      },
      tongshen: {
        charlotte: true,
        forced: true,
        trigger: { player: "damageBegin4" },
        filter(event) {
          return !event.hasNature("thunder");
        },
        async content(event, trigger, player2) {
          trigger.cancel();
        },
        ai: {
          nofire: true,
          nodamage: true,
          effect: {
            target(card2, player2, target, current) {
              if (get.tag(card2, "damage") && !get.tag(card2, "thunderDamage")) {
                return [0, 0];
              }
            }
          }
        },
        nopop: true,
        marktext: "金",
        intro: {
          name: "金(通神)",
          name2: "金(通神)",
          content: "当你受到非雷电伤害时，防止之"
        }
      },
      yongbi: {
        charlotte: true,
        forced: true,
        trigger: { player: "phaseZhunbeiBegin" },
        async content(event, trigger, player2) {
          player2.skip("phaseDraw");
        },
        ai: {
          neg: true,
          nokeep: true
        },
        nopop: true,
        marktext: "金",
        intro: {
          name: "金(拥蔽)",
          name2: "金(拥蔽)",
          content: "准备阶段，跳过下一个摸牌阶段"
        }
      },
      houren: {
        charlotte: true,
        forced: true,
        trigger: { player: "phaseEnd" },
        async content(event, trigger, player2) {
          await player2.recover(3);
        },
        nopop: true,
        marktext: "金",
        intro: {
          name: "金(厚任)",
          name2: "金(厚任)",
          content: "回合结束时，回复3点体力"
        }
      }
    }
  },
  guanzong: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.countPlayer((current) => current != player2) >= 2;
    },
    filterTarget: lib.filter.notMe,
    selectTarget: 2,
    multitarget: true,
    targetprompt: ["伤害来源", "受伤角色"],
    async content(event, trigger, player2) {
      const targets2 = event.targets;
      await targets2[1].damage(targets2[0], "unreal");
    },
    ai: {
      result: {
        target(player2, target) {
          if (game.countPlayer((i) => i != player2) < 2) {
            return 0;
          }
          var list = game.filterPlayer((current) => current != player2).map((current) => {
            var _hp = current.hp, _maxhp = current.maxHp;
            current.hp = 10;
            current.maxHp = 10;
            var att = -get.sgnAttitude(player2, current);
            var val = get.damageEffect(current, player2, current) * att;
            current.getSkills(null, false, false).forEach((skill) => {
              var info = get.info(skill);
              if (info && info.ai && (info.ai.maixie || info.ai.maixie_hp || info.ai.maixie_defend)) {
                val = Math[val > 0 ? "max" : "min"](val > 0 ? 0.1 : -0.1, val + 2 * att);
              }
            });
            var eff = 100 / val + 15;
            current.hp = _hp;
            current.maxHp = _maxhp;
            return [current, eff];
          }).sort((a, b) => b[1] - a[1])[0];
          if (list[1] < 0) {
            return 0;
          }
          var targetx = list[0], sign = get.sgnAttitude(player2, target);
          if (ui.selected.targets.length) {
            return target == targetx ? sign : 0;
          }
          return sign * (game.filterPlayer((current) => {
            return current != player2 && current != targetx;
          }).map((current) => {
            var _hp = targetx.hp, _maxhp = targetx.maxHp;
            targetx.hp = 10;
            targetx.maxHp = 10;
            var eff = -get.damageEffect(targetx, current, current);
            targetx.hp = _hp;
            targetx.maxHp = _maxhp;
            return [current, eff];
          }).sort((a, b) => b[1] - a[1])[0][0] == target ? 10 : 1);
        }
      },
      order: 9.5,
      expose: 0.2
    }
  },
  //马日磾
  chengye: {
    audio: 3,
    liujing_filter: [
      function(card2) {
        return get.type(card2, null, false) == "trick" && get.is.damageCard(card2);
      },
      (card2) => get.type(card2, null, false) == "basic",
      (card2) => get.name(card2, false) == "wuxie",
      (card2) => get.name(card2, false) == "wuzhong",
      (card2) => get.name(card2, false) == "lebu",
      (card2) => get.type(card2, null, false) == "equip"
    ],
    getLiujing(player2, index) {
      var filter = lib.skill.chengye.liujing_filter[index], expansion = player2.getExpansions("chengye");
      for (var i of expansion) {
        if (filter(i)) {
          return i;
        }
      }
      return false;
    },
    trigger: {
      global: ["useCardAfter", "loseAfter", "cardsDiscardAfter", "loseAsyncAfter", "equipAfter"]
    },
    forced: true,
    filter(event, player2) {
      if (player2 == event.player) {
        return false;
      }
      if (event.name == "useCard") {
        if (!event.card.isCard) {
          return false;
        }
        var cards2 = event.cards.filterInD();
        if (!cards2.length) {
          return false;
        }
      } else if (event.name != "cardsDiscard") {
        var cards2 = event.getd(null, "cards2").filter(function(card2) {
          if (get.position(card2, true) != "d") {
            return false;
          }
          var type = get.type(card2, null, false);
          return type == "delay" || type == "equip";
        });
        cards2.removeArray(event.getd(player2, "cards2"));
        if (!cards2.length) {
          return false;
        }
      } else {
        var evtx = event.getParent();
        if (evtx.name != "orderingDiscard") {
          return false;
        }
        var evt2 = evtx.relatedEvent || evtx.getParent();
        if (evt2.name != "phaseJudge" || evt2.player == player2) {
          return;
        }
        var cards2 = event.cards.filter(function(card2) {
          if (get.position(card2, true) != "d") {
            return false;
          }
          var type = get.type(card2, null, false);
          return type == "delay";
        });
        if (!cards2.length) {
          return false;
        }
      }
      for (var i = 0; i < 6; i++) {
        if (lib.skill.chengye.getLiujing(player2, i)) {
          continue;
        }
        for (var j of cards2) {
          if (lib.skill.chengye.liujing_filter[i](j)) {
            return true;
          }
        }
      }
      return false;
    },
    async content(event, trigger, player2) {
      var cards2, cards22 = [];
      if (trigger.name == "useCard") {
        cards2 = trigger.cards.filterInD();
      } else if (trigger.name != "cardsDiscard") {
        cards2 = trigger.getd().filter(function(card2) {
          if (card2.original == "j" || get.position(card2, true) != "d") {
            return false;
          }
          var type = get.type(card2, null, false);
          return type == "delay" || type == "equip";
        });
        cards2.removeArray(trigger.getd(player2));
      } else {
        cards2 = trigger.cards.filter(function(card2) {
          if (get.position(card2, true) != "d") {
            return false;
          }
          var type = get.type(card2, null, false);
          return type == "delay";
        });
      }
      for (var i = 0; i < 6; i++) {
        if (lib.skill.chengye.getLiujing(player2, i)) {
          continue;
        }
        for (var j of cards2) {
          if (lib.skill.chengye.liujing_filter[i](j)) {
            cards2.remove(j);
            cards22.push(j);
            break;
          }
        }
        if (!cards2.length) {
          break;
        }
      }
      const next = player2.addToExpansion(cards22, "gain2");
      next.gaintag.add("chengye");
      await next;
    },
    onremove(player2, skill) {
      var cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    mark: true,
    marktext: "经",
    intro: {
      name: "六经",
      markcount: "expansion",
      content: "expansion",
      mark(dialog, storage, player2) {
        let list1 = [], list2 = [];
        var list = ["《诗经》", "《尚书》", "《仪礼》", "《易经》", "《乐经》", "《春秋》"];
        var desc = ["伤害类锦囊牌", "基本牌", "无懈可击", "无中生有", "乐不思蜀", "装备牌"];
        const addNewRow = lib.element.dialog.addNewRow.bind(dialog);
        dialog.css({ width: "60%" });
        for (var i = 0; i < 6; i++) {
          var card2 = lib.skill.chengye.getLiujing(player2, i);
          (i <= 2 ? list1 : list2).addArray([
            { item: list[i] + '<div class="text center">' + desc[i] + "</div>", ratio: 6 },
            { item: card2 ? [card2] : [], ratio: 6 }
          ]);
        }
        addNewRow(...list1);
        addNewRow(...list2);
      }
    },
    group: "chengye_gain",
    subSkill: {
      gain: {
        audio: "chengye",
        trigger: { player: "phaseUseBegin" },
        filter(event, player2) {
          return player2.getExpansions("chengye").length >= 6;
        },
        forced: true,
        async content(event, trigger, player2) {
          await player2.gain(player2.getExpansions("chengye"), "gain2");
        }
      }
    }
  },
  buxu: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player2) {
      if (!player2.hasSkill("chengye", null, null, false)) {
        return false;
      }
      const num = player2.countMark("buxu_mark") + 1;
      return player2.countCards("he") >= num && player2.getExpansions("chengye").length < 6;
    },
    chooseButton: {
      chooseControl(event, player2) {
        const list = ["诗经", "尚书", "仪礼", "易经", "乐经", "春秋"];
        const choices = [];
        for (let i = 0; i < 6; i++) {
          if (!lib.skill.chengye.getLiujing(player2, i)) {
            choices.push(list[i]);
          }
        }
        choices.push("cancel2");
        return choices;
      },
      check(event, player2) {
        const list = [4, 3, 5, 0, 2, 1];
        const choice = list.find((num) => {
          if (lib.skill.chengye.getLiujing(player2, num)) {
            return false;
          }
          const jingdian = ["诗经", "尚书", "仪礼", "易经", "乐经", "春秋"][num];
          return !player2.getStorage("buxu_ai").includes(jingdian);
        });
        if (choice) {
          return choice;
        }
        return "cancel2";
      },
      dialog(event, player2) {
        const num = player2.countMark("buxu_mark") + 1;
        return ui.create.dialog("###补续###弃置" + get.cnNumber(num) + "张牌并补充一张“六经”");
      },
      prompt(links, player2) {
        const num = player2.countMark("buxu_mark") + 1;
        return "弃置" + get.cnNumber(num) + "张牌并补充一张《" + links.control + "》";
      },
      backup(result, player2) {
        return {
          audio: "buxu",
          index: ["诗经", "尚书", "仪礼", "易经", "乐经", "春秋"].indexOf(result.control),
          filterCard: lib.filter.cardDiscardable,
          position: "he",
          selectCard: () => {
            return get.player().countMark("buxu_mark") + 1;
          },
          ai1(card2) {
            const player3 = get.player();
            if (player3.needsToDiscard(0, (i, player4) => {
              return !ui.selected.cards.includes(i) && !player4.canIgnoreHandcard(i);
            })) {
              return 10 / Math.max(0.1, get.value(card2));
            }
            return 5 - (player3.countMark("buxu_mark") + 1) - get.value(card2);
          },
          ai2: () => 1,
          async content(event, trigger, player3) {
            const index = get.info("buxu_backup").index;
            const chice = ["诗经", "尚书", "仪礼", "易经", "乐经", "春秋"][index];
            const filter = get.info("chengye").liujing_filter[index];
            const card2 = get.cardPile2(filter);
            if (card2) {
              event.getParent().buxu = true;
              player3.addTempSkill("buxu_mark", "phaseUseAfter");
              player3.addMark("buxu_mark", 1, false);
              const next = player3.addToExpansion(card2, "gain2");
              next.gaintag.add("chengye");
              await next;
            } else {
              game.log(`但是牌堆里没有《${chice}》`);
              player3.chat("我chovy！你们拿经典你们给我拿有的啊！");
              player3.addTempSkill("buxu_ai", "phaseUseAfter");
              player3.markAuto("buxu_ai", [chice]);
            }
          },
          ai: { result: { player: 1 } }
        };
      }
    },
    ai: {
      combo: "chengye",
      order(item, player2) {
        const num = player2.countMark("buxu_mark");
        if (player2.countCards("he", (card2) => get.value(card2) <= 6) >= num * 2) {
          return 10;
        }
        return 1;
      },
      result: { player: 1 }
    },
    subSkill: {
      backup: {},
      mark: {
        charlotte: true,
        onremove: true,
        intro: { content: "本阶段已成功“补续”#次" }
      },
      ai: {
        charlotte: true,
        onremove: true
      }
    }
  },
  //阮慧
  mingcha: {
    audio: 2,
    trigger: { player: "phaseDrawBegin1" },
    forced: true,
    locked: false,
    filter: (event) => !event.numFixed,
    async content(event, trigger, player2) {
      var cards2 = game.cardsGotoOrdering(get.cards(3)).cards;
      cards2.slice(0);
      var cardsx = cards2.filter(function(i) {
        return get.number(i) < 9;
      });
      await player2.showCards(cards2, get.translation(player2) + "发动了【明察】");
      if (!cardsx.length) {
        return;
      }
      const result = await player2.chooseBool("是否放弃摸牌并获得" + get.translation(cards2)).set("goon", trigger.num - cards2.length <= 1).forResult();
      if (!result.bool) {
        return;
      }
      trigger.changeToZero();
      await player2.gain(cards2, "gain2");
      const result2 = await player2.chooseTarget("是否随机获得其他角色的一张牌？", function(card2, player3, target2) {
        return target2 != player3 && target2.countCards("he") > 0;
      }).set("ai", function(target2) {
        return 3 - get.attitude(player2, target2);
      }).forResult();
      if (result2.bool) {
        var target = result2.targets[0], cards3 = target.getGainableCards(player2, "he");
        player2.line(target, "green");
        if (cards3.length) {
          await player2.gain(cards3.randomGet(), target, "giveAuto", "bySelf");
        }
      }
    }
  },
  jingzhong: {
    audio: 2,
    trigger: { player: "phaseDiscardAfter" },
    filter(event, player2) {
      var num = 0;
      player2.getHistory("lose", function(evt) {
        if (evt.type == "discard" && evt.getParent("phaseDiscard") == event) {
          for (var i of evt.cards2) {
            if (get.color(i, player2) == "black") {
              num++;
            }
          }
        }
      });
      return num > 1;
    },
    direct: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt("jingzhong"), "获得一名其他角色下回合出牌阶段内使用的牌", lib.filter.notMe).set("ai", function(target2) {
        return Math.sqrt(target2.countCards("h")) * get.threaten(target2);
      }).forResult();
      if (!result.bool) {
        return;
      }
      var target = result.targets[0];
      player2.logSkill("jingzhong", target);
      player2.addSkill("jingzhong_effect");
      player2.markAuto("jingzhong_effect", [target]);
      await game.delayx();
    },
    subSkill: {
      effect: {
        audio: "jingzhong",
        trigger: { global: "useCardAfter" },
        forced: true,
        charlotte: true,
        filter(event, player2) {
          if (!player2.getStorage("jingzhong_effect").includes(event.player) || !event.cards.filterInD().length) {
            return false;
          }
          var evt = event.getParent("phaseUse");
          if (!evt || evt.player != event.player) {
            return false;
          }
          return player2.getHistory("useSkill", function(evtx) {
            return evtx.skill == "jingzhong_effect" && evtx.event.getParent("phaseUse") == evt;
          }).length < 3;
        },
        logTarget: "player",
        async content(event, trigger, player2) {
          await player2.gain(trigger.cards.filterInD(), "gain2");
        },
        mark: true,
        intro: { content: "已指定$为目标" },
        group: "jingzhong_remove"
      },
      remove: {
        trigger: { global: "phaseAfter" },
        forced: true,
        charlotte: true,
        popup: false,
        firstDo: true,
        filter(event, player2) {
          return player2.getStorage("jingzhong_effect").includes(event.player);
        },
        async content(event, trigger, player2) {
          var storage = player2.getStorage("jingzhong_effect");
          storage.remove(trigger.player);
          if (!storage.length) {
            player2.removeSkill("jingzhong_effect");
          }
        }
      }
    }
  },
  //全琮
  sbyaoming: {
    init(player2, skill) {
      const history = game.getAllGlobalHistory("everything", (evt) => evt.name == "sbyaoming_backup" && evt.player == player2);
      if (history.length) {
        const index = history.at(-1).index;
        player2.addTip(skill, `${get.translation(skill)} ${index == 0 ? "弃牌" : "摸牌"}`);
      }
      const num = lib.skill[skill].beginMarkCount;
      player2.addCharge(num, false);
    },
    onremove(player2, skill) {
      player2.removeTip(skill);
    },
    getNum(player2, target) {
      get.attitude(player2, target);
      let eff = [0, 0];
      const hs = player2.countCards("h"), ht = target.countCards("h");
      if (hs >= ht) {
        eff[0] = get.effect(target, { name: "draw" }, player2, player2);
        if (player2.storage.sbyaoming_status == 0) {
          eff[0] *= 1.2;
        }
      }
      if (hs <= ht && player2 != target && target.hasDiscardableCards(player2, "he")) {
        eff[1] = get.effect(target, { name: "guohe_copy2" }, player2, player2);
        if (player2.storage.sbyaoming_status == 1) {
          eff[1] *= 1.2;
        }
      }
      return eff;
    },
    audio: 2,
    beginMarkCount: 2,
    chargeSkill: 4,
    enable: "phaseUse",
    filter(event, player2) {
      return player2.countCharge() > 0;
    },
    chooseButton: {
      dialog(event, player2) {
        const num = player2.storage.sbyaoming_status;
        const list = [
          ["discard", "弃置一名手牌数不小于你的其他角色的一张牌"],
          ["draw", "令一名手牌数不大于你的角色摸一张牌"]
        ];
        if (typeof num == "number") {
          list[1 - num][1] += "（选择此项可获得蓄力值）";
        }
        const dialog = ui.create.dialog(`邀名：你可以消耗1点蓄力值并…`, [list, "textbutton"], "hidden");
        return dialog;
      },
      filter(button, player2) {
        if (button.link === "discard") {
          return game.hasPlayer((current) => current != player2 && current.hasDiscardableCards(player2, "he") && current.countCards("h") >= player2.countCards("h"));
        }
        return true;
      },
      check(button) {
        const player2 = get.player();
        const link = button.link;
        const list = game.filterPlayer().map((current) => [current, get.info("sbyaoming").getNum(player2, current)]);
        const draw = Math.max(...list.map((item) => item[1][0]));
        const discard = Math.max(...list.map((item) => item[1][1]));
        const choice = draw >= discard ? "draw" : "discard";
        if (link == choice) {
          return 10;
        }
        return 0;
      },
      backup(links, player2) {
        const backup = get.copy(lib.skill["sbyaoming_backup"]);
        backup.link = links[0];
        return backup;
      },
      prompt(links, player2) {
        const link = links[0];
        return `###邀名###消耗1点蓄力值并${link == "discard" ? "弃置一名手牌数不小于你的其他角色的一张牌" : "令一名手牌数不大于你的角色摸一张牌"}`;
      }
    },
    ai: {
      order(item, player2) {
        if (game.hasPlayer((current) => {
          const num = Math.max.apply(Math, get.info("sbyaoming").getNum(player2, current));
          return num > 0;
        })) {
          return 7.1;
        }
        return 0.1;
      },
      result: { player: 1 }
    },
    group: ["sbyaoming_damage"],
    subSkill: {
      backup: {
        audio: "sbyaoming",
        filterCard: () => false,
        selectCard: -1,
        filterTarget(card2, player2, target) {
          const link = lib.skill.sbyaoming_backup.link;
          if (link === "discard") {
            return target != player2 && target.hasDiscardableCards(player2, "he") && target.countCards("h") >= player2.countCards("h");
          }
          return target.countCards("h") <= player2.countCards("h");
        },
        async content(event, trigger, player2) {
          const link = lib.skill.sbyaoming_backup.link;
          const index = link === "discard" ? 0 : 1;
          event.index = index;
          player2.addTip("sbyaoming", `${get.translation("sbyaoming")} ${index == 0 ? "弃牌" : "摸牌"}`);
          player2.removeCharge();
          const { target } = event;
          if (link === "discard") {
            await player2.discardPlayerCard(target, true, "he");
          } else if (link === "draw") {
            await target.draw();
          }
          if (typeof player2.storage.sbyaoming_status == "number" && index != player2.storage.sbyaoming_status && player2.countCharge(true)) {
            player2.addCharge();
            delete player2.storage.sbyaoming_status;
          } else {
            player2.storage.sbyaoming_status = index;
          }
          player2.markSkill("sbyaoming");
        },
        ai: {
          result: {
            player(player2, target) {
              switch (lib.skill.sbyaoming_backup.link) {
                case "discard": {
                  let eff = get.effect(target, { name: "guohe_copy2" }, player2, player2);
                  if (player2.storage.sbyaoming_status == 1) {
                    eff *= 1.2;
                  }
                  return eff;
                }
                case "draw": {
                  let eff = get.effect(target, { name: "draw" }, player2, player2);
                  if (player2.storage.sbyaoming_status == 0) {
                    eff *= 1.2;
                  }
                  return eff;
                }
              }
            }
          }
        }
      },
      damage: {
        audio: "sbyaoming",
        trigger: { player: "damageEnd" },
        direct: true,
        async content(event, trigger, player2) {
          if (player2.countCharge(true)) {
            player2.addCharge(trigger.num);
            await game.delayx();
          }
          if (!player2.countCharge()) {
            return;
          }
          const num = player2.storage.sbyaoming_status;
          const list = [
            ["discard", "弃置一名手牌数不小于你的其他角色的一张牌"],
            ["draw", "令一名手牌数不大于你的角色摸一张牌"]
          ];
          if (typeof num == "number") {
            list[1 - num][1] += "（选择此项可获得蓄力值）";
          }
          const result = await player2.chooseButtonTarget({
            createDialog: ["邀名：你可以消耗1点蓄力值并…", [list, "textbutton"]],
            filterButton(button) {
              if (button.link === "discard") {
                return game.hasPlayer((current) => current != player2 && current.hasDiscardableCards(player2, "he") && current.countCards("h") >= player2.countCards("h"));
              }
              return true;
            },
            filterTarget(card2, player3, target) {
              if (!ui.selected.buttons.length) {
                return false;
              }
              const link = ui.selected.buttons[0].link;
              if (link === "discard") {
                return target != player3 && target.hasDiscardableCards(player3, "he") && target.countCards("h") >= player3.countCards("h");
              }
              return target.countCards("h") <= player3.countCards("h");
            },
            ai1(button) {
              const player3 = get.player();
              const link = button.link;
              const list2 = game.filterPlayer().map((current) => [current, get.info("sbyaoming").getNum(player3, current)]);
              const draw = Math.max(...list2.map((item) => item[1][0]));
              const discard = Math.max(...list2.map((item) => item[1][1]));
              const choice = draw >= discard ? "draw" : "discard";
              if (link == choice) {
                return 10;
              }
              return 0;
            },
            ai2(target) {
              const { player: player3, numx } = get.event();
              const link = ui.selected.buttons[0].link;
              switch (link) {
                case "discard": {
                  let eff = get.effect(target, { name: "guohe_copy2" }, player3, player3);
                  if (player3.storage.sbyaoming_status == 1) {
                    eff *= 1.2;
                  }
                  return eff;
                }
                case "draw": {
                  let eff = get.effect(target, { name: "draw" }, player3, player3);
                  if (player3.storage.sbyaoming_status == 0) {
                    eff *= 1.2;
                  }
                  return eff;
                }
                default: {
                  return 0;
                }
              }
            }
          }).forResult();
          if (result?.links?.length && result.targets?.length) {
            const link = result.links[0];
            game.broadcastAll((link2) => {
              lib.skill.sbyaoming_backup.link = link2;
            }, link);
            await player2.useSkill("sbyaoming_backup", result.targets);
          }
        }
      }
    }
  },
  //手杀界荀彧
  rejieming: {
    audio: 2,
    trigger: { player: "damageEnd" },
    filter(event, player2) {
      return event.num > 0;
    },
    getIndex: (event) => event.num,
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt2(event.skill)).set("ai", (target) => {
        const att = get.attitude(get.player(), target);
        if (att > 2) {
          if (target.maxHp - target.countCards("h") > 2) {
            return 2 * att;
          }
          return att;
        }
        return att / 3;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      player2.line(target, "thunder");
      await target.draw(2);
      if (target.countCards("h") < target.maxHp) {
        await player2.draw();
      }
    },
    ai: {
      maixie: true,
      maixie_hp: true,
      effect: {
        target(card2, player2, target, current) {
          if (get.tag(card2, "damage") && target.hp > 1) {
            if (player2.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            var max = 0;
            var players = game.filterPlayer();
            for (var i = 0; i < players.length; i++) {
              if (get.attitude(target, players[i]) > 0) {
                max = Math.max(Math.min(5, players[i].hp) - players[i].countCards("h"), max);
              }
            }
            switch (max) {
              case 0:
                return 2;
              case 1:
                return 1.5;
              case 2:
                return [1, 2];
              default:
                return [0, max];
            }
          }
          if ((card2.name == "tao" || card2.name == "caoyao") && target.hp > 1 && target.countCards("h") <= target.hp) {
            return [0, 0];
          }
        }
      }
    }
  },
  //沮授
  xinjianying: {
    audio: 2,
    subfrequent: ["draw"],
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return get.inpileVCardList((info) => info[0] === "basic").some((info) => {
        return player2.hasCard((card2) => {
          const vcard = get.autoViewAs({ name: info[2], nature: info[3], cards: [card2] }, [card2]);
          return event.filterCard(vcard, player2, event);
        }, "hes");
      });
    },
    onChooseToUse(event) {
      if (event.type == "phase" && !game.online) {
        var last = event.player.getLastUsed();
        if (last && last.getParent("phaseUse") == event.getParent()) {
          var suit = get.suit(last.card, false);
          if (suit != "none") {
            event.set("xinjianying_suit", suit);
          }
        }
      }
    },
    chooseButton: {
      dialog(event, player2) {
        const str = get.translation(event.xinjianying_suit || "");
        return ui.create.dialog("渐营", [
          get.inpileVCardList((info) => info[0] === "basic").filter((info) => {
            return player2.hasCard((card2) => {
              const vcard = get.autoViewAs({ name: info[2], nature: info[3], cards: [card2] }, [card2]);
              return event.filterCard(vcard, player2, event);
            }, "hes");
          }).map((info) => {
            info[1] = str;
            return info;
          }),
          "vcard"
        ]);
      },
      check(button) {
        return get.player().getUseValue({ name: button.link[2], nature: button.link[3] });
      },
      backup(links, player2) {
        const event = get.event();
        var next = {
          audio: "xinjianying",
          filterCard: true,
          popname: true,
          position: "hes",
          viewAs: {
            name: links[0][2],
            nature: links[0][3]
          },
          ai1(card2) {
            const player3 = get.player();
            return 7 - player3.getUseValue(card2, null, true);
          }
        };
        if (event.xinjianying_suit) next.viewAs.suit = event.xinjianying_suit;
        return next;
      },
      prompt(links) {
        const event = get.event();
        return "将一张牌当做" + (get.translation(links[0][3]) || "") + "【" + get.translation(links[0][2]) + (event.xinjianying_suit ? "(" + get.translation(event.xinjianying_suit) + ")" : "") + "】使用";
      }
    },
    ai: {
      order(item, player2) {
        const event = get.event();
        player2 = player2 || event.player;
        const list = get.inpileVCardList((info) => info[0] === "basic").filter((info) => {
          return player2.hasCard((card2) => {
            const vcard = get.autoViewAs({ name: info[2], nature: info[3], cards: [card2] }, [card2]);
            return event.filterCard(vcard, player2, event);
          }, "hes");
        });
        if (!list.length) return 0;
        return Math.max(...list.map((info) => get.order({ name: info[2], nature: info[3] }, player2))) - 0.1;
      },
      result: { player: 7 }
    },
    group: ["xinjianying_draw", "jianying_mark"],
    init(player2) {
      if (player2.isPhaseUsing()) {
        var evt = _status.event.getParent("phaseUse");
        var history = player2.getHistory("useCard", function(evt2) {
          return evt2.getParent("phaseUse") == evt;
        });
        if (history.length) {
          var trigger = history[history.length - 1];
          player2.storage.jianying_mark = trigger.card;
          player2.markSkill("jianying_mark");
          game.broadcastAll(
            function(player3, suit) {
              if (player3.marks.jianying_mark) {
                player3.marks.jianying_mark.firstChild.innerHTML = get.translation(suit);
              }
            },
            player2,
            get.suit(trigger.card, player2)
          );
          player2.when("phaseUseAfter").step(async () => {
            player2.unmarkSkill("jianying_mark");
            delete player2.storage.jianying_mark;
          });
        }
      }
    },
    onremove(player2) {
      player2.unmarkSkill("jianying_mark");
      delete player2.storage.jianying_mark;
    },
    subSkill: {
      draw: {
        audio: "xinjianying",
        inherit: "jianying"
      }
    }
  },
  //步练师
  reanxu: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.countPlayer() > 2 && game.hasPlayer(function(current) {
        return current != player2 && current.countCards("he");
      });
    },
    selectTarget: 2,
    filterTarget(card2, player2, target) {
      if (target == player2) {
        return false;
      }
      if (!ui.selected.targets.length) {
        return target.countCards("he") > 0;
      }
      return target != ui.selected.targets[0] && ui.selected.targets[0].countGainableCards(target, "he") > 0;
    },
    multitarget: true,
    targetprompt: ["被拿牌", "得到牌"],
    async content(event, trigger, player2) {
      const targets2 = event.targets;
      await targets2[1].gainPlayerCard(targets2[0], "he", true);
      if (targets2[0].getHistory("lose", function(evt) {
        return evt.getParent(3) == event && !evt.es.length;
      }).length) {
        await player2.draw();
      }
      if (targets2[0].isIn() && targets2[1].isIn() && targets2[0].countCards("h") != targets2[1].countCards("h")) {
        event.target = targets2[targets2[0].countCards("h") > targets2[1].countCards("h") ? 1 : 0];
        const result = await player2.chooseBool("是否令" + get.translation(event.target) + "摸一张牌？").set("ai", function() {
          var evt = _status.event.getParent();
          return get.attitude(evt.player, evt.target) > 0;
        }).forResult();
        if (result.bool) {
          await event.target.draw();
        }
      }
    },
    ai: {
      expose: 0.2,
      threaten: 2,
      order: 9,
      result: {
        player(player2, target) {
          if (ui.selected.targets.length) {
            return 0.01;
          }
          return target.countCards("e") ? 0 : 0.5;
        },
        target(player2, target) {
          if (ui.selected.targets.length) {
            player2 = target;
            target = ui.selected.targets[0];
            if (get.attitude(player2, target) > 1) {
              return 0;
            }
            return target.countCards("h") - player2.countCards("h") > (target.countCards("e") ? 2 : 1) ? 2 : 1;
          } else {
            if (get.attitude(player2, target) <= 0) {
              return target.countCards("he", function(card2) {
                return card2.name == "tengjia" || get.value(card2) > 0;
              }) > 0 ? -1.5 : 1.5;
            }
            return target.countCards("he", function(card2) {
              return card2.name != "tengjia" && get.value(card2) <= 0;
            }) > 0 ? 1.5 : -1.5;
          }
        }
      }
    }
  },
  //蒋干
  spdaoshu: {
    audio: 3,
    trigger: { global: "phaseUseBegin" },
    filter(event, player2) {
      var goon = event.player != player2 && (get.mode() == "identity" || get.mode() == "guozhan" || event.player.isEnemyOf(player2));
      return goon && event.player.countCards("h") > 0 && event.player.hasUseTarget({ name: "jiu", isCard: true }, null, true);
    },
    round: 1,
    logTarget: "player",
    check(event, player2) {
      var target = event.player;
      var att = get.attitude(player2, target);
      if (att > 0) {
        return false;
      }
      if (att == 0) {
        return !player2.inRangeOf(target);
      }
      return true;
    },
    logAudio: () => 1,
    async content(event, trigger, player2) {
      const target = trigger.player;
      await target.chooseUseTarget("jiu", true);
      if (!target.countCards("h")) {
        return;
      }
      var list = [];
      for (var i of lib.inpile) {
        if (get.type(i) == "basic") {
          list.push(i);
        }
      }
      if (!list.length) {
        return;
      }
      const result1 = await target.chooseControl(list).set("prompt", "请声明一种基本牌").set("ai", () => _status.event.rand).set("rand", get.rand(0, list.length - 1)).forResult();
      const cardname = result1.control;
      target.chat("我声明" + get.translation(cardname));
      game.log(target, "声明的牌名为", "#y" + get.translation(cardname));
      await game.delayx();
      const result2 = await player2.chooseControl("有！", "没有！").set("prompt", "你觉得" + get.translation(target) + "的手牌区里有" + get.translation(cardname) + "吗？").set("ai", function() {
        return _status.event.choice;
      }).set(
        "choice",
        (function() {
          var rand = {
            sha: 0.273,
            shan: 0.149,
            tao: 0.074,
            jiu: 0.031
          }[cardname] || 0.1;
          return 1 - Math.pow(1 - rand, target.countCards("h")) > 0.5 ? "有！" : "没有！";
        })()
      ).forResult();
      player2.chat(result2.control);
      game.log(player2, "认为", "#y" + result2.control);
      await game.delayx();
      var bool1 = result2.index == 0;
      var bool2 = target.hasCard(function(card2) {
        return get.name(card2, target) == cardname;
      }, "h");
      if (bool1 == bool2) {
        player2.popup("判断正确", "wood");
        game.broadcastAll(function() {
          if (lib.config.background_speak) {
            game.playAudio("skill", "spdaoshu2");
          }
        });
        await player2.gainPlayerCard(target, "h", 2, true);
      } else {
        player2.popup("判断错误", "fire");
        game.broadcastAll(function() {
          if (lib.config.background_speak) {
            game.playAudio("skill", "spdaoshu3");
          }
        });
      }
    },
    ai: { expose: 0.3 },
    subSkill: {
      respond: {
        trigger: { global: "useCard1" },
        forced: true,
        popup: false,
        filter(event, player2) {
          return event.player == _status.currentPhase;
        },
        async content(event, trigger, player2) {
          trigger.directHit.add(player2);
        }
      }
    }
  },
  mbdaoshu: {
    audio: 3,
    enable: "phaseUse",
    filter(event, player2) {
      return game.hasPlayer((target) => lib.skill.mbdaoshu.filterTarget(event, player2, target));
    },
    filterTarget(card2, player2, target) {
      if (!["guozhan", "identity"].includes(get.mode()) && target.isFriendOf(player2)) {
        return false;
      }
      return target != player2 && target.countCards("h") >= 2;
    },
    usable: 1,
    logAudio: () => 1,
    async content(event, trigger, player2) {
      var target = event.target;
      var targets2 = [player2], names = lib.inpile.randomGets(3);
      if (!names.length) {
        return;
      }
      var map = {};
      names.forEach((name) => map[get.translation(name)] = name);
      if (get.mode() != "identity" && get.mode() != "guozhan") {
        targets2.addArray(player2.getFriends());
      }
      targets2.remove(target);
      targets2.sortBySeat();
      var result = await target.chooseButton(["盗书：请选择伪装的牌和牌名", target.getCards("h"), [Object.keys(map), "tdnodes"]], 2, true).set("filterButton", (button) => {
        var map2 = _status.event.map;
        if (!ui.selected.buttons.length) {
          return true;
        }
        if (typeof button.link == typeof ui.selected.buttons[0].link) {
          return false;
        }
        if (typeof button.link == "string") {
          return get.name(ui.selected.buttons[0].link, false) != map2[button.link];
        }
        return map2[ui.selected.buttons[0].link] != get.name(button.link, false);
      }).set("ai", (button) => {
        var map2 = _status.event.map;
        if (!ui.selected.buttons.length) {
          if (typeof button.link == "object") {
            if (Object.values(map2).some((name) => lib.card.list.some((card3) => card3[0] == get.suit(button.link, false) && card3[1] == get.number(button.link, false) && card3[2] == name))) {
              return 5;
            }
            return 3.5 + Math.random();
          }
          return 0;
        }
        if (typeof button.link == "string") {
          var cardx = ui.selected.buttons[0].link;
          if (lib.card.list.some((card3) => card3[0] == get.suit(cardx, false) && card3[1] == get.number(cardx, false) && card3[2] == map2[button.link])) {
            return 2 + Math.random();
          }
          return 1;
        }
        return 0;
      }).set("map", map).forResult();
      if (result.bool) {
        var guessWinner = [];
        if (typeof result.links[0] == "string") {
          result.links.reverse();
        }
        var OriginCard = result.links[0], ChangeName = map[result.links[1]], cards2 = target.getCards("h").slice();
        var card2 = game.createCard(ChangeName, get.suit(OriginCard, false), get.number(OriginCard, false));
        cards2[cards2.indexOf(OriginCard)] = card2;
        var list = targets2.map((target2) => [target2, ["请猜测" + get.translation(target) + "伪装的手牌", cards2], true]);
        var result2 = await player2.chooseButtonOL(list).set("switchToAuto", () => _status.event.result = "ai").set("processAI", () => {
          var cards3 = _status.event.getParent().cards ?? _status.event.dialog.buttons.map((button) => button.link);
          var card3 = cards3.find((card4) => lib.card.list.some((cardx) => cardx[2] == card4.name) && !lib.card.list.some((cardx) => cardx[2] == card4.name && cardx[0] == get.suit(card4, false) && cardx[0] == get.number(card4, false)));
          return {
            bool: true,
            links: [card3 ? card3 : cards3.randomGet()]
          };
        }).set("cards", cards2).forResult();
        for (var i in result2) {
          if (result2[i].links?.[0] == card2) {
            guessWinner.push((_status.connectMode ? lib.playerOL : game.playerMap)[i]);
          }
        }
        for (const target2 of targets2) {
          if (guessWinner.includes(target2)) {
            target2.popup("判断正确", "wood");
            game.log(target2, "猜测", "#g正确");
            game.broadcastAll(() => {
              if (lib.config.background_speak) {
                game.playAudio("skill", "mbdaoshu2");
              }
            });
            target2.line(target);
            await target.damage(1, target2);
          } else {
            target2.popup("判断错误", "fire");
            game.log(target2, "猜测", "#y错误");
            game.broadcastAll(() => {
              if (lib.config.background_speak) {
                game.playAudio("skill", "mbdaoshu3");
              }
            });
            if (target2.countDiscardableCards(target, "h") >= 2) {
              await target2.discard(target2.getDiscardableCards(target, "h").randomGets(2));
            } else {
              await target2.loseHp();
            }
          }
        }
      }
    },
    ai: {
      order: 9,
      result: {
        target(player2, target) {
          return -1 / target.countCards("h");
        }
      }
    }
  },
  spdaizui: {
    audio: 2,
    trigger: { player: "damageBegin2" },
    limited: true,
    logTarget: "source",
    filter(event, player2) {
      return event.num >= player2.hp && event.source && event.source.isIn() && event.cards && event.cards.filterInD().length > 0;
    },
    prompt2(event) {
      return "防止即将受到的" + get.cnNumber(event.num) + "点伤害，并令" + get.translation(event.source) + "将" + get.translation(event.cards.filterInD()) + "置于武将牌上且回合结束时收回";
    },
    skillAnimation: true,
    animationColor: "thunder",
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      trigger.source.addSkill("spdaizui2");
      const next = trigger.source.addToExpansion(trigger.cards.filterInD(), "gain2");
      next.gaintag.add("spdaizui2");
      await next;
      trigger.cancel();
    }
  },
  spdaizui2: {
    trigger: { global: "phaseEnd" },
    forced: true,
    charlotte: true,
    sourceSkill: "spdaizui",
    filter(event, player2) {
      return player2.getExpansions("spdaizui2").length > 0;
    },
    async content(event, trigger, player2) {
      var cards2 = player2.getExpansions("spdaizui2");
      await player2.gain(cards2, "gain2");
      player2.removeSkill("spdaizui2");
    },
    marktext: "释",
    intro: {
      markcount: "expansion",
      content: "expansion"
    }
  },
  //裴秀
  xingtu: {
    audio: 2,
    trigger: { player: "useCard" },
    filter(event, player2) {
      player2.addTip("xingtu", `行图 ${get.translation(get.number(event.card, player2))}`);
      const evt = lib.skill.dcjianying.getLastUsed(player2, event);
      if (!evt?.card) {
        return false;
      }
      const num1 = get.number(event.card), num2 = get.number(evt.card);
      return typeof num1 == "number" && typeof num2 == "number" && num2 != 0 && num2 % num1 == 0;
    },
    forced: true,
    async content(event, trigger, player2) {
      await player2.draw();
    },
    mod: {
      cardUsable(card2, player2) {
        if (typeof card2 == "object") {
          let num1 = get.number(card2);
          if (num1 != "unsure" && typeof num1 != "number") {
            return;
          }
          if ([card2].concat(card2.cards || []).some((cardx) => get.itemtype(cardx) === "card" && cardx.hasGaintag("xingtu1"))) {
            return Infinity;
          }
          let num2 = player2.storage.xingtu_mark;
          if (typeof num2 == "number" && num1 % num2 == 0) {
            return Infinity;
          }
        }
      },
      aiOrder(player2, card2, num) {
        if (typeof card2 == "object") {
          let num1 = get.number(card2);
          if (num1 != "unsure" && typeof num1 != "number") {
            return;
          }
          if (!card2.cards) {
            return;
          }
          for (var i of card2.cards) {
            if (i.hasGaintag("xingtu1")) {
              return num + 5;
            }
          }
          let num2 = player2.storage.xingtu_mark;
          if (typeof num2 == "number" && num1 % num2 == 0) {
            return num + 5;
          }
        }
      }
    },
    init(player2) {
      player2.addSkill("xingtu_mark");
      const history = player2.getAllHistory("useCard");
      if (history.length) {
        const trigger = history[history.length - 1], num = get.number(trigger.card);
        player2.storage.xingtu_mark = num;
        player2[typeof num != "number" ? "unmarkSkill" : "markSkill"]("xingtu_mark");
      }
    },
    onremove(player2) {
      player2.removeSkill("xingtu_mark");
      player2.removeGaintag("xingtu1");
      player2.removeGaintag("xingtu2");
      player2.removeTip("xingtu");
      delete player2.storage.xingtu_mark;
    },
    subSkill: {
      mark: {
        charlotte: true,
        trigger: {
          player: ["useCard1", "gainAfter"],
          global: "loseAsyncAfter"
        },
        filter(event, player2, name) {
          return name == "useCard1" || event.getg?.(player2)?.length && player2.countCards("h");
        },
        direct: true,
        firstDo: true,
        async content(event, trigger, player2) {
          player2.removeGaintag("xingtu1");
          player2.removeGaintag("xingtu2");
          if (event.triggername == "useCard1") {
            const num2 = get.number(trigger.card, player2);
            player2.storage.xingtu_mark = num2;
            player2[typeof num2 != "number" ? "unmarkSkill" : "markSkill"]("xingtu_mark");
            if (typeof num2 != "number") {
              return;
            }
          }
          const cards1 = [], cards2 = [], num = player2.storage.xingtu_mark;
          player2.getCards("h").forEach((card2) => {
            const numx = get.number(card2, player2);
            if (typeof numx == "number") {
              if (numx % num == 0) {
                cards1.push(card2);
              }
              if (num % numx == 0 && typeof num == "number" && num != 0) {
                cards2.push(card2);
              }
            }
          });
          if (cards1.length) {
            player2.addGaintag(cards1, "xingtu1");
          }
          if (cards2.length) {
            player2.addGaintag(cards2, "xingtu2");
          }
        },
        intro: { content: "上一张牌的点数：#" }
      }
    }
  },
  juezhi: {
    audio: 2,
    enable: "phaseUse",
    filter(event, player2) {
      return player2.countCards("he") > 1;
    },
    filterCard: true,
    position: "he",
    selectCard: [2, Infinity],
    check(card2) {
      if (ui.selected.cards.length > 1) {
        return 0;
      }
      var player2 = _status.event.player;
      if (player2.hasSkill("xingtu") && player2.storage.xingtu) {
        var cards2 = player2.getCards("he");
        var num = player2.storage.xingtu, stop = false;
        for (var i = 0; i <= cards2.length; i++) {
          if (i != cards2.length) {
            var num1 = get.number(cards2[i], player2);
            if (typeof num1 != "number") {
              continue;
            }
            for (var j = 0; j < cards2.length; j++) {
              if (i == j) {
                continue;
              }
              var num2 = get.number(cards2[j], player2);
              if (typeof num2 != "number") {
                continue;
              }
              var sum = num1 + num2;
              if (sum % num == 0 || num % sum == 0) {
                stop = true;
                break;
              }
            }
            if (stop) {
              break;
            }
          }
        }
        if (i != cards2.length) {
          var cardx = [cards2[i], cards2[j]];
          if (cardx.includes(card2)) {
            return 10 - get.value(card2);
          }
        }
      }
      return 5 - get.value(card2);
    },
    allowChooseAll: true,
    prompt(event, player2) {
      if (!game.online) {
        event.custom ??= {};
        event.custom.add ??= {};
        if (!event.custom.add.card?._juezhi) {
          const addCard = event.custom.add.card;
          const fn = function() {
            const evt = get.event();
            if (evt && evt.skill == "juezhi") {
              let sum = 0;
              for (const card2 of ui.selected.cards) {
                sum += get.number(card2, evt.player);
              }
              let num = sum % 13;
              if (num == 0) {
                num = 13;
              }
              const text = sum > 0 ? '<div style="width:100%;text-align:center">将获得' + get.cnNumber(num) + "点的牌</div>" : "选择至少两张牌";
              const dialog = evt.skillDialog;
              if (dialog && get.objtype(dialog) == "div") {
                const captions = dialog.querySelectorAll(".caption");
                if (captions.length > 1) {
                  captions[captions.length - 1].innerHTML = text;
                }
              }
            }
            if (typeof addCard == "function") {
              addCard.call(this);
            }
          };
          fn._juezhi = true;
          event.custom.add.card = fn;
        }
      }
      return "选择至少两张牌，点数之和取模后将获得对应点数的牌";
    },
    async content(event, trigger, player2) {
      var num = 0;
      for (var i of event.cards) {
        num += get.number(i, player2);
      }
      num = num % 13;
      if (num == 0) {
        num = 13;
      }
      var card2 = get.cardPile2(function(card3) {
        return get.number(card3, false) == num;
      });
      if (card2) {
        await player2.gain(card2, "gain2");
      }
    },
    ai: {
      order: 1,
      result: { player: 1 }
    }
  },
  reganlu: {
    enable: "phaseUse",
    usable: 1,
    audio: 2,
    selectTarget: 2,
    delay: 0,
    filterTarget(card2, player2, target) {
      if (target.isMin()) {
        return false;
      }
      if (ui.selected.targets.length == 0) {
        return true;
      }
      if (ui.selected.targets[0].countCards("e") == 0 && target.countCards("e") == 0) {
        return false;
      }
      return target == player2 || ui.selected.targets[0] == player2 || Math.abs(ui.selected.targets[0].countCards("e") - target.countCards("e")) <= player2.maxHp - player2.hp;
    },
    multitarget: true,
    multiline: true,
    async content(event, trigger, player2) {
      const { targets: targets2 } = event;
      targets2[0].swapEquip(targets2[1]);
    },
    ai: {
      order: 10,
      threaten(player2, target) {
        return 0.8 * Math.max(1 + target.maxHp - target.hp);
      },
      result: {
        target(player2, target) {
          var list1 = [];
          var list2 = [];
          var num = player2.maxHp - player2.hp;
          var players = game.filterPlayer();
          for (var i = 0; i < players.length; i++) {
            if (get.attitude(player2, players[i]) > 0) {
              list1.push(players[i]);
            } else if (get.attitude(player2, players[i]) < 0) {
              list2.push(players[i]);
            }
          }
          list1.sort(function(a, b) {
            return a.countCards("e") - b.countCards("e");
          });
          list2.sort(function(a, b) {
            return b.countCards("e") - a.countCards("e");
          });
          var delta;
          for (var i = 0; i < list1.length; i++) {
            for (var j = 0; j < list2.length; j++) {
              delta = list2[j].countCards("e") - list1[i].countCards("e");
              if (delta <= 0) {
                continue;
              }
              if (delta <= num || list1[i] == player2 || list2[j] == player2) {
                if (target == list1[i] || target == list2[j]) {
                  return get.attitude(player2, target);
                }
                return 0;
              }
            }
          }
          return 0;
        }
      }
    }
  },
  //孙休
  mobilexingxue: {
    audio: 2,
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player2) {
      return (player2.storage.mobileyanzhu ? player2.maxHp : player2.hp) > 0;
    },
    async cost(event, trigger, player2) {
      const num = player2.storage.mobileyanzhu ? player2.maxHp : player2.hp;
      event.result = await player2.chooseTarget([1, num], get.prompt2(event.skill)).set("ai", function(target) {
        var att = get.attitude(_status.event.player, target);
        if (target.countCards("he")) {
          return att;
        }
        return att / 10;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const { targets: targets2 } = event;
      await game.doAsyncInOrder(targets2, async (target) => {
        await target.draw();
        if (target.countCards("he")) {
          let result;
          if (!player2.storage.mobileyanzhu || targets2.length == 1) {
            result = await target.chooseCard("选择一张牌置于牌堆顶", "he", true).forResult();
          } else {
            result = await target.chooseCardTarget({
              prompt: "将一张牌置于牌堆顶，或交给其他目标角色",
              filterCard: true,
              position: "he",
              filterTarget(card2, player3, target2) {
                return target2 != player3 && get.event().targets.includes(target2);
              },
              targets: targets2,
              forced: true,
              selectTarget: [0, 1],
              ai1: (card2) => 6 - get.value(card2),
              ai2: (target2) => get.attitude(_status.event.player, target2)
            }).forResult();
          }
          if (result.bool && result.cards?.length) {
            const { cards: cards2, targets: targets3 } = result;
            if (targets3?.length) {
              await target.give(cards2, targets3[0]);
            } else {
              target.$throw(cards2.length, 1e3);
              game.log(target, `将${get.cnNumber(cards2.length)}张牌置于牌堆顶`);
              await target.lose(cards2, ui.cardPile, "insert");
            }
          }
        }
      });
    },
    derivation: "mobilexingxuex"
  },
  mobileyanzhu: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card2, player2, target) {
      return target.countCards("he") > 0 && target != player2;
    },
    async content(event, trigger, player2) {
      const { target } = event;
      let result;
      if (target.countCards("e")) {
        result = await target.chooseBool("是否将装备区内的所有牌交给" + get.translation(player2) + "？", "若选择“取消”，则其将获得你区域里的一张牌").set("ai", function() {
          if (_status.event.effect > 0) {
            return false;
          }
          if (_status.event.player.countCards("e") >= 3) {
            return false;
          }
          return true;
        }).set("effect", get.effect(target, { name: "shunshou" }, player2, target)).forResult();
      } else {
        result = { bool: false };
      }
      if (result.bool) {
        const es = target.getCards("e");
        await target.give(es, player2, "give");
        await player2.removeSkills("mobileyanzhu");
        player2.storage.mobileyanzhu = true;
        player2.markSkill("mobileyanzhu");
        player2.popup("兴学");
        game.log(player2, "修改了技能", "#g【兴学】");
      } else {
        await player2.gainPlayerCard(target, true, "he");
      }
    },
    ai: {
      order: 6,
      result: {
        target(player2, target) {
          var ne = target.countCards("e"), nj = target.countCards("j");
          if (nj) {
            return 2.5;
          }
          if (!ne) {
            return -2;
          }
          if (ne >= 2) {
            return -ne;
          }
          return 0;
        }
      }
    }
  },
  //毛玠
  bingqing: {
    init(player2, skill) {
      player2.addSkill(skill + "_mark");
    },
    onremove(player2, skill) {
      player2.removeSkill(skill + "_mark");
    },
    audio: 2,
    trigger: { player: "useCardAfter" },
    filter(event, player2) {
      const evt = event.getParent("phaseUse");
      if (!evt?.player || evt.player != player2) {
        return false;
      }
      const suit = get.suit(event.card);
      if (!lib.suit.includes(suit)) {
        return false;
      }
      if (player2.getHistory("useCard", (evtx) => {
        return evtx.getParent("phaseUse") == evt && get.suit(evtx.card) == suit;
      }).indexOf(event) != 0) {
        return false;
      }
      return Array.from({ length: 3 }).map((_, i) => i + 2).includes(
        player2.getHistory(
          "useCard",
          (evtx) => {
            return evtx.getParent("phaseUse") == evt && lib.suit.includes(get.suit(evtx.card));
          },
          event
        ).reduce((list, evtx) => list.add(get.suit(evtx.card)), []).length
      );
    },
    async cost(event, trigger, player2) {
      const evt = trigger.getParent("phaseUse");
      const num = player2.getHistory(
        "useCard",
        (evtx) => {
          return evtx.getParent("phaseUse") == evt && lib.suit.includes(get.suit(evtx.card));
        },
        trigger
      ).reduce((list, evtx) => list.add(get.suit(evtx.card)), []).length;
      let prompt, filterTarget, ai;
      switch (num) {
        case 2:
          prompt = "令一名角色摸两张牌";
          filterTarget = function(card2, player3, target) {
            return true;
          };
          ai = function(target) {
            const player3 = get.player();
            let att = get.attitude(player3, target);
            if (target.hasSkill("nogain")) {
              att /= 10;
            }
            return att / Math.sqrt(Math.min(5, 1 + target.countCards("h")));
          };
          break;
        case 3:
          prompt = "弃置一名角色区域内的一张牌";
          filterTarget = function(card2, player3, target) {
            return target.hasCard(function(card3) {
              return lib.filter.canBeDiscarded(card3, player3, target);
            }, "hej");
          };
          ai = function(target) {
            const player3 = get.player();
            return get.effect(target, { name: "guohe_copy" }, player3, player3);
          };
          break;
        case 4:
          prompt = "对一名其他角色造成1点伤害";
          filterTarget = function(card2, player3, target) {
            return target != player3;
          };
          ai = function(target) {
            const player3 = get.player();
            return get.damageEffect(target, player3, player3);
          };
          break;
        default:
          event.result = { bool: false };
          return;
      }
      let result = await player2.chooseTarget(get.prompt(event.skill), prompt, filterTarget).set("ai", ai).forResult();
      result.cost_data = num;
      event.result = result;
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      switch (event.cost_data) {
        case 2:
          await target.draw(2);
          break;
        case 3:
          await player2.discardPlayerCard(target, true, "hej");
          break;
        case 4:
          await target.damage();
          break;
      }
    },
    subSkill: {
      mark: {
        init(player2, skill) {
          const evt = get.event().getParent("phaseUse");
          if (!evt?.player || evt.player != player2) {
            return;
          }
          const suits = player2.getHistory("useCard", (evtx) => evtx.getParent("phaseUse") == evt && lib.suit.includes(get.suit(evtx.card))).map((evtx) => get.suit(evtx.card)).toUniqued();
          if (suits.length) {
            player2.markAuto(skill, suits);
            player2.addTip(
              skill,
              `${get.translation(skill)} ${player2.getStorage(skill).sort((a, b) => lib.suit.indexOf(b) - lib.suit.indexOf(a)).reduce((str, suit) => str + get.translation(suit), "")}`
            );
          }
        },
        charlotte: true,
        onremove(player2, skill) {
          delete player2.storage[skill];
          player2.removeTip(skill);
        },
        trigger: { player: ["useCard1", "phaseUseAfter"] },
        filter(event, player2) {
          if (event.name == "phaseUse") {
            return true;
          }
          const evt = event.getParent("phaseUse");
          if (!evt?.player || evt.player != player2) {
            return false;
          }
          const suit = get.suit(event.card);
          if (!lib.suit.includes(suit)) {
            return false;
          }
          return true;
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player2) {
          if (trigger.name == "phaseUse") {
            delete player2.storage[event.name];
            player2.removeTip(event.name);
            player2.unmarkSkill(event.name);
          } else {
            player2.markAuto(event.name, [get.suit(trigger.card)]);
            player2.addTip(
              event.name,
              `${get.translation(event.name)} ${player2.getStorage(event.name).sort((a, b) => lib.suit.indexOf(b) - lib.suit.indexOf(a)).reduce((str, suit) => str + get.translation(suit), "")}`
            );
          }
        },
        intro: { content: "本阶段已使用$花色" }
      }
    }
  },
  yingfeng: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt2(event.skill), (card2, player3, target) => {
        return !target.hasSkill("yingfeng_mark");
      }).set("ai", (target) => {
        const player3 = get.player();
        let att = get.attitude(player3, target);
        if (att <= 0) {
          return 0;
        }
        let eff = 0.1;
        const preTarget = game.findPlayer((current) => {
          return current != target && current.hasSkill("yingfeng_mark");
        });
        if (preTarget) {
          if (get.attitude(player3, preTarget) < 0) {
            eff += 4;
          } else if (preTarget.hasValueTarget({ name: "sha" }, false) && !preTarget.hasValueTarget({ name: "sha" })) {
            eff -= 3;
          }
        }
        if (target.hasValueTarget({ name: "sha" }, false) && !target.hasValueTarget({ name: "sha" })) {
          eff += 3;
        }
        if (player3 == target) {
          att *= 1.2;
        }
        return 0.01 + att * eff;
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      target.addAdditionalSkill("yingfeng_" + player2.playerid, "yingfeng_mark");
      game.countPlayer((current) => {
        if (current != target && current.hasSkill("yingfeng_mark")) {
          current.removeSkill("yingfeng_mark");
          current.removeAdditionalSkill("yingfeng_" + player2.playerid);
        }
      });
    },
    subSkill: {
      mark: {
        charlotte: true,
        mark: true,
        marktext: "奉",
        mod: {
          targetInRange: () => true
        },
        intro: { content: "使用牌无距离限制" }
      }
    }
  },
  //虞翻
  rezongxuan: {
    inherit: "zongxuan",
    group: "rezongxuan_place"
  },
  rezongxuan_place: {
    audio: "rezongxuan",
    enable: "phaseUse",
    usable: 1,
    sourceSkill: "rezongxuan",
    async content(event, trigger, player2) {
      await player2.draw();
      const result = await player2.chooseCard("he", true, "将一张牌置于牌堆顶").forResult();
      if (result && result.cards) {
        const card2 = result.cards[0];
        await player2.lose(result.cards, ui.cardPile, "insert");
        game.log(player2, "将", get.position(card2) == "h" ? "一张牌" : card2, "置于牌堆顶");
        game.broadcastAll(function(player3) {
          var cardx = ui.create.card();
          cardx.classList.add("infohidden");
          cardx.classList.add("infoflip");
          player3.$throw(cardx, 1e3, "nobroadcast");
        }, player2);
      }
    },
    ai: {
      order: 1,
      result: { player: 1 }
    }
  },
  //孙寒华
  chongxu: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    manualConfirm: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseToPlayBeatmap(lib.skill.chongxu.beatmaps.randomGet()).forResult();
      var score = Math.floor(Math.min(5, result.accuracy / 17));
      game.log(player2, "的演奏评级为", "#y" + result.rank[0], "，获得积分点数", "#y" + score, "分");
      if (score < 3) {
        if (score >= 2) {
          await player2.draw();
        }
        return;
      }
      var list = [];
      if (player2.countMark("miaojian") < 2 && player2.hasSkill("miaojian")) {
        list.push("修改【妙剑】");
      }
      if (player2.countMark("shhlianhua") < 2 && player2.hasSkill("shhlianhua")) {
        list.push("修改【莲华】");
      }
      let result2;
      if (list.length) {
        list.push("全部摸牌");
        result2 = await player2.chooseControl(list).set("prompt", "冲虚：修改技能" + (score == 5 ? "并摸一张牌" : "") + "；或摸" + Math.floor(score / 2) + "张牌").forResult();
      } else {
        result2 = { control: "全部摸牌" };
      }
      if (result2.control != "全部摸牌") {
        score -= 3;
        var skill = result2.control == "修改【妙剑】" ? "miaojian" : "shhlianhua";
        player2.addMark(skill, 1, false);
        game.log(player2, "修改了技能", "#g【" + get.translation(skill) + "】");
      }
      if (score > 1) {
        await player2.draw(Math.floor(score / 2));
      }
    },
    ai: {
      order: 10,
      result: {
        player: 1
      }
    },
    beatmaps: [
      {
        //歌曲名称
        name: "鳥の詩",
        //歌曲文件名（默认在audio/effect文件夹下 若要重定向到扩展 请写为'ext:扩展名称/文件名'的格式）
        filename: "tori_no_uta",
        //每个音符的开始时间点（毫秒，相对未偏移的开始播放时间）
        timeleap: [1047, 3012, 4978, 5469, 5961, 6452, 6698, 7435, 8909, 10875, 12840],
        //开始播放时间的偏移量（毫秒）
        current: -110,
        //判定栏高度（相对整个对话框高度比例）
        judgebar_height: 0.16,
        //Good/Great/Prefect的位置判定范围（百分比，相对于整个对话框。以滑条的底部作为判定基准）
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        //滑条每相对于整个对话框下落1%所需的时间（毫秒）
        speed: 25
      },
      {
        name: "竹取飛翔　～ Lunatic Princess",
        filename: "taketori_hishou",
        timeleap: [1021, 1490, 1959, 2896, 3834, 4537, 4771, 5709, 6646, 7585, 8039, 8494, 9403, 10291, 11180, 11832, 12049, 12920, 13345, 13771, 14196],
        current: -110,
        judgebar_height: 0.16,
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        speed: 25,
        node_color: "linear-gradient(rgba(250, 170, 190, 1), rgba(240, 160, 180, 1))",
        judgebar_color: "linear-gradient(rgba(240, 120, 243, 1), rgba(245, 106, 230, 1))"
      },
      {
        name: "ignotus",
        filename: "ignotus",
        //Number of tracks
        //轨道数量
        number_of_tracks: 4,
        //Customize the track to generate for every note (0 is the first track)
        //自定义每个音符生成的轨道（0是第一个轨道）
        mapping: [0, 2, 3, 1, 1, 0, 3, 0, 0, 3, 0, 0, 2, 1, 2],
        //Convert from beats (0 is the first beat) to timeleap
        //将节拍（0是第一拍）转换为开始时间点
        timeleap: game.generateBeatmapTimeleap(170, [0, 4, 8, 12, 14, 16, 16.5, 23.5, 24, 31, 32, 40, 45, 46, 47]),
        current: -110,
        judgebar_height: 0.16,
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        speed: 25,
        node_color: "linear-gradient(rgba(240, 250, 240, 1), rgba(230, 240, 230, 1))",
        judgebar_color: "linear-gradient(rgba(161, 59, 150, 1), rgba(58, 43, 74, 1))"
      },
      {
        name: "Super Mario 3D World Theme",
        filename: "sm3dw_overworld",
        //Random (Randomly choose tracks to generate notes each play)
        //随机（每次演奏时音符会随机选择轨道生成）
        mapping: "random",
        timeleap: [0, 1071, 1518, 2054, 4018, 4286, 5357, 6429, 7500, 8571, 9643, 10714, 11786, 12321, 12589, 12857, 13929, 15e3, 16071, 17143, 18214, 18482, 18750, 19018, 19286, 20357],
        current: -110,
        judgebar_height: 0.16,
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        speed: 25,
        node_color: "linear-gradient(rgba(120, 130, 240, 1), rgba(100, 100, 230, 1))",
        judgebar_color: "linear-gradient(rgba(230, 40, 30, 1), rgba(220, 30, 10, 1))"
      },
      {
        name: "只因你太美",
        filename: "chicken_you_are_so_beautiful",
        number_of_tracks: 7,
        mapping: [3, 6, 4, 5, 6, 2, 3, 2, 1, 2, 0, 4, 3, 6, 5, 4, 3, 6, 3, 2, 3, 1, 0, 1, 2, 3, 4, 5, 6],
        timeleap: game.generateBeatmapTimeleap(107, [2, 3.5, 4.5, 5.5, 6.5, 8.5, 10, 11.5, 12.5, 13.5, 14.5, 15.5, 18, 19.5, 20.5, 21.5, 22.5, 24.5, 26, 27.5, 28.5, 29.5, 30.5, 31, 31.5, 32, 32.5, 33, 33.5]),
        //Hitsound file name (By default in the audio/effect folder. To redirect to the extension, please write in the format of 'ext:extension_name')
        //打击音文件名（默认在audio/effect文件夹下 若要重定向到扩展 请写为'ext:扩展名称'的格式）
        hitsound: "chickun.wav",
        current: -110,
        judgebar_height: 0.16,
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        speed: 25,
        node_color: "linear-gradient(#99f, #66c)",
        judgebar_color: "linear-gradient(#ccf, #99c)"
      },
      {
        name: "Croatian Rhapsody",
        filename: "croatian_rhapsody",
        mapping: [4, 1, 2, 1, 0, 0, 4, 5, 1, 3, 2, 1, 0, 0],
        timeleap: game.generateBeatmapTimeleap(96, [4, 6, 8, 9, 10, 11, 12, 13.5, 14, 15.5, 16, 17, 18, 19]),
        current: -110,
        judgebar_height: 0.16,
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        speed: 25,
        node_color: "linear-gradient(#fff, #ccc)",
        judgebar_color: "linear-gradient(#fff, #ccc)"
      },
      {
        name: "罗刹海市",
        filename: "rakshasa_sea_city",
        number_of_tracks: 7,
        mapping: "random",
        timeleap: game.generateBeatmapTimeleap(150, [0, 2, 4, 6, 7, 9, 11, 13, 14, 16, 18, 20, 21, 23, 25, 27]),
        current: -110,
        judgebar_height: 0.16,
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        speed: 25,
        node_color: "linear-gradient(#333, #000)",
        judgebar_color: "linear-gradient(#c66, #933)"
      },
      {
        name: "Pigstep (Stereo Mix)",
        filename: "pigstep",
        number_of_tracks: 16,
        timeleap: game.generateBeatmapTimeleap(170, [3, 4, 6, 6.5, 7.5, 11, 12, 14, 14.5, 15.5, 19, 20, 22, 22.5, 23.5, 27, 28, 30, 30.5, 31.5, 35, 36, 38, 38.5, 39.5, 43, 44, 46, 46.5, 47.5, 51, 52, 54, 54.5, 55.5, 59, 60, 62, 62.5]),
        current: -110,
        judgebar_height: 0.16,
        range1: [84, 110],
        range2: [90, 104],
        range3: [94, 100],
        speed: 25,
        node_color: "linear-gradient(#066, #033)",
        judgebar_color: "linear-gradient(#633, #300)"
      }
    ],
    derivation: "chongxu_faq"
  },
  miaojian: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      var level = player2.countMark("miaojian");
      if (event.filterCard({ name: "sha", nature: "stab" }, player2, event)) {
        if (level == 2) {
          return true;
        }
        if (level == 1 && player2.hasCard(function(card2) {
          return get.type2(card2) == "basic";
        }, "hs")) {
          return true;
        }
        if (level == 0 && player2.hasCard(function(card2) {
          return get.name(card2) == "sha";
        }, "hs")) {
          return true;
        }
      }
      if (event.filterCard({ name: "wuzhong" }, player2, event)) {
        if (level == 2) {
          return true;
        }
        if (level == 1 && player2.hasCard(function(card2) {
          return get.type2(card2) != "basic";
        }, "hes")) {
          return true;
        }
        if (level == 0 && player2.hasCard(function(card2) {
          return get.type2(card2) == "trick";
        }, "hs")) {
          return true;
        }
      }
      return false;
    },
    chooseButton: {
      dialog() {
        return ui.create.dialog("妙剑", [
          [
            ["基本", "", "sha", "stab"],
            ["锦囊", "", "wuzhong"]
          ],
          "vcard"
        ]);
      },
      filter(button, player2) {
        var event = _status.event.getParent(), level = player2.countMark("miaojian");
        if (button.link[2] == "sha") {
          if (!event.filterCard({ name: "sha", nature: "stab" }, player2, event)) {
            return false;
          }
          if (level == 2) {
            return true;
          }
          if (level == 1) {
            return player2.hasCard(function(card2) {
              return get.type2(card2) == "basic";
            }, "hs");
          }
          return level == 0 && player2.hasCard(function(card2) {
            return get.name(card2) == "sha";
          }, "hs");
        }
        if (button.link[2] == "wuzhong") {
          if (!event.filterCard({ name: "wuzhong" }, player2, event)) {
            return false;
          }
          if (level == 2) {
            return true;
          }
          if (level == 1) {
            return player2.hasCard(function(card2) {
              return get.type2(card2) != "basic";
            }, "hes");
          }
          return level == 0 && player2.hasCard(function(card2) {
            return get.type2(card2) == "trick";
          }, "hs");
        }
      },
      check(button) {
        var card2 = { name: button.link[2], nature: button.link[3] }, player2 = _status.event.player;
        return get.value(card2, player2) * get.sgn(player2.getUseValue(card2));
      },
      backup(links, player2) {
        var index = links[0][2] == "sha" ? 0 : 1, level = player2.countMark("miaojian");
        var next = {
          audio: "miaojian",
          filterCard: [
            [
              function(card2) {
                return get.name(card2) == "sha";
              },
              function(card2) {
                return get.type(card2) == "basic";
              },
              () => false
            ],
            [
              function(card2) {
                return get.type2(card2) == "trick";
              },
              function(card2) {
                return get.type(card2) != "basic";
              },
              () => false
            ]
          ][index][level],
          position: "hes",
          check(card2) {
            if (card2) {
              return 6.5 - get.value(card2);
            }
            return 1;
          },
          viewAs: [
            {
              name: "sha",
              nature: "stab"
            },
            {
              name: "wuzhong"
            }
          ][index]
        };
        if (level == 2) {
          next.selectCard = -1;
          next.viewAs.isCard = true;
        }
        return next;
      },
      prompt(links, player2) {
        var index = links[0][2] == "sha" ? 0 : 1, level = player2.countMark("miaojian");
        return [
          ["将一张【杀】当做刺【杀】使用", "将一张基本牌当做刺【杀】使用", "请选择刺【杀】的目标"],
          ["将一张锦囊牌当做【无中生有】使用", "将一张非基本牌当做【无中生有】使用", "请选择【无中生有】的目标"]
        ][index][level];
      }
    },
    onremove: true,
    derivation: ["miaojian1", "miaojian2"],
    subSkill: { backup: { audio: "miaojian" } },
    ai: {
      order: 7,
      result: { player: 1 }
    }
  },
  shhlianhua: {
    audio: 2,
    derivation: ["shhlianhua1", "shhlianhua2"],
    trigger: { target: "useCardToTarget" },
    forced: true,
    filter(event, player2) {
      return event.card.name == "sha";
    },
    async content(event, trigger, player2) {
      await player2.draw();
      const level = player2.countMark("shhlianhua");
      if (!level) {
        return;
      } else if (level == 2) {
        const eff = get.effect(player2, trigger.card, trigger.player, trigger.player);
        const result = await trigger.player.chooseToDiscard("he", `${get.translation(player2)}发动了【莲华】`, `你可以弃置一张牌，或令${get.translation(trigger.card)}对其无效`).set("ai", (card2) => {
          if (get.event().eff > 0) {
            return 10 - get.value(card2);
          }
          return 0;
        }).set("eff", eff).forResult();
        if (!result?.bool) {
          trigger.targets.remove(player2);
          trigger.getParent().triggeredTargets2.remove(player2);
          trigger.untrigger();
        }
      } else {
        const result = await player2.judge((card2) => {
          return get.suit(card2) == "spade" ? 1 : -1;
        }).set("judge2", (result2) => result2.bool).forResult();
        if (result?.bool) {
          trigger.targets.remove(player2);
          trigger.getParent().triggeredTargets2.remove(player2);
          trigger.untrigger();
        }
      }
    },
    ai: {
      effect: {
        target_use(card2, player2, target, current) {
          if (card2.name == "sha" && current < 0) {
            return 0.7;
          }
        }
      }
    }
  },
  //阎圃
  huantu: {
    audio: 2,
    trigger: { global: "phaseDrawBefore" },
    round: 1,
    filter(event, player2) {
      return player2.countCards("he") > 0 && player2.inRange(event.player);
    },
    checkx(event, player2) {
      const target = event.player;
      return get.attitude(player2, target) > 0 && (target.hasSkill("pingkou") || target.skipList.includes("phaseUse") || target.isDamaged() && target.hp <= 2 || target.needsToDiscard());
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseCard(get.prompt(event.skill, trigger.player), "交给其一张牌并令其暂时跳过摸牌阶段", "he").set("ai", function(card2) {
        if (!_status.event.checkx) {
          return 0;
        }
        return 1 + Math.random();
      }).set("checkx", lib.skill.huantu.checkx(trigger, player2)).forResult();
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      const target = event.targets[0], { cards: cards2 } = event;
      await player2.give(cards2, target);
      trigger.cancel();
      player2.addTempSkill(event.name + "_effect");
    },
    subSkill: {
      effect: {
        audio: "huantu",
        trigger: { global: "phaseJieshuBegin" },
        forced: true,
        charlotte: true,
        logTarget: "player",
        filter(event, player2) {
          return event.player.isIn();
        },
        async content(event, trigger, player2) {
          const target = trigger.player, str = get.translation(target);
          const result = await player2.chooseControl().set("choiceList", ["令" + str + "回复1点体力并摸两张牌", "摸三张牌，然后交给" + str + "两张手牌"]).set("choice", target.isDamaged() ? 0 : 1).forResult();
          if (result?.index == 0) {
            await target.recover();
            await target.draw(2);
          } else if (result?.index == 1) {
            await player2.draw(3);
            if (player2.countCards("h") && target.isIn()) {
              await player2.chooseToGive(target, 2, true, "h");
            }
          }
        }
      }
    }
  },
  bihuo: {
    audio: 2,
    trigger: { global: "dyingAfter" },
    logTarget: "player",
    limited: true,
    skillAnimation: true,
    animationColor: "gray",
    filter(event, player2) {
      return event.player.isIn();
    },
    check(event, player2) {
      return get.attitude(player2, event.player) > 0;
    },
    async content(event, trigger, player2) {
      const target = trigger.player;
      player2.awakenSkill(event.name);
      await target.draw(3);
      target.addTempSkill("bihuo_effect", "roundStart");
      target.addMark("bihuo_effect", game.countPlayer(), false);
    },
    subSkill: {
      effect: {
        onremove: true,
        charlotte: true,
        mod: {
          globalTo(from, to, distance) {
            return distance + to.countMark("bihuo_effect");
          }
        },
        intro: { content: "其他角色至你的距离+#" }
      }
    }
  },
  //马元义
  jibing: {
    audio: 2,
    enable: ["chooseToUse", "chooseToRespond"],
    filter(event, player2) {
      return player2.getExpansions("jibing").length > 0 && (event.filterCard(get.autoViewAs({ name: "sha" }, "unsure"), player2, event) || event.filterCard(get.autoViewAs({ name: "shan" }, "unsure"), player2, event));
    },
    chooseButton: {
      dialog(event, player2) {
        var dialog = ui.create.dialog("集兵", "hidden");
        if (event.filterCard(get.autoViewAs({ name: "sha" }, "unsure"), player2, event) && event.filterCard(get.autoViewAs({ name: "shan" }, "unsure"), player2, event)) {
          dialog._chooseButton = 2;
          var list = ["sha", "shan"];
          dialog.add([
            list.map((i) => {
              return [i, get.translation(i)];
            }),
            "tdnodes"
          ]);
        } else {
          dialog._cardName = event.filterCard(get.autoViewAs({ name: "sha" }, "unsure"), player2, event) ? "sha" : "shan";
        }
        dialog.add(player2.getExpansions("jibing"));
        return dialog;
      },
      filter(button) {
        var evt = _status.event, player2 = _status.event.player;
        if (evt.dialog) {
          if (!evt.dialog._chooseButton) {
            var evt2 = _status.event.getParent();
            return evt2.filterCard(get.autoViewAs({ name: evt.dialog._cardName }, [button.link]), player2, evt2);
          }
          if (ui.selected.buttons.length) {
            var str = ui.selected.buttons[0].link;
            if (typeof str != "string" || typeof button.link == "string") {
              return false;
            }
            var evt2 = _status.event.getParent();
            return evt2.filterCard(get.autoViewAs({ name: str }, [button.link]), player2, evt2);
          }
          return typeof button.link == "string";
        }
        return false;
      },
      select() {
        return _status.event.dialog ? _status.event.dialog._chooseButton || 1 : 1;
      },
      backup(links, player2) {
        var card2, name;
        if (links.length == 2) {
          name = links[0];
          card2 = links[1];
        } else {
          card2 = links[0];
          var event = _status.event;
          name = event.filterCard(get.autoViewAs({ name: "sha" }, [card2]), player2, event) ? "sha" : "shan";
        }
        return {
          audio: "jibing",
          filterCard(card3) {
            return card3 == lib.skill.jibing_backup.card;
          },
          selectCard: -1,
          position: "x",
          viewAs: { name },
          card: card2
        };
      },
      prompt(links, player2) {
        return "请选择【杀】的目标";
      }
    },
    ai: {
      respondSha: true,
      respondShan: true,
      skillTagFilter(player2, tag, arg) {
        return player2.getExpansions("jibing").length > 0;
      },
      order(item, player2) {
        if (player2.hasSkill("binghuo")) {
          return 6;
        }
        return 1;
      },
      result: {
        player: 1
      }
    },
    group: "jibing_place",
    subSkill: {
      place: {
        audio: "jibing",
        trigger: { player: "phaseDrawBegin1" },
        prompt2: "摸牌阶段开始时，若你的“兵”数小于势力数，则你可以改为将牌堆顶的两张牌置于你的武将牌上，称为“兵”。",
        filter(event, player2) {
          return !event.numFixed && player2.getExpansions("jibing").length < game.countGroup();
        },
        async content(event, trigger, player2) {
          trigger.changeToZero();
          var cards2 = get.cards(2);
          player2.addToExpansion(cards2, "gain2").gaintag.add("jibing");
        }
      },
      backup: { audio: "jibing" }
    },
    intro: { content: "expansion", markcount: "expansion" },
    onremove(player2, skill) {
      var cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    }
  },
  wangjing: {
    audio: 2,
    trigger: { player: ["useCard", "respond"] },
    filter(event, player2) {
      if (event.skill != "jibing_backup") {
        return false;
      }
      var target = lib.skill.wangjing.logTarget(event, player2);
      return target && target.isMaxHp();
    },
    logTarget(event, player2) {
      if (event.name == "respond") {
        return event.source;
      }
      if (event.card.name == "sha") {
        return event.targets[0];
      }
      return event.respondTo[0];
    },
    forced: true,
    async content(event, trigger, player2) {
      player2.draw();
    },
    ai: {
      combo: "jibing",
      mingzhi: false,
      effect: {
        target(card2, player2, target, current) {
          if ((get.tag(card2, "respondShan") || get.tag(card2, "respondSha")) && target.getExpansions("jibing").length > 0 && player2.isMaxHp()) {
            if (get.attitude(target, player2) <= 0) {
              return [0, 0, 1, 0.3];
            }
          }
        }
      }
    }
  },
  moucuan: {
    audio: 2,
    derivation: "binghuo",
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    juexingji: true,
    skillAnimation: true,
    animationColor: "metal",
    filter(event, player2) {
      return player2.getExpansions("jibing").length >= game.countGroup();
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      player2.loseMaxHp();
      player2.addSkills("binghuo");
    },
    ai: { combo: "jibing" }
  },
  binghuo: {
    audio: 2,
    trigger: { global: "phaseJieshuBegin" },
    direct: true,
    filter(event, player2) {
      return player2.hasHistory("useCard", function(evt) {
        return evt.skill == "jibing_backup";
      }) || player2.hasHistory("respond", function(evt) {
        return evt.skill == "jibing_backup";
      });
    },
    async content(event, trigger, player2) {
      const chooseResult = await player2.chooseTarget(get.prompt2("binghuo")).set("ai", function(target2) {
        var player3 = _status.event.player;
        return get.damageEffect(target2, player3, player3);
      }).forResult();
      if (!chooseResult.bool) {
        return;
      }
      const target = chooseResult.targets[0];
      player2.logSkill("binghuo", target);
      const judgeResult = await target.judge(function(card2) {
        if (get.color(card2) == "black") {
          return -2;
        }
        return 0.1;
      }).set("judge2", function(result) {
        return result.bool === false ? true : false;
      }).forResult();
      if (judgeResult.bool == false) {
        await target.damage("thunder");
      }
    },
    ai: { combo: "jibing", expose: 0.2 }
  },
  //傅佥
  jueyong: {
    audio: 2,
    trigger: { target: "useCardToTarget" },
    forced: true,
    filter(event, player2) {
      return event.card.name != "jiu" && event.card.name != "tao" && event.targets.length == 1 && event.card.isCard && event.cards.length == 1 && event.getParent(2).name != "jueyong_timeout" && get.position(event.cards[0], true) == "o" && event.card.name == event.cards[0].name && (!player2.storage.jueyong || player2.storage.jueyong[0].length < player2.getHp());
    },
    async content(event, trigger, player2) {
      trigger.targets.remove(player2);
      trigger.getParent().triggeredTargets2.remove(player2);
      trigger.untrigger();
      var card2 = trigger.cards[0];
      player2.addToExpansion(card2, "gain2").gaintag.add("jueyong");
      if (!player2.storage.jueyong) {
        player2.storage.jueyong = [[], []];
      }
      player2.storage.jueyong[0].push(card2);
      player2.storage.jueyong[1].push(trigger.player);
      game.delayx();
    },
    onremove(player2, skill) {
      var cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
      delete player2.storage[skill];
    },
    intro: {
      markcount(storage) {
        if (!storage) {
          return 0;
        }
        return storage[0].length;
      },
      mark(dialog, storage, player2) {
        if (!storage) {
          return;
        }
        dialog.addAuto(storage[0]);
        dialog.addText(get.translation(storage[1]));
      },
      onunmark(storage, player2) {
        player2.storage.jueyong = [[], []];
      }
    },
    ai: {
      reverseEquip: true,
      effect: {
        target_use(card2, player2, target, current) {
          if (get.type(card2) == "equip" && !get.tag(card2, "gifts") && target.storage.jueyong && target.storage.jueyong[1].length) {
            var result1 = get.equipResult(player2, target, card2), subtype = get.subtype(card2);
            for (var i of target.storage.jueyong[0]) {
              if (get.subtype(i, false) == subtype && get.equipResult(target, target, i) >= result1) {
                return "zerotarget";
              }
            }
          }
        }
      }
    },
    group: "jueyong_timeout",
    subSkill: {
      timeout: {
        audio: "jueyong",
        trigger: { player: "phaseJieshuBegin" },
        forced: true,
        filter(event, player2) {
          return player2.storage.jueyong && player2.storage.jueyong[0].length > 0;
        },
        async content(event, trigger, player2) {
          var list = player2.storage.jueyong, card2 = list[0].shift(), source = list[1].shift();
          if (player2.getExpansions("jueyong").includes(card2)) {
            if (source && source.isIn() && source.canUse(card2, player2, false)) {
              source.useCard(card2, player2, false);
            } else {
              player2.loseToDiscardpile(card2);
            }
          }
          if (list[0].length) {
            event.redo();
          }
        }
      }
    }
  },
  poxiang: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter: (event, player2) => player2.countCards("he") > 0,
    filterCard: true,
    filterTarget: lib.filter.notMe,
    position: "he",
    discard: false,
    lose: false,
    delay: false,
    check(card2) {
      var player2 = _status.event.player;
      if (!player2.storage.jueyong || !player2.storage.jueyong[0].length || player2.hp <= 1 && !player2.storage.jueyong[0].some(function(card3) {
        return get.tag(card3, "damage") > 0;
      }) || !player2.storage.jueyong[0].some(function(card3) {
        return get.effect(player2, card3, player2.storage.jueyong[1][player2.storage.jueyong[0].indexOf(card3)], player2) < 0;
      })) {
        return -1;
      }
      return 20 - get.value(card2);
    },
    async content(event, trigger, player2) {
      const { cards: cards2 } = event;
      const target = event.targets[0];
      await player2.give(cards2, target);
      const drawEvent = player2.draw(3);
      drawEvent.gaintag = ["poxiang"];
      await drawEvent;
      player2.addTempSkill("poxiang_mark");
      const expansionCards = player2.getExpansions("jueyong");
      if (expansionCards.length) {
        await player2.loseToDiscardpile(expansionCards);
      }
      player2.unmarkSkill("jueyong");
      await player2.loseHp();
      await game.delayx();
    },
    ai: {
      order: 12,
      result: {
        player: 4,
        target: 1
      }
    },
    subSkill: {
      mark: {
        charlotte: true,
        onremove(player2) {
          player2.removeGaintag("poxiang");
        },
        mod: {
          ignoredHandcard(card2, player2) {
            if (card2.hasGaintag("poxiang")) {
              return true;
            }
          },
          cardDiscardable(card2, player2, name) {
            if (name == "phaseDiscard" && card2.hasGaintag("poxiang")) {
              return false;
            }
          }
        }
      }
    }
  },
  //曹真
  disordersidi: { audio: 2 },
  discretesidi: {
    audio: "disordersidi",
    trigger: { player: "useCardAfter" },
    direct: true,
    filter(event, player2) {
      return get.type(event.card, null, false) != "delay" && game.hasPlayer(function(current) {
        return player2 != current && (!player2.storage.discretesidi || !player2.storage.discretesidi.includes(current));
      });
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt("discretesidi"), "选择两名角色a,b建立二元序偶<a,b>，或仅选择一名角色，建立二元序偶<a,a>", [1, 2], function(card2, player3, target) {
        if (ui.selected.targets.length) {
          return true;
        }
        return target != player3 && (!player3.storage.discretesidi || !player3.storage.discretesidi.includes(target));
      }).set("complexTarget", true).set("complexSelect", true).set("targetprompt", ["第一元素", "第二元素"]).set("ai", function(target) {
        _status.event.player;
        if (!ui.selected.targets.length) {
          if (target.getEnemies().length == 1) {
            return 2 + Math.random();
          }
          return 1 + Math.random();
        }
        var targetx = ui.selected.targets[0];
        if (targetx.getEnemies().includes(target) && targetx.inRange(target)) {
          return Math.random() - 0.5;
        }
        return 0;
      }).set("animate", false).forResult();
      if (result.bool && result.targets.length) {
        const targets2 = result.targets;
        player2.logSkill("discretesidi", targets2[0]);
        if (targets2.length == 1) {
          targets2.push(targets2[0]);
        }
        if (!player2.storage.discretesidi) {
          player2.storage.discretesidi = [];
        }
        if (!player2.storage.discretesidi2) {
          player2.storage.discretesidi2 = [];
        }
        player2.storage.discretesidi.push(targets2[0]);
        player2.storage.discretesidi2.push(targets2[1]);
        player2.markSkill("discretesidi");
        await game.delayx();
      }
    },
    intro: {
      content(storage, player2) {
        if ((player2 == game.me || player2.isUnderControl()) && !game.observe) {
          var str = "R={ ";
          for (var i = 0; i < storage.length; i++) {
            str += "&lt;" + get.translation(storage[i]) + ", " + get.translation(player2.storage.discretesidi2[i]) + "&gt;";
            if (i < storage.length - 1) {
              str += ", ";
            }
          }
          str += " }";
          return str;
        }
        return "已指定" + get.translation(storage) + "为目标";
      }
    },
    onremove(player2) {
      delete player2.storage.discretesidi;
      delete player2.storage.discretesidi2;
    },
    group: ["discretesidi_clear", "discretesidi_exec"],
    subSkill: {
      clear: {
        trigger: { global: ["useCardToPlayered", "die"] },
        forced: true,
        popup: false,
        locked: false,
        filter(event, player2) {
          if (!player2.storage.discretesidi || !player2.storage.discretesidi.includes(event.player)) {
            return false;
          }
          if (event.name == "die") {
            return true;
          }
          if (get.type(event.card, null, false) != "delay") {
            var index = player2.storage.discretesidi.indexOf(event.player);
            return index != -1 && (player2.storage.discretesidi2[index] != event.target || event.targets.length != 1);
          }
          return false;
        },
        async content(event, trigger, player2) {
          player2.storage.discretesidi2.splice(player2.storage.discretesidi.indexOf(trigger.player), 1);
          player2.unmarkAuto("discretesidi", [trigger.player]);
        }
      },
      exec: {
        audio: "disordersidi",
        trigger: { global: "useCardToPlayered" },
        forced: true,
        locked: false,
        filter(event, player2) {
          if (get.type(event.card, null, false) == "delay" || !player2.storage.discretesidi || event.targets.length != 1) {
            return false;
          }
          var index = player2.storage.discretesidi.indexOf(event.player);
          return index != -1 && player2.storage.discretesidi2[index] == event.target;
        },
        logTarget: "player",
        async content(event, trigger, player2) {
          player2.storage.discretesidi2.splice(player2.storage.discretesidi.indexOf(trigger.player), 1);
          player2.unmarkAuto("discretesidi", [trigger.player]);
          if (trigger.target == player2) {
            await player2.draw();
            return;
          }
          const target = trigger.player;
          const result = await player2.chooseControl("cancel2").set("choiceList", ["取消" + get.translation(trigger.card) + "的所有目标并对" + get.translation(target) + "造成1点伤害", "摸两张牌"]).set("ai", function() {
            var player3 = _status.event.player, evt = _status.event.getTrigger();
            if (get.damageEffect(evt.player, player3, player3) > 0 && get.effect(evt.target, evt.card, evt.player, player3) < 0) {
              return 0;
            }
            return 1;
          }).forResult();
          if (result.index == 0) {
            trigger.cancel();
            trigger.targets.length = 0;
            trigger.getParent().triggeredTargets1.length = 0;
            if (!_status.dying.length) {
              await target.damage();
            }
          } else if (result.index == 1) {
            await player2.draw(2);
          }
        }
      }
    }
  },
  //数学家
  mbsidi: {
    audio: "disordersidi",
    trigger: { player: "useCardAfter" },
    filter(event, player2) {
      return get.type(event.card, false) != "delay" && game.hasPlayer(function(current) {
        return player2 != current && (!player2.storage.mbsidi || !player2.storage.mbsidi.includes(current));
      });
    },
    direct: true,
    async content(event, trigger, player2) {
      const result1 = await player2.chooseTarget(get.prompt("mbsidi"), "选择一名角色，为其选择一名“司敌”目标角色", function(card2, player3, target2) {
        return target2 != player3 && (!player3.storage.mbsidi || !player3.storage.mbsidi.includes(target2));
      }).set("ai", function(target2) {
        _status.event.player;
        if (target2.getEnemies().length == 1) {
          return 2 + Math.random();
        }
        return 1 + Math.random();
      }).set("animate", false).forResult();
      if (!result1.bool) {
        return;
      }
      const target = result1.targets[0];
      const result2 = await player2.chooseTarget("为" + get.translation(target) + "选择一名“司敌”目标角色").set("ai", function(target2) {
        _status.event.player;
        var targetx = _status.event.target;
        if (targetx.getEnemies().includes(target2) && targetx.inRange(target2)) {
          return Math.random() + 1.5;
        }
        return targetx == target2 ? 1 : -1;
      }).set("target", target).set("animate", false).forResult();
      if (result2.bool) {
        result2.targets.unshift();
        player2.logSkill("mbsidi", target);
        if (!player2.storage.mbsidi) {
          player2.storage.mbsidi = [];
        }
        if (!player2.storage.mbsidi2) {
          player2.storage.mbsidi2 = [];
        }
        player2.storage.mbsidi.push(target);
        player2.storage.mbsidi2.push(result2.targets[0]);
        player2.markSkill("mbsidi");
        await game.delayx();
      }
    },
    intro: {
      content(storage, player2) {
        if ((player2 == game.me || player2.isUnderControl()) && !game.observe) {
          var storage2 = player2.storage.mbsidi2, str = "";
          for (var i = 0; i < storage.length; i++) {
            str += get.translation(storage[i]) + "=>" + get.translation(storage2[i]);
            if (i < storage.length - 1) {
              str += "<br>";
            }
          }
          return str;
        }
        return "已指定" + get.translation(storage) + "为目标";
      }
    },
    onremove(player2) {
      delete player2.storage.mbsidi;
      delete player2.storage.mbsidi2;
    },
    group: ["mbsidi_clear", "mbsidi_exec"],
    subSkill: {
      clear: {
        trigger: { global: ["useCardToPlayered", "die"] },
        filter(event, player2) {
          if (!player2.storage.mbsidi || !player2.storage.mbsidi.includes(event.player)) {
            return false;
          }
          if (event.name == "die") {
            return true;
          }
          if (get.type(event.card, false) != "delay") {
            var index = player2.storage.mbsidi.indexOf(event.player);
            return index != -1 && (player2.storage.mbsidi2[index] != event.target || event.targets.length != 1);
          }
          return false;
        },
        forced: true,
        locked: false,
        popup: false,
        async content(event, trigger, player2) {
          player2.storage.mbsidi2.splice(player2.storage.mbsidi.indexOf(trigger.player), 1);
          player2.unmarkAuto("mbsidi", [trigger.player]);
        }
      },
      exec: {
        audio: "disordersidi",
        trigger: { global: "useCardToPlayered" },
        filter(event, player2) {
          if (get.type(event.card, false) == "delay" || !player2.storage.mbsidi || event.targets.length != 1) {
            return false;
          }
          var index = player2.storage.mbsidi.indexOf(event.player);
          return index != -1 && player2.storage.mbsidi2[index] == event.target;
        },
        logTarget: "player",
        forced: true,
        locked: false,
        async content(event, trigger, player2) {
          player2.storage.mbsidi2.splice(player2.storage.mbsidi.indexOf(trigger.player), 1);
          player2.unmarkAuto("mbsidi", [trigger.player]);
          if (trigger.target == player2) {
            await player2.draw();
            return;
          }
          const target = trigger.player;
          const result = await player2.chooseControl("cancel2").set("choiceList", ["取消" + get.translation(trigger.card) + "的所有目标" + (_status.dying.length ? "" : "，然后对" + get.translation(target) + "造成1点伤害"), "摸两张牌"]).set("ai", function() {
            var player3 = _status.event.player, evt = _status.event.getTrigger();
            if (get.damageEffect(evt.player, player3, player3) > 0 && get.effect(evt.target, evt.card, evt.player, player3) < 0) {
              return 0;
            }
            return 1;
          }).forResult();
          if (result.index == 0) {
            trigger.cancel();
            trigger.targets.length = 0;
            trigger.getParent().triggeredTargets1.length = 0;
            if (!_status.dying.length) {
              await target.damage();
            }
          } else if (result.index == 1) {
            await player2.draw(2);
          }
        }
      }
    }
  },
  //孙鲁班
  xinzenhui: {
    audio: 2,
    trigger: { player: "useCardToPlayer" },
    filter(event, player2) {
      if (event.targets.length != 1) {
        return false;
      }
      var card2 = event.card;
      if (card2.name != "sha" && (get.type(card2, null, false) != "trick" || get.color(card2, false) != "black")) {
        return false;
      }
      if (!player2.isPhaseUsing() || player2.hasSkill("xinzenhui2")) {
        return false;
      }
      return game.hasPlayer(function(current) {
        return current != player2 && current != event.target && lib.filter.targetEnabled2(card2, player2, current) && lib.filter.targetInRange(card2, player2, current);
      });
    },
    direct: true,
    async content(event, trigger, player2) {
      const result1 = await player2.chooseTarget(get.prompt2("xinzenhui"), function(card2, player3, target2) {
        if (player3 == target2) {
          return false;
        }
        var evt = _status.event.getTrigger();
        return !evt.targets.includes(target2) && lib.filter.targetEnabled2(evt.card, player3, target2) && lib.filter.targetInRange(evt.card, player3, target2);
      }).set("ai", function(target2) {
        var trigger2 = _status.event.getTrigger();
        var player3 = _status.event.player;
        return Math.max(target2.countGainableCards(player3, "he") ? get.effect(target2, { name: "shunshou_copy2" }, player3, player3) : 0, get.effect(target2, trigger2.card, player3, player3));
      }).forResult();
      if (!result1.bool) {
        return;
      }
      player2.addTempSkill("xinzenhui2", "phaseUseAfter");
      const target = result1.targets[0];
      const str = get.translation(target);
      event.target = target;
      player2.logSkill("xinzenhui", target);
      let result2 = { index: 0 };
      if (target.countGainableCards(player2, "he")) {
        result2 = await player2.chooseControl().set("choiceList", ["令" + str + "也成为" + get.translation(trigger.card) + "的目标", "获得" + str + "的一张牌，然后其成为" + get.translation(trigger.card) + "的使用者"]).set("ai", function() {
          var trigger2 = _status.event.getTrigger();
          var player3 = _status.event.player, target2 = _status.event.getParent().target;
          return (target2.countGainableCards(player3, "he") ? get.effect(target2, { name: "shunshou_copy2" }, player3, player3) : 0) > get.effect(target2, trigger2.card, player3, player3) ? 1 : 0;
        }).forResult();
      }
      if (result2.index == 1) {
        trigger.untrigger();
        trigger.getParent().player = target;
        game.log(target, "成为了", trigger.card, "的使用者");
        await player2.gainPlayerCard(target, true, "he");
      } else {
        game.log(target, "成为了", trigger.card, "的额外目标");
        trigger.getParent().targets.push(target);
      }
    }
  },
  xinzenhui2: {},
  xinjiaojin: {
    audio: 2,
    trigger: { player: "damageBegin4" },
    filter(event, player2) {
      return player2.countCards("he", { type: "equip" }) && event.source && event.source.hasSex("male");
    },
    direct: true,
    async content(event, trigger, player2) {
      const next = player2.chooseToDiscard("he", "骄矜：是否弃置一张装备牌防止伤害？", function(card2, player3) {
        return get.type(card2) == "equip";
      });
      next.set("ai", function(card2) {
        var player3 = _status.event.player;
        if (player3.hp == 1 || _status.event.getTrigger().num > 1) {
          return 9 - get.value(card2);
        }
        if (player3.hp == 2) {
          return 8 - get.value(card2);
        }
        return 7 - get.value(card2);
      });
      next.logSkill = "xinjiaojin";
      const result = await next.forResult();
      if (result.bool) {
        await game.delay(0.5);
        trigger.cancel();
      }
    }
  },
  //谯周
  zhiming: {
    audio: 2,
    trigger: { player: ["phaseZhunbeiBegin", "phaseDiscardEnd"] },
    frequent: true,
    async content(event, trigger, player2) {
      await player2.draw();
      if (player2.countCards("he") > 0) {
        const next = player2.chooseCard("he", "知命：是否将一张牌置于牌堆顶？");
        if (trigger.name == "phaseZhunbei") {
          next.set("ai", function(card2) {
            var player3 = _status.event.player, js = player3.getCards("j");
            if (js.length) {
              var judge = get.judge(js[0]);
              if (judge && judge(card2) >= 0) {
                return 20 - get.value(card2);
              }
            }
            return 0;
          });
        } else {
          next.set("ai", function(card2) {
            var player3 = _status.event.player, js = player3.next.getCards("j");
            if (js.length) {
              var judge = get.judge(js[0]);
              if (judge && (judge(card2) + 0.01) * get.attitude(player3, player3.next) > 0) {
                return 20 - get.value(card2);
              }
            }
            return 0;
          });
        }
        const result = await next.forResult();
        if (result.bool && result.cards?.length) {
          player2.$throw(get.position(result.cards[0]) == "e" ? result.cards[0] : 1, 1e3);
          game.log(player2, "将", get.position(result.cards[0]) == "e" ? result.cards[0] : "#y一张手牌", "置于了牌堆顶");
          await player2.lose(result.cards, ui.cardPile, "insert");
          await game.delayx();
        }
      }
    },
    ai: { guanxing: true }
  },
  xingbu: {
    audio: 2,
    trigger: { player: "phaseJieshuBegin" },
    prompt2: "亮出牌堆顶的三张牌，并可以根据其中红色牌的数量，令一名其他角色获得一种效果",
    async content(event, trigger, player2) {
      const cards2 = get.cards(3, true);
      await player2.showCards(cards2, get.translation(player2) + "发动了【星卜】", true).set("clearArena", false);
      let num = cards2.filter((card2) => get.color(card2, false) == "red").length;
      event.num = num;
      let prompt = "";
      if (num <= 1) {
        prompt = "其于准备阶段开始时弃置一张手牌";
      } else if (num == 2) {
        prompt = "其使用【杀】的次数上限-1，跳过弃牌阶段";
      } else {
        prompt = "其摸牌阶段多摸两张牌，使用【杀】的次数上限+1";
      }
      if (!game.hasPlayer((current) => player2 != current)) {
        return;
      }
      const result = await player2.chooseTarget("星卜：你可以一名其他角色直到其回合结束获得以下效果", prompt, lib.filter.notMe).set("ai", (target) => {
        const player3 = get.player(), num2 = get.event().getParent().num;
        let att = get.attitude(player3, target);
        if (num2 <= 1) {
          return -att * (target.countCards("h") + 0.1);
        }
        if (num2 == 2) {
          if (target.hasJudge("lebu")) {
            if (target.needsToDiscard() > 2) {
              return att;
            }
            return -1.4 * att;
          }
          if (att < 0 && target.countCards("h") <= 3) {
            return -1.4 * att;
          }
          return 0;
        }
        if (num2 == 3) {
          return att;
        }
        return 0;
      }).forResult();
      game.broadcastAll(ui.clear);
      if (result?.bool && result.targets?.length) {
        const skill = "xingbu_effect" + Math.max(1, num), target = result.targets[0];
        player2.line(target, "green");
        game.log(player2, "选择了", target);
        target.addTempSkill(skill, { player: "phaseEnd" });
        target.addMark(skill, 1, false);
        await game.delayx();
      }
    },
    subSkill: {
      effect1: {
        charlotte: true,
        onremove: true,
        intro: { content: "准备阶段开始时弃置#张手牌" },
        trigger: { player: "phaseZhunbeiBegin" },
        forced: true,
        filter(event, player2) {
          return player2.countCards("h") > 0;
        },
        async content(event, trigger, player2) {
          await player2.chooseToDiscard("h", true, player2.countMark(event.name));
        }
      },
      effect2: {
        charlotte: true,
        onremove: true,
        intro: { content: "使用【杀】的次数上限-#，跳过弃牌阶段" },
        mod: {
          cardUsable(card2, player2, num) {
            if (card2.name == "sha") {
              return num - player2.countMark("xingbu_effect2");
            }
          }
        },
        trigger: { player: "phaseDiscardBegin" },
        forced: true,
        async content(event, trigger, player2) {
          trigger.cancel();
        }
      },
      effect3: {
        charlotte: true,
        onremove: true,
        intro: { content: "摸牌阶段多摸2*#张牌，使用【杀】的次数上限+#。" },
        trigger: { player: ["phaseDrawBegin2"] },
        forced: true,
        filter(event, player2) {
          return !event.numFixed;
        },
        async content(event, trigger, player2) {
          if (trigger.name == "phaseDraw") {
            trigger.num += player2.countMark(event.name) * 2;
          }
        },
        mod: {
          cardUsable(card2, player2, num) {
            if (card2.name == "sha") {
              return num + player2.countMark("xingbu_effect3");
            }
          }
        }
      }
    }
  },
  //顾雍
  xinshenxing: {
    audio: 2,
    enable: "phaseUse",
    usable(skill, player2) {
      return player2.hp;
    },
    filter(event, player2) {
      return player2.countCards("he") > 1;
    },
    selectCard: 2,
    position: "he",
    check(card2) {
      if (!ui.selected.cards.length || get.color(card2) != get.color(ui.selected.cards[0])) {
        return 6.5 - get.value(card2);
      }
      return 6.5 - get.value(card2) - get.value(ui.selected.cards[0]);
    },
    filterCard: lib.filter.cardDiscardable,
    async content(event, trigger, player2) {
      const { cards: cards2 } = event;
      player2.draw(get.color(cards2) == "none" ? 2 : 1);
    },
    ai: {
      order: 1,
      result: { player: 1 }
    }
  },
  xinbingyi: {
    audio: "bingyi",
    audioname: ["xin_guyong"],
    trigger: { player: "phaseJieshuBegin" },
    filter(event, player2) {
      return player2.countCards("h") > 0;
    },
    filterx(event, player2) {
      const cards2 = player2.getCards("h");
      if (cards2.length == 1) {
        return true;
      }
      const colors = cards2.map((card2) => get.color(card2, player2)).unique();
      const types = cards2.map((card2) => get.type2(card2, player2)).unique();
      return colors?.length == 1 || types?.length == 1;
    },
    prompt2(event, player2) {
      if (lib.skill.xinbingyi.filterx(event, player2)) {
        return `展示所有手牌，并选择至多${get.cnNumber(player2.countCards("h"))}名角色各摸一张牌`;
      }
      return "展示所有手牌，然后无事发生！！";
    },
    async content(event, trigger, player2) {
      await player2.showHandcards(get.translation(player2) + "发动了〖秉壹〗");
      if (lib.skill.xinbingyi.filterx(trigger, player2)) {
        const result = await player2.chooseTarget(`秉壹：选择至多${get.cnNumber(player2.countCards("h"))}名角色各摸一张牌`, [1, player2.countCards("h")]).set("ai", function(target) {
          return get.attitude(get.player(), target);
        }).forResult();
        if (result.bool) {
          const targets2 = result.targets.sortBySeat();
          player2.line(targets2, "green");
          await game.asyncDraw(targets2);
        }
      }
    },
    ai: {
      expose: 0.1
    }
  },
  //钟会
  requanji: {
    audio: 2,
    trigger: { player: ["damageEnd", "phaseUseEnd"] },
    frequent: true,
    locked: false,
    filter(event, player2) {
      if (event.name == "phaseUse") {
        return player2.countCards("h") > player2.hp;
      }
      return event.num > 0;
    },
    getIndex(event, player2) {
      return event.num || 1;
    },
    async content(event, trigger, player2) {
      await player2.draw();
      if (!player2.countCards("h")) {
        return;
      }
      const result = await player2.chooseCard("将一张手牌置于武将牌上作为“权”", true).forResult();
      if (result?.bool && result?.cards?.length) {
        const next = player2.addToExpansion(result.cards, player2, "give");
        next.gaintag.add("quanji");
        await next;
      }
    },
    mod: {
      maxHandcard(player2, num) {
        return num + player2.getExpansions("quanji").length;
      },
      aiOrder(player2, card2, num) {
        if (num <= 0 || typeof card2 !== "object" || !player2.isPhaseUsing()) {
          return num;
        }
        if (player2.countCards("h") > player2.hp + 1) {
          return num;
        }
        if (!player2.hasSkill("zili") || player2.hasSkill("paiyi")) {
          return num;
        }
        if (player2.getExpansions("quanji").length < 3) {
          if (get.type(card2) == "equip" && !["equip2", "equip3"].includes(get.subtype(card2))) {
            return 0;
          }
          let eff = 6 + player2.hp;
          if (!get.tag(card2, "gain") && !get.tag(card2, "draw")) {
            eff += 3;
          }
          if (player2.getUseValue(card2) < eff) {
            return 0;
          }
        }
      }
    },
    onremove(player2, skill) {
      const cards2 = player2.getExpansions("quanji");
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    ai: {
      maixie: true,
      maixie_hp: true,
      notemp: true,
      threaten: 0.8,
      effect: {
        target(card2, player2, target) {
          if (get.tag(card2, "damage")) {
            if (player2.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            if (!target.hasFriend()) {
              return;
            }
            if (target.hp >= 4) {
              return [0.5, get.tag(card2, "damage") * 2];
            }
            if (!target.hasSkill("paiyi") && target.hp > 1) {
              return [0.5, get.tag(card2, "damage") * 1.5];
            }
            if (target.hp == 3) {
              return [0.5, get.tag(card2, "damage") * 1.5];
            }
            if (target.hp == 2) {
              return [1, get.tag(card2, "damage") * 0.5];
            }
          }
        }
      }
    }
  },
  //蔡夫人
  xinqieting: {
    audio: 2,
    trigger: { global: "phaseEnd" },
    direct: true,
    filter(event, player2) {
      return player2 != event.player && event.player.getHistory("sourceDamage", function(evt) {
        return evt.player != event.player;
      }).length == 0;
    },
    async content(event, trigger, player2) {
      const target = trigger.player;
      const str = get.translation(target);
      event.target = target;
      event.addIndex = 0;
      const list = ["摸一张牌"];
      if (target.countCards("h") > 0) {
        list.push("观看" + str + "的两张手牌并获得其中一张");
      } else {
        event.addIndex++;
      }
      if (target.countCards("e", function(card2) {
        return player2.canEquip(card2);
      }) > 0) {
        list.push("将" + str + "装备区内的一张牌移动至自己的装备区");
      }
      const result1 = await player2.chooseControl("cancel2").set("choiceList", list).set("prompt", get.prompt("xinqieting", target)).set("ai", function() {
        var evt = _status.event.getParent();
        if (get.attitude(evt.player, evt.target) > 0) {
          return 0;
        }
        var val = evt.target.hasSkillTag("noe") ? 6 : 0;
        if (evt.target.countCards("e", function(card2) {
          return evt.player.canEquip(card2) && get.value(card2, evt.target) > val && get.effect(evt.player, card2, evt.player, evt.player) > 0;
        }) > 0) {
          return 2 - evt.addIndex;
        }
        if (evt.target.countCards("h") > 0) {
          return 1;
        }
        return 0;
      }).forResult();
      if (result1.control == "cancel2") {
        return;
      }
      player2.logSkill("xinqieting", target);
      if (result1.index == 0) {
        await player2.draw();
        return;
      }
      if (result1.index + event.addIndex == 1) {
        const result2 = await player2.choosePlayerCard(target, "h", 2, true).forResult();
        player2.addExpose(0.2);
        if (result2.bool) {
          const result3 = await player2.chooseButton(["选择获得一张牌", result2.cards], true).forResult();
          if (result3.bool) {
            const card2 = result3.links[0];
            if (lib.filter.canBeGained(card2, player2, target)) {
              await player2.gain(card2, target, "giveAuto", "bySelf");
            } else {
              game.log("但", card2, "不能被", player2, "获得！");
            }
          }
        }
        return;
      }
      player2.addExpose(0.1);
      const result4 = await player2.choosePlayerCard(target, "e", true).set("filterButton", function(button) {
        return _status.event.player.canEquip(button.link);
      }).set("ai", function(button) {
        var player3 = _status.event.player;
        return get.effect(player3, button.link, player3, player3);
      }).forResult();
      if (result4.bool) {
        const card2 = result4.cards[0];
        target.$give(card2, player2, false);
        await game.delay(0.5);
        await player2.equip(card2);
      }
    }
  },
  mobilezhongyong: {
    audio: 2,
    trigger: { player: "useCardAfter" },
    filter(event, player2) {
      if (event.card.name != "sha" || !event.isPhaseUsing(player2)) {
        return false;
      }
      const list = get.info("mobilezhongyong").getResponds(event);
      if (list.length) {
        return list.some((evt) => evt.cards.someInD("od"));
      }
      return event.cards.someInD();
    },
    getResponds(event) {
      const list = [];
      for (const target of event.targets) {
        list.addArray(
          target.getHistory("useCard", (evt) => {
            return evt.card.name == "shan" && evt.respondTo?.[1] == event.card;
          })
        );
      }
      return list;
    },
    async cost(event, trigger, player2) {
      const list = get.info(event.skill).getResponds(trigger);
      if (list.length) {
        const shans = list.map((evt) => evt.cards.filterInD("od")).flat();
        const shas = trigger.cards.filterInD();
        const others = game.filterPlayer((current) => current != player2 && !trigger.targets.includes(current));
        const result = await player2.chooseButtonTarget({
          createDialog: [
            get.prompt(event.skill),
            [
              [
                [
                  "gain",
                  (() => {
                    let str = `获得${get.translation(shans)}`;
                    if (shas.length && others.length) {
                      str += `，然后可以令另一名其他角色获得${get.translation(shas)}`;
                    }
                    return str;
                  })()
                ],
                ["give", `令另一名其他角色获得${get.translation(shans)}，然后你于本回合内使用【杀】的次数上限+1且下一张【杀】的伤害值基数+1`]
              ],
              "tdnodes"
            ]
          ],
          filterButton(button) {
            if (button.link == "give") {
              return get.event().others.length;
            }
            return true;
          },
          filterTarget(card2, player3, target) {
            return get.event().others.includes(target);
          },
          selectTarget() {
            if (ui.selected.buttons.length) {
              const link = ui.selected.buttons[0].link;
              if (link == "give") {
                return 1;
              }
              return 0;
            }
            return 0;
          },
          filterOk() {
            if (ui.selected.buttons.length) {
              const link = ui.selected.buttons[0].link;
              if (link == "give") {
                return ui.selected.targets.length == 1;
              }
              return true;
            }
            return false;
          },
          ai1(button) {
            const { player: player3, shans: shans2, others: others2 } = get.event();
            const { link } = button;
            if (link == "give" && others2.some((current) => get.attitude(player3, current) > 0) && player3.countCards("hs", (card2) => {
              return get.name(card2) == "sha" && player3.hasValueTarget(card2);
            }) > player3.getCardUsable({ name: "sha" })) {
              return 10;
            }
            if (link == "gain" && get.value(shans2, player3) > 0) {
              return 9;
            }
            return 0;
          },
          ai2(target) {
            if (ui.selected.buttons[0].link != "give") {
              return 1;
            }
            const player3 = get.player(), att = get.attitude(player3, target);
            if (att <= 0) {
              return att;
            }
            if (target.hasSkillTag("nogain")) {
              return att / 10;
            }
            if (!target.hasSha()) {
              return 2 * att;
            }
            return att;
          },
          shans,
          others
        }).forResult();
        event.result = {
          bool: result?.bool,
          targets: result?.targets,
          cost_data: result?.links
        };
      } else {
        const cards2 = trigger.cards.filterInD();
        event.result = await player2.chooseBool(get.prompt(event.skill), `获得${get.translation(cards2)}`).set("choice", get.value(cards2, player2) > 0).forResult();
      }
    },
    async content(event, trigger, player2) {
      const list = get.info(event.name).getResponds(trigger);
      if (list.length) {
        const {
          targets: targets2,
          cost_data: [choice]
        } = event;
        const shans = list.map((evt) => evt.cards.filterInD("od")).flat();
        if (choice == "gain") {
          player2.addTempSkill(event.name + "_buff");
          const next = player2.gain(shans, "gain2");
          next.gaintag.add(event.name + "_buff");
          await next;
          const shas = trigger.cards.filterInD();
          const others = game.filterPlayer((current) => current != player2 && !trigger.targets.includes(current));
          if (shas.length && others.length) {
            const result = await player2.chooseTarget(`是否令一名其他角色获得${get.translation(shas)}？`, (card2, player3, target) => {
              return get.event().others.includes(target);
            }).set("ai", (target) => {
              const player3 = get.player(), att = get.attitude(player3, target);
              if (att <= 0) {
                return att;
              }
              if (target.hasSkillTag("nogain")) {
                return att / 10;
              }
              if (!target.hasSha()) {
                return 2 * att;
              }
              return att;
            }).set("others", others).forResult();
            if (result?.targets?.length) {
              const [target] = result.targets;
              player2.line(target, "green");
              await target.gain(shas, "gain2");
            }
          }
        } else if (choice == "give") {
          const [target] = targets2;
          await target.gain(shans, "gain2");
          const effect = event.name + "_effect";
          player2.storage[effect] ??= [0, 0];
          player2.storage[effect][0] += 1;
          player2.storage[effect][1] += 1;
          player2.addTempSkill(effect);
          player2.markSkill(effect);
        }
      } else {
        const cards2 = trigger.cards.filterInD();
        if (cards2.length) {
          player2.addTempSkill(event.name + "_buff");
          const next = player2.gain(cards2, "gain2");
          next.gaintag.add(event.name + "_buff");
          await next;
        }
      }
    },
    subSkill: {
      buff: {
        mod: {
          cardEnabled2(card2, player2) {
            if (get.itemtype(card2) == "card" && card2.hasGaintag("mobilezhongyong_buff")) {
              return false;
            }
          }
        },
        charlotte: true,
        onremove(player2, skill) {
          player2.removeGaintag(skill);
        }
      },
      effect: {
        charlotte: true,
        onremove: true,
        mod: {
          cardUsable(card2, player2, num) {
            if (card2.name == "sha" && typeof player2.storage.mobilezhongyong_effect?.[0] == "number") {
              return num + player2.storage.mobilezhongyong_effect?.[0];
            }
          }
        },
        trigger: { player: "useCard1" },
        filter(event, player2) {
          return event.card.name == "sha";
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const skillName = event.name;
          if (typeof player2.storage[skillName]?.[1] == "number") {
            trigger.baseDamage += player2.storage[skillName][1];
          }
          player2.storage[skillName] = [player2.storage[skillName]?.[0] ?? 0, 0];
          player2.markSkill(skillName);
        },
        intro: {
          markcount: () => 0,
          content(storage, player2) {
            let str = "";
            if (typeof storage[0] == "number" && storage[0] > 0) {
              str += `<li>本回合使用【杀】的次数上限+${storage[0]}`;
            }
            if (typeof storage[1] == "number" && storage[1] > 0) {
              str += `<br><li>下一张【杀】的伤害基数+${storage[1]}`;
            }
            return str;
          }
        }
      }
    }
  },
  rejieyue: {
    audio: 2,
    trigger: { player: "phaseJieshuBegin" },
    direct: true,
    filter(event, player2) {
      return player2.countCards("he") > 0;
    },
    async content(event, trigger, player2) {
      const result1 = await player2.chooseCardTarget({
        prompt: get.prompt2("rejieyue"),
        filterCard: true,
        position: "he",
        filterTarget: lib.filter.notMe,
        ai1(card2) {
          var player3 = _status.event.player;
          if (get.name(card2) == "du") {
            return 20;
          }
          if (get.position(card2) == "e" && get.value(card2) <= 0) {
            return 14;
          }
          if (get.position(card2) == "h" && game.hasPlayer(function(current) {
            return current != player3 && get.attitude(player3, current) > 0 && current.getUseValue(card2) > player3.getUseValue(card2) && current.getUseValue(card2) > player3.getUseValue(card2);
          })) {
            return 12;
          }
          if (game.hasPlayer(function(current) {
            return current != player3 && get.attitude(player3, current) > 0;
          })) {
            if (card2.name == "wuxie") {
              return 11;
            }
            if (card2.name == "shan" && player3.countCards("h", "shan") > 1) {
              return 9;
            }
          }
          return 6 / Math.max(1, get.value(card2));
        },
        ai2(target2) {
          var player3 = _status.event.player;
          var card2 = ui.selected.cards[0];
          var att = get.attitude(player3, target2);
          if (card2.name == "du") {
            return -6 * att;
          }
          if (att > 0) {
            if (get.position(card2) == "h" && target2.getUseValue(card2) > player3.getUseValue(card2)) {
              return 4 * att;
            }
            if (get.value(card2, target2) > get.value(card2, player3)) {
              return 2 * att;
            }
            return 1.2 * att;
          }
          return -att * Math.min(4, target2.countCards("he")) / 4;
        }
      }).forResult();
      if (!result1.bool) {
        return;
      }
      const target = result1.targets[0];
      player2.logSkill("rejieyue", target);
      await player2.give(result1.cards, target);
      let num = 0;
      if (target.countCards("h")) {
        num++;
      }
      if (target.countCards("e")) {
        num++;
      }
      let result2 = { bool: false };
      if (num > 0) {
        const next = target.chooseCard("he", num, "选择保留每个区域的各一张牌，然后弃置其余的牌。或点取消，令" + get.translation(player2) + "摸三张牌", function(card2) {
          for (var i = 0; i < ui.selected.cards.length; i++) {
            if (get.position(ui.selected.cards[i]) == get.position(card2)) {
              return false;
            }
          }
          return true;
        });
        next.set("complexCard", true);
        next.set("goon", get.attitude(target, player2) >= 0);
        next.set("maxNum", num);
        next.set("ai", function(card2) {
          if (_status.event.goon) {
            return -1;
          }
          var num2 = _status.event.maxNum;
          if (ui.selected.cards.length >= num2 - 1) {
            var cards2 = player2.getCards("he", function(cardx2) {
              return cardx2 != card2 && !ui.selected.cards.includes(cardx2);
            });
            var val = 0;
            for (var cardx of cards2) {
              val += get.value(cardx);
            }
            if (val >= 14) {
              return 0;
            }
          }
          return get.value(card2);
        });
        result2 = await next.forResult();
      }
      if (!result2.bool) {
        await player2.draw(3);
      } else {
        const cards2 = target.getCards("he");
        cards2.removeArray(result2.cards);
        if (cards2.length) {
          await target.discard(cards2);
        }
      }
    },
    ai: {
      threaten: 1.3,
      expose: 0.2
    }
  },
  tiansuan: {
    audio: 2,
    enable: "phaseUse",
    manualConfirm: true,
    filter(event, player2) {
      return !player2.storage.tiansuan2;
    },
    async content(event, trigger, player2) {
      const result1 = await player2.chooseControl("上上签", "上签", "中签", "下签", "下下签", "cancel2").set("prompt", "天算：是否增加其中一个命运签的权重？").set("ai", function() {
        return Math.random() < 0.5 ? 0 : 4;
      }).forResult();
      const list = [0, 1, 1, 2, 2, 2, 3, 3, 4];
      if (result1.control != "cancel2") {
        list.push(result1.index);
      }
      const num = list.randomGet();
      event.num = num;
      const str = get.translation(player2) + "抽取的命运签为：" + lib.skill["tiansuan2_" + num].name;
      game.log(player2, "抽取出了", "#g" + lib.skill["tiansuan2_" + num].name);
      const dialog = ui.create.dialog(str);
      const videoId = lib.status.videoId++;
      game.broadcast("createDialog", videoId, str);
      game.pause();
      await game.delay(1.5);
      game.resume();
      dialog.close();
      game.broadcast("closeDialog", videoId);
      const result2 = await player2.chooseTarget(true, "令一名角色获得“" + lib.skill["tiansuan2_" + num].name + "”").set("ai", lib.skill["tiansuan2_" + num].aiCheck).forResult();
      if (result2.bool) {
        const target = result2.targets[0];
        player2.line(target, "green");
        game.log(player2, "令", target, "获得了命运签");
        player2.storage.tiansuan2 = target;
        player2.storage.tiansuan3 = "tiansuan2_" + num;
        player2.addTempSkill("tiansuan2", { player: "phaseBegin" });
        target.addSkill("tiansuan2_" + num);
        let pos = "e";
        if (target != player2) {
          pos += "h";
        }
        if (num == 0) {
          pos += "j";
        }
        if (num < 2 && target.countGainableCards(player2, pos) > 0) {
          const next = player2.gainPlayerCard(target, pos, true);
          if (num == 0) {
            next.visible = true;
          }
          await next;
        } else {
          await game.delayx();
        }
      }
    },
    derivation: "tiansuan_faq",
    ai: {
      order: 7,
      result: {
        player: 1
      }
    }
  },
  tiansuan2: {
    charlotte: true,
    onremove(player2, skill) {
      if (player2.storage.tiansuan2) {
        player2.storage.tiansuan2.removeSkill(player2.storage.tiansuan3);
      }
      delete player2.storage.tiansuan2;
      delete player2.storage.tiansuan3;
    }
  },
  tiansuan2_0: {
    name: "上上签",
    trigger: { player: "damageBegin4" },
    forced: true,
    charlotte: true,
    sourceSkill: "tiansuan",
    async content(event, trigger, player2) {
      trigger.cancel();
    },
    mark: true,
    intro: {
      content: "当你受到伤害时，防止此伤害。"
    },
    aiCheck(target) {
      if (target.hasSkill("tiansuan2_0")) {
        return 0;
      }
      var player2 = _status.event.player;
      var att = get.attitude(player2, target);
      if (target.countCards("e", function(card2) {
        return get.value(card2, target) <= 0;
      })) {
        att *= 2;
      }
      return att / Math.sqrt(Math.max(1, target.hp));
    },
    ai: {
      effect: {
        target(card2, player2, target, current) {
          if (get.tag(card2, "damage") && !player2.hasSkillTag("jueqing", false, target)) {
            return "zeroplayertarget";
          }
        }
      }
    }
  },
  tiansuan2_1: {
    name: "上签",
    trigger: { player: "damageBegin4" },
    forced: true,
    charlotte: true,
    sourceSkill: "tiansuan",
    filter(event, player2) {
      return event.num > 1;
    },
    async content(event, trigger, player2) {
      trigger.num = 1;
    },
    group: "tiansuan2_damage",
    mark: true,
    intro: {
      content: "当你受到伤害时，你令伤害值改为1；当你受到1点伤害后，你摸一张牌。"
    },
    aiCheck(target) {
      if (target.hasSkill("tiansuan2_1")) {
        return 0;
      }
      var player2 = _status.event.player;
      var att = get.attitude(player2, target);
      if (target.countCards("e", function(card2) {
        return get.value(card2, target) <= 0;
      })) {
        att *= 2;
      }
      if (target.hp == 1) {
        return att / 2;
      }
      return att / Math.sqrt(Math.max(1, target.hp));
    },
    ai: {
      filterDamage: true,
      skillTagFilter(player2, tag, arg) {
        if (arg && arg.player) {
          if (arg.player.hasSkillTag("jueqing", false, player2)) {
            return false;
          }
        }
      },
      effect: {
        target(card2, player2, target, current) {
          if (target && target.hp > 1 && get.tag(card2, "damage") && !player2.hasSkillTag("jueqing", false, target)) {
            return 0.8;
          }
        }
      }
    }
  },
  tiansuan2_damage: {
    trigger: { player: "damageEnd" },
    charlotte: true,
    sourceSkill: "tiansuan",
    async content(event, trigger, player2) {
      player2.draw(trigger.num);
    }
  },
  tiansuan2_2: {
    name: "中签",
    trigger: { player: "damageBegin4" },
    forced: true,
    charlotte: true,
    sourceSkill: "tiansuan",
    filter(event, player2) {
      return event.num > 1;
    },
    async content(event, trigger, player2) {
      trigger.num = 1;
    },
    mark: true,
    intro: {
      content: "当你受到伤害时，你令伤害属性改为火属性并将伤害值改为1。"
    },
    aiCheck(target) {
      if (target.hasSkill("tiansuan2_2")) {
        return 0;
      }
      let player2 = _status.event.player, original = get.damageEffect(target, player2, player2);
      target.addSkill("tiansuan2_ai");
      let fire = get.damageEffect(target, player2, player2, "fire");
      target.removeSkill("tiansuan2_ai");
      return (fire - original) * get.attitude(player2, target);
    },
    group: ["tiansuan2_fire", "tiansuan2_ai"]
  },
  tiansuan2_ai: {
    ai: {
      filterDamage: true,
      skillTagFilter(player2, tag, arg) {
        if (arg && arg.player) {
          if (arg.player.hasSkillTag("jueqing", false, player2)) {
            return false;
          }
        }
      }
    }
  },
  tiansuan2_fire: {
    trigger: { player: "damageBefore" },
    forced: true,
    charlotte: true,
    sourceSkill: "tiansuan",
    filter(event, player2) {
      return !event.hasNature("fire");
    },
    async content(event, trigger, player2) {
      game.setNature(trigger, "fire");
    }
  },
  tiansuan2_3: {
    name: "下签",
    trigger: { player: "damageBegin3" },
    forced: true,
    charlotte: true,
    sourceSkill: "tiansuan",
    async content(event, trigger, player2) {
      trigger.num++;
    },
    mark: true,
    intro: {
      content: "当你受到伤害时，你令此伤害+1。"
    },
    aiCheck(target) {
      if (target.hasSkill("tiansuan2_3")) {
        return 0;
      }
      var player2 = _status.event.player;
      var att = get.attitude(player2, target);
      return -att / Math.sqrt(Math.max(1, target.hp));
    },
    ai: {
      effect: {
        target(card2, player2, target, current) {
          if (get.tag(card2, "damage") && !player2.hasSkillTag("jueqing", false, target) && current < 0) {
            return 1.3;
          }
        }
      }
    }
  },
  tiansuan2_4: {
    name: "下下签",
    trigger: { player: "damageBegin3" },
    forced: true,
    charlotte: true,
    sourceSkill: "tiansuan",
    async content(event, trigger, player2) {
      trigger.num++;
    },
    mod: {
      cardEnabled(card2, player2) {
        if (card2.name == "tao" || card2.name == "jiu") {
          return false;
        }
      },
      cardSavable(card2, player2) {
        if (card2.name == "tao" || card2.name == "jiu") {
          return false;
        }
      }
    },
    mark: true,
    intro: {
      content: "当你受到伤害时，你令此伤害+1。你不能使用【酒】或【桃】。"
    },
    aiCheck(target) {
      if (target.hasSkill("tiansuan2_4")) {
        return 0;
      }
      var player2 = _status.event.player;
      var att = get.attitude(player2, target);
      return -att / Math.sqrt(Math.max(1, target.hp));
    },
    ai: {
      effect: {
        target(card2, player2, target, current) {
          if (get.tag(card2, "damage") && !player2.hasSkillTag("jueqing", false, target) && current < 0) {
            return 1.3;
          }
        }
      }
    }
  },
  relieren: {
    audio: 2,
    audioname: ["boss_lvbu3"],
    trigger: { player: "useCardToPlayered" },
    filter(event, player2) {
      return event.card.name == "sha" && player2.canCompare(event.target);
    },
    check(event, player2) {
      return get.attitude(player2, event.target) < 0;
    },
    //priority:5,
    async content(event, trigger, player2) {
      const result = await player2.chooseToCompare(trigger.target).set("clear", false).forResult();
      if (result.bool) {
        if (trigger.target.countGainableCards(player2, "he")) {
          await player2.gainPlayerCard(trigger.target, true, "he");
        }
        ui.clear();
      } else {
        const card1 = result.player;
        const card2 = result.target;
        if (get.position(card1) == "d") {
          await trigger.target.gain(card1, "gain2");
        }
        if (get.position(card2) == "d") {
          await player2.gain(card2, "gain2");
        }
      }
    }
  },
  retiaoxin: {
    audio: "tiaoxin",
    audioname: ["sp_jiangwei", "xiahouba", "re_jiangwei"],
    enable: "phaseUse",
    usable: 1,
    filterTarget(card2, player2, target) {
      return target != player2 && target.countCards("he");
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const result = await target.chooseToUse(
        function(card2, player3, event2) {
          if (get.name(card2) != "sha") {
            return false;
          }
          return lib.filter.filterCard.apply(this, arguments);
        },
        "挑衅：对" + get.translation(player2) + "使用一张杀，或令其弃置你的一张牌"
      ).set("targetRequired", true).set("complexSelect", true).set("complexTarget", true).set("filterTarget", function(card2, player3, target2) {
        if (target2 != _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
          return false;
        }
        return lib.filter.filterTarget.apply(this, arguments);
      }).set("sourcex", player2).forResult();
      if (result.bool == false && target.countCards("he") > 0) {
        await player2.discardPlayerCard(target, "he", true);
      }
    },
    ai: {
      order: 4,
      expose: 0.2,
      result: {
        target: -1,
        player(player2, target) {
          if (!target.canUse("sha", player2)) {
            return 0;
          }
          if (target.countCards("h") == 0) {
            return 0;
          }
          if (target.countCards("h") == 1) {
            return -0.1;
          }
          if (player2.hp <= 2) {
            return -2;
          }
          if (player2.countCards("h", "shan") == 0) {
            return -1;
          }
          return -0.5;
        }
      },
      threaten: 1.1
    }
  },
  //南华老仙
  yufeng: {
    inherit: "yufeng_old",
    manualConfirm: true,
    async content(event, trigger, player2) {
      if (_status.connectMode) {
        event.time = lib.configOL.choose_timeout;
      }
      event.videoId = lib.status.videoId++;
      const maxScore = Math.max(2, 1 + player2.countMark("yufeng"));
      if (player2.isUnderControl()) {
        game.swapPlayerAuto(player2);
      }
      const switchToAuto = function() {
        game.pause();
        game.countChoose();
        setTimeout(function() {
          _status.imchoosing = false;
          var max2 = Math.max(2, 1 + player2.countMark("yufeng"));
          var score = Math.random() < 0.5 ? max2 : get.rand(1, max2);
          event._result = {
            bool: true,
            score,
            win: score >= max2
          };
          if (event.dialog) {
            event.dialog.close();
          }
          if (event.control) {
            event.control.close();
          }
          game.resume();
        }, 5e3);
      };
      const createDialog = function(player3, id) {
        if (_status.connectMode) {
          lib.configOL.choose_timeout = "30";
        }
        if (player3 == game.me) {
          return;
        }
        var str = get.translation(player3) + "正在表演《御风飞行》...<br>";
        ui.create.dialog(str).videoId = id;
      };
      const chooseButton = function(maxScore2) {
        lib.skill.yufeng.$playFlappyBird(maxScore2);
      };
      game.broadcastAll(createDialog, player2, event.videoId);
      if (event.isMine()) {
        chooseButton(maxScore);
      } else if (event.isOnline()) {
        event.player.send(chooseButton, maxScore);
        event.player.wait();
        game.pause();
      } else {
        switchToAuto();
      }
      await game.pause();
      game.broadcastAll(
        function(id, time) {
          if (_status.connectMode) {
            lib.configOL.choose_timeout = time;
          }
          var dialog = get.idDialog(id);
          if (dialog) {
            dialog.close();
          }
        },
        event.videoId,
        event.time
      );
      const result = event.result || event._result || {};
      player2.popup(get.cnNumber(result.score) + "分", result.win ? "wood" : "fire");
      game.log(player2, "御风飞行", result.win ? "#g成功" : "#y失败");
      game.log(player2, "获得了", "#g" + result.score + "分");
      const max = player2.countMark("yufeng");
      if (!result.win) {
        if (result.score) {
          await player2.draw(result.score);
        }
        if (max) {
          await player2.removeMark("yufeng", max, false);
        }
        return;
      }
      if (max < 2) {
        await player2.addMark("yufeng", 1, false);
      }
      const result2 = await player2.chooseTarget("请选择【御风】的目标", [1, result.score], function(card2, player3, target) {
        return target != player3 && !target.hasSkill(event.name + "_yufeng2");
      }).set("ai", function(target) {
        var player3 = _status.event.player;
        var att = -get.attitude(player3, target), attx = att * 2;
        if (att <= 0 || target.hasSkill("xinfu_pdgyingshi")) {
          return 0;
        }
        if (target.hasJudge("lebu")) {
          attx -= att;
        }
        if (target.hasJudge("bingliang")) {
          attx -= att;
        }
        return attx / Math.max(2.25, Math.sqrt(target.countCards("h") + 1));
      }).forResult();
      if (result2.bool) {
        result2.targets.sortBySeat();
        player2.line(result2.targets, "green");
        game.log(result2.targets, "获得了", "#y“御风”", "效果");
        for (const i of result2.targets) {
          i.addSkill(event.name + "_yufeng2");
        }
        if (result.score > result2.targets.length) {
          await player2.draw(result.score - result2.targets.length);
        }
      } else {
        await player2.draw(result.score);
      }
    },
    $playFlappyBird(maxScore, title) {
      const event = _status.event;
      const dialog = ui.create.dialog("forcebutton", "hidden");
      dialog.textPrompt = dialog.add('<div class="text center">准备好了吗？</div>');
      dialog.classList.add("fixed");
      dialog.classList.add("scroll1");
      dialog.classList.add("scroll2");
      dialog.classList.add("fullwidth");
      dialog.classList.add("fullheight");
      dialog.classList.add("noupdate");
      const updateText = function(str) {
        dialog.textPrompt.innerHTML = '<div class="text center">' + str + "</div>";
      };
      const canvas = document.createElement("canvas");
      dialog.appendChild(canvas);
      canvas.style.position = "absolute";
      canvas.style.width = "276px";
      canvas.style.height = "414px";
      canvas.style.left = "calc(50% - 141px)";
      canvas.style.top = "calc(50% - 200px)";
      canvas.width = 276;
      canvas.height = 414;
      canvas.style.border = "3px solid";
      const RAD = Math.PI / 180;
      const ctx = canvas.getContext("2d");
      let frames = 0;
      let dx = 0.1;
      let previousDOMHighResTimeStamp = performance.now();
      let deltaTime = 0;
      const state = {
        curr: 0,
        getReady: 0,
        Play: 1,
        gameOver: 2,
        gameSuccess: 3
      };
      const SFX = {
        start: new Audio(),
        flap: new Audio(),
        score: new Audio(),
        hit: new Audio(),
        die: new Audio(),
        played: false
      };
      const gnd = {
        sprite: new Image(),
        x: 0,
        y: 0,
        draw() {
          this.y = parseFloat(canvas.height - this.sprite.height);
          ctx.drawImage(this.sprite, this.x, this.y);
        },
        update() {
          if (state.curr == state.gameOver || state.curr == state.gameSuccess) {
            return;
          }
          this.x -= dx * deltaTime;
          const halfWidth = this.sprite.width / 4;
          if (this.x <= -halfWidth) {
            this.x += halfWidth;
          }
        }
      };
      const bg = {
        sprite: new Image(),
        x: 0,
        y: 0,
        draw() {
          let y = parseFloat(canvas.height - this.sprite.height);
          ctx.drawImage(this.sprite, this.x, y);
        }
      };
      const pipe = {
        top: { sprite: new Image() },
        bot: { sprite: new Image() },
        gap: 127,
        moved: true,
        pipes: [],
        numberOfPipes: 1,
        timeElapsed: 0,
        draw() {
          for (let i = 0; i < this.pipes.length; i++) {
            let p = this.pipes[i];
            ctx.drawImage(this.top.sprite, p.x, p.y);
            ctx.drawImage(this.bot.sprite, p.x, p.y + parseFloat(this.top.sprite.height) + this.gap);
          }
        },
        update() {
          if (state.curr != state.Play) {
            return;
          }
          this.timeElapsed += deltaTime;
          if (this.timeElapsed >= 1600) {
            this.timeElapsed -= 1600;
            this.pipes.push({
              x: parseFloat(canvas.width),
              y: -210 * Math.min(Math.random() * 0.8 + 1.2, 1.8)
            });
          }
          this.pipes.forEach((pipe2) => {
            pipe2.x -= dx * deltaTime;
          });
          if (this.pipes.length && this.pipes[0].x < -this.top.sprite.width) {
            this.pipes.shift();
            this.moved = true;
          }
        }
      };
      const bird = {
        animations: [{ sprite: new Image() }, { sprite: new Image() }, { sprite: new Image() }, { sprite: new Image() }],
        rotatation: 0,
        x: 50,
        y: 100,
        speed: 0,
        gravity: 4e-4,
        thrust: 0.18,
        frame: 0,
        timeElapsed: 0,
        totalTimeElapsed: 0,
        draw() {
          let h = this.animations[this.frame].sprite.height;
          let w = this.animations[this.frame].sprite.width;
          ctx.save();
          ctx.translate(this.x, this.y);
          ctx.rotate(this.rotatation * RAD);
          ctx.drawImage(this.animations[this.frame].sprite, -w / 2, -h / 2);
          ctx.restore();
        },
        update() {
          this.totalTimeElapsed += deltaTime;
          let r = parseFloat(this.animations[0].sprite.width) / 2;
          switch (state.curr) {
            case state.getReady:
            case state.gameSuccess:
              this.rotatation = 0;
              this.timeElapsed += deltaTime;
              if (this.timeElapsed >= 200) {
                this.timeElapsed -= 200;
                this.y += Math.sin(this.totalTimeElapsed / 10 * RAD);
                this.frame++;
              }
              break;
            case state.Play:
              this.timeElapsed += deltaTime;
              if (this.timeElapsed >= 100) {
                this.timeElapsed -= 100;
                this.frame++;
              }
              this.y += this.speed * deltaTime;
              this.setRotation();
              this.speed += this.gravity * deltaTime;
              if (UI.score.curr >= maxScore) {
                state.curr = state.gameSuccess;
                this.timeElapsed = 0;
                updateText(`${title || "御风飞行"}表演成功！`);
                setTimeout(switchToAuto, 2e3);
              } else if (this.y + r >= gnd.y || this.collisioned()) {
                state.curr = state.gameOver;
                this.timeElapsed = 0;
                updateText(`${title || "御风飞行"}表演失败……`);
                setTimeout(switchToAuto, 2e3);
              }
              break;
            case state.gameOver:
              this.frame = 1;
              if (this.y + r < gnd.y) {
                this.y += this.speed * deltaTime;
                this.setRotation();
                this.speed += this.gravity * deltaTime;
              } else {
                this.speed = 0;
                this.y = gnd.y - r;
                this.rotatation = 90;
                if (!SFX.played) {
                  Promise.resolve(SFX.die.play()).catch(() => void 0);
                  SFX.played = true;
                }
              }
              break;
          }
          const animationsLength = this.animations.length;
          if (this.frame >= animationsLength) {
            this.frame -= animationsLength;
          }
        },
        flap() {
          if (this.y <= 0) {
            return;
          }
          const flap = SFX.flap;
          flap.currentTime = 0;
          if (flap.paused) {
            Promise.resolve(flap.play()).catch(() => void 0);
          }
          this.speed = -this.thrust;
        },
        setRotation() {
          if (this.speed <= 0) {
            this.rotatation = Math.max(-25, -25 * this.speed / (-1 * this.thrust));
          } else if (this.speed > 0) {
            this.rotatation = Math.min(90, 90 * this.speed / (this.thrust * 2));
          }
        },
        collisioned() {
          if (!pipe.pipes.length) {
            return;
          }
          let bird2 = this.animations[0].sprite;
          let x = pipe.pipes[0].x;
          let y = pipe.pipes[0].y;
          let r = bird2.height / 4 + bird2.width / 4;
          let roof = y + parseFloat(pipe.top.sprite.height);
          let floor = roof + pipe.gap;
          let w = parseFloat(pipe.top.sprite.width);
          if (this.x + r >= x) {
            if (this.x + r < x + w) {
              if (this.y - r <= roof || this.y + r >= floor) {
                Promise.resolve(SFX.hit.play()).catch(() => void 0);
                return true;
              }
            } else if (pipe.moved) {
              updateText(`当前分数：${++UI.score.curr}`);
              const score = SFX.score;
              score.currentTime = 0;
              if (score.paused) {
                Promise.resolve(score.play()).catch(() => void 0);
              }
              pipe.moved = false;
            }
          }
        }
      };
      const UI = {
        getReady: { sprite: new Image() },
        gameOver: { sprite: new Image() },
        gameClear: { sprite: new Image() },
        tap: [{ sprite: new Image() }, { sprite: new Image() }],
        score: {
          curr: 0,
          best: 0
        },
        x: 0,
        y: 0,
        tx: 0,
        ty: 0,
        frame: 0,
        timeElapsed: 0,
        draw() {
          switch (state.curr) {
            case state.getReady:
              this.y = parseFloat(canvas.height - this.getReady.sprite.height) / 2;
              this.x = parseFloat(canvas.width - this.getReady.sprite.width) / 2;
              this.tx = parseFloat(canvas.width - this.tap[0].sprite.width) / 2;
              this.ty = this.y + this.getReady.sprite.height - this.tap[0].sprite.height;
              ctx.drawImage(this.getReady.sprite, this.x, this.y);
              ctx.drawImage(this.tap[this.frame].sprite, this.tx, this.ty);
              break;
            case state.gameOver:
            case state.gameSuccess:
              this.y = parseFloat(canvas.height - this.gameOver.sprite.height) / 2;
              this.x = parseFloat(canvas.width - this.gameOver.sprite.width) / 2;
              this.tx = parseFloat(canvas.width - this.tap[0].sprite.width) / 2;
              this.ty = this.y + this.gameOver.sprite.height - this.tap[0].sprite.height;
              ctx.drawImage((state.curr == state.gameOver ? this.gameOver : this.gameClear).sprite, this.x, this.y);
          }
        },
        update() {
          if (state.curr == state.Play) {
            return;
          }
          this.timeElapsed += deltaTime;
          if (this.timeElapsed >= 200) {
            this.timeElapsed -= 200;
            this.frame++;
          }
          const tapLength = this.tap.length;
          if (this.frame >= tapLength) {
            this.frame -= tapLength;
          }
        }
      };
      gnd.sprite.src = lib.assetURL + "image/flappybird/ground.png";
      bg.sprite.src = lib.assetURL + "image/flappybird/BG.png";
      pipe.top.sprite.src = lib.assetURL + "image/flappybird/toppipe.png";
      pipe.bot.sprite.src = lib.assetURL + "image/flappybird/botpipe.png";
      UI.gameOver.sprite.src = lib.assetURL + "image/flappybird/gameover.png";
      UI.gameClear.sprite.src = lib.assetURL + "image/flappybird/gameclear.png";
      UI.getReady.sprite.src = lib.assetURL + "image/flappybird/getready.png";
      UI.tap[0].sprite.src = lib.assetURL + "image/flappybird/tap/t0.png";
      UI.tap[1].sprite.src = lib.assetURL + "image/flappybird/tap/t1.png";
      bird.animations[0].sprite.src = lib.assetURL + "image/flappybird/bird/b0.png";
      bird.animations[1].sprite.src = lib.assetURL + "image/flappybird/bird/b1.png";
      bird.animations[2].sprite.src = lib.assetURL + "image/flappybird/bird/b2.png";
      bird.animations[3].sprite.src = lib.assetURL + "image/flappybird/bird/b0.png";
      SFX.start.src = lib.assetURL + "audio/effect/flappybird_start.wav";
      SFX.flap.src = lib.assetURL + "audio/effect/flappybird_flap.wav";
      SFX.score.src = lib.assetURL + "audio/effect/flappybird_score.wav";
      SFX.hit.src = lib.assetURL + "audio/effect/flappybird_hit.wav";
      SFX.die.src = lib.assetURL + "audio/effect/flappybird_die.wav";
      const gameLoop = (domHighResTimeStamp) => {
        if (frames < 0) {
          return;
        }
        deltaTime = domHighResTimeStamp - previousDOMHighResTimeStamp;
        previousDOMHighResTimeStamp = domHighResTimeStamp;
        update();
        draw();
        frames++;
        window.requestAnimationFrame(gameLoop);
      };
      const update = function() {
        bird.update();
        gnd.update();
        pipe.update();
        UI.update();
      };
      const draw = function() {
        ctx.fillStyle = "#30c0df";
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        bg.draw();
        pipe.draw();
        bird.draw();
        gnd.draw();
        UI.draw();
      };
      const click = function() {
        switch (state.curr) {
          case state.getReady:
            state.curr = state.Play;
            bird.timeElapsed = 0;
            Promise.resolve(SFX.start.play()).catch(() => void 0);
            updateText(`当前分数：${UI.score.curr}`);
            break;
          case state.Play:
            bird.flap();
        }
      };
      const switchToAuto = function() {
        event._result = {
          bool: true,
          score: UI.score.curr,
          win: UI.score.curr >= maxScore
        };
        dialog.close();
        game.resume();
        _status.imchoosing = false;
        frames = -1;
        document.removeEventListener(lib.config.touchscreen ? "touchstart" : "mousedown", click);
      };
      dialog.open();
      game.pause();
      game.countChoose();
      document.addEventListener(lib.config.touchscreen ? "touchstart" : "mousedown", click);
      window.requestAnimationFrame(gameLoop);
    },
    subSkill: {
      yufeng2: {
        trigger: { player: "phaseZhunbeiBegin" },
        audio: false,
        forced: true,
        charlotte: true,
        sourceSkill: "yufeng",
        async content(event, trigger, player2) {
          player2.removeSkill("yufeng_yufeng2");
          const result = await player2.judge().forResult();
          switch (result.color) {
            case "red":
              player2.skip("phaseDraw");
              break;
            case "black":
              player2.skip("phaseUse");
              player2.skip("phaseDiscard");
              break;
          }
        },
        mark: true,
        intro: {
          content: "准备阶段时进行判定，结果为红则跳过摸牌阶段，为黑则跳过出牌阶段和弃牌阶段"
        },
        ai: {
          order: 7,
          result: { player: 1 }
        }
      }
    }
  },
  yufeng_old: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    async content(event, trigger, player2) {
      if (_status.connectMode) {
        event.time = lib.configOL.choose_timeout;
      }
      event.videoId = lib.status.videoId++;
      if (player2.isUnderControl()) {
        game.swapPlayerAuto(player2);
      }
      const switchToAuto = function() {
        game.pause();
        game.countChoose();
        setTimeout(function() {
          _status.imchoosing = false;
          var max2 = Math.max(2, 1 + game.me.countMark("yufeng"));
          var score = Math.random() < 0.5 ? max2 : get.rand(1, max2);
          event._result = {
            bool: true,
            score,
            win: score >= max2
          };
          if (event.dialog) {
            event.dialog.close();
          }
          if (event.control) {
            event.control.close();
          }
          game.resume();
        }, 5e3);
      };
      const createDialog = function(player3, id) {
        if (_status.connectMode) {
          lib.configOL.choose_timeout = "30";
        }
        if (player3 == game.me) {
          return;
        }
        var str = get.translation(player3) + "正在表演《御风飞行》...<br>";
        ui.create.dialog(str).videoId = id;
      };
      const chooseButton = function() {
        var roundmenu = false;
        if (ui.roundmenu && ui.roundmenu.display != "none") {
          roundmenu = true;
          ui.roundmenu.style.display = "none";
        }
        var event2 = _status.event;
        event2.settleed = false;
        event2.score = 0;
        event2.dialog = ui.create.dialog("forcebutton", "hidden");
        event2.dialog.textPrompt = event2.dialog.add('<div class="text center">准备好了吗？准备好了的话就点击屏幕开始吧！</div>');
        var max2 = Math.max(2, 1 + game.me.countMark("yufeng"));
        event2.dialog.textPrompt.style["z-index"] = 10;
        event2.switchToAuto = function() {
          event2._result = {
            bool: true,
            score: event2.score,
            win: event2.score >= max2
          };
          event2.dialog.close();
          game.resume();
          _status.imchoosing = false;
          if (roundmenu) {
            ui.roundmenu.style.display = "";
          }
        };
        event2.dialog.classList.add("fixed");
        event2.dialog.classList.add("scroll1");
        event2.dialog.classList.add("scroll2");
        event2.dialog.classList.add("fullwidth");
        event2.dialog.classList.add("fullheight");
        event2.dialog.classList.add("noupdate");
        event2.dialog.style.overflow = "hidden";
        event2.dialog.open();
        var height = event2.dialog.offsetHeight;
        var width = event2.dialog.offsetWidth;
        var top = 50;
        var speed = 0;
        var start = false;
        var bird = ui.create.div("");
        bird.style["background-image"] = "linear-gradient(rgba(240, 235, 3, 1), rgba(230, 225, 5, 1))";
        bird.style["border-radius"] = "3px";
        var pipes = [];
        bird.style.position = "absolute";
        bird.style.height = "40px";
        bird.style.width = "40px";
        bird.style.left = Math.ceil(width / 3) + "px";
        bird.style.top = top / 100 * height + "px";
        bird.updatePosition = function() {
          bird.style.transform = "translateY(" + (top / 100 * height - bird.offsetTop) + "px)";
        };
        event2.dialog.appendChild(bird);
        var isDead = function() {
          if (top > 100 || top < 0) {
            return true;
          }
          var btop = top;
          var bleft = 100 / 3;
          var bdown = btop + 5;
          var bright = bleft + 5;
          for (var i of pipes) {
            var left2 = i.left;
            var right2 = left2 + 10;
            var bottom2 = i.height1;
            var top2 = i.height2;
            if (left2 > bright || right2 < bleft) {
              continue;
            }
            if (btop < bottom2) {
              return true;
            }
            if (bdown > top2) {
              return true;
            }
            return false;
          }
          return false;
        };
        var fly = function() {
          if (!start) {
            start = true;
            event2.dialog.textPrompt.innerHTML = '<div class="text center">当前分数：' + event2.score + "</div>";
            speed = -4;
            event2.fly = setInterval(function() {
              top += speed;
              if (top < 0) {
                top = 0;
              }
              bird.updatePosition();
              for (var i of pipes) {
                i.left -= 0.5;
                i.updateLeft();
              }
              speed += 0.5;
              if (speed > 2.5) {
                speed = 2.5;
              }
              if (isDead() == true) {
                event2.settle();
              }
            }, 35);
            var addPipe = function() {
              var num = get.rand(5, 55);
              var pipe1 = ui.create.div("");
              pipe1.style["background-image"] = "linear-gradient(rgba(57, 133, 4, 1), rgba(60, 135, 6, 1))";
              pipe1.style["border-radius"] = "3px";
              pipe1.style.position = "absolute";
              pipe1.height1 = num;
              pipe1.height2 = num + 50;
              pipe1.left = 110;
              pipe1.num = 1;
              pipe1.style.height = Math.ceil(height * num / 100) + "px";
              pipe1.style.width = width / 10 + "px";
              pipe1.style.left = pipe1.left * width / 100 + "px";
              pipe1.style.top = "0px";
              var pipe2 = ui.create.div("");
              pipe2.style["background-image"] = "linear-gradient(rgba(57, 133, 4, 1), rgba(60, 135, 6, 1))";
              pipe2.style["border-radius"] = "3px";
              pipe1.pipe2 = pipe2;
              pipe2.style.position = "absolute";
              pipe2.style.height = Math.ceil((100 - pipe1.height2) * height / 100) + "px";
              pipe2.style.width = width / 10 + "px";
              pipe2.style.left = pipe1.left * width / 100 + "px";
              pipe2.style.top = Math.ceil(pipe1.height2 * height / 100) + "px";
              pipes.add(pipe1);
              event2.dialog.appendChild(pipe1);
              event2.dialog.appendChild(pipe2);
              pipe1.updateLeft = function() {
                this.style.transform = "translateX(" + (this.left / 100 * width - this.offsetLeft) + "px)";
                this.pipe2.style.transform = "translateX(" + (this.left / 100 * width - this.pipe2.offsetLeft) + "px)";
                if (this.left < 25 && !this.score) {
                  this.score = true;
                  event2.score++;
                  event2.dialog.textPrompt.innerHTML = '<div class="text center">当前分数：' + event2.score + "</div>";
                  if (event2.score >= max2) {
                    event2.settle();
                  }
                }
                if (this.left < -15) {
                  this.remove();
                  this.pipe2.remove();
                  pipes.remove(this);
                }
              };
            };
            event2.addPipe = setInterval(addPipe, 2500);
          } else if (speed > 0) {
            speed = -4;
          }
        };
        document.addEventListener(lib.config.touchscreen ? "touchstart" : "mousedown", fly);
        event2.settle = function() {
          clearInterval(event2.fly);
          clearInterval(event2.addPipe);
          document.removeEventListener(lib.config.touchscreen ? "touchstart" : "mousedown", fly);
          setTimeout(function() {
            event2.switchToAuto();
          }, 1e3);
        };
        game.pause();
        game.countChoose();
      };
      game.broadcastAll(createDialog, player2, event.videoId);
      if (event.isMine()) {
        chooseButton();
      } else if (event.isOnline()) {
        event.player.send(chooseButton);
        event.player.wait();
        game.pause();
      } else {
        switchToAuto();
      }
      await game.pause();
      game.broadcastAll(
        function(id, time) {
          if (_status.connectMode) {
            lib.configOL.choose_timeout = time;
          }
          var dialog = get.idDialog(id);
          if (dialog) {
            dialog.close();
          }
        },
        event.videoId,
        event.time
      );
      const result = event.result || event._result || {};
      player2.popup(get.cnNumber(result.score) + "分", result.win ? "wood" : "fire");
      game.log(player2, "御风飞行", result.win ? "#g成功" : "#y失败");
      game.log(player2, "获得了", "#g" + result.score + "分");
      const max = player2.countMark("yufeng");
      if (!result.win) {
        if (result.score) {
          await player2.draw(result.score);
        }
        if (max) {
          await player2.removeMark("yufeng", max, false);
        }
        return;
      }
      if (max < 2) {
        await player2.addMark("yufeng", 1, false);
      }
      const result2 = await player2.chooseTarget("请选择【御风】的目标", [1, result.score], function(card2, player3, target) {
        return target != player3 && !target.hasSkill(event.name + "_yufeng2");
      }).set("ai", function(target) {
        var player3 = _status.event.player;
        var att = -get.attitude(player3, target), attx = att * 2;
        if (att <= 0 || target.hasSkill("xinfu_pdgyingshi")) {
          return 0;
        }
        if (target.hasJudge("lebu")) {
          attx -= att;
        }
        if (target.hasJudge("bingliang")) {
          attx -= att;
        }
        return attx / Math.max(2.25, Math.sqrt(target.countCards("h") + 1));
      }).forResult();
      if (result2.bool) {
        result2.targets.sortBySeat();
        player2.line(result2.targets, "green");
        game.log(result2.targets, "获得了", "#y“御风”", "效果");
        for (const i of result2.targets) {
          i.addSkill(event.name + "_yufeng2");
        }
        if (result.score > result2.targets.length) {
          await player2.draw(result.score - result2.targets.length);
        }
      } else {
        await player2.draw(result.score);
      }
    },
    ai: {
      order: 10,
      result: { player: 1 },
      threaten: 3.2
    },
    subSkill: {
      yufeng2: {
        trigger: { player: "phaseZhunbeiBegin" },
        audio: false,
        forced: true,
        charlotte: true,
        sourceSkill: "yufeng_old",
        async content(event, trigger, player2) {
          player2.removeSkill("yufeng_old_yufeng2");
          const result = await player2.judge().forResult();
          switch (result.color) {
            case "red":
              player2.skip("phaseDraw");
              break;
            case "black":
              player2.skip("phaseUse");
              player2.skip("phaseDiscard");
              break;
          }
        },
        mark: true,
        intro: {
          content: "准备阶段时进行判定，结果为红则跳过摸牌阶段，为黑则跳过出牌阶段和弃牌阶段"
        },
        ai: {
          order: 7,
          result: { player: 1 }
        }
      }
    }
  },
  tianshu: {
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return player2.countCards("he") && !game.hasPlayer(function(current) {
        return current.countCards("ej", "taipingyaoshu");
      });
    },
    direct: true,
    async content(event, trigger, player2) {
      const result1 = await player2.chooseCardTarget({
        prompt: get.prompt2("tianshu"),
        filterCard: true,
        position: "he",
        ai1(card3) {
          return 5 - get.value(card3);
        },
        ai2(target2) {
          var player3 = _status.event.player;
          if (get.attitude(player3, target2) > 0 && !target2.hasEmptySlot(2)) {
            return 0;
          }
          return get.attitude(player3, target2);
        }
      }).forResult();
      if (!result1.bool) {
        return;
      }
      const target = result1.targets[0];
      player2.logSkill("tianshu", target);
      await player2.discard(result1.cards);
      let card2;
      if (!lib.inpile.includes("taipingyaoshu")) {
        lib.inpile.push("taipingyaoshu");
        card2 = game.createCard2("taipingyaoshu", "heart", 3);
      } else {
        card2 = get.cardPile(function(card3) {
          return card3.name == "taipingyaoshu";
        });
      }
      if (!card2) {
        return;
      }
      await target.gain(card2, "gain2");
      if (target.getCards("h").includes(card2) && get.name(card2, target) == "taipingyaoshu") {
        await target.chooseUseTarget(card2, "nopopup", true).forResult();
      }
    }
  },
  //界伏寿
  xinzhuikong: {
    audio: 2,
    trigger: { global: "phaseZhunbeiBegin" },
    check(event, player2) {
      if (get.attitude(player2, event.player) < -2) {
        var cards2 = player2.getCards("h");
        if (cards2.length > player2.hp) {
          return true;
        }
        for (var i = 0; i < cards2.length; i++) {
          var useful = get.useful(cards2[i]);
          if (useful < 5) {
            return true;
          }
          if (cards2[i].number > 7 && useful < 7) {
            return true;
          }
        }
      }
      return false;
    },
    logTarget: "player",
    filter(event, player2) {
      return !player2.hasSkill("xinzhuikong2") && player2.hp <= event.player.hp && player2.canCompare(event.player);
    },
    async content(event, trigger, player2) {
      player2.addTempSkill("xinzhuikong2", "roundStart");
      const result = await player2.chooseToCompare(trigger.player).set("small", player2.hp > 1 && get.effect(player2, { name: "sha" }, trigger.player, player2) > 0 && Math.random() < 0.9).forResult();
      if (result.bool) {
        trigger.player.addTempSkill("zishou2");
        return;
      }
      if (result.target && get.position(result.target) == "d") {
        await player2.gain(result.target, "gain2", "log");
      }
      const card2 = { name: "sha", isCard: true };
      if (trigger.player.canUse(card2, player2, false)) {
        await trigger.player.useCard(card2, player2, false);
      }
    }
  },
  xinzhuikong2: { charlotte: true },
  xinqiuyuan: {
    inherit: "qiuyuan",
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      const { card: card2 } = trigger;
      const result = await target.chooseToGive(
        (card3, player3) => {
          const name = get.name(card3, player3);
          return name != "sha" && get.type(name) == "basic";
        },
        `交给${get.translation(player2)}一张不为【杀】的基本牌，或成为${get.translation(card2)}的额外目标`,
        player2
      ).set("ai", (card3) => {
        const { player: player3, target: target2 } = get.event();
        return get.attitude(player3, target2) >= 0 ? 1 : -1;
      }).forResult();
      if (!result?.bool) {
        trigger.getParent().targets.push(target);
        trigger.getParent().triggeredTargets2.push(target);
        game.log(target, "成为了", card2, "的额外目标");
      }
    }
  },
  //界潘璋马忠
  xinduodao: {
    audio: 2,
    trigger: { player: "damageEnd" },
    logTarget: "source",
    filter(event, player2) {
      var source = event.source;
      if (!source) {
        return false;
      }
      var cards2 = source.getEquips(1);
      return cards2.some((card2) => lib.filter.canBeGained(card2, player2, source));
    },
    prompt2(event, player2) {
      var source = event.source;
      var cards2 = source.getEquips(1).filter((card2) => lib.filter.canBeGained(card2, player2, source));
      return "获得其装备区中的" + get.translation(cards2);
    },
    check(event, player2) {
      let es = event.source.getEquips(1).filter((card2) => {
        return lib.filter.canBeGained(card2, player2, event.source);
      });
      if (get.attitude(player2, event.source) > 0) {
        return es.reduce((acc, card2) => {
          return acc + get.value(card2, event.source);
        }, 0) < 0 || event.source.hasSkillTag("noe");
      }
      return es.reduce((acc, card2) => {
        return acc + get.value(card2, player2);
      }, 0);
    },
    async content(event, trigger, player2) {
      var source = trigger.source;
      var cards2 = source.getEquips(1).filter((card2) => lib.filter.canBeGained(card2, player2, source));
      player2.gain(cards2, source, "give", "bySelf");
    }
  },
  xinanjian: {
    audio: 2,
    trigger: { player: "useCardToPlayered" },
    forced: true,
    logTarget: "target",
    filter(event, player2) {
      return event.card.name == "sha" && !player2.inRangeOf(event.target);
    },
    async content(event, trigger, player2) {
      const card2 = get.translation(trigger.card);
      const target = get.translation(trigger.target);
      const result = await player2.chooseControl().set("prompt", "暗箭：请选择一项").set("choiceList", ["令" + target + "不能响应" + card2, "令" + card2 + "对" + target + "的伤害值基数+1"]).set("ai", function() {
        var target2 = _status.event.getTrigger().target;
        var player3 = _status.event.player;
        var num = target2.mayHaveShan(player3, "use") ? 0 : 1;
        if (get.attitude(player3, target2) > 0) {
          num = 1 - num;
        }
        return num;
      }).forResult();
      if (result.index == 0) {
        game.log(player2, "令", trigger.card, "不能被", trigger.target, "响应");
        trigger.directHit.push(trigger.target);
      } else {
        game.log(player2, "令", trigger.card, "对", trigger.target, "的伤害+1");
        const id = trigger.target.playerid;
        const map = trigger.customArgs;
        if (!map[id]) {
          map[id] = {};
        }
        if (!map[id].extraDamage) {
          map[id].extraDamage = 0;
        }
        map[id].extraDamage++;
      }
    },
    ai: {
      directHit_ai: true,
      skillTagFilter(player2, tag, arg) {
        if (!arg || !arg.card || !arg.target || arg.card.name != "sha" || arg.target.inRange(player2) || get.attitude(player2, arg.target) > 0) {
          return false;
        }
      }
    }
  },
  //界郭笨
  mobilejingce: {
    audio: 2,
    trigger: { player: "phaseJieshuBegin" },
    frequent: true,
    filter(event, player2) {
      var num = 0;
      game.getGlobalHistory("cardMove", function(evt) {
        if (evt.name != "cardsDiscard") {
          return;
        }
        var evtx = evt.getParent();
        if (evtx.name != "orderingDiscard") {
          return false;
        }
        var evt2 = evtx.relatedEvent || evtx.getParent();
        if (evt2 && (evt2.name == "useCard" || evt2.name == "respond")) {
          num += evt.cards.length;
        }
      });
      return num >= player2.hp;
    },
    async content(event, trigger, player2) {
      player2.draw(2);
    },
    group: "mobilejingce_count",
    intro: {
      content(num, player2) {
        if (num == 0) {
          return "一张都没有？就这？";
        }
        if (num < player2.hp) {
          return "才" + get.cnNumber(num) + "张？就这？";
        }
        return "卧槽，牛逼啊，居然" + get.cnNumber(num) + "张了！";
      }
    }
  },
  mobilejingce_count: {
    trigger: {
      global: ["cardsDiscardEnd", "phaseBefore"],
      player: "phaseAfter"
    },
    silent: true,
    firstDo: true,
    sourceSkill: "mobilejingce",
    filter(evt, player2) {
      if (evt.name == "phase") {
        return true;
      }
      if (player2 != _status.currentPhase) {
        return false;
      }
      var evtx = evt.getParent();
      if (evtx.name != "orderingDiscard") {
        return false;
      }
      var evt2 = evtx.relatedEvent || evtx.getParent();
      return evt2 && (evt2.name == "useCard" || evt2.name == "respond");
    },
    async content(event, trigger, player2) {
      if (trigger.name == "phase") {
        player2.unmarkSkill("mobilejingce");
      } else {
        var num = 0;
        game.getGlobalHistory("cardMove", function(evt) {
          if (evt.name != "cardsDiscard") {
            return;
          }
          var evtx = evt.getParent();
          if (evtx.name != "orderingDiscard") {
            return false;
          }
          var evt2 = evtx.relatedEvent || evtx.getParent();
          if (evt2 && (evt2.name == "useCard" || evt2.name == "respond")) {
            num += evt.cards.length;
          }
        });
        player2.storage.mobilejingce = num;
        player2.markSkill("mobilejingce");
      }
    }
  },
  //公孙康
  juliao: {
    mod: {
      globalTo(from, to, distance) {
        return distance + game.countGroup() - 1;
      }
    }
  },
  taomie: {
    audio: 3,
    group: ["taomie1", "taomie2", "taomie3"],
    trigger: { source: "damageBegin1" },
    forced: true,
    locked: false,
    direct: true,
    filter(event, player2) {
      return event.player.hasMark("taomie");
    },
    async content(event, trigger, player2) {
      player2.logSkill(Math.random() < 0.5 ? "taomie2" : "taomie3", trigger.player);
      const target = get.translation(trigger.player);
      const result1 = await player2.chooseControl().set("prompt", "讨灭：请选择一项").set("choiceList", ["令即将对" + target + "造成的伤害+1", "获得" + target + "的一张牌，并可将其交给另一名其他角色", "依次执行以上所有选项，并移去" + target + "的“讨灭”标记"]).set("ai", function() {
        var evt = _status.event.getTrigger();
        var player3 = _status.event.player;
        var target2 = evt.player;
        var bool1 = !target2.hasSkillTag("filterDamage", null, {
          player: player3,
          card: evt.card
        });
        var bool2 = get.effect(target2, { name: "shunshou" }, player3, player3) > 0;
        if (bool1 && bool2 && target2.hp <= evt.num + 1) {
          return 2;
        }
        if (bool1) {
          return 0;
        }
        return 1;
      }).forResult();
      if (result1.index == 2) {
        trigger.taomie_player = trigger.player;
        trigger.player.addTempSkill("taomie4");
      }
      if (result1.index != 1) {
        trigger.num++;
      }
      if (result1.index == 0 || !trigger.player.countGainableCards(player2, "hej")) {
        return;
      }
      const result2 = await player2.gainPlayerCard(trigger.player, "hej", true).forResult();
      const card2 = result2.cards[0];
      if (card2 && player2.getCards("h").includes(card2) && game.hasPlayer(function(current) {
        return current != player2 && current != trigger.player;
      })) {
        event.card = card2;
        const result3 = await player2.chooseTarget("是否将" + get.translation(card2) + "交给一名其他角色？", function(card3, player3, target2) {
          return target2 != player3 && target2 != _status.event.getTrigger().player;
        }).set("ai", function(target2) {
          const event2 = get.event();
          const { player: player3, giveCheck } = event2;
          const card3 = event2.getParent().card;
          if (target2.hasSkillTag("nogain") || !player3.needsToDiscard() || giveCheck === false) {
            return 0;
          }
          if (typeof giveCheck === "number") {
            if (target2.getUseValue(card3) < giveCheck) {
              return 0;
            }
          }
          return get.attitude(player3, target2) / (1 + target2.countCards("h"));
        }).set(
          "giveCheck",
          (function() {
            if (!player2.needsToDiscard() || player2.hasValueTarget(card2, null, false)) {
              return false;
            }
            return player2.getUseValue(card2);
          })()
        ).forResult();
        if (result3.bool) {
          const target3 = result3.targets[0];
          player2.line(target3);
          await player2.give(card2, target3);
        }
      }
    },
    mod: {
      inRangeOf(from, to) {
        if (from.hasMark("taomie")) {
          return true;
        }
      },
      inRange(from, to) {
        if (to.hasMark("taomie")) {
          return true;
        }
      }
    },
    intro: {
      content: "mark"
    },
    ai: {
      effect: {
        player(card2, player2, target) {
          if (target && get.tag(card2, "damage") && target.hasMark("taomie")) {
            if (player2.hasSkillTag("jueqing", false, target)) {
              return;
            }
            if (get.attitude(player2, target) > 0) {
              return 0.7;
            }
            return 1.2;
          }
        }
      }
    }
  },
  taomie1: {
    audio: true,
    trigger: {
      player: "damageEnd",
      source: "damageSource"
    },
    sourceSkill: "taomie",
    logTarget(trigger, player2) {
      if (player2 == trigger.player) {
        return trigger.source;
      }
      return trigger.player;
    },
    filter(event, player2) {
      var target = lib.skill.taomie1.logTarget(event, player2);
      return target && target.isIn() && !target.hasMark("taomie");
    },
    check(event, player2) {
      var target = lib.skill.taomie1.logTarget(event, player2);
      if (get.attitude(player2, target) > 0) {
        return false;
      }
      var target0 = game.findPlayer(function(current) {
        return current.hasMark("taomie");
      });
      if (!target0) {
        return true;
      }
      var eff1 = 0, eff2 = 0;
      player2.countCards("h", function(card2) {
        if (!get.tag(card2, "damage")) {
          return false;
        }
        if (player2.hasValueTarget(card2, null, true) > 0) {
          if (player2.canUse(card2, target, null, true)) {
            var eff = get.effect(target, card2, player2, player2);
            if (eff > 0) {
              eff1 += eff;
            }
          }
          if (player2.canUse(card2, target0, null, true)) {
            var eff = get.effect(target0, card2, player2, player2);
            if (eff > 0) {
              eff2 += eff;
            }
          }
        }
      });
      return eff1 > eff2;
    },
    prompt2(event, player2) {
      var target = lib.skill.taomie1.logTarget(event, player2);
      var str = "令" + get.translation(target) + "获得“讨灭”标记";
      if (game.hasPlayer(function(current) {
        return current.hasMark("taomie");
      })) {
        str += "，并移去场上已有的“讨灭”标记";
      }
      return str;
    },
    async content(event, trigger, player2) {
      game.countPlayer(function(current) {
        var num = current.countMark("taomie");
        if (num) {
          current.removeMark("taomie");
        }
      });
      lib.skill.taomie1.logTarget(trigger, player2).addMark("taomie", 1);
    }
  },
  taomie2: { audio: true },
  taomie3: { audio: true },
  taomie4: {
    trigger: {
      global: ["damageAfter", "damageCancelled", "damageZero"],
      player: "dieBegin"
    },
    forced: true,
    popup: false,
    charlotte: true,
    sourceSkill: "taomie",
    filter(event, player2) {
      return player2.hasMark("taomie") && (event.name == "die" || event.taomie_player == player2);
    },
    async content(event, trigger, player2) {
      player2.removeMark("taomie", player2.countMark("taomie"));
      player2.removeSkill("taomie2");
    }
  },
  //铁骑飞
  liyong: {
    audio: "retishen",
    trigger: { player: "shaMiss" },
    forced: true,
    filter(event, player2) {
      return player2.isPhaseUsing();
    },
    async content(event, trigger, player2) {
      trigger.getParent().liyong = true;
      player2.addTempSkill("liyong2", "phaseUseEnd");
    }
  },
  liyong2: {
    audio: "retishen",
    mark: true,
    intro: {
      content: "铁骑！强命！加伤！然后掉血嘞…"
    },
    trigger: { player: "useCardToPlayered" },
    forced: true,
    sourceSkill: "liyong",
    filter(event, player2) {
      if (!event.card || event.card.name != "sha") {
        return false;
      }
      var evt = event.getParent();
      if (evt.liyong) {
        return false;
      }
      var history = player2.getHistory("useCard", function(evt3) {
        return evt3.card.name == "sha";
      });
      var evt2 = history[history.indexOf(evt) - 1];
      return evt2 && evt2.liyong;
    },
    logTarget: "target",
    async content(event, trigger, player2) {
      var target = trigger.target;
      target.addTempSkill("fengyin");
      trigger.directHit.add(target);
      var id = target.playerid;
      var map = trigger.customArgs;
      if (!map[id]) {
        map[id] = {};
      }
      if (!map[id].extraDamage) {
        map[id].extraDamage = 0;
      }
      map[id].extraDamage++;
      trigger.getParent().liyong2 = true;
    },
    group: ["liyong3", "liyong4"]
  },
  liyong3: {
    trigger: { source: "damageSource" },
    forced: true,
    popup: false,
    sourceSkill: "liyong",
    filter(event, player2) {
      return event.card && event.card.name == "sha" && event.player.isIn() && event.getParent(2).liyong2 == true;
    },
    async content(event, trigger, player2) {
      player2.loseHp();
    }
  },
  liyong4: {
    trigger: { player: "useCardAfter" },
    forced: true,
    silent: true,
    sourceSkill: "liyong",
    filter(evt, player2) {
      if (!evt.card || evt.card.name != "sha") {
        return false;
      }
      if (evt.liyong) {
        return false;
      }
      var history = player2.getHistory("useCard", function(evt3) {
        return evt3.card.name == "sha";
      });
      var evt2 = history[history.indexOf(evt) - 1];
      return evt2 && evt2.liyong;
    },
    async content(event, trigger, player2) {
      player2.removeSkill("liyong2");
    }
  },
  //韩遂
  xinniluan: {
    audio: "niluan",
    trigger: { global: "phaseJieshuBegin" },
    direct: true,
    filter(event, player2) {
      const target = event.player;
      if (!target.isIn() || !target.hasHistory("useCard", (evt) => evt.targets?.some((targetx) => targetx != target))) {
        return false;
      }
      if (get.mode() == "versus" && _status.mode == "two") {
        return player2.getEnemies().includes(target);
      }
      return player2 != target;
    },
    clearTime: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseToUse({
        logSkill: "xinniluan",
        preTarget: trigger.player,
        prompt: "是否发动【逆乱】，对" + get.translation(trigger.player) + "使用一张【杀】？",
        filterCard(card2, player3) {
          return get.name(card2) == "sha" && lib.filter.filterCard.apply(this, arguments);
        },
        filterTarget(card2, player3, target) {
          return target == _status.event.preTarget && lib.filter.targetEnabled.apply(this, arguments);
        },
        addCount: false
      }).forResult();
      if (result.bool && player2.getHistory("sourceDamage", function(evt) {
        return evt.getParent(4) == event;
      }).length && trigger.player.countDiscardableCards(player2, "he") > 0) {
        const discardEvent = player2.discardPlayerCard(trigger.player, true, "he");
        discardEvent.boolline = true;
        await discardEvent;
      }
    }
  },
  xiaoxi_hansui: {
    audio: 2,
    enable: ["chooseToRespond", "chooseToUse"],
    filterCard(card2, player2) {
      return get.color(card2) == "black";
    },
    position: "hse",
    viewAs: { name: "sha" },
    viewAsFilter(player2) {
      if (!player2.countCards("hse", { color: "black" })) {
        return false;
      }
    },
    prompt: "将一张黑色牌当杀使用或打出",
    check(card2) {
      return 4.5 - get.value(card2);
    },
    ai: {
      skillTagFilter(player2) {
        if (!player2.countCards("hes", { color: "black" })) {
          return false;
        }
      },
      respondSha: true
    }
  },
  //胡车儿
  daoji: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.countCards("he", function(card2) {
        return get.type(card2) != "basic";
      }) && game.hasPlayer(function(target) {
        return target != player2 && target.countGainableCards(player2, "e") > 0;
      });
    },
    filterCard(card2) {
      return get.type(card2) != "basic";
    },
    position: "he",
    filterTarget(card2, player2, target) {
      return target != player2 && target.countGainableCards(player2, "e") > 0;
    },
    check(card2) {
      var player2 = _status.event.player;
      if (game.hasPlayer(function(current) {
        return current != player2 && get.attitude(player2, current) < 0 && get.damageEffect(current, player2, player2) > 0 && current.getEquip(1);
      })) {
        return 8 - get.value(card2);
      }
      return 5 - get.value(card2);
    },
    async content(event, trigger, player2) {
      const { target } = event;
      const result = await player2.gainPlayerCard(target, "e", true).set("ai", function(button) {
        const card3 = button.link;
        const player3 = _status.event.player;
        if (get.subtype(card3) == "equip1" && get.damageEffect(_status.event.target, player3, player3) > 0) {
          return 6 + get.value(card3);
        }
        return get.value(card3);
      }).forResult();
      if (!result?.bool || !result.cards?.length) {
        return;
      }
      const card2 = result.cards[0];
      if (player2.getCards("h").includes(card2) && get.type(card2) == "equip") {
        await player2.chooseUseTarget(card2, true, "nopopup");
      }
      if (get.subtype(card2, false) == "equip1") {
        await target.damage();
      }
    },
    ai: {
      order: 6,
      result: {
        target(player2, current) {
          if (get.damageEffect(current, player2, player2) > 0 && current.getEquip(1)) {
            return -1.5;
          }
          return -1;
        }
      }
    }
  },
  //司马师夫妇
  //垃圾
  baiyi: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filterTarget: lib.filter.notMe,
    selectTarget: 2,
    limited: true,
    skillAnimation: false,
    //animationColor:'thunder',
    filter(event, player2) {
      return player2.isDamaged() && game.players.length > 2;
    },
    multitarget: true,
    multiline: true,
    seatRelated: "changeSeat",
    async contentBefore(event, trigger, player2) {
      player2.$fullscreenpop("败移", "thunder");
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      game.broadcastAll(
        function(target1, target2) {
          game.swapSeat(target1, target2);
        },
        targets[0],
        targets[1]
      );
    },
    ai: {
      order() {
        return get.order({ name: "tao" }) + 1;
      },
      result: {
        target(player2, target) {
          if (player2.hasUnknown() && target != player2.next && target != player2.previous) {
            return 0;
          }
          var distance = Math.pow(get.distance(player2, target, "absolute"), 2);
          if (!ui.selected.targets.length) {
            return distance;
          }
          var distance2 = Math.pow(get.distance(player2, ui.selected.targets[0], "absolute"), 2);
          return Math.min(0, distance - distance2);
        }
      }
    }
  },
  jinglve: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      if (player2.hasSkill("jinglve2")) {
        return false;
      }
      return game.hasPlayer(function(current) {
        return current != player2 && current.countCards("h") > 0;
      });
    },
    filterTarget(card2, player2, target) {
      return target != player2 && target.countCards("h") > 0;
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      event.target = target;
      player2.markAuto("jinglve4", [target]);
      const result = await player2.chooseButton(["选择一张牌作为「死士」", target.getCards("h")], true).set("ai", function(button) {
        var target2 = _status.event.getParent().target;
        var card2 = button.link;
        var val = target2.getUseValue(card2);
        if (val > 0) {
          return val;
        }
        return get.value(card2);
      }).forResult();
      if (result.bool) {
        player2.storage.jinglve2 = target;
        player2.storage.jinglve3 = result.links[0];
        player2.addSkill("jinglve2");
      }
    },
    ai: {
      order: 12,
      result: {
        target: -1
      }
    }
  },
  jinglve2: {
    mark: true,
    intro: {
      name: "死士",
      mark(dialog, content, player2) {
        dialog.addText("记录目标");
        dialog.add([content]);
        if (player2 == game.me || player2.isUnderControl()) {
          dialog.addText("死士牌");
          dialog.add([player2.storage.jinglve3]);
        }
      }
    },
    sourceSkill: "jinglve",
    onremove(player2) {
      delete player2.storage.jinglve2;
      delete player2.storage.jinglve3;
    },
    trigger: { global: ["dieEnd", "loseEnd", "gainEnd"] },
    silent: true,
    lastDo: true,
    charlotte: true,
    filter(event, player2) {
      if (event.name != "gain" && event.player != player2.storage.jinglve2) {
        return false;
      }
      return event.name == "die" || event.cards.includes(player2.storage.jinglve3) && (event.name == "gain" || event.position != ui.ordering && event.position != ui.discardPile);
    },
    async content(event, trigger, player2) {
      player2.removeSkill("jinglve2");
    },
    group: "jinglve3"
  },
  jinglve3: {
    audio: "jinglve",
    trigger: {
      global: ["loseAfter", "useCard", "phaseAfter", "cardsDiscardAfter", "loseAsyncAfter"]
    },
    sourceSkill: "jinglve",
    filter(event, player2) {
      if (event.player && event.player != player2.storage.jinglve2) {
        return false;
      }
      var card2 = player2.storage.jinglve3;
      if (event.name == "phase") {
        return event.player.getCards("hej").includes(card2);
      }
      if (event.name == "useCard") {
        return event.cards.includes(card2);
      }
      return get.position(card2, true) == "d" && event.getd().includes(card2);
    },
    forced: true,
    charlotte: true,
    logTarget: "player",
    async content(event, trigger, player2) {
      if (trigger.name == "useCard") {
        trigger.all_excluded = true;
        trigger.targets.length = 0;
      } else {
        if (trigger.name == "phase") {
          player2.gain(player2.storage.jinglve3, trigger.player, "giveAuto", "bySelf");
        } else if (get.position(player2.storage.jinglve3, true) == "d") {
          player2.gain(player2.storage.jinglve3, "gain2");
        }
      }
      player2.removeSkill("jinglve2");
    }
  },
  shanli: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    juexingji: true,
    forced: true,
    skillAnimation: true,
    animationColor: "thunder",
    filter(event, player2) {
      return player2.storage.baiyi && player2.getStorage("jinglve4").length > 1;
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      await player2.loseMaxHp();
      const result1 = await player2.chooseTarget(true, "选择【擅立】的目标").set("ai", function(target2) {
        var att = get.attitude(_status.event.player, target2);
        if (target2 == game.me || target2.isUnderControl() && target2.isOnline()) {
          return 2 * att;
        }
        return att;
      }).forResult();
      const target = result1.targets[0];
      player2.line(target, "green");
      game.log(player2, "拥立", target);
      var list = [];
      if (!_status.characterlist) {
        if (_status.connectMode) {
          var list = get.charactersOL();
        } else {
          var list = [];
          for (var i in lib.character) {
            if (lib.filter.characterDisabled2(i) || lib.filter.characterDisabled(i)) {
              continue;
            }
            list.push(i);
          }
        }
        game.countPlayer2(function(current) {
          list.remove(current.name);
          list.remove(current.name1);
          list.remove(current.name2);
          if (current.storage.rehuashen && current.storage.rehuashen.character) {
            list.removeArray(current.storage.rehuashen.character);
          }
        });
        _status.characterlist = list;
      }
      _status.characterlist.randomSort();
      var chara = [];
      var skills2 = [];
      for (var i of _status.characterlist) {
        if (i == "key_yuri") {
          continue;
        }
        var character = lib.character[i];
        if (character && character[3]) {
          for (var j of character[3]) {
            if (skills2.includes(j) || j == "yuri_wangxi" || target.hasSkill(j)) {
              continue;
            }
            var info = get.info(j);
            if (info && info.zhuSkill) {
              skills2.add(j);
              chara.add(i);
              continue;
            }
          }
        }
        if (skills2.length >= 3) {
          break;
        }
      }
      if (!skills2.length) {
        return;
      }
      const result2 = await player2.chooseControl(skills2).set("dialog", ["选择令" + get.translation(target) + "获得一个技能", [chara, "character"]]).forResult();
      target.addSkills(result2.control);
      target.setAvatarQueue(target.name1 || target.name, [chara[skills2.indexOf(result2.control)]]);
    },
    ai: {
      combo: "baiyi"
    }
  },
  hongyi: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filterTarget: lib.filter.notMe,
    async content(event, trigger, player2) {
      const skill = event.name + "_effect", target = event.target;
      player2.addTempSkill(skill, { player: "phaseBeginStart" });
      player2.markAuto(skill, target);
    },
    ai: {
      order: 1,
      result: {
        target(player2, target) {
          if (target.hasJudge("lebu")) {
            return -0.5;
          }
          return -1 - target.countCards("h");
        }
      }
    },
    subSkill: {
      effect: {
        audio: "hongyi",
        trigger: { global: "damageBegin1" },
        charlotte: true,
        forced: true,
        logTarget: "source",
        filter(event, player2) {
          return event.source && player2.getStorage("hongyi_effect").includes(event.source);
        },
        async content(event, trigger, player2) {
          const result = await trigger.source.judge().forResult();
          if (result.color == "black") {
            trigger.num--;
          } else {
            await trigger.player.draw();
          }
        },
        onremove: true,
        intro: {
          content: "已选中$为技能目标"
        }
      }
    }
  },
  requanfeng: {
    audio: "quanfeng",
    enable: "chooseToUse",
    limited: true,
    skillAnimation: true,
    animationColor: "thunder",
    prompt2: "（限定技）失去技能【劝封】，并获得该角色武将牌上的所有技能，然后加1点体力上限并回复1点体力",
    logTarget: "player",
    trigger: { global: "die" },
    check: (event, player2) => {
      if (event.player.getStockSkills("仲村由理", "天下第一").filter((skill) => {
        let info = get.info(skill);
        return info && !info.hiddenSkill && !info.zhuSkill && !info.charlotte;
      }).some((i) => {
        let info = get.info(i);
        if (info && info.ai) {
          return info.ai.neg || info.ai.halfneg;
        }
      })) {
        return false;
      }
      return true;
    },
    filter(event, player2) {
      if (event.name == "die") {
        return player2.hasSkill("hongyi") && event.player.getStockSkills("仲村由理", "天下第一").filter(function(skill) {
          var info = get.info(skill);
          return info && !info.hiddenSkill && !info.zhuSkill && !info.charlotte;
        }).length > 0;
      }
      return event.type == "dying" && player2 == event.dying;
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      if (trigger?.name == "die") {
        await player2.removeSkills("hongyi");
        const skills2 = trigger.player.getStockSkills("仲村由理", "天下第一").filter(function(skill) {
          const info = get.info(skill);
          return info && !info.hiddenSkill && !info.zhuSkill && !info.charlotte;
        });
        if (skills2.length) {
          await player2.addSkills(skills2);
          game.broadcastAll(function(list) {
            game.expandSkills(list);
            for (const i of list) {
              const info = lib.skill[i];
              if (!info) {
                continue;
              }
              if (!info.audioname2) {
                info.audioname2 = {};
              }
              info.audioname2.yanghuiyu = "quanfeng";
            }
          }, skills2);
        }
        await player2.gainMaxHp();
        await player2.recover();
      } else {
        await player2.gainMaxHp(2);
        await player2.recover(4);
      }
    },
    ai: {
      save: true,
      skillTagFilter(player2, tag, arg) {
        return player2 == arg;
      },
      order: 10,
      result: {
        player: 1
      }
    }
  },
  quanfeng: {
    audio: 2,
    trigger: { global: "die" },
    filter(event, player2) {
      return event.player.getStockSkills("仲村由理", "天下第一").filter(function(skill) {
        var info = get.info(skill);
        return info && !info.juexingji && !info.hiddenSkill && !info.zhuSkill && !info.charlotte && !info.limited && !info.dutySkill;
      }).length > 0;
    },
    logTarget: "player",
    skillAnimation: true,
    limited: true,
    forced: true,
    animationColor: "thunder",
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      const list = trigger.player.getStockSkills("仲村由理", "天下第一").filter(function(skill) {
        var info = get.info(skill);
        return info && !info.juexingji && !info.hiddenSkill && !info.zhuSkill && !info.charlotte && !info.limited && !info.dutySkill;
      });
      let control;
      if (list.length == 1) {
        control = list[0];
      } else {
        const result = await player2.chooseControl(list).set("prompt", "选择获得" + get.translation(trigger.player) + "的一个技能").set("forceDie", true).set("ai", function() {
          return list.randomGet();
        }).forResult();
        control = result.control;
      }
      player2.addSkills(control);
      game.broadcastAll(function(skill) {
        var list2 = [skill];
        game.expandSkills(list2);
        for (var i of list2) {
          var info = lib.skill[i];
          if (!info) {
            continue;
          }
          if (!info.audioname2) {
            info.audioname2 = {};
          }
          info.audioname2.yanghuiyu = "quanfeng";
        }
      }, control);
      await player2.gainMaxHp();
      await player2.recover();
    }
  },
  //手杀界朱然
  //设计师你改技能有瘾🐴
  mobiledanshou: {
    trigger: { global: "phaseJieshuBegin" },
    audio: 2,
    direct: true,
    filter(event, player2) {
      if (player2 == event.player) {
        return false;
      }
      var num = event.player.getHistory("useCard", function(evt) {
        return evt.targets.includes(player2);
      }).length;
      return num == 0 || event.player.isIn() && num <= player2.countCards("he");
    },
    async content(event, trigger, player2) {
      const num = trigger.player.getHistory("useCard", function(evt) {
        return evt.targets.includes(player2);
      }).length;
      event.num = num;
      if (num == 0) {
        let bool;
        if (player2.hasSkill("mobiledanshou")) {
          bool = true;
        } else {
          const result = await player2.chooseBool("是否发动【胆守】摸一张牌？", lib.translate.mobiledanshou_info).forResult();
          bool = result.bool;
        }
        if (bool) {
          player2.logSkill("mobiledanshou");
          await player2.draw();
        }
        return;
      }
      const result2 = await player2.chooseToDiscard(num, get.prompt("mobiledanshou", trigger.player), "弃置" + get.translation(num) + "张牌并对其造成1点伤害", "he").set("ai", function(card2) {
        if (!_status.event.goon) {
          return 0;
        }
        var num2 = _status.event.getParent().num;
        if (num2 == 1) {
          return 8 - get.value(card2);
        }
        if (num2 == 2) {
          return 6.5 - get.value(card2);
        }
        return 5 - get.value(card2);
      }).set("goon", get.damageEffect(trigger.player, player2, player2) > 0).set("logSkill", ["mobiledanshou", trigger.player]).forResult();
      if (result2.bool) {
        player2.addExpose(0.2);
        await trigger.player.damage();
      }
    }
  },
  //丁原
  //程序员和设计师至少有一个脑子有坑
  beizhu: {
    audio: 3,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.hasPlayer(function(target) {
        return lib.skill.beizhu.filterTarget(null, player2, target);
      });
    },
    filterTarget(card2, player2, target) {
      return target != player2 && target.countCards("h") > 0;
    },
    logAudio: () => 2,
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      player2.addTempSkill("beizhu_draw");
      player2.viewHandcards(target);
      let cards2 = target.getCards("h", "sha");
      if (cards2.length) {
        while (cards2.length) {
          const card2 = cards2.randomRemove(1)[0];
          const useEvent = target.useCard(player2, false, card2);
          useEvent.card.beizhu = true;
          await useEvent;
          const hs = target.getCards("h");
          cards2 = cards2.filter(function(card3) {
            return hs.includes(card3) && get.name(card3, target) == "sha" && target.canUse(
              {
                name: "sha",
                isCard: true,
                cards: [card3]
              },
              player2,
              false
            );
          });
        }
        return;
      }
      await player2.discardPlayerCard("he", target, "visible", true);
      const result = await player2.chooseBool("是否令" + get.translation(target) + "获得一张【杀】？").set("choice", get.attitude(player2, target) > 0).forResult();
      if (result.bool) {
        const card2 = get.cardPile2(function(card3) {
          return card3.name == "sha";
        });
        if (card2) {
          await target.gain(card2, "gain2");
        }
      } else {
        return;
      }
      game.updateRoundNumber();
    },
    ai: {
      order: 7,
      threaten: 1.14 + 5.14,
      result: {
        player(player2, target) {
          var eff = get.effect(target, { name: "guohe_copy2" }, player2, player2);
          var cards2 = target.getCards("h", { name: "sha" });
          if (!cards2.length) {
            return eff;
          }
          return eff / (cards2.length + 3);
        }
      }
    }
  },
  beizhu_draw: {
    charlotte: true,
    audio: "beizhu3.mp3",
    trigger: { player: "damageEnd" },
    sourceSkill: "beizhu",
    filter(event, player2) {
      return event.card?.beizhu;
    },
    forced: true,
    async content(event, trigger, player2) {
      player2.draw(trigger.num);
    }
  },
  //新简雍
  xinqiaoshui: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filterTarget(card2, player2, target) {
      return player2.canCompare(target);
    },
    filter(event, player2) {
      return player2.countCards("h") > 0;
    },
    async content(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      const result = await player2.chooseToCompare(target).forResult();
      if (result.bool) {
        player2.addTempSkill("xinqiaoshui_target", "phaseUseEnd");
      } else {
        player2.addTempSkill("qiaoshui2", "phaseUseEnd");
      }
    },
    subSkill: {
      target: {
        audio: "xinqiaoshui",
        inherit: "qiaoshui3",
        sourceSkill: "xinqiaoshui"
      }
    },
    ai: {
      order(item, player2) {
        if (player2.countCards("h", function(card2) {
          return player2.hasValueTarget(card2);
        })) {
          return 10;
        }
        return 1;
      },
      result: {
        target(player2, target) {
          if (player2.countCards("h", function(card2) {
            return player2.hasValueTarget(card2);
          })) {
            if (player2.hasSkill("xinqiaoshui_target")) {
              return 0;
            }
            var nd = !player2.needsToDiscard();
            if (player2.hasCard(function(card2) {
              if (get.position(card2) != "h") {
                return false;
              }
              var val = get.value(card2);
              if (nd && val < 0) {
                return true;
              }
              if (val <= 5) {
                return card2.number >= 12;
              }
              if (val <= 6) {
                return card2.number >= 13;
              }
              return false;
            })) {
              return -1;
            }
            return 0;
          }
          return -1;
        }
      }
    }
  },
  xinjyzongshi: {
    audio: 2,
    trigger: {
      global: ["chooseToCompareAfter", "compareMultipleAfter"]
    },
    filter(event, player2) {
      if (event.preserve) {
        return false;
      }
      if (event.compareMultiple) {
        return false;
      }
      if (event.compareMeanwhile) {
        const index = [...event.targets, event.player].indexOf(player2);
        return index >= 0;
      }
      return [event.player, event.target].some((current) => current == player2);
    },
    forced: true,
    locked: false,
    async content(event, trigger, player2) {
      let str = '<div class="text center">牌堆顶';
      const cards2 = get.cards(1, true);
      if (trigger.name == "chooseToCompare" && trigger.compareMeanwhile) {
        const result2 = trigger.result;
        const list = [[result2.num1[0], result2.player]];
        list.addArray(
          result2.num2.map(function(card2, i) {
            return [card2, result2.targets[i]];
          })
        );
        list.sort(function(a, b) {
          return a[0] - b[0];
        });
        if (list[0][0] < list[1][0] && get.position(list[0][1], true) == "o") {
          str += "/拼点牌";
          cards2.push(list[0][1]);
        }
      } else {
        if (player2 == trigger.player) {
          if (trigger.num1 > trigger.num2 && get.position(trigger.card2, true) == "o") {
            str += "/拼点牌";
            cards2.push(trigger.card2);
          } else if (trigger.num1 < trigger.num2 && get.position(trigger.card1, true) == "o") {
            str += "/拼点牌";
            cards2.push(trigger.card1);
          }
        } else {
          if (trigger.num1 < trigger.num2 && get.position(trigger.card1, true) == "o") {
            str += "/拼点牌";
            cards2.push(trigger.card1);
          } else if (trigger.num1 > trigger.num2 && get.position(trigger.card2, true) == "o") {
            str += "/拼点牌";
            cards2.push(trigger.card2);
          }
        }
      }
      str += "</div>";
      const result = await player2.chooseButton(["纵适：选择要获得的牌", str, cards2]).set("ai", get.buttonValue).forResult();
      if (result.bool) {
        const draw = result.links[0] == cards2[0];
        game.log(player2, "获得了", draw ? "牌堆顶的一张牌" : result.links);
        await player2.gain(result.links, draw ? "draw" : "gain2").set("log", false);
      }
    }
  },
  //通渠张恭
  rezhenxing: {
    audio: "xinfu_zhenxing",
    trigger: {
      player: ["damageEnd", "phaseJieshuBegin"]
    },
    frequent: true,
    async content(event, trigger, player2) {
      const cards2 = get.cards(3, true);
      const result = await player2.chooseButton(["【镇行】：请选择要获得的牌", cards2]).set("filterButton", function(button) {
        var cards3 = _status.event.cards;
        for (var i = 0; i < cards3.length; i++) {
          if (button.link != cards3[i] && get.suit(cards3[i]) == get.suit(button.link)) {
            return false;
          }
        }
        return true;
      }).set("ai", function(button) {
        return get.value(button.link);
      }).set("cards", cards2).forResult();
      if (result.bool && result.links?.length) {
        await player2.gain(result.links, "gain2");
      }
    }
  },
  //芙蓉，手杀界廖化，手杀界曹彰
  rejiangchi: {
    audio: 2,
    trigger: {
      player: "phaseUseBegin"
    },
    direct: true,
    logAudio: (index) => typeof index === "number" ? "rejiangchi" + index + ".mp3" : 2,
    async content(event, trigger, player2) {
      const list = ["弃牌", "摸牌", "取消"];
      if (!player2.countCards("he")) {
        list.remove("弃牌");
      }
      const result = await player2.chooseControl(list, function() {
        var player3 = _status.event.player;
        if (list.includes("弃牌")) {
          if (player3.countCards("h") > 3 && player3.countCards("h", "sha") > 1) {
            return "弃牌";
          }
          if (player3.countCards("h", "sha") > 2) {
            return "弃牌";
          }
        }
        if (!player3.countCards("h", "sha")) {
          return "摸牌";
        }
        return "cancel2";
      }).set("prompt", get.prompt2("rejiangchi")).forResult();
      player2.logSkill("rejiangchi", null, null, null, [result.control == "弃牌" ? 2 : 1]);
      if (result.control == "弃牌") {
        await player2.chooseToDiscard(true, "he").forResult();
        player2.addTempSkill("jiangchi2", "phaseUseEnd");
      } else if (result.control == "摸牌") {
        await player2.draw();
        player2.addTempSkill("rejiangchi3", "phaseUseEnd");
      }
    }
  },
  rejiangchi3: {
    mod: {
      cardEnabled(card2) {
        if (card2.name == "sha") {
          return false;
        }
      }
    }
  },
  refuli: {
    skillAnimation: true,
    animationColor: "soil",
    audio: 2,
    limited: true,
    enable: "chooseToUse",
    filter(event, player2) {
      if (event.type != "dying") {
        return false;
      }
      if (player2 != event.dying) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      const num = game.countGroup();
      await player2.recoverTo(num);
      if (player2.isMaxHp(true)) {
        await player2.turnOver();
      }
    },
    ai: {
      save: true,
      skillTagFilter(player2, arg, target) {
        return player2 == target;
      },
      result: { player: 10 },
      threaten(player2, target) {
        if (!target.storage.refuli) {
          return 0.9;
        }
      }
    }
  },
  redangxian: {
    trigger: { player: "phaseBegin" },
    forced: true,
    audio: "dangxian",
    audioname: ["xin_liaohua"],
    audioname2: {
      guansuo: "dangxian_guansuo",
      re_baosanniang: "dangxian_re_baosanniang",
      tw_baosanniang: "dangxian_re_baosanniang"
    },
    async content(event, trigger, player2) {
      const card2 = get.discardPile((card3) => card3.name == "sha");
      if (card2) {
        await player2.gain(card2, "gain2");
      }
      game.updateRoundNumber();
      trigger.phaseList.splice(trigger.num, 0, `phaseUse|${event.name}`);
    }
  },
  xuewei: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    direct: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt2("xuewei"), lib.filter.notMe).set("ai", function(target) {
        var player3 = _status.event.player;
        if (player3 == get.zhu(player3) && player3.hp <= 2) {
          return 0;
        }
        return get.attitude(player3, target) - 4;
      }).set("animate", false).forResult();
      if (result.bool) {
        const target = result.targets[0];
        player2.logSkill("xuewei");
        player2.addTempSkill("xuewei2", { player: "phaseBegin" });
        player2.storage.xuewei2 = target;
      }
    },
    ai: {
      threaten: 1.05
    }
  },
  xuewei2: {
    audio: "xuewei",
    forced: true,
    onremove: true,
    trigger: { global: "damageBegin4" },
    charlotte: true,
    sourceSkill: "xuewei",
    filter(event, player2) {
      return event.player == player2.storage.xuewei2;
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      player2.removeSkill("xuewei2");
      trigger.cancel();
      player2.damage(trigger.num, trigger.source || "nosource");
      if (trigger.source && trigger.source.isIn()) {
        trigger.source.damage(trigger.num, trigger.nature, player2);
      }
    }
  },
  liechi: {
    trigger: { player: "dying" },
    forced: true,
    filter(event, player2) {
      return event.getParent().name == "damage" && event.source && event.source.countCards("he");
    },
    audio: 2,
    async content(event, trigger, player2) {
      trigger.source.chooseToDiscard("he", true);
    }
  },
  rejiuchi: {
    group: ["jiuchi"],
    audioname: ["re_dongzhuo"],
    trigger: { source: "damage" },
    forced: true,
    popup: false,
    locked: false,
    audio: "jiuchi",
    filter(event, player2) {
      return event.card && event.card.name == "sha" && event.getParent(2).jiu == true && !player2.isTempBanned("benghuai");
    },
    async content(event, trigger, player2) {
      player2.logSkill("rejiuchi");
      player2.tempBanSkill("benghuai");
    }
  },
  //苏飞，新贾逵
  tongqu: {
    audio: 2,
    trigger: {
      global: ["phaseBefore", "dying", "phaseDrawBegin2"],
      player: ["enterGame", "phaseZhunbeiBegin"]
    },
    direct: true,
    filter(event, player2) {
      if (event.name == "phaseDraw") {
        return event.player.hasMark("tongqu");
      }
      if (event.name == "dying") {
        return event.player.hasMark("tongqu");
      }
      if (event.name == "phaseZhunbei") {
        return game.hasPlayer(function(current) {
          return !current.hasMark("tongqu");
        });
      }
      return !player2.hasMark("tongqu") && (event.name != "phase" || game.phaseNumber == 0);
    },
    async content(event, trigger, player2) {
      if (trigger.name == "phaseDraw") {
        player2.logSkill("tongqu", trigger.player);
        await trigger.player.draw("nodelay");
        trigger.player.addTempSkill("tongqu2", "phaseDrawAfter");
        return;
      }
      if (trigger.name == "dying") {
        player2.logSkill("tongqu", trigger.player);
        trigger.player.removeMark("tongqu", 1);
        return;
      }
      if (trigger.name == "phaseZhunbei") {
        const result = await player2.chooseTarget(get.prompt2("tongqu"), function(card2, player3, target) {
          return !target.hasMark("tongqu");
        }).set("ai", function(target) {
          if (_status.event.player.hp < 3) {
            return 0;
          }
          return get.attitude(_status.event.player, target);
        }).forResult();
        if (result.bool) {
          const target = result.targets[0];
          await player2.loseHp();
          player2.logSkill("tongqu", target);
          target.addMark("tongqu", 1);
        }
        return;
      }
      player2.logSkill("tongqu");
      player2.addMark("tongqu", 1);
    },
    marktext: "渠",
    intro: { content: "mark", name2: "渠" }
  },
  tongqu2: {
    trigger: { player: "phaseDrawEnd" },
    forced: true,
    silent: true,
    sourceSkill: "tongqu",
    filter(event, player2) {
      var bool = game.hasPlayer(function(current) {
        return current != player2 && current.hasMark("tongqu");
      });
      return player2.countCards("he", function(card2) {
        if (bool) {
          return true;
        }
        return lib.filter.cardDiscardable(card2, player2);
      }) > 0;
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseCardTarget({
        forced: true,
        position: "he",
        filterCard: true,
        filterTarget(card2, player3, target) {
          return player3 != target && target.hasMark("tongqu");
        },
        selectTarget() {
          if (ui.selected.cards.length && !lib.filter.cardDiscardable(ui.selected.cards[0], _status.event.player)) {
            return [1, 1];
          }
          return [0, 1];
        },
        prompt: "弃置一张牌，或将一张牌交给一名有“渠”的其他角色",
        ai1(card2) {
          var player3 = _status.event.player;
          if (get.name(card2) == "du") {
            return 20;
          }
          if (get.position(card2) == "e" && get.value(card2) <= 0) {
            return 14;
          }
          if (get.position(card2) == "h" && game.hasPlayer(function(current) {
            return current != player3 && current.hasMark("tongqu") && get.attitude(player3, current) > 0 && current.getUseValue(card2) > player3.getUseValue(card2) && current.getUseValue(card2) > player3.getUseValue(card2);
          })) {
            return 12;
          }
          if (game.hasPlayer(function(current) {
            return current != player3 && current.hasMark("tongqu") && get.attitude(player3, current) > 0;
          })) {
            if (card2.name == "wuxie") {
              return 11;
            }
            if (card2.name == "shan" && player3.countCards("h", "shan") > 1) {
              return 9;
            }
          }
          return 6 / Math.max(1, get.value(card2));
        },
        ai2(target) {
          var player3 = _status.event.player;
          var card2 = ui.selected.cards[0];
          var att = get.attitude(player3, target);
          if (card2.name == "du") {
            return -6 * att;
          }
          if (att > 0) {
            if (get.position(card2) == "h" && target.getUseValue(card2) > player3.getUseValue(card2)) {
              return 4 * att;
            }
            if (target.hasUseTarget(card2)) {
              return 2 * att;
            }
            return 1.2 * att;
          }
          return 0;
        }
      }).forResult();
      if (!result.bool) {
        return;
      }
      if (result.targets.length) {
        const target = result.targets[0];
        await player2.give(result.cards, target);
        const card2 = result.cards[0];
        if (target.getCards("h").includes(card2) && get.type(card2) == "equip") {
          await target.chooseUseTarget(card2, true).forResult();
        }
      } else {
        await player2.discard(result.cards);
      }
    }
  },
  xinwanlan: {
    audio: "wanlan",
    trigger: { global: "damageBegin4" },
    filter(event, player2) {
      return event.player.hp <= event.num && player2.countCards("e") >= 1;
    },
    logTarget: "player",
    check(event, player2) {
      if (get.attitude(player2, event.player) < 4) {
        return false;
      }
      if (player2.countCards("hs", (card2) => player2.canSaveCard(card2, event.player)) >= 1 + event.num - event.player.hp) {
        return false;
      }
      if (event.player == player2 || event.player == get.zhu(player2)) {
        return true;
      }
      return !player2.hasUnknown();
    },
    async content(event, trigger, player2) {
      player2.discard(player2.getCards("e"));
      trigger.cancel();
    }
  },
  zhengjian: {
    audio: 2,
    trigger: { player: "phaseJieshuBegin" },
    locked: true,
    direct: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget("请选择【诤荐】的目标", lib.translate.zhengjian_info).set("ai", function(target) {
        if (target.hasSkill("zhengjian_mark")) {
          return 0;
        }
        if (player2 == target) {
          return 0.5;
        }
        return get.attitude(_status.event.player, target) * (1 + target.countCards("h"));
      }).forResult();
      if (result.bool) {
        const target = result.targets[0];
        player2.logSkill("zhengjian", target);
        target.addSkill("zhengjian_mark");
      }
    },
    group: "zhengjian_draw",
    ai: {
      notemp: true
    }
  },
  zhengjian_draw: {
    audio: "zhengjian",
    trigger: { player: "phaseBegin" },
    forced: true,
    sourceSkill: "zhengjian",
    filter(event) {
      return game.hasPlayer(function(current) {
        return current.hasSkill("zhengjian_mark");
      });
    },
    logTarget(event) {
      return game.filterPlayer(function(current) {
        return current.hasSkill("zhengjian_mark");
      });
    },
    async content(event, trigger, player2) {
      const list = game.filterPlayer(function(current) {
        return current.countMark("zhengjian_mark") > 0;
      });
      if (list.length > 1) {
        await game.asyncDraw(list, function(target) {
          return Math.min(5, target.maxHp, target.countMark("zhengjian_mark"));
        });
      } else if (list.length == 1) {
        await list[0].draw(Math.min(5, list[0].maxHp, list[0].countMark("zhengjian_mark")));
      }
      game.countPlayer(function(current) {
        current.removeSkill("zhengjian_mark");
      });
      if (list.length > 1) {
        await game.delayx();
      }
    }
  },
  zhengjian_mark: {
    trigger: { player: ["useCard1", "respond"] },
    silent: true,
    firstDo: true,
    onremove: true,
    charlotte: true,
    sourceSkill: "zhengjian",
    async content(event, trigger, player2) {
      player2.addMark("zhengjian_mark", 1, false);
    },
    init(player2, skill) {
      if (!player2.storage[skill]) {
        player2.storage[skill] = 0;
      }
    },
    mark: true,
    intro: {
      content: "已使用/打出过#张牌"
    }
  },
  gaoyuan: {
    audio: 2,
    trigger: { target: "useCardToTarget" },
    direct: true,
    filter(event, player2) {
      if (event.card.name != "sha") {
        return false;
      }
      if (player2.countCards("he") == 0) {
        return false;
      }
      return game.hasPlayer(function(current) {
        return current != event.player && current != player2 && current.hasSkill("zhengjian_mark") && lib.filter.targetEnabled(event.card, event.player, current);
      });
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseCardTarget({
        position: "he",
        filterCard: lib.filter.cardDiscardable,
        filterTarget(card2, player3, target) {
          var trigger2 = _status.event;
          if (target != player3 && target != trigger2.source) {
            if (target.hasSkill("zhengjian_mark") && lib.filter.targetEnabled(trigger2.card, trigger2.source, target)) {
              return true;
            }
          }
          return false;
        },
        ai1(card2) {
          return get.unuseful(card2) + 9;
        },
        ai2(target) {
          if (_status.event.player.countCards("h", "shan")) {
            return -get.attitude(_status.event.player, target);
          }
          if (get.attitude(_status.event.player, target) < 5) {
            return 6 - get.attitude(_status.event.player, target);
          }
          if (_status.event.player.hp == 1 && player2.countCards("h", "shan") == 0) {
            return 10 - get.attitude(_status.event.player, target);
          }
          if (_status.event.player.hp == 2 && player2.countCards("h", "shan") == 0) {
            return 8 - get.attitude(_status.event.player, target);
          }
          return -1;
        },
        prompt: get.prompt("gaoyuan"),
        prompt2: "弃置一张牌，将此【杀】转移给一名有“诤”的角色",
        source: trigger.player,
        card: trigger.card
      }).forResult();
      if (result.bool) {
        const target = result.targets[0];
        player2.logSkill(event.name, target);
        await player2.discard(result.cards);
        const evt = trigger.getParent();
        evt.triggeredTargets2.remove(player2);
        evt.targets.remove(player2);
        evt.targets.push(target);
      }
    },
    ai: {
      combo: "zhengjian"
    }
  },
  //一 将 成 名
  zhilve: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    async content(event, trigger, player2) {
      const result = !player2.canMoveCard() ? { index: 1 } : await player2.chooseControl().set("choiceList", ["移动场上的一张牌，若你以此法移动了：装备区的牌，你失去1点体力；判定区的牌，本回合你手牌上限-1", "本回合的摸牌阶段多摸一张牌且第一张杀无距离次数限制"]).set("ai", () => {
        if (get.player().canMoveCard(true)) return 0;
        return 1;
      }).forResult();
      if (result?.index === 0) {
        const result2 = await player2.moveCard(true, "请移动场上的一张牌，若你以此法移动了：装备区的牌，你失去1点体力；判定区的牌，本回合你手牌上限-1").forResult();
        if (result2?.position === "e") {
          await player2.loseHp();
        } else if (result2?.position === "j") {
          player2.addTempSkill(event.name + "_dis");
        }
      } else if (result?.index === 1) {
        player2.addTempSkill(event.name + "_yingzi");
        if (!player2.hasHistory("useCard", (evt) => evt.card.name == "sha")) {
          player2.addTempSkill(event.name + "_xiandeng");
        }
      }
    },
    subSkill: {
      dis: {
        charlotte: true,
        mark: true,
        markimage: "image/card/handcard.png",
        intro: { content: "本回合手牌上限-1" },
        mod: {
          maxHandcard(player2, num) {
            return num - 1;
          }
        }
      },
      yingzi: {
        charlotte: true,
        mark: true,
        intro: { content: "本回合摸牌阶段多摸一张牌" },
        trigger: { player: "phaseDrawBegin2" },
        forced: true,
        popup: false,
        filter(event, player2) {
          return !event.numFixed;
        },
        async content(event, trigger, player2) {
          trigger.num++;
        }
      },
      xiandeng: {
        charlotte: true,
        mark: true,
        intro: { content: "本回合使用的第一张【杀】无距离限制且不计入次数限制" },
        mod: {
          targetInRange(card2, player2) {
            if (card2.name == "sha") {
              return true;
            }
          }
        },
        trigger: { player: "useCard1" },
        forced: true,
        popup: false,
        firstDo: true,
        filter(event, player2) {
          return event.card.name == "sha";
        },
        async content(event, trigger, player2) {
          player2.removeSkill(event.name);
          if (trigger.addCount !== false) {
            trigger.addCount = false;
            const stat = player2.getStat().card, name = trigger.card.name;
            if (typeof stat[name] === "number") {
              stat[name]--;
            }
          }
        }
      }
    }
  },
  xhzhiyan: {
    enable: "phaseUse",
    audio: 2,
    filter(event, player2) {
      return player2.countCards("h") != player2.maxHp;
    },
    filterCard: true,
    selectCard() {
      var player2 = _status.event.player;
      var num = Math.max(0, player2.countCards("h") - player2.maxHp);
      return [num, num];
    },
    check(card2) {
      var player2 = _status.event.player;
      if (player2.getUseValue(card2) <= 0 && game.hasPlayer(function(current) {
        return current != player2 && get.value(card2, current) * get.attitude(player2, current) > 0;
      })) {
        return 1;
      }
      return 0;
    },
    async content(event, trigger, player2) {
      let cards2 = event.cards;
      if (!cards2.length) {
        await player2.draw(player2.maxHp - player2.countCards("h"));
        player2.addTempSkill("zishou2");
        return;
      }
      cards2 = cards2.filterInD("d");
      if (!cards2.length) {
        return;
      }
      const result1 = await player2.chooseButton(["是否将其中的一张牌交给一名其他角色？", cards2]).set("", function(button) {
        var player3 = _status.event.player;
        if (game.hasPlayer(function(current) {
          return current != player3 && get.value(button.link, current) * get.attitude(player3, current) > 0;
        })) {
          return Math.abs(get.value(button.link));
        }
        return 0;
      }).forResult();
      if (!result1.bool || !game.hasPlayer((current) => current != player2)) {
        return;
      }
      const card2 = result1.links[0];
      event.card = card2;
      const result2 = await player2.chooseTarget(true, lib.filter.notMe, "选择一名其他角色获得" + get.translation(card2)).set("ai", function(target2) {
        return get.value(_status.event.getParent().card, target2) * get.attitude(_status.event.player, target2);
      }).forResult();
      const target = result2.targets[0];
      player2.line(target, "green");
      await target.gain(card2, "gain2", "log");
    },
    ai: {
      order(obj, player2) {
        if (player2.countCards("h") > player2.maxHp) {
          return 10;
        }
        return 0.5;
      },
      result: {
        player: 1
      }
    }
  },
  //水 果 忍 者
  zhengjing_guanju: { audio: true },
  zhengjing: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    manualConfirm: true,
    filter(event, player2) {
      return !player2.hasSkill("zhengjing3");
    },
    async content(event, trigger, player2) {
      if (_status.connectMode) {
        event.time = lib.configOL.choose_timeout;
      }
      var cards2 = [];
      var names = [];
      while (true) {
        var card2 = get.cardPile(function(carde) {
          return carde.name != "du" && !names.includes(carde.name);
        });
        if (card2) {
          cards2.push(card2);
          names.push(card2.name);
          if (get.mode() == "doudizhu") {
            if (cards2.length == 1 && !get.isLuckyStar(player2) && Math.random() < 0.33) {
              break;
            }
            if (cards2.length == 2 && !get.isLuckyStar(player2) && Math.random() < 0.5) {
              break;
            }
            if (cards2.length >= 3) {
              break;
            }
          } else {
            if (cards2.length == 3 && !get.isLuckyStar(player2) && Math.random() < 0.33) {
              break;
            }
            if (cards2.length == 4 && !get.isLuckyStar(player2) && Math.random() < 0.5) {
              break;
            }
            if (cards2.length >= 5) {
              break;
            }
          }
        } else {
          break;
        }
      }
      event.cards = cards2;
      if (!cards2.length) {
        return;
      }
      event.videoId = lib.status.videoId++;
      if (player2.isUnderControl()) {
        game.swapPlayerAuto(player2);
      }
      var switchToAuto = function() {
        names.remove("du");
        game.pause();
        game.countChoose();
        setTimeout(function() {
          _status.imchoosing = false;
          event._result = {
            bool: true,
            links: names.slice(0)
          };
          if (event.dialog) {
            event.dialog.close();
          }
          if (event.control) {
            event.control.close();
          }
          game.resume();
        }, 5e3);
      };
      var createDialog = function(player3, id) {
        if (_status.connectMode) {
          lib.configOL.choose_timeout = "30";
        }
        if (player3 == game.me) {
          return;
        }
        var str = get.translation(player3) + "正在整理经书...<br>";
        ui.create.dialog(str).videoId = id;
      };
      var chooseButton = function(list) {
        var roundmenu = false;
        if (ui.roundmenu && ui.roundmenu.display != "none") {
          roundmenu = true;
          ui.roundmenu.style.display = "none";
        }
        var event2 = _status.event;
        event2.settleed = false;
        event2.finishedx = [];
        event2.map = {};
        var names2 = list.slice(0);
        event2.zhengjing_nodes = [];
        names2.push("du");
        names2.randomSort();
        var names22 = names2.slice(0);
        for (var i2 = 0; i2 < 2; i2++) {
          names22.randomSort();
          names2 = names2.concat(names22);
        }
        event2.zhengjing = names2;
        for (var i2 of list) {
          event2.map[i2] = 0;
        }
        event2.dialog = ui.create.dialog("forcebutton", "hidden");
        event2.dialog.textPrompt = event2.dialog.add('<div class="text center">及时点击卡牌，但不要点到毒了！</div>');
        var str = '<div class="text center">';
        for (var i2 of list) {
          str += get.translation(i2) + ":" + Math.min(2, event2.map[i2]) + "/2 ";
        }
        str += "</div>";
        event2.dialog.textPrompt2 = event2.dialog.add(str);
        event2.switchToAuto = function() {
          event2._result = {
            bool: true,
            links: event2.finishedx.slice(0)
          };
          event2.dialog.close();
          game.resume();
          _status.imchoosing = false;
          if (roundmenu) {
            ui.roundmenu.style.display = "";
          }
        };
        event2.dialog.classList.add("fixed");
        event2.dialog.classList.add("scroll1");
        event2.dialog.classList.add("scroll2");
        event2.dialog.classList.add("fullwidth");
        event2.dialog.classList.add("fullheight");
        event2.dialog.classList.add("noupdate");
        event2.dialog.open();
        event2.settle = function(du) {
          if (event2.settleed) {
            return;
          }
          event2.settleed = true;
          event2.dialog.textPrompt2.innerHTML = "";
          if (du) {
            if (lib.config.background_speak) {
              game.playAudio("skill", "zhengjing_boom");
            }
            event2.dialog.textPrompt.innerHTML = '<div class="text center">叫你别点毒你非得点 这下翻车了吧</div>';
          } else {
            if (lib.config.background_speak) {
              game.playAudio("skill", "zhengjing_finish");
            }
            event2.dialog.textPrompt.innerHTML = '<div class="text center">整理经典结束！共整理出' + get.cnNumber(event2.finishedx.length) + "份经典</div>";
          }
          while (event2.zhengjing_nodes.length) {
            event2.zhengjing_nodes.shift().delete();
          }
          setTimeout(function() {
            event2.switchToAuto();
          }, 1e3);
        };
        var click = function() {
          var name = this.name;
          if (name == "du") {
            event2.zhengjing.length = 0;
            event2.settle(true);
          } else {
            if (lib.config.background_speak) {
              game.playAudio("skill", "zhengjing_click");
            }
            event2.map[name]++;
            if (event2.map[name] > 1) {
              event2.finishedx.add(name);
            }
            if (event2.finishedx.length < list.length) {
              var str2 = '<div class="text center">';
              for (var i3 of list) {
                str2 += get.translation(i3) + ":" + Math.min(2, event2.map[i3]) + "/2 ";
              }
              str2 += "</div>";
              event2.dialog.textPrompt2.innerHTML = str2;
            } else {
              event2.zhengjing.length = 0;
              event2.settle();
            }
          }
          event2.zhengjing_nodes.remove(this);
          this.style.transition = "all 0.5s";
          this.style.transform = "scale(1.2)";
          this.delete();
        };
        var addNode = function() {
          if (event2.zhengjing.length) {
            var card3 = ui.create.card(ui.special, "noclick", true);
            card3.init(["", "", event2.zhengjing.shift()]);
            card3.addEventListener(lib.config.touchscreen ? "touchstart" : "mousedown", click);
            event2.zhengjing_nodes.push(card3);
            card3.style.position = "absolute";
            var rand1 = Math.round(Math.random() * 100);
            var rand2 = Math.round(Math.random() * 100);
            var rand3 = Math.round(Math.random() * 40) - 20;
            card3.style.left = "calc(" + rand1 + "% - " + rand1 + "px)";
            card3.style.top = "calc(" + rand2 + "% - " + rand2 + "px)";
            card3.style.transform = "scale(0.8) rotate(" + rand3 + "deg)";
            card3.style.opacity = 0;
            event2.dialog.appendChild(card3);
            ui.refresh(card3);
            card3.style.opacity = 1;
            card3.style.transform = "scale(1) rotate(" + rand3 + "deg)";
          }
          if (event2.zhengjing_nodes.length > (event2.zhengjing.length > 0 ? 2 : 0)) {
            event2.zhengjing_nodes.shift().delete();
          }
          if (event2.zhengjing.length || event2.zhengjing_nodes.length) {
            setTimeout(function() {
              addNode();
            }, 800);
          } else {
            event2.settle();
          }
        };
        game.pause();
        game.countChoose();
        addNode();
      };
      game.broadcastAll(createDialog, player2, event.videoId);
      if (event.isMine()) {
        chooseButton(names);
      } else if (event.isOnline()) {
        event.player.send(chooseButton, names);
        event.player.wait();
        game.pause();
      } else {
        switchToAuto();
      }
      await game.pause();
      game.broadcastAll(
        function(id, time) {
          if (_status.connectMode) {
            lib.configOL.choose_timeout = time;
          }
          var dialog = get.idDialog(id);
          if (dialog) {
            dialog.close();
          }
        },
        event.videoId,
        event.time
      );
      const result = event.result || event._result || {};
      for (var i = 0; i < cards2.length; i++) {
        if (!result.links.includes(cards2[i].name)) {
          cards2.splice(i--, 1);
        }
      }
      if (cards2.length) {
        player2.showCards(cards2, get.translation(player2) + "整理出了以下经典");
        await game.cardsGotoOrdering(cards2);
      } else {
        game.log(player2, "并没有整理出经典");
        player2.popup("杯具");
        return;
      }
      game.updateRoundNumber();
      const result2 = await player2.chooseTarget(true, "将整理出的经典置于一名角色的武将牌上").set("ai", function(target2) {
        if (target2.hasSkill("xinfu_pdgyingshi")) {
          return 0;
        }
        let player3 = _status.event.player;
        _status.event.getParent().cards;
        get.attitude(player3, target2);
        let js = target2.getCards("j", (i2) => {
          let name = i2.viewAs || i2.name, info = lib.card[name];
          if (!info || !info.judge) {
            return false;
          }
          return true;
        }), eff = -1.5 * get.effect(target2, { name: "draw" }, player3, player3);
        if (js.length) {
          eff += js.reduce((acc, i2) => {
            let name = i2.viewAs || i2.name;
            return acc - 0.7 * get.effect(target2, get.autoViewAs({ name }, [i2]), target2, player3);
          }, 0);
        }
        return eff;
      }).forResult();
      if (!result2.bool) {
        return;
      }
      const target = result2.targets[0];
      player2.line(target, "thunder");
      if (cards2.length == 1) {
        return;
      }
      const result3 = await player2.chooseToMove("整经：请分配整理出的经典", true).set("list", [["置于" + get.translation(target) + "的武将牌上", cards2], ["自己获得"]]).set("filterMove", function(from, to, moved) {
        if (moved[0].length == 1 && to == 1 && from.link == moved[0][0]) {
          return false;
        }
        return true;
      }).set("filterOk", function(moved) {
        return moved[0].length > 0;
      }).set("processAI", function(list) {
        var cards3 = list[0][1].slice(0).sort(function(a, b) {
          return get.value(a) - get.value(b);
        });
        return [cards3.splice(0, 1), cards3];
      }).forResult();
      if (result3.bool) {
        const cards3 = result3.moved[0], gains = result3.moved[1];
        target.addSkill("zhengjing2");
        const addExpansionEvent = target.addToExpansion(cards3, "gain2");
        addExpansionEvent.gaintag.add("zhengjing2");
        await addExpansionEvent;
        if (gains.length) {
          await player2.gain(gains, "gain2");
        }
      }
    },
    ai: {
      order: 10,
      result: { player: 1 },
      threaten: 3.2
    }
  },
  //恁就是仲村由理？
  zhengjing2: {
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    charlotte: true,
    intro: { content: "expansion", markcount: "expansion" },
    sourceSkill: "zhengjing",
    onremove(player2, skill) {
      var cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    async content(event, trigger, player2) {
      await player2.gain(player2.getExpansions("zhengjing2"), "gain2");
      player2.skip("phaseJudge");
      player2.skip("phaseDraw");
      player2.removeSkill("zhengjing2");
    }
  },
  zhengjing3: {},
  //邓芝
  jimeng: {
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    direct: true,
    filter(event, player2) {
      return game.hasPlayer(function(current) {
        return current.countGainableCards(player2, "he") > 0;
      });
    },
    async content(event, trigger, player2) {
      const result1 = await player2.chooseTarget(get.prompt2("jimeng"), function(card2, player3, target2) {
        return target2 != player3 && target2.countGainableCards(player3, "he") > 0;
      }).set("ai", function(target2) {
        var player3 = _status.event.player;
        if (player3.hp > 1 && get.attitude(player3, target2) < 2) {
          return 0;
        }
        return get.effect(target2, { name: "shunshou" }, player3, player3);
      }).forResult();
      if (!result1.bool) {
        return;
      }
      const target = result1.targets[0];
      player2.logSkill("jimeng", target);
      await player2.gainPlayerCard(target, "he", true);
      const hs = player2.getCards("he");
      if (!(player2.hp > 0 && hs.length)) {
        return;
      }
      let cards2;
      if (hs.length <= player2.hp) {
        cards2 = hs;
      } else {
        const result2 = await player2.chooseCard(player2.hp, true, "交给" + get.translation(target) + get.cnNumber(player2.hp) + "张牌", "he", true).forResult();
        cards2 = result2.cards;
      }
      await player2.give(cards2, target);
    }
  },
  shuaiyan: {
    audio: 2,
    trigger: { player: "phaseDiscardBegin" },
    filter(event, player2) {
      return player2.countCards("h") > 1;
    },
    check(event, player2) {
      return game.hasPlayer(function(current) {
        return current != player2 && current.countCards("he") && lib.skill.shuaiyan.check2(current, player2);
      });
    },
    check2(target, player2) {
      if (get.itemtype(player2) != "player") {
        player2 = _status.event.player;
      }
      return -get.attitude(player2, target) / target.countCards("he");
    },
    async content(event, trigger, player2) {
      player2.showHandcards(get.translation(player2) + "发动了【率言】");
      const filter = function(card2, player3, target2) {
        return player3 != target2 && target2.countCards("he") > 0;
      };
      if (!game.hasPlayer(function(current) {
        return filter("我约等于白板", player2, current);
      })) {
        return;
      }
      const result1 = await player2.chooseTarget(true, filter, "选择一名其他角色，令其交给你一张牌").set("ai", lib.skill.shuaiyan.check2).forResult();
      const target = result1.targets[0];
      player2.line(target, "green");
      const result2 = await target.chooseCard("he", true, "交给" + get.translation(player2) + "一张牌").forResult();
      await target.give(result2.cards, player2);
    }
  },
  relihuo: {
    audio: 2,
    group: ["relihuo_baigei", "relihuo_damage"],
    trigger: { player: "useCard1" },
    filter(event, player2) {
      if (event.card.name == "sha" && !game.hasNature(event.card)) {
        return true;
      }
    },
    check(event, player2) {
      return false;
    },
    async content(event, trigger, player2) {
      game.setNature(trigger.card, "fire");
      trigger.relihuo = true;
    }
  },
  relihuo_damage: {
    trigger: { source: "damageBegin1" },
    forced: true,
    audio: "relihuo",
    sourceSkill: "relihuo",
    filter(event, player2) {
      return event.getParent(2).relihuo == true && event.player.isLinked();
    },
    async content(event, trigger, player2) {
      trigger.num++;
    }
  },
  relihuo_baigei: {
    trigger: { player: "useCardAfter" },
    forced: true,
    audio: "relihuo",
    sourceSkill: "relihuo",
    filter(event, player2) {
      if (event.card.name != "sha" || !game.hasNature(event.card, "fire")) {
        return false;
      }
      var num = 0;
      player2.getHistory("sourceDamage", function(evt) {
        if (evt.card == event.card) {
          num += evt.num;
        }
      });
      return num > 1;
    },
    async content(event, trigger, player2) {
      var num = 0;
      player2.getHistory("sourceDamage", function(evt) {
        if (evt.card == trigger.card) {
          num += evt.num;
        }
      });
      player2.loseHp(Math.floor(num / 2));
    }
  },
  gongsun: {
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    direct: true,
    filter(event, player2) {
      return player2.countCards("he") > 1;
    },
    async content(event, trigger, player2) {
      const result1 = await player2.chooseCardTarget({
        prompt: get.prompt2("gongsun"),
        selectCard: 2,
        filterCard: lib.filter.cardDiscardable,
        filterTarget: lib.filter.notMe,
        position: "he",
        ai1(card2) {
          var friend = 0, enemy = 0, player3 = _status.event.player;
          var num = game.countPlayer(function(target2) {
            var att = get.attitude(player3, target2);
            if (att < 0) {
              enemy++;
            }
            if (target2 != player3 && att > 0) {
              friend++;
            }
            return true;
          });
          if (num > friend + enemy + 2) {
            return 0;
          }
          if (friend < enemy) {
            return 0;
          }
          if (card2.name == "sha") {
            return 10 - enemy;
          }
          return 10 - enemy - get.value(card2);
        },
        ai2(target2) {
          return -get.attitude(_status.event.player, target2) * (1 + target2.countCards("h"));
        }
      }).forResult();
      if (!result1.bool) {
        return;
      }
      const target = result1.targets[0];
      player2.logSkill("gongsun", target);
      await player2.discard(result1.cards);
      player2.addTempSkill("gongsun_shadow", { player: ["phaseBegin", "die"] });
      const list = [];
      for (var i = 0; i < lib.inpile.length; i++) {
        const name = lib.inpile[i];
        if (get.type(name) == "trick") {
          list.push(["锦囊", "", name]);
        } else if (get.type(name) == "basic") {
          list.push(["基本", "", name]);
        }
      }
      const result2 = await player2.chooseButton(["请选择一个牌名", [list, "vcard"], true]).set("ai", function(button) {
        return button.link[2] == "sha" ? 1 : 0;
      }).forResult();
      player2.storage.gongsun_shadow.push([target, result2.links[0][2]]);
      player2.popup(result2.links[0][2], "soil");
      game.log(player2, "选择了", "" + get.translation(result2.links[0][2]));
      player2.markSkill("gongsun_shadow");
    }
  },
  gongsun_shadow: {
    global: "gongsun_shadow2",
    sourceSkill: "gongsun",
    init(player2, skill) {
      if (!player2.storage[skill]) {
        player2.storage[skill] = [];
      }
    },
    marktext: "损",
    onremove: true,
    intro: {
      content(shadow) {
        var str = "";
        for (var i = 0; i < shadow.length; i++) {
          if (i > 0) {
            str += "<br>";
          }
          str += get.translation(shadow[i][0]);
          str += "：";
          str += get.translation(shadow[i][1]);
        }
        return str;
      }
    },
    mod: {
      cardEnabled(card2, player2) {
        var list = player2.storage.gongsun_shadow;
        for (var i = 0; i < list.length; i++) {
          if (list[i][1] == card2.name) {
            return false;
          }
        }
      },
      cardRespondable(card2, player2) {
        var list = player2.storage.gongsun_shadow;
        for (var i = 0; i < list.length; i++) {
          if (list[i][1] == card2.name) {
            return false;
          }
        }
      },
      cardSavable(card2, player2) {
        var list = player2.storage.gongsun_shadow;
        for (var i = 0; i < list.length; i++) {
          if (list[i][1] == card2.name) {
            return false;
          }
        }
      },
      cardDiscardable(card2, player2) {
        var list = player2.storage.gongsun_shadow;
        for (var i = 0; i < list.length; i++) {
          if (list[i][1] == card2.name) {
            return false;
          }
        }
      }
    }
  },
  gongsun_shadow2: {
    mod: {
      cardEnabled(card2, player2) {
        if (game.hasPlayer(function(current) {
          var list = current.storage.gongsun_shadow;
          if (!list) {
            return false;
          }
          for (var i = 0; i < list.length; i++) {
            if (list[i][0] == player2 && list[i][1] == card2.name) {
              return true;
            }
          }
          return false;
        })) {
          return false;
        }
      },
      cardSavable(card2, player2) {
        if (game.hasPlayer(function(current) {
          var list = current.storage.gongsun_shadow;
          if (!list) {
            return false;
          }
          for (var i = 0; i < list.length; i++) {
            if (list[i][0] == player2 && list[i][1] == card2.name) {
              return true;
            }
          }
          return false;
        })) {
          return false;
        }
      },
      cardRespondable(card2, player2) {
        if (game.hasPlayer(function(current) {
          var list = current.storage.gongsun_shadow;
          if (!list) {
            return false;
          }
          for (var i = 0; i < list.length; i++) {
            if (list[i][0] == player2 && list[i][1] == card2.name) {
              return true;
            }
          }
          return false;
        })) {
          return false;
        }
      },
      cardDiscardable(card2, player2) {
        if (game.hasPlayer(function(current) {
          var list = current.storage.gongsun_shadow;
          if (!list) {
            return false;
          }
          for (var i = 0; i < list.length; i++) {
            if (list[i][0] == player2 && list[i][1] == card2.name) {
              return true;
            }
          }
          return false;
        })) {
          return false;
        }
      }
    }
  },
  duoduan: {
    audio: 2,
    trigger: { target: "useCardToTargeted" },
    usable: 1,
    filter(event, player2) {
      return event.card.name == "sha" && player2.countCards("he") > 0;
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseCard("he", get.prompt2(event.skill), lib.filter.cardRecastable).set("ai", function(card2) {
        if (_status.event.goon) {
          return 8 - get.value(card2);
        }
        return 0;
      }).set(
        "goon",
        (function() {
          if (get.attitude(trigger.player, player2) > 0) {
            return true;
          }
          if (!trigger.player.countCards("he")) {
            return true;
          }
          if (!player2.hasShan()) {
            return true;
          }
          return event.getRand() < 0.5;
        })()
      ).forResult();
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      await player2.recast(event.cards);
      const bool = trigger.player.countCards("he", (card2) => {
        return lib.filter.cardDiscardable(card2, trigger.player, "duoduan");
      }) > 0;
      const result = bool ? await player2.chooseControl().set("choiceList", [`令其摸两张牌，然后令${get.translation(trigger.card)}对你无效`, `令其弃置一张牌，然后你不可响应${get.translation(trigger.card)}`]).set("prompt", `度断：令${get.translation(trigger.player)}执行一项`).set("ai", function() {
        let player3 = _status.event.player;
        let source = _status.event.getTrigger().player;
        if (get.attitude(player3, source) > 0) {
          return 0;
        }
        if (!player3.hasShan() && player3.hp >= 2) {
          return 1;
        }
        return 0;
      }).forResult() : {
        index: 0
      };
      if (result.index == 0) {
        await trigger.player.draw(2);
        trigger.excluded.add(player2);
      } else {
        const result2 = await trigger.player.chooseToDiscard("弃置一张牌令" + get.translation(player2) + "不能闪避此【杀】", "he", true).forResult();
        if (result2?.bool) {
          trigger.directHit.add(player2);
        }
      }
    }
  },
  chengzhao: {
    audio: 2,
    trigger: { global: "phaseJieshuBegin" },
    filter(event, player2) {
      let num = 0;
      player2.checkHistory("gain", function(evt) {
        num += evt.cards.length;
      });
      if (num < 2) {
        return false;
      }
      return player2.hasCards("h") && game.hasPlayer((current) => current.canCompare(player2));
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt2(event.skill), (card2, player3, target) => {
        return player3.canCompare(target);
      }).set("ai", (target) => {
        const player3 = get.player();
        return -get.attitude(player3, target) / target.countCards("h");
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const result = await player2.chooseToCompare(target).forResult();
      if (result?.bool) {
        const card2 = { name: "sha", isCard: true, storage: { chengzhao: true } };
        if (player2.canUse(card2, target, false)) {
          player2.addTempSkill(event.name + "_effect");
          await player2.useCard(card2, target, false);
        }
      }
    },
    subSkill: {
      effect: {
        charlotte: true,
        ai: {
          unequip: true,
          skillTagFilter(player2, tag, arg) {
            if (!arg?.card?.storage?.chengzhao) {
              return false;
            }
          }
        }
      }
    }
  },
  rezhengrong: {
    trigger: { player: "useCardAfter" },
    audio: "drlt_zhenrong",
    filter(event, player2) {
      if (!event.targets) {
        return false;
      }
      if (!event.isPhaseUsing(player2)) {
        return false;
      }
      var bool = false;
      for (var i = 0; i < event.targets.length; i++) {
        if (event.targets[i] != player2) {
          bool = true;
          break;
        }
      }
      if (!bool) {
        return false;
      }
      return player2.getAllHistory("useCard", function(evt) {
        if (!evt.isPhaseUsing(player2)) {
          return false;
        }
        for (var i2 = 0; i2 < evt.targets.length; i2++) {
          if (evt.targets[i2] != player2) {
            return true;
          }
        }
        return false;
      }).indexOf(event) % 2 == 1 && game.hasPlayer((target) => target != player2 && target.countCards("he") > 0);
    },
    locked: true,
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt(event.skill), true, "将一名其他角色的随机一张牌置于你的武将牌上，称为「荣」", function(card2, player3, target) {
        return target != player3 && target.countCards("he") > 0;
      }).set("ai", function(target) {
        return (1 - get.attitude(_status.event.player, target)) / target.countCards("he");
      }).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const card2 = target.getCards("he").randomGet();
      player2.addToExpansion(card2, target, "give").gaintag.add("rezhengrong");
    },
    marktext: "荣",
    onremove(player2, skill) {
      var cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    intro: {
      content: "expansion",
      markcount: "expansion"
    }
  },
  rehongju: {
    trigger: { player: "phaseZhunbeiBegin" },
    audio: "drlt_hongju",
    forced: true,
    juexingji: true,
    skillAnimation: true,
    animationColor: "thunder",
    derivation: "reqingce",
    filter(event, player2) {
      return player2.getExpansions("rezhengrong").length >= 3 && game.dead.length > 0;
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      await player2.draw(player2.getExpansions("rezhengrong").length);
      if (player2.countCards("h") == 0) ;
      else {
        const dialog = ["请选择要交换的手牌和「荣」，或点「取消」", '<div class="text center">「征荣」牌</div>', player2.getExpansions("rezhengrong"), '<div class="text center">手牌区</div>', player2.getCards("h")];
        const result = await player2.chooseButton(dialog).set("filterButton", function(button) {
          var ss = _status.event.player.getExpansions("rezhengrong");
          var hs = _status.event.player.getCards("h");
          var sn = 0;
          var hn = 0;
          var ub = ui.selected.buttons;
          for (var i2 = 0; i2 < ub.length; i2++) {
            if (ss.includes(ub[i2].link)) {
              sn++;
            } else {
              hn++;
            }
          }
          return !(sn >= hs.length && ss.includes(button.link) || hn >= ss.length && hs.includes(button.link));
        }).set("selectButton", function() {
          if (ui.selected.buttons.length == 0) {
            return 2;
          }
          var ss = _status.event.player.getExpansions("rezhengrong");
          var hs = _status.event.player.getCards("h");
          var sn = 0;
          var hn = 0;
          var ub = ui.selected.buttons;
          for (var i2 = 0; i2 < ub.length; i2++) {
            if (ss.includes(ub[i2].link)) {
              sn++;
            } else {
              hn++;
            }
          }
          if (sn != hn) {
            return 2 * Math.max(sn, hn);
          } else {
            if (sn == ss.length || hn == hs.length || sn == hs.length || hn == ss.length) {
              return ub.length;
            }
            return [ub.length, ub.length + 1];
          }
        }).set("ai", function() {
          return -1;
        }).forResult();
        if (result.bool) {
          const gains = [];
          const pushs = [];
          const expansions = player2.getExpansions("rezhengrong");
          for (var i = 0; i < result.links.length; i++) {
            const card2 = result.links[i];
            if (expansions.includes(card2)) {
              gains.push(card2);
            } else {
              pushs.push(card2);
            }
          }
          const addExpansionEvent = player2.addToExpansion(pushs, player2, "give");
          addExpansionEvent.gaintag.add("rezhengrong");
          await addExpansionEvent;
          await player2.gain(gains, "gain2");
        }
      }
      player2.addSkills("reqingce");
      await player2.loseMaxHp();
    },
    ai: { combo: "rezhengrong" }
  },
  reqingce: {
    enable: "phaseUse",
    audio: "drlt_qingce",
    filter(event, player2) {
      return player2.getExpansions("rezhengrong").length > 0;
    },
    chooseButton: {
      dialog(event, player2) {
        return ui.create.dialog("请选择要移去的「荣」", player2.getExpansions("rezhengrong"), "hidden");
      },
      backup(links, player2) {
        return {
          card: links[0],
          filterCard() {
            return false;
          },
          selectCard: -1,
          filterTarget(card2, player3, target) {
            return target.countDiscardableCards(player3, "ej") > 0;
          },
          delay: false,
          audio: "drlt_qingce",
          content: lib.skill.reqingce.contentx,
          ai: {
            result: {
              target(player3, target) {
                var att = get.attitude(player3, target);
                if (att > 0 && (target.countCards("j") > 0 || target.countCards("e", function(card2) {
                  return get.value(card2, target) < 0;
                }))) {
                  return 2;
                }
                if (att < 0 && target.countCards("e") > 0 && !target.hasSkillTag("noe")) {
                  return -1;
                }
                return 0;
              }
            }
          }
        };
      },
      prompt(links, player2) {
        return "弃置一名角色装备区或判定区内的一张牌";
      }
    },
    async contentx(event, trigger, player2) {
      const {
        targets: [target]
      } = event;
      const card2 = lib.skill.reqingce_backup.card;
      await player2.loseToDiscardpile(card2);
      if (target.countDiscardableCards(player2, "ej") > 0) {
        await player2.discardPlayerCard("ej", true, target).forResult();
      }
    },
    ai: {
      combo: "rezhengrong",
      order: 8,
      result: {
        player(player2) {
          if (game.hasPlayer(function(current) {
            var att = get.attitude(player2, current);
            if (att > 0 && current.countCards("j") > 0 || att < 0 && current.countCards("e") > 0) {
              return true;
            }
            return false;
          })) {
            return 1;
          }
          return 0;
        }
      }
    }
  },
  fengji: {
    audio: 2,
    trigger: { player: "phaseBegin" },
    forced: true,
    filter(event, player2) {
      return typeof player2.storage.fengji == "number" && player2.countCards("h") >= player2.storage.fengji;
    },
    async content(event, trigger, player2) {
      await player2.draw(3);
      player2.addTempSkill("fengji_effect");
    },
    group: "fengji_mark",
    intro: { content: "上回合结束时的手牌数：#" },
    subSkill: {
      mark: {
        trigger: { player: "phaseEnd" },
        silent: true,
        async content(event, trigger, player2) {
          player2.storage.fengji = player2.countCards("h");
          if (player2.hasSkill("fengji")) {
            player2.markSkill("fengji");
          }
        }
      },
      effect: {
        charlotte: true,
        mark: { content: "本回合手牌上限为体力上限" },
        mod: {
          maxHandcardBase(player2, num) {
            return player2.maxHp;
          }
        }
      }
    }
  },
  zhouxuan: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.hasPlayer((current) => get.info("zhouxuan").filterTarget(null, player2, current));
    },
    filterTarget(card2, player2, target) {
      if (!["identity", "doudizhu"].includes(get.mode()) && target.isFriendOf(player2)) {
        return false;
      }
      return target != player2;
    },
    // filterCard: lib.filter.cardDiscardable,
    // position: "he",
    // check(card) {
    // 	return 6 - get.value(card);
    // },
    async content(event, trigger, player2) {
      const { target } = event;
      const list = lib.inpile.filter((name) => get.type2(name) !== "basic").map((name) => get.type2(name)).toUniqued();
      const basic = lib.inpile.filter((name) => get.type(name) == "basic");
      if (!list.length && !basic.length) {
        return;
      }
      const result = await player2.chooseButton([`###周旋：请选择一个牌名或类别###<div class='text center'>${get.translation(target)}使用或打出下一张牌时，若此牌的名称或类型和你选择的相同，则你观看并分配牌堆顶的三张牌</div>`, [basic, "vcard"], [list.map((type) => [type, get.translation(type)]), "tdnodes"]], true).set("ai", (button) => {
        const { player: player3, target: target2 } = get.event();
        const link = button.link;
        const cards2 = target2.getCards("h", (card2) => target2.hasUseTarget(card2));
        const map = {};
        for (let i = 0; i < cards2.length; i++) {
          const type = get.type2(cards2[i]);
          map[type == "basic" ? get.name(cards2[i]) : type] = true;
        }
        const list2 = player3.getStorage("zhouxuan_effect").find((list3) => list3[0] == target2);
        if (list2?.[1]?.some(([name, type]) => {
          if (Array.isArray(link) && get.name(card) == button.link[2]) {
            return true;
          }
          if (typeof link == "string" && get.type2(card) == link) {
            return true;
          }
          return false;
        })) {
          return 0;
        }
        const priorityMap = {
          sha: 7,
          shan: 6,
          tao: 7,
          trick: 11,
          equip: 8
        };
        let key = typeof link == "string" ? link : link[2];
        if (map[key]) {
          return priorityMap[key];
        }
        return priorityMap[key] ? priorityMap[key] - 6 * Math.random() : 1.2 * Math.random();
      }).set("target", target).forResult();
      if (result?.bool) {
        let name, type;
        if (typeof result.links[0] == "string") {
          type = result.links[0];
        } else {
          name = result.links[0][2];
        }
        target.addSkill(event.name + "_ai");
        const effect = event.name + "_effect";
        player2.addSkill(effect);
        const list2 = player2.getStorage(effect).find((list3) => list3[0] == target);
        if (!list2) {
          player2.markAuto(effect, [[target, [[name, type]]]]);
        } else {
          player2.storage[effect][player2.getStorage(effect).indexOf(list2)] = [target, list2[1].concat([[name, type]])];
        }
      }
    },
    ai: {
      order: 10,
      result: {
        target(player2, target) {
          if (get.attitude(player2, target) > 0) {
            return Math.max(1, target.hp) * target.countCards("h", (card2) => target.getUseValue(card2) > 0);
          }
          return -1;
        }
      }
    },
    subSkill: {
      effect: {
        audio: "zhouxuan",
        charlotte: true,
        onremove: true,
        trigger: { global: ["useCard1", "respond"] },
        filter(event, player2) {
          return player2.getStorage("zhouxuan_effect").some((list) => list[0] == event.player);
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const target = trigger.player;
          const { card: card2 } = trigger;
          const list = player2.getStorage(event.name).find((list2) => list2[0] == target);
          player2.unmarkAuto(event.name, [list]);
          if (!player2.getStorage(event.name).length) {
            player2.removeSkill(event.name);
          }
          if (list[1].some(([name, type]) => get.name(card2) == name || get.type2(card2) == type)) {
            player2.logSkill(event.name, target);
            const cards2 = get.cards(3);
            await game.cardsGotoOrdering(cards2);
            if (_status.connectMode) {
              game.broadcastAll(function() {
                _status.noclearcountdown = true;
              });
            }
            const given_map = /* @__PURE__ */ new Map();
            while (cards2.length > 0) {
              const result = cards2.length > 1 ? await player2.chooseButtonTarget({
                createDialog: [`丰积：请选择要分配的牌`, cards2],
                selectButton: [1, Infinity],
                forced: true,
                filterTarget: true,
                ai1(button) {
                  return get.value(button.link);
                },
                canHidden: true,
                ai2(target2) {
                  const player3 = get.player();
                  const card3 = ui.selected.buttons[0].link;
                  if (card3) {
                    return get.value(card3, target2) * get.attitude(player3, target2);
                  }
                  return 1;
                }
              }).set("allowChooseAll", true).forResult() : await player2.chooseTarget(`丰积：令一名角色获得${get.translation(cards2)}`, true).set("ai", (target2) => {
                const { player: player3, enemy } = get.event();
                const att = get.attitude(player3, target2);
                if (enemy) {
                  return -att;
                } else if (att > 0) {
                  return att / (1 + target2.countCards("h"));
                } else {
                  return att / 100;
                }
              }).set("enemy", get.value(cards2[0], player2, "raw") < 0).forResult();
              if (result?.bool) {
                let links;
                if (!result.links?.length) {
                  links = cards2.slice();
                } else {
                  links = result.links;
                }
                cards2.removeArray(links);
                const [target2] = result.targets;
                if (!given_map.has(target2)) {
                  given_map.set(target2, links);
                } else {
                  given_map.get(target2).addArray(links);
                }
              } else {
                break;
              }
            }
            if (_status.connectMode) {
              game.broadcastAll(() => {
                delete _status.noclearcountdown;
                game.stopCountChoose();
              });
            }
            if (given_map.size) {
              await game.loseAsync({
                gain_list: Array.from(given_map),
                giver: player2,
                animate: "draw"
              }).setContent("gaincardMultiple");
            }
          }
        },
        marktext: "旋",
        intro: {
          content(storage = [], player2) {
            if (!storage?.length) {
              return "";
            }
            let str = `${get.translation(player2)}正在和${get.translation(storage.map((item) => get.translation(item[0])))}周旋中`;
            if (player2 == game.me || player2.isUnderControl()) {
              str = "记录的角色及条件";
              for (const [target, item] of storage) {
                const names = [];
                const types = [];
                for (const [name, type] of item) {
                  if (name) {
                    names.add(name);
                  }
                  if (type) {
                    types.add(type);
                  }
                }
                str += `<li>${get.translation(target)}：`;
                if (names.length) {
                  str += `牌名：${names.map((name) => get.translation(name)).join("、")}`;
                }
                if (types.length) {
                  if (names.length) {
                    str += "；";
                  }
                  str += `类型：${types.map((type) => get.translation(type)).join("、")}`;
                }
              }
            }
            return str;
          }
        }
      },
      ai: {
        charlotte: true,
        mod: {
          aiOrder(player2, card2, num) {
            if (game.hasPlayer((current) => {
              if (get.attitude(player2, current) <= 0) {
                return false;
              }
              return current.getStorage("zhouxuan_effect").some((item) => item[0] == player2 && item[1].some(([name, type]) => get.name(card2) == name || get.type2(card2) == type));
            })) {
              return num + 10;
            }
          }
        }
      }
    }
  },
  reshanxi: {
    audio: "shanxi",
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.hasCards("h", (card2) => get.info("reshanxi").filterCard(card2, player2)) && game.hasPlayer((current) => get.info("reshanxi").filterTarget(null, player2, current));
    },
    filterCard(card2, player2) {
      return get.type(card2) == "basic" && lib.filter.cardDiscardable(card2, player2);
    },
    filterTarget(card2, player2, target) {
      return target !== player2 && target.hasCards("h");
    },
    check(card2) {
      return 7 - get.value(card2);
    },
    async content(event, trigger, player2) {
      const target = event.target;
      const discardCard = event.cards[0];
      const discardColor = get.color(discardCard, player2);
      const result = await player2.discardPlayerCard({
        target,
        position: "h",
        forced: true,
        prompt: "闪袭：请选择弃置对方一张手牌"
      }).forResult();
      if (result?.cards?.length) {
        const targetCard = result.cards[0];
        const targetCardName = get.name(targetCard, target);
        if (discardColor === "red" && result.cards.someInD("d")) {
          await player2.gain(result.cards.filterInD("d"), "gain2");
        }
        if (!player2.getStorage(event.name).includes(targetCardName)) {
          player2.markAuto(event.name, [targetCardName]);
          await target.damage();
        }
      }
    },
    ai: {
      order: 9,
      result: { target: -2 }
    },
    onremove: true,
    intro: { content: "已弃置牌名：$" }
  },
  reqizhou: {
    audio: 2,
    trigger: { player: "useCard" },
    forced: true,
    filter(event, player2) {
      return get.type(event.card) == "equip";
    },
    derivation: ["yingzi", "jiang", "xinpojun", "dbchongjian", "fenwei"],
    async content(event, trigger, player2) {
      player2.markAuto(event.name, [trigger.card.name]);
      const count = player2.getStorage(event.name).length;
      const skills2 = get.info(event.name).derivation.slice(0, Math.min(count, 5));
      player2.removeAdditionalSkill("dchqxinqizhou");
      if (skills2.length > 0) {
        player2.addAdditionalSkill("dchqxinqizhou", skills2);
      }
    },
    onremove: true,
    intro: { content: "已使用过装备牌名：$" }
  },
  zhaohan: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    filter(event, player2) {
      return player2.phaseNumber < 8;
    },
    check(event, player2) {
      return player2.phaseNumber < 3;
    },
    logAudio(event, player2) {
      const num = player2.phaseNumber < 5 ? 1 : 2;
      return `zhaohan${num}.mp3`;
    },
    async content(event, trigger, player2) {
      if (player2.phaseNumber < 5) {
        await player2.gainMaxHp();
        await player2.recover();
      } else {
        await player2.loseMaxHp();
      }
    }
  },
  rangjie: {
    audio: 2,
    trigger: { player: "damageEnd" },
    getIndex(event) {
      return event.num;
    },
    async cost(event, trigger, player2) {
      let choiceList = ["获得一张指定类型的牌"];
      if (player2.canMoveCard()) {
        choiceList.push("移动场上的一张牌");
      }
      const result = await player2.chooseControl("cancel2").set("choiceList", choiceList).set("prompt", get.prompt(event.skill)).set("ai", function() {
        var player3 = _status.event.player;
        if (player3.canMoveCard(true)) {
          return 1;
        }
        return 0;
      }).forResult();
      event.result = {
        bool: result.control != "cancel2",
        cost_data: result.index
      };
    },
    async content(event, trigger, player2) {
      if (event.cost_data) {
        player2.moveCard(true);
      } else {
        const result = await player2.chooseControl("basic", "trick", "equip").set("prompt", "选择获得一种类型的牌").set("ai", function() {
          var player3 = _status.event.player;
          if (player3.hp <= 3 && !player3.countCards("h", { name: ["shan", "tao"] })) {
            return "basic";
          }
          if (player3.countCards("he", { type: "equip" }) < 2) {
            return "equip";
          }
          return "trick";
        }).forResult();
        const card2 = get.cardPile(function(card3) {
          return get.type(card3, "trick") == result.control;
        });
        if (card2) {
          await player2.gain(card2, "gain2", "log");
        }
      }
      await player2.draw();
    },
    ai: {
      maixie: true,
      maixie_hp: true,
      effect: {
        target(card2, player2, target) {
          if (get.tag(card2, "damage")) {
            if (player2.hasSkillTag("jueqing", false, target)) {
              return [1, -2];
            }
            if (!target.hasFriend()) {
              return;
            }
            var num = 1;
            if (get.attitude(player2, target) > 0) {
              if (player2.needsToDiscard()) {
                num = 0.7;
              } else {
                num = 0.5;
              }
            }
            if (target.hp >= 4) {
              return [1, num * 2];
            }
            if (target.hp == 3) {
              return [1, num * 1.5];
            }
            if (target.hp == 2) {
              return [1, num * 0.5];
            }
          }
        }
      }
    }
  },
  yizheng: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.hasPlayer((current) => get.info("yizheng").filterTarget(null, player2, current));
    },
    filterTarget(card2, player2, current) {
      return current.hp <= player2.hp && player2.canCompare(current);
    },
    async content(event, trigger, player2) {
      const { target } = event;
      const result = await player2.chooseToCompare(target).forResult();
      if (result?.bool) {
        target.skip("phaseDraw");
        target.addTempSkill(event.name + "_mark", { player: "phaseDrawSkipped" });
      } else {
        await player2.loseMaxHp();
      }
    },
    ai: {
      order: 1,
      result: {
        player: (player2, target) => {
          let hs = player2.getCards("h").sort(function(a2, b2) {
            return get.number(b2) - get.number(a2);
          });
          if (!hs.length) {
            return 0;
          }
          let a = get.number(hs[0]), b = 4;
          if (player2.getDamagedHp()) {
            b = 2;
          }
          return -b * (1 - Math.pow((a - 1) / 13, target.countCards("h")));
        },
        target: (player2, target) => {
          if (target.skipList.includes("phaseDraw") || target.hasSkill("pingkou") || target.hasSkill("xinpingkou")) {
            return 0;
          }
          let hs = player2.getCards("h").sort(function(a, b) {
            return get.number(b) - get.number(a);
          });
          if (!hs.length) {
            return 0;
          }
          return -Math.pow((get.number(hs[0]) - 1) / 13, target.countCards("h")) * 2;
        }
      }
    },
    subSkill: {
      mark: {
        charlotte: true,
        mark: true,
        intro: { content: "跳过下回合的摸牌阶段" }
      }
    }
  },
  rw_zhuge_skill: {
    equipSkill: true,
    audio: true,
    firstDo: true,
    trigger: { player: "useCard1" },
    forced: true,
    filter(event, player2) {
      return !event.audioed && event.card.name == "sha" && player2.countUsed("sha", true) > 1 && event.getParent().type == "phase";
    },
    async content(event, trigger, player2) {
      trigger.audioed = true;
    },
    mod: {
      cardUsable(card2, player2, num) {
        var cards2 = player2.getEquips("rewrite_zhuge");
        if (card2.name == "sha") {
          if (!cards2.length || player2.hasSkill("rw_zhuge_skill", null, false) || cards2.some((card3) => card3 != _status.rw_zhuge_temp && !ui.selected.cards.includes(card3))) {
            if (get.is.versus() || get.is.changban()) {
              return num + 3;
            }
            return Infinity;
          }
        }
      },
      cardEnabled2(card2, player2) {
        if (!_status.event.addCount_extra || player2.hasSkill("rw_zhuge_skill", null, false)) {
          return;
        }
        var cards2 = player2.getEquips("rewrite_zhuge");
        if (card2 && cards2.includes(card2)) {
          try {
            var cardz = get.card();
          } catch (e) {
            return;
          }
          if (!cardz || cardz.name != "sha") {
            return;
          }
          _status.rw_zhuge_temp = card2;
          var bool = lib.filter.cardUsable(get.autoViewAs({ name: "sha" }, ui.selected.cards.concat([card2])), player2);
          delete _status.rw_zhuge_temp;
          if (!bool) {
            return false;
          }
        }
      }
    }
  },
  xinqingjian: {
    audio: "qingjian",
    trigger: {
      player: "gainAfter",
      global: "loseAsyncAfter"
    },
    usable: 1,
    filter(event, player2) {
      return event.getg(player2).length && event.getParent("phaseDraw").player != player2 && player2.hasCards("h");
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseCard(get.prompt2(event.name.slice(0, -5)), "h", [1, player2.countCards("h")], "allowChooseAll").set("ai", (card2) => {
        const player3 = get.player();
        if (!game.hasPlayer((target) => player3 != target && get.attitude(player3, target) > 0)) {
          return 0;
        }
        return 4 - get.value(card2);
      }).forResult();
    },
    async content(event, trigger, player2) {
      player2.addSkill(event.name + "_effect");
      const next = player2.addToExpansion(event.cards, "giveAuto", player2);
      next.gaintag.add(event.name + "_effect");
      await next;
    },
    subSkill: {
      effect: {
        charlotte: true,
        audio: "xinqingjian",
        trigger: { global: "phaseAfter" },
        filter(event, player2) {
          return player2.hasExpansions("xinqingjian_effect") && game.hasPlayer((current) => current != player2);
        },
        forced: true,
        async content(event, trigger, player2) {
          const cards2 = player2.getExpansions(event.name);
          if (_status.connectMode) {
            game.broadcastAll(function() {
              _status.noclearcountdown = true;
            });
          }
          const goon = cards2.length > 1;
          const given_map = /* @__PURE__ */ new Map();
          while (cards2.length > 0 && game.hasPlayer((current) => current != player2)) {
            const result = cards2.length > 1 ? await player2.chooseButtonTarget({
              createDialog: [`清俭：请选择要分配的牌`, cards2],
              selectButton: [1, Infinity],
              forced: true,
              filterTarget: lib.filter.notMe,
              ai1(button) {
                return get.value(button.link);
              },
              canHidden: true,
              ai2(target) {
                const player3 = get.player();
                const card2 = ui.selected.buttons[0].link;
                if (card2) {
                  return get.value(card2, target) * get.attitude(player3, target);
                }
                return 1;
              }
            }).set("allowChooseAll", true).forResult() : await player2.chooseTarget(`清俭：令一名其他角色获得${get.translation(cards2)}`, true, lib.filter.notMe).set("ai", (target) => {
              const { player: player3, enemy } = get.event();
              const att = get.attitude(player3, target);
              if (enemy) {
                return -att;
              } else if (att > 0) {
                return att / (1 + target.countCards("h"));
              } else {
                return att / 100;
              }
            }).set("enemy", get.value(cards2[0], player2, "raw") < 0).forResult();
            if (result?.bool) {
              let links;
              if (!result.links?.length) {
                links = cards2.slice();
              } else {
                links = result.links;
              }
              cards2.removeArray(links);
              const [target] = result.targets;
              if (!given_map.has(target)) {
                given_map.set(target, links);
              } else {
                given_map.get(target).addArray(links);
              }
            } else {
              break;
            }
          }
          if (_status.connectMode) {
            game.broadcastAll(() => {
              delete _status.noclearcountdown;
              game.stopCountChoose();
            });
          }
          if (given_map.size) {
            await game.loseAsync({
              gain_list: Array.from(given_map),
              giver: player2,
              animate: "gain2"
            }).setContent("gaincardMultiple");
            if (goon) {
              await player2.draw();
            }
          }
          player2.removeSkill(event.name);
        },
        onremove(player2, skill) {
          const cards2 = player2.getExpansions(skill);
          if (cards2.length) {
            player2.loseToDiscardpile(cards2);
          }
        },
        intro: {
          markcount: "expansion",
          mark(dialog, storage, player2) {
            var cards2 = player2.getExpansions("xinqingjian_effect");
            if (player2.isUnderControl(true)) {
              dialog.addAuto(cards2);
            } else {
              return "共有" + get.cnNumber(cards2.length) + "张牌";
            }
          }
        }
      }
    }
  },
  zhongzuo: {
    audio: 2,
    trigger: { global: "phaseJieshuBegin" },
    direct: true,
    filter(event, player2) {
      return player2.getHistory("damage").length > 0 || player2.getHistory("sourceDamage").length > 0;
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt("zhongzuo"), "令一名角色摸两张牌。若其已受伤，则你摸一张牌。").set("ai", function(target) {
        if (target.hasSkillTag("nogain")) {
          return target.isDamaged() ? 0 : 1;
        }
        let att = get.attitude(_status.event.player, target);
        if (att <= 0) {
          return 0;
        }
        if (target.isDamaged()) {
          return 1 + att / 5;
        }
        return att / 5;
      }).forResult();
      if (result.bool) {
        const target = result.targets[0];
        player2.logSkill("zhongzuo", target);
        await target.draw(2);
        if (target.isDamaged()) {
          await player2.draw();
        }
      }
    }
  },
  wanlan: {
    audio: 2,
    trigger: { global: "dying" },
    check(event, player2) {
      if (get.attitude(player2, event.player) < 4) {
        return false;
      }
      if (player2.countCards("hs", (card2) => player2.canSaveCard(card2, event.player)) >= 1 - event.player.hp) {
        return false;
      }
      if (event.player == player2 || event.player == get.zhu(player2)) {
        return true;
      }
      if (_status.currentPhase && get.damageEffect(_status.currentPhase, player2, player2) < 0) {
        return false;
      }
      if (get.recoverEffect(event.player, player2, player2) <= 0) {
        return false;
      }
      return !player2.hasUnknown();
    },
    limited: true,
    filter(event, player2) {
      return event.player.hp <= 0;
    },
    skillAnimation: true,
    animationColor: "thunder",
    logTarget: "player",
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      const hs = player2.getCards("h");
      if (hs.length) {
        await player2.modedDiscard(hs);
      }
      await trigger.player.recoverTo(1);
      if (_status.currentPhase?.isIn()) {
        player2.when({ global: "dyingAfter" }).filter((evt) => evt === trigger).step(async () => await _status.currentPhase?.damage());
      }
    }
  },
  rezhiyi: {
    audio: "zhiyi",
    trigger: { global: "phaseJieshuBegin" },
    forced: true,
    filter(event, player2) {
      return player2.getHistory("useCard", function(card2) {
        return get.type(card2.card) == "basic";
      }).length > 0 || player2.getHistory("respond", function(card2) {
        return get.type(card2.card) == "basic";
      }).length > 0;
    },
    async content(event, trigger, player2) {
      const list = [];
      player2.getHistory("useCard", function(evt) {
        if (get.type(evt.card) != "basic") {
          return;
        }
        var name = evt.card.name;
        if (name == "sha") {
          var nature = evt.card.nature;
          switch (nature) {
            case "fire":
              name = "huosha";
              break;
            case "thunder":
              name = "leisha";
              break;
            case "kami":
              name = "kamisha";
              break;
            case "ice":
              name = "icesha";
              break;
            case "stab":
              name = "cisha";
              break;
          }
        }
        list.add(name);
      });
      player2.getHistory("respond", function(evt) {
        if (get.type(evt.card) != "basic") {
          return;
        }
        var name = evt.card.name;
        if (name == "sha") {
          var nature = evt.card.nature;
          switch (nature) {
            case "fire":
              name = "huosha";
              break;
            case "thunder":
              name = "leisha";
              break;
            case "kami":
              name = "kamisha";
              break;
            case "ice":
              name = "icesha";
              break;
            case "stab":
              name = "cisha";
              break;
          }
        }
        list.add(name);
      });
      const result = await player2.chooseButton(
        [
          "执义：选择要使用的牌，或点取消摸一张牌",
          [
            list.map(function(name) {
              return ["基本", "", name];
            }),
            "vcard"
          ]
        ],
        function(button) {
          return _status.event.player.getUseValue({
            name: button.link[2],
            nature: button.link[3]
          });
        },
        function(button) {
          return _status.event.player.hasUseTarget({
            name: button.link[2],
            nature: button.link[3]
          });
        }
      ).forResult();
      if (!result.bool) {
        await player2.draw();
      } else {
        await player2.chooseUseTarget({ name: result.links[0][2], isCard: true, nature: result.links[0][3] }, true).forResult();
      }
    }
  },
  zhiyi: {
    audio: 2,
    trigger: { player: ["useCard", "respond"] },
    forced: true,
    filter(event, player2) {
      if (get.type(event.card) != "basic") {
        return false;
      }
      var history = player2.getHistory("useCard", function(evt) {
        return get.type(evt.card) == "basic";
      }).concat(
        player2.getHistory("respond", function(evt) {
          return get.type(evt.card) == "basic";
        })
      );
      return history.length == 1 && history[0] == event;
    },
    async content(event, trigger, player2) {
      const info = get.info(trigger.card);
      let result;
      if (!info || !info.enable) {
        result = { index: 0 };
      } else {
        let evt = trigger;
        if (evt.respondTo && evt.getParent("useCard").name == "useCard") {
          evt = evt.getParent("useCard");
        }
        event.evt = evt;
        result = await player2.chooseControl().set("prompt", "执义：请选择一项").set("choiceList", [
          "摸一张牌",
          "于" + get.translation(evt.card) + "的使用结算结束之后视为使用一张" + get.translation({
            name: trigger.card.name,
            nature: trigger.card.nature,
            isCard: true
          })
        ]).set("ai", function() {
          return _status.event.choice;
        }).set(
          "choice",
          (function() {
            var card2 = {
              name: trigger.card.name,
              nature: trigger.card.nature,
              isCard: true
            };
            if (card2.name == "sha") {
              if (player2.getUseValue(card2) > 0) {
                return 1;
              }
            } else if (card2.name == "tao") {
              var hp = player2.maxHp - player2.hp;
              if (trigger.targets.includes(player2)) {
                hp--;
              }
              return hp > 0 ? 1 : 0;
            }
            return 0;
          })()
        ).forResult();
      }
      if (result.index == 0) {
        await player2.draw();
      } else {
        const next = player2.chooseUseTarget({ name: trigger.card.name, nature: trigger.card.nature, isCard: true }, false, true);
        _status.event.next.remove(next);
        event.evt.after.push(next);
        next.logSkill = "zhiyi";
      }
    }
  },
  //表演测试
  qiaosi_map: {
    charlotte: true,
    nopop: true
  },
  qiaosi: {
    audio: 2,
    derivation: "qiaosi_map",
    enable: "phaseUse",
    usable: 1,
    manualConfirm: true,
    async content(event, trigger, player2) {
      event.videoId = lib.status.videoId++;
      if (player2.isUnderControl()) {
        game.swapPlayerAuto(player2);
      }
      var switchToAuto = function() {
        game.pause();
        game.countChoose();
        setTimeout(function() {
          _status.imchoosing = false;
          event._result = {
            bool: true,
            links: ["qiaosi_c1", "qiaosi_c6"].concat(["qiaosi_c2", "qiaosi_c3", "qiaosi_c4", "qiaosi_c5"].randomGets(1))
          };
          if (event.dialog) {
            event.dialog.close();
          }
          if (event.controls) {
            for (var i of event.controls) {
              i.close();
            }
          }
          game.resume();
        }, 5e3);
      };
      var createDialog = function(player3, id) {
        if (player3 == game.me) {
          return;
        }
        var str = get.translation(player3) + "正在表演...<br>";
        for (var i = 1; i < 7; i++) {
          str += get.translation("qiaosi_c" + i);
          if (i % 3 != 0) {
            str += "　　";
          }
          if (i == 3) {
            str += "<br>";
          }
        }
        ui.create.dialog(str, "forcebutton").videoId = id;
      };
      var chooseButton = function(player3) {
        var event2 = _status.event;
        player3 = player3 || event2.player;
        event2.status = {
          qiaosi_c1: 0,
          qiaosi_c2: 0,
          qiaosi_c3: 0,
          qiaosi_c4: 0,
          qiaosi_c5: 0,
          qiaosi_c6: 0
        };
        event2.map = {
          qiaosi_c1: [40, 60],
          qiaosi_c2: [80, 120],
          qiaosi_c3: [90, 110],
          qiaosi_c4: [90, 110],
          qiaosi_c5: [80, 120],
          qiaosi_c6: [40, 60]
        };
        event2.finishedx = [];
        event2.str = '请开始你的表演<br><img src="' + lib.assetURL + 'image/card/qiaosi_card1.png" width="60" height="60">qiaosi_c1% <img src="' + lib.assetURL + 'image/card/qiaosi_card2.png" width="60" height="60">qiaosi_c2% <img src="' + lib.assetURL + 'image/card/qiaosi_card3.png" width="60" height="60">qiaosi_c3%<br><img src="' + lib.assetURL + 'image/card/qiaosi_card4.png" width="60" height="60">qiaosi_c4%<img src="' + lib.assetURL + 'image/card/qiaosi_card5.png" width="60" height="60">qiaosi_c5% <img src="' + lib.assetURL + 'image/card/qiaosi_card6.png" width="60" height="60">qiaosi_c6%';
        event2.dialog = ui.create.dialog(event2.str, "forcebutton", "hidden");
        event2.dialog.addText("<li>点击下方的按钮，可以增加按钮对应的角色的「表演完成度」。对于不同的角色，点击时增加的完成度不同，最终获得的牌也不同。一次表演最多只能完成3名角色的进度。", false);
        event2.dialog.open();
        for (var i in event2.status) {
          event2.dialog.content.childNodes[0].innerHTML = event2.dialog.content.childNodes[0].innerHTML.replace(i, event2.status[i]);
        }
        for (var i = 0; i < event2.dialog.buttons.length; i++) {
          event2.dialog.buttons[i].classList.add("pointerdiv");
        }
        event2.switchToAuto = function() {
          event2._result = {
            bool: true,
            links: event2.finishedx.slice(0)
          };
          event2.dialog.close();
          for (var i2 of event2.controls) {
            i2.close();
          }
          game.resume();
          _status.imchoosing = false;
        }, event2.controls = [];
        for (var i = 1; i <= 6; i++) {
          event2.controls.push(
            ui.create.control("qiaosi_c" + i, function(link) {
              var event3 = _status.event;
              if (event3.finishedx.includes(link)) {
                return;
              }
              event3.status[link] += get.rand.apply(get, event3.map[link]);
              if (event3.status[link] >= 100) {
                event3.status[link] = 100;
                var str = event3.str.slice(0);
                for (var i2 in event3.status) {
                  str = str.replace(i2, event3.status[i2]);
                }
                event3.dialog.content.childNodes[0].innerHTML = str;
                event3.finishedx.push(link);
                if (event3.finishedx.length >= 3) {
                  event3._result = {
                    bool: true,
                    links: event3.finishedx.slice(0)
                  };
                  event3.dialog.close();
                  for (var i2 of event3.controls) {
                    i2.close();
                  }
                  game.resume();
                  _status.imchoosing = false;
                }
              } else {
                var str = event3.str.slice(0);
                for (var i2 in event3.status) {
                  str = str.replace(i2, event3.status[i2]);
                }
                event3.dialog.content.childNodes[0].innerHTML = str;
              }
            })
          );
        }
        for (var i = 0; i < event2.dialog.buttons.length; i++) {
          event2.dialog.buttons[i].classList.add("selectable");
        }
        game.pause();
        game.countChoose();
      };
      game.broadcastAll(createDialog, player2, event.videoId);
      if (event.isMine()) {
        chooseButton();
      } else if (event.isOnline()) {
        event.player.send(chooseButton, event.player);
        event.player.wait();
        game.pause();
      } else {
        switchToAuto();
      }
      await game.pause();
      game.broadcastAll("closeDialog", event.videoId);
      const map = event.result || event._result || {};
      if (!map || !map.bool || !map.links) {
        game.log(player2, "表演失败");
        return;
      }
      const list = map.links;
      if (!list.length) {
        game.log(player2, "表演失败");
        return;
      }
      const cards2 = [];
      const list2 = [];
      if (list.includes("qiaosi_c1")) {
        list2.push("trick");
        list2.push("trick");
      }
      if (list.includes("qiaosi_c2")) {
        if (list.includes("qiaosi_c6")) {
          list2.push(["sha", "jiu"]);
        } else {
          list2.push(Math.random() < 0.66 ? "equip" : ["sha", "jiu"]);
        }
      }
      if (list.includes("qiaosi_c3")) {
        list2.push([Math.random() < 0.66 ? "sha" : "jiu"]);
      }
      if (list.includes("qiaosi_c4")) {
        list2.push([Math.random() < 0.66 ? "shan" : "tao"]);
      }
      if (list.includes("qiaosi_c5")) {
        if (list.includes("qiaosi_c1")) {
          list2.push(["shan", "tao"]);
        } else {
          list2.push(Math.random() < 0.66 ? "trick" : ["shan", "tao"]);
        }
      }
      if (list.includes("qiaosi_c6")) {
        list2.push("equip");
        list2.push("equip");
      }
      while (list2.length) {
        var filter = list2.shift();
        var card2 = get.cardPile(function(x) {
          if (cards2.includes(x)) {
            return false;
          }
          if (typeof filter == "string" && get.type(x, "trick") == filter) {
            return true;
          }
          if (typeof filter == "object" && filter.includes(x.name)) {
            return true;
          }
        });
        if (card2) {
          cards2.push(card2);
        } else {
          var card2 = get.cardPile(function(x) {
            return !cards2.includes(x);
          });
          if (card2) {
            cards2.push(card2);
          }
        }
      }
      if (!cards2.length) {
        return;
      }
      event.cards = cards2;
      const num = cards2.length;
      event.num = num;
      player2.showCards(cards2);
      await player2.gain(cards2, "gain2");
      const goon = game.hasPlayer((current) => current != player2);
      if (!player2.hasCards("he") && goon) {
        return;
      }
      let index;
      if (player2.hasCards("he") && goon) {
        const result = await player2.chooseControl().set("choiceList", ["将" + get.cnNumber(num) + "张牌交给一名其他角色", "弃置" + get.cnNumber(num) + "张牌"]).set("ai", function() {
          if (game.hasPlayer(function(current) {
            return current != player2 && get.attitude(player2, current) > 2;
          })) {
            return 0;
          }
          return 1;
        }).forResult();
        index = result.index;
      } else {
        index = goon ? 0 : 1;
      }
      if (index == 0) {
        const result2 = await player2.chooseCardTarget({
          position: "he",
          filterCard: true,
          selectCard: Math.min(num, player2.countCards("he")),
          filterTarget(card3, player3, target) {
            return player3 != target;
          },
          ai1(card3) {
            return 1;
          },
          ai2(target) {
            var att = get.attitude(_status.event.player, target);
            if (target.hasSkillTag("nogain")) {
              att /= 10;
            }
            if (target.hasJudge("lebu")) {
              att /= 5;
            }
            return att;
          },
          prompt: "选择" + get.cnNumber(num) + "张牌，交给一名其他角色。",
          forced: true
        }).forResult();
        if (result2.bool) {
          const target = result2.targets[0];
          await player2.give(result2.cards, target);
        }
      } else {
        await player2.chooseToDiscard(num, true, "he", "allowChooseAll").forResult();
      }
    },
    ai: {
      order: 10,
      result: { player: 1 },
      threaten: 3.2
    }
  },
  refuhai: {
    audio: "xinfu_fuhai",
    enable: "phaseUse",
    usable: 1,
    async content(event, trigger, player2) {
      event.current = player2.next;
      const upper = [];
      const lower = [];
      const acted = [];
      let num = 0;
      let stopped = false;
      let chosen;
      while (true) {
        acted.push(event.current);
        const result = await event.current.chooseControl("潮起", "潮落").set("prompt", "潮鸣起乎？潮鸣落乎？").set("ai", function() {
          return Math.random() < 0.5 ? 0 : 1;
        }).forResult();
        if (!chosen) {
          chosen = result.control;
        }
        if (chosen != result.control) {
          stopped = true;
        }
        if (!stopped) {
          num++;
        }
        if (result.control == "潮起") {
          upper.push(event.current);
        } else {
          lower.push(event.current);
        }
        event.current = event.current.next;
        if (event.current == player2 || acted.includes(event.current)) {
          break;
        }
      }
      for (var i = 0; i < acted.length; i++) {
        const bool = upper.includes(acted[i]);
        game.log(acted[i], "选择了", bool ? "#g潮起" : "#y潮落");
        acted[i].popup(bool ? "潮起" : "潮落", bool ? "wood" : "orange");
      }
      await game.delay(1);
      if (num > 1) {
        await player2.draw(num);
      }
    },
    ai: {
      order: 10,
      result: { player: 1 }
    }
  },
  rebiaozhao: {
    audio: "biaozhao",
    // intro: {
    // 	content: "expansion",
    // 	markcount: "expansion",
    // },
    trigger: {
      player: "phaseJieshuBegin"
    },
    direct: true,
    filter(event, player2) {
      return player2.countCards("he") > 0 && !player2.getExpansions("rebiaozhao").length;
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseCard("he", get.prompt("rebiaozhao"), "将一张牌置于武将牌上作为“表”").set("ai", function(card2) {
        return 6 - get.value(card2);
      }).forResult();
      if (result.bool) {
        player2.logSkill("rebiaozhao");
        const addExpansionEvent = player2.addToExpansion(player2, result.cards);
        addExpansionEvent.gaintag.add("rebiaozhao");
        await addExpansionEvent;
      }
    },
    intro: {
      markcount: "expansion",
      mark(dialog, content, player2) {
        var content = player2.getExpansions("rebiaozhao");
        if (content && content.length) {
          if (player2 == game.me || player2.isUnderControl()) {
            dialog.addAuto(content);
          } else {
            return "共有" + get.cnNumber(content.length) + "张表";
          }
        }
      },
      content(content, player2) {
        var content = player2.getExpansions("rebiaozhao");
        if (content && content.length) {
          if (player2 == game.me || player2.isUnderControl()) {
            return get.translation(content);
          }
          return "共有" + get.cnNumber(content.length) + "张表";
        }
      }
    },
    ai: {
      notemp: true
    },
    group: ["rebiaozhao2", "rebiaozhao3"]
  },
  rebiaozhao2: {
    trigger: {
      global: ["loseAfter", "cardsDiscardAfter", "loseAsyncAfter", "equipAfter"]
    },
    forced: true,
    audio: "biaozhao",
    sourceSkill: "rebiaozhao",
    filter(event, player2) {
      var cards3 = player2.getExpansions("rebiaozhao"), cards2 = event.getd();
      if (!cards3.length || !cards2.length) {
        return false;
      }
      var num = get.number(cards3[0]);
      var cards3 = event.getd();
      for (var card2 of cards3) {
        if (get.number(card2) == num) {
          return true;
        }
      }
      return false;
    },
    async content(event, trigger, player2) {
      player2.loseToDiscardpile(player2.getExpansions("rebiaozhao"));
      player2.loseHp();
    }
  },
  rebiaozhao3: {
    trigger: {
      player: "phaseZhunbeiBegin"
    },
    forced: true,
    charlotte: true,
    audio: "biaozhao",
    sourceSkill: "rebiaozhao",
    filter(event, player2) {
      return player2.getExpansions("rebiaozhao").length > 0;
    },
    async content(event, trigger, player2) {
      await player2.loseToDiscardpile(player2.getExpansions("rebiaozhao"));
      const result = await player2.chooseTarget("令一名角色摸三张牌并回复1点体力", true).set("ai", function(target) {
        var num = 2;
        if (target.isDamaged()) {
          num++;
        }
        return num * get.attitude(_status.event.player, target);
      }).forResult();
      if (result.bool) {
        const target = result.targets[0];
        player2.line(target, "green");
        await target.draw(3);
        await target.recover();
      }
    }
  },
  reqianxin: {
    audio: "xinfu_qianxin",
    enable: "phaseUse",
    usable: 1,
    filterCard: true,
    selectCard() {
      return [1, Math.min(2, game.players.length - 1)];
    },
    check(card2) {
      return 6 - get.value(card2);
    },
    discard: false,
    lose: false,
    delay: false,
    async content(event, trigger, player2) {
      var targets2 = game.filterPlayer(function(current) {
        return current != player2;
      }).randomGets(cards.length);
      var map = [];
      for (var i = 0; i < targets2.length; i++) {
        var target = targets2[i];
        target.addSkill("reqianxin2");
        target.storage.reqianxin2.push([cards[i], player2]);
        map.push([target, cards[i]]);
      }
      game.loseAsync({
        gain_list: map,
        player: player2,
        cards,
        giver: player2,
        animate: "giveAuto"
      }).setContent("gaincardMultiple");
    },
    ai: {
      order: 1,
      result: {
        player: 1
      }
    }
  },
  reqianxin2: {
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    popup: false,
    charlotte: true,
    sourceSkill: "reqianxin",
    init(player2, skill) {
      if (!player2.storage[skill]) {
        player2.storage[skill] = [];
      }
    },
    onremove: true,
    filter(event, player2) {
      var list = player2.storage.reqianxin2;
      if (Array.isArray(list)) {
        var hs = player2.getCards("h");
        for (var i = 0; i < list.length; i++) {
          if (hs.includes(list[i][0]) && list[i][1].isIn()) {
            return true;
          }
        }
      }
      return false;
    },
    async content(event, trigger, player2) {
      while (player2.storage.reqianxin2.length) {
        const current = player2.storage.reqianxin2.shift();
        const source = current[1];
        if (!source.isIn() || !player2.getCards("h").includes(current[0])) {
          continue;
        }
        source.logSkill("reqianxin", player2);
        const result = await player2.chooseControl().set("choiceList", ["令" + get.translation(source) + "摸两张牌", "令自己本回合的手牌上限-2"]).set("prompt", get.translation(source) + "发动了【遣信】，请选择一项").set("source", source).set("ai", function() {
          var player3 = _status.event.player;
          if (get.attitude(player3, _status.event.source) > 0) {
            return 0;
          }
          if (player3.maxHp - player3.countCards("h") > 1) {
            return 1;
          }
          return Math.random() > 0.5 ? 0 : 1;
        }).forResult();
        if (result.index == 0) {
          await source.draw(2);
        } else {
          player2.addTempSkill("reqianxin3");
          player2.addMark("reqianxin3", 2, false);
        }
      }
      player2.removeSkill("reqianxin2");
    }
  },
  reqianxin3: {
    onremove: true,
    mod: {
      maxHandcard(player2, num) {
        return num - player2.countMark("reqianxin3");
      }
    }
  },
  renshi: {
    audio: 2,
    trigger: { player: "damageBegin4" },
    forced: true,
    filter(event, player2) {
      return player2.isDamaged() && event.card && event.card.name == "sha";
    },
    async content(event, trigger, player2) {
      trigger.cancel();
      if (trigger.cards) {
        const cards2 = trigger.cards.filterInD();
        if (cards2.length) {
          await player2.gain(cards2, "gain2", "log");
        }
      }
      await player2.loseMaxHp();
    },
    ai: {
      halfneg: true,
      filterDamage: true,
      skillTagFilter(player2, tag, arg) {
        if (arg && arg.card && arg.card.name == "sha") {
          return true;
        }
        return false;
      }
    }
  },
  wuyuan: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.countCards("h", "sha") > 0;
    },
    filterCard: { name: "sha" },
    filterTarget: lib.filter.notMe,
    check(card2) {
      var player2 = _status.event.player;
      if (get.color(card2) == "red" && game.hasPlayer(function(current) {
        return current != player2 && current.isDamaged() && get.attitude(player2, current) > 2;
      })) {
        return 2;
      }
      if (get.natureList(card2).length) {
        return 1.5;
      }
      return 1;
    },
    discard: false,
    lose: false,
    delay: false,
    async content(event, trigger, player2) {
      const {
        cards: cards2,
        targets: [target]
      } = event;
      await player2.give(cards2, target, "give");
      await player2.recover();
      const num = 1 + (get.natureList(cards2[0]).length ? 1 : 0);
      await target.draw(num);
      if (get.color(cards2[0]) == "red") {
        await target.recover();
      }
    },
    ai: {
      order: 1,
      result: {
        player(player2, target) {
          if (player2.isDamaged()) {
            return 1;
          }
          return 0;
        },
        target(player2, target) {
          if (ui.selected.cards.length) {
            var num = 1;
            if (get.natureList(ui.selected.cards[0]).length) {
              num++;
            }
            if (target.hasSkillTag("nogain")) {
              num = 0;
            }
            if (get.color(ui.selected.cards[0]) == "red") {
              return num + 2;
            } else {
              return num + 1;
            }
          }
          return 1;
        }
      }
    }
  },
  huaizi: {
    mod: {
      maxHandcardBase(player2, num) {
        return player2.maxHp;
      }
    },
    //audio:2,
    //trigger:{player:'phaseDiscardBegin'},
    forced: true,
    firstDo: true,
    filter(event, player2) {
      return player2.isDamaged() && player2.countCards("h") > player2.hp;
    },
    async content(event, trigger, player2) {
    }
  },
  rexushen: {
    derivation: ["new_rewusheng", "redangxian"],
    audio: "xinfu_xushen",
    limited: true,
    enable: "phaseUse",
    filter(event, player2) {
      return game.hasPlayer(function(current) {
        return current.hasSex("male");
      });
    },
    skillAnimation: true,
    animationColor: "fire",
    async content(event, trigger, player2) {
      player2.addSkill("rexushen2");
      player2.awakenSkill(event.name);
      player2.loseHp(
        game.countPlayer(function(current) {
          return current.hasSex("male");
        })
      );
    },
    ai: {
      order: 10,
      result: {
        player(player2) {
          if (player2.hp != game.countPlayer(function(current) {
            return current.hasSex("male");
          })) {
            return 0;
          }
          return game.hasPlayer(function(current) {
            return get.attitude(player2, current) > 4 && current.countCards("h", "tao");
          }) ? 1 : 0;
        }
      }
    }
  },
  rexushen2: {
    charlotte: true,
    subSkill: {
      count: {
        trigger: {
          player: "recoverBegin"
        },
        forced: true,
        silent: true,
        popup: false,
        filter(event, player2) {
          if (!event.source) {
            return false;
          }
          if (!player2.isDying()) {
            return false;
          }
          var evt = event.getParent("dying").getParent(2);
          return evt.name == "rexushen" && evt.player == player2;
        },
        async content(event, trigger, player2) {
          trigger.rexushen = true;
        },
        sub: true
      }
    },
    group: ["rexushen2_count"],
    trigger: {
      player: "recoverAfter"
    },
    sourceSkill: "rexushen",
    filter(event, player2) {
      if (player2.isDying()) {
        return false;
      }
      return event.rexushen == true;
    },
    direct: true,
    silent: true,
    popup: false,
    async content(event, trigger, player2) {
      player2.removeSkill("rexushen2");
      const result = await player2.chooseBool("是否令" + get.translation(trigger.source) + "获得技能〖武圣〗和〖当先〗").set("ai", function() {
        return get.attitude(player2, trigger.source) > 0;
      }).forResult();
      if (result.bool) {
        player2.line(trigger.source, "fire");
        trigger.source.addSkills(["new_rewusheng", "redangxian"]);
      }
    }
  },
  rezhennan: {
    audio: "xinfu_zhennan",
    trigger: { target: "useCardToTargeted" },
    filter(event, player2) {
      return event.player != player2 && event.targets && event.targets.length && event.targets.length > event.player.hp;
    },
    direct: true,
    async content(event, trigger, player2) {
      const result = await player2.chooseToDiscard(get.prompt("rezhennan", trigger.player), "弃置一张牌并对其造成1点伤害", "he").set("logSkill", ["rezhennan", trigger.player]).set("ai", function(card2) {
        var player3 = _status.event.player;
        var target = _status.event.getTrigger().player;
        if (get.damageEffect(target, player3, player3) > 0) {
          return 7 - get.value(card2);
        }
        return -1;
      }).forResult();
      if (result.bool) {
        await trigger.player.damage();
      }
    }
  },
  meiyong: {
    inherit: "xinfu_wuniang",
    audio: "xinfu_wuniang",
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt("meiyong"), "获得一名其他角色区域内的一张牌，然后其摸一张牌。", function(card2, player3, target2) {
        if (player3 == target2) {
          return false;
        }
        return target2.countGainableCards(player3, "hej") > 0;
      }).set("ai", function(target2) {
        return 10 - get.attitude(_status.event.player, target2);
      }).forResult();
      if (!result.bool) {
        return;
      }
      const target = result.targets[0];
      player2.logSkill("meiyong", target);
      await player2.gainPlayerCard(target, "hej", true);
      await target.draw();
    }
  },
  relianji: {
    audio: "wylianji",
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return game.countPlayer((current) => current != player2) > 1;
    },
    filterTarget: lib.filter.notMe,
    targetprompt: ["打人", "被打"],
    selectTarget: 2,
    multitarget: true,
    async content(event, trigger, player2) {
      await game.delay(0.5);
      const [target1, target2] = event.targets;
      if (target1.hasEquipableSlot(1)) {
        let equip1 = get.cardPile2((card2) => card2.name == "qinggang");
        if (!equip1 || Math.random() > 0.5) {
          equip1 = get.cardPile2((card2) => get.subtype(card2) == "equip1" && target1.hasUseTarget(card2), "random");
        }
        if (equip1) {
          if (equip1.name == "qinggang" && !lib.inpile.includes("qibaodao")) {
            game.broadcastAll((card2) => {
              card2.init([card2.suit, card2.number, "qibaodao"]);
            }, equip1);
          }
          target1.$draw(equip1);
          await target1.chooseUseTarget(equip1, "noanimate", "nopopup", true);
        }
      }
      const list = ["nanman", "wanjian", "huogong", "juedou", "sha"].filter((name2) => target1.canUse({ name: name2, isCard: true }, target2, false));
      if (!list.length) {
        return;
      }
      let list2;
      const name = list.randomGet();
      if (["nanman", "wanjian"].includes(name)) {
        list2 = game.filterPlayer((current) => target1.canUse({ name, isCard: true }, current, false));
      } else {
        list2 = target2;
      }
      if (list2) {
        player2.addTempSkill(event.name + "_mark");
        await target1.useCard({ name, isCard: true }, list2, "noai");
        await game.delay(0.5);
      }
    },
    onremove: true,
    marktext: "计",
    intro: {
      name: "连计(连计/谋逞)",
      name2: "连计",
      content: "mark"
    },
    ai: {
      order: 8,
      result: {
        target(player2, target) {
          if (ui.selected.targets.length == 0) {
            return 1;
          } else {
            return -1;
          }
        }
      },
      expose: 0.4,
      threaten: 3
    },
    subSkill: {
      mark: {
        charlotte: true,
        trigger: { global: "changeHpAfter" },
        filter(event, player2) {
          const evt = event.getParent(4);
          if (!evt || evt.name !== "relianji" || evt.player !== player2) {
            return false;
          }
          const source = evt.targets[0];
          return event.changedHp < 0 && event.getParent().source == source && event.getParent(3).player == source;
        },
        forced: true,
        popup: false,
        silent: true,
        async content(event, trigger, player2) {
          player2.addMark("relianji", Math.abs(trigger.changedHp));
        }
      }
    }
  },
  remoucheng: {
    derivation: ["jingong", "relianji"],
    trigger: { global: "damageSource" },
    forced: true,
    filter(event, player2) {
      return player2.countMark("relianji") > 2;
    },
    audio: "moucheng",
    juexingji: true,
    skillAnimation: true,
    animationColor: "thunder",
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      await player2.gainMaxHp();
      await player2.recover();
      await player2.changeSkills(["jingong"], ["relianji"]);
    },
    ai: { combo: "relianji" }
  },
  shouye: {
    audio: 2,
    trigger: { target: "useCardToTargeted" },
    filter(event, player2) {
      return event.player != player2 && event.targets.length == 1;
    },
    check(event, player2) {
      if (event.player == game.me || event.player.isOnline()) {
        return get.attitude(player2, event.player) < 0;
      }
      return get.effect(player2, event.card, event.player, player2) < 0;
    },
    usable: 1,
    logTarget: "player",
    async content(event, trigger, player2) {
      const result = await player2.chooseToDuiben(trigger.player).forResult();
      if (result?.bool) {
        player2.addTempSkill(event.name + "_after");
        player2.markAuto(event.name + "_after", [trigger.getParent()]);
        trigger.getParent().excluded.add(player2);
      }
    },
    subSkill: {
      after: {
        charlotte: true,
        onremove: true,
        trigger: { global: "cardsDiscardEnd" },
        filter(event, player2) {
          if (!event.cards.filterInD("d").length) return false;
          const evt = event.getParent();
          if (evt.name != "orderingDiscard") return false;
          const evtx = evt.relatedEvent || evt.getParent();
          return evtx.player.hasHistory("useCard", (evtxx) => {
            return evtx.getParent() == (evtxx.relatedEvent || evtxx.getParent()) && player2.getStorage("shouye_after").includes(evtxx);
          });
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const evt = trigger.getParent();
          const evtx = evt.relatedEvent || evt.getParent();
          player2.unmarkAuto(event.name, [evtx]);
          if (!player2.getStorage(event.name).length) {
            player2.removeSkill(event.name);
          }
          const cards2 = trigger.cards.filterInD("d");
          if (cards2.length) {
            await player2.gain(cards2, "gain2", "log");
          }
        }
      }
    }
  },
  liezhi: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    filter(event, player2) {
      return game.hasPlayer((current) => current != player2 && current.hasDiscardableCards(player2, "hej"));
    },
    async cost(event, trigger, player2) {
      event.result = await player2.chooseTarget(get.prompt(event.skill), "弃置至多两名其他角色区域内的各一张牌", [1, 2], (card2, player3, target) => {
        return target != player3 && target.hasDiscardableCards(player3, "hej");
      }).set("ai", (target) => {
        const player3 = _status.event.player;
        return get.effect(target, { name: "guohe_copy" }, player3, player3);
      }).forResult();
    },
    logAudio: () => 1,
    async content(event, trigger, player2) {
      for (const target of event.targets.sortBySeat()) {
        await player2.discardPlayerCard(target, "hej", true);
      }
    },
    group: "liezhi_damage",
    subSkill: {
      damage: {
        audio: "liezhi",
        trigger: { player: "damage" },
        forced: true,
        locked: false,
        logAudio: () => "liezhi2.mp3",
        async content(event, trigger, player2) {
          player2.tempBanSkill("liezhi", { player: "phaseJieshuBegin" });
        }
      }
    }
  },
  xinzhanyi: {
    audio: "zhanyi",
    enable: "phaseUse",
    usable: 1,
    filterCard: lib.filter.cardDiscardable,
    position: "he",
    check(card2) {
      const player2 = get.player();
      const type = get.type2(card2);
      if (!["basic", "equip", "trick"].includes(type)) {
        return 0;
      }
      if (get.effect(player2, { name: "losehp" }, player2, player2) < 0 && player2.hp <= 2) {
        return 0;
      }
      if (type == "basic") {
        if (player2.hasCards("hs", (cardx) => cardx != card2 && get.type(cardx) == "basic") && (player2.isDamaged() || player2.countCards("h") >= 7)) {
          if (!player2.hasValueTarget(card2)) {
            return 10;
          }
          return 6.5 - get.value(card2);
        }
      } else if (type == "trick") {
        if (!player2.hasValueTarget(card2)) {
          return 10;
        }
        return 7 - get.value(card2);
      } else if (type == "equip") {
        if (player2.hasSha() && game.hasPlayer((current) => {
          return player2.canUse({ name: "sha" }, current) && get.attitude(player2, current) < 0 && get.effect(current, { name: "sha" }, player2, player2) > 0 && current.hasCards("he");
        })) {
          return 7 - get.value(card2);
        }
      }
      return 0;
    },
    async content(event, trigger, player2) {
      const { cards: cards2 } = event;
      const type = get.type(cards2[0], "trick", cards2[0].original == "h" ? player2 : false);
      await player2.loseHp();
      if (["basic", "equip", "trick"].includes(type)) {
        player2.addTempSkill(`${event.name}_${type}`, "phaseUseAfter");
        if (type == "basic") {
          player2.addTempSkill(`${event.name}_effect`, "phaseUseAfter");
        }
        if (type == "trick") {
          await player2.draw(3);
        }
      }
    },
    ai: {
      order: 9.1,
      result: {
        player(player2) {
          if (get.effect(player2, { name: "losehp" }, player2, player2) < 0 && player2.hp <= 2) {
            return 0;
          }
          if (player2.hasCards("he", (card2) => {
            if (!lib.filter.cardDiscardable(card2, player2, "xinzhanyi")) {
              return false;
            }
            const type = get.type2(card2);
            if (["equip", "trick"].includes(type)) {
              return true;
            }
            if (type == "basic" && player2.hasCards("hs", (cardx) => cardx != card2 && get.type(cardx) == "basic")) {
              return true;
            }
            return false;
          })) {
            return 1;
          }
          return 0;
        }
      }
    },
    subSkill: {
      basic: {
        charlotte: true,
        audio: "zhanyi",
        hiddenCard(player2, name) {
          return get.type(name) == "basic" && player2.hasCards("hs", { type: "basic" });
        },
        enable: "chooseToUse",
        filter(event, player2) {
          if (!_status.connectMode && !player2.hasCards("hs", (card2) => {
            return get.type(card2) == "basic";
          })) {
            return false;
          }
          return get.inpileVCardList((info) => {
            if (info[0] != "basic") {
              return false;
            }
            return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player2, event);
          }).length;
        },
        chooseButton: {
          dialog(event, player2) {
            const vcards = get.inpileVCardList((info) => {
              if (info[0] != "basic") {
                return false;
              }
              return event.filterCard(get.autoViewAs({ name: info[2], nature: info[3] }, "unsure"), player2, event);
            });
            return ui.create.dialog("战意", [vcards, "vcard"], "hidden");
          },
          check(button) {
            if (get.event().getParent().type != "phase") {
              return 1;
            }
            return get.player().getUseValue({ name: button.link[2], nature: button.link[3] });
          },
          backup(links, player2) {
            return {
              audio: "zhanyi",
              filterCard(card2, player3, target) {
                return get.type(card2) == "basic";
              },
              check(card2) {
                return 9 - get.value(card2);
              },
              viewAs: { name: links[0][2], nature: links[0][3] },
              position: "hs",
              popname: true
            };
          },
          prompt(links, player2) {
            return "将一张基本牌当" + (get.translation(links[0][3]) || "") + get.translation(links[0][2]) + "使用";
          }
        },
        hiddenCard(player2, name) {
          return get.type(name) == "basic" && player2.countCards("hs") > 0;
        },
        order: 6,
        respondSha: true,
        skillTagFilter(player2, tag, arg) {
          if (player2.hasCards("hs", (card2) => get.type(card2) == "basic")) {
            if (tag == "respondSha") {
              if (arg === "respond") {
                return false;
              }
            }
          } else {
            return false;
          }
        },
        result: {
          player(player2) {
            if (get.event().dying) {
              return get.attitude(player2, get.event().dying);
            }
            return 1;
          }
        },
        mark: true,
        intro: { content: "本阶段可以将一张基本牌当成任意基本牌使用" }
      },
      basic_backup: {},
      effect: {
        trigger: { player: "useCard1" },
        filter(event, player2) {
          return get.type(event.card, null, false) == "basic";
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player2) {
          player2.removeSkill(event.name);
          player2.addTempSkill("xinzhanyi_buff");
          trigger.card.storage ??= {};
          trigger.card.storage.xinzhanyi_buff = true;
        },
        mark: true,
        intro: { content: "本阶段下一张使用的基本牌造成的回复值/伤害值+1" }
      },
      buff: {
        charlotte: true,
        trigger: {
          source: "damageBegin1",
          global: "recoverBegin"
        },
        filter(event, player2) {
          const card2 = event.card;
          const evt = event.getParent();
          if (evt.player != player2 || !card2?.storage?.xinzhanyi_buff) {
            return false;
          }
          return true;
        },
        forced: true,
        popup: false,
        firstDo: true,
        async content(event, trigger, player2) {
          trigger.num++;
        }
      },
      equip: {
        charlotte: true,
        audio: "zhanyi",
        trigger: { player: "useCardToPlayered" },
        filter(event, player2) {
          return event.card.name == "sha" && event.target.hasCards("he") && event.targets.length == 1;
        },
        forced: true,
        check(event, player2) {
          return get.attitude(player2, event.target) < 0;
        },
        logTarget: "target",
        async content(event, trigger, player2) {
          const { target } = trigger;
          let result = await target.chooseToDiscard("he", true, 2).forResult();
          if (result?.cards?.someInD("d")) {
            const cards2 = result.cards.filterInD("d");
            result = await player2.chooseButton(["战意：选择获得其中的一张牌", cards2], true).set("ai", (button) => {
              return get.value(button.link);
            }).set("direct", true).forResult();
            if (result?.links?.length) {
              await player2.gain(result.links, "gain2");
            }
          }
        },
        mark: true,
        intro: { content: "本阶段使用【杀】指定唯一一名角色为目标后，其须弃置两张牌，然后你选择其中一张获得之" }
      },
      trick: {
        charlotte: true,
        mod: {
          wuxieRespondable() {
            return false;
          }
        },
        mark: true,
        intro: { content: "本阶段使用的锦囊牌不能被【无懈可击】响应" }
      }
    }
  },
  xinfu_daigong: {
    usable: 1,
    audio: 2,
    trigger: {
      player: "damageBegin4"
    },
    filter(event, player2) {
      return event.source != void 0 && player2.countCards("h") > 0;
    },
    async content(event, trigger, player2) {
      player2.showHandcards();
      const cards2 = player2.getCards("h");
      const suits = [];
      for (var i = 0; i < cards2.length; i++) {
        suits.add(get.suit(cards2[i]));
      }
      const result = await trigger.source.chooseCard("he", "交给" + get.translation(player2) + "一张满足条件的牌，否则防止此伤害。", function(card2) {
        return !_status.event.suits.includes(get.suit(card2));
      }).set("suits", suits).set("ai", function(card2) {
        var player3 = _status.event.player;
        var target = _status.event.getParent("xinfu_daigong").player;
        if (get.damageEffect(target, player3, player3) > 0) {
          return 6.5 - get.value(card2);
        }
        return 0;
      }).forResult();
      if (result.bool) {
        await trigger.source.give(result.cards, player2, true);
      } else {
        trigger.cancel();
      }
    }
  },
  xinfu_zhaoxin: {
    audio: 2,
    enable: "phaseUse",
    usable: 1,
    filter(event, player2) {
      return player2.hasCards("he") && player2.countExpansions("xinfu_zhaoxin") < 3;
    },
    filterCard: true,
    selectCard() {
      const player2 = get.player();
      return [1, 3 - player2.getExpansions("xinfu_zhaoxin").length];
    },
    position: "he",
    discard: false,
    lose: false,
    delay: false,
    allowChooseAll: true,
    async content(event, trigger, player2) {
      const { cards: cards2 } = event;
      const next = player2.addToExpansion(player2, "give", cards2);
      next.gaintag.add(event.name);
      await next;
      await player2.draw(cards2.length);
    },
    check(card2) {
      return 6 - get.value(card2);
    },
    marktext: "望",
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    onremove(player2, skill) {
      const cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    ai: {
      order: 10,
      result: { player: 1 }
    },
    group: ["xinfu_zhaoxin_give"],
    subSkill: {
      give: {
        audio: "xinfu_zhaoxin",
        trigger: { global: "phaseDrawAfter" },
        filter(event, player2) {
          if (!player2.hasExpansions("xinfu_zhaoxin")) {
            return false;
          }
          return player2 == event.player || player2.inRange(event.player);
        },
        async cost(event, trigger, player2) {
          const target = trigger.player;
          const result = await target.chooseButton([`###昭然：你可以获得其中一张牌###<div class='text center'>然后${get.translation(player2)}可以对你造成1点伤害</div>`, player2.getExpansions("xinfu_zhaoxin")]).set("ai", (button) => {
            const { player: player3, target: target2 } = get.event();
            const att = get.attitude(player3, target2);
            const eff = get.damageEffect(player3, target2, player3);
            const card2 = button.link;
            if (att > 0 || eff > 0) {
              return get.value(card2);
            }
            return 0;
          }).set("target", player2).forResult();
          event.result = {
            bool: result?.bool,
            cost_data: result?.links
          };
        },
        logTarget: "player",
        async content(event, trigger, player2) {
          const {
            targets: [target],
            cost_data: cards2
          } = event;
          await target.gain(cards2, "give", player2, "bySelf");
          const result = await player2.chooseBool(`是否对${get.translation(target)}造成1点伤害？`).set("choice", get.damageEffect(target, player2, player2) > 0).forResult();
          if (result?.bool) {
            await target.damage("nocard");
          }
        }
      }
    }
  },
  xinfu_qianchong: {
    audio: 1,
    init(player2, skill) {
      const es = player2.getCards("e");
      if (es.length) {
        if (es.every((card2) => get.color(card2) == "red")) {
          player2.addAdditionalSkill(skill, "mingzhe");
        } else if (es.every((card2) => get.color(card2) == "black")) {
          player2.addAdditionalSkill(skill, "weimu");
        } else {
          player2.removeAdditionalSkill(skill);
        }
      } else {
        player2.removeAdditionalSkill(skill);
      }
    },
    onremove(player2, skill) {
      player2.removeAdditionalSkill(skill);
    },
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      if (["basic", "trick", "equip"].every((type) => player2.getStorage("xinfu_qianchong_effect").includes(type))) {
        return false;
      }
      const es = player2.getCards("e");
      if (!es.length) {
        return true;
      }
      const col = get.color(es[0]);
      for (let i = 0; i < es.length; i++) {
        if (get.color(es[i]) != col) {
          return true;
        }
      }
      return false;
    },
    locked: true,
    async cost(event, trigger, player2) {
      const list = ["basic", "trick", "equip", "cancel2"];
      list.removeArray(player2.getStorage("xinfu_qianchong_effect"));
      const result = await player2.chooseControl(list).set("ai", () => {
        get.player();
        const choices = get.event().controls.slice().remove("cancel2");
        return choices.includes("basic") ? "basic" : choices.includes("trick") ? "trick" : choices.randomGet();
      }).set("prompt", get.prompt(event.skill)).set("prompt2", "你可以选择一种类别的牌，然后你本回合内使用该类别的牌时没有次数和距离限制。").forResult();
      event.result = {
        bool: result?.control != "cancel2",
        cost_data: result?.control
      };
    },
    async content(event, trigger, player2) {
      const { cost_data: type } = event;
      player2.addTempSkill(event.name + "_effect");
      player2.markAuto(event.name + "_effect", [type]);
      const str = get.translation(type) + "牌";
      game.log(player2, "声明了", "#y" + str);
      player2.popup(str, "thunder");
    },
    derivation: ["weimu", "mingzhe"],
    group: "xinfu_qianchong_change",
    subSkill: {
      effect: {
        charlotte: true,
        onremove: true,
        intro: { content: "本回合内使用$牌没有次数和距离限制" },
        mod: {
          cardUsable(card2, player2) {
            const type = get.type2(card2);
            if (player2.getStorage("xinfu_qianchong_effect").includes(type)) {
              return Infinity;
            }
          },
          targetInRange(card2, player2) {
            const type = get.type2(card2);
            if (player2.getStorage("xinfu_qianchong_effect").includes(type)) {
              return true;
            }
          }
        }
      },
      change: {
        trigger: {
          player: "loseAfter",
          global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
        },
        filter(event, player2) {
          if (event.name == "equip" && event.player == player2) {
            return true;
          }
          return event.getl?.(player2)?.es?.length;
        },
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const skill = "xinfu_qianchong";
          get.info(skill).init(player2, skill);
        }
      }
    }
  },
  qc_weimu: { audio: true },
  qc_mingzhe: { audio: true },
  xinfu_shangjian: {
    audio: 2,
    getNum(player2) {
      let num = 0;
      player2.getHistory("lose", (evt) => {
        const evt2 = evt.relatedEvent || evt.getParent();
        if (evt2.name == "useCard" && evt2.player == player2 && get.type(evt2.card, null, false) == "equip") {
          return;
        }
        if (evt.cards2?.length) {
          num += evt.cards2.length;
        }
      });
      return num;
    },
    trigger: { global: "phaseJieshuBegin" },
    filter(event, player2) {
      const num = get.info("xinfu_shangjian").getNum(player2);
      return num > 0 && num <= player2.hp;
    },
    forced: true,
    async content(event, trigger, player2) {
      const num = get.info(event.name).getNum(player2);
      if (num > 0) {
        await player2.draw(num);
      }
    }
  },
  rw_bagua_skill: {
    inherit: "bagua_skill",
    audio: true,
    async content(event, trigger, player2) {
      const result = await player2.judge("rewrite_bagua", function(card2) {
        return get.suit(card2) != "spade" ? 1.5 : -0.5;
      }).set("judge2", function(result2) {
        return result2.bool;
      }).forResult();
      if (result.judge > 0) {
        trigger.untrigger();
        trigger.set("responded", true);
        trigger.result = { bool: true, card: { name: "shan" } };
      }
    }
  },
  rw_baiyin_skill: {
    inherit: "baiyin_skill",
    audio: true,
    subSkill: {
      lose: {
        audio: "rw_baiyin_skill",
        forced: true,
        charlotte: true,
        equipSkill: true,
        trigger: {
          player: "loseAfter",
          global: ["equipAfter", "addJudgeAfter", "gainAfter", "loseAsyncAfter", "addToExpansionAfter"]
        },
        filter: (event, player2) => {
          return !player2.hasSkillTag("unequip2");
        },
        getIndex(event, player2) {
          const evt = event.getl(player2);
          const lostCards = [];
          evt.es.forEach((card2) => {
            const VEquip = evt.vcard_map.get(card2);
            if (VEquip.name === "rewrite_baiyin") {
              lostCards.add(VEquip);
            }
          });
          return lostCards.length;
        },
        async content(event, trigger, player2) {
          await player2.recover();
          await player2.draw(2);
        }
      }
    }
  },
  rw_lanyinjia: {
    inherit: "lanyinjia",
    audio: "lanyinjia"
  },
  rw_minguangkai_cancel: {
    inherit: "minguangkai_cancel"
  },
  rw_minguangkai_link: {
    inherit: "minguangkai_link",
    trigger: {
      player: "linkBefore"
    },
    forced: true,
    filter(event, player2) {
      return !player2.isLinked();
    }
  },
  rw_renwang_skill: {
    inherit: "renwang_skill",
    audio: true,
    filter(event, player2) {
      if (player2.hasSkillTag("unequip2")) {
        return false;
      }
      if (event.player.hasSkillTag("unequip", false, {
        name: event.card ? event.card.name : null,
        target: player2,
        card: event.card
      })) {
        return false;
      }
      return event.card.name == "sha" && (get.suit(event.card) == "heart" || get.color(event.card) == "black");
    },
    ai: {
      effect: {
        target(card2, player2, target) {
          if (typeof card2 !== "object" || target.hasSkillTag("unequip2")) {
            return;
          }
          if (player2.hasSkillTag("unequip", false, {
            name: card2 ? card2.name : null,
            target: player2,
            card: card2
          }) || player2.hasSkillTag("unequip_ai", false, {
            name: card2 ? card2.name : null,
            target: player2,
            card: card2
          })) {
            return;
          }
          if (card2.name == "sha" && ["spade", "club", "heart"].includes(get.suit(card2))) {
            return "zeroplayertarget";
          }
        }
      }
    }
  },
  rw_tengjia1: {
    inherit: "tengjia1",
    audio: true
  },
  rw_tengjia2: {
    inherit: "tengjia2",
    audio: true
  },
  rw_tengjia3: {
    audio: "rw_tengjia1",
    inherit: "rw_minguangkai_link"
  },
  rw_tengjia4: {
    inherit: "tengjia3",
    audio: "rw_tengjia1"
  },
  xinfu_pingcai: {
    subSkill: { backup: {} },
    async wolong_card(event, trigger, player2) {
      const ingame = game.hasPlayer(function(current) {
        const translate = get.translation(current);
        return ["诸葛亮", "卧龙", "孔明", "诸葛孔明"].some((name) => translate.includes(name));
      }) ? true : false;
      let prompt = "请选择";
      prompt += ingame ? "至多两名" : "一名";
      prompt += "角色，对其造成1点火焰伤害";
      const range = ingame ? [1, 2] : [1, 1];
      const result = await player2.chooseTarget(prompt, range).set("ai", function(target) {
        var player3 = _status.event.player;
        return get.damageEffect(target, player3, player3, "fire");
      }).forResult();
      if (result.bool && result.targets.length) {
        player2.line(result.targets, "fire");
        result.targets.sortBySeat();
        for (var i = 0; i < result.targets.length; i++) {
          await result.targets[i].damage("fire");
        }
      }
    },
    async fengchu_card(event, trigger, player2) {
      const ingame = game.hasPlayer(function(current) {
        const translate = get.translation(current);
        return ["庞统", "庞士元", "凤雏"].some((name) => translate.includes(name));
      }) ? true : false;
      let prompt = "请选择";
      prompt += ingame ? "至多四名" : "至多三名";
      prompt += "要横置的角色";
      const range = ingame ? [1, 4] : [1, 3];
      const result = await player2.chooseTarget(prompt, range, (card2, player3, target) => {
        return !target.isLinked();
      }).set("ai", function(target) {
        var player3 = _status.event.player;
        return get.effect(target, { name: "tiesuo" }, player3, player3);
      }).forResult();
      if (result.bool && result.targets.length) {
        player2.line(result.targets, "green");
        result.targets.sortBySeat();
        for (var i = 0; i < result.targets.length; i++) {
          await result.targets[i].link();
        }
      }
    },
    async xuanjian_card(event, trigger, player2) {
      const ingame = game.hasPlayer(function(current) {
        const translate = get.translation(current);
        return ["徐庶", "徐元直", "单福"].some((name) => translate.includes(name));
      }) ? true : false;
      let prompt = "请选择一名角色，令其回复1点体力并摸一张牌";
      prompt += ingame ? "，然后你摸一张牌。" : "。";
      const result = await player2.chooseTarget(prompt).set("ai", function(target) {
        var player3 = _status.event.player;
        return get.attitude(player3, target) * (target.isDamaged() ? 2 : 1);
      }).forResult();
      if (result.bool && result.targets.length) {
        const target = result.targets[0];
        player2.line(target, "thunder");
        await target.draw();
        await target.recover();
        if (ingame) {
          await player2.draw();
        }
      }
    },
    async shuijing_card(event, trigger, player2) {
      const ingame = game.hasPlayer(function(current) {
        const translate = get.translation(current);
        return ["司马徽"].some((name) => translate.includes(name));
      }) ? true : false;
      let prompt = "将一名角色装备区中的";
      prompt += ingame ? "一张牌" : "防具牌";
      prompt += "移动到另一名角色的装备区中";
      const result = await player2.chooseTarget(2, function(card2, player3, target) {
        if (ui.selected.targets.length) {
          if (!_status.event.ingame) {
            var cards2 = ui.selected.targets[0].getEquips(2);
            return cards2.some((card3) => target.canEquip(card3));
          }
          var from = ui.selected.targets[0];
          if (target.isMin()) {
            return false;
          }
          var es = from.getCards("e");
          for (var i = 0; i < es.length; i++) {
            if (target.canEquip(es[i])) {
              return true;
            }
          }
          return false;
        } else {
          if (!event.ingame) {
            if (target.getEquips(2).length) {
              return true;
            }
            return false;
          }
          return target.countCards("e") > 0;
        }
      }).set("ingame", ingame).set("ai", function(target) {
        var player3 = _status.event.player;
        var att = get.attitude(player3, target);
        if (ui.selected.targets.length == 0) {
          if (att < 0) {
            if (game.hasPlayer(function(current) {
              if (get.attitude(player3, current) > 0) {
                var es2 = target.getCards("e");
                for (var i2 = 0; i2 < es2.length; i2++) {
                  if (current.canEquip(es2[i2])) {
                    return true;
                  }
                }
                return false;
              }
            })) {
              return -att;
            }
          }
          return 0;
        }
        if (att > 0) {
          var es = ui.selected.targets[0].getCards("e");
          var i;
          for (i = 0; i < es.length; i++) {
            if (target.canEquip(es[i])) {
              break;
            }
          }
          if (i == es.length) {
            return 0;
          }
        }
        return -att * get.attitude(player3, ui.selected.targets[0]);
      }).set("multitarget", true).set("targetprompt", ["被移走", "移动目标"]).set("prompt", prompt).forResult();
      if (!result.bool) {
        return;
      }
      player2.line2(result.targets, "green");
      const targets2 = result.targets;
      await game.delay();
      if (targets2.length == 2) {
        let links;
        if (!ingame) {
          const cards2 = targets2[0].getEquips(2);
          if (cards2.length == 1) {
            links = cards2;
          } else {
            const chooseResult = await player2.choosePlayerCard(
              "e",
              true,
              function(button) {
                return get.equipValue(button.link);
              },
              targets2[0]
            ).set("targets0", targets2[0]).set("targets1", targets2[1]).set("filterButton", function(button) {
              if (!get.subtypes(button.link, false).includes("equip2")) {
                return false;
              }
              var targets1 = _status.event.targets1;
              return targets1.canEquip(button.link);
            }).forResult();
            if (chooseResult.bool && chooseResult.links.length) {
              links = chooseResult.links;
            }
          }
        } else {
          const chooseResult = await player2.choosePlayerCard(
            "e",
            true,
            function(button) {
              return get.equipValue(button.link);
            },
            targets2[0]
          ).set("targets0", targets2[0]).set("targets1", targets2[1]).set("filterButton", function(button) {
            var targets1 = _status.event.targets1;
            return targets1.canEquip(button.link);
          }).forResult();
          if (chooseResult.bool && chooseResult.links.length) {
            links = chooseResult.links;
          }
        }
        if (links?.length) {
          const link = links[0];
          if (get.position(link) == "e") {
            await targets2[1].equip(link);
          } else if (link.viewAs) {
            await targets2[1].addJudge({ name: link.viewAs }, [link]);
          } else {
            await targets2[1].addJudge(link);
          }
          targets2[0].$give(link, targets2[1], false);
          await game.delay();
        }
      }
    },
    audio: 5,
    enable: "phaseUse",
    usable: 1,
    prompt: "点击确定来选择要擦拭的宝物",
    chooseButton: {
      dialog() {
        var list = ["wolong", "fengchu", "xuanjian", "shuijing"];
        for (var i = 0; i < list.length; i++) {
          list[i] = ["", "", list[i] + "_card"];
        }
        return ui.create.dialog("评才", [list, "vcard"]);
      },
      check(button) {
        var name = button.link[2];
        var player2 = _status.event.player;
        if (name == "xuanjian_card") {
          if (game.hasPlayer(function(current) {
            return current.isDamaged() && current.hp < 3 && get.attitude(player2, current) > 1;
          })) {
            return 1 + Math.random();
          } else {
            return 1;
          }
        } else if (name == "wolong_card") {
          if (game.hasPlayer(function(current) {
            return get.damageEffect(current, player2, player2, "fire") > 0;
          })) {
            return 1.2 + Math.random();
          } else {
            return 0.5;
          }
        } else {
          return 0.6;
        }
      },
      backup(links, player2) {
        return {
          audio: "xinfu_pingcai1.mp3",
          filterCard: () => false,
          selectCard: -1,
          takara: links[0][2],
          content: lib.skill.xinfu_pingcai.contentx
        };
      }
    },
    async contentx(event, trigger, player2) {
      event.pingcai_delayed = true;
      const name = lib.skill.xinfu_pingcai_backup.takara;
      event.cardname = name;
      event.videoId = lib.status.videoId++;
      if (player2.isUnderControl()) {
        game.swapPlayerAuto(player2);
      }
      var switchToAuto = function() {
        game.pause();
        game.countChoose();
        event.timeout = setTimeout(function() {
          _status.imchoosing = false;
          event._result = {
            bool: true
          };
          game.resume();
        }, 9e3);
      };
      var createDialog = function(player3, id, name2) {
        if (player3 == game.me) {
          return;
        }
        var dialog = ui.create.dialog("forcebutton", "hidden");
        var str = get.translation(player3) + "正在擦拭宝物上的灰尘…";
        var canSkip = !_status.connectMode;
        if (canSkip) {
          str += "<br>（点击宝物可以跳过等待AI操作）";
        }
        dialog.textPrompt = dialog.add('<div class="text center">' + str + "</div>");
        dialog.classList.add("fixed");
        dialog.classList.add("scroll1");
        dialog.classList.add("scroll2");
        dialog.classList.add("fullwidth");
        dialog.classList.add("fullheight");
        dialog.classList.add("noupdate");
        dialog.videoId = id;
        var canvas2 = document.createElement("canvas");
        dialog.canvas_viewer = canvas2;
        dialog.appendChild(canvas2);
        canvas2.classList.add("grayscale");
        canvas2.style.position = "absolute";
        canvas2.style.width = "249px";
        canvas2.style.height = "249px";
        canvas2.style["border-radius"] = "6px";
        canvas2.style.left = "calc(50% - 125px)";
        canvas2.style.top = "calc(50% - 125px)";
        canvas2.width = 249;
        canvas2.height = 249;
        canvas2.style.border = "3px solid";
        var ctx2 = canvas2.getContext("2d");
        var img = new Image();
        img.src = lib.assetURL + "image/card/" + name2 + ".png";
        img.onload = function() {
          ctx2.drawImage(this, 0, 0, this.width, this.height, 0, 0, canvas2.width, canvas2.height);
        };
        if (canSkip) {
          var skip = function() {
            if (event.pingcai_delayed) {
              delete event.pingcai_delayed;
              clearTimeout(event.timeout);
              event._result = {
                bool: true
              };
              game.resume();
              canvas2.removeEventListener(lib.config.touchscreen ? "touchend" : "click", skip);
            }
          };
          canvas2.addEventListener(lib.config.touchscreen ? "touchend" : "click", skip);
        }
        dialog.open();
      };
      var chooseButton = function(id, name2) {
        var event2 = _status.event;
        _status.xinfu_pingcai_finished = false;
        var dialog = ui.create.dialog("forcebutton", "hidden");
        dialog.textPrompt = dialog.add('<div class="text center">擦拭掉宝物上的灰尘吧！</div>');
        event2.switchToAuto = function() {
          event2._result = {
            bool: _status.xinfu_pingcai_finished
          };
          game.resume();
          _status.imchoosing = false;
          _status.xinfu_pingcai_finished = true;
        };
        dialog.classList.add("fixed");
        dialog.classList.add("scroll1");
        dialog.classList.add("scroll2");
        dialog.classList.add("fullwidth");
        dialog.classList.add("fullheight");
        dialog.classList.add("noupdate");
        dialog.videoId = id;
        var canvas = document.createElement("canvas");
        var canvas2 = document.createElement("canvas");
        dialog.appendChild(canvas2);
        dialog.appendChild(canvas);
        canvas.style.position = "absolute";
        canvas.style.width = "249px";
        canvas.style.height = "249px";
        canvas.style["border-radius"] = "6px";
        canvas.style.left = "calc(50% - 125px)";
        canvas.style.top = "calc(50% - 125px)";
        canvas.width = 249;
        canvas.height = 249;
        canvas.style.border = "3px solid";
        canvas2.style.position = "absolute";
        canvas2.style.width = "249px";
        canvas2.style.height = "249px";
        canvas2.style["border-radius"] = "6px";
        canvas2.style.left = "calc(50% - 125px)";
        canvas2.style.top = "calc(50% - 125px)";
        canvas2.width = 249;
        canvas2.height = 249;
        canvas2.style.border = "3px solid";
        var ctx = canvas.getContext("2d");
        var ctx2 = canvas2.getContext("2d");
        var img = new Image();
        img.src = lib.assetURL + "image/card/" + name2 + ".png";
        img.onload = function() {
          ctx2.drawImage(this, 0, 0, this.width, this.height, 0, 0, canvas2.width, canvas2.height);
        };
        ctx.fillStyle = "lightgray";
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        canvas.onmousedown = function(ev) {
          canvas.onmousemove = function(e) {
            if (_status.xinfu_pingcai_finished) {
              return;
            }
            ctx.beginPath();
            ctx.clearRect(e.offsetX - 16, e.offsetY - 16, 32, 32);
            var data = ctx.getImageData(canvas.width * 0.1, canvas.height * 0.1, canvas.width * 0.8, canvas.height * 0.8).data;
            var sum = 0;
            for (var i = 3; i < data.length; i += 4) {
              if (data[i] == 0) {
                sum++;
              }
            }
            if (sum >= canvas.width * canvas.height * 0.6) {
              if (!_status.xinfu_pingcai_finished) {
                _status.xinfu_pingcai_finished = true;
                event2.switchToAuto();
              }
            }
          };
        };
        canvas.ontouchstart = function(ev) {
          canvas.ontouchmove = function(e) {
            if (_status.xinfu_pingcai_finished) {
              return;
            }
            ctx.beginPath();
            var rect = canvas.getBoundingClientRect();
            var X = (e.touches[0].clientX - rect.left) / rect.width * canvas.width;
            var Y = (e.touches[0].clientY - rect.top) / rect.height * canvas.height;
            ctx.clearRect(X - 16, Y - 16, 32, 32);
            var data = ctx.getImageData(canvas.width * 0.1, canvas.height * 0.1, canvas.width * 0.8, canvas.height * 0.8).data;
            var sum = 0;
            for (var i = 3; i < data.length; i += 4) {
              if (data[i] == 0) {
                sum++;
              }
            }
            if (sum >= canvas.width * canvas.height * 0.6) {
              if (!_status.xinfu_pingcai_finished) {
                _status.xinfu_pingcai_finished = true;
                event2.switchToAuto();
              }
            }
          };
        };
        canvas.onmouseup = function(ev) {
          canvas.onmousemove = null;
        };
        canvas.ontouchend = function(ev) {
          canvas.ontouchmove = null;
        };
        dialog.open();
        game.pause();
        game.countChoose();
      };
      game.broadcastAll(createDialog, player2, event.videoId, name);
      if (event.isMine()) {
        chooseButton(event.videoId, name);
      } else if (event.isOnline()) {
        event.player.send(chooseButton, event.videoId, name);
        event.player.wait();
        game.pause();
      } else {
        switchToAuto();
      }
      await game.pause();
      let result = event.result || event._result;
      if (!result) {
        result = { bool: false };
      }
      event._result = result;
      game.broadcastAll(
        function(id, result2, player3) {
          _status.xinfu_pingcai_finished = true;
          var dialog = get.idDialog(id);
          if (dialog) {
            dialog.textPrompt.innerHTML = '<div class="text center">' + (get.translation(player3) + "擦拭宝物" + (result2.bool ? "成功！" : "失败…")) + "</div>";
            if (result2.bool && dialog.canvas_viewer) {
              dialog.canvas_viewer.classList.remove("grayscale");
            }
          }
          if (!_status.connectMode) {
            delete event.pingcai_delayed;
          }
        },
        event.videoId,
        result,
        player2
      );
      await game.delay(2.5);
      game.broadcastAll("closeDialog", event.videoId);
      if (result.bool) {
        player2.logSkill("pcaudio_" + event.cardname);
        event.insert(lib.skill.xinfu_pingcai[event.cardname], {
          player: player2
        });
      }
    },
    ai: {
      order: 7,
      fireAttack: true,
      threaten: 1.7,
      result: {
        player: 1
      }
    }
  },
  xinfu_pdgyingshi: {
    mod: {
      targetEnabled(card2, player2, target) {
        if (get.type(card2) == "delay") {
          return false;
        }
      }
    },
    trigger: {
      player: ["phaseZhunbeiBefore", "phaseJieshuBefore"]
    },
    forced: true,
    group: "xinfu_pdgyingshi2",
    async content(event, trigger, player2) {
      trigger.cancel();
      game.log(player2, "跳过了", event.triggername == "phaseZhunbeiBefore" ? "准备阶段" : "结束阶段");
    }
  },
  xinfu_pdgyingshi2: {
    popup: false,
    trigger: {
      player: "phaseJudgeBefore"
    },
    forced: true,
    sourceSkill: "xinfu_pdgyingshi",
    async content(event, trigger, player2) {
      trigger.cancel();
      game.log(player2, "跳过了判定阶段");
    }
  },
  pcaudio_wolong_card: {
    audio: "xinfu_pingcai2.mp3"
  },
  pcaudio_fengchu_card: {
    audio: "xinfu_pingcai3.mp3"
  },
  pcaudio_shuijing_card: {
    audio: "xinfu_pingcai4.mp3"
  },
  pcaudio_xuanjian_card: {
    audio: "xinfu_pingcai5.mp3"
  },
  yizan_use: {
    audio: "yizan_respond_shan",
    intro: {
      content: "已发动过#次"
    },
    enable: ["chooseToUse", "chooseToRespond"],
    hiddenCard(player2, name) {
      if (get.type(name) != "basic") {
        return false;
      }
      if (!player2.storage.yizan && player2.countCards("hes") < 2) {
        return false;
      }
      return player2.hasCard(function(card2) {
        return get.type(card2) == "basic";
      }, "hs");
    },
    filter(event, player2) {
      if (!player2.storage.yizan && player2.countCards("hes") < 2) {
        return false;
      }
      if (!player2.hasCard(function(card2) {
        return get.type(card2) == "basic";
      }, "hs")) {
        return false;
      }
      for (var name of lib.inpile) {
        if (get.type(name) != "basic") {
          continue;
        }
        if (event.filterCard(get.autoViewAs({ name }, "unsure"), player2, event)) {
          return true;
        }
        if (name == "sha") {
          for (var nature of lib.inpile_nature) {
            if (event.filterCard(get.autoViewAs({ name, nature }, "unsure"), player2, event)) {
              return true;
            }
          }
        }
      }
      return false;
    },
    chooseButton: {
      dialog(event, player2) {
        var list = [];
        for (var name of lib.inpile) {
          if (get.type(name) != "basic") {
            continue;
          }
          if (event.filterCard(get.autoViewAs({ name }, "unsure"), player2, event)) {
            list.push(["基本", "", name]);
          }
          if (name == "sha") {
            for (var nature of lib.inpile_nature) {
              if (event.filterCard(get.autoViewAs({ name, nature }, "unsure"), player2, event)) {
                list.push(["基本", "", "sha", nature]);
              }
            }
          }
        }
        return ui.create.dialog("翊赞", [list, "vcard"], "hidden");
      },
      check(button) {
        var player2 = _status.event.player;
        var card2 = { name: button.link[2], nature: button.link[3] };
        if (_status.event.getParent().type != "phase" || game.hasPlayer(function(current) {
          return player2.canUse(card2, current) && get.effect(current, card2, player2, player2) > 0;
        })) {
          switch (button.link[2]) {
            case "tao":
            case "shan":
              return 5;
            case "jiu": {
              if (player2.storage.yizan && player2.countCards("hs", { type: "basic" }) > 2) {
                return 3;
              }
              return 0;
            }
            case "sha":
              if (button.link[3] == "fire") {
                return 2.95;
              } else if (button.link[3] == "thunder" || button.link[3] == "ice") {
                return 2.92;
              } else {
                return 2.9;
              }
          }
        }
        return 0;
      },
      backup(links, player2) {
        return {
          audio: "yizan_respond_shan",
          filterCard(card2, player3, target) {
            if (player3.storage.yizan) {
              return get.type(card2) == "basic";
            } else if (ui.selected.cards.length) {
              if (get.type(ui.selected.cards[0]) == "basic") {
                return true;
              }
              return get.type(card2) == "basic";
            }
            return true;
          },
          complexCard: true,
          selectCard() {
            var player3 = _status.event.player;
            if (player3.storage.yizan) {
              return 1;
            }
            return 2;
          },
          check(card2, player3, target) {
            if (!ui.selected.cards.length && get.type(card2) == "basic") {
              return 6;
            } else {
              return 6 - get.value(card2);
            }
          },
          viewAs: { name: links[0][2], nature: links[0][3] },
          position: "hes",
          popname: true,
          async precontent(event, trigger, player3) {
            player3.addMark("yizan_use", 1, false);
          }
        };
      },
      prompt(links, player2) {
        var str = player2.storage.yizan ? "一张基本牌" : "两张牌(其中至少应有一张基本牌)";
        return "将" + str + "当做" + get.translation(links[0][3] || "") + get.translation(links[0][2]) + "使用或打出";
      }
    },
    ai: {
      order() {
        var player2 = _status.event.player;
        var event = _status.event;
        if (event.filterCard({ name: "jiu" }, player2, event) && get.effect(player2, { name: "jiu" }) > 0 && player2.storage.yizan && player2.countCards("hs", { type: "basic" }) > 2) {
          return 3.3;
        }
        return 3.1;
      },
      skillTagFilter(player2, tag, arg) {
        if (tag == "fireAttack") {
          return true;
        }
        if (!player2.storage.yizan && player2.countCards("hes") < 2) {
          return false;
        }
        if (!player2.hasCard(function(card2) {
          return get.type(card2) == "basic";
        }, "hes")) {
          return false;
        }
      },
      result: {
        player: 1
      },
      respondSha: true,
      respondShan: true,
      fireAttack: true
    },
    subSkill: {
      backup: {}
    }
  },
  yizan_respond_shan: {
    audio: 2
  },
  xinfu_longyuan: {
    audio: 2,
    forced: true,
    juexingji: true,
    trigger: { player: "phaseZhunbeiBegin" },
    skillAnimation: true,
    animationColor: "orange",
    filter(event, player2) {
      return player2.countMark("yizan_use") >= 3;
    },
    async content(event, trigger, player2) {
      player2.awakenSkill(event.name);
      player2.storage.yizan = true;
    },
    derivation: "yizan_rewrite",
    ai: { combo: "yizan_use" }
  },
  xinfu_jingxie: {
    audio: 2,
    video(player2, info) {
      var l2 = player2.getCards(info[0] ? "e" : "h"), l1 = info[1];
      for (var j = 0; j < l2.length; j++) {
        if (l2[j].suit == l1[0] && l2[j].number == l1[1] && l2[j].name == l1[2]) {
          l2[j].init([l2[j].suit, l2[j].number, "rewrite_" + l2[j].name]);
          break;
        }
      }
    },
    position: "he",
    enable: "phaseUse",
    filter(event, player2) {
      var he = player2.getCards("he");
      for (var i = 0; i < he.length; i++) {
        if (["bagua", "baiyin", "lanyinjia", "renwang", "tengjia", "zhuge"].includes(he[i].name)) {
          return true;
        }
      }
      return false;
    },
    filterCard(card2) {
      return ["bagua", "baiyin", "lanyinjia", "renwang", "tengjia", "zhuge"].includes(card2.name);
    },
    discard: false,
    lose: false,
    delay: false,
    check() {
      return 1;
    },
    async content(event, trigger, player2) {
      const { cards: cards2 } = event;
      await player2.showCards(cards2);
      const card2 = cards2[0];
      const bool = get.position(card2) == "e";
      if (bool) {
        player2.removeEquipTrigger(card2.card || card2);
      }
      game.addVideo("skill", player2, ["xinfu_jingxie", [bool, get.cardInfo(card2)]]);
      game.broadcastAll(
        function(card3, bool2, player3) {
          card3.init([card3.suit, card3.number, "rewrite_" + card3.name]);
          let vcard = card3[card3.cardSymbol];
          if (bool2 && vcard && player3.vcardsMap?.equips) {
            const cardx = get.autoViewAs(card3, void 0, false);
            player3.vcardsMap.equips[player3.vcardsMap.equips.indexOf(vcard)] = cardx;
            card3[card3.cardSymbol] = cardx;
          }
        },
        card2,
        bool,
        player2
      );
      if (bool) {
        player2.addEquipTrigger(card2.card || card2);
      }
    },
    ai: {
      basic: {
        order: 10
      },
      result: {
        player: 1
      }
    },
    group: ["xinfu_jingxie_recast"],
    subSkill: {
      recast: {
        audio: "xinfu_jingxie",
        enable: "chooseToUse",
        filterCard: (card2, player2) => get.subtype(card2) == "equip2" && player2.canRecast(card2),
        filter: (event, player2) => {
          if (event.type != "dying") {
            return false;
          }
          if (player2 != event.dying) {
            return false;
          }
          return player2.hasCard((card2) => lib.skill.xinfu_jingxie.subSkill.recast.filterCard(card2, player2), "he");
        },
        position: "he",
        discard: false,
        lose: false,
        delay: false,
        prompt: "重铸一张防具牌，然后将体力回复至1点。",
        async content(event, trigger, player2) {
          const { cards: cards2 } = event;
          await player2.recast(cards2);
          const num = 1 - player2.hp;
          if (num) {
            await player2.recover(num);
          }
        },
        ai: {
          order: 0.5,
          skillTagFilter(player2, arg, target) {
            if (player2 != target) {
              return false;
            }
            return player2.hasCard((card2) => _status.connectMode && get.position(card2) == "h" || get.subtype(card2) == "equip2" && player2.canRecast(card2), "he");
          },
          save: true,
          result: {
            player(player2) {
              return 10;
            }
          }
        }
      }
    }
  },
  zhaohuo: {
    audio: 2,
    audioname: ["re_taoqian"],
    trigger: { global: "dying" },
    forced: true,
    //priority:12,
    filter(event, player2) {
      return event.player != player2 && player2.maxHp > 1;
    },
    async content(event, trigger, player2) {
      const num = player2.maxHp - 1;
      await player2.loseMaxHp(num, true);
      await player2.draw(num);
    },
    ai: {
      neg: true
    }
  },
  yixiang: {
    audio: 2,
    audioname: ["re_taoqian"],
    trigger: { target: "useCardToTargeted" },
    frequent: true,
    filter(event, player2) {
      if (event.player.hp <= player2.hp) {
        return false;
      }
      var hs = player2.getCards("h");
      var names = ["sha", "shan", "tao", "jiu", "du"];
      for (var i = 0; i < hs.length; i++) {
        names.remove(hs[i].name);
      }
      if (!names.length) {
        return false;
      }
      for (var i = 0; i < ui.cardPile.childElementCount; i++) {
        if (names.includes(ui.cardPile.childNodes[i].name)) {
          return true;
        }
      }
      return false;
    },
    usable: 1,
    async content(event, trigger, player2) {
      var hs = player2.getCards("h");
      var list = [];
      var names = ["sha", "shan", "tao", "jiu", "du"];
      for (var i = 0; i < hs.length; i++) {
        names.remove(hs[i].name);
      }
      for (var i = 0; i < ui.cardPile.childElementCount; i++) {
        if (names.includes(ui.cardPile.childNodes[i].name)) {
          list.push(ui.cardPile.childNodes[i]);
        }
      }
      if (list.length) {
        player2.gain(list.randomGet(), "draw");
      }
    }
  },
  yirang: {
    audio: 2,
    audioname: ["re_taoqian"],
    trigger: { player: "phaseUseBegin" },
    direct: true,
    filter(event, player2) {
      if (!player2.countCards("he", function(card2) {
        return get.type(card2) != "basic";
      })) {
        return false;
      }
      return game.hasPlayer(function(current) {
        return current.maxHp > player2.maxHp;
      });
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt2("yirang"), function(card2, player3, target) {
        return target.maxHp > player3.maxHp;
      }).set("ai", function(target) {
        return (get.attitude(_status.event.player, target) - 2) * target.maxHp;
      }).forResult();
      if (result.bool) {
        const cards2 = player2.getCards("he", function(card2) {
          return get.type(card2) != "basic";
        });
        const target = result.targets[0];
        const types = [];
        for (var i = 0; i < cards2.length; i++) {
          types.add(get.type(cards2[i], "trick"));
        }
        player2.logSkill("yirang", target);
        await player2.give(cards2, target);
        await player2.gainMaxHp(target.maxHp - player2.maxHp, true);
        await player2.recover(types.length);
        await game.delay();
      }
    }
  },
  kuangcai: {
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return !event.player.isMad();
    },
    async content(event, trigger, player2) {
      game.broadcastAll(function(player3) {
        if (!player3.forceCountChoose) {
          player3.forceCountChoose = {};
        }
        player3.forceCountChoose.phaseUse = 5;
      }, player2);
      player2.addSkill("kuangcai_use");
      player2.addSkill("kuangcai_cancel");
    },
    subSkill: {
      use: {
        mod: {
          cardUsable(card2) {
            if (get.info(card2) && get.info(card2).forceUsable) {
              return;
            }
            return Infinity;
          },
          targetInRange() {
            return true;
          },
          aiOrder(player2, card2, num) {
            var name = get.name(card2);
            if (name == "tao") {
              return num + 7 + Math.pow(player2.getDamagedHp(), 2);
            }
            if (name == "sha") {
              return num + 6;
            }
            if (get.subtype(card2) == "equip2") {
              return num + get.value(card2) / 3;
            }
          }
        },
        trigger: { player: "useCard" },
        forced: true,
        charlotte: true,
        silent: true,
        popup: false,
        filter(event, player2) {
          if (!player2.forceCountChoose || !player2.forceCountChoose.phaseUse) {
            return false;
          }
          return true;
        },
        async content(event, trigger, player2) {
          player2.draw();
          if (player2.forceCountChoose.phaseUse == 1) {
            var evt = event.getParent("phaseUse");
            if (evt) {
              evt.skipped = true;
            }
          } else {
            game.broadcastAll(function(player3) {
              player3.forceCountChoose.phaseUse--;
            }, player2);
          }
        }
      },
      cancel: {
        trigger: { player: "phaseUseEnd" },
        firstDo: true,
        silent: true,
        charlotte: true,
        async content(event, trigger, player2) {
          game.broadcastAll(function(player3) {
            delete player3.forceCountChoose;
          }, player2);
          player2.removeSkill("kuangcai_use");
          player2.removeSkill("kuangcai_cancel");
        }
      }
    },
    ai: {
      threaten: 4.5
    }
  },
  shejian: {
    audio: 2,
    trigger: { player: "phaseDiscardEnd" },
    direct: true,
    filter(event, player2) {
      var cards2 = [];
      player2.getHistory("lose", function(evt) {
        if (evt.type == "discard" && evt.getParent("phaseDiscard") == event) {
          cards2.addArray(evt.cards2);
        }
      });
      if (cards2) {
        if (cards2.length < 2) {
          return false;
        }
        var suits = [];
        for (var i = 0; i < cards2.length; i++) {
          var suit = get.suit(cards2[i]);
          if (suits.includes(suit)) {
            return false;
          } else {
            suits.push(suit);
          }
        }
        return true;
      }
      return false;
    },
    async content(event, trigger, player2) {
      const result = await player2.chooseTarget(get.prompt("shejian"), "弃置一名其他角色的一张牌", function(card2, player3, target) {
        if (player3 == target) {
          return false;
        }
        return target.countDiscardableCards(player3, "he") > 0;
      }).set("ai", function(target) {
        return -get.attitude(player2, target);
      }).forResult();
      if (result.bool) {
        player2.logSkill("shejian", result.targets);
        await player2.discardPlayerCard(result.targets[0], "he", true);
      }
    }
  },
  shixin: {
    audio: 2,
    trigger: { player: "damageBegin4" },
    filter(event) {
      return event.hasNature("fire");
    },
    forced: true,
    async content(event, trigger, player2) {
      trigger.cancel();
    },
    ai: {
      nofire: true,
      effect: {
        target(card2, player2, target, current) {
          if (get.tag(card2, "fireDamage")) {
            return "zeroplayertarget";
          }
        }
      }
    }
  },
  fenyin: {
    locked: false,
    mod: {
      aiOrder(player2, card2, num) {
        if (typeof card2 == "object" && player2 == _status.currentPhase) {
          var evt = player2.getLastUsed();
          if (evt && evt.card && get.color(evt.card) != "none" && get.color(card2) != "none" && get.color(evt.card) != get.color(card2)) {
            return num + 10;
          }
        }
      }
    },
    audio: 2,
    trigger: { player: "useCard" },
    frequent: true,
    //usable:3,
    filter(event, player2) {
      if (_status.currentPhase != player2) {
        return false;
      }
      var color2 = get.color(event.card);
      player2.addTip("fenyin", "奋音 " + get.translation(color2), true);
      var evt = player2.getLastUsed(1);
      if (!evt) {
        return false;
      }
      var color1 = get.color(evt.card);
      return color1 && color2 && color1 != "none" && color2 != "none" && color1 != color2;
    },
    async content(event, trigger, player2) {
      player2.draw("nodelay");
    },
    ai: {
      threaten: 3
    }
  },
  dujin: {
    audio: 2,
    trigger: { player: "phaseDrawBegin2" },
    frequent: true,
    preHidden: true,
    filter(event, player2) {
      return !event.numFixed;
    },
    async content(event, trigger, player2) {
      trigger.num += 1 + Math.ceil(player2.countCards("e") / 2);
    }
  },
  yingjian: {
    trigger: { player: "phaseZhunbeiBegin" },
    direct: true,
    audio: "qingyi",
    async content(event, trigger, player2) {
      player2.chooseUseTarget("###是否发动【影箭】？###视为使用一张没有距离限制的【杀】", { name: "sha" }, false, "nodistance").logSkill = "yingjian";
    },
    ai: {
      threaten(player2, target) {
        return 1.6;
      }
    }
  },
  tunchu: {
    audio: 2,
    trigger: { player: "phaseDrawBegin2" },
    frequent: true,
    preHidden: true,
    locked: false,
    filter(event, player2) {
      if (event.numFixed || player2.getExpansions("tunchu").length) {
        return false;
      }
      return true;
    },
    async content(event, trigger, player2) {
      trigger.num += 2;
      player2.addTempSkill("tunchu_choose", "phaseDrawAfter");
    },
    onremove(player2, skill) {
      var cards2 = player2.getExpansions(skill);
      if (cards2.length) {
        player2.loseToDiscardpile(cards2);
      }
    },
    intro: {
      content: "expansion",
      markcount: "expansion"
    },
    mod: {
      cardEnabled(card2, player2) {
        if (player2.getExpansions("tunchu").length && card2.name == "sha") {
          return false;
        }
      }
    },
    subSkill: {
      choose: {
        trigger: { player: "phaseDrawEnd" },
        forced: true,
        popup: false,
        charlotte: true,
        async content(event, trigger, player2) {
          player2.removeSkill("tunchu_choose");
          const nh = player2.countCards("h");
          if (nh) {
            const result = await player2.chooseCard("h", [1, nh], "将任意张手牌置于你的武将牌上", "allowChooseAll").set("ai", function(card2) {
              var player3 = _status.event.player;
              var count = game.countPlayer(function(current) {
                return get.attitude(player3, current) > 2 && current.hp - current.countCards("h") > 1;
              });
              if (ui.selected.cards.length >= count) {
                return -get.value(card2);
              }
              return 5 - get.value(card2);
            }).forResult();
            if (result.bool) {
              const next = player2.addToExpansion(result.cards, player2, "giveAuto");
              next.gaintag.add("tunchu");
              await next;
            }
          }
        }
      }
    }
  },
  shuliang: {
    audio: 2,
    trigger: { global: "phaseJieshuBegin" },
    direct: true,
    filter(event, player2) {
      return player2.getExpansions("tunchu").length > 0 && event.player.countCards("h") < event.player.hp && event.player.isIn();
    },
    async content(event, trigger, player2) {
      const goon = get.attitude(player2, trigger.player) > 0;
      const result = await player2.chooseCardButton(get.prompt("shuliang", trigger.player), player2.getExpansions("tunchu")).set("ai", function() {
        if (_status.event.goon) {
          return 1;
        }
        return 0;
      }).set("goon", goon).forResult();
      if (result.bool) {
        player2.logSkill("shuliang", trigger.player);
        await player2.loseToDiscardpile(result.links);
        await trigger.player.draw(2);
      }
    },
    ai: { combo: "tunchu" }
  },
  choulve: {
    init(player2, skill) {
      player2.addSkill(skill + "_mark");
    },
    onremove(player2, skill) {
      player2.removeSkill(skill + "_mark");
    },
    getCard(player2) {
      let card2;
      const history = player2.getAllHistory("damage", (evt) => {
        return evt.card && get.type(evt.card) != "delay";
      });
      if (history.length) {
        const evt = history.at(-1);
        card2 = {
          name: evt.card.name,
          nature: evt.card.nature,
          isCard: true
        };
      }
      return card2;
    },
    audio: 2,
    trigger: { player: "phaseUseBegin" },
    filter(event, player2) {
      return game.hasPlayer((current) => {
        return current != player2 && current.hasCards("he");
      });
    },
    async cost(event, trigger, player2) {
      let str = "令一名其他角色交给你一张牌";
      const card2 = get.info(event.skill).getCard(player2);
      if (card2) {
        str += `，若其如此做，视为你使用${get.translation(card2)}`;
      }
      let goon = true;
      if (card2) {
        goon = player2.hasValueTarget(card2);
      }
      event.result = await player2.chooseTarget(get.prompt(event.skill), str, (card3, player3, target) => {
        return target != player3 && target.hasCards("he");
      }).set("ai", (target) => {
        const { goon: goon2, player: player3 } = get.event();
        if (!goon2) {
          return 0;
        }
        if (!game.hasPlayer((current) => current != player3 && current.hasCards("he") && get.attitude(player3, current) >= 0 && get.attitude(current, player3) >= 0)) {
          return get.threaten(target, player3);
        }
        if (get.attitude(player3, target) >= 0 && get.attitude(target, player3) >= 0) {
          return Math.sqrt(target.countCards("he"));
        }
        return 0;
      }).set("goon", goon).forResult();
    },
    async content(event, trigger, player2) {
      const target = event.targets[0];
      const card2 = get.info(event.name).getCard(player2);
      event.choulveCard = card2;
      const result = await target.chooseToGive("he", player2, `${get.translation(player2)}对你发动了【筹略】`, `是否交给其一张牌${card2 ? `，然后其视为使用${get.translation(card2)}` : ""}`).set("ai", (card3) => {
        const { player: player3, target: target2 } = get.event();
        const att = get.attitude(player3, target2);
        if (att > 0) {
          return 7 - get.value(card3);
        }
        return 0;
      }).forResult();
      target.addExpose(0.1);
      if (result?.bool) {
        if (card2) {
          await player2.chooseUseTarget(card2, true, false);
        }
      }
    },
    subSkill: {
      mark: {
        init(player2, skill) {
          const card2 = get.info("choulve").getCard(player2);
          if (card2) {
            player2.storage[skill] = card2;
            player2.markSkill(skill);
            player2.addTip(skill, `${get.translation(skill)} ${get.translation(card2)}`);
          }
        },
        onremove(player2, skill) {
          delete player2.storage[skill];
          player2.removeTip(skill);
        },
        trigger: { player: "damageEnd" },
        filter(event, player2) {
          return event.card && get.type(event.card) != "delay";
        },
        firstDo: true,
        forced: true,
        popup: false,
        async content(event, trigger, player2) {
          const card2 = {
            name: trigger.card.name,
            nature: trigger.card.nature,
            isCard: true
          };
          player2.storage[event.name] = card2;
          player2.markSkill(event.name);
          player2.addTip(event.name, `${get.translation(event.name)} ${get.translation(card2)}`);
        },
        intro: { content: "下次【筹略】可以视为使用$" }
      }
    }
  },
  polu: {
    audio: 2,
    trigger: { player: "phaseZhunbeiBegin" },
    forced: true,
    filter(event, player2) {
      if (!lib.inpile.includes("ly_piliche")) {
        return true;
      }
      return get.cardPile((card2) => card2.name == "ly_piliche");
    },
    async content(event, trigger, player2) {
      let card2;
      if (!lib.inpile.includes("ly_piliche")) {
        card2 = game.createCard2("ly_piliche", "diamond", 9);
        lib.inpile.push("ly_piliche");
      } else {
        card2 = get.cardPile((card3) => card3.name == "ly_piliche");
      }
      if (card2) {
        await player2.gain(card2, "gain2");
        if (player2.hasUseTarget(card2) && player2.getCards("h").includes(card2) && get.name(card2, player2) == "ly_piliche") {
          await player2.chooseUseTarget(card2, true, "nopopup");
        }
      }
    },
    group: "polu_damage",
    subSkill: {
      damage: {
        audio: "polu",
        trigger: { player: "damageEnd" },
        forced: true,
        filter(event, player2) {
          return !player2.getEquips("ly_piliche").length && event.num > 0;
        },
        getIndex: (event) => event.num,
        async content(event, trigger, player2) {
          await player2.draw();
          const card2 = get.cardPile2((card3) => get.subtypes(card3).includes("equip1") && player2.hasUseTarget(card3));
          if (card2) {
            await player2.gain(card2, "gain2");
            if (player2.hasUseTarget(card2) && player2.getCards("h").includes(card2) && get.name(card2, player2) == card2.name) {
              await player2.chooseUseTarget(card2, true, "nopopup");
            }
          }
        }
      }
    }
  },
  ly_piliche: {
    equipSkill: true,
    trigger: { source: "damageSource" },
    check(event, player2) {
      return get.attitude(player2, event.player) * get.value(event.player.getDiscardableCards(player2, "e"), event.player) <= 0;
    },
    filter(event, player2) {
      return player2 != event.player && event.player.hasDiscardableCards(player2, "he") && event.player.isIn();
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      await player2.discardPlayerCard(trigger.player, "e", true, trigger.player.countCards("e"));
    }
  },
  mbjieyuan: {
    audio: ["jieyuan_more.mp3", "jieyuan_less.mp3"],
    logAudio: (event, player2, name) => name == "damageBegin1" ? "jieyuan_more.mp3" : "jieyuan_less.mp3",
    trigger: {
      source: "damageBegin1",
      player: "damageBegin4"
    },
    onremove(player2, skill) {
      player2.removeTip(skill);
      delete player2.storage[skill + "_beishui"];
      delete player2.storage[skill + "_removed"];
    },
    filter(event, player2, name) {
      const beishui = player2.getStorage("mbjieyuan_beishui", false);
      const removed = player2.getStorage("mbjieyuan_removed", "");
      if (beishui) {
        if (name === "damageBegin1" && removed === "damageSource" || name === "damageBegin4" && removed === "damage") {
          return false;
        }
      }
      if (name === "damageBegin1") {
        return event.num > 0;
      }
      if (name === "damageBegin4") {
        return event.num > 0;
      }
      return false;
    },
    async cost(event, trigger, player2) {
      const name = event.triggername;
      const beishui = player2.getStorage("mbjieyuan_beishui", false);
      const removed = player2.getStorage("mbjieyuan_removed", "");
      const isSource = name === "damageBegin1";
      const color = isSource ? "black" : "red";
      const colorText = isSource ? "黑" : "红";
      const discardNum = 1;
      const effectNum = beishui ? 2 : 1;
      const canDiscard = player2.countDiscardableCards(player2, "he", (card2) => get.color(card2) === color) >= discardNum;
      const target = isSource ? trigger.player : trigger.source;
      const promptText = isSource ? get.prompt(event.skill, target) : get.prompt(event.skill);
      const choices = [];
      const choiceList = [];
      if (canDiscard) {
        choices.push("弃" + colorText + "牌伤害" + (isSource ? "+" : "-") + effectNum);
        choiceList.push("弃置一张" + get.translation(color) + "牌，令此伤害" + (isSource ? "+" : "-") + effectNum);
      }
      choices.push("获得" + colorText + "牌");
      choiceList.push("从牌堆中获得" + get.cnNumber(effectNum) + "张" + get.translation(color) + "牌");
      if (!beishui && !removed) {
        choices.push("背水");
        const beishuiDesc = isSource ? "删除受到伤害时的效果" : "删除造成伤害时的效果";
        choiceList.push("背水！" + beishuiDesc + "，升级技能");
      }
      choices.push("cancel2");
      const result = await player2.chooseControl({
        controls: choices,
        choiceList,
        prompt: promptText,
        ai() {
          const { controls } = get.event();
          if (controls.includes("背水")) {
            return "背水";
          }
          return controls.slice().randomGet();
        }
      }).forResult();
      if (typeof result?.control == "string" && result.control != "cancel2") {
        event.result = {
          bool: true,
          cost_data: result.control
        };
      }
    },
    async content(event, trigger, player2) {
      const control = event.cost_data;
      const name = event.triggername;
      const beishui = player2.getStorage("mbjieyuan_beishui", false);
      const isSource = name === "damageBegin1";
      const color = isSource ? "black" : "red";
      const colorText = isSource ? "黑" : "红";
      const discardNum = 1;
      const effectNum = beishui ? 2 : 1;
      const isOption1 = control.startsWith("弃" + colorText + "牌");
      const isOption2 = control.startsWith("获得" + colorText + "牌");
      const isBeishui = control === "背水";
      if (isOption2 || isBeishui) {
        const pileCards = Array.from(ui.cardPile.childNodes);
        const colorCards = pileCards.filter((card2) => get.color(card2) === color);
        const toGain = colorCards.slice(0, effectNum);
        if (toGain.length > 0) {
          await player2.gain({ cards: toGain, animate: "draw" });
        }
      }
      const canDiscard = player2.countDiscardableCards(player2, "he", (card2) => get.color(card2) === color) >= discardNum;
      if (isOption1 || isBeishui) {
        if (canDiscard) {
          const result = await player2.chooseToDiscard({
            position: "he",
            prompt: "竭缘：请选择一张" + get.translation(color) + "牌弃置",
            filterCard(card2) {
              return get.color(card2) === get.event().color;
            },
            ai(card2) {
              return 10 - get.value(card2);
            }
          }).set("color", color).forResult();
          if (result?.bool && result.cards?.length > 0) {
            if (isSource) {
              trigger.num += effectNum;
              game.log(player2, "令此次伤害+" + effectNum);
            } else {
              trigger.num -= effectNum;
              game.log(player2, "令此次伤害-" + effectNum);
            }
          }
        }
      }
      if (isBeishui) {
        player2.setStorage("mbjieyuan_beishui", true);
        const otherTrigger = isSource ? "damage" : "damageSource";
        player2.setStorage("mbjieyuan_removed", otherTrigger);
        game.log(player2, "发动了【竭缘】背水，删除了" + (isSource ? "受到伤害" : "造成伤害") + "时的效果");
        player2.addTip(event.name, isSource ? "竭缘加伤" : "竭缘减伤");
      }
    },
    ai: {
      threaten: 1.5
    }
  },
  mbfenxin: {
    //mode: ["identity", "doudizhu"],
    audio: "fenxin",
    skillAnimation: true,
    animationColor: "wood",
    trigger: {
      source: "dieBegin"
    },
    filter(event, player2) {
      if (!["identity", "doudizhu"].includes(get.mode())) return true;
      const validIdentities = ["zhong", "fan", "nei", "zhu", "min"];
      return !event.reverseOut && validIdentities.includes(event.player.identity) && validIdentities.includes(player2.identity);
    },
    async cost(event, trigger, player2) {
      const target = trigger.player;
      const validSkills = target.getStockSkills(true, true).filter((skill) => {
        const info = get.info(skill);
        return info && !info.charlotte && !info.limited && !info.juexingji && !info.zhuSkill && !info.dutySkill && !info.persevereSkill;
      });
      const isIdentityMode = get.mode() === "identity";
      const canSwap = isIdentityMode && !player2.identityShown && !target.identityShown;
      const choices = [];
      const choiceList = [];
      if (validSkills.length) {
        choices.push("获得技能");
        choiceList.push("获得" + get.translation(target) + "的所有技能（限定技、觉醒技、使命技、主公技、持恒技除外）");
      }
      if (canSwap) {
        choices.push("交换身份牌");
        choiceList.push("与其交换身份牌");
      }
      if (!choices.length) return;
      choices.push("cancel2");
      const result = await player2.chooseControl({
        controls: choices,
        choiceList,
        prompt: get.prompt(event.skill, target),
        choice: (() => {
          if (validSkills.length > 0) {
            return 0;
          }
          if (canSwap) {
            return 1;
          }
          return 2;
        })()
      }).forResult();
      event.result = {
        bool: result.control !== "cancel2",
        cost_data: {
          choice: result.control,
          skills: validSkills
        },
        targets: [target]
      };
    },
    logTarget: "player",
    async content(event, trigger, player2) {
      const { choice, skills: skills2 } = event.cost_data;
      const target = trigger.player;
      if (choice === "获得技能") {
        if (player2.identity !== target.identity || player2.side != target.side) {
          if (skills2.length > 0) {
            await player2.addSkills(skills2);
          }
        } else {
          game.log(player2, "与", target, "身份相同，无法获得技能");
        }
      } else if (choice === "交换身份牌") {
        game.broadcastAll(
          function(player3, target2, shown) {
            const identity = player3.identity;
            player3.identity = target2.identity;
            if (shown || player3 === game.me) {
              player3.setIdentity();
            }
            target2.identity = identity;
            if (player3.identity === "zhu") {
              game.zhu = player3;
            } else if (target2.identity === "zhu") {
              game.zhu = target2;
            }
          },
          player2,
          target,
          target.identityShown
        );
        player2.line(target, "green");
        game.log(player2, "与", target, "交换了身份牌");
      }
    }
  }
};
const translates = {
  mb_shen_lvbu: "手杀神吕布",
  mb_shen_lvbu_prefix: "手杀神",
  mbwumou: "无谋",
  mbwumou_info: "锁定技，你的普通锦囊牌只能当普通【杀】使用或打出，此【杀】的目标改为原锦囊牌的目标。",
  mbwuqian: "无前",
  mbwuqian_info: "出牌阶段，你可以弃置2枚“暴怒”，令一名角色防具牌失效且你获得" + get.poptip("wushuang") + "，直至你下次使用伤害牌但未造成伤害；你的出【杀】次数+X（X为当前被“无前”的角色数）；结束阶段，若你的手牌中没有伤害牌，你随机获得一张伤害牌。",
  mb_shen_caocao: "手杀神曹操",
  mb_shen_caocao_prefix: "手杀神",
  mb_cheliji: "手杀彻里吉",
  mb_cheliji_prefix: "手杀",
  mbqucheng: "驱乘",
  mbqucheng_info: "锁定技，你使用【杀】无距离限制，且不因此技能使用的【杀】只能指定上家/下家为目标；当你使用指定唯一目标的【杀】结算后，若此【杀】目标角色的上家/下家不是你且未造成伤害，此【杀】不计入次数，然后你视为对其的上家/下家使用一张普通【杀】。",
  mbqiangyong: "羌勇",
  mbqiangyong_info: "锁定技，你使用【杀】造成伤害时，弃置受伤角色的X张牌（X为本回合你使用【杀】的次数），然后若其没有手牌，此【杀】伤害+1。",
  mb_dingshangwan: "手杀丁尚涴",
  mb_dingshangwan_prefix: "手杀",
  mbzhaofu: "照抚",
  mbzhaofu_info: "每回合限一次，你可弃置所有手牌视为使用一张：1.【杀】；2.【闪】；3.【酒】；4.【桃】。若你因此恰好弃置了序号数张牌，你可令一名其他角色获得一个对应效果的【照抚】，且其发动后或其回合结束时失去对应效果的【照抚】。",
  mbzhaofu_sha: "照抚",
  mbzhaofu_sha_info: "每回合限一次，你可弃置所有手牌视为使用一张【杀】。发动后或你的回合结束时失去该技能。",
  mbzhaofu_shan: "照抚",
  mbzhaofu_shan_info: "每回合限一次，你可弃置所有手牌视为使用一张【闪】。发动后或你的回合结束时失去该技能。",
  mbzhaofu_jiu: "照抚",
  mbzhaofu_jiu_info: "每回合限一次，你可弃置所有手牌视为使用一张【酒】。发动后或你的回合结束时失去该技能。",
  mbzhaofu_tao: "照抚",
  mbzhaofu_tao_info: "每回合限一次，你可弃置所有手牌视为使用一张【桃】。发动后或你的回合结束时失去该技能。",
  mbqiliu: "泣留",
  mbqiliu_info: `锁定技，当你每轮首次因${get.poptip("mbzhaofu")}失去所有手牌后，你摸X张牌（X为你已执行过的回合数）；当你于回合外对其他角色使用【桃】后，防止其本回合受到的下一次伤害。若其因此防止了伤害，此技能本轮失效。`,
  re_wangji: "手杀界王基",
  re_wangji_prefix: "手杀界",
  reqizhi: "奇制",
  reqizhi_info: "蓄力技（1/3），出牌阶段，你可以消耗1点蓄力点令一名角色摸三张牌，然后本回合你使用基本牌或普通锦囊牌指定目标后，你依次观看本回合因此摸牌的角色的手牌并弃置其中一张。",
  rejinqu: "进趋",
  rejinqu_info: "锁定技，你每使用三张牌后，你令一名拥有蓄力技的角色获得1点蓄力点；结束阶段，你摸X张牌（X为本回合你蓄力点点数存在过的最高值）。",
  zhuji: "朱绩",
  potjiezhu: "竭逐",
  potjiezhu_info: "每回合限一次，你可将手牌弃置至最接近的唯一值，视为使用一张指定至多X名角色为目标，无距离限制的普通【杀】（X为弃置牌数）。此【杀】结算后，若此【杀】目标数为X且对所有目标造成伤害，你将手牌摸至最接近的唯一值。",
  pothuanshi: "还施",
  pothuanshi_info: `使命技，你每回合使用首张【杀】伤害+1；你于非濒死状态时无法使用但可重铸【酒】。成功：你造成或受到伤害后，若本次伤害值等于你体力值，你获得${get.poptip("potjianlv")}。`,
  potjianlv: "兼虑",
  potjianlv_info: "你一次性弃置至少X张牌时（X为本技能已触发次数+1），你可对一名其他角色造成1点伤害。若其因此死亡，你选择一项：1.重置此技能的X；2.对一名其他角色造成1点伤害。",
  re_zhoufei: "手杀界周妃",
  re_zhoufei_prefix: "手杀界",
  reliangyin: "良姻",
  reliangyin_info: "①当有牌被置于武将牌上时，你可令一名角色摸一张牌。②当有角色获得一名角色武将牌上的牌时，你可令一名角色弃置一张牌。③每轮结束时，你可令本轮唯一成为此技能目标的角色回复1点体力，或令本轮唯一未成为此技能目标的角色失去1点体力。",
  rekongsheng: "箜声",
  rekongsheng_info: "准备阶段、当你失去或回复体力后，你可以将当前回合角色的任意张牌扣置于其武将牌上。若如此做，该角色的结束阶段，你可以使用其中一张牌，然后其获得其余的牌。当你死亡时，所有角色收回其“箜声”牌。",
  re_yanyan: "手杀界严颜",
  re_yanyan_prefix: "手杀界",
  rejuzhan: "拒战",
  rejuzhan_info: `转换技，游戏开始时，若你的势力为蜀，此技能的初始状态改为阴；阳：当你或者此状态下第三次有角色成为【杀】的目标后，你可以与此【杀】的使用者各摸一张牌，然后其本回合不能对你使用牌；阴：当你或者此状态下第三次有角色使用【杀】指定目标后，你可以获得此【杀】的目标角色一张牌，然后你本回合不能对其使用牌。${get.poptip("rule_chengshi")}：你使用【杀】的次数上限+1，且此【杀】结算完毕后，你获得之。`,
  mb_gaoxiang: "手杀高翔",
  mb_gaoxiang_prefix: "手杀",
  mbgxchiyuan: "驰援",
  mbgxchiyuan_info: "出牌阶段限一次，你可以交给一名体力值小于你的角色一张牌，然后视为对攻击范围内包含其的一名角色使用一张【杀】。此【杀】结算后，若造成过伤害，其回复1点体力值；若未造成过伤害，目标角色依次视为对你使用一张【杀】。",
  mberdi: "饵敌",
  mberdi_info: "结束阶段，你可以指定一名手牌数大于你的角色，你失去1点体力值并摸一张牌，然后你与其同时弃置任意张手牌。此法弃置牌后，若你弃置的牌数：大于其，你回复1点体力值并对其造成1点伤害；小于其，你摸等同于其弃置的牌数。",
  mb_cuimao: "手杀崔琰毛玠",
  mb_cuimao_prefix: "手杀",
  mbzhengbi: "征辟",
  mbzhengbi_info: "准备阶段，你可以选择至多一名其他角色，其选择令你获得其区域里的一至三张牌，然后你交给其等量张牌。每名角色限一次，若其选择三，此技能可选择的角色数+1。",
  mbfengying: "奉迎",
  mbfengying_info: "限定技，出牌阶段，你可以选择一名角色并令其攻击范围内的所有其他角色各将一张手牌当【无中生有】对其使用。若该角色体力上限为全场最大，此回合结束时你获得一个额外的回合。",
  mb_zhongyao: "手杀钟繇",
  mb_zhongyao_prefix: "手杀",
  mbzuoding: "佐定",
  mbzuoding_info: "当其他角色于其出牌阶段内使用♠️牌指定目标后，若你或者目标角色于此阶段内没有受到过伤害，你可以令其中一名目标角色摸一张牌。",
  mb_wutugu: "手杀兀突骨",
  mb_wutugu_prefix: "手杀",
  mbranshang: "燃殇",
  mbranshang_info: "锁定技，①当你受到1点火焰伤害后，你获得1枚“燃”标记。②结束阶段，你失去X点体力（X为你的“燃”标记数）；然后若你的“燃”数量大于1，你减少2点体力上限并摸两张牌。",
  mbhanyong: "悍勇",
  mbhanyong_info: "当你使用牌指定的目标数大于1时，若你已受伤，你可以选择一项：1.摸两张牌；2.令此牌造成的伤害+1。背水：获得一个“燃”。",
  mb_chengyu: "手杀程昱",
  mb_chengyu_prefix: "手杀",
  mbshefu: "设伏",
  mbshefu_info: "①结束阶段，你可以记录一个未被记录的非装备牌名并扣置一张牌，称为“伏兵”。②你的回合外，当其他角色使用牌时，你可以移去一张记录名称相同的“伏兵”，令此牌无效并摸一张牌。",
  mbbenyu: "贲育",
  mbbenyu_info: "当你受到伤害后，若你的手牌数：小于X，你可以将手牌摸至X张（至多摸五张）；大于X，你可以弃置至少X+1张手牌，对伤害来源造成1点伤害（X为伤害来源的手牌数）。",
  mb_dongbai: "手杀董白",
  mb_dongbai_prefix: "手杀",
  mblianzhu: "连诛",
  mblianzhu_info: "①出牌阶段限一次，你可以展示一张牌并将此牌交给一名其他角色。若如此做，你对持有此牌的角色获得以下效果直至出牌阶段结束：1.你对其使用牌无距离次数限制；2.当你对其造成伤害后，你可以获得此牌。②一名其他角色于此阶段内因此技能获得此牌后，其须选择一项：1.交给你一张除此牌以外的其他牌，然后将此牌交给其上家角色；2.令你摸两张牌。",
  mbxiahui: "黠慧",
  mbxiahui_info: "锁定技，①黑色牌不计入你的手牌上限；②当其他角色获得“连诛”牌或者你的黑色牌时，其不能使用、打出或弃置这些牌直到其体力值减少为止。",
  mb_zhugeguo: "手杀诸葛果",
  mb_zhugeguo_prefix: "手杀",
  mbqirang: "祈禳",
  mbqirang_info: "当有装备牌进入你的装备区时，你可从牌堆中获得一张锦囊牌，你使用此锦囊牌时无距离限制并摸一张牌。",
  mbyuhua: "羽化",
  mbyuhua_info: "锁定技，你的非基本牌不计入手牌上限；结束阶段，若你的手牌数大于体力值，你观看牌堆顶X张牌（X为你手牌类别数），将其中任意数量的牌置于牌堆顶，其余的牌置于牌堆底。",
  mb_caochun: "手杀曹纯",
  mb_caochun_prefix: "手杀",
  mbshanjia: "缮甲",
  mbshanjia_info: "出牌阶段限一次，你可以摸三张牌，然后弃置X张牌（X为3-你本局失去过的装备区里的牌数）。若你未因此弃置：基本牌，你可视为使用一张无任何次数限制的【杀】；锦囊牌，你本回合使用牌无距离限制。",
  mb_xiahoumao: "手杀夏侯楙",
  mb_xiahoumao_prefix: "手杀",
  mbtongwei: "统围",
  mbtongwei_info: `出牌阶段限一次，你可重铸手牌中任意张基本牌。然后你选择一项：①令所有手牌中有同名牌的角色弃置一张基本牌。②获得本回合因弃置进入弃牌堆的同名基本牌。③${get.poptip("rule_beishui")}：移除一个可触发${get.poptip("mbcuguo")}的牌名。`,
  mbcuguo: "蹙国",
  mbcuguo_info: "锁定技，当你使用伤害牌指定目标后，弃置你与目标角色点数最大的手牌。若弃置了点数小于此牌的牌，此牌对所有目标额外结算一次。",
  xin_simayi: "手杀神司马懿",
  xin_simayi_prefix: "手杀神",
  xinrenjie: "忍戒",
  xinrenjie_info: "锁定技。①当你需要响应一张延时锦囊牌或其他角色使用的牌时，若你未响应此牌，你获得1枚“忍”标记（你每轮以此法至多获得4枚“忍”标记）。②游戏开始时，若你为神势力且你未执行神武将势力选择，则你可以变更势力。",
  xinbaiyin: "拜印",
  xinbaiyin_info: `觉醒技，准备阶段，若你的“忍”标记数不小于4，你减少1点体力上限，然后获得${get.poptip("xinjilve")}。`,
  xinlianpo: "连破",
  xinlianpo_info: "当你杀死一名角色后，你可以选择一项：1.于此回合结束后获得一个额外回合（每回合限一次）；2.若你拥有〖极略〗，你获得一个你未拥有的〖极略〗技能。",
  xinjilve: "极略",
  xinjilve_info: `①当你获得此技能时，你获得${get.poptip("guicai")}并根据你的势力获得以下对应技能：魏：${get.poptip("fangzhu")}；蜀：${get.poptip("jizhi")}；吴：${get.poptip("zhiheng")}；群：${get.poptip("wansha")}。②出牌阶段开始时，你可以选择一项：1.弃置X枚“忍”标记并获得一个你未拥有的〖极略〗技能（X为你选择此项的次数+1且至少为2）；2.弃置至多2枚“忍”标记并摸等量张牌。`,
  liuzan: "手杀留赞",
  liuzan_prefix: "手杀",
  re_sp_zhugeliang: "手杀界卧龙",
  re_sp_zhugeliang_prefix: "手杀界",
  ly_piliche: "霹雳车",
  ly_piliche_info: "当你对其他角色造成伤害后，你可以弃置其装备区内的所有牌。",
  polu: "破橹",
  polu_info: `锁定技，回合开始时，若${get.poptip("ly_piliche")}未加入游戏或在牌堆/弃牌堆内，则你获得并使用之；当你受到1点伤害后，若你的装备区里没有${get.poptip("ly_piliche")}，你摸一张牌，然后你获得牌堆中的一张随机武器牌并使用之。`,
  choulve: "筹略",
  choulve_info: "出牌阶段开始时，你可以令一名其他角色交给你一张牌，若其如此做，视为你使用上一张对你造成过伤害且不为延时锦囊牌的牌。",
  tunchu: "屯储",
  tunchu_info: "摸牌阶段，若你没有「粮」，你可以多摸两张牌。若如此做，摸牌阶段结束时，你可以将任意张手牌置于你的武将上，称为「粮」，只要你的武将牌上有「粮」，你便不能使用【杀】。",
  shuliang: "输粮",
  shuliang_info: "一名角色的结束阶段开始时，若其手牌数少于体力值，你可以移去一张「粮」，然后该角色摸两张牌。",
  fenyin: "奋音",
  yingjian: "影箭",
  fenyin_info: "你的回合内，当你使用牌时，若此牌与你于此回合内使用的上一张牌的颜色不同，则你可以摸一张牌。",
  yingjian_info: "准备阶段开始时，你可以视为使用一张无距离限制的【杀】。",
  dujin: "独进",
  dujin_info: "摸牌阶段，你可以多摸X+1张牌（X为你装备区里牌数的一半且向上取整）。",
  shixin: "释衅",
  shixin_info: "锁定技，当你受到火属性伤害时，你防止此伤害。",
  zhaohuo: "招祸",
  zhaohuo_info: "锁定技，当其他角色进入濒死状态时，你将体力上限调整为1点。若你的体力上限因此减少，则你摸X张牌。（X为你以此法减少的体力上限）",
  yixiang: "义襄",
  yixiang_info: "每名角色的回合限一次，当你成为一名角色使用牌的目标后，若该角色的体力值大于你的体力值，你可以随机获得牌堆里的一张你没有的基本牌。",
  yirang: "揖让",
  yirang_info: "出牌阶段开始时，你可以将所有非基本牌交给一名体力上限大于你的其他角色，然后调整体力上限至与该角色相同并回复X点体力（X为你以此法交给其的牌的类别数）。",
  kuangcai: "狂才",
  kuangcai_info: "出牌阶段开始时，你可以令你此阶段内的主动出牌时间变为5秒。若如此做，你于此阶段内使用牌没距离和次数限制，且每当你于此阶段内使用牌时，你摸一张牌且主动出牌时间-1秒。若主动出牌时间减至0，则你结束出牌阶段。",
  shejian: "舌剑",
  shejian_info: "弃牌阶段结束时，若你于此阶段弃置过至少两张牌且这些牌花色均不相同，则你可以弃置一名其他角色的一张牌。",
  xinfu_daigong: "怠攻",
  xinfu_daigong_info: "每回合限一次。当你受到伤害时，你可以展示所有手牌，然后令伤害来源选择一项：交给你一张与你所有手牌花色均不相同的一张牌，或防止此伤害。",
  xinfu_zhaoxin: "昭心",
  xinfu_zhaoxin_info: "出牌阶段限一次，你可以将任意张牌置于武将牌上并摸等量的牌，称之为“望”（你至多拥有三张“望”）。你或你攻击范围内的角色的摸牌阶段结束后，其可以获得一张“望”，然后你可以对其造成1点伤害。",
  xinfu_qianchong: "谦冲",
  xinfu_qianchong_info: `锁定技，若你的装备区内有牌且：均为红色，则你视为拥有技能${get.poptip("mingzhe")}。均为黑色，则你视为拥有技能${get.poptip("weimu")}。若均不满足，则出牌阶段开始时，你可以选择一种类别的牌，然后你本回合内使用该类别的牌时没有次数和距离限制。`,
  xinfu_shangjian: "尚俭",
  xinfu_shangjian_info: "锁定技。一名角色的结束阶段开始时，若你于此回合内不因使用装备牌而失去了X张或更少的牌，则你摸等量的牌（X为你的体力值）。",
  rw_bagua_skill: "先天八卦阵",
  rw_bagua_skill_info: "当你需要使用或打出一张【闪】时，你可以进行判定，若判定结果不为黑桃，视为你使用或打出了一张【闪】。",
  rw_baiyin_skill: "照月狮子盔",
  rw_baiyin_skill_info: "锁定技，当你受到大于1的伤害时，你将伤害值改为1；当你失去装备区里的【照月狮子盔】时，你回复1点体力并摸两张牌。",
  rw_lanyinjia: "精银甲",
  rw_lanyinjia_info: "你可以将一张手牌当做【闪】使用或打出。锁定技，【精银甲】不会无效。",
  rw_minguangkai_cancel: "耀光铠",
  rw_minguangkai_cancel_info: "锁定技，当你成为【火烧连营】、【火攻】或火【杀】的目标时，或即将被横置时，取消之。",
  rw_minguangkai_link: "耀光铠",
  rw_minguangkai_link_info: "锁定技，当你成为【火烧连营】、【火攻】或火【杀】的目标时，或即将被横置时，取消之。",
  rw_renwang_skill: "仁王金刚盾",
  rw_renwang_skill_info: "黑色【杀】和红桃【杀】对你无效。",
  rw_tengjia1: "桐油百韧甲",
  rw_tengjia1_info: "锁定技，【南蛮入侵】、【万箭齐发】和普【杀】对你无效。当你受到火焰伤害时，此伤害+1。当你即将被横置时，取消之。",
  rw_tengjia2: "桐油百韧甲",
  rw_tengjia2_info: "锁定技，【南蛮入侵】、【万箭齐发】和普【杀】对你无效。当你受到火焰伤害时，此伤害+1。当你即将被横置时，取消之。",
  rw_tengjia3: "桐油百韧甲",
  rw_tengjia3_info: "锁定技，【南蛮入侵】、【万箭齐发】和普【杀】对你无效。当你受到火焰伤害时，此伤害+1。当你即将被横置时，取消之。",
  rw_tengjia4: "桐油百韧甲",
  rewrite_bagua: "先天八卦阵",
  rewrite_bagua_info: "当你需要使用或打出一张【闪】时，你可以进行判定，若判定结果不为黑桃，视为你使用或打出了一张【闪】。",
  rewrite_baiyin: "照月狮子盔",
  rewrite_baiyin_info: "锁定技，当你受到大于1的伤害时，你将伤害值改为1；当你失去装备区里的【照月狮子盔】时，你回复1点体力并摸两张牌。",
  rewrite_lanyinjia: "精银甲",
  rewrite_lanyinjia_info: "你可以将一张手牌当做【闪】使用或打出。锁定技，【精银甲】不会无效。",
  rewrite_minguangkai: "耀光铠",
  rewrite_minguangkai_info: "锁定技，当你成为【火烧连营】、【火攻】或火【杀】的目标时，或即将被横置时，取消之。",
  rewrite_renwang: "仁王金刚盾",
  rewrite_renwang_info: "黑色【杀】和红桃【杀】对你无效。",
  rewrite_tengjia: "桐油百韧甲",
  rewrite_tengjia_info: "锁定技，【南蛮入侵】、【万箭齐发】和普【杀】对你无效。当你受到火焰伤害时，此伤害+1。当你即将被横置时，取消之。",
  rewrite_zhuge: "元戎精械弩",
  rewrite_zhuge_info: "锁定技，你于出牌阶段内使用【杀】无次数限制。",
  rw_zhuge_skill: "诸葛连弩",
  rw_zhuge_skill_info: "锁定技，你于出牌阶段内使用【杀】无次数限制。",
  takaramono: "宝物",
  wolong_card: "卧龙",
  wolong_card_info: "对一名角色造成1点火焰伤害。若场上有存活的诸葛亮，则改为对至多两名角色各造成1点火焰伤害。",
  fengchu_card: "凤雏",
  fengchu_card_info: "横置至多三名角色。若场上有存活的庞统，则改为横置至多四名角色。",
  xuanjian_card: "玄剑",
  xuanjian_card_info: "令一名角色摸一张牌并回复1点体力。若场上有存活的徐庶，则改为令一名角色摸一张牌并回复1点体力，然后你摸一张牌。",
  shuijing_card: "水镜",
  shuijing_card_info: "将一名角色装备区内的防具牌移动到另一名角色对应区域。若场上有存活的司马徽，则改为将1名角色装备区内的1件装备移动到另1角色对应区域。",
  xinfu_pingcai: "评才",
  xinfu_pingcai_info: "出牌阶段限一次，你可以挑选一个宝物并擦拭掉其上面的灰尘。然后，你可以根据宝物类型执行对应的效果。",
  xinfu_pdgyingshi: "隐世",
  xinfu_pdgyingshi2: "隐世",
  xinfu_pdgyingshi_info: "锁定技，你始终跳过准备阶段，判定阶段，结束阶段。你不能被选择为延时锦囊牌的目标。",
  pcaudio_wolong_card: "卧龙",
  pcaudio_wolong_card_info: "",
  pcaudio_fengchu_card: "凤雏",
  pcaudio_fengchu_card_info: "",
  pcaudio_shuijing_card: "水镜",
  pcaudio_shuijing_card_info: "",
  pcaudio_xuanjian_card: "玄剑",
  pcaudio_xuanjian_card_info: "",
  yizan_respond_sha: "翊赞",
  yizan_respond_sha_info: "",
  yizan_use: "翊赞",
  yizan_use_backup: "翊赞",
  yizan_use_info: "你可以将两张牌（其中至少一张为基本牌）当做任意基本牌使用或打出。",
  yizan_respond_shan: "翊赞",
  yizan_respond_shan_info: "",
  xinfu_longyuan: "龙渊",
  xinfu_longyuan_info: "觉醒技，准备阶段，若你本局游戏内发动过〖翊赞〗的次数大于等于3，则你修改〖翊赞〗。",
  yizan_rewrite: "翊赞·改",
  yizan_rewrite_info: "你可以将一张基本牌当做任意基本牌使用或打出。",
  yizan_count: "翊赞",
  yizan_count_info: "",
  xinfu_jingxie: "精械",
  xinfu_jingxie_info: "出牌阶段，你可以展示一张未强化过的【诸葛连弩】或标准包/军争包/SP包中的防具牌，然后对其进行强化。当你处于濒死状态时，你可以重铸一张防具牌，然后将体力回复至1点。",
  xin_xiahoudun: "手杀界夏侯惇",
  xin_xiahoudun_prefix: "手杀界",
  xinqingjian: "清俭",
  xinqingjian_info: "每回合限一次。当你不因摸牌阶段的额定摸牌而得到牌后，你可以将任意张手牌扣置于武将牌上。一名角色的回合结束后，你将这些牌交给任意名其他角色。若你此次交出牌的数量大于1，你摸一张牌。",
  zhangyì: "张翼",
  jiakui: "贾逵",
  zhiyi: "执义",
  zhiyi_info: "锁定技，当你于一回合内使用或打出第一张基本牌时，你选择一项：1.摸一张牌。2.于此牌A（若此牌是因响应牌B而使用或打出的，则改为牌B）的使用或打出流程结算完成后，视为使用一张与此牌名称和属性相同的卡牌。",
  rezhiyi: "执义",
  rezhiyi_info: "锁定技，一名角色的结束阶段开始时，若你本回合内使用或打出过基本牌，则你选择一项：1.摸一张牌。2.视为使用一张你本回合内使用或打出过的基本牌。",
  zhongzuo: "忠佐",
  zhongzuo_info: "一名角色的结束阶段开始时，若你于此回合内造成或受到过伤害，则你可以令一名角色摸两张牌。若该角色已受伤，则你摸一张牌。",
  wanlan: "挽澜",
  wanlan_info: "限定技，当一名角色进入濒死状态时，你可以弃置所有手牌（无牌可不弃）。其回复体力至1点，然后你对当前回合角色造成1点伤害。",
  re_jikang: "手杀嵇康",
  re_jikang_prefix: "手杀",
  shenpei: "审配",
  re_wangyun: "手杀王允",
  re_wangyun_prefix: "手杀",
  relianji: "连计",
  relianji_info: "出牌阶段限一次，你可以选择两名其他角色A和B。第一名角色A随机使用牌堆中的一张武器牌，然后A视为对另一名角色B随机使用一张下列的牌名的牌：【决斗】、【火攻】、【南蛮入侵】、【万箭齐发】或普【杀】。然后若此牌造成伤害且A为伤害来源，你获得X枚“连计”（X为此次扣减的体力值点数）",
  remoucheng: "谋逞",
  remoucheng_info: `觉醒技，当一名角色造成伤害后，若你拥有的“连计”数大于2，你增加1点体力上限，回复1点体力，失去${get.poptip("relianji")}，获得${get.poptip("jingong")}。`,
  shouye: "守邺",
  shouye_info: "每回合限一次。当你成为其他角色使用牌的唯一目标后，你可以与其进行【对策】。若你赢，则此牌对你无效，且你于此牌进入弃牌堆时获得其对应的所有实体牌。",
  liezhi: "烈直",
  liezhi_info: "准备阶段，你可以依次弃置至多两名其他角色区域内的各一张牌。当你受到伤害时，此技能失效直至你的下个结束阶段。",
  xinzhanyi: "战意",
  xinzhanyi_info: "出牌阶段限一次，你可以弃置一张牌并失去1点体力，然后根据你弃置的牌获得对应效果直到此阶段结束：基本牌，你可以将一张基本牌当作任意基本牌使用，且你此阶段下一张使用的基本牌获得以下效果：{当你因执行你使用此牌的效果而造成伤害时，此伤害值+1；当一名角色因执行你使用此牌的效果而回复体力时，此回复值+1。}；锦囊牌，摸三张牌且你使用的牌不能被【无懈可击】响应；装备牌，你使用【杀】指定唯一目标后，其弃置两张牌，然后你获得其中的一张。",
  meiyong: "姝勇",
  meiyong_info: "当你使用或打出【杀】时，你可以获得一名其他角色区域内的一张牌，然后其摸一张牌。",
  rexushen: "许身",
  rexushen_info: `限定技，出牌阶段，你可以失去X点体力（X为场上男性角色的数量）。若你以此法进入了濒死状态，则当你因一名角色而脱离此濒死状态后，你可以令其获得${get.poptip("new_rewusheng")}和${get.poptip("redangxian")}。`,
  rezhennan: "镇南",
  rezhennan_info: "当你成为其他角色使用的牌的目标后，若此牌的目标数大于该角色的体力值，则你可以弃置一张牌并对其造成1点伤害。",
  hujinding: "手杀胡金定",
  hujinding_prefix: "手杀",
  huaizi: "怀子",
  huaizi_info: "锁定技，你的手牌上限为你的体力上限。",
  renshi: "仁释",
  renshi_info: "锁定技，当你受到【杀】的伤害时，若你已受伤，则你防止此伤害并获得此【杀】对应的所有实体牌，然后减1点体力上限。",
  wuyuan: "武缘",
  wuyuan_info: "出牌阶段限一次，你可将一张【杀】交给一名其他角色，然后你回复1点体力，其摸一张牌。若此【杀】为：红色【杀】，其回复1点体力；属性【杀】，其改为摸两张牌。",
  re_weiwenzhugezhi: "手杀卫温诸葛直",
  re_weiwenzhugezhi_prefix: "手杀",
  gz_re_xugong: "许贡",
  re_xugong: "手杀许贡",
  re_xugong_prefix: "手杀",
  re_zhanggong: "手杀张恭",
  re_zhanggong_prefix: "手杀",
  reqianxin: "遣信",
  reqianxin_info: "出牌阶段限一次，你可将至多两张手牌随机交给等量的其他角色，称为「信」。这些角色的准备阶段开始时，若其手牌中有「信」，则其选择一项：令你摸两张牌，本回合手牌上限-2。",
  rebiaozhao: "表召",
  rebiaozhao_info: "结束阶段，你可以将一张牌置于武将牌上，称为「表」。当有一张与「表」点数相同的牌进入弃牌堆后，你将「表」置入弃牌堆并失去1点体力。准备阶段，若你的武将牌上有「表」，则你移去「表」并选择一名角色，该角色回复1点体力并摸三张牌。",
  rebiaozhao2: "表召",
  rebiaozhao2_info: "",
  rebiaozhao3: "表召",
  rebiaozhao3_info: "",
  refuhai: "浮海",
  refuhai_info: "出牌阶段限一次，你可令其他角色同时在「潮起」和「潮落」中选择一项，并依次展示这些角色的选项。若从你下家开始选择了相同选项的角色数目大于1，则你摸X张牌（X为连续相同结果的数量）。",
  qiaosi: "巧思",
  qiaosi_info: "出牌阶段限一次，你可以表演「大键角色图」并根据表演结果获得相应的牌。然后，你选择一项：1.弃置X张牌。2.将X张牌交给一名其他角色。（X为你以此法得到的牌数）",
  qiaosi_map: "大键角色图",
  qiaosi_map_info: "<br><li>星野 梦美：锦囊牌*2<br><li>能美 库特莉亚芙卡：装备牌/【杀】/【酒】*1<br><li>友利 奈绪：【杀】/【酒】*1<br><li>神尾 观铃：【闪】/【桃】*1<br><li>伊吹 风子：锦囊牌/【闪】/【桃】*1<br><li>仲村 由理：装备牌*2<br><li>Illustration:うら;Twitter:@ura530",
  qiaosi_c1: '<img src="' + lib.assetURL + 'image/card/qiaosi_card1.png" width="60" height="60"> ',
  //星野 梦美
  qiaosi_c2: '<img src="' + lib.assetURL + 'image/card/qiaosi_card2.png" width="60" height="60"> ',
  //能美 库特莉亚芙卡
  qiaosi_c3: '<img src="' + lib.assetURL + 'image/card/qiaosi_card3.png" width="60" height="60"> ',
  //友利 奈绪
  qiaosi_c4: '<img src="' + lib.assetURL + 'image/card/qiaosi_card4.png" width="60" height="60"> ',
  //神尾 观铃
  qiaosi_c5: '<img src="' + lib.assetURL + 'image/card/qiaosi_card5.png" width="60" height="60"> ',
  //伊吹 风子
  qiaosi_c6: '<img src="' + lib.assetURL + 'image/card/qiaosi_card6.png" width="60" height="60"> ',
  //仲村 由理
  yangbiao: "手杀杨彪",
  yangbiao_prefix: "手杀",
  zhaohan: "昭汉",
  zhaohan_info: "锁定技，你的第1-4个准备阶段开始时，你加1点体力上限并回复1点体力，你的第5-7个准备阶段开始时，你减1点体力上限。",
  rangjie: "让节",
  rangjie_info: "当你受到1点伤害后，你可以选择一项并摸一张牌：获得牌堆或弃牌堆里你选择的类型的一张牌，或移动场上的一张牌。",
  yizheng: "义争",
  yizheng_info: "出牌阶段限一次，你可以和一名体力值不大于你的其他角色拼点。若你赢，其跳过下个摸牌阶段。若你没赢，你减1点体力上限。",
  re_heqi: "手杀贺齐",
  re_heqi_prefix: "手杀",
  reqizhou: "绮胄",
  reqizhou_info: `锁定技，当你使用装备时，你根据使用过的装备牌名数获得以下技能：1种或以上：${get.poptip("yingzi")}；2种或以上：${get.poptip("jiang")}；3种或以上：${get.poptip("xinpojun")}；4种或以上：${get.poptip("dbchongjian")}；5种或以上：${get.poptip("fenwei")}。`,
  reshanxi: "闪袭",
  reshanxi_info: "出牌阶段限一次，你可以弃置一张基本牌，然后弃置一名其他角色的一张手牌。若你弃置的牌为红色，你获得其弃置的牌；若其因此弃置的牌名未因此技能弃置过，你对其造成1点伤害。",
  chendeng: "手杀陈登",
  chendeng_prefix: "手杀",
  zhouxuan: "周旋",
  zhouxuan_info: "出牌阶段限一次，你可以指定一名敌方角色，然后选择一个基本牌的名称或非基本牌的类型。其使用或打出下一张牌时，若此牌的名称或类型和你选择的相同，则你观看牌堆顶的三张，然后将这些牌分配给任意名角色。",
  zhouxuan_info_identity: "出牌阶段限一次，你可以指定一名其他角色，然后选择一个基本牌的名称或非基本牌的类型。其使用或打出下一张牌时，若此牌的名称或类型和你选择的相同，则你观看牌堆顶的三张牌，然后将这些牌分配给任意名角色。",
  zhouxuan_info_doudizhu: "出牌阶段限一次，你可以指定一名其他角色，然后选择一个基本牌的名称或非基本牌的类型。其使用或打出下一张牌时，若此牌的名称或类型和你选择的相同，则你观看牌堆顶的三张牌，然后将这些牌分配给任意名角色。",
  fengji: "丰积",
  fengji_info: "锁定技，回合结束时，你记录你的手牌数。回合开始时，若你的手牌数不小于你记录的手牌数，则你摸三张牌且本回合手牌上限为体力上限。",
  re_guanqiujian: "手杀毌丘俭",
  re_guanqiujian_prefix: "手杀",
  rezhengrong: "征荣",
  rehongju: "鸿举",
  reqingce: "清侧",
  reqingce_backup: "清侧",
  rezhengrong_info: "锁定技。当你于出牌阶段使用的指定了其他角色为目标的牌结算完成后，若此牌是你本局游戏内于出牌阶段使用的指定了其他角色为目标的第偶数张牌，则你将一名其他角色的随机一张牌置于你的武将牌上，称为「荣」。",
  rehongju_info: `觉醒技，准备阶段，若你武将牌上「荣」的数量不小于3且有角色死亡，则你摸等同于「荣」数量的牌。然后可以用任意数量的手牌交换等量的「荣」。你减1点体力上限并获得技能${get.poptip("reqingce")}。`,
  reqingce_info: "出牌阶段，你可以将一张「荣」置入弃牌堆，然后弃置场上的一张牌。",
  re_pangtong: "手杀界庞统",
  re_pangtong_prefix: "手杀界",
  dongcheng: "手杀董承",
  dongcheng_prefix: "手杀",
  chengzhao: "承诏",
  chengzhao_info: "一名角色的结束阶段，若你于本回合内获得了至少两张牌，则你可以与一名角色拼点。若你赢，你视为对其使用一张无视防具的【杀】。",
  yangyi: "手杀杨仪",
  yangyi_prefix: "手杀",
  duoduan: "度断",
  duoduan_info: "每回合限一次，当你成为【杀】的目标后，你可以重铸一张牌。若如此做，你选择一项：①令使用者摸两张牌，且此【杀】无效。②令使用者弃置一张牌，且你不能响应此【杀】。",
  gongsun: "共损",
  gongsun_info: "出牌阶段开始时，你可以弃置两张牌并指定一名其他角色。你选择一个基本牌或普通锦囊牌的牌名。直到你的下回合开始或你死亡，你与其不能使用或打出或弃置此名称的牌。",
  gongsun_shadow: "共损",
  xin_chengpu: "手杀界程普",
  xin_chengpu_prefix: "手杀界",
  relihuo: "疠火",
  relihuo_damage: "疠火",
  relihuo_baigei: "疠火",
  relihuo_info: "当你使用普【杀】时，你可以将此【杀】改为火属性。若如此做，当你因执行此【杀】的效果而对横置角色造成伤害时，此伤害+1；当你使用的火【杀】结算完成后，你失去X点体力（X为你因此【杀】造成的伤害总点数的一半且向下取整）。",
  dengzhi: "手杀邓芝",
  dengzhi_prefix: "手杀",
  jimeng: "急盟",
  jimeng_info: "出牌阶段开始时，你可以获得一名其他角色的一张牌，然后交给该角色X张牌（X为你当前体力值）。",
  shuaiyan: "率言",
  shuaiyan_info: "弃牌阶段开始时，若你的手牌数大于1，则你可以展示所有手牌，然后你令一名其他角色交给你一张牌。",
  zhengxuan: "郑玄",
  zhengjing: "整经",
  zhengjing_info: "出牌阶段，你可以整理卡牌。然后，你将整理出的卡牌中的至少一张作为“经”置于一名角色的武将牌上，然后获得其余的牌。该角色的准备阶段获得这些牌，且跳过此回合的判定和摸牌阶段。",
  zhengjing2: "整经",
  sp_sufei: "手杀苏飞",
  sp_sufei_prefix: "手杀",
  zhengjian: "诤荐",
  zhengjian_draw: "诤荐",
  zhengjian_mark: "诤荐",
  zhengjian_info: "锁定技，结束阶段，你令一名角色获得一枚“诤”。回合开始时，你令有“诤”的角色移去“诤”并摸X张牌（X为其最后一次获得“诤”后使用和打出的牌数，且至多不能超过其体力上限或5）。",
  gaoyuan: "告援",
  gaoyuan_info: "当你成为【杀】的目标时，你可弃置一张牌将此【杀】转移给一名有“诤”且不是此【杀】使用者的其他角色。",
  tongqu: "通渠",
  tongqu_info: "游戏开始时，你拥有一个“渠”标记。准备阶段，你可以失去1点体力令一名没有“渠”标记的角色获得“渠”标记。有“渠”的角色摸牌阶段额外摸一张牌，然后将一张牌交给其他有“渠”的角色或弃置。若以此法给出的是装备牌则使用之。当有“渠”的角色进入濒死状态时，移除其“渠”标记。",
  xinwanlan: "挽澜",
  xinwanlan_info: "当一名角色受到伤害值不小于体力值的伤害时，你可以弃置装备区中的所有牌（至少一张） 防止此伤害。",
  re_xusheng: "手杀界徐盛",
  re_xusheng_prefix: "手杀界",
  re_dongzhuo: "手杀界董卓",
  re_dongzhuo_prefix: "手杀界",
  rejiuchi: "酒池",
  rejiuchi_info: "你可以将一张黑桃手牌当做【酒】使用。当你于回合内使用带有【酒】效果的【杀】造成伤害时，你令你的〖崩坏〗失效直到回合结束。",
  furong: "手杀傅肜",
  furong_prefix: "手杀",
  xuewei: "血卫",
  xuewei_info: "准备阶段，你可以选择一名其他角色（仅自己可见）。若如此做，直到你的下回合开始前，当其第一次受到伤害时，你防止此伤害，改为由你受到等量的伤害并对伤害来源造成等量同属性的伤害。",
  liechi: "烈斥",
  liechi_info: "锁定技，当你进入濒死状态时，伤害来源弃置一张牌。",
  xin_liaohua: "手杀界廖化",
  xin_liaohua_prefix: "手杀界",
  redangxian: "当先",
  redangxian_info: "锁定技，回合开始时，你从弃牌堆中获得一张【杀】并进行一个额外的出牌阶段。",
  refuli: "伏枥",
  refuli_info: "限定技，当你处于濒死状态时，你可以将体力值回复至X点（X为势力数）。然后若你的体力值为全场唯一最多，你翻面。",
  xin_caozhang: "手杀界曹彰",
  xin_caozhang_prefix: "手杀界",
  rejiangchi: "将驰",
  rejiangchi_info: "出牌阶段开始时，你可以选择一项：1、摸一张牌，若如此做，你本阶段内不能使用【杀】。 2、弃置一张牌，若如此做，此阶段你使用【杀】无距离限制且你可以额外使用一张【杀】。",
  rezhenxing: "镇行",
  rezhenxing_info: "结束阶段开始时或当你受到伤害后，你可以观看牌堆顶的三张牌，然后你获得其中与其余牌花色均不相同的一张牌，将剩余牌以原顺序放回牌堆顶。",
  xin_jianyong: "手杀界简雍",
  xin_jianyong_prefix: "手杀界",
  xinqiaoshui: "巧说",
  xinqiaoshui_info: "出牌阶段限一次，你可以和一名其他角色拼点。若你赢，则你本阶段内使用下一张基本牌或普通锦囊牌时，可以为此牌增加或减少一个目标。若你没赢，你本阶段内不能使用锦囊牌。",
  xinjyzongshi: "纵适",
  xinjyzongshi_info: "当你拼点后，你观看牌堆顶的一张牌，然后可以选择一项：获得此牌，或获得两张拼点牌中点数较小的一张。",
  dingyuan: "手杀丁原",
  dingyuan_prefix: "手杀",
  beizhu: "备诛",
  beizhu_draw: "备诛",
  beizhu_info: "出牌阶段限一次，你可以观看一名其他角色的手牌。若其中：没有【杀】，你弃置其一张牌，然后你可令其获得牌堆中的一张【杀】；有【杀】，其依次对你使用这些【杀】，当你因此受到1点伤害后，你摸一张牌。",
  xin_zhuran: "手杀界朱然",
  xin_zhuran_prefix: "手杀界",
  mobiledanshou: "胆守",
  mobiledanshou_info: "一名其他角色的结束阶段开始时，若X：为0，你摸一张牌；不等于0，你可弃置X张牌并对其造成1点伤害（X为其本回合内使用的目标包含你的牌的数量）。",
  yanghuiyu: "羊徽瑜",
  hongyi: "弘仪",
  hongyi_info: "出牌阶段限一次，你可以选择一名其他角色。你的下回合开始前，该角色造成伤害时进行判定，若结果为：黑色，此伤害-1。红色，受到伤害的角色摸一张牌。",
  requanfeng: "劝封",
  requanfeng_gain: "劝封",
  requanfeng_info: "限定技。①其他角色死亡时，你可失去〖弘仪〗，然后获得其武将牌上的所有非主公技，非隐匿技和非Charlotte技，加1点体力上限并回复1点体力。②当你处于濒死状态时，你可以加2点体力上限，然后回复4点体力。",
  quanfeng: "劝封",
  quanfeng_info: "锁定技，限定技，一名角色死亡时，你选择获得其的一个技能（主公技，限定技，觉醒技，隐匿技，使命技，带有Charlotte标签的技能除外），然后加1点体力上限并回复1点体力。",
  simashi: "司马师",
  baiyi: "败移",
  baiyi_info: "限定技，出牌阶段，若你已受伤，你可以交换两名其他角色的座次。",
  jinglve: "景略",
  jinglve2: "景略",
  jinglve3: "景略",
  jinglve_info: "出牌阶段限一次，若场上没有与你对应的「死士」牌，则你可以观看一名其他角色的手牌，将其中一张牌标记为「死士」。当其使用对应的实体牌中包含「死士」的牌时，你取消此牌的所有目标。当「死士」牌不因使用而进入弃牌堆，或其回合结束后，若「死士」牌仍在其区域内，则你获得此牌。",
  shanli: "擅立",
  shanli_info: `觉醒技，准备阶段，若你已发动过〖败移〗且对至少两名角色发动过〖景略〗，则你减1点体力上限并选择一名角色。系统随机选择三个不为${get.poptip("yuri_wangxi")}的主公技，然后你选择其中一个技能，令其获得之。其将交互表情中的【拖鞋】和【酒杯】替换为【枷锁】和【玉玺】。`,
  re_lingtong: "手杀界凌统",
  re_lingtong_prefix: "手杀界",
  re_liubiao: "手杀界刘表",
  re_liubiao_prefix: "手杀界",
  hucheer: "手杀胡车儿",
  hucheer_prefix: "手杀",
  daoji: "盗戟",
  daoji_info: "出牌阶段限一次，你可以弃置一张非基本牌并选择一名装备区里有牌的其他角色，你获得其装备区中的一张牌并使用之。若你以此法得到的牌是武器牌，则你使用此牌后对其造成1点伤害。",
  xin_hansui: "手杀韩遂",
  xin_hansui_prefix: "手杀",
  xinniluan: "逆乱",
  xinniluan_info: "其他角色的结束阶段，若其本回合对除其以外的角色使用过牌，则你可以对其使用一张【杀】。若以此法使用的【杀】造成伤害，则你弃置其一张牌。",
  xinniluan_info_versus_two: "敌方角色的结束阶段，若其本回合对除其以外的角色使用过牌，则你可以对其使用一张【杀】。若以此法使用的【杀】造成伤害，则你弃置其一张牌。",
  xiaoxi_hansui: "骁袭",
  xiaoxi_hansui_info: "你可以将一张黑色牌当做【杀】使用或打出。",
  xin_zhangfei: "手杀界张飞",
  xin_zhangfei_prefix: "手杀界",
  liyong: "厉勇",
  liyong2: "厉勇",
  liyong3: "厉勇",
  liyong_info: "锁定技，若你于出牌阶段内使用的【杀】被【闪】抵消，则你获得如下效果：你本回合使用的下一张【杀】不可被响应且伤害+1，指定的目标本回合非锁定技失效，当此【杀】造成伤害后，若目标角色未死亡，你失去1点体力。",
  gongsunkang: "公孙康",
  juliao: "据辽",
  juliao_info: "锁定技，其他角色计算与你的距离始终+X（X为场上势力数-1）。",
  taomie: "讨灭",
  taomie1: "讨灭",
  taomie2: "讨灭",
  taomie3: "讨灭",
  taomie4: "讨灭",
  taomie_info: "当你受到伤害后或当你造成伤害后，你可以令伤害来源或受伤角色获得“讨灭”标记（如场上已有标记则转移给该角色）；你和拥有“讨灭”标记的角色视为在彼此的攻击范围内，且当你对该角色造成伤害时，选择一项：1. 此伤害+1；2. 你获得其区域内的一张牌并可将之交给另一名角色；3. 依次执行前两项并于伤害结算后弃置其“讨灭”标记。",
  xin_guohuai: "手杀界郭淮",
  xin_guohuai_prefix: "手杀界",
  mobilejingce: "精策",
  mobilejingce_info: "结束阶段，若此回合因使用或打出而置入弃牌堆的牌的数量不小于你的体力值，则你可以摸两张牌。",
  xin_panzhangmazhong: "手杀界潘璋马忠",
  xin_panzhangmazhong_prefix: "手杀界",
  xinduodao: "夺刀",
  xinduodao_info: "当你受到伤害后，你可以获得伤害来源装备区里的武器牌。",
  xinanjian: "暗箭",
  xinanjian_info: "锁定技，当你使用【杀】指定目标后，若你不在目标角色攻击范围内，你选择一项：1. 令其无法响应此【杀】；2. 其受到此【杀】造成的伤害+1。",
  xin_fuhuanghou: "手杀界伏寿",
  xin_fuhuanghou_prefix: "手杀界",
  xinzhuikong: "惴恐",
  xinzhuikong_info: "每轮限一次，其他角色的回合开始时，若其体力值不小于你，你可与其拼点。若你赢，其本回合无法使用牌指定除其以外的角色为目标；若你没赢，你获得其拼点的牌，然后其视为对你使用一张【杀】。",
  xinqiuyuan: "求援",
  xinqiuyuan_info: "当你成为【杀】的目标时，你可以令另一名其他角色交给你一张除【杀】以外的基本牌，否则其也成为此【杀】的目标。",
  xin_gongsunzan: "手杀界公孙瓒",
  xin_gongsunzan_prefix: "手杀界",
  re_dengai: "手杀界邓艾",
  re_dengai_prefix: "手杀界",
  flappybird: "飞鸟",
  flappybird_info: "出牌阶段，你可游玩一局Flappy Bird。",
  re_handang: "手杀界韩当",
  re_handang_prefix: "手杀界",
  nanhualaoxian: "手杀南华老仙",
  nanhualaoxian_prefix: "手杀",
  yufeng: "御风",
  yufeng2: "御风",
  yufeng_info: "出牌阶段限一次，你可以表演“御风飞行”。若表演失败，则你摸X张牌。若表演成功，则你可以选择至多X名其他角色获得“御风”效果，然后摸X-Y张牌（准备阶段开始时，你进行判定。若结果为：红色，你跳过摸牌阶段；黑色，你跳过出牌阶段和弃牌阶段。X为你的得分。Y为你选择的角色数）。",
  yufeng_old: "御风",
  yufeng_old_info: "看我干嘛，飞啊。",
  tianshu: "天书",
  tianshu_info: "出牌阶段开始时，若场上没有【太平要术】，则你可以弃置一张牌并选择一名角色。该角色获得并使用【太平要术】。",
  re_jiangwei: "手杀界姜维",
  re_jiangwei_prefix: "手杀界",
  retiaoxin: "挑衅",
  retiaoxin_info: "出牌阶段限一次，你可以指定一名有牌的其他角色，该角色需对你使用一张【杀】，否则你弃置其一张牌。",
  re_zhurong: "手杀界祝融",
  re_zhurong_prefix: "手杀界",
  relieren: "烈刃",
  relieren_info: "当你使用【杀】指定目标后，你可以和目标角色进行拼点。若你赢，你获得其一张牌。若你没赢，你获得对方的拼点牌，其获得你的拼点牌。",
  zhouqun: "周群",
  tiansuan: "天算",
  tiansuan_info: "出牌阶段限一次，若场上没有因你产生的“命运签”，则你可以抽取一张“命运签”，然后令一名角色获得“命运签”直到你的下回合开始。若你以此法抽取的“命运签”为：上上签，你观看其手牌并获得其区域内的一张牌；上签，你获得其一张牌。",
  tiansuan_faq: "【命运签】说明",
  tiansuan_faq_info: "当你抽取“命运签”时，你可以令其中一种命运签的权重+1。<br>上上签（权重1）：当你受到伤害时，防止此伤害。<br>上签（权重2）：当你受到伤害时，你令伤害值改为1；当你受到1点伤害后，你摸一张牌。<br>中签（权重3）：当你受到伤害时，你令伤害属性改为火属性并将伤害值改为1。<br>下签（权重2）：当你受到伤害时，你令此伤害+1。<br>下下签（权重1）：当你受到伤害时，你令此伤害+1。你不能使用【酒】或【桃】。",
  tiansuan2_0: "命运签",
  tiansuan2_1: "命运签",
  tiansuan2_2: "命运签",
  tiansuan2_damage: "命运签",
  tiansuan2_fire: "命运签",
  tiansuan2_3: "命运签",
  tiansuan2_4: "命运签",
  ol_yujin: "手杀界于禁",
  ol_yujin_prefix: "手杀界",
  rejieyue: "节钺",
  rejieyue_info: "结束阶段开始时，你可以将一张牌交给一名其他角色。然后其选择一项：令你摸三张牌：或其保留一张手牌和装备区的牌，然后弃置其余的牌。",
  xin_zhoucang: "手杀界周仓",
  xin_zhoucang_prefix: "手杀界",
  mobilezhongyong: "忠勇",
  mobilezhongyong_info: "当你于出牌阶段内使用的【杀】结算结束后，若没有目标角色使用【闪】响应过此【杀】，则你可获得此【杀】；否则你可选择一项：①获得目标角色使用的【闪】，然后可将此【杀】交给另一名其他角色。②将目标角色使用的【闪】交给另一名其他角色，然后你本回合使用【杀】的次数上限+1且下一张【杀】的伤害值基数+1。（你不能使用本回合因执行〖忠勇〗的效果得到的牌）",
  xin_caifuren: "手杀界蔡夫人",
  xin_caifuren_prefix: "手杀界",
  xinqieting: "窃听",
  xinqieting_info: "其他角色的回合结束时，若其本回合内未对其他角色造成过伤害，则你可选择一项：①摸一张牌。②观看其两张手牌并获得其中的一张。③将其装备区内的一张牌移动至你的装备区。",
  xinguixiu: "闺秀",
  xinguixiu_info: "锁定技。结束阶段，若你的体力值为：奇数，你摸一张牌；偶数，你回复1点体力。",
  re_zhonghui: "手杀界钟会",
  re_zhonghui_prefix: "手杀界",
  requanji: "权计",
  requanji_info: "出牌阶段结束时，若你的手牌数大于体力值，或当你受到1点伤害后，你可以摸一张牌，然后将一张手牌置于武将牌上，称为“权”；你的手牌上限+X（X为“权”的数量）。",
  xin_guyong: "手杀界顾雍",
  xin_guyong_prefix: "手杀界",
  xinshenxing: "慎行",
  xinshenxing_info: "出牌阶段限X次（X为你的体力值），你可以弃置两张牌，然后摸一张牌。若这两张牌颜色不同，则改为摸两张牌。",
  xinbingyi: "秉壹",
  xinbingyi_info: "结束阶段，你可展示所有手牌。若这些牌的颜色或类型均相同，则你可以令至多X名角色各摸一张牌（X为你的手牌数）。",
  qiaozhou: "手杀谯周",
  qiaozhou_prefix: "手杀",
  zhiming: "知命",
  zhiming_info: "准备阶段开始时或弃牌阶段结束时，你摸一张牌，然后可以将一张牌置于牌堆顶。",
  xingbu: "星卜",
  xingbu_info: "结束阶段，你可以亮出牌堆顶的三张牌，然后你可以根据X值（X为这三张牌中红色牌的数量），令一名其他角色获得对应的效果直到其下回合结束：①三张：其摸牌阶段多摸两张牌，使用【杀】的次数上限+1。②两张：其使用【杀】的次数上限-1，跳过弃牌阶段。③小于两张：其于准备阶段开始时弃置一张手牌。",
  xin_sunluban: "手杀界孙鲁班",
  xin_sunluban_prefix: "手杀界",
  xinzenhui: "谮毁",
  xinzenhui_info: "出牌阶段限一次。当你使用【杀】或黑色普通锦囊牌指定唯一目标时，你可选择另一名能成为此牌目标的其他角色并选择一项：①令其也成为此牌的目标。②获得其一张牌，然后将此牌的使用者改为该角色。",
  xinjiaojin: "骄矜",
  xinjiaojin_info: "当你受到男性角色造成的伤害时，你可以弃置一张装备牌并防止此伤害。",
  xin_caozhen: "手杀界曹真",
  xin_caozhen_prefix: "手杀界",
  discretesidi: "司敌",
  discretesidi_info: "①当你使用的不为延时锦囊牌的牌结算结束后，你可选择一名R内不存在以a为第一序偶的二元序偶的其他角色a，并选择一名角色b，在关系R内建立二元序偶&lt;a,b&gt;（b对其他角色不可见）。②一名角色a使用不为延时锦囊牌的牌指定b为目标时，若(aRb)∧(此牌目标数为1)为真，则{你从R内移除&lt;a,b&gt;，且：若b为你，你摸一张牌；若b不为你，你可选择：⒈取消此牌的目标，然后若场上没有处于濒死状态的角色，则你对a造成1点伤害。⒉摸两张牌}；否则{你清除R内以a为第一元素的二元序偶}。",
  mbsidi: "司敌",
  mbsidi_info: "当你使用除延时锦囊牌外的牌结算完毕后，你可以选择一名未指定“司敌”目标的其他角色，并为其指定一名“司敌”目标角色（仅你可见）。其使用的下一张除延时锦囊牌外的牌指定目标后，清除你为其指定的“司敌”目标角色，若此时其使用此牌仅指定“司敌”目标为唯一目标，且目标：为你，你摸一张牌；不为你，你可以选择一项：⒈取消此牌目标，然后若场上没有处于濒死的角色，你对其造成1点伤害；⒉摸两张牌。",
  fuqian: "傅佥",
  jueyong: "绝勇",
  jueyong_info: "锁定技。①当你不因〖绝勇〗成为唯一牌的目标时，若此牌不为转化牌且对应的实体牌牌数为1且不为【桃】或【酒】且你的“绝”数小于你的体力值，则你将此牌置于你的武将牌上，称为“绝”，且取消此牌的目标。②结束阶段开始时，若你有“绝”，则你令所有“绝”的原使用者依次对你使用所有“绝”，将无法使用的“绝”置入弃牌堆。",
  poxiang: "破降",
  poxiang_info: "出牌阶段限一次。你可以将一张牌交给一名其他角色。你摸三张牌（不计入本回合的手牌上限），移去所有“绝”并失去1点体力。",
  mayuanyi: "马元义",
  jibing: "集兵",
  jibing_info: "①摸牌阶段开始时，若你的“兵”数小于势力数，则你可以改为将牌堆顶的两张牌置于你的武将牌上，称为“兵”。②你可以将一张“兵”当做【杀】或【闪】使用或打出。",
  wangjing: "往京",
  wangjing_info: "锁定技。当你因〖集兵〗而使用或打出牌时，若对方是场上体力值最高的角色，则你摸一张牌。",
  moucuan: "谋篡",
  moucuan_info: `觉醒技。准备阶段，若你的“兵”数不小于势力数，则你减1点体力上限并获得${get.poptip("binghuo")}。`,
  binghuo: "兵祸",
  binghuo_info: "一名角色的结束阶段开始时，若你本回合内因〖集兵〗而使用或打出过牌，则你可令一名角色判定。若判定结果为黑色，则你对其造成1点雷属性伤害。",
  yanpu: "阎圃",
  huantu: "缓图",
  huantu_info: "每轮限一次。一名角色的摸牌阶段开始前，若其在你攻击范围内，则你可以交给其一张牌并令其跳过此阶段。然后你于此回合的结束阶段选择一项：①令其回复1点体力并摸两张牌。②你摸三张牌，然后交给其两张手牌。",
  bihuo: "避祸",
  bihuo_info: "限定技。一名角色脱离濒死状态时，你可以令其摸三张牌，然后其他角色计算至其的距离时+X直到本轮结束（X为角色数）。",
  sunhanhua: "手杀孙寒华",
  sunhanhua_prefix: "手杀",
  chongxu: "冲虚",
  chongxu_info: "出牌阶段限一次，你可以随机演奏一首音乐，并根据完成度来获得相应的分数（至多五分）。然后你可修改〖妙剑〗或〖莲华〗（消耗3分），并使用剩余的分数进行摸牌（每张2分）。",
  chongxu_faq: "目前的曲库",
  chongxu_faq_info: "　<br>《鸟之诗》- 折户伸治<br>《竹取飛翔　～ Lunatic Princess》- ZUN<br>《ignotus》- ak+q<br>《Super Mario 3D World Theme》- 横田真人<br>《只因你太美》- SWIN-S<br>《Croatian Rhapsody》- Maksim<br>《罗刹海市》- 刀郎<br>《Pigstep (Stereo Mix)》- Lena Raine",
  miaojian: "妙剑",
  miaojian_info: "出牌阶段限一次。你可将一张【杀】当做刺【杀】使用，或将一张锦囊牌当做【无中生有】使用。",
  miaojian1: "妙剑·改",
  miaojian1_info: "出牌阶段限一次。你可将一张基本牌当做刺【杀】使用，或将一张非基本牌当做【无中生有】使用。",
  miaojian2: "妙剑·极",
  miaojian2_info: "出牌阶段限一次。你可视为使用一张刺【杀】或【无中生有】。",
  shhlianhua: "莲华",
  shhlianhua_info: "当你成为【杀】的目标时，你摸一张牌。",
  shhlianhua1: "莲华·改",
  shhlianhua1_info: "当你成为【杀】的目标时，你摸一张牌。然后你进行判定，若结果为黑桃，则取消此目标。",
  shhlianhua2: "莲华·极",
  shhlianhua2_info: "当你成为【杀】的目标时，你摸一张牌。然后此【杀】的使用者选择一项：①弃置一张牌。②取消此目标。",
  re_yufan: "手杀界虞翻",
  re_yufan_prefix: "手杀界",
  rezongxuan: "纵玄",
  rezongxuan_place: "纵玄",
  rezongxuan_info: "当你的牌因弃置而进入弃牌堆后，你可以将其中任意张牌以任意顺序置于牌堆顶。出牌阶段限一次，你可以摸一张牌，然后将一张牌置于牌堆顶。",
  sp_maojie: "毛玠",
  bingqing: "秉清",
  bingqing_info: "当你于出牌阶段内使用的牌结算完毕后，若你于本阶段内此前使用的所有牌的花色与此牌花色均不相同，则你可根据X的值执行对应效果：为2，你令一名角色摸两张牌；为3，你弃置一名角色区域内的一张牌；为4，你对一名其他角色造成1点伤害。（X为你于本阶段内此前使用的所有牌包含的花色数+1）",
  yingfeng: "迎奉",
  yingfeng_info: "准备阶段，你可以令一名角色获得“奉”标记并移除场上所有其他的“奉”标记。有“奉”标记的角色使用牌没有距离限制。",
  xin_sunxiu: "手杀界孙休",
  xin_sunxiu_prefix: "手杀界",
  mobileyanzhu: "宴诛",
  mobileyanzhu_info: "出牌阶段限一次，你可以令一名有牌的其他角色选择一项：①你获得其装备区里所有的牌，然后你失去技能〖宴诛〗并修改技能〖兴学〗。②你获得其区域里的一张牌。",
  mobilexingxue: "兴学",
  mobilexingxue_info: "结束阶段开始时，你可以令至多X名角色依次摸一张牌并将一张牌置于牌堆顶（X为你的体力值）。",
  mobilexingxuex: "兴学·改",
  mobilexingxuex_info: "结束阶段开始时，你可以令至多X名角色依次摸一张牌并将一张牌置于牌堆顶或交给一名其他目标角色（X为你的体力上限）。",
  re_wuguotai: "手杀界吴国太",
  re_wuguotai_prefix: "手杀界",
  reganlu: "甘露",
  reganlu_info: "出牌阶段限一次，你可以选择装备区牌数之差的绝对值不大于X的两名角色或包含你在内的两名角色，然后交换这两名角色装备区内的牌。（X为你已损失的体力值）",
  taoqian: "手杀陶谦",
  taoqian_prefix: "手杀",
  miheng: "手杀祢衡",
  miheng_prefix: "手杀",
  re_gaoshun: "手杀界高顺",
  re_gaoshun_prefix: "手杀界",
  peixiu: "手杀裴秀",
  peixiu_prefix: "手杀",
  xingtu: "行图",
  xingtu1: "倍数",
  xingtu2: "约数",
  xingtu_info: "锁定技。你使用点数为X的倍数的牌无次数限制，你使用点数为X的约数的牌时摸一张牌（X为你本局游戏使用的上一张牌的点数）。",
  juezhi: "爵制",
  juezhi_info: "出牌阶段，你可以弃置至少两张牌，然后从牌堆中获得一张点数为Y的牌（Y为这些牌的点数和对13取余，余数为0时Y取13）。",
  sp_jianggan: "手杀蒋干",
  sp_jianggan_prefix: "手杀",
  spdaoshu: "盗书",
  spdaoshu_info: "每轮限一次。一名敌方角色的出牌阶段开始时，若其有手牌，则你可以令其视为使用一张【酒】。其须声明一个基本牌的牌名，然后你判断其手牌区内是否有该牌名的牌。若你判断正确，则你获得其两张手牌。",
  spdaoshu_info_identity: "每轮限一次。一名其他角色的出牌阶段开始时，若其有手牌，则你可以令其视为使用一张【酒】。其须声明一个基本牌的牌名，然后你判断其手牌区内是否有该牌名的牌。若你判断正确，则你获得其两张手牌。",
  spdaoshu_info_guozhan: "每轮限一次。一名其他角色的出牌阶段开始时，若其有手牌，则你可以令其视为使用一张【酒】。其须声明一个基本牌的牌名，然后你判断其手牌区内是否有该牌名的牌。若你判断正确，则你获得其两张手牌。",
  mbdaoshu: "盗书",
  mbdaoshu_info: "出牌阶段限一次，你可以选择一名手牌数不少于两张的敌方角色，其随机获得三个牌名并将一张手牌的牌名伪装成其中一个与原牌名不同的牌名，然后你和队友观看其手牌并猜测其伪装的手牌，猜对的角色对其造成1点伤害，猜错的角色随机弃置两张手牌（手牌数不足两张则改为失去1点体力）。",
  mbdaoshu_info_identity: "出牌阶段限一次，你可以选择一名手牌数不少于两张的其他角色，其随机获得三个牌名并将一张手牌的牌名伪装成其中一个与原牌名不同的牌名，然后你观看其手牌并猜测其伪装的手牌。若猜中，你对其造成1点伤害；若猜错，你随机弃置两张手牌（手牌数不足两张则改为失去1点体力）。",
  mbdaoshu_info_guozhan: "出牌阶段限一次，你可以选择一名手牌数不少于两张的其他角色，其随机获得三个牌名并将一张手牌的牌名伪装成其中一个与原牌名不同的牌名，然后你观看其手牌并猜测其伪装的手牌。若猜中，你对其造成1点伤害；若猜错，你随机弃置两张手牌（手牌数不足两张则改为失去1点体力）。",
  spdaizui: "戴罪",
  spdaizui2: "戴罪",
  spdaizui_info: "限定技。当你受到伤害值不小于体力值的伤害时，你可防止此伤害并将此伤害渠道对应的所有实体牌置于伤害来源的武将牌上，称为“释”。本回合结束时，其获得所有“释”。",
  re_caiwenji: "手杀界蔡琰",
  re_caiwenji_prefix: "手杀界",
  re_bulianshi: "手杀界步练师",
  re_bulianshi_prefix: "手杀界",
  reanxu: "安恤",
  reanxu_info: "出牌阶段限一次，你可以选择两名其他角色，令其中一名角色获得另一名角色的一张牌。若以此法移动的牌不来自装备区，则你摸一张牌。然后你可以令二者中手牌数较少的一名角色摸一张牌。",
  xin_jushou: "手杀界沮授",
  xin_jushou_prefix: "手杀界",
  xinjianying: "渐营",
  xinjianying_info: "①当你使用与你使用的上一张牌点数或花色相同的牌时，你可以摸一张牌。②出牌阶段限一次，你可以将一张牌当做任意基本牌使用。若你于此阶段内使用的上一张牌有花色，则此牌的花色视为上一张牌的花色。",
  re_xunyu: "手杀界荀彧",
  re_xunyu_prefix: "手杀界",
  rejieming: "节命",
  rejieming_info: "当你受到1点伤害后，你可以令一名角色摸两张牌。然后若其手牌数小于体力上限，则你摸一张牌。",
  xin_quancong: "手杀界全琮",
  xin_quancong_prefix: "手杀界",
  sbyaoming: "邀名",
  sbyaoming_info: "蓄力技（2/4）。①当你受到1点伤害后，你获得1点蓄力值。②出牌阶段或当你受到伤害后，你可消耗1点蓄力值并选择一项：⒈弃置一名手牌数不小于你的其他角色的一张牌。⒉令一名手牌数不大于你的角色摸一张牌。若你上次发动〖邀名②〗时未获得过蓄力值且你选择的选项和上次不同，则你获得1点蓄力值。",
  ruanhui: "阮慧",
  mingcha: "明察",
  mingcha_info: "摸牌阶段开始时，你亮出牌堆顶的三张牌。若这三张牌中有点数小于9的牌，则你可以放弃摸牌并获得这些牌，然后你可以获得一名其他角色的随机一张牌。",
  jingzhong: "敬重",
  jingzhong_info: "弃牌阶段结束时，若你于本阶段内弃置过至少两张黑色牌，则你可以选择一名其他角色并获得如下效果直到其回合结束：每阶段限三次，其于出牌阶段内使用的牌结算结束后，你获得此牌对应的实体牌。",
  xin_mamidi: "手杀马日磾",
  xin_mamidi_prefix: "手杀",
  chengye: "承业",
  chengye_info: `锁定技。①其他角色使用的非转化牌结算结束后，或其他角色的装备牌和延时锦囊牌进入弃牌堆后，或有延时锦囊牌因其他角色执行判定阶段的流程而进入弃牌堆后，若你的${get.poptip({
    id: "chengye_liujing",
    name: "六经",
    type: "character",
    info: '<span style="font-family:yuanli"><li>《诗经》：伤害类锦囊牌<br><li>《尚书》：基本牌<br><li>《仪礼》：无懈可击<br><li>《易经》：无中生有<br><li>《乐经》：乐不思蜀<br><li>《春秋》：装备牌</span>'
  })}有空缺的位置可以置入此牌，则你将此牌置于你武将牌上，填补${get.poptip("chengye_liujing")}的对应位置。②出牌阶段开始时，若你的${get.poptip("chengye_liujing")}没有空缺的位置，则你获得所有${get.poptip("chengye_liujing")}。`,
  buxu: "补续",
  buxu_info: `出牌阶段，若你拥有${get.poptip("chengye")}且你的${get.poptip("chengye_liujing")}中有空缺的位置，则你可以弃置X+1张牌并选择一种空缺的${get.poptip("chengye_liujing")}（X为你本阶段内发动过〖补续〗成功填补${get.poptip("chengye_liujing")}的次数）。系统从牌堆中检索一张对应的牌，然后你将此牌置于你武将牌上，填补${get.poptip("chengye_liujing")}的对应位置。`,
  re_dianwei: "手杀界典韦",
  re_dianwei_prefix: "手杀界",
  liuye: "手杀刘晔",
  liuye_prefix: "手杀",
  sp_caosong: "手杀曹嵩",
  sp_caosong_prefix: "手杀",
  yijin: "亿金",
  yijin_info: "锁定技。①游戏开始时，你获得“膴仕”、“金迷”、“贾凶”、“通神”、“拥蔽”、“厚任”各1枚（均称为“金”）。②出牌阶段开始时，你选择一名没有“金”的其他角色。你交给其1枚“金”，且令其获得对应效果。③一名角色的回合结束后，若其有你交给其的“金”，其移去此“金”。④当你死亡时，移去场上所有你交出的“金”。⑤回合开始时，若你没有“金”，你死亡。",
  yijin_wushi: "膴仕",
  yijin_wushi_info: "锁定技。①摸牌阶段，你多摸四张牌。②你使用【杀】的次数上限+1。",
  yijin_jinmi: "金迷",
  yijin_jinmi_info: "锁定技。回合开始时，你跳过下一个出牌阶段和弃牌阶段。",
  yijin_guxiong: "贾凶",
  yijin_guxiong_info: "锁定技。①出牌阶段开始时，你失去1点体力。②你的手牌上限-3。",
  yijin_tongshen: "通神",
  yijin_tongshen_info: "锁定技。当你受到非雷电伤害时，防止之。",
  yijin_yongbi: "拥蔽",
  yijin_yongbi_info: "锁定技。准备阶段，你跳过下一个摸牌阶段。",
  yijin_houren: "厚任",
  yijin_houren_info: "锁定技。回合结束时，你回复3点体力。",
  guanzong: "惯纵",
  guanzong_info: "出牌阶段限一次。你可以令一名其他角色视为对另一名其他角色造成过1点伤害。",
  yangfu: "杨阜",
  jiebing: "借兵",
  jiebing_info: "锁定技。当你受到伤害后，你选择来源外的一名其他角色，随机获得其一张牌并展示。若此牌为装备牌，你使用之。",
  hannan: "扞难",
  hannan_info: "出牌阶段限一次。你可以与一名角色拼点，赢的角色对没赢的角色造成1点伤害。",
  xin_wuyi: "手杀界吴懿",
  xin_wuyi_prefix: "手杀界",
  sbbenxi: "奔袭",
  sbbenxi_info: "出牌阶段开始时，你可以弃置至少一张牌，然后你于此阶段获得如下效果：①你至其他角色距离-X；②当你使用的下一张基本牌或普通锦囊牌A选择目标后，你可以额外指定至多X名距离为1的角色为目标；③牌A结算结束后，若此牌造成过伤害，你摸五张牌（X为你以此法弃置的牌数）。",
  xin_zhuzhi: "手杀界朱治",
  xin_zhuzhi_prefix: "手杀界",
  sbanguo: "安国",
  sbanguo_info: "①游戏开始时，你令一名其他角色获得1枚“安国”标记（有“安国”的角色手牌上限基数等于体力上限）。②出牌阶段开始时，你可以将一名有“安国”的角色的所有“安国”移动给一名本局游戏未获得过“安国”的其他角色。③当你受到伤害时，若有有“安国”的角色且伤害值不小于你的体力值且此伤害没有来源或来源没有“安国”，防止此伤害。④一名角色进入濒死状态时，若其有你因〖安国①〗获得的“安国”，你移去其该“安国”，令其将体力回复至1点。然后你选择一项：1.若你的体力值大于1，你失去体力至1点；2.若你的体力上限大于1，你将体力上限减至1。最后你令其获得1点护甲。",
  wangjun: "手杀王濬",
  wangjun_prefix: "手杀",
  zhujian: "筑舰",
  zhujian_info: "出牌阶段限一次。你可以令至少两名装备区里有牌的角色各摸一张牌。",
  duansuo: "断索",
  duansuo_info: "出牌阶段限一次。你可以重置任意名处于连环状态的角色，然后对这些角色各造成1点火焰伤害。",
  sp_pengyang: "手杀彭羕",
  sp_pengyang_prefix: "手杀",
  spdaming: "达命",
  spdaming_info: "①游戏开始时，你获得3点“达命”值。②其他角色A的出牌阶段限一次。其可以交给你一张牌，然后你选择另一名其他角色B。若B有与此牌相同类型的牌，其将一张该类型的牌交给A，你获得1点“达命”值；否则你将此牌交给A。",
  spxiaoni: "嚣逆",
  spxiaoni_info: "①出牌阶段限一次。若你的“达命”值大于0，你可以将一张牌当任意一种【杀】或伤害类锦囊牌使用。然后你减少等同于此牌指定目标数的“达命”值。②你的手牌上限基数为“达命”值和体力值中较小的一项，【杀】的次数上限为其中较大的一项。",
  xin_zhuhuan: "手杀界朱桓",
  xin_zhuhuan_prefix: "手杀界",
  xinpingkou: "平寇",
  xinpingkou_info: "回合结束时，你可以对至多X名其他角色各造成1点伤害（X为你本回合跳过的阶段数）。然后你从牌堆中获得一张装备牌。",
  xin_caoxiu: "手杀界曹休",
  xin_caoxiu_prefix: "手杀界",
  xinqingxi: "倾袭",
  xinqingxi_info: "每回合限一次。当你对其他角色造成伤害时，你可以令其选择一项：1.弃置X张手牌（X为4减去你至其的距离，至少为1）；2.令此伤害+1。",
  xin_zhoutai: "手杀界周泰",
  xin_zhoutai_prefix: "手杀界",
  re_yanwen: "手杀界颜良文丑",
  re_yanwen_prefix: "手杀界",
  qianzhao: "手杀牵招",
  qianzhao_prefix: "手杀",
  mbshihe: "势吓",
  mbshihe_info_identity: "出牌阶段限一次。你可以与一名角色拼点。若你：赢，当其于其下回合结束前对你造成伤害时，取消之；没赢，你随机弃置一张牌。",
  mbshihe_info: "出牌阶段限一次。你可以与一名角色拼点。若你：赢，当其于其下回合结束前对你和你的友方角色造成伤害时，取消之；没赢，你随机弃置一张牌。",
  mbzhenfu: "镇抚",
  mbzhenfu_info: "结束阶段，若你本回合因弃置失去过牌，你可以令一名其他角色获得1点护甲。",
  shichangshi: "十常侍",
  mbdanggu: "党锢",
  mbdanggu_info: "锁定技。①游戏开始时，你获得十张“常侍”牌，然后你进行一次结党。②当你修整结束后，你进行一次结党并摸一张牌。③若你有亮出的“常侍”牌，你视为拥有这些牌的技能。",
  mbdanggu_faq: "关于结党",
  mbdanggu_faq_info: "<br>系统随机选择一张未亮出过的“常侍”牌，然后选择四张未亮出过的“常侍”牌。你观看前者，然后从后者中选择一名认可前者的“常侍”牌。然后若此时不为双将模式，你将这两张武将牌作为你的武将牌（不移除原有技能）；否则你获得这两张武将牌上的技能。",
  mbdanggu_faq2: "关于认可",
  mbdanggu_faq2_info: "<br>双向不认可常侍为固定组合：<br><li>郭胜、段珪<br><li>韩悝、毕岚<br>单向不认可常侍为系统随机分配。<br>每次结党至多存在一张不认可主将的常侍牌，且若此次结党仅有一张常侍牌，则不会存在不认可情况。",
  mbmowang: "殁亡",
  mbmowang_info: "锁定技。①当你死亡前，若你有未亮出的“常侍”牌且体力上限大于0，你将死亡改为修整至你的下个回合开始前，然后你复原武将牌，且不于此次死亡事件中进行展示身份牌、检测游戏胜利条件与执行奖惩的流程。②回合结束后，你死亡。",
  mbmowang_faq: "关于修整",
  mbmowang_faq_info: "<br>将武将牌移出游戏（视为你存活）。当该角色修整结束，其移回游戏。",
  scs_zhangrang: "张让",
  scstaoluan: "滔乱",
  scstaoluan_info: "出牌阶段限一次。你可以将一张牌当任意一种基本牌或普通锦囊牌使用。",
  scs_zhaozhong: "赵忠",
  scschiyan: "鸱咽",
  scschiyan_info: "①当你使用【杀】指定目标后，你可以将其的一张牌置于其武将牌上，然后其于当前回合结束时获得这些牌。②当你因执行【杀】的效果对一名角色造成伤害时，若该角色的手牌数和装备区内的牌数均不大于你，此伤害+1。",
  scs_sunzhang: "孙璋",
  scszimou: "自谋",
  scszimou_info: "锁定技。出牌阶段，当你使用第二/四/六张牌时，你从牌堆中获得一张【酒】/【杀】/【决斗】。",
  scs_bilan: "毕岚",
  scspicai: "庀材",
  scspicai_info: "出牌阶段限一次。你可进行判定牌不置入弃牌堆的判定。若判定结果与本次发动技能时的其他判定结果的花色均不相同，则你可以重复此流程。然后你将所有位于处理区的判定牌交给一名角色。",
  scs_xiayun: "夏恽",
  scsyaozhuo: "谣诼",
  scsyaozhuo_info: "出牌阶段限一次。你可以与一名角色拼点，若你赢，其跳过下一个摸牌阶段；若你没赢，你弃置两张牌。",
  scs_hankui: "韩悝",
  scsxiaolu: "宵赂",
  scsxiaolu_info: "出牌阶段限一次。你可以摸两张牌，然后选择一项：1.弃置两张手牌；2.将两张手牌交给一名其他角色。",
  scs_lisong: "栗嵩",
  scskuiji: "窥机",
  scskuiji_info: "出牌阶段限一次。你可以观看一名其他角色的手牌，然后弃置你与其的共计四张花色各不相同的手牌。",
  scs_duangui: "段珪",
  scschihe: "叱吓",
  scschihe_info: "当你使用【杀】指定唯一目标后，你可亮出牌堆顶的两张牌，令此【杀】的伤害值基数+X（X为亮出牌中花色与此【杀】相同的牌数），且目标角色不能使用亮出牌包含的花色的牌响应此【杀】。",
  scs_guosheng: "郭胜",
  scsniqu: "逆取",
  scsniqu_info: "出牌阶段限一次。你可以对一名角色造成1点火焰伤害。",
  scs_gaowang: "高望",
  scsanruo: "安弱",
  scsanruo_info: "你可以将一张♥牌当【桃】、♦牌当火【杀】、♣牌当【闪】、♠牌当【无懈可击】使用。当你以此法使用或打出【杀】或【闪】时，你可以获得对方的一张牌；当你以此法使用【桃】时，你可以获得一名其他角色的一张牌；当你以此法使用【无懈可击】时，你可以获得此牌响应的普通锦囊牌的使用者的一张牌。",
  scsmiaoyu: "妙语",
  scsmiaoyu_info: "你可以将至多两张相同花色的牌按照以下规则使用或打出：♦牌当作火【杀】，♥牌当作【桃】，♣牌当作【闪】，♠牌当作【无懈可击】。若你以此法使用了两张红色牌，则此牌回复值或伤害值+1。若你以此法使用了两张黑色牌，则你弃置当前回合角色一张牌。",
  re_xiaoqiao: "手杀界小乔",
  re_xiaoqiao_prefix: "手杀界",
  xin_sunliang: "手杀孙亮",
  xin_sunliang_prefix: "手杀",
  xinkuizhu: "溃诛",
  xinkuizhu_info: "弃牌阶段结束后，你可以选择一项：1.令至多X名角色各摸一张牌。2.对任意名体力值之和为X的角色造成1点伤害，若你以此法选择的角色数不小于2，你失去1点体力。（X为你此阶段弃置的牌数）",
  xinzhizheng: "掣政",
  xinzhizheng_info: "锁定技，你的出牌阶段内，攻击范围内不包含你的其他角色不能成为你使用牌的目标。出牌阶段结束时，若你本阶段内使用的牌数小于这些角色的数量，则你弃置其中一名角色的一张牌。",
  xinlijun: "立军",
  xinlijun_info: "主公技，其他吴势力角色于其回合内使用【杀】结算完毕后，其可以将此【杀】对应的实体牌交给你，然后你可以令其摸一张牌。",
  xin_zhangyi: "手杀界张嶷",
  xin_zhangyi_prefix: "手杀界",
  xinwurong: "怃戎",
  xinwurong_info: "出牌阶段限一次，你可以与一名其他角色进行谋弈：<br><li>若你选择“镇压”且其选择“反抗”，你对其造成1点伤害，然后你摸一张牌。<br><li>若你选择“安抚”且其选择“归顺”，其须交给你两张牌（若其牌数不足两张，则改为令其跳过其下个摸牌阶段）。<br><li>若你选择“镇压”且其选择“归顺”，你获得其一张牌，然后你交给其两张牌。<br><li>若你选择“安抚”且其选择“反抗”，你受到1点伤害，然后你摸一张牌。",
  xin_guozhao: "手杀郭照",
  xin_guozhao_prefix: "手杀",
  yichong: "易宠",
  yichong_info: "①准备阶段，你可以选择一名其他角色并选择一个花色，然后你获得其所有此花色的牌，移除场上的所有“雀”标记，令其获得“雀”标记直到你的下个回合开始。②拥有“雀”标记的角色获得你最后一次发动〖易宠①〗选择的花色的牌后，你获得这些牌（你至多通过每个“雀”得到一张牌）。",
  wufei: "诬诽",
  wufei_info: "若场上存在拥有“雀”标记的角色A，则：①你即将由【杀】或伤害类普通锦囊牌造成的伤害的来源为A。②当你受到伤害后，若A的体力值大于3且大于你，则你可以令A受到1点无来源伤害。",
  xin_yuanshao: "手杀界袁绍",
  xin_yuanshao_prefix: "手杀界",
  re_baosanniang: "手杀鲍三娘",
  re_baosanniang_prefix: "手杀",
  re_liushan: "手杀界刘禅",
  re_liushan_prefix: "手杀界",
  re_sunben: "界孙笨",
  re_sunben_prefix: "界",
  re_zhangzhang: "手杀界张昭张纮",
  re_zhangzhang_prefix: "手杀界",
  re_caozhi: "手杀界曹植",
  re_caozhi_prefix: "手杀界",
  re_sunjian: "手杀界孙坚",
  re_sunjian_prefix: "手杀界",
  sunru: "手杀孙茹",
  sunru_prefix: "手杀",
  pangdegong: "手杀庞德公",
  pangdegong_prefix: "手杀",
  zhaotongzhaoguang: "手杀赵统赵广",
  zhaotongzhaoguang_prefix: "手杀",
  re_liru: "手杀界李儒",
  re_liru_prefix: "手杀界",
  re_chenqun: "手杀界陈群",
  re_chenqun_prefix: "手杀界",
  old_yuanshu: "手杀袁术",
  old_yuanshu_prefix: "手杀",
  baoxin: "鲍信",
  mutao: "募讨",
  mutao_info: "出牌阶段限一次。你可以选择一名角色，令其将手牌中所有的【杀】置于武将牌上，然后将这些牌依次随机交给其下家开始的每一名角色。然后其对最后一名以此法获得【杀】的角色A造成X点伤害（X为A手牌中【杀】的数量且至多为2）。",
  yimou: "毅谋",
  yimou_info: "当与你距离1以内的一名角色受到伤害后，你可以选择一项：1.令其从牌堆中获得一张【杀】；2.令其将一张手牌交给另一名角色，然后摸一张牌。",
  jiangji: "蒋济",
  jilun: "机论",
  jilun_info: "当你受到伤害后，若你拥有技能〖急筹〗，你可以摸X张牌，或视为使用一张未被〖机论〗记录过的基本牌或〖急筹①〗记录过且未被〖机论〗记录过的合法目标数不大于X的普通锦囊牌并记录此牌（X为〖急筹①〗记录数且至少为1，至多为3）。",
  liwei: "李遗",
  jiaohua: "教化",
  jiaohua_backup: "教化",
  jiaohua_info: "出牌阶段限两次，你可以选择一个未被〖教化〗记录过的牌的类型，令一名角色从牌堆中获得一张此类型的牌，然后记录此类型，若基本、锦囊、装备均已被你发动〖教化〗记录，则你清空〖教化〗记录。",
  laimin: "来敏",
  laishou: "来寿",
  laishou_info: "锁定技。①当你受到伤害值大于等于你的体力值的伤害时，若你的体力上限小于9，你防止此伤害并增加等量体力上限。②准备阶段，若你的体力上限不小于9，你死亡。",
  luanqun: "乱群",
  luanqun_info: "出牌阶段限一次，若你有手牌，则你可以令所有有手牌的角色同时展示一张手牌，然后你可以获得其中至多两张与你展示的牌颜色相同的展示牌，所有本次展示牌颜色与你展示的牌颜色不同的角色的下个出牌阶段使用的第一张【杀】只能对你使用，且你无法响应其于回合内使用的【杀】。",
  luanqun_info_identity: "出牌阶段限一次，若你有手牌，则你可以令所有有手牌的角色同时展示一张手牌，然后你可以获得其中至多四张与你展示的牌颜色相同的展示牌，所有本次展示牌颜色与你展示的牌颜色不同的角色的下个出牌阶段使用的第一张【杀】只能对你使用，且你无法响应其于回合内使用的【杀】。",
  xin_wuban: "吴班",
  xinjintao: "进讨",
  xinjintao_info: "锁定技，你使用【杀】无距离限制且次数上限+1。你于出牌阶段内使用的第一张【杀】不可被响应，第二张【杀】伤害+1。",
  xinlianhuan: "连环",
  xinlianhuan_info: "你可以将一张♣手牌当【铁索连环】使用或重铸。你使用【铁索连环】选择目标后，可以给此牌增加一个目标。",
  mb_sunluyu: "手杀孙鲁育",
  mb_sunluyu_prefix: "手杀",
  mbmumu: "穆穆",
  mbmumu_info: "出牌阶段开始时，你可以选择一项：1.弃置场上的一张装备牌；2.获得场上的一张防具牌，然后你本回合不能使用或打出【杀】。",
  mbmeibu: "魅步",
  mbmeibu_info: `其他角色的出牌阶段开始时，若你在其攻击范围内，你可以弃置一张牌，令该角色于本回合内获得${get.poptip("mbzhixi")}。若你以此法弃置的牌不是【杀】或黑色锦囊牌，则本回合其与你的距离视为1。`,
  mbzhixi: "止息",
  mbzhixi_info: "锁定技。出牌阶段，若你于此阶段使用过的牌数不小于X，你不能使用牌（X为你的体力值）；当你使用锦囊牌时，你结束此阶段。",
  yanxiang: "阎象",
  kujian: "苦谏",
  kujian_info: "出牌阶段限一次，你可以将至多两张手牌称为“谏”并交给一名其他角色，然后你获得以下效果：其他角色失去“谏”后，若为因使用或打出而失去，你与其各摸两张牌；否则你与其各弃置一张牌。",
  ruilian: "睿敛",
  ruilian_info: "每轮开始时，你可以选择一名角色。其下回合结束时，若其本回合弃置过牌，你可以选择其本回合弃置过的一种类别，你与其各从弃牌堆中获得一张此类别的牌。",
  mb_xianglang: "手杀向朗",
  mb_xianglang_prefix: "手杀",
  naxue: "纳学",
  naxue_info: "你可以跳过出牌阶段。若如此做，你可以弃置任意张牌并摸等量的牌，然后你可以交给至多两名其他角色各一张手牌。",
  yijie: "遗诫",
  yijie_info: "锁定技。当你死亡时，你令所有其他角色将体力回复或失去至X（X为所有其他角色的体力之和除以所有其他角色数，向下取整，且X至少为1）。",
  mb_chengui: "手杀陈珪",
  mb_chengui_prefix: "手杀",
  guimou: "诡谋",
  guimou_info: "锁定技。游戏开始时/回合结束时，你随机/须选择以下一项直到你的下个准备阶段：①记录场上期间角色使用牌数；②记录期间场上角色弃置牌数；③记录期间场上角色获得牌数。准备阶段，你可以选择一名场上对应记录数值最少的其他角色，观看其手牌并选择其中一张牌，然后你将此牌交给另一名其他角色或弃置此牌。",
  zhouxian: "州贤",
  zhouxian_info: "锁定技。当你成为其他角色使用的伤害类卡牌的目标时，你亮出牌堆顶的三张牌，然后其须选择一项：①弃置一张与亮出牌之一类别相同的牌；②取消你为此牌的目标。",
  mb_huban: "手杀胡班",
  mb_huban_prefix: "手杀",
  mbyilie: "义烈",
  mbyilie2: "义烈",
  mbyilie3: "义烈",
  mbyilie_info: "锁定技。①游戏开始时，你选择一名其他角色，然后你获得以下效果：其受到伤害时，若你没有“烈”，则你获得等同于伤害值的“烈”标记，然后防止此伤害；其对其他角色造成伤害后，你回复1点体力。②结束阶段，若你拥有“烈”标记，你摸一张牌并失去X点体力，然后移去所有“烈”（X为你拥有的“烈”标记数）。",
  muludawang: "木鹿大王",
  shoufa: "兽法",
  shoufa_info: "①当你于一回合首次造成伤害后，你可以选择一名距离2以内的角色。②每回合限五次，当你受到伤害后，你可以选择一名与你距离不小于2的角色。其随机执行以下一项：豹，其受到1点无来源伤害；鹰，你随机获得其一张牌；熊，你随机弃置其装备区的一张牌；兔，其摸一张牌。",
  shoufa_info_doudizhu: "①当你于一回合首次造成伤害后，你可以选择一名距离1以内的角色。②每回合限五次，当你受到伤害后，你可以选择一名与你距离不小于1的角色。其随机执行以下一项：豹，其受到1点无来源伤害；鹰，你随机获得其一张牌；熊，你随机弃置其装备区的一张牌；兔，其摸一张牌。",
  yuxiang: "御象",
  yuxiang_info: "锁定技，若你有护甲值，则：①你计算与其他角色的距离-1，其他角色计算与你的距离+1；②当你受到火焰伤害时，此伤害+1。",
  zhoulin: "咒鳞",
  zhoulin_info: `限定技，出牌阶段，你可以获得2点护甲值，然后选择一个“兽法”效果，你发动${get.poptip("shoufa")}的执行效果改为你选择的效果直到你的下个回合开始。`,
  xin_huojun: "手杀霍峻",
  xin_huojun_prefix: "手杀",
  sidai: "伺怠",
  sidai_info: "限定技，出牌阶段，你可以将手牌区内的所有基本牌当做【杀】使用。若此牌对应的实体牌中：包含【闪】，则目标角色成为此牌的目标后，需弃置一张基本牌，否则不可响应此牌；包含【桃】，则当目标角色受到此牌的伤害后，其减1点体力上限。",
  jieyu: "竭御",
  jieyu_info: "结束阶段，你可以从弃牌堆中获得共X张不同牌名的基本牌（X为3-你上次发动〖竭御〗至今你成为其他角色使用伤害类卡牌目标的次数，且X至少为1）。",
  //jieyu_info_identity: "结束阶段，你可以从弃牌堆中获得共X张不同牌名的基本牌（X为3-你上次发动〖竭御〗至今你成为其他角色使用伤害类卡牌目标的次数，且X至少为1）。",
  yangfeng: "杨奉",
  mbxuetu: "血途",
  mbxuetu_info: "转换技。出牌阶段限一次，阳：你可以令一名角色回复1点体力；阴：你可以令一名角色摸两张牌。",
  mbxuetu_achieve: "血途·成功",
  mbxuetu_achieve_info: "出牌阶段各限一次。1.你可以令一名角色回复1点体力；2.你可以令一名角色摸两张牌。",
  mbxuetu_fail: "血途·失败",
  mbxuetu_fail_info: "转换技。出牌阶段限一次，阳：你可以回复1点体力，然后令一名其他角色弃置两张牌；阴：你可以摸一张牌，然后对一名其他角色造成1点伤害。",
  mbweiming: "威命",
  mbweiming_info: "使命技，锁定技。①出牌阶段开始时，你记录一名未以此法记录过的其他角色。②成功：当你杀死一名未被〖威命①〗记录过的角色后，修改〖血途〗为成功版本。③失败：当一名被〖威命①〗记录过的角色死亡后，你修改〖血途〗为失败版本。",
  lizhaojiaobo: "李昭焦伯",
  mbzuoyou: "佐佑",
  mbzuoyou_info: "转换技。出牌阶段限一次，阳：你可以令一名角色摸三张牌，然后其弃置两张手牌；阴：你可以令一名角色获得1点护甲。",
  mbshishou: "侍守",
  mbshishou_info: "锁定技。当你发动〖佐佑〗后，若目标角色不为你，你执行〖佐佑〗中目标角色未执行的一项。",
  chengji: "成济",
  mbkuangli: "狂戾",
  mbkuangli_info: "锁定技。①出牌阶段开始时，你随机令场上任意名其他角色获得“狂戾”标记。②出牌阶段限两次，当你使用牌指定有“狂戾”的角色为目标后，你随机弃置你与其各一张牌，然后你摸两张牌。③回合结束时，你移除所有角色的“狂戾”。",
  mbxiongsi: "凶肆",
  mbxiongsi_info: "限定技。出牌阶段，若你的手牌数不少于三张，你可以弃置所有手牌，然后令所有其他角色依次失去1点体力。",
  mb_sp_guanqiujian: "玄毌丘俭",
  mb_sp_guanqiujian_prefix: "玄",
  mbcuizhen: "摧阵",
  mbcuizhen_info: "①游戏开始时，你可以废除至多三名其他角色的武器栏。②当你于出牌阶段使用伤害类牌指定其他角色为目标后，若目标角色的手牌数不小于体力值，你可以废除其武器栏。③摸牌阶段，你令额定摸牌数+X（X为所有角色被废除的武器栏数之和+1，至多为3）。",
  mbkuili: "溃离",
  mbkuili_info: "锁定技。当你受到伤害后，若来源有被废除的武器栏，你令其恢复武器栏。",
  mb_caomao: "手杀曹髦",
  mb_caomao_prefix: "手杀",
  mbqianlong: "潜龙",
  mbqianlong_info: `${get.poptip("rule_chihengji")}。①游戏开始时，你获得20枚“道心”标记。②当你得到牌后/受到1点伤害后/造成1点伤害后，你获得5/10/15枚“道心”（上限为99枚）。③若你的“道心”数不小于25/50/75/99，你视为拥有${get.poptip("mbcmqingzheng")}/${get.poptip("mbcmjiushi")}/${get.poptip("mbcmfangzhu")}/${get.poptip("mbjuejin")}。`,
  mbcmqingzheng: "清正",
  mbcmqingzheng_info: `${get.poptip("rule_chihengji")}。出牌阶段开始时，你可以弃置一种花色的所有手牌，并观看一名有手牌的其他角色的手牌，你弃置其中一种花色的所有牌。若其被弃置的牌数小于你以此法弃置的牌数，你对其造成1点伤害。`,
  mbcmjiushi: "酒诗",
  mbcmjiushi_info: `${get.poptip("rule_chihengji")}。①当你需要使用【酒】时，若你的武将牌正面向上，你可以翻面，视为使用一张【酒】。②当你受到伤害后，若你武将牌背面朝上且未因此次伤害发动过〖酒诗〗，你可以翻面。③当你翻面后，你获得牌堆里的一张锦囊牌。`,
  mbcmfangzhu: "放逐",
  mbcmfangzhu_info: `${get.poptip("rule_chihengji")}。出牌阶段限一次，你可以选择一名不为你上个出牌阶段发动〖放逐〗的目标的其他角色，选择一项：⒈令其不能使用手牌中的非锦囊牌直到其回合结束；⒉令其所有非Charlotte技能失效直到其回合结束。`,
  mbjuejin: "决进",
  mbjuejin_info: `${get.poptip("rule_chihengji")}，限定技。出牌阶段，你可以令所有角色依次将体力值调整至1并获得X点护甲（X为一名角色以此法减少的体力值且你以此法获得的护甲数额外+2）。然后你将牌堆、弃牌堆、场上及所有角色手牌中的【闪】、【桃】和【酒】移出游戏且增加全局技能“${get.poptip({
    id: "juejin_xiangsicunwei",
    name: "向死存魏",
    type: "character",
    info: "当有牌进入弃牌堆后，系统将这些牌中的【闪】、【桃】和【酒】移出游戏。"
  })}”。`,
  mbweitong: "卫统",
  mbweitong_info: `${get.poptip("rule_chihengji")}，主公技。游戏开始时，若你有〖潜龙〗且场上有其他魏势力角色，你因“潜龙”于游戏开始时获得的“道心”标记数修改为60枚。`,
  mb_simafu: "手杀司马孚",
  mb_simafu_prefix: "手杀",
  mbpanxiang: "蹒襄",
  mbpanxiang_info: "当一名角色受到伤害时，你可以选择一项（不能与上次你因其发动此技能时选择的选项相同）：⒈令此伤害-1，伤害来源摸两张牌；⒉令此伤害+1，其摸三张牌。",
  mbchenjie: "臣节",
  mbchenjie_info: "锁定技。当一名角色死亡后，若你有〖蹒襄〗且其成为过你〖蹒襄〗的目标，你弃置区域里的所有牌，摸四张牌。",
  zhangbu: "张布",
  mbchengxiong: "惩凶",
  mbchengxiong_info: "你使用锦囊牌仅指定其他角色为目标后，可选择一名牌数不小于X的其他角色，弃置其一张牌（X为你此阶段使用的牌数）。若此牌颜色与你使用的锦囊牌相同，你对其造成1点伤害。",
  mbwangzhuang: "妄专",
  mbwangzhuang_info: "一名角色受到非牌造成的伤害后，若你是伤害来源或受伤角色，你可以摸一张牌，令当前回合角色本回合非锁定技失效。",
  mb_wangjing: "王经",
  mbzujin: "阻进",
  mbzujin_info: "每回合每种牌名限一次，若你未受伤或体力值不为最低，你可将一张基本牌当作【杀】使用或打出；若你已受伤，你可将一张基本牌当作【闪】或【无懈可击】使用或打出。",
  mbjiejian: "节谏",
  mbjiejian_info: "准备阶段，你可将任意张手牌交给一名其他角色，并令这些角色获得“节谏”标记。每回合每名角色限一次，“节谏”角色成为一张非你使用的非装备牌的唯一目标时，你可将此牌转移给你，然后摸一张牌。“节谏”角色的回合结束时，移去其“节谏”标记，若其体力值不小于X（X为你交给其牌时其的体力值），你摸两张牌。",
  mbjiejian_tag: "已分配",
  mb_wenqin: "玄文钦",
  mb_wenqin_prefix: "玄",
  mbbeiming: "孛明",
  mbbeiming_info: "游戏开始时，你可令至多两名角色获得牌堆中一张攻击范围为X的武器牌（X为其手牌中的花色数）。",
  mbchoumang: "仇铓",
  mbchoumang_info: `每回合限一次，你使用【杀】指定唯一目标或成为【杀】的唯一目标后，可以选择一项：①令此【杀】伤害+1；②若此【杀】被【闪】抵消，你可以获得你距离1以内的一名其他角色区域里的一张牌；③${get.poptip("rule_beishui")}：弃置你与其装备栏里的武器牌。`,
  mb_simazhou: "手杀司马伷",
  mb_simazhou_prefix: "手杀",
  mbbifeng: "避锋",
  mbbifeng_info: "你成为一张基本牌或普通锦囊牌的目标时，若此牌目标数不大于4，你可取消之。若如此做，此牌结算完成后若没有其他角色响应此牌，你失去1点体力，否则你摸两张牌。",
  mbsuwang: "宿望",
  mbsuwang_info: "一名角色的回合结束时，若其此回合对你使用过牌且你未受到过伤害，则你将牌堆顶的一张牌置于武将牌上。摸牌阶段开始时，你可以改为获得武将牌上的“宿望”牌，然后你可以令一名其他角色摸两张牌。",
  mbsuwang_info_versus_two: "一名角色的回合结束时，若其此回合对你使用过牌且你本回合受到的伤害值之和不大于1，则你将牌堆顶的一张牌置于武将牌上。摸牌阶段开始时，你可以改为获得武将牌上的“宿望”牌，然后你可以令一名其他角色摸两张牌。",
  mb_jiachong: "手杀贾充",
  mb_jiachong_prefix: "手杀",
  mbbeini: "悖逆",
  mbbeini_info: "出牌阶段限一次，你可以选择一名体力值不小于你的其他角色，令你或其摸两张牌，然后未摸牌的角色选择一项：1.视为对摸牌的角色使用一张【杀】；2.获得摸牌的角色场上的一张牌。",
  mbdingfa: "定法",
  mbdingfa_info: "弃牌阶段结束时，若本回合你失去的牌数不小于4，你可以选择一项：1、回复1点体力；2、弃置一名角色至多两张牌。",
  mb_zhangfen: "手杀张奋",
  mbquchong: "渠冲",
  mbquchong_effect: "大攻车",
  mbquchong_info: `①出牌阶段，你可以重铸装备牌。②一名角色的回合结束时，你将弃牌堆中的所有装备移出游戏，然后获得等量的铸造值。③出牌阶段开始时，若场上：没有【大攻车】，则你可以依次消耗0、2、5、5点铸造值，从${get.poptip("dagongche_attack")}和${get.poptip("dagongche_defend")}中选择一个令一名角色获得（你任选此牌花色和点数）并令其使用此牌；有【大攻车】，则你可以将场上的一张【大攻车】交给不为此牌拥有者的一名角色并令其使用此牌。`,
  mbxunjie: "逊节",
  mbxunjie_info: "锁定技，当你受到有来源的伤害时，若其体力值大于你且场上没有【大攻车】，则你进行一次判定，若结果为红色，则此伤害-1。",
  mb_zhangfen_prefix: "手杀",
  dagongche_attack: "大攻车·攻",
  dagongche_defend: "大攻车·守",
  dagongche_attack_skill: "大攻车",
  dagongche_defend_skill: "大攻车",
  dagongche_attack_info: "②此牌拥有2点耐久度，耐久度为0时销毁此牌。①当此牌进入你的装备区时，弃置你装备区里的其他牌。②其他装备区进入你的装备区前，改为将这些牌置于弃牌堆。③当你对一名角色造成伤害时，你可减少1点此牌的耐久度，令此伤害+X（X为游戏轮数且至多为3）。④当此牌不因〖渠冲〗离开装备区时，减少1点此牌的耐久度并防止之。",
  dagongche_defend_info: "②此牌拥有3点耐久度，耐久度为0时销毁此牌。①当此牌进入你的装备区时，弃置你装备区里的其他牌。②其他装备区进入你的装备区前，改为将这些牌置于弃牌堆。③当你受到伤害时，减少X点此牌的耐久度，令此伤害-X（X为伤害值且至多为此牌耐久度）。④当此牌不因〖渠冲〗离开装备区时，减少1点此牌的耐久度并防止之。",
  mb_sp_zhenji: "手杀SP甄宓",
  mb_sp_zhenji_prefix: "手杀SP",
  mbbojian: "博鉴",
  mbbojian_info: "锁定技。出牌阶段结束时，若你本阶段使用牌数与花色数与上个出牌阶段皆不同（没有上个出牌阶段则均视为0），你摸一张牌；否则你将一张弃牌堆中本阶段你因使用而失去的牌交给一名角色。",
  mbjiwei: "济危",
  mbjiwei_info: "锁定技。①其他角色的回合结束时，你摸X张牌（X为本回合满足的项数：1.至少[1]名角色失去过牌；2.至少[1]名角色受到过伤害）。②准备阶段，若所有角色均存活且你的手牌数不小于5，你将手牌中颜色较多的牌分配给任意名其他角色。",
  mbjiwei_info_identity: "锁定技。①其他角色的回合结束时，若本回合有至少一名角色失去过牌，你摸一张牌。②准备阶段，若你的手牌数不小于Y，你将手牌中颜色较多的牌分配给任意名其他角色（Y为存活人数与你体力值的最大值）。",
  mbjiwei_info_doudizhu: "锁定技。①其他角色的回合结束时，若本回合有：至少一名角色失去过牌，你摸一张牌；至少一名角色受到过伤害，你摸两张牌。②准备阶段，若所有角色均存活且你的手牌数不小于5，你将手牌中颜色较多的牌分配给任意名其他角色。",
  mb_ganfuren: "手杀甘夫人",
  mb_ganfuren_prefix: "手杀",
  mbzhijie: "智诫",
  mbzhijie_info: "每轮限一次，一名角色的出牌阶段开始时，你可以展示其一张手牌。本阶段其使用与此牌类别相同的牌后，其摸一张牌并弃置X张牌（X为其本回合使用此牌类型的次数-1）；此阶段结束时，若其阶段因此获得的牌数大于因此弃置的牌数，你与其各摸一张牌。",
  mbshushen: "淑慎",
  mbshushen_info: "每回合每项各限一次。当你回复体力时，你可以令一名其他角色摸两张牌；当你获得至少两张牌后，你可以令一名其他角色回复1点体力。",
  mb_xuezong: "手杀薛综",
  mb_xuezong_prefix: "手杀",
  mbfunan: "复难",
  mbfunan_info: "其他角色使用或打出牌响应你使用的牌时，你可以获得其使用或打出的牌。",
  mbfunan_rewrite: "复难·改",
  mbfunan_rewrite_info: "其他角色使用或打出牌响应你使用的牌时，你可以获得其使用或打出的牌，且你使用以此法获得的牌结算结束后，若没有其他角色响应此牌，你摸一张牌。",
  mbjiexun: "诫训",
  mbjiexun_info: `结束阶段，你可以选择一种花色，令一名其他角色摸等同于场上此花色数量的牌（至多为5），然后弃置X张牌（X为此技能发动次数）。若其以此法弃置了所有手牌，你修改${get.poptip("mbfunan")}`,
  friendgongli: "共砺",
  friendgongli_info: "空技能，唯一作用是引用确认阵营一致判断时字符写少一些。",
  friend_zhugeliang: "友诸葛亮",
  friend_zhugeliang_prefix: "友",
  friendyance: "演策",
  friendyance_minigame: "卧龙演策",
  friendyance_minigame_info: ["执行者声明“颜色”或“类别”，然后选择猜测接下来X张连续被使用的牌的对应颜色或类别，X初始为3，至多为7，当接下来的X张牌被使用时，你每猜对一次则摸一张牌，且单次“卧龙演策”至多摸五张牌。", "单次“卧龙演策”的所有猜测均被验证后，或执行者发起下一次“卧龙演策”时，若本次“卧龙演策”猜测正确的次数为：<br>" + ["0，执行者失去[1]点体力，其X数值-[1]", "不足X的一半，执行者弃置[1]张牌", "超过X的一半，执行者根据自己本次声明的猜测方式从牌堆中获得[1]张符合类别的牌", "X，执行者摸[2]张牌，其X数值+[1]"].map((str) => "<li>" + str).join("；<br>") + "。"].join("<br>"),
  friendyance_info: `每轮限一次，首轮开始时或准备阶段，你可以选择一项：①从牌堆中随机获得一张锦囊牌；②执行${get.poptip("friendyance_minigame")}。`,
  friendfangqiu: "方遒",
  friendfangqiu_info: `限定技，当你执行${get.poptip("friendyance_minigame")}后，你可以展示你的${get.poptip("friendyance_minigame")}预测。若如此做，本次${get.poptip("friendyance_minigame")}的预测全部验证后，执行“[]”中效果的值均+1，然后若${get.poptip("friendyance_minigame")}预测的牌数大于等于3且预测全部正确，该技能可以再次发动。`,
  friendzhugelianggongli: "共砺",
  friendzhugelianggongli_info: `锁定技，若友庞统在场且确认与你阵营一致，你执行${get.poptip("friendyance_minigame")}初始可预测的牌数+1；若友徐庶在场且确认与你阵营一致，你执行${get.poptip("friendyance_minigame")}预测的第一张牌的结果始终视为正确。`,
  friend_xushu: "友徐庶",
  friend_xushu_prefix: "友",
  friendxiaxing: "侠行",
  friendxiaxing_info: `游戏开始时，你获得并使用${get.poptip("xuanjian")}；当${get.poptip("xuanjian")}进入弃牌堆时，你可以移除2个“启诲”标记并获得之。`,
  friendqihui: "启诲",
  friendqihui_info: "锁定技。当你使用牌时，你获得1个此牌对应类别的“启诲”标记（每个类别标记上限为1），然后若你至少拥有3个“启诲”标记，你移去2或3个“启诲”标记并选择一项执行：①回复1点体力；②摸两张牌；③使用的下一张牌无任何次数限制；④令一名其他角色摸三张牌。若选择移除3个则每个类别的标记上限+1。",
  friendqihui_append: `<span style="font-family:yuanli">该启诲为海外版本，覆盖了手杀版本</span>`,
  friendxushugongli: "共砺",
  friendxushugongli_info: "锁定技，若友诸葛亮在场且确认与你阵营一致，你发动〖玄剑〗改为将一张手牌当【杀】使用；若友庞统在场且确认与你阵营一致、你发动〖玄剑〗使用的【杀】无距离限制。",
  xuanjian: "玄剑",
  xuanjian_info: "你可以将一种花色的所有手牌当作【杀】使用。",
  xuanjian_skill: "玄剑",
  xuanjian_skill_info: "你可以将一种花色的所有手牌当作【杀】使用。",
  friend_pangtong: "友庞统",
  friend_pangtong_prefix: "友",
  friendmanjuan: "漫卷",
  friendmanjuan_info: "每轮限五次。当你不因本技能一次性获得至少两张牌后，你可以将这些牌中的任意张牌以任意顺序置于牌堆顶。若如此做，你每放置一张牌，便从弃牌堆中随机获得一张与此牌类别不同的牌（至多获得五张）。",
  friendyangming: "养名",
  friendyangming_info: "出牌阶段结束时，若你本阶段失去过至少两张手牌，则你可以将牌堆顶的四张牌置于你的武将牌上，然后使用其中任意张花色各不相同的牌（无次数限制），将剩余牌置入弃牌堆。",
  friendpangtonggongli: "共砺",
  friendpangtonggongli_info: "锁定技，若友诸葛亮在场且确认与你阵营一致，你发动〖养名〗亮出牌的张数+1；若友徐庶在场且确认与你阵营一致，你发动〖养名〗后，获得一张本次亮出牌中未使用过的花色的牌。",
  mb_qinghegongzhu: "手杀清河公主",
  mb_qinghegongzhu_prefix: "手杀",
  mbzengou: "谮构",
  mbzengou_info: "出牌阶段限一次，你可以观看一名其他角色的手牌，然后你选择一项：①视为使用两张其手牌中未拥有的基本牌牌名的牌（不计入次数且无次数限制且不可重复）；②将你与其手牌中共同牌名的牌替换为牌堆中等量的【杀】，你的这些牌不计入手牌上限直到你的结束阶段。然后你选择一个基本牌牌名，令其获得1枚此牌名对应的“诬”标记（每回合使用的前三张牌结算完毕后，若拥有此牌名的“诬”标记，则失去1点体力并移去1枚此牌名的“诬”标记）。",
  mbfeili: "诽离",
  mbfeili_info: "当你受到伤害时，若你拥有〖谮构〗，则你可以弃置两张牌并防止此伤害；若此伤害存在来源且伤害来源拥有“诬”标记，则你可以改为移去其“诬”标记并防止此伤害，你摸四张牌且你不能再对其发动〖谮构〗。",
  friend_cuijun: "友崔均",
  friend_cuijun_prefix: "友",
  friendshunyi: "顺逸",
  friendshunyi_info: "当你使用点数最小的手牌时，若此牌为红桃且此牌点数大于你本回合发动此技能的次数，则你可以将所有与此花色相同的手牌扣置于武将牌上直到回合结束（无此花色手牌则跳过此步骤），然后摸一张牌。",
  friendbiwei: "鄙位",
  friendbiwei_info: "出牌阶段限一次，你可以弃置手牌中点数最大的手牌并令一名其他角色弃置大于等于此牌点数的所有手牌，若其未因此弃牌，则你重置此技能。",
  friendgongli_cuijun: "共砺",
  friendgongli_cuijun_info: "锁定技，游戏开始时，你为〖顺逸〗增加X个可触发花色（X为场上的友系列武将数）。",
  friend_shitao: "友石韬",
  friend_shitao_prefix: "友",
  friendqinying: "钦英",
  friendqinying_info: "出牌阶段限一次，你可以重铸任意张牌，视为使用一张【决斗】。此【决斗】结算流程中限X次（X为你以此法重铸的牌数），你或目标角色可以弃置自己区域里的一张牌，视为打出一张【杀】。",
  friendlunxiong: "论雄",
  friendlunxiong_info: "当你造成或受到伤害后，你可以弃置手牌中点数最大的牌并摸三张牌，本局游戏你再次发动此技能弃置牌的点数须大于等于此牌的点数。",
  friendlunxiong_append: `<span style="font-family:yuanli">该技能为海外版本</span>`,
  friendgongli_shitao: "共砺",
  friendgongli_shitao_info: "锁定技，游戏开始时，你为〖钦英〗减少X个可弃置类别（X为场上的友系列武将数）。",
  simazhao: "SP司马昭",
  simazhao_prefix: "SP",
  gz_simazhao: "司马昭",
  mb_simazhao: "玄司马昭",
  mb_simazhao_ab: "司马昭",
  mbqiantun: "谦吞",
  mbqiantun_tag: "invisible",
  mbqiantun_info: "魏势力技。出牌阶段限一次，你可以令一名其他角色展示至少一张手牌，然后与其拼点，其本次拼点只能从展示牌中选择。若你赢，你获得其展示的手牌；若你没赢，你获得其未展示的手牌。然后你展示手牌。",
  mbqiantun_info_doudizhu: "魏势力技。出牌阶段限一次，你可以令一名其他角色展示至少一张手牌，然后与其拼点，其本次拼点只能从展示牌中选择。若你赢，你获得其展示的两张手牌；若你没赢，你获得其未展示的两张手牌。然后你展示手牌。",
  mbxiezheng: "挟征",
  mbxiezheng_info: "你的登场势力为魏。结束阶段，你可以令一名角色将随机一张手牌置于牌堆顶，然后你视为使用一张【兵临城下】。然后若此牌若未造成伤害，你失去1点体力。",
  mbxiezheng_info_identity: "你的登场势力为魏。结束阶段，你可以令一名角色将随机一张手牌置于牌堆顶，然后你视为使用一张【兵临城下】（若你未发动〖昭凶〗，则此牌须优先指定同势力角色）。然后若此牌未造成伤害，你失去1点体力。",
  mbxiezheng_info_doudizhu: "你的登场势力为魏。每局游戏限一次。结束阶段，你可以令至多两名角色依次将随机一张手牌置于牌堆顶，然后你视为使用一张【兵临城下】。然后若此牌未造成伤害，你失去1点体力。",
  mbzhaoxiong: "昭凶",
  mbzhaoxiong_info: `${get.poptip("rule_chihengji")}，限定技。准备阶段，若你已受伤，你可以变更势力为群，然后获得技能${get.poptip("mbdangyi")}。`,
  mbweisi: "威肆",
  mbweisi_info: "群势力技。出牌阶段限一次，你可以选择一名其他角色，令其将任意张手牌移出游戏直到回合结束，然后视为对其使用一张【决斗】；此牌对其造成伤害后，你获得其所有手牌。",
  mbweisi_info_doudizhu: "群势力技。出牌阶段限一次，你可以选择一名其他角色，令其将任意张手牌移出游戏直到回合结束，然后视为对其使用一张【决斗】；此牌对其造成伤害后，你获得其一张手牌。",
  mbdangyi: "荡异",
  mbdangyi_info: `${get.poptip("rule_chihengji")}，主公技，每回合限一次，每局游戏限两次。当你造成伤害时，你可以令此伤害+1。`,
  wuke: "吴珂",
  mbzhuguo: "助国",
  mbzhuguo_info: "出牌阶段限一次，你可令一名角色将手牌调整至X（X为其体力上限且至多为5）。然后若其未摸牌，其回复1点体力，否则若其手牌数全场最多，你可以令其选择是否对你指定的另一名角色使用一张无任何次数和距离限制的【杀】。",
  mbanda: "谙达",
  mbanda_info: "每轮限一次，当一名角色进入濒死状态时，你可令伤害来源交给其两张颜色不同的牌，否则该角色回复1点体力。",
  mb_xingdaorong: "手杀邢道荣",
  mb_xingdaorong_prefix: "手杀",
  mbkuangwu: "诳武",
  mbkuangwu_info: "其他角色的出牌阶段开始时，你可将手牌调整至与其手牌数相同（至多摸至五张，弃至一张），然后视为对其使用一张【决斗】。此【决斗】结算结束后，若其未因此牌受到伤害，则你失去1点体力且此技能失效直到本轮结束。",
  mb_guanyinping: "手杀关银屏",
  mb_guanyinping_prefix: "手杀",
  mbxuehen: "雪恨",
  mbxuehen_info: "当你每回合首次造成或受到伤害后，你可展示至多X张手牌（X为你已损失体力值），这些牌只能当不计入次数且无次数限制的【杀】使用或打出，直到你使用这些牌造成伤害。",
  mbxuehen_rewrite: "雪恨·改",
  mbxuehen_rewrite_info: "当你每回合首次造成或受到伤害后，你可展示至多X张手牌（X为你已损失体力值），这些牌只能当不计入次数且无次数限制的【杀】使用或打出，直到你使用这些牌造成伤害，且你使用以此法转化的【杀】结算结束后摸一张牌。",
  mbhuxiao: "虎啸",
  mbhuxiao_info: `出牌阶段限一次，你可选择一项：1.对一名体力值大于等于你的角色造成1点火焰伤害：2.本回合使用牌无距离限制；3.${get.poptip("rule_beishui")}：弃置一张红色牌。`,
  mbwuji: "武继",
  mbwuji_info: "限定技，出牌阶段，你可修改〖雪恨〗直到本阶段结束。此阶段结束时，若你因〖雪恨〗获得大于等于两张牌，你改为永久修改〖雪恨〗。",
  mb_mengda: "手杀孟达",
  mb_mengda_prefix: "手杀",
  mbjili: "积戾",
  mbjili_info: "一名其他角色的回合开始时，若其在你的攻击范围内，你可以从0~2中秘密选择一个本轮未选择过的数字X。若如此做，本回合结束阶段，若其本回合使用牌指定你为目标的次数：小于X，你摸4-X张牌；等于X，你交给其X张牌；大于X，你可以视为对其使用一张无距离限制的【杀】。",
  mbshishu: "恃术",
  mbshishu_info: "锁定技，一名其他角色于其回合内获得你的牌后，你令其选择一项：1.弃置这些牌；2.交给你一张与这些牌类型均不同的牌。",
  mb_caoxing: "射曹性",
  mb_caoxing_prefix: "射",
  mbjinzu: "劲镞",
  mbjinzu_info: "出牌阶段限一次，你可以选择一名其他角色，与其分别同时展示一/两张手牌，若你的展示牌点数在所有牌中：为最值，你弃置所有展示牌，重置此技能；不为唯一最值，你本回合使用的下一张【杀】对其造成伤害+1且其不可响应。",
  mbanxian: "暗弦",
  mbanxian_info: "你每回合首次弃置手牌时，可以获得并使用其中的一张【杀】（无距离次数限制）。",
  mb_zerong: "手杀笮融",
  mb_zerong_prefix: "手杀",
  mbfutu: "浮图",
  mbfutu_info: "①一名角色的回合结束时，若你本回合造成的伤害值/令任意角色回复的体力值为全场最高，你将牌堆顶首张黑色/红色牌置于武将牌上，称为“业”。②你受到伤害时，可以弃置一张“业”并防止此伤害。",
  mbjingtu: "净土",
  mbjingtu_info: `限定技，出牌阶段，你可选择一项：1.获得所有黑色“业”并对一名角色造成等量伤害；2.获得所有红色“业”，令一名角色增加等量点体力上限并恢复等量体力；3.${get.poptip("rule_beishui")}，两种颜色的“业”数量相同。然后你失去〖浮图〗并获得${get.poptip("mbfozong")}。`,
  mbjiebian: "劫辩",
  mbjiebian_info: "一名角色的出牌阶段结束时，若本阶段没有角色受到过伤害，你可与当前回合角色或体力值最低的角色拼点（你因此拼点时可以使用“业”进行拼点），若你赢，你选择一项：1.对没赢的角色造成1点伤害；2.令没赢的角色恢复1点体力并摸一张牌，然后获得其至多两张牌。",
  mbfozong: "佛宗",
  mbfozong_info: "锁定技，与你因〖净土〗获得的牌颜色相同的手牌不计入手牌上限，造成的伤害值和恢复值+1。",
  mbshuyong: "姝勇",
  mbshuyong_info: "当你使用或打出【杀】时，你可以获得一名其他角色区域内的一张牌。若本轮你以此法获得其区域内的牌数大于1，其摸一张牌。",
  mbxushen: "许身",
  mbxushen_info: `限定技，出牌阶段，你可以摸至多三张牌并失去等量体力，然后若你因此进入濒死状态，则当你脱离此次濒死状态后，你可以将${get.poptip("new_rewusheng")}${get.poptip("redangxian")}${get.poptip("rezhiman")}分配给本次濒死令你回复过体力的角色（若其已拥有，改为摸三张牌）。`,
  mbzhennan: "镇南",
  mbzhennan_info: "一名角色使用普通锦囊牌指定首个目标后，若此牌目标包含你且此牌目标数大于X（X为此牌使用者的体力值且至少为1），则你可以弃置一张牌并对一名角色造成1点伤害。",
  mbfangxu: "芳许",
  mbfangxu_info: "每回合限两次，你可以将一张本回合获得的牌当作【杀】或【闪】使用，你因此使用牌时选择一项（不公开选择）：①若此牌造成伤害，弃置受伤角色至多两张牌；②若此牌未造成伤害，当前回合角色摸两张牌。",
  mbzhuguan: "筑关",
  mbzhuguan_info: "锁定技。①当你使用红色基本牌时，你摸一张牌（不计入手牌上限）。②回合开始时，你弃置所有“筑关”牌。",
  mblisuo: "栗索",
  mblisuo_info: "出牌阶段限一次，你可以与一名有手牌的角色同时展示任意张手牌。若你展示的手牌数大于其，则其下个出牌阶段至多使用其展示牌数张牌；若你展示的手牌数小于其，则你重铸你展示的牌，然后可以对其使用至多你展示牌数张无距离和任何次数限制的【杀】。",
  mb_caohong: "手杀曹洪",
  mb_caohong_prefix: "手杀",
  mbyuanhu: "援护",
  mbyuanhu_info: `①游戏开始时，你获得${get.poptip("twjuezhu")}。②出牌阶段限一次。你可将一张装备牌置入一名角色的装备区内。若此牌为：武器牌，你弃置与其距离为1的另一名角色区域的一张牌；防具牌，其摸一张牌；坐骑牌，其回复1点体力；宝物牌，你选择基本牌或普通锦囊牌从牌堆中获得一张，其获得另一类型的一张牌。然后若其体力值或手牌数不大于你，则你可摸一张牌，且你可以于结束阶段发动一次〖援护〗。`,
  mb_caocao: "手杀SP曹操",
  mb_caocao_prefix: "手杀|SP",
  mblingfa: "令法",
  mblingfa_info: `①第一轮开始时，你可令本轮其他角色使用【杀】时，其需弃置一张牌，否则你对其造成1点伤害。②第二轮游戏开始时，你可令本轮其他角色使用【桃】结算结束后，其需交给你一张牌，否则你对其造成1点伤害。③非前两轮游戏开始时，你失去〖令法〗并获得${get.poptip("twzhian")}和${get.poptip("new_rejianxiong")}。`,
  mb_luyu: "礼卢毓",
  mb_luyu_prefix: "礼",
  mbbingfa: "秉法",
  mbbingfa_info: `每轮开始时，你可选择两条${get.poptip({
    id: "bingfa_lvfa",
    name: "律法",
    type: "character",
    info: `<li>本轮造成伤害最多者受到2点无来源伤害<br><li>本轮获得牌数最多者手牌上限-2<br><li>本轮体力变化次数最多者减少1点体力上限<br><li>区域内牌数最多者交给每名其他角色各一张牌`
  })}并令所有未翻面的角色投票，得票最多的${get.poptip("bingfa_lvfa")}于本轮结束时执行。`,
  mbshuxing: "束刑",
  mbshuxing_info: "你的回合内每名角色限一次，一名其他角色成为【杀】的目标时，你可令此【杀】对其无效并展示其手牌，若其中有【闪】，你令其选择一项：1.失去1点体力；2.交给你其手牌中【闪】和其下次〖秉法〗的投票权。",
  mb_cuilingyi: "手杀崔令仪",
  mb_cuilingyi_ab: "手杀崔芙",
  mb_cuilingyi_prefix: "手杀",
  mbcaiqiu: "裁裘",
  mbcaiqiu_info: "①每轮开始时，你观看牌堆顶X张牌（X为游戏人数），然后可以获得其中任意张牌。②每轮限一次，其他角色使用牌结算结束后，若你本轮因〖裁裘〗获得过同名牌，你失去1点体力。",
  mbxishang: "袭裳",
  mbxishang_info: `锁定技，①游戏开始时，你选择本局形象并获得${get.poptip("mbweizhuang")}。②你不因摸牌而获得牌时，明置之。`,
  mbxishang_append: `<span style="font-family:yuanli">明置牌（移动服定义）：对所有玩家可见的牌。一名角色装备区和判定区的牌都是明置牌，但是一名角色的明置牌不包括其判定区的牌。</span>`,
  faceup_tag: "明置牌",
  mbweizhuang: "褽装",
  mbweizhuang_info: `此技能效果根据你的形象发生变化：${get.poptip("mbweizhuang_guidianx")}${get.poptip("mbweizhuang_dongjiaox")}${get.poptip("mbweizhuang_xiugex")}。`,
  mbweizhuang_guidianx: "桂殿",
  mbweizhuang_guidianx_info: `①有明置牌的角色的结束阶段，你可令以下一个数值-1并发动${get.poptip("mbcaiqiu")}：1.摸牌阶段摸牌数；2.出【杀】次数；3.手牌上限；4.体力值。②每局游戏限X+1次，每X+1张牌被明置后，你令以上一项数值+1（X为游戏人数）。`,
  mbweizhuang_dongjiaox: "东郊",
  mbweizhuang_dongjiaox_info: `每回合每项各限一次，若你的明置牌包含类型数不小于：1，你使用基本牌数值+1；2，你使用锦囊牌指定目标后，可以获得其中一个目标一张牌；3，你使用装备牌结算结束后，可以令一名有明置牌的角色摸两张牌。`,
  mbweizhuang_xiugex: "绣阁",
  mbweizhuang_xiugex_info: `①每回合每项各限一次，你可以弃置一张指定副类别的牌并视为使用一张无次数限制的对应牌，此牌结算结束后获得一张与弃置牌花色相同的牌：1.武器牌，【杀】；2.防具牌，【闪】；3.防御坐骑牌，【桃】；4.进攻坐骑牌，【酒】。②若你的明置牌包含四种花色，将此技能的“弃置”改为“展示”。`,
  mb_zhangzhi: "书张芝",
  mb_zhangzhi_prefix: "书",
  mbshiju: "势举",
  mbshiju_info: `锁定技，你使用牌结算结束后，若此牌与上一张被使用的牌：1.类型相同，你获得牌堆顶一张牌；2.花色相同，你获得牌堆底一张牌；${get.poptip("rule_chengshi")}：若牌名也相同，你获得或升级${get.poptip("mbkubai")}。`,
  mbkubai: "枯白",
  mbkubai_info: "你于回合内使用每种颜色的第一张牌时，摸一张牌；你的回合内，其他角色只能使用你使用过的颜色的牌。",
  mbkubai_suit: "枯白·二级",
  mbkubai_suit_info: "你于回合内使用每种花色的第一张牌时，摸一张牌；你的回合内，其他角色只能使用你使用过的花色的牌。",
  mbkubai_number: "枯白·三级",
  mbkubai_number_info: "你于回合内使用每种点数的第一张牌时，摸一张牌；你的回合内，其他角色只能使用你使用过的点数的牌。",
  mb_caozhi: "御曹植",
  mb_caozhi_prefix: "御",
  mbchongsi: "冲司",
  mbchongsi_info: `出牌阶段，若你本轮未选择过最后一项，你可选择一名其他角色并执行一项，然后令其选择一项执行：1.使用一张【杀】；2.弃置两张手牌；3.对自己或装备${get.poptip("cz_liulongcanjia")}的角色造成1点伤害。`,
  mbpeidong: "辔东",
  mbpeidong_info: `你可按以下方式操作${get.poptip("cz_liulongcanjia")}并视为使用对应牌：【杀】，从装备区移动至下家；【闪】，从其他角色场上获得；【桃】，从手牌或牌堆中亮出；【酒】，从游戏外置入宝物栏。`,
  cz_liulongcanjia: "六龙骖驾",
  cz_liulongcanjia_info: "你计算与其他角色的距离-X，其他角色计算与你的距离+X（X为所有角色场上点数为K的牌数）。",
  cz_liulongcanjia_skill: "六龙骖驾",
  cz_liulongcanjia_skill_info: "你计算与其他角色的距离-X，其他角色计算与你的距离+X（X为所有角色场上点数为K的牌数）。",
  mb_zhouyu: "手杀乐周瑜",
  mb_zhouyu_prefix: "手杀乐",
  mbshouyue: "授乐",
  mbshouyue_info: `摸牌阶段开始时，或你的体力值减少后，你可选择一项：1.摸一张牌，令一名角色获得${get.poptip("qinyin")}（已有则改为摸一张牌）；2.令一名角色复原武将牌。`,
  mbdieyin: "叠音",
  mbdieyin_info: "出牌阶段限一次，你可将武将牌翻至背面，于此阶段结束后执行一个指定的额外阶段。",
  mb_liuhui: "数刘徽",
  mb_liuhui_prefix: "数",
  mbgeyuan: "割圆",
  mbgeyuan_info: "当你使用点数为X的牌时（X为圆周率中小数点后第一位的值），你可以摸Y张牌并令X改为后移一位的值（Y为你本回合本技能发动的次数+1，且至多为π)。",
  mbchongcha: "重差",
  mbchongcha_info: "①你的点数大于等于10的牌不计入手牌上限，且使用时视为满足〖割圆〗中的点数0。②出牌阶段限一次，你可以弃置一张牌，令〖割圆〗中的X改为后移一位的值。",
  hefei_zhangliao: "骥张辽",
  hefei_zhangliao_prefix: "骥",
  hefei_lidian: "骥李典",
  hefei_lidian_prefix: "骥",
  hefei_yuejin: "骥乐进",
  hefei_yuejin_prefix: "骥",
  hefeichonglei: "冲垒",
  hefeichonglei_info: "锁定技。①你的出牌阶段内，所有其他角色的非基本手牌均只能作为【闪】使用或打出。②出牌阶段限X次（X为其他角色数），当你使用的牌被其他角色响应后，或当你响应其他角色使用的牌后，你获得该角色一张手牌。",
  hefeidangshi: "荡势",
  hefeidangshi_info: "当你使用伤害牌结算结束后，你可令一名其他目标角色选择一项：1.对你使用一张非转化、非虚拟且与此牌牌名相同的伤害牌；2.弃置X张牌（X为所有角色本轮选择此项的次数且至少为1）；3.受到你造成的1点伤害。若其选择了本阶段未被执行过的选项，你摸一张牌且本阶段出【杀】次数+1。",
  hefeidangshi_append: `<span style="font-family:yuanli">该技能为海外版本</span>`,
  hefeiheyuzhangliao: "合御",
  hefeiheyuzhangliao_info: `锁定技，①若${get.poptip({
    id: "characterx_hefei_yuejin",
    name: "骥乐进",
    type: "character",
    dialog: "characterDialog"
  })}在场且与你阵营相同，修改${get.poptip("hefeichonglei")}中的“非基本手牌”为“手牌”。②若${get.poptip({
    id: "characterx_hefei_lidian",
    name: "骥李典",
    type: "character",
    dialog: "characterDialog"
  })}在场且与你阵营相同，将${get.poptip("hefeidangshi")}中的X固定为3。`,
  hefeiduanjin: "断津",
  hefeiduanjin_info: "你使用基本牌结算结束后，可以弃置一名其他角色一张牌。",
  hefeigaigong: "慨公",
  hefeigaigong_info: "每回合限一次，你对其他角色造成伤害后，或受到其他角色造成的伤害后，你可以展示你或其的至多两张手牌，然后交换这些牌与牌堆底等量张牌。若此交换流程中的牌包含至少两种花色，你可使用其中一张牌（无次数与距离限制）。",
  hefeiheyulidian: "合御",
  hefeiheyulidian_info: `锁定技，①若${get.poptip("characterx_hefei_yuejin")}在场且与你阵营相同，你获得因${get.poptip("hefeiduanjin")}弃置的牌。②若${get.poptip({
    id: "characterx_hefei_zhangliao",
    name: "骥张辽",
    type: "character",
    dialog: "characterDialog"
  })}在场且与你阵营相同，令${get.poptip("hefeigaigong")}使用的牌不可被响应。`,
  hefeixianjian: "陷坚",
  hefeixianjian_info: "你使用非转化且非虚拟的【杀】指定唯一目标后，可以选择一项：1.摸一张牌，其弃置X张牌（X为目标场上牌数且至少为1）；2.此【杀】结算结束后，你将此【杀】置入其一个空置装备栏，称为“陷坚”牌。",
  hefei_xianjian: "陷坚",
  hefei_xianjian_info: "先登陷城！",
  hefeizherui: "折锐",
  hefeizherui_info: `①装备区有“陷坚”牌的角色使用非转化且非虚拟的【杀】指定目标后，你可以对其发动一次其选择选项的${get.poptip("hefeixianjian")}。②有角色失去装备栏里的一张“陷坚”牌后，你对其造成1点伤害。`,
  hefeiheyuyuejin: "合御",
  hefeiheyuyuejin_info: `锁定技，①若${get.poptip("characterx_hefei_zhangliao")}在场且与你阵营相同，你发动过${get.poptip("hefeixianjian")}的【杀】对一名角色造成伤害后，你可对其执行未选择的一项。②若${get.poptip("characterx_hefei_lidian")}在场且与你阵营相同，每回合首次有角色失去“陷坚”牌后，你摸一张牌。`,
  hanxuan: "韩玄",
  mbweizhan: "诿战",
  mbweizhan_info: "其他角色出牌阶段开始时，若你计算与其距离为1，你可以摸一张牌并交给其至多三张牌，本回合结束时若其本回合造成伤害值：不小于X，你摸两张牌；小于X，你获得其区域里两张牌（X为你交给其的牌数）。",
  mbyilv: "遗履",
  mbyilv_info: "每回合限一次，一名角色使用【杀】指定你为目标后，你可与其谋奕。若你赢且你选择的选项为：“金蝉脱壳”，随机弃置其一张手牌，若为基本牌你获得之；“弃履狂奔”，此【杀】伤害-1。",
  o_tanshihuai: "缘檀石槐",
  o_tanshihuai_prefix: "缘",
  mblianzhan: "连斩",
  mblianzhan_info: "你使用指定唯一目标的有点数的【杀】结算结束后，你可以对其使用一张点数更大的【杀】（无距离次数限制）并摸一张牌。",
  mbkoulve: "寇掠",
  mbkoulve_info: "锁定技，你于摸牌阶段外获得的手牌不计入手牌上限且只能当做【杀】使用或打出。",
  o_lvbu: "缘吕布",
  o_lvbu_prefix: "缘",
  mblvezhen: "掠阵",
  mblvezhen_info: "出牌阶段开始时，你可以令所有其他角色依次选择一项：1.展示X张手牌（X为其选择此选项的次数+1）；2.令你视为对其使用一张无距离和次数限制的【杀】。",
  mbhengwei: "横威",
  mbhengwei_info: "锁定技，当你对一名其他角色造成伤害时，除非其展示一张手牌并交给你，否则你令此伤害+1。你的回合内，其他角色展示牌后，其不能使用与展示牌相同颜色的手牌直到本回合结束。",
  o_gaoshun: "缘高顺",
  o_gaoshun_prefix: "缘",
  mbjiren: "激刃",
  mbjiren_info: "限定技，出牌阶段你可以发动此技能，直到本局游戏结束，所有角色使用非武器牌不能指定自己为目标。",
  mbjuezhi: "决止",
  mbjuezhi_info: "出牌阶段每项限一次：1.你可以摸两张牌，视为使用一张【决斗】，然后弃置手牌中所有非伤害牌；2.你可以摸两张牌，然后回复1点体力并弃置手牌中所有伤害牌。",
  mb_tw_zhangji: "手杀张既",
  mb_tw_zhangji_prefix: "手杀",
  mbdingzhen: "定镇",
  mbdingzhen_info: "每轮开始时，你可令与你距离X以内的至多X名其他角色依次选择一项（X为你的体力值）：1.弃一张【杀】；2.本轮其于回合内不能指定你为锦囊牌的目标。",
  mbyouye: "攸业",
  mbyouye_info: "锁定技。①其他角色的结束阶段，若其本回合未对你造成过伤害且“蓄”数小于5，你将牌堆顶的牌置于武将牌上，称为“蓄”。②当你造成或受到伤害后，若你有“蓄”，你将所有“蓄”分配给任意角色。",
  o_sunquan: "缘孙权",
  o_sunquan_prefix: "缘",
  mbshizhong: "势众",
  mbshizhong_info: "锁定技，结束阶段，你摸一张牌并展示所有手牌；准备阶段，你摸X张牌（X为你手牌中剩余以此法展示过的牌数），若X与你上次以此法展示牌数相等，本回合你造成伤害时，将伤害值改为十万！",
  mbcaowei: "操微",
  mbcaowei_info: "锁定技，当你受到伤害后，你重铸至少一个类别的所有牌并摸一张牌。",
  o_guanyu: "缘关羽",
  o_guanyu_prefix: "缘",
  mbwusheng: "武圣",
  mbwusheng_info: "你可以将任意张牌当无距离限制的任意【杀】使用或打出。此【杀】结算结束后，若你本回合首次以此数量转化牌，则你摸两张牌且此【杀】不计入次数限制。",
  mbyijue: "义绝",
  mbyijue_info: "锁定技，当你对一名角色造成到大于等于其体力值的伤害时，其选择交给你任意张牌，然后你选择一项：1.本回合其不能使用或打出牌，且你弃置所有与这些牌花色相同的牌；2.你防止此伤害，且本轮你与其使用牌指定对方为目标时，取消之。",
  o_meicheng: "缘梅成",
  o_meicheng_prefix: "缘",
  bixian: "壁险",
  bixian_info: "锁定技，你成为过牌的目标且体力值未变化过的阶段结束时，你视为使用一张【决斗】。",
  o_chenlan: "缘陈兰",
  o_chenlan_prefix: "缘",
  jujun: "据峻",
  jujun_info: "锁定技，你成为过牌的目标且体力值变化过的阶段结束时，你视为使用一张【万箭齐发】。",
  o_tadun: "缘蹋顿",
  o_tadun_prefix: "缘",
  youlve: "游掠",
  youlve_info: "锁定技，当你于回合内不以此法获得一张牌时，你摸一张牌；当你于回合外不以此法失去一张牌时，你弃置一张牌。",
  lianxi: "连袭",
  lianxi_info: "【杀】不计入你的手牌上限；当你的【杀】因弃置进入弃牌堆时，你可以视为使用一张无距离和任何次数限制的【杀】。",
  mb_lingju: "手杀灵雎",
  mb_lingju_prefix: "手杀",
  mbjieyuan: "竭缘",
  mbjieyuan_info: `你造成伤害时，你可以选择一项：1、弃置一张黑色牌，令此伤害+1；2、从牌堆中获得一张黑色牌。你受到伤害时，你可以选择一项：1、弃置一张红色牌，令此伤害-1；2、从牌堆中获得一张红色牌。${get.poptip("rule_beishui")}：删除另一个时机的效果，将伤害的增减、获得牌的数量改为2，然后失去“背水”选项。`,
  mbfenxin: "焚心",
  mbfenxin_info: "被你杀死的角色亮出其身份牌前，你可选择一项：1、其亮出身份牌后，若与你身份不同，你获得其武将牌上所有技能（限定技、觉醒技、使命技、主公技、持恒技除外）；2、与其交换身份牌（你与其的身份牌非明置时方可选择）。"
};
const characterIntro = {
  zhuji: "朱绩（？—270年），字公绪，丹阳故鄣（今浙江安吉）人，三国时期孙吴名将。本为左大司马、当阳侯朱然之子，后于吴少帝孙亮五凤年间（254年—256年）改回其父本姓“施”，故史书多称施绩。年轻时因父荫任郎官，后拜建忠都尉，随太常潘濬征讨五溪蛮，以胆勇著称，迁偏将军、营下督，执法不偏不倚。鲁王孙霸欲与之结交，朱绩推辞不受，保持中立。赤乌十二年（249年），朱然去世，朱绩袭爵当阳侯，拜平魏将军、乐乡督，负责荆州防务。次年，曹魏征南将军王昶攻江陵不克而退，朱绩率军追击，先胜后败，损将钟离茂、许旻，然孙权仍嘉其勇，斥责失约未至的诸葛融，朱绩由此与诸葛兄弟结怨。孙权死后，朱绩历任镇东将军、骠骑将军。孙綝秉政时，朝中猜疑不安，朱绩恐魏国乘虚而入，曾密结蜀汉共御强敌。永安元年（258年），孙休即位，诛孙綝，任朱绩为上大将军、都护督，统辖巴丘至西陵一线军务。元兴元年（264年），孙皓即位，拜朱绩为左大司马，为当时东吴最高武职。建衡二年（270年），朱绩去世。陈寿评其“咸有将领之才，克绍堂构”，为东吴中后期柱石之臣。",
  tanshihuai: "檀石槐（137年—181年），东汉鲜卑首领。统一鲜卑各部，占据匈奴故地，立庭于弹汗山，分东中西三部，兵马强盛，屡犯东汉幽、并、凉三州，拒受封王和亲，为北方大患。",
  hanxuan: "韩玄（生卒年不详），东汉末年长沙太守。刘备在赤壁之战后，推荐刘琦担任荆州刺史，又南征荆州四郡。武陵太守金旋、长沙太守韩玄、桂阳太守赵范、零陵太守刘度都望风而降。",
  luyu: "卢毓（183-257年），字子家，涿郡涿县（今河北省涿州市）人，三国时期曹魏大臣，良乡侯卢植之子。出身范阳卢氏，以学术和品行，见称于世，授门下赋曹，在崔琰举荐下，出任冀州主簿。上书批评“士亡法”量刑过重，得到丞相曹操的赏识，授丞相府属官，历任魏国吏部郎。曹魏建立后，历任黄门侍郎、济阴相、梁谯二郡太守。反对大量迁移百姓充实谯郡的做法，贬为睢阳典农校尉，历任安平、广平二郡太守，颇得百姓爱戴。青龙二年，授侍中，历任吏部尚书、尚书仆射，累迁司空，进爵容城县侯。甘露二年（257年），去世，谥号为成。",
  wuke: "吴氏，原籍吴（今江苏省苏州市），后迁钱塘（今浙江省杭州市），是三国时期孙吴政权创建过程中的重要人物。吴辉之女，孙坚之妻，孙策、孙权之母。吴氏早失父母，与孙坚成婚后，生有四子孙策、孙权、孙翊、孙匡和一女。孙策平定江东时，受她礼贤下士的影响，优遇士人，开启了孙吴的“江东化”进程。东汉建安五年（200年），孙策遇刺身亡后，吴夫人笼络流寓人士代表，以辅佐孙权。建安七年（202年），曹操命孙权“质任子”，吴夫人明确表示“遂不送质”，这一关键性战略选择，使孙氏政权免于成为曹操附庸，确定了独立发展的方向。《三国志》中记载吴夫人卒于建安七年（202年），《志林》中记载卒于建安十二年（207年）。",
  cuijun: "崔钧，字州平，博陵安平（今河北省衡水市安平县）人。东汉大臣，太尉崔烈之子，议郎崔均之弟。少结英豪，颇有名称。历任虎贲中郎将，迁西河太守。汉献帝即位，父亲崔烈为董卓所害，遂从袁绍一同起兵山东，讨伐董卓。因参加董卓讨伐战使其父为董卓收押。后游历于荆襄之地，与诸葛亮孔明、徐庶元直等交善。",
  lizhaojiaobo: "李昭（？—约公元260年），三国时期曹魏官吏，官拜冗从仆射，为魏帝曹髦的亲信。<br>焦伯，三国时期人物，魏帝曹髦护卫，官拜黄门从官。<br>曹髦见自己权力威势日渐削弱，感到不胜忿恨，于公元260年（甘露五年）五月初六夜里，令李昭和焦伯等在陵云台布署甲士，出讨司马昭，但最终失败，曹髦为成济所弑，李昭和焦伯等应该也战死。在《三国演义》里，焦伯挺枪出战成济，亦被成济所杀。",
  chengji: "成济（？～260年6月21日），三国时期曹魏官员。官至太子舍人。在魏帝曹髦起兵讨司马昭时，受到司马昭之心腹贾充指使，刺死曹髦。司马昭为平息众怒，将成倅、成济兄弟二人杀死。",
  yangfeng: "杨奉（？－197年/198年），东汉末年将领。杨奉早年参加白波军起义，曾经帮助李傕对抗郭汜，后来背叛李傕。兴平二年，加号兴义将军，护送汉献帝刘协东归雒阳，颇有功勋，加号车骑将军，屯兵于梁县。建安元年，随着汉献帝迁都许县，失去权力，先后依附于袁术、吕布，劫掠徐州和扬州地区。建安二年（一说三年），带兵进犯刘备领地，被刘备诱杀。",
  muludawang: "古典小说《三国演义》中的虚构人物，八纳洞主，孟获盟友。擅驱兽法，能行风雨，控制猛兽并指挥它们作战。在法术帮助下最初对蜀军取得一些胜利，但在诸葛亮的喷火木兽将他的动物吓跑后战败，最后死于乱军之中。",
  laimin: "来敏（165年—261年），字敬达，义阳新野人，东汉太中大夫来歙之后，司空来艳之子，三国时期蜀汉官员。东汉末年，逢董卓之乱，来敏跟随姐夫黄琬到荆州避难，黄琬是刘璋祖母的侄子，来敏又与姐姐来氏入蜀，被刘璋引为宾客。来敏喜欢读书，尤其喜欢《左氏春秋》。刘备平定益州后，以来敏为典学校尉，后立太子，来敏为家令。刘禅继位后，任命来敏为虎贲中郎将，诸葛亮驻汉中，请来敏为军祭酒、辅军将军。却因其口出狂言而被罢官，诸葛亮死后，来敏历任大长秋、光禄大夫、执慎将军等职，期间多次因说错话而被免官，蜀汉景耀年间，来敏去世，时年九十七岁。",
  shichangshi: "十常侍，指中国东汉（公元25年—220年）灵帝时期（168年-189年）操纵政权的十二个宦官：张让、赵忠、夏恽、郭胜、孙璋、毕岚、栗嵩、段珪、高望、张恭、韩悝、宋典（在小说《三国演义》里，十常侍指的是指张让、赵忠、封谞、段珪、曹节、侯览、蹇硕、程旷、夏恽、郭胜十人），他们都任职中常侍。玩弄小皇帝于股掌之中，以至灵帝称“张常侍是我父，赵常侍是我母”。十常侍自己横征暴敛，卖官鬻爵，他们的父兄子弟遍布天下，横行乡里，祸害百姓，无官敢管。人民不堪剥削、压迫，纷纷起来反抗。当时一些比较清醒的官吏，已看出宦官集团的黑暗腐败，导致大规模农民起义的形势。郎中张钧在给皇帝的奏章中明确指出，黄巾起义是外戚宦官专权逼出来的，他说：“张角所以能兴兵作乱，万人所以乐附之者，其源皆由十常侍多放父兄、子弟、婚宗、宾客典据州郡，辜确财利，侵略百姓，百姓之怨无所告诉，故谋议不轨，聚为‘盗贼’。”后被曹操、袁绍所歼。",
  sunzhang: "孙璋（？-189年？）东汉末期汉灵帝的宦官，官居中常侍，为十常侍之一，在张让、赵忠之下位居第三。光熹元年（189年），张让、赵忠、段珪等诛杀何进，袁绍率军入宫，诛杀孙璋。",
  bilan: "毕岚（？—189），东汉宦官，十常侍之一。十常侍朋比为奸，祸乱朝纲，制造出党锢之祸，后被袁绍诛杀。另外，毕岚曾制造翻车，用于取河水洒路。",
  xiayun: "夏恽（？—189），东汉宦官，十常侍之一。后被袁绍、曹操诛于翠花楼前。",
  hankui: "韩悝（？—189），东汉宦官，十常侍之一。后被袁绍所诛。",
  lisong: "栗嵩，东汉宦官，十常侍之一，与张让等人均一同任职中常侍。",
  duangui: "段珪（？—189），东汉宦官，十常侍之一。段珪与张让、赵忠等人朋比为奸，党同伐异，横征暴敛，卖官鬻爵，引起了以何进为首的外戚集团的不满。何进谋诛宦官，却不纳曹操、陈琳之谏，多结外镇军阀，翘首京师，与袁绍等谋诛宦竖。终于事泄，被宦官集团查知。张让、段珪等人先下手为强，斩杀何进于嘉德殿前，却也引起了京师的变乱。袁绍等人趁机带兵冲入皇宫，大肆诛杀太监。洛阳卫军变乱后，段珪与张让等人劫持少帝刘辩、陈留王刘协出逃，后被杀。",
  guosheng: "郭胜，南阳宛（今河南南阳）人。汉灵帝时为中常侍，与张让、赵忠等狼狈为奸，并皆封侯。曾助同乡何进、何太后贵幸。后被袁绍等人率兵诛灭。",
  gaowang: "高望（？~189年），京兆人，东汉宦官，十常侍之一，为小黄门兼任尚药监，颇得皇子刘辩的信任。中平六年（189年），张让等人杀害大将军何进，袁绍与王匡等人率兵杀入雒阳端门，在承明堂上斩杀中常侍高望。",
  qianzhao: "牵招（？—231年），字子经，安平郡观津县（今河北省武邑县）人。三国时期曹魏名将。牵招早年师从乐隐，与刘备成为刎颈之交。投靠冀州牧袁绍，担任冀州从事，统领乌丸突骑。袁绍死后，跟随冀州刺史袁尚，受到并州牧高干加害，投奔丞相曹操，授军谋掾。从平乌丸，出任护乌丸校尉。举茂才出身，从平汉中，授中护军。拜平虏校尉，平定东莱郡叛乱。魏文帝曹丕时期，授护鲜卑校尉，驻扎昌平，迁右中郎将、雁门太守，遏制鲜卑轲比能发展壮大。魏明帝即位，赐爵关内侯，营救护乌丸校尉田豫。太和五年，去世。",
  fuqian: "傅佥（？~263年），义阳（治今湖北省枣阳市）人，蜀汉将领傅彤之子，三国时期蜀汉名将。金长于谋略，并颇有胆勇，姜维甚爱之。傅佥官至关中都督。魏国攻伐蜀汉时，傅佥和蒋舒防守阳安关，兵败战死。",
  wangjun: "王濬（207年～286年），字士治，小名阿童，弘农郡湖县（今河南省灵宝市阌乡）人。西晋时期名将。王濬出身世家，博学多闻，容颜英俊，多谋善战。举秀才出身，起家河东郡从事。泰始八年（272年），担任广汉太守，平定益州叛乱，迁益州刺史。利用长江上游地势之利，修造战船，组建强大的水军。上书晋武帝，促成晋灭吴之战。咸宁六年（280年），率兵顺流而下，熔毁横江铁链，攻克丹阳郡，率先攻取石头城，接受吴末帝孙皓投降，完成西晋统一大业。凭借功勋，拜辅国将军、步兵校尉，册封襄阳侯。为避猜忌，纵情享受，累迁特进、抚军大将军、开府仪同三司、散骑常侍、后军将军等。太康六年十二月（286年1月18日），王濬去世，享年八十岁，谥号为“武”，安葬于柏谷山。",
  yangfu: "杨阜（172年—244年），字义山，汉天水冀县（今甘谷县东南）人。三国时期曹魏名臣。汉献帝建安初年，任凉州从事，旋拜安定长史；韦康任刺史后辟为别驾，改任州参军；后因讨马超有功，赐爵关内侯。曹操征汉中时，杨阜担任益州刺史，回来后又担任武都太守。魏明帝时，由将作大匠改少府。杨阜不但卓识远见，而且刚正不阿，敢于直言，对朝廷弊政多有诤谏，六次进言谏魏明帝应勤政爱民，魏明帝对他颇有敬畏之心。杨阜勤政廉洁，在宫内担任少府时，专管宝器、珍膳、衣物等，而他死后则家无馀财。皇帝让杨阜的孙子杨豹继任了少府之职。原甘谷县文昌宫西侧有杨氏家祠，内悬“两代尚书”匾额。",
  ruanhui: "阮氏女，是指三国时期曹魏名士许允之妻阮氏，陈留尉氏人。阮氏女是中国古代四大丑女之一，貌丑而见识非凡。她出身士族之家，是卫尉阮共（字伯彦）之女、阮侃（字德如）之妹。嫁与许允后生有二子：许奇，官至司隶校尉；许猛，官至幽州刺史。",
  peixiu: "裴秀（224年－271年4月3日），字季彦。河东郡闻喜县（今山西省闻喜县）人。魏晋时期名臣、地图学家，东汉尚书令裴茂之孙、曹魏光禄大夫裴潜之子。出身河东裴氏，少年时便颇有名气，后被大将军曹爽辟为掾属，袭爵清阳亭侯，又迁黄门侍郎。高平陵之变后，因是曹爽的故吏而被罢免。此后历任廷尉正、安东将军及卫将军司马，参与谋划军国之政，参与平定诸葛诞叛乱。因功转任尚书，进封鲁阳乡侯。魏元帝继位后，进爵鲁阳县侯，任尚书仆射。咸熙元年（264年），受命负责修改官制，又提议恢复五等爵制。五等制恢复后，获封济川侯。他建议立司马炎为世子，司马炎继位晋王后，拜裴秀为尚书令、右光禄大夫。西晋建立后，加左光禄大夫，封钜鹿郡公。泰始三年（267年），升任司空。泰始七年（271年），裴秀因服食寒食散后饮冷酒而逝世，年四十八，谥号“元”。有文集三卷。裴秀作《禹贡地域图》，开创了中国古代地图绘制学。李约瑟称他为“中国科学制图学之父”，与古希腊著名地图学家托勒密齐名，是世界古代地图学史上东西辉映的两颗灿烂明星。为纪念这位中国地图科学创始人而设立的“裴秀奖”，每两年评选一次，是中国地图学界最高奖项。由于他的贡献突出，联合国天文组织将月球正面的一个环形山命名为“裴秀环形山”。",
  liucheng: "游卡桌游的原创人物。设定为黄忠的妻子。",
  jiangwan: "蒋琬（？－246年），字公琰。零陵郡湘乡县人。三国时期蜀汉政治家，与诸葛亮、董允、费祎合称“蜀汉四相”。蒋琬最初随刘备入蜀，为广都县长。因其不理政事，惹怒刘备，在诸葛亮的劝说下才免于一死。后重获启用，受到诸葛亮的悉心培养，累官丞相长史兼抚军将军。建兴十二年（234年），诸葛亮去世，蒋琬继其执政，拜尚书令，又加行都护、假节，领益州刺史，再迁大将军，录尚书事，封安阳亭侯。延熙元年（238年），受命开府，加大司马，总揽蜀汉军政。曾制定由水路进攻曹魏的计划，但未被采纳。延熙九年（246年），蒋琬病逝，谥号为恭。",
  sunhanhua: "孙寒华，三国时女仙。梁·陶弘景《真诰》卷一三、五代·杜光庭《墉城集仙录》卷七：其父孙贲为孙权堂兄，寒华少时与杜契有私情，后从杜契受玄白之要，颜容日少。周旋吴越诸山十馀年，得道仙去。《鉴后集》卷四：一云即吴大帝孙权之女。于茅山修道，道成，冲虚而去，因号其山为华姥山。山在茅山崇禧观前。《神异典》卷二五九人于明代，误。",
  yanpu: "阎圃，生卒年不详，是益州巴西安汉（今四川省南充市）人。张鲁割据巴、汉，就以阎圃为功曹。东汉末年人物，本为张鲁部下谋士，后随张鲁一同投降曹操，封平乐乡侯。",
  mayuanyi: "马元义（？—184年6月7日），东汉末年黄巾之乱的组织者之一，是黄巾大方首领。光和六年（183年），马元义奉黄巾军首领张角之命，筹划起事的准备工作。马元义聚集荆州、扬州信徒数万人约定于邺城相会，并收买中常侍封谞、徐奉等为内应，准备于次年（甲子年）三月初五（戊申日）内外同时起事。由于张角弟子济南人唐周的告密，马元义于于中平元年（184年）正月在河内郡山阳县被捕，送往洛阳车裂，同时被杀的还有教众近千人。由于马元义的被杀，迫使张角提前于二月起兵发动叛乱。",
  simafu: "司马孚（180年－272年4月3日），字叔达。河内郡温县（今河南温县）人。三国曹魏至西晋初年重臣，西晋宗室，东汉京兆尹司马防第三子，晋宣帝司马懿之弟。自曹操时代起，司马孚就任文学掾，而后历仕魏国五代皇帝，累迁至太傅。司马孚在“高平陵事变”之际，协助司马懿控制京师，诛杀曹爽一党。后又督军成功防御吴、蜀的进攻，为司马氏政权的稳固多有功劳。但他性格十分谨慎，自司马懿执掌大权起，便逐渐引退，更未直接参与司马氏废立魏帝的谋划。西晋代魏后，司马孚进拜太宰，封安平王。晋武帝司马炎对他十分尊宠，但他并不以此为荣，至死仍以魏臣自称。泰始八年二月壬辰日（272年4月3日），司马孚去世，年九十三。武帝为其举哀三日，葬礼规格比照东汉东平献王刘苍，赐谥献王。",
  sunchen: "孙綝（231年－259年1月18日），字子通，吴郡富春（今浙江杭州市富阳区）人。三国时期吴国宗室、权臣，昭义中郎将孙静曾孙、定武中郎将孙暠之孙、安民都尉孙绰的儿子。门荫入仕，初任偏将军。太平元年（256年），升任侍中、武卫将军、都督中外诸军事。掌权以后，诛杀大司马滕胤、骠骑将军吕据等重臣，升任大将军，册封永宁县侯。嗜好杀戮，与皇帝孙亮的矛盾激化，最终废黜孙亮，拥立琅琊王孙休即位，累迁丞相、荆州牧。永安元年十二月（259年1月），在左将军张布的协助下，遭到皇帝孙休捕杀，时年二十八岁，削除宗籍，改姓故氏。",
  qiaozhou: "谯周（201年－270年），字允南，巴西郡西充国县（今四川西充县槐树镇）人。三国时期蜀汉大臣、学者、儒学家、史学家。幼年丧父，少读典籍，精研六经，颇晓天文，成为蜀地大儒之一，门下有陈寿、罗宪等学生。诸葛亮担任益州牧时，授劝学从事。诸葛亮死后，不顾朝令，前往奔丧。后主刘禅册立太子时，授太子仆，迁太子家令、光禄大夫。反对姜维北伐，著《仇国论》，力陈北伐之失。炎兴元年（263年），邓艾攻打成都时期，力劝刘禅投降，“有全国之功”，投降魏国后，受封阳城亭侯，迁骑都尉。泰始六年（270年），授散骑常侍，同年去世，时年七十岁。",
  gongsunkang: "公孙康（生卒年不详），辽东襄平（今辽宁辽阳）人。东汉末年辽东地区割据军阀，辽东太守公孙度长子（一作庶子）、车骑将军公孙恭之兄。公孙康在其父死后继任辽东太守。建安十二年（207年），擒斩图谋不轨的袁尚、袁熙兄弟，将其首级献予曹操，被拜为左将军，封襄平侯。建安十四年（209年），公孙康大破高句丽，陷其国都，并讨伐韩濊，设置带方郡。死后因二子年幼，由公孙恭继任。曹丕称帝后，被追赠为大司马。",
  zhouqun: "周群（生卒年不详），字仲直，巴西阆中（今四川阆中）人。周群年少时随父周舒学习占验天算之术。后来刘璋聘请周群为师友从事，其预言大多得以应验。刘备割据益州，任命周群为儒林校尉。曾劝说刘备攻汉中必定不利，事后得到应验，被举荐为茂才。",
  dongzhao: "董昭（156年－236年7月4日），字公仁，济阴定陶（今山东省菏泽市定陶区）人。东汉末年曹魏谋士、重臣、开国元勋。董昭年轻时被举为孝廉，后担任袁绍帐下参军。多有战功，但是袁绍听信谗言，董昭不得已离开袁绍投奔张杨。张杨率军迎接汉献帝时，董昭随行，并拜为议郎。后与曹操在洛阳相见，又建议曹操将汉献帝迎接到许昌。董昭自此成为曹操的谋士。建安年间（198年）历任河南尹、冀州牧、徐州牧、魏郡太守等职，多有功劳，深受曹操器重。曹操受封魏公、魏王的谋划都是出自董昭之手。曹丕继魏王位后，董昭任将作大匠。曹丕称帝后，升任大鸿胪。此后先后担任侍中、太常、光禄大夫、太仆等重要职位。魏明帝曹叡即位后转任卫尉。太和六年（232年）升任司徒。青龙四年（236年）卒，时年八十一，谥曰定。",
  nanhualaoxian: "南华老仙，是古典小说《三国演义》中的虚拟人物。其原型来自道教典籍中对庄子的封号，又称“南华仙人”、“南华真人”等。在古典小说《三国演义》通行本的第一回中，描述了南华老仙将《太平要术》赠送给张角的情节。",
  chenzhen: "陈震（？—公元235年），字孝起。荆州南阳郡（今河南南阳）人。三国时期蜀汉官员。刘备领荆州牧时，辟陈震为从事。后随刘备入蜀，为蜀郡北部都尉、汶山太守、犍为太守。建兴三年（225年），拜尚书，迁尚书令。建兴七年（229年），孙权称帝。蜀汉以陈震为卫尉，前往祝贺，与孙权开坛歃盟，交分天下。还蜀，封城阳亭侯。建兴十三年（235年），卒。",
  hucheer: "胡车儿（生卒年不详），东汉末年武将，初从张绣，为其心腹猛将，勇冠三军，与贾诩交情甚佳。宛城大战后，张绣投降曹操，曹操爱胡车儿之骁勇，手以黄金与之。后因曹操私纳张绣亡叔张济的遗孀邹氏，张绣深感其辱，欲杀曹操，与贾诩商议后决心反曹。《三国演义》中，作者考虑到典韦的勇猛，便增加了令胡车儿盗走典韦的双戟的情节。最终典韦、曹昂（曹操长子）、曹安民（曹操侄子）皆死于此次战斗。野史说胡车儿跟随曹操征战，被赵云在长坂坡上红枪挑死。",
  simashi: "司马师（208年～255年3月23日），字子元，河内温县（今河南省温县）人。三国时期曹魏权臣，西晋王朝的奠基人之一，晋宣帝司马懿与宣穆皇后张春华的长子，晋文帝司马昭的同母兄，晋武帝司马炎的伯父。司马师沉着坚强，雄才大略，早年与夏侯玄、何晏齐名。高平陵政变后，以功封长平乡侯，旋加卫将军。司马懿死后，以抚军大将军辅政，独揽朝廷大权，次年升为大将军。掌权后，他制定选拔官吏的法规，命百官推荐贤才，整顿纲纪，使其各有职掌，朝野肃然。司马师也有卓越的军事才能，曾用计于新城之战击溃吴国诸葛恪的大军。嘉平六年（254年），魏帝曹芳与中书令李丰等人密谋除司马师，事情泄露，司马师杀死参与者，迫郭太后废曹芳，改立高贵乡公曹髦为帝。次年，亲自率兵平定毌丘俭、文钦之乱。回师途中病死，时年四十八岁，谥号“忠武”。后被追尊为晋景王。西晋建立后，被追尊为景皇帝，庙号世宗。",
  yanghuiyu: "羊徽瑜（214年－278年），泰山南城（今山东新泰）人，晋景帝司马师第三任妻子。羊徽瑜出身官宦世家泰山羊氏，她是南阳太守羊续的孙女，上党太守羊衜之女；其母为东汉名士左中郎将蔡邕之女、蔡文姬的姐妹。羊徽瑜聪慧贤德，嫁给司马师后未有子女。以司马师之弟司马昭的次子司马攸为继子。司马师死后，司马攸侍奉羊徽瑜非常孝顺。泰始元年（265年），司马昭长子司马炎受禅登基，建立西晋，追谥伯父司马师为景皇帝。泰始二年（266年），尊奉羊徽瑜为景皇后，因居弘训宫，故称弘训太后。咸宁四年（278年），羊徽瑜去世，时年六十五岁，谥号景献皇后，与司马师合葬峻平陵。",
  dingyuan: "丁原（？—189年），字建阳，兖州泰山郡南城县（山东省平邑县郑城镇）人。东汉末年军阀，官拜执金吾、并州刺史。任并州刺史期间，他亲近、善待主簿吕布，又调遣部下张杨、张辽等名将到洛阳任职。汉灵帝驾崩后，带兵进入洛阳，担任执金吾，并与大将军何进谋划诛杀十常侍，但不久事情败露，何进被宦官杀死。在宦官被诛灭之后，丁原与军阀董卓在废帝问题上意见不合产生矛盾，于是董卓诱使吕布将其杀害，其势力亦被吞并。",
  furong: "傅肜（？－222年），义阳（今湖北枣阳）人，三国时蜀汉将领。刘备攻伐吴国时，傅肜为别督。后刘备被陆逊击败，傅肜率部断后，奋战至死。死前怒斥道：“吴狗！何有汉将军降者！”",
  zhengxuan: "郑玄（127年－200年），字康成。北海郡高密县（今山东省高密市）人。东汉末年儒家学者、经学大师。郑玄曾入太学攻《京氏易》、《公羊春秋》及《三统历》、《九章算术》，又从张恭祖学《古文尚书》、《周礼》和《左传》等，最后从马融学古文经。游学归里之后，复客耕东莱，聚徒授课，弟子达数千人，家贫好学，终为大儒。党锢之祸起，遭禁锢，杜门注疏，潜心著述。晚年守节不仕，却遭逼迫从军，最终病逝于元城，年七十四。郑玄治学以古文经学为主，兼采今文经学。他遍注儒家经典，以毕生精力整理古代文化遗产，使经学进入了一个“小统一时代”。著有《天文七政论》、《中侯》等书，共百万余言，世称“郑学”，为汉代经学的集大成者。唐贞观年间，列郑玄于二十二“先师”之列，配享孔庙。宋代时被追封为高密伯。后人建有郑公祠以纪念。",
  dengzhi: "邓芝（178年－251年），字伯苗。义阳郡新野县（今河南新野）人。东汉名将邓禹之后，三国时期蜀汉重臣。邓芝早年曾被预言能位至大将，后被刘备任为郫令，升迁为广汉太守。因任官公廉且有治绩，被征入朝为尚书。刘备逝世后，奉命出使吴国，成功修复两国关系，并深为吴大帝孙权所赏识。建兴六年（228年），丞相诸葛亮策划北伐，命邓芝与大将赵云佯攻郿城，以吸引魏国曹真军主力。建兴十二年（234年），迁前军师、前将军，领兖州刺史，封阳武亭侯，不久督领江州。延熙六年（243年），迁车骑将军，后授假节。又率军平定涪陵叛乱。延熙十四年（251年），邓芝病逝。邓芝性格正直、简单，不刻意修饰情绪。他为将二十多年，赏罚明断，体恤士卒。身上的衣食取自官府，从未经营过私产，妻儿甚至还有忍饥挨饿之时，死时家中也没有多余财物。",
  yangyi: "杨仪（？－235年），字威公，襄阳（今湖北襄阳）人，三国时期蜀汉官员。最初为荆州刺史傅群的主簿，后投奔关羽，任为功曹。关羽遣其至成都，大受刘备赞赏，擢为尚书。因与尚书令刘巴不和，调为弘农太守。建兴三年（225年）任丞相参军，此后一直跟随诸葛亮战斗。亮卒，他部署安全退军。诸葛亮在生前定蒋琬继己任，杨仪仅拜中军师。建兴十三年（235年），因多出怨言，被削职流放至汉嘉郡。但杨仪仍不自省，又上书诽谤，言辞激烈，最后下狱，自杀身亡。",
  dongcheng: "董承（？~200年），字号不详，冀州河间人（今河北献县）人。东汉末年外戚大臣，汉灵帝母亲董太后侄子，汉献帝嫔妃董贵人之父。初从西凉军，为董卓女婿牛辅部曲。护卫汉献帝刘协从长安东归洛阳，拜为卫将军，受封列侯。建安四年（199年），拜车骑将军。自称领受汉献帝衣带诏，联合刘备、种辑、吴子兰、王子服、吴硕等人密谋诛杀曹操。五年正月，图谋泄露，董承诸人及董贵人全部遇害。",
  chendeng: "陈登（163—201），字元龙，下邳淮浦（今江苏涟水西）人。东汉末年将领、官员。沛相陈珪之子。为人爽朗，性格沈静，智谋过人，少年时有扶世济民之志，并且博览群书，学识渊博。二十五岁时，举孝廉，任东阳县长。虽然年轻，但他能够体察民情，抚弱育孤，深得百姓敬重。后来，徐州牧陶谦提拔他为典农校尉，主管一州农业生产。他亲自考察徐州的土壤状况，开发水利，发展农田灌溉，使汉末迭遭破坏的徐州农业得到一定程度的恢复，百姓们安居乐业，“秔稻丰积”。建安初奉使赴许，向曹操献灭吕布之策，被授广陵太守。以灭吕布有功，加伏波将军。又迁东城太守。年三十九卒。其子陈肃，魏文帝时追陈登之功，为郎中。",
  yangbiao: "杨彪（142年－225年），字文先。弘农郡华阴县（今陕西华阴）人。东汉末年名臣，太尉杨赐之子、名士杨修之父。杨彪少年时受家学熏陶，初举孝廉、茂才。其后征拜为议郎，参与续写《东观汉记》。任京兆尹时，揭发黄门令王甫的贪赃行为，使其被定罪诛杀。累任侍中、五官中郎将、卫尉等职。汉献帝时，遍历三公（司空、司徒、太尉）之职。权臣董卓意图迁都时，他据理力争。献帝东迁时，杨彪尽节护主。曹操挟献帝定都许县后，将杨彪下狱，获释后又任太常。他见汉室衰微，便诈称脚疾，不理世事。曹魏建立后，拒绝出任太尉，改光禄大夫，待以客礼。黄初六年（225年），杨彪去世，年八十四。其著作现仅存《答曹公书》一篇。",
  zhangyì: "张翼（？－264年），字伯恭，益州犍为郡武阳县人。三国时期蜀汉将领。历任梓潼、广汉、蜀郡三郡太守，出任庲降都督，后随诸葛亮和姜维北伐，官至左车骑将军，领冀州刺史。初封关内侯，进爵都亭侯。蜀汉灭亡后，魏将钟会密谋造反，成都大乱，张翼亦为乱兵所杀。张翼是蜀汉第三任庲降都督，由于执法严厉，不得南夷欢心。在北伐上，张翼认为国小民疲，不应滥用武力，是蜀汉朝廷当时极少敢当朝和姜维争辩北伐问题的大臣。",
  jiakui: "贾逵（174年—228年），本名贾衢，字梁道，河东襄陵人（今山西临汾县）。汉末三国时期魏国名臣，西晋开国功臣贾充父亲。初为并州郡吏，迁渑池县令，拜弘农太守，历仕曹操、曹丕、曹叡三世，是曹魏政权中具有军政才干的人物，终其一生为魏国统一事业作出卓越贡献。担任豫州刺史期间，兴修水利，凿通运河二百余里，时称“贾侯渠”，便利民生。随同曹丕伐吴，进封阳里亭侯，加号建威将军。石亭之战，率军救出曹休。太和二年，去世，赠本官，谥号为肃，《唐会要》将其尊为魏晋八君子之一。",
  shenpei: "审配（？－204年），字正南，魏郡阴安（今河北清丰北）人。为人正直， 袁绍领冀州，审配被委以腹心之任，并总幕府。河北平定，袁绍以审配、逢纪统军事，审配恃其强盛，力主与曹操决战。曾率领弓弩手大破曹军于官渡。官渡战败，审配二子被俘，反因此受谮见疑，幸得逢纪力保。袁绍病死，审配等矫诏立袁尚为嗣，导致兄弟相争，被曹操各个击破。曹操围邺，审配死守数月，终城破被擒，拒不投降，慷慨受死。",
  hujinding: "胡金定，女，传说中关羽之妻。关索之母，配偶关羽，出处《花关索传》和元代《三国志评话》民间传说人物。",
  zhangbu: "张布（？-264年），三国时吴国将领。张布在孙休受封为琅琊王时，为左右将督。太平三年（258年），孙休即位后，张布由长水校尉迁辅义将军，封永康侯。不久，孙休听说孙綝阴谋反叛，于是暗中和张布商量计策，孙綝被杀后，左将军张布因功加封中军督。由于孙休对张布厚加恩宠，因此张布专擅朝廷大权，多行无礼之事。永安七年（264年），孙休去世，丞相濮阳兴、左将军张布推荐孙皓为帝。同年八月初三，孙皓即皇帝位，封张布为骠骑将军，加侍中。孙皓粗暴骄盈、暴虐治国，又好酒色，濮阳兴和张布暗地里感到后悔，有人将此事报告孙皓，濮阳兴和张布二人被诛。",
  mb_wangjing: "王经（？—260年），字彦纬，冀州清河郡人，三国时代曹魏大臣。《三国志》无传。王经为农民出身，因得到同乡崔林的赏识，被提拔任官。其母说他太快出头会不吉利，但他平步青云，历任江夏太守、雍州刺史。正元二年（255年），蜀将姜维攻入陇西郡时，他率军出狄道城迎击蜀军，却被击败。被包围在城中，陷入穷途末路的境况。幸亏得到大将陈泰和邓艾的援助，合力击破姜维，才脱险。此后，他被朝廷召回。不久迁司隶校尉、尚书。甘露五年（260年），魏帝曹髦召见王沈、王经、王业，提出进讨司马昭的计划。王经进谏，但曹髦不听；王沈、王业向司马昭告密，王经不从。司马昭弑君后，王经因未向司马昭告急，而和其母一同被逮捕并被处死。"
};
const characterFilters = {
  simashi(mode) {
    if (["boss", "chess", "tafang", "stone"].includes(mode)) {
      return false;
    }
    if (mode == "versus") {
      return _status.mode != "three";
    }
    return true;
  }
};
const characterTitles = {
  //mb_dingshangwan: "",
  //re_wangji: "",
  mb_shen_caocao: "天下归心",
  re_zhoufei: "舫玉游鸾",
  re_yanyan: "皎日震邦",
  //mb_gaoxiang: "",
  mb_cuimao: "日出月盛",
  zhuji: "克绍箕裘",
  mb_cheliji: "北境寒锋",
  mb_zhongyao: "正楷萧曹",
  mb_wutugu: "霸体金刚",
  mb_chengyu: "泰山捧日",
  mb_dongbai: "魔姬",
  mb_caochun: "纲纪督御",
  mb_zhugeguo: "祈福安宁",
  //mb_xiahoumao: "",
  mb_tw_zhangji: "边安人宁",
  o_tanshihuai: "最终恐惧",
  o_lvbu: "人中赤兔",
  o_gaoshun: "白刃对决",
  o_sunquan: "合肥战神",
  mb_zhangzhi: "一笔飞白",
  mb_caozhi: "行驰道中",
  mb_zhouyu: "琴音同鸣",
  mb_liuhui: "理推数算",
  mb_lingju: "情随梦逝",
  hefei_zhangliao: "气夺三军",
  hefei_lidian: "义忘私隙",
  hefei_yuejin: "折冲御晦",
  //hanxuan: "",
  xin_simayi: "控权曹魏",
  mb_cuilingyi: "戴冠金尾",
  mb_luyu: "守礼贞固",
  mb_sunluyu: "舍身饲虎",
  mb_caohong: "福将",
  mb_guanyinping: "武姬",
  re_heqi: "马踏群峦",
  re_weiwenzhugezhi: "帆至夷洲",
  dongcheng: "沥胆卫汉",
  yangyi: "孤鹬",
  taoqian: "膺秉温仁",
  re_wangyun: "忠魂不泯",
  jiakui: "肃齐万里",
  re_zhanggong: "西域长歌",
  sp_sufei: "诤友投明",
  re_xugong: "独计击流",
  dengzhi: "绝境外交家",
  furong: "危汉烈义",
  zhangyì: "亢锐怀忠",
  zhouqun: "后圣",
  chendeng: "雄气壮节",
  dingyuan: "饲虎成患",
  gongsunkang: "沸流腾蛟",
  hucheer: "夜盗神行",
  fuqian: "危汉绝勇",
  qiaozhou: "观星知命",
  ruanhui: "明察祸福",
  sp_maojie: "清公素履",
  wangjun: "顺流长驱",
  yanpu: "盱衡识势",
  mayuanyi: "黄天擎炬",
  xin_mamidi: "少传融业",
  hujinding: "怀子求怜",
  zhaotongzhaoguang: "翊赞季兴",
  yanghuiyu: "温慧母仪",
  liuye: "佐世之才",
  simazhao: "四海威服",
  yangbiao: "德彰海内",
  sp_caosong: "舆金贾权",
  sp_pengyang: "难别菽麦",
  liwei: "伏被俞元",
  xin_guozhao: "文德皇后",
  qianzhao: "威风远振",
  peixiu: "晋图开秘",
  yanxiang: "明尚夙达",
  xin_hansui: "雄踞北疆",
  re_pangtong: "凤雏",
  yangfu: "鹤德璋声",
  mb_ganfuren: "昭烈皇后",
  xin_huojun: "葭萌铁狮",
  laimin: "悖骴乱群",
  xin_wuban: "碧血的英豪",
  mb_huban: "昭义烈勇",
  sp_jianggan: "虚义伪诚",
  baoxin: "坚朴的忠相",
  mb_chengui: "弄辞巧掇",
  muludawang: "八纳洞主",
  yangfeng: "忠勇半途",
  mb_mengda: "怠军反复",
  mb_caoxing: "喙睛阴鸷",
  mb_qinghegongzhu: "蛊虿之谗",
  mb_wangjing: "青云孤竹",
  mb_sp_zhenji: "明珠锦玉",
  mb_xianglang: "积藏诱学",
  jiangji: "盛魏昌杰",
  zhangbu: "主胜辅义",
  wuke: "智略权谲",
  mb_xingdaorong: "零陵上将",
  mb_caocao: "峥嵘而立",
  sunru: "出水青莲",
  liuzan: "啸天亢声",
  pangdegong: "德懿举世",
  miheng: "鸷鹗啄孤凤",
  zhengxuan: "兼采定道",
  simashi: "摧坚荡异",
  nanhualaoxian: "冯虚御风",
  shichangshi: "祸乱纲常",
  sunhanhua: "挣绽的青莲",
  mb_zhangfen: "究械菁杰",
  mb_zerong: "持宗事魔",
  mb_simazhao: "堕节肇业",
  mb_simafu: "徒难夷惠",
  mb_wenqin: "淮山骄腕",
  mb_simazhou: "温恭克己",
  mb_sp_guanqiujian: "才识拔干",
  mb_caomao: "向死存魏",
  chengji: "劣犬良弓",
  lizhaojiaobo: "竭诚尽节",
  mb_jiachong: "凶凶踽行",
  friend_zhugeliang: "龙骧九天",
  friend_xushu: "潜悟诲人",
  friend_pangtong: "凤翥南地",
  friend_cuijun: "日奋金丝",
  friend_shitao: "月堕窠臼",
  xin_xiahoudun: "开疆烈血",
  xin_zhangfei: "雄猛震世",
  ld_yuanshu: "野心渐增",
  re_xiaoqiao: "矫情之花",
  xin_zhoutai: "历战之躯",
  re_pangtonh: "凤雏",
  re_sp_zhugeliang: "卧龙",
  re_xunyu: "王佐之才",
  re_dianwei: "古之恶来",
  re_yanwen: "虎狼兄弟",
  xin_yuanshao: "高贵的名门",
  re_dongzhuo: "魔王",
  re_sunjian: "武烈帝",
  re_zhurong: "野性的女王",
  re_liushan: "无为的真命主",
  re_dengai: "矫然的壮士",
  re_jiangwei: "龙的衣钵",
  re_caiwenji: "异乡的孤女",
  re_zhangzhang: "经天纬地",
  re_sunben: "江东的小霸王",
  xin_sunliang: "寒江枯木",
  re_guanqiujian: "镌功铭征荣",
  villain_daomaigou: "因利失考妣",
  re_xusheng: "江东的铁壁",
  re_lingtong: "豪情烈胆",
  ol_yujin: "讨暴坚垒",
  re_wuguotai: "慈怀瑾瑜",
  re_gaoshun: "攻无不克",
  re_caozhi: "八斗之才",
  xin_liaohua: "历尽沧桑",
  xin_caozhang: "黄须儿",
  re_liubiao: "跨蹈汉南",
  re_handang: "石城侯",
  xin_chengpu: "三朝虎臣",
  xin_gongsunzan: "白马将军",
  re_zhonghui: "桀骜的野心家",
  re_bulianshi: "无冕之后",
  re_liru: "魔仕",
  re_caozhen: "荷国天督",
  re_hanhaoshihuan: "中军之主",
  xin_jianyong: "悠游风议",
  xin_zhuran: "不动之督",
  xin_guohuai: "垂问秦雍",
  xin_panzhangmazhong: "擒龙伏虎",
  xin_fuhuanghou: "孤注一掷",
  re_yufan: "狂直之士",
  xin_zhoucang: "披肝沥胆",
  xin_caifuren: "襄江的蒲苇",
  xin_guyong: "庙堂的玉磐",
  xin_sunluban: "为虎作伥",
  xin_caozhen: "荷国天督",
  xin_jushou: "监军谋国",
  xin_wuyi: "建兴鞍辔",
  xin_zhuhuan: "中洲拒天人",
  re_chenqun: "万世臣表",
  xin_zhangyi: "通壮逾古",
  xin_sunxiu: "弥殇的景君",
  xin_quancong: "慕势耀族",
  xin_zhuzhi: "功崇信重",
  xin_caoxiu: "龙光骐骥",
  re_jikang: "合天琴师",
  mb_xuezong: "彬彬名儒",
  old_yuanshu: "南方霸主",
  re_baosanniang: "南中武娘",
  fx_baosanniang: "慕花之姝",
  shenpei: "正南义北",
  scs_zhangrang: "玉翎琢雉",
  scs_zhaozhong: "奉天司鸣",
  scs_sunzhang: "半身执礼",
  scs_bilan: "竭荒净尘",
  scs_xiayun: "截谋囚鸟",
  scs_hankui: "嘲嬉弄权",
  scs_lisong: "令指天元",
  scs_duangui: "逐书交困",
  scs_guosheng: "毋同岁凤",
  scs_gaowang: "龙凤交欢",
  sy_baosanniang: "绝色巾帼",
  old_zhangqiying: "禳祷西东",
  junk_xuyou: "献计投曹",
  old_bulianshi: "无冕之后",
  ol_maliang: "多谋善断",
  mb_shen_lvbu: "修罗之道"
};
const dynamicTranslates = {
  rejuzhan(player2, skill) {
    const bool = player2.storage[skill];
    let yang = "当你或者此状态下第三次有角色成为【杀】的目标后，你可以与此【杀】的使用者各摸一张牌，然后其本回合不能对你使用牌", yin = "当你或者此状态下第三次有角色使用【杀】指定目标后，你可以获得此【杀】的目标角色一张牌，然后你本回合不能对其使用牌";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技，游戏开始时，若你的势力为蜀，此技能的初始状态改为阴；", end = `。${get.poptip("rule_chengshi")}：你使用【杀】的次数上限+1，且此【杀】结算完毕后，你获得之。`;
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  mbkubai(player2, skill) {
    const level = player2.countMark(skill);
    if (!level) {
      return lib.translate[`${skill}_info`];
    }
    if (level === 1) {
      return lib.translate[`${skill}_suit_info`];
    }
    return lib.translate[`${skill}_number_info`];
  },
  mbweizhuang(player2, skill) {
    if (!player2) {
      return lib.translate[`${skill}_info`];
    }
    if (get.nameList(player2).includes("mb_cuilingyi")) {
      const skin = player2.skin[player2.name2 === "mb_cuilingyi" ? "name2" : "name"], index = lib.characterSubstitute["mb_cuilingyi"].map((i) => i[0]).indexOf(skin);
      if (index >= 0) {
        const trueSkill = `${skill}_${skin.slice(13, -1)}x`;
        return get.skillInfoTranslation(trueSkill, player2, false);
      }
    }
    return "这衣服，岂是你配穿的？";
  },
  mbfozong(player2) {
    const list = player2.getStorage("mbfozong");
    if (!list?.length) {
      return lib.translate["mbfozong"];
    }
    const colors = list?.map((i) => get.translation(i))?.join("和");
    return `锁定技，你的${colors}手牌不计入手牌上限，造成的伤害值和恢复值+1。`;
  },
  yizan_use(player2) {
    if (player2.storage.yizan) {
      return "你可以将一张基本牌当做任意基本牌使用或打出。";
    }
    return "你可以将两张牌（其中至少一张为基本牌）当做任意基本牌使用或打出。";
  },
  miaojian(player2) {
    return ["出牌阶段限一次。你可将一张【杀】当做刺【杀】使用，或将一张锦囊牌当做【无中生有】使用。", "出牌阶段限一次。你可将一张基本牌当做刺【杀】使用，或将一张非基本牌当做【无中生有】使用。", "出牌阶段限一次。你可视为使用一张刺【杀】或【无中生有】。"][player2.countMark("miaojian")];
  },
  shhlianhua(player2) {
    return ["当你成为【杀】的目标时，你摸一张牌。", "当你成为【杀】的目标时，你摸一张牌。然后你进行判定，若结果为黑桃，则取消此目标。", "当你成为【杀】的目标时，你摸一张牌。然后此【杀】的使用者选择一项：①弃置一张牌。②取消此目标。"][player2.countMark("shhlianhua")];
  },
  mobilexingxue(player2) {
    return lib.translate[(player2.storage.mobileyanzhu ? "mobilexingxuex" : "mobilexingxue") + "_info"];
  },
  shoufa(player2) {
    const zhoufa = player2.storage.zhoulin_zhoufa;
    const num = get.mode() == "doudizhu" ? 1 : 2;
    return `①当你于一回合首次造成伤害后，你可以选择一名距离${num}以内的角色。②每回合限五次，当你受到伤害后，你可以选择一名与你距离不小于${num}的角色。${!zhoufa ? "其随机执行以下一项：豹，其受到1点无来源伤害；鹰，你随机获得其一张牌；熊，你随机弃置其装备区的一张牌；兔，其摸一张牌" : ["其受到1点无来源伤害", "你随机获得其一张牌", "你随机弃置其装备区的一张牌", "其摸一张牌"][["豹", "鹰", "熊", "兔"].indexOf(zhoufa)]}。`;
  },
  mbxuetu(player2) {
    const bool = player2.storage.mbxuetu, status = player2.countMark("mbxuetu_status");
    if (status === 1) {
      return lib.translate.mbxuetu_achieve_info;
    }
    let yang = status === 0 ? "你可以令一名角色回复1点体力" : "你可以回复1点体力，然后令一名其他角色弃置两张牌", yin = status === 0 ? "你可以令一名角色摸两张牌" : "你可以摸一张牌，然后对一名其他角色造成1点伤害";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。出牌阶段限一次，", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  mbzuoyou(player2) {
    const bool = player2.storage.mbzuoyou;
    let yang = "你可以令一名角色摸三张牌，然后其弃置两张牌", yin = "你可以令一名角色获得1点护甲";
    if (bool) {
      yin = `<span class='bluetext'>${yin}</span>`;
    } else {
      yang = `<span class='firetext'>${yang}</span>`;
    }
    let start = "转换技。出牌阶段限一次，", end = "。";
    return `${start}阳：${yang}；阴：${yin}${end}`;
  },
  mbfunan(player2) {
    if (player2.storage.mbfunan_rewrite) {
      return lib.translate[`mbfunan_rewrite_info`];
    }
    return lib.translate[`mbfunan_info`];
  },
  mbxuehen(player2) {
    const storage = player2.storage.mbxuehen;
    let str = "当你每回合首次造成或受到伤害后，你可展示至多X张手牌（X为你已损失体力值），这些牌只能当不计入次数且无次数限制的【杀】使用或打出，直到你使用这些牌造成伤害";
    if (storage) {
      str += "，且你使用以此法转化的【杀】结算结束后摸一张牌";
    }
    str += "。";
    return str;
  },
  mbjieyuan(player2) {
    const beishui = player2?.getStorage("mbjieyuan_beishui", false);
    const removed = player2?.getStorage("mbjieyuan_removed", "");
    if (beishui) {
      if (removed === "damageSource") {
        return "锁定技，你受到伤害时，你可以选择一项：1、弃置一张红色牌，令此伤害-2；2、从牌堆中获得两张红色牌。";
      } else if (removed === "damage") {
        return "锁定技，你造成伤害时，你可以选择一项：1、弃置一张黑色牌，令此伤害+2；2、从牌堆中获得两张黑色牌。";
      }
    }
    return `你造成伤害时，你可以选择一项：1、弃置一张黑色牌，令此伤害+1；2、从牌堆中获得一张黑色牌。你受到伤害时，你可以选择一项：1、弃置一张红色牌，令此伤害-1；2、从牌堆中获得一张红色牌。${get.poptip("rule_beishui")}：删除另一个时机的效果，将伤害的增减、获得牌的数量改为2，然后失去背水选项。`;
  }
};
const perfectPairs = {};
const voices = {
  "#reqizhi1": "今非缓成之功，当先夺争城之利也。",
  "#reqizhi2": "先发有夺民之望，此用兵之势也！",
  "#reqizhi3": "将有专擅之权，方能临机制胜。",
  "#reqizhi4": "兵谋欲以奇胜，贵在临阵之判！",
  "#rejinqu1": "伐吴当有经年之备，岂争朝夕之间。",
  "#rejinqu2": "积蓄水陆之实，用时必有所为！",
  "#rejinqu3": "夫民逸则易虑，民苦则思难。",
  "#rejinqu4": "和远先和己身，定众当在己心。",
  "#re_wangji:die": "此生学行坚白，已无愧己身… ",
  "#wushuang_mb_shen_lvbu1": "盖世无双，何人能挡？",
  "#wushuang_mb_shen_lvbu2": "承受天神的怒火吧！",
  "#mbzhengbi1": "用人使天下白治，正身率一世之杰。",
  "#mbzhengbi2": "正时风谓之清雅，秉忠直四海归心。",
  "#mbzhengbi3": "共掌东曹，为君遴羽，为民择天。",
  "#mbzhengbi4": "清明颂于山野，天下莫不砥砺！",
  "#mbfengying1": "夫天道佐义，民望助顺，得者自成霸业。",
  "#mbfengying2": "如今之势，自是奉顺者昌，奉逆者亡。",
  "#mbfengying3": "为君为公，臣等至死不渝！",
  "#mbfengying4": "公既奉迎天子，则天下莫敌也。",
  "#mb_cuimao:die": "尽忠言非搏美名，问心无愧而己……",
  "#rejuzhan1": "纵公放虎为祸，老夫亦独守关前。",
  "#rejuzhan2": "川中壮士，岂是汝等可欺？",
  "#rejuzhan3": "若老夫壮年之时，必生擒此黑厮！",
  "#rejuzhan4": "哈哈哈哈，尽逞口舌之快，于战又有何益？",
  "#re_yanyan:die": "颜念将军仁义，愿为大汉献身。",
  "#mbfangxu1": "关将军，这一招教我如何？",
  "#mbfangxu2": "且看我如何制你。",
  "#mbfangxu3": "挽弓如月，一箭封喉。",
  "#mbfangxu4": "余生只愿与君度。",
  "#mbzhuguan1": "尔等贼子，休想进犯一步。",
  "#mbzhuguan2": "妾与夫君，共守此关！",
  "#mbzhuguan3": "大汉危亡，此关必竭力相保。",
  "#mbzhuguan4": "愿为大汉铜墙铁壁！",
  "#mblisuo1": "大胆贼人，安敢欺良家女子。",
  "#mblisuo2": "套索一出，还不手到擒来。",
  "#fx_baosanniang:die": "今生之缘，来世再续。",
  "#reliangyin1": "愿作深山木，枝枝连理生。",
  "#reliangyin2": "河汉清且浅，相去复几许。",
  "#reliangyin3": "少年夫妻老来伴，执手相看两不厌。",
  "#reliangyin4": "至高至明日月，至亲至疏夫妻。",
  "#rekongsheng1": "弦动人欲醉，声闻一时醒。",
  "#rekongsheng2": "静弹箜篌引，寄我悠悠心。",
  "#re_zhoufei:die": "相思相见知何日，此时此夜难为情。",
  "#potjiezhu1": "趁魏军退走，我等可急令进军。",
  "#potjiezhu2": "虽攻敌不足，然退守有余矣。",
  "#potjiezhu_zhuji_shadow1": "城头旌旗不倒，吴土寸步不让！",
  "#potjiezhu_zhuji_shadow2": "魏虏虽众，岂能撼我吴疆！",
  "#pothuanshi1": "当效父之胆，拒犯我之贼！",
  "#pothuanshi2": "美酒虽好，不可误我政事。",
  "#pothuanshi3": "养育之恩虽重，血脉之源难断！",
  "#potjianlv1": "今观曹魏之兵，更胜当年十倍。",
  "#potjianlv2": "蜀汉若灭，恐唇亡齿寒啊。",
  "#potjianlv3": "卫护江东，以全节义。",
  "#zhuji:die": "诸葛融何以引军不发，致我有此惨败。",
  "#zhuji_shadow:die": "哎，内忧至此，回天无力啊。",
  "#mbqucheng1": "今日驱车应敌，让蜀军见识我铁车的厉害。",
  "#mbqucheng2": "便用此车，击败汝等小儿。",
  "#mbqiangyong1": "诸葛小儿，可速献关纳降。",
  "#mbqiangyong2": "未想西凉旧部，今为蜀犬耳。",
  "#mbqiangyong3": "今日势破西平关，以震羌胡之威！",
  "#mbqiangyong4": "铁车军所向无敌，何惧蜀中孺子？",
  "#mb_cheliji:die": "诸葛妖人，害我雄师。",
  "#mbwusheng1": "斩奸除贼，誓卫汉祚！",
  "#mbwusheng2": "非某好杀，实诛不义！",
  "#mbwusheng3": "纵有百万贼兵，难挡关某一骑！",
  "#mbwusheng4": "身陷重围？正合吾杀敌之志！",
  "#mbyijue1": "财帛不足以动吾心，爵禄不足以移吾志！",
  "#mbyijue2": "身外之物，岂及兄弟手足之情？",
  "#mbyijue3": "今日放公一命，以报昔日之恩。",
  "#mbyijue4": "吾读春秋，岂不知大义所在？",
  "#o_guanyu:die": "非关某无情，实各为其主。",
  "#youlve1": "秋高马肥，正宜南下！",
  "#youlve2": "追兵在即，吾等还需亲身上阵！",
  "#lianxi1": "尔等孤军深入，真乃自投死路！",
  "#lianxi2": "汉室衰落，乌桓当兴！",
  "#o_tadun:die": "袁氏误我啊！",
  "#jujun1": "我等万箭齐下，敌军安可上得山来！",
  "#o_chenlan:die": "天柱虽险，难挡真虎将…",
  "#bixian1": "今日形势危急，少不得要出全力！",
  "#o_meicheng:die": "这张辽，果是一把好手…",
  "#mbshizhong1": "孤率大军亲征，今日誓破此城！",
  "#mbshizhong2": "今以众凌寡，岂有不克之理？",
  "#mbshizhong3": "而今兵临城下，尔等唯死而已！",
  "#mbcaowei1": "张辽不过死士八百，何敢逆我十万雄兵！",
  "#mbcaowei2": "敌军寡弱，卿等围而击之！",
  "#o_sunquan:die": "大业未成，当整军再战！",
  "#hefeichonglei1": "今日何须多言，不过一战而已！",
  "#hefeichonglei2": "纵对千峰万壑，亦当驱前踏破！",
  "#hefeidangshi1": "尔等退路已绝，还不下马受戮！",
  "#hefeidangshi2": "此皆吾敢死之士，尔等可有胆一战？",
  "#hefeidangshi3": "今承天威荡寇，贼子何不束手！",
  "#hefeidangshi4": "敌军已溃，急令追击！",
  "#hefei_zhangliao:die": "人生世间，谁无一死乎……",
  "#hefeiduanjin1": "此桥乃吴军退路要害，断之则已握胜机。",
  "#hefeiduanjin2": "分兵毁桥，阻援绝路，必可全胜而归。",
  "#hefeigaigong1": "往昔仇怨，今日尽消。",
  "#hefeigaigong2": "同为国家，岂存私念？",
  "#hefeigaigong3": "明公军令在此，典自当以大局为重。",
  "#hefeigaigong4": "此国家大事，吾可以私憾而忘公义乎？",
  "#hefei_lidian:die": "大丈夫死则死矣，唯声名不容有污！",
  "#hefeixianjian1": "纵尔固若金汤，不若吾刃之利！",
  "#hefeixianjian2": "攻城有进无退，吾当身先士卒！",
  "#hefeizherui1": "乐进在此，谁敢与吾决死？",
  "#hefeizherui2": "吾虽力竭，犹能再战！",
  "#hefeizherui3": "彼众我寡，正可显名！",
  "#hefeizherui4": "非好杀戮，实止干戈。",
  "#hefei_yuejin:die": "箭疮崩裂，犹可杀贼！",
  "#new_simayi:die": "洛水滔滔，难诉吾一生坎坷……",
  "#xinrenjie1": "朝中大小事宜，自有大将军定夺。",
  "#xinrenjie2": "朝论政事，老夫唯大将军马首是瞻",
  "#xinbaiyin1": "乱世已尽，老夫当再开万世河山！",
  "#xinbaiyin2": "明出地上，自昭天德，此为晋也。",
  "#xinjilve1": "三分一统，天下归一！",
  "#xinjilve2": "大权独揽，朝野皆平！",
  "#jilue1": "运筹成略，统军持国！",
  "#jilue2": "英雄皆殁，天命终归吾司马一族！",
  "#wansha_new_simayi1": "连诛其族，翦其党羽，以夷后患！",
  "#wansha_new_simayi2": "绝汝生死，断汝轮回！",
  "#lianpo_new_simayi1": "能战当战，不能战当死耳！",
  "#lianpo_new_simayi2": "连下诸城以筑京观，足永平辽东之患。",
  "#reguicai_new_simayi1": "风雨雷电，皆由老夫决之！",
  "#reguicai_new_simayi2": "天地造化，不过老夫一念之间！",
  "#fangzhu_new_simayi1": "此非老夫不仁，实乃汝咎由自取。",
  "#rejizhi_new_simayi1": "一策一划，皆为成吾之远图！",
  "#rezhiheng_new_simayi1": "轮回不止，因果不休。",
  "#mbjiren1": "久闻文远盛名，今日且试君斤两！",
  "#mbjiren2": "文远来得正好，顺正欲讨教一二！",
  "#mbjuezhi1": "刀剑无眼，文远小心提防！",
  "#mbjuezhi2": "闻君枪法通神，今日且来领教！",
  "#mbjuezhi3": "但较武艺，无论输赢！",
  "#mbjuezhi4": "此为切磋，点到为止！",
  "#o_gaoshun:die": "文远，真英雄也！",
  "#mblvezhen1": "哼！曹操万军，吾视之如草芥！",
  "#mblvezhen2": "吾当亲率兵马，掠阵斩此逆贼！",
  "#mbhengwei1": "曹操小儿尔等何虑？待其下寨吾自擒之！",
  "#mbhengwei2": "十八路诸侯尚惧我三分，况尔等小卒？",
  "#o_lvbu:die": "刘备小儿，安敢害我！",
  "#mblianzhan1": "城门已开，还不杀人城去！",
  "#mblianzhan2": "哈哈哈哈，连斩两员，痛快痛快！",
  "#mbkoulve1": "掠其粟米、焚其城郭，使汉人闻我名丧胆！",
  "#mbkoulve2": "血染黄沙、暴尸于野，此战必杀个痛快！",
  "#o_tanshihuai:die": "儿郎们暂退阴山，他日我必卷土重来！",
  "#wusheng_re_baosanniang1": "逆贼可识此套刀法! ",
  "#dangxian_re_baosanniang1": "夫君且让妾身先战! ",
  "#zhiman_re_baosanniang1": "蛮人亦为百姓，还须以柔制之。",
  "#mbgeyuan1": " 割之弥细，所失弥少，以至不可割。",
  "#mbgeyuan2": " 周径为至然之数，非周三径一之率也。",
  "#mbchongcha1": " 凡望极高、测绝深，必用重差。",
  "#mbchongcha2": " 重表连锁累距，乃为重差之法。",
  "#mb_liuhui:die": " 九章注述已尽，吾可安心去矣。",
  "#mbshouyue1": " 身无哀乐，乐无雅俗，但记己心而已。",
  "#mbshouyue2": " 听之忘忧，习之养性，诚为修身之宝。",
  "#mbdieyin1": " 琴音之变，在技，更在心。",
  "#mbdieyin2": " 弦振如雷奔，韵转若涡旋。",
  "#qinyin_mb_zhouyu1": " 散音沉沉，若松根盘壑。",
  "#qinyin_mb_zhouyu2": " 泛音粼粼，如江水流渚。",
  "#mb_zhouyu:die": " 伯牙碎琴酬知己，非止绝响更绝情。",
  "#mbchongsi1": " 仆夫早严驾，吾行将远游。",
  "#mbchongsi2": " 远游欲何之，吴国为我仇。",
  "#mbpeidong1": " 扶桑之所出，乃在朝阳溪。",
  "#mbpeidong2": " 中心陵苍昊，布叶盖天涯。",
  "#mbpeidong3": " 日出登东干，既夕没西枝。",
  "#mbpeidong4": " 愿得纤阳辔，回日使东驰。",
  "#mb_caozhi:die": " 闲居非吾志，甘心赴国忧。",
  "#mbshiju1": "凡家之衣帛，必先书而后练之。",
  "#mbshiju2": "临池学书，池水尽墨。",
  "#mbshiju3": "坐忘尘鞅，笔与神遇。",
  "#mbkubai1": "字之体势，须一笔而成。",
  "#mbkubai2": "偶有不连，而血脉不断。",
  "#mbkubai3": "专精一体，方致绝伦。",
  "#mbkubai4": "五合交臻，神融笔畅。",
  "#mbkubai5": "转精其巧，亦可后来居上。",
  "#mbkubai6": "一笔飞白，可堪独步无双。",
  "#mb_zhangzhi:die": "笔锋尽处，已无死生之隔。",
  "#mbbingfa1": "法者，天下之程式，万世之仪表。",
  "#mbbingfa2": "明主之国，无书简之文，以法为教。",
  "#mbbingfa3": "成有功则疏贱必赏，成有过则近爱必诛。",
  "#mbbingfa4": "赏偷，则功臣惰其业；赦罚，则奸臣易其非。",
  "#mbshuxing1": "事无目见耳闻，岂可臆断论罪？",
  "#mbshuxing2": "夫女子之情，以接见而恩生，成妇而义重。",
  "#mb_luyu:die": "臣闻君明则臣直，此所以有敢过谏也……",
  "#mbcaiqiu1": "衣为礼之大者，岂可草草而决？",
  "#mbcaiqiu2": "事关颜面，自是要百里挑一。",
  "#mbcaiqiu3": "贱婢好不知耻，竟敢效我衣着。",
  "#mbcaiqiu4": "想是夫君宠汝太甚，竟不知尊卑之礼。",
  "#mbxishang1": "此次观礼，我定可艳压群芳。",
  "#mbxishang2": "妾身天生丽质，岂可对镜独赏？",
  "#mbxishang3": "此等霓裳，方配此山野之趣。",
  "#mbxishang4": "既未逾礼，此衣何不可服？",
  "#mbxishang5": "虽身处闺阁，一不可自屈。",
  "#mbxishang6": "炫服靓妆，不过聊以自娱。",
  "#mbxishang7": "宝衣自当示人，然非等闲可观。",
  "#mbxishang8": "珠以蚌蔽之，衣以箧藏之。",
  "#mbweizhuang_guidian1": "出门难得，唯以自妆为乐。",
  "#mbweizhuang_guidian2": "当择一华服，以尽显妾身之美。",
  "#mbweizhuang_guidian3": "嘉宴在即，自要盛装以待。",
  "#mbweizhuang_guidian4": "衣饰岂可复着，当更新衣以庆。",
  "#mbweizhuang_dongjiao1": "貌美非吾本意，奈何难拒天恩。",
  "#mbweizhuang_dongjiao2": "沉鱼落雁难参比，质本天然第一流。",
  "#mbweizhuang_dongjiao3": "固为妾之所得，何来赏赐直说？",
  "#mbweizhuang_dongjiao4": "此衣妃者所服，贱婢岂敢逾制。",
  "#mbweizhuang_dongjiao5": "此婢不尊主母，该当何罪？",
  "#mbweizhuang_dongjiao6": "妾身受屈良久，夫君何以为谢？",
  "#mbweizhuang_xiuge1": "妾身之美，可入夫君眸中？",
  "#mbweizhuang_xiuge2": "夫君所选，自是雍容非凡。",
  "#mbweizhuang_xiuge3": "哼，尔等婢妾，岂配与我相较。",
  "#mbweizhuang_xiuge4": "景好使人悦，该当小酌一杯。",
  "#mbweizhuang_xiuge5": "面若春桃，亦需华服来饰。",
  "#mbweizhuang_xiuge6": "纵我舍得，夫君可否舍得？",
  "#mb_cuilingyi:die": "铅华难驻容千载，贵贱同归土一丘。",
  "#mblingfa1": "令行禁止，法外无情。",
  "#mblingfa2": "法令如山，岂容尔等造次！",
  "#mblingfa3": "吾明令在此，汝何以犯之？",
  "#mblingfa4": "法不阿贵，绳不挠曲！",
  "#rejianxiong_mb_caocao1": "是非是过，皆由后人评去。",
  "#rejianxiong_mb_caocao2": "征乌桓，平诸辽。孤，可为汉家将军第一人！",
  "#mb_caocao1:die": "孤的大业，还未开始……",
  "#mb_caocao2:die": "奸宦当道，难以匡正啊……",
  "#mbfutu1": "未度有情令得度，已度之者使成佛。",
  "#mbfutu2": "愿我得佛清净声，法音普及无边界。",
  "#mbfutu3": "慈悲为怀，普度众生。",
  "#mbfutu4": "我不入地狱，谁入地狱？",
  "#mbfutu5": "莫道世无因果，远近已在己身。",
  "#mbfutu6": "欲界无边烦恼，西天无挂无碍。",
  "#mbfutu7": "身口意业净，智慧乐多闻。",
  "#mbfutu8": "多欲为苦本，知足即富乐。",
  "#mbjingtu1": "拔诸勤苦生死根本，速成无上正等正觉。",
  "#mbjingtu2": "当信佛经语深，当信积善得福。",
  "#mbjingtu3": "钵器自然在前，饮食自然盈满。",
  "#mbjingtu4": "思衣得衣，思食得食，一切唯心造。",
  "#mbjingtu5": "众生速至宝刹，噤声闻吾妙音。",
  "#mbjingtu6": "以己为灯，清除自证。",
  "#mbjiebian1": "世事无常，因果不昧。",
  "#mbjiebian2": "常运慈心拨有情，度尽无边苦众生。",
  "#mbfozong1": "世尊割肉饲鹰，而吾舍身渡人。",
  "#mbfozong2": "诸恶莫作，众善奉行。",
  "#mbfozong3": "善恶报应，祸福相承，素来无谁代者。",
  "#mbfozong4": "信我法者，即可见佛。",
  "#mbfozong5": "而今现身说法，愿汝等皆得三昧。",
  "#mbfozong6": "离欲解脱，是为极乐。",
  "#mb_zerong:die": " 众生不悟，迷之者多！",
  "#mb_caoxing:die": "未想此子，竟恐怖如斯。",
  "#mbanxian1": "今日让尔，知我箭术的厉害！",
  "#mbanxian2": "暗挽雕弓，箭引霹雳之声！",
  "#mbjinzu1": "此箭一出，必丧我流矢之下！",
  "#mbjinzu2": "纵汝武艺通天，亦躲不过我穿心一箭。",
  "#mb_zhangyan:die": "与其四下奔走，不如归降曹公。",
  "#mbfeijing1": "我要看到赤地千里，荒无人烟！",
  "#mbfeijing2": "可知我军中飞燕的厉害！",
  "#mbfeijing3": "家家皆掠，鸡犬不留！",
  "#mbfeijing4": "怪汝时运不佳，栽在我的手里！",
  "#mbxiaoge1": "有此骁勇将士，我何战不可得胜！",
  "#mbxiaoge2": "此战收获颇丰，尔等当有奖赏。",
  "#mbxiaoge3": "小子，可能再与我战百八回合？",
  "#mbxiaoge4": "我也不以多欺少，可敢与我单挑？",
  "#mb_mengda:die": "啊？司马公何以如此神速……",
  "#mbjili1": "势利所加，改亲为雠，况非亲亲乎！",
  "#mbjili2": "臣委质以来，愆戾山积，臣犹自知，况于君乎？",
  "#mbjili3": "陛下思贤若渴，虚心侧席，智者莫不归命。",
  "#mbjili4": "臣窃慕陛下圣资，远来委质，岂为刘备刺客乎？",
  "#mbjili5": "臣固未可始终，然实为封欺人太甚！",
  "#mbjili6": "沧溟纵容百川，尚有平波之激荡。",
  "#mbjili7": "臣但镇疆御土，安有别图？",
  "#mbjili8": "臣之心也，陛下素有所知，今何可疑乎？",
  "#mbjili9": "臣虽卑鄙，亦知礼义二字。",
  "#mbshishu1": "达为形势所迫，实乃不得不行也。",
  "#mbshishu2": "臣过奉教于君子，愿君王勉之也。",
  "#mbzhuguo1": "以妾之微智，成国之远图。",
  "#mbzhuguo2": "此非一时之权，实乃万世之利。",
  "#mbzhuguo3": "宁与其生死一决，亦不可送质于彼。",
  "#mbanda1": "于先生亦助军作福，医护将士，不可杀之。",
  "#mbanda2": "汝新造江南，其事未集，安可诛此地英士？",
  "#wuke:die": "诸君竭辅仲谋，万事务以江东为虑。",
  "#mbkuangwu1": "呔！可听过我零陵上将的名号？",
  "#mbkuangwu2": "还敢夸口，待吾取汝狗头！",
  "#mbkuangwu3": "小的知错，求军师放我一条生路哇！",
  "#mbkuangwu4": "哎哟！将军，可否解气？",
  "#mb_xingdaorong:die": "这赵云，好生厉害呀……",
  "#friendqinying1": "虽穷不处亡国之势，虽贫不受污君之禄。",
  "#friendqinying2": "太公七十而不自达，孙叔敖三去相而不自悔。",
  "#friendqinying3": "但因明主未遇，故潜居抱道以待其时。",
  "#friendqinying4": "知命者待时而举，岂曰时无英雄乎？",
  "#friendlunxiong1": "英以其聪谋始，以其明见机，待雄之胆行之。",
  "#friendlunxiong2": "雄以其力服众，以其勇排难，待英之智成之。",
  "#friendgongli_shitao1": "天下失道，诸君可有意共匡社稷？",
  "#friendgongli_shitao2": "既志同道合，吾等何不一道？",
  "#friend_shitao:die": "空有一腔热血，却是报国无门。",
  "#friendshunyi1": "将军岂不知顺天者逸，逆天者劳乎？",
  "#friendshunyi2": "我本山野村夫，不足与将军论天下大事。",
  "#friendbiwei1": "何羡殿上公卿？徒惹一身铜臭。",
  "#friendbiwei2": "但见朱门酒肉臭，谁知人间疾苦久？",
  "#friendgongli_cuijun1": "一味不能合伊鼎之甘，独木岂能致邓林之茂？",
  "#friendgongli_cuijun2": "良友结，则辅仁之道弘矣。",
  "#friend_cuijun:die": "与君等交何其之快，只惜无再聚之日矣。",
  "#mbzengou1": "汝既负我在先，就休怪我心狠手辣。",
  "#mbzengou2": "哼，只有如此，方解我所受之辱。",
  "#mbzengou3": "有此把柄在手，教汝有口难言。",
  "#mbfeili1": "夏侯楙，事已至此，何必再惺惺作态。",
  "#mbfeili2": "怪我未下狠手，让你侥幸生还。",
  "#mb_qinghegongzhu:die": "夏侯楙徒有形表，实非良人。",
  "#friendyance1": "以今日之时局，唯以此策解之。",
  "#friendyance2": "今世变化难测，需赖以演策之术。",
  "#friendyance3": "百策百中，勤在推演而已。",
  "#friendyance4": "哎，算计百般，终是徒劳。",
  "#friendyance5": "吾已尽全力，奈何老天仍胜我一筹。",
  "#friendyance6": "未思周密，使敌有残喘之机。",
  "#friendyance7": "所谓算无遗策，亦不过如此而已。",
  "#friendfangqiu1": "一举可成之事，何必再增变数。",
  "#friendfangqiu2": "破敌便在此刻，吾等勿负良机。",
  "#friendfangqiu3": "哈哈哈哈，果不出我所料。",
  "#friendzhugelianggongli1": "其志远兮，当与诤友共进。",
  "#friendzhugelianggongli2": "共以济世为志，今与诸兄勉之。",
  "#friend_zhugeliang:die": "吾既得明主，纵不得良时，亦当全力一试。",
  "#friendxiaxing1": "大丈夫当行侠重义，仗剑天下。",
  "#friendxiaxing2": "路见不平，拔刀相助，乃侠者之义也。",
  "#friendqihui1": "天乃高且远，安可事事自下。",
  "#friendqihui2": "吾等当上体天心，下济黎民。",
  "#friendqihui3": "若除贪官恶吏，天下自为之一清。",
  "#friendxushugongli1": "吾等并力同心相通，大事何不可成哉。",
  "#friendxushugongli2": "以吾等之才，何不同辅一主，共成王霸之业。",
  "#friend_xushu:die": "百姓陷于苦海，而吾却难以济之。",
  "#friendmanjuan1": "十行俱下犹觉浅，一朝闭门书五车。",
  "#friendmanjuan2": "有此神目，何愁观之未遍。",
  "#friendyangming1": "但为国养士，为主选才耳。",
  "#friendyangming2": "贤人何其之多，但无识才之人也。",
  "#friendpangtonggongli1": "你我同有此志，更应砥砺共进。",
  "#friendpangtonggongli2": "三人同心，诸事可期。",
  "#friend_pangtong:die": "大事未竟，惜哉，惜哉。",
  "#mbzhijie1": "昔子罕不以玉为宝，《春秋》美之。",
  "#mbzhijie2": "今吴、魏未灭，安以妖玩继怀？",
  "#mbshushen1": "此者国亡之象，夫君岂不知乎？",
  "#mbshushen2": "为人妻者，当为夫计。",
  "#mb_ganfuren:die": "只愿夫君，大事可成，兴汉有期。",
  "#sidai": "趁贼军战意懈怠，出城破敌！",
  "#jieyu": "使君之托，峻虽粉身碎骨亦当御敌。",
  "#tongqu1": "断山蓄水，以备不虞之变。",
  "#tongqu2": "开渠联镇，但求百姓得利。",
  "#mbbojian1": "闻古者贤女，未有不学前事成败而以为己诫。",
  "#mbbojian2": "视字辄识，方知何为礼义。",
  "#mbjiwei1": "乱世之宝，非金银田产，而在仁心。",
  "#mbjiwei2": "匹夫怀璧为罪，更况吾豪门大族。",
  "#mbjiwei3": "左右乡邻，当共力时艰。",
  "#mbjiwei4": "民不逢时，吾又何忍视其饥苦。",
  "#mb_sp_zhenji:die": "悔入帝王家，万愿皆成空……",
  "#mbquchong1": "器有九距之备，亦有九攻之变。",
  "#mbquchong2": "攻城之机变，于此车皆可解之！",
  "#mbquchong3": "大攻起兮，可辟山海之艰！",
  "#mbquchong4": "破坚阵如朽木，履高城如平地。",
  "#mbxunjie1": "藏于心者竭爱，动于身者竭恭。",
  "#mbxunjie2": "修身如藏器，大巧若无工。",
  "#mb_zhangfen:die": "而立之年，未立功名，实憾也……",
  "#qiaomeng_xin_gongsunzan1": "夺汝兵刃战马，尔等必败无疑。",
  "#qiaomeng_xin_gongsunzan2": "摧敌似折枯，荡寇如反掌。",
  "#mbbifeng1": "众士暂避其锋，万不可冲撞圣驾。",
  "#mbbifeng2": "陛下今日所为，实令臣民失望。",
  "#mbbifeng3": "事已至此，当速禀南阙之急。",
  "#mbsuwang1": "居上处事，当极绥怀之仁。",
  "#mbsuwang2": "国治吏和，百姓自存怀化之心。",
  "#mb_simazhou:die": "臣所求唯葬伏太妃陵次，分国封四子而已……",
  "#mbbeiming1": "孛星起于吴楚，吾等应举刀兵！",
  "#mbbeiming2": "尽点淮南兵马，以讨司马逆臣！",
  "#mbchoumang1": "司马氏之罪，尽洛水亦难清！",
  "#mbchoumang2": "汝司马氏世受魏恩，今安敢如此！",
  "#mb_wenqin:die": "伺君兵败之日，必报此仇于九泉！",
  "#mbpanxiang1": "殿下当以国事为重，奈何效匹夫之孝乎？",
  "#mbpanxiang2": "诸君当早拜嗣君，以镇海内，而但哭邪？",
  "#mbpanxiang3": "身负托孤之重，但坐谈清论，此亦可乎？",
  "#mbpanxiang4": "老臣受命督军，自竭拒吴蜀于疆外。",
  "#mbchenjie1": "身为魏臣，终不背魏。",
  "#mbchenjie2": "杀陛下者，臣之罪也。",
  "#mb_simafu:die": "生此篡逆之事，罪臣难辞其咎……",
  "#mbzujin1": "蜀军远来必疲，今当先发以制。",
  "#mbzujin2": "错估军情，今唯退守敌道矣。",
  "#mbzujin3": "静守待援，不可中诱敌之计。",
  "#mbjiejian1": "昔鲁昭公败走失国，陛下因而更宜深虑。",
  "#mbjiejian2": "今权在其门，为日已久，陛下何以违抗。",
  "#mbjiejian3": "陛下何急一时，今当忍而待机啊。",
  "#mb_wangjing:die": "有母此言，今死之无悔……",
  "#mbchengxiong1": "天子殿前，岂容不洁之臣。",
  "#mbchengxiong2": "陛下有令，还不将其斩之。",
  "#mbwangzhuang1": "此事若入君耳，后果汝当自知。",
  "#mbwangzhuang2": "孙綝既诛，便是我一言之堂。",
  "#zhangbu:die": "悔不遵先帝遗命啊……",
  "#mbcuizhen1": "欲活命者，还不弃兵卸甲！",
  "#mbcuizhen2": "全军大进，誓讨司马乱贼！",
  "#mbkuili1": "此犹有转胜之机，吾等切不可自乱。",
  "#mbkuili2": "不患败战于人，但恐军心已溃啊。",
  "#mb_sp_guanqiujian:die": "汝不讨篡权逆臣，何杀吾讨贼义军……",
  "#mbqianlong1": "朕为天子，岂忍威权日去！",
  "#mbqianlong2": "朕行之决矣，正使死又何惧！",
  "#mbqianlong3": "权臣震主，竟视天子于无物！",
  "#mbqianlong4": "假以时日，必讨司马一族！",
  "#mbqianlong5": "若安司马于外，或则皇权可收。",
  "#mbqianlong6": "暗恤忠君之士，以待破局之机。",
  "#mbweitong1": "手无实权难卫统，朦胧成睡，睡去还惊。",
  "#mb_caomao:die": "纵不成身死，朕亦为太祖子孙，大魏君王……",
  "#mbkuangli1": "我已受命弑君，汝等还不散去！",
  "#mbkuangli2": "谁再聚众作乱，我就将其杀之！",
  "#mbxiongsi1": "既想杀人灭口，那就同归于尽！",
  "#mbxiongsi2": "贾充！你不仁就别怪我不义！",
  "#chengji:die": "汝等要卸磨杀驴吗？呃啊……",
  "#mbzuoyou1": "陛下亲讨乱贼，臣等安不随护！",
  "#mbzuoyou2": "纵有亡身之险，亦忠陛下一人。",
  "#mbshishou1": "此乃天子御驾，尔等谁敢近前！",
  "#mbshishou2": "吾等侍卫在侧，必保陛下无虞！",
  "#lizhaojiaobo:die": "陛下！尔等乱臣，安敢弑君，呃啊……",
  "#mbxuetu1": "天子依仗在此，逆贼安扰圣驾。",
  "#mbxuetu2": "末将救驾来迟，还望陛下恕罪。",
  "#mbxuetu3": "徐、扬粮草甚多，众将随我前往。",
  "#mbxuetu4": "哈哈哈哈，所过之处，粒粟不留。",
  "#mbweiming1": "诸位东归洛阳，奉愿随驾以护。",
  "#mbweiming2": "不遵皇命，视同倡乱之贼。",
  "#mbweiming3": "布局良久，于今功亏一篑啊！",
  "#yangfeng:die": "刘备！本共图吕布，何设鸿门相欺！",
  "#shoufa1": "虎豹豺狼，皆听我令！",
  "#shoufa2": "毒蛇恶蝎，奉旨而行！",
  "#zhoulin1": "本大王承天大法，岂与诸葛亮小计等同！",
  "#zhoulin2": "料一山野书生，安识我南中御兽之术！",
  "#yuxiang": "呃啊啊，好大的火光啊！",
  "#muludawang:die": "啊啊，诸葛亮神人降世，吾等难挡天威……",
  "#guimou1": "不过卒合之师，岂是将军之敌乎？",
  "#guimou2": "连鸡势不俱栖，依珪计便可一一解离。",
  "#zhouxian1": "今未有苛暴之乱，汝敢言失政之语。",
  "#zhouxian2": "曹将军神武应期，如何以以身试祸。",
  "#mb_chengui:die": "布非忠良之士，将军宜早图之……",
  "#mbyilie1": "禽兽尚且知义，而况于人乎？",
  "#mbyilie2": "班虽无名，亦有忠义在骨！",
  "#mbyilie3": "身不慕生，宁比泰山之重！",
  "#mb_huban:die": "生虽微而志不可改，位虽卑而节不可夺……",
  "#naxue1": "诸位正值年少，勿负此读书良时。",
  "#naxue2": "老夫积书万卷，可供诸生阅览。",
  "#yijie1": "《传》称师克在和不在众，此言天地和则万物生。",
  "#yijie2": "君臣和则国家平，九族和则家族兴。",
  "#mb_xianglang:die": "子曰：有教无类。惜哉，未入学者多矣……",
  "#laishou1": "黄耇鲐背，谓之永年。",
  "#laishou2": "养怡和之福，得乔松之寿。",
  "#laishou3": "福寿将终，竟未得期颐！",
  "#luanqun1": "年过杖朝，自是从心所欲，何来逾矩之理？",
  "#luanqun2": "位居执慎，博涉多闻，更应秉性而论！",
  "#laimin:die": "狂嚣之言，一言十过啊……",
  "#yichong1": "处椒房之尊，得陛下隆宠！",
  "#yichong2": "三千宠爱？当聚于我一身！",
  "#wufei1": "巫蛊实乃凶邪之术，陛下不可不察！",
  "#wufei2": "妾不该多言，只怕陛下为其所害。",
  "#xin_guozhao:die": "不觉泪下……沾衣裳……",
  "#xinwurong1": "平乱羌！抚蛮夷！开旧道！复驿亭！",
  "#xinwurong2": "识断明果，以肃越嵩千里蛮疆。",
  "#xinwurong3": "蛮不从化，化不及蛮，此嶷之过矣。",
  "#xin_zhangyi:die": "北伐未捷，臣必杀身以报陛下！",
  "#tianxiang_re_xiaoqiao1": "盈盈一笑，娇花照水~",
  "#tianxiang_re_xiaoqiao2": "月容花貌难自弃。",
  "#re_xiaoqiao:die": "周郎……等我……",
  "#rezhijian1": "为臣之道，在于直言无讳。",
  "#rezhijian2": "谏言或逆耳，于国无一害。",
  "#guzheng_re_zhangzhang1": "为君者，不可肆兴土木，奢费物力。",
  "#guzheng_re_zhangzhang2": "安民固国，方可思动。",
  "#re_zhangzhang:die": "只恨不能为东吴百姓再谋一日福祉……",
  "#mbshihe1": "此举关乎福祸，还请峭王明察！",
  "#mbshihe2": "汉乃天朝上国，岂是辽东下郡可比？",
  "#mbzhenfu1": "储资粮，牧良畜，镇外贼，抚黎庶。",
  "#mbzhenfu2": "通民户十万余众，镇大小夷虏晏息。",
  "#qianzhao:die": "治边数载，虽不敢称功，亦可谓无过……",
  "#shuangxiong_re_yanwen1": "哥哥，且看我与赵云一战！——先与他战个五十回合！",
  "#shuangxiong_re_yanwen2": "此战如有你我一人在此，何惧华雄？——定叫他有去无回！",
  "#re_yanwen:die": "不是叫你看好我身后吗……",
  "#reluoying1": "转蓬离本根，飘摇随长风。",
  "#reluoying2": "高树多悲风，海水扬其波。",
  "#rejiushi1": "乐饮过三爵，缓带倾庶羞。",
  "#rejiushi2": "归来宴平乐，美酒斗十千。",
  "#chengzhang1": "弦急悲声发，聆我慷慨言。",
  "#chengzhang2": "盛时不再来，百年忽我遒。",
  "#re_caozhi:die": "先民谁不死，知命复何忧？",
  "#redingpin1": "察举旧制已隳，简拔当立中正。",
  "#redingpin2": "置州郡中正，以九品进退人才。",
  "#refaen1": "法不可容之事，情或能原宥。",
  "#refaen2": "严刑峻法，万望慎行。",
  "#re_chenqun:die": "立身纯且粹，一死复何忧……",
  "#xinqingxi1": "此残兵败将，胜之若儿戏耳！",
  "#xinqingxi2": "有休在此，主公何虑？哈哈哈哈！",
  "#xin_caoxiu:die": "此战大败，休甚是羞惭啊……",
  "#fenli_xin_zhuhuan1": "为主制客，乃百战百胜之势。",
  "#fenli_xin_zhuhuan2": "诸位且与我勠力一战，自可得胜。",
  "#xinpingkou1": "等候多时，为的便是今日之胜。",
  "#xinpingkou2": "一鼓作气，击败疲敝之敌！",
  "#xin_zhuhuan:die": "为将不行前而为人下，非可生受之辱……",
  "#spdaming1": "幸蒙士元斟酌，诣公于葭萌，达命于蜀川。",
  "#spdaming2": "论治图王，助吾主成就大业。",
  "#spdaming3": "心大志广，愧公知遇之恩。",
  "#spxiaoni1": "织席贩履之辈，果无用人之能乎？",
  "#spxiaoni2": "古今天下，岂有重屠沽之流而轻贤达者乎？",
  "#sp_pengyang:die": "招祸自咎，无不自己……",
  "#zhujian1": "修橹筑楼舫，伺时补金瓯。",
  "#zhujian2": "连舫披金甲，王气自可收。",
  "#duansuo1": "吾心如炬，无碍寒江铁索。",
  "#duansuo2": "熔金断索，克敌建功！",
  "#wangjun:die": "问鼎金瓯碎，临江铁索寒……",
  "#sbanguo1": "感文台知遇，自当鞠躬尽瘁，扶其身后之业。",
  "#sbanguo2": "安国定邦，克成东南一统！",
  "#sbanguo3": "孙氏为危难之际，吾当尽力辅之！",
  "#xin_zhuzhi:die": "臣辅孙氏三代之业，今年近古稀，死而无憾……",
  "#sbbenxi1": "战事唯论成败，何惜此等无用之物？",
  "#sbbenxi2": "汝等惊弓之鸟，亦难逃吾奔战穷击！",
  "#sbbenxi3": "袍染雍凉落日，马过岐山残雪！",
  "#xin_wuyi:die": "吾等虽不惧蜀道之险，却亦难过这渭水长安……",
  "#jiebing1": "敌寇势大，情况危急，只能多谢阁下。",
  "#jiebing2": "将军借兵之恩，阜退敌后自当报还。",
  "#hannan1": "贼寇虽勇，阜亦戮力以捍！",
  "#hannan2": "纵使信布之勇，亦非无策可当！",
  "#yangfu:die": "汝背父叛君，吾誓，杀……",
  "#yijin1": "吾家资巨万，无惜此两贯三钱！",
  "#yijin2": "小儿持金过闹市，哼！杀人何需我多劳！",
  "#yijin3": "普天之下，竟有吾难市之职？",
  "#guanzong1": "汝为叔父，怎可与小辈计较！",
  "#guanzong2": "阿瞒生龙活虎，汝切勿胡言！",
  "#sp_caosong:die": "长恨人心不如水，等闲平地起波澜……",
  "#rejuece1": "束手就擒吧！",
  "#rejuece2": "斩草除根，以绝后患！",
  "#remieji1": "就是要让你无路可走！",
  "#remieji2": "你也逃不了！",
  "#xinfencheng_re_liru1": "千里皇城，尽作焦土！",
  "#xinfencheng_re_liru2": "荣耀、权力、欲望、统统让这大火焚灭吧！",
  "#re_liru:die": "吾等皆死于妇人之手矣！",
  "#reqiangxi1": "铁戟双提八十斤，威风凛凛震乾坤！",
  "#reqiangxi2": "勇字当头，义字当先！",
  "#re_dianwei:die": "汝等小儿，竟敢害我！拿命来！",
  "#chengye1": "勤学于未长，立志于未壮。",
  "#chengye2": "志在坚且行，学在勤且久。",
  "#chengye3": "承继族贤之业，弘彰孔儒之学。",
  "#buxu1": "今世俗儒穿凿，不加补续，恐疑误后学。",
  "#buxu2": "经籍去圣久远，文字多谬，当正定《六经》。",
  "#xin_mamidi:die": "袁公路！汝怎可欺我！",
  "#mingcha1": "明主可以理夺，怎可以情求之？",
  "#mingcha2": "祸见于此，何免之有？",
  "#jingzhong1": "妾所乏为容，试问君有几德？",
  "#jingzhong2": "君好色轻德，何谓百德皆备？",
  "#ruanhui:die": "贱妾茕茕守空房，忧来思君不敢忘……",
  "#sbyaoming1": "山不让纤介，而成其危；海不辞丰盈，而成其邃。",
  "#sbyaoming2": "取上方可得中，取下则无所得矣。",
  "#xin_quancong:die": "吾逐名如筑室道谋，而是用终不溃于成……",
  "#quhu_re_xunyu1": "驱虎伤敌，保我无虞。",
  "#quhu_re_xunyu2": "无需费我一兵一卒。",
  "#rejieming1": "因势利导，是为良计。",
  "#rejieming2": "杀身成仁，不负皇恩。",
  "#re_xunyu:die": "命不由人，徒叹奈何……",
  "#xinjianying1": "良谋百出，渐定决战胜势！",
  "#xinjianying2": "佳策数成，破敌垂手可得！",
  "#shibei_xin_jushou1": "只有杀身士，绝无降曹夫！",
  "#shibei_xin_jushou2": "心向袁氏，绝无背离可言！",
  "#xin_jushou:die": "授，无愧主公之恩……",
  "#reanxu1": "贤淑重礼，育人育己。",
  "#reanxu2": "雨露均沾，后宫不乱。",
  "#zhuiyi_re_bulianshi1": "化作桃园只为君。",
  "#zhuiyi_re_bulianshi2": "魂若有灵，当助夫君。",
  "#re_bulianshi:die": "今生先君逝，来世再侍君……",
  "#beige_re_caiwenji1": "人多暴猛兮如虺蛇，控弦披甲兮为骄奢。",
  "#beige_re_caiwenji2": "两拍张弦兮弦欲绝，志摧心折兮自悲嗟。",
  "#duanchang_re_caiwenji1": "雁飞高兮邈难寻，空断肠兮思愔愔。",
  "#duanchang_re_caiwenji2": "为天有眼兮，为何使我独飘流？",
  "#re_caiwenji:die": "今别子兮归故乡，旧怨平兮新怨长！",
  "#mbdaoshu1": "嗨！不过区区信件，何妨故友一观？",
  "#mbdaoshu2": "幸吾有备而来，不然为汝所戏矣。",
  "#mbdaoshu3": "亏我一世英名，竟上了周瑜的大当。",
  "#spdaizui1": "望丞相权且记过，容干将功折罪啊！",
  "#spdaizui2": "干，谢丞相不杀之恩！",
  "#sp_jianggan:die": "唉！假信害我不浅啊！",
  "#xingtu1": "制图之体有六，缺一不可言精。",
  "#xingtu2": "图设分率，则宇内地域皆可绘于一尺。",
  "#juezhi1": "复设五等之制，以解天下土崩之势。",
  "#juezhi2": "表为建爵五等，实则藩卫帝室。",
  "#peixiu:die": "既食寒石散，便不可饮冷酒啊……",
  "#re_gaoshun:die": "可叹主公知而不用啊！",
  "#reganlu1": "玄德实乃佳婿呀。",
  "#reganlu2": "好一个郎才女貌，真是天作之合啊。",
  "#buyi_re_wuguotai1": "有我在，定保贤婿无余！",
  "#buyi_re_wuguotai2": "东吴，岂容汝等儿戏！",
  "#re_wuguotai:die": "诸位卿家，还请尽力辅佐仲谋啊……",
  "#mobileyanzhu1": "计设辞阳宴，只为断汝头！",
  "#mobileyanzhu2": "何需待午正？即刻送汝行！",
  "#mobilexingxue1": "古者建国，教学为先，为时养器！",
  "#mobilexingxue2": "偃武修文，以崇大化！",
  "#xin_sunxiu:die": "不求外取城地，但保大吴永安……",
  "#bingqing1": "常怀圣言，以是自励。",
  "#bingqing2": "身受贵宠，不忘初心。",
  "#yingfeng1": "公内奉天子，外令诸侯，则天下可定。",
  "#yingfeng2": "公怀奉君之心，则民有安固之志。",
  "#sp_maojie:die": "废立大事，公不可不慎……",
  "#zhiyan_re_yufan1": "失忠于信，何以侍君！",
  "#zhiyan_re_yufan2": "恕臣耿直，犯言谏正！",
  "#rezongxuan1": "凡夫俗子，怎识得卦象玄妙？",
  "#rezongxuan2": "微妙玄通，深不可识。",
  "#re_yufan:die": "唉，主公不能容我……",
  "#chongxu1": "阳炁冲三关，斩尸除阴魔。",
  "#chongxu2": "蒲团清静坐，神归了道真。",
  "#miaojian1": "谨以三尺玄锋，代天行化，布令宣威。",
  "#miaojian2": "布天罡，踏北斗，有秽皆除，无妖不斩。",
  "#shhlianhua1": "刀兵水火，速离身形。",
  "#shhlianhua2": "体有金光，覆映全身。",
  "#sunhanhua:die": "天有寒暑，人有死生……",
  "#huantu1": "今群雄蜂起，主公宜外收内敛，勿为祸先。",
  "#huantu2": "昔陈胜之事，足为今日之师，望主公熟虑。",
  "#bihuo1": "公以败兵之身投之，功轻且恐难保身也。",
  "#bihuo2": "公不若附之他人与相拒，然后委质，功必多。",
  "#yanpu:die": "公皆听吾计，圃岂敢不专……",
  "#jibing1": "集荆、扬精兵，而后共举大义！",
  "#jibing2": "教众快快集合，不可误了大事！",
  "#wangjing1": "联络朝中中常侍，共抗朝廷不义师！",
  "#wangjing2": "往来京城，与众常侍密谋举事！",
  "#moucuan1": "汉失民心，天赐良机！",
  "#moucuan2": "天下正主，正是大贤良师！",
  "#mayuanyi:die": "唐周……无耻！",
  "#jueyong1": "敌围何惧，有死而已！",
  "#jueyong2": "身陷敌阵，战而弥勇！",
  "#poxiang1": "王瓘既然假降，吾等可将计就计。",
  "#poxiang2": "佥率已降两千魏兵，便可大破魏军主力。",
  "#fuqian:die": "生为蜀臣，死……亦当为蜀！",
  "#disordersidi1": "司敌之动，先发而制。",
  "#disordersidi2": "料敌之行，伏兵灭之。",
  "#xin_caozhen:die": "雍凉动乱，皆吾之过也……",
  "#xinzenhui1": "本公主说你忤逆，岂能有假？",
  "#xinzenhui2": "不用挣扎了，你们谁都逃不了！",
  "#xinjiaojin1": "狂妄之徒！忘了你自己的身份了吗？",
  "#xinjiaojin2": "和本公主比心机谋算？哼，可笑！",
  "#xin_sunluban:die": "妹妹，姐姐是迫不得已……",
  "#zhiming1": "天定人命，仅可一窥。",
  "#zhiming2": "知命而行，尽诸人事。",
  "#xingbu1": "天现祥瑞，此乃大吉之兆。",
  "#xingbu2": "天象显异，北伐万不可期。",
  "#qiaozhou:die": "老夫死不足惜，但求蜀地百姓无虞！",
  "#xinshenxing1": "事前多思，事后少悔。",
  "#xinshenxing2": "权衡斟酌，再虑一番。",
  "#xin_guyong:die": "陛下厚爱，雍……",
  "#requanji1": "备兵驯马，以待战机。",
  "#requanji2": "避其锋芒，权且忍让。",
  "#zili_re_zhonghui1": "金鳞，岂是池中之物！",
  "#zili_re_zhonghui2": "千载一时，鼎足而立！",
  "#re_zhonghui:die": "伯约，我已无力回天……",
  "#xinqieting1": "密言？哼！早已入我耳中。",
  "#xinqieting2": "此子不除，久必为患！",
  "#xianzhou_xin_caifuren1": "既是诸位之议，妾身复有何疑？",
  "#xianzhou_xin_caifuren2": "我虽女流，亦知献州乃为长久之计。",
  "#xin_caifuren:die": "琮儿！啊啊……",
  "#mobilezhongyong1": "关将军，接刀！",
  "#mobilezhongyong2": "青龙三停刀，斩敌万千条！",
  "#xin_zhoucang:die": "九泉之下，仓陪将军再走一遭……",
  "#rejieyue1": "按丞相之命，此部今由余统摄。",
  "#rejieyue2": "奉法行令，事上之节，岂有宽宥之理？",
  "#ol_yujin:die": "忍辱偷生，无颜以面丞相厚恩！",
  "#tiansuan1": "汝既持签问卜，亦当应天授命。",
  "#tiansuan2": "尔若居正体道，福寿自当天成。",
  "#zhouqun:die": "及时止损，过犹不及……",
  "#juxiang1_re_zhurong1": "今日，就让这群汉人长长见识。",
  "#juxiang1_re_zhurong2": "我的大象，终于有了用武之地。",
  "#relieren1": "有我手中飞刀在，何惧蜀军！",
  "#relieren2": "长矛，飞刀，烈火，都来吧！",
  "#re_zhurong:die": "这群汉人使诈……",
  "#tiaoxin_re_jiangwei1": "黄口竖子，何必上阵送命？",
  "#tiaoxin_re_jiangwei2": "汝如欲大败而归，则可进军一战！",
  "#zhiji_re_jiangwei1": "维定当奋身以复汉室。",
  "#zhiji_re_jiangwei2": "丞相之志，维必竭力而为。",
  "#re_jiangwei:die": "可惜大计未成，吾已身陨……",
  "#yufeng1": "广开兮天门，纷吾乘兮玄云。",
  "#yufeng2": "高飞兮安翔，乘清气兮御阴阳。",
  "#tianshu1": "其耆欲深者，其天机浅。",
  "#tianshu2": "杀生者不死，生生者不生。",
  "#nanhualaoxian:die": "天机求而近，执而远……",
  "#re_handang:die": "箭尽弓折，力竭人亡。",
  "#retuntian1": "休养生息，是为以备不虞。",
  "#retuntian2": "战损难免，应以军务减之。",
  "#zaoxian_re_dengai1": "用兵以险，则战之以胜！",
  "#zaoxian_re_dengai2": "已至马阁山，宜速进军破蜀！",
  "#re_dengai:die": "一片忠心，换来这般田地……",
  "#xinzhuikong1": "万事必须小心为妙。",
  "#xinzhuikong2": "我虽妇人，亦当铲除曹贼。",
  "#xinqiuyuan1": "这是最后的希望了。",
  "#xinqiuyuan2": "诛此国贼者，加官进爵！",
  "#xin_fuhuanghou:die": "父亲大人，你竟如此优柔寡断……",
  "#xinduodao1": "避其锋芒，夺其兵刃！",
  "#xinduodao2": "好兵器啊！哈哈哈！",
  "#xinanjian1": "看我一箭索命！",
  "#xinanjian2": "明枪易躲，暗箭难防！",
  "#xin_panzhangmazhong:die": "埋伏得这么好，怎会……",
  "#mobilejingce1": "得一寸，进一尺。",
  "#mobilejingce2": "良策佐君王，率征万精兵。",
  "#xin_guohuai:die": "穷寇莫追……",
  "#taomie1": "犯我辽东疆界，必遭后报！",
  "#taomie2": "韩濊之乱，再无可生之机！",
  "#taomie3": "颅且远行万里，要席何用？",
  "#gongsunkang:die": "枭雄一世，何有所憾！",
  "#xiaoxi_hansui1": "打你个措手不及！",
  "#xiaoxi_hansui2": "两军交战，勇者为胜！",
  "#daoji1": "八十斤双戟？于我如探囊取物！",
  "#daoji2": "以汝之矛，攻汝之盾！",
  "#hucheer:die": "未料一伸手，便被……敌酋捉……",
  "#re_lingtong:die": "先……停一下吧……",
  "#zishou_re_liubiao1": "忍时待机，以期坐收渔利！",
  "#zishou_re_liubiao2": "按兵不动，徐图荆襄霸业！",
  "#rezongshi1": "这天下，尽是大汉疆土！",
  "#rezongshi2": "汉室之威，犹然彰存！",
  "#re_liubiao:die": "垂垂老矣，已忘壮年雄心……",
  "#baiyi1": "吾不听公休之言，以致须行此策。",
  "#baiyi2": "诸将无过，且按吾之略再图破敌。",
  "#jinglve1": "安待良机，自有舍身报吾之士。",
  "#jinglve2": "察局备间，保诸事不虞。",
  "#shanli1": "荡尘涤污，重整河山，便在今日！",
  "#shanli2": "效伊尹霍光，以返天下清明。",
  "#simashi:die": "吾家夙愿，得偿与否，尽看子上……",
  "#hongyi1": "克明礼教，约束不端之行。",
  "#hongyi2": "训成弘操，以扬正明之德。",
  "#quanfeng1": "媛容德懿，应追谥之。",
  "#quanfeng2": "景怀之号，方配得上前人之德。",
  "#yanghuiyu:die": "桃符，一定要平安啊……",
  "#mobiledanshou1": "此诚危难，我定当竭尽全力！",
  "#mobiledanshou2": "大丈夫屈伸有道，不在一时胜负。",
  "#xin_zhuran:die": "大耳贼就在眼前，快追……",
  "#beizhu1": "检阅士卒，备将行之役。",
  "#beizhu2": "点选将校，讨乱汉之贼。",
  "#beizhu3": "乱贼势大，且暂勿力战。",
  "#dingyuan:die": "奉先何故心变，啊！",
  "#xinqiaoshui1": "此事听我一言，定有分明之理。",
  "#xinqiaoshui2": "今日之事，听我一言便是。",
  "#xinjyzongshi1": "空拘小节，难成大事。",
  "#xinjyzongshi2": "繁文缛节，不过是缚人之物。",
  "#xin_jianyong:die": "行事无矩，为人所恶矣……",
  "#rejiangchi1": "将飞翼伏，三军整肃。",
  "#rejiangchi2": "策马扬鞭，奔驰万里。",
  "#xin_caozhang:die": "黄须金甲，也难敌骨肉毒心！",
  "#dangxian_xin_liaohua1": "谁言蜀汉已无大将？",
  "#dangxian_xin_liaohua2": "老将虽白发，宝刀刃犹锋！",
  "#refuli1": "未破敌军，岂可轻易服输？",
  "#refuli2": "看老夫再奋身一战！",
  "#xin_liaohua:die": "兴复大业，就靠你们了……",
  "#xuewei1": "吾主之尊，岂容尔等贼寇近前？",
  "#xuewei2": "血佑忠魂，身卫英主。",
  "#liechi1": "吾受汉帝恩，岂容吴贼辱？",
  "#liechi2": "汉将有死无降，怎会如吴狗一般？",
  "#furong:die": "吾主既然得返，此番已是功成……",
  "#jiuchi_re_dongzhuo1": "无酒不欢，来来来，再饮，再饮！",
  "#jiuchi_re_dongzhuo2": "黄金美酒夜光杯，舍我其谁啊，哈哈哈哈哈哈！",
  "#roulin_re_dongzhuo1": "美女，美酒，美食，嘿嘿，尽我享用！",
  "#roulin_re_dongzhuo2": "嗯啊，尽享天下美味，哼哼哼哼哈哈哈哈！",
  "#benghuai_re_dongzhuo1": "什么礼制纲常，我说的就是纲常！",
  "#benghuai_re_dongzhuo2": "谁有权力，谁掌生死！",
  "#baonue2_re_dongzhuo1": "不施严法，怎治乱民，休得啰嗦！",
  "#baonue2_re_dongzhuo2": "天子在我手里，我怕谁！",
  "#re_dongzhuo:die": "竖子，竟敢反我！",
  "#repojun1": "犯大吴疆土者，盛必击而破之！",
  "#repojun2": "若敢来犯，必叫你大败而归！",
  "#re_xusheng:die": "盛只恨，不能再为主公，破敌致胜了……",
  "#zhengjian1": "此人有雄猛逸才，还请明公观之。",
  "#zhengjian2": "若明公得此人才，定当如虎添翼。",
  "#gaoyuan1": "还请告知兴霸，请他务必相助。",
  "#gaoyuan2": "如今事急，唯有兴霸可救。",
  "#sp_sufei:die": "本可共图大业，奈何主公量狭器小啊……",
  "#yinghun_re_sunjian1": "义定四野，武匡海内。",
  "#yinghun_re_sunjian2": "江东男儿，皆胸怀匡扶天下之志。",
  "#re_sunjian:die": "吾身虽死，忠勇须传……",
  "#zhengjing1": "兼采今古，博学并蓄，择善以教之。",
  "#zhengjing2": "君子需通六艺，亦当识明三礼。",
  "#zhengxuan:die": "注易未毕，奈何寿数将近……",
  "#jimeng1": "今日之言，皆是为保两国无虞。",
  "#jimeng2": "天下之势已如水火，还望重修盟好。",
  "#shuaiyan1": "并魏之日，想来便是两国争战之时。",
  "#shuaiyan2": "在下所言，至诚至率。",
  "#dengzhi:die": "使命既成，但死无妨！",
  "#relihuo1": "此火只为全歼敌寇，无需妇人之仁。",
  "#relihuo2": "战胜攻取，以火修功。",
  "#chunlao_xin_chengpu1": "备下佳酿，以做庆功之用。",
  "#chunlao_xin_chengpu2": "饮此壮行酒，当立先头功。",
  "#xin_chengpu:die": "以暴讨贼，竟遭报应么……",
  "#duoduan1": "北伐之事，丞相亦听我定夺。",
  "#duoduan2": "筹定规画，片刻既定！",
  "#gongsun1": "我岂能与魏延这种莽夫共事！",
  "#gongsun2": "早知如此，投靠魏国又如何！",
  "#yangyi:die": "我功勋卓著，只为昏君奸臣所害，唉！",
  "#chengzhao1": "定当为皇上诛杀首害！",
  "#chengzhao2": "此诏字字诛心，岂能不斩曹贼！",
  "#dongcheng:die": "九泉之下，我等着你曹贼到来！",
  "#xinlianhuan1": "将多兵众，不可以敌，使其自累，以杀其势。",
  "#xinlianhuan2": "善用兵者，运巧必防损，立谋虑中变。",
  "#niepan_re_pangtong1": "凤凰折翅，涅槃再生。",
  "#niepan_re_pangtong2": "九天之志，展翅翱翔。",
  "#re_pangtong:die": "落……凤……坡……",
  "#zhouxuan1": "孰为虎？孰为鹰？于吾都如棋子。",
  "#zhouxuan2": "群雄逐鹿之际，唯有洞明时势方有所成。",
  "#fengji1": "巡土田之宜，尽凿溉之利。",
  "#fengji2": "养耆育孤，视民如伤，以丰定徐州。",
  "#chendeng:die": "诸卿何患无令君乎？",
  "#zhaohan1": "天道昭昭，再兴如光武亦可期。",
  "#zhaohan2": "汉祚将终，我又岂能无憾。",
  "#rangjie1": "公既执掌权柄，又何必令君臣遭乱。",
  "#rangjie2": "公虽权倾朝野，亦当尊圣上之意。",
  "#yizheng1": "一人劫天子，一人质公卿，此可行邪？",
  "#yizheng2": "诸军举事，当上顺天心，奈何如是！",
  "#yangbiao:die": "未能效死佑汉，只因宗族之重……",
  //换cv不换台词的手杀界卧龙是屑
  "#bazhen_re_sp_zhugeliang1": "你可识得此阵？",
  "#bazhen_re_sp_zhugeliang2": "太极生两仪，两仪生四象，四象生八卦。",
  "#rehuoji1": "燃烧吧！",
  "#rehuoji2": "此火可助我军大获全胜。",
  "#rekanpo1": "雕虫小技。",
  "#rekanpo2": "你的计谋被识破了。",
  "#re_sp_zhugeliang:die": "我的计谋竟被……",
  "#zhiyi1": "岂可擅退而误国家之功？",
  "#zhiyi2": "统摄不懈，只为破敌！",
  "#zhangyì:die": "唯愿百姓，不受此乱所害，哎……",
  "#zhongzuo1": "历经磨难，不改佐国之志。",
  "#zhongzuo2": "建功立业，唯愿天下早定。",
  "#wanlan1": "挽狂澜于既倒，扶大厦于将倾。",
  "#wanlan2": "深受国恩，今日便是报偿之时！",
  "#jiakui:die": "不斩孙权，九泉之下羞见先帝啊！",
  "#kuangcai1": "博古揽今，信手拈来。",
  "#kuangcai2": "功名为尘，光阴为金。",
  "#shejian1": "尔等竖子，不堪为伍！",
  "#shejian2": "请君洗耳，听我之言。",
  "#miheng:die": "呵呵呵呵……这天地都容不下我！……",
  "#zhaohuo1": "我获罪于天，致使徐州之民，受此大难！",
  "#zhaohuo2": "如此一来，徐州危矣……",
  "#yixiang1": "一方有难，八方应援。",
  "#yixiang2": "昔日有恩，还望此时来报。",
  "#yirang1": "明公切勿推辞！",
  "#yirang2": "万望明公可怜汉家城池为重！",
  "#taoqian:die": "悔不该差使小人，招此祸患……",
  "#fenyin1": "披发亢歌，至死不休！",
  "#fenyin2": "力不竭，战不止！",
  "#liuzan:die": "今病困若此，固命也……",
  "#dujin1": "带兵十万，不如老夫多甲一件！",
  "#dujin2": "轻舟独进，破敌先锋！",
  "#lingcao:die": "呃啊！（扑通）此箭……何来……",
  "#qingyi1": "没牌，又有何不可能的？",
  "#qingyi2": "翩翩逸云端，仿若桃花仙。",
  "#shixin1": "释怀之戾气，化君之不悦。",
  "#shixin2": "星星之火，安能伤我？",
  "#sunru:die": "佑我江东，虽死无怨……",
  "#tunchu1": "屯粮事大，暂不与尔等计较。",
  "#tunchu2": "屯粮待战，莫动刀枪。",
  "#shuliang1": "将军驰劳，酒肉慰劳。",
  "#shuliang2": "将军，牌来了。",
  "#lifeng:die": "吾，有负丞相重托……",
  "#zhanyi1": "以战养战，视敌而战。",
  "#zhanyi2": "战，可以破敌。意，可以守御。",
  "#zhuling:die": "此生得遇曹公，再无他求……",
  "#polu1": "设此发石车，可破袁军高橹。",
  "#polu2": "霹雳之声，震丧敌胆。",
  "#choulve1": "依此计行，可安军心。",
  "#choulve2": "破袁之策，吾已有计。",
  "#liuye:die": "唉，于上不能佐君主，于下不能亲同僚，吾愧为佐世人臣……",
  "#yizan_respond_shan1": "承吾父之勇，翊军立阵。",
  "#yizan_respond_shan2": "继先帝之志，季兴大汉。",
  "#xinfu_longyuan1": "金鳞岂是池中物，一遇风云便化龙。",
  "#xinfu_longyuan2": "忍时待机，今日终于可以建功立业。",
  "#zhaotongzhaoguang:die": "守业死战，不愧初心……",
  "#xinfu_jingxie1": "军具精巧，方保无虞。",
  "#xinfu_jingxie2": "巧则巧矣，未尽善也。",
  "#qiaosi1": "待我稍作思量，更益其巧。",
  "#qiaosi2": "虚争空言，不如思而试之。",
  "#majun:die": "衡石不用，美玉见诬啊！",
  "#xinfu_daigong1": "不急，只等敌军士气渐殆。",
  "#xinfu_daigong2": "敌谋吾已尽料，可以长策縻之。",
  "#xinfu_zhaoxin1": "吾心昭昭，何惧天下之口！",
  "#xinfu_zhaoxin2": "公此行欲何为，吾自有量度。",
  "#simazhao:die": "安世，接下来，就看你的了……",
  "#xinfu_qianchong1": "细行筹划，只盼能助夫君一臂之力。",
  "#qc_weimu": "宫闱之内，何必擅涉外事！",
  "#qc_mingzhe": "谦瑾行事，方能多吉少恙。",
  "#xinfu_shangjian1": "如今乱世，当秉俭行之节。",
  "#xinfu_shangjian2": "百姓尚处寒饥之困，吾等不可奢费财力。",
  "#wangyuanji:die": "世事沉浮，非是一人可逆啊……",
  "#xinfu_pingcai1": "吾有众好友，分为卧龙、凤雏、水镜、元直。",
  "#xinfu_pingcai2": "孔明能借天火之势。",
  "#xinfu_pingcai3": "士元虑事环环相扣。",
  "#xinfu_pingcai4": "德操深谙处世之道。",
  "#xinfu_pingcai5": "元直侠客惩恶扬善。",
  "#pangdegong:die": "吾知人而不自知，何等荒唐……",
  "#shouye1": "敌军攻势渐怠，还望诸位依策坚守。",
  "#shouye2": "袁幽州不日便至，当行策建功以报之。",
  "#liezhi1": "只恨箭支太少，不能射杀汝等！",
  "#liezhi2": "身陨事小，秉节事大。",
  "#shenpei:die": "吾君在北，但求面北而亡！",
  "#xinfu_zhennan1": "镇守南中，夫君无忧。",
  "#xinfu_zhennan2": "与君携手，定平蛮夷。",
  "#renshi1": "巾帼于乱世，只能飘零如尘。",
  "#renshi2": "还望您可以手下留情！",
  "#wuyuan1": "夫君，此次出征，还望您记挂妾身！",
  "#wuyuan2": "云长，一定要平安归来啊！",
  "#hujinding:die": "云长，重逢不久，又要相别么……",
  "#reluanji1": "万箭穿心，灭其士气！",
  "#reluanji2": "卿当与本公同心戮力，共安社稷！",
  "#xueyi1": "名门侍周，朝野敬仰。",
  "#xueyi2": "吾乃名门望族，岂能与汝等为伍？",
  "#xin_yuanshao:die": "袁门不幸啊！",
  "#xiangle_re_liushan1": "天府之国，自然民安国泰。",
  "#xiangle_re_liushan2": "战事扰乱民生，不如作罢。",
  "#refangquan1": "爱卿自行定夺便是。",
  "#refangquan2": "北伐事重，相父全权处理即可啊！",
  "#ruoyu_re_liushan1": "唯有自认庸主之名，方能保蜀地官民无虞啊！",
  "#ruoyu_re_liushan2": "既无争雄天下之才，只好做守成之主。",
  "#re_liushan:die": "实在有愧父皇与相父啊……",
  "#jiang_re_sunben1": "我会把胜利带回江东。",
  "#jiang_re_sunben2": "天下英雄，谁能与我一战？",
  "#rehunzi1": "小霸王之名响彻天下，何人不知？",
  "#rehunzi2": "江东已平，中原动荡，直取许昌。",
  "#zhiba_re_sunben1": "我的霸业才刚刚开始。",
  "#zhiba_re_sunben2": "汝是战是降，我皆奉陪。",
  "#re_sunben:die": "大业未就，中世尔殒……",
  "#scstaoluan1": "罗绮朱紫，皆若吾等手中傀儡。",
  "#scschiyan1": "逆臣乱党，都要受这啄心之刑。",
  "#scszimou1": "在宫里当差，还不是为这利字！",
  "#scspicai1": "修得广厦千万，可庇汉室不倾。",
  "#scsyaozhuo1": "上蔽天听，下诓朝野！",
  "#scsxiaolu1": "咱家上下打点，自是要费些银子。",
  "#scskuiji1": "同道者为忠，殊途者为奸！",
  "#scschihe1": "想见圣上？哼哼，你怕是没这个福分了！",
  "#scsniqu1": "离心离德，为吾等所不容！",
  "#scsanruo1": "小伤无碍，安心修养便可。",
  "#shichangshi:die": "（魂魄散去的声音）",
  "#wangzun_old_yuanshu1": "这玉玺，当然是能者居之。",
  "#wangzun_old_yuanshu2": "我就是皇帝，我就是天！",
  "#mbcmqingzheng1": "朕虽不德，昧于大道，思与宇内共臻兹路。",
  "#mbcmqingzheng2": "愿遵前人教诲，为一国明帝贤君。",
  "#mbcmjiushi1": "心忿无所表，下笔即成篇。",
  "#mbcmjiushi2": "气幽但求醉，醒后寻复来。",
  "#mbcmfangzhu1": "卿当竭命纳忠，何为此逾矩之举！",
  "#mbcmfangzhu2": "朕继文帝风流，亦当效其权略。",
  "#mbjuejin1": "朕宁拼一死，逆贼安敢一战！",
  "#mbjuejin2": "朕安可坐受废辱，今日当与卿自出讨之！",
  "#yanxiang:die": "若遇明主，或可青史留名……",
  "#yimou": "绍因权专利，久必生变，不若屯军以观。",
  "#baoxin:die": "区区黄巾流寇，如何挡我？呃啊……",
  "#jilun1": "树化建业，慎在民时。",
  "#jilun2": "推极利弊，当禁文巧之词。",
  "#jiangji:die": "洛水之誓，言犹在耳……咳咳咳……",
  "#liwei:die": "安南重任，万不可轻之……",
  "#binghuo1": "黄巾既起，必灭不义之师！",
  "#binghuo2": "诛官杀吏，尽诛朝廷爪牙！",
  "#paiyi_re_zhonghui1": "艾命不尊，死有余辜。",
  "#paiyi_re_zhonghui2": "非我族类，其心必异。",
  "#guanxing_re_jiangwei1": "知天易则观之，逆天难亦行之。",
  "#guanxing_re_jiangwei2": "欲尽人事，亦先听天命。",
  "#jixi_re_dengai1": "攻敌之不备，斩将夺辎！",
  "#jixi_re_dengai2": "奇兵正攻，敌何能为？",
  "#jijiang1_re_liushan1": "还望诸卿勠力同心，以保国祚。",
  "#jijiang1_re_liushan2": "哪位爱卿愿意报效国家？",
  "#reyingzi_re_sunben1": "有公瑾助我，可平天下。",
  "#reyingzi_re_sunben2": "所到之处，战无不胜。",
  "#yinghun_re_sunben1": "武烈之魂，助我扬名。",
  "#yinghun_re_sunben2": "江东之主，众望所归。"
};
const characterSort = {
  extra_mb: ["xin_simayi", "mb_shen_caocao", "mb_shen_lvbu"],
  mobile_sp2: ["mb_sunluyu"],
  mobile_sp3: ["mb_guanyinping", "mb_caohong"],
  mobile_sp4: ["re_heqi", "mb_chengyu"],
  mobile_sp5: ["mb_dongbai"],
  mobile_sp6: ["dongcheng", "re_weiwenzhugezhi"],
  mobile_sp7: ["taoqian", "yangyi"],
  mobile_sp8: ["shenpei", "re_wangyun"],
  mobile_sp9: ["jiakui", "sp_sufei", "re_zhanggong", "re_xugong", "re_baosanniang"],
  mobile_sp10: ["furong", "zhouqun", "zhangyì", "dengzhi", "gongsunkang", "hucheer", "chendeng", "dingyuan"],
  mobile_sp11: ["mb_wutugu", "fuqian", "qiaozhou", "ruanhui", "sp_maojie", "wangjun", "yanpu", "mayuanyi", "xin_mamidi", "mb_lingju"],
  mobile_sp12: ["hujinding", "lifeng", "zhaotongzhaoguang", "wangyuanji", "yanghuiyu", "liuye", "simazhao", "zhuling", "yangbiao", "mb_zhugeguo"],
  mobile_sp13: ["sp_caosong", "sp_pengyang", "liwei", "xin_guozhao", "qianzhao", "peixiu", "yanxiang", "xin_hansui", "yangfu"],
  mobile_sp14: ["mb_ganfuren", "xin_huojun", "laimin", "xin_wuban", "mb_huban", "sp_jianggan", "baoxin", "mb_chengui", "muludawang", "yangfeng"],
  mobile_sp15: ["mb_mengda", "mb_qinghegongzhu", "mb_wangjing", "mb_sp_zhenji", "jiangji", "zhangbu", "wuke", "mb_caocao", "mb_zerong"],
  mobile_sp_default: ["mb_gaoxiang", "mb_cuimao", "mb_tw_zhangji", "hanxuan", "mb_xianglang", "mb_xingdaorong", "mb_dingshangwan", "mb_xiahoumao"],
  mobile_xlqk1: ["lingcao", "sunru", "liuzan"],
  mobile_xlqk2: ["pangdegong", "miheng", "mb_caochun"],
  mobile_xlqk3: ["majun", "zhengxuan", "simashi"],
  mobile_xlqk4: ["nanhualaoxian", "shichangshi", "sunhanhua"],
  mobile_xlqk5: ["mb_zhangfen", "mb_cuilingyi", "zhuji"],
  mobile_xlqk6: ["mb_caoxing", "mb_luyu", "mb_zhangzhi", "mb_caozhi", "mb_zhouyu", "mb_liuhui"],
  mobile_xlqk7: ["mb_cheliji"],
  mobile_longxue: ["mb_simazhao", "mb_simafu", "mb_wenqin", "mb_simazhou", "mb_sp_guanqiujian", "mb_caomao", "chengji", "lizhaojiaobo", "mb_jiachong"],
  mobile_laoyouji: ["friend_zhugeliang", "friend_xushu", "friend_pangtong", "friend_cuijun", "friend_shitao"],
  mobile_laoyouji_hefei: ["hefei_zhangliao", "hefei_lidian", "hefei_yuejin"],
  mobile_standard: ["xin_xiahoudun", "xin_zhangfei", "old_yuanshu"],
  mobile_shenhua_feng: ["re_xiaoqiao", "xin_zhoutai"],
  mobile_shenhua_huo: ["re_pangtong", "re_sp_zhugeliang", "re_xunyu", "re_dianwei", "re_yanwen", "xin_yuanshao"],
  mobile_shenhua_lin: ["re_dongzhuo", "re_sunjian", "re_zhurong"],
  mobile_shenhua_shan: ["re_liushan", "re_dengai", "re_jiangwei", "re_caiwenji", "re_zhangzhang", "re_sunben"],
  mobile_shenhua_yin: ["xin_sunliang", "re_yanyan", "re_wangji"],
  mobile_shenhua_lei: ["re_guanqiujian", "re_zhoufei"],
  mobile_yijiang1: ["re_xusheng", "re_lingtong", "ol_yujin", "re_wuguotai", "re_gaoshun", "re_caozhi"],
  mobile_yijiang2: ["xin_liaohua", "xin_caozhang", "re_liubiao", "re_handang", "xin_chengpu", "xin_gongsunzan", "re_zhonghui", "re_bulianshi"],
  mobile_yijiang3: ["re_liru", "xin_jianyong", "xin_zhuran", "xin_guohuai", "xin_panzhangmazhong", "xin_fuhuanghou", "re_yufan"],
  mobile_yijiang4: ["xin_zhoucang", "xin_caifuren", "xin_guyong", "xin_sunluban", "xin_caozhen", "xin_jushou", "xin_wuyi", "xin_zhuhuan", "re_chenqun"],
  mobile_yijiang5: ["xin_zhangyi", "xin_sunxiu", "xin_quancong", "xin_zhuzhi", "xin_caoxiu", "mb_zhongyao"],
  mobile_yijiang7: ["re_jikang", "mb_xuezong"],
  mobile_changshi: ["scs_zhangrang", "scs_zhaozhong", "scs_sunzhang", "scs_bilan", "scs_xiayun", "scs_hankui", "scs_lisong", "scs_duangui", "scs_guosheng", "scs_gaowang"],
  mobile_yuan: ["o_sunquan", "o_tanshihuai", "o_lvbu", "o_gaoshun", "o_guanyu", "o_meicheng", "o_chenlan", "o_tadun"],
  mobile_others: []
};
const characterSortTranslate = {
  mobile_standard: "手杀异构·标准包",
  mobile_shenhua_feng: "手杀异构·其疾如风",
  mobile_shenhua_huo: "手杀异构·侵掠如火",
  mobile_shenhua_lin: "手杀异构·其徐如林",
  mobile_shenhua_shan: "手杀异构·不动如山",
  mobile_shenhua_yin: "手杀异构·难知如阴",
  mobile_shenhua_lei: "手杀异构·动如雷霆",
  mobile_yijiang1: "手杀异构·将1",
  mobile_yijiang2: "手杀异构·将2",
  mobile_yijiang3: "手杀异构·将3",
  mobile_yijiang4: "手杀异构·将4",
  mobile_yijiang5: "手杀异构·将5",
  mobile_yijiang7: "手杀异构·原7",
  mobile_sp2: "SP·组2",
  mobile_sp3: "SP·组3",
  mobile_sp4: "SP·组4",
  mobile_sp5: "SP·组5",
  mobile_sp6: "SP·组6",
  mobile_sp7: "SP·组7",
  mobile_sp8: "SP·组8",
  mobile_sp9: "SP·组9",
  mobile_sp10: "SP·组10",
  mobile_sp11: "SP·组11",
  mobile_sp12: "SP·组12",
  mobile_sp13: "SP·组13",
  mobile_sp14: "SP·组14",
  mobile_sp15: "SP·组15",
  mobile_sp_default: "SP·未分组",
  mobile_xlqk1: "袖里乾坤·组1",
  mobile_xlqk2: "袖里乾坤·组2",
  mobile_xlqk3: "袖里乾坤·组3",
  mobile_xlqk4: "袖里乾坤·组4",
  mobile_xlqk5: "袖里乾坤·组5",
  mobile_xlqk6: "袖里乾坤·君子六艺",
  mobile_xlqk7: "袖里乾坤·组6",
  mobile_longxue: "龙血玄黄",
  mobile_laoyouji: "老友季",
  mobile_laoyouji_hefei: "千里之骥",
  mobile_yuan: "移动版·缘",
  mobile_others: "其他",
  mobile_changshi: "十常侍单体",
  extra_mb: "神将异构·移动版"
};
game.import("character", function() {
  return {
    name: "mobile",
    connect: true,
    character: { ...characters },
    characterSort: {
      mobile: characterSort
    },
    characterFilter: { ...characterFilters },
    characterTitle: { ...characterTitles },
    dynamicTranslate: { ...dynamicTranslates },
    characterIntro: { ...characterIntro },
    characterSubstitute: {
      mb_caomao: [
        ["mb_caomao_shadow", ["die:mb_caomao"]],
        ["mb_caomao_dead", ["die:mb_caomao"]]
      ],
      shichangshi: [["shichangshi_dead", ["die:shichangshi"]]],
      scs_zhangrang: [["scs_zhangrang_dead", ["die:shichangshi"]]],
      scs_zhaozhong: [["scs_zhaozhong_dead", ["die:shichangshi"]]],
      scs_sunzhang: [["scs_sunzhang_dead", ["die:shichangshi"]]],
      scs_bilan: [["scs_bilan_dead", ["die:shichangshi"]]],
      scs_xiayun: [["scs_xiayun_dead", ["die:shichangshi"]]],
      scs_hankui: [["scs_hankui_dead", ["die:shichangshi"]]],
      scs_lisong: [["scs_lisong_dead", ["die:shichangshi"]]],
      scs_duangui: [["scs_duangui_dead", ["die:shichangshi"]]],
      scs_guosheng: [["scs_guosheng_dead", ["die:shichangshi"]]],
      scs_gaowang: [["scs_gaowang_dead", ["die:shichangshi"]]],
      pot_taishici: [
        ["pot_taishici_shadow1", ["die:pot_taishici"]],
        ["pot_taishici_shadow2", ["die:pot_taishici"]],
        ["pot_taishici_shadow3", ["die:pot_taishici"]],
        ["pot_taishici_shadow4", ["die:pot_taishici"]]
      ],
      pot_weiyan: [
        ["pot_weiyan_achieve", []],
        ["pot_weiyan_fail", []]
      ],
      mb_simazhao: [["jin_jsrg_simazhao", []]],
      pot_yuji: [["pot_yuji_shadow", []]],
      mb_zerong: [
        ["mb_zerong_red", ["die:mb_zerong"]],
        ["mb_zerong_black", ["die:mb_zerong"]],
        ["mb_zerong_all", ["die:mb_zerong"]]
      ],
      mb_cuilingyi: [
        ["mb_cuilingyi_guidian1", ["character:mb_cuilingyi", "die:mb_cuilingyi"]],
        ["mb_cuilingyi_guidian2", ["die:mb_cuilingyi"]],
        ["mb_cuilingyi_guidian3", ["die:mb_cuilingyi"]],
        ["mb_cuilingyi_dongjiao1", ["die:mb_cuilingyi"]],
        ["mb_cuilingyi_dongjiao2", ["die:mb_cuilingyi"]],
        ["mb_cuilingyi_dongjiao3", ["die:mb_cuilingyi"]],
        ["mb_cuilingyi_xiuge1", ["die:mb_cuilingyi"]],
        ["mb_cuilingyi_xiuge2", ["die:mb_cuilingyi"]],
        ["mb_cuilingyi_xiuge3", ["die:mb_cuilingyi"]]
      ],
      zhuji: [["zhuji_shadow", []]],
      pot_zhouyu: [
        ["pot_zhouyu_shadow", []]
      ]
    },
    card: { ...cards$1 },
    skill: { ...skills },
    perfectPair: { ...perfectPairs },
    translate: { ...translates, ...voices, ...characterSortTranslate },
    pinyins: { ...pinyins }
  };
});
